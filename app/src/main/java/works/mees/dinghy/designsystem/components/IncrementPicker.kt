package works.mees.dinghy.designsystem.components

/**
 * Format an increment step for display: drop .0 on whole numbers, show necessary decimals.
 * Examples: 5.0 → "±5", 0.001 → "±0.001", 10.0 → "±10", 0.05 → "±0.05" (no trailing-zero noise).
 *
 * TODO(26-01 T2): placeholder body — real implementation in Task 2.
 */
internal fun formatStep(step: Double): String = ""
