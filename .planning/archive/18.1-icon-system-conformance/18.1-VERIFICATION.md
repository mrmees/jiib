---
phase: 18.1-icon-system-conformance
verified: 2026-06-07T20:15:00Z
status: passed
score: 12/12 must-haves verified
overrides_applied: 1
overrides:
  - must_have: "Font refreshed to a current Material Symbols Outlined cut (Roadmap SC-1)"
    reason: "D-13 owner amendment relaxes SC-1 from 'refresh the binary' to 'verify the bundled v2.944 is a current variable cut that resolves every needed glyph and record provenance'. verify_ligatures.py confirms missing:[] at 48 needed / 3953 ligatures. No refresh needed; verification IS the deliverable."
    accepted_by: "matthew"
    accepted_at: "2026-06-07T00:00:00Z"
---

# Phase 18.1: Icon System Conformance Verification Report

**Phase Goal:** Apply Phase 18's icon-SOURCE policy app-wide — convert the remaining hand-traced "Material-Symbol-wannabe" drawables to Material Symbols font ligatures or official Google vectors; keep ONLY genuinely-custom printer-domain glyphs; mop up cosmetic KDoc doc-link warnings.
**Verified:** 2026-06-07T20:15:00Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Font v2.944 retained and verified sufficient; verify_ligatures.py exits 0 (`missing: []`) | PASSED (override) | `python tools/verify_ligatures.py` → `48 needed, 3953 ligatures in font, missing: []`, exit 0. D-13 owner amendment accepted. File exists at `tools/verify_ligatures.py` with `#!/usr/bin/env python3` shebang, `resolvable_ligatures` function, `text_select_move_forward_word` (with `_word` suffix, truncated form count = 0). |
| 2 | All 13 superseded hand-traced drawables physically deleted from `app/src/main/res/drawable/` | VERIFIED | `ls` confirms: ic_status_octagon, ic_status_triangle, ic_babystep_compress, ic_babystep_expand, detector, ic_lock_closed, ic_lock_open, add, remove, arrow_upward, arrow_downward, expand, text_select_move_forward_word — all absent. Only 7 files in the drawable directory. |
| 3 | Zero dangling `R.drawable.*` references to the 13 deleted drawables remain in `app/src/` | VERIFIED | `grep -rE "R[.]drawable[.](ic_status_octagon|...)" app/src/` returns 0 results across all 13 deleted names (including the tricky `add`/`remove` names). |
| 4 | Six registry `IconRef.Drawable` entries flipped to `IconRef.Ligature`; only Nozzle + HeatBed remain drawable-backed | VERIFIED | `DinghyIcons.kt`: `grep -c 'IconRef.Drawable'` == 2 (Nozzle + HeatBed). All six flipped: compress ✓, expand ✓, disabled_by_default ✓, text_select_move_forward_word ✓, remove ✓, add ✓ (each count == 1). `_word` suffix present; truncated form absent. |
| 5 | `StatusOctagon` renamed `StatusStop` (token + alternate); zero `StatusOctagon` references remain in `app/src/` | VERIFIED | `grep -rc "StatusOctagon" app/src/` == 0. `DinghyIcons.kt` has `val StatusStop`, `alternate = "status_stop"`, KDoc note citing D-06/D-14. `PrintStatusScreen.kt` uses `DinghyIcons.StatusStop` at line ~971 (count == 1, StatusOctagon count == 0). |
| 6 | `ConsoleRowsAdapter.kt` swapped to official vectors; DrawableCompat.setTint machinery untouched; no setFontVariationSettings | VERIFIED | `R.drawable.disabled_by_default` + `R.drawable.warning` each == 1. Old `ic_status_(octagon|triangle)` == 0. `DrawableCompat.setTint` count == 1 (unchanged). `setFontVariationSettings` count == 0. |
| 7 | Raw Compose call sites in ProbeCalibrateScreen/MoveScreen/ThemeEditorScreen swapped to ligature render path; nozzle kept | VERIFIED | ProbeCalibrate: 6 raw R.drawable refs gone, R.drawable.nozzle == 1, IconRef.Ligature count == 3. MoveScreen: ic_status_triangle/ic_lock_open/ic_lock_closed == 0; warning/lock_open_right/lock ligatures present. ThemeEditor: ic_status_(octagon|triangle) == 0; disabled_by_default count == 4, warning count == 4. |
| 8 | 7 keeper drawables survive: nozzle, heat_bed, bed_tilt, benchy, ic_launcher_foreground, disabled_by_default, warning | VERIFIED | `ls app/src/main/res/drawable/` shows exactly these 7 files. All physically present on disk. |
| 9 | DinghyIconsTest drift-guard `registryDrawableEntries_areOnlyTheCustomKeepers` exists and asserts `setOf("nozzle", "heat_bed")` | VERIFIED | Test method present (count == 1). Body filters `IconRef.Drawable`, maps `it.alternate`, asserts `setOf("nozzle", "heat_bed")`. Now GREEN post-Plan-02 flips. |
| 10 | KDoc doc-links fixed: `[DinghyPreviews]`→`[Nexus7Previews]` (×3), `[PrintStatusThemeMatrix]`→theme-asterisk (×1), `[AppContainer]` de-bracketed in PrintStatusPreviews.kt (×1) | VERIFIED | `grep -rE "\[DinghyPreviews\]\|\[PrintStatusThemeMatrix\]" app/src/` == 0. `[AppContainer]` in PrintStatusPreviews.kt == 0. Nexus7Previews count ≥ 1 in each of the 3 preview files (11/12/11). WR-02 fix: line 26 uses backtick `PrintStatusTheme*` notation instead of broken link. |
| 11 | a11y WR-01 fix: decorative status shapes silent to TalkBack (`clearAndSetSemantics {}`) | VERIFIED | MoveScreen.StatusShape: `clearAndSetSemantics {}` applied at line 457. ThemeEditorScreen.DataSwatch: `clearAndSetSemantics {}` at line 555. ThemeEditorScreen.StatusSlotSwatch: `clearAndSetSemantics {}` at line 642. DinghyIconView: null-cd path applies `clearAndSetSemantics {}`. ProbeCalibrate real-cd sites route through DinghyIconView (correct). |
| 12 | On-device spot check on flox (Adreno 320 / API 30) owner-approved; glyphs render without tofu; status shapes shape-distinct | VERIFIED | Plan-04 SUMMARY records owner approval: "Looks good" on flox (device 0a64b42e, LineageOS 18.1/API 30, genuine Adreno 320). All six visual checks passed. This is a blocking human-verify gate per Plan-04 Task 3. |

**Score:** 12/12 truths verified (1 via D-13 owner override for SC-1 font-refresh wording)

### Deferred Items

No items deferred to later phases. Phase 22 owns icon-call-site tokenization/string-extraction backfill (D-09) — this is planned out-of-scope, not a gap.

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `tools/verify_ligatures.py` | Device-free D-04 ligature gate | VERIFIED | Exists; shebang line ✓; `resolvable_ligatures` function ✓; `text_select_move_forward_word` with `_word` suffix ✓; exits 0 with `missing: []` ✓ |
| `app/src/main/res/drawable/disabled_by_default.xml` | Official MS vector, Views console ERROR keeper | VERIFIED | Exists; `viewportWidth/Height="960"` ✓; `translateY="960"` group ✓; `fillColor="#FFFFFFFF"` only ✓; header says OFFICIAL + verbatim ✓ |
| `app/src/main/res/drawable/warning.xml` | Official MS vector, Views console WARNING keeper | VERIFIED | Exists; `viewportWidth/Height="960"` ✓; `translateY="960"` group ✓; `fillColor="#FFFFFFFF"` only ✓; header says OFFICIAL + verbatim ✓ |
| `app/src/test/java/works/mees/dinghy/designsystem/icons/DinghyIconsTest.kt` | Drift-guard + uniqueness tests | VERIFIED | `registryDrawableEntries_areOnlyTheCustomKeepers` method exists ✓; asserts `setOf("nozzle", "heat_bed")` ✓; GREEN post-Plan-02 ✓ |
| `app/src/main/java/works/mees/dinghy/designsystem/icons/DinghyIcons.kt` | Six drawable→ligature flips + StatusStop rename | VERIFIED | `IconRef.Drawable` count == 2 ✓; all six ligature names present ✓; `StatusStop` val ✓; `status_stop` alternate ✓; D-06/D-14 KDoc ✓ |
| `app/src/main/java/works/mees/dinghy/ui/console/ConsoleRowsAdapter.kt` | Official vectors for Views status site | VERIFIED | `R.drawable.disabled_by_default` + `R.drawable.warning` each == 1 ✓; old names gone ✓; tint machinery unchanged ✓ |
| `app/src/main/java/works/mees/dinghy/ui/calibration/ProbeCalibrateScreen.kt` | Six raw Compose sites swapped to ligatures; nozzle kept | VERIFIED | Six raw R.drawable refs gone ✓; R.drawable.nozzle kept ✓; IconRef.Ligature present ✓ |
| `app/src/main/java/works/mees/dinghy/ui/move/MoveScreen.kt` | Caution + lock sites swapped to ligatures; clearAndSetSemantics applied | VERIFIED | Old R.drawable refs gone ✓; warning/lock_open_right/lock ligatures present ✓; clearAndSetSemantics in StatusShape ✓ |
| `app/src/main/java/works/mees/dinghy/ui/screen/ThemeEditorScreen.kt` | Status-preview swatches swapped to ligatures; clearAndSetSemantics applied | VERIFIED | Old ic_status_* refs gone ✓; disabled_by_default/warning present ✓; clearAndSetSemantics in DataSwatch + StatusSlotSwatch ✓ |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `ConsoleRowsAdapter.applySeverityGlyph` | `R.drawable.disabled_by_default` / `R.drawable.warning` | resId assignment + DrawableCompat.setTint | WIRED | Both resIds assigned; tint machinery calls `DrawableCompat.setTint(d, tint)` → `d.setBounds` → `setCompoundDrawables` |
| `DinghyIcons.StatusStop` | `IconRef.Ligature("disabled_by_default")` | registry entry | WIRED | Val present, ligature name resolves in font (verify_ligatures.py) |
| `PrintStatusScreen.kt:971` | `DinghyIcons.StatusStop` | direct call | WIRED | `DinghyIconView(DinghyIcons.StatusStop, ...)` with real contentDescription |
| `MoveScreen.StatusShape` | `MaterialSymbol` + `clearAndSetSemantics {}` | helper-internal render | WIRED | Signature `glyphName: String`; renders `MaterialSymbol(name=glyphName, modifier=modifier.clearAndSetSemantics {})` |
| `ThemeEditorScreen.DataSwatch` | `MaterialSymbol` + `clearAndSetSemantics {}` | conditional render | WIRED | `if (glyphName != null) MaterialSymbol(..., modifier = Modifier.clearAndSetSemantics {})` |
| `ProbeCalibrateScreen` detector/expand sites | `DinghyIconView(DinghyIcon(IconRef.Ligature(...)))` | inline call | WIRED | Real contentDescription → DinghyIconView with overridden semantics (not raw ligature name to TalkBack) |

### Data-Flow Trace (Level 4)

Not applicable. This phase modifies static icon assets and their render paths — no dynamic data state introduced. The icon registry (`DinghyIcons.kt`) is compile-time constants; no data-source / data-flow verification needed.

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| verify_ligatures.py exits 0 with all needed glyphs | `python tools/verify_ligatures.py` | `48 needed, 3953 ligatures in font, missing: []`, exit 0 | PASS |
| No dangling R.drawable refs to deleted names | `grep -rE "R[.]drawable[.](ic_status_octagon|...)" app/src/` | 0 results | PASS |
| StatusOctagon absent from app/src/ | `grep -rn "StatusOctagon" app/src/` | 0 results | PASS |
| 7 keeper drawables present | `ls app/src/main/res/drawable/` | Exactly 7 files listed | PASS |
| DinghyIcons.kt has exactly 2 IconRef.Drawable | `grep -c 'IconRef.Drawable' DinghyIcons.kt` | 2 | PASS |
| clearAndSetSemantics on all 3 decorative sites | grep in MoveScreen + ThemeEditorScreen | 3 occurrences found across the two files | PASS |
| Stale KDoc links removed | `grep -rE "\[DinghyPreviews\]\|\[PrintStatusThemeMatrix\]" app/src/` | 0 results | PASS |

### Probe Execution

No probes declared. `tools/verify_ligatures.py` is the phase's functional gate and was run directly above.

### Requirements Coverage

Phase 18.1 uses design D-tokens (D-01..D-16) and SC-codes (SC-1..SC-5) from `18.1-CONTEXT.md`, not REQUIREMENTS.md REQ-IDs. REQUIREMENTS.md traceability is N/A per phase instructions.

| Requirement | Source Plan | Status | Evidence |
|-------------|-------------|--------|----------|
| SC-1 (font current cut) | Plan-01 | PASSED (D-13 override) | v2.944 retained, verified sufficient via verify_ligatures.py |
| SC-2 (shape-distinct status coding) | Plans 02/03/04 | VERIFIED (D-06 relaxation) | disabled_by_default (square+✕) vs warning (triangle+!) confirmed on-device |
| SC-3 / SC-3b (only custom keepers drawable-backed) | Plans 02/04 + drift-guard | VERIFIED | Drift-guard GREEN; `IconRef.Drawable` count == 2 (nozzle/heat_bed only) |
| SC-4 (KDoc doc-links fixed) | Plan-04 | VERIFIED | No stale `[DinghyPreviews]` / `[PrintStatusThemeMatrix]` / `[AppContainer]` in PrintStatusPreviews |
| SC-5 (no regression: build + unit suite green) | Plans 02/03/04 | VERIFIED | Orchestrator-confirmed BUILD SUCCESSFUL for assembleDebug + testDebugUnitTest; post-fix commits 04d86f4/fa9a8b4/656a2d6 committed and build confirmed green |
| D-04 (device-free ligature gate) | Plan-01 | VERIFIED | verify_ligatures.py exits 0, missing: [] |
| D-05/D-06 (shape-distinct official vectors for status) | Plans 01/02/03 | VERIFIED | disabled_by_default + warning official vectors present; all call sites converted; shapes distinct |
| D-07 (Views status site uses official vectors, not font ligature) | Plan-02 | VERIFIED | ConsoleRowsAdapter uses R.drawable.disabled_by_default/warning; no setFontVariationSettings |
| D-08 (13 hand-traced drawables deleted) | Plan-04 | VERIFIED | All 13 physically absent from drawable/ directory |
| D-10 (keeper drawables preserved) | Plan-04 | VERIFIED | nozzle/heat_bed/bed_tilt/benchy/ic_launcher_foreground/disabled_by_default/warning all present |
| D-11/D-16 (KDoc broken links) | Plan-04 | VERIFIED | Five fixes applied; no stale links remain |
| D-12 (no regression) | Plan-04 | VERIFIED | Orchestrator-confirmed full build + test suite green |
| D-13 (font v2.944 retained) | Plan-01 | VERIFIED | Font file unchanged at 10,600,212 bytes; verify_ligatures.py confirms sufficiency |
| D-14 (StatusOctagon→StatusStop rename) | Plan-02 | VERIFIED | Zero StatusOctagon in app/src/; StatusStop val + status_stop alternate + KDoc present |
| D-15 (six-entry flip set; D-09 "four" was non-exhaustive) | Plans 01/02 | VERIFIED | Drift-guard asserts exactly {nozzle, heat_bed}; all six flipped entries confirmed |
| WR-01 (a11y: decorative status shapes silent) | Post-review fix (04d86f4) | VERIFIED | clearAndSetSemantics {} present in MoveScreen.StatusShape, ThemeEditorScreen.DataSwatch, ThemeEditorScreen.StatusSlotSwatch |
| WR-02 (PrintStatusPreviews theme-matrix link) | Post-review fix (fa9a8b4) | VERIFIED | Line 26 uses backtick `PrintStatusTheme*` notation; no broken link |
| WR-03 (DinghyIcons stale FineTune KDoc) | Post-review fix (656a2d6) | VERIFIED | Comment at lines 86-87 now reads "The FineTune glyphs below now ALSO render the OFFICIAL Material Symbols glyph via the bundled font (Ligature) — the 18.1 flip retired their hand-traced local vectors (D-08/D-15)" |

### Anti-Patterns Found

| File | Pattern | Severity | Impact |
|------|---------|----------|--------|
| `18.1-REVIEW.md` | `status: issues_found` in frontmatter | Info | The REVIEW.md was written before the 3 post-review fixes (commits 04d86f4, fa9a8b4, 656a2d6). The status is stale — all three warnings were fixed. Not a code concern; informational only. |

No TBD/FIXME/XXX markers found in phase-modified files. No stubs. No hardcoded empty data.

### Human Verification Required

None. The sole human-verify gate (Task 3 of Plan-04: on-device icon spot check on flox) was completed and owner-approved during phase execution ("Looks good" — all glyphs render without tofu, status shapes shape-distinct). This satisfies the SC-2/D-04/D-06 on-device gate. No further human verification items remain.

### Gaps Summary

No gaps. All 12 must-have truths verified. Phase goal achieved.

The REVIEW.md `status: issues_found` field is stale documentation (the 3 warnings WR-01/WR-02/WR-03 were all fixed in commits 04d86f4, fa9a8b4, 656a2d6 post-review). This is a documentation artifact, not a code gap.

---

_Verified: 2026-06-07T20:15:00Z_
_Verifier: Claude (gsd-verifier)_
