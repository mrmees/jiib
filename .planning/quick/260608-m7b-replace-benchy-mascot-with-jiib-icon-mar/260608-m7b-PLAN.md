---
phase: quick-260608-m7b
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/main/res/drawable/jiib_icon.xml
  - app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusScreen.kt
autonomous: true
requirements: [M7B-01]
must_haves:
  truths:
    - "Print Status idle (no-job) ring center shows the jiib mark, accent2-tinted, square 1:1"
    - "Print Status Focus backdrop shows the jiib mark, accent2-tinted at 0.45 alpha, Crop-filled"
    - ":app:assembleDebug builds clean and the new drawable resolves with no aapt error"
  artifacts:
    - path: "app/src/main/res/drawable/jiib_icon.xml"
      provides: "Tintable single-fill full-bleed jiib VectorDrawable"
      contains: "android:fillColor"
    - path: "app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusScreen.kt"
      provides: "Both benchy use-sites swapped to jiib_icon"
      contains: "R.drawable.jiib_icon"
  key_links:
    - from: "PrintStatusScreen.kt"
      to: "R.drawable.jiib_icon"
      via: "painterResource"
      pattern: "R\\.drawable\\.jiib_icon"
---

<objective>
Replace the benchy mascot with the jiib icon mark on the Print Status (home) screen,
accent-tinted, in both benchy use-sites (idle ring-center Icon + faint Focus backdrop Image).

Purpose: Carry the Phase-18.2 jiib rebrand onto the Print Status home surface — the benchy
mascot was a placeholder; the home screen should wear the brand mark.
Output: A new tintable `jiib_icon.xml` drawable + two swapped call-sites in PrintStatusScreen.kt.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md

# The existing converted jiib mark — REUSE this path geometry (do NOT hand-convert raw SVG circles)
@app/src/main/res/drawable/ic_jiib_foreground.xml
# The drawable being swapped out — keep it (still referenced elsewhere / per-printer brandable)
@app/src/main/res/drawable/benchy.xml

Build helper (Windows-side, repo root):
`/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat <args>" | tr -d '\r'` (exit code is authoritative)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the tintable full-bleed jiib_icon.xml drawable</name>
  <files>app/src/main/res/drawable/jiib_icon.xml</files>
  <action>
Create `app/src/main/res/drawable/jiib_icon.xml` as a TINTABLE single-fill Android VectorDrawable
of the jiib mark, full-bleed in its own square viewport.

REUSE the path geometry from `ic_jiib_foreground.xml` — that file already converted
`img/jiib_icon.svg` (3 paths + 3 circles) into valid Android pathData during the 18.2 rebrand.
Copy its SIX `<path android:pathData="...">` strings VERBATIM (the three sail paths and the three
two-arc circle paths). Do NOT hand-convert the raw SVG `<circle>` elements — Android vectors have
no circle primitive, and that conversion is already done in ic_jiib_foreground.xml.

Strip the launcher safe-zone inset: ic_jiib_foreground.xml wraps the paths in a
`<group android:scaleX="0.16" android:scaleY="0.16" android:translateX="11.0" android:translateY="22.4">`
that shrinks the mark to ~16% to fit the adaptive-icon mask. DROP that group entirely so the paths
draw at their native coordinates and FILL the viewport. The native path coordinates span roughly
x: 66.5–471, y: 20.1–382.4 — set a SQUARE viewport `android:viewportWidth="480" android:viewportHeight="480"`
so the mark is full-bleed with minimal margin (square keeps the 1:1 aspect the call-sites expect).
Set `android:width="480dp" android:height="480dp"` to match the viewport.

Use a single solid `android:fillColor="#FFFFFFFF"` on ALL six paths (as ic_jiib_foreground already
does) so a Compose `Icon` tint / `ColorFilter.tint` recolors the whole mark. No theme tokens in the
drawable itself — tint is applied at the call-site, same pattern as benchy.xml's white-strokes.
  </action>
  <verify>
    <automated>test -f app/src/main/res/drawable/jiib_icon.xml &amp;&amp; grep -c 'android:fillColor="#FFFFFFFF"' app/src/main/res/drawable/jiib_icon.xml</automated>
  </verify>
  <done>jiib_icon.xml exists, has a 480x480 square viewport, NO `<group>` transform, and six `<path>` elements each with `android:fillColor="#FFFFFFFF"` (count == 6).</done>
</task>

<task type="auto">
  <name>Task 2: Swap both benchy use-sites in PrintStatusScreen.kt to jiib_icon</name>
  <files>app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusScreen.kt</files>
  <action>
Swap the two benchy references to the new drawable.

Site A — idle no-job Icon in the progress-ring center (~line 667):
  - `painter = painterResource(R.drawable.benchy)` → `painter = painterResource(R.drawable.jiib_icon)`
  - `modifier = Modifier.fillMaxWidth().aspectRatio(1600f / 900f)` → `modifier = Modifier.fillMaxWidth().aspectRatio(1f)`
    (jiib mark is square 1:1; benchy was 16:9.)
  - KEEP `tint = t.accent2` and `contentDescription = null` unchanged.

Site B — faint backdrop Image in the Focus region (~line 1030):
  - `painter = painterResource(R.drawable.benchy)` → `painter = painterResource(R.drawable.jiib_icon)`
  - KEEP `contentScale = ContentScale.Crop`, `colorFilter = ColorFilter.tint(t.accent2)`,
    `alpha = 0.45f`, and the `fillMaxSize()` modifier unchanged.

Do NOT touch `benchy.xml` or `img/benchy.svg` — benchy remains referenced elsewhere and is the
per-printer brandable default. Only the two PrintStatusScreen.kt references change.

Refresh the two now-stale code comments that name "Benchy" if you wish (the idle-image comment ~656
and the backdrop comment ~1025), but that is optional polish — the functional change is the two
resource swaps + the aspectRatio.
  </action>
  <verify>
    <automated>grep -c "R.drawable.benchy" app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusScreen.kt; grep -c "R.drawable.jiib_icon" app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusScreen.kt; /mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:assembleDebug --no-daemon" | tr -d '\r'</automated>
  </verify>
  <done>No remaining `R.drawable.benchy` in PrintStatusScreen.kt (count == 0), exactly two `R.drawable.jiib_icon` references (count == 2), Site A uses `aspectRatio(1f)`, and `:app:assembleDebug` exits 0 with no aapt resource error.</done>
</task>

</tasks>

<verification>
- `:app:assembleDebug` builds clean Windows-side (exit code 0).
- New drawable `jiib_icon.xml` resolves with no aapt resource-linking error.
- `grep -c "R.drawable.benchy" PrintStatusScreen.kt` == 0; `grep -c "R.drawable.jiib_icon" PrintStatusScreen.kt` == 2.
- Final step: `:app:installDebug` on flox, then OWNER eyeballs the idle ring center + Focus backdrop
  (jiib mark accent-tinted, square, not stretched) — owner does the visual confirmation.
</verification>

<success_criteria>
- jiib_icon.xml is a single-fill, full-bleed, square VectorDrawable that tints via Compose.
- Print Status idle ring center shows the accent2-tinted jiib mark at 1:1 (not stretched 16:9).
- Print Status Focus backdrop shows the accent2-tinted jiib mark, Crop-filled at 0.45 alpha.
- benchy.xml / img/benchy.svg untouched.
- Release build path unaffected; debug build clean.
</success_criteria>

<output>
Create `.planning/quick/260608-m7b-replace-benchy-mascot-with-jiib-icon-mar/260608-m7b-SUMMARY.md` when done
</output>
