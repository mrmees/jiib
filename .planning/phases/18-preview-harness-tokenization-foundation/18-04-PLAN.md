---
phase: 18-preview-harness-tokenization-foundation
plan: 04
type: execute
wave: 2
depends_on:
  - 18-01
files_modified:
  - app/src/main/java/works/mees/dinghy/MainActivity.kt
  - app/src/main/java/works/mees/dinghy/ui/shell/RootController.kt
  - app/src/test/java/works/mees/dinghy/shell/StartDestMappingTest.kt
autonomous: false
requirements:
  - SC-4 (debug start_dest hook jumps the running app to a screen; absent/inert in release)
  - D-06 (live/perf truth stays on start_dest + flox + the existing BenchActivity/ViewsBenchScene path; NO new standalone View-rendering harness this phase)

must_haves:
  truths:
    - "A debug-gated start_dest intent extra on MainActivity seeds ShellNavState.dest once, gated on devCyclerEnabled (default FALSE). Release-inert is verified from a CLEAN app-data state (pm clear / fresh install) AND by confirming the release UI exposes no way to set devCyclerEnabled=true — not merely by trusting the default"
    - "The extra is parsed through a safe Dest-with-fallback (unknown value → ignored/default, never crash)"
    - "The seeded dest lands when Klippy is Ready (the RootController Splash gate applies on top — land-with-whatever-state), seeded ONCE in RootController not from a LaunchedEffect in AppShell"
    - "Live/perf truth for the classic-View surfaces stays on this start_dest + flox path (D-06) — no new standalone View-rendering harness is built this phase"
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/MainActivity.kt"
      provides: "Dev-gated start_dest read in onCreate, threaded into RootController"
      contains: "start_dest"
    - path: "app/src/main/java/works/mees/dinghy/ui/shell/RootController.kt"
      provides: "Optional startDest: Dest? threaded into rememberShellNavState, applied once"
      contains: "startDest"
    - path: "app/src/test/java/works/mees/dinghy/shell/StartDestMappingTest.kt"
      provides: "Pure extra-string→Dest mapping unit test incl. unknown→null"
      contains: "StartDestMapping"
  key_links:
    - from: "MainActivity.onCreate"
      to: "container.devCyclerEnabled gate"
      via: "read start_dest extra ONLY when devCyclerEnabled is true"
      pattern: "devCyclerEnabled"
    - from: "MainActivity"
      to: "RootController(startDest=)"
      via: "thread parsed Dest? into the hoisted ShellNavState"
      pattern: "startDest"
---

<objective>
Add the debug-gated `start_dest` intent hook on `MainActivity` (mirroring `BenchActivity`'s
`EXTRA_SCENE`) so the must-be-live cases the preview harness cannot cover (real Moonraker/Klippy state,
classic-View perf truth on flox) can be jumped to directly via `am start ... --es start_dest <Dest>`.

Purpose: SC-4b + D-06. This is the SECONDARY tool (the preview harness is primary for deterministic
state-injected review); per D-06 it is also THE path for live/perf truth of the classic-View surfaces
(GraphView/BedMeshHeatmapView/WebcamView) on flox via the existing BenchActivity/ViewsBenchScene route —
NO new standalone View-rendering harness is built this phase. The security-critical surface of the
phase: `MainActivity` is `exported="true"`, so the hook MUST be gated by the existing `devCyclerEnabled`
DataStore flag (default FALSE — inert in release), NOT `BuildConfig.DEBUG`, and the extra MUST be safe-parsed.
Output: dev-gated `start_dest` read in `MainActivity`, `startDest` threaded into `RootController`,
StartDestMappingTest GREEN. The instrumented flox jump is verified at the checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-preview-harness-tokenization-foundation/18-CONTEXT.md
@.planning/phases/18-preview-harness-tokenization-foundation/18-RESEARCH.md
@.planning/phases/18-preview-harness-tokenization-foundation/18-PATTERNS.md
@.planning/phases/18-preview-harness-tokenization-foundation/18-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Safe start_dest parser + StartDestMappingTest GREEN</name>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/route/TopRoute.kt (line 37 — the Dest enum, 15 values)
    - app/src/main/java/works/mees/dinghy/bench/BenchActivity.kt (line 62 EXTRA_SCENE read; line 176 companion const)
    - app/src/test/java/works/mees/dinghy/shell/StartDestMappingTest.kt (the compile scaffold from 18-01 — replace its `// TODO(18-NN):` pending marker)
    - 18-RESEARCH.md §Q3 + §Security Domain (V5 — safe enum parse with default) + 18-PATTERNS.md §"MainActivity.kt modification"
  </read_first>
  <action>
    Implement a pure `parseStartDest(raw: String?): Dest?` mapping fn (place it where it is unit-testable
    without an Activity — e.g. a top-level fn in the shell package or alongside `Dest`). Map the extra
    string through `Dest.valueOf`-with-fallback: a recognized name → the matching `Dest`; null/blank/unknown
    → `null` (ignore, never crash). This is the SC-4b unit half + the V5 input-validation mitigation
    (untrusted intent extra). Turn the 18-01 `StartDestMappingTest.kt` compile scaffold (replace its `// TODO(18-04):` pending marker) into real assertions:
    each valid name maps to its `Dest`; garbage/empty/null → `null`.
  </action>
  <verify>
    <automated>cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --tests '*StartDestMappingTest' --no-daemon" | tr -d '\r'  (exit 0)</automated>
  </verify>
  <acceptance_criteria>
    - CI: `StartDestMappingTest` passes — valid names map; unknown/blank/null → null (SC-4b unit) — CI-assertable
    - CI: `parseStartDest` never throws on arbitrary input (test feeds garbage) — CI-assertable (V5 mitigation)
  </acceptance_criteria>
  <done>Pure safe mapping fn exists; mapping test GREEN incl. garbage→null.</done>
</task>

<task type="auto">
  <name>Task 2: Dev-gated read in MainActivity + thread startDest through RootController</name>
  <read_first>
    - app/src/main/java/works/mees/dinghy/MainActivity.kt (lines 31-55 — onCreate; container at :34; setContent at :40; reads NO intent today)
    - app/src/main/java/works/mees/dinghy/ui/shell/RootController.kt (line 44 fun signature; line 55 rememberShellNavState hoist; line 121 Splash gate; line 133 Ready/else branch composes AppShell)
    - app/src/main/java/works/mees/dinghy/ui/shell/ShellNavState.kt (line 45 dest default PrintStatus; lines 123-124 rememberShellNavState initializer)
    - app/src/main/java/works/mees/dinghy/di/AppContainer.kt (line 242 `val devCyclerEnabled: Flow<Boolean> = themePrefs.devEnableFlow` — it is a Flow, NOT a cached value; line 118 writeScope — read-only here, no write needed)
    - app/src/main/java/works/mees/dinghy/theme/ThemePrefs.kt (line 117 `devEnableFlow`; line 179 `dev_cycler_enabled` key — confirms there is NO synchronous cached accessor; a bounded first() read is required)
    - app/src/main/AndroidManifest.xml (lines 64-65 — MainActivity exported="true")
    - 18-RESEARCH.md §Q3 (land-with-whatever-state; seed once in RootController; anti-pattern: LaunchedEffect in AppShell)
    - 18-PATTERNS.md §"RootController.kt modification" + §"Dev-gate" shared pattern
  </read_first>
  <action>
    EXACT READ SEAM (Codex MEDIUM-4): `container.devCyclerEnabled` is a `Flow<Boolean>`
    (AppContainer.kt:242) — there is NO cached synchronous accessor, so do NOT do an unbounded
    blocking DataStore read on the UI thread. In `MainActivity.onCreate` (after `container`
    resolves at :34, before `setContent`), read the gate via a BOUNDED one-shot first-emission
    read: `runBlocking { withTimeoutOrNull(500) { container.devCyclerEnabled.first() } ?: false }`
    (DataStore's first read is fast; the timeout guarantees onCreate cannot hang if the store is
    slow/empty — gate defaults to FALSE on timeout, the safe/inert value). Prefer a small private
    helper (e.g. `private fun AppContainer.devCyclerEnabledBlocking(): Boolean`) so the seam is
    named and unit-reasonable. Only when that returns true, read the
    `start_dest` extra (one-shot READ — no DataStore write, so no writeScope needed); parse via
    `parseStartDest(intent.getStringExtra("start_dest"))`;
    pass the resulting `Dest?` into `RootController(container, startDest = ...)`. Add a companion const
    for the extra key (mirror `BenchActivity.EXTRA_SCENE`). In `RootController`, add a
    `startDest: Dest? = null` param and seed `ShellNavState.dest` ONCE from it (apply in the
    `rememberShellNavState` initializer or a Unit-keyed init guarded so it cannot re-fire) — do NOT seed
    from a `LaunchedEffect` inside `AppShell` (re-fires on recompose/recovery — RESEARCH anti-pattern).
    The Splash gate (RootController:121) applies on top: the user boots to Splash and lands on the
    seeded screen when Klippy goes Ready (land-with-whatever-state, RESEARCH Q3). Per D-06 this is the
    live path for the classic-View surfaces' perf truth (jump to Temperature/Webcam/BedMesh on flox) —
    no new View harness is added. Gate-OFF behavior (devCyclerEnabled=false) must leave `dest` at its
    `PrintStatus` default.
  </action>
  <verify>
    <automated>cmd.exe /c "E:\Android\gw.bat :app:assembleDebug :app:testDebugUnitTest --no-daemon" | tr -d '\r'  (exit 0 — no regression)</automated>
  </verify>
  <acceptance_criteria>
    - CI: `MainActivity.kt` reads the `start_dest` extra gated on a BOUNDED `devCyclerEnabled` first-emission read (grep `devCyclerEnabled` AND `withTimeoutOrNull` — NOT an unbounded `.first()`/`runBlocking` without a timeout) — CI-assertable
    - CI: `RootController.kt` accepts `startDest: Dest?` and seeds it once (grep `startDest`); NO `LaunchedEffect`-in-AppShell seeding — CI-assertable
    - CI: `:app:assembleDebug` + full `:app:testDebugUnitTest` green (no regression, SC-5) — CI-assertable
    - flox: see Task 3 (live jump + release-inert) — flox tier
  </acceptance_criteria>
  <done>Dev-gated start_dest read + threaded + seeded once; gated off by default; suite green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: flox — start_dest jumps the running app; inert in release</name>
  <what-built>
    A debug-gated `start_dest` intent extra on the exported `MainActivity` that seeds the initial screen
    when `devCyclerEnabled` is on, landing when Klippy is Ready; inert (no-op) when the gate is off / in
    release. Also the D-06 live path for classic-View perf truth on flox (jump to Temperature/Webcam/BedMesh).
  </what-built>
  <action>Build + install the debug APK on flox, then run the am-start verification steps below; this is a blocking on-device gate (the harness cannot prove the live jump or release-inert behavior).</action>
  <how-to-verify>
    1. Build + install the debug APK on flox (Windows-side via `E:\Android\gw.bat`; `adb` via interop).
    2. With dev-enable ON: `adb shell am start -n works.mees.dinghy/.MainActivity --es start_dest FineTune`
       — confirm the app boots and lands on the FineTune screen (after Splash, once Klippy Ready).
       Repeat with `--es start_dest Spool` and `--es start_dest PrintStatus`.
    3. Garbage check: `--es start_dest NotARealScreen` — confirm the app launches normally to the default
       screen and does NOT crash.
    4. Release-inert check (Codex MEDIUM-5) — prove inert from a CLEAN data state, not a leaked
       debug flag: `adb shell pm clear works.mees.dinghy` (or uninstall+reinstall the RELEASE APK)
       so `dev_cycler_enabled` is at its FALSE default, then repeat step 2 and confirm the extra is
       IGNORED (lands on the default screen). ALSO confirm the release build exposes NO UI path that
       can set `setDevCyclerEnabled(true)` (the dev cycler overlay is itself dev-gated) — so a
       release user cannot enable the flag and reach the jump. Record both observations in the SUMMARY.
    5. (D-06 spot-check, optional) jump to a classic-View screen (e.g. `--es start_dest Temperature` or
       `Webcam`) and confirm it renders live on-device — proving the View-surface perf path stays on flox.
  </how-to-verify>
  <resume-signal>Type "approved" or describe what failed (which Dest, crash, gate leak)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OS / other apps → exported MainActivity | `am start ... --es start_dest <value>` crosses an exported-activity boundary; the extra is UNTRUSTED input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-04-01 | Elevation (minor) | exported MainActivity honoring a deep-jump in release | mitigate | Gate the `start_dest` read behind `container.devCyclerEnabled` (DataStore, default FALSE) — NOT `BuildConfig.DEBUG`. Release-inert is only guaranteed if the flag cannot be enabled/persisted in release: verified at Task 3 step 4 from a CLEAN app-data state (`pm clear`/fresh install) AND by confirming the release UI exposes no `setDevCyclerEnabled(true)` path. The bounded `withTimeoutOrNull` read also defaults to FALSE on any read failure. |
| T-18-04-02 | Tampering / DoS | malicious `am start --es start_dest <garbage>` | mitigate | Safe `parseStartDest` enum-with-fallback: unknown/blank/null → `null`, never crash (Task 1 unit-tested with garbage input; Task 3 step 3 on flox). |
| T-18-04-03 | (none) | DataStore | accept | The jump is a one-shot intent READ — no DataStore WRITE — so the write-scope-cancellation trap does not apply here. (If a future change toggles `devCyclerEnabled`, route through `AppContainer.writeScope`.) |
</threat_model>

<verification>
- `StartDestMappingTest` green (incl. garbage→null).
- `:app:assembleDebug` + full `:app:testDebugUnitTest` green.
- flox (Task 3): jump lands on FineTune/Spool/PrintStatus; garbage → no crash; gate-off → ignored; D-06 View-screen renders live.
</verification>

<success_criteria>
SC-4b: debug `start_dest` hook jumps the running app to a screen, dev-gated (not BuildConfig.DEBUG),
safe-parsed, and absent/inert in release. D-06: live/perf truth path for the classic-View surfaces.
</success_criteria>

<output>
Create `.planning/phases/18-preview-harness-tokenization-foundation/18-04-SUMMARY.md` when done.
</output>
