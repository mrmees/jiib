---
phase: 18-preview-harness-tokenization-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 18-01
files_modified:
  - app/src/main/java/works/mees/dinghy/preview/PreviewTheming.kt
  - app/src/main/java/works/mees/dinghy/preview/SampleFixtures.kt
  - app/src/main/java/works/mees/dinghy/preview/DinghyPreviews.kt
  - app/src/main/java/works/mees/dinghy/preview/PreviewPlaceholders.kt
  - app/src/main/java/works/mees/dinghy/render/GraphViewHost.kt
  - app/src/main/java/works/mees/dinghy/render/BedMeshHeatmapHost.kt
  - app/src/main/java/works/mees/dinghy/render/WebcamViewHost.kt
  - app/src/test/java/works/mees/dinghy/preview/SampleFixturesTest.kt
  - app/src/test/java/works/mees/dinghy/preview/PreviewBoxSmokeTest.kt
autonomous: true
requirements:
  - SC-1 (preview-token provider + Nexus-7 profile + fs=L matrix — the infra)
  - SC-2 (reusable fixture module + provider + device profile)
  - D-04 (classic Views excluded from dedicated previews — documented)
  - D-05 (LocalInspectionMode placeholder branch at View embed hosts)

must_haves:
  truths:
    - "A PreviewBox(tuple) wrapper renders any composable through the existing DinghyTheme(tokensFlow=)/bake boundary across the 6 theme combos + an fs=L seed"
    - "The PreviewBox bake/theme seam is proven by a compile-only smoke target (a trivial sample @Preview + a PreviewBoxSmokeTest that bakes each seed tuple) so a wrong overload/constructor assumption fails loudly in Wave 2, not in every downstream exemplar"
    - "SampleFixtures exposes all 4/6 PrintStatusMode states, FineTune present/absent/busy variants, a dense spool list, and temp series — reusable, pure, NO live Moonraker"
    - "A Nexus-7 2013 device profile (portrait + landscape geometry) exists as a reusable multipreview annotation; the six THEME combos come from explicit PreviewBox wrapper functions, NOT the annotation"
    - "GraphViewHost (BOTH overloads), BedMeshHeatmapHost, and WebcamViewHost render a labeled stand-in box under LocalInspectionMode instead of a blank/broken AndroidView region"
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/preview/PreviewTheming.kt"
      provides: "PreviewBox wrapper + the 6 ThemeTuple combo seeds + the fs=L seed"
      contains: "fun PreviewBox"
    - path: "app/src/main/java/works/mees/dinghy/preview/SampleFixtures.kt"
      provides: "Reusable pure fixtures (modes, fineTune variants, spool, temps)"
      contains: "object SampleFixtures"
    - path: "app/src/main/java/works/mees/dinghy/preview/DinghyPreviews.kt"
      provides: "@Nexus7Previews + @DeviceAndLocalePreviews multipreview annotations (device/uiMode/locale ONLY — NOT theme selection)"
      contains: "annotation class"
    - path: "app/src/main/java/works/mees/dinghy/preview/PreviewPlaceholders.kt"
      provides: "PreviewPlaceholderBox + LocalInspectionMode helpers (D-05/Q5)"
      contains: "PreviewPlaceholderBox"
    - path: "app/src/test/java/works/mees/dinghy/preview/PreviewBoxSmokeTest.kt"
      provides: "Compile/unit smoke proving ThemeResolver().bake(seed) for each of the 6 seeds returns distinct baked tokens (the bake/theme seam)"
      contains: "bake"
  key_links:
    - from: "preview/PreviewTheming.kt"
      to: "theme/compose/DinghyTheme.kt tokensFlow overload"
      via: "ThemeResolver().bake(tuple) → flowOf → DinghyTheme(tokensFlow=)"
      pattern: "DinghyTheme\\(|\\.bake\\("
    - from: "render/GraphViewHost.kt"
      to: "preview/PreviewPlaceholders.kt"
      via: "if (LocalInspectionMode.current) PreviewPlaceholderBox(...) return"
      pattern: "LocalInspectionMode"
---

<objective>
Build the reusable `preview/` package — the themed fixture/fake-state infrastructure that every
later UI phase builds against — plus the D-05 `LocalInspectionMode` placeholder branch at the three
classic-View embed hosts so screens that embed a View still preview cleanly.

Purpose: This is the SC-1/SC-2 infrastructure backbone. It establishes the ONE preview theme
boundary (no preview-only theme code — reuses the real `DinghyTheme(tokensFlow=)` overload +
`ThemeResolver.bake`), the Nexus-7 device profile, the fs=L injection mechanism (via `ThemeTuple.fs`,
NOT `@Preview(fontScale=)` which is a NO-OP here), and the D-05 placeholder idiom the exemplars
consume. Because this seam is the ROOT every downstream exemplar depends on, Task 1's FIRST
acceptance criterion is a compile-only smoke target proving the bake/theme seam — so a wrong overload
or constructor assumption fails in Wave 2, not silently across every screen.
Output: `preview/PreviewTheming.kt`, `SampleFixtures.kt`, `DinghyPreviews.kt`, `PreviewPlaceholders.kt`;
`PreviewBoxSmokeTest.kt`; D-05 branch in 3 hosts; SampleFixturesTest turned into live assertions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-preview-harness-tokenization-foundation/18-CONTEXT.md
@.planning/phases/18-preview-harness-tokenization-foundation/18-RESEARCH.md
@.planning/phases/18-preview-harness-tokenization-foundation/18-PATTERNS.md
@.planning/phases/18-preview-harness-tokenization-foundation/18-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: PreviewTheming + Nexus-7 + multipreview annotations + the PreviewBox bake-seam smoke target</name>
  <read_first>
    - app/src/main/java/works/mees/dinghy/theme/compose/DinghyTheme.kt (BOTH public overloads: `fun DinghyTheme(tokensFlow: Flow<ThemeTokens>, content)` at :37-51 AND `fun DinghyTheme(resolver: ThemeResolver, content)` at :59-63; fontScale=1f pin at :48) — VERIFY the exact signatures here before coding
    - app/src/main/java/works/mees/dinghy/theme/ThemeResolver.kt (class header :31-42 — ALL constructor params default, so `ThemeResolver()` no-arg compiles, confirmed by AppContainer.kt:232 + BenchActivity.kt:80; `fun bake(tuple): ThemeTokens` at :162 — the pure per-tuple bake fn)
    - app/src/main/java/works/mees/dinghy/di/AppContainer.kt (lines 452-456 — the production bake CALL idiom `themeResolver.bake(tuple)`)
    - app/src/main/java/works/mees/dinghy/theme/ThemePrefs.kt (line 151 ThemeTuple data class; line 162 `val fs: Float`)
    - app/src/main/java/works/mees/dinghy/bench/BenchActivity.kt (lines 88-98 — the existing DinghyTheme(resolver){} host shape)
    - 18-RESEARCH.md §Pattern 1 + §Pattern 2 + §Q8 (fs=L injection, Nexus-7 spec, multipreview)
    - 18-PATTERNS.md §"PreviewTheming.kt" section (the bake-and-flow seam + ⚠ fs=L trap)
  </read_first>
  <action>
    SEAM VERIFICATION FIRST (Codex HIGH-2): confirm in `DinghyTheme.kt` that the public
    `fun DinghyTheme(tokensFlow: Flow<ThemeTokens>, content)` overload (:37-51) exists and that
    `ThemeResolver()` (no-arg) + `bake(tuple)` are callable. The verified facts (this session):
    both overloads are public; `ThemeResolver()` constructs with all-default params; `bake(tuple)` is
    a pure per-tuple fn. So `DinghyTheme(tokensFlow = flowOf(ThemeResolver().bake(tuple))) { content() }`
    is the correct, compiling seam — use it. If reading the source reveals the signature has changed,
    fall back to the `DinghyTheme(resolver = ThemeResolver(<seeded fields>))` overload (:59-63), which
    is the RESEARCH-cited alternative; record which overload was used in the SUMMARY.
    Create `preview/PreviewTheming.kt` with `@Composable fun PreviewBox(tuple: ThemePrefs.ThemeTuple, content)`
    seeding tokens via that overload — reuse the ONE token boundary, NO preview-only token map (per
    RESEARCH Don't-Hand-Roll). Expose the SIX combo seeds as named `ThemeTuple`s
    ({Colorful,Simple,HighContrast}×{light,dark}) sourced from `ThemePrefs.ThemeTuple` with the correct
    `paletteMode`/`dark`/`seedHex`, PLUS one extra seed with `fs = L` for the overflow check (inject via
    `ThemeTuple.fs` — `@Preview(fontScale=)` is a NO-OP because OS fontScale is pinned to 1f at
    DinghyTheme.kt:48; document this loudly in a KDoc).
    SMOKE TARGET (the FIRST acceptance criterion): create `app/src/test/.../preview/PreviewBoxSmokeTest.kt`
    — a pure JVM unit test that, for each of the 6 named seed tuples + the fs=L seed, calls
    `ThemeResolver().bake(seed)` and asserts it returns a non-null `ThemeTokens` and that the six combos
    are not all identical (light≠dark, mode differences). This proves the bake/theme seam compiles AND
    works without rendering a single @Preview, so a wrong overload/constructor assumption fails HERE in
    Wave 2 rather than silently in every downstream exemplar. (The @Composable PreviewBox render path
    itself is still Studio-eyeballed at the phase gate; the smoke test guards the data seam.)
    Create `preview/DinghyPreviews.kt` with the Nexus-7 2013 device specs as constants
    (`NEXUS7 = "spec:width=1920px,height=1200px,dpi=320,orientation=landscape"` + a portrait variant)
    and a `@Nexus7Previews` multipreview annotation (portrait + landscape) and a
    `@DeviceAndLocalePreviews` annotation bundling the canonical device/uiMode/locale review group.
    ⚠ NAMING (Codex MEDIUM-3): do NOT name this `@DinghyThemePreviews`. A `@Preview` multipreview
    annotation can set device/uiMode/locale but CANNOT select the Colorful/Simple/HighContrast token
    sets — those come ONLY from explicit `PreviewBox(seed)` wrapper functions (which 18-05/06/07 use).
    Add a one-line KDoc to the annotation stating exactly that, so Phases 19-21 (which COPY this
    template) don't believe the annotation mints the six themes. Caveat in KDoc: device spec is GEOMETRY
    only — says nothing about Adreno-320 perf (host-rendered).
  </action>
  <verify>
    <automated>cmd.exe /c "E:\Android\gw.bat :app:compileDebugKotlin :app:testDebugUnitTest --tests '*PreviewBoxSmokeTest' --no-daemon" | tr -d '\r'  (exit 0)</automated>
  </verify>
  <acceptance_criteria>
    - CI (FIRST, the seam guard): `PreviewBoxSmokeTest` passes — `ThemeResolver().bake(seed)` returns non-null tokens for all 6 combos + fs=L, and the combos are not all identical (proves the bake/theme seam compiles + works) — CI-assertable
    - CI: `:app:compileDebugKotlin` exits 0 — CI-assertable
    - CI: `preview/PreviewTheming.kt` defines `fun PreviewBox` AND exposes ≥6 named seed tuples + an fs=L seed (grep `ThemeTuple` count ≥7) — CI-assertable
    - CI: `preview/DinghyPreviews.kt` defines `NEXUS7` spec string with `dpi=320` and a `@Nexus7Previews` annotation class AND a `@DeviceAndLocalePreviews` annotation (NOT `@DinghyThemePreviews`) with the "device/locale only — themes come from PreviewBox wrappers" KDoc note (grep) — CI-assertable
    - Studio: opening a trivial `@Preview` wrapped in `PreviewBox(colorfulDark)` renders themed (not default) tokens — Studio human-eyeball (deferred to phase gate)
  </acceptance_criteria>
  <done>PreviewBox seeds 6 combos + fs=L through the real theme boundary; the bake-seam smoke test passes; Nexus-7 + device/locale multipreviews exist with honest naming; compiles.</done>
</task>

<task type="auto">
  <name>Task 2: SampleFixtures (pure fake-state) + PreviewPlaceholders + turn SampleFixturesTest live</name>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusMode.kt (lines 23-38 — the 4/6 states + TerminalKind)
    - app/src/main/java/works/mees/dinghy/ui/finetune/FineTuneVm.kt (lines 49-76 — the capability gates + groupBusy fixtures flip)
    - app/src/main/java/works/mees/dinghy/ui/spool/SpoolScreen.kt + SpoolHolder.kt (the spool state shape the dense fixture builds to)
    - app/src/main/java/works/mees/dinghy/bench/SyntheticFeed.kt (existing deterministic-fixture analog)
    - app/src/test/java/works/mees/dinghy/preview/SampleFixturesTest.kt (the compile scaffold from 18-01 to make live)
    - 18-PATTERNS.md §"SampleFixtures.kt" section (the state types + caveat: plain immutable, reusable objects)
  </read_first>
  <action>
    Create `preview/SampleFixtures.kt` as `object SampleFixtures` exposing REUSABLE, pure, immutable
    stand-ins (NOT per-preview one-offs — they are the seed the Phase-22 backfill reuses):
    all 4/6 `PrintStatusMode` states (`Standby`, `Printing`, `Paused`, `Terminal(Complete|Cancelled|Error)`)
    + a `forMode(mode)` builder shaping the PrintStatus screen state per mode; FineTune fixtures
    `fineTuneAllPresent` / `fineTuneNoFwRetraction` (absent→HIDDEN path) / `fineTuneBusy` (`groupBusy=true`)
    built off `FineTuneVm` defaults (FineTuneVm.kt:66-75); a dense `spoolList`; a temp series. NO live
    Moonraker — pure data. Create `preview/PreviewPlaceholders.kt` with `@Composable fun PreviewPlaceholderBox(label, modifier)`
    painting a `LocalTokens.current`-colored outline + centered label (D-05 "labeled stand-in, not blank")
    — mirror `MaterialSymbol`'s `tint = LocalTokens.current.text` idiom. Then turn the 18-01
    `SampleFixturesTest.kt` compile scaffold (replace its `// TODO(18-02):` pending marker) into real
    typed assertions against `SampleFixtures` (SC-2).
  </action>
  <verify>
    <automated>cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --tests '*SampleFixturesTest' --no-daemon" | tr -d '\r'  (exit 0)</automated>
  </verify>
  <acceptance_criteria>
    - CI: `SampleFixturesTest` passes asserting all 4/6 PrintStatusMode states present + non-empty spool/temp + 3 FineTune variants (SC-2) — CI-assertable
    - CI: grep `SampleFixtures.kt` for NO Moonraker/network import (no `MoonrakerClient`, `okhttp`, `RpcConnection`) — CI-assertable
    - CI: `preview/PreviewPlaceholders.kt` defines `PreviewPlaceholderBox` reading `LocalTokens.current` (grep) — CI-assertable
  </acceptance_criteria>
  <done>SampleFixtures pure + complete; placeholder box token-aware; SampleFixturesTest live + GREEN.</done>
</task>

<task type="auto">
  <name>Task 3: D-05 LocalInspectionMode branch in the 3 classic-View embed hosts</name>
  <read_first>
    - app/src/main/java/works/mees/dinghy/render/GraphViewHost.kt (lines 27-43 single-snapshot overload AND lines 64-86 multi-trace overload — BOTH wrap AndroidView, BOTH need the branch)
    - app/src/main/java/works/mees/dinghy/render/BedMeshHeatmapHost.kt (fun :29, AndroidView :35)
    - app/src/main/java/works/mees/dinghy/render/WebcamViewHost.kt (fun :37, AndroidView :49)
    - app/src/main/java/works/mees/dinghy/preview/PreviewPlaceholders.kt (PreviewPlaceholderBox from Task 2)
    - 18-RESEARCH.md §Q5 flavor 2 + §Pitfall 4
    - 18-PATTERNS.md §"PreviewPlaceholders.kt + the D-05 host branch" (drift note: GraphViewHost has TWO overloads)
  </read_first>
  <action>
    In each interop host, add `if (LocalInspectionMode.current) { PreviewPlaceholderBox(label = "<name> (live on device)", modifier); return }` BEFORE the `AndroidView` call.
    GraphViewHost has TWO `@Composable` overloads (single-snapshot :27-43 AND multi-trace :64-86) —
    branch BOTH. BedMeshHeatmapHost (:29) and WebcamViewHost (:37) each get the branch once. Label each
    placeholder distinctly ("GraphView (live on device)", "Bed mesh (live on device)", "Webcam (live on device)").
    This is the D-05 deliverable so embedded-View screens (PrintStatus's heater-trace GraphView) preview
    cleanly. Document D-04 (these Views get NO dedicated Compose preview — known limitation) in the host
    KDoc or the convention doc (the convention doc is finalized in 18-07).
  </action>
  <verify>
    <automated>cmd.exe /c "E:\Android\gw.bat :app:assembleDebug :app:testDebugUnitTest --no-daemon" | tr -d '\r'  (exit 0 — no regression)</automated>
  </verify>
  <acceptance_criteria>
    - CI: grep finds `LocalInspectionMode` in GraphViewHost.kt (≥2 occurrences for the 2 overloads), BedMeshHeatmapHost.kt, WebcamViewHost.kt — CI-assertable
    - CI: `:app:assembleDebug` + full `:app:testDebugUnitTest` green (no regression, SC-5) — CI-assertable
    - Studio: a `@Preview` embedding GraphViewHost shows a labeled stand-in box, not a blank region — Studio human-eyeball (phase gate)
  </acceptance_criteria>
  <done>All 3 hosts (4 composable bodies) branch to a labeled placeholder under inspection mode; suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | This plan adds host-rendered preview infrastructure + pure fixtures + a render-time inspection branch. No untrusted input, no auth, no external surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-02-01 | (none) | preview infra, fixtures, D-05 branch | accept | No threat surface — fixtures are pure data, the inspection branch only changes host-rendered preview output, never device behavior. Stated per security-gate guidance. |
</threat_model>

<verification>
- `:app:compileDebugKotlin` green; `PreviewBoxSmokeTest` green (the bake/theme seam); `SampleFixturesTest` green; full `:app:testDebugUnitTest` green.
- D-05 branch present in all 3 hosts (4 composable bodies).
- Studio (phase gate): PreviewBox renders 6 combos + fs=L distinctly; placeholders render labeled.
</verification>

<success_criteria>
SC-1 infra (PreviewBox + 6 seeds + fs=L + Nexus-7 + proven bake seam) and SC-2 (fixture module +
provider + device profile) exist and compile; D-04 documented; D-05 placeholder branch in all embed hosts.
</success_criteria>

<output>
Create `.planning/phases/18-preview-harness-tokenization-foundation/18-02-SUMMARY.md` when done.
</output>
