---
phase: 18-preview-harness-tokenization-foundation
plan: 06
type: execute
wave: 4
depends_on:
  - 18-05
files_modified:
  - app/src/main/java/works/mees/dinghy/ui/finetune/FineTuneHubScreen.kt
  - app/src/main/java/works/mees/dinghy/ui/finetune/MotionScreen.kt
  - app/src/main/java/works/mees/dinghy/ui/finetune/ExtrusionScreen.kt
  - app/src/main/java/works/mees/dinghy/ui/finetune/FineTuneTile.kt
  - app/src/main/java/works/mees/dinghy/preview/FineTunePreviews.kt
  - app/src/main/res/values/strings.xml
  - app/src/main/java/works/mees/dinghy/designsystem/icons/DinghyIcons.kt
  - app/src/androidTest/java/works/mees/dinghy/ui/finetune/FineTuneScreenTest.kt
autonomous: true
requirements:
  - SC-1 (FineTune renders under 6 combos + fs=L with fixtures, no live Moonraker)
  - SC-3 (FineTune uses stringResource + DinghyIconView tokens)
  - D-01 (FineTune = the present/absent capability-gating + busy-lock archetype; de-risks Phase 19)
  - D-02 (core three + mechanical riders)

must_haves:
  truths:
    - "FineTune (Hub + Motion/Extrusion) renders in Studio Preview across present-all / fwRetraction-absent (HIDDEN, not disabled) / groupBusy=true fixtures, under the theme combos + fs=L, no live Moonraker"
    - "FineTune's user-facing literals are stringResource and its glyphs are DinghyIconView tokens; cd_* + ≥48dp + start/end-relative modifiers applied"
    - "The absent-capability path proves a capability is HIDDEN when its gate is false (the Phase-19 Output-Controls de-risk), and the busy-lock edge renders the whole-group lock"
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/preview/FineTunePreviews.kt"
      provides: "The @Preview matrix driven by FineTuneVm present/absent/busy fixtures"
      contains: "SampleFixtures.fineTune"
    - path: "app/src/main/java/works/mees/dinghy/ui/finetune/MotionScreen.kt"
      provides: "Tokenized capability-gating exemplar"
      contains: "stringResource"
  key_links:
    - from: "preview/FineTunePreviews.kt"
      to: "SampleFixtures (fineTuneAllPresent / fineTuneNoFwRetraction / fineTuneBusy)"
      via: "PreviewBox(seed) { FineTuneHubScreen(vm = SampleFixtures.fineTuneNoFwRetraction) }"
      pattern: "fineTune(AllPresent|NoFwRetraction|Busy)"
    - from: "ui/finetune/*Screen.kt"
      to: "res/values/strings.xml"
      via: "stringResource(R.string.finetune_*)"
      pattern: "stringResource\\(R\\.string\\.finetune"
---

<objective>
Exemplar #2: apply the D-02 template to `FineTuneScreen` (Hub + Motion/Extrusion) — the
present/absent capability-gating + busy-lock-edge archetype that directly de-risks Phase 19
(Output Controls, also capability-gated).

Purpose: Proves SC-1/SC-3/D-01/D-02 on the capability-gating pattern. The absent path
(`hasFwRetraction=false` → HIDDEN, not disabled) and the `groupBusy=true` lock are the edge states
later phases must reproduce. Shares `strings.xml` + `DinghyIcons.kt` with 18-05/18-07 → sequenced
after 18-05. EXCLUDED per D-03: `@Stable`/`@Immutable`/`ImmutableList`. FW-retraction screen stays
build-blind (Phase 17 state) — preview the Hub + Motion + Extrusion (the proven surfaces).
Output: `preview/FineTunePreviews.kt`, tokenized FineTune screens, exemplar keys/icons added,
instrumented matchers migrated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-preview-harness-tokenization-foundation/18-CONTEXT.md
@.planning/phases/18-preview-harness-tokenization-foundation/18-RESEARCH.md
@.planning/phases/18-preview-harness-tokenization-foundation/18-PATTERNS.md
@.planning/phases/18-preview-harness-tokenization-foundation/18-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: FineTune @Preview matrix (present / absent / busy)</name>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/finetune/FineTuneVm.kt (lines 49-76 — the capability gates + groupBusy)
    - app/src/main/java/works/mees/dinghy/ui/finetune/FineTuneHubScreen.kt + MotionScreen.kt + ExtrusionScreen.kt (the surfaces to preview)
    - app/src/main/java/works/mees/dinghy/preview/SampleFixtures.kt (fineTuneAllPresent / fineTuneNoFwRetraction / fineTuneBusy from 18-02)
    - app/src/main/java/works/mees/dinghy/preview/PreviewTheming.kt + DinghyPreviews.kt
    - 18-PATTERNS.md §"FineTuneScreen/*Screen.kt + FineTuneVm" + 18-RESEARCH.md §Q8 (matrix minimization)
  </read_first>
  <action>
    Create `preview/FineTunePreviews.kt`: preview fns wrapping the Hub + Motion + Extrusion screens in
    `PreviewBox(seed) { ... }` driven by the three FineTune fixtures — `fineTuneAllPresent`,
    `fineTuneNoFwRetraction` (the absent→HIDDEN proof), and `fineTuneBusy` (`groupBusy=true` lock).
    Minimize proliferation per RESEARCH Q8: render the full theme matrix on ONE representative state,
    the present/absent/busy variants on ONE representative theme, plus the fs=L overflow shot. Apply
    `@Nexus7Previews` geometry. Do NOT preview the build-blind FW-retraction screen.
  </action>
  <verify>
    <automated>cmd.exe /c "E:\Android\gw.bat :app:compileDebugKotlin --no-daemon" | tr -d '\r'  (exit 0)</automated>
  </verify>
  <acceptance_criteria>
    - CI: `FineTunePreviews.kt` references all three fixtures (grep `fineTuneAllPresent`, `fineTuneNoFwRetraction`, `fineTuneBusy`) — CI-assertable
    - CI: `:app:compileDebugKotlin` exits 0 — CI-assertable
    - Studio: the absent variant HIDES the FW-retraction tile (not disabled); the busy variant shows the whole-group lock (phase gate) — Studio human-eyeball
  </acceptance_criteria>
  <done>FineTune preview matrix (present/absent/busy × theme + fs=L) compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Tokenize FineTune Hub + Motion + Extrusion — strings + icons + RTL/a11y</name>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/finetune/FineTuneHubScreen.kt + MotionScreen.kt + ExtrusionScreen.kt + FineTuneTile.kt (the literal + glyph sites)
    - app/src/main/res/values/strings.xml (extend with finetune_* keys; cd_* as needed)
    - app/src/main/java/works/mees/dinghy/designsystem/icons/DinghyIcons.kt (extend with FineTune glyphs)
    - app/src/main/java/works/mees/dinghy/designsystem/icons/DinghyIconView.kt
    - 18-PATTERNS.md §"Font-size floor" + [[dinghy-font-sizes-too-small]] (fsSp floors)
  </read_first>
  <action>
    Apply the D-02 template to FineTuneHubScreen, MotionScreen, ExtrusionScreen, FineTuneTile:
    (a) user-facing literals → `stringResource(R.string.finetune_*)` (ADD keys to strings.xml; app
    vocabulary only); (b) glyphs → `DinghyIconView(DinghyIcons.X, contentDescription = stringResource(R.string.cd_*))`
    (ADD entries to DinghyIcons); (c) mechanical riders — `start`/`end`-relative modifiers, tokenized
    `cd_*`, ≥48dp touch targets; honor fsSp floors. The FineTune tile is the label-less `[glyph][value][−+]`
    layout from Phase 17 (commit 73296fd) — preserve that layout while tokenizing the glyph + any aria
    label. DO NOT add `@Stable`/`@Immutable`/`ImmutableList` (D-03).
  </action>
  <verify>
    <automated>cmd.exe /c "E:\Android\gw.bat :app:assembleDebug :app:testDebugUnitTest --no-daemon" | tr -d '\r'  (exit 0 + detekt gate green)</automated>
  </verify>
  <acceptance_criteria>
    - CI: FineTune screens contain `stringResource(R.string.finetune_` AND `DinghyIconView(DinghyIcons.` (grep) — CI-assertable (SC-3)
    - CI: no NEW hardcoded `Text("literal")` passes the detekt gate (if adopted) — CI-assertable
    - CI: `:app:assembleDebug` + full `:app:testDebugUnitTest` green (no regression) — CI-assertable
    - CI: no `@Stable`/`@Immutable`/`ImmutableList` added (grep absence — D-03) — CI-assertable
    - Studio: 6 combos + fs=L render with no overflow; FineTune tile layout preserved (phase gate) — Studio human-eyeball
  </acceptance_criteria>
  <done>FineTune Hub/Motion/Extrusion tokenized per D-02; tile layout preserved; suite + gate green.</done>
</task>

<task type="auto">
  <name>Task 3: Migrate FineTune instrumented test matchers off literal text</name>
  <read_first>
    - app/src/androidTest/java/works/mees/dinghy/ui/finetune/FineTuneScreenTest.kt (if present — else the FineTune instrumented test location)
    - 18-RESEARCH.md §Runtime State Inventory (test-matcher migration scope = 3 exemplars only)
  </read_first>
  <action>
    Migrate any `onNodeWithText("...")` matcher on the FineTune screens to `context.getString(R.string.finetune_*)`
    or a `testTag`. ONLY FineTune's tests this phase. If none reference literals, record the no-op and skip.
  </action>
  <verify>
    <automated>cmd.exe /c "E:\Android\gw.bat :app:compileDebugAndroidTestKotlin --no-daemon" | tr -d '\r'  (exit 0)</automated>
  </verify>
  <acceptance_criteria>
    - CI: `:app:compileDebugAndroidTestKotlin` exits 0 — CI-assertable
    - flox/Studio: `:app:connectedDebugAndroidTest` FineTune tests pass (phase gate) — flox tier
    - Doc: no-op recorded if no literal matchers existed — doc check
  </acceptance_criteria>
  <done>FineTune instrumented matchers reference resources/testTags (or no-op recorded); androidTest compiles.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Behavior-neutral tokenization + host-rendered previews of an existing screen. No new untrusted-input/auth/external surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-06-01 | (none) | FineTune exemplar | accept | No threat surface. Stated per security-gate guidance. |
</threat_model>

<verification>
- `:app:compileDebugKotlin` + `:app:assembleDebug` + full `:app:testDebugUnitTest` green; detekt gate green.
- `:app:compileDebugAndroidTestKotlin` green.
- Studio (phase gate): present/absent/busy render; absent HIDES FW-retraction; busy shows group lock; 6 combos + fs=L no clipping.
</verification>

<success_criteria>
SC-1, SC-3, D-01 (capability-gating + busy-lock — de-risks Phase 19), D-02 — proven on FineTune.
</success_criteria>

<output>
Create `.planning/phases/18-preview-harness-tokenization-foundation/18-06-SUMMARY.md` when done.
</output>
