---
phase: 18-preview-harness-tokenization-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/build.gradle.kts
  - gradle/libs.versions.toml
  - config/detekt/detekt.yml
  - config/detekt/detekt-baseline.xml
  - app/src/test/java/works/mees/dinghy/preview/SampleFixturesTest.kt
  - app/src/test/java/works/mees/dinghy/designsystem/icons/DinghyIconsTest.kt
  - app/src/test/java/works/mees/dinghy/shell/StartDestMappingTest.kt
autonomous: false
requirements:
  - SC-3 (pseudolocale en-XA + hardcoded-literal lint gate wired)
  - SC-4 (build wiring confirmed correct + documented)
  - D-03 (no @Stable/ImmutableList in scope — recorded, not built)
user_setup: []

must_haves:
  truths:
    - "Debug build renders en-XA / ar-XB pseudolocales (isPseudoLocalesEnabled=true)"
    - "A hardcoded-literal lint gate exists, baseline-scoped so the ~240 deferred literals do NOT fail the build but a NEW literal does — OR, if no compatible Compose string-literal rule is verified in Wave 1, the scoped pseudolocale-only SC-3 downgrade is recorded and the phase is NOT blocked"
    - "compose-ui-tooling-preview stays `implementation` (compile classpath of main) with an explaining comment; compose-ui-tooling stays `debugImplementation`"
    - "Three compile scaffolds compile day-one against typed (existing) symbols, with comment-only references to not-yet-built symbols"
  artifacts:
    - path: "app/build.gradle.kts"
      provides: "debug { isPseudoLocalesEnabled = true } + explaining comment on the -preview line + detekt plugin wiring (if adopted)"
      contains: "isPseudoLocalesEnabled"
    - path: "config/detekt/detekt-baseline.xml"
      provides: "Baseline capturing the ~240 deferred hardcoded literals (only if detekt adopted)"
    - path: "app/src/test/java/works/mees/dinghy/preview/SampleFixturesTest.kt"
      provides: "Compile scaffold asserting fixture completeness (SC-2)"
    - path: "app/src/test/java/works/mees/dinghy/designsystem/icons/DinghyIconsTest.kt"
      provides: "Compile scaffold asserting icon-token completeness (SC-3b)"
    - path: "app/src/test/java/works/mees/dinghy/shell/StartDestMappingTest.kt"
      provides: "Compile scaffold asserting extra-string→Dest mapping (SC-4b)"
  key_links:
    - from: "app/build.gradle.kts debug build type"
      to: "en-XA pseudolocale runtime"
      via: "isPseudoLocalesEnabled = true"
      pattern: "isPseudoLocalesEnabled"
---

<objective>
Establish the build-wiring, lint-gate, and pseudolocale FOUNDATION for the phase, plus the
Wave-0 compile scaffolds that the building plans (02/03/04) turn into live assertions. This is the
gate-first plan: it confirms SC-4 build wiring is already correct (and documents WHY),
wires `en-XA` pseudolocale generation (SC-3c), and makes the detekt-vs-pseudolocale lint-gate
decision (SC-3) — a Codex-reviewed Wave-0 call per [[codex-review-final-plans]].

Purpose: Lock the enforcement gates BEFORE any tokenization happens, so new debt cannot grow
back, and provide compiling scaffolds so per-wave `--tests` filtering is never bricked
([[dinghy-wave0-red-scaffold-compile.md]]). Because the Compose hardcoded-string ruleset is NOT
yet proven against Kotlin 2.1.21 / AGP 8.7, the detekt route carries an EXPLICIT fallback: if the
ruleset cannot be verified within Wave 1, record the scoped pseudolocale-only SC-3 downgrade and
proceed — the phase does NOT block on the lint gate.
Output: build wiring confirmed+commented, pseudolocale on debug, lint gate (detekt baseline or
documented pseudolocale-only fallback), 3 compiling compile scaffolds.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-preview-harness-tokenization-foundation/18-CONTEXT.md
@.planning/phases/18-preview-harness-tokenization-foundation/18-RESEARCH.md
@.planning/phases/18-preview-harness-tokenization-foundation/18-PATTERNS.md
@.planning/phases/18-preview-harness-tokenization-foundation/18-VALIDATION.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Decide the hardcoded-literal lint gate (detekt-baseline vs pseudolocale-only) — with an explicit fallback branch</name>
  <decision>Which mechanism enforces "no NEW hardcoded Text/label/contentDescription literal" for SC-3 — AND, if detekt is selected, what is the fallback if no compatible Compose string-literal rule is verified during Wave 1?</decision>
  <context>
    RESEARCH Q7 + A5/A6: the platform `HardcodedText` lint is XML-only and does NOT catch Compose
    `Text("...")`. The plan previously claimed the Compose ruleset is "verified-compatible" with
    Kotlin 2.1.21 / AGP 8.7, but that is NOT proven — it is the phase's biggest execution risk
    (Codex HIGH-1). Two routes, and the detekt route MUST reserve an explicit fallback so a missing/
    incompatible rule cannot eat all of Wave 1 or silently block the phase. This is the SC-3 lint-gate
    deliverable and is explicitly a Wave-0 Codex-review item per [[codex-review-final-plans]]. The
    chosen route's full setup (and the fallback decision, if detekt) happens in Task 3 of this plan.
  </context>
  <action>Present the three branches below; Codex-review the choice; the executor records the decision (including which fallback branch fires if detekt verification fails in Task 3) and the chosen route is fully set up in Task 3.</action>
  <options>
    <option id="detekt-baseline">
      <name>detekt + a Compose ruleset, baseline-scoped — IF a compatible rule is verified in Task 3</name>
      <pros>Real "can't regress" guard (SC-3 fully delivered); the ~240 deferred literals are captured in a baseline so the build stays green; new literals (exemplars + all future screens) fail. Build-time tool only — no runtime/minSdk impact.</pros>
      <cons>Adds a build-tool dependency that must be VERIFIED for Kotlin 2.1.21 / AGP 8.7 compatibility (`io.nlopez.compose.rules:detekt` OR `ru.kode:detekt-rules-compose`) — NOT proven; the verification happens in Task 3 and may fail.</cons>
    </option>
    <option id="detekt-fallback-to-pseudolocale">
      <name>detekt FALLBACK: if no compatible Compose string-literal rule is verified in Task 3, drop to pseudolocale-only as a scoped SC-3 downgrade (do NOT block the phase)</name>
      <pros>Bounds the detekt execution risk: Wave 1 cannot stall on a missing/incompatible ruleset. The downgrade is explicit, documented in VALIDATION.md + the SUMMARY, and SC-3's pseudolocale sub-criterion still ships.</pros>
      <cons>If it fires, the automated "can't regress" guard is deferred to Phase 22 — a documented partial SC-3 (recorded, not silent).</cons>
    </option>
    <option id="pseudolocale-only">
      <name>Pseudolocale-only (en-XA visual sweep) up front — defer automated gate to Phase 22</name>
      <pros>Zero new dependency; lighter foundation phase; no detekt-verification risk at all.</pros>
      <cons>Loses the automated "can't regress" guard from day one; this is a documented SCOPE REDUCTION of SC-3 (must be recorded in the SUMMARY + VALIDATION.md downgrade).</cons>
    </option>
  </options>
  <resume-signal>Select: detekt-baseline (with detekt-fallback-to-pseudolocale as the Task-3 escape hatch) OR pseudolocale-only</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Confirm + comment build wiring; wire en-XA pseudolocale</name>
  <read_first>
    - app/build.gradle.kts (lines 95-139; the dependencies block + buildTypes block)
    - gradle/libs.versions.toml (lines 76-77 — compose.ui.tooling / compose.ui.tooling.preview catalog entries)
    - 18-RESEARCH.md §Q1 (VERIFIED leave-as-is verdict) + §Q7 (pseudolocale)
    - 18-PATTERNS.md "app/build.gradle.kts modification (config)" section
  </read_first>
  <action>
    Confirm `implementation(libs.compose.ui.tooling.preview)` stays at app/build.gradle.kts:103 and
    `debugImplementation(libs.compose.ui.tooling)` stays at :104 — do NOT move `-preview` to
    `debugImplementation` (per RESEARCH Q1: @Preview fns live in `main`, the annotation must be on
    main's compile classpath; it is an inert annotations jar R8-strips). Add a code comment on the
    `-preview` line stating WHY it is `implementation` (so a future cleanup doesn't "fix" it and
    break previews). Add `isPseudoLocalesEnabled = true` to the `debug` buildType (renders en-XA /
    ar-XB at runtime — no resConfigs trimming needed, project has none). This satisfies SC-4a build
    wiring (verified-correct, not changed) and SC-3c pseudolocale generation.
  </action>
  <verify>
    <automated>cmd.exe /c "E:\Android\gw.bat :app:assembleDebug --no-daemon" | tr -d '\r'  (exit 0)</automated>
  </verify>
  <acceptance_criteria>
    - CI: `app/build.gradle.kts` debug buildType contains `isPseudoLocalesEnabled = true` (grep) — CI-assertable
    - CI: `app/build.gradle.kts:103` retains `implementation(libs.compose.ui.tooling.preview)` AND has an adjacent comment explaining the configuration choice (grep for both) — CI-assertable
    - CI: `:app:assembleDebug` exits 0 — CI-assertable
  </acceptance_criteria>
  <done>Build wiring confirmed+commented; debug build enables pseudolocales; assembleDebug green.</done>
</task>

<task type="auto">
  <name>Task 3: Install the chosen lint gate (or fire the fallback) + write 3 compiling compile scaffolds</name>
  <read_first>
    - app/src/test/java/works/mees/dinghy/FixtureSanityTest.kt (existing JVM unit-test idiom to mirror)
    - 18-VALIDATION.md §"Wave 0 Requirements" + §"Wave 0 Gaps"
    - 18-RESEARCH.md §Q6 (IconRef/DinghyIcon shape the icon test asserts against) + §Q8 (PrintStatusMode provider)
    - 18-PATTERNS.md §"Drift corrections" (for the typed symbol names: ThemeTuple, FineTuneVm, PrintStatusMode)
    - [[dinghy-wave0-red-scaffold-compile.md]] — scaffolds MUST compile day-one
  </read_first>
  <action>
    Lint gate, per Task-1 decision:
    - If Task 1 selected detekt-baseline: FIRST verify a compatible Compose hardcoded-string rule
      resolves and runs under Kotlin 2.1.21 / AGP 8.7 — resolve `io.nlopez.compose.rules:detekt`
      (or `ru.kode:detekt-rules-compose`) from Maven Central, add the detekt plugin + ruleset to
      `gradle/libs.versions.toml` + `app/build.gradle.kts`, create `config/detekt/detekt.yml` enabling
      the hardcoded-Compose-string rule, and confirm `:app:detekt` actually evaluates that rule (not
      just that the plugin applies). Generate `config/detekt/detekt-baseline.xml` capturing the existing
      ~240 literals (run `:app:detektBaseline` so the baseline is real, not hand-written).
    - FALLBACK (the Task-1 `detekt-fallback-to-pseudolocale` branch): if the ruleset cannot be resolved
      OR does not expose a working hardcoded-Compose-string check under this toolchain, DO NOT keep
      fighting it and DO NOT block the phase. Remove any partial detekt wiring, record the scoped
      pseudolocale-only SC-3 downgrade in `18-VALIDATION.md` frontmatter/notes + the SUMMARY (SC-3's
      automated "can't regress" sub-criterion deferred to Phase 22; the en-XA pseudolocale sweep from
      Task 2 still ships), and proceed. This fallback firing is a SUCCESS, not a failure.
    - If Task 1 selected pseudolocale-only: skip detekt entirely and record the SC-3 lint-gate
      downgrade in VALIDATION.md frontmatter + the SUMMARY.
    Then write THREE compile scaffolds (NOT failing "RED" tests — see note) that COMPILE day-one with
    typed assertions against EXISTING symbols (NEVER `fail()` bodies referencing unbuilt symbols —
    Gradle compiles the whole test sourceset before the `--tests` filter):
    (1) `preview/SampleFixturesTest.kt` — assert against EXISTING `PrintStatusMode.Standby` etc. and
    `FineTuneVm()` defaults (real today). **CRITICAL — compile day-one rule:** any reference to a
    not-yet-built symbol (`SampleFixtures`, `DinghyIcons`/`IconRef`, the `parseStartDest` mapping fn)
    MUST be a code COMMENT ONLY — NEVER a live Kotlin import or symbol reference, which would fail the
    whole test sourceset compile and brick per-wave `--tests` filtering
    ([[dinghy-wave0-red-scaffold-compile.md]]). The future `works.mees.dinghy.preview.SampleFixtures`
    assertion is left as a `// TODO(18-02):` comment that the building plan converts to a live assertion
    when the symbol exists.
    (2) `designsystem/icons/DinghyIconsTest.kt` — same comment-only-reference rule for `DinghyIcons`/`IconRef`
    (`// TODO(18-03):` comment; turns real in 18-03).
    (3) `shell/StartDestMappingTest.kt` — same comment-only-reference rule for the extra-string→`Dest` mapping fn
    (`// TODO(18-04):` comment; turns real in 18-04).
    Use `Dest` from `ui/route/TopRoute.kt:37` (real today) so the mapping test at least compiles against `Dest`.

    ⚠ NAMING — these are "compile scaffolds," NOT RED tests (Codex LOW-7). Comment-only future
    references compile but never FAIL until converted, so calling them RED is misleading. Each scaffold
    body MUST assert a CURRENT fact (about existing symbols) that passes today, and carry a
    `// TODO(18-NN): convert to live assertion when <symbol> exists` pending-marker comment. The
    building plan (02/03/04) replaces the TODO with the real typed assertion. Keep this wording
    consistent across all three files and in the SUMMARY.
  </action>
  <verify>
    <automated>cmd.exe /c "E:\Android\gw.bat :app:compileDebugUnitTestKotlin --no-daemon" | tr -d '\r'  (exit 0 — whole test sourceset compiles)</automated>
  </verify>
  <acceptance_criteria>
    - CI: `:app:compileDebugUnitTestKotlin` exits 0 — the three new scaffolds compile (SC-2/SC-3b/SC-4b) — CI-assertable
    - CI: each scaffold passes today (asserts a current fact) AND carries a `// TODO(18-NN):` pending marker (grep) — CI-assertable
    - CI: if detekt VERIFIED+adopted — `config/detekt/detekt-baseline.xml` exists and `:app:detekt` (or `detektMain`) exits 0 with the baseline applied — CI-assertable
    - CI: if detekt VERIFIED+adopted — a NEW hardcoded `Text("literal")` added to a throwaway file fails detekt (negative test, run manually then revert) — CI-assertable
    - Doc: if the detekt fallback fired OR pseudolocale-only was chosen — VALIDATION.md frontmatter `nyquist_compliant` note + SUMMARY record the scoped SC-3 lint downgrade — Studio/doc check
  </acceptance_criteria>
  <done>Lint gate installed (detekt verified) OR scoped SC-3 downgrade recorded (fallback fired / pseudolocale-only); 3 compile scaffolds compile day-one and pass on current facts with TODO markers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build tooling → CI/dev machine | detekt ruleset is a build-time dependency (no runtime/minSdk surface) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01-SC | Tampering | new detekt Gradle dependency (if adopted) | mitigate | Build-tool only, no runtime surface; verify the chosen ruleset (`io.nlopez.compose.rules:detekt` or `ru.kode:detekt-rules-compose`) on Maven Central + Kotlin-2.1/AGP-8.7 compat before adding (Task 3); if verification fails, fire the documented pseudolocale-only fallback (Task 1/3) rather than force a questionable dependency; Codex-review the choice (Task 1). |
| T-18-01-02 | (none) | pseudolocale flag, compile scaffolds | accept | `isPseudoLocalesEnabled` is a debug-only AGP flag; test scaffolds are no runtime surface. Low/no threat. |
</threat_model>

<verification>
- `:app:assembleDebug` green (pseudolocale + wiring).
- `:app:compileDebugUnitTestKotlin` green (compile scaffolds compile and pass on current facts).
- If detekt verified+adopted: baseline applied, gate fails a new literal (negative test). If the fallback fired: scoped SC-3 downgrade recorded.
</verification>

<success_criteria>
SC-4a build wiring confirmed+documented; SC-3c pseudolocale wired; SC-3 lint gate installed (detekt
verified) OR scoped downgrade recorded (fallback fired); 3 compiling compile scaffolds present.
</success_criteria>

<output>
Create `.planning/phases/18-preview-harness-tokenization-foundation/18-01-SUMMARY.md` when done.
</output>
