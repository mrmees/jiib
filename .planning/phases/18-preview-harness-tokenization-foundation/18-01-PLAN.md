---
phase: 18-preview-harness-tokenization-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/build.gradle.kts
  - gradle/libs.versions.toml
  - config/detekt/detekt.yml
  - config/detekt/detekt-baseline.xml
  - app/src/test/java/works/mees/dinghy/preview/SampleFixturesTest.kt
  - app/src/test/java/works/mees/dinghy/designsystem/icons/DinghyIconsTest.kt
  - app/src/test/java/works/mees/dinghy/shell/StartDestMappingTest.kt
autonomous: false
requirements:
  - SC-3 (pseudolocale en-XA + hardcoded-literal lint gate wired)
  - SC-4 (build wiring confirmed correct + documented)
  - D-03 (no @Stable/ImmutableList in scope — recorded, not built)
user_setup: []

must_haves:
  truths:
    - "Debug build renders en-XA / ar-XB pseudolocales (isPseudoLocalesEnabled=true)"
    - "A hardcoded-literal lint gate exists, baseline-scoped so the ~240 deferred literals do NOT fail the build but a NEW literal does"
    - "compose-ui-tooling-preview stays `implementation` (compile classpath of main) with an explaining comment; compose-ui-tooling stays `debugImplementation`"
    - "Three RED test scaffolds compile day-one against typed (not-yet-built) symbols"
  artifacts:
    - path: "app/build.gradle.kts"
      provides: "debug { isPseudoLocalesEnabled = true } + explaining comment on the -preview line + detekt plugin wiring (if adopted)"
      contains: "isPseudoLocalesEnabled"
    - path: "config/detekt/detekt-baseline.xml"
      provides: "Baseline capturing the ~240 deferred hardcoded literals (only if detekt adopted)"
    - path: "app/src/test/java/works/mees/dinghy/preview/SampleFixturesTest.kt"
      provides: "RED scaffold asserting fixture completeness (SC-2)"
    - path: "app/src/test/java/works/mees/dinghy/designsystem/icons/DinghyIconsTest.kt"
      provides: "RED scaffold asserting icon-token completeness (SC-3b)"
    - path: "app/src/test/java/works/mees/dinghy/shell/StartDestMappingTest.kt"
      provides: "RED scaffold asserting extra-string→Dest mapping (SC-4b)"
  key_links:
    - from: "app/build.gradle.kts debug build type"
      to: "en-XA pseudolocale runtime"
      via: "isPseudoLocalesEnabled = true"
      pattern: "isPseudoLocalesEnabled"
---

<objective>
Establish the build-wiring, lint-gate, and pseudolocale FOUNDATION for the phase, plus the
Wave-0 RED test scaffolds that the building plans (02/03/04) turn green. This is the
gate-first plan: it confirms SC-4 build wiring is already correct (and documents WHY),
wires `en-XA` pseudolocale generation (SC-3c), and makes the detekt-vs-pseudolocale lint-gate
decision (SC-3) — a Codex-reviewed Wave-0 call per [[codex-review-final-plans]].

Purpose: Lock the enforcement gates BEFORE any tokenization happens, so new debt cannot grow
back, and provide compiling RED scaffolds so per-wave `--tests` filtering is never bricked
([[dinghy-wave0-red-scaffold-compile.md]]).
Output: build wiring confirmed+commented, pseudolocale on debug, lint gate (detekt baseline or
documented pseudolocale-only fallback), 3 compiling RED test scaffolds.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-preview-harness-tokenization-foundation/18-CONTEXT.md
@.planning/phases/18-preview-harness-tokenization-foundation/18-RESEARCH.md
@.planning/phases/18-preview-harness-tokenization-foundation/18-PATTERNS.md
@.planning/phases/18-preview-harness-tokenization-foundation/18-VALIDATION.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Decide the hardcoded-literal lint gate (detekt-baseline vs pseudolocale-only)</name>
  <decision>Which mechanism enforces "no NEW hardcoded Text/label/contentDescription literal" for SC-3?</decision>
  <context>
    RESEARCH Q7 + A5/A6: the platform `HardcodedText` lint is XML-only and does NOT catch Compose
    `Text("...")`. Two routes. This is the SC-3 lint-gate deliverable and is explicitly a Wave-0
    Codex-review item per [[codex-review-final-plans]]. The chosen route's full setup happens in
    Task 3 of this plan.
  </context>
  <action>Present both options with the pros/cons below; Codex-review the choice; the executor records the decision and the chosen route is fully set up in Task 3 of this plan.</action>
  <options>
    <option id="detekt-baseline">
      <name>detekt + a Compose ruleset, introduced WARN/baseline-scoped</name>
      <pros>Real "can't regress" guard (SC-3 fully delivered); the ~240 deferred literals are captured in a baseline so the build stays green; new literals (exemplars + all future screens) fail. Build-time tool only — no runtime/minSdk impact.</pros>
      <cons>Adds a build-tool dependency that must be verified for Kotlin 2.1.21 / AGP 8.7 compatibility (`io.nlopez.compose.rules:detekt` OR `ru.kode:detekt-rules-compose`); slightly heavier for a foundation phase.</cons>
    </option>
    <option id="pseudolocale-only">
      <name>Pseudolocale-only (en-XA visual sweep) — defer automated gate to Phase 22</name>
      <pros>Zero new dependency; lighter foundation phase.</pros>
      <cons>Loses the automated "can't regress" guard; this is a documented SCOPE REDUCTION of SC-3 (must be recorded in the SUMMARY + VALIDATION.md downgrade).</cons>
    </option>
  </options>
  <resume-signal>Select: detekt-baseline or pseudolocale-only</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Confirm + comment build wiring; wire en-XA pseudolocale</name>
  <read_first>
    - app/build.gradle.kts (lines 95-139; the dependencies block + buildTypes block)
    - gradle/libs.versions.toml (lines 76-77 — compose.ui.tooling / compose.ui.tooling.preview catalog entries)
    - 18-RESEARCH.md §Q1 (VERIFIED leave-as-is verdict) + §Q7 (pseudolocale)
    - 18-PATTERNS.md "app/build.gradle.kts modification (config)" section
  </read_first>
  <action>
    Confirm `implementation(libs.compose.ui.tooling.preview)` stays at app/build.gradle.kts:103 and
    `debugImplementation(libs.compose.ui.tooling)` stays at :104 — do NOT move `-preview` to
    `debugImplementation` (per RESEARCH Q1: @Preview fns live in `main`, the annotation must be on
    main's compile classpath; it is an inert annotations jar R8-strips). Add a code comment on the
    `-preview` line stating WHY it is `implementation` (so a future cleanup doesn't "fix" it and
    break previews). Add `isPseudoLocalesEnabled = true` to the `debug` buildType (renders en-XA /
    ar-XB at runtime — no resConfigs trimming needed, project has none). This satisfies SC-4a build
    wiring (verified-correct, not changed) and SC-3c pseudolocale generation.
  </action>
  <verify>
    <automated>cmd.exe /c "E:\Android\gw.bat :app:assembleDebug --no-daemon" | tr -d '\r'  (exit 0)</automated>
  </verify>
  <acceptance_criteria>
    - CI: `app/build.gradle.kts` debug buildType contains `isPseudoLocalesEnabled = true` (grep) — CI-assertable
    - CI: `app/build.gradle.kts:103` retains `implementation(libs.compose.ui.tooling.preview)` AND has an adjacent comment explaining the configuration choice (grep for both) — CI-assertable
    - CI: `:app:assembleDebug` exits 0 — CI-assertable
  </acceptance_criteria>
  <done>Build wiring confirmed+commented; debug build enables pseudolocales; assembleDebug green.</done>
</task>

<task type="auto">
  <name>Task 3: Install the chosen lint gate + write 3 compiling RED scaffolds</name>
  <read_first>
    - app/src/test/java/works/mees/dinghy/FixtureSanityTest.kt (existing JVM unit-test idiom to mirror)
    - 18-VALIDATION.md §"Wave 0 Requirements" + §"Wave 0 Gaps"
    - 18-RESEARCH.md §Q6 (IconRef/DinghyIcon shape the icon test asserts against) + §Q8 (PrintStatusMode provider)
    - 18-PATTERNS.md §"Drift corrections" (for the typed symbol names: ThemeTuple, FineTuneVm, PrintStatusMode)
    - [[dinghy-wave0-red-scaffold-compile.md]] — scaffolds MUST compile day-one
  </read_first>
  <action>
    If Task 1 selected detekt-baseline: add the detekt plugin + the verified-compatible Compose
    ruleset to `gradle/libs.versions.toml` + `app/build.gradle.kts`; create `config/detekt/detekt.yml`
    enabling the hardcoded-Compose-string rule; generate `config/detekt/detekt-baseline.xml` capturing
    the existing ~240 literals (run `:app:detektBaseline` or equivalent so the baseline is real, not
    hand-written). If Task 1 selected pseudolocale-only: skip detekt, and record the SC-3 lint-gate
    downgrade in VALIDATION.md frontmatter + the SUMMARY.
    Then write THREE RED test scaffolds that COMPILE day-one with typed assertions (NOT `fail()` bodies
    referencing unbuilt symbols — Gradle compiles the whole test sourceset before the `--tests` filter):
    (1) `preview/SampleFixturesTest.kt` — assert `SampleFixtures` exposes all 4/6 `PrintStatusMode`s +
    non-empty spool/temp fixtures + the FineTune present/absent/busy variants (SC-2). To compile day-one
    without the not-yet-built `SampleFixtures`, gate the assertion behind a typed reference that exists
    once 18-02 lands — use a minimal placeholder approach that compiles: assert against the EXISTING
    production state types (`PrintStatusMode.Standby` etc., `FineTuneVm()` defaults) and leave a typed
    TODO referencing `works.mees.dinghy.preview.SampleFixtures` so the assertion turns real in 18-02.
    (2) `designsystem/icons/DinghyIconsTest.kt` — same approach for `DinghyIcons`/`IconRef` (turns real in 18-03).
    (3) `shell/StartDestMappingTest.kt` — same approach for the extra-string→`Dest` mapping fn (turns real in 18-04).
    Use `Dest` from `ui/route/TopRoute.kt:37` (real today) so the mapping test at least compiles against `Dest`.
  </action>
  <verify>
    <automated>cmd.exe /c "E:\Android\gw.bat :app:compileDebugUnitTestKotlin --no-daemon" | tr -d '\r'  (exit 0 — whole test sourceset compiles)</automated>
  </verify>
  <acceptance_criteria>
    - CI: `:app:compileDebugUnitTestKotlin` exits 0 — the three new scaffolds compile (SC-2/SC-3b/SC-4b RED) — CI-assertable
    - CI: if detekt adopted — `config/detekt/detekt-baseline.xml` exists and `:app:detekt` (or `detektMain`) exits 0 with the baseline applied — CI-assertable
    - CI: if detekt adopted — a NEW hardcoded `Text("literal")` added to a throwaway file fails detekt (negative test, run manually then revert) — CI-assertable
    - Doc: if pseudolocale-only chosen — VALIDATION.md frontmatter `nyquist_compliant` note + SUMMARY record the SC-3 lint downgrade — Studio/doc check
  </acceptance_criteria>
  <done>Lint gate installed (or downgrade recorded); 3 RED scaffolds compile day-one.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build tooling → CI/dev machine | detekt ruleset is a build-time dependency (no runtime/minSdk surface) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01-SC | Tampering | new detekt Gradle dependency (if adopted) | mitigate | Build-tool only, no runtime surface; verify the chosen ruleset (`io.nlopez.compose.rules:detekt` or `ru.kode:detekt-rules-compose`) on Maven Central + Kotlin-2.1/AGP-8.7 compat before adding (Task 3); Codex-review the choice (Task 1). |
| T-18-01-02 | (none) | pseudolocale flag, RED scaffolds | accept | `isPseudoLocalesEnabled` is a debug-only AGP flag; test scaffolds are no runtime surface. Low/no threat. |
</threat_model>

<verification>
- `:app:assembleDebug` green (pseudolocale + wiring).
- `:app:compileDebugUnitTestKotlin` green (RED scaffolds compile).
- If detekt: baseline applied, gate fails a new literal (negative test).
</verification>

<success_criteria>
SC-4a build wiring confirmed+documented; SC-3c pseudolocale wired; SC-3 lint gate installed or
downgrade recorded; 3 compiling RED scaffolds present.
</success_criteria>

<output>
Create `.planning/phases/18-preview-harness-tokenization-foundation/18-01-SUMMARY.md` when done.
</output>
