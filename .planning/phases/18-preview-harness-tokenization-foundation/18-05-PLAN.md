---
phase: 18-preview-harness-tokenization-foundation
plan: 05
type: execute
wave: 3
depends_on:
  - 18-02
  - 18-03
files_modified:
  - app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusScreen.kt
  - app/src/main/java/works/mees/dinghy/preview/PrintStatusPreviews.kt
  - app/src/main/res/values/strings.xml
  - app/src/main/java/works/mees/dinghy/designsystem/icons/DinghyIcons.kt
  - app/src/androidTest/java/works/mees/dinghy/ui/printstatus/PrintStatusScreenTest.kt
autonomous: true
requirements:
  - SC-1 (PrintStatus renders under 6 combos + fs=L with fixtures, no live Moonraker — the anchor exemplar)
  - SC-3 (PrintStatus uses stringResource + DinghyIconView tokens)
  - D-01 (PrintStatus = the multi-state @PreviewParameter archetype)
  - D-02 (core three + mechanical riders: @Preview matrix + strings + icons + RTL + a11y/≥48dp)
  - D-05 (embedded GraphView previews via the host inspection branch)

must_haves:
  truths:
    - "PrintStatusScreen renders in Studio Preview across all 4/6 PrintStatusMode states via a PreviewParameterProvider, plus the 6 theme combos + an fs=L overflow shot, with NO live Moonraker"
    - "PrintStatusScreen's user-facing literals are stringResource(R.string.printstatus_*) and its icons are DinghyIconView(DinghyIcons.*); contentDescriptions are tokenized cd_* and touch targets are ≥48dp"
    - "PrintStatus's embedded GraphView region previews as a labeled stand-in (via the D-05 host branch) and Coil thumbnails branch on LocalInspectionMode"
    - "start/end-relative modifiers are used (RTL-correct), proven by the RTL preview spot-check"
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/preview/PrintStatusPreviews.kt"
      provides: "PrintStatusModeProvider + the @Preview matrix functions (mode×theme+fs=L)"
      contains: "PreviewParameterProvider<PrintStatusMode>"
    - path: "app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusScreen.kt"
      provides: "Tokenized anchor exemplar (strings + icons + Coil inspection branch + RTL/a11y)"
      contains: "stringResource"
  key_links:
    - from: "preview/PrintStatusPreviews.kt"
      to: "preview/PreviewTheming.kt + SampleFixtures"
      via: "PreviewBox(tuple) { PrintStatusScreen(state = SampleFixtures.forMode(mode)) }"
      pattern: "PreviewBox\\(|SampleFixtures"
    - from: "ui/printstatus/PrintStatusScreen.kt"
      to: "res/values/strings.xml"
      via: "stringResource(R.string.printstatus_*)"
      pattern: "stringResource\\(R\\.string\\."
---

<objective>
Exemplar #1 (the ANCHOR): apply the full "core three + mechanical riders" template (D-02) to
`PrintStatusScreen` — the multi-state `@PreviewParameter` archetype (its 4/6 `PrintStatusMode` states)
that also forces the embedded-GraphView D-05 case and the Coil-thumbnail inspection branch.

Purpose: PrintStatus IS the convention Phases 19-21 copy. Whatever riders it demonstrates get built
into later phases; riders it omits won't. This proves SC-1 (renders under 6 combos + fs=L, no live
Moonraker), SC-3 (strings + icons), D-01 (multi-state), D-02 (template), D-05 (embedded View placeholder).
EXCLUDED per D-03: `@Stable`/`@Immutable`/`ImmutableList` — deferred to Phase 22. Do NOT add them.
Output: `preview/PrintStatusPreviews.kt` (provider + matrix), tokenized `PrintStatusScreen.kt`, its
exemplar keys added to `strings.xml`, its icons added to `DinghyIcons`, instrumented test migrated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-preview-harness-tokenization-foundation/18-CONTEXT.md
@.planning/phases/18-preview-harness-tokenization-foundation/18-RESEARCH.md
@.planning/phases/18-preview-harness-tokenization-foundation/18-PATTERNS.md
@.planning/phases/18-preview-harness-tokenization-foundation/18-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: PrintStatusModeProvider + the @Preview matrix</name>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusMode.kt (lines 23-38 — the states + TerminalKind)
    - app/src/main/java/works/mees/dinghy/preview/SampleFixtures.kt (forMode builder from 18-02)
    - app/src/main/java/works/mees/dinghy/preview/PreviewTheming.kt (PreviewBox + the 6 seeds + fs=L seed)
    - app/src/main/java/works/mees/dinghy/preview/DinghyPreviews.kt (@Nexus7Previews / @DeviceAndLocalePreviews — the six THEME combos come from explicit PreviewBox wrappers below, NOT this annotation)
    - 18-RESEARCH.md §Q8 (PreviewParameter for state-axis; theme via wrapper; minimize proliferation — full theme matrix on ONE state, full state matrix on ONE theme, plus the fs=L shot)
  </read_first>
  <action>
    Create `preview/PrintStatusPreviews.kt` with `class PrintStatusModeProvider : PreviewParameterProvider<PrintStatusMode>`
    emitting Standby/Printing/Paused/Terminal(Complete|Cancelled|Error). Author the matrix to MINIMIZE
    proliferation per RESEARCH Q8: render the full STATE matrix on ONE representative theme (mode as the
    `@PreviewParameter`), the full THEME matrix (6 combos via `PreviewBox(seed)` wrappers) on ONE
    representative state, plus ONE `fs=L` overflow shot (inject via the fs=L `ThemeTuple` seed — NOT
    `@Preview(fontScale=)`, which is a NO-OP here), plus one RTL spot-check. Apply `@Nexus7Previews`
    geometry. Each preview fn wraps the screen in `PreviewBox(seed) { PrintStatusScreen(state = SampleFixtures.forMode(mode), ...) }`.
  </action>
  <verify>
    <automated>cmd.exe /c "E:\Android\gw.bat :app:compileDebugKotlin --no-daemon" | tr -d '\r'  (exit 0)</automated>
  </verify>
  <acceptance_criteria>
    - CI: `PrintStatusPreviews.kt` defines `PreviewParameterProvider<PrintStatusMode>` emitting ≥4 states (grep) — CI-assertable
    - CI: preview fns wrap in `PreviewBox(` and use `SampleFixtures` (grep) — CI-assertable
    - CI: `:app:compileDebugKotlin` exits 0 — CI-assertable
    - Studio: the provider yields one panel per state; the 6-combo + fs=L wrappers render distinctly (phase gate) — Studio human-eyeball
  </acceptance_criteria>
  <done>Provider + minimized matrix (states × one theme; 6 themes × one state; fs=L; RTL) compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Tokenize PrintStatusScreen — strings + icons + Coil branch + RTL/a11y</name>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusScreen.kt (Coil AsyncImage at :515/:523/:875/:1164; MaterialSymbol sites :571,612,618,648,656,667,988,1010; fsSp usage :988,1010)
    - app/src/main/res/values/strings.xml (the master file + convention from 18-03)
    - app/src/main/java/works/mees/dinghy/designsystem/icons/DinghyIcons.kt (registry to extend with PrintStatus icons)
    - app/src/main/java/works/mees/dinghy/designsystem/icons/DinghyIconView.kt (the render primitive to call)
    - app/src/main/java/works/mees/dinghy/preview/PreviewPlaceholders.kt (PreviewPlaceholderBox for the Coil branch)
    - 18-PATTERNS.md §"Exemplar screens (3)" PrintStatus section + §"Font-size floor" shared pattern
    - [[dinghy-font-sizes-too-small]] — use fsSp(base, fs); floors: 15sp metadata, 17-18 body, 20-22 titles, 26 tabular, 30+ focus
  </read_first>
  <action>
    Apply the D-02 template to `PrintStatusScreen.kt`: (a) replace user-facing `Text("...")` / label
    literals with `stringResource(R.string.printstatus_*)` — ADD those keys to `strings.xml` (app
    vocabulary only; pass through filenames/printer data raw per the IN/OUT boundary); (b) replace the
    listed `MaterialSymbol(...)` call sites with `DinghyIconView(DinghyIcons.X, contentDescription = stringResource(R.string.cd_*))`
    — ADD those entries to `DinghyIcons`; (c) branch each Coil `AsyncImage` on `LocalInspectionMode.current`
    → `PreviewPlaceholderBox` (the embedded GraphView is already covered by the 18-02 D-05 host branch);
    (d) mechanical riders — use `start`/`end`-relative modifiers (RTL-correct), tokenized `cd_*`
    contentDescriptions, and ≥48dp touch targets. Honor fsSp floors. DO NOT add `@Stable`/`@Immutable`/
    `ImmutableList` (D-03 — Phase 22). Preserve the existing four-state layout/tokens (UI LAW binding).
    ⚠ TOKENIZATION BOUNDARY (Codex MEDIUM-6 — prevents Phase-22 backfill creep): tokenize literals in
    `PrintStatusScreen.kt` itself + child components DIRECTLY EDITED for this exemplar ONLY. Do NOT
    tokenize shared components reused across UNRELATED screens (e.g. a generic button/row primitive
    used app-wide) — those ride the Phase-22 co-sequenced backfill. If a literal lives in a shared
    component, leave it (the detekt baseline already accepts it) and note it for Phase 22 in the SUMMARY.
  </action>
  <verify>
    <automated>cmd.exe /c "E:\Android\gw.bat :app:assembleDebug :app:testDebugUnitTest --no-daemon" | tr -d '\r'  (exit 0 + detekt gate green: no NEW hardcoded literal)</automated>
  </verify>
  <acceptance_criteria>
    - CI: `PrintStatusScreen.kt` contains `stringResource(R.string.printstatus_` AND `DinghyIconView(DinghyIcons.` (grep) — CI-assertable (SC-3)
    - CI: every Coil `AsyncImage` in the file is guarded by a `LocalInspectionMode` branch (grep co-occurrence) — CI-assertable (D-05)
    - CI: no NEW hardcoded `Text("literal")` in the file passes the detekt gate (if adopted in 18-01) — CI-assertable
    - CI: `:app:assembleDebug` + full `:app:testDebugUnitTest` green (no regression) — CI-assertable
    - CI: file does NOT add `@Stable`/`@Immutable`/`ImmutableList` (grep absence — D-03) — CI-assertable
    - Studio: 6 combos + fs=L render with no overflow/clipping; RTL spot-check mirrors correctly (phase gate) — Studio human-eyeball
  </acceptance_criteria>
  <done>PrintStatus fully tokenized per D-02; Coil branched; riders applied; D-03 exclusions respected; suite + gate green.</done>
</task>

<task type="auto">
  <name>Task 3: Migrate PrintStatus instrumented test matchers off literal text</name>
  <read_first>
    - app/src/androidTest/java/works/mees/dinghy/ui/printstatus/PrintStatusScreenTest.kt (if present — else the existing PrintStatus instrumented test location)
    - 18-RESEARCH.md §Runtime State Inventory (test-matcher migration: only the 3 exemplars this phase)
  </read_first>
  <action>
    Externalizing PrintStatus strings breaks any `onNodeWithText("...")` instrumented matcher on this
    screen. Migrate those matchers to `context.getString(R.string.printstatus_*)` or a `testTag`. ONLY
    PrintStatus's tests are touched this phase (the rest ride Phase 22). If no instrumented test
    references PrintStatus literals today, record that (no-op) in the SUMMARY and skip.
  </action>
  <verify>
    <automated>cmd.exe /c "E:\Android\gw.bat :app:compileDebugAndroidTestKotlin --no-daemon" | tr -d '\r'  (exit 0 — instrumented sourceset compiles)</automated>
  </verify>
  <acceptance_criteria>
    - CI: `:app:compileDebugAndroidTestKotlin` exits 0 (matchers compile against R.string / testTag) — CI-assertable
    - flox/Studio: `:app:connectedDebugAndroidTest` PrintStatus tests pass on a device/emulator (phase gate, not CI) — flox tier
    - Doc: if no PrintStatus literal matchers existed, the no-op is recorded — doc check
  </acceptance_criteria>
  <done>PrintStatus instrumented matchers reference resources/testTags (or no-op recorded); androidTest compiles.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Tokenizing an existing screen + adding host-rendered previews. No new untrusted-input, auth, or external surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-05-01 | (none) | PrintStatus exemplar | accept | Behavior-neutral tokenization + host-rendered previews; no threat surface. Stated per security-gate guidance. |
</threat_model>

<verification>
- `:app:compileDebugKotlin` + `:app:assembleDebug` + full `:app:testDebugUnitTest` green; detekt gate green.
- `:app:compileDebugAndroidTestKotlin` green.
- Studio (phase gate): PrintStatus renders 4/6 states × 6 combos + fs=L + RTL with no clipping; GraphView placeholder + Coil placeholder render labeled.
</verification>

<success_criteria>
SC-1 (renders under combos + fs=L, no Moonraker), SC-3 (strings + icons), D-01 (multi-state),
D-02 (template incl. RTL/a11y), D-05 (embedded View placeholder) — all proven on the PrintStatus anchor.
</success_criteria>

<output>
Create `.planning/phases/18-preview-harness-tokenization-foundation/18-05-SUMMARY.md` when done.
</output>
