---
phase: 18-preview-harness-tokenization-foundation
plan: 07
type: execute
wave: 5
depends_on:
  - 18-06
files_modified:
  - app/src/main/java/works/mees/dinghy/ui/spool/SpoolScreen.kt
  - app/src/main/java/works/mees/dinghy/preview/SpoolPreviews.kt
  - app/src/main/res/values/strings.xml
  - app/src/main/java/works/mees/dinghy/designsystem/icons/DinghyIcons.kt
  - app/src/androidTest/java/works/mees/dinghy/ui/spool/SpoolScreenTest.kt
  - docs/ui_design/PREVIEW_AND_TOKENS.md
  - CLAUDE.md
autonomous: true
requirements:
  - SC-1 (Spool renders under 6 combos + fs=L with dense fixtures, no live Moonraker)
  - SC-2 (the written convention future phases follow — PREVIEW_AND_TOKENS.md + CLAUDE.md pointer)
  - SC-3 (Spool uses stringResource + DinghyIconView tokens)
  - SC-5 (backfill explicitly deferred/recorded as Phase-22; no regression)
  - D-01 (Spool = the preview-safe Coil/image + dense-data archetype)
  - D-02 (core three + mechanical riders)
  - D-05 (Coil filament-thumb inspection branch)

must_haves:
  truths:
    - "SpoolScreen renders in Studio Preview with dense spool fixtures across the theme combos + fs=L, no live Moonraker, with the Coil filament-thumb path branched on LocalInspectionMode"
    - "SpoolScreen's user-facing literals are stringResource and its glyphs are DinghyIconView tokens; cd_* + ≥48dp + start/end-relative modifiers applied"
    - "docs/ui_design/PREVIEW_AND_TOKENS.md documents the preview-first/tokenized-first convention (PreviewBox idiom, multipreview names, fixture-add steps, <area>_<element>/cd_*/format-args/plurals, DinghyIcon registration, RTL start/end rule + the fs=L-via-ThemeTuple gotcha, ≥48dp + cd_* rider) and CLAUDE.md points to it"
    - "The exhaustive every-screen backfill is explicitly recorded as Phase-22 scope; no existing screen regresses"
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/preview/SpoolPreviews.kt"
      provides: "Spool @Preview matrix with dense fixtures + Coil-thumb proof"
      contains: "PreviewBox"
    - path: "docs/ui_design/PREVIEW_AND_TOKENS.md"
      provides: "The authoritative preview-first/tokenized-first convention Phases 19-21 follow"
      contains: "PreviewBox"
    - path: "CLAUDE.md"
      provides: "One-line enforcement pointer to PREVIEW_AND_TOKENS.md in the UI Design System LAW block"
      contains: "PREVIEW_AND_TOKENS"
  key_links:
    - from: "CLAUDE.md UI Design System LAW block"
      to: "docs/ui_design/PREVIEW_AND_TOKENS.md"
      via: "every new screen ships @Preview + tokenized strings + DinghyIcon tokens — see PREVIEW_AND_TOKENS.md"
      pattern: "PREVIEW_AND_TOKENS"
    - from: "ui/spool/SpoolScreen.kt"
      to: "preview/PreviewPlaceholders.kt"
      via: "Coil filament-thumb branch on LocalInspectionMode"
      pattern: "LocalInspectionMode"
---

<objective>
Exemplar #3: apply the D-02 template to `SpoolScreen` — the preview-safe Coil/image + dense-data
archetype. THEN write the convention doc that makes the whole foundation enforceable (SC-2) and record
the Phase-22 deferral + no-regression (SC-5). This is the closing plan: it proves the third archetype
and turns the infrastructure into a documented LAW Phases 19-21 follow.

Purpose: SC-1/SC-2/SC-3/SC-5 + D-01/D-02/D-05. Spool proves the Coil filament-thumb inspection branch
and dense fixtures (the QR at SpoolScreen.kt:379 is `painterResource`, NOT Coil — the Coil proof is the
thumb path). The convention doc is the enforcement hook: without it, Phases 19-21 won't follow the
pattern. Shares strings.xml + DinghyIcons.kt → sequenced after 18-06. EXCLUDED per D-03:
`@Stable`/`@Immutable`/`ImmutableList`.
Output: `preview/SpoolPreviews.kt`, tokenized SpoolScreen, exemplar keys/icons, instrumented matchers
migrated, `docs/ui_design/PREVIEW_AND_TOKENS.md` + a CLAUDE.md pointer, Phase-22 deferral recorded.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-preview-harness-tokenization-foundation/18-CONTEXT.md
@.planning/phases/18-preview-harness-tokenization-foundation/18-RESEARCH.md
@.planning/phases/18-preview-harness-tokenization-foundation/18-PATTERNS.md
@.planning/phases/18-preview-harness-tokenization-foundation/18-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Spool @Preview matrix (dense fixtures + Coil-thumb branch) + tokenize SpoolScreen</name>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/spool/SpoolScreen.kt (line 379 painterResource QR; symbol sites :250,282,284,295,305,312,407; fsSp :250)
    - app/src/main/java/works/mees/dinghy/preview/SampleFixtures.kt (spoolList dense fixture from 18-02)
    - app/src/main/java/works/mees/dinghy/preview/PreviewTheming.kt + DinghyPreviews.kt + PreviewPlaceholders.kt
    - app/src/main/res/values/strings.xml + designsystem/icons/DinghyIcons.kt + DinghyIconView.kt
    - 18-PATTERNS.md §"SpoolScreen.kt" + §"Font-size floor" + [[dinghy-font-sizes-too-small]]
  </read_first>
  <action>
    Create `preview/SpoolPreviews.kt`: preview fns wrapping SpoolScreen in `PreviewBox(seed) { ... }`
    with the dense `SampleFixtures.spoolList`; full theme matrix on ONE state, plus fs=L + RTL spot-check
    (RESEARCH Q8 minimization), `@Nexus7Previews` geometry. Apply the D-02 template to `SpoolScreen.kt`:
    (a) literals → `stringResource(R.string.spool_*)` (ADD keys); (b) glyph sites → `DinghyIconView(DinghyIcons.X, contentDescription=stringResource(R.string.cd_*))`
    (ADD entries); (c) branch the Coil FILAMENT-THUMB `AsyncImage` on `LocalInspectionMode.current` →
    `PreviewPlaceholderBox` (the QR stays `painterResource`, no branch needed); (d) mechanical riders —
    start/end modifiers, cd_*, ≥48dp; honor fsSp floors. DO NOT add `@Stable`/`@Immutable`/`ImmutableList` (D-03).
  </action>
  <verify>
    <automated>cmd.exe /c "E:\Android\gw.bat :app:assembleDebug :app:testDebugUnitTest --no-daemon" | tr -d '\r'  (exit 0 + detekt gate green)</automated>
  </verify>
  <acceptance_criteria>
    - CI: `SpoolPreviews.kt` uses `PreviewBox(` + `SampleFixtures.spoolList` (grep) — CI-assertable
    - CI: `SpoolScreen.kt` contains `stringResource(R.string.spool_` + `DinghyIconView(DinghyIcons.` + a `LocalInspectionMode` branch on the filament-thumb AsyncImage (grep) — CI-assertable (SC-3/D-05)
    - CI: `:app:assembleDebug` + full `:app:testDebugUnitTest` green; detekt gate green; no `@Stable`/`@Immutable`/`ImmutableList` (grep absence) — CI-assertable
    - Studio: dense spool list renders; Coil thumb shows the labeled placeholder; 6 combos + fs=L no clipping (phase gate) — Studio human-eyeball
  </acceptance_criteria>
  <done>Spool preview matrix + tokenization + Coil-thumb branch complete; D-03 respected; suite + gate green.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate Spool instrumented matchers + write PREVIEW_AND_TOKENS.md + CLAUDE.md pointer</name>
  <read_first>
    - app/src/androidTest/java/works/mees/dinghy/ui/spool/SpoolScreenTest.kt (if present)
    - docs/ui_design/THEMING.md (the sibling LAW-doc format to mirror)
    - CLAUDE.md (the "UI Design System (LAW — read docs/ui_design/...)" block to add the pointer into)
    - 18-RESEARCH.md §Q4 (convention-doc content list + the CLAUDE.md enforcement hook)
    - 18-PATTERNS.md §"docs/ui_design/PREVIEW_AND_TOKENS.md"
  </read_first>
  <action>
    Migrate any `onNodeWithText("...")` Spool matcher to `context.getString(R.string.spool_*)` or `testTag`
    (Spool tests only; no-op recorded if none). Write `docs/ui_design/PREVIEW_AND_TOKENS.md` as the
    authoritative convention spec covering (per RESEARCH Q4, so Phases 19-21 are mechanical): the
    `PreviewBox(tuple)` wrapper idiom; the multipreview annotation names (`@Nexus7Previews`,
    `@DinghyThemePreviews`); how to add a fixture to `SampleFixtures`; the `<area>_<element>` + `cd_*` key
    convention + a format-arg + plurals example; the `DinghyIcon`/`DinghyIcons`/`DinghyIconView`
    registration pattern (D-07 alternate handle, D-08 separable label); the RTL `start`/`end` rule +
    hardware-spatial exceptions; the ≥48dp touch-target + tokenized-`cd_*` rider; the D-03 EXCLUSION
    (`@Stable`/`ImmutableList` is Phase-22, not the template); the D-04/D-05 classic-View limitation +
    placeholder idiom; and the ⚠ `fs=L`-via-`ThemeTuple.fs` gotcha (`@Preview(fontScale=)` is a NO-OP).
    Add ONE line to the CLAUDE.md UI Design System LAW block: every new screen ships `@Preview` coverage
    (6 combos + fs=L), tokenized strings, and `DinghyIcon` tokens from day one — see PREVIEW_AND_TOKENS.md.
  </action>
  <verify>
    <automated>cmd.exe /c "E:\Android\gw.bat :app:compileDebugAndroidTestKotlin --no-daemon" | tr -d '\r'  (exit 0)</automated>
  </verify>
  <acceptance_criteria>
    - CI: `:app:compileDebugAndroidTestKotlin` exits 0 — CI-assertable
    - CI: `docs/ui_design/PREVIEW_AND_TOKENS.md` exists and mentions `PreviewBox`, the key convention, `DinghyIcon`, the RTL start/end rule, the fs=L gotcha, and the D-03/D-05 notes (grep the section keywords) — CI-assertable (doc existence + content)
    - CI: `CLAUDE.md` contains `PREVIEW_AND_TOKENS` in the UI Design System LAW block (grep) — CI-assertable (SC-2 enforcement hook)
    - flox/Studio: `:app:connectedDebugAndroidTest` Spool tests pass (phase gate) — flox tier
  </acceptance_criteria>
  <done>Spool matchers migrated; convention doc written + CLAUDE.md pointer added (SC-2 enforcement live).</done>
</task>

<task type="auto">
  <name>Task 3: Record Phase-22 deferral + no-regression (SC-5) + finalize VALIDATION map</name>
  <read_first>
    - .planning/ROADMAP.md §"Phase 18" + §"Phase 22" (the deferral target)
    - .planning/phases/18-preview-harness-tokenization-foundation/18-CONTEXT.md §"Deferred Ideas"
    - .planning/phases/18-preview-harness-tokenization-foundation/18-VALIDATION.md (the per-task map to finalize + nyquist flag)
  </read_first>
  <action>
    Explicitly record (in the SUMMARY + a note confirming the ROADMAP §Phase 22 line) that the exhaustive
    every-screen backfill — all remaining `@Previews`, the ~240 string-literal extraction, full icon
    call-site migration, a11y/RTL/`@Stable` riders, and the `compose-preview-screenshot` golden-image net
    — is DEFERRED to the Phase-22 conformance sweep as ONE co-sequenced per-screen pass (SC-5, D-03).
    Confirm no existing screen regressed: full `:app:testDebugUnitTest` + the existing instrumented suite
    stay green and the foundation changes are additive (the 3 hosts' D-05 branch is preview-only; the 3
    exemplars preserve their layout/tokens). Finalize the 18-VALIDATION.md per-task verification map
    (map each task's acceptance criteria to its CI/Studio/flox tier) and flip `nyquist_compliant: true`
    in its frontmatter now that the map is filled.
  </action>
  <verify>
    <automated>cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --no-daemon" | tr -d '\r'  (exit 0 — full suite, no regression)</automated>
  </verify>
  <acceptance_criteria>
    - CI: full `:app:testDebugUnitTest` green (no regression, SC-5) — CI-assertable
    - Doc: ROADMAP §Phase 22 confirmed to hold the backfill deferral; SUMMARY records the explicit deferral — doc check
    - Doc: `18-VALIDATION.md` per-task map filled and `nyquist_compliant: true` — doc check
  </acceptance_criteria>
  <done>Phase-22 deferral + no-regression recorded; VALIDATION map finalized + nyquist flag flipped.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Behavior-neutral tokenization + host-rendered previews + docs. No new untrusted-input/auth/external surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-07-01 | (none) | Spool exemplar + convention docs | accept | No threat surface. Stated per security-gate guidance. |
</threat_model>

<verification>
- `:app:assembleDebug` + full `:app:testDebugUnitTest` green; detekt gate green; `:app:compileDebugAndroidTestKotlin` green.
- PREVIEW_AND_TOKENS.md exists + CLAUDE.md pointer present.
- VALIDATION map finalized; nyquist_compliant flipped true.
- Studio (phase gate): Spool dense list + Coil placeholder render; 6 combos + fs=L no clipping.
</verification>

<success_criteria>
SC-1, SC-2 (convention doc + enforcement pointer), SC-3, SC-5 (deferral + no regression), D-01/D-02/D-05
— all proven; the foundation is documented LAW Phases 19-21 follow.
</success_criteria>

<output>
Create `.planning/phases/18-preview-harness-tokenization-foundation/18-07-SUMMARY.md` when done.
</output>
