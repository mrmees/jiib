---
phase: 04-service-shell-settings-print-status-home
plan: 04
type: execute
wave: 3
depends_on: [04-01, 04-03]
files_modified:
  - app/src/main/java/works/mees/dinghy/settings/SettingsScreen.kt
  - app/src/main/java/works/mees/dinghy/settings/SettingsViewModel.kt
  - app/src/main/java/works/mees/dinghy/settings/TokenTextField.kt
  - app/src/test/java/works/mees/dinghy/settings/SettingsViewModelTest.kt
autonomous: true
requirements: [SET-01, PRIM-02, CONN-01]

must_haves:
  truths:
    - "User can enter host/port/key with the system keyboard in Settings"
    - "Saving persists via ConnectionStore and triggers session rebuild"
    - "Theme (dark/light), accent, text size are editable and persist"
    - "A saved API key is never echoed back, but a 'key saved' indicator tells the user one exists"
    - "Material text fields are bridged to the Phase-3 semantic tokens (no off-spec colors)"
  artifacts:
    - path: app/src/main/java/works/mees/dinghy/settings/SettingsScreen.kt
      provides: "Conventional Android settings list (token-themed)"
    - path: app/src/main/java/works/mees/dinghy/settings/SettingsViewModel.kt
      provides: "Settings state + persistence orchestration"
    - path: app/src/main/java/works/mees/dinghy/settings/TokenTextField.kt
      provides: "Token-aware OutlinedTextField wrapper bridging Material colors to semantic tokens"
  key_links:
    - from: SettingsViewModel
      to: ConnectionStore
      via: "save persists connection"
      pattern: "ConnectionStore"

---

<objective>
The Settings screen — the one keyboard-allowed surface (PRIM-02) — for connection entry
(host/port/key + mDNS scan), theme (dark/light), accent color, and text size (S/M/L).
Conventional Android scrollable list (D-15), token-themed, NOT Focus/Field/Gutter.

Purpose: SET-01 + CONN-01 + PRIM-02 — the user-facing config surface that makes the
connection durable and the theme adjustable.

This revision adds a token-aware text-field wrapper (concern #7) so Material `OutlinedTextField`
chrome maps to the Phase-3 semantic tokens, and an "API key saved" affordance (concern #10) so
the always-blank secure field still tells the user a key is stored.
Output: `SettingsScreen`, `SettingsViewModel`, `TokenTextField`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-service-shell-settings-print-status-home/04-CONTEXT.md
@.planning/phases/04-service-shell-settings-print-status-home/04-RESEARCH.md
@.planning/phases/04-service-shell-settings-print-status-home/04-PATTERNS.md

Source-of-truth files to read before implementing:
- app/src/main/java/works/mees/dinghy/config/ConnectionStore.kt (from 04-01)
- app/src/main/java/works/mees/dinghy/di/AppContainer.kt (ConnectionStore + sessionControl + ThemePrefs access, from 04-03)
- app/src/main/java/works/mees/dinghy/theme/ThemePrefs.kt (accent TokenDelta target)
- app/src/main/java/works/mees/dinghy/theme/LocalTokens.kt (semantic token access in Compose)
- app/src/main/java/works/mees/dinghy/designsystem/control/OutlinedControl.kt (control language)
- docs/ui_design/THEMING.md (token values, button-intent colors)
</context>

<tasks>

<task type="auto">
  <name>Task 1: SettingsViewModel + persistence (+ key-saved state)</name>
  <files>
    app/src/main/java/works/mees/dinghy/settings/SettingsViewModel.kt
    app/src/test/java/works/mees/dinghy/settings/SettingsViewModelTest.kt
  </files>
  <read_first>
    app/src/main/java/works/mees/dinghy/config/ConnectionStore.kt
    app/src/main/java/works/mees/dinghy/theme/ThemePrefs.kt
  </read_first>
  <action>
    Create `SettingsViewModel` exposing editable connection (host/port/key) + theme/accent/
    text-size state, backed by ConnectionStore + ThemePrefs. On save: validate via
    `ConnectionConfig.sanitize`, persist through ConnectionStore, then trigger session rebuild —
    the rebuild is driven by ConnectionStore.config emitting (the service observes it, 04-03,
    D-03); the ViewModel does NOT call into the service directly. Expose mDNS scan results as a
    lazy flow (collected only when scan is tapped → 04-01 `MdnsDiscovery.discover()`). Never
    expose the saved API key back to the UI: keep the key edit field blank on load, and expose a
    separate `apiKeySaved: Boolean` derived from whether ConnectionStore holds a non-null key
    (concern #10) so the screen can show a "key saved" indicator. Saving an empty key field MUST
    NOT clobber an existing saved key unless the user explicitly clears it (define a distinct
    "clear key" action).
  </action>
  <verify>
    <automated>cd /mnt/e/claude/personal/github/dinghy-display && /mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --tests *SettingsViewModelTest* --no-daemon" 2>&1 | tr -d '\r'</automated>
  </verify>
  <acceptance_criteria>
    - Save test: a valid host/port persists to ConnectionStore (round-trip asserted via a fake/in-memory store).
    - Invalid input (blank host / out-of-range port) is rejected before persist (no write to the store).
    - `apiKeySaved` is `true` when the store holds a non-null key and `false` otherwise; the key edit field's exposed value is always blank on load (never the secret).
    - Saving with the key field left blank preserves the previously saved key (no clobber); the explicit clear-key action sets it to null.
    - mDNS scan flow is collected only when the scan action is invoked (not on init) — asserted via a discovery double whose invocation count is 0 until scan.
    - `:app:testDebugUnitTest --tests *SettingsViewModelTest*` exits 0.
  </acceptance_criteria>
  <done>ViewModel validates, persists, drives rebuild via store, exposes lazy mDNS scan + apiKeySaved, never echoes the key.</done>
</task>

<task type="auto">
  <name>Task 2: TokenTextField + SettingsScreen (conventional list, token-themed)</name>
  <files>
    app/src/main/java/works/mees/dinghy/settings/TokenTextField.kt
    app/src/main/java/works/mees/dinghy/settings/SettingsScreen.kt
  </files>
  <read_first>
    app/src/main/java/works/mees/dinghy/settings/SettingsViewModel.kt
    app/src/main/java/works/mees/dinghy/theme/LocalTokens.kt
    docs/ui_design/THEMING.md
    app/src/main/java/works/mees/dinghy/designsystem/control/OutlinedControl.kt
  </read_first>
  <action>
    First create `TokenTextField` (concern #7): a thin wrapper over Material3 `OutlinedTextField`
    that maps its `OutlinedTextFieldDefaults.colors(...)` (focused/unfocused border, label,
    cursor, text, container) to `LocalTokens` semantic roles (outline → border, accent → focused
    border/cursor, text/surface roles → text/background) so Settings does not drift off the
    Phase-3 theme. Then create `SettingsScreen` — conventional vertical scrollable list (NOT
    Focus/Field/Gutter, D-15). Sections: Connection (host/port/key fields via TokenTextField +
    a "key saved" indicator when `apiKeySaved` + a Clear-key action + mDNS Scan button), Theme
    (dark/light toggle + accent picker writing a `TokenDelta` via ThemePrefs), Text size (S/M/L).
    System keyboard allowed here only (PRIM-02). Buttons use OutlinedControl. Wire to
    SettingsViewModel. The mDNS scan button collects the lazy discovery flow on tap.
  </action>
  <verify>
    <automated>cd /mnt/e/claude/personal/github/dinghy-display && /mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:assembleDebug --no-daemon" 2>&1 | tr -d '\r'</automated>
  </verify>
  <acceptance_criteria>
    - `TokenTextField` derives every color it passes to `OutlinedTextFieldDefaults.colors(...)` from `LocalTokens` — grep shows no raw `Color(` literal in TokenTextField.kt; colors reference token roles.
    - `SettingsScreen` uses `TokenTextField` for host/port/key (no bare `OutlinedTextField` for these fields).
    - The key section renders a "saved" indicator (text/icon) when `apiKeySaved` is true and never renders the secret value.
    - The accent picker writes a `TokenDelta` through `ThemePrefs` (grep: `ThemePrefs` + `TokenDelta` reference present).
    - `:app:assembleDebug` exits 0.
  </acceptance_criteria>
  <done>Settings renders all sections via token-aware fields, shows key-saved indicator, keyboard confined here, persists via ViewModel.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| keyboard input → ConnectionStore | host/port/key entry, the only keyboard surface |
| theme picker → ThemePrefs | accent override persisted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04-01 | Info disclosure | API key field | mitigate | never render saved key; blank-on-load edit field; only a non-secret "saved" indicator |
| T-04-04-02 | Tampering | host/port input | mitigate | sanitize before persist (reuse ConnectionConfig.validate) |

</threat_model>

<verification>
- SettingsViewModel unit tests green (persist, reject, key-saved, no-clobber, lazy scan)
- assembleDebug compiles
- Manual: keyboard only appears in Settings; field colors match the active theme
</verification>

<success_criteria>
- Connection editable + persisted; rebuild driven by store
- Theme/accent/text-size editable + persisted via ThemePrefs
- Keyboard confined to Settings; text fields token-themed (TokenTextField)
- API key never echoed; "key saved" indicator present
</success_criteria>

<output>
Create `.planning/phases/04-service-shell-settings-print-status-home/04-04-SUMMARY.md` when done
</output>
