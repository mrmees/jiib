---
phase: 04-service-shell-settings-print-status-home
plan: 03
type: execute
wave: 2
depends_on: ["04-01", "04-02"]
files_modified:
  - app/src/main/AndroidManifest.xml
  - app/src/main/java/works/mees/dinghy/DinghyApp.kt
  - app/src/main/java/works/mees/dinghy/di/AppContainer.kt
  - app/src/main/java/works/mees/dinghy/service/MoonrakerService.kt
  - app/src/test/java/works/mees/dinghy/service/MoonrakerServiceTest.kt
  - app/src/androidTest/java/works/mees/dinghy/service/ServiceSurvivesRotationTest.kt
autonomous: false
requirements: [CONN-01, SHELL-03]
must_haves:
  truths:
    - "A started foreground service (specialUse) owns the spine and keeps the connection alive across Activity recreation (rotation) and screen-off"
    - "Saving a new connection rebuilds the MoonrakerSession with the new host/key without leaking the old socket/scope (D-03)"
    - "App state (printerState/connectionState/capabilities) lives in a process-scoped service-locator the Activity collects — no service binding for state"
    - "A null/empty ConnectionStore config leaves the service idle (no socket) so routing shows the Connect prompt (D-11)"
  artifacts:
    - path: "app/src/main/AndroidManifest.xml"
      provides: "FGS perms + specialUse service + property subtype; Application registration"
      contains: "FOREGROUND_SERVICE_SPECIAL_USE"
    - path: "app/src/main/java/works/mees/dinghy/DinghyApp.kt"
      provides: "Application holding the AppContainer singleton + DataStore files"
      contains: "class DinghyApp"
    - path: "app/src/main/java/works/mees/dinghy/di/AppContainer.kt"
      provides: "Process-scoped service-locator exposing spine StateFlows + dispatcher + resolver + stores"
      contains: "class AppContainer"
    - path: "app/src/main/java/works/mees/dinghy/service/MoonrakerService.kt"
      provides: "Started FGS assembling/rebuilding the spine from ConnectionStore.config"
      contains: "foregroundServiceType"
    - path: "app/src/test/java/works/mees/dinghy/service/MoonrakerServiceTest.kt"
      provides: "JVM virtual-time proof of the config-rebuild seam: null config idle, config change cancels prior session before relaunch (D-03)"
      contains: "cancelAndJoin"
    - path: "app/src/androidTest/java/works/mees/dinghy/service/ServiceSurvivesRotationTest.kt"
      provides: "Instrumented rotation-survival proof of ConnectionState continuity"
  key_links:
    - from: "service/MoonrakerService.kt"
      to: "config/ConnectionStore.kt"
      via: "config.collectLatest { rebuild session }"
      pattern: "collectLatest"
    - from: "service/MoonrakerService.kt"
      to: "net/MoonrakerSession.kt"
      via: "constructs + run() in serviceScope; cancelAndJoin on rebuild"
      pattern: "MoonrakerSession"
    - from: "di/AppContainer.kt"
      to: "state/PrinterStateStore.kt"
      via: "service publishes spine flows into container"
      pattern: "publishSpine|printerState"
---

<objective>
Bring the app to life: a process-held service-locator (`AppContainer`), an `Application`
(`DinghyApp`) that owns it plus the real DataStore files, and a started **foreground service**
(`specialUse`) that assembles and runs the Phase-2 Moonraker spine from the persisted
`ConnectionStore` config — surviving Activity recreation and screen-off (SHELL-03, D-01..D-03).
Saving a new connection rebuilds the session cleanly (D-03). This is the load-bearing wave:
every Wave-3 screen reads its state from the container the service publishes into.

Purpose: The connection now belongs to the OS, not the Activity — rotation/config-change/screen-off
no longer drop it; the user's saved config (04-01) becomes the live source replacing DevConfig.
Output: manifest perms + service + Application registration, `DinghyApp`, `AppContainer`,
`MoonrakerService`, a JVM config-rebuild-seam test, and an instrumented rotation-survival test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-service-shell-settings-print-status-home/04-CONTEXT.md
@.planning/phases/04-service-shell-settings-print-status-home/04-PATTERNS.md
@.planning/phases/02-connection-state-foundation/02-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: [BLOCKING] Manifest perms + specialUse service + Application registration</name>
  <files>app/src/main/AndroidManifest.xml</files>
  <read_first>
    - app/src/main/AndroidManifest.xml (current manifest — header L2-14 says do NOT touch the cleartext posture / network_security_config.xml, both Phase-1 owned; .MainActivity registration at L29-37; ADD only)
    - .planning/phases/04-service-shell-settings-print-status-home/04-PATTERNS.md (AndroidManifest section ~L373-395 — verified declaration shapes)
  </read_first>
  <action>
    Edit `app/src/main/AndroidManifest.xml` — ADD ONLY, do not modify `usesCleartextTraffic`,
    `networkSecurityConfig`, or the existing activity registrations. Add `<uses-permission>` for
    `android.permission.FOREGROUND_SERVICE`, `android.permission.FOREGROUND_SERVICE_SPECIAL_USE`,
    `android.permission.POST_NOTIFICATIONS`, and `android.permission.CHANGE_WIFI_MULTICAST_STATE`
    (the last for the 04-01 mDNS scan). Add `android:name=".DinghyApp"` to the `<application>` element.
    Inside `<application>`, register the service:
    `<service android:name=".service.MoonrakerService" android:exported="false"
    android:foregroundServiceType="specialUse">` with a child `<property
    android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
    android:value="Maintains a persistent LAN websocket session to a Klipper/Moonraker 3D printer for live status and control" />`.
    Rationale (D-01): `specialUse` avoids Android-15's `dataSync` 6h cap; Play-Store justification is moot
    (sideload). Enforcement is a no-op on the API-30 test device but must be declared for targetSdk 35 / newer
    hardware. This is BLOCKING because DinghyApp + MoonrakerService (Tasks 2-3) will not start without it.
  </action>
  <verify>
    <automated>cmd.exe /c "E:\Android\gw.bat :app:processDebugMainManifest --no-daemon" && grep -c 'FOREGROUND_SERVICE_SPECIAL_USE\|specialUse\|.DinghyApp' app/src/main/AndroidManifest.xml</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'FOREGROUND_SERVICE_SPECIAL_USE' app/src/main/AndroidManifest.xml` matches
    - `grep -n 'android:foregroundServiceType="specialUse"' app/src/main/AndroidManifest.xml` matches
    - `grep -n 'PROPERTY_SPECIAL_USE_FGS_SUBTYPE' app/src/main/AndroidManifest.xml` matches
    - `grep -n 'android:name=".DinghyApp"' app/src/main/AndroidManifest.xml` matches
    - `grep -n 'usesCleartextTraffic\|networkSecurityConfig' app/src/main/AndroidManifest.xml` STILL present and unchanged (Phase-1 posture untouched)
    - `:app:processDebugMainManifest` succeeds (manifest merges)
  </acceptance_criteria>
  <done>Manifest declares FGS perms, the specialUse service + subtype property, multicast perm, and the DinghyApp Application — without touching the Phase-1 cleartext posture.</done>
</task>

<task type="auto">
  <name>Task 2: DinghyApp (Application) + AppContainer (process-scoped service-locator)</name>
  <files>app/src/main/java/works/mees/dinghy/DinghyApp.kt, app/src/main/java/works/mees/dinghy/di/AppContainer.kt</files>
  <read_first>
    - app/src/debug/java/works/mees/dinghy/gallery/GalleryActivity.kt (the sole-assembler idiom L50-58: build ThemeResolver + PrinterStateStore and hand them down — AppContainer promotes this to an Application singleton)
    - app/src/main/java/works/mees/dinghy/theme/ThemeResolver.kt (ctor L82-86: base/deltas/fs; seed from ThemePrefs.flow then resolver.apply(...) ~L115; setBase/setFs/setDeltas L97-109)
    - app/src/main/java/works/mees/dinghy/theme/ThemePrefs.kt (ctor takes DataStore<Preferences>; DEFAULT)
    - app/src/main/java/works/mees/dinghy/config/ConnectionStore.kt (04-01 — the connection DataStore-backed store the container exposes)
    - app/src/main/java/works/mees/dinghy/state/PrinterStateStore.kt (StateFlows the service publishes; ctor L39-42)
    - app/src/main/java/works/mees/dinghy/command/CommandDispatcher.kt (04-02 — published once per session)
    - .planning/phases/04-service-shell-settings-print-status-home/04-PATTERNS.md (AppContainer + DinghyApp section ~L152-172; DECIDE note: separate connection.preferences_pb from theme.preferences_pb)
  </read_first>
  <action>
    Create `class DinghyApp : Application()` in `works.mees.dinghy`. In `onCreate`, create TWO DataStore files
    via `PreferenceDataStoreFactory.create` (or the `preferencesDataStore` delegate): `connection.preferences_pb`
    and `theme.preferences_pb` — SEPARATE files (cleaner key-redaction boundary per PATTERNS DECIDE). Construct
    `container = AppContainer(themeDataStore, connectionDataStore)` and expose it as `val container: AppContainer`.

    Create `class AppContainer(themeDataStore: DataStore<Preferences>, connectionDataStore: DataStore<Preferences>)`
    in `works.mees.dinghy.di` (NO Hilt — D-02). It owns: a `ThemePrefs(themeDataStore)`, a `ConnectionStore(connectionDataStore)`,
    a `ThemeResolver(...)` seeded once from `ThemePrefs.flow` (collect-once → `resolver.apply(base, deltas, fs)`
    mirroring GalleryActivity), and the *holders* for the spine the SERVICE publishes into: expose
    `val printerState: StateFlow<PrinterState>`, `val connectionState: StateFlow<ConnectionState>`,
    `val capabilities: StateFlow<Capabilities>`, and `val dispatcher: StateFlow<CommandDispatcher?>` (null until a
    session is built). Provide `fun publishSpine(store: PrinterStateStore, session: MoonrakerSession,
    dispatcher: CommandDispatcher)` that wires the container's exposed flows to the live ones (e.g. back the public
    StateFlows with `MutableStateFlow` the service updates, or re-point via `flatMapLatest` of a
    `MutableStateFlow<Spine?>`). Also expose `val hasConfig: Flow<Boolean>` derived from
    `connectionStore.config.map { it != null }` for routing. Apply the GalleryActivity discipline: the container
    assembles; screens construct nothing. Do NOT construct the socket/session here — the SERVICE does that and
    publishes in (RESEARCH Open Q2: service constructs, publishes into container).
  </action>
  <verify>
    <automated>cmd.exe /c "E:\Android\gw.bat :app:compileDebugKotlin --no-daemon"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'class DinghyApp : Application' app/src/main/java/works/mees/dinghy/DinghyApp.kt` matches
    - `grep -n 'connection.preferences_pb' app/src/main/java/works/mees/dinghy/DinghyApp.kt` AND `grep -n 'theme.preferences_pb'` both match (two separate stores)
    - `grep -n 'fun publishSpine' app/src/main/java/works/mees/dinghy/di/AppContainer.kt` matches
    - `grep -n 'Hilt\|@Inject\|dagger' app/src/main/java/works/mees/dinghy/di/AppContainer.kt` returns NOTHING (no DI framework, D-02)
    - AppContainer constructs NO MoonrakerSocket/MoonrakerSession (`grep -n 'MoonrakerSocket\|MoonrakerSession' app/src/main/java/works/mees/dinghy/di/AppContainer.kt` returns nothing — the service owns assembly)
    - `:app:compileDebugKotlin` succeeds
  </acceptance_criteria>
  <done>DinghyApp holds the AppContainer + two DataStore files; AppContainer exposes spine flows (published by the service) + dispatcher + resolver + stores + hasConfig, with no Hilt and no socket construction.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: MoonrakerService — started FGS assembling + rebuilding the spine, with a JVM seam test</name>
  <files>app/src/main/java/works/mees/dinghy/service/MoonrakerService.kt, app/src/test/java/works/mees/dinghy/service/MoonrakerServiceTest.kt</files>
  <read_first>
    - .planning/phases/04-service-shell-settings-print-status-home/04-PATTERNS.md (MoonrakerService section ~L39-121 — the VERIFIED spine constructors + config-rebuild seam; copy the assembly order, use REAL signatures NOT the RESEARCH skeleton)
    - app/src/main/java/works/mees/dinghy/net/MoonrakerSession.kt (real ctor L60-74: store, rpc, socketEvents:(token?)->Flow<SocketEvent>, auth?, baseWsUrl, …, clientUrl defaults to https://mees.works/dinghy-display — DO NOT regress; connectionState StateFlow + requestReconnectNow L82; run() suspend; cancelAndJoin at L209,241)
    - app/src/main/java/works/mees/dinghy/net/MoonrakerSocket.kt (real(client, wsUrl) L120-127 — wsUrl DEFAULTS to DevConfig.wsUrl so you MUST override with ConnectionConfig.wsUrl; defaultClient() = the one shared OkHttpClient; .events())
    - app/src/main/java/works/mees/dinghy/net/JsonRpcClient.kt (ctor L50-52 default timeout 10_000L; request())
    - app/src/main/java/works/mees/dinghy/auth/MoonrakerAuth.kt (ctor L36-40: callFactory=shared OkHttpClient, httpBase=ConnectionConfig.httpBase, apiKey; construct ONLY when keyed; redaction L113-114,130-136)
    - app/src/main/java/works/mees/dinghy/state/PrinterStateStore.kt (ctor L39-42: pass serviceScope; DEFAULT_SAMPLE_MS 250 — do NOT add a second throttle)
    - app/src/main/java/works/mees/dinghy/config/ConnectionStore.kt (04-01 — config: Flow<ConnectionConfig?>)
    - app/src/main/java/works/mees/dinghy/command/CommandDispatcher.kt (04-02 — built per session, published into container)
    - app/src/test/java/works/mees/dinghy/command/CommandDispatcherTest.kt (04-02 — the existing `runTest`/virtual-time idiom + fake-flow harness to mirror)
  </read_first>
  <behavior>
    MoonrakerServiceTest (JVM, `runTest` + `TestCoroutineScheduler` virtual time; exercise the
    config-rebuild loop as an extracted suspend fun / testable seam, NOT the Android Service lifecycle):
    - Test 1 (idle on null): emitting a `null` config leaves NO session job running — no MoonrakerSession
      is constructed and nothing is launched (assert the fake `buildSession`/launch hook was never invoked,
      or the tracked `sessionJob` is null/not-active).
    - Test 2 (rebuild cancels prior): emit config A (a job launches and stays active), then emit config B —
      the loop calls `cancelAndJoin()` on the previous job BEFORE the new one is launched (assert ordering:
      the old job is cancelled/completed before the new session is built; exactly one active job afterward).
    Use a fake `ConnectionStore` exposing a controllable `MutableStateFlow<ConnectionConfig?>` and fake
    session-build/launch hooks so no real socket/OkHttp is touched.
  </behavior>
  <action>
    Create `class MoonrakerService : Service()` in `works.mees.dinghy.service`. Hold a `serviceScope =
    CoroutineScope(SupervisorJob() + Dispatchers.Default)` and a nullable `sessionJob: Job?`. In `onCreate`:
    register an `IMPORTANCE_LOW` `NotificationChannel` (Pitfall 1) and call `startForeground(NOTIF_ID, notif)`
    within ~5s with a persistent low-importance notification showing connection/printer status that taps back to
    MainActivity — NEVER put the API key in the notification text (redaction discipline). `onBind` returns null
    (started, NOT bound — state is process-held in AppContainer, D-02). `onStartCommand` returns `START_STICKY`.

    Launch the config-rebuild loop in serviceScope: `container.connectionStore.config.collectLatest { cfg ->
    sessionJob?.cancelAndJoin()` (the clean teardown seam, D-03 — cancel BEFORE building the new session so no
    socket/scope leaks); `if (cfg == null) { update notification "Set up printer"; return@collectLatest }`;
    else build the spine with the VERIFIED constructors: one shared `OkHttpClient = MoonrakerSocket.defaultClient()`;
    `store = PrinterStateStore(scope = serviceScope)`; `rpc = JsonRpcClient()`; a `socketEvents: (token: String?) ->
    Flow<SocketEvent>` lambda that calls `MoonrakerSocket.real(client, wsUrl = cfg.wsUrl).events()` (OVERRIDE the
    DevConfig.wsUrl default with cfg.wsUrl; for the auth path build the token-bearing URL via
    MoonrakerAuth.buildAuthedWsUrl); `auth = if (cfg.apiKey != null) MoonrakerAuth(client, cfg.httpBase, cfg.apiKey)
    else null`; `session = MoonrakerSession(store, rpc, socketEvents, auth, baseWsUrl = cfg.wsUrl)` — KEEP the
    default `clientUrl = "https://mees.works/dinghy-display"` (the live-verified identify-url; regressing it
    re-breaks Connected, see 02-04-SUMMARY). `dispatcher = CommandDispatcher(rpc, serviceScope)`. Call
    `container.publishSpine(store, session, dispatcher)`; then `sessionJob = serviceScope.launch { session.run() }`.
    Drive the notification text from `session.connectionState`. `onDestroy { serviceScope.cancel() }`.
    Resolve `container` via `(application as DinghyApp).container`.

    To make the rebuild loop JVM-testable (Warning 1 / Nyquist seam), extract the collect body into an internal
    suspend fun (e.g. `internal suspend fun runConfigLoop(configFlow: Flow<ConnectionConfig?>, scope:
    CoroutineScope, buildAndLaunch: (ConnectionConfig) -> Job)`) that the Service calls with the real builder and
    MoonrakerServiceTest calls with fakes — so the `collectLatest` + `cancelAndJoin()`-before-relaunch ordering is
    asserted in virtual time without the Android lifecycle or a real socket. Write `MoonrakerServiceTest` per the
    `<behavior>` block above, mirroring CommandDispatcherTest's `runTest` harness.
  </action>
  <verify>
    <automated>cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --no-daemon --tests works.mees.dinghy.service.MoonrakerServiceTest" && cmd.exe /c "E:\Android\gw.bat :app:compileDebugKotlin --no-daemon"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'collectLatest' app/src/main/java/works/mees/dinghy/service/MoonrakerService.kt` matches (config-driven rebuild)
    - `grep -n 'cancelAndJoin' app/src/main/java/works/mees/dinghy/service/MoonrakerService.kt` matches (old session torn down before rebuild, D-03)
    - `grep -n 'wsUrl = cfg.wsUrl\|cfg.wsUrl' app/src/main/java/works/mees/dinghy/service/MoonrakerService.kt` matches (DevConfig default overridden)
    - `grep -n 'clientUrl' app/src/main/java/works/mees/dinghy/service/MoonrakerService.kt` either absent or set to "https://mees.works/dinghy-display" (NOT regressed)
    - `grep -n 'startForeground\|START_STICKY\|IMPORTANCE_LOW' app/src/main/java/works/mees/dinghy/service/MoonrakerService.kt` all match
    - `onBind` returns null (`grep -n 'override fun onBind' -A2` shows `return null`)
    - The notification text construction does not reference apiKey
    - `MoonrakerServiceTest` passes: null config launches no session job; a config change cancelAndJoins the prior job before launching the new one (assert ordering + single active job)
    - `:app:compileDebugKotlin` succeeds
  </acceptance_criteria>
  <done>A started specialUse FGS owns serviceScope, assembles the real spine from ConnectionStore.config with the verified constructors (DevConfig wsUrl overridden, identify clientUrl preserved), rebuilds cleanly on config change (cancelAndJoin), publishes into AppContainer, shows a key-free status notification, and the JVM MoonrakerServiceTest proves the null-idle + cancel-before-relaunch seam in virtual time.</done>
</task>

<task type="auto">
  <name>Task 4: ServiceSurvivesRotationTest — instrumented rotation-survival proof (SHELL-03)</name>
  <files>app/src/androidTest/java/works/mees/dinghy/service/ServiceSurvivesRotationTest.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/service/MoonrakerService.kt (Task 3 — the FGS that owns the spine; START_STICKY, publishes into AppContainer)
    - app/src/main/java/works/mees/dinghy/di/AppContainer.kt (Task 2 — `connectionState: StateFlow<ConnectionState>` is the observable the test samples; resolve via `(app as DinghyApp).container`)
    - app/src/main/java/works/mees/dinghy/DinghyApp.kt (Task 2 — Application holding the container; reach it from the instrumentation `targetContext.applicationContext as DinghyApp`)
    - app/src/main/AndroidManifest.xml (Task 1 — the registered service the test starts)
    - app/src/main/java/works/mees/dinghy/state/ConnectionState.kt (the sealed/enum surface — the Connecting/Syncing transient states the test asserts do NOT recur across rotation)
    - .planning/phases/02-connection-state-foundation/02-04-SUMMARY.md (the live-proven connect→Connected lifecycle this test guards against regressing)
  </read_first>
  <action>
    Create `ServiceSurvivesRotationTest` in `app/src/androidTest/java/works/mees/dinghy/service/` (AndroidX Test +
    UiAutomator — the on-device source set already exists from Phase 2). It proves SHELL-03 / D-02: the FGS owns
    the spine, so an Activity recreation (rotation) does NOT churn the connection through Connecting/Syncing.

    Setup: obtain `val app = ApplicationProvider.getApplicationContext<DinghyApp>()` and `val container =
    app.container`. Seed a connection so the service connects — either (a) write a known-good ConnectionConfig
    into `container.connectionStore` (pointing at a reachable test Moonraker / the Ender 5 Plus), or (b) if no
    live printer is guaranteed in CI, seed a config whose host resolves and assert the spine does not recreate;
    the load-bearing assertion is *continuity across rotation*, not reaching Connected. Start the service via
    `context.startForegroundService(Intent(context, MoonrakerService::class.java))` and launch MainActivity with
    an `ActivityScenario`. Wait until `container.connectionState` settles (collect on a background scope into a
    recorded list, or use a latch/`first { it is Connected || it is Disconnected }`).

    Rotation: `val device = UiDevice.getInstance(InstrumentationRegistry.getInstrumentation())`; record the
    `connectionState` value list from this point; `device.setOrientationLeft()`, await Activity recreation,
    `device.setOrientationNatural()`. After rotation, assert the recorded `connectionState` sequence did NOT
    contain a fresh `Connecting`/`Syncing` transition (i.e. the spine was NOT torn down + rebuilt by the Activity
    — the FGS kept it). `@After`: `device.setOrientationNatural()` and stop the service. Keep the assertion on the
    *process-scoped* `AppContainer.connectionState` (the published spine flow), NOT on any Activity-held state.
  </action>
  <verify>
    <automated>cmd.exe /c "E:\Android\gw.bat :app:connectedDebugAndroidTest --no-daemon --tests works.mees.dinghy.service.ServiceSurvivesRotationTest"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'class ServiceSurvivesRotationTest' app/src/androidTest/java/works/mees/dinghy/service/ServiceSurvivesRotationTest.kt` matches
    - `grep -nE 'setOrientationLeft|setOrientationNatural' …ServiceSurvivesRotationTest.kt` both match (rotation driven via UiDevice)
    - `grep -n 'connectionState' …ServiceSurvivesRotationTest.kt` matches (samples the process-scoped AppContainer flow, not Activity state)
    - the test asserts NO fresh Connecting/Syncing transition occurs across the rotation (continuity assertion present)
    - `:app:connectedDebugAndroidTest --tests …ServiceSurvivesRotationTest` runs on flox and PASSES
  </acceptance_criteria>
  <done>An instrumented ServiceSurvivesRotationTest exists and passes on flox: it seeds a connection, rotates via UiDevice, and asserts AppContainer.connectionState does not churn through Connecting/Syncing — proving the FGS (not the Activity) owns the spine (SHELL-03/D-02).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: On-device service-survival checkpoint (rotation + screen-off) on flox</name>
  <action>Verify on the real flox device that the foreground service keeps the Moonraker connection alive across Activity recreation (rotation) and screen-off, and that the instrumented ServiceSurvivesRotationTest (Task 4) passes. Capture the observed ConnectionState continuity (no Connecting/Syncing transition) as evidence.</action>
  <what-built>
    A started foreground service (specialUse) that owns the Moonraker spine in a process-scoped
    AppContainer, so the connection survives Activity recreation (rotation) and screen-off (SHELL-03).
    The instrumented `ServiceSurvivesRotationTest` (authored in Task 4) lives in
    `app/src/androidTest/.../service/ServiceSurvivesRotationTest.kt`: it seeds a config, rotates via
    `UiDevice.setOrientationLeft()`/`setOrientationNatural()`, and asserts `AppContainer.connectionState`
    does NOT transition back through Connecting/Syncing (the spine was not recreated by the Activity).
    The DevConfig static path is replaced — the service now connects from the persisted ConnectionStore config.
  </what-built>
  <how-to-verify>
    On the real flox device (LineageOS 18.1 / API 30), with the Ender 5 Plus reachable on the LAN:
    1. Build + install the debug APK:
       `cmd.exe /c "E:\Android\gw.bat :app:installDebug --no-daemon"`
    2. Seed a real connection (via Settings once 04-04 lands, or temporarily push a config) so the service connects.
    3. Confirm the persistent low-importance notification appears and shows connection status WITHOUT the API key.
    4. Rotate the device portrait↔landscape several times. Expected: the status stays Connected; temps remain
       live (not stale) — the connection does NOT drop/reconnect on each rotation.
    5. Screen off for ~60s, screen on. Expected: still Connected, temps live not stale.
    6. Run the instrumented gate:
       `cmd.exe /c "E:\Android\gw.bat :app:connectedDebugAndroidTest --no-daemon --tests works.mees.dinghy.service.ServiceSurvivesRotationTest"`
       Expected: PASS (no Connecting/Syncing transition across rotation).
  </how-to-verify>
  <resume-signal>Type "approved" once rotation + screen-off keep the connection alive and the instrumented test passes, or describe what dropped.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stored config → spine | persisted host/port/key crosses into the live socket/auth construction |
| API key → FGS notification / logs | the optional key must never surface in persistent UI or logcat |
| LAN socket ↔ printer | cleartext ws://+http:// to a trusted-LAN Moonraker (Phase-1 accepted posture) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03-I | Information Disclosure | FGS notification + service logs | mitigate | notification text driven by ConnectionState only — never apiKey; MoonrakerAuth redaction (redactWsUrl, toString) reused; acceptance grep asserts notification has no apiKey reference |
| T-04-03-cleartext | Information Disclosure | ws:// + http:// transport | accept | reference Phase-1 owned manifest + network_security_config.xml; local-LAN trusted printer, no internet exposure; this plan adds NO cleartext config (untouched) |
| T-04-03-D | Denial of Service / leak | session rebuild on config change | mitigate | `sessionJob?.cancelAndJoin()` before rebuild guarantees the old socket/scope is torn down (no leak, no dual-connection storm, D-03); proven by MoonrakerServiceTest (cancel-before-relaunch ordering in virtual time) |
| T-04-03-SC | Tampering | npm/pip/cargo installs | mitigate | NO new third-party packages added this plan (NsdManager/FGS are platform APIs); no install task, so no package-legitimacy gate needed |
</threat_model>

<verification>
- `:app:processDebugMainManifest` green; manifest greps confirm FGS perms/service/property + DinghyApp, cleartext posture untouched
- `:app:compileDebugKotlin` green (DinghyApp + AppContainer + MoonrakerService)
- `:app:testDebugUnitTest --tests …MoonrakerServiceTest` green (config-rebuild seam: null-idle + cancel-before-relaunch in virtual time)
- `:app:connectedDebugAndroidTest --tests …ServiceSurvivesRotationTest` green on flox (rotation continuity)
- On-device checkpoint: rotation + screen-off keep ConnectionState=Connected, temps live
</verification>

<success_criteria>
- A started specialUse FGS owns the spine across Activity recreation + screen-off (SHELL-03)
- The service connects from the persisted ConnectionStore config, replacing DevConfig (CONN-01)
- Config change rebuilds the session cleanly with no socket/scope leak (D-03), proven by MoonrakerServiceTest
- State lives in a process-scoped AppContainer the Activity collects; no binding for state (D-02)
- The identify clientUrl is preserved; the DevConfig wsUrl default is overridden
- Rotation survival is proven by the instrumented ServiceSurvivesRotationTest on flox
</success_criteria>

<output>
Create `.planning/phases/04-service-shell-settings-print-status-home/04-03-SUMMARY.md` when done
</output>
</output>
