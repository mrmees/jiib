---
phase: 04-service-shell-settings-print-status-home
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/main/java/works/mees/dinghy/dispatch/CommandDispatcher.kt
  - app/src/main/java/works/mees/dinghy/dispatch/CommandResult.kt
  - app/src/main/java/works/mees/dinghy/shell/TopRoute.kt
  - app/src/test/java/works/mees/dinghy/dispatch/CommandDispatcherTest.kt
  - app/src/test/java/works/mees/dinghy/shell/TopRouteTest.kt
autonomous: true
requirements: [PRIM-05]

must_haves:
  truths:
    - "Action calls enforce an explicit timeout (no infinite hang)"
    - "A second tap while in-flight is debounced/ignored"
    - "klippy_state deterministically maps to exactly one top-level route (Splash | Shell)"
  artifacts:
    - path: app/src/main/java/works/mees/dinghy/dispatch/CommandDispatcher.kt
      provides: "Shared command-dispatch wrapper (timeout + in-flight + debounce)"
    - path: app/src/main/java/works/mees/dinghy/shell/TopRoute.kt
      provides: "Pure klippy_state → route derivation (Splash | Shell); the single routing authority the root controller consumes"
  key_links:
    - from: CommandDispatcher
      to: SeverityToast
      via: "failures surface as toast"
      pattern: "CommandResult"

---

<objective>
Two pure primitives the rest of the phase builds on: the shared **command-dispatch wrapper**
(PRIM-05 — timeout + in-flight/busy + debounce) and the pure **klippy_state → TopRoute**
derivation (D-05/D-06 routing logic, no Android deps).

Purpose: PRIM-05 wraps every Moonraker action call; `TopRoute.derive()` is the pure routing
core the SINGLE root controller (04-07, review concern #2) consumes — `MainActivity` is the
only place that branches on it. `TopRoute` is intentionally two-valued (`Splash`, `Shell`);
Settings is NOT a top route — it is a Shell destination plus a controlled escape state the root
controller owns (resolved in 04-07). Both primitives are host-testable in isolation.
Output: `CommandDispatcher`, `CommandResult`, `TopRoute` + `derive()`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-service-shell-settings-print-status-home/04-CONTEXT.md
@.planning/phases/04-service-shell-settings-print-status-home/04-RESEARCH.md
@.planning/phases/04-service-shell-settings-print-status-home/04-PATTERNS.md

Source-of-truth files to read before implementing:
- app/src/main/java/works/mees/dinghy/state/PrinterState.kt (klippyState/printState enums)
- app/src/main/java/works/mees/dinghy/designsystem/SeverityToast.kt (failure feedback target)
</context>

<tasks>

<task type="auto">
  <name>Task 1: CommandDispatcher (timeout + in-flight + debounce)</name>
  <files>
    app/src/main/java/works/mees/dinghy/dispatch/CommandDispatcher.kt
    app/src/main/java/works/mees/dinghy/dispatch/CommandResult.kt
    app/src/test/java/works/mees/dinghy/dispatch/CommandDispatcherTest.kt
  </files>
  <read_first>
    app/src/main/java/works/mees/dinghy/state/PrinterState.kt
    app/src/main/java/works/mees/dinghy/designsystem/SeverityToast.kt
  </read_first>
  <action>
    Create `CommandDispatcher` — a shared wrapper for Moonraker action calls. Enforces:
    (1) explicit timeout via `withTimeout` (default ~10s, configurable);
    (2) in-flight/busy `StateFlow<Boolean>` so the UI disables the control;
    (3) tap debounce — a second `dispatch()` while one is in-flight is ignored (returns
    a `CommandResult.Busy`, does not enqueue). Return a sealed `CommandResult`
    (`Success` / `Timeout` / `Failure(cause)` / `Busy`). No Android deps — pure coroutines for
    host testing using `kotlinx-coroutines-test` virtual time.
  </action>
  <verify>
    <automated>cd /mnt/e/claude/personal/github/dinghy-display && /mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --tests *CommandDispatcherTest* --no-daemon" 2>&1 | tr -d '\r'</automated>
  </verify>
  <acceptance_criteria>
    - Timeout test (virtual time): a block that never completes yields `CommandResult.Timeout` after the configured timeout; `inFlight` returns to `false` afterward.
    - In-flight test: while one `dispatch()` is suspended, `inFlight` StateFlow value is `true`.
    - Debounce test: a second `dispatch()` invoked while the first is in-flight returns `CommandResult.Busy` and the first block is invoked exactly once (concurrent re-fire blocked).
    - Success path returns `CommandResult.Success` and `inFlight` is `false` at completion.
    - File contains no `import android.`; tests run on the JVM unit source set.
    - `:app:testDebugUnitTest --tests *CommandDispatcherTest*` exits 0.
  </acceptance_criteria>
  <done>Timeout fires, in-flight blocks concurrent dispatch, debounce verified — all host-tested.</done>
</task>

<task type="auto">
  <name>Task 2: TopRoute pure derivation</name>
  <files>
    app/src/main/java/works/mees/dinghy/shell/TopRoute.kt
    app/src/test/java/works/mees/dinghy/shell/TopRouteTest.kt
  </files>
  <read_first>
    app/src/main/java/works/mees/dinghy/state/PrinterState.kt
  </read_first>
  <action>
    Create pure `TopRoute` sealed type with EXACTLY two cases (`Splash`, `Shell`) +
    `derive(klippyState, printState, hasConfig): TopRoute`. Rules (D-05/D-06): no saved config
    → `Splash` (first-run); klippy not `Ready` (Startup/Error/Shutdown/Disconnected) → `Splash`
    (hard override); klippy `Ready` → `Shell` (Print Status home). Settings is deliberately NOT
    a `TopRoute` case — it is reached as a Shell destination or as a root-owned escape overlay
    (04-07, concern #2). Pure function, exhaustive `when`, host-tested across all
    klippyState × printState × hasConfig combinations.
  </action>
  <verify>
    <automated>cd /mnt/e/claude/personal/github/dinghy-display && /mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --tests *TopRouteTest* --no-daemon" 2>&1 | tr -d '\r'</automated>
  </verify>
  <acceptance_criteria>
    - `TopRoute` has exactly two subtypes: `Splash` and `Shell` (asserted/inspected; no `Settings`/`Connect` case).
    - `derive(_, _, hasConfig=false)` → `Splash` for every klippyState (first-run override dominates).
    - `derive(Ready, _, hasConfig=true)` → `Shell`; `derive(Startup|Error|Shutdown|Disconnected, _, true)` → `Splash`.
    - Test enumerates all KlippyState × PrintState × {hasConfig true/false} combinations and asserts each maps to exactly one route (totality).
    - `:app:testDebugUnitTest --tests *TopRouteTest*` exits 0.
  </acceptance_criteria>
  <done>derive() is exhaustive and total; every state combo maps to exactly one route; tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI tap → CommandDispatcher → Moonraker | user action crosses into network calls |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-01 | DoS (self) | rapid taps | mitigate | debounce + in-flight guard prevents flooding the printer |
| T-04-02-02 | DoS (hang) | dropped packet | mitigate | explicit withTimeout bounds every call |

</threat_model>

<verification>
Run all 04-02 unit tests:
`:app:testDebugUnitTest --tests *CommandDispatcherTest* --tests *TopRouteTest*`
All green; dispatcher enforces all three guarantees; derive() is total and two-valued.
</verification>

<success_criteria>
- CommandDispatcher enforces timeout + in-flight + debounce
- TopRoute.derive() is pure, exhaustive, and exactly two-valued (Splash | Shell)
- All unit tests green
</success_criteria>

<output>
Create `.planning/phases/04-service-shell-settings-print-status-home/04-02-SUMMARY.md` when done
</output>
