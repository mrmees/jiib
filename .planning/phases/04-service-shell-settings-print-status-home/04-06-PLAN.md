---
phase: 04-service-shell-settings-print-status-home
plan: 06
type: execute
wave: 3
depends_on: ["04-03"]
files_modified:
  - app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusHolder.kt
  - app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusScreen.kt
  - app/src/test/java/works/mees/dinghy/ui/printstatus/PrintStatusHolderTest.kt
autonomous: true
requirements: [SHELL-04, SHELL-02]
must_haves:
  truths:
    - "Print Status is the single home surface whose Focus adapts: printing → ProgressRing; idle → live temp/status 'Ready' readout (D-08)"
    - "The screen composes ring + 2x3 numeric stat grid with a reserved sparkline slot (the GraphView sparkline + perf gate land in 04-06b, review #4)"
    - "A Stop control routes through the full-screen ConfirmGuard and fires printer.emergency_stop via the dispatcher/SessionControl (SHELL-02/D-10 / review #1)"
    - "The stat grid degrades deterministically for printers lacking a bed, with nonstandard heater names, or with chamber heaters (review #9)"
    - "Live state drives a per-heater RingBuffer fed from the already-throttled store (no second throttle added)"
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusHolder.kt"
      provides: "Toolkit-agnostic holder: PrinterState → ring-buffer series + grid values with explicit capability fallback (review #9)"
      contains: "RingBuffer"
    - path: "app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusScreen.kt"
      provides: "State-adaptive Focus + stat grid + reserved sparkline slot + Stop→ConfirmGuard"
      contains: "ProgressRing"
    - path: "app/src/test/java/works/mees/dinghy/ui/printstatus/PrintStatusHolderTest.kt"
      provides: "Host-side proof: bounded ring off PrinterState + capability-fallback cases"
  key_links:
    - from: "ui/printstatus/PrintStatusScreen.kt"
      to: "designsystem/ConfirmGuard.kt"
      via: "Stop tile → ConfirmGuard → dispatch emergency_stop"
      pattern: "ConfirmGuard|emergency_stop|EMERGENCY_STOP"
    - from: "ui/printstatus/PrintStatusHolder.kt"
      to: "render/RingBuffer.kt"
      via: "push(state.heaters[...].temperature)"
      pattern: "push\\(|RingBuffer"
---

<objective>
Build the Print Status home (part 1 of 2, SHELL-04) — the monitor/landing surface: a state-adaptive
Focus (printing → `ProgressRing`; idle → live temp/status readout, D-08), a 2×3 numeric stat grid,
and a Stop control routing through the full-screen `ConfirmGuard` to fire `printer.emergency_stop`
(SHELL-02/D-10). The gutter shows Stop + greyed Tune/Pause placeholders (D-07).

Review fixes applied to the original plan:
- **#4 (04-06 overloaded):** the original carried adaptive home + holder + grid + ConfirmGuard +
  the heater sparkline (GraphView) + dispatcher-failure toast + the on-device combined-render perf
  gate — the highest overrun risk on the Adreno 320 floor. This plan delivers the adaptive home,
  the grid, and the wired Stop, and RESERVES a sparkline slot in the Field. The heater sparkline
  (GraphView) and the combined-render perf gate move to **04-06b** (wave 4).
- **#9 (capability gating too loose):** the holder defines EXPLICIT fallback rules for printers
  without a bed, with nonstandard heater names, or with chamber heaters.
- **#1 (SessionControl):** Stop fires via the per-session dispatcher / SessionControl — not a raw
  session.

Purpose: The app finally shows a live printer at a glance and gives the firmware panic halt one
confirmed tap away.
Output: `PrintStatusHolder` (toolkit-agnostic, with capability fallback), `PrintStatusScreen` (with a
reserved sparkline slot), and a host-side holder test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-service-shell-settings-print-status-home/04-CONTEXT.md
@.planning/phases/04-service-shell-settings-print-status-home/04-PATTERNS.md
@docs/ui_design/LAYOUT.md
@docs/ui_design/images/03-print-status.png
@docs/ui_design/images/08-confirm.png
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PrintStatusHolder — PrinterState → ring-buffer series + grid values with strict capability fallback (review #9)</name>
  <files>app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusHolder.kt, app/src/test/java/works/mees/dinghy/ui/printstatus/PrintStatusHolderTest.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/render/RingBuffer.kt (L23 capacity default 120; push(Float); snapshot(): FloatArray — the bounded rolling window, already concurrency-safe; do NOT reimplement)
    - app/src/main/java/works/mees/dinghy/state/PrinterStateStore.kt (printerState StateFlow; the ~4Hz throttle is ALREADY built at DEFAULT_SAMPLE_MS=250 L146 — do NOT add a second throttle)
    - app/src/main/java/works/mees/dinghy/state/PrinterState.kt (heaters: Map<String,HeaterState> L27; HeaterState.temperature/target L59-62; progress L46; printState/klippyState; printFilename L43)
    - app/src/main/java/works/mees/dinghy/state/Capabilities.kt (deriveCapabilities output — heater names/extruder set; the source for the fallback rules, review #9)
    - app/src/main/java/works/mees/dinghy/gallery/GalleryScreen.kt (the live-feed collect idiom L114-122: store.printerState.collect { ring.push(extruder temp); snapshot = ring.snapshot() })
    - .planning/phases/04-service-shell-settings-print-status-home/04-PATTERNS.md (PrintStatus holder section ~L283-294 — mirror PrinterStateStore's StateFlow idiom; the only new piece is a per-heater RingBuffer owner)
  </read_first>
  <behavior>
    - onState(PrinterState with extruder temp=200.0) → the primary-heater RingBuffer's latest snapshot value reflects 200f
    - feeding N>capacity states keeps the ring bounded (snapshot length <= capacity 120)
    - standard printer (extruder + heater_bed): grid has a nozzle slot AND a bed slot with correct cur/target
    - no-bed printer: grid renders WITHOUT a bed slot (no fabricated 0°/— bed cell) and promotes the next available heater
    - chamber-only secondary: a `heater_generic chamber` is surfaced by its object name when no bed exists
    - nonstandard extruder name: primary resolves via the `extruder`-prefix rule when no exact `extruder` key exists
    - fewer than 6 stats available: empty cells render a placeholder, never fabricated values
    - holder adds NO throttle of its own (consumes the store's already-conflated flow)
    - holder is plain Kotlin (no Compose annotations) — host-testable
  </behavior>
  <action>
    Create `class PrintStatusHolder(scope: CoroutineScope, private val store: PrinterStateStore)` in
    `works.mees.dinghy.ui.printstatus`. Define EXPLICIT capability fallback rules (review #9): (1) primary heater =
    the `extruder` heater if present, else the first heater whose name starts with `extruder`; (2) secondary =
    `heater_bed` if present, else OMIT the bed slot and promote the next available heater (e.g. a chamber heater
    `heater_generic chamber`) into the grid; (3) printers with no bed render the grid WITHOUT a bed slot (no
    fabricated 0°/—); (4) nonstandard heater names are shown by their Moonraker object name; (5) when fewer than 6
    stats are available, remaining grid cells render an empty/"—" placeholder rather than fabricating data. Own one
    `RingBuffer` for the PRIMARY heater (consumed by the 04-06b sparkline). On construction,
    `scope.launch { store.printerState.collect { state -> push the primary heater's temperature into the RingBuffer;
    recompute exposed snapshots + grid values per the fallback rules } }` — mirror the GalleryScreen collect idiom;
    do NOT add a sample/throttle (the store already conflates at 250ms). Expose, via the
    `MutableStateFlow(...).asStateFlow()` idiom: a `StateFlow<FloatArray>` sparkline snapshot (primary heater), and
    a `StateFlow<PrintStatusGrid>` data class carrying the resolved 2×3 numeric cells (primary cur/target, secondary
    cur/target or omitted, progress%, and one of Z-height/filename) derived purely from PrinterState + Capabilities.
    Keep it toolkit-agnostic (no Compose) so it is host-unit-testable.

    Write `PrintStatusHolderTest` host-side covering EVERY `<behavior>` case (including all capability-fallback
    cases, review #9): drive the collect with a MutableStateFlow / seeded store + seeded Capabilities; assert the
    primary snapshot reflects the latest pushed temp, the ring stays bounded past 120, the bed slot is present/
    omitted/promoted per the rules, nonstandard names resolve, and under-6-stats cells are placeholders.
  </action>
  <verify>
    <automated>cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --no-daemon --tests works.mees.dinghy.ui.printstatus.PrintStatusHolderTest"</automated>
  </verify>
  <acceptance_criteria>
    - `PrintStatusHolderTest` passes: latest-temp reflected, ring bounded ≤120, AND all capability-fallback cases (standard, no-bed, chamber-only, nonstandard name, under-6-stats placeholder) — review #9
    - `grep -n 'RingBuffer' app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusHolder.kt` matches
    - holder adds no throttle: `grep -nE 'sample\(|debounce\(|delay\(' …PrintStatusHolder.kt` returns NOTHING
    - no Compose import in the holder (`grep -n 'androidx.compose' …PrintStatusHolder.kt` returns nothing)
  </acceptance_criteria>
  <done>PrintStatusHolder turns the already-throttled printerState into a bounded primary-heater ring snapshot + a 2×3 grid model with explicit capability fallback (no fabricated bed/data, review #9), toolkit-agnostic and host-tested, with no second throttle.</done>
</task>

<task type="auto">
  <name>Task 2: PrintStatusScreen — state-adaptive Focus + stat grid + reserved sparkline slot + Stop→ConfirmGuard</name>
  <files>app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusScreen.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/gallery/GalleryScreen.kt (EXACT analog L189-204: ProgressRing in Box.aspectRatio(1f) + GraphViewHost(tokens, snapshot) side-by-side fed by one RingBuffer; SectionLabel/grid text idiom L241-246/L326-337 — note the GraphViewHost wiring is deferred to 04-06b here)
    - app/src/main/java/works/mees/dinghy/render/ProgressRing.kt (L40 ProgressRing(progress: Float, modifier) — drive from PrinterState.progress 0..1; wrap in Modifier.aspectRatio(1f) sacred square)
    - app/src/main/java/works/mees/dinghy/designsystem/layout/ScreenScaffold.kt (L48 — focus/field/gutter; printing focus=ring, idle focus=temp readout D-08; gutter=Stop+greyed Tune/Pause D-07)
    - app/src/main/java/works/mees/dinghy/designsystem/ConfirmGuard.kt (L54-62 ConfirmGuard(title,message,confirmLabel,onConfirm,onCancel,destructive=true) — DISPATCHES NOTHING itself; wire onConfirm to dispatch; show/hide toggle idiom GalleryScreen.kt:280-299)
    - app/src/main/java/works/mees/dinghy/designsystem/control/OutlinedControl.kt (Stop tile = Intent.Danger; greyed Tune/Pause = disabled/no-op)
    - app/src/main/java/works/mees/dinghy/di/AppContainer.kt (04-03 — container.dispatcher / container.sessionControl; Stop dispatches via these, NOT a raw rpc — review #1)
    - app/src/main/java/works/mees/dinghy/net/JsonRpc.kt (04-02 — JsonRpcMethods.EMERGENCY_STOP for the Stop dispatch)
    - .planning/phases/04-service-shell-settings-print-status-home/04-PATTERNS.md (PrintStatusScreen + Stop sections ~L257-322 — the EXACT composition + CONTEXT-wins-over-RESEARCH note: E-stop uses ConfirmGuard, not a hold gesture)
    - docs/ui_design/images/03-print-status.png + 08-confirm.png (mockups; note 03 shows PRINTING only — Phase 4 also renders idle D-08; the sparkline D-09 lands in 04-06b)
  </read_first>
  <action>
    Create `ui/printstatus/PrintStatusScreen.kt` with `@Composable fun PrintStatusScreen(container: AppContainer,
    holder: PrintStatusHolder)`. Build with `ScreenScaffold`. Read `LocalTokens.current`; no raw colors;
    GeistMono tabular numerals via fsSp for all live numbers.

    Focus (state-adaptive, D-08): collect `container.printerState` with `collectAsStateWithLifecycle()`; when
    `printState == Printing || Paused` → `Box(Modifier.aspectRatio(1f)) { ProgressRing(progress =
    state.progress.toFloat(), Modifier.fillMaxSize()) }`; else (idle/Ready) → a live temp/status "Ready" readout
    (current primary/secondary temps + a "Ready" label) filling the same Focus region usefully (do NOT show a 0%
    ring — D-08, design philosophy "fill usable space").

    Field: the 2×3 numeric stat grid from `holder.grid` (primary cur/target, secondary cur/target or omitted per
    the fallback rules, progress%, Z/filename) AND a RESERVED sparkline placeholder slot (review #4): leave a sized
    Box/slot in the Field where 04-06b will host the GraphView sparkline — sized and positioned now so 04-06b is an
    additive fill, NOT a relayout. Do NOT host GraphViewHost in this plan.

    Gutter (D-07): three tiles — `OutlinedControl("Stop", Intent.Danger)` (wired), plus "Tune" and "Pause"
    rendered as greyed/disabled "coming soon" placeholders (no-op; Phase 5/7). The Stop tile sets a
    `showEstopGuard` state true; when true, overlay the full-screen `ConfirmGuard(title = "Emergency stop?",
    message = "This halts the printer.", confirmLabel = "STOP", destructive = true, onConfirm = {
    container.dispatcher.value?.dispatch("estop", JsonRpcMethods.EMERGENCY_STOP); showEstopGuard = false },
    onCancel = { showEstopGuard = false })`. The Stop dispatch goes through the per-session CommandDispatcher /
    SessionControl (review #1) — NOT a raw rpc.request. ConfirmGuard dispatches nothing itself — the onConfirm
    does (PRIM-03 + PRIM-05). Firing it drives klippy_state → shutdown → routes to the splash recovery surface
    (D-10, 04-05) automatically; do not navigate manually. Collect a dispatch-failure event from the dispatcher
    and show `SeverityToast(Severity.Error, …)` on failure. Render at the throttled cadence from the store (do NOT
    add a second sampling layer).
  </action>
  <verify>
    <automated>cmd.exe /c "E:\Android\gw.bat :app:compileDebugKotlin --no-daemon"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'ProgressRing' app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusScreen.kt` matches (ring in Focus)
    - state-adaptive Focus present: a `when`/`if` on `printState` selecting ring vs the idle temp readout (D-08) — `grep -nE 'Printing|Paused|aspectRatio' …` matches; no bare 0% ring on idle
    - a reserved sparkline slot is present in the Field (review #4) and `grep -n 'GraphViewHost' …PrintStatusScreen.kt` returns NOTHING (sparkline deferred to 04-06b)
    - `grep -n 'ConfirmGuard' …PrintStatusScreen.kt` AND `grep -n 'EMERGENCY_STOP\|emergency_stop' …` match (Stop→Confirm→estop, SHELL-02/D-10)
    - Stop dispatch goes through `container.dispatcher`/`sessionControl` (PRIM-05/review #1), not a raw rpc.request (`grep -n 'rpc.request' …PrintStatusScreen.kt` returns nothing)
    - greyed Tune + Pause placeholders present (D-07)
    - no second sampling/throttle layer (`grep -nE 'sample\(|debounce\(|delay\(' …` returns nothing)
    - Token-purity: no raw Color literals (`grep -nE '0x[0-9A-Fa-f]{8}|Color\(' …` returns nothing)
    - `:app:compileDebugKotlin` succeeds
  </acceptance_criteria>
  <done>PrintStatusScreen composes a state-adaptive Focus (ring printing / temp readout idle), a 2×3 GeistMono stat grid, a RESERVED sparkline slot (filled in 04-06b, review #4), and a Stop tile that routes through ConfirmGuard to dispatch printer.emergency_stop via the dispatcher/SessionControl (review #1) — all token-themed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Stop tap → ConfirmGuard → Moonraker | a destructive firmware panic-halt crosses to the printer, gated by the confirm guard + dispatcher on a trusted LAN |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06-E | Elevation/destructive | Stop → emergency_stop | mitigate | the destructive E-stop is gated behind the full-screen ConfirmGuard (PRIM-03) AND debounced/in-flight-guarded by CommandDispatcher (PRIM-05) — no accidental single-tap halt, no double-fire |
| T-04-06-A | Availability (no remote auth) | emergency_stop on trusted LAN | accept | only the optional Moonraker key gates the command; this is a sideloaded local-LAN printer console, not internet-facing — physical/LAN access is the trust boundary, proportionate to a printer touchscreen |
| T-04-06-DoS | Denial of Service (render) | live ring + grid surface | mitigate | consumes the store's existing ~4Hz conflation (no second throttle); the COMBINED ring+sparkline+grid perf gate is enforced on-device in 04-06b |
</threat_model>

<verification>
- `:app:testDebugUnitTest --tests …PrintStatusHolderTest` green (bounded ring + all capability-fallback cases, review #9)
- `:app:compileDebugKotlin` green
- greps confirm ring + reserved sparkline slot + ConfirmGuard + estop-via-dispatcher + state-adaptive Focus + token purity
- (Combined-render perf gate + GraphView sparkline are in 04-06b, review #4)
</verification>

<success_criteria>
- Print Status is the single state-adaptive home surface (ring printing / temp readout idle) with a 2×3 grid + reserved sparkline slot (SHELL-04/D-08; sparkline in 04-06b)
- The grid degrades deterministically for non-standard heater configs (review #9)
- Stop routes through ConfirmGuard and fires printer.emergency_stop via the dispatcher/SessionControl (SHELL-02/D-10/review #1)
</success_criteria>

<output>
Create `.planning/phases/04-service-shell-settings-print-status-home/04-06-SUMMARY.md` when done
</output>
