---
phase: 04-service-shell-settings-print-status-home
plan: 06
type: execute
wave: 3
depends_on: [04-02, 04-03]
files_modified:
  - app/src/main/java/works/mees/dinghy/printstatus/PrintStatusScreen.kt
  - app/src/main/java/works/mees/dinghy/printstatus/PrintStatusHolder.kt
  - app/src/main/java/works/mees/dinghy/printstatus/StatGrid.kt
  - app/src/test/java/works/mees/dinghy/printstatus/PrintStatusHolderTest.kt
autonomous: true
requirements: [SHELL-04, SHELL-02]

must_haves:
  truths:
    - "Print Status home renders live job/thermal state at the store's throttled cadence"
    - "Focus adapts: printing → ProgressRing, idle → temp/status readout"
    - "Stop routes through ConfirmGuard and fires emergency_stop"
    - "The stat grid degrades deterministically for printers lacking expected heaters/fields"
  artifacts:
    - path: app/src/main/java/works/mees/dinghy/printstatus/PrintStatusScreen.kt
      provides: "State-adaptive Print Status home surface (ring/readout + grid + Stop)"
    - path: app/src/main/java/works/mees/dinghy/printstatus/PrintStatusHolder.kt
      provides: "Holds derived display state from PrinterState with explicit capability fallback"
  key_links:
    - from: PrintStatusScreen
      to: ConfirmGuard
      via: "Stop routes through confirm"
      pattern: "ConfirmGuard"

---

<objective>
The Print Status home (part 1 of 2) — the monitor/landing surface. State-adaptive Focus
(printing → ProgressRing, idle → temp readout), a 2×3 numeric StatGrid, and a Stop control
routing through ConfirmGuard → emergency_stop (D-07, D-08, D-10, SHELL-04/SHELL-02).

Per review concern #4, the original 04-06 was overloaded; the heater sparkline (GraphView) and
the on-device combined-render perf gate are split into **04-06b**. This plan delivers the
adaptive home, the grid, and the wired Stop — no Views graph yet.

Per review concern #9, capability gating is made strict: the holder defines explicit fallback
rules for printers without a bed, with nonstandard heater names, or with chamber heaters.

Purpose: SHELL-04 + SHELL-02 — the home surface and the wired Stop control.
Output: `PrintStatusScreen`, `PrintStatusHolder`, `StatGrid`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-service-shell-settings-print-status-home/04-CONTEXT.md
@.planning/phases/04-service-shell-settings-print-status-home/04-RESEARCH.md
@.planning/phases/04-service-shell-settings-print-status-home/04-PATTERNS.md

Source-of-truth files to read before implementing:
- app/src/main/java/works/mees/dinghy/state/PrinterState.kt (printState, heaters, progress)
- app/src/main/java/works/mees/dinghy/state/Capabilities.kt (deriveCapabilities output — heater names/extruder count)
- app/src/main/java/works/mees/dinghy/render/ProgressRing.kt (Focus printing state)
- app/src/main/java/works/mees/dinghy/render/RingBuffer.kt (sparkline data — populated here, drawn in 04-06b)
- app/src/main/java/works/mees/dinghy/designsystem/ConfirmGuard.kt (Stop guard)
- app/src/main/java/works/mees/dinghy/di/SessionControl.kt (Stop fires emergency_stop via the narrow contract / dispatcher, 04-03)
- app/src/main/java/works/mees/dinghy/designsystem/layout/ScreenScaffold.kt (Focus/Field/Gutter)
</context>

<tasks>

<task type="auto">
  <name>Task 1: PrintStatusHolder (derived display state + strict capability fallback)</name>
  <files>
    app/src/main/java/works/mees/dinghy/printstatus/PrintStatusHolder.kt
    app/src/test/java/works/mees/dinghy/printstatus/PrintStatusHolderTest.kt
  </files>
  <read_first>
    app/src/main/java/works/mees/dinghy/state/PrinterState.kt
    app/src/main/java/works/mees/dinghy/state/Capabilities.kt
    app/src/main/java/works/mees/dinghy/render/RingBuffer.kt
  </read_first>
  <action>
    Create `PrintStatusHolder` deriving display state from PrinterState + Capabilities: progress
    %, active/target temps per heater, and a 2×3 stat selection. Define EXPLICIT capability
    fallback rules (concern #9): (1) primary heater = the `extruder` heater if present, else the
    first heater whose name starts with `extruder`; (2) secondary = `heater_bed` if present, else
    OMIT the bed slot and promote the next available heater (e.g. a chamber heater
    `heater_generic chamber`) into the grid; (3) printers with no bed render the grid WITHOUT a
    bed slot rather than showing 0°/—; (4) nonstandard heater names are shown by their Moonraker
    object name; (5) when fewer than 6 stats are available, remaining grid cells render an empty/
    "—" placeholder rather than fabricating data. Also push the primary-heater temperature into
    the heater `RingBuffer` (consumed by the 04-06b sparkline). Pure/host-testable (no Android).
  </action>
  <verify>
    <automated>cd /mnt/e/claude/personal/github/dinghy-display && /mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --tests *PrintStatusHolderTest* --no-daemon" 2>&1 | tr -d '\r'</automated>
  </verify>
  <acceptance_criteria>
    - Standard printer (extruder + heater_bed) test: grid includes a nozzle slot and a bed slot with correct active/target temps.
    - No-bed printer test: grid renders WITHOUT a bed slot (no fabricated 0°/— bed cell) and promotes the next available heater.
    - Chamber-heater test: a `heater_generic chamber` is surfaced by its object name when no bed exists.
    - Nonstandard extruder-name test: primary heater resolves via the `extruder`-prefix rule when no exact `extruder` key exists.
    - Under-6-stats test: empty cells render a placeholder, never fabricated values.
    - Holder pushes the primary-heater temperature into the RingBuffer (asserted on a buffer snapshot).
    - File contains no `import android.`; `:app:testDebugUnitTest --tests *PrintStatusHolderTest*` exits 0.
  </acceptance_criteria>
  <done>Holder derives progress/temps/stats with explicit capability fallback and feeds the sparkline buffer; tests green.</done>
</task>

<task type="auto">
  <name>Task 2: PrintStatusScreen + StatGrid + Stop wiring (no graph yet)</name>
  <files>
    app/src/main/java/works/mees/dinghy/printstatus/PrintStatusScreen.kt
    app/src/main/java/works/mees/dinghy/printstatus/StatGrid.kt
  </files>
  <read_first>
    app/src/main/java/works/mees/dinghy/render/ProgressRing.kt
    app/src/main/java/works/mees/dinghy/designsystem/ConfirmGuard.kt
    app/src/main/java/works/mees/dinghy/di/SessionControl.kt
    app/src/main/java/works/mees/dinghy/designsystem/layout/ScreenScaffold.kt
  </read_first>
  <action>
    Create `PrintStatusScreen` using ScreenScaffold (Focus/Field/Gutter). Focus is state-adaptive
    (D-08): printing → ProgressRing; idle → temp/status readout. Field = 2×3 `StatGrid` (a
    sparkline placeholder slot is left in the Field layout for 04-06b to fill). Gutter = Stop
    (wired) + greyed Tune/Pause (D-07). Stop → ConfirmGuard (terse copy, D-10: "Emergency stop?"
    / "This halts the printer.") → `printer.emergency_stop`. The Stop call goes through
    SessionControl/CommandDispatcher (04-03) — NOT a raw session. Render at the throttled cadence
    from the store (do NOT add a second sampling layer).
  </action>
  <verify>
    <automated>cd /mnt/e/claude/personal/github/dinghy-display && /mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:assembleDebug --no-daemon" 2>&1 | tr -d '\r'</automated>
  </verify>
  <acceptance_criteria>
    - Focus renders ProgressRing when printState=Printing and the temp/status readout when idle (state-adaptive branch present).
    - Gutter shows Stop active + Tune/Pause greyed/disabled (D-07).
    - Stop opens ConfirmGuard with the terse copy and, on confirm, dispatches `printer.emergency_stop` via the dispatcher/SessionControl (grep: `emergency_stop`; no raw `MoonrakerSession` reference).
    - The Field reserves a sparkline placeholder slot (so 04-06b is an additive fill, not a relayout).
    - No second sampling/throttle layer is introduced (grep: no new sampling timer; consumes the store flow directly).
    - `:app:assembleDebug` exits 0.
  </acceptance_criteria>
  <done>Print Status renders adaptive Focus + grid + Stop→Confirm→emergency_stop; sparkline slot reserved for 04-06b.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Stop tap → emergency_stop | high-impact firmware halt crosses to network via SessionControl/dispatcher |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06-01 | DoS (accidental) | Stop button | mitigate | full-screen ConfirmGuard prevents accidental E-stop |
| T-04-06-02 | DoS (hang) | emergency_stop dispatch | mitigate | CommandDispatcher timeout + debounce |

</threat_model>

<verification>
- PrintStatusHolder unit tests green (all capability-fallback cases)
- assembleDebug compiles
- (Combined-render perf gate moved to 04-06b, where the sparkline lands)

</verification>

<success_criteria>
- Print Status home renders adaptive Focus + grid with strict capability fallback
- Stop routes through ConfirmGuard → emergency_stop via SessionControl/dispatcher
- Sparkline placeholder slot reserved for 04-06b
</success_criteria>

<output>
Create `.planning/phases/04-service-shell-settings-print-status-home/04-06-SUMMARY.md` when done
</output>
