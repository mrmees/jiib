---
phase: 06-command-reference-capability-matrix
plan: 04
type: execute
wave: 4
depends_on:
  - 06-03
files_modified:
  - docs/commands/klipper-gcode.md
  - docs/commands/moonraker-api.md
  - docs/commands/spoolman-api.md
  - docs/commands/printer-availability-matrix.md
  - docs/commands/catalog.json
  - docs/commands/printer-matrix.json
  - docs/moonraker-capabilities.md
  - app/src/test/java/works/mees/dinghy/command/CommandCatalogDriftTest.kt
autonomous: false
requirements:
  - PHASE-06-REFERENCE
must_haves:
  truths:
    - "D-01/D-02: catalog covers Klipper G-Code, full Moonraker API, and full Spoolman API with tiered depth"
    - "D-03: every catalog row has a stable greppable catalog ID and upstream URL"
    - "D-07/D-08: E5/E3 availability matrix documents live evidence and runtime predicate per command"
  artifacts:
    - path: "docs/commands/klipper-gcode.md"
      provides: "Human-facing Klipper G-Code catalog"
    - path: "docs/commands/moonraker-api.md"
      provides: "Human-facing Moonraker JSON-RPC/REST catalog"
    - path: "docs/commands/spoolman-api.md"
      provides: "Human-facing Spoolman API catalog"
    - path: "docs/commands/printer-availability-matrix.md"
      provides: "Human-facing E5/E3 command availability matrix"
    - path: "docs/commands/catalog.json"
      provides: "Machine-readable command catalog"
    - path: "docs/commands/printer-matrix.json"
      provides: "Machine-readable printer evidence and predicates"
  key_links:
    - from: "docs/commands/catalog.json"
      to: "docs/commands/*.md"
      via: "stable catalog IDs"
      pattern: "KGC-|MR-|SPM-"
    - from: "docs/commands/printer-matrix.json"
      to: "docs/moonraker-capabilities.md"
      via: "capture metadata and reconciled object/component evidence"
      pattern: "ender5plus|ender3"
---

<objective>
Complete the human-facing command catalog and per-printer availability matrix, backed by machine-readable sidecars and refreshed E5/E3 evidence.

Purpose: Later planning and implementation phases need doc-grounded command semantics and a dev-facing matrix that maps those commands to the two known printers while runtime gates stay live for any user printer.
Output: Markdown catalogs, catalog.json, printer-matrix.json, and reconciled moonraker capability notes with drift tests green.
</objective>

<execution_context>
@/mnt/c/Users/matth/.codex/get-shit-done/workflows/execute-plan.md
@/mnt/c/Users/matth/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-command-reference-capability-matrix/06-CONTEXT.md
@.planning/phases/06-command-reference-capability-matrix/06-RESEARCH.md
@.planning/phases/06-command-reference-capability-matrix/06-VALIDATION.md
@.planning/phases/06-command-reference-capability-matrix/06-PATTERNS.md
@docs/moonraker-capabilities.md
@docs/commands/catalog.json
@docs/commands/printer-matrix.json
@app/src/main/java/works/mees/dinghy/command/CommandRegistry.kt
@app/src/test/java/works/mees/dinghy/command/CommandCatalogDriftTest.kt
</context>

<tasks>

<task type="checkpoint" gate="blocking">
  <name>Task 1: Refresh live E5/E3 command availability evidence</name>
  <checkpoint_type>human-verify</checkpoint_type>
  <files>docs/moonraker-capabilities.md, docs/commands/printer-matrix.json, docs/commands/printer-availability-matrix.md</files>
  <read_first>
    @docs/moonraker-capabilities.md
    @docs/commands/printer-matrix.json
    @.planning/phases/06-command-reference-capability-matrix/06-VALIDATION.md
    @.planning/phases/06-command-reference-capability-matrix/06-RESEARCH.md
  </read_first>
  <what-built>The executor must attempt a live refresh before filling strict matrix predicates. Use the known hosts from docs: Ender 5 Plus `192.168.1.120:7125` and Ender 3 `192.168.1.121:7125` unless the user supplies replacements.</what-built>
  <action>Before Task 2, obtain or confirm live E5/E3 introspection evidence for the matrix, following the `docs/moonraker-capabilities.md` capture/provenance analog from 06-PATTERNS.md. The executor should run the listed read-only curl probes when the printers are reachable; if a host is unreachable or an endpoint is unsupported, pause for human confirmation before representing it as `unknown` or `unsupported`. Do not proceed by treating a missing `printer.gcode.help` response as proof a command is absent.</action>
  <how-to-verify>
    1. For each printer, run read-only probes from WSL: `curl -s "http://$H/server/info"`, `curl -s "http://$H/printer/info"`, `curl -s "http://$H/printer/objects/list"`, `curl -s "http://$H/printer/objects/query?gcode"`, and `curl -s "http://$H/printer/gcode/help"` where available.
    2. Update `docs/moonraker-capabilities.md` capture notes and `docs/commands/printer-matrix.json` with object, macro, component, and gcode-help evidence. If a probe endpoint is unsupported, record `unsupported` with response code/body summary; do not infer absence from `printer.gcode.help` alone.
    3. Continue only when both printers are captured, or when the user explicitly approves an `unknown`/`unreachable` matrix entry for a printer that is powered off or unavailable.
  </how-to-verify>
  <acceptance_criteria>
    - `printer-matrix.json` records `captured_at`, `klipper_version`, `moonraker_version`, `api_version`, `components`, `objects`, `macros`, and `not_on_printers` for each printer.
    - Unsupported or unreachable probes are represented explicitly as `unknown` or `unsupported`, not as false absence.
    - Runtime gating remains live; no E5/E3 static data is added to app runtime resources.
  </acceptance_criteria>
  <verify>
    <automated>python3 -m json.tool docs/commands/printer-matrix.json >/dev/null || true</automated>
  </verify>
  <done>Live evidence is refreshed or an explicit human-approved capture caveat is recorded before strict predicate reconciliation proceeds.</done>
  <resume-signal>Type "approved" after the live refresh evidence is captured, or provide the replacement hostnames / explicit unreachable approval.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fill comprehensive catalog sidecar and Markdown docs</name>
  <files>docs/commands/catalog.json, docs/commands/klipper-gcode.md, docs/commands/moonraker-api.md, docs/commands/spoolman-api.md</files>
  <read_first>
    @docs/commands/catalog.json
    @.planning/phases/06-command-reference-capability-matrix/06-CONTEXT.md
    @.planning/phases/06-command-reference-capability-matrix/06-RESEARCH.md
    @.planning/ROADMAP.md
  </read_first>
  <behavior>
    - Catalog is comprehensive across the three named APIs per D-01, with tiered full/light detail per D-02.
    - Markdown and JSON share stable IDs per D-03.
    - Clearly unsafe/admin/deprecated Moonraker operations are still cataloged as light/reference-only where official, and not registered for runtime sending unless Dinghy v1 sends them.
  </behavior>
  <action>Populate `catalog.json` and the three Markdown docs from the authoritative sources listed in 06-RESEARCH. For Markdown docs, follow the human-facing reference structure analog in `docs/moonraker-capabilities.md`; for `catalog.json`, follow the machine-readable sidecar and `GoldenFixtures.kt` parse-fail-fast analogs from 06-PATTERNS.md. For Klipper, catalog sendable G-Code commands from `https://www.klipper3d.org/G-Codes.html` and use `https://www.klipper3d.org/Status_Reference.html` only for status-object predicates, not config-section option catalogs. For Moonraker, catalog official API operations from the current split `external_api/*` docs, including JSON-RPC method names and REST endpoints where both exist; mark admin/system/update/deprecated operations as `reference_only` light entries when Dinghy will not send them in v1. For Spoolman, catalog `/api/v1/` REST surface and QR payload semantics from the official ReDoc/repo sources. Full-detail all current and v1-roadmap operations named in 06-RESEARCH; lighter entries still need purpose, key params, and upstream URL. Do not add runtime registry entries for operations Dinghy will not send in v1.</action>
  <verify>
    <automated>python3 -m json.tool docs/commands/catalog.json >/dev/null && test -f docs/commands/klipper-gcode.md && test -f docs/commands/moonraker-api.md && test -f docs/commands/spoolman-api.md && /mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testReleaseUnitTest --tests *CommandCatalogDriftTest --no-daemon" | tr -d '\r'</automated>
  </verify>
  <acceptance_criteria>
    - `catalog.json` contains entries whose IDs start with `KGC-`, `MR-`, and `SPM-`.
    - Every entry includes `id`, `source_api`, `name`, `purpose`, `params`, `semantics_tier`, `upstream_url`, `availability`, and `runtime_registry`.
    - Markdown docs include the same stable IDs as their corresponding JSON entries.
    - Drift test passes without parsing Markdown.
  </acceptance_criteria>
  <done>Command catalog is comprehensive, tiered, source-cited, and machine-enforced.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Complete per-printer matrix and predicate reconciliation</name>
  <files>docs/commands/printer-matrix.json, docs/commands/printer-availability-matrix.md, docs/moonraker-capabilities.md, app/src/test/java/works/mees/dinghy/command/CommandCatalogDriftTest.kt</files>
  <read_first>
    @docs/commands/catalog.json
    @docs/commands/printer-matrix.json
    @docs/commands/printer-availability-matrix.md
    @docs/moonraker-capabilities.md
    @app/src/test/java/works/mees/dinghy/command/CommandCatalogDriftTest.kt
  </read_first>
  <behavior>
    - Matrix maps command predicates to E5/E3 evidence, not to hardcoded runtime behavior.
    - `ObjectPresent`, `MacroPresent`, and `ComponentPresent` predicates are backed by live evidence or explicit `not_on_printers` / `unknown` entries.
    - The matrix distinguishes present, absent, not-on-our-printers, unknown, runtime predicate, and confirmation/status source.
  </behavior>
  <action>Reconcile `catalog.json` availability predicates against refreshed E5/E3 evidence. Fill `printer-matrix.json` with per-printer objects, macros, components, gcode-help positives where available, and explicit `not_on_printers` records for cataloged commands neither test rig exposes, following the JSON fixture sidecar analog from 06-PATTERNS.md. Write `printer-availability-matrix.md` as the human-facing table with stable catalog ID, command, runtime predicate, E5 status, E3 status, confirmation/status source, and caveats, following the `docs/moonraker-capabilities.md` degrade/caveat pattern. Keep the matrix dev-facing and cite D-07 in notes: runtime gating evaluates live `Capabilities`, not the committed E5/E3 matrix. Update drift tests if the JSON schema evolved, but do not weaken requirements that registry predicates have evidence or an explicit escape hatch.</action>
  <verify>
    <automated>python3 -m json.tool docs/commands/printer-matrix.json >/dev/null && /mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testReleaseUnitTest --tests *CommandCatalogDriftTest --no-daemon" | tr -d '\r'</automated>
  </verify>
  <acceptance_criteria>
    - `printer-availability-matrix.md` contains sections for Ender 5 Plus and Ender 3.
    - `printer-matrix.json` contains no unresolved predicate for registry commands unless explicitly `unknown` with user-approved capture caveat.
    - Drift test passes and checks JSON sidecars only.
  </acceptance_criteria>
  <done>Per-printer availability matrix is complete enough for future phases to plan capability gates and for tests to enforce registry predicate drift.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| upstream docs -> local catalog | External docs are transformed into committed reference artifacts. |
| live printers -> matrix JSON | Network probe responses become predicate evidence. |
| matrix JSON -> future phases | Dev-facing evidence informs later capability-gated UI plans. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04-T | Tampering | catalog/matrix sidecars | mitigate | JSON parse tests plus drift tests; Markdown is not the enforcement source. |
| T-06-04-R | Repudiation | live capture evidence | mitigate | Record capture timestamps, versions, hosts, endpoint support, and caveats in JSON and docs. |
| T-06-04-I | Information Disclosure | live capture commands | mitigate | Capture read-only metadata only; do not store API keys, tokens, local usernames beyond existing docs. |
| T-06-04-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs are planned. |
</threat_model>

<verification>
After checkpoint approval, run:
`python3 -m json.tool docs/commands/catalog.json >/dev/null`
`python3 -m json.tool docs/commands/printer-matrix.json >/dev/null`
`/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testReleaseUnitTest --tests *CommandCatalogDriftTest --no-daemon" | tr -d '\r'`
</verification>

<success_criteria>
- Catalog and matrix artifacts exist in both Markdown and JSON.
- Full catalog breadth from D-01 is represented, with tiered depth from D-02.
- E5/E3 matrix is reconciled with live captures and does not become runtime shipped static gating data.
</success_criteria>

<output>
Create `.planning/phases/06-command-reference-capability-matrix/06-04-SUMMARY.md` when done.
</output>
