---
phase: 06-command-reference-capability-matrix
plan: 02
type: execute
wave: 1
depends_on:
  - 06-01
files_modified:
  - app/src/main/java/works/mees/dinghy/command/CommandSpec.kt
  - app/src/main/java/works/mees/dinghy/command/CommandRegistry.kt
  - app/src/main/java/works/mees/dinghy/command/CommandDispatchExtensions.kt
  - app/src/main/java/works/mees/dinghy/state/Capabilities.kt
  - app/src/main/java/works/mees/dinghy/state/DeriveCapabilities.kt
  - app/src/test/java/works/mees/dinghy/command/CommandCatalogDriftTest.kt
  - app/src/test/java/works/mees/dinghy/command/CommandRegistryGcodeTest.kt
  - app/src/test/java/works/mees/dinghy/state/DeriveCapabilitiesTest.kt
  - app/src/test/java/works/mees/dinghy/command/CommandDispatcherTest.kt
autonomous: true
requirements:
  - PHASE-06-REFERENCE
must_haves:
  truths:
    - "D-04: a canonical in-code command registry exists for Dinghy-sent commands"
    - "D-06: registry gcode entries wrap PrinterCommands builders verbatim"
    - "D-08/D-09: availability predicates can be evaluated against live Capabilities"
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/command/CommandSpec.kt"
      provides: "CommandTransport, AvailabilityPredicate, CommandSemantics, CommandSpec model"
    - path: "app/src/main/java/works/mees/dinghy/command/CommandRegistry.kt"
      provides: "Registry entries for current Phase 1-5 outbound operations"
    - path: "app/src/main/java/works/mees/dinghy/command/CommandDispatchExtensions.kt"
      provides: "Thin dispatcher/request helpers that preserve existing semantics"
    - path: "app/src/main/java/works/mees/dinghy/state/Capabilities.kt"
      provides: "Raw object-name retention and hasObject()"
  key_links:
    - from: "CommandRegistry"
      to: "PrinterCommands"
      via: "builder function references"
      pattern: "PrinterCommands\\."
    - from: "CommandDispatchExtensions"
      to: "CommandDispatcher.dispatch"
      via: "delegate call"
      pattern: "dispatch\\(.*method"
---

<objective>
Implement the headless registry foundation and live capability predicate surface.

Purpose: Later phases need one canonical command definition per Dinghy-sent operation and a generic live predicate model without shipping static E5/E3 data.
Output: Plain Kotlin command spec/registry/dispatch helpers plus `Capabilities.objects` and `hasObject()` with Wave 0 tests green for registry, gcode wrapping, and capability behavior.
</objective>

<execution_context>
@/mnt/c/Users/matth/.codex/get-shit-done/workflows/execute-plan.md
@/mnt/c/Users/matth/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-command-reference-capability-matrix/06-CONTEXT.md
@.planning/phases/06-command-reference-capability-matrix/06-RESEARCH.md
@.planning/phases/06-command-reference-capability-matrix/06-PATTERNS.md
@docs/commands/catalog.json
@docs/commands/printer-matrix.json
@app/src/main/java/works/mees/dinghy/command/PrinterCommands.kt
@app/src/main/java/works/mees/dinghy/command/CommandDispatcher.kt
@app/src/main/java/works/mees/dinghy/net/JsonRpc.kt
@app/src/main/java/works/mees/dinghy/net/JsonRpcClient.kt
@app/src/main/java/works/mees/dinghy/state/Capabilities.kt
@app/src/main/java/works/mees/dinghy/state/DeriveCapabilities.kt
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define command spec types and registry entries</name>
  <files>app/src/main/java/works/mees/dinghy/command/CommandSpec.kt, app/src/main/java/works/mees/dinghy/command/CommandRegistry.kt, app/src/test/java/works/mees/dinghy/command/CommandCatalogDriftTest.kt, app/src/test/java/works/mees/dinghy/command/CommandRegistryGcodeTest.kt</files>
  <read_first>
    @app/src/main/java/works/mees/dinghy/command/PrinterCommands.kt
    @app/src/main/java/works/mees/dinghy/net/JsonRpc.kt
    @app/src/test/java/works/mees/dinghy/command/CommandCatalogDriftTest.kt
    @app/src/test/java/works/mees/dinghy/command/CommandRegistryGcodeTest.kt
    @.planning/phases/06-command-reference-capability-matrix/06-CONTEXT.md
    @.planning/phases/06-command-reference-capability-matrix/06-RESEARCH.md
  </read_first>
  <behavior>
    - `CommandSpec.kt` is plain Kotlin with no Android, Compose, or service dependency.
    - `CommandRegistry.all` contains current outbound Phase 1-5 operations and uses stable catalog IDs from catalog.json per D-03/D-04.
    - Gcode entries produce params by calling `PrinterCommands.*` and `PrinterCommands.scriptParams()` verbatim per D-06.
  </behavior>
  <action>Create `CommandTransport`, `AvailabilityPredicate`, `CommandSemantics`, and generic `CommandSpec<P>` in the `works.mees.dinghy.command` package. Include predicate variants `Always`, `ObjectPresent`, `MacroPresent`, `ComponentPresent`, `GcodeCommandPresent`, `AnyOf`, and `NotOnOurPrinters`. Create `CommandRegistry` with typed argument data classes only where needed: `SetHeaterArgs`, `PresetArgs`, `JogArgs`, `ForceMoveArgs`, `HomeAxisArgs`, `ExtrudeArgs`, `SelectToolArgs`, `MetadataArgs`, `HistoryListArgs`, and `ObjectSubsetArgs`; use `Unit` for no-param commands. Register current outbound operations: identify, oneshot token, objects.list/query/subscribe, temperature_store, files.metadata, history.list, emergency_stop, firmware_restart, restart, and all current PrinterCommands gcode actions. Do not register inbound notify names as commands; keep them in `JsonRpcMethods` because the registry models outbound operations only.</action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testReleaseUnitTest --tests *CommandCatalogDriftTest --tests *CommandRegistryGcodeTest --no-daemon" | tr -d '\r'</automated>
  </verify>
  <acceptance_criteria>
    - `CommandRegistry.all.map { it.catalogId }` has no duplicates.
    - Tests prove every registry `catalogId` exists in `docs/commands/catalog.json`.
    - Tests prove registry gcode params match `PrinterCommands.*` output byte-for-byte for representative and clamp-boundary inputs.
    - `grep -R "data class CommandSpec" app/src/main/java/works/mees/dinghy/command/CommandSpec.kt` returns one match.
  </acceptance_criteria>
  <done>Registry model and entries exist, link to catalog IDs, and pass drift/gcode Wave 0 tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add thin registry dispatch and request helpers</name>
  <files>app/src/main/java/works/mees/dinghy/command/CommandDispatchExtensions.kt, app/src/main/java/works/mees/dinghy/command/CommandDispatcher.kt, app/src/main/java/works/mees/dinghy/net/JsonRpcClient.kt, app/src/test/java/works/mees/dinghy/command/CommandDispatcherTest.kt</files>
  <read_first>
    @app/src/main/java/works/mees/dinghy/command/CommandDispatcher.kt
    @app/src/main/java/works/mees/dinghy/net/JsonRpcClient.kt
    @app/src/main/java/works/mees/dinghy/command/CommandSpec.kt
    @app/src/main/java/works/mees/dinghy/command/CommandRegistry.kt
    @app/src/test/java/works/mees/dinghy/command/CommandDispatcherTest.kt
    @.planning/phases/06-command-reference-capability-matrix/06-PATTERNS.md
  </read_first>
  <behavior>
    - Registry dispatch delegates to the existing `CommandDispatcher.dispatch(key, method, params)` path, preserving debounce, in-flight, RpcError, redaction, and timeout behavior.
    - `printer.gcode.script` still takes `CommandDispatcher.GCODE_TIMEOUT_MS == 120_000L`.
    - `printer.emergency_stop` remains an immediate JSON-RPC command, not queued `M112` through gcode.script.
  </behavior>
  <action>Add extension helpers only; do not rewrite `CommandDispatcher` internals. Provide `fun <P> CommandDispatcher.dispatch(command: CommandSpec<P>, args: P)` and `suspend fun <P> JsonRpcClient.request(command: CommandSpec<P>, args: P, timeoutMs: Long = JsonRpcClient.DEFAULT_REQUEST_TIMEOUT_MS)` or the closest existing timeout constant name. The dispatcher overload must compute key/method/params from the spec and immediately delegate to `dispatch(key, method, params)`. The request helper must reject non-JSON-RPC specs with a clear `require` message so REST/Spoolman catalog entries cannot be accidentally sent through JsonRpcClient. Preserve D-12: gcode timeout, e-stop immediate path, RpcError handling, and secret redaction.</action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testReleaseUnitTest --tests *CommandDispatcherTest --no-daemon" | tr -d '\r'</automated>
  </verify>
  <acceptance_criteria>
    - Existing dispatcher tests still pass.
    - New registry-overload tests prove gcode registry dispatch records `CommandDispatcher.GCODE_TIMEOUT_MS` and emergency stop records `CommandDispatcher.DEFAULT_TIMEOUT_MS`.
    - `grep -R "M112" app/src/main/java/works/mees/dinghy | grep -v '^#'` returns no production e-stop replacement.
  </acceptance_criteria>
  <done>Registry dispatch/request helpers exist without changing the proven dispatcher semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend live capabilities with raw objects and predicates</name>
  <files>app/src/main/java/works/mees/dinghy/state/Capabilities.kt, app/src/main/java/works/mees/dinghy/state/DeriveCapabilities.kt, app/src/test/java/works/mees/dinghy/state/DeriveCapabilitiesTest.kt, app/src/test/java/works/mees/dinghy/command/CommandCatalogDriftTest.kt</files>
  <read_first>
    @app/src/main/java/works/mees/dinghy/state/Capabilities.kt
    @app/src/main/java/works/mees/dinghy/state/DeriveCapabilities.kt
    @app/src/test/java/works/mees/dinghy/state/DeriveCapabilitiesTest.kt
    @app/src/test/java/works/mees/dinghy/command/CommandCatalogDriftTest.kt
    @docs/commands/printer-matrix.json
    @.planning/phases/06-command-reference-capability-matrix/06-CONTEXT.md
  </read_first>
  <behavior>
    - `Capabilities.objects` preserves the raw `printer.objects.list` set, including objects that do not yet have typed fields.
    - `Capabilities.hasObject(name)` is an exact live-gating helper alongside the existing case-insensitive macro helper.
    - Existing typed fields still derive as before; runtime gating stays live and generic per D-07/D-09.
  </behavior>
  <action>Add `objects: Set<String> = emptySet()` to `Capabilities` and `fun hasObject(name: String): Boolean = name in objects`. Populate it in `deriveCapabilities(objects: List<String>)` using `objects.toSet()` while preserving `hasBed`, `extruderCount`, `fans`, `macros`, `powerDevices`, `heaters`, and `hasMacroIgnoreCase()` behavior. Add component predicates only if Plan 02 tests require runtime component evaluation now; otherwise keep components in matrix JSON for docs/test evidence and leave runtime component expansion to a later command that needs it. This honors D-07: static E5/E3 matrix is dev-facing, runtime gates evaluate live capabilities.</action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testReleaseUnitTest --tests *DeriveCapabilitiesTest --tests *CommandCatalogDriftTest --no-daemon" | tr -d '\r'</automated>
  </verify>
  <acceptance_criteria>
    - `DeriveCapabilitiesTest` proves raw object retention, exact `hasObject()`, and no regression in `hasMacroIgnoreCase()`.
    - Existing minimal/full printer capability tests still pass.
    - Predicate drift tests can evaluate `ObjectPresent` and `MacroPresent` references against matrix evidence or explicit not-on-printers entries.
  </acceptance_criteria>
  <done>Live capability predicates are queryable and Wave 0 capability/predicate tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI/service -> CommandRegistry | User actions and service one-shots resolve to command specs before crossing transport. |
| CommandRegistry -> PrinterCommands | Registry params become actual gcode via existing builders. |
| objects.list -> Capabilities | Untrusted printer object names become live-gating predicates. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02-T | Tampering | CommandRegistry gcode entries | mitigate | Wrap `PrinterCommands.*` verbatim and require byte-identical registry tests per D-06/D-11. |
| T-06-02-D | Denial of Service | CommandDispatcher overload | mitigate | Delegate to existing dispatcher and preserve 120s finite gcode timeout plus key removal tests. |
| T-06-02-E | Elevation of Privilege | JsonRpcClient request helper | mitigate | Restrict helper to JSON-RPC specs; REST/Spoolman entries are catalog-only until their phases implement appropriate clients. |
| T-06-02-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs are planned. |
</threat_model>

<verification>
Run targeted Wave 0 tests until green, then run:
`/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testReleaseUnitTest --no-daemon" | tr -d '\r'`
</verification>

<success_criteria>
- Registry foundation compiles.
- Command drift, byte-identical gcode, dispatcher, and capability tests pass.
- No gcode generation is reimplemented as generic templates.
</success_criteria>

<output>
Create `.planning/phases/06-command-reference-capability-matrix/06-02-SUMMARY.md` when done.
</output>
