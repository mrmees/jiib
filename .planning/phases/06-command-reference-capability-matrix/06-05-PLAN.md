---
phase: 06-command-reference-capability-matrix
plan: 05
type: execute
wave: 4
depends_on:
  - 06-04
files_modified:
  - .planning/phases/06-command-reference-capability-matrix/06-VERIFICATION.md
  - .planning/STATE.md
autonomous: false
requirements:
  - PHASE-06-REFERENCE
must_haves:
  truths:
    - "D-12: artifacts are committed, host CI gates are green, and flox regression is manually verified"
    - "Phase success criteria 1-4 are independently checkable from docs, registry, matrix, and tests"
    - "Later phases can register commands and capability-gate from the registry/predicate model"
  artifacts:
    - path: ".planning/phases/06-command-reference-capability-matrix/06-VERIFICATION.md"
      provides: "Phase 6 verification record and flox regression evidence"
    - path: ".planning/STATE.md"
      provides: "Updated current position after phase verification"
  key_links:
    - from: "06-VERIFICATION.md"
      to: "docs/commands/catalog.json"
      via: "verification checklist"
      pattern: "catalog.json"
    - from: "06-VERIFICATION.md"
      to: "CommandRegistry"
      via: "test evidence"
      pattern: "CommandRegistry"
---

<objective>
Run final automated verification and perform the required blocking flox regression checkpoint after the registry refactor.

Purpose: The call-site refactor touched live-proven printer send paths, so Phase 6 is not done until host tests and a focused real-device regression both pass.
Output: Verification record with automated command output summary and human-approved flox regression checklist.
</objective>

<execution_context>
@/mnt/c/Users/matth/.codex/get-shit-done/workflows/execute-plan.md
@/mnt/c/Users/matth/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-command-reference-capability-matrix/06-CONTEXT.md
@.planning/phases/06-command-reference-capability-matrix/06-RESEARCH.md
@.planning/phases/06-command-reference-capability-matrix/06-VALIDATION.md
@.planning/phases/06-command-reference-capability-matrix/06-01-SUMMARY.md
@.planning/phases/06-command-reference-capability-matrix/06-02-SUMMARY.md
@.planning/phases/06-command-reference-capability-matrix/06-03-SUMMARY.md
@.planning/phases/06-command-reference-capability-matrix/06-04-SUMMARY.md
@docs/commands/catalog.json
@docs/commands/printer-matrix.json
@docs/commands/printer-availability-matrix.md
@app/src/main/java/works/mees/dinghy/command/CommandRegistry.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run final automated verification and record evidence</name>
  <files>.planning/phases/06-command-reference-capability-matrix/06-VERIFICATION.md</files>
  <read_first>
    @.planning/phases/06-command-reference-capability-matrix/06-VALIDATION.md
    @docs/commands/catalog.json
    @docs/commands/printer-matrix.json
    @app/src/test/java/works/mees/dinghy/command/CommandCatalogDriftTest.kt
    @app/src/test/java/works/mees/dinghy/command/CommandRegistryGcodeTest.kt
    @app/src/test/java/works/mees/dinghy/command/CommandDispatcherTest.kt
    @app/src/test/java/works/mees/dinghy/net/HandshakeTest.kt
  </read_first>
  <action>Run JSON parse checks, targeted Phase 6 tests, full release unit suite, and release Kotlin compile. Create or update `06-VERIFICATION.md` with command names, pass/fail status, and concise output evidence. If any check fails, fix the underlying production/docs/test drift before proceeding; do not mark the manual checkpoint ready while host gates are red.</action>
  <verify>
    <automated>python3 -m json.tool docs/commands/catalog.json >/dev/null && python3 -m json.tool docs/commands/printer-matrix.json >/dev/null && /mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testReleaseUnitTest :app:compileReleaseKotlin --no-daemon" | tr -d '\r'</automated>
  </verify>
  <acceptance_criteria>
    - `catalog.json` and `printer-matrix.json` parse.
    - `:app:testReleaseUnitTest` passes.
    - `:app:compileReleaseKotlin` passes.
    - `06-VERIFICATION.md` records the exact Windows wrapper commands used and their pass status.
  </acceptance_criteria>
  <done>Automated gates are green and recorded before human flox regression.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Focused flox regression after registry refactor</name>
  <files>.planning/phases/06-command-reference-capability-matrix/06-VERIFICATION.md</files>
  <read_first>
    @.planning/phases/06-command-reference-capability-matrix/06-VERIFICATION.md
    @.planning/phases/06-command-reference-capability-matrix/06-CONTEXT.md
    @.planning/phases/06-command-reference-capability-matrix/06-VALIDATION.md
  </read_first>
  <what-built>The app has been refactored so existing send paths use `CommandRegistry`; this checkpoint verifies the unchanged behavior on flox plus live Ender 5 Plus per D-05/D-12.</what-built>
  <action>Perform the focused manual flox regression only after Task 1 automated gates are green. Record pass/fail notes in `06-VERIFICATION.md`; if any action fails, stop and describe the failed action instead of approving the checkpoint. This is a blocking checkpoint because D-05 touched live-proven send paths and D-12 requires real-device regression.</action>
  <how-to-verify>
    1. Build/install the current release APK using the repo's established Windows-side flow. If release signing is still debug-sign helper based, use `E:\Android\sign-release.bat` as described in `CLAUDE.md`.
    2. On flox connected to the Ender 5 Plus, verify Move: jog X/Y/Z, home all or axis, disable steppers through ConfirmGuard.
    3. Verify Temperature: set a heater target, apply a preset, and cooldown.
    4. Verify Extrude: extrude, retract, LOAD_FILAMENT/UNLOAD_FILAMENT when present, and missing-macro informational popup on a printer/profile where absent.
    5. Verify Print Status stop: Stop opens ConfirmGuard, confirm sends `printer.emergency_stop`, and klippy shutdown routes to Splash.
    6. If reachable after stop, verify recovery actions still dispatch firmware restart / host restart through the narrow `SessionControl`.
  </how-to-verify>
  <acceptance_criteria>
    - No action regresses relative to Phase 5 observed behavior.
    - Long gcode actions do not show the old false 10s "could not be sent" failure.
    - E-stop is immediate through `printer.emergency_stop`; no queued `M112` path is observed or present.
    - `06-VERIFICATION.md` is updated with manual pass/fail notes and any printer-specific caveats.
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/06-command-reference-capability-matrix/06-VERIFICATION.md && grep -v '^#' .planning/phases/06-command-reference-capability-matrix/06-VERIFICATION.md | grep -c "flox regression" || true</automated>
  </verify>
  <done>Human approval confirms the registry-refactored send paths behave correctly on flox plus live Ender 5 Plus, or the phase remains blocked with the failure recorded.</done>
  <resume-signal>Type "approved" if flox regression passes, or describe the exact failed action and observed behavior.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Finalize Phase 6 state after approval</name>
  <files>.planning/phases/06-command-reference-capability-matrix/06-VERIFICATION.md, .planning/STATE.md</files>
  <read_first>
    @.planning/phases/06-command-reference-capability-matrix/06-VERIFICATION.md
    @.planning/STATE.md
    @.planning/ROADMAP.md
  </read_first>
  <action>After the checkpoint resume signal is approved, update `06-VERIFICATION.md` final status and `STATE.md` current position to note Phase 6 verification completion and readiness for Phase 7. Do not mark the phase done if the human regression reports any failed live send path; instead record the failure and leave the status awaiting fix.</action>
  <verify>
    <automated>test -f .planning/phases/06-command-reference-capability-matrix/06-VERIFICATION.md && grep -v '^#' .planning/phases/06-command-reference-capability-matrix/06-VERIFICATION.md | grep -c "flox regression"</automated>
  </verify>
  <acceptance_criteria>
    - `06-VERIFICATION.md` includes automated gates and flox regression result.
    - `STATE.md` no longer says Phase 6 is not started once all gates pass.
    - The phase remains blocked if the checkpoint is not approved.
  </acceptance_criteria>
  <done>Phase 6 verification is recorded and state reflects the approved outcome.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host tests -> release readiness | Automated checks determine whether manual regression can start. |
| flox UI -> live Ender 5 Plus | Human touch actions become physical printer movement/heating/stopping. |
| verification notes -> workflow state | Manual evidence controls whether the phase can be considered complete. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05-S | Spoofing | manual regression evidence | mitigate | Require explicit user approval or exact failed behavior before final state update. |
| T-06-05-T | Tampering | verification record | mitigate | Record exact commands and pass/fail results in `06-VERIFICATION.md`; do not proceed on red automated gates. |
| T-06-05-Safety | Elevation of Privilege | live printer controls | mitigate | ConfirmGuard remains in high-impact paths; test e-stop, disable steppers, and recovery on real hardware. |
| T-06-05-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs are planned. |
</threat_model>

<verification>
Automated:
`python3 -m json.tool docs/commands/catalog.json >/dev/null`
`python3 -m json.tool docs/commands/printer-matrix.json >/dev/null`
`/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testReleaseUnitTest :app:compileReleaseKotlin --no-daemon" | tr -d '\r'`

Human:
Focused flox + live Ender 5 Plus regression checklist in Task 2.
</verification>

<success_criteria>
- All Phase 6 docs/registry/matrix artifacts exist.
- Full host suite and release Kotlin compile pass.
- Human approves flox regression after the registry call-site refactor.
- Verification/state files record the outcome.
</success_criteria>

<output>
Create `.planning/phases/06-command-reference-capability-matrix/06-05-SUMMARY.md` when done.
</output>
