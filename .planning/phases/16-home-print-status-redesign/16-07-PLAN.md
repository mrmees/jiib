---
phase: 16-home-print-status-redesign
plan: 07
type: execute
wave: 4
depends_on: ["16-06"]
files_modified:
  - docs/ui_design/README.md
  - docs/ui_design/LAYOUT.md
autonomous: false
requirements: [SC-1, "documentation merge direction (CONTEXT specifics + RESEARCH §6)"]
user_setup: []

must_haves:
  truths:
    - "docs/ui_design/README.md describes the four-state PrintStatusMode model (Standby/Printing/Paused/Terminal) and their Focus/Field/Gutter layouts, replacing the stale Print Status section"
    - "docs/ui_design/LAYOUT.md carries the interactive-grid flexible-tile rule as hard law, scoped to interactive grids ONLY, with the babystep horizontal-row C3 exception recorded"
    - "THEMING.md is NOT edited"
    - "The Print-Status artboard regeneration is surfaced as an EXPLICIT owner-side manual gate (checkpoint), not a casual follow-up"
  artifacts:
    - path: "docs/ui_design/README.md"
      provides: "Four-state Print Status section (merged from the staging note)"
      contains: "Terminal"
    - path: "docs/ui_design/LAYOUT.md"
      provides: "Interactive-grid flexible-tile rule (hard law) + babystep C3 exception"
      contains: "flexible-tile"
  key_links:
    - from: "README four-state section"
      to: "the implemented PrintStatusMode modes"
      via: "documents the as-built Standby/Printing/Paused/Terminal layouts"
      pattern: "Standby|Printing|Paused|Terminal"
---

<objective>
Execute the in-scope Documentation Merge Direction: fold the four-state model into the UI LAW so the docs
match the as-built home screen and the foundation is locked for Phases 17–20. This is a phase deliverable
with checker sign-off (UI-SPEC), not trailing cleanup.

Purpose: RESEARCH §6 + CONTEXT specifics — replace the stale README Print Status section with the
four-state model; promote the interactive-grid flexible-tile rule into LAYOUT.md as hard law (scoped to
interactive grids ONLY); record the babystep horizontal-row C3 exception. Do NOT touch THEMING.md.

Output: updated `docs/ui_design/README.md` + `docs/ui_design/LAYOUT.md`, PLUS an EXPLICIT owner-side
checkpoint for the Print-Status artboard image regeneration (the staging note + CONTEXT put the
Print-Status artboards in scope — they cannot be auto-generated, so this plan is `autonomous: false` and
surfaces the artboard work as a blocking manual gate rather than letting it disappear).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-home-print-status-redesign/16-UI-SPEC.md
@/mnt/e/claude/personal/github/parallel_dinghy/phase-16-home-status-redesign-staging.md
@docs/ui_design/README.md
@docs/ui_design/LAYOUT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: README four-state Print Status section</name>
  <files>docs/ui_design/README.md</files>
  <read_first>
    - docs/ui_design/README.md (locate the STALE Print Status section to replace)
    - /mnt/e/claude/personal/github/parallel_dinghy/phase-16-home-status-redesign-staging.md (the four-state model + per-state layouts — the source content)
    - 16-UI-SPEC.md (Focus/Field/Gutter-per-state table; Conflicts & Resolutions — accent-temperature supersedes the amber mockup)
    - 16-RESEARCH.md §6 (Documentation Merge Direction)
  </read_first>
  <action>
    Replace the stale Print Status section in `docs/ui_design/README.md` with the four-state
    `PrintStatusMode` model: the classifier (Moonraker-derived, `standby→Standby` even with stale file;
    klippy shutdown/error is NOT terminal) and each state's Focus/Field/Gutter layout (Standby launcher +
    glance; Printing cockpit + stats rows + shortcut/babystep row; Paused dimmed; Terminal hero + stats
    frame + ≤3 error lines). Note the accent-temperature resolution (nozzle/bed = `directional.temperature`
    = accent, the `03-print-status.png` amber is SUPERSEDED — Conflict 1). Reference the new babystep row,
    spool-aware Preheat, and the Dismiss/Reprint terminal gutter. Docs explain the layouts + merge
    direction (need not be strict declarative law). Keep the existing README structure/tone.
  </action>
  <verify>
    <automated>grep -niq "PrintStatusMode\|Terminal" docs/ui_design/README.md && grep -niq "Standby\|Paused\|Printing" docs/ui_design/README.md && echo OK-four-state</automated>
  </verify>
  <acceptance_criteria>
    - The stale Print Status section is gone; the four states (Standby/Printing/Paused/Terminal) and their Focus/Field/Gutter layouts are documented — read the section.
    - The accent-temperature resolution is noted (amber mockup superseded).
    - `grep` confirms the four-state terms present.
  </acceptance_criteria>
  <done>README Print Status section replaced with the as-built four-state model.</done>
</task>

<task type="auto">
  <name>Task 2: LAYOUT.md interactive-grid flexible-tile rule (hard law) + babystep C3 exception</name>
  <files>docs/ui_design/LAYOUT.md</files>
  <read_first>
    - docs/ui_design/LAYOUT.md (existing NON-NEGOTIABLEs + C1–C7 criteria — where to slot the new rule)
    - 16-UI-SPEC.md (Interactive-grid flexible-tile rule block + Conflict 2 babystep C3 exception)
    - /mnt/e/claude/personal/github/parallel_dinghy/phase-16-home-status-redesign-staging.md (§Universal Layout Rules — the flexible-tile rule + babystep exception)
  </read_first>
  <action>
    Add the interactive-grid flexible-tile rule to `docs/ui_design/LAYOUT.md` as HARD LAW, scoped EXACTLY
    to grids of user-interaction surfaces ONLY (never stat grids, text lists, graphs, or non-interactive
    info frames): when such a grid has awkward leftover space, ONE explicitly chosen tile may grow so the
    remaining controls stay regular, touch-friendly, predictable. Name the Phase-16 flexible tiles (Standby
    launcher → Drawer; active shortcut grid → Tune). Record the babystep horizontal-3-cell-row as a
    documented C3 exception (vertical Z quantity in a horizontal row is explicitly allowed here, staging-note
    authority) alongside the rule. Do NOT edit THEMING.md (no color/shape-status rule changes this phase).
  </action>
  <verify>
    <automated>grep -niq "flexible-tile\|flexible tile" docs/ui_design/LAYOUT.md && grep -niq "babystep" docs/ui_design/LAYOUT.md && git diff --quiet docs/ui_design/THEMING.md && echo OK-layout-no-theming</automated>
  </verify>
  <acceptance_criteria>
    - LAYOUT.md states the interactive-grid flexible-tile rule scoped to interactive grids only, names Drawer/Tune as the P16 flexible tiles — read the section.
    - The babystep horizontal-row C3 exception is recorded.
    - `git diff --quiet docs/ui_design/THEMING.md` succeeds (THEMING.md untouched).
  </acceptance_criteria>
  <done>LAYOUT.md gains the flexible-tile hard law + babystep C3 exception; THEMING.md untouched.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 3: Print-Status artboard regeneration (owner-side manual asset gate)</name>
  <what-built>
    The four-state Print-Status home (16-06) + the updated README/LAYOUT docs (Tasks 1-2). The hi-fi
    artboards in `docs/ui_design/images/` (`03-print-status.png` + the per-state Standby/Paused/Terminal
    artboards) are now STALE relative to the as-built four-state model and the accent-temperature
    resolution. Regenerating these is an owner-side design-asset task — Claude cannot render the hi-fi PNGs.
  </what-built>
  <action>
    This is a BLOCKING owner-side manual gate (NOT auto-editable). It exists so the in-scope artboard
    regeneration does not disappear as a casual follow-up. The owner regenerates/updates the Print-Status
    artboards (or explicitly defers them with a recorded decision) before the phase is marked complete.
  </action>
  <how-to-verify>
    1. Owner updates the Print-Status hi-fi artboards in `docs/ui_design/images/` to the four-state model:
       the Printing cockpit with accent (not amber) nozzle/bed temps, and new/updated Standby launcher,
       Paused dimmed, and Terminal hero + stats-frame artboards.
    2. OR the owner explicitly DEFERS artboard regeneration with a one-line recorded decision in
       `16-07-SUMMARY.md` (e.g. "artboards deferred to a later design pass — README/LAYOUT prose is the
       authority for now"). Either outcome is acceptable; silent omission is not.
  </how-to-verify>
  <acceptance_criteria>
    - The owner either regenerated the Print-Status artboards OR recorded an explicit deferral decision in 16-07-SUMMARY.md — the artboard status is not left ambiguous.
  </acceptance_criteria>
  <resume-signal>Type "artboards updated" or "artboards deferred" (with a one-line reason) once the artboard status is recorded.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Documentation-only changes — no runtime trust boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-07 | n/a | docs | accept | Markdown doc edits only; no executable surface, no V5 input path. |
| T-16-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase. |
</threat_model>

<verification>
- README documents the four-state model; LAYOUT carries the flexible-tile hard law + babystep exception; THEMING.md untouched; the artboard status is explicitly resolved (updated or recorded-deferred).
</verification>

<success_criteria>
The UI LAW matches the as-built four-state home and locks the flexible-tile rule for the downstream
feature phases; the Print-Status artboard regeneration is an explicit owner gate, not a lost follow-up.
</success_criteria>

<output>
Create `.planning/phases/16-home-print-status-redesign/16-07-SUMMARY.md` when done, recording the artboard
outcome (regenerated or explicitly deferred) from the Task 3 owner gate.
</output>
