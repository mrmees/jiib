---
phase: 16-home-print-status-redesign
plan: 03
type: execute
wave: 1
depends_on: ["16-01"]
files_modified:
  - app/src/main/java/works/mees/dinghy/command/PrinterCommands.kt
  - app/src/main/java/works/mees/dinghy/command/CommandRegistry.kt
  - app/src/test/java/works/mees/dinghy/command/PrinterCommandsTest.kt
  - docs/commands/catalog.json
  - docs/commands/printer-matrix.json
autonomous: true
requirements: [SC-5, D-05, "babystep command (SC-5)", "terminal-dismiss (D-05)"]
user_setup: []

must_haves:
  truths:
    - "setGcodeOffsetZAdjust emits SET_GCODE_OFFSET Z_ADJUST=<signed step> MOVE=1 with the step canonicalized against the fixed cycle set"
    - "SDCARD_RESET_FILE is the literal const gcode, no interpolation"
    - "The babystep + dismiss CommandSpecs are present in CommandRegistry.all (not dead wiring)"
    - "babystepZ is gated ObjectPresent(gcode_move); dismiss is gated ObjectPresent(virtual_sdcard)"
    - "CommandCatalogDriftTest stays GREEN after the two new command rows are added to docs/commands/catalog.json + printer-matrix.json"
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/command/PrinterCommands.kt"
      provides: "setGcodeOffsetZAdjust builder + BABYSTEP_STEPS const + SDCARD_RESET_FILE const"
      contains: "fun setGcodeOffsetZAdjust"
    - path: "app/src/main/java/works/mees/dinghy/command/CommandRegistry.kt"
      provides: "babystepZ + dismissPrint CommandSpecs registered in all"
      contains: "BabystepArgs"
  key_links:
    - from: "CommandRegistry.babystepZ / dismissPrint"
      to: "CommandRegistry.all list"
      via: "appended to the all listOf(...)"
      pattern: "babystepZ|dismissPrint"
---

<objective>
Add the two net-new gcode CommandSpecs Phase 16 needs: `SET_GCODE_OFFSET Z_ADJUST=±step MOVE=1`
(babystep) and `SDCARD_RESET_FILE` (Terminal Dismiss). Both follow the existing `PrinterCommands`
clamp/validate-before-format discipline (ASVS V5) and MUST be appended to `CommandRegistry.all` — a spec
missing from `all` is dead wiring (the exact class the plan-checker caught on Phase 11).

Purpose: babystep dispatch (SC-5) and Terminal Dismiss (D-05) need canonical registry specs before the
Wave-3 screen wires their gutter/row actions.

Output: extended `PrinterCommands.kt` (builder + 2 consts) + `CommandRegistry.kt` (2 specs, registered)
+ catalog/matrix sidecar rows (`docs/commands/catalog.json` + `docs/commands/printer-matrix.json` — the
REAL paths `CommandCatalogDriftTest.kt:145,151` reads) + `PrinterCommandsTest` GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-home-print-status-redesign/16-RESEARCH.md
@.planning/phases/16-home-print-status-redesign/16-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PrinterCommands — setGcodeOffsetZAdjust builder + BABYSTEP_STEPS + SDCARD_RESET_FILE</name>
  <files>app/src/main/java/works/mees/dinghy/command/PrinterCommands.kt, app/src/test/java/works/mees/dinghy/command/PrinterCommandsTest.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/command/PrinterCommands.kt (header @7-22 V5 discipline; testZ @198 clamp-before-format; COOLDOWN @60 const; MATERIAL_PRESETS @104 fixed-set discipline)
    - app/src/test/java/works/mees/dinghy/command/PrinterCommandsTest.kt (testZ test analog; the Wave-0 RED cases to turn GREEN)
    - 16-RESEARCH.md Code Examples (SET_GCODE_OFFSET builder) + Security Domain (V5)
    - 16-PATTERNS.md (CommandRegistry/PrinterCommands section — babystep builder, validate-against-set)
  </read_first>
  <behavior>
    - Test: setGcodeOffsetZAdjust(-0.05) == "SET_GCODE_OFFSET Z_ADJUST=-0.05 MOVE=1" (Compress = negative)
    - Test: setGcodeOffsetZAdjust(0.10) == "SET_GCODE_OFFSET Z_ADJUST=0.1 MOVE=1" (Expand = positive)
    - Test: BABYSTEP_STEPS == [0.02, 0.05, 0.10, 0.15, 0.20]
    - Test: SDCARD_RESET_FILE == "SDCARD_RESET_FILE"
    - Test (invalid-input, V5): setGcodeOffsetZAdjust(0.037) canonicalizes |delta| to the nearest BABYSTEP_STEPS member (0.05) preserving sign → "SET_GCODE_OFFSET Z_ADJUST=0.05 MOVE=1"
    - Test (invalid-input, V5): setGcodeOffsetZAdjust(0.0) / a NaN / out-of-range value canonicalizes to a valid member (never emits a raw/garbage Z_ADJUST)
  </behavior>
  <action>
    Add `val BABYSTEP_STEPS = listOf(0.02, 0.05, 0.10, 0.15, 0.20)` (the staging-note fixed cycle) and
    `fun setGcodeOffsetZAdjust(deltaMm: Double): String` returning `"SET_GCODE_OFFSET Z_ADJUST=<canonical>
    MOVE=1"`. Per the V5 discipline (no free-text), the caller supplies `deltaMm` as ±(one of
    BABYSTEP_STEPS); **CANONICALIZE explicitly (do NOT reject)** inside the builder: take `sign(deltaMm)`,
    then snap `abs(deltaMm)` to the nearest BABYSTEP_STEPS member using an epsilon-tolerant nearest-match
    (`minByOrNull { abs(it - abs(deltaMm)) }`, guarding NaN/0/empty by defaulting to the first member), and
    re-apply the sign — so a malformed/off-grid value can never reach the gcode string and the builder is
    total (always returns a valid Z_ADJUST). Format the Double cleanly with `Locale.US` (avoid
    trailing-zero/locale-comma noise — match how `jog`/`testZ` format; `0.10 → "0.1"`, `0.05 → "0.05"`).
    Mirror the `testZ` clamp-before-format pattern. Add `const val SDCARD_RESET_FILE = "SDCARD_RESET_FILE"`
    next to COOLDOWN/SAVE_CONFIG. Turn the Wave-0 RED PrinterCommandsTest cases GREEN by pointing them at
    the real builder/const, and add the two invalid-input canonicalization cases above.
  </action>
  <verify>
    <automated>cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --tests 'works.mees.dinghy.command.PrinterCommandsTest'" 2>&1 | tr -d '\r' | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - PrinterCommandsTest exits 0 (GREEN) including the new SET_GCODE_OFFSET sign + SDCARD_RESET_FILE cases AND the two invalid-input canonicalization cases.
    - `setGcodeOffsetZAdjust` never concatenates a free-text/user string — only the canonicalized numeric (grep + read the builder); an off-grid input snaps to a BABYSTEP_STEPS member, never emitted raw.
    - The Double is formatted with `Locale.US` (grep `Locale.US` in the builder) — no locale-comma/trailing-zero noise.
    - `BABYSTEP_STEPS` is defined once and is the canonical fixed cycle.
  </acceptance_criteria>
  <done>Builder + consts added with V5 canonicalize-against-set + Locale.US formatting; PrinterCommandsTest GREEN incl. invalid-input.</done>
</task>

<task type="auto">
  <name>Task 2: CommandRegistry — babystepZ + dismissPrint specs, registered in all + catalog sidecar</name>
  <files>app/src/main/java/works/mees/dinghy/command/CommandRegistry.kt, docs/commands/catalog.json, docs/commands/printer-matrix.json</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/command/CommandRegistry.kt (testZ spec @483-488; setHeater @316; printStart @187 virtual_sdcard gate; the gcode(...) factory; the `all` list @512)
    - app/src/test/java/works/mees/dinghy/command/CommandCatalogDriftTest.kt (@145 reads docs/commands/catalog.json; @151 reads docs/commands/printer-matrix.json — the REAL sidecar paths)
    - 16-PATTERNS.md (CommandRegistry section — spec pattern, args data-class, append-to-all rule)
    - docs/commands/catalog.json + docs/commands/printer-matrix.json (existing sidecar row shapes)
  </read_first>
  <action>
    Add `data class BabystepArgs(val deltaMm: Double)` near the other args data classes. Add
    `val babystepZ: CommandSpec<BabystepArgs> = gcode(catalogId = "KGC-SETGCODEOFFSET", key = { "babystep"
    }, gcode = { PrinterCommands.setGcodeOffsetZAdjust(it.deltaMm) }, availability =
    AvailabilityPredicate.ObjectPresent("gcode_move"))` (gcode_move carries homing_origin). Add
    `val dismissPrint: CommandSpec<*>` as a no-param const-gcode spec (mirror cooldown/disableSteppers):
    `gcode = { PrinterCommands.SDCARD_RESET_FILE }`, availability `ObjectPresent("virtual_sdcard")`
    (mirrors printStart). Both specs MUST be appended to the `all` listOf(...) @512 — a spec missing from
    `all` is dead. Reprint reuses the EXISTING `printStart(PrintStartArgs(filename))` — do NOT add a new
    reprint spec. Update `docs/commands/catalog.json` + `docs/commands/printer-matrix.json` (the REAL paths
    `CommandCatalogDriftTest.kt:145,151` reads — NOT `docs/command-catalog.json`/`docs/printer-matrix.json`,
    which do not exist) with the two new command rows (registered = true; availability per object —
    gcode_move present on both E5/E3, virtual_sdcard present on both) so `CommandCatalogDriftTest` stays GREEN.
  </action>
  <verify>
    <automated>cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --tests 'works.mees.dinghy.command.PrinterCommandsTest' --tests 'works.mees.dinghy.command.CommandRegistryTest' --tests 'works.mees.dinghy.command.CommandCatalogDriftTest'" 2>&1 | tr -d '\r' | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `CommandRegistry.all` contains both `babystepZ` and `dismissPrint` — grep the `all` listOf body.
    - babystepZ availability == ObjectPresent("gcode_move"); dismissPrint availability == ObjectPresent("virtual_sdcard") — read the specs.
    - The two new rows were written to `docs/commands/catalog.json` + `docs/commands/printer-matrix.json` (grep the new catalogId in those exact files); NO edit to a `docs/command-catalog.json` / `docs/printer-matrix.json` dead file.
    - `CommandCatalogDriftTest` exits 0 — the sidecar rows match the registry.
    - No new reprint spec — Reprint uses existing printStart.
  </acceptance_criteria>
  <done>Both specs registered in all, object-gated, correct-path catalog/matrix sidecars updated, CommandCatalogDriftTest GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app → Moonraker (printer.gcode.script) | gcode strings cross from the app into the printer's command interpreter. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-03-01 | Tampering (V5 Input Validation) | `setGcodeOffsetZAdjust` builder | mitigate | The step is canonicalized against the fixed BABYSTEP_STEPS set (epsilon nearest-match, sign preserved) before Locale.US formatting; only a validated signed numeric reaches the string — never a user/free-text value, never an off-grid/NaN value. Mirrors the existing `testZ` clamp discipline. |
| T-16-03-02 | Tampering (V5) | `SDCARD_RESET_FILE` const | mitigate | Fixed const gcode, zero interpolation. |
| T-16-03-03 | Tampering (physical motion) | babystep `MOVE=1` Z jog | mitigate | `MOVE=1` is a small Z jog; gated by the Wave-3 early-layer window + the gcode_move availability predicate; session-only, no SAVE_CONFIG. |
| T-16-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase. |
</threat_model>

<verification>
- `command.*` host tests GREEN incl. CommandCatalogDriftTest (reads docs/commands/*.json — the real paths).
- Both specs in `all`, object-gated; builder is V5-clean (canonicalize-against-set, Locale.US).
</verification>

<success_criteria>
The babystep and dismiss commands are canonically registered, object-gated, V5-validated, and proven by
host tests; the correct `docs/commands/*.json` sidecars stay drift-free.
</success_criteria>

<output>
Create `.planning/phases/16-home-print-status-redesign/16-03-SUMMARY.md` when done.
</output>
