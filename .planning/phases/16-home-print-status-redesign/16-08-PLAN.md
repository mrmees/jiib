---
phase: 16-home-print-status-redesign
plan: 08
type: execute
wave: 5
depends_on: ["16-06", "16-07"]
files_modified:
  - .planning/phases/16-home-print-status-redesign/16-UAT.md
autonomous: false
requirements: [SC-3, SC-4, SC-5, "babystep live round-trip (SC-5)", "perf no-regression (SC-3)", "monitor loop preserved (SC-4)"]
user_setup: []

must_haves:
  truths:
    - "Before the device gates: the full unit suite is GREEN and a cleanup grep finds NO leftover fail(\"not yet implemented placeholders, NO rememberCoroutineScope in babystep/prefs paths, and NO proc_stats"
    - "On a real first layer (flox + Ender 5 Plus), a babystep nudge moves gcode_move.homing_origin[2] the correct direction (Compress=closer) and the live readout matches; the row hides after the layer window; session-only (no config write)"
    - "On the release build on flox, Print-Status (Standby + Printing) shows no frozen frames / no jank vs the current home (Adreno-320, SC-3)"
    - "The connect -> monitor -> drive-a-print loop is behavior-preserving on the live printer (SC-4)"
  artifacts:
    - path: ".planning/phases/16-home-print-status-redesign/16-UAT.md"
      provides: "Recorded on-device UAT results (SC-5 babystep, SC-3 perf, SC-4 monitor loop) + the pre-UAT cleanup-gate result"
  key_links:
    - from: "babystep nudge (SET_GCODE_OFFSET Z_ADJUST=±n MOVE=1)"
      to: "gcode_move.homing_origin[2] readback + physical nozzle"
      via: "live first-layer eyeball — sign cannot be trusted from a mock"
      pattern: "homing_origin"
---

<objective>
Close the three binding on-device gates that host tests cannot prove — the project's #1 recurring failure
class is "green host suite, broken on real hardware" ([[dinghy-display-mock-vs-reality]], 5+ strikes) —
preceded by a mechanical pre-UAT cleanup gate (no leftover RED placeholders, no write-scope trap, no
deferred proc_stats sneaking in). The device gates are full builds + manual verification on the real flox
tablet (LineageOS / genuine Adreno 320) and a live printer; no mock can substitute.

Purpose: a clean phase-completion bar — SC-5 (babystep live round-trip + correct sign on a real first
layer), SC-3 (Adreno-320 perf no-regression), SC-4 (core monitor loop preserved), plus the no-`fail(` /
no-`rememberCoroutineScope` / no-`proc_stats` cleanup check before the phase can complete.

Output: a recorded `16-UAT.md` with the cleanup-gate result + PASS/FAIL per device gate. A FAIL spawns
gap-closure, NOT a phase-complete mark.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-home-print-status-redesign/16-VALIDATION.md
@.planning/phases/16-home-print-status-redesign/16-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Pre-UAT cleanup gate + build/sign/install + capture UAT scaffold</name>
  <what-built>
    The four-state Print-Status home (16-06) + the babystep wiring + the doc-merge (16-07). This task FIRST
    runs the mechanical pre-UAT cleanup gate, THEN assembles + signs the RELEASE APK and installs it on flox
    for the manual gates below. (Per CLAUDE.md, build is Windows-side via `E:\Android\gw.bat`; sign a
    release with `E:\Android\sign-release.bat`; install via adb interop. Guard hung Gradle with `timeout`
    + `taskkill /F /T`.)
  </what-built>
  <action>
    Orchestrator: FIRST run the pre-UAT cleanup gate (the greps in `<verify>`): the full
    `:app:testDebugUnitTest` suite GREEN, and NO leftover `fail("not yet implemented` RED placeholders
    anywhere in the test sources, NO `rememberCoroutineScope` in the babystep/prefs paths
    (BabystepPrefs/AppContainer babystep helpers/the Settings babystep section — the #1 recurring trap),
    and NO `proc_stat` host-load reference in the state package (deferred to P19). If any cleanup grep
    trips, STOP and fix before building — these are completion blockers, not warnings. THEN run
    `:app:assembleRelease` (Windows-side, guarded with `timeout` + `taskkill /F /T`), sign with
    `E:\Android\sign-release.bat`, `adb install` on flox, and scaffold `16-UAT.md` with the cleanup-gate
    result + the three device-gate checkboxes + printer addresses. This is a gate-and-stage step for the
    manual gates below — no production code changes (only fixes if the cleanup gate trips).
  </action>
  <how-to-verify>
    1. The orchestrator runs `:app:testDebugUnitTest` (full suite GREEN) and the three cleanup greps below
       (each MUST be empty): leftover `fail("not yet implemented`, `rememberCoroutineScope` in the
       babystep/prefs files, `proc_stat` in the state package.
    2. The orchestrator runs `:app:assembleRelease`, signs the APK, and `adb install`s it on flox.
    3. Create `.planning/phases/16-home-print-status-redesign/16-UAT.md` with the cleanup-gate result + the
       three device gates below as checkboxes (SC-5 babystep, SC-3 perf, SC-4 monitor loop) and the printer
       addresses (E5 = 192.168.1.120:7125, E3 = 192.168.1.121:7125).
    4. Confirm the app launches on flox and routes to the correct mode (Standby when idle).
  </how-to-verify>
  <verify>
    <automated>cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest" 2>&1 | tr -d '\r' | tail -15; echo "--- cleanup greps (each MUST print NONE) ---"; grep -rn 'fail("not yet implemented' app/src/test app/src/androidTest || echo NONE-fail-placeholder; grep -rn 'rememberCoroutineScope' app/src/main/java/works/mees/dinghy/ui/settings/BabystepPrefs.kt app/src/main/java/works/mees/dinghy/di/AppContainer.kt 2>/dev/null || echo NONE-remember-scope; grep -rni 'proc_stat' app/src/main/java/works/mees/dinghy/state || echo NONE-proc-stat</automated>
  </verify>
  <acceptance_criteria>
    - Full `:app:testDebugUnitTest` GREEN; the three cleanup greps each print NONE (no `fail("not yet implemented`, no `rememberCoroutineScope` in babystep/prefs paths, no `proc_stat` in state) — completion blockers if any trips.
    - `:app:assembleRelease` SUCCESSFUL (exit code authoritative); signed APK installed on flox.
    - `16-UAT.md` exists with the cleanup-gate result + the three device gates scaffolded.
  </acceptance_criteria>
  <resume-signal>Type "installed" once the cleanup gate is clean, the signed release build is on flox, and 16-UAT.md is scaffolded.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: SC-5 — live babystep round-trip on a real first layer (E5 Plus)</name>
  <what-built>
    The babystep 3-cell row (Compress/step/Expand) dispatching `SET_GCODE_OFFSET Z_ADJUST=±step MOVE=1`,
    reading back `gcode_move.homing_origin[2]`, gated to the early-layer window, session-only.
  </what-built>
  <action>
    Manual on-device gate (no code change): drive the live first-layer babystep round-trip on flox + the Ender 5 Plus per the steps in `<how-to-verify>` and record the observed `homing_origin[2]` values + PASS/FAIL into `16-UAT.md`. A sign/direction failure is a blocking gap (gap-closure), not a phase-complete.
  </action>
  <how-to-verify>
    1. Start a print on flox + the Ender 5 Plus (192.168.1.120:7125). Confirm the babystep row APPEARS
       during the first ~N layers (the default 5) and that it is HIDDEN if the slicer doesn't report
       `current_layer` (no time-based fallback — expected).
    2. Tap **Compress** — verify the nozzle moves CLOSER to the bed AND `gcode_move.homing_origin[2]`
       (the Applied-Z-offset stat) decreases by the step. Tap **Expand** — nozzle moves FARTHER and the
       offset increases. The on-screen sign/direction MUST match the physical nozzle (a sign error is
       invisible to unit tests and dangerous on a first layer — this is the whole point of the gate).
    3. Tap the center cell — verify it cycles .02 → .05 → .10 → .15 → .20.
    4. Continue past the layer window — verify the babystep row HIDES and the normal shortcut row returns.
    5. Confirm session-only: no SAVE_CONFIG is issued; the offset is not written to config.
    6. Record PASS/FAIL + the observed `homing_origin[2]` values in 16-UAT.md.
  </how-to-verify>
  <acceptance_criteria>
    - Compress moves the nozzle closer AND decreases homing_origin[2]; Expand the reverse — sign verified against physical motion.
    - Step cycles correctly; row appears/hides on the layer window; no config write.
    - Result recorded in 16-UAT.md (PASS or a documented gap for gap-closure).
  </acceptance_criteria>
  <resume-signal>Report the babystep UAT result (pass, or describe the sign/direction/visibility issue).</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: SC-3 perf no-regression + SC-4 monitor loop preserved</name>
  <what-built>
    The reworked four-state home — Compose ProgressRing + StatGrid throttled at the store's 250ms; the
    Views GraphView render path untouched (ADR-0001 hybrid).
  </what-built>
  <action>
    Manual on-device gate (no code change): capture `gfxinfo framestats` + eyeball Print-Status perf on flox (Standby + Printing) and run the connect→monitor→drive-a-print loop on a live printer per `<how-to-verify>`; record SC-3 + SC-4 PASS/FAIL into `16-UAT.md`.
  </action>
  <how-to-verify>
    1. **SC-3 (perf):** On the release build on flox (genuine Adreno 320), eyeball + `gfxinfo framestats`
       on Print-Status in BOTH Standby and Printing. Confirm NO frozen frames / no sustained jank vs the
       current home (emulators lie about old-GPU fill rate — must be on flox). Pay attention to the live
       Printing cockpit (ring + stats updating at ~2–4 Hz).
    2. **SC-4 (monitor loop):** On flox + a live printer, run the core loop: connect → monitor live
       progress/temps/Z on the Status home → drive a print (start/pause/resume/cancel). Confirm no
       regression vs the prior home — the connect/monitor/control loop still works flawlessly (the
       project's core value).
    3. Also spot-check Terminal: let a print complete (or cancel one) → navigate Home → confirm the
       Terminal hero + stats + Dismiss/Reprint render (passive — it does NOT yank you from another screen),
       and that Terminal(Error) shows the projected error lines.
    4. Record PASS/FAIL for SC-3 and SC-4 in 16-UAT.md.
  </how-to-verify>
  <acceptance_criteria>
    - No frozen frames / jank on Print-Status (Standby + Printing) on flox vs the current home (SC-3).
    - Connect → monitor → drive-a-print loop behavior-preserving on the live printer (SC-4).
    - Terminal renders passively (Dismiss/Reprint work; error lines show on Terminal(Error)); results recorded in 16-UAT.md.
  </acceptance_criteria>
  <resume-signal>Report the perf + monitor-loop UAT result (pass, or describe the regression).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app → live printer (babystep MOVE=1) | a real physical Z jog on a live first layer — the one place a sign error is physically consequential. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-08-01 | Tampering (physical motion) | live babystep round-trip | mitigate | The gate EXISTS to catch a wrong-sign/unsafe-motion bug on real hardware before ship; `MOVE=1` is a small jog, gated to the early-layer window, session-only, no SAVE_CONFIG. Wrong direction is a FAIL that blocks the phase. |
| T-16-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase. |
</threat_model>

<verification>
- Pre-UAT cleanup gate clean (no `fail("not yet implemented`, no `rememberCoroutineScope` in babystep/prefs, no `proc_stat` in state) + full unit suite GREEN.
- 16-UAT.md records PASS for SC-5 (babystep sign/round-trip), SC-3 (perf), SC-4 (monitor loop).
- A FAIL spawns gap-closure (`/gsd-plan-phase 16 --gaps`), not a phase-complete mark.
</verification>

<success_criteria>
The mechanical cleanup gate is clean and the three on-device binding gates pass on real flox + a live
printer, proving the babystep direction, no perf regression, and a preserved monitor loop.
</success_criteria>

<output>
Create `.planning/phases/16-home-print-status-redesign/16-UAT.md` (results) + `16-08-SUMMARY.md` when the
gates pass. Do NOT write the SUMMARY or mark the phase complete on a FAIL — spawn gap-closure instead.
</output>
