---
phase: 16-home-print-status-redesign
plan: 02
type: execute
wave: 1
depends_on: ["16-01"]
files_modified:
  - app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusMode.kt
  - app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusControlModel.kt
  - app/src/test/java/works/mees/dinghy/ui/printstatus/PrintStatusModeTest.kt
autonomous: true
requirements: [SC-1, SC-5, "classifier-core (refines SHELL-*)", D-05]
user_setup: []

must_haves:
  truths:
    - "classifyPrintStatus maps all 6 raw PrintState values to the 4-state PrintStatusMode"
    - "Standby classifies as Standby even when restartFilename/lastJob is stale (behavior change vs derivePrintStatusControls)"
    - "Klippy shutdown/error does NOT produce a Terminal mode (classifier reads only printState)"
    - "Babystep gating: null currentLayer hides; <= threshold shows; > threshold hides (no time fallback)"
    - "The babystep step cycle advances .02 -> .05 -> .10 -> .15 -> .20 -> .02"
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusMode.kt"
      provides: "Pure PrintStatusMode classifier + babystep gating/step-cycle helpers"
      contains: "fun classifyPrintStatus"
    - path: "app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusControlModel.kt"
      provides: "Extended control actions (Preheat/Dismiss/Power) + per-mode gutter control sets"
      contains: "PrintStatusControlAction"
  key_links:
    - from: "PrintStatusMode.kt"
      to: "PrinterState.printState"
      via: "pure when(state.printState)"
      pattern: "when \\(state.printState\\)"
---

<objective>
Build the locked core move: a pure, host-testable `PrintStatusMode` classifier and the extended
per-mode gutter-control derivation. Composables (Wave 3) route off the mode instead of scattering raw
`printState` checks. This turns the Wave-0 `PrintStatusModeTest` GREEN.

Purpose: ONE classifier is the phase's central architectural intent (CONTEXT domain + RESEARCH §1).
The deliberate behavior change — Standby is ALWAYS Standby (no terminal masquerade on a leftover file)
— lives here and MUST be called out so an executor does not copy the old `PrintStatusControlModel`
Standby branch verbatim.

Output: `PrintStatusMode.kt` (new, pure Kotlin, no Compose) + extended `PrintStatusControlModel.kt`
(new Preheat/Dismiss/Power actions + per-mode control sets); `PrintStatusModeTest` GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-home-print-status-redesign/16-RESEARCH.md
@.planning/phases/16-home-print-status-redesign/16-PATTERNS.md
@.planning/phases/16-home-print-status-redesign/16-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PrintStatusMode classifier + babystep gating/step-cycle (pure)</name>
  <files>app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusMode.kt, app/src/test/java/works/mees/dinghy/ui/printstatus/PrintStatusModeTest.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusControlModel.kt (THE analog @43-75 — same pure-fun-of-PrinterState discipline; note the Standby->terminal masquerade @67-74 the classifier MUST NOT copy)
    - app/src/main/java/works/mees/dinghy/state/PrinterState.kt (PrintState enum @214; klippyState distinct axis @33; currentLayer @75)
    - 16-RESEARCH.md §1 (recommended classifier shape) + Pattern 4 (babystep gating, step cycle)
    - 16-PATTERNS.md (PrintStatusMode.kt section — copy-from shape + the behavior-change warning)
    - app/src/test/java/works/mees/dinghy/ui/printstatus/PrintStatusModeTest.kt (the RED scaffold to turn GREEN)
  </read_first>
  <behavior>
    - Test: Printing->Printing, Paused->Paused, Complete/Cancelled/Error->Terminal(kind), Standby->Standby
    - Test: Standby with stale restartFilename still classifies Standby (NOT Terminal)
    - Test: a state whose printState is Standby but klippyState is Shutdown/Error still classifies Standby (classifier ignores klippyState)
    - Test: babystepVisible == false when currentLayer is null
    - Test: babystepVisible == true when currentLayer <= threshold; false when > threshold
    - Test: nextBabystepStep cycles .02 -> .05 -> .10 -> .15 -> .20 -> .02
  </behavior>
  <action>
    Create `PrintStatusMode.kt` as plain Kotlin (no Compose import) in the `ui.printstatus` package.
    Define `sealed interface PrintStatusMode { Standby; Printing; Paused; data class Terminal(kind:
    TerminalKind); enum TerminalKind { Complete, Cancelled, Error } }` and
    `fun classifyPrintStatus(state: PrinterState): PrintStatusMode = when (state.printState) { ... }`
    mapping exactly per RESEARCH §1 — Standby branch returns `PrintStatusMode.Standby` with NO read of
    `printFilename`/`lastJob`/`restartFilename` (the behavior change vs PrintStatusControlModel.kt:67).
    Add the babystep helpers as pure fns in this file: `fun babystepVisible(settingEnabled: Boolean,
    currentLayer: Int?, layerThreshold: Int): Boolean = settingEnabled && (currentLayer?.let { it <=
    layerThreshold } == true)` (null layer -> hidden, NO time fallback) and `fun nextBabystepStep(current:
    Double): Double` cycling the fixed list {0.02, 0.05, 0.10, 0.15, 0.20} (wrap to first). Reference the
    fixed step set from `PrinterCommands.BABYSTEP_STEPS` if 16-03 lands first, else inline the list here and
    note it must stay in sync (16-03 owns the canonical const). Turn the Wave-0 `PrintStatusModeTest` GREEN
    by replacing its fail()/literal bodies with real assertions against the new symbols.
  </action>
  <verify>
    <automated>cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --tests 'works.mees.dinghy.ui.printstatus.PrintStatusModeTest'" 2>&1 | tr -d '\r' | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - PrintStatusModeTest exits 0 (GREEN).
    - `PrintStatusMode.kt` has NO `import androidx.compose` (pure, host-testable) — grep clean.
    - `classifyPrintStatus` Standby branch does NOT read printFilename/lastJob/restartFilename — grep the fn body.
    - `babystepVisible(true, null, 5) == false` and `babystepVisible(true, 3, 5) == true` and `babystepVisible(true, 6, 5) == false` asserted.
  </acceptance_criteria>
  <done>Classifier + babystep helpers built pure; PrintStatusModeTest GREEN; behavior change (Standby=Standby) proven by test.</done>
</task>

<task type="auto">
  <name>Task 2: Extend PrintStatusControlModel — Preheat / Dismiss / Power actions + per-mode control sets</name>
  <files>app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusControlModel.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusControlModel.kt (self — enum @20-28; activeControls/terminalControls/standbyControls @104-155; pending-action debounce @79-102)
    - 16-UI-SPEC.md (Per-state button intents table + per-state interaction contract for the gutter sets)
    - 16-PATTERNS.md (PrintStatusControlModel section)
    - 16-RESEARCH.md §2 harvest map (derivePrintStatusControls "Extend" row)
  </read_first>
  <action>
    Extend the `PrintStatusControlAction` enum with `Preheat`, `Dismiss`, and `Power` (Power = inert in
    P16, D-04) members — reuse the existing `PrintStatusPendingAction` debounce machinery
    (`clearPrintStatusPendingAction` @79-102), do NOT re-roll it. Add/adjust the per-mode gutter control
    sets to match the UI-SPEC binding contract: Standby gutter = `Preheat` (accent) + `Power` (inert
    stop-intent), NO E-Stop; Printing gutter = `Pause` + `Cancel` + `EStop`; Paused gutter = `Resume` +
    `Cancel`, NO E-Stop; Terminal gutter = `Dismiss` + `Reprint`. The standby control set MUST NOT emit
    terminal controls when a restartFilename is present (delete/replace the `@67-74` masquerade branch) —
    restart-from-idle moves to the Standby launcher Files tile (Wave 3), not the gutter. Keep
    `terminalControls`/`activeControls` shapes for the Printing/Paused/Terminal gutters; the screen's
    `controlColor` intent mapping is updated in Wave 3 (16-06). Per D-03 the active-print Tune tile is a
    Field shortcut (not a gutter control) — do not add it to the gutter set.
  </action>
  <verify>
    <automated>cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --tests 'works.mees.dinghy.ui.printstatus.*'" 2>&1 | tr -d '\r' | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `PrintStatusControlAction` enum contains `Preheat`, `Dismiss`, `Power` — grep the enum.
    - The Standby control-set helper no longer returns terminal controls on a non-null restartFilename — grep shows the masquerade branch removed.
    - The existing `PrintStatusControlModel`/`PrintStatusHolder` host tests still pass (no regression).
  </acceptance_criteria>
  <done>Control actions extended; Standby masquerade removed; per-mode gutter sets match UI-SPEC; tests GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Pure derivation only — no input crosses a runtime boundary in this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-02-V5 | Tampering (V5 Input Validation) | classifier + control derivation | accept | Pure functions of `PrinterState` (Moonraker-derived) — no user-string interpolation, no gcode emitted here. The command builders (V5-relevant) live in 16-03. |
| T-16-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase. |
</threat_model>

<verification>
- PrintStatusModeTest GREEN; full `ui.printstatus.*` host tests GREEN.
- Classifier is Compose-free; Standby-stays-Standby behavior change proven by test.
</verification>

<success_criteria>
The pure four-state classifier and extended control sets exist and are host-tested; the Standby
behavior change is locked by a passing test.
</success_criteria>

<output>
Create `.planning/phases/16-home-print-status-redesign/16-02-SUMMARY.md` when done.
</output>
