---
phase: 16-home-print-status-redesign
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - app/src/test/java/works/mees/dinghy/ui/printstatus/PrintStatusModeTest.kt
  - app/src/test/java/works/mees/dinghy/ui/printstatus/PreheatTest.kt
  - app/src/test/java/works/mees/dinghy/ui/printstatus/GlanceSensorTest.kt
  - app/src/test/java/works/mees/dinghy/command/PrinterCommandsTest.kt
  - app/src/test/java/works/mees/dinghy/state/PrinterStateReducerTest.kt
  - app/src/test/java/works/mees/dinghy/ui/settings/BabystepPrefsTest.kt
autonomous: true
requirements: [SC-1, SC-5, D-01, D-05, D-06, "classifier-core (refines SHELL-*)"]
user_setup: []

must_haves:
  truths:
    - "Host unit suite compiles with the new RED scaffolds present (no broken test sourceset)"
    - "Each RED scaffold fails until its production code is built in a later wave"
  artifacts:
    - path: "app/src/test/java/works/mees/dinghy/ui/printstatus/PrintStatusModeTest.kt"
      provides: "RED classifier + babystep-gating + step-cycle test"
    - path: "app/src/test/java/works/mees/dinghy/ui/printstatus/PreheatTest.kt"
      provides: "RED spool-aware preheat selection test (gates the new pure selectPreheatPath fn in 16-02)"
    - path: "app/src/test/java/works/mees/dinghy/ui/printstatus/GlanceSensorTest.kt"
      provides: "RED glance-sensor selection test (gates the pure selectGlanceSensor fn in 16-04: MCU>host>first, stable, empty->null)"
    - path: "app/src/test/java/works/mees/dinghy/ui/settings/BabystepPrefsTest.kt"
      provides: "RED BabystepPrefs persistence test (defaults + fail-safe read + write-through)"
  key_links:
    - from: "PrintStatusModeTest"
      to: "classifyPrintStatus (Wave 2)"
      via: "test references the future pure fn — must use fail() bodies, NOT call unbuilt symbols"
      pattern: "fail\\("
    - from: "PreheatTest"
      to: "selectPreheatPath (Wave 2, 16-02)"
      via: "RED until the pure preheat-selection fn exists; fail()/literal bodies only"
      pattern: "fail\\("
    - from: "GlanceSensorTest"
      to: "selectGlanceSensor (Wave 1, 16-04)"
      via: "RED until the pure glance-selection fn exists; fail()/literal bodies only"
      pattern: "fail\\("
    - from: "BabystepPrefsTest"
      to: "BabystepPrefs (16-05)"
      via: "RED until the prefs class exists; mirrors the MacroPrefs test analog"
      pattern: "fail\\("
---

<objective>
Establish the Wave-0 RED test scaffolds for Phase 16 so every later wave has an automated gate to turn
GREEN. These cover the host-testable seams: the `PrintStatusMode` classifier, the pure spool-aware
Preheat selection (`selectPreheatPath`), the pure glance-sensor selection (`selectGlanceSensor`), the two
new gcode command builders, the `homing_origin[2]` reducer parse, and the `BabystepPrefs` persistence class.

Purpose: Nyquist compliance — wire each later task to a concrete `--tests` command. The whole test
sourceset compiles before `--tests` filtering runs, so these scaffolds MUST compile day-one
([[dinghy-wave0-red-scaffold-compile]]) or they brick every per-wave run. The `BabystepPrefs` scaffold
in particular unit-gates the project's #1 recurring trap (write-scope cancellation), so persistence
correctness is proven by a test, not just `assembleDebug`. The `GlanceSensorTest` scaffold unit-gates the
partial-diff sensor-flip fix (16-04) before the UI consumes it.

Output: Four new test files (PrintStatusModeTest, PreheatTest, GlanceSensorTest, BabystepPrefsTest) +
extensions to two existing test files (PrinterCommandsTest, PrinterStateReducerTest), all RED (fail()
bodies / asserts that fail until the production code lands).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-home-print-status-redesign/16-VALIDATION.md
@.planning/phases/16-home-print-status-redesign/16-RESEARCH.md
@.planning/phases/16-home-print-status-redesign/16-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: PrintStatusModeTest + PreheatTest + GlanceSensorTest RED scaffolds (compile-clean)</name>
  <files>app/src/test/java/works/mees/dinghy/ui/printstatus/PrintStatusModeTest.kt, app/src/test/java/works/mees/dinghy/ui/printstatus/PreheatTest.kt, app/src/test/java/works/mees/dinghy/ui/printstatus/GlanceSensorTest.kt</files>
  <read_first>
    - app/src/test/java/works/mees/dinghy/ui/printstatus/ (existing PrintStatusControlModel test — copy its imports/package/PrinterState fixture-builder style)
    - app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusControlModel.kt (the analog for the classifier; shows PrintState enum usage)
    - app/src/main/java/works/mees/dinghy/state/PrinterState.kt (PrintState enum @214; currentLayer @75 — for fixture construction)
    - 16-RESEARCH.md §1 (classifier shape), §3 (glance-sensor rule), §5 (preheat rule), Validation Architecture
    - [[dinghy-wave0-red-scaffold-compile]] memory note
  </read_first>
  <action>
    Create `PrintStatusModeTest.kt` with RED test cases that DESCRIBE (do not yet implement) the
    classifier contract: maps all 6 raw `PrintState` values (Printing, Paused, Complete, Cancelled,
    Error, Standby) to the 4 modes; `Standby` stays `Standby` even when a stale `restartFilename`/lastJob
    exists (the deliberate behavior change vs `derivePrintStatusControls`); klippy shutdown/error does NOT
    produce a Terminal mode (classifier reads only `printState`); babystep gating (null `currentLayer`
    hides; `currentLayer <= threshold` shows; `> threshold` hides — NO time fallback); step-cycle
    `.02 → .05 → .10 → .15 → .20`. Use `fail("not yet implemented — Wave 2 16-02")` bodies OR
    `assertEquals` against literals that cannot pass yet — but do NOT reference `classifyPrintStatus`,
    `PrintStatusMode`, or any babystep helper that does not exist yet (those land in 16-02). Build
    PrinterState fixtures with the existing test builder style. Create `PreheatTest.kt` with RED cases
    for the PURE preheat-selection fn that 16-02 will build (`selectPreheatPath(spoolmanPresent, nozzleTemp,
    bedTemp)` returning a sealed result — `DirectTemps(nozzle?, bed?)` / `OpenSelector`): spool present +
    both temps → `DirectTemps(noz, bed)`; spool present + only one temp → `DirectTemps` carrying only the
    present temp (the other null — never 0); spool absent OR both temps null → `OpenSelector`. Create
    `GlanceSensorTest.kt` with RED cases for the PURE glance-selection fn 16-04 will build
    (`selectGlanceSensor(sensors: Map<String, Double>): GlanceSensor?`): a map with both
    `temperature_sensor mcu` and `temperature_sensor host` prefers the mcu entry; host-only returns host;
    neither-mcu-nor-host returns the first by stable key order; the SAME map yields the SAME choice across
    calls (stability); empty map → null. Same fail()/literal discipline — do not call the not-yet-built
    `selectPreheatPath`/`PreheatPath`/`selectGlanceSensor`/`GlanceSensor`. Keep every symbol referenced an
    already-existing one so the sourceset compiles.
  </action>
  <verify>
    <automated>cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --tests 'works.mees.dinghy.ui.printstatus.PrintStatusModeTest' --tests 'works.mees.dinghy.ui.printstatus.PreheatTest' --tests 'works.mees.dinghy.ui.printstatus.GlanceSensorTest'" 2>&1 | tr -d '\r' | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - The three new test files exist and the FULL `:app:testDebugUnitTest` sourceset COMPILES (no unresolved-reference errors anywhere).
    - PrintStatusModeTest, PreheatTest, and GlanceSensorTest are RED (their cases fail), proving they gate Wave-1/2 work.
    - PreheatTest's cases encode the `selectPreheatPath` contract (DirectTemps/OpenSelector, per-temp guard, never 0); GlanceSensorTest's cases encode the `selectGlanceSensor` contract (MCU>host>first, stable, empty->null).
    - No reference to any unbuilt symbol (`classifyPrintStatus`, `PrintStatusMode`, `selectPreheatPath`, `PreheatPath`, `selectGlanceSensor`, `GlanceSensor`, babystep helpers) — grep the files: only `fail(` bodies or literal asserts.
  </acceptance_criteria>
  <done>Three RED scaffolds present, sourceset compiles, the three test classes fail (RED).</done>
</task>

<task type="auto">
  <name>Task 2: Extend PrinterCommandsTest + PrinterStateReducerTest (RED)</name>
  <files>app/src/test/java/works/mees/dinghy/command/PrinterCommandsTest.kt, app/src/test/java/works/mees/dinghy/state/PrinterStateReducerTest.kt</files>
  <read_first>
    - app/src/test/java/works/mees/dinghy/command/PrinterCommandsTest.kt (existing — testZ test is the analog)
    - app/src/main/java/works/mees/dinghy/command/PrinterCommands.kt (testZ @198 clamp-before-format; COOLDOWN @60 const-gcode)
    - app/src/test/java/works/mees/dinghy/state/PrinterStateReducerTest.kt (existing gcode_move parse tests)
    - app/src/main/java/works/mees/dinghy/state/PrinterStateReducer.kt (gcode_move block @106; doubleListOrNull @252)
    - 16-PATTERNS.md (CommandRegistry/PrinterCommands + reducer analog sections)
  </read_first>
  <action>
    Extend `PrinterCommandsTest.kt` with RED cases for the two NEW builders: `setGcodeOffsetZAdjust(±step)`
    emits `SET_GCODE_OFFSET Z_ADJUST=<value> MOVE=1` with the correct sign (Compress = negative delta,
    Expand = positive delta) and the step is one of the fixed set {0.02, 0.05, 0.10, 0.15, 0.20}
    (validate/canonicalize-against-set, never free-text — include an off-grid input case expecting it to
    snap to the nearest member); `SDCARD_RESET_FILE` const is the literal `"SDCARD_RESET_FILE"`.
    Use `fail()`/literal-assert bodies that do NOT call the unbuilt `PrinterCommands.setGcodeOffsetZAdjust`
    / `PrinterCommands.SDCARD_RESET_FILE` (they land in 16-03) — assert against the EXPECTED gcode strings
    as literals so the case is RED but compiles. Extend `PrinterStateReducerTest.kt` with a RED case
    asserting that a `notify_status_update` carrying `gcode_move.homing_origin = [x, y, z, e]` parses into
    `PrinterState.gcodeZOffset == z` (index 2), and a malformed/short array yields null/retains prior —
    again as a RED case that does not yet reference the not-yet-added `gcodeZOffset` field directly if it
    breaks compilation (if the field does not exist yet, encode the expectation as a fail() with a comment
    naming the field + reducer line so 16-04 turns it GREEN).
  </action>
  <verify>
    <automated>cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --tests 'works.mees.dinghy.command.PrinterCommandsTest' --tests 'works.mees.dinghy.state.PrinterStateReducerTest'" 2>&1 | tr -d '\r' | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - The full `:app:testDebugUnitTest` sourceset still COMPILES after the extensions.
    - The new SET_GCODE_OFFSET / SDCARD_RESET_FILE / homing_origin[2] cases are RED.
    - No call to an unbuilt production symbol — grep shows only literal/fail() RED bodies for the new cases.
  </acceptance_criteria>
  <done>Both existing test files extended with RED cases; sourceset compiles; new cases fail.</done>
</task>

<task type="auto">
  <name>Task 3: BabystepPrefsTest RED scaffold (persistence — mirrors MacroPrefs test)</name>
  <files>app/src/test/java/works/mees/dinghy/ui/settings/BabystepPrefsTest.kt</files>
  <read_first>
    - app/src/test/java/works/mees/dinghy/ui/macros/ (the MacroPrefs test analog — copy its in-memory/temp DataStore harness, default-read + write-through + IOException fail-safe assertions)
    - app/src/main/java/works/mees/dinghy/ui/macros/MacroPrefs.kt (the prefs shape BabystepPrefs mirrors — defaults, fail-safe read, suspend writes)
    - 16-PATTERNS.md (Babystep app-setting persistence section — BabystepPrefs shape + keys)
    - 16-RESEARCH.md Pitfall 6 + Runtime State Inventory (DataStore key, defaults enabled/5)
    - [[dinghy-compose-write-scope-cancellation]] memory note
  </read_first>
  <action>
    Create `BabystepPrefsTest.kt` as a RED scaffold mirroring the `MacroPrefs` test analog: a host JUnit
    test over a temp/in-memory `DataStore<Preferences>` covering — default `enabled == true`; default
    `layerCount == 5`; an IOException-throwing read fails SAFE to defaults (enabled=true / layers=5) rather
    than propagating; `setEnabled(false)` then read flows `enabled == false`; `setLayerCount(3)` then read
    flows `layerCount == 3`; `setLayerCount(0)` coerces to ≥1. Use `fail("not yet implemented — 16-05
    BabystepPrefs")`/literal-assert bodies that do NOT reference the not-yet-built `BabystepPrefs` class or
    its keys (it lands in 16-05) — keep the scaffold compile-clean per [[dinghy-wave0-red-scaffold-compile]].
    Copy the MacroPrefs test's DataStore harness verbatim (it already compiles) so the test infra is in place
    for 16-05 to swap in real `BabystepPrefs(dataStore)` assertions.
  </action>
  <verify>
    <automated>cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --tests 'works.mees.dinghy.ui.settings.BabystepPrefsTest'" 2>&1 | tr -d '\r' | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `BabystepPrefsTest.kt` exists and the full `:app:testDebugUnitTest` sourceset COMPILES.
    - The test is RED (cases fail), encoding the defaults (enabled=true/layers=5), IOException-fail-safe, and write-through contract 16-05 must satisfy.
    - No reference to the unbuilt `BabystepPrefs` symbol — grep shows only `fail(`/literal bodies.
  </acceptance_criteria>
  <done>BabystepPrefsTest RED scaffold present; sourceset compiles; the persistence cases fail (RED), gating 16-05.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan creates only host unit tests — no runtime trust boundary crossed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01-V5 | Tampering (V5 Input Validation) | the gcode-builder tests (SET_GCODE_OFFSET / SDCARD_RESET_FILE) | mitigate | The RED tests ENCODE the V5 contract for the later builders: canonicalize the step against the fixed set {0.02..0.20}, never concatenate free text; const SDCARD_RESET_FILE has no interpolation. The assertions are the regression guard 16-03 must satisfy. |
| T-16-01-D | Denial of correctness (partial-diff staleness) | GlanceSensorTest | mitigate | The RED glance-selection test ENCODES the stable MCU>host>first preference that 16-04's pure `selectGlanceSensor` must satisfy — the unit guard against a diff-order-dependent flip. |
| T-16-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase (Android/Gradle, no new deps — RESEARCH "no Package Legitimacy Audit needed"). |
</threat_model>

<verification>
- Full `:app:testDebugUnitTest` compiles with all six scaffolds present.
- PrintStatusModeTest, PreheatTest, GlanceSensorTest, BabystepPrefsTest, and the new PrinterCommandsTest/PrinterStateReducerTest cases are all RED.
</verification>

<success_criteria>
RED scaffolds compile day-one and gate Waves 1–3; nyquist per-task automated commands established
(incl. the BabystepPrefs persistence gate that unit-proves the write-scope trap is handled, and the
GlanceSensor gate that unit-proves the partial-diff selection is stable).
</success_criteria>

<output>
Create `.planning/phases/16-home-print-status-redesign/16-01-SUMMARY.md` when done.
</output>
