---
phase: 16-home-print-status-redesign
plan: 06
type: execute
wave: 3
depends_on: ["16-02", "16-03", "16-04", "16-05"]
files_modified:
  - app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusUiModel.kt
  - app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusScreen.kt
  - app/src/main/java/works/mees/dinghy/ui/shell/AppShell.kt
  - app/src/main/java/works/mees/dinghy/ui/temperature/TemperatureScreen.kt
  - app/src/main/res/drawable/ic_babystep_compress.xml
  - app/src/main/res/drawable/ic_babystep_expand.xml
  - app/src/test/java/works/mees/dinghy/ui/printstatus/PrintStatusUiModelTest.kt
autonomous: true
requirements: [SC-1, SC-2, SC-4, SC-5, D-01, D-02, D-03, D-04, D-05, "four-state home (refines SHELL-* / JOB-*)"]
user_setup: []

must_haves:
  truths:
    - "A pure PrintStatusUiModel maps PrintStatusMode -> the per-mode layout/control derivation (host-testable; the screen renders from it)"
    - "The home screen routes off classifyPrintStatus(state) — Standby / Printing / Paused / Terminal each render their own Focus/Field/Gutter"
    - "AppShell passes the launcher nav (onNavigate(Dest) + onOpenDrawer) AND a bounded <=3 error-line projection into PrintStatusScreen; every launcher tile dispatches to a real Dest (no no-op tile)"
    - "Standby shows the app-icon Focus + minimal glance overlay (glance temp via selectGlanceSensor) + launcher grid (Drawer = flexible tile) + Preheat/Power(inert) gutter, no E-Stop"
    - "Preheat is spool-aware via selectPreheatPath: per-temp setHeater on DirectTemps (never 0), each gated on that heater's capability; PresetSelector (now internal) on OpenSelector"
    - "Printing shows the 03-print-status composition + framed stats (incl. Applied Z offset) + shortcut row (Tune flexible) + Pause/Cancel/E-Stop gutter"
    - "Babystep 3-cell row (Compress/step/Expand) replaces the shortcut row inside the early-layer window; hidden when layer data unavailable"
    - "Paused = dimmed Printing focus + pause overlay + Resume/Cancel gutter; Terminal = clean hero + stats frame + Dismiss/Reprint (Terminal(Error) appends <=3 projected error lines)"
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusUiModel.kt"
      provides: "Pure mode->layout/control mapping (host-testable UI model)"
      contains: "PrintStatusUiModel"
    - path: "app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusScreen.kt"
      provides: "Four-mode state-driven home routed off PrintStatusMode + the UI model"
      contains: "classifyPrintStatus"
    - path: "app/src/main/java/works/mees/dinghy/ui/shell/AppShell.kt"
      provides: "Launcher nav callbacks (onNavigate(Dest)/onOpenDrawer) + bounded error-line projection passed to PrintStatusScreen"
      contains: "onNavigate"
    - path: "app/src/main/java/works/mees/dinghy/ui/temperature/TemperatureScreen.kt"
      provides: "PresetSelector promoted private -> internal (shared with PrintStatusScreen Preheat fallback)"
      contains: "internal fun PresetSelector"
    - path: "app/src/main/res/drawable/ic_babystep_compress.xml"
      provides: "distinct Compress glyph (nozzle closer)"
    - path: "app/src/main/res/drawable/ic_babystep_expand.xml"
      provides: "distinct Expand glyph (nozzle farther)"
  key_links:
    - from: "AppShell Dest.PrintStatus call site"
      to: "PrintStatusScreen(onNavigate, onOpenDrawer, errorLines)"
      via: "onNavigate(dest) -> navigateTo(dest); onOpenDrawer -> drawerOpen = true; errorLines = bounded <=3 projection of consoleHolder.state ERROR lines"
      pattern: "onNavigate|onOpenDrawer"
    - from: "PrintStatusScreen (Preheat handler)"
      to: "selectPreheatPath (16-02) + per-heater capability gate"
      via: "calls the pure fn, dispatches per-temp setHeater (capability-gated) on DirectTemps / opens internal PresetSelector on OpenSelector"
      pattern: "selectPreheatPath"
---

<objective>
Rework `PrintStatusScreen` into its definitive four-state form, routed off `classifyPrintStatus(state)`
instead of the single coarse `printing` boolean. Each mode (Standby / Printing / Paused / Terminal)
renders its own Focus/Field/Gutter by RECOMPOSING the existing harvested primitives (StatGrid,
PrintStatusFocus, StopButton, PrintStatusControlTile, ConfirmGuard, ProgressRing, PresetSelector,
DrawerTileSpec) — do NOT rewrite from scratch, do NOT touch the Views GraphView render path (SC-3).

The per-mode layout/control derivation is first extracted into a PURE `PrintStatusUiModel` (host-testable,
ADR-0001 toolkit-agnostic) so the big Compose edit has an automated gate BEFORE the 16-08 device pass.
The launcher nav, the spool-aware Preheat fallback, and the Terminal error lines all get their wiring
made concrete here (the three blockers Codex caught): AppShell threads `onNavigate(Dest)`/`onOpenDrawer`
+ a bounded error-line projection into the screen; `PresetSelector` is promoted to `internal`; per-temp
Preheat is gated on each heater's capability.

Purpose: the visual FOUNDATION the remaining feature phases slot into. Implements SC-1 (hierarchy +
forward entry points), SC-2 (portrait+landscape via Focus/Field/Gutter), the spool-aware Preheat (D-01),
the babystep row (SC-5 wiring), and the Terminal Dismiss/Reprint (D-05).

Output: NEW pure `PrintStatusUiModel.kt` (+ test) + reworked `PrintStatusScreen.kt` + AppShell wiring +
`PresetSelector` made internal + two distinct babystep glyphs. The live babystep round-trip (SC-5) and
perf no-regression (SC-3) are proven on-device in 16-08.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-home-print-status-redesign/16-UI-SPEC.md
@.planning/phases/16-home-print-status-redesign/16-PATTERNS.md
@.planning/phases/16-home-print-status-redesign/16-RESEARCH.md
@/mnt/e/claude/personal/github/parallel_dinghy/phase-16-home-status-redesign-staging.md
@docs/ui_design/LAYOUT.md
@docs/ui_design/CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure PrintStatusUiModel (mode -> layout/control derivation) + AppShell nav/error-line wiring</name>
  <files>app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusUiModel.kt, app/src/main/java/works/mees/dinghy/ui/shell/AppShell.kt, app/src/test/java/works/mees/dinghy/ui/printstatus/PrintStatusUiModelTest.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusMode.kt (the classifier + babystepVisible from 16-02 — the UI model consumes these)
    - app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusControlModel.kt (per-mode gutter control sets from 16-02 — the UI model assembles from these, does NOT re-derive them)
    - app/src/main/java/works/mees/dinghy/ui/shell/AppShell.kt (the Dest.PrintStatus call site @455-461 — onOpenFiles/onOpenSpool/onScanSpool today; navigateTo(Dest) @457; drawerOpen var @140 set true to open the swipe-up drawer; consoleHolder @312 with `consoleHolder.state: StateFlow<List<ConsoleLine>>`)
    - app/src/main/java/works/mees/dinghy/ui/route/TopRoute.kt (@37 enum Dest { PrintStatus, Temperature, Move, Extrude, Files, Macros, Console, Calibration, Webcam, Spool, ... } — the launcher targets)
    - app/src/main/java/works/mees/dinghy/ui/console/ConsoleSeverity.kt (@16 ERROR — the projection filter) + ConsoleLine.kt
    - 16-UI-SPEC.md (per-state Focus/Field/Gutter table; launcher order; terminal error-line contract <=3)
  </read_first>
  <behavior>
    - Test: uiModel(Standby) yields the Standby gutter set (Preheat + inert Power, NO EStop) and the curated launcher destination list (Files, Temperature, Move, Extrude, Calibration, [Spool if present], [Macros if bookmarked], Console, Drawer-flexible) in the fixed order
    - Test: uiModel(Printing) yields the Printing gutter (Pause + Cancel + EStop) and the Tune-flexible shortcut row (no Drawer tile mid-print)
    - Test: uiModel(Paused) yields Resume + Cancel, NO EStop
    - Test: uiModel(Terminal(Error)) flags the error-line area shown; Terminal(Complete/Cancelled) flags it hidden
    - Test: babystep replaces the shortcut row when babystepVisible is true (the model exposes which row is active)
  </behavior>
  <action>
    Create NEW pure `ui/printstatus/PrintStatusUiModel.kt` (plain Kotlin, NO `import androidx.compose`) that
    maps a `PrintStatusMode` (+ the small inputs: spoolmanPresent, hasBookmarkedMacros, babystepVisible,
    terminalKind) to a host-testable description of the per-mode surface: the ordered launcher destination
    list (Standby), the active gutter control set (reusing the 16-02 `PrintStatusControlModel` sets — do NOT
    re-derive them), which Field row is active (shortcut vs babystep), and whether the Terminal error-line
    area shows. This is the seam the 942-line Compose screen renders FROM, so the mode→layout/control logic
    is unit-tested before the big edit (ADR-0001 toolkit-agnostic). Write `PrintStatusUiModelTest` covering
    the behaviors above. Then wire `AppShell.kt`: at the `Dest.PrintStatus` call site @455, replace the
    three single-purpose callbacks with ONE clean launcher shape — `onNavigate: (Dest) -> Unit = {
    navigateTo(it) }` and `onOpenDrawer: () -> Unit = { drawerOpen = true }` (keep `onScanSpool` as-is for
    the active-spool Scan; Files/Spool become `onNavigate(Dest.Files)`/`onNavigate(Dest.Spool)`). ALSO pass
    `errorLines: List<String>` = a bounded projection (`consoleHolder.state` collected, filtered to
    `ConsoleSeverity.ERROR`, `takeLast(3)`, mapped to `rawMessage`) — this is the Terminal(Error) data path
    (FIX 6 decision: AppShell projects a bounded error-line list; PrintStatusScreen does NOT reach into the
    store itself). The `PrintStatusScreen` signature gains `onNavigate`, `onOpenDrawer`, `errorLines`
    params; this task updates the call site + the signature stub (the bodies consuming them land in Tasks 2-4).
  </action>
  <verify>
    <automated>cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --tests 'works.mees.dinghy.ui.printstatus.PrintStatusUiModelTest'" 2>&1 | tr -d '\r' | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `PrintStatusUiModel.kt` has NO `import androidx.compose` (pure) and `PrintStatusUiModelTest` exits 0 (GREEN) covering all four modes' gutter sets + launcher order + babystep-row + terminal-error-area flags.
    - AppShell's `Dest.PrintStatus` call passes `onNavigate(Dest)` + `onOpenDrawer` (drawerOpen=true) + a bounded `errorLines` (<=3, ConsoleSeverity.ERROR, from consoleHolder.state) — grep the call site.
    - The `PrintStatusScreen` signature declares `onNavigate`, `onOpenDrawer`, `errorLines` — grep.
    - `:app:assembleDebug` SUCCESSFUL (signature stub compiles even before Tasks 2-4 fill the bodies).
  </acceptance_criteria>
  <done>Pure PrintStatusUiModel + test GREEN; AppShell threads onNavigate/onOpenDrawer + bounded error-line projection; screen signature extended; assembleDebug green.</done>
</task>

<task type="auto">
  <name>Task 2: Route the screen off PrintStatusMode — Standby + Printing modes (launcher dispatches real Dests)</name>
  <files>app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusScreen.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusScreen.kt (self — harvest: PrintStatusFocus @350, StatGrid @441, IconTwoRowCell @700, IconValueCell @732, StopButton @828, PrintStatusControlTile @761, controlColor @810, LastJobCard @509; the coarse `printing` boolean @220,357 being replaced; spool resolution @133-155; the new onNavigate/onOpenDrawer/errorLines params from Task 1)
    - app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusMode.kt (the classifier from 16-02) + PrintStatusUiModel.kt (from Task 1 — render FROM it)
    - app/src/main/java/works/mees/dinghy/ui/printstatus/GlanceSensor.kt (selectGlanceSensor from 16-04 — the Standby glance temp)
    - app/src/main/java/works/mees/dinghy/ui/route/TopRoute.kt (@37 the Dest targets each launcher tile dispatches to via onNavigate)
    - 16-UI-SPEC.md (Standby + Printing per-state contract, Focus/Field/Gutter table, button-intent table, launcher order, fsSp ramp, accent-temperature resolution)
    - staging note §Standby, §Printing
  </read_first>
  <action>
    Replace the coarse `printing` boolean routing with `when (classifyPrintStatus(state))`, rendering from
    the `PrintStatusUiModel` (Task 1). Build the Standby mode: Focus = app-icon base + a minimal centered
    glance overlay (Nozzle temp · Bed temp · glance temp from `selectGlanceSensor(state.temperatureSensors)`
    — render the line ONLY if non-null, else omit · Active spool remaining, hidden when Spoolman
    unavailable; NO connection-state line); Field = the adaptive launcher grid in the fixed curated order
    (Files · Temperature · Move · Extrusion · Calibration · Spoolman[if available] · Macros[only if
    bookmarked macros exist] · Console[low-priority filler] · Drawer[always, flexible/growing tile per the
    interactive-grid flexible-tile rule]). EVERY launcher tile dispatches a real target: Files→
    `onNavigate(Dest.Files)`, Temperature→`onNavigate(Dest.Temperature)`, Move→`onNavigate(Dest.Move)`,
    Extrusion→`onNavigate(Dest.Extrude)`, Calibration→`onNavigate(Dest.Calibration)`, Spoolman→
    `onNavigate(Dest.Spool)`, Macros→`onNavigate(Dest.Macros)`, Console→`onNavigate(Dest.Console)`, Drawer→
    `onOpenDrawer()` — NO tile wired to a no-op `{}`. Gutter = Preheat (accent) + Power (red, INERT per
    D-04 — render as the drawer's red Power tile, no-op), NO E-Stop. Forward stubs do NOT appear in the
    launcher (D-02 — drawer only). Build the Printing mode: Focus = preserve the `03-print-status.png`
    composition (thumbnail base + accent ProgressRing + numeric % inside ring + time-remaining hero +
    filename + printer subtitle) via the harvested `PrintStatusFocus`; Field = ONE framed StatGrid (Nozzle ·
    Bed · Elapsed/Remaining · Current layer/total[placeholder —] · Current Z · Applied Z offset[when
    non-zero OR in babystep window]) — nozzle/bed values in `seriesColor(0)/seriesColor(1)` (accent/pool,
    NOT amber) + the optional Spoolman print line (accent if available<required) + the shortcut row (Tune =
    flexible tile per the staging-note combination matrix; Tune is the P17 stub per D-03; no Drawer tile
    mid-print); Gutter = Pause (accent, immediate, no guard) · Cancel (red, ConfirmGuard) · E-Stop (red
    octagon StopButton, tap=ConfirmGuard/hold=immediate). Use the extended control sets + `controlColor`
    intents (Preheat=accent, Power=inert-stop) from 16-02. All sizing via `fsSp` + role tokens; do NOT add
    a second throttle or animate the ring fill (SC-3). Babystep row + Paused/Terminal land in Tasks 3/4.
  </action>
  <verify>
    <automated>cmd.exe /c "E:\Android\gw.bat :app:assembleDebug --no-daemon" 2>&1 | tr -d '\r' | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - The screen's top-level render dispatches on `classifyPrintStatus(state)` and renders from `PrintStatusUiModel` — grep `classifyPrintStatus` + the model usage in PrintStatusScreen.
    - EVERY Standby launcher tile dispatches a real `onNavigate(Dest.*)` or `onOpenDrawer()` — grep the launcher: NO tile bound to a bare `{}`/no-op (Files/Temperature/Move/Extrude/Calibration/Spool/Macros/Console + Drawer all wired).
    - Standby glance line uses `selectGlanceSensor(state.temperatureSensors)` and is omitted when null — grep.
    - Standby gutter = Preheat + inert Power, NO E-Stop — read the Standby composable.
    - Printing Field includes an Applied-Z-offset row sourced from `state.gcodeZOffset`; nozzle/bed use `seriesColor(0)`/`seriesColor(1)`, NOT `.heat`/amber — grep.
    - No second throttle / no animated ProgressRing fill introduced — grep clean.
    - `:app:assembleDebug` SUCCESSFUL.
  </acceptance_criteria>
  <done>Screen routes off the classifier/UI-model; Standby launcher dispatches real Dests (no no-op); Printing renders per the UI-SPEC; assembleDebug green.</done>
</task>

<task type="auto">
  <name>Task 3: Spool-aware Preheat (selectPreheatPath, capability-gated) + babystep 3-cell row + glyphs + PresetSelector internal</name>
  <files>app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusScreen.kt, app/src/main/java/works/mees/dinghy/ui/temperature/TemperatureScreen.kt, app/src/main/res/drawable/ic_babystep_compress.xml, app/src/main/res/drawable/ic_babystep_expand.xml</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/printstatus/Preheat.kt (the pure selectPreheatPath / PreheatPath from 16-02 — CALL it, do NOT re-implement the decision inline)
    - app/src/main/java/works/mees/dinghy/ui/temperature/TemperatureScreen.kt (PresetSelector @405 — currently `private fun PresetSelector(inFlight: Set<String>, onPreset: (PrinterCommands.Preset) -> Unit, onDismiss: () -> Unit)`; applyPreset dispatch @221)
    - app/src/main/java/works/mees/dinghy/command/CommandRegistry.kt (setHeater @316 SetHeaterArgs gated ObjectPresent("extruder"); applyPreset @323 gated ObjectPresent("heater_bed"); the availability predicates — how a command's capability is checked before dispatch; babystepZ + BabystepArgs from 16-03)
    - app/src/main/java/works/mees/dinghy/spool/SpoolmanModels.kt (settingsExtruderTemp @64 / settingsBedTemp @65 — both Int?, A2 VERIFIED)
    - app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusMode.kt (babystepVisible + nextBabystepStep from 16-02)
    - app/src/main/res/drawable/ic_status_octagon.xml (vector-glyph drawable convention to mirror)
    - 16-UI-SPEC.md (Preheat spool-aware section; Babystep row section + Accessibility contentDescription contract; Color intents: babystep = accent)
    - 16-RESEARCH.md §5 Pitfall 5 (never pass 0); Pattern 4 (babystep wiring)
  </read_first>
  <action>
    FIRST promote `PresetSelector` in `TemperatureScreen.kt` from `private fun` to `internal fun` (same
    Gradle module — PrintStatusScreen can then reuse it; do NOT change its signature/behavior, just the
    visibility) so the Preheat OpenSelector fallback compiles. Then implement spool-aware Preheat (D-01) by
    CALLING the pure `selectPreheatPath` fn from 16-02 — do NOT inline the decision in the composable. On
    the Standby Preheat tap, read `spoolDetail?.filament?.settingsExtruderTemp` (noz) and `settingsBedTemp`
    (bed) from the already-resolved spool, then call `selectPreheatPath(spoolmanPresent, noz, bed)` and
    switch on the result: on `PreheatPath.DirectTemps`, dispatch per-temp setHeater ONLY for the non-null
    temps the result carries AND ONLY when that heater is available — gate the nozzle dispatch on the
    `extruder` capability and the bed dispatch on the `heater_bed` capability (read how command availability
    is resolved today — e.g. `capabilities.hasObject("heater_bed")` / the registry availability predicate;
    do NOT route a bed temp through an extruder-gated path). Never `applyPreset` with a 0, never pass 0 for
    a missing temp (Pitfall 5). On `PreheatPath.OpenSelector`, open the now-`internal` Phase-5
    `PresetSelector` (fixed PLA/PETG/ABS/TPU) as the fallback. The composable does NOT re-derive the
    direct-vs-selector branch — that logic is owned by `selectPreheatPath` (unit-gated by PreheatTest in
    16-02). Build the babystep 3-cell row that REPLACES the Printing shortcut row when
    `babystepVisible(babystepEnabled, state.currentLayer, babystepLayers)` is true (collect
    `babystepEnabled`/`babystepLayers` from the container flows): `[Compress] [step value] [Expand]`, exempt
    from the flexible-tile rule. Compress dispatches `babystepZ(BabystepArgs(-step))` (nozzle closer), Expand
    dispatches `babystepZ(BabystepArgs(+step))` (farther); center cell shows the step value only and tap
    cycles via `nextBabystepStep`. Both controls are accent-outline, icon-only (no text labels), each
    carrying its verbatim `contentDescription` from the UI-SPEC Accessibility contract. The applied Z offset
    lives in the StatGrid (Task 2), NOT in the row. Author two DISTINCT vector glyphs
    `ic_babystep_compress.xml` / `ic_babystep_expand.xml` (distinct silhouettes — icon-never-twice rule),
    mirroring the `ic_status_octagon.xml` drawable convention.
  </action>
  <verify>
    <automated>cmd.exe /c "E:\Android\gw.bat :app:assembleDebug --no-daemon" 2>&1 | tr -d '\r' | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `PresetSelector` in TemperatureScreen.kt is `internal fun` (grep `internal fun PresetSelector`) and PrintStatusScreen's OpenSelector path calls it — grep the Preheat handler.
    - The Preheat handler CALLS `selectPreheatPath(...)` and dispatches per-temp `setHeater` on `DirectTemps` — grep `selectPreheatPath`; no inline direct-vs-selector `if` re-deriving the decision.
    - Each per-temp dispatch is capability-gated: the nozzle dispatch is guarded on extruder availability, the bed dispatch on `heater_bed` availability — read the handler (a bed temp is NEVER dispatched when heater_bed is absent).
    - No path passes 0 for a missing temp and `applyPreset` is never called with a 0 — read the Preheat handler.
    - Compress fires `babystepZ(BabystepArgs(<negative step>))`, Expand `<positive step>`; center tap cycles the step — grep.
    - The babystep row is gated on `babystepVisible(...)` and replaces the shortcut row in-window — read the Printing Field.
    - Two distinct babystep drawables exist; Compress/Expand carry the verbatim UI-SPEC contentDescription strings — grep.
    - `:app:assembleDebug` SUCCESSFUL.
  </acceptance_criteria>
  <done>PresetSelector made internal; Preheat dispatch calls selectPreheatPath with per-heater capability gating (never 0); babystep row + distinct glyphs implemented; assembleDebug green.</done>
</task>

<task type="auto">
  <name>Task 4: Paused + Terminal modes (Dismiss / Reprint, projected error lines)</name>
  <files>app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusScreen.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusScreen.kt (PrintStatusFocus to wrap/dim; StatGrid stats frame; LastJobCard hero treatment @509; the `errorLines: List<String>` param from Task 1)
    - app/src/main/java/works/mees/dinghy/command/CommandRegistry.kt (dismissPrint from 16-03; printStart @187 PrintStartArgs for Reprint)
    - app/src/main/java/works/mees/dinghy/designsystem/ (ConfirmGuard; SeverityToast for Dismiss failure)
    - 16-UI-SPEC.md (Paused + Terminal per-state contract; Dismiss=neutral, Reprint=accent no-guard; error <=3 lines; Copywriting contract)
    - staging note §Paused, §Terminal
    - 16-RESEARCH.md Open Question 1 (StatGrid as the Terminal stats frame — recommended)
  </read_first>
  <action>
    Build Paused: Focus = the Printing focus DIMMED with a static pause-icon overlay
    (contentDescription "Print paused"); Field/toolset identical to Printing (no extra pause-status row);
    babystep replacement still applies if in-window; Gutter = Resume (go/green) · Cancel (red, ConfirmGuard),
    NO E-Stop. Build Terminal (Complete/Cancelled/Error — Moonraker-derived, passive per D-05, no
    app-remembered state): Focus = clean result hero (preview/thumbnail, NO ring/dim/result-icon overlay;
    app-icon fallback); Field = a single framed StatGrid reusing the Printing stats frame with live-only
    fields omitted (use the em-dash `—` placeholder for stable missing core stats); for `Terminal(Error)`
    ONLY, append the `errorLines` param (the bounded ≤3 projection AppShell passes in from
    `consoleHolder.state` ERROR lines — Task 1) — hide the area if `errorLines` is empty; no special
    stop/error chrome beyond the lines; Gutter = Dismiss · Reprint. Dismiss dispatches `dismissPrint`
    (`SDCARD_RESET_FILE`), may optimistically clear the local terminal presentation, surfaces a
    SeverityToast on failure (do not silently lose context), neutral intent (clears, does not discard
    input); allowed after error; hot heaters do not block it. Reprint dispatches the existing
    `printStart(PrintStartArgs(<Moonraker current/last file path>))` directly — NO ConfirmGuard, does NOT
    SDCARD_RESET_FILE first, available when Moonraker exposes a usable file path; command/state feedback
    handles a not-ready printer. Use the exact ConfirmGuard copy + button labels from the UI-SPEC Copywriting
    contract for Cancel/E-Stop. Render error lines as plain text (no markup execution).
  </action>
  <verify>
    <automated>cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --tests 'works.mees.dinghy.ui.printstatus.*'" 2>&1 | tr -d '\r' | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Paused gutter = Resume + Cancel, NO E-Stop; Focus is dimmed with a pause overlay (contentDescription "Print paused") — read the Paused composable.
    - Terminal Dismiss dispatches `dismissPrint`; Reprint dispatches `printStart` with NO ConfirmGuard and no SDCARD_RESET_FILE-first — grep.
    - `Terminal(Error)` renders the passed-in `errorLines` (≤3) and hides the area when empty; Complete/Cancelled have no error chrome — read the Terminal composable (the screen does NOT reach into the store — it uses the AppShell-projected `errorLines`).
    - Missing core stats use the em-dash `—` placeholder (stable rows) — grep.
    - `ui.printstatus.*` host tests GREEN (incl. PrintStatusUiModelTest); `:app:assembleDebug` builds.
  </acceptance_criteria>
  <done>Paused + Terminal modes complete; Dismiss/Reprint wired; Terminal(Error) shows the AppShell-projected error lines; ui.printstatus tests GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app → Moonraker (gutter/row actions) | Preheat/Pause/Cancel/E-Stop/Babystep/Dismiss/Reprint dispatch gcode/JSON-RPC to the printer. |
| Moonraker → app (terminal error lines) | printer/console history strings projected (bounded <=3, AppShell) and rendered as the Terminal(Error) lines. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-06-01 | Tampering (V5 Input Validation) | babystep dispatch | mitigate | Step comes only from the fixed-cycle `nextBabystepStep`; sign is code-fixed (Compress=−/Expand=+); dispatched via the V5-validated `babystepZ` spec (16-03). Gated to the early-layer window; session-only. |
| T-16-06-02 | Tampering (V5) | Preheat / Reprint / Dismiss dispatch | mitigate | Preheat path decided by the unit-gated `selectPreheatPath`; temps come from typed `Int?` spool fields (per-temp `setHeater`, never 0), EACH gated on that heater's capability (no bed temp to an absent heater_bed); Reprint uses the Moonraker-provided file path through the existing `printStart` spec; Dismiss is the fixed `SDCARD_RESET_FILE` const — no free-text concatenation anywhere. |
| T-16-06-03 | Tampering (physical motion) | E-Stop / Cancel / babystep MOVE=1 | mitigate | E-Stop + Cancel route through the full-screen ConfirmGuard (E-Stop tap=confirm/hold=immediate); babystep `MOVE=1` is a small Z jog gated to the early-layer window, session-only, no SAVE_CONFIG. |
| T-16-06-04 | Injection (terminal error lines) | Terminal(Error) display | mitigate | Error lines are projected (bounded ≤3, AppShell) and rendered as plain text (no markup execution); source = the existing classified ConsoleLine ERROR lines, never interpolated into a command. |
| T-16-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase. |
</threat_model>

<verification>
- `ui.printstatus.*` host tests GREEN (incl. PrintStatusUiModelTest); `:app:assembleDebug` SUCCESSFUL.
- All four modes render their Focus/Field/Gutter; every launcher tile dispatches a real Dest; no second throttle; Views GraphView path untouched (SC-3 measured in 16-08).
- Tokens/fsSp clean; nozzle/bed = seriesColor (not amber); Preheat decision routed through selectPreheatPath (not inlined) with per-heater capability gating; PresetSelector reused as internal; Terminal error lines from the AppShell projection.
</verification>

<success_criteria>
The home screen is the definitive four-state surface routed off the classifier + a unit-tested
PrintStatusUiModel, recomposed from existing primitives, with launcher nav wired to real Dests, spool-aware
capability-gated Preheat (via the unit-gated selectPreheatPath + the now-internal PresetSelector), the
babystep row, and Terminal Dismiss/Reprint with AppShell-projected error lines all wired.
</success_criteria>

<output>
Create `.planning/phases/16-home-print-status-redesign/16-06-SUMMARY.md` when done.
</output>
