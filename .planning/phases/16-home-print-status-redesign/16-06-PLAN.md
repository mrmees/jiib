---
phase: 16-home-print-status-redesign
plan: 06
type: execute
wave: 3
depends_on: ["16-02", "16-03", "16-04", "16-05"]
files_modified:
  - app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusScreen.kt
  - app/src/main/res/drawable/ic_babystep_compress.xml
  - app/src/main/res/drawable/ic_babystep_expand.xml
autonomous: true
requirements: [SC-1, SC-2, SC-4, SC-5, D-01, D-02, D-03, D-04, D-05, "four-state home (refines SHELL-* / JOB-*)"]
user_setup: []

must_haves:
  truths:
    - "The home screen routes off classifyPrintStatus(state) — Standby / Printing / Paused / Terminal each render their own Focus/Field/Gutter"
    - "Standby shows the app-icon Focus + minimal glance overlay + launcher grid (Drawer = flexible tile) + Preheat/Power(inert) gutter, no E-Stop"
    - "Preheat is spool-aware via selectPreheatPath: per-temp setHeater on DirectTemps (never 0); PresetSelector on OpenSelector"
    - "Printing shows the 03-print-status composition + framed stats (incl. Applied Z offset) + shortcut row (Tune flexible) + Pause/Cancel/E-Stop gutter"
    - "Babystep 3-cell row (Compress/step/Expand) replaces the shortcut row inside the early-layer window; hidden when layer data unavailable"
    - "Paused = dimmed Printing focus + pause overlay + Resume/Cancel gutter; Terminal = clean hero + stats frame + Dismiss/Reprint (error appends <=3 lines)"
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusScreen.kt"
      provides: "Four-mode state-driven home routed off PrintStatusMode"
      contains: "classifyPrintStatus"
    - path: "app/src/main/res/drawable/ic_babystep_compress.xml"
      provides: "distinct Compress glyph (nozzle closer)"
    - path: "app/src/main/res/drawable/ic_babystep_expand.xml"
      provides: "distinct Expand glyph (nozzle farther)"
  key_links:
    - from: "PrintStatusScreen"
      to: "CommandRegistry.babystepZ / setHeater / dismissPrint / printStart"
      via: "dispatcher.dispatch from gutter/row actions"
      pattern: "babystepZ|setHeater|dismissPrint"
    - from: "PrintStatusScreen (Preheat handler)"
      to: "selectPreheatPath (16-02)"
      via: "calls the pure fn, dispatches per-temp setHeater on DirectTemps / opens PresetSelector on OpenSelector"
      pattern: "selectPreheatPath"
---

<objective>
Rework `PrintStatusScreen` into its definitive four-state form, routed off `classifyPrintStatus(state)`
instead of the single coarse `printing` boolean. Each mode (Standby / Printing / Paused / Terminal)
renders its own Focus/Field/Gutter by RECOMPOSING the existing harvested primitives (StatGrid,
PrintStatusFocus, StopButton, PrintStatusControlTile, ConfirmGuard, ProgressRing, PresetSelector,
DrawerTileSpec) — do NOT rewrite from scratch, do NOT touch the Views GraphView render path (SC-3).

Purpose: the visual FOUNDATION the remaining feature phases slot into. Implements SC-1 (hierarchy +
forward entry points), SC-2 (portrait+landscape via Focus/Field/Gutter), the spool-aware Preheat (D-01),
the babystep row (SC-5 wiring), and the Terminal Dismiss/Reprint (D-05).

Output: reworked `PrintStatusScreen.kt` + two distinct babystep glyphs. The live babystep round-trip
(SC-5) and perf no-regression (SC-3) are proven on-device in 16-08.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-home-print-status-redesign/16-UI-SPEC.md
@.planning/phases/16-home-print-status-redesign/16-PATTERNS.md
@.planning/phases/16-home-print-status-redesign/16-RESEARCH.md
@/mnt/e/claude/personal/github/parallel_dinghy/phase-16-home-status-redesign-staging.md
@docs/ui_design/LAYOUT.md
@docs/ui_design/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Route the screen off PrintStatusMode — Standby + Printing modes</name>
  <files>app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusScreen.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusScreen.kt (self — harvest: PrintStatusFocus @350, StatGrid @441, IconTwoRowCell @700, IconValueCell @732, StopButton @828, PrintStatusControlTile @761, controlColor @810, LastJobCard @509; the coarse `printing` boolean @220,357 being replaced; spool resolution @133-155)
    - app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusMode.kt (the classifier from 16-02)
    - app/src/main/java/works/mees/dinghy/ui/shell/AppDrawer.kt (Drawer = the Standby launcher flexible/growing tile)
    - 16-UI-SPEC.md (Standby + Printing per-state contract, Focus/Field/Gutter table, button-intent table, launcher order, fsSp ramp, accent-temperature resolution)
    - staging note §Standby, §Printing
  </read_first>
  <action>
    Replace the coarse `printing` boolean routing with `when (classifyPrintStatus(state))`. Build the
    Standby mode: Focus = app-icon base + a minimal centered glance overlay (Nozzle temp · Bed temp ·
    glance MCU/host temp from `glanceSensorTemp` IF non-null else omit the line · Active spool remaining,
    hidden when Spoolman unavailable; NO connection-state line); Field = the adaptive launcher grid in the
    fixed curated order (Files · Temperature · Move · Extrusion · Calibration · Spoolman[if available] ·
    Macros[only if bookmarked macros exist] · Console[low-priority filler] · Drawer[always, flexible/growing
    tile per the interactive-grid flexible-tile rule]); Gutter = Preheat (accent) + Power (red, INERT per
    D-04 — render as the drawer's red Power tile, no-op), NO E-Stop. Forward stubs do NOT appear in the
    launcher (D-02 — drawer only). Build the Printing mode: Focus = preserve the `03-print-status.png`
    composition (thumbnail base + accent ProgressRing + numeric % inside ring + time-remaining hero +
    filename + printer subtitle) via the harvested `PrintStatusFocus`; Field = ONE framed StatGrid (Nozzle ·
    Bed · Elapsed/Remaining · Current layer/total[placeholder —] · Current Z · Applied Z offset[when
    non-zero OR in babystep window]) — nozzle/bed values in `seriesColor(0)/seriesColor(1)` (accent/pool,
    NOT amber) + the optional Spoolman print line (accent if available<required) + the shortcut row (Tune =
    flexible tile per the staging-note combination matrix; Tune is the P17 stub per D-03; no Drawer tile
    mid-print); Gutter = Pause (accent, immediate, no guard) · Cancel (red, ConfirmGuard) · E-Stop (red
    octagon StopButton, tap=ConfirmGuard/hold=immediate). Use the extended control sets + `controlColor`
    intents (Preheat=accent, Power=inert-stop) from 16-02. All sizing via `fsSp` + role tokens; do NOT add
    a second throttle or animate the ring fill (SC-3). Babystep row + Paused/Terminal land in Tasks 2/3.
  </action>
  <verify>
    <automated>cmd.exe /c "E:\Android\gw.bat :app:assembleDebug --no-daemon" 2>&1 | tr -d '\r' | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - The screen's top-level render dispatches on `classifyPrintStatus(state)` — grep `classifyPrintStatus` in PrintStatusScreen.
    - Standby gutter = Preheat + inert Power, NO E-Stop; Standby launcher uses the fixed curated order with Drawer as the flexible tile — read the Standby composable.
    - Printing Field includes an Applied-Z-offset row sourced from `state.gcodeZOffset` — grep.
    - Nozzle/bed readouts use `seriesColor(0)`/`seriesColor(1)`, NOT `.heat`/amber — grep.
    - No second throttle / no animated ProgressRing fill introduced — grep clean.
    - `:app:assembleDebug` SUCCESSFUL.
  </acceptance_criteria>
  <done>Screen routes off the classifier; Standby + Printing modes render per the UI-SPEC; assembleDebug green.</done>
</task>

<task type="auto">
  <name>Task 2: Spool-aware Preheat (calls selectPreheatPath) + babystep 3-cell row + glyphs</name>
  <files>app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusScreen.kt, app/src/main/res/drawable/ic_babystep_compress.xml, app/src/main/res/drawable/ic_babystep_expand.xml</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/printstatus/Preheat.kt (the pure selectPreheatPath / PreheatPath from 16-02 — CALL it, do NOT re-implement the decision inline)
    - app/src/main/java/works/mees/dinghy/ui/temperature/TemperatureScreen.kt (PresetSelector @405; applyPreset dispatch @221)
    - app/src/main/java/works/mees/dinghy/spool/SpoolmanModels.kt (settingsExtruderTemp @64 / settingsBedTemp @65 — both Int?, A2 VERIFIED)
    - app/src/main/java/works/mees/dinghy/command/CommandRegistry.kt (setHeater @316 SetHeaterArgs; babystepZ + BabystepArgs from 16-03)
    - app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusMode.kt (babystepVisible + nextBabystepStep from 16-02)
    - app/src/main/res/drawable/ic_status_octagon.xml (vector-glyph drawable convention to mirror)
    - 16-UI-SPEC.md (Preheat spool-aware section; Babystep row section + Accessibility contentDescription contract; Color intents: babystep = accent)
    - 16-RESEARCH.md §5 Pitfall 5 (never pass 0); Pattern 4 (babystep wiring)
  </read_first>
  <action>
    Implement spool-aware Preheat (D-01) by CALLING the pure `selectPreheatPath` fn from 16-02 — do NOT
    inline the decision in the composable. On the Standby Preheat tap, read `spoolDetail?.filament?.
    settingsExtruderTemp` (noz) and `settingsBedTemp` (bed) from the already-resolved spool, then call
    `selectPreheatPath(spoolmanPresent, noz, bed)` and switch on the result: on `PreheatPath.DirectTemps`,
    dispatch per-temp `CommandRegistry.setHeater(SetHeaterArgs("extruder", nozzle, "preheat_noz"))` /
    `("heater_bed", bed, "preheat_bed"))` ONLY for the non-null temps the result carries (never `applyPreset`
    with a 0, never pass 0 for a missing temp — Pitfall 5); on `PreheatPath.OpenSelector`, open the Phase-5
    `PresetSelector` (fixed PLA/PETG/ABS/TPU) as the fallback. The composable does NOT re-derive the
    direct-vs-selector branch — that logic is owned by `selectPreheatPath` (unit-gated by PreheatTest in
    16-02). Build the babystep 3-cell row that REPLACES the Printing shortcut row when
    `babystepVisible(babystepEnabled, state.currentLayer, babystepLayers)` is true (collect
    `babystepEnabled`/`babystepLayers` from the container flows): `[Compress] [step value] [Expand]`, exempt
    from the flexible-tile rule. Compress dispatches `babystepZ(BabystepArgs(-step))` (nozzle closer), Expand
    dispatches `babystepZ(BabystepArgs(+step))` (farther); center cell shows the step value only and tap
    cycles via `nextBabystepStep`. Both controls are accent-outline, icon-only (no text labels), each
    carrying its verbatim `contentDescription` from the UI-SPEC Accessibility contract. The applied Z offset
    lives in the StatGrid (Task 1), NOT in the row. Author two DISTINCT vector glyphs
    `ic_babystep_compress.xml` / `ic_babystep_expand.xml` (distinct silhouettes — icon-never-twice rule),
    mirroring the `ic_status_octagon.xml` drawable convention.
  </action>
  <verify>
    <automated>cmd.exe /c "E:\Android\gw.bat :app:assembleDebug --no-daemon" 2>&1 | tr -d '\r' | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - The Preheat handler CALLS `selectPreheatPath(...)` and dispatches per-temp `setHeater` on `DirectTemps` (only non-null temps) / opens `PresetSelector` on `OpenSelector` — grep `selectPreheatPath` in PrintStatusScreen; no inline direct-vs-selector `if` re-deriving the decision.
    - No path passes 0 for a missing temp and `applyPreset` is never called with a 0 — read the Preheat handler.
    - Compress fires `babystepZ(BabystepArgs(<negative step>))`, Expand `<positive step>`; center tap cycles the step — grep.
    - The babystep row is gated on `babystepVisible(...)` and replaces the shortcut row in-window — read the Printing Field.
    - Two distinct babystep drawables exist; Compress/Expand carry the verbatim UI-SPEC contentDescription strings — grep.
    - `:app:assembleDebug` SUCCESSFUL.
  </acceptance_criteria>
  <done>Preheat dispatch calls the unit-gated selectPreheatPath (per-temp setHeater, never 0) + babystep row + distinct glyphs implemented; assembleDebug green.</done>
</task>

<task type="auto">
  <name>Task 3: Paused + Terminal modes (Dismiss / Reprint, error lines)</name>
  <files>app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusScreen.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusScreen.kt (PrintStatusFocus to wrap/dim; StatGrid stats frame; LastJobCard hero treatment @509)
    - app/src/main/java/works/mees/dinghy/command/CommandRegistry.kt (dismissPrint from 16-03; printStart @187 PrintStartArgs for Reprint)
    - app/src/main/java/works/mees/dinghy/designsystem/ (ConfirmGuard; SeverityToast for Dismiss failure)
    - 16-UI-SPEC.md (Paused + Terminal per-state contract; Dismiss=neutral, Reprint=accent no-guard; error <=3 lines; Copywriting contract)
    - staging note §Paused, §Terminal
    - 16-RESEARCH.md Open Question 1 (StatGrid as the Terminal stats frame — recommended)
  </read_first>
  <action>
    Build Paused: Focus = the Printing focus DIMMED with a static pause-icon overlay
    (contentDescription "Print paused"); Field/toolset identical to Printing (no extra pause-status row);
    babystep replacement still applies if in-window; Gutter = Resume (go/green) · Cancel (red, ConfirmGuard),
    NO E-Stop. Build Terminal (Complete/Cancelled/Error — Moonraker-derived, passive per D-05, no
    app-remembered state): Focus = clean result hero (preview/thumbnail, NO ring/dim/result-icon overlay;
    app-icon fallback); Field = a single framed StatGrid reusing the Printing stats frame with live-only
    fields omitted (use the em-dash `—` placeholder for stable missing core stats); for `Terminal(Error)`
    ONLY, append the last ≤3 meaningful printer error lines from console/printer history (hide the area if
    none) — no special stop/error chrome beyond the lines; Gutter = Dismiss · Reprint. Dismiss dispatches
    `dismissPrint` (`SDCARD_RESET_FILE`), may optimistically clear the local terminal presentation, surfaces
    a SeverityToast on failure (do not silently lose context), neutral intent (clears, does not discard
    input); allowed after error; hot heaters do not block it. Reprint dispatches the existing
    `printStart(PrintStartArgs(<Moonraker current/last file path>))` directly — NO ConfirmGuard, does NOT
    SDCARD_RESET_FILE first, available when Moonraker exposes a usable file path; command/state feedback
    handles a not-ready printer. Use the exact ConfirmGuard copy + button labels from the UI-SPEC Copywriting
    contract for Cancel/E-Stop.
  </action>
  <verify>
    <automated>cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --tests 'works.mees.dinghy.ui.printstatus.*'" 2>&1 | tr -d '\r' | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Paused gutter = Resume + Cancel, NO E-Stop; Focus is dimmed with a pause overlay (contentDescription "Print paused") — read the Paused composable.
    - Terminal Dismiss dispatches `dismissPrint`; Reprint dispatches `printStart` with NO ConfirmGuard and no SDCARD_RESET_FILE-first — grep.
    - `Terminal(Error)` appends ≤3 error lines and hides the area when none; Complete/Cancelled have no error chrome — read the Terminal composable.
    - Missing core stats use the em-dash `—` placeholder (stable rows) — grep.
    - `ui.printstatus.*` host tests GREEN; `:app:assembleDebug` builds (covered by the prior tasks' gate).
  </acceptance_criteria>
  <done>Paused + Terminal modes complete; Dismiss/Reprint wired per the contract; ui.printstatus tests GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app → Moonraker (gutter/row actions) | Preheat/Pause/Cancel/E-Stop/Babystep/Dismiss/Reprint dispatch gcode/JSON-RPC to the printer. |
| Moonraker → app (terminal error lines) | printer/console history strings rendered as the Terminal(Error) lines. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-06-01 | Tampering (V5 Input Validation) | babystep dispatch | mitigate | Step comes only from the fixed-cycle `nextBabystepStep`; sign is code-fixed (Compress=−/Expand=+); dispatched via the V5-validated `babystepZ` spec (16-03). Gated to the early-layer window; session-only. |
| T-16-06-02 | Tampering (V5) | Preheat / Reprint / Dismiss dispatch | mitigate | Preheat path decided by the unit-gated `selectPreheatPath`; temps come from typed `Int?` spool fields (per-temp `setHeater`, never 0); Reprint uses the Moonraker-provided file path through the existing `printStart` spec; Dismiss is the fixed `SDCARD_RESET_FILE` const — no free-text concatenation anywhere. |
| T-16-06-03 | Tampering (physical motion) | E-Stop / Cancel / babystep MOVE=1 | mitigate | E-Stop + Cancel route through the full-screen ConfirmGuard (E-Stop tap=confirm/hold=immediate); babystep `MOVE=1` is a small Z jog gated to the early-layer window, session-only, no SAVE_CONFIG. |
| T-16-06-04 | Injection (terminal error lines) | Terminal(Error) display | mitigate | Error lines are rendered as plain text (no markup execution); ≤3 lines, source = the existing console/printer history (already-classified ConsoleLine), never interpolated into a command. |
| T-16-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase. |
</threat_model>

<verification>
- `ui.printstatus.*` host tests GREEN; `:app:assembleDebug` SUCCESSFUL.
- All four modes render their Focus/Field/Gutter; no second throttle; Views GraphView path untouched (SC-3 measured in 16-08).
- Tokens/fsSp clean; nozzle/bed = seriesColor (not amber); Preheat decision routed through selectPreheatPath (not inlined).
</verification>

<success_criteria>
The home screen is the definitive four-state surface routed off the classifier, recomposed from existing
primitives, with spool-aware Preheat (via the unit-gated selectPreheatPath), the babystep row, and
Terminal Dismiss/Reprint all wired.
</success_criteria>

<output>
Create `.planning/phases/16-home-print-status-redesign/16-06-SUMMARY.md` when done.
</output>
