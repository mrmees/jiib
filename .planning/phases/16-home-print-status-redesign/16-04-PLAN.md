---
phase: 16-home-print-status-redesign
plan: 04
type: execute
wave: 1
depends_on: ["16-01"]
files_modified:
  - app/src/main/java/works/mees/dinghy/state/PrinterState.kt
  - app/src/main/java/works/mees/dinghy/state/PrinterStateReducer.kt
  - app/src/main/java/works/mees/dinghy/state/DeriveCapabilities.kt
  - app/src/main/java/works/mees/dinghy/ui/printstatus/GlanceSensor.kt
  - app/src/test/java/works/mees/dinghy/state/PrinterStateReducerTest.kt
  - app/src/test/java/works/mees/dinghy/state/DeriveCapabilitiesTest.kt
  - app/src/test/java/works/mees/dinghy/ui/printstatus/GlanceSensorTest.kt
autonomous: true
requirements: [SC-5, "babystep readback (SC-5)", "standby glance temp (Claude discretion -> temperature_sensor)"]
user_setup: []

must_haves:
  truths:
    - "gcode_move.homing_origin[2] parses into PrinterState.gcodeZOffset (applied Z offset readback)"
    - "temperature_sensor readings accumulate into a RETAINED Map<String, Double> on PrinterState (retain-on-absent, update-on-present) — a partial diff that omits a sensor object does NOT drop its prior value"
    - "A pure selectGlanceSensor(sensors) derives the preferred glance sensor (stable MCU > host > first) from the retained map, never from the raw current diff"
    - "temperature_sensor objects are added to the subscribe set via a dynamic rule (mirrors heater_generic)"
    - "machine.proc_stats host-load is NOT pulled in (deferred to Phase 19)"
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/state/PrinterState.kt"
      provides: "gcodeZOffset + retained temperatureSensors map fields"
      contains: "gcodeZOffset"
    - path: "app/src/main/java/works/mees/dinghy/state/PrinterStateReducer.kt"
      provides: "homing_origin[2] parse + retained temperature_sensor accumulation"
      contains: "homing_origin"
    - path: "app/src/main/java/works/mees/dinghy/ui/printstatus/GlanceSensor.kt"
      provides: "pure selectGlanceSensor(sensors) -> GlanceSensor? (MCU > host > first, stable order)"
      contains: "fun selectGlanceSensor"
    - path: "app/src/main/java/works/mees/dinghy/state/DeriveCapabilities.kt"
      provides: "temperature_sensor dynamic subscribe rule"
  key_links:
    - from: "PrinterStateReducer (gcode_move block)"
      to: "PrinterState.gcodeZOffset"
      via: "gm.doubleListOrNull(homing_origin)[2]"
      pattern: "homing_origin"
    - from: "PrinterStateReducer (temperature_sensor walk)"
      to: "PrinterState.temperatureSensors (retained map)"
      via: "update-on-present, retain-on-absent merge into the prior map"
      pattern: "temperatureSensors"
    - from: "GlanceSensor.selectGlanceSensor"
      to: "PrinterState.temperatureSensors"
      via: "pure selection over the retained map (NOT the raw diff)"
      pattern: "selectGlanceSensor"
---

<objective>
Add the two net-new state sources Phase 16 needs: the applied Z-offset readback
(`gcode_move.homing_origin[2]` → `PrinterState.gcodeZOffset`, for the babystep applied-offset stat) and
the Standby glance temperature. The glance temp is modelled as **retained sensor state** — a
`Map<String, Double>` accumulated in the reducer (update-on-present, retain-on-absent, mirroring this
project's existing retained-object reducer pattern) — plus a **pure host-testable `selectGlanceSensor`**
that derives the preferred glance sensor from that map. This is the FIX for the partial-diff flip:
a single field selected from the current `notify_status_update` object can flip MCU→chamber/host or go
stale when only that object appears in a diff; the retained map + stable selection rule prevent it.

Purpose: babystep readback (SC-5) and the resolved glance-metric decision (real `temperature_sensor`
reading; host-load DEFERRED to Phase 19 — do NOT add proc_stats). Keeps P16's "no new backend" boundary.

Output: extended `PrinterState.kt` (gcodeZOffset + retained `temperatureSensors` map) +
`PrinterStateReducer.kt` (homing_origin line + retained temp_sensor accumulation) +
`DeriveCapabilities.kt` (temperature_sensor subscribe rule) + NEW pure `GlanceSensor.kt`
(`selectGlanceSensor`); reducer/derive/glance tests GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-home-print-status-redesign/16-RESEARCH.md
@.planning/phases/16-home-print-status-redesign/16-PATTERNS.md
@docs/moonraker-capabilities.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PrinterState + reducer — parse homing_origin[2] (applied Z offset)</name>
  <files>app/src/main/java/works/mees/dinghy/state/PrinterState.kt, app/src/main/java/works/mees/dinghy/state/PrinterStateReducer.kt, app/src/test/java/works/mees/dinghy/state/PrinterStateReducerTest.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/state/PrinterStateReducer.kt (gcode_move block @106-111; doubleListOrNull helper @252)
    - app/src/main/java/works/mees/dinghy/state/PrinterState.kt (existing nullable fields — currentLayer @75, gcodePosition @47)
    - app/src/test/java/works/mees/dinghy/state/PrinterStateReducerTest.kt (gcode_move parse tests; the Wave-0 RED case)
    - 16-RESEARCH.md Pattern 4 + Code Examples (the one reducer line) + Assumption A4 (homing_origin = [X,Y,Z,E], Z at index 2)
    - 16-PATTERNS.md (PrinterStateReducer + PrinterState section — exact line to add)
  </read_first>
  <behavior>
    - Test: notify_status_update with gcode_move.homing_origin = [0,0,-0.05,0] -> PrinterState.gcodeZOffset == -0.05
    - Test: a missing/short/non-numeric homing_origin -> gcodeZOffset null OR retains prior (matches doubleListOrNull null-safety)
  </behavior>
  <action>
    Add `val gcodeZOffset: Double? = null` to `PrinterState` (doc: "gcode_move.homing_origin[2] — applied
    Z offset (babystep); null until first reported"). Add ONE line inside the EXISTING `gcode_move` walk
    (@106-111): `gm.doubleListOrNull("homing_origin")?.let { s = s.copy(gcodeZOffset = it.getOrNull(2)) }`
    — reuse the existing `doubleListOrNull` helper verbatim. Turn the Wave-0 RED reducer case GREEN against
    the real field. (Z index = 2 is verified on device in 16-08; the reducer just reads index 2.)
  </action>
  <verify>
    <automated>cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --tests 'works.mees.dinghy.state.PrinterStateReducerTest'" 2>&1 | tr -d '\r' | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - PrinterStateReducerTest exits 0 (GREEN) including the homing_origin[2] case.
    - The parse is a single line inside the existing gcode_move block (not a new object walk) — read the diff.
    - `gcodeZOffset` is nullable with a doc comment.
  </acceptance_criteria>
  <done>gcodeZOffset field + one-line reducer parse added; reducer test GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Retained temperature_sensor map — subscribe rule + reducer accumulation + pure selectGlanceSensor</name>
  <files>app/src/main/java/works/mees/dinghy/state/DeriveCapabilities.kt, app/src/main/java/works/mees/dinghy/state/PrinterStateReducer.kt, app/src/main/java/works/mees/dinghy/state/PrinterState.kt, app/src/main/java/works/mees/dinghy/ui/printstatus/GlanceSensor.kt, app/src/test/java/works/mees/dinghy/state/DeriveCapabilitiesTest.kt, app/src/test/java/works/mees/dinghy/state/PrinterStateReducerTest.kt, app/src/test/java/works/mees/dinghy/ui/printstatus/GlanceSensorTest.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/state/DeriveCapabilities.kt (V1_SUBSCRIBE_CORE @50; deriveSubscribeSet dynamic loop; heater_generic clause)
    - app/src/main/java/works/mees/dinghy/state/PrinterStateReducer.kt (isHeaterObject @212 — EXCLUDES temperature_sensor, so this needs its own small walk; the existing retained-map / copy-prior merge pattern for accumulated objects — mirror it so an absent object retains its prior value)
    - app/src/main/java/works/mees/dinghy/state/PrinterState.kt (existing map/collection fields for the retained-Map shape to mirror)
    - app/src/test/java/works/mees/dinghy/ui/printstatus/ (PrintStatusControlModel test style — package/imports for the new GlanceSensorTest)
    - 16-RESEARCH.md §3 (glance metric rule: prefer *mcu* sensor, else *host*, else first temperature_sensor; OMIT if none; defer host-load) + Pitfall 4
    - 16-PATTERNS.md (DeriveCapabilities section — the new dynamic rule)
    - docs/moonraker-capabilities.md (@47/200 MCU/chamber temp sensors)
  </read_first>
  <behavior>
    - Test (subscribe): deriveSubscribeSet over an objects list containing "temperature_sensor mcu" includes it in the subscribe set
    - Test (retain-on-absent): reduce a diff carrying temperature_sensor mcu=45.0, THEN a diff that OMITS the temperature_sensor object entirely -> temperatureSensors["temperature_sensor mcu"] STILL == 45.0 (the prior value is retained, NOT dropped — this is the partial-diff fix)
    - Test (update-on-present): a later diff with temperature_sensor mcu=46.5 -> map value updates to 46.5
    - Test (no-sensor): a state with no temperature_sensor ever reported -> temperatureSensors is empty AND selectGlanceSensor(emptyMap) == null
    - Test (selection MCU>host>first): selectGlanceSensor({"temperature_sensor host"->50.0, "temperature_sensor mcu"->45.0}) prefers the mcu entry; with only host present, returns host; with neither mcu nor host, returns the first (stable iteration) — and the SAME input map always yields the SAME choice across calls (preference is stable, not diff-order-dependent)
  </behavior>
  <action>
    Add a dynamic subscribe rule in `deriveSubscribeSet`'s loop mirroring the `heater_generic` clause:
    `name.startsWith("temperature_sensor ") -> result += name`. Add a RETAINED PrinterState field
    `val temperatureSensors: Map<String, Double> = emptyMap()` (doc: "retained temperature_sensor
    name→temperature; accumulated across partial diffs — an absent object keeps its prior value"). In the
    reducer, add a small walk (SEPARATE from the heater loop — `isHeaterObject` @212 excludes
    temperature_sensor) that, for each `temperature_sensor <name>` object PRESENT in the current diff with a
    numeric `temperature`, does an UPDATE-ON-PRESENT merge into the prior map (`s.copy(temperatureSensors =
    s.temperatureSensors + (name to temp))`) — objects ABSENT from the diff are simply not touched, so their
    prior values are RETAINED (mirror the existing retained-object merge pattern; do NOT rebuild the map
    from only the current diff, which is the bug). Create NEW pure `ui/printstatus/GlanceSensor.kt` (plain
    Kotlin, no Compose) defining `data class GlanceSensor(val name: String, val temperature: Double)` and
    `fun selectGlanceSensor(sensors: Map<String, Double>): GlanceSensor?` encoding the stable preference:
    pick the entry whose name contains "mcu" (case-insensitive), else "host", else the first entry by a
    stable key order (e.g. `sensors.toSortedMap()` so iteration order is deterministic, NOT
    diff-arrival-order); return null when the map is empty. The Standby UI (16-06) calls `selectGlanceSensor`
    on `state.temperatureSensors` — the screen never re-derives the preference. Do NOT model
    `machine.proc_stats` / host-load — that is Phase 19 (Pitfall 4). Extend DeriveCapabilitiesTest
    (subscribe rule), PrinterStateReducerTest (retain-on-absent + update-on-present), and create
    GlanceSensorTest (selection MCU>host>first + stability + empty→null).
  </action>
  <verify>
    <automated>cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --tests 'works.mees.dinghy.state.DeriveCapabilitiesTest' --tests 'works.mees.dinghy.state.PrinterStateReducerTest' --tests 'works.mees.dinghy.ui.printstatus.GlanceSensorTest'" 2>&1 | tr -d '\r' | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - DeriveCapabilitiesTest asserts a `temperature_sensor X` object is added to the subscribe set.
    - PrinterStateReducerTest proves retain-on-absent (a diff omitting the sensor keeps the prior value) AND update-on-present — read the two assertions; this is the partial-diff fix.
    - `GlanceSensor.kt` has NO `import androidx.compose` (pure) and `selectGlanceSensor` returns null on empty + prefers mcu>host>first with STABLE ordering — grep + read the fn; GlanceSensorTest GREEN.
    - The screen-side glance selection reads `selectGlanceSensor(state.temperatureSensors)`, never the raw current diff — recorded as the 16-06 contract.
    - No `machine.proc_stats` / `proc_stat` / host-load subscribe appears anywhere — grep the diff clean (Pitfall 4).
  </acceptance_criteria>
  <done>temperature_sensor subscribe rule + RETAINED reducer map + pure selectGlanceSensor added; partial-diff retain proven by test; defer host-load honored; tests GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Moonraker → app (notify_status_update) | streaming printer state diffs deep-merged into PrinterState. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-04-V5 | Tampering (V5 Input Validation) | reducer parse of homing_origin / temperature_sensor | mitigate | All reads go through the existing null-safe `doubleListOrNull`/typed walks — malformed/short/non-numeric payloads yield null and are not merged, never a crash or injected value. Inbound printer data is parsed, never executed. |
| T-16-04-D | Denial of correctness (partial-diff staleness) | retained `temperatureSensors` map + `selectGlanceSensor` | mitigate | The glance temp is derived from a RETAINED map (update-on-present, retain-on-absent) via a pure stable-order selection — a partial diff cannot flip MCU→host or strand a stale value; unit-gated by GlanceSensorTest + the reducer retain-on-absent test. |
| T-16-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase. |
</threat_model>

<verification>
- `state.*` reducer + derive tests GREEN; `ui.printstatus.GlanceSensorTest` GREEN.
- homing_origin[2] readback works; glance temp is retained across partial diffs and selected MCU>host>first stably; no proc_stats pulled in.
</verification>

<success_criteria>
The two net-new state sources (applied Z offset, retained glance temperature) are parsed and host-tested
without pulling a new Moonraker surface into the phase; the partial-diff sensor-flip class is closed by a
retained map + a pure stable selector.
</success_criteria>

<output>
Create `.planning/phases/16-home-print-status-redesign/16-04-SUMMARY.md` when done.
</output>
