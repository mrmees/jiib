---
phase: 09-calibration-maintenance
plan: 02
type: execute
wave: 1
depends_on: ["09-01"]
files_modified:
  - app/src/main/java/works/mees/dinghy/command/PrinterCommands.kt
  - app/src/main/java/works/mees/dinghy/command/CommandRegistry.kt
  - app/src/main/java/works/mees/dinghy/state/DeriveCapabilities.kt
  - app/src/main/java/works/mees/dinghy/state/PrinterState.kt
  - app/src/main/java/works/mees/dinghy/state/PrinterStateReducer.kt
  - app/src/main/java/works/mees/dinghy/state/PrinterStateStore.kt
  - app/src/test/java/works/mees/dinghy/state/DeriveCapabilitiesTest.kt
  - app/src/test/java/works/mees/dinghy/state/PrinterStateReducerTest.kt
autonomous: true
requirements: [BEDM-01, BEDL-01, ZCAL-01, CALIB-02, CALIB-03, CALIB-04, CALIB-05]
user_setup: []

must_haves:
  truths:
    - "The five calibration objects subscribe ONLY when the connected printer reports them (A3 intersect-with-detected)"
    - "notify_status_update for bed_mesh/z_tilt/quad_gantry_level/screws_tilt_adjust/manual_probe merges into retained PrinterState fields, null-safe"
    - "Every calibration gcode command is registered with an ObjectPresent gating predicate and inherits the G4 120s timeout automatically"
    - "The [screws_tilt_adjust] config (coords/names) is read once at handshake and exposed as a StateFlow holders combine"
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/command/CommandRegistry.kt"
      provides: "Registered calibration CommandSpecs (screwsTiltCalculate, zTiltAdjust, quadGantryLevel, bedMeshCalibrate, bedMeshProfile*, probeCalibrate, zEndstopCalibrate, testZ, accept, abort, saveConfig)"
      contains: "ObjectPresent"
    - path: "app/src/main/java/works/mees/dinghy/state/PrinterState.kt"
      provides: "Nullable calibration object fields (screwsTilt, zTiltApplied, qglApplied, bedMesh, manualProbe)"
    - path: "app/src/main/java/works/mees/dinghy/state/PrinterStateReducer.kt"
      provides: "Null-safe reducer walks of the five new objects + a double2dListOrNull accessor"
    - path: "app/src/main/java/works/mees/dinghy/state/DeriveCapabilities.kt"
      provides: "The five objects added to V1_SUBSCRIBE_CORE"
  key_links:
    - from: "PrinterStateReducer.applyStatus"
      to: "PrinterState calibration fields"
      via: "status.objectOrNull(\"bed_mesh\") etc. null-safe walk"
      pattern: "objectOrNull\\(\"(bed_mesh|z_tilt|quad_gantry_level|screws_tilt_adjust|manual_probe)\""
    - from: "CommandRegistry calibration specs"
      to: "CommandDispatcher G4 120s timeout"
      via: "gcode(...) factory → method == GCODE_SCRIPT"
      pattern: "gcode\\(|GCODE_SCRIPT"
---

<objective>
Lay the spine plumbing every Phase-9 screen codes against: register the ~12 calibration commands, surface the
five calibration live objects through the reducer into retained PrinterState fields, add them to the subscribe
superset (intersected-with-detected, A3), and read the [screws_tilt_adjust] config once at handshake. This is
the load-bearing foundation — pure/headless, ADR-0001 toolkit-agnostic, no UI. Done from the structured live
objects (RESEARCH Pattern 1), NOT console parsing.

Purpose: holders + screens (09-03..09-06) consume these fields/commands/flows; without them every downstream
plan is blocked.
Output: extended command registry + PrinterState + reducer + DeriveCapabilities + store one-shot flow; GREEN
reducer/derive tests over the captured fixtures.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-calibration-maintenance/09-RESEARCH.md
@.planning/phases/09-calibration-maintenance/09-PATTERNS.md
@app/src/test/resources/fixtures/bed_mesh_e5.json
@app/src/test/resources/fixtures/configfile_screws_e5.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Register calibration commands + pure gcode builders</name>
  <files>app/src/main/java/works/mees/dinghy/command/PrinterCommands.kt, app/src/main/java/works/mees/dinghy/command/CommandRegistry.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/command/CommandRegistry.kt (the `gcode(...)` private factory ~lines 394-407, the `all` list ~lines 334-369, and the existing KGC-* gcode specs ~lines 237-303 as the exact pattern)
    - app/src/main/java/works/mees/dinghy/command/CommandSpec.kt:19-26 (the AvailabilityPredicate sealed interface — use ObjectPresent)
    - app/src/main/java/works/mees/dinghy/command/PrinterCommands.kt:14-66,88-149 (clamp-before-format discipline, the COOLDOWN/DISABLE_STEPPERS constant pattern, the Material preset block, scriptParams)
    - app/src/main/java/works/mees/dinghy/command/CommandDispatcher.kt:118-123,166-175 (proof the G4 120s timeout keys off method==GCODE_SCRIPT — do NOT add a new timeout path)
    - .planning/phases/09-calibration-maintenance/09-PATTERNS.md (§ CommandRegistry + § PrinterCommands — the predicate map and builder list)
    - .planning/phases/09-calibration-maintenance/09-UI-SPEC.md (§ Color — confirms which commands exist: SCREWS_TILT_CALCULATE, Z_TILT_ADJUST, QUAD_GANTRY_LEVEL, BED_MESH_CALIBRATE, BED_MESH_PROFILE SAVE/LOAD/REMOVE, PROBE_CALIBRATE, Z_ENDSTOP_CALIBRATE, TESTZ, ACCEPT, ABORT, SAVE_CONFIG)
  </read_first>
  <action>
    In PrinterCommands.kt add the constant action gcodes (no params) mirroring COOLDOWN/DISABLE_STEPPERS:
    SCREWS_TILT_CALCULATE, Z_TILT_ADJUST, QUAD_GANTRY_LEVEL, BED_MESH_CALIBRATE (bare — let KAMP/printer config
    defaults apply per RESEARCH Open-Q2; do NOT inject METHOD/ADAPTIVE), PROBE_CALIBRATE, Z_ENDSTOP_CALIBRATE,
    SAVE_CONFIG, ACCEPT, ABORT. Add two parameterized builders following clamp-before-format (ASVS V5):
    `testZ(step: Double): String` = "TESTZ Z=" + step.coerceIn(-MAX_TESTZ_MM, MAX_TESTZ_MM) with a named
    MAX_TESTZ_MM bound (per D-01 step presets 1/0.1/0.05/0.025 → bound ~5mm is ample); and the bed-mesh profile
    builders `bedMeshProfileSave/Load/Remove(name)` = "BED_MESH_PROFILE SAVE=$name" etc. The SAVE name DEFAULTS to
    the app-generated YY.MM.DD_HH.MM (D-10) BUT is keyboard-editable per the UI-SPEC owner carve-out — so it IS a
    user-controlled injection surface. The builders MUST validate the name against a strict allowlist before
    formatting: define `sanitizeProfileName(name)` accepting only `[A-Za-z0-9_.-]+` (no whitespace, no control
    chars, no newline — a newline would inject a second gcode line), rejecting blank/over-length; require()-fail or
    return a typed error rather than concatenating raw input. This is a pure host-testable function (add
    BedMeshProfileNameTest: a name with a newline / space / `;` / empty is rejected; a timestamp name passes).

    In CommandRegistry.kt add one `gcode(...)` CommandSpec per command via the existing private factory (which
    routes through scriptParams AND inherits the G4 120s timeout automatically — do NOT add timeout code):
    - screwsTiltCalculate → catalogId "KGC-SCREWS_TILT_CALCULATE", availability ObjectPresent("screws_tilt_adjust")
    - zTiltAdjust → "KGC-Z_TILT_ADJUST", ObjectPresent("z_tilt")
    - quadGantryLevel → "KGC-QUAD_GANTRY_LEVEL", ObjectPresent("quad_gantry_level") (built blind, gates itself off, D-02)
    - bedMeshCalibrate → "KGC-BED_MESH_CALIBRATE", ObjectPresent("bed_mesh")
    - bedMeshProfileSave/Load/Remove → "KGC-BED_MESH_PROFILE_{SAVE,LOAD,REMOVE}", ObjectPresent("bed_mesh"), param = name
    - probeCalibrate → "KGC-PROBE_CALIBRATE", ObjectPresent("probe")
    - zEndstopCalibrate → "KGC-Z_ENDSTOP_CALIBRATE", GcodeCommandPresent("Z_ENDSTOP_CALIBRATE") — this is the
      probe-LESS sibling, so it must NOT require a probe object (ObjectPresent("probe") would be backwards: it would
      hide the command on exactly the printers that need it). GcodeCommandPresent gates on the printer actually
      exposing the command (mirrors the FORCE_MOVE registration at CommandRegistry.kt:268). The 09-03
      probeCalibrateGate(caps) A3 selector still picks PROBE_CALIBRATE-vs-Z_ENDSTOP_CALIBRATE for the page; this
      registry predicate just governs each command's own availability.
    - testZ → "KGC-TESTZ", ObjectPresent("manual_probe"), param = step
    - accept → "KGC-ACCEPT", ObjectPresent("manual_probe")
    - abort → "KGC-ABORT", ObjectPresent("manual_probe")
    - saveConfig → "KGC-SAVE_CONFIG", AvailabilityPredicate.Always (host action, not object-gated)
    Add every new spec to the `all` list (CommandCatalogDriftTest will flag any omission). Use unique
    operation-level catalog IDs (Phase-6 convention from STATE.md). If a docs/commands JSON sidecar drift guard
    fires, add the matching catalog rows (mirror the Phase-8 08-04 precedent where registering a new command
    required a sidecar row).
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testReleaseUnitTest --tests '*CommandCatalogDrift*' --tests '*CommandRegistry*' --tests '*PrinterCommands*' --tests '*BedMeshProfileName*' --no-daemon" 2>&1 | tr -d '\r' | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep "KGC-SCREWS_TILT_CALCULATE" AND "KGC-BED_MESH_CALIBRATE" AND "KGC-PROBE_CALIBRATE" AND "KGC-SAVE_CONFIG"
      AND "KGC-TESTZ" all hit CommandRegistry.kt.
    - Every new spec carries an `availability =` predicate; zEndstopCalibrate uses GcodeCommandPresent("Z_ENDSTOP_CALIBRATE")
      (NOT ObjectPresent("probe")); saveConfig is Always.
    - PrinterCommands.testZ clamps via coerceIn to a named bound (grep "coerceIn" near "TESTZ").
    - bedMeshProfileSave routes the name through sanitizeProfileName (allowlist [A-Za-z0-9_.-]+); BedMeshProfileNameTest
      GREEN — newline/space/`;`/empty rejected, timestamp name accepted.
    - CommandCatalogDriftTest passes (all new specs are in the catalog/sidecar).
    - `gw.bat :app:assembleRelease` would compile (the registry test run above passing implies compilation).
  </acceptance_criteria>
  <done>~12 calibration commands registered with object-gating + the G4 timeout inherited; drift test green; clamp-before-format on TESTZ.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Surface the five live objects in PrinterState + reducer + subscribe superset + screws config one-shot</name>
  <files>app/src/main/java/works/mees/dinghy/state/PrinterState.kt, app/src/main/java/works/mees/dinghy/state/PrinterStateReducer.kt, app/src/main/java/works/mees/dinghy/state/DeriveCapabilities.kt, app/src/main/java/works/mees/dinghy/state/PrinterStateStore.kt, app/src/test/java/works/mees/dinghy/state/DeriveCapabilitiesTest.kt, app/src/test/java/works/mees/dinghy/state/PrinterStateReducerTest.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/state/PrinterState.kt:20-109 (data class shape, HeaterState nested model, fail-safe defaults, NO Compose annotations — ADR-0001)
    - app/src/main/java/works/mees/dinghy/state/PrinterStateReducer.kt:66-140 (applyStatus walker) and :168-184 (the null-safe accessors objectOrNull/stringOrNull/doubleOrNullAt/booleanOrNull/doubleListOrNull)
    - app/src/main/java/works/mees/dinghy/state/DeriveCapabilities.kt:50-90 (V1_SUBSCRIBE_CORE + deriveSubscribeSet intersect-with-detected loop)
    - app/src/main/java/works/mees/dinghy/state/PrinterStateStore.kt:64-109,186-220 (the minExtrudeTemp/maxExtrudeDistance one-shot StateFlow seam + the configfile one-shot read pattern to model the screws config on)
    - app/src/test/resources/fixtures/bed_mesh_e5.json + screws_tilt_adjust_e5.json + manual_probe_e5.json + z_tilt_e5.json + configfile_screws_e5.json (the REAL shapes — read mesh_min/mesh_max actual form: array vs object)
    - .planning/phases/09-calibration-maintenance/09-PATTERNS.md (§ PrinterState + PrinterStateReducer; § PrinterStateStore; § DeriveCapabilities)
    - .planning/phases/09-calibration-maintenance/09-RESEARCH.md (§ Common Pitfalls 1/2/4/6, § Code Examples 1-4 for field shapes)
  </read_first>
  <behavior>
    - Subscribe: deriveSubscribeSet(objects) includes "screws_tilt_adjust"/"z_tilt"/"quad_gantry_level"/"bed_mesh"/"manual_probe"/"probe" ONLY when each is in the detected objects list; omitted otherwise (A3) — DeriveCapabilitiesTest asserts present-when-detected AND absent-when-missing.
    - Reduce: feeding the captured *_e5.json fixtures through applyStatus populates state.bedMesh (matrices + min/max + profile_name + profiles), state.screwsTilt (results + error), state.zTiltApplied, state.manualProbe (is_active + z_position) — PrinterStateReducerTest asserts each.
    - Defensive: a status diff with a garbage/missing calibration field is SKIPPED, retaining prior value, never throwing (mock-vs-reality defense, Pitfall 6) — test a malformed bed_mesh entry retains the prior mesh.
  </behavior>
  <action>
    PrinterState.kt: add nullable fields mirroring HeaterState's all-defaulted pattern — `screwsTilt: ScrewsTiltObject? = null` (results map + error + max_deviation), `zTiltApplied: Boolean? = null` (null=never run, false=running/failed, true=converged — Pitfall 2), `qglApplied: Boolean? = null`, `bedMesh: BedMeshObject? = null` (profileName, meshMin, meshMax, probedMatrix, meshMatrix, profileNames), `manualProbe: ManualProbeObject? = null` (isActive, zPosition, zPositionLower, zPositionUpper). Keep these PLAIN data classes, no Compose annotations.

    PrinterStateReducer.kt: in applyStatus add `status.objectOrNull("bed_mesh")?.let { ... }` etc. for all five
    objects, reading via the existing null-safe accessors. The matrices are arrays-of-arrays → add a
    `double2dListOrNull(key)` accessor following the doubleListOrNull pattern at :182-183. mesh_min/mesh_max are
    JSON arrays [x,y] (Pitfall 6 — confirm against the captured fixture's ACTUAL shape and read accordingly via
    doubleListOrNull). Carry the house rule verbatim: every walk null-safe, a missing/garbage field is skipped
    (retained), never fatal.

    DeriveCapabilities.kt: add the five objects + "probe" to V1_SUBSCRIBE_CORE (always-requested-IF-present; the
    intersect loop already enforces A3 — do NOT add unconditional requests).

    PrinterStateStore.kt: add a one-shot StateFlow `screwsTiltConfig: StateFlow<ScrewConfig?>` (screw coords +
    names from configfile.settings["screws_tilt_adjust"]) modeled EXACTLY on the minExtrudeTemp one-shot seam —
    read at handshake from the SINGLE existing configfile query (Pitfall 3, no duplicate query), best-effort
    runCatching, exposed for holders to combine (D-04/D-06). Per RESEARCH Open-Q1, if the 09-01 capture showed
    screws_tilt_adjust.results does NOT persist post-run, also expose a hook for a belt-and-braces one-shot
    objects/query?screws_tilt_adjust fired on dispatch completion (the holder in 09-04 wires it).

    Extend the test files: DeriveCapabilitiesTest (subscribe present/absent per object) and PrinterStateReducerTest
    (feed each captured fixture, assert the populated field; one malformed-entry-retains-prior case).
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testReleaseUnitTest --tests '*DeriveCapabilities*' --tests '*PrinterStateReducer*' --no-daemon" 2>&1 | tr -d '\r' | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - PrinterStateReducerTest GREEN: feeding bed_mesh_e5.json populates state.bedMesh with a non-empty meshMatrix
      and the mesh_min/mesh_max read as [x,y]; feeding screws_tilt_adjust_e5.json populates state.screwsTilt with
      ≥1 result and error present; feeding manual_probe_e5.json populates state.manualProbe.isActive.
    - PrinterStateReducerTest GREEN: a malformed bed_mesh diff retains the prior mesh (no throw).
    - DeriveCapabilitiesTest GREEN: each of the five objects + probe is subscribed when in the detected list and
      omitted when absent (grep the new assertions).
    - PrinterState.kt contains `screwsTilt`, `zTiltApplied`, `qglApplied`, `bedMesh`, `manualProbe` nullable fields
      (grep each).
    - DeriveCapabilities.kt V1_SUBSCRIBE_CORE contains all five object names + "probe" (grep each).
  </acceptance_criteria>
  <done>Five live objects retained in PrinterState via null-safe reducer walks; subscribe-when-present proven; screws config one-shot exposed; reducer/derive tests green over the real fixtures.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Moonraker notify_status_update → reducer | Untrusted, schema-loose printer JSON crosses into retained app state |
| app → printer.gcode.script (calibration commands) | App dispatches printer-altering gcode across the LAN |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-02-01 | Tampering | TESTZ Z=<step> param | mitigate | Clamp-before-format: testZ coerceIn(±MAX_TESTZ_MM) before string-build (ASVS V5), mirroring jog/extrude — no free-text reaches the gcode string |
| T-09-02-02 | Tampering | BED_MESH_PROFILE SAVE=<name> (keyboard-editable per UI-SPEC carve-out — a REAL injection surface) | mitigate | The name defaults to an app-generated timestamp but is user-editable, so bedMeshProfileSave/Load/Remove route it through sanitizeProfileName: strict allowlist `[A-Za-z0-9_.-]+`, reject whitespace/control/newline/empty (a newline would inject a second gcode line) before string-build (ASVS V5). Pure host-tested (BedMeshProfileNameTest) |
| T-09-02-03 | Denial of Service | malformed/garbage calibration object in a status diff | mitigate | Null-safe reducer accessors + runCatching skip-and-retain (Pitfall 6) — a bad field never crashes the spine |
| T-09-02-04 | Tampering | unsupported command offered (dead/unsafe button) | mitigate | Every spec carries ObjectPresent gating; hasObject is re-derived live every reconnect (no static table) so an unsupported command is never dispatchable |
| T-09-02-SC | Tampering | npm/pip/cargo installs | mitigate | Zero packages installed (RESEARCH § Package Legitimacy Audit: N/A) |
</threat_model>

<verification>
- Quick run: `gw.bat :app:testReleaseUnitTest --tests '*DeriveCapabilities*' --tests '*PrinterStateReducer*' --tests '*CommandCatalogDrift*' --no-daemon` green.
- Per-wave merge: full `:app:testReleaseUnitTest` green (no regressions in the 615-test baseline).
</verification>

<success_criteria>
- ~12 calibration commands registered with ObjectPresent gating + inherited G4 timeout; catalog drift test green.
- Five calibration objects retained in PrinterState via null-safe reducer walks; subscribe-when-present proven over fixtures.
- Screws-tilt config one-shot StateFlow exposed (modeled on minExtrudeTemp seam).
- No new timeout/reconnect code (G4/G2 reused).
</success_criteria>

<output>
Create `.planning/phases/09-calibration-maintenance/09-02-SUMMARY.md` when done — note the actual mesh_min/mesh_max wire shape and whether the screws results-persistence hook was needed.
</output>
