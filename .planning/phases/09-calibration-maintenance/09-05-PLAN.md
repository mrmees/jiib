---
phase: 09-calibration-maintenance
plan: 05
type: execute
wave: 3
depends_on: ["09-02", "09-03"]
files_modified:
  - app/src/main/java/works/mees/dinghy/designsystem/ConfirmGuard.kt
  - app/src/main/java/works/mees/dinghy/render/BedMeshHeatmapView.kt
  - app/src/main/java/works/mees/dinghy/render/BedMeshHeatmapHost.kt
  - app/src/main/java/works/mees/dinghy/calibration/TiltHolder.kt
  - app/src/main/java/works/mees/dinghy/calibration/BedMeshHolder.kt
  - app/src/main/java/works/mees/dinghy/ui/calibration/TiltScreen.kt
  - app/src/main/java/works/mees/dinghy/ui/calibration/BedMeshScreen.kt
  - app/src/test/java/works/mees/dinghy/calibration/BedMeshHolderTest.kt
  - app/src/test/java/works/mees/dinghy/calibration/TiltHolderTest.kt
autonomous: true
requirements: [BEDM-01, BEDL-01, CALIB-03, CALIB-04]
user_setup: []

must_haves:
  truths:
    - "Z-tilt/QGL share one automatic run-and-show-convergence screen; done from applied==true, failed from RpcError, no abort"
    - "Bed-mesh shows an overhead red/blue 2D heatmap of the live mesh with faint density-scaled probe dots and a user-cyclable color scale"
    - "Bed-mesh profile save/load/remove work; save auto-names YY.MM.DD_HH.MM and persists via the amber SAVE_CONFIG restart gate"
    - "ConfirmGuard gains an amber proceed-at-peril variant for SAVE_CONFIG (config-save + restart is intended, not destructive)"
    - "The heatmap is an allocation-free Views Canvas sibling of GraphView (not a fork), recoloring on theme flip"
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/render/BedMeshHeatmapView.kt"
      provides: "The NEW classic-Views Canvas heatmap (ThemeableView, allocation-free onDraw)"
      min_lines: 40
    - path: "app/src/main/java/works/mees/dinghy/designsystem/ConfirmGuard.kt"
      provides: "Amber proceed-at-peril intent variant alongside red destructive"
      contains: "warn"
    - path: "app/src/main/java/works/mees/dinghy/ui/calibration/BedMeshScreen.kt"
      provides: "Heatmap Focus + scale toggle + Activate/Save/Load field + profile dialogs"
      min_lines: 50
    - path: "app/src/main/java/works/mees/dinghy/ui/calibration/TiltScreen.kt"
      provides: "Z-tilt/QGL shared automatic-flow screen"
      min_lines: 30
  key_links:
    - from: "BedMeshScreen Focus"
      to: "BedMeshHeatmapHost"
      via: "AndroidView host passing tokens + BedMeshModel (mirrors GraphViewHost consumer)"
      pattern: "BedMeshHeatmapHost|AndroidView"
    - from: "BedMeshScreen Save"
      to: "ConfirmGuard amber + SAVE_CONFIG"
      via: "BED_MESH_PROFILE SAVE → amber guard → SAVE_CONFIG dispatch"
      pattern: "bedMeshProfileSave|saveConfig|warn"
    - from: "TiltScreen done-detection"
      to: "tiltState(applied)"
      via: "TiltHolder folds applied + dispatcher Failure"
      pattern: "tiltState|applied"
---

<objective>
Build the Z-tilt/QGL screen (CALIB-03 — shared automatic flow, no abort), the bed-mesh screen (CALIB-04 — the
overhead heatmap + profile management), the one genuinely-new render surface (BedMeshHeatmapView, a sibling of
GraphView, NOT a fork), and the ConfirmGuard amber proceed-at-peril variant the SAVE_CONFIG restart gate needs.
All per docs/ui_design/ LAW + 09-UI-SPEC §3/§4. Done-detection comes from the structured object (applied / mesh
populated), failure from the dispatcher's RpcError — never a bare gcode ack (RESEARCH Pattern 2). SAVE_CONFIG
reuses the Phase-5 G2 re-handshake (the re-handshake wiring is finalized in 09-07; this plan emits the dispatch +
guard).

Purpose: automatic multi-Z leveling + bed-mesh visualization/profiles from the printer without a browser.
Output: heatmap View+Host, ConfirmGuard amber variant, Z-tilt + bed-mesh screens + headless holders; holders host-tested.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-calibration-maintenance/09-UI-SPEC.md
@.planning/phases/09-calibration-maintenance/09-PATTERNS.md
@.planning/phases/09-calibration-maintenance/09-RESEARCH.md
@docs/ui_design/LAYOUT.md
@docs/ui_design/THEMING.md
@app/src/test/resources/fixtures/bed_mesh_e5.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: ConfirmGuard amber variant + BedMeshHeatmapView (+ Host)</name>
  <files>app/src/main/java/works/mees/dinghy/designsystem/ConfirmGuard.kt, app/src/main/java/works/mees/dinghy/render/BedMeshHeatmapView.kt, app/src/main/java/works/mees/dinghy/render/BedMeshHeatmapHost.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/designsystem/ConfirmGuard.kt:52-122 (the existing destructive:Boolean param toggling Intent.Danger/Intent.Go + the stopSoft/goSoft tint branch — EXTEND with an amber Intent.Warn + heatSoft branch)
    - app/src/main/java/works/mees/dinghy/render/GraphView.kt:55-90 (the DISCIPLINE to copy: ThemeableView, pre-allocated Paint/Path in init, allocation-free onDraw, applyTokens then invalidate, no animation loop — D-13)
    - app/src/main/java/works/mees/dinghy/render/GraphViewHost.kt:27-43 (the AndroidView host: factory once, update pushes tokens + data)
    - app/src/main/java/works/mees/dinghy/calibration/BedMeshModel.kt (the model the heatmap renders, from 09-03)
    - .planning/phases/09-calibration-maintenance/09-UI-SPEC.md (§ Color heatmap-ramp note: --stop high / --accent low endpoints, --text-3 faint dots; § Amber reserved-for: SAVE_CONFIG restart gate; § Reused Primitives BedMeshHeatmapView)
    - .planning/phases/09-calibration-maintenance/09-PATTERNS.md (§ BedMeshHeatmapView — sibling not fork; the scale-mode endpoints)
  </read_first>
  <action>
    ConfirmGuard.kt: add an amber proceed-at-peril variant WITHOUT breaking the existing red/green API. Add a
    `warn: Boolean = false` (or an Intent-style param) that, when set, picks Intent.Warn + a `t.heatSoft` tint
    branch alongside the existing `destructive` red and positive green branches (lines 63-67). Keep the cancel/
    dismiss always Intent.Neutral. This is the reusable SAVE_CONFIG restart gate used by bed-mesh Save here and
    probe-calibrate Save in 09-06.

    render/BedMeshHeatmapView.kt: a NEW `class BedMeshHeatmapView(context) : View(context), ThemeableView` — a
    SIBLING of GraphView (a 2D heatmap, not a 1D line; do NOT extend/fork GraphView). Copy GraphView's
    allocation-free contract verbatim: pre-allocate every Paint/Path/Rect in init, NEVER allocate in onDraw (the
    Adreno-320 GC-churn trap). Render the interpolated mesh_matrix as a red(high)/blue(low) overhead 2D heatmap of the current mesh (D-07) (ramp endpoints =
    --stop high / --accent low, set by the active scale mode), faint density-scaled probed_matrix dots (--text-3
    low opacity, dots shrink with density 3x3 to 50x50, D-08). Implement applyTokens(tokens) → recolor from role
    tokens + invalidate() (no raw hex, THEME-01). Repaint trigger is an imperative `setMesh(model, scaleMode)` on
    a new sample — NOT an animation loop (D-13). The scale-mode parameter selects the saturation endpoints
    (RELATIVE measured-min/max, PLATE z=0-symmetric, ±0.10/±0.25/±0.50/±1.00 fixed) per UI-SPEC §4 — pure
    color-mapping, no re-probe.

    render/BedMeshHeatmapHost.kt: copy GraphViewHost.kt verbatim in shape — AndroidView(factory = { ctx ->
    BedMeshHeatmapView(ctx) }, update = { it.applyTokens(tokens); it.setMesh(model, scaleMode) }). factory once,
    update pushes tokens + data so a theme flip recolors without recreation.
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:assembleRelease --no-daemon" 2>&1 | tr -d '\r' | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `:app:assembleRelease` SUCCESSFUL.
    - ConfirmGuard.kt now has an amber branch (grep "heatSoft" OR "Intent.Warn" added; existing destructive/Go
      branches remain — grep "destructive" still present).
    - BedMeshHeatmapView allocates NO Paint/Path/Bitmap inside onDraw: inspect the onDraw body — no `Paint(` /
      `Path(` / `createBitmap` / `Rect(` allocation (all pre-allocated in init).
    - BedMeshHeatmapView implements ThemeableView.applyTokens and calls invalidate() (grep both).
    - Token-purity: grep `0xFF` / `Color(0x` in BedMeshHeatmapView.kt + ConfirmGuard.kt returns ZERO new raw-hex hits.
    - No animation loop in BedMeshHeatmapView (no `ValueAnimator` / `postInvalidateOnAnimation` self-loop — grep).
  </acceptance_criteria>
  <done>ConfirmGuard amber variant + the allocation-free heatmap View/Host compile; sibling-not-fork, token-driven recolor, no animation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TiltHolder + TiltScreen (Z-tilt/QGL shared automatic flow)</name>
  <files>app/src/main/java/works/mees/dinghy/calibration/TiltHolder.kt, app/src/main/java/works/mees/dinghy/ui/calibration/TiltScreen.kt, app/src/test/java/works/mees/dinghy/calibration/TiltHolderTest.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/extrude/ExtrudeHolder.kt (holder template) + app/src/test/java/works/mees/dinghy/ui/extrude/ExtrudeHolderTest.kt (test pattern)
    - app/src/main/java/works/mees/dinghy/calibration/TiltResult.kt (the tiltState pure state machine from 09-03)
    - app/src/main/java/works/mees/dinghy/command/CommandDispatcher.kt:94 (events SharedFlow — fold Failure)
    - app/src/main/java/works/mees/dinghy/ui/files/FilesScreen.kt (full screen with gutter + dispatch + SeverityToast)
    - app/src/main/java/works/mees/dinghy/designsystem/layout/ScreenScaffold.kt (Focus/Field/Gutter slots)
    - img/bed_tilt.svg (the Z-tilt/QGL Focus asset, theme-tinted)
    - .planning/phases/09-calibration-maintenance/09-UI-SPEC.md (§ Per-Screen §3 — bed_tilt.svg 90%H Focus, question_exchange not-yet-run overlay accent, data_table on results, Run blue/Back green, NO Abort, homed gate; done=applied==true, fail=Failure)
    - .planning/phases/09-calibration-maintenance/09-RESEARCH.md (Pitfall 2 — applied==false is both running AND failed)
  </read_first>
  <behavior>
    - TiltHolder.vm = tiltState(dispatched, applied, failed) folded from printerState.zTiltApplied (or qglApplied for the QGL variant), a dispatched flag, and dispatcher Failure → {Idle, Running, Done, Failed} + the homed gate + error text. Done is applied==true; Failed is a Failure event; Running is dispatched-and-not-yet-applied (never Failed from applied==false alone).
  </behavior>
  <action>
    Create calibration/TiltHolder.kt copying ExtrudeHolder structure — combine printerState + capabilities + a
    dispatched flag + dispatcher.events Failure into a TiltVm carrying the tiltState, homedGate, errorText. The
    holder is parameterized by which object it watches (z_tilt vs quad_gantry_level) so the ONE screen serves both
    (D-02 shared code path). Add TiltHolderTest mirroring ExtrudeHolderTest: seed applied==true → Done; seed a
    Failure event → Failed; seed dispatched + applied==false + no-failure → Running.

    Create ui/calibration/TiltScreen.kt per UI-SPEC §3: Focus = img/bed_tilt.svg square 90%H, theme-tinted; overlay
    question_exchange (accent) when not-yet-run; swap to data_table + populate the Field with the per-stepper Z
    adjustment readout once results exist. Gutter = Run (blue, dispatch zTiltAdjust OR quadGantryLevel per variant)
    + Back (green). NO Abort, no e-stop framing (D-11 — Klipper has no clean cancel; routine is short/hands-off);
    Run disabled while Running and gated on homed (inline blue Home offer, D-13). Failure → SeverityToast(Error,
    RpcError text, never transport e.message T-05-11-01). Ratio-only sizing, token-only color.
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testReleaseUnitTest --tests '*TiltHolderTest*' --no-daemon" 2>&1 | tr -d '\r' | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - TiltHolderTest GREEN: applied==true → Done; Failure event → Failed; dispatched+applied==false+no-failure → Running.
    - TiltHolder contains no `import androidx.compose` (headless, ADR-0001) and no second throttle on store.printerState.
    - TiltScreen Run dispatches zTiltAdjust OR quadGantryLevel per variant (grep both) and has NO Abort button (grep).
    - Token-purity: no raw hex in TiltScreen.kt.
  </acceptance_criteria>
  <done>Z-tilt/QGL shared screen + holder; done/failed from object+RpcError (Pitfall 2), no abort, homed gate; holder green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: BedMeshHolder + BedMeshScreen (heatmap + scale toggle + profiles)</name>
  <files>app/src/main/java/works/mees/dinghy/calibration/BedMeshHolder.kt, app/src/main/java/works/mees/dinghy/ui/calibration/BedMeshScreen.kt, app/src/test/java/works/mees/dinghy/calibration/BedMeshHolderTest.kt, docs/ui_design/CLAUDE.md</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/extrude/ExtrudeHolder.kt + app/src/test/java/works/mees/dinghy/ui/extrude/ExtrudeHolderTest.kt
    - app/src/main/java/works/mees/dinghy/calibration/BedMeshModel.kt (BedMeshModel.from, from 09-03)
    - app/src/main/java/works/mees/dinghy/render/BedMeshHeatmapHost.kt (Task 1 — the Focus consumer)
    - app/src/main/java/works/mees/dinghy/designsystem/ConfirmGuard.kt (the amber variant from Task 1, for Save; red for Remove)
    - app/src/main/java/works/mees/dinghy/ui/settings/SettingsScreen.kt OR the TokenTextField site (the keyboard text-entry pattern for the Save-name dialog carve-out) + app/src/main/java/works/mees/dinghy/ui/files/FilesScreen.kt (the scrollable-list discipline for the Load selector)
    - app/src/main/java/works/mees/dinghy/command/CommandRegistry.kt (bedMeshCalibrate, bedMeshProfileSave/Load/Remove, saveConfig from 09-02)
    - .planning/phases/09-calibration-maintenance/09-UI-SPEC.md (§ Per-Screen §4 — heatmap Focus, scale-mode toggle cycle white/setting, 50/25/25 Activate/Save/Load field, Save dialog YY.MM.DD_HH.MM keyboard-editable carve-out, Load scrollable selector + per-row red Remove, empty-state copy)
    - .planning/phases/09-calibration-maintenance/09-RESEARCH.md (Pitfall 4 empty-state SEPARATE from profiles; Pitfall 5 SAVE then SAVE_CONFIG persists; Open-Q2 bare BED_MESH_CALIBRATE)
  </read_first>
  <behavior>
    - BedMeshHolder.vm = BedMeshModel.from(printerState.bedMesh) + the cyclable scaleMode + profileNames + isEmpty + folded dispatcher Failure/Success → a BedMeshVm the screen renders. Cycling the scale mode re-colors the existing mesh only (no re-probe). Empty-state is meshMatrix-empty/profileName=="" SEPARATE from profiles-non-empty (Pitfall 4).
  </behavior>
  <action>
    Create calibration/BedMeshHolder.kt copying ExtrudeHolder structure: combine store.printerState +
    store.capabilities into a BedMeshVm (BedMeshModel.from(state.bedMesh), profileNames, isEmpty, errorText from
    dispatcher Failure). Hold the active scaleMode as holder-owned state (the D-09 user-adjustable color scale) cycled by an imperative `cycleScaleMode()`
    (RELATIVE → PLATE → ±0.10 → ±0.25 → ±0.50 → ±1.00 → RELATIVE, default RELATIVE) — the setActiveTool-style
    imperative-setter precedent. Add BedMeshHolderTest: seed bed_mesh from the fixture → vm exposes matrices +
    profileNames; empty bed_mesh → isEmpty true while a non-empty profiles map still lists names.

    Create ui/calibration/BedMeshScreen.kt per UI-SPEC §4: Focus = BedMeshHeatmapHost (passing collected tokens +
    BedMeshModel + scaleMode), aspectRatio(1f) centered. Overlay the scale-mode toggle inset top-left (expand glyph
    + current mode text, white/setting intent, ≥64dp, taps cycleScaleMode). Field = three ratio-split buttons
    50/25/25: Activate (blue, dispatch bedMeshCalibrate — bare, KAMP defaults; optional amber bed-preheat
    pre-flight D-13), Save (blue → full-screen Save dialog: name pre-filled YY.MM.DD_HH.MM via a generated
    timestamp, system-keyboard-EDITABLE per the carve-out, gutter Save/Cancel → dispatch bedMeshProfileSave(name)
    → amber ConfirmGuard restart gate → dispatch saveConfig), Load (full-screen scrollable selector of
    bed_mesh.profiles keys, gutter Apply/Cancel → bedMeshProfileLoad(selected); each row has a red ConfirmGuard
    Remove → bedMeshProfileRemove(name)). Gutter (main) = single green Back. Empty-state (Pitfall 4): "No active
    mesh" Focus copy with Activate/Load still present. SeverityToast(Success "Profile {name} saved" / Error
    RpcError text). All sizing ratio-only; all color via tokens.

    Record the bed-mesh Save-name keyboard carve-out as precedent in docs/ui_design/ (the UI-SPEC keyboard carve-out
    note) — a one-line rule addition so future keyboard requests reference it instead of reopening the debate.
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testReleaseUnitTest --tests '*BedMeshHolderTest*' --no-daemon" 2>&1 | tr -d '\r' | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - BedMeshHolderTest GREEN: fixture-seeded bed_mesh → matrices + profileNames present; empty mesh → isEmpty true
      while profiles map still lists names (Pitfall 4 distinction).
    - BedMeshScreen Focus hosts BedMeshHeatmapHost (grep) passing the collected tokens + BedMeshModel + scaleMode.
    - Save path dispatches bedMeshProfileSave THEN routes through the amber ConfirmGuard THEN dispatches saveConfig
      (grep all three in order); Remove uses the red ConfirmGuard.
    - Scale-mode toggle cycles all six modes (grep RELATIVE/PLATE and the ± values) with white/setting intent.
    - docs/ui_design/ gains the keyboard carve-out precedent line (grep "carve-out" OR "Save-name" in docs/ui_design/).
    - Token-purity: no raw hex in BedMeshScreen.kt.
  </acceptance_criteria>
  <done>Bed-mesh heatmap + scale toggle + Activate/Save(amber+SAVE_CONFIG)/Load+Remove(red) per UI-SPEC §4; holder green; carve-out recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user tap → BED_MESH_CALIBRATE / Z_TILT_ADJUST dispatch | App probes/moves the toolhead on a tap |
| SAVE_CONFIG dispatch → printer.cfg rewrite + host restart | App mutates persistent printer config and restarts klippy |
| bed_mesh object → heatmap Canvas | Schema-loose matrix JSON drives a fill-rate-heavy 2D draw |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-05-01 | Tampering | destructive SAVE_CONFIG without warning | mitigate | Amber ConfirmGuard before SAVE_CONFIG (D-12) — user warned it rewrites config + restarts the printer; config-save is the intended result, hence amber not red |
| T-09-05-02 | Tampering/safety | running bed-mesh/Z-tilt unhomed | mitigate | D-13 pre-flight gates Run on homedAxes; offers Home; no force-move escape on these pages |
| T-09-05-03 | Tampering | BED_MESH_PROFILE SAVE name injection | mitigate | Name is APP-GENERATED YY.MM.DD_HH.MM timestamp (the dialog override is a Settings-class field, not concatenated into other gcode); SAVE/LOAD/REMOVE take only a profile name |
| T-09-05-04 | Spoofing | inferring done from a bare ack | mitigate | Done = applied==true / populated mesh (object signal); failed = dispatcher Failure (RpcError) — never the gcode reply (Pitfall 2/3) |
| T-09-05-05 | Denial of Service | 50x50 heatmap fill stalling Adreno-320 | mitigate | Allocation-free onDraw + pre-allocated paints (GraphView discipline); on-device gfxinfo gate in 09-07 is the backstop |
| T-09-05-SC | Tampering | npm/pip/cargo installs | mitigate | Zero packages installed (RESEARCH § Package Legitimacy Audit: N/A) |
</threat_model>

<verification>
- Quick run: `gw.bat :app:testReleaseUnitTest --tests '*TiltHolderTest*' --tests '*BedMeshHolderTest*' --no-daemon` green.
- `:app:assembleRelease` SUCCESSFUL; token-purity grep clean over the new render + screen files.
- Per-wave merge: full `:app:testReleaseUnitTest` green.
- Heatmap Adreno-320 fill-rate gate is on-device (deferred to 09-07 gfxinfo checkpoint).
</verification>

<success_criteria>
- Z-tilt/QGL shared automatic screen (done from applied, failed from RpcError, no abort); bed-mesh heatmap + scale toggle + full profile management.
- ConfirmGuard amber variant; SAVE_CONFIG routed through amber guard, not red.
- Heatmap is an allocation-free sibling of GraphView, token-recolored, no animation; holders headless + host-tested.
</success_criteria>

<output>
Create `.planning/phases/09-calibration-maintenance/09-05-SUMMARY.md` when done — note the heatmap scale-mode math + any Adreno-320 concern for the 09-07 perf gate.
</output>
