---
context: phase
phase: 09-calibration-maintenance
task: 2
total_tasks: 2
status: in_progress
last_updated: 2026-06-03T16:03:03Z
---

# Continue Here — Phase 9 (Calibration & Maintenance), plan 09-07

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Mock-vs-reality (the recurring strike) | Green unit suites repeatedly hid real bugs that only the live E5 exposed: worst-screw metric (max\|z\| vs deviation-from-base), the Z-adjustment block arriving as ONE gcode_response with embedded `\n`, the screws-config one-shot never wired at handshake, partial `bed_mesh` deltas wiping retained state. | advisory | Keep driving the on-device UAT loop on flox + live E5 before declaring an item done — do NOT trust the green unit suite alone for items 5 & 6. |
| Seam built but never wired | `setScrewsTiltConfig` existed + was unit-tested but was never called in production (handshake only parsed `min_extrude_temp`). | advisory | When a holder COMBINEs a store one-shot, grep that the setter has a production caller, not just a test caller. |

<current_state>
Mid the 09-07 **human-verify checkpoint** (Task 2 = on-device live-E5 UAT). 09-07 Task 1 (nav wiring + G2 re-handshake regression) is committed (`ea95a19`). The 09-07 executor returned the checkpoint; the **orchestrator has been driving the UAT directly** (build/sign/install + adb screenshots; Matthew navigates and eyeballs).

UAT items 1–4 PASS (items 3 z-tilt and 4 screws-tilt were substantially reworked on-device and committed). **Items 5 and 6 are NOT yet tested.** All code is committed; tree clean (except pre-existing untracked `img/bed_tilt.svg`).
</current_state>

<completed_work>
- 09-01..09-06: executed + committed (fixtures, spine, parsers, hub+screws-tilt, z-tilt/QGL+bed-mesh, probe-calibrate+Files-delete-fix).
- Worst-screw correctness fix (`ef260cb`): rank by deviation-from-base, not max|z|.
- Bed-mesh UAT batch (`a66e8e0` reducer partial-delta merge, `f872643` opaque alerts, `f4b8745` page polish + heatmap orientation flip).
- Z-tilt rework (`517ab4a`): load-scoped state, parsed per-stepper adjustments, overlay glyph removed.
- Screws-tilt rework (`b47a955`): handshake now loads `[screws_tilt_adjust]` config (coords+names); to-scale bed focus; portrait 50/50; field rows (icon + name + minutes/degrees, degrees signed CCW=−); direction colors (CW green/CCW red/in-tol accent/anchor neutral); fresh-instance gating (`setShowResults = armed && !running`).
- 09-07 Task 1 nav wiring (`ea95a19`).
- UAT items 1 (hub), 2 (bed-mesh + heatmap gfxinfo perf — "fine"), 3 (z-tilt), 4 (screws-tilt) PASS.
</completed_work>

<remaining_work>
- **UAT item 5** — probe-calibrate: Start → TESTZ jog → Accept → Save → amber SAVE_CONFIG gate → klippy restarts → app **auto-reconnects (G2), no force-stop**. Verify on flox + live E5.
- **UAT item 6** — Files delete scoping: start a print, in Files confirm **only the printing file** is undeletable; all other idle files deletable.
- After 5 & 6 pass: write `09-07-SUMMARY.md`, record heatmap gfxinfo numbers in `docs/moonraker-capabilities.md`, sign off `09-UAT.md`, then re-run `/gsd-execute-phase 9` so it proceeds to verification + `phase.complete`.
</remaining_work>

<decisions_made>
- Worst screw = max deviation-from-base z (== Klipper adjust clock); never max|z|.
- Screws-tilt: fresh instance each visit; measured turns only after a Run completes this load.
- Bed-mesh Save = no SAVE_CONFIG restart (meshes load dynamically); reducer field-merges partial `bed_mesh` deltas.
- Z-tilt: load-scoped (ignore persisted `z_tilt.applied`); show parsed per-stepper adjustments.
- Alerts (`SeverityToast`) opaque (surface base under soft tint); heatmap rendered front-at-bottom/rear-at-top.
</decisions_made>

<blockers>
- None technical. Item 5 & 6 require the live E5 + flox + Matthew driving.
</blockers>

## Required Reading (in order)
1. `.planning/phases/09-calibration-maintenance/09-07-PLAN.md` — Task 2 `how-to-verify` has the verbatim 6-item UAT checklist + the SUMMARY/sign-off close-out steps.
2. `.planning/HANDOFF.json` — structured state.
3. Memory: `dinghy-display-ondevice-iteration` + `dinghy-display-mock-vs-reality` — the UAT loop + why live gates matter.

## Infrastructure State
- flox (Nexus 7 2013, `0a64b42e`, LineageOS 18.1 / API 30) USB-connected to Windows; adb at `E:\Android\Sdk\platform-tools\adb.exe`.
- Live Ender 5 Plus Moonraker at `192.168.1.120:7125` (ready).
- Current signed release APK on flox = commit `b47a955` (up to date).
- Build/install loop (from repo root, WSL): `/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:assembleRelease --no-daemon"` → `E:\Android\sign-release.bat <unsigned> <signed>` (no inner quotes on the paths) → `adb install -r`. Screenshot: `adb exec-out screencap -p > /tmp/x.png`. Glob `--tests '*X*'` mangles over WSL→cmd; use fully-qualified test names.

<context>
This is a long interactive on-device UAT polish session for Phase 9. The pattern: Claude edits → builds Windows-side → signs → installs on flox → Matthew navigates/eyeballs the live printer → reports. Items 5 & 6 are the last two of the 09-07 UAT checkpoint. Once they pass, close out 09-07 (SUMMARY + gfxinfo record + 09-UAT sign-off) and resume `/gsd-execute-phase 9` for verification + completion.
</context>

<next_action>
Resume the 09-07 on-device UAT: relaunch the app on flox (`adb shell monkey -p works.mees.dinghy -c android.intent.category.LAUNCHER 1`) and have Matthew run UAT item 5 (probe-calibrate → SAVE_CONFIG → auto-reconnect) then item 6 (Files delete scoping during a print). Build is current; no rebuild needed to start testing.
</next_action>
