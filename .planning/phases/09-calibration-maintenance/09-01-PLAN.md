---
phase: 09-calibration-maintenance
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - app/src/test/resources/fixtures/screws_tilt_adjust_e5.json
  - app/src/test/resources/fixtures/bed_mesh_e5.json
  - app/src/test/resources/fixtures/z_tilt_e5.json
  - app/src/test/resources/fixtures/manual_probe_e5.json
  - app/src/test/resources/fixtures/configfile_screws_e5.json
  - app/src/test/java/works/mees/dinghy/calibration/ScrewsTiltResultTest.kt
  - app/src/test/java/works/mees/dinghy/calibration/TiltResultTest.kt
  - app/src/test/java/works/mees/dinghy/calibration/BedMeshModelTest.kt
  - app/src/test/java/works/mees/dinghy/calibration/ManualProbeStateTest.kt
  - app/src/test/java/works/mees/dinghy/calibration/ProbePresentGateTest.kt
  - app/src/test/java/works/mees/dinghy/calibration/CalibrationGateTest.kt
  - app/src/test/java/works/mees/dinghy/ui/files/FilesDeleteGateTest.kt
autonomous: false
requirements: [BEDM-01, BEDL-01, ZCAL-01, CALIB-01, CALIB-02, CALIB-03, CALIB-04, CALIB-05, CALIB-06]
user_setup: []

must_haves:
  truths:
    - "Five REAL Ender-5-Plus JSON fixtures captured verbatim before any parser is written"
    - "Seven RED test scaffolds reference the captured fixtures and fail-compile/fail-assert until the parsers land"
    - "The exact JSON shapes (tuple-vs-array, matrix nesting, 1-based screw keys) are recorded from real hardware, not assumed"
  artifacts:
    - path: "app/src/test/resources/fixtures/screws_tilt_adjust_e5.json"
      provides: "Real SCREWS_TILT_CALCULATE results object (CALIB-02 fixture)"
    - path: "app/src/test/resources/fixtures/bed_mesh_e5.json"
      provides: "Real bed_mesh object post-calibrate (CALIB-04 fixture)"
    - path: "app/src/test/resources/fixtures/z_tilt_e5.json"
      provides: "Real z_tilt object post-run (CALIB-03 fixture)"
    - path: "app/src/test/resources/fixtures/manual_probe_e5.json"
      provides: "Real manual_probe mid-session object (CALIB-05 fixture)"
    - path: "app/src/test/resources/fixtures/configfile_screws_e5.json"
      provides: "Real configfile.settings[screws_tilt_adjust] (D-04/D-06 screw coords/names)"
  key_links:
    - from: "RED test scaffolds"
      to: "app/src/test/resources/fixtures/*_e5.json"
      via: "classpath resource load (mirror GcodeStoreParseTest fixture loading)"
      pattern: "getResourceAsStream|readResource|fixtures/"
---

<objective>
Capture the five REAL calibration JSON shapes from the live Ender 5 Plus and lay down seven RED test
scaffolds that read those fixtures — BEFORE any parser is written. This is the Wave-0 fixture-capture-first
gate (per 09-VALIDATION.md): the third mock-vs-reality strike is waiting (Phases 2 and 5 were the first two —
a lenient fake socket hid a real protocol bug behind a green unit suite). Capturing the verbatim wire shapes
(Python tuple→JSON array for mesh_min/mesh_max, array-of-arrays matrices, 1-based screwN keys per RESEARCH
Pitfall 1/6) and writing tests against them is what kills that bug class.

Purpose: every Phase-9 result parser is a PURE function fed a captured-from-real-hardware fixture; this plan
produces the fixtures + the RED scaffolds the parser plans (09-03..09-06) turn GREEN.
Output: 5 fixtures + 7 RED test files (calibration/* + ui/files/FilesDeleteGateTest).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-calibration-maintenance/09-RESEARCH.md
@.planning/phases/09-calibration-maintenance/09-VALIDATION.md
@.planning/phases/09-calibration-maintenance/09-PATTERNS.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Capture the five REAL E5 calibration fixtures on-device</name>
  <files>app/src/test/resources/fixtures/screws_tilt_adjust_e5.json, app/src/test/resources/fixtures/bed_mesh_e5.json, app/src/test/resources/fixtures/z_tilt_e5.json, app/src/test/resources/fixtures/manual_probe_e5.json, app/src/test/resources/fixtures/configfile_screws_e5.json</files>
  <read_first>
    - .planning/phases/09-calibration-maintenance/09-RESEARCH.md (§ Common Pitfalls / Pitfall 6 "probe the real E5 FIRST"; § Code Examples 1-5 for the expected shapes; § Wave 0 Gaps)
    - app/src/test/resources/fixtures/gcode_store_e5.json (the Phase-8 precedent for what a captured fixture looks like — verbatim Moonraker JSON, pretty-printed)
    - docs/moonraker-capabilities.md (E5 endpoint 192.168.1.120:7125; confirm dual stepper_z / KAMP / klicky probe presence)
  </read_first>
  <action>
    Matthew runs these read-only/calibration captures on the live Ender 5 Plus (192.168.1.120:7125) and saves
    each response body VERBATIM (pretty-printed JSON, no hand-editing) into app/src/test/resources/fixtures/.
    Use curl from WSL or the Moonraker host. Per RESEARCH Open-Q1, capture screws_tilt_adjust BOTH via subscribe
    delivery AND a one-shot query after the run completes so we know whether `results` persists post-run.

    Captures (filename ← endpoint):
    - screws_tilt_adjust_e5.json ← run `SCREWS_TILT_CALCULATE` via printer.gcode.script, then
      `GET /printer/objects/query?screws_tilt_adjust` — capture the full result object (the `results` dict keyed
      screw1..screwN, plus `error`/`max_deviation`). [VERIFIED shape: RESEARCH Code Examples 1]
    - bed_mesh_e5.json ← run `BED_MESH_CALIBRATE` (bare — let KAMP defaults apply, RESEARCH Open-Q2), then
      `GET /printer/objects/query?bed_mesh` — capture profile_name, mesh_min, mesh_max, probed_matrix,
      mesh_matrix, profiles. [RESEARCH Code Examples 2]
    - z_tilt_e5.json ← run `Z_TILT_ADJUST`, then `GET /printer/objects/query?z_tilt` — capture `applied`.
      [RESEARCH Code Examples 3]
    - manual_probe_e5.json ← `PROBE_CALIBRATE`, do ONE `TESTZ Z=-0.1`, then
      `GET /printer/objects/query?manual_probe` MID-SESSION (is_active==true) — capture is_active, z_position,
      z_position_lower, z_position_upper. Then `ABORT` to close the session. ALSO capture the
      `notify_gcode_response` "Z position: a --> b <-- c" line text into a comment or sibling note if convenient.
      [RESEARCH Code Examples 4]
    - configfile_screws_e5.json ← `GET /printer/objects/query?configfile` and save just the
      `result.status.configfile.settings.screws_tilt_adjust` subtree (screwN coords + screwN_name + screw_thread).
      [RESEARCH Code Examples 1 config block]

    Record any shape surprises (tuple-vs-array, null where a number was expected, extra wrapping) in the SUMMARY
    — that is the whole point of this capture. If `results` clears immediately after the run (Open-Q1/A2), note
    it so 09-03 reads it on the completion edge instead of post-hoc.
  </action>
  <verify>
    <automated>test -s app/src/test/resources/fixtures/screws_tilt_adjust_e5.json && test -s app/src/test/resources/fixtures/bed_mesh_e5.json && test -s app/src/test/resources/fixtures/z_tilt_e5.json && test -s app/src/test/resources/fixtures/manual_probe_e5.json && test -s app/src/test/resources/fixtures/configfile_screws_e5.json && python3 -c "import json,glob,sys; [json.load(open(f)) for f in glob.glob('app/src/test/resources/fixtures/*_e5.json')]; print('all fixtures valid JSON')"</automated>
  </verify>
  <acceptance_criteria>
    - All five files exist, are non-empty, and parse as valid JSON (the python one-liner above prints
      "all fixtures valid JSON" and exits 0).
    - bed_mesh_e5.json contains keys `mesh_matrix` AND `probed_matrix` AND `mesh_min` AND `mesh_max` AND
      `profiles` (grep each key present).
    - screws_tilt_adjust_e5.json contains a `results` object with at least one `screw1`-style key AND a top-level
      `error` boolean.
    - manual_probe_e5.json contains `is_active` AND `z_position`.
    - configfile_screws_e5.json contains at least one `screw1`/`screw2` coordinate entry.
    - SUMMARY records the observed shape of mesh_min/mesh_max (array `[x,y]` vs object) and whether
      screws_tilt_adjust.results persists after the run (Open-Q1/A2).
  </acceptance_criteria>
  <done>Five verbatim real-E5 fixtures committed to app/src/test/resources/fixtures/; shape surprises recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Write the seven RED test scaffolds against the captured fixtures</name>
  <files>app/src/test/java/works/mees/dinghy/calibration/ScrewsTiltResultTest.kt, app/src/test/java/works/mees/dinghy/calibration/TiltResultTest.kt, app/src/test/java/works/mees/dinghy/calibration/BedMeshModelTest.kt, app/src/test/java/works/mees/dinghy/calibration/ManualProbeStateTest.kt, app/src/test/java/works/mees/dinghy/calibration/ProbePresentGateTest.kt, app/src/test/java/works/mees/dinghy/calibration/CalibrationGateTest.kt, app/src/test/java/works/mees/dinghy/ui/files/FilesDeleteGateTest.kt</files>
  <read_first>
    - app/src/test/java/works/mees/dinghy/ui/console/GcodeStoreParseTest.kt (THE fixture-loading + pure-parser-test template — copy its resource-load idiom and assertion style verbatim)
    - app/src/test/java/works/mees/dinghy/ui/extrude/ExtrudeHolderTest.kt (the holder-test pattern, for reference only — Wave-0 scaffolds here are parser tests, not holder tests)
    - .planning/phases/09-calibration-maintenance/09-VALIDATION.md (§ Per-Task Verification Map + § Wave 0 Requirements — the exact `--tests` filter names each scaffold must match)
    - .planning/phases/09-calibration-maintenance/09-RESEARCH.md (§ Code Examples 1-6 for the asserted values; § Common Pitfalls 1/2/4 for the edge cases each test must encode)
    - .planning/phases/09-calibration-maintenance/09-PATTERNS.md (the parser-signature table under "Pure result parsers")
    - app/src/main/java/works/mees/dinghy/ui/files/FilesScreen.kt:76-94 (the deleteEnabled predicate the FilesDeleteGateTest pins)
  </read_first>
  <action>
    Create seven RED test files whose class names match the 09-VALIDATION.md `--tests` filters EXACTLY, each
    loading the real fixture via the GcodeStoreParseTest resource idiom and asserting the behavior the
    yet-unwritten parser must satisfy. The scaffolds reference parser symbols that do NOT exist yet — so the
    suite is RED (compile-fail is acceptable RED for these; 09-03..09-06 introduce the symbols to turn GREEN).

    1. calibration/ScrewsTiltResultTest.kt — load screws_tilt_adjust_e5.json + configfile_screws_e5.json; assert
       `parseScrewsTilt(results, config)` returns the WORST out-of-tolerance screw (max |adjust-minutes|, is_base
       excluded), the "X of N in tolerance" count, and that results["screwN"] (1-based) joins to config screwN_name
       by index (RESEARCH Pitfall 1). Include a 3-screw AND a 4-screw assertion (D-04 generic).
    2. calibration/TiltResultTest.kt — assert `tiltState(dispatched, applied, failed)` pure state machine:
       applied==true → Done; (dispatched && !applied && !failed) → Running; failed (RpcError) → Failed; never
       infer Failed from applied==false alone (RESEARCH Pitfall 2).
    3. calibration/BedMeshModelTest.kt — load bed_mesh_e5.json; assert `BedMeshModel.from(bedMesh)` yields the
       mesh_matrix (2D), probed_matrix (dots), mesh_min/mesh_max as [x,y], profile names from `profiles` KEYS, and
       empty-state derived from mesh_matrix-empty/profile_name=="" SEPARATE from profiles-non-empty (Pitfall 4).
    4. calibration/ManualProbeStateTest.kt — load manual_probe_e5.json; assert `parseZPosition("Z position: 5.001 --> 4.901 <-- 4.800")`
       returns the bracket (lower=5.001, current=4.901, upper=4.800), a malformed line returns null, and the
       page-state derivation reads `is_active` (Pattern 3).
    5. calibration/ProbePresentGateTest.kt — assert `probeCalibrateGate(caps)` returns PROBE_CALIBRATE when
       caps.hasObject("probe"), else Z_ENDSTOP_CALIBRATE (A3).
    6. calibration/CalibrationGateTest.kt — assert the hub gating predicate marks a routine SUPPORTED iff
       caps.hasObject(<routine object>) for each of screws_tilt_adjust/z_tilt/quad_gantry_level/bed_mesh/probe;
       all five render (UI-SPEC §1 owner override — unsupported greyed, not omitted) but `isSupported` is the gate.
    7. ui/files/FilesDeleteGateTest.kt — assert `deleteAllowed(selectedPath, activePrintFilename, printState)`:
       during a print, only selectedPath == activePrintFilename is undeletable; every other idle file stays
       deletable; idle (activePrintFilename=="") → all deletable (D-15). Use the relative dir-prefixed path form
       "miata/airbox-bracket.gcode" (verify vs docs/moonraker-capabilities.md, NO leading "gcodes/").

    Do NOT create the production parser files in this plan — only the tests. Mark each test file with a header
    comment `// RED scaffold (Wave 0) — turns GREEN in 09-0X` naming the plan that implements it.
  </action>
  <verify>
    <automated>grep -rl "fixtures/" app/src/test/java/works/mees/dinghy/calibration/ | wc -l | grep -q 6 && test -f app/src/test/java/works/mees/dinghy/ui/files/FilesDeleteGateTest.kt && echo "7 RED scaffolds present"</automated>
  </verify>
  <acceptance_criteria>
    - Seven test files exist at the paths in `files_modified`.
    - Class names match the 09-VALIDATION.md filters: `*ScrewsTiltResultTest*`, `*TiltResultTest*`,
      `*BedMeshModelTest*`, `*ManualProbeStateTest*`, `*ProbePresentGateTest*`, `*CalibrationGateTest*`,
      `*FilesDeleteGateTest*` (grep each class name).
    - Each calibration test loads its fixture via a `fixtures/..._e5.json` classpath read (grep "fixtures/" hits
      6 calibration files; the FilesDelete test is pure-value, no fixture needed).
    - The suite is RED: running `gw.bat :app:testReleaseUnitTest --tests '*ScrewsTiltResultTest*' --no-daemon`
      fails to compile OR fails its assertions (the parser symbols do not exist yet) — recorded in SUMMARY as the
      intended RED baseline. Do NOT make them pass by stubbing production code here.
  </acceptance_criteria>
  <done>Seven RED scaffolds committed; each binds to a real fixture; suite RED on the new symbols, recorded as the baseline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live E5 Moonraker → captured fixture | Untrusted-but-local printer JSON crosses into the test corpus; capture is read-only |
| test fixture → parser-under-test | The captured shape is the contract the pure parsers (09-03+) are written against |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01-01 | Tampering | SCREWS_TILT_CALCULATE / BED_MESH_CALIBRATE / Z_TILT_ADJUST dispatched during capture | accept | Capture is run by Matthew on his own hardware with the printer attended; these are the routine's normal calibration commands (homed pre-flight per D-13 applies at runtime, not at one-off capture) |
| T-09-01-02 | Information Disclosure | configfile fixture may embed host paths/printer.cfg fragments | mitigate | Capture ONLY the `screws_tilt_adjust` subtree of configfile.settings, not the whole config; no secrets/API keys live in that subtree |
| T-09-01-SC | Tampering | npm/pip/cargo installs | mitigate | None — this plan installs zero packages (RESEARCH § Package Legitimacy Audit: N/A); python3 (stdlib json) and curl are pre-present |
</threat_model>

<verification>
- All five fixtures valid JSON and contain the load-bearing keys (Task 1 automated check).
- Seven RED scaffolds present and bound to fixtures (Task 2 automated check).
- Quick run `gw.bat :app:testReleaseUnitTest --tests '*ScrewsTilt*' --tests '*BedMesh*' --tests '*ManualProbe*' --no-daemon` is RED (parser symbols absent) — the intended Wave-0 baseline.
</verification>

<success_criteria>
- Five verbatim real-E5 fixtures committed; shape surprises (tuple/array, persistence of results) recorded in SUMMARY.
- Seven RED test scaffolds committed, class names matching the validation filters, each bound to its fixture.
- No production parser code written in this plan (Wave 0 is fixtures + RED only).
</success_criteria>

<output>
Create `.planning/phases/09-calibration-maintenance/09-01-SUMMARY.md` when done — record the captured shapes (especially mesh_min/mesh_max array-vs-object and whether screws_tilt_adjust.results persists post-run).
</output>
