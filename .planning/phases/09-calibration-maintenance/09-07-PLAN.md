---
phase: 09-calibration-maintenance
plan: 07
type: execute
wave: 5
depends_on: ["09-04", "09-05", "09-06"]
files_modified:
  - app/src/main/java/works/mees/dinghy/ui/route/TopRoute.kt
  - app/src/main/java/works/mees/dinghy/ui/shell/AppDrawer.kt
  - app/src/main/java/works/mees/dinghy/ui/shell/AppShell.kt
  - app/src/test/java/works/mees/dinghy/net/KlippyReadyResyncTest.kt
autonomous: false
requirements: [BEDM-01, BEDL-01, ZCAL-01, CALIB-01, CALIB-02, CALIB-03, CALIB-04, CALIB-05, CALIB-06]
user_setup: []

must_haves:
  truths:
    - "A single Calibration drawer tile opens the hub, which sub-routes to each of the five routine pages"
    - "SAVE_CONFIG (bed-mesh Save / probe-calibrate Save) restarts klippy and the spine self-heals via the existing G2 re-handshake — no force-stop"
    - "The bed-mesh heatmap clears the Adreno-320 perf gate on flox (gfxinfo two-part liveness+latency)"
    - "On real flox + live Ender 5 Plus: screws-tilt loop, Z-tilt converge, bed-mesh heatmap, probe-calibrate Accept→SAVE_CONFIG recovery, and the D-15 delete scoping all pass"
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/ui/shell/AppDrawer.kt"
      provides: "Single Calibration drawer tile (D-14)"
      contains: "Calibration"
    - path: "app/src/main/java/works/mees/dinghy/ui/shell/AppShell.kt"
      provides: "Dest.Calibration arm building the holders + hub→routine sub-routing"
  key_links:
    - from: "AppDrawer Calibration tile"
      to: "Dest.Calibration → CalibrationHubScreen"
      via: "DRAWER_TILES entry + AppShell when(dest) arm"
      pattern: "Dest.Calibration|CalibrationHubScreen"
    - from: "SAVE_CONFIG dispatch"
      to: "G2 notify_klippy_ready re-handshake"
      via: "the existing 05-10 runHandshake re-run on klippy shutdown→ready"
      pattern: "notify_klippy_ready|runHandshake"
---

<objective>
Wire the Calibration hub into the shell (single drawer tile → hub → five routine sub-routes, D-14), verify the
SAVE_CONFIG firmware-restart re-handshake (D-12) reuses the proven Phase-5 G2 path, and run the on-device gate:
the Adreno-320 heatmap perf check plus the full live-Ender-5 UAT (the mock-vs-reality backstop — third strike
prevention). This is the integration + proof wave; no new screens, just connect and verify.

Purpose: the phase is only "done" when the routines are reachable from the drawer AND proven on real hardware
(two prior phases shipped green-unit-suite bugs that only the on-device gate caught).
Output: nav wiring + the G2 re-handshake regression extension + the on-device UAT/perf sign-off.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-calibration-maintenance/09-UI-SPEC.md
@.planning/phases/09-calibration-maintenance/09-PATTERNS.md
@.planning/phases/09-calibration-maintenance/09-VALIDATION.md
@.planning/phases/09-calibration-maintenance/09-UAT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Nav wiring (Dest.Calibration + drawer tile + hub sub-routing) + G2 re-handshake regression</name>
  <files>app/src/main/java/works/mees/dinghy/ui/route/TopRoute.kt, app/src/main/java/works/mees/dinghy/ui/shell/AppDrawer.kt, app/src/main/java/works/mees/dinghy/ui/shell/AppShell.kt, app/src/test/java/works/mees/dinghy/net/KlippyReadyResyncTest.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/route/TopRoute.kt:30 (the enum class Dest — add Calibration)
    - app/src/main/java/works/mees/dinghy/ui/shell/AppDrawer.kt:116-129 (DRAWER_TILES — add ONE Calibration tile, D-14 single tile)
    - app/src/main/java/works/mees/dinghy/ui/shell/AppShell.kt:99-129,222,230-287 (the dest state + Macros sub-navigation precedent within a single Dest, the drawer-swipe-suppression set at :222, the when(dest) arms at :230-287, the remember(store){ Holder(...) } wiring at :136-143)
    - app/src/main/java/works/mees/dinghy/calibration/* (the five holders from 09-04/05/06 — CalibrationHubHolder, ScrewsTiltHolder, TiltHolder, BedMeshHolder, ProbeCalibrateHolder)
    - app/src/main/java/works/mees/dinghy/ui/calibration/* (the five screens to route to)
    - app/src/test/java/works/mees/dinghy/net/KlippyReadyResyncTest.kt (the existing 05-10 G2 re-handshake test to EXTEND, not fork)
    - .planning/phases/09-calibration-maintenance/09-PATTERNS.md (§ Navigation wiring — lean local back-stack within Dest.Calibration, mirroring Dest.Macros; § SAVE_CONFIG re-handshake)
    - .planning/phases/09-calibration-maintenance/09-RESEARCH.md (§ Code Examples 5 SAVE_CONFIG; § Don't Hand-Roll — reuse the G2 path verbatim, write no new reconnect code)
  </read_first>
  <action>
    TopRoute.kt: add `Calibration` to enum class Dest.

    AppDrawer.kt: add ONE DrawerTileSpec(label = "Calibration", symbol = a unique calibration glyph,
    dest = Dest.Calibration) to DRAWER_TILES (D-14 single tile). Flip the previously-greyed/INERT calibration
    affordance (if any) to live, mirroring how 08-07 flipped the Console/Macros tiles.

    AppShell.kt: build the five calibration holders with `remember(store) { …Holder(scope, store) }` (re-keyed on
    spine rebuild), passing the live-session CommandDispatcher. Add a `Dest.Calibration -> CalibrationHubScreen(...)`
    arm to when(dest). Route hub→routine via a lean LOCAL back-stack WITHIN Dest.Calibration (a calibration
    sub-dest state, mirroring how Dest.Macros hosts Bookmarked-vs-System sub-screens at :103-123,257-277) — NOT five
    new top-level Dests. The hub's onNavigate(routine) pushes the sub-dest; a BackHandler within Dest.Calibration
    pops to the hub. Because BedMeshScreen's Load selector is a scrollable Field, add Dest.Calibration to the
    swipe-drawer-suppression set at :222 (the Files Views-in-Compose scroll lesson). Wire the Z-tilt vs QGL variant
    selection and the bed-mesh Save/probe Save SAVE_CONFIG dispatch through the per-session dispatcher (the screens
    already emit the dispatch; this just supplies the live dispatcher + sub-routing).

    SAVE_CONFIG re-handshake (D-12): NO new reconnect code. The screens dispatch SAVE_CONFIG; the existing 05-10 G2
    path (notify_klippy_shutdown then notify_klippy_ready on the still-open socket → runHandshake re-runs
    objects/subscribe) already recovers the spine. EXTEND KlippyReadyResyncTest to assert a SAVE_CONFIG-induced
    shutdown→ready cycle re-runs the full handshake (re-subscribe + the screws-config + temperature one-shot reads),
    so the new calibration objects re-subscribe after the restart — proving the calibration result objects are
    re-populated post-SAVE_CONFIG without a force-stop.
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testReleaseUnitTest --tests '*KlippyReadyResync*' --tests '*ReHandshake*' --no-daemon" 2>&1 | tr -d '\r' | tail -15 && /mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:assembleRelease --no-daemon" 2>&1 | tr -d '\r' | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - `:app:assembleRelease` SUCCESSFUL with the calibration route live.
    - TopRoute.kt Dest enum contains Calibration (grep); AppDrawer DRAWER_TILES has exactly ONE Calibration tile
      (grep "Calibration" in AppDrawer.kt hits a single DrawerTileSpec).
    - AppShell when(dest) has a Dest.Calibration arm rendering CalibrationHubScreen (grep both); the five holders
      are built via remember(store) (grep each Holder).
    - Dest.Calibration is in the swipe-suppression set (grep at the suppression line includes Calibration).
    - KlippyReadyResyncTest GREEN and extended to cover the SAVE_CONFIG shutdown→ready→re-subscribe cycle including
      the calibration objects (grep the new assertion references a calibration object name or the full handshake).
    - Full `:app:testReleaseUnitTest` green (no regression to the prior suite).
  </acceptance_criteria>
  <done>Calibration reachable from a single drawer tile → hub → five sub-routes; SAVE_CONFIG reuses the G2 re-handshake, regression-extended; build + full suite green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: On-device gate — heatmap perf + full live-E5 UAT (the mock-vs-reality backstop)</name>
  <files>.planning/phases/09-calibration-maintenance/09-UAT.md, docs/moonraker-capabilities.md</files>
  <action>
    Build, sign, and install the release APK on flox; run the 09-UAT.md checklist on the live Ender 5 Plus;
    record gfxinfo numbers in docs/moonraker-capabilities.md and the per-item PASS/FAIL in 09-UAT.md. See
    how-to-verify for the exact six items. Claude automates build/install/gfxinfo-capture; Matthew navigates the
    app, turns screws, and confirms the visual/functional results.
  </action>
  <what-built>
    The complete Calibration phase: hub + screws-tilt + Z-tilt/QGL + bed-mesh (heatmap + profiles) +
    probe-calibrate, wired into the drawer, with SAVE_CONFIG routed through the G2 re-handshake, and the D-15
    Files-delete scoping fix. Release APK built and installed on flox.
  </what-built>
  <how-to-verify>
    Install the release build on flox (sign per E:\Android\sign-release.bat, adb install) with the live Ender 5
    Plus reachable (192.168.1.120:7125). Run the 09-UAT.md checklist:

    1. **Hub gating** — open the Calibration drawer tile; confirm supported routines (screws-tilt, z_tilt, bed_mesh,
       probe) render accent-first and QGL renders greyed-but-tappable last (E5 has no gantry, D-02).
    2. **Bed-mesh heatmap + Adreno-320 perf gate (CALIB-04)** — open bed-mesh on the live mesh; capture gfxinfo
       (`adb shell dumpsys gfxinfo <pkg> framestats`) over a scale-toggle interaction; apply the Phase-3/5 two-part
       liveness+latency gate (allocation-free/no-loop/0 frozen + sparse-redraw p95 within the established budget).
       Record the numbers in docs/moonraker-capabilities.md (the system-of-record on flox/API 30).
    3. **Z_TILT_ADJUST converge (CALIB-03, the QGL proxy D-02)** — Run; confirm the live feed streams, done =
       applied (data_table view populates), no false "could not be sent".
    4. **Screws-tilt guided loop (CALIB-02)** — Run; confirm the worst-screw + clock-face turn display; turn a screw
       by hand; Run again (re-probe); confirm it advances toward "X of N in tolerance".
    5. **Probe-calibrate Accept → SAVE_CONFIG recovery (CALIB-05/D-12)** — Start, jog with TESTZ, Accept, Save →
       amber gate → SAVE_CONFIG; confirm klippy shutdown→ready and the spine reconnects automatically (G2), no
       force-stop.
    6. **D-15 delete scoping (CALIB-06)** — start a print; in Files confirm ONLY the printing file is undeletable;
       every other idle file stays deletable.

    Report PASS/FAIL per item + the gfxinfo numbers. On any FAIL, capture the runtime behavior (logcat / the actual
    object shape) — that is exactly the mock-vs-reality evidence the green unit suite can't provide.
  </how-to-verify>
  <verify>
    <human-check>All six 09-UAT items pass on flox + live Ender 5 Plus; heatmap gfxinfo within the Adreno-320 budget; numbers recorded in docs/moonraker-capabilities.md.</human-check>
  </verify>
  <acceptance_criteria>
    - All six UAT items report PASS (or any FAILs are captured with runtime evidence and routed to a gap-closure plan).
    - The heatmap gfxinfo two-part gate passes (0 frozen frames + p95 within budget) and the numbers are recorded.
    - The SAVE_CONFIG → G2 recovery is observed live (spine reconnects without force-stop).
    - The D-15 delete-scoping check passes (only the printing file undeletable).
  </acceptance_criteria>
  <resume-signal>Type "approved" with the per-item PASS/FAIL + gfxinfo numbers, or describe which item failed and the observed behavior.</resume-signal>
  <done>All six 09-UAT items pass on flox + live Ender 5 Plus; heatmap gfxinfo within the Adreno-320 budget and recorded; phase proven on real hardware.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| drawer tap → Dest.Calibration sub-routes | Navigation into the printer-altering routine pages |
| SAVE_CONFIG → host restart → re-handshake | App-triggered klippy restart; the spine must resync, not lie about stale state |
| live E5 object shapes → on-device render/parse | The real wire shapes are the ultimate contract (mock-vs-reality backstop) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-07-01 | Spoofing | stale state after SAVE_CONFIG restart | mitigate | The G2 re-handshake re-runs objects/query + subscribe on klippy ready (CONN-04), overwriting stale state; regression-extended in KlippyReadyResyncTest + verified live in UAT item 5 |
| T-09-07-02 | Tampering | dead/unsafe calibration button on a printer lacking the object | mitigate | Hub greys unsupported routines (hasObject); QGL greyed on E5 (D-02) — verified in UAT item 1 |
| T-09-07-03 | Denial of Service | heatmap fill-rate stalling the Adreno-320 floor | mitigate | On-device gfxinfo two-part gate (UAT item 2) is the backstop for the allocation-free heatmap |
| T-09-07-04 | Information Disclosure | a mock-vs-reality shape gap shipping silently | mitigate | The full live-E5 UAT is the mandatory backstop (two prior strikes); any FAIL captures runtime evidence, not a green-suite assumption |
| T-09-07-SC | Tampering | npm/pip/cargo installs | mitigate | Zero packages installed (RESEARCH § Package Legitimacy Audit: N/A) |
</threat_model>

<verification>
- Quick run: `gw.bat :app:testReleaseUnitTest --tests '*KlippyReadyResync*' --no-daemon` green.
- `:app:assembleRelease` SUCCESSFUL; full `:app:testReleaseUnitTest` green.
- On-device: all six 09-UAT items pass on flox + live Ender 5 Plus; heatmap gfxinfo within budget (recorded).
</verification>

<success_criteria>
- Single Calibration drawer tile → hub → five routine sub-routes; SAVE_CONFIG reuses the G2 re-handshake (regression-extended).
- Heatmap clears the Adreno-320 perf gate on flox.
- Full live-E5 UAT (screws loop, Z-tilt converge, heatmap, probe Accept→SAVE_CONFIG recovery, D-15 delete scoping) passes — the mock-vs-reality backstop held.
</success_criteria>

<output>
Create `.planning/phases/09-calibration-maintenance/09-07-SUMMARY.md` when done — record the UAT results + gfxinfo numbers, and update 09-UAT.md with the sign-off.
</output>
