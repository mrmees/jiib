---
phase: 09-calibration-maintenance
plan: 03
type: execute
wave: 2
depends_on: ["09-01", "09-02"]
files_modified:
  - app/src/main/java/works/mees/dinghy/calibration/ScrewsTiltResult.kt
  - app/src/main/java/works/mees/dinghy/calibration/BedMeshModel.kt
  - app/src/main/java/works/mees/dinghy/calibration/TiltResult.kt
  - app/src/main/java/works/mees/dinghy/calibration/ManualProbeState.kt
  - app/src/main/java/works/mees/dinghy/calibration/ProbePresentGate.kt
  - app/src/main/java/works/mees/dinghy/calibration/CalibrationGate.kt
  - app/src/main/java/works/mees/dinghy/calibration/DeleteGate.kt
autonomous: true
requirements: [BEDM-01, BEDL-01, ZCAL-01, CALIB-01, CALIB-02, CALIB-03, CALIB-04, CALIB-05, CALIB-06]
user_setup: []

must_haves:
  truths:
    - "Screws-tilt result parses to the worst out-of-tolerance screw + 'X of N in tolerance', joined to config names by 1-based index"
    - "Bed-mesh model derives matrices, min/max, empty-state, and profile names from the live object — never console text"
    - "Z-tilt/QGL is a pure state machine: Done from applied==true, Failed from RpcError, never Failed from applied==false alone"
    - "Manual-probe Z bracket parses from the console line; page state derives from is_active"
    - "Files delete is allowed for every file except the one matching print_stats.filename during a print"
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/calibration/ScrewsTiltResult.kt"
      provides: "Pure parseScrewsTilt(results, config): GuidedLoopState"
      min_lines: 25
    - path: "app/src/main/java/works/mees/dinghy/calibration/BedMeshModel.kt"
      provides: "Pure BedMeshModel.from(bedMesh): BedMeshModel"
      min_lines: 25
    - path: "app/src/main/java/works/mees/dinghy/calibration/TiltResult.kt"
      provides: "Pure tiltState(dispatched, applied, failed): TiltState"
    - path: "app/src/main/java/works/mees/dinghy/calibration/ManualProbeState.kt"
      provides: "Pure parseZPosition(line) + is_active page-state derivation"
    - path: "app/src/main/java/works/mees/dinghy/calibration/DeleteGate.kt"
      provides: "Pure deleteAllowed(selectedPath, activePrintFilename, printState): Boolean (D-15)"
  key_links:
    - from: "calibration parsers"
      to: "captured *_e5.json fixtures"
      via: "the RED scaffolds from 09-01 now compile + pass"
      pattern: "fixtures/"
    - from: "BedMeshModel.from"
      to: "PrinterState.bedMesh object"
      via: "pure transform of the reducer-surfaced model"
      pattern: "bedMesh|mesh_matrix|profiles"
---

<objective>
Build the five pure result parsers + two pure gating predicates — the Nyquist core of Phase 9. Each is a pure
host-testable function fed a captured-from-real-hardware fixture, mirroring GcodeStoreParse.kt purity discipline
(runCatching/getOrDefault/mapNotNull, never `!!`). These turn the 09-01 RED scaffolds GREEN. Every result-display
sources the STRUCTURED live object (RESEARCH Pattern 1) — console parsing is reserved for the in-progress feed
only (handled in the screen plans).

Purpose: the holders + screens (09-04..09-06) render these parsers' outputs; FilesScreen (09-06) consumes the
delete gate. This is the bug-class-killing layer — pure, fixture-fed, off-hardware.
Output: 7 pure files in calibration/; the 7 Wave-0 scaffolds GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-calibration-maintenance/09-RESEARCH.md
@.planning/phases/09-calibration-maintenance/09-PATTERNS.md
@app/src/test/resources/fixtures/screws_tilt_adjust_e5.json
@app/src/test/resources/fixtures/bed_mesh_e5.json
@app/src/test/resources/fixtures/manual_probe_e5.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Screws-tilt, bed-mesh, and tilt parsers (the three result displays)</name>
  <files>app/src/main/java/works/mees/dinghy/calibration/ScrewsTiltResult.kt, app/src/main/java/works/mees/dinghy/calibration/BedMeshModel.kt, app/src/main/java/works/mees/dinghy/calibration/TiltResult.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/console/GcodeStoreParse.kt:9-38 (THE pure-parser template — runCatching{}.getOrDefault, mapNotNull, no I/O/coroutines/Compose)
    - app/src/main/java/works/mees/dinghy/ui/route/TopRoute.kt:47-51 (the pure when-based state-machine idiom for tiltState)
    - app/src/main/java/works/mees/dinghy/state/PrinterState.kt (the ScrewsTiltObject/BedMeshObject/ScrewConfig model types added in 09-02 — read their actual field names)
    - app/src/test/java/works/mees/dinghy/calibration/ScrewsTiltResultTest.kt + BedMeshModelTest.kt + TiltResultTest.kt (the RED scaffolds — write the parsers to their asserted contract)
    - app/src/test/resources/fixtures/screws_tilt_adjust_e5.json + bed_mesh_e5.json + configfile_screws_e5.json (the real shapes)
    - .planning/phases/09-calibration-maintenance/09-RESEARCH.md (§ Code Examples 1/2/3; § Common Pitfalls 1/2/4; § Don't Hand-Roll — read results[*].adjust/.sign, DON'T recompute clock-minute math)
    - .planning/phases/09-calibration-maintenance/09-PATTERNS.md (the parser-signature table)
  </read_first>
  <behavior>
    - parseScrewsTilt: given the captured results + config, returns the WORST out-of-tolerance screw (max |adjust as minutes|, is_base==true excluded as the reference), the in-tol count "X of N", and each screw's display joining results["screwN"] (1-based) to config screwN_name BY INDEX (Pitfall 1). 3-screw AND 4-screw inputs both work (D-04). adjust/sign read verbatim from the object ("01:15"/"CW") — no math.
    - BedMeshModel.from: returns meshMatrix (2D), probedMatrix (dots), meshMin/meshMax ([x,y]), profileName, and profileNames (the `profiles` KEYS). Empty-state = meshMatrix empty OR profileName=="" — SEPARATE from profileNames-non-empty (Pitfall 4).
    - tiltState(dispatched, applied, failed): applied==true → Done; (dispatched && !applied && !failed) → Running; failed → Failed; idle otherwise. NEVER infers Failed from applied==false (Pitfall 2).
  </behavior>
  <action>
    Create calibration/ScrewsTiltResult.kt — `parseScrewsTilt(results: ..., config: ScrewConfig): GuidedLoopState`
    as a pure free function with the GcodeStoreParse runCatching/getOrDefault discipline. GuidedLoopState data
    class carries the per-screw list (name, sign, adjust hh:mm, degrees, isInTol, isBase), the worst screw, and the
    in-tol count. "Worst" = max absolute adjust-minutes among non-base, non-in-tol screws. The degrees view derives
    from the hh:mm value (clock-minutes → degrees) for the UI-SPEC row's second column — a pure formatting helper.

    Create calibration/BedMeshModel.kt — `BedMeshModel.from(bedMesh: BedMeshObject?): BedMeshModel` (companion or
    top-level), pure. Exposes meshMatrix, probedMatrix, meshMin, meshMax, profileName, profileNames (sorted keys),
    and isEmpty (Pitfall 4 condition). Reuse PrinterState's already-parsed BedMeshObject from 09-02 (do not re-walk
    raw JSON here — the reducer owns that; this is the view-model transform).

    Create calibration/TiltResult.kt — `tiltState(dispatched: Boolean, applied: Boolean?, failed: Boolean): TiltState`
    enum/sealed, mirroring the TopRoute.derive when-idiom. Shared by Z-tilt AND QGL (D-02 same code path).
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testReleaseUnitTest --tests '*ScrewsTiltResultTest*' --tests '*BedMeshModelTest*' --tests '*TiltResultTest*' --no-daemon" 2>&1 | tr -d '\r' | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - ScrewsTiltResultTest GREEN: worst-screw + in-tol count correct over the real fixture; 3-screw and 4-screw
      cases both pass; results join to config names by 1-based index.
    - BedMeshModelTest GREEN: matrices/min/max/profile names parsed; empty-state distinguished from profiles-list.
    - TiltResultTest GREEN: applied==false with no failure → Running (not Failed); failed → Failed; applied==true → Done.
    - No `!!` in any of the three files (grep "!!" returns nothing in these files).
    - No Compose import, no coroutine import in these files (pure — grep).
  </acceptance_criteria>
  <done>Three structured-object result parsers green over real fixtures; pure, fixture-fed, no console parsing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Manual-probe, probe-present gate, calibration gate, delete gate</name>
  <files>app/src/main/java/works/mees/dinghy/calibration/ManualProbeState.kt, app/src/main/java/works/mees/dinghy/calibration/ProbePresentGate.kt, app/src/main/java/works/mees/dinghy/calibration/CalibrationGate.kt, app/src/main/java/works/mees/dinghy/calibration/DeleteGate.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/console/GcodeStoreParse.kt (the line-parse purity template for parseZPosition)
    - app/src/main/java/works/mees/dinghy/state/Capabilities.kt:52 (hasObject predicate the gates read)
    - app/src/main/java/works/mees/dinghy/ui/files/FilesScreen.kt:76-94 (the deleteEnabled predicate DeleteGate replaces; printFilename path-form)
    - docs/moonraker-capabilities.md (the print_stats.filename relative path-form, e.g. "miata/airbox-bracket.gcode", NO leading "gcodes/")
    - app/src/test/java/works/mees/dinghy/calibration/ManualProbeStateTest.kt + ProbePresentGateTest.kt + CalibrationGateTest.kt + app/src/test/java/works/mees/dinghy/ui/files/FilesDeleteGateTest.kt (the RED scaffolds)
    - app/src/test/resources/fixtures/manual_probe_e5.json (the real shape + the "Z position: a --> b <-- c" line text recorded in 09-01)
    - .planning/phases/09-calibration-maintenance/09-RESEARCH.md (§ Code Examples 4/6; § Assumptions A3; § Architecture Patterns Pattern 3)
  </read_first>
  <behavior>
    - parseZPosition("Z position: 5.001 --> 4.901 <-- 4.800") → bracket(lower=5.001, current=4.901, upper=4.800); a malformed/non-matching line → null.
    - manualProbePageState(manualProbe): derives Idle/Active/Accepted from is_active + whether an offset was captured (Pattern 3).
    - probeCalibrateGate(caps): caps.hasObject("probe") → PROBE_CALIBRATE, else Z_ENDSTOP_CALIBRATE (A3).
    - calibrationSupport(caps): per routine → isSupported = caps.hasObject(routine-object); all five always listed (UI-SPEC §1), supported sorted first.
    - deleteAllowed(selectedPath, activePrintFilename, printState): during Printing/Paused, false ONLY when selectedPath == activePrintFilename (and activePrintFilename non-empty); otherwise true. Idle → always true (D-15).
  </behavior>
  <action>
    Create calibration/ManualProbeState.kt — pure `parseZPosition(line: String): ZPositionBracket?` (regex/parse,
    runCatching → null on mismatch) + `manualProbePageState(...)` deriving the Idle/Active/Accepted page state off
    `manual_probe.is_active` (Pattern 3).

    Create calibration/ProbePresentGate.kt — pure `probeCalibrateGate(caps: Capabilities): CommandSpec<*>` (or a
    small enum the screen maps) returning PROBE_CALIBRATE vs Z_ENDSTOP_CALIBRATE per A3.

    Create calibration/CalibrationGate.kt — pure `calibrationSupport(caps): List<RoutineEntry>` marking each of
    the five routines supported/unsupported via hasObject, supported-first ordering (the hub's data source; all
    five always present per the owner override — unsupported is greyed-but-listed, not omitted).

    Create calibration/DeleteGate.kt — pure `deleteAllowed(selectedPath: String?, activePrintFilename: String,
    printState: PrintState): Boolean` (D-15). Compare in the SAME relative dir-prefixed form (no leading
    "gcodes/"). FilesScreen (09-06) calls this; the helper is the host-tested seam.
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testReleaseUnitTest --tests '*ManualProbeStateTest*' --tests '*ProbePresentGateTest*' --tests '*CalibrationGateTest*' --tests '*FilesDeleteGateTest*' --no-daemon" 2>&1 | tr -d '\r' | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - ManualProbeStateTest GREEN: bracket parse correct; malformed line → null; page state from is_active.
    - ProbePresentGateTest GREEN: probe-present → PROBE_CALIBRATE; absent → Z_ENDSTOP_CALIBRATE.
    - CalibrationGateTest GREEN: each routine isSupported iff hasObject(object); all five listed.
    - FilesDeleteGateTest GREEN: during print only the print_stats.filename-matching file is undeletable; other
      idle files deletable; idle → all deletable.
    - All four files pure (no Compose/coroutine import; no `!!`).
  </acceptance_criteria>
  <done>Manual-probe parse + three gating predicates green; D-15 delete logic host-tested as a pure helper.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| captured fixture / live object → pure parser | Schema-loose printer JSON parsed into typed view-models |
| delete-gate predicate → FilesScreen | The pure gate decides which file is destructively deletable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03-01 | Denial of Service | malformed result/mesh/probe JSON | mitigate | runCatching{}.getOrDefault + null-returning parse (GcodeStoreParse discipline); a bad shape degrades to safe-empty, never throws |
| T-09-03-02 | Tampering | accidental deletion of the actively-printing file | mitigate | deleteAllowed returns false for the print_stats.filename-matching file during a print (D-15); host-tested as a pure predicate so the path-form match can't silently regress |
| T-09-03-03 | Spoofing | inferring "done" from a wrong signal | mitigate | tiltState never infers Failed from applied==false (Pitfall 2); Done is the explicit applied==true / object signal, not a bare ack |
| T-09-03-SC | Tampering | npm/pip/cargo installs | mitigate | Zero packages installed (RESEARCH § Package Legitimacy Audit: N/A) |
</threat_model>

<verification>
- Quick run: `gw.bat :app:testReleaseUnitTest --tests '*ScrewsTilt*' --tests '*BedMesh*' --tests '*TiltResult*' --tests '*ManualProbe*' --tests '*ProbePresentGate*' --tests '*CalibrationGate*' --tests '*FilesDelete*' --no-daemon` — all seven Wave-0 scaffolds GREEN.
- Per-wave merge: full `:app:testReleaseUnitTest` green.
</verification>

<success_criteria>
- Seven Wave-0 RED scaffolds now GREEN; every parser pure + fixture-fed.
- Result displays source the structured object, console parse reserved for the in-progress feed (screen plans).
- D-15 delete gate is a host-tested pure predicate.
</success_criteria>

<output>
Create `.planning/phases/09-calibration-maintenance/09-03-SUMMARY.md` when done.
</output>
