---
phase: 09-calibration-maintenance
plan: 06
type: execute
wave: 4
depends_on: ["09-03", "09-05"]
files_modified:
  - app/src/main/java/works/mees/dinghy/calibration/ProbeCalibrateHolder.kt
  - app/src/main/java/works/mees/dinghy/ui/calibration/ProbeCalibrateScreen.kt
  - app/src/main/java/works/mees/dinghy/ui/files/FilesScreen.kt
  - app/src/main/java/works/mees/dinghy/ui/files/FileBrowserHolder.kt
  - app/src/test/java/works/mees/dinghy/calibration/ProbeCalibrateHolderTest.kt
  - app/src/test/java/works/mees/dinghy/ui/files/FileBrowserHolderDeleteTest.kt
  - docs/ui_design/CLAUDE.md
  - .planning/phases/09-calibration-maintenance/09-UAT.md
autonomous: true
requirements: [ZCAL-01, CALIB-05, CALIB-06]
user_setup: []

must_haves:
  truths:
    - "Probe-Calibrate is the one interactive page: Start opens a manual-probe session, TESTZ jogs Z, Accept/Abort closes it, Save persists via amber SAVE_CONFIG gate"
    - "Page enable/disable is driven by manual_probe.is_active; Back is suppressed while a session is live"
    - "Probe-less printers surface Z_ENDSTOP_CALIBRATE via the same page (gated by probe-present, A3)"
    - "Files delete is scoped: during a print only the currently-printing file is undeletable; every other idle file stays deletable"
    - "The relaxed delete rule is recorded in docs/ui_design/ and the scoping check is added to 09-UAT.md"
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/ui/calibration/ProbeCalibrateScreen.kt"
      provides: "The interactive manual-probe Z-calibrate page (Idle/Active/Accepted states)"
      min_lines: 45
    - path: "app/src/main/java/works/mees/dinghy/calibration/ProbeCalibrateHolder.kt"
      provides: "Headless holder driving page state off manual_probe.is_active + z_position"
    - path: "app/src/main/java/works/mees/dinghy/ui/files/FilesScreen.kt"
      provides: "D-15 fix (screen gate): deleteEnabled gates on print_stats.filename, not global printingActive"
    - path: "app/src/main/java/works/mees/dinghy/ui/files/FileBrowserHolder.kt"
      provides: "D-15 fix (holder gate): requestDeleteSelected() blocks ONLY the actively-printing file, not every delete during a print"
    - path: ".planning/phases/09-calibration-maintenance/09-UAT.md"
      provides: "On-device UAT checklist incl. the delete-during-print scoping check"
  key_links:
    - from: "ProbeCalibrateScreen"
      to: "manual_probe.is_active"
      via: "ProbeCalibrateHolder drives Idle/Active/Accepted page state"
      pattern: "isActive|is_active|manualProbe"
    - from: "FilesScreen.deleteEnabled"
      to: "deleteAllowed(...)"
      via: "the pure D-15 helper from 09-03"
      pattern: "deleteAllowed"
---

<objective>
Build the Probe-Calibrate page (CALIB-05/D-01 — the ONE interactive/stateful calibration page: a manual-probe
Z-calibrate session) and fold in the Phase-7 Files-delete-gating fix (CALIB-06/D-15). The probe page is a
specialized fine Z-move page driven by manual_probe.is_active: Start → TESTZ jog → Accept/Abort → amber
SAVE_CONFIG. The D-15 fix is a one-predicate correctness change routed through the host-tested deleteAllowed
helper, plus recording the relaxed UI rule and the UAT check. Per docs/ui_design/ LAW + 09-UI-SPEC §5/§cross-cutting.

Purpose: manual-probe Z offset calibration from the printer without a browser; and stop blocking deletion of
non-printing files during a print.
Output: probe-calibrate screen + holder, FilesScreen D-15 fix, the docs/ui_design rule, and 09-UAT.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-calibration-maintenance/09-UI-SPEC.md
@.planning/phases/09-calibration-maintenance/09-PATTERNS.md
@.planning/phases/09-calibration-maintenance/09-RESEARCH.md
@docs/ui_design/CLAUDE.md
@app/src/test/resources/fixtures/manual_probe_e5.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ProbeCalibrateHolder + ProbeCalibrateScreen (the interactive session page)</name>
  <files>app/src/main/java/works/mees/dinghy/calibration/ProbeCalibrateHolder.kt, app/src/main/java/works/mees/dinghy/ui/calibration/ProbeCalibrateScreen.kt, app/src/test/java/works/mees/dinghy/calibration/ProbeCalibrateHolderTest.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/extrude/ExtrudeHolder.kt + app/src/test/java/works/mees/dinghy/ui/extrude/ExtrudeHolderTest.kt (holder template + test; the setActiveTool imperative-setter precedent for the step-preset selection)
    - app/src/main/java/works/mees/dinghy/calibration/ManualProbeState.kt + ProbePresentGate.kt (parseZPosition, manualProbePageState, probeCalibrateGate — from 09-03)
    - app/src/main/java/works/mees/dinghy/ui/move/MoveScreen.kt (the jog-pad grammar: Z▲/Z▼ nudge buttons + step-preset pills — the page mirrors this)
    - app/src/main/java/works/mees/dinghy/designsystem/ConfirmGuard.kt (the amber variant from 09-05, for the Save restart gate)
    - app/src/main/java/works/mees/dinghy/command/CommandRegistry.kt (probeCalibrate, zEndstopCalibrate, testZ, accept, abort, saveConfig — from 09-02)
    - img/nozzle.svg (the idle Focus asset)
    - .planning/phases/09-calibration-maintenance/09-UI-SPEC.md (§ Per-Screen §5 — the three states driven by is_active, the live z_position hero, the Z position bracket render, the white step-preset pills 1/0.1/0.05/0.025, the blue Z▲/Z▼ TESTZ, the state-adaptive gutter Start/Accept/Abort/Save/Back, Back-suppressed-while-active)
    - .planning/phases/09-calibration-maintenance/09-RESEARCH.md (§ Architecture Pattern 3; § Code Examples 4; A3 probe-present gate)
  </read_first>
  <behavior>
    - ProbeCalibrateHolder.vm = manualProbePageState(printerState.manualProbe) → {Idle, Active, Accepted} + the live zPosition + the parsed Z-position bracket (parsed from the latest `store.gcodeResponses` line via parseZPosition — the SharedFlow the holder must collect, NOT a printerState field) + the selected TESTZ step + which command to Start (probeCalibrateGate → PROBE_CALIBRATE vs Z_ENDSTOP_CALIBRATE) + folded dispatcher Failure error text.
    - Page enable/disable derives from is_active: jog pad + Accept/Abort enabled only while Active; Start only while Idle; Save only while Accepted.
  </behavior>
  <action>
    Create calibration/ProbeCalibrateHolder.kt copying ExtrudeHolder structure — combine store.printerState +
    store.capabilities into a ProbeCalibrateVm. Drive page state off manual_probe.is_active (Pattern 3). Hold the
    selected TESTZ step (default a sensible fine step, e.g. 0.05mm) via an imperative setStep() (setActiveTool
    precedent). Resolve the Start command via probeCalibrateGate(caps) (A3). The "Z position: a --> b <-- c" bracket
    is NOT in printerState/manual_probe — it arrives only on the un-throttled `store.gcodeResponses` SharedFlow
    (the same raw stream the console taps, Phase-8 D-04). In init, `scope.launch { store.gcodeResponses.collect { … } }`,
    run each line through parseZPosition, and keep the latest parseable bracket in a MutableStateFlow that `combine`s
    into the VM (clear it when is_active flips false). Fold dispatcher Failure into errorText. Add
    ProbeCalibrateHolderTest: seed manual_probe.is_active=false → Idle; is_active=true → Active with live
    z_position; emit a real "Z position: 0.30 --> 0.25 <-- 0.20" frame on store.gcodeResponses while Active and
    assert the VM exposes the parsed bracket; after Accept (is_active flips false with a captured offset) → Accepted.

    Create ui/calibration/ProbeCalibrateScreen.kt per UI-SPEC §5 (designed fresh in the grammar, no mockup):
    - Focus idle: img/nozzle.svg square ~90%H + "Start, then lower the nozzle to the bed."
    - Focus active: the live manual_probe.z_position as a big Geist Mono Display hero (updates as you nudge) + the
      active prompt; if the "Z position: {lower} --> {current} <-- {upper}" line is parseable, render the bracket
      under the hero in Mono.
    - Focus accepted: the captured offset + "Offset set. Save to keep it (restarts printer)."
    - Field = the jog pad (disabled until is_active): step-preset row (white pills 1/0.1/0.05/0.025mm, selecting a
      step is a setting) + Z▲/Z▼ (blue, fire testZ(+step)/testZ(-step)).
    - Gutter state-adaptive: Idle → Start (blue, dispatch the gated PROBE_CALIBRATE/Z_ENDSTOP_CALIBRATE) + Back
      (green); Active → Accept (green, dispatch accept) + Abort (red, dispatch abort) — BACK SUPPRESSED while a
      session is live (leaving mid-probe = dangling session); Accepted → Save (amber → ConfirmGuard amber restart
      gate → dispatch saveConfig) + Back (green, discards the in-memory offset).
    - Failure → SeverityToast(Error, RpcError text, never transport e.message). Ratio-only sizing, token-only color.
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testReleaseUnitTest --tests '*ProbeCalibrateHolderTest*' --no-daemon" 2>&1 | tr -d '\r' | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - ProbeCalibrateHolderTest GREEN: is_active=false → Idle; is_active=true → Active with the live z_position; a
      "Z position:" frame emitted on store.gcodeResponses surfaces the parsed bracket in the VM; post-Accept → Accepted.
    - ProbeCalibrateHolder collects store.gcodeResponses (grep "gcodeResponses" in ProbeCalibrateHolder.kt) — the
      bracket is not sourced from a printerState field.
    - ProbeCalibrateScreen gutter is state-adaptive (grep Start/Accept/Abort/Save) and Back is suppressed while
      Active (the Active branch renders no Back — grep confirms only Accept + Abort in the Active gutter).
    - Z▲/Z▼ dispatch testZ(+step)/testZ(-step) (grep "testZ"); step pills are white/setting intent.
    - Save routes through the amber ConfirmGuard then dispatches saveConfig (grep both in order).
    - Start resolves via probeCalibrateGate (grep) so a probe-less printer gets Z_ENDSTOP_CALIBRATE.
    - Token-purity: no raw hex in ProbeCalibrateScreen.kt; holder has no Compose import + no second throttle.
  </acceptance_criteria>
  <done>Interactive manual-probe page (Idle/Active/Accepted off is_active), TESTZ jog, Accept/Abort, amber Save; A3 probe-less fallback; holder green.</done>
</task>

<task type="auto">
  <name>Task 2: D-15 Files-delete scoping fix (BOTH gate layers) + UI rule record + 09-UAT.md</name>
  <files>app/src/main/java/works/mees/dinghy/ui/files/FilesScreen.kt, app/src/main/java/works/mees/dinghy/ui/files/FileBrowserHolder.kt, app/src/test/java/works/mees/dinghy/ui/files/FileBrowserHolderDeleteTest.kt, docs/ui_design/CLAUDE.md, .planning/phases/09-calibration-maintenance/09-UAT.md</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/files/FilesScreen.kt:76-94 (the current too-broad deleteEnabled = !printingActive predicate — the SCREEN gate)
    - app/src/main/java/works/mees/dinghy/ui/files/FileBrowserHolder.kt:90-99 (requestDeleteSelected() — the HOLDER gate that unconditionally errors "Cannot delete while a print is active." during Printing/Paused; this is the second gate that must ALSO be scoped or the screen fix is invisible)
    - app/src/main/java/works/mees/dinghy/state/FileBrowserModels.kt:22-44 (FileBrowserRow fields: there is NO `.path` — the relative form is `relativeFilename`, the API form is `rootPrefixedPath` = "gcodes/{rel}"; deleteAllowed compares the RELATIVE form against print_stats.filename, while client.deleteFile() still takes rootPrefixedPath)
    - app/src/main/java/works/mees/dinghy/calibration/DeleteGate.kt (the pure deleteAllowed helper from 09-03 — signature deleteAllowed(selectedPath, activePrintFilename, printState))
    - app/src/test/java/works/mees/dinghy/ui/files/FilesDeleteGateTest.kt (the green host test backing the helper)
    - docs/moonraker-capabilities.md (the print_stats.filename relative path-form — e.g. "miata/airbox-bracket.gcode", NO leading "gcodes/" — which matches FileBrowserRow.relativeFilename, NOT rootPrefixedPath)
    - docs/ui_design/CLAUDE.md (where to record the relaxed "Delete is scoped to the active file, not idle-only" rule)
    - .planning/phases/07-files-print-control-core-print-loop-gate/07-UAT.md (the prior UAT format to mirror for 09-UAT.md) + .planning/todos/pending/files-delete-gating-too-broad.md (the folded todo)
    - .planning/phases/09-calibration-maintenance/09-VALIDATION.md (§ Manual-Only Verifications — the on-device checks 09-UAT.md must enumerate)
  </read_first>
  <action>
    There are TWO gates and BOTH must be scoped — fixing only the screen leaves the holder silently blocking every
    delete during a print (the screen change would be invisible).

    (a) FilesScreen.kt (SCREEN gate): replace the global `deleteEnabled = selected != null && !printingActive` with
    the D-15 scoped form using the pure helper from 09-03: `deleteEnabled = selected != null &&
    deleteAllowed(selected.relativeFilename, printerState.printFilename, printerState.printState)`. Use
    `relativeFilename` (the dir-prefixed, no-leading-"gcodes/" form that matches print_stats.filename) — NOT `.path`
    (no such field) and NOT `rootPrefixedPath` (that carries the "gcodes/" prefix and won't match printFilename).
    Remove the now-unused `printingActive` local if nothing else uses it.

    (b) FileBrowserHolder.kt (HOLDER gate, lines 90-99): `requestDeleteSelected()` currently rejects ALL deletes
    during Printing/Paused with the bare `if (printState == Printing || Paused)` block. Replace that bare check with
    the same scoped predicate: only reject (set the "Cannot delete the file that is currently printing." error) when
    `!deleteAllowed(selected.relativeFilename, printerState.value.printFilename, printState)`; otherwise fall through
    to `client.deleteFile(selected.rootPrefixedPath)` (the API still needs the gcodes-prefixed path). Keep the
    existing post-delete row-removal/state update. Add FileBrowserHolderDeleteTest: with printState=Printing and
    printFilename set, deleting the matching file is rejected (error set, no client.deleteFile), deleting a DIFFERENT
    idle file calls client.deleteFile; idle printState → any delete proceeds.

    Both layers route through the SAME host-tested deleteAllowed helper so the path-form match can't silently regress.

    docs/ui_design/CLAUDE.md: record the relaxed rule — "Delete is scoped to the active print file, not idle-only:
    during a print only the currently-printing file (print_stats.filename) is undeletable; every other file stays
    deletable." This supersedes the original "Delete is idle-only" rule (D-15 deliverable). Also fold in the
    bed-mesh Save-name keyboard carve-out cross-reference if not already recorded in 09-05 (single source of truth).

    Create .planning/phases/09-calibration-maintenance/09-UAT.md mirroring 07-UAT.md's format — the on-device UAT
    checklist for the phase, enumerating the 09-VALIDATION § Manual-Only Verifications: (1) bed-mesh heatmap render
    + Adreno-320 gfxinfo gate on flox; (2) Z_TILT_ADJUST run-and-converge on live E5 (the QGL proxy, D-02);
    (3) screws-tilt guided loop end-to-end (turn → re-probe → advance); (4) Z-calibrate Accept → SAVE_CONFIG →
    spine recovers (G2); (5) **the D-15 delete-during-print scoping check**: start a print, confirm ONLY the
    printing file is undeletable while every other idle file stays deletable. These run in 09-07's on-device gate.
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testReleaseUnitTest --tests '*FilesDeleteGateTest*' --tests '*FileBrowserHolderDeleteTest*' --no-daemon" 2>&1 | tr -d '\r' | tail -10 && grep -q "deleteAllowed" app/src/main/java/works/mees/dinghy/ui/files/FilesScreen.kt && grep -q "deleteAllowed" app/src/main/java/works/mees/dinghy/ui/files/FileBrowserHolder.kt && ! grep -q "printState == PrintState.Printing || printState == PrintState.Paused" app/src/main/java/works/mees/dinghy/ui/files/FileBrowserHolder.kt && test -f .planning/phases/09-calibration-maintenance/09-UAT.md && grep -qi "scoped\|active print file\|print_stats.filename" docs/ui_design/CLAUDE.md && echo "D-15 deliverables present"</automated>
  </verify>
  <acceptance_criteria>
    - FilesScreen.kt deleteEnabled now calls deleteAllowed(selected.relativeFilename, printerState.printFilename,
      printerState.printState) (grep "deleteAllowed"); the bare `!printingActive` gate is gone (grep returns no
      `deleteEnabled = selected != null && !printingActive`).
    - FileBrowserHolder.kt requestDeleteSelected() routes through deleteAllowed (grep "deleteAllowed"); the bare
      unconditional `printState == PrintState.Printing || printState == PrintState.Paused` block is gone (grep
      returns no match); deleting a non-printing file during a print now reaches client.deleteFile.
    - FilesDeleteGateTest remains GREEN; FileBrowserHolderDeleteTest GREEN: matching-file delete rejected during
      print, different-idle-file delete proceeds during print, all deletes proceed when idle.
    - docs/ui_design/CLAUDE.md records the relaxed scoped-delete rule (grep "scoped"/"active print file").
    - .planning/phases/09-calibration-maintenance/09-UAT.md exists and contains the delete-during-print scoping
      check plus the four other manual-only verifications.
  </acceptance_criteria>
  <done>D-15 delete scoped via the host-tested helper; relaxed rule recorded in docs/ui_design/; 09-UAT.md created with the scoping check.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user tap → PROBE_CALIBRATE / TESTZ / SAVE_CONFIG | App opens a manual-probe session, nudges Z, and persists config + restarts klippy |
| selected file + print_stats.filename → delete dispatch | The delete gate decides which file is destructively deletable during a print |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-06-01 | Tampering | TESTZ Z=<step> param | mitigate | step comes from a fixed preset set, clamped in PrinterCommands.testZ (09-02, ASVS V5) — no free-text |
| T-09-06-02 | Tampering | dangling manual-probe session if user leaves mid-probe | mitigate | Back suppressed while is_active (UI-SPEC §5); user must Accept or Abort to leave the live session |
| T-09-06-03 | Tampering | destructive SAVE_CONFIG (probe offset persist) without warning | mitigate | Amber ConfirmGuard before SAVE_CONFIG (D-12); G2 re-handshake recovers the spine (wired in 09-07) |
| T-09-06-04 | Tampering | deleting the actively-printing file | mitigate | deleteAllowed scopes the gate to print_stats.filename (D-15); host-tested pure predicate; relaxing the gate does NOT widen to the printing file |
| T-09-06-05 | Information Disclosure | failure toast leaking transport detail | mitigate | SeverityToast shows only the gcode-rejection RpcError text, never transport e.message (T-05-11-01) |
| T-09-06-SC | Tampering | npm/pip/cargo installs | mitigate | Zero packages installed (RESEARCH § Package Legitimacy Audit: N/A) |
</threat_model>

<verification>
- Quick run: `gw.bat :app:testReleaseUnitTest --tests '*ProbeCalibrateHolderTest*' --tests '*FilesDeleteGateTest*' --no-daemon` green.
- `:app:assembleRelease` SUCCESSFUL; token-purity grep clean over ProbeCalibrateScreen.
- D-15 deliverables present (FilesScreen helper call, docs/ui_design rule, 09-UAT.md scoping check).
- Per-wave merge: full `:app:testReleaseUnitTest` green.
</verification>

<success_criteria>
- Interactive probe-calibrate page (Idle/Active/Accepted off is_active), TESTZ jog, Accept/Abort, amber SAVE_CONFIG; A3 probe-less fallback.
- D-15 Files delete scoped to the active print file via the host-tested helper; relaxed rule recorded; 09-UAT.md created.
- Headless holder, token-pure screen, no second throttle.
</success_criteria>

<output>
Create `.planning/phases/09-calibration-maintenance/09-06-SUMMARY.md` when done.
</output>
