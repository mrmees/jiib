---
phase: 09-calibration-maintenance
plan: 04
type: execute
wave: 3
depends_on: ["09-02", "09-03"]
files_modified:
  - app/src/main/java/works/mees/dinghy/calibration/CalibrationHubHolder.kt
  - app/src/main/java/works/mees/dinghy/calibration/ScrewsTiltHolder.kt
  - app/src/main/java/works/mees/dinghy/ui/calibration/CalibrationHubScreen.kt
  - app/src/main/java/works/mees/dinghy/ui/calibration/ScrewsTiltScreen.kt
  - app/src/test/java/works/mees/dinghy/calibration/ScrewsTiltHolderTest.kt
autonomous: true
requirements: [BEDL-01, CALIB-01, CALIB-02]
user_setup: []

must_haves:
  truths:
    - "The Calibration hub lists all five routines, supported ones first (accent), unsupported greyed-but-tappable last (owner override)"
    - "Screws-tilt renders the guided one-screw-at-a-time loop: worst out-of-tolerance screw + clock-face turn + 'X of N in tolerance'"
    - "Screws-tilt draws the bed to scale with point indicators at real coords when config coords exist; falls back to a labeled list otherwise"
    - "Run (re)probes via SCREWS_TILT_CALCULATE gated on homed state; Back is the only nav (one-shot, nothing to accept/abort)"
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/ui/calibration/CalibrationHubScreen.kt"
      provides: "The D-14 hub tile grid"
      min_lines: 30
    - path: "app/src/main/java/works/mees/dinghy/ui/calibration/ScrewsTiltScreen.kt"
      provides: "The owner-authored to-scale-bed + point-list screws-tilt screen"
      min_lines: 40
    - path: "app/src/main/java/works/mees/dinghy/calibration/ScrewsTiltHolder.kt"
      provides: "Headless StateFlow<ScrewsTiltVm> combining state + screws config + dispatcher events"
  key_links:
    - from: "ScrewsTiltHolder"
      to: "PrinterStateStore + parseScrewsTilt"
      via: "combine(printerState, screwsTiltConfig) → buildVm"
      pattern: "combine|parseScrewsTilt|screwsTiltConfig"
    - from: "ScrewsTiltScreen Run button"
      to: "CommandRegistry.screwsTiltCalculate"
      via: "dispatcher.dispatch(...)"
      pattern: "dispatch\\(.*screwsTilt"
    - from: "CalibrationHubScreen tiles"
      to: "calibrationSupport(caps)"
      via: "hasObject gating → supported/greyed"
      pattern: "calibrationSupport|hasObject"
---

<objective>
Build the Calibration hub (D-14/CALIB-01) and the screws-tilt page (CALIB-02), the locked-mockup routine. The
hub is the single drawer destination listing all five routines (owner override: all shown, unsupported greyed
but tappable, sorted last). Screws-tilt is the owner-authored spatial layout — a to-scale bed with
location-accurate point indicators + a point list — driving the D-03 guided one-screw-at-a-time loop off the
structured screws_tilt_adjust object (NOT console text). Headless holders per ADR-0001; screens per the
docs/ui_design/ LAW and 09-UI-SPEC §1/§2.

Purpose: the user runs manual bed leveling from the printer without a browser, getting per-screw clock-face turns.
Output: hub + screws-tilt screens + their headless holders; ScrewsTiltHolder host-tested.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-calibration-maintenance/09-UI-SPEC.md
@.planning/phases/09-calibration-maintenance/09-PATTERNS.md
@docs/ui_design/CLAUDE.md
@docs/ui_design/LAYOUT.md
@docs/ui_design/THEMING.md
@docs/ui_design/images/05-screws-tilt.png
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ScrewsTiltHolder + CalibrationHubHolder (headless)</name>
  <files>app/src/main/java/works/mees/dinghy/calibration/ScrewsTiltHolder.kt, app/src/main/java/works/mees/dinghy/calibration/CalibrationHubHolder.kt, app/src/test/java/works/mees/dinghy/calibration/ScrewsTiltHolderTest.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/extrude/ExtrudeHolder.kt:40-114 (THE holder template — ctor(scope, store), MutableStateFlow→StateFlow vm, init{ scope.launch{ combine(...).collect{...} } }, pure buildVm, no second throttle)
    - app/src/test/java/works/mees/dinghy/ui/extrude/ExtrudeHolderTest.kt:35-70 (the holder-test pattern — runTest(UnconfinedTestDispatcher), real PrinterStateStore(backgroundScope), store.seed(...) + runCurrent(), assert vm.value)
    - app/src/main/java/works/mees/dinghy/calibration/ScrewsTiltResult.kt + CalibrationGate.kt (the pure parsers from 09-03 the holders fold)
    - app/src/main/java/works/mees/dinghy/state/PrinterStateStore.kt (printerState, capabilities, screwsTiltConfig one-shot from 09-02)
    - app/src/main/java/works/mees/dinghy/command/CommandDispatcher.kt:94 (events: SharedFlow<DispatchEvent> — fold Failure for failed-detection)
    - .planning/phases/09-calibration-maintenance/09-PATTERNS.md (§ Headless holders; § Done/failed detection)
  </read_first>
  <behavior>
    - ScrewsTiltHolder.vm combines printerState (screwsTilt object), capabilities, and screwsTiltConfig into a ScrewsTiltVm: the GuidedLoopState (worst screw + in-tol count + per-screw rows joined to config names), homed-gate flag (D-13), and a latest-error string folded from dispatcher Failure events. No second throttle (store conflates at 250ms).
    - CalibrationHubHolder.vm exposes calibrationSupport(caps) → the ordered routine list (supported first, greyed last).
    - Failed-detection comes from CommandDispatcher.events Failure (RpcError text), never from parsing the feed.
  </behavior>
  <action>
    Create calibration/ScrewsTiltHolder.kt copying ExtrudeHolder structure exactly: ctor(scope, store), combine
    store.printerState + store.capabilities + store.screwsTiltConfig into a pure buildVm producing ScrewsTiltVm
    (GuidedLoopState from parseScrewsTilt, homedGate from PrinterState.homedAxes, errorText folded from a separate
    collector of dispatcher.events Failure). Per RESEARCH Open-Q1, if 09-01 found results does NOT persist post-run,
    have the holder capture the GuidedLoopState on the dispatch-completion edge (read the belt-and-braces one-shot
    hook exposed by the store in 09-02) rather than expecting a later status diff.

    Create calibration/CalibrationHubHolder.kt — a thin holder exposing calibrationSupport(store.capabilities) as a
    StateFlow (re-derives when capabilities change on reconnect).

    Add calibration/ScrewsTiltHolderTest.kt mirroring ExtrudeHolderTest: seed a PrinterState carrying the captured
    screws result + a ScrewConfig, runCurrent(), assert vm.worstScrew + inTolCount + that homedGate reflects
    homedAxes. Plain Kotlin holders — NO Compose annotations (ADR-0001).
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testReleaseUnitTest --tests '*ScrewsTiltHolderTest*' --no-daemon" 2>&1 | tr -d '\r' | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - ScrewsTiltHolderTest GREEN: vm.worstScrew + inTolCount correct from a seeded state; homedGate reflects homedAxes.
    - ScrewsTiltHolder + CalibrationHubHolder contain no `import androidx.compose` (grep — headless, ADR-0001).
    - ScrewsTiltHolder does NOT add a second throttle (no `.sample(`/`.debounce(` on store.printerState — grep).
    - errorText is folded from dispatcher.events Failure (grep "events" + "Failure").
  </acceptance_criteria>
  <done>Two headless holders; ScrewsTiltHolder host-tested over the captured result; no Compose, no second throttle.</done>
</task>

<task type="auto">
  <name>Task 2: CalibrationHubScreen + ScrewsTiltScreen (Compose, per UI-SPEC §1/§2)</name>
  <files>app/src/main/java/works/mees/dinghy/ui/calibration/CalibrationHubScreen.kt, app/src/main/java/works/mees/dinghy/ui/calibration/ScrewsTiltScreen.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/shell/AppDrawer.kt:61-192 (the square-outline-tile grid grammar the hub mirrors — LazyVerticalGrid + DrawerTile, accent outline vs greyed)
    - app/src/main/java/works/mees/dinghy/ui/files/FilesScreen.kt:76-214 (full Focus/Field/Gutter screen: ScreenScaffold slots, gutter OutlinedControl Row with Intent, SeverityToast consumer, dispatch)
    - app/src/main/java/works/mees/dinghy/designsystem/layout/ScreenScaffold.kt:48-63 (focus/field/gutter slots, portraitFocusAspect sacred-square)
    - app/src/main/java/works/mees/dinghy/ui/move/MoveScreen.kt (the jog-pad / value-on-glyph grammar reference for spatial point rendering)
    - app/src/main/java/works/mees/dinghy/designsystem/MaterialSymbol.kt (ligature glyph API)
    - .planning/phases/09-calibration-maintenance/09-UI-SPEC.md (§ Per-Screen §1 Hub + §2 Screws-Tilt — the EXACT composition, glyphs, intent colors, copy, the 20/40/40 point-row split, the clock_loader_10 rotation derivation, the to-scale-bed Focus + list-fallback)
    - docs/ui_design/LAYOUT.md + THEMING.md (the LAW: Focus/Field/Gutter, ratio-only sizing, intent-by-safety colors, no hardcoded px)
    - img/bed_tilt.svg (NOT used here; screws-tilt Focus is the derived to-scale bed, not this asset)
  </read_first>
  <action>
    CalibrationHubScreen.kt (CALIB-01/D-14): Field-only ScreenScaffold (Focus omitted). LazyVerticalGrid of square
    aspectRatio(1f) OutlinedControl tiles mirroring AppDrawer's DrawerTile — one per routine from
    CalibrationHubHolder. Supported routines render accent-capable (first); unsupported render greyed (--outline on
    --surface-2) sorted last but STILL tappable (owner override §1 — tapping opens the page for inspection). Tile
    glyphs, each unique: screws-tilt `architecture`, Z-tilt `vertical_align_center`, QGL `crop_square`, bed-mesh
    `grid_on`, probe-calibrate `straighten`. Gutter = single green Back. Drawer-swipe NOT suppressed (tiles aren't a
    scroll list). Heading "Calibration". Tapping a tile invokes an onNavigate(routine) callback (09-07 wires the
    sub-routes).

    ScrewsTiltScreen.kt (CALIB-02, owner-authored §2 — supersedes the mockup card): Focus = a to-scale
    representation of the bed derived from the ScrewConfig coords + bed extents (toolhead axis_minimum/axis_maximum
    already in PrinterState), with location-accurate point indicators at real screw coords; each point is a state
    icon (point_scan unmeasured / rotate_left CCW / rotate_right CW / commit green in-tol) with the direction
    indicator (clock_loader_10) rotated to point from bed-center toward the screw's real location — DERIVE the
    rotation at runtime (bearing(center→screw) adjusted for the icon's ~30° native wedge offset and the printer-Y-up
    vs screen-Y-down flip; calibrate the constant against the rendered asset, do NOT hardcode per-screw angles).
    GRACEFUL FALLBACK (D-06): if config coords are unavailable, fall back to the labeled point list / abstract
    corner layout. Field = the point list, 3-or-4 rows, each ratio-split 20%/40%/40% (clock_loader_10 indicator /
    hh:mm turn (Mono) / degrees (Mono)). The Focus headline = "X of N in tolerance" + the worst screw's turn (Mono
    Display). Generic N-screw (D-04 — count/coords/names from config, do NOT hardcode 4). Gutter = Run (blue,
    dispatch CommandRegistry.screwsTiltCalculate via dispatcher.dispatch) + Back (green). NOTHING applied (one-shot,
    D-05 — Run=(re)probe, Back=leave; no Accept/Cancel). Run gated on homed state (offer inline blue Home per D-13
    when !homed). Surface dispatcher Failure via SeverityToast(Severity.Error) with the verbatim RpcError text
    (never transport e.message, T-05-11-01). All sizing ratio-only; all color via LocalTokens (no raw hex).
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:assembleRelease --no-daemon" 2>&1 | tr -d '\r' | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `:app:assembleRelease` SUCCESSFUL (both screens compile).
    - Token-purity: grep for raw color literals (`Color(0x` / `0xFF`) in ui/calibration/CalibrationHubScreen.kt and
      ScrewsTiltScreen.kt returns ZERO hits (all color via LocalTokens).
    - CalibrationHubScreen uses MaterialSymbol with the five unique glyphs (grep each ligature; no glyph repeats on
      the screen).
    - ScrewsTiltScreen Run dispatches CommandRegistry.screwsTiltCalculate (grep "screwsTiltCalculate") and has a
      green Back; NO Accept/Cancel button (grep confirms only Run + Back + optional Home).
    - ScrewsTiltScreen renders the point list rows with the 20/40/40 ratio split (weight modifiers, no fixed px —
      grep for `weight(` not `.dp` on the row columns; the ≥64dp touch floor and 2px outline are the only allowed
      fixed values).
  </acceptance_criteria>
  <done>Hub + screws-tilt compile, token-pure, per UI-SPEC §1/§2; Run dispatches the gcode, Back-only nav, homed pre-flight.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user tap → SCREWS_TILT_CALCULATE dispatch | App moves/probes the toolhead on a tap |
| RpcError text → SeverityToast | Printer rejection text surfaced to the UI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-04-01 | Tampering/safety | running screws-tilt unhomed | mitigate | D-13 pre-flight gates Run on homedAxes; offers inline Home; no force-move/override escape on this page |
| T-09-04-02 | Information Disclosure | failure toast leaking transport detail | mitigate | SeverityToast shows the gcode-rejection RpcError text only (e.g. "bed level exceeds configured limits"), never transport e.message (T-05-11-01 redaction) |
| T-09-04-03 | Tampering | dead/unsafe button on an unsupported printer | mitigate | Hub greys unsupported routines via hasObject gating; the Run CommandSpec is ObjectPresent-gated so it can't dispatch on a printer lacking screws_tilt_adjust |
| T-09-04-SC | Tampering | npm/pip/cargo installs | mitigate | Zero packages installed (RESEARCH § Package Legitimacy Audit: N/A) |
</threat_model>

<verification>
- Quick run: `gw.bat :app:testReleaseUnitTest --tests '*ScrewsTiltHolderTest*' --no-daemon` green.
- `:app:assembleRelease` SUCCESSFUL; token-purity grep clean over the two new screens.
- Per-wave merge: full `:app:testReleaseUnitTest` green.
</verification>

<success_criteria>
- Hub lists all five routines (supported first, unsupported greyed-tappable last); screws-tilt renders the guided loop + to-scale bed (or list fallback).
- Run dispatches SCREWS_TILT_CALCULATE gated on homed; Back-only nav (one-shot, D-05).
- Headless holders, no second throttle, token-pure screens.
</success_criteria>

<output>
Create `.planning/phases/09-calibration-maintenance/09-04-SUMMARY.md` when done.
</output>
