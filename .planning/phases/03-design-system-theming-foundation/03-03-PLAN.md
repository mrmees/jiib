---
phase: 03-design-system-theming-foundation
plan: 03
type: execute
wave: 2
depends_on: ["03-01", "03-02"]
files_modified:
  - app/src/main/java/works/mees/dinghy/theme/compose/LocalTokens.kt
  - app/src/main/java/works/mees/dinghy/theme/compose/DinghyTheme.kt
  - app/src/main/java/works/mees/dinghy/designsystem/layout/ScreenScaffold.kt
  - app/src/main/java/works/mees/dinghy/designsystem/control/OutlinedControl.kt
autonomous: true
requirements: [THEME-01, THEME-02, UI-01, UI-02]

must_haves:
  truths:
    - "Compose components read role tokens via LocalTokens.current, never a raw color — THEME-01/D-05"
    - "DinghyTheme overrides LocalDensity to fontScale=1f so --fs is the SOLE text-size authority — D-04/THEME-02"
    - "ScreenScaffold lays out Focus/Field/Gutter portrait-stacked vs landscape 50/50+full-width-gutter on one shared grid — UI-01"
    - "OutlinedControl renders a 2px outline + intent color with a ≥64dp touch target — UI-02"
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/theme/compose/LocalTokens.kt"
      provides: "staticCompositionLocalOf<ThemeTokens> token boundary"
      contains: "staticCompositionLocalOf"
    - path: "app/src/main/java/works/mees/dinghy/theme/compose/DinghyTheme.kt"
      provides: "Theme boundary: collects resolver flow, fontScale=1f, provides LocalTokens"
      contains: "collectAsStateWithLifecycle"
    - path: "app/src/main/java/works/mees/dinghy/designsystem/layout/ScreenScaffold.kt"
      provides: "Focus/Field/Gutter responsive layout primitive"
      contains: "ScreenScaffold"
    - path: "app/src/main/java/works/mees/dinghy/designsystem/control/OutlinedControl.kt"
      provides: "Outline-led intent-colored touch control (UI-02)"
      contains: "OutlinedControl"
  key_links:
    - from: "DinghyTheme"
      to: "ThemeResolver"
      via: "resolver.tokens.collectAsStateWithLifecycle() at the theme boundary"
      pattern: "collectAsStateWithLifecycle"
    - from: "OutlinedControl"
      to: "LocalTokens"
      via: "reads t.outline/t.accentLine/t.heat/t.stop/t.go by intent"
      pattern: "LocalTokens.current"
---

<objective>
Establish the Compose half of the token seam and the two most-reused visual primitives: the
`staticCompositionLocalOf` token boundary (D-05/Pitfall 3), the `DinghyTheme` boundary that collects the
resolver flow and pins `LocalDensity(fontScale=1f)` so `--fs` is the sole text authority (D-04), the
slot-based `ScreenScaffold` Focus/Field/Gutter layout primitive (UI-01, RESEARCH Pattern 1), and the
token-driven `OutlinedControl` control language with button-intent colors (UI-02).

Purpose: This is the Compose boundary every later panel composes inside. "A theme is a token remap" holds
on the Compose side; the layout grammar and control language are settled here, not redesigned per panel.
Output: LocalTokens, DinghyTheme, ScreenScaffold, OutlinedControl.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-design-system-theming-foundation/03-CONTEXT.md
@.planning/phases/03-design-system-theming-foundation/03-RESEARCH.md
@.planning/phases/03-design-system-theming-foundation/03-PATTERNS.md
@app/src/main/java/works/mees/dinghy/theme/ThemeTokens.kt
@app/src/main/java/works/mees/dinghy/theme/ThemeResolver.kt
@app/src/main/java/works/mees/dinghy/theme/Geist.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: LocalTokens boundary + DinghyTheme (fontScale=1f, flow collection)</name>
  <files>app/src/main/java/works/mees/dinghy/theme/compose/LocalTokens.kt, app/src/main/java/works/mees/dinghy/theme/compose/DinghyTheme.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/theme/ThemeTokens.kt (the token type provided through the local; the fsSp helper)
    - app/src/main/java/works/mees/dinghy/theme/ThemeResolver.kt (the StateFlow<ThemeTokens> DinghyTheme collects)
    - app/src/main/java/works/mees/dinghy/MainActivity.kt (the setContent/MaterialTheme host idiom — partial analog for the theme boundary)
    - 03-RESEARCH.md Pattern 3 (staticCompositionLocalOf bridge + collectAsStateWithLifecycle + CompositionLocalProvider snippet) and Pattern 5 (LocalDensity fontScale=1f override) and Pitfall 3 (static vs dynamic local)
    - .planning/phases/03-design-system-theming-foundation/03-PATTERNS.md "theme/compose/*" No-Analog rows (greenfield — follow RESEARCH Pattern 3 & 5)
    - gradle/libs.versions.toml (lifecycle-runtime-compose 2.8.7 provides collectAsStateWithLifecycle — already in catalog)
  </read_first>
  <action>
    Create `app/src/main/java/works/mees/dinghy/theme/compose/LocalTokens.kt` (package
    `works.mees.dinghy.theme.compose`): `val LocalTokens = staticCompositionLocalOf<ThemeTokens> {
    error("No ThemeTokens provided — wrap in DinghyTheme") }`. Use `staticCompositionLocalOf` (NOT
    `compositionLocalOf`) per Pitfall 3 — a theme swap touches nearly every node, so subtree recomposition
    is correct and reads are untracked/cheaper.

    Create `DinghyTheme.kt` (same package): `@Composable fun DinghyTheme(resolver: ThemeResolver, content:
    @Composable () -> Unit)` that does `val tokens by resolver.tokens.collectAsStateWithLifecycle()` at the
    boundary, then `CompositionLocalProvider(LocalTokens provides tokens, LocalDensity provides
    Density(density = LocalDensity.current.density, fontScale = 1f), content = content)`. The fontScale=1f
    override (D-04) neutralizes the OS accessibility fontScale so `--fs` (via fsSp) is the ONLY multiplier
    — document this as an intentional appliance-screen accessibility trade (Pattern 5). Keep the override
    at exactly this one place (Pitfall: no second density path downstream).
  </action>
  <verify>
    <automated>cmd.exe /c "E:\Android\gw.bat :app:assembleDebug --no-daemon"</automated>
  </verify>
  <acceptance_criteria>
    - LocalTokens.kt declares `val LocalTokens = staticCompositionLocalOf<ThemeTokens>` (not compositionLocalOf)
    - DinghyTheme.kt calls `collectAsStateWithLifecycle()` on the resolver flow and provides both LocalTokens and a `Density(..., fontScale = 1f)` via CompositionLocalProvider
    - `:app:assembleDebug` compiles
    - On-device proof that OS fontScale does NOT double-apply is Manual-Only (03-VALIDATION.md): verified in the gallery on flox with device fontScale at max
  </acceptance_criteria>
  <done>The Compose token boundary exists; components inside DinghyTheme read tokens and --fs governs all type.</done>
</task>

<task type="auto">
  <name>Task 2: ScreenScaffold (Focus/Field/Gutter, UI-01) + OutlinedControl (UI-02)</name>
  <files>app/src/main/java/works/mees/dinghy/designsystem/layout/ScreenScaffold.kt, app/src/main/java/works/mees/dinghy/designsystem/control/OutlinedControl.kt</files>
  <read_first>
    - docs/ui_design/LAYOUT.md (the FULL grammar + the three ⚠ NON-NEGOTIABLES: one shared grid / sacred aspect ratios / ratio-only sizing; portrait stack ~40/40/20 with --focus-grow/--field-grow; landscape Focus|Field 50/50 + full-width gutter on same column lines)
    - docs/ui_design/CLAUDE.md (Focus/Field/Gutter philosophy; outline-led ≥64px controls; button intent = color; square the smallest buttons)
    - docs/ui_design/THEMING.md "The control language (the outline rule)" + "Button intent = color" + shape tokens (--r-ctrl=16px)
    - docs/ui_design/reference/hifi.css (.screen.port/.land, .stage, .focus, .field, .gutter, .ctl/.ctl.accent/.warn/.danger/.go — reproduce VALUES, do not copy verbatim)
    - 03-RESEARCH.md Pattern 1 (ScreenScaffold slot composable: BoxWithConstraints + Row/Column + weight/aspectRatio; the one-shared-grid trick; sacred-square content wrapped in aspectRatio(1f) INSIDE its region) and Code Examples "Token-driven outline control" (Intent enum → token mapping, heightIn(min=64.dp), 2px border) and Open Question 3 / A4 (weighted-gutter alignment risk — flag for in-gallery validation, custom Layout fallback for gutter row only)
    - app/src/main/java/works/mees/dinghy/theme/compose/LocalTokens.kt (read tokens via LocalTokens.current)
    - app/src/main/java/works/mees/dinghy/theme/ThemeTokens.kt (fsSp + the role tokens: outline, accentLine, heat, stop, go, rCtrl, text)
  </read_first>
  <action>
    Create `app/src/main/java/works/mees/dinghy/designsystem/layout/ScreenScaffold.kt` (package
    `works.mees.dinghy.designsystem.layout`): a slot composable `ScreenScaffold(modifier, focus:
    (@Composable ColumnScope.() -> Unit)? , field: (@Composable ColumnScope.() -> Unit)? , gutter:
    (@Composable () -> Unit)? , focusGrow: Float = 1f, fieldGrow: Float = 1f)`. Inside `BoxWithConstraints`,
    branch on `maxWidth > maxHeight`: LANDSCAPE = a `Row(weight(1f))` stage of two `weight(focusGrow)` /
    `weight(fieldGrow)` columns + a full-width gutter `Box` below sharing the same width (so a 3-equal-button
    gutter's middle-button center lands on the Focus/Field divide — LAYOUT.md §1); PORTRAIT = a Column
    stacking focus(weight)/field(weight)/full-width gutter (~40/40/20 tunable via the grows). Use only
    `weight`/`fillMax`/`aspectRatio` — NO hardcoded px for regions (NON-NEGOTIABLE 3). Either focus or
    field may be null and the other takes the freed space (LAYOUT.md). Document that sacred-square content
    (ring, jog pad) is wrapped by the CALLER in `Modifier.aspectRatio(1f)` INSIDE its region — the region
    is never made square (NON-NEGOTIABLE 2; hifi.css ringwrap). If fractional weighted-gutter buttons drift
    off the divide in the gallery (A4 risk), escalate to a custom `Layout` for the gutter row ONLY.

    Create `OutlinedControl.kt` (package `works.mees.dinghy.designsystem.control`) per the RESEARCH Code
    Example: `enum class Intent { Neutral, Accent, Warn, Danger, Go }` mapping to t.outline / t.accentLine /
    t.heat / t.stop / t.go; a `@Composable fun OutlinedControl(label, intent = Neutral, onClick, modifier)`
    with `heightIn(min = 64.dp)` (≥64px touch floor), `clip(RoundedCornerShape(t.rCtrl))`, a `2.dp` border
    in the intent color, transparent fill, and label `Text(fontFamily = Geist, fontSize = fsSp(18f, t.fs),
    color = t.text)`. Glow is a STATIC shadow/elevation layer (D-13) — NEVER animated; NO breathing/sheen.
  </action>
  <verify>
    <automated>cmd.exe /c "E:\Android\gw.bat :app:assembleDebug --no-daemon"</automated>
  </verify>
  <acceptance_criteria>
    - ScreenScaffold.kt declares `fun ScreenScaffold(...)` taking focus/field/gutter slots and branches portrait vs landscape via BoxWithConstraints with NO hardcoded px region/cell sizes (only weight/fillMax/aspectRatio; the 64dp touch floor + --fs are the only permitted fixed values)
    - OutlinedControl.kt declares an `Intent` enum mapping to t.outline/t.accentLine/t.heat/t.stop/t.go, uses `heightIn(min = 64.dp)` and a `2.dp` border, and reads colors only via `LocalTokens.current`
    - No `@keyframes`-equivalent / infinite animation anywhere (grep finds no `rememberInfiniteTransition` / `animate*AsState` loop in these files — D-13)
    - TOKEN-PURITY GATE (success criterion #1 "never raw color"): no raw `Color(0x…` / hex literal in the new `designsystem/` source — `grep -rEn 'Color\(0[xX]' app/src/main/java/works/mees/dinghy/designsystem/ | grep -v '^#'` returns ZERO matches (BakedTokens.kt is the only sanctioned literal-sRGB home and lives under theme/, not designsystem/)
    - `:app:assembleDebug` compiles
    - Portrait/landscape grid-line alignment + sacred-square rendering + ≥64px finger-test are Manual-Only (03-VALIDATION.md): verified in the gallery on flox vs docs/ui_design/images/*.png
  </acceptance_criteria>
  <done>The responsive layout primitive and the intent-colored outline control exist and compile, reading all values from tokens (no raw color literals).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Pure Compose UI composables reading in-process tokens. No trust boundary crossed: no sockets, no
persistence, no untrusted input.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Information Disclosure | DinghyTheme / Compose token surfaces | accept | Renders only local theme tokens and static labels; no PII, no secrets, no network — nothing to disclose |

Note: LOCAL-ONLY UI substrate. No HIGH-severity threats; no network/auth threats applicable.
</threat_model>

<verification>
- `:app:assembleDebug` compiles with LocalTokens/DinghyTheme/ScreenScaffold/OutlinedControl.
- Token-purity grep over `designsystem/` returns zero raw-color literals.
- Visual/layout/perf correctness is deferred to the on-device gallery (Manual-Only, 03-VALIDATION.md).
</verification>

<success_criteria>
- THEME-01: Compose components reference role tokens via LocalTokens, never raw color.
- THEME-02/D-04: DinghyTheme pins fontScale=1f so --fs is the sole text authority.
- UI-01: ScreenScaffold renders Focus/Field/Gutter responsively on one shared grid.
- UI-02: OutlinedControl is a 2px-outline, intent-colored, ≥64dp control.
</success_criteria>

<output>
Create `.planning/phases/03-design-system-theming-foundation/03-03-SUMMARY.md` when done.
</output>
