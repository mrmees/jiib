---
phase: 03-design-system-theming-foundation
plan: 05
type: execute
wave: 3
depends_on: ["03-03"]
files_modified:
  - app/src/main/java/works/mees/dinghy/render/ProgressRing.kt
  - app/src/main/java/works/mees/dinghy/render/GraphView.kt
  - app/src/main/java/works/mees/dinghy/render/GraphViewHost.kt
  - app/src/test/java/works/mees/dinghy/render/GraphDownsampleTest.kt
autonomous: true
requirements: [THEME-01]

must_haves:
  truths:
    - "ProgressRing is a Compose Canvas single arc that redraws straight to the current value at the throttled cadence — D-11/D-13"
    - "GraphView is a classic-View custom-Canvas drawing the bounded RingBuffer snapshot with reused Path/Paint (no per-frame alloc) — D-11/Pitfall 4"
    - "GraphView sanitizes its input (empty/1-point/constant/NaN/Infinity) and never crashes or divides by zero — Pitfall 4"
    - "Rendered points never exceed the horizontal pixel width (downsample cap) so the UI-thread copy stays bounded — Pitfall 4"
    - "A theme swap recolors the Views graph via applyTokens + invalidate (no view recreation) — D-06"
    - "Neither primitive runs a continuous/looping animation; glow is a static layer — D-13"
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/render/ProgressRing.kt"
      provides: "Compose Canvas progress ring (D-11)"
      contains: "Canvas"
    - path: "app/src/main/java/works/mees/dinghy/render/GraphView.kt"
      provides: "Views custom-Canvas line graph implementing ThemeableView (D-11/D-06)"
      contains: "ThemeableView"
    - path: "app/src/main/java/works/mees/dinghy/render/GraphViewHost.kt"
      provides: "AndroidView host that pushes tokens + ring-buffer snapshot into GraphView"
      contains: "AndroidView"
  key_links:
    - from: "GraphViewHost (AndroidView)"
      to: "GraphView.applyTokens + GraphView.setData"
      via: "update = { view.applyTokens(tokens); view.setData(snapshot) }"
      pattern: "applyTokens"
    - from: "GraphView"
      to: "RingBuffer.snapshot()"
      via: "draws the bounded buffer's FloatArray snapshot"
      pattern: "snapshot"
---

<objective>
The shared render primitives (criterion #5; "settled HERE, not late"): the progress ring as a Compose
Canvas single arc (D-11, low-churn, themeable inline) and the scrolling line graph as a classic-Views
custom-Canvas (D-11, high-churn per ADR 0001) drawing the bounded `RingBuffer` snapshot, plus the
`AndroidView` host that pushes tokens (D-06) and data into it. Motion is static redraw at the throttled
~2–4 Hz cadence only — no per-update tween, no breathing dot, no perpetual sheen; glow is a static layer
(D-13). This is the render half of the "shared render/throttle" primitive (the throttle half already
exists in PrinterStateStore).

Purpose: Phase 4 Print Status consumes this; Phase 5 Temperature EXTENDS the line graph. Getting the
render split + the cross-toolkit repaint right here is what every later live surface inherits. Because
later panels inherit this primitive's edge-case behavior, the input-sanitization + downsample-cap rules
are pinned down HERE, not left to each consumer.
Output: ProgressRing, GraphView (implements ThemeableView), GraphViewHost.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-design-system-theming-foundation/03-CONTEXT.md
@.planning/phases/03-design-system-theming-foundation/03-RESEARCH.md
@.planning/phases/03-design-system-theming-foundation/03-PATTERNS.md
@app/src/main/java/works/mees/dinghy/render/RingBuffer.kt
@app/src/main/java/works/mees/dinghy/bench/ViewsBenchScene.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: ProgressRing (Compose Canvas, D-11/D-13)</name>
  <files>app/src/main/java/works/mees/dinghy/render/ProgressRing.kt</files>
  <read_first>
    - 03-RESEARCH.md Code Example "Progress ring (D-11) — Compose Canvas, single arc, no animation" and D-11/D-13 (ring = Compose Canvas; redraw on value change at cadence, NO tween)
    - docs/ui_design/reference/hifi.css (.ringblock / .ringwrap{aspect-ratio:1} L267-271 — sacred square; ring stroke proportions; --accent arc on --surface-2 track)
    - docs/ui_design/LAYOUT.md NON-NEGOTIABLE 2 (sacred square: ring content wrapped in aspectRatio(1f), centered in its cell, NOT the region made square)
    - app/src/main/java/works/mees/dinghy/theme/compose/LocalTokens.kt + ThemeTokens.kt (accent / surface2 tokens; rounded-cap arc)
  </read_first>
  <action>
    Create `app/src/main/java/works/mees/dinghy/render/ProgressRing.kt` (package
    `works.mees.dinghy.render`): `@Composable fun ProgressRing(progress: Float, modifier: Modifier =
    Modifier)` drawing on a `Canvas(modifier.aspectRatio(1f))` — the sacred square. Draw a track arc in
    `t.surface2` (full 360°) then the progress arc in `t.accent` from -90° sweeping `360f * progress`, both
    `Stroke` with `StrokeCap.Round`, stroke width derived as a fraction of `size.minDimension` (ratio, not
    px). Coerce `progress` into 0f..1f and treat a NaN progress as 0f (no malformed sweep). Read tokens via
    `LocalTokens.current`. It redraws ONLY when `progress` changes (which arrives at the throttled cadence
    from the caller) — NO `animateFloatAsState`/tween/infinite transition (D-13). Glow, if any, is a static
    shadow layer.
  </action>
  <verify>
    <automated>cmd.exe /c "E:\Android\gw.bat :app:assembleDebug --no-daemon"</automated>
  </verify>
  <acceptance_criteria>
    - ProgressRing.kt declares `fun ProgressRing(progress: Float, ...)` using `Canvas` with `Modifier.aspectRatio(1f)` and two `drawArc` calls (track + progress)
    - `progress` is coerced into 0f..1f and a NaN value is treated as 0f (no malformed sweep)
    - Colors read via LocalTokens.current (t.accent / t.surface2); stroke width is a fraction of size.minDimension (no hardcoded px)
    - No `animate*AsState` / `rememberInfiniteTransition` anywhere (grep clean — D-13)
    - `:app:assembleDebug` compiles
  </acceptance_criteria>
  <done>The Compose-Canvas progress ring renders a single themeable arc with no animation and tolerates out-of-range/NaN progress.</done>
</task>

<task type="auto">
  <name>Task 2: GraphView (Views Canvas, D-11/D-06) + AndroidView host + input sanitization/downsample test</name>
  <files>app/src/main/java/works/mees/dinghy/render/GraphView.kt, app/src/main/java/works/mees/dinghy/render/GraphViewHost.kt, app/src/test/java/works/mees/dinghy/render/GraphDownsampleTest.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/bench/ViewsBenchScene.kt lines 182-218 (TempGraphView — the DIRECT prototype: pre-allocated Paint, setHistory+invalidate, onDraw draws straight from data; the dp() density helper at line 107)
    - app/src/main/java/works/mees/dinghy/theme/views/ThemeableView.kt (the applyTokens interface to implement — D-06; built in plan 03-04)
    - app/src/main/java/works/mees/dinghy/render/RingBuffer.kt (snapshot(): FloatArray — the data GraphView draws)
    - app/src/test/java/works/mees/dinghy/state/ConflationTest.kt (repo host unit-test idiom — for GraphDownsampleTest)
    - 03-RESEARCH.md Code Example "GraphView (D-11/D-12)" + Pitfall 4 (reuse ONE Path via rewind, pre-allocated Paint, cap points to pixel width, single filled area path) + Pattern 3 Views-half snippet (AndroidView update pushes applyTokens + setData; host collects resolver.tokens.collectAsStateWithLifecycle) + D-06 (push-tokens + invalidate, NO view recreation)
    - .planning/phases/03-design-system-theming-foundation/03-PATTERNS.md "theme/views/ThemeableView.kt + render/GraphView.kt" section (required deltas from the bench prototype)
    - app/src/main/java/works/mees/dinghy/theme/ThemeTokens.kt (toArgb() the accent/heat colors for the Paints)
  </read_first>
  <action>
    Create `app/src/main/java/works/mees/dinghy/render/GraphView.kt` (package `works.mees.dinghy.render`):
    `class GraphView(ctx: Context) : View(ctx), ThemeableView`. Generalize the bench TempGraphView: hold
    ONE reusable `Path` (`line.rewind()` each draw — never `new Path()`), pre-allocated `Paint`s (stroke +
    a translucent fill area), and a `FloatArray` data snapshot. `override fun applyTokens(t: ThemeTokens) {
    linePaint.color = t.accent.toArgb(); /* fill, axis-label mono paint via ResourcesCompat.getFont(...,
    R.font.geist_mono_medium) */ invalidate() }` (D-06). `fun setData(snapshot: FloatArray) { ...;
    invalidate() }` called only when a new throttled sample arrives (~2–4 Hz — D-13, NOT every Choreographer
    frame). `onDraw` maps the snapshot to the canvas with the reused Path.

    INPUT-EDGE CONTRACT (pin it here; later panels inherit it — make these behaviors explicit, not prose):
      • empty snapshot (length 0) → draw nothing (return early; no crash)
      • 1-point snapshot → draw nothing or a single dot (no line that needs ≥2 points)
      • constant-value series (zero-height value range) → center the flat line cleanly; guard the
        value→y mapping against divide-by-zero on a zero range (use a safe denominator / center fallback)
      • NaN/Infinity samples → discard them BEFORE drawing (filter the snapshot so the draw path only sees
        finite values)
    DOWNSAMPLE CAP (explicit rule, not prose): the number of rendered vertices NEVER exceeds the horizontal
    pixel width. Apply the cap + the NaN/Infinity filter in `setData(...)` (or a pure helper it calls) so
    the bounded, finite working copy is produced ONCE per throttled sample — keeping the UI-thread copy
    bounded and the onDraw path allocation-free (reuse the single Path; Pitfall 4).

    Create `GraphViewHost.kt` (same package): `@Composable fun GraphViewHost(tokens: ThemeTokens, snapshot:
    FloatArray, modifier: Modifier = Modifier)` wrapping `AndroidView(factory = { ctx -> GraphView(ctx) },
    update = { view -> view.applyTokens(tokens); view.setData(snapshot) }, modifier = modifier)`. The
    CALLER passes the current tokens (collected via collectAsStateWithLifecycle at its level) so a
    dark→light/custom flip recolors the Canvas graph too (the "theme is a token remap across BOTH toolkits"
    proof) — no view recreation, the same GraphView instance just repaints.

    Create `app/src/test/java/works/mees/dinghy/render/GraphDownsampleTest.kt`: a host-pure JUnit4 test over
    the downsample/sanitize helper (extract it as a pure function `sanitize(snapshot, pixelWidth): FloatArray`
    so it is testable without a Canvas). Assert: a snapshot longer than pixelWidth is capped to ≤ pixelWidth
    points; NaN/Infinity samples are removed; an empty input returns empty; a constant series survives intact
    (no NaN introduced by the zero-range guard). Keep it pure Float/array assertions (no Robolectric).
  </action>
  <verify>
    <automated>cmd.exe /c "E:\Android\gw.bat :app:assembleDebug :app:testDebugUnitTest --tests *GraphDownsampleTest --no-daemon"</automated>
  </verify>
  <acceptance_criteria>
    - GraphView.kt declares `class GraphView(...) : View(...), ThemeableView`, implements `applyTokens(t)` (sets Paint colors from tokens + invalidate), and `onDraw` reuses a single `Path` via `rewind()` (no `Path()` allocation inside onDraw — Pitfall 4)
    - GraphView's input-edge contract holds: empty→draws nothing, 1-point→nothing/dot, constant series→centered with no divide-by-zero on a zero range, NaN/Infinity discarded before draw
    - The downsample cap is enforced in `setData(...)`/its pure helper (rendered vertices ≤ horizontal pixel width), NOT left as prose
    - GraphDownsampleTest.kt exists and asserts the cap, the NaN/Infinity filter, the empty case, and the constant-series case; the verify command runs it green
    - GraphView reads its line color from the pushed ThemeTokens (t.accent.toArgb()), not a hardcoded hex
    - GraphViewHost.kt uses `AndroidView` with an `update` block that calls `applyTokens` + `setData` (no recreation in `factory` on theme change)
    - TOKEN-PURITY GATE (success criterion #1): no raw `Color(0x…` / hex literal in the new `render/` source — `grep -rEn 'Color\(0[xX]' app/src/main/java/works/mees/dinghy/render/ | grep -v '^#'` returns ZERO matches
    - `:app:assembleDebug` compiles
    - The cross-toolkit recolor + perf (criterion #5) are Manual/on-device (03-VALIDATION.md): proven in the gallery + gfxinfo on flox (plans 03-06/03-07)
  </acceptance_criteria>
  <done>The Views custom-Canvas line graph (themed via push-tokens, allocation-free draw, sanitized + downsample-capped input) and its AndroidView host exist and compile, with a host test proving the cap + NaN filter.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Render-only primitives drawing in-process numeric data. No trust boundary crossed: no sockets, no
persistence, no untrusted input.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Denial of Service | GraphView.onDraw per-frame allocation + unbounded/garbage input on Adreno 320 | mitigate | Reuse ONE Path (rewind) + pre-allocated Paints + cap vertices to pixel width AND filter NaN/Infinity in setData() before onDraw (Pitfall 4); guard the value→y mapping against a zero-height range; invalidate only on a new ~2–4 Hz sample, not per Choreographer frame (D-13) — keeps the weak-GPU fill-rate budget; the cap+filter are proven host-side by GraphDownsampleTest and on-device by the gfxinfo perf gate (03-07) |

Note: LOCAL-ONLY render layer. No HIGH-severity threats; no network/auth threats applicable.
</threat_model>

<verification>
- `:app:assembleDebug` compiles with ProgressRing/GraphView/GraphViewHost.
- `:app:testDebugUnitTest --tests *GraphDownsampleTest` is green (downsample cap + NaN filter + edge cases).
- Token-purity grep over `render/` returns zero raw-color literals.
- The render split's cross-toolkit recolor and the no-jank perf are on-device gates (gallery + gfxinfo, plans 03-06/03-07).
</verification>

<success_criteria>
- D-11: ring = Compose Canvas; line graph = Views custom-Canvas hosted via AndroidView.
- D-06: GraphView repaints via applyTokens + invalidate (no recreation), so a theme swap recolors it.
- D-13: static redraw at cadence only; no continuous animation; glow is static.
- Pitfall 4: input sanitized (empty/1-point/constant/NaN/Inf) and rendered vertices capped to pixel width (proven by GraphDownsampleTest).
</success_criteria>

<output>
Create `.planning/phases/03-design-system-theming-foundation/03-05-SUMMARY.md` when done.
</output>
