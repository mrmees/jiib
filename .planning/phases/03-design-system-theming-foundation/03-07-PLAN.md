---
phase: 03-design-system-theming-foundation
plan: 07
type: execute
wave: 5
depends_on: ["03-05", "03-06"]
files_modified:
  - app/src/main/java/works/mees/dinghy/bench/RenderBenchScene.kt
  - app/src/main/AndroidManifest.xml
  - macrobenchmark/src/main/java/works/mees/dinghy/macrobenchmark/RenderBenchmark.kt
  - .planning/phases/03-design-system-theming-foundation/03-PERF-RESULTS.md
autonomous: false
requirements: [UI-01]   # UI-01 rationale: UI-01 was moved off 03-05 during the checker-revision pass; this plan's on-device gfxinfo proof that the Focus/Field/Gutter render surfaces (ring+graph) draw without jank IS the UI-01 closure. Deliberate — do not re-flag.

must_haves:
  truths:
    - "The ring + line-graph draw a bounded ring buffer driven by the deterministic 2-4 Hz synthetic feed — criterion #5/D-10"
    - "gfxinfo framestats p95 on the real flox tablet is within the ADR-0001 budget for the ring+graph scene (no jank) — D-10"
    - "The perf proof reuses the Phase-1 :macrobenchmark + bench + parse_framestats harness, not a new rig"
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/bench/RenderBenchScene.kt"
      provides: "ring+graph perf scene driven by SyntheticFeed for the gfxinfo capture"
      contains: "SyntheticFeed"
    - path: "macrobenchmark/src/main/java/works/mees/dinghy/macrobenchmark/RenderBenchmark.kt"
      provides: "UiAutomator driver for the ring+graph scene"
      contains: "MacrobenchmarkRule"
    - path: ".planning/phases/03-design-system-theming-foundation/03-PERF-RESULTS.md"
      provides: "captured gfxinfo p50/p90/p95 + frozen-frame count on flox (system of record)"
  key_links:
    - from: "RenderBenchScene"
      to: "ProgressRing + GraphView + RingBuffer + SyntheticFeed"
      via: "the deterministic feed fills the ring buffer that both primitives draw"
      pattern: "RingBuffer|SyntheticFeed"
---

<objective>
Close criterion #5 with hard on-device evidence (D-10): a ring+graph perf scene driven by the deterministic
2–4 Hz SyntheticFeed, captured with `dumpsys gfxinfo framestats` on the real `flox` tablet and parsed by
the Phase-1 `parse_framestats.py` for p50/p90/p95 + frozen-frame count. gfxinfo is THE system of record
(per ADR 0001). This REUSES the existing `:macrobenchmark` + `bench/*` + parser harness — no new perf rig.

Purpose: The render primitives every later live surface inherits must draw without jank on the Adreno 320
floor. Vibes don't count; the gfxinfo p95 does. This is the phase's hardest gate and the reason the
toolkit split (D-11) was settled here, not late.
Output: a RenderBenchScene, a RenderBenchmark driver, and committed 03-PERF-RESULTS.md.

This plan is NON-AUTONOMOUS: the perf capture runs on the physical USB-attached `flox` tablet (the build
is Windows-side; the device is hardware). A human runs the on-device step and confirms the result.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-design-system-theming-foundation/03-CONTEXT.md
@.planning/phases/03-design-system-theming-foundation/03-RESEARCH.md
@.planning/phases/03-design-system-theming-foundation/03-PATTERNS.md
@app/src/main/java/works/mees/dinghy/bench/BenchActivity.kt
@app/src/main/java/works/mees/dinghy/bench/SyntheticFeed.kt
@macrobenchmark/src/main/java/works/mees/dinghy/macrobenchmark/ToolkitBenchmark.kt
@docs/adr/0001-ui-toolkit-decision.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: RenderBenchScene + a bench-route to the ring/graph driven by SyntheticFeed</name>
  <files>app/src/main/java/works/mees/dinghy/bench/RenderBenchScene.kt, app/src/main/AndroidManifest.xml, macrobenchmark/src/main/java/works/mees/dinghy/macrobenchmark/RenderBenchmark.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/bench/BenchActivity.kt (the EXTRA_SCENE intent contract + the SyntheticFeed().events().collect bounded-deque driving loop + FLAG_KEEP_SCREEN_ON; add a new "render" scene branch)
    - app/src/main/java/works/mees/dinghy/bench/ViewsBenchScene.kt (the AndroidView/custom-View hosting idiom for the Views graph)
    - app/src/main/java/works/mees/dinghy/bench/SyntheticFeed.kt (REUSE verbatim — GraphSample, ~3 Hz; do NOT author a second feed)
    - app/src/main/java/works/mees/dinghy/render/ProgressRing.kt + GraphView.kt + GraphViewHost.kt + RingBuffer.kt (the primitives the scene mounts)
    - macrobenchmark/src/main/java/works/mees/dinghy/macrobenchmark/ToolkitBenchmark.kt (the measureRepeated/setupBlock/driveScene shape + the gfxinfo-is-SoR header to mirror)
    - macrobenchmark/build.gradle.kts (benchmark/release build-type + matchingFallbacks wiring — already settled in Phase 1)
    - .planning/phases/03-design-system-theming-foundation/03-PATTERNS.md "On-device perf proof (criterion #5) — extend the existing macrobenchmark" (the exact reuse instructions + ADR-0001 floors: Views graph p95 ≈ 41.9 ms, zero frames > 700 ms)
    - 03-RESEARCH.md § Validation Architecture criterion #5 row + D-10
  </read_first>
  <action>
    Add a `SCENE_RENDER = "render"` branch to BenchActivity's `when(scene)` (alongside compose/views) that
    mounts a new `RenderBenchScene` driven by the SAME `SyntheticFeed`: feed events fill a `RingBuffer`
    (GRAPH_MAX=120 window) whose snapshot drives the Views `GraphView` (via GraphViewHost or direct
    AndroidView) AND the Compose `ProgressRing` (progress from the feed) — i.e. exercise BOTH render
    primitives at the throttled ~3 Hz cadence in release mode. Keep FLAG_KEEP_SCREEN_ON. This is the D-10
    perf scene; reuse the feed verbatim. BenchActivity is already registered+exported in the main manifest
    — you may add a `<meta-data>` or simply rely on the existing `--es scene render` launch contract; if
    the main manifest needs the render scene documented, update only the BenchActivity comment (the
    activity registration itself is unchanged — minimal edit to the SINGLE-OWNER file, scene routing is an
    intent extra, not a new activity).

    Create `macrobenchmark/.../RenderBenchmark.kt` mirroring ToolkitBenchmark: a `MacrobenchmarkRule`
    `@Test renderScene()` that COLD-launches `BenchActivity` with `--es scene render`, dwells while the
    feed pushes frames, and (since the ring/graph are not scrolled) holds for a fixed measured window.
    FrameTimingMetric is corroboration only; the gfxinfo CSV captured in Task 2 is the system of record
    (mirror the ToolkitBenchmark header verbatim).
  </action>
  <verify>
    <automated>cmd.exe /c "E:\Android\gw.bat :app:assembleRelease :macrobenchmark:assembleBenchmark --no-daemon"</automated>
  </verify>
  <acceptance_criteria>
    - RenderBenchScene.kt mounts ProgressRing + GraphView fed by a RingBuffer filled from SyntheticFeed (grep finds RingBuffer + SyntheticFeed + ProgressRing + GraphView)
    - BenchActivity routes `--es scene render` to RenderBenchScene; the main manifest's BenchActivity registration is otherwise unchanged
    - RenderBenchmark.kt declares a MacrobenchmarkRule @Test that cold-launches the render scene and carries the gfxinfo-is-SoR header
    - `:app:assembleRelease :macrobenchmark:assembleBenchmark` compiles
  </acceptance_criteria>
  <done>The release-mode ring+graph perf scene and its macrobenchmark driver build, ready for the on-device gfxinfo capture.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: On-device gfxinfo perf capture on flox + record results</name>
  <what-built>
    A release-mode ring+graph render scene (RenderBenchScene) driven by the deterministic 2–4 Hz
    SyntheticFeed, plus the RenderBenchmark driver — reusing the Phase-1 gfxinfo framestats methodology.
    The remaining step is the on-device capture: install the release build on the USB-attached `flox`
    tablet, drive the render scene, capture `dumpsys gfxinfo works.mees.dinghy framestats`, parse it with
    `tools/gfxinfo-parser/parse_framestats.py`, and record p50/p90/p95 + frozen-frame count.
  </what-built>
  <read_first>
    - macrobenchmark/src/main/java/works/mees/dinghy/macrobenchmark/ToolkitBenchmark.kt (the launch contract + gfxinfo SoR header)
    - tools/gfxinfo-parser/parse_framestats.py (the authoritative parser — p50/p90/p95 + count(frames > 700 ms))
    - docs/adr/0001-ui-toolkit-decision.md (the measured floors to compare against: p50 ≪ 16.6 ms, zero frames > 700 ms, Views-graph p95 ≈ 41.9 ms)
    - CLAUDE.md § "Local Build Environment" (E:\Android\sign-release.bat to debug-sign the release APK for install; adb at E:\Android\Sdk\platform-tools\adb.exe; pipe gradle through tr -d '\r'; exit code authoritative)
    - .planning/phases/03-design-system-theming-foundation/03-RESEARCH.md § Environment Availability (flox is the project device; emulators lie about Adreno)
  </read_first>
  <how-to-verify>
    1. Build + debug-sign the release APK (the release APK is unsigned until Phase 8): `E:\Android\sign-release.bat <release.apk> <signed.apk>`, then `adb.exe install -r <signed.apk>`. (Or install the debug APK and open the gallery's render scene — but the gfxinfo SoR run must be the RELEASE build per ADR 0001.)
    2. Reset frame stats then launch the render scene: `adb shell dumpsys gfxinfo works.mees.dinghy reset`; `adb shell am start -n works.mees.dinghy/.bench.BenchActivity --es scene render`.
    3. Let the deterministic feed drive the ring+graph for a fixed window (~30–60 s), then capture: `adb shell dumpsys gfxinfo works.mees.dinghy framestats > render_framestats.csv`.
    4. Parse: `python tools/gfxinfo-parser/parse_framestats.py render_framestats.csv` → p50/p90/p95 + count(frames > 700 ms).
    5. Confirm AGAINST the ADR-0001 budget: p50 ≪ 16.6 ms, zero frames > 700 ms, p95 within Views-graph tolerance (~42 ms). Confirm by eye in the gallery that there is NO continuous animation (static glow only — D-13) and the graph recolors on a theme flip.
    6. Record the raw numbers + verdict in `.planning/phases/03-design-system-theming-foundation/03-PERF-RESULTS.md`.
  </how-to-verify>
  <acceptance_criteria>
    - 03-PERF-RESULTS.md exists and records p50/p90/p95 + frozen-frame count from the real flox capture, with the pass/fail verdict against the ADR-0001 budget
    - The capture is from the RELEASE build (or an explicit caveat is recorded if the debug build was used)
    - Criterion #5 is signed off: ring + graph draw the bounded buffer at ~2–4 Hz WITHOUT jank on flox (gfxinfo p95 within budget), static glow only (no continuous animation) — per 03-VALIDATION.md Manual-Only / on-device gate
  </acceptance_criteria>
  <resume-signal>Type "approved" with the captured p95 + frozen-frame count, or describe the jank/perf issue found.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

A release/benchmark build driving an in-process deterministic feed on a local device. No trust boundary
crossed: no sockets, no untrusted input. The only consideration is that the perf scene runs the bench
route (an exported BenchActivity already present since Phase 1, measurement-only).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-08 | Denial of Service | ring+graph rendering exceeding the Adreno-320 fill-rate budget (jank) | mitigate | gfxinfo framestats p95 gate on real flox against the ADR-0001 budget; allocation-free GraphView draw (Pitfall 4) + invalidate only on a ~2–4 Hz sample (D-13). A failing p95 blocks the phase gate. |

Note: LOCAL-ONLY measurement. No HIGH-severity threats; no network/auth threats applicable.
</threat_model>

<verification>
- `:app:assembleRelease :macrobenchmark:assembleBenchmark` compiles.
- gfxinfo framestats p95 on real flox within the ADR-0001 budget for the ring+graph scene (03-PERF-RESULTS.md).
</verification>

<success_criteria>
- Criterion #5: ring + line-graph draw the bounded buffer at ~2–4 Hz WITHOUT jank on the Nexus 7 (gfxinfo proof).
- D-10: reused the Phase-1 gfxinfo methodology; gfxinfo is the system of record; hard p95 evidence committed.
</success_criteria>

<output>
Create `.planning/phases/03-design-system-theming-foundation/03-07-SUMMARY.md` when done.
</output>
