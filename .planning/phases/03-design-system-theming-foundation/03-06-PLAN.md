---
phase: 03-design-system-theming-foundation
plan: 06
type: execute
wave: 4
depends_on: ["03-03", "03-04", "03-05"]
files_modified:
  - app/src/main/java/works/mees/dinghy/gallery/GalleryScreen.kt
  - app/src/debug/AndroidManifest.xml
  - app/src/debug/java/works/mees/dinghy/gallery/GalleryActivity.kt
  - tools/check-release-no-gallery.sh
autonomous: true
requirements: [THEME-01, THEME-02, UI-01, UI-02, PRIM-01, PRIM-03, PRIM-04]

must_haves:
  truths:
    - "The in-APK gallery renders every token/component × dark/light/custom × S/M/L on the real device — D-07"
    - "The gallery is the DEBUG launcher only and is ABSENT from the release APK (src/debug source set, not BuildConfig.DEBUG) — D-08"
    - "GalleryScreen ACCEPTS injected deps (ThemeResolver, nullable PrinterStateStore?, feed-source selector); GalleryActivity is the ONLY place that assembles them — no app/connection bootstrap in the screen"
    - "The gallery drives the ring+graph from BOTH the deterministic synthetic feed AND an already-provided live Phase-2 PrinterStateStore StateFlow — never opening a Moonraker connection itself — D-14"
    - "Flipping dark→light/custom in the gallery recolors the Compose surfaces AND the Views Canvas graph (theme is a token remap across both toolkits)"
    - "Sign-off is on-device gallery + manual review against docs/ui_design — NO screenshot-regression tests in v1 (D-09)"
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/gallery/GalleryScreen.kt"
      provides: "token × component × theme × fs preview matrix wired to injected feed + live spine"
      contains: "GalleryScreen"
    - path: "app/src/debug/AndroidManifest.xml"
      provides: "debug-only LAUNCHER registration of GalleryActivity (D-08)"
      contains: "LAUNCHER"
    - path: "app/src/debug/java/works/mees/dinghy/gallery/GalleryActivity.kt"
      provides: "debug launcher Activity — the SOLE assembler of GalleryScreen's deps"
      contains: "ComponentActivity"
  key_links:
    - from: "GalleryScreen"
      to: "injected ThemeResolver + PrinterStateStore? + feed-source selector"
      via: "live temps (D-14) and deterministic feed both drive the ring/graph via injected deps"
      pattern: "printerState|SyntheticFeed"
    - from: "GalleryActivity"
      to: "DinghyTheme + GalleryScreen"
      via: "debug launcher assembles the deps + mounts the theme boundary + preview matrix"
      pattern: "DinghyTheme"
---

<objective>
The in-APK component gallery (D-07) — the primary on-device preview surface and the way Matthew signs off:
install the debug APK, the gallery opens, eyeball every token/component × dark/light/custom × S/M/L on the
real flox tablet, flip themes and confirm the Views graph recolors with the Compose surfaces. Registered
as the DEBUG-ONLY launcher via a `src/debug/AndroidManifest.xml` source set (D-08 — absent from release,
NOT a runtime BuildConfig.DEBUG guard). Wired to BOTH the deterministic SyntheticFeed (perf proof) and an
ALREADY-PROVIDED live Phase-2 PrinterStateStore StateFlow (D-14, real temps) so the toolkit-agnostic
render seam is proven end-to-end — WITHOUT the gallery owning any connection/app-bootstrap lifecycle
(that is Phase 4).

Purpose: This harness is the home for the manual sign-off of criteria #1–#4 and the host for the criterion
#5 perf proof (plan 03-07). It is the first UI surface to read the Phase-2 spine — strictly as a CONSUMER
of an injected store, never as an owner.
Output: GalleryScreen, GalleryActivity, src/debug/AndroidManifest.xml, the release-guard check script.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-design-system-theming-foundation/03-CONTEXT.md
@.planning/phases/03-design-system-theming-foundation/03-RESEARCH.md
@.planning/phases/03-design-system-theming-foundation/03-PATTERNS.md
@app/src/main/java/works/mees/dinghy/state/PrinterStateStore.kt
@app/src/main/java/works/mees/dinghy/bench/BenchActivity.kt
@app/src/main/java/works/mees/dinghy/bench/SyntheticFeed.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: GalleryScreen — token/component matrix wired to INJECTED feed + live spine (D-14)</name>
  <files>app/src/main/java/works/mees/dinghy/gallery/GalleryScreen.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/bench/BenchActivity.kt lines 57-91 (the SyntheticFeed().events().collect bounded-deque driving loop to reuse for the ring/graph) and lines 43-47 (FLAG_KEEP_SCREEN_ON for the perf run)
    - app/src/main/java/works/mees/dinghy/bench/SyntheticFeed.kt lines 185-189 + 129-132 (GraphSample shape; ~3 Hz DEFAULT_PERIOD_MS — REUSE this feed, do NOT author a second one)
    - app/src/main/java/works/mees/dinghy/state/PrinterStateStore.kt (printerState StateFlow — the live wire for D-14; the gallery CONSUMES an injected store, it does NOT construct/own a Moonraker connection — Phase-4 boundary)
    - all Wave-2/3 component files: theme/compose/DinghyTheme.kt + LocalTokens.kt, theme/ThemeResolver.kt, designsystem/layout/ScreenScaffold.kt, designsystem/control/OutlinedControl.kt, designsystem/ConfirmGuard.kt, designsystem/ScrubberPage.kt, designsystem/SeverityToast.kt, render/ProgressRing.kt, render/GraphView.kt + GraphViewHost.kt, render/RingBuffer.kt
    - docs/ui_design/images/*.png (the hi-fi reference screens to eyeball the matrix against)
    - 03-RESEARCH.md D-07/D-08/D-14 + § Validation Architecture (criterion map #1–#5) ; 03-VALIDATION.md Manual-Only table
  </read_first>
  <action>
    Create `app/src/main/java/works/mees/dinghy/gallery/GalleryScreen.kt` (package
    `works.mees.dinghy.gallery`): a Compose screen that ACCEPTS its dependencies as parameters and
    constructs NONE of them itself. Its signature takes: a `ThemeResolver` (theme control), a NULLABLE
    `PrinterStateStore?` (the live spine for D-14 — null when no live store is provided), and a feed-source
    selector that chooses between the synthetic feed and the live store. The screen exposes controls to
    switch base (Dark/Light), apply a sample custom delta (D-01 accent/heat/go/stop recolor), and pick
    S/M/L — all driving the injected resolver so the WHOLE matrix re-themes live.

    Render a scrollable matrix exercising every primitive: OutlinedControl in all five intents, a
    ScreenScaffold instance (portrait + landscape via device rotation), ConfirmGuard, ScrubberPage,
    SeverityToast in all four severities, ProgressRing, and the GraphViewHost (Views graph). Feed the ring +
    graph from a `RingBuffer` fed by TWO selectable sources (D-14): (a) the deterministic
    `SyntheticFeed().events()` at ~3 Hz for the perf proof, and (b) the INJECTED
    `PrinterStateStore?.printerState` StateFlow's heater temps for real data — the feed-source selector
    toggle picks the source; when the live store is null, the toggle simply offers only the synthetic
    source. Pass the current tokens (collectAsStateWithLifecycle) to GraphViewHost so a theme flip recolors
    the Canvas graph too — the cross-toolkit remap proof.

    SCOPE GUARD (keep Phase-4 out): GalleryScreen creates NO socket, NO Moonraker connection, NO
    Application/DI/app-bootstrap ownership. D-14's "live wire" is satisfied by CONSUMING the already-provided
    `PrinterStateStore.printerState` StateFlow that was assembled by GalleryActivity (Task 2) — never by the
    screen opening or owning a connection lifecycle.
  </action>
  <verify>
    <automated>cmd.exe /c "E:\Android\gw.bat :app:assembleDebug --no-daemon"</automated>
  </verify>
  <acceptance_criteria>
    - GalleryScreen.kt declares `fun GalleryScreen(...)` whose parameters INCLUDE an injected `ThemeResolver`, a nullable `PrinterStateStore?`, and a feed-source selector; the screen constructs none of these itself
    - GalleryScreen switches base/custom-delta/S-M-L via the injected ThemeResolver, and renders OutlinedControl (5 intents) + ScreenScaffold + ConfirmGuard + ScrubberPage + SeverityToast (4 severities) + ProgressRing + GraphViewHost
    - The ring/graph RingBuffer is fed from BOTH SyntheticFeed AND the injected PrinterStateStore.printerState (D-14) with a source toggle (grep finds both `SyntheticFeed` and `printerState` in the file)
    - SCOPE GUARD: GalleryScreen opens NO connection — a grep for `OkHttp|WebSocket|newWebSocket|MoonrakerClient` in GalleryScreen.kt returns ZERO matches (it consumes the injected store only)
    - TOKEN-PURITY GATE (success criterion #1): a grep for raw `Color(0x` / `Color(0X` literals under `app/src/main/java/works/mees/dinghy/gallery/` returns ZERO matches
    - GraphViewHost receives the current tokens so a theme flip recolors it
    - `:app:assembleDebug` compiles
    - Criteria #1–#4 (theme remap across both toolkits, S/M/L, Focus/Field/Gutter orientation, controls/primitives) are Manual-Only sign-offs in this gallery on flox per 03-VALIDATION.md
  </acceptance_criteria>
  <done>The gallery renders the full token/component matrix from INJECTED deps, re-themes live, and shows the ring/graph from both the synthetic feed and the (injected) live spine — owning no connection lifecycle.</done>
</task>

<task type="auto">
  <name>Task 2: Debug-only launcher (src/debug source set, D-08) — sole assembler + release-manifest guard</name>
  <files>app/src/debug/AndroidManifest.xml, app/src/debug/java/works/mees/dinghy/gallery/GalleryActivity.kt, tools/check-release-no-gallery.sh</files>
  <read_first>
    - app/src/main/AndroidManifest.xml lines 28-47 (the LAUNCHER intent-filter pattern for MainActivity + the exported BenchActivity; note the SINGLE-OWNER rule — do NOT edit this file; the debug source set is the clean seam)
    - app/src/main/java/works/mees/dinghy/MainActivity.kt lines 23-34 (the ComponentActivity + setContent host shape to copy, swapping the body for GalleryScreen inside DinghyTheme)
    - app/src/main/java/works/mees/dinghy/state/PrinterStateStore.kt (the store GalleryActivity may assemble + inject; it provides the StateFlow only — no connection is opened here)
    - tools/check-release-no-gallery.sh (the committed packaging-guard script this task's verify runs — confirm its aapt + APK paths match the local build env)
    - 03-RESEARCH.md "Debug-only gallery launcher (D-08)" Code Example (src/debug/AndroidManifest.xml LAUNCHER block) + Pitfall 5 (do NOT runtime-gate with BuildConfig.DEBUG; src/debug source set keeps it ABSENT from release) + Open Question 1 (two launchers in debug is acceptable — accept two)
    - .planning/phases/03-design-system-theming-foundation/03-PATTERNS.md "src/debug/AndroidManifest.xml" section
    - CLAUDE.md § "Local Build Environment" (aapt under E:\Android\Sdk\build-tools\34.0.0; gw.bat build; pipe gradle through tr -d '\r', exit code authoritative)
  </read_first>
  <action>
    Create `app/src/debug/AndroidManifest.xml` registering `.gallery.GalleryActivity` with
    `android:exported="true"` and a MAIN/LAUNCHER intent-filter — placed in the DEBUG source set so the
    manifest merge adds the gallery launcher ONLY in the debug variant (Pitfall 5; D-08). Do NOT touch the
    main manifest (single-owner rule). Accepting two launcher icons in debug (MainActivity + Gallery) is
    fine (RESEARCH Open Question 1).

    Create `app/src/debug/java/works/mees/dinghy/gallery/GalleryActivity.kt`: a `ComponentActivity`
    mirroring MainActivity's `setContent` host. GalleryActivity is the SOLE ASSEMBLER of GalleryScreen's
    dependencies: it constructs a `ThemeResolver` (seeded from ThemePrefs or defaults), constructs (or wires
    an already-available) `PrinterStateStore` to PASS as the nullable live store, and constructs the
    feed-source selector — then wraps the body in `DinghyTheme(resolver)` and mounts
    `GalleryScreen(resolver, printerStateStore, feedSelector)`. The live store is provided as a StateFlow
    source ONLY; GalleryActivity opens NO Moonraker connection (no socket/bootstrap — that is Phase 4). Add
    `FLAG_KEEP_SCREEN_ON` (reuse the bench idiom) so the gallery stays awake for the on-device perf run.

    Confirm the committed `tools/check-release-no-gallery.sh` (the packaging guard) matches the local build
    env (its aapt path + release-APK path); adjust the build-tools version digit if the local SDK differs.
    This script is the runnable form of the D-08 release-manifest guard — it builds the release variant and
    asserts no gallery launchable-activity is present.
  </action>
  <verify>
    <automated>bash tools/check-release-no-gallery.sh</automated>
  </verify>
  <acceptance_criteria>
    - app/src/debug/AndroidManifest.xml registers `.gallery.GalleryActivity` with a MAIN/LAUNCHER intent-filter and the main manifest is unmodified
    - GalleryActivity.kt is under app/src/debug/java/... (debug source set), is a ComponentActivity, is the SOLE assembler of GalleryScreen's deps (ThemeResolver + nullable PrinterStateStore + feed selector), wraps GalleryScreen in DinghyTheme, opens NO Moonraker connection, and sets FLAG_KEEP_SCREEN_ON
    - `bash tools/check-release-no-gallery.sh` exits 0: it builds `:app:assembleRelease` and proves via `aapt dump badging` that the release APK has NO gallery launchable-activity (only MainActivity) — the automated D-08 packaging guard (Pitfall 5)
    - `:app:assembleDebug` produces an APK whose debug launcher includes GalleryActivity (installable on flox for the manual sign-off)
  </acceptance_criteria>
  <done>The gallery is the debug launcher, the sole dep-assembler owns no connection, and an automated guard proves the gallery is absent from the release APK.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Release-APK packaging surface | The debug-only gallery launcher must NOT ship in the release variant (an exposure/packaging concern, not a network one). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06 | Elevation of Privilege / Exposure | GalleryActivity leaking into the release APK | mitigate | Register the launcher ONLY in `app/src/debug/AndroidManifest.xml` and place the class only in `app/src/debug/java/...` (D-08); the release variant never sees it. Proven by the automated `tools/check-release-no-gallery.sh` guard (`aapt dump badging` on the release APK shows no gallery launchable-activity — Pitfall 5). NOT a runtime BuildConfig.DEBUG guard (which would still register it). |
| T-03-07 | Information Disclosure | Gallery reading the live PrinterStateStore (D-14) | accept | Reads only local in-process temps from an INJECTED LAN-only spine in a debug build; the gallery opens no new socket, owns no connection, exposes no secret |

Note: LOCAL-ONLY UI harness. The only real consideration is the packaging/exposure of the debug launcher,
mitigated by the automated guard above. No HIGH-severity threats; no network/auth threats applicable.
</threat_model>

<verification>
- `:app:assembleDebug` compiles; `bash tools/check-release-no-gallery.sh` exits 0 (release build + no gallery launcher).
- Token-purity grep over `gallery/` returns zero raw-color literals; GalleryScreen opens no connection.
- Criteria #1–#4 are signed off manually in the gallery on flox (03-VALIDATION.md Manual-Only table).
</verification>

<success_criteria>
- D-07: the in-APK gallery renders the full token/component × theme × fs matrix on the real device.
- D-08: the gallery is the debug launcher only and is proven absent from the release APK by an automated guard.
- D-14: the ring/graph are driven by BOTH the synthetic feed and the injected live PrinterStateStore StateFlow.
- The cross-toolkit theme remap (Compose + Views graph) is demonstrable in the gallery, with no connection ownership in the screen.
</success_criteria>

<output>
Create `.planning/phases/03-design-system-theming-foundation/03-06-SUMMARY.md` when done.
</output>
