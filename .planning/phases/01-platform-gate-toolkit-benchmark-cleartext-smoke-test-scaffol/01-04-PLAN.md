---
phase: 01-platform-gate-toolkit-benchmark-cleartext-smoke-test-scaffol
plan: 04
type: execute
wave: 3
depends_on: [01-01, 01-03]
files_modified:
  - .planning/phases/01-platform-gate-toolkit-benchmark-cleartext-smoke-test-scaffol/captures/
  - docs/adr/0001-ui-toolkit-decision.md
autonomous: false
requirements: [PKG-02]
must_haves:
  truths:
    - "Both scenes were run in release mode on the real Nexus 7 2013 at 1920x1200"
    - "gfxinfo framestats CSVs were captured for both Compose and Views runs"
    - "The parser produced p50/p90/p95 + count(frames>700ms) for each scene"
    - "A toolkit decision (Compose-everywhere vs hybrid-Views) is recorded as an ADR with raw captures"
  artifacts:
    - path: "docs/adr/0001-ui-toolkit-decision.md"
      provides: "The recorded Compose-vs-Views ADR (D-02/D-05 deliverable)"
      contains: "Decision"
    - path: ".planning/phases/01-platform-gate-toolkit-benchmark-cleartext-smoke-test-scaffol/captures/"
      provides: "Raw gfxinfo framestats CSVs + parser outputs from the on-device run"
      min_lines: 0
  key_links:
    - from: "docs/adr/0001-ui-toolkit-decision.md"
      to: "captures/ framestats CSVs + parser output"
      via: "ADR cites raw on-device measurements"
      pattern: "framestats|p95"
---

<objective>
Run the head-to-head benchmark on the REAL Nexus 7 2013 in release mode, capture gfxinfo
framestats for both scenes, run the parser, and record the Compose-vs-Views verdict as an ADR
with the raw captures attached. This finalizes the toolkit-decision deliverable (D-02/D-05) of
this PKG-02 gate phase.

Purpose: The verdict is only valid if measured on the actual weak hardware in release mode with
the identical feed + identical UiAutomator script (D-02/D-03/D-07). The ADR + raw captures are
the durable record subsequent phases build on.

Output: raw framestats CSVs + parser outputs under the phase `captures/` dir, and
`docs/adr/0001-ui-toolkit-decision.md`. No new app code here — this is run + record only.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-platform-gate-toolkit-benchmark-cleartext-smoke-test-scaffol/01-CONTEXT.md
@.planning/phases/01-platform-gate-toolkit-benchmark-cleartext-smoke-test-scaffol/01-RESEARCH.md
@.planning/phases/01-platform-gate-toolkit-benchmark-cleartext-smoke-test-scaffol/01-VALIDATION.md
@CLAUDE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Run both scenes on the real Nexus 7 and capture gfxinfo framestats</name>
  <read_first>
    - macrobenchmark/src/main/java/works/mees/dinghy/macrobenchmark/ToolkitBenchmark.kt (from plan 01-03)
    - tools/gfxinfo-parser/parse_framestats.py (from plan 01-03)
    - .planning/phases/01-platform-gate-toolkit-benchmark-cleartext-smoke-test-scaffol/01-CONTEXT.md — D-03 (release/R8 on real Nexus 7 2013, never emulator/debug/modern phone), D-07 (gfxinfo system of record), D-04 (thresholds)
    - .planning/phases/01-platform-gate-toolkit-benchmark-cleartext-smoke-test-scaffol/01-RESEARCH.md — gfxinfo capture procedure (reset/dump-repeatedly/warmup-exclude); FrameTimingMetric corroboration
  </read_first>
  <what-built>
    A deterministic synthetic feed, Compose + Views benchmark scenes, an identical UiAutomator
    driver against the release variant, and a gfxinfo framestats parser (all from plan 01-03).
  </what-built>
  <how-to-verify>
    1. Connect the real Nexus 7 2013 via adb (`adb devices`) — NOT an emulator, NOT a modern phone.
    2. Install the RELEASE build: `./gradlew :app:installRelease`.
    3. For the COMPOSE scene: `adb shell dumpsys gfxinfo <pkg> reset`, launch BenchActivity in
       Compose mode (or run `./gradlew :macrobenchmark:connectedReleaseAndroidTest` for the Compose
       run), let the identical drive sequence complete (dumping framestats repeatedly to beat the
       ~120-frame ring buffer), then save the concatenated CSV to `captures/compose-framestats.csv`.
    4. Repeat identically for the VIEWS scene → `captures/views-framestats.csv`.
    5. Run the parser on each CSV:
       `python tools/gfxinfo-parser/parse_framestats.py captures/compose-framestats.csv` and the
       views CSV; save outputs to `captures/compose-summary.txt` / `captures/views-summary.txt`.
    6. Confirm both runs were release-mode on the physical device at 1920x1200 with the same drive
       script, that each CSV has real frame data, and watch logcat for OOM / GC storm during the
       Coil thumbnail scroll (D-04 absolute floors).
  </how-to-verify>
  <acceptance_criteria>
    - Two framestats CSVs exist under the phase `captures/` dir (compose + views), each with real frame rows
    - Parser output for each CSV reports p50/p90/p95 + count(frames>700ms)
    - Captures confirmed release-mode, real Nexus 7 2013, 1920x1200, identical UiAutomator drive
    - No OOM / GC storm observed during the Coil thumbnail scroll (D-04 floor)
    - Macrobenchmark `FrameTimingMetric` result captured as corroboration (noted, not the decision basis)
  </acceptance_criteria>
  <resume-signal>Type "approved" and paste the per-scene parser summaries, or describe the failure.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Record the toolkit-decision ADR with the raw captures</name>
  <read_first>
    - .planning/phases/01-platform-gate-toolkit-benchmark-cleartext-smoke-test-scaffol/captures/ (CSVs + parser summaries from Task 1)
    - .planning/phases/01-platform-gate-toolkit-benchmark-cleartext-smoke-test-scaffol/01-CONTEXT.md — D-02 (record as ADR with raw captures), D-04 (thresholds), D-05 (Compose-everywhere vs hybrid-Views; ADR gates Phase 2+)
    - CLAUDE.md (repo root) § The Big Decision / TL;DR — locked stack leans Compose; the ADR must justify against measured data, not assert
  </read_first>
  <files>docs/adr/0001-ui-toolkit-decision.md, .planning/phases/01-platform-gate-toolkit-benchmark-cleartext-smoke-test-scaffol/captures/</files>
  <action>
    Write `docs/adr/0001-ui-toolkit-decision.md` in standard ADR form (Context / Decision /
    Consequences). Context: the Adreno 320 / 2GB / 1920x1200 / API-23 constraint and the
    head-to-head methodology (identical SyntheticFeed, identical UiAutomator script, release/R8 on
    real device, gfxinfo system of record). Tabulate the per-scene p50/p90/p95 + count(frames>700ms)
    from the captures against the D-04 floors (p50 < 16.6 ms; zero frames > 700 ms; p95 tolerance
    ~33-50 ms) and cite the corroborating FrameTimingMetric. Decision: state "Compose-everywhere" or
    "hybrid-Views" driven by the measured numbers (do NOT pre-decide — the locked stack favors
    Compose, but the ADR must show the data supports the verdict; if the data contradicts Compose,
    record that honestly and flag the named hybrid-Views fallback set — Files list, temp graph,
    Console scrollback). Consequences: implications for Phase 2+ panel architecture. Ensure the raw
    CSVs + parser summaries live under the phase `captures/` dir and are referenced from the ADR.
  </action>
  <verify>
    <automated>test -f docs/adr/0001-ui-toolkit-decision.md && grep -qi 'Decision' docs/adr/0001-ui-toolkit-decision.md && grep -Eqi 'p95|framestats' docs/adr/0001-ui-toolkit-decision.md && ls .planning/phases/01-platform-gate-toolkit-benchmark-cleartext-smoke-test-scaffol/captures/compose-framestats.csv .planning/phases/01-platform-gate-toolkit-benchmark-cleartext-smoke-test-scaffol/captures/views-framestats.csv</automated>
  </verify>
  <acceptance_criteria>
    - `docs/adr/0001-ui-toolkit-decision.md` exists with Context / Decision / Consequences sections
    - The ADR tabulates per-scene p50/p90/p95 + count(frames>700ms) against the D-04 floors
    - The Decision is justified by the measured data (not asserted); the hybrid-Views fallback is named if Compose loses
    - Raw framestats CSVs + parser summaries exist under the phase `captures/` dir and are cited by the ADR
  </acceptance_criteria>
  <done>The Compose-vs-Views verdict is recorded as an ADR backed by raw on-device captures, gating Phase 2+.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| on-device measurement → recorded verdict | The ADR must reflect raw captures, not a pre-decided preference |
| dev box → real Nexus 7 (adb) | Capture must come from the real 32-bit API-23 device in release mode |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-09 | Repudiation | toolkit verdict provenance | mitigate | ADR cites raw framestats CSVs retained under captures/; verdict derived from D-04 metrics, not assertion |
| T-01-10 | Tampering | measurement environment (debug/emulator/modern phone would lie) | mitigate | Acceptance requires release/R8 on the real Nexus 7 2013 at 1920x1200; emulator/debug explicitly rejected |
| T-01-04 | Information Disclosure | cleartext posture (inherited) | accept | Owned/documented by 01-01; this plan does no networking |
</threat_model>

<verification>
- Raw framestats CSVs for both scenes exist with parser-computed D-04 metrics.
- The ADR records the verdict, justified by the captures, and gates Phase 2+.
</verification>

<success_criteria>
The toolkit go/no-go is recorded: an on-device release-mode benchmark on the real Nexus 7
produces a documented Compose-everywhere vs hybrid-Views ADR with raw captures attached
(ROADMAP success criterion 1; D-02/D-05).
</success_criteria>

<output>
Create `.planning/phases/01-platform-gate-toolkit-benchmark-cleartext-smoke-test-scaffol/01-04-SUMMARY.md` when done
</output>
