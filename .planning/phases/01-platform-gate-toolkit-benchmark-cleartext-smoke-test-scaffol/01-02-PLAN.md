---
phase: 01-platform-gate-toolkit-benchmark-cleartext-smoke-test-scaffol
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - app/src/androidTest/java/works/mees/dinghy/smoke/CleartextMoonrakerSmokeTest.kt
autonomous: false
requirements: [CONN-05]
must_haves:
  truths:
    - "A cleartext ws:// websocket opens to the live Moonraker on the real API-23 device"
    - "A cleartext http:// REST GET to server.info / printer.info returns successfully"
    - "An objects/subscribe yields a deterministic, awaited status update (not a stray notify_*)"
    - "Cleartext is permitted on API 23 via the manifest flag AND on API 24+ via the NSC"
  artifacts:
    - path: "app/src/androidTest/java/works/mees/dinghy/smoke/CleartextMoonrakerSmokeTest.kt"
      provides: "Throwaway 3-step cleartext smoke probe"
      min_lines: 30
  key_links:
    - from: "CleartextMoonrakerSmokeTest"
      to: "live Moonraker objects/subscribe"
      via: "ws:// JSON-RPC subscribe + await deterministic update"
      pattern: "objects/subscribe"
---

<objective>
Prove cleartext `ws://` + `http://` to the real Moonraker on the actual Nexus 7 2013 (API 23).
This is the CONN-05 go/no-go probe.

Purpose: CONN-05 must be proven on real hardware against the real API surface — cleartext is
the ONLY Moonraker integration path on a trusted LAN. The cleartext config (manifest flag for
API 23 + NSC for API 24+, per RESEARCH #6 / D-10) is owned by plan 01-01; this plan consumes it
and writes the throwaway smoke probe.

Output: a THROWAWAY 3-step instrumented smoke test. This is a disposable probe — it is
explicitly NOT the start of the connection layer / state machine (that is Phase 2+). No
PrinterState, no reconnect, no capability gating here.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/01-platform-gate-toolkit-benchmark-cleartext-smoke-test-scaffol/01-CONTEXT.md
@.planning/phases/01-platform-gate-toolkit-benchmark-cleartext-smoke-test-scaffol/01-RESEARCH.md
@.planning/phases/01-platform-gate-toolkit-benchmark-cleartext-smoke-test-scaffol/01-VALIDATION.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Confirm the inherited cleartext config (manifest flag + NSC) is correct</name>
  <read_first>
    - app/src/main/AndroidManifest.xml (created/owned by plan 01-01)
    - app/src/main/res/xml/network_security_config.xml (created/owned by plan 01-01)
    - .planning/phases/01-platform-gate-toolkit-benchmark-cleartext-smoke-test-scaffol/01-RESEARCH.md — RESEARCH #6, cleartext precedence (manifest flag governs API 23; NSC overrides on API 24+)
    - .planning/phases/01-platform-gate-toolkit-benchmark-cleartext-smoke-test-scaffol/01-CONTEXT.md — D-10 (smoke-test definition), D-11 (broad-cleartext flagged-deferred risk)
  </read_first>
  <files>(read-only verification — no files modified in this task)</files>
  <action>
    Plan 01-01 is the single owner of the manifest + NSC and already configures the cleartext
    posture (INTERNET permission, `android:usesCleartextTraffic="true"` for API 23, and
    `android:networkSecurityConfig="@xml/network_security_config"` permitting cleartext for API 24+
    per RESEARCH #6 / D-10). Do NOT edit those files here (avoids Wave-2 file conflicts). Verify the
    inherited config is present and correct before writing the smoke test; if any element is missing,
    STOP and report a dependency gap against 01-01 rather than editing the shared manifest. Record in
    the SUMMARY the D-11 deferred-validation item: broad cleartext also permits it on API 24+ devices,
    to be scoped to specific domains in a later phase.
  </action>
  <verify>
    <automated>grep -q 'cleartextTrafficPermitted' app/src/main/res/xml/network_security_config.xml && grep -q 'usesCleartextTraffic="true"' app/src/main/AndroidManifest.xml && grep -q 'networkSecurityConfig' app/src/main/AndroidManifest.xml</automated>
  </verify>
  <acceptance_criteria>
    - NSC (owned by 01-01) sets `cleartextTrafficPermitted="true"`
    - Manifest (owned by 01-01) has BOTH `android:usesCleartextTraffic="true"` and `android:networkSecurityConfig="@xml/network_security_config"` and declares `android.permission.INTERNET`
    - No file in this task's scope edits the shared manifest or NSC
    - SUMMARY records the D-11 deferred risk (broad cleartext also permits API 24+ devices)
  </acceptance_criteria>
  <done>The inherited cleartext config is confirmed present; the smoke test can proceed without touching shared files.</done>
</task>

<task type="auto">
  <name>Task 2: Write the throwaway 3-step cleartext Moonraker smoke test</name>
  <read_first>
    - app/src/main/res/xml/network_security_config.xml (owned by 01-01)
    - .planning/phases/01-platform-gate-toolkit-benchmark-cleartext-smoke-test-scaffol/01-CONTEXT.md — D-10 (exact 3 steps), D-01b (pre-configured live Ender 5 Plus Moonraker)
    - .planning/phases/01-platform-gate-toolkit-benchmark-cleartext-smoke-test-scaffol/01-RESEARCH.md — Moonraker JSON-RPC model; objects/subscribe deltas
    - CLAUDE.md (repo root) § Networking deep-dive — OkHttp websocket + JSON-RPC id correlation
  </read_first>
  <files>app/src/androidTest/java/works/mees/dinghy/smoke/CleartextMoonrakerSmokeTest.kt</files>
  <action>
    Write an instrumented (`androidTest`) test `CleartextMoonrakerSmokeTest` performing D-10's
    three steps against the live Moonraker host (host/port read from an instrumentation arg or a
    test constant the executor sets to the Ender 5 Plus Moonraker IP): (1) open a cleartext `ws://`
    websocket via OkHttp and confirm `onOpen`; (2) cleartext `http://` REST GET `server.info` (and
    `printer.info`) and assert a 2xx with expected JSON keys; (3) send `objects/subscribe` over the
    socket and AWAIT a deterministic `notify_status_update` for a subscribed object — correlate by
    JSON-RPC `id` for the subscribe ack, then block on the specific subscribed-object update with a
    timeout; FAIL if it does not arrive. Do NOT rely on a stray unsolicited `notify_*`. Keep this
    self-contained and disposable — no shared connection class, no StateFlow, no reconnect. Mark
    in code/comments that this is a throwaway Phase-1 probe.
  </action>
  <verify>
    <automated>grep -q 'objects/subscribe' app/src/androidTest/java/works/mees/dinghy/smoke/CleartextMoonrakerSmokeTest.kt && grep -q 'server.info' app/src/androidTest/java/works/mees/dinghy/smoke/CleartextMoonrakerSmokeTest.kt && ./gradlew :app:assembleAndroidTest --no-daemon</automated>
  </verify>
  <acceptance_criteria>
    - Test source contains all three steps: ws:// open, REST GET server.info/printer.info, objects/subscribe-and-await
    - The awaited update is a SPECIFIC subscribed object correlated by request id/timeout — NOT any stray notify_*
    - `./gradlew :app:assembleAndroidTest` exits 0 (test compiles)
    - No PrinterState / reconnect / capability-gating / shared connection class is introduced
  </acceptance_criteria>
  <done>A compiling, self-contained 3-step cleartext smoke test exists, ready to run on-device.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Run the cleartext smoke test on the real Nexus 7 against live Moonraker</name>
  <read_first>
    - app/src/androidTest/java/works/mees/dinghy/smoke/CleartextMoonrakerSmokeTest.kt (Task 2)
    - .planning/phases/01-platform-gate-toolkit-benchmark-cleartext-smoke-test-scaffol/01-CONTEXT.md — D-01b (pre-configure trusted-client/auth on the live Moonraker so failures are unambiguous)
  </read_first>
  <what-built>
    A cleartext NSC + manifest config (from 01-01) and a throwaway 3-step instrumented smoke test
    (ws:// open → REST GET server.info/printer.info → objects/subscribe-and-await-deterministic-update).
  </what-built>
  <how-to-verify>
    1. Ensure the Ender 5 Plus Moonraker is reachable on the LAN and its trusted-client/API-key
       config is pre-set (D-01b) so an auth failure would be unambiguous, not a silent pass.
    2. Connect the real Nexus 7 2013 via adb (`adb devices` shows it).
    3. Set the Moonraker host/port (instrumentation arg or test constant) to the live printer IP.
    4. Run: `./gradlew :app:connectedAndroidTest` (installs the androidTest build and runs the
       smoke test on the device).
    5. Expected: the test PASSES — ws:// onOpen fired, server.info returned 2xx with expected keys,
       and the subscribed object's deterministic update arrived within the timeout.
    6. Capture the test report / logcat snippet into the SUMMARY as the CONN-05 evidence.
  </how-to-verify>
  <acceptance_criteria>
    - `./gradlew :app:connectedAndroidTest` runs against the physical Nexus 7 2013 and the smoke test PASSES
    - Evidence (test report or logcat) showing all three steps succeeding is recorded in the SUMMARY
    - Run is on the real device + live Moonraker (not emulator, not a mock)
  </acceptance_criteria>
  <resume-signal>Type "approved" with the passing result, or describe the failure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tablet → Moonraker (LAN) | Cleartext ws:// + http:// crosses the local network unencrypted |
| same APK → API 24+ devices | Broad NSC permits cleartext beyond the API-23 target too |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Information Disclosure | cleartext ws://+http:// on LAN | accept | Scoped decision: Moonraker is the only integration surface and printer+tablet share a trusted LAN; documented as a deliberate decision, not an oversight (CONN-05) |
| T-01-05 | Tampering | LAN MITM of cleartext printer control | accept | Trusted-LAN threat model; no PII, low-value target; TLS not offered by typical Moonraker setups |
| T-01-06 | Information Disclosure | broad NSC permits cleartext on API 24+ devices | accept (flagged-deferred) | D-11: noted as deferred-validation item to scope cleartext to specific domains later; NOT a Phase-1 blocker |
</threat_model>

<verification>
- The 3-step smoke test passes on the real Nexus 7 against the live Moonraker.
- Cleartext config (inherited from 01-01) permits cleartext on both API 23 and API 24+.
</verification>

<success_criteria>
CONN-05 satisfied: cleartext ws:// connects, REST GET server.info/printer.info returns, and a
deterministic objects/subscribe update is awaited and received — proven on the real API-23 device.
</success_criteria>

<output>
Create `.planning/phases/01-platform-gate-toolkit-benchmark-cleartext-smoke-test-scaffol/01-02-SUMMARY.md` when done
</output>
