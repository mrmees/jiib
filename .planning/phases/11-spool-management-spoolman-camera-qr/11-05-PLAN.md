---
phase: 11-spool-management-spoolman-camera-qr
plan: 05
type: execute
wave: 3
depends_on: ["11-01", "11-03"]
files_modified:
  - gradle/libs.versions.toml
  - app/build.gradle.kts
  - app/src/main/AndroidManifest.xml
  - app/src/main/java/works/mees/dinghy/ui/spool/scan/QrCodeAnalyzer.kt
  - app/src/test/java/works/mees/dinghy/spool/ZxingDecodeVersionTest.kt
autonomous: true
requirements: [SPOOL-05, SPOOL-06]
must_haves:
  truths:
    - "CameraX 1.5.x + zxing:core 3.3.3 are pinned in the version catalog and wired into app deps without moving the merged-manifest minSdk off 23"
    - "AndroidManifest declares CAMERA permission + camera uses-feature required=false so a camera-less device still installs"
    - "QrCodeAnalyzer decodes QR-only frames from planes[0] using rowStride, closes the ImageProxy in finally, and swallows NotFoundException"
    - "ZxingDecodeVersionTest decodes a known QR on the JVM, locking the 3.3.3 pin against the API-23 decode-path regression"
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/ui/spool/scan/QrCodeAnalyzer.kt"
      provides: "ZXing QR-only ImageAnalysis.Analyzer with injectable onResult"
    - path: "app/src/test/java/works/mees/dinghy/spool/ZxingDecodeVersionTest.kt"
      provides: "JVM decode guard locking the zxing 3.3.3 pin"
    - path: "gradle/libs.versions.toml"
      provides: "Pinned cameraX + zxingCore versions + libraries"
      contains: "zxingCore"
  key_links:
    - from: "app/build.gradle.kts"
      to: "libs.versions.toml cameraX/zxingCore"
      via: "implementation(libs.*)"
      pattern: "implementation\\(libs\\."
---

<objective>
Add the camera + QR-decode toolchain: pin CameraX 1.5.x + `com.google.zxing:core:3.3.3` in the version catalog, wire the five `implementation` lines into `app/build.gradle.kts`, declare the CAMERA permission + `uses-feature required="false"` in the manifest, build the lean `QrCodeAnalyzer` (the project's first `ImageAnalysis.Analyzer` / first ZXing use), and lock the 3.3.3 pin with a JVM `ZxingDecodeVersionTest`. Re-run `verifyMinSdkRelease` to prove the floor held.

Purpose: This is the version-landmine wave (RESEARCH Pitfall 1/2). CameraX 1.5.0 raised its floor to EXACTLY 23 (matching ours) and zxing 3.4.0+ silently crashes the decode path on API 23 — an emulator (API 30+) would NOT catch it, which is exactly the mock-vs-reality class of bug the JVM decode test + the 3.3.3 pin prevent. All packages are VERIFIED first-party Google/AndroidX + canonical ZXing (RESEARCH Package Legitimacy Audit — no ASSUMED/SUS/SLOP, so no legitimacy checkpoint).

Output: 3 modified build/manifest files + 1 new analyzer + 1 new JVM guard test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-spool-management-spoolman-camera-qr/11-PATTERNS.md
@.planning/phases/11-spool-management-spoolman-camera-qr/11-RESEARCH.md
@gradle/libs.versions.toml
@app/build.gradle.kts
@app/src/main/AndroidManifest.xml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pin CameraX + zxing in the catalog, wire deps, declare manifest, prove the floor</name>
  <files>gradle/libs.versions.toml, app/build.gradle.kts, app/src/main/AndroidManifest.xml</files>
  <read_first>
    - gradle/libs.versions.toml lines 18-58 (the pinned-with-comment [versions] idiom + the [libraries] coordinate idiom) + lines 25, 49 (the existing minSdk floor + verifyMinSdk commentary)
    - app/build.gradle.kts lines 8, 32, 155 (the verifyMinSdk task registration + the minSdk floor + the existing dependencies block end) — find the dependencies{} block (deps added after the last existing implementation line ~126)
    - app/src/main/AndroidManifest.xml lines ~30-40 (the existing uses-permission block; the Phase-1 cleartext posture is untouched — additive only)
    - .planning/phases/11-spool-management-spoolman-camera-qr/11-PATTERNS.md §"gradle/libs.versions.toml + app/build.gradle.kts" + §"AndroidManifest.xml"
    - .planning/phases/11-spool-management-spoolman-camera-qr/11-RESEARCH.md lines 122 (verifyMinSdkRelease proof) + 124-138 (Package Legitimacy Audit — all VERIFIED first-party) + 246-250 (the 3.4.0 API-23 landmine)
  </read_first>
  <action>
    In libs.versions.toml add to [versions]: cameraX = "1.5.0" (comment: minSdk 23 == floor; PROVEN via verifyMinSdk) and zxingCore = "3.3.3" (comment: NOT 3.4.0+ — decode path uses List.sort/API 24 above 3.3.3, RESEARCH Pitfall 1), mirroring the pinned-with-comment idiom. Add to [libraries] the four androidx.camera:camera-{core,camera2,lifecycle,view} (all version.ref = "cameraX") and com.google.zxing:core (version.ref = "zxingCore"). In app/build.gradle.kts add the five implementation(libs.…) lines in the dependencies block after the last existing implementation. In AndroidManifest.xml add (additive, NOT touching the cleartext posture): uses-feature android.hardware.camera required="false", uses-feature android.hardware.camera.autofocus required="false", and uses-permission android.permission.CAMERA. required="false" is load-bearing (D-15) — a camera-less device still installs and degrades to picker-only. Do NOT add coreLibraryDesugaring (the 3.3.3 pin avoids it).
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:verifyMinSdkRelease :app:compileDebugKotlin --no-daemon" 2>&1 | tr -d '\r' | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - libs.versions.toml pins cameraX 1.5.0 + zxingCore 3.3.3 with comments; [libraries] carries all five coordinates.
    - app/build.gradle.kts wires the five implementation(libs.*) lines; no coreLibraryDesugaring added.
    - AndroidManifest declares CAMERA + both camera uses-feature required="false".
    - verifyMinSdkRelease PASSES (merged-manifest minSdk still EXACTLY 23) and compileDebugKotlin succeeds.
  </acceptance_criteria>
  <done>Camera/zxing deps pinned + wired; manifest declares camera; the minSdk-23 floor is proven held.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ZxingDecodeVersionTest (lock the 3.3.3 pin on the JVM)</name>
  <files>app/src/test/java/works/mees/dinghy/spool/ZxingDecodeVersionTest.kt</files>
  <behavior>
    - Encoding a known string ("web+spoolman:s-42") to a QR BitMatrix via QRCodeWriter, then decoding it back via MultiFormatReader QR-hinted, yields the exact same string on the JVM.
    - The decode path executes without the API-24 List.sort crash (proves the 3.3.3 pin is the resolved coordinate, not 3.4.0+).
  </behavior>
  <read_first>
    - .planning/phases/11-spool-management-spoolman-camera-qr/11-RESEARCH.md lines 200-224 (the QrCodeAnalyzer MultiFormatReader QR-hint setup) + lines 246-250 (the 3.4.0 decode-path landmine this test guards) + line 440 (the ZxingDecodeVersionTest intent)
    - app/src/test/java/works/mees/dinghy/spool/QrPayloadParserTest.kt (11-01 — the sibling spool test package layout/imports)
  </read_first>
  <action>
    Create ZxingDecodeVersionTest.kt: a pure JVM JUnit test (no Android) that round-trips a QR. Encode "web+spoolman:s-42" with com.google.zxing.qrcode.QRCodeWriter to a BitMatrix, wrap it in a BufferedImage/luminance source the reader can consume (a BitMatrix → boolean grid → a simple BooleanArrayLuminanceSource or the BitMatrix-to-BinaryBitmap path zxing's test idiom uses), then decode it with MultiFormatReader hinted POSSIBLE_FORMATS = [QR_CODE], asserting the decoded text equals the input. The test's real job is to FAIL TO RESOLVE/RUN if the coordinate ever drifts to 3.4.0+ (the decode path is the regression surface) — so keep it a genuine decode, not a stub. This satisfies the Wave-0 ZxingDecodeVersionTest placeholder.
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --tests *ZxingDecodeVersionTest --no-daemon" 2>&1 | tr -d '\r' | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - ZxingDecodeVersionTest GREEN: the encoded-then-decoded QR round-trips to the exact input string on the JVM.
    - The test is a real decode (not a stub), so a future drift to zxing 3.4.0+ surfaces as a failing/erroring test.
  </acceptance_criteria>
  <done>ZxingDecodeVersionTest GREEN; the 3.3.3 pin is locked by a real JVM decode.</done>
</task>

<task type="auto">
  <name>Task 3: QrCodeAnalyzer (QR-only ImageAnalysis.Analyzer)</name>
  <files>app/src/main/java/works/mees/dinghy/ui/spool/scan/QrCodeAnalyzer.kt</files>
  <read_first>
    - .planning/phases/11-spool-management-spoolman-camera-qr/11-RESEARCH.md lines 200-224 (Pattern 1 — the verbatim analyzer: MultiFormatReader QR-hint, planes[0].buffer, PlanarYUVLuminanceSource with rowStride NOT width, HybridBinarizer, decodeWithState, catch NotFoundException no-op, image.close() in finally) + lines 247-250 (the rowStride + image.close anti-patterns)
    - .planning/phases/11-spool-management-spoolman-camera-qr/11-PATTERNS.md §"ui/spool/scan/QrCodeAnalyzer.kt (utility, streaming) — NO ANALOG"
    - app/src/main/java/works/mees/dinghy/ui/spool/scan/QrPayloadParser.kt (11-03 — onResult feeds the parser; keep the boundary string→parser clean)
  </read_first>
  <action>
    Create QrCodeAnalyzer.kt implementing androidx.camera.core.ImageAnalysis.Analyzer with a constructor `onResult: (String) -> Unit` (injectable so the parse side stays headless-testable). Copy RESEARCH Pattern 1 verbatim: a MultiFormatReader configured with DecodeHintType.POSSIBLE_FORMATS = listOf(BarcodeFormat.QR_CODE); in analyze(image: ImageProxy) read image.planes[0].buffer into a ByteArray, build PlanarYUVLuminanceSource(data, image.planes[0].rowStride, image.height, 0, 0, image.width, image.height, false) — rowStride NOT width (the common bug) — wrap in BinaryBitmap(HybridBinarizer(source)), try onResult(reader.decodeWithState(bitmap).text), catch NotFoundException as a no-op (no QR this frame is normal), catch any other Throwable as ignore-keep-scanning, and ALWAYS image.close() in finally (KEEP_ONLY_LATEST stalls otherwise). The analyzer does NOT parse the payload — it emits the raw decoded string to onResult; QrPayloadParser (11-03) owns the untrusted-input parse. Keep it lean and stateless beyond the reused MultiFormatReader.
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:compileDebugKotlin --no-daemon" 2>&1 | tr -d '\r' | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - QrCodeAnalyzer implements ImageAnalysis.Analyzer with an injectable onResult: (String) -> Unit.
    - The luminance source uses planes[0].rowStride (not width); image.close() is in a finally; NotFoundException is a silent no-op.
    - The analyzer emits the raw decoded string only — it does NOT parse/validate (that is QrPayloadParser's job).
    - :app:compileDebugKotlin succeeds.
  </acceptance_criteria>
  <done>QrCodeAnalyzer compiles; QR-only, rowStride-correct, always-close, parse-free.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| zxing decode path → app (on API 23) | A version-drift to 3.4.0+ silently crashes decode on the floor device; the pin + JVM test are the control. |
| camera frame → decoded string | Raw decoded text is untrusted; the analyzer emits it as-is, QrPayloadParser (11-03) validates. |
| Maven coordinates → build | Supply-chain: only canonical first-party Google/AndroidX + ZXing coordinates. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-05-01 | Tampering | zxing 3.4.0+ silent API-23 decode crash | mitigate | Pin 3.3.3 + ZxingDecodeVersionTest (real JVM decode) + the version-catalog comment (RESEARCH Pitfall 1) |
| T-11-05-02 | DoS (floor break) | A camera dep raises merged-manifest minSdk off 23 | mitigate | CameraX 1.5.0 (floor==23) + verifyMinSdkRelease re-run is the build gate (RESEARCH Pitfall 2) |
| T-11-05-03 | Info disclosure | Over-broad camera permission / camera-required device exclusion | mitigate | uses-feature required="false" (camera-less installs); CAMERA permission requested only at scan entry (11-07) |
| T-11-05-04 | Tampering (frame proxy) | Unclosed ImageProxy stalls KEEP_ONLY_LATEST | mitigate | image.close() in finally; rowStride (not width) luminance source |
| T-11-05-SC | Tampering | Maven/Gradle dependency installs | accept | RESEARCH Package Legitimacy Audit: all five coordinates VERIFIED first-party Google/AndroidX + canonical ZXing (github.com/zxing/zxing); slopcheck targets npm/PyPI not Maven; zero ASSUMED/SUS/SLOP → no human-verify legitimacy checkpoint required |
</threat_model>

<verification>
- :app:verifyMinSdkRelease PASSES (merged minSdk EXACTLY 23) after the camera deps land.
- :app:testDebugUnitTest --tests *ZxingDecodeVersionTest GREEN.
- QrCodeAnalyzer uses rowStride (grep `rowStride` present, `image.width` only in the crop-width slot) and closes in finally.
- AndroidManifest carries CAMERA + camera uses-feature required="false".
</verification>

<success_criteria>
The camera/QR toolchain is in and proven: CameraX 1.5.x + zxing 3.3.3 pinned and wired without moving the minSdk-23 floor (verifyMinSdkRelease green), the manifest declares an optional camera, the QrCodeAnalyzer decodes QR-only frames correctly (rowStride, always-close, parse-free), and the 3.3.3 pin is locked by a real JVM decode test.
</success_criteria>

<output>
Create `.planning/phases/11-spool-management-spoolman-camera-qr/11-05-SUMMARY.md` when done
</output>
</output>
