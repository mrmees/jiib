---
phase: 11-spool-management-spoolman-camera-qr
plan: 03
type: execute
wave: 1
depends_on: ["11-01"]
files_modified:
  - app/src/main/java/works/mees/dinghy/ui/spool/scan/QrPayloadParser.kt
  - app/src/main/java/works/mees/dinghy/ui/spool/PrintStartGate.kt
  - app/src/main/java/works/mees/dinghy/ui/spool/scan/ScanState.kt
autonomous: true
requirements: [SPOOL-05, SPOOL-06, SPOOL-07]
must_haves:
  truths:
    - "QR parser accepts web+spoolman:s-<id> (case-insensitive) + /spool/show/<id> URL, rejects f-/UPC/non-numeric/hostile-host, never trusts the host"
    - "PrintStartGate is warn-only: every D-01 condition returns a warning, none hard-blocks"
    - "ScanState machine derives every degrade state (granted/denied/no-camera/busy/unreadable/no-QR/unsupported)"
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/ui/spool/scan/QrPayloadParser.kt"
      provides: "D-12 untrusted-input parser returning a sealed SpoolQrResult"
    - path: "app/src/main/java/works/mees/dinghy/ui/spool/PrintStartGate.kt"
      provides: "D-01 warn-only gate decision function"
    - path: "app/src/main/java/works/mees/dinghy/ui/spool/scan/ScanState.kt"
      provides: "Headless scan/permission/degrade state machine"
  key_links:
    - from: "PrintStartGate"
      to: "SpoolmanModels + FilePreviewMetadata"
      via: "pure function inputs"
      pattern: "fun .*[Gg]ate\\("
---

<objective>
Build the three pure, host-tested decision functions that carry this phase's load-bearing logic: the QR payload parser (D-12, the security-critical untrusted-input control), the warn-only print-start gate (D-01), and the headless scan/permission/degrade state machine (D-15). All no-Android, no-I/O, exhaustively unit-tested. Turns the Wave-0 QR/gate/state-machine scaffolds GREEN.

Purpose: These are the "let nothing slip past in the wrong shape" functions — especially the QR parser, which is the V5 input-validation boundary (a scanned label is untrusted). They are pure so they can be locked by exhaustive tables before any UI exists.

Output: 3 new headless files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-spool-management-spoolman-camera-qr/11-PATTERNS.md
@.planning/phases/11-spool-management-spoolman-camera-qr/11-RESEARCH.md
@docs/view_specific_notes/spoolman.md
@app/src/main/java/works/mees/dinghy/ui/files/DeleteGate.kt
@app/src/main/java/works/mees/dinghy/ui/route/TopRoute.kt
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: QR payload parser (D-12, untrusted input)</name>
  <files>app/src/main/java/works/mees/dinghy/ui/spool/scan/QrPayloadParser.kt</files>
  <behavior>
    - "web+spoolman:s-123" → Spool(123); "WEB+SPOOLMAN:S-123" → Spool(123) (case-insensitive).
    - "https://spoolman.local/spool/show/7" → Spool(7) (id only; host NEVER used or returned).
    - "http://evil.example/spool/show/9" → Spool(9) (still id-only; the hostile host is ignored, never navigated).
    - "web+spoolman:f-3" → UnsupportedSpoolmanCode.
    - "012345678905" (UPC) / "abc" / "web+spoolman:s-" (no digits) / "https://x/other/5" → NotASpoolCode.
    - Overlong/garbage id strings → NotASpoolCode (Int-validated; no overflow crash).
  </behavior>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/files/DeleteGate.kt lines 22-32 (the pure host-tested single-purpose predicate idiom + exhaustive contract doc)
    - .planning/phases/11-spool-management-spoolman-camera-qr/11-RESEARCH.md lines 343-349 (parser contract) + lines 461-471 (Security V5 / STRIDE: parse id only, never navigate/trust host, allow-list)
    - .planning/phases/11-spool-management-spoolman-camera-qr/11-PATTERNS.md §QrPayloadParser.kt
    - docs/view_specific_notes/spoolman.md lines 440-450 (the scan→parse→fetch→confirm flow; id is the only thing carried forward)
  </read_first>
  <action>
    Create QrPayloadParser.kt with a sealed SpoolQrResult { data class Spool(val id: Int); object UnsupportedSpoolmanCode; object NotASpoolCode } and `fun parseSpoolId(payload: String): SpoolQrResult`. Accept (case-insensitive) `web+spoolman:s-<digits>` → Spool(id) and `http(s)://<host>/spool/show/<digits>` → Spool(id) parsing ONLY the trailing id (never read, return, or act on the host — D-12/V5). Reject `web+spoolman:f-<id>` and any other scheme → UnsupportedSpoolmanCode; reject UPC/EAN, non-numeric, missing id, or a URL without `/spool/show/<digits>` → NotASpoolCode. Int-validate the id (toIntOrNull; an overlong digit run → NotASpoolCode, no overflow throw). Document every accept/reject class in the KDoc exactly like DeleteGate. Pure function, no Android. Conform the sealed result to whatever QrPayloadParserTest (11-01) expects so it goes GREEN.
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --tests *QrPayloadParserTest --no-daemon" 2>&1 | tr -d '\r' | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - QrPayloadParserTest GREEN across all four accept/reject classes incl. uppercase + hostile-host URL.
    - The host substring is never returned or used; only the Int id is carried.
    - An overlong/garbage id yields NotASpoolCode, not a crash.
  </acceptance_criteria>
  <done>QrPayloadParser compiles; QrPayloadParserTest GREEN; host never trusted.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Warn-only print-start gate (D-01)</name>
  <files>app/src/main/java/works/mees/dinghy/ui/spool/PrintStartGate.kt</files>
  <behavior>
    - No active spool → one amber warning ("No active spool selected for Spoolman tracking"); NEVER blocks.
    - Material family mismatch (file wants PLA, active is PETG, family-match per D-05) → warning naming both sides.
    - remaining_weight < filament_weight_total + margin → warning ("File needs ~Ng. Spool reports Mg.").
    - filament_weight_total == null → low-filament check SKIPPED (no length/density fallback).
    - Active spool archived → warning (D-09 allow-with-warning).
    - Non-empty pending_reports → "usage queued / remaining may be stale" warning (D-11).
    - Active-spool detail fetch failed → warning allowing retry/override.
    - All clear → empty warning list (the normal Print confirm shows unchanged).
    - Returns a LIST of warnings; nothing in the function hard-blocks.
  </behavior>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/files/DeleteGate.kt (exact pure-predicate role + same directory)
    - docs/view_specific_notes/spoolman.md lines 505-515 (the gate condition list) + .planning/.../11-RESEARCH.md lines 23 (D-01 full decision table) + line 40 (the discretionary safety-margin recommendation: max(total*1.05, total+10g))
    - .planning/phases/11-spool-management-spoolman-camera-qr/11-PATTERNS.md §PrintStartGate.kt + §"Material family match" (D-05 partial case-insensitive)
    - app/src/main/java/works/mees/dinghy/state/PrintMetadata.kt (the extended FilePreviewMetadata shape from 11-02 — filamentType[], filamentWeights[], filament_weight_total)
  </read_first>
  <action>
    Create PrintStartGate.kt with a SpoolWarning sealed type (or a data class carrying a stable kind + display text + the relevant action affordances) and `fun evaluatePrintStartGate(activeSpool: SpoolmanSpool?, status: SpoolmanStatus, fetchFailed: Boolean, file: FilePreviewMetadata, marginGrams: Double = ...): List<SpoolWarning>`. Implement each D-01 branch as an appended warning: no active spool (status.spool_id null OR activeSpool null), material family mismatch (compare file.filamentType family-partial case-insensitive against activeSpool.filament.material per D-05 — multi-family file types match if ANY family matches), remaining < filament_weight_total + margin (skip entirely when filament_weight_total == null), archived, non-empty pending_reports (stale), fetchFailed. Pick the discretionary safety margin as `max(filamentWeightTotal * 1.05, filamentWeightTotal + 10.0)` per RESEARCH recommendation and document the constant. NEVER return a "block" — the function only produces warnings the confirm flow taps past. All-clear → empty list. Pure, host-tested. Turn PrintStartGateTest GREEN.
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --tests *PrintStartGateTest --no-daemon" 2>&1 | tr -d '\r' | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - PrintStartGateTest GREEN: every D-01 condition produces its warning; all-clear → empty; nothing blocks.
    - filament_weight_total == null skips the low-filament check (no fallback).
    - Material family match is partial + case-insensitive (PLA matches PLA+); multi-family file matches if any family matches.
  </acceptance_criteria>
  <done>PrintStartGate compiles; PrintStartGateTest GREEN; warn-only contract proven.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Headless scan/permission/degrade state machine (D-15)</name>
  <files>app/src/main/java/works/mees/dinghy/ui/spool/scan/ScanState.kt</files>
  <behavior>
    - permission granted + camera present → Active (scanning).
    - permission denied → PermissionDenied with a "Use picker instead" escape.
    - no camera hardware → NoCamera with the picker escape.
    - camera busy / bind failure → Busy/Error with escape.
    - decode produced UnsupportedSpoolmanCode → Unsupported (stay in scan flow, D-12).
    - decode produced NotASpoolCode → NotRecognized (stay in scan flow).
    - a valid Spool(id) decoded → AwaitingConfirm(id) — NEVER auto-loads (D-12 confirm-first).
    - every terminal/degrade state exposes a path back to the manual picker (D-15).
  </behavior>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/route/TopRoute.kt lines 57-62 (the pure derive() state-derivation idiom to mirror)
    - .planning/phases/11-spool-management-spoolman-camera-qr/11-RESEARCH.md lines 237-244 (Pattern 3 permission flow) + line 251 (auto-load anti-pattern) + line 37 (D-15 degrade states)
    - .planning/phases/11-spool-management-spoolman-camera-qr/11-PATTERNS.md §CameraPermission.kt (headless ScanState machine = derive()-style; Activity Result glue is net-new and deferred to 11-06)
    - app/src/main/java/works/mees/dinghy/ui/spool/scan/QrPayloadParser.kt (the SpoolQrResult this machine consumes — from Task 1)
  </read_first>
  <action>
    Create ScanState.kt with a sealed ScanState (Idle, RequestingPermission, Active, PermissionDenied, NoCamera, Busy/Error, Unsupported, NotRecognized, AwaitingConfirm(id: Int)) and a pure derivation function that maps inputs (permissionGranted: Boolean, cameraAvailable: Boolean, bindFailed: Boolean, lastDecode: SpoolQrResult?) → ScanState following the behavior table. A valid Spool(id) decode → AwaitingConfirm(id) (confirm-first, NEVER auto-load — D-12). Unsupported/NotRecognized stay in the scan flow. Every degrade/terminal state must be reachable to the manual picker (model this as a flag or a shared "canFallBackToPicker = true" — D-15). Keep this PURE (no rememberLauncherForActivityResult, no Context — that Compose glue lands in 11-06's CameraPermission wiring). Turn ScanStateMachineTest GREEN.
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --tests *ScanStateMachineTest --no-daemon" 2>&1 | tr -d '\r' | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - ScanStateMachineTest GREEN: granted/denied/no-camera/busy/unsupported/not-recognized/valid-decode all derive correctly.
    - A valid decode → AwaitingConfirm (never an auto-set/auto-load state).
    - Every degrade state exposes the picker fallback.
  </acceptance_criteria>
  <done>ScanState compiles; ScanStateMachineTest GREEN; confirm-first + degrade-to-picker proven.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scanned QR text → spool id | **The load-bearing V5 boundary.** Untrusted payload; allow-list parse, id-only, host never used. |
| file metadata + Spoolman state → gate warnings | Defensive read; warn-only never blocks the user's print. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-03-01 | Tampering / Spoofing | QR with hostile host or non-spoolman scheme | mitigate | D-12: parseSpoolId returns id only, never the host; allow-list s-/`/spool/show/`; everything else → Unsupported/NotASpoolCode |
| T-11-03-02 | Tampering | QR injects a huge/garbage id | mitigate | toIntOrNull validation → NotASpoolCode; downstream 404 handled in UI (11-08), no crash |
| T-11-03-03 | Tampering (state) | Auto-load on decode bypasses confirm | mitigate | D-12: valid decode → AwaitingConfirm only; no auto-set state exists |
| T-11-03-SC | Tampering | npm/pip/cargo installs | accept | No package installs (pure Kotlin) |
</threat_model>

<verification>
- `--tests *QrPayloadParserTest *PrintStartGateTest *ScanStateMachineTest` all GREEN.
- Grep QrPayloadParser for any host/url field carried into the result → none.
</verification>

<success_criteria>
The three pure decision functions are complete and green: QR parser allow-lists id-only (host never trusted), the gate is provably warn-only across every D-01 condition, and the scan state machine confirms-first and always degrades to the picker.
</success_criteria>

<output>
Create `.planning/phases/11-spool-management-spoolman-camera-qr/11-03-SUMMARY.md` when done
</output>
