---
phase: 11-spool-management-spoolman-camera-qr
plan: 09
type: execute
wave: 6
depends_on: ["11-06", "11-07", "11-08"]
files_modified:
  - docs/view_specific_notes/spoolman_uat.md
autonomous: false
requirements: [SPOOL-01, SPOOL-05, SPOOL-06]
must_haves:
  truths:
    - "On a real printer + Spoolman: load filament → scan its label → confirm → the active spool flips in Spoolman (the headline end-to-end loop, SC-4)"
    - "Camera permission is requested gracefully and a no-camera path degrades to the manual picker (SC-3)"
    - "The camera releases on nav-away/background (no held camera after leaving the scan surface)"
    - "All live writes are bracketed by the save→change→restore protocol; baselines restored (E5 active=5, E3 active=1, sacrificial spool=3)"
  artifacts:
    - path: "docs/view_specific_notes/spoolman_uat.md"
      provides: "Recorded on-device UAT results + restore confirmation"
  key_links:
    - from: "scan a real label"
      to: "active spool flips in Spoolman"
      via: "QrPayloadParser → confirm → set_spool_id"
      pattern: "active spool flips"
---

<objective>
The one-shot end-to-end gate: build + install the full app on the real device (flox) against a live printer + Spoolman, and verify the whole view hands-on — the headline scan-to-assign loop, the capability gate, graceful permission + no-camera degrade, the active-spool card variants, the picker filters, change-during-print, the warn-only gate, and measured-weight — all under the mandatory Live-Test Safety save→change→restore protocol. This is a BLOCKING human-verify checkpoint: Claude builds/installs and drives what it can, Matthew eyeballs/scans the physical label and confirms the active spool flips in Spoolman.

Purpose: SPOOL-01 (umbrella — the whole view works), SC-2/SC-3/SC-4 (scan decodes + resolves + sets active; graceful permission + no-camera degrade; provable end-to-end on a real printer). The project has taken SEVEN mock-vs-reality strikes — green unit suites have repeatedly hidden runtime bugs only hands-on live testing caught. This gate is the 8th-strike prevention. Per Matthew's one-shot directive, UAT is presented only now, with EVERYTHING done.

Output: 1 UAT record doc + the save→change→restore restore confirmation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-spool-management-spoolman-camera-qr/11-CONTEXT.md
@.planning/phases/11-spool-management-spoolman-camera-qr/11-VALIDATION.md
@docs/view_specific_notes/spoolman_live_validation.md
@docs/view_specific_notes/spoolman.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build, install, and capture pre-test baselines (save step)</name>
  <files>docs/view_specific_notes/spoolman_uat.md</files>
  <read_first>
    - CLAUDE.md (project) §"Local Build Environment" (the E:\Android\gw.bat build helper; release-on-device sign step E:\Android\sign-release.bat; flox runs LineageOS 18.1 / API 30)
    - .planning/phases/11-spool-management-spoolman-camera-qr/11-CONTEXT.md §"Live-Test Safety Protocol" (the mandatory save→change→restore: capture server.spoolman.status active spool_id + GET /api/v1/spool/{id} of any spool to be modified → *-before.json; E5 baseline active=5, E3 baseline active=1, sacrificial spool=3)
    - tools/spoolman-probe.py + docs/view_specific_notes/spoolman_live_probe.md (the read-only probe to capture before/after fixtures + --listen for notify payloads)
    - MEMORY "Dinghy Display printer addresses" (E5 Plus 192.168.1.120:7125, E3 Pro 192.168.1.121:7125, Spoolman 192.168.1.253:7912)
  </read_first>
  <action>
    Build the app (E:\Android\gw.bat :app:assembleRelease or assembleDebug per the device-install norm; sign via E:\Android\sign-release.bat if release) and adb install to flox. BEFORE any live write, run the save step of the Live-Test Safety protocol: use tools/spoolman-probe.py to capture the target printer's current server.spoolman.status (active spool_id) AND a full GET /api/v1/spool/{id} for the sacrificial spool (3) and the baseline active spool → save as *-before.json fixtures (mirroring the existing spoolman-live-ender5-status-before-set.json / …-direct-spool3-before.json pattern). Record the captured baselines (E5 active=5, E3 active=1, sacrificial=3) at the top of a new docs/view_specific_notes/spoolman_uat.md so the restore values are unambiguous. Confirm the build installed and the Spool drawer tile is LIVE (not greyed) on the printer that runs the spoolman component.
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:assembleDebug --no-daemon" 2>&1 | tr -d '\r' | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - The app builds and installs on flox; the Spool drawer tile is live (capability gate passes) against the spoolman-running printer.
    - Pre-test baselines captured to *-before.json (active spool_id + sacrificial spool 3) and recorded in spoolman_uat.md (E5=5 / E3=1 / sacrificial=3).
  </acceptance_criteria>
  <done>Build installed; baselines saved; restore values recorded before any write.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Hands-on end-to-end UAT on flox + live printer (SC-2/SC-3/SC-4)</name>
  <action>
    BLOCKING human-verify checkpoint — pause for Matthew; never auto-advance (this writes to a real printer, so workflow.auto_advance is ignored). Claude has already built/installed and captured the *-before.json baselines (Task 1) and drives everything it can (probe captures, build, adb, the Fluidd/probe external-change in check 8, and all restore writes). Matthew physically scans the printed Spoolman QR label (check 3) and eyeballs the on-device UI. Walk all nine checks in <how-to-verify> on flox against a live printer; bracket every write with the save→change→restore protocol; then RESTORE the baselines (active back to E5=5 / E3=1, spool 3 fields restored, re-GET to confirm) before the checkpoint resolves. Record per-check PASS/FAIL + any runtime findings in docs/view_specific_notes/spoolman_uat.md.
  </action>
  <files>docs/view_specific_notes/spoolman_uat.md</files>
  <what-built>
    The complete Spool view: capability-greyed Dest.Spool drawer tile, the Print-Status active-spool card (all D-03 state variants + Scan/Change/Clear), the Files-style picker (material/color/vendor/location filters + recent/low sorts), the full-screen QR scan surface (CameraX + ZXing, back camera, lifecycle release), confirm-first scan-to-assign, the warn-only print-start gate folded into the Files confirm, change-during-print, and measured gross-weight correction. Built one-shot per Matthew's directive; this is the single UAT presentation with everything done.
  </what-built>
  <how-to-verify>
    Run each check on flox against the live printer (E5 Plus 192.168.1.120 active baseline 5, or E3 Pro 192.168.1.121 active baseline 1) + Spoolman 192.168.1.253:7912. Save→change→restore brackets every write.

    1. CAPABILITY GATE (SC-1): On the spoolman-running printer the Spool drawer tile is LIVE; the active-spool card on Print Status shows the loaded spool (material / color swatch / vendor·name / remaining). On a printer/connection WITHOUT the spoolman component the tile greys and the card shows "unavailable".
    2. PICKER (SPOOL-03): Open Dest.Spool. The dense list loads; material family chip (e.g. PLA) filters to the expected rows (live-proven PLA → 7 rows incl. PLA+ 2.0); tapping a color swatch triggers the similarity filter; each result row shows its ACTUAL swatch; recent/low-remaining sorts reorder.
    3. HEADLINE SCAN-TO-ASSIGN (SC-2/SC-4 — the load-bearing check): Load filament whose Spoolman spool is NOT the current active. Launch Scan from the card/Spool screen. Grant the camera permission when prompted. Point the BACK camera at the printed Spoolman QR label. The decode resolves to the spool; a confirm card shows its detail (NEVER auto-loads). Confirm "Set active". VERIFY in Spoolman (192.168.1.253:7912 web UI or a probe GET) that the active spool flipped to the scanned id. This is the phase's success criterion 4 — load → scan → active flips.
    4. PERMISSION + NO-CAMERA DEGRADE (SC-3): Deny the camera permission once → the surface degrades to a clear state with a "Use picker instead" escape and the manual picker still sets active. (If feasible, confirm a no-camera path also routes to the picker.)
    5. CAMERA RELEASE (D-14): After a scan, navigate away / background the app → the camera is released (no held-camera indicator; re-entering re-acquires cleanly).
    6. WARN-ONLY GATE (SPOOL-07/D-01): Start a print of a file whose material mismatches the active spool (or with no active spool) → an amber proceed-at-peril warning appears in the Files confirm with Pick spool / Scan / Print anyway / Back; "Print anyway" proceeds in ONE tap (never blocks); a matching file shows the normal confirm unchanged.
    7. CHANGE-DURING-PRINT (SPOOL-09): With a print running, change the active spool from the card/picker → it sets without interrupting the print; the card reconciles to the new id.
    8. EXTERNAL-CHANGE RECONCILE (SPOOL-08/D-10): Change the active spool from Fluidd/Mainsail (or a probe set_spool_id) → Dinghy's card flips to the new id without a manual refresh.
    9. MEASURED WEIGHT (SPOOL-09/D-04): On the SACRIFICIAL spool (3), use measured gross-weight; the page shows remaining/used are linked; the corrected remaining reflects after the write.

    RESTORE (mandatory, after the checks): restore the active spool to its baseline (POST server.spoolman.set_spool_id to 5 for E5 / 1 for E3) and PATCH any modified field on spool 3 back to its saved value; re-GET to confirm (*-after-restore.json). Never issue delete calls.
  </how-to-verify>
  <verify>
    <human-check>All nine checks PASS on flox + the live printer; the headline scan→flip is confirmed in Spoolman; permission/no-camera degrade to the picker; the camera releases on nav-away; and the save→change→restore restore is confirmed (baselines back: E5=5 / E3=1 / spool 3 restored).</human-check>
  </verify>
  <done>All nine UAT checks pass hands-on; the headline scan-to-assign flips the active spool in Spoolman; baselines restored and confirmed.</done>
  <resume-signal>Record per-check PASS/FAIL (with any runtime findings) in docs/view_specific_notes/spoolman_uat.md, confirm the save→change→restore restore completed (baselines back: E5=5 / E3=1 / spool 3 restored), then type "approved" or describe issues for gap-closure planning.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live UAT writes → real printer + Spoolman | Every write is bracketed by save→change→restore; no delete calls; baselines restored. |
| physical QR label → set active | The end-to-end proof that the id-only confirm-first path (D-12) works against a real printed label. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-09-01 | Tampering (real device) | UAT write leaves a printer/spool in a wrong state | mitigate | Mandatory save→change→restore (CONTEXT Live-Test Safety): *-before.json capture, restore to E5=5 / E3=1 / spool 3, re-GET confirm, no deletes |
| T-11-09-02 | Tampering (mock-vs-reality) | Green unit suites hid a runtime bug (8th-strike risk) | mitigate | This hands-on live gate IS the mitigation — the headline scan→flip is verified against the real Spoolman, not a fake |
| T-11-09-03 | Info disclosure | Camera left running after the scan | mitigate | Check 5 explicitly verifies release on nav-away/background (D-14) |
| T-11-09-SC | Tampering | npm/pip/cargo installs | accept | No package installs (build + UAT only; deps + legitimacy audit handled in 11-05) |
</threat_model>

<verification>
- The app builds + installs on flox; the Spool tile is live on the spoolman-running printer.
- The headline loop (load → scan real label → confirm → active flips in Spoolman) PASSES live (SC-4).
- Permission/no-camera degrade to the picker (SC-3); camera releases on nav-away (D-14).
- Save→change→restore restore confirmed: baselines back (E5=5 / E3=1 / spool 3 restored).
</verification>

<success_criteria>
The whole view is proven end-to-end on real hardware: the capability gate, picker, headline scan-to-assign (active spool flips in Spoolman), graceful permission + no-camera degrade, camera release, warn-only gate, change-during-print, external-change reconcile, and measured weight all pass hands-on — with every live write safely restored.
</success_criteria>

<output>
Create `.planning/phases/11-spool-management-spoolman-camera-qr/11-09-SUMMARY.md` when done
</output>
</output>
