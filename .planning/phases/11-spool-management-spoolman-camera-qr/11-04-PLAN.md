---
phase: 11-spool-management-spoolman-camera-qr
plan: 04
type: execute
wave: 2
depends_on: ["11-01", "11-02"]
files_modified:
  - app/src/main/java/works/mees/dinghy/net/JsonRpc.kt
  - app/src/main/java/works/mees/dinghy/net/JsonRpcClient.kt
  - app/src/main/java/works/mees/dinghy/command/CommandRegistry.kt
  - app/src/main/java/works/mees/dinghy/spool/SpoolmanClient.kt
  - app/src/main/java/works/mees/dinghy/spool/ActiveSpoolFacade.kt
  - app/src/main/java/works/mees/dinghy/di/SpineHandle.kt
  - app/src/main/java/works/mees/dinghy/di/AppContainer.kt
  - app/src/main/java/works/mees/dinghy/service/MoonrakerService.kt
autonomous: true
requirements: [SPOOL-01, SPOOL-04, SPOOL-08]
must_haves:
  truths:
    - "Active-spool get/set/clear/status route through the existing Moonraker JSON-RPC session (server.spoolman.*), capability-gated on the spoolman component"
    - "notify_active_spool_set and notify_spoolman_status_changed are routed off the 1-element params array into bounded SharedFlows; unrelated notifies fall through untouched"
    - "ActiveSpoolFacade fetches status on the connect handshake edge AND reconciles to the pushed notify id (D-10 — never re-asserts stale local state)"
    - "MoonrakerService constructs ActiveSpoolFacade (mirroring WebcamsHolder) and publishes its activeSpool flow on SpineHandle"
    - "AppContainer exposes activeSpool + spoolmanPresent derived flows mirroring the webcams/webcamCount chain"
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/spool/SpoolmanClient.kt"
      provides: "Lean proxy-v2 inventory client wrapping the session JsonRpcClient"
      contains: "interface SpoolmanClient"
    - path: "app/src/main/java/works/mees/dinghy/spool/ActiveSpoolFacade.kt"
      provides: "Session-scoped edge-driven + notify-reconciled active-spool truth"
    - path: "app/src/main/java/works/mees/dinghy/service/MoonrakerService.kt"
      provides: "Constructs ActiveSpoolFacade (WebcamsHolder precedent) + publishes activeSpool on SpineHandle"
      contains: "ActiveSpoolFacade"
    - path: "app/src/main/java/works/mees/dinghy/di/AppContainer.kt"
      provides: "activeSpool + spoolmanPresent derived flows"
      contains: "spoolmanPresent"
  key_links:
    - from: "ActiveSpoolFacade"
      to: "JsonRpcClient.activeSpoolSet / spoolmanStatusChanged SharedFlows"
      via: "notify subscription + reconcile"
      pattern: "activeSpoolSet|spoolmanStatusChanged"
    - from: "MoonrakerService"
      to: "SpineHandle.activeSpool"
      via: "constructs ActiveSpoolFacade + publishes activeFacade.activeSpool"
      pattern: "ActiveSpoolFacade"
    - from: "SpineHandle.activeSpool"
      to: "AppContainer.activeSpool"
      via: "flatMapLatest"
      pattern: "flatMapLatest"
---

<objective>
Build the Spoolman spine: the JSON-RPC method constants + the two notification routes, the four `server.spoolman.*` command specs (capability-gated), the lean `SpoolmanClient` proxy-v2 inventory reader, the `ActiveSpoolFacade` (handshake-edge fetch + D-10 notify reconciliation), the `MoonrakerService` construction/publication of that facade, and the `SpineHandle`/`AppContainer` derived flows — mirroring the `WebcamsHolder → SpineHandle.webcams → AppContainer.webcams/webcamCount` chain exactly.

Purpose: Active-spool state is Moonraker-owned (JSON-RPC); inventory is Spoolman-owned (proxy v2). This wires both into the session spine so the UI (built in later waves) consumes derived flows and never sees a raw `JsonRpcClient` (D-02 service-constructs/UI-consumes). The notify routing + reconcile satisfies the mandatory external-change handling (D-10/SPOOL-08). The service is what actually constructs the facade per session (the WebcamsHolder precedent) — without it the facade is never instantiated and the flow never publishes.

Output: 2 new spool files (`SpoolmanClient`, `ActiveSpoolFacade`) + 6 modified spine files (JsonRpc, JsonRpcClient, CommandRegistry, SpineHandle, AppContainer, MoonrakerService).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-spool-management-spoolman-camera-qr/11-PATTERNS.md
@.planning/phases/11-spool-management-spoolman-camera-qr/11-RESEARCH.md
@docs/view_specific_notes/spoolman.md
@app/src/main/java/works/mees/dinghy/webcam/WebcamsHolder.kt
@app/src/main/java/works/mees/dinghy/net/JsonRpcClient.kt
@app/src/main/java/works/mees/dinghy/net/JsonRpc.kt
@app/src/main/java/works/mees/dinghy/command/CommandRegistry.kt
@app/src/main/java/works/mees/dinghy/ui/files/FileBrowserClient.kt
@app/src/main/java/works/mees/dinghy/di/SpineHandle.kt
@app/src/main/java/works/mees/dinghy/di/AppContainer.kt
@app/src/main/java/works/mees/dinghy/service/MoonrakerService.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: JSON-RPC method constants, the two notify routes, and the four spoolman command specs</name>
  <files>app/src/main/java/works/mees/dinghy/net/JsonRpc.kt, app/src/main/java/works/mees/dinghy/net/JsonRpcClient.kt, app/src/main/java/works/mees/dinghy/command/CommandRegistry.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/net/JsonRpc.kt lines 83-87 (the ONE MoonrakerJson) + lines 124, 139-144 (the WEBCAMS_LIST + NOTIFY_* const block to mirror for the six new constants)
    - app/src/main/java/works/mees/dinghy/net/JsonRpcClient.kt lines 60-74 (the bounded _statusUpdates/_klippyEvents MutableSharedFlow + asSharedFlow precedent) + lines 178-194 (the dispatch() when(method) notification branch) + lines 217-224 (the statusDiff/gcodeLine 1-element-array extractors)
    - app/src/main/java/works/mees/dinghy/command/CommandRegistry.kt lines 26-34 (existing arg classes) + lines 208-219 (historyList args-carrying spec template) + lines 448-497 (the `all` list to append to) + lines 505-520 (the private jsonRpc(...) helper)
    - app/src/main/java/works/mees/dinghy/command/CommandSpec.kt lines 23 (AvailabilityPredicate.ComponentPresent)
    - .planning/phases/11-spool-management-spoolman-camera-qr/11-PATTERNS.md §"net/JsonRpc.kt JsonRpcMethods" + §"net/JsonRpcClient.kt" + §"command/CommandRegistry.kt"
    - docs/commands/moonraker-api.md (the now-documented notify_active_spool_set/notify_spoolman_status_changed entries added in 11-01)
  </read_first>
  <action>
    In JsonRpc.kt add to JsonRpcMethods the six constants SPOOLMAN_STATUS, SPOOLMAN_GET_SPOOL_ID, SPOOLMAN_POST_SPOOL_ID, SPOOLMAN_PROXY (the four server.spoolman.* methods) and NOTIFY_ACTIVE_SPOOL_SET, NOTIFY_SPOOLMAN_STATUS_CHANGED, mirroring the existing WEBCAMS_LIST + NOTIFY_* const block.
    In JsonRpcClient.kt add two bounded MutableSharedFlow fields (activeSpoolSet, spoolmanStatusChanged) following the _statusUpdates/_klippyEvents bounded-extraBufferCapacity precedent, expose each via asSharedFlow(); add a private spoolNotifyParam(obj) extractor that pulls obj["params"].jsonArray.firstOrNull().jsonObject inside runCatching (D-10 — Moonraker sends params as a 1-element array), mirroring statusDiff; add two when(method) arms in dispatch() that emit the extracted 1-element param object via tryEmit. Leave the existing else -> Unit fall-through untouched so unmodeled notifies (e.g. notify_proc_stat_update) are still ignored.
    In CommandRegistry.kt add SetSpoolArgs(spoolId: Int?) (null = clear per D-13 → post_spool_id {}) and SpoolmanProxyArgs(method: String, path: String, query: String? = null) next to the existing arg classes. Define four CommandSpecs via the private jsonRpc(...) helper: spoolmanStatus, spoolmanGetSpoolId, spoolmanPostSpoolId (params build {} when spoolId null else {spool_id}), and spoolmanProxy (params build use_v2_response=true, request_method, path, optional query — args-carrying like historyList). Gate spoolmanProxy and spoolmanPostSpoolId with availability = AvailabilityPredicate.ComponentPresent("spoolman") (D-02 — the spoolman component gate). Append all four specs to the `all` list.
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:compileDebugKotlin --no-daemon" 2>&1 | tr -d '\r' | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - JsonRpcMethods carries all six new constants; CommandRegistry carries four specs appended to `all`; both args classes exist.
    - spoolmanProxy + spoolmanPostSpoolId carry AvailabilityPredicate.ComponentPresent("spoolman"); the read specs route via the existing jsonRpc helper.
    - JsonRpcClient exposes activeSpoolSet + spoolmanStatusChanged SharedFlows, fed by a 1-element-array extractor; the else -> Unit fall-through is intact.
    - :app:compileDebugKotlin succeeds.
  </acceptance_criteria>
  <done>Spoolman method constants, notify routing, and four capability-gated specs compile.</done>
</task>

<task type="auto">
  <name>Task 2: Lean SpoolmanClient proxy-v2 inventory reader</name>
  <files>app/src/main/java/works/mees/dinghy/spool/SpoolmanClient.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/files/FileBrowserClient.kt lines 15-43 (interface of suspend reads returning JsonElement?, each runCatching{rpc.request(spec, args)}.getOrNull() — the EXACT shape to mirror)
    - app/src/main/java/works/mees/dinghy/command/CommandDispatchExtensions.kt (JsonRpcClient.request(spec, args) + dispatcher.dispatch(spec, args) — the verified read vs set seams)
    - app/src/main/java/works/mees/dinghy/spool/SpoolmanParsers.kt (the parseProxyEnvelope / parseSpoolmanFilaments / parseSpoolmanStatus functions from 11-02 this client feeds)
    - .planning/phases/11-spool-management-spoolman-camera-qr/11-PATTERNS.md §"spool/SpoolmanClient.kt" (the interface+impl idiom; D-07 URL-encode dotted query keys before they hit the proxy query string)
    - app/src/test/java/works/mees/dinghy/spool/FakeSpoolmanClient.kt (11-01 — the read surface the real client must satisfy: getSpool/listSpools/listFilaments/listMaterials/listVendors/listLocations/measureSpool returning JsonElement? envelopes)
  </read_first>
  <action>
    Create SpoolmanClient.kt: an `interface SpoolmanClient` of suspend reads returning JsonElement? (getSpool(id), listSpools(query: String? = null), listFilaments(query: String? = null), listMaterials(), listVendors(), listLocations(), and measureSpool(id, grossGrams) for D-04) — conforming the method names/signatures to FakeSpoolmanClient (11-01) so the same tests bind to both. Implement MoonrakerSpoolmanClient(private val rpc: JsonRpcClient): each method builds a SpoolmanProxyArgs(method=<GET|PUT>, path=<"/v1/spool"/"/v1/filament"/"/v1/material"/"/v1/vendor"/"/v1/location"/"/v1/spool/{id}/measure">, query) and returns runCatching { rpc.request(CommandRegistry.spoolmanProxy, args) }.getOrNull(). D-07/Pitfall 5: URL-encode dotted query keys (e.g. filament.material) in THIS layer before they reach the proxy query string. measureSpool issues the PUT /v1/spool/{id}/measure with the gross grams (D-04). The client returns the raw proxy-v2 envelope JsonElement?; the parsers (11-02) walk it. Keep it lean — wrap the session JsonRpcClient, NOT forced through CommandDispatcher (D-07/Discretion).
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:compileDebugKotlin --no-daemon" 2>&1 | tr -d '\r' | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - SpoolmanClient interface method names/signatures match FakeSpoolmanClient (11-01) so tests bind to both.
    - Each read returns the raw JsonElement? proxy-v2 envelope via runCatching{rpc.request(spoolmanProxy, args)}.getOrNull(); a failure degrades to null, never throws.
    - Dotted query keys are URL-encoded in this layer; measureSpool issues the PUT measure path.
    - :app:compileDebugKotlin succeeds.
  </acceptance_criteria>
  <done>SpoolmanClient compiles and mirrors the FileBrowserClient idiom + the Fake's read surface.</done>
</task>

<task type="auto">
  <name>Task 3: ActiveSpoolFacade + MoonrakerService construction + SpineHandle/AppContainer derived flows</name>
  <files>app/src/main/java/works/mees/dinghy/spool/ActiveSpoolFacade.kt, app/src/main/java/works/mees/dinghy/service/MoonrakerService.kt, app/src/main/java/works/mees/dinghy/di/SpineHandle.kt, app/src/main/java/works/mees/dinghy/di/AppContainer.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/webcam/WebcamsHolder.kt lines 51-77 (the whole edge-driven once-per-handshake best-effort class — the EXACT template: injectable fetch, substitutable StateFlow<ConnectionState>, wasConnected rising-edge)
    - app/src/main/java/works/mees/dinghy/service/MoonrakerService.kt lines 162-187 (the WebcamsHolder CONSTRUCTION block — `val webcamsHolder = WebcamsHolder(serviceScope, session.connectionState) { runCatching { rpc.request(...) }.getOrNull() }` — AND the SpineHandle construction block where `webcams = webcamsHolder.webcams` is assigned; lines 118/130/138 for the in-scope `rpc`/`session`/`dispatcher` locals)
    - app/src/main/java/works/mees/dinghy/di/SpineHandle.kt lines 72-91 (the webcams/metadata/lastJob StateFlow fields to mirror for activeSpool)
    - app/src/main/java/works/mees/dinghy/di/AppContainer.kt lines 143-152 (the webcams/webcamCount flatMapLatest/map derived-flow + capability-map precedent)
    - app/src/main/java/works/mees/dinghy/net/JsonRpcClient.kt (the activeSpoolSet/spoolmanStatusChanged SharedFlows added in Task 1 this facade subscribes to)
    - app/src/main/java/works/mees/dinghy/spool/SpoolmanParsers.kt (parseSpoolmanStatus — the facade decodes the status reply into SpoolmanStatus)
    - .planning/phases/11-spool-management-spoolman-camera-qr/11-PATTERNS.md §"spool/ActiveSpoolFacade.kt" + §"di/SpineHandle.kt + di/AppContainer.kt"
  </read_first>
  <action>
    Create ActiveSpoolFacade.kt copying WebcamsHolder's structure: an `_activeSpool: MutableStateFlow<SpoolmanStatus?>` exposed as `activeSpool: StateFlow<SpoolmanStatus?>`, an injectable `fetchStatus: suspend () -> JsonElement?` (the server.spoolman.status read), a substitutable `connectionState: StateFlow<ConnectionState>`, and the two notify SharedFlows from JsonRpcClient. In init launch three collectors: (a) the rising-edge `!Connected → Connected` handshake that calls fetchStatus() then parseSpoolmanStatus(...) into _activeSpool (one-shot per handshake, runCatching best-effort — exactly WebcamsHolder's wasConnected pattern); (b) collect activeSpoolSet → reconcile the pushed spool_id into _activeSpool (D-10 — OVERWRITE with the pushed value, never re-assert stale local); (c) collect spoolmanStatusChanged → re-fetch status (a status-changed push may carry connected/pending changes — re-read for truth, D-10). Keep it plain Kotlin (no Android) so it is host-unit-testable like WebcamsHolder.

    In MoonrakerService.kt, in the per-session start block alongside the WebcamsHolder construction (~lines 162-187), construct the facade mirroring the WebcamsHolder block VERBATIM in shape: `val activeFacade = ActiveSpoolFacade(serviceScope, session.connectionState, rpc.activeSpoolSet, rpc.spoolmanStatusChanged, fetchStatus = { runCatching { rpc.request(CommandRegistry.spoolmanStatus, Unit) }.getOrNull() })` (use the in-scope serviceScope/session/rpc locals — the same ones WebcamsHolder/lastJobHolder use). Then in the SpineHandle(...) construction block add `activeSpool = activeFacade.activeSpool` right next to `webcams = webcamsHolder.webcams` so the facade's flow is published on the handle (the WebcamsHolder→SpineHandle.webcams precedent, exactly). Do NOT change the atomic `container.publishSpine(handle)` swap — only add the facade construction + the one handle field assignment.

    In SpineHandle.kt add `activeSpool: StateFlow<SpoolmanStatus?>` mirroring the webcams field (so MoonrakerService can assign activeFacade.activeSpool into it).
    In AppContainer.kt add `val activeSpool: Flow<SpoolmanStatus?> = spine.flatMapLatest { it?.activeSpool ?: flowOf(null) }` and `val spoolmanPresent: Flow<Boolean> = capabilities.map { it.hasComponent("spoolman") }` mirroring the webcams/webcamCount lines. spoolmanPresent is the D-02 drawer-greying input (the role webcamCount > 0 plays for Webcam).
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:compileDebugKotlin --no-daemon" 2>&1 | tr -d '\r' | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - ActiveSpoolFacade fetches status on the rising connect edge (one-shot per handshake), reconciles activeSpoolSet by overwriting with the pushed id (D-10), and re-fetches on spoolmanStatusChanged.
    - The facade is plain Kotlin (injectable fetch + substitutable StateFlow) — no Android imports, host-testable like WebcamsHolder.
    - MoonrakerService.kt constructs ActiveSpoolFacade (mirroring the WebcamsHolder construction block) and assigns activeFacade.activeSpool onto the SpineHandle(...) construction (next to webcams = webcamsHolder.webcams).
    - SpineHandle exposes activeSpool; AppContainer exposes activeSpool + spoolmanPresent derived flows mirroring webcams/webcamCount.
    - :app:compileDebugKotlin succeeds.
  </acceptance_criteria>
  <done>The active-spool spine compiles end-to-end; the service constructs the facade and publishes activeSpool on SpineHandle; facade reconciles external changes per D-10; derived flows published.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Moonraker notify frame → facade state | External mutators (Fluidd/runout macros) push active-spool changes; D-10 reconcile must not be clobbered by stale local state. |
| proxy query string → Spoolman REST | Dotted/encoded query keys cross the proxy; client URL-encodes (D-07). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-04-01 | Tampering (state) | External active-spool change races local state | mitigate | D-10: facade overwrites local with pushed notify id; spoolmanStatusChanged triggers a re-fetch for truth |
| T-11-04-02 | DoS (UI crash) | Malformed status/notify payload | mitigate | parseSpoolmanStatus runCatching walk + spoolNotifyParam runCatching extractor — never throws |
| T-11-04-03 | Tampering | Unencoded dotted query key mis-routes the proxy | mitigate | D-07: URL-encode dotted keys in the SpoolmanClient layer before the proxy query string |
| T-11-04-SC | Tampering | npm/pip/cargo installs | accept | No package installs (pure Kotlin; reuses pinned kotlinx.serialization + OkHttp) — camera/zxing installs gated in 11-05 |
</threat_model>

<verification>
- :app:compileDebugKotlin succeeds.
- ActiveSpoolFacade has NO Android import (grep `import android` in the file → none).
- JsonRpcClient exposes activeSpoolSet + spoolmanStatusChanged; spoolNotifyParam reads params[0].
- spoolmanProxy + spoolmanPostSpoolId carry ComponentPresent("spoolman").
- MoonrakerService.kt constructs ActiveSpoolFacade and assigns activeSpool onto SpineHandle (grep `ActiveSpoolFacade` in MoonrakerService.kt → present).
</verification>

<success_criteria>
The Spoolman spine is complete and capability-gated: active-spool get/set/clear/status flow via JSON-RPC, the two notifications route into the facade which reconciles external changes (D-10), the service constructs the facade per session and publishes its flow on SpineHandle, the lean proxy-v2 SpoolmanClient reads inventory, and AppContainer publishes activeSpool + spoolmanPresent for the UI waves.
</success_criteria>

<output>
Create `.planning/phases/11-spool-management-spoolman-camera-qr/11-04-SUMMARY.md` when done
</output>
