---
phase: 11-spool-management-spoolman-camera-qr
plan: 08
type: execute
wave: 5
depends_on: ["11-03", "11-06", "11-07"]
files_modified:
  - app/src/main/java/works/mees/dinghy/ui/files/FilesScreen.kt
  - app/src/main/java/works/mees/dinghy/ui/spool/SpoolScreen.kt
  - app/src/main/java/works/mees/dinghy/ui/spool/SpoolHolder.kt
autonomous: true
requirements: [SPOOL-07, SPOOL-09]
must_haves:
  truths:
    - "The print-start gate folds into the existing Files print-confirm flow: warnings surface as amber proceed-at-peril text the user taps past in one action; a clean pass shows the normal confirm unchanged (D-01, warn-only never blocks)"
    - "Opening the picker from a Files spool-warning pre-filters by the file's filament_type[] (material) and filament_colors[] (color hint) — gcode-aware prefilter (D-04)"
    - "Change-during-print sets a new active spool without interrupting the print; the card/picker reconcile to the new id"
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/ui/files/FilesScreen.kt"
      provides: "D-01 warn-only gate hook before printer.print.start"
    - path: "app/src/main/java/works/mees/dinghy/ui/spool/SpoolHolder.kt"
      provides: "gcode-aware prefilter seed + change-during-print mutator"
  key_links:
    - from: "FilesScreen print-confirm"
      to: "PrintStartGate.evaluatePrintStartGate"
      via: "warnings computed before requestStartSelected"
      pattern: "evaluatePrintStartGate|PrintStartGate"
    - from: "Files spool-warning Pick-spool action"
      to: "SpoolHolder gcode-aware prefilter"
      via: "filament_type[] + filament_colors[] seed"
      pattern: "filamentType|filamentColors"
---

<objective>
Wire the headless decision logic into the live flows: fold the warn-only `PrintStartGate` (11-03) into the existing Files print-confirm flow (D-01 — amber warnings tapped past in one action, never blocking), seed the picker's gcode-aware prefilter from the selected file's `filament_type[]`/`filament_colors[]` when opened from a Files warning (D-04), and add the change-during-print path (set a new active spool mid-print without interrupting it, reconciled via the facade). This is the integration wave — every collaborator already exists; this plan connects them.

Purpose: SPOOL-07 (the gate, the least-naggy choice — a mounted printer screen must not stop a print over inventory bookkeeping) and SPOOL-09's gcode-aware prefilter + change-during-print. The gate hooks at the EXACT point `FileBrowserClient.startPrint` dispatches `printer.print.start`. Files are minimal and disjoint from the wave-6 UAT.

Output: 3 modified files (Files confirm hook, SpoolScreen prefilter entry, SpoolHolder prefilter+change mutators).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-spool-management-spoolman-camera-qr/11-PATTERNS.md
@docs/view_specific_notes/spoolman.md
@app/src/main/java/works/mees/dinghy/ui/files/FilesScreen.kt
@app/src/main/java/works/mees/dinghy/ui/spool/PrintStartGate.kt
@app/src/main/java/works/mees/dinghy/ui/spool/SpoolHolder.kt
@app/src/main/java/works/mees/dinghy/ui/spool/SpoolScreen.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fold the warn-only print-start gate into the Files confirm flow (D-01)</name>
  <files>app/src/main/java/works/mees/dinghy/ui/files/FilesScreen.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/files/FilesScreen.kt lines 149-163 (the FileGuard.Start ConfirmGuard — where the gate hooks) + lines 156 (holder.requestStartSelected() — the dispatch point printer.print.start fires; the hook is BEFORE this) + lines 433-441 (the ConfirmGuard message slot carrying selectedFileDetails — where the amber warning text + actions render) + lines 425-431 (intentColor for the amber Intent.Warn)
    - app/src/main/java/works/mees/dinghy/ui/spool/PrintStartGate.kt (11-03 — evaluatePrintStartGate(activeSpool, status, fetchFailed, file, margin) → List<SpoolWarning>; warn-only, empty list = clean pass)
    - app/src/main/java/works/mees/dinghy/di/AppContainer.kt (11-04 — activeSpool + spoolmanPresent flows the screen reads for the gate inputs) + state/PrintMetadata.kt (the extended FilePreviewMetadata filament arrays the gate consumes, from 11-02)
    - .planning/phases/11-spool-management-spoolman-camera-qr/11-PATTERNS.md §"ui/files/FilesScreen.kt (gate hook)" + §"Button intent colors"
    - docs/view_specific_notes/spoolman.md lines 505-515 (the gate condition list) + D-01 (the four actions: Pick spool / Scan / Print anyway / Back)
  </read_first>
  <action>
    Hook the gate into the existing FileGuard.Start ConfirmGuard. BEFORE showing the plain "Print file" confirm (the point just before holder.requestStartSelected() dispatches printer.print.start), compute the warnings via evaluatePrintStartGate(activeSpool, status, fetchFailed, state.selectedPreview, margin) using the activeSpool + spoolmanPresent flows + the extended FilePreviewMetadata. If the warning list is NON-empty: surface them as amber Intent.Warn proceed-at-peril text in the SAME ConfirmGuard (the message slot already carries selectedFileDetails), and offer the D-01 actions: "Pick spool" (launches the Spool picker with the gcode-aware prefilter seed — Task 2), "Scan" (launches the scan surface), "Print anyway" (proceeds to requestStartSelected — DOES NOT BLOCK), "Back" (cancels). If the warning list is EMPTY (clean pass): show the existing "Print file" confirm UNCHANGED. When the spoolman component is absent, skip the gate entirely (no warnings). The gate NEVER hard-blocks — "Print anyway" is always available and is one tap (D-01). Use LocalTokens amber for the warning text (THEME-01, no raw color).
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:compileDebugKotlin --no-daemon" 2>&1 | tr -d '\r' | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - Gate warnings compute via evaluatePrintStartGate BEFORE requestStartSelected; a non-empty list surfaces amber text + Pick spool/Scan/Print anyway/Back in the existing ConfirmGuard.
    - "Print anyway" always proceeds in one tap — the gate never hard-blocks (D-01); a clean pass shows the unchanged confirm.
    - The gate is skipped when the spoolman component is absent; warning text uses LocalTokens amber (no raw color).
    - :app:compileDebugKotlin succeeds.
  </acceptance_criteria>
  <done>The warn-only gate is folded into the Files confirm; never blocks; clean pass unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: gcode-aware picker prefilter + change-during-print path</name>
  <files>app/src/main/java/works/mees/dinghy/ui/spool/SpoolScreen.kt, app/src/main/java/works/mees/dinghy/ui/spool/SpoolHolder.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/spool/SpoolHolder.kt (11-06 — the SpoolPickerState + applyMaterialFilter/applyColorSwatch mutators to seed; the selectSpool/set-active path to extend for change-during-print)
    - app/src/main/java/works/mees/dinghy/ui/spool/SpoolScreen.kt (11-06 — the Dest.Spool entry; add a prefilter-seed entry param)
    - app/src/main/java/works/mees/dinghy/state/PrintMetadata.kt (11-02 — FilePreviewMetadata.filamentType[] / filamentColors[] the prefilter reads) + app/src/main/java/works/mees/dinghy/spool/ActiveSpoolFacade.kt (11-04 — the set-active path reconciles via notify, so change-during-print just dispatches SetSpoolArgs)
    - .planning/phases/11-spool-management-spoolman-camera-qr/11-PATTERNS.md §"ui/spool/SpoolScreen.kt + SpoolPicker.kt ... Holder:" + the D-04 gcode-aware prefilter + D-05 material family / D-06 color-hint rules
    - docs/view_specific_notes/spoolman.md (D-04 gcode-aware prefilter: filament_type→material, filament_colors→color-similarity HINT not strict; change-during-print does NOT interrupt the print)
  </read_first>
  <action>
    Add a gcode-aware prefilter entry to SpoolScreen/SpoolHolder: when the picker is opened from a Files spool-warning, accept an optional prefilter seed carrying the file's filamentType[] (material) and filamentColors[] (color hint). On entry, seed the picker filters — apply the material family chip(s) from filamentType (comma-separated multi-family per D-05) and surface the file's colors as a color-similarity HINT (warning, NOT strict per D-04/D-06 — pre-select the nearest palette swatch or show a hint, do not hard-filter out non-matching colors). The seed is a one-time entry default the user can clear/refine. Add the change-during-print mutator to SpoolHolder: setActiveSpool(id) dispatches SetSpoolArgs(id) via the existing set-active path (the same one the Status card Change action uses) — this must NOT interrupt the running print (it only re-points active-spool tracking; Moonraker reconciles via notify_active_spool_set, and the facade D-10 reconcile flips the card/picker to the new id). No print-state gating on the change action (avoid the Files Delete-blocks-all defect). Confirm the change is allowed during a print (the whole point of change-during-print).
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:compileDebugKotlin --no-daemon" 2>&1 | tr -d '\r' | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - Opening the picker from a Files warning seeds the material chip(s) from filamentType[] (comma D-05) and the color as a HINT from filamentColors[] (not a hard filter, D-04/D-06).
    - The seed is a clearable one-time default; the user can refine.
    - setActiveSpool(id) dispatches mid-print without interrupting the print and without print-state gating; the card/picker reconcile to the new id via the facade (D-10).
    - :app:compileDebugKotlin succeeds.
  </acceptance_criteria>
  <done>gcode-aware prefilter seeds the picker; change-during-print sets active without interrupting the print.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| file metadata + Spoolman state → gate warnings | Defensive read; the gate is warn-only and never blocks the user's print (D-01). |
| change-during-print → running print | Re-pointing active-spool tracking must not interrupt or corrupt the running print. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-08-01 | DoS (user blocked) | A buggy gate hard-blocks a print over inventory bookkeeping | mitigate | D-01: "Print anyway" is always one tap; evaluatePrintStartGate only returns warnings (proven warn-only in 11-03); empty list = unchanged confirm |
| T-11-08-02 | Tampering (real device) | Change-during-print interrupts/corrupts the running print | mitigate | setActiveSpool only re-points tracking (SetSpoolArgs); no print-state gating bug copied from Files; UAT (11-09) exercises change-during-print on a live printer with restore |
| T-11-08-03 | DoS (UI crash) | Missing/malformed filament arrays in the prefilter seed | mitigate | FilePreviewMetadata arrays default empty (11-02); an empty seed simply applies no prefilter |
| T-11-08-SC | Tampering | npm/pip/cargo installs | accept | No package installs (wiring only) |
</threat_model>

<verification>
- :app:compileDebugKotlin succeeds.
- FilesScreen computes evaluatePrintStartGate before requestStartSelected; "Print anyway" reaches the dispatch (grep the gate hook precedes requestStartSelected).
- The picker prefilter reads filamentType[]/filamentColors[]; change-during-print dispatches SetSpoolArgs with no print-state guard.
</verification>

<success_criteria>
The decision logic is live: the warn-only gate folds into the Files confirm (amber, one-tap-past, never blocks, clean pass unchanged), the picker pre-filters by the file's material + color hint when opened from a warning, and change-during-print re-points the active spool without interrupting the running print.
</success_criteria>

<output>
Create `.planning/phases/11-spool-management-spoolman-camera-qr/11-08-SUMMARY.md` when done
</output>
</output>
