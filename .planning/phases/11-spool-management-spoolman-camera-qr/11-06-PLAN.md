---
phase: 11-spool-management-spoolman-camera-qr
plan: 06
type: execute
wave: 4
depends_on: ["11-02", "11-04"]
files_modified:
  - app/src/main/java/works/mees/dinghy/ui/route/TopRoute.kt
  - app/src/main/java/works/mees/dinghy/ui/shell/AppDrawer.kt
  - app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusScreen.kt
  - app/src/main/java/works/mees/dinghy/ui/spool/SpoolHolder.kt
  - app/src/main/java/works/mees/dinghy/ui/spool/ActiveSpoolCard.kt
  - app/src/main/java/works/mees/dinghy/ui/spool/SpoolScreen.kt
  - app/src/main/java/works/mees/dinghy/ui/spool/SpoolPicker.kt
autonomous: true
requirements: [SPOOL-02, SPOOL-03]
must_haves:
  truths:
    - "Dest.Spool exists; the drawer Spool tile is capability-greyed when the spoolman component is absent (mirrors the Webcam tile gate)"
    - "An active-spool card on Print Status shows material / color swatch / vendor·name / remaining / state across all D-03 state variants (unavailable/disconnected/no-active/loading/loaded/stale/pending/changed) with Scan/Change/Clear quick actions"
    - "The Spool screen hosts a Files-style picker: Focus=spool detail, Field=dense list + material/color/vendor/location chips + recent/low-remaining sorts, portrait collapse"
    - "Color is rendered from the D-08 normalized swatch (multi_color split), never a raw untrusted hex"
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/ui/spool/ActiveSpoolCard.kt"
      provides: "Compact active-spool Status card with state variants + quick actions"
    - path: "app/src/main/java/works/mees/dinghy/ui/spool/SpoolScreen.kt"
      provides: "Dest.Spool host: Focus/Field/Gutter picker + detail"
    - path: "app/src/main/java/works/mees/dinghy/ui/spool/SpoolHolder.kt"
      provides: "Picker state holder (list/filters/sorts/selection) over SpoolmanClient + activeSpool flow"
  key_links:
    - from: "AppDrawer Spool tile"
      to: "AppContainer.spoolmanPresent"
      via: "spoolEnabled greying param"
      pattern: "spoolEnabled"
    - from: "SpoolHolder"
      to: "SpoolmanClient + AppContainer.activeSpool"
      via: "list reads + active-spool reconcile"
      pattern: "SpoolmanClient|activeSpool"
---

<objective>
Build the non-camera Spool UI surfaces: add `Dest.Spool` + the capability-greyed drawer tile, the compact active-spool card on Print Status (all D-03 state variants + Scan/Change/Clear quick actions), and the `Dest.Spool` screen hosting the Files-style picker (Focus=detail / Field=dense list + material/color/vendor/location chips + recent/low-remaining sorts, portrait collapse) with its `SpoolHolder` state. Inventory reads come from `SpoolmanClient` (11-04); active-spool truth from `AppContainer.activeSpool` (11-04); color rendering from the D-08 normalized swatch (11-02).

Purpose: This is the "what's loaded + browse/select" half of the view (SPOOL-02 card, SPOOL-03 picker). It mirrors Files (dense list), the Webcam tile (capability greying), and LastJobCard (filament row + swatch). The QR scan surface, measured-weight page, and the print-start gate hook live in sibling plans (11-07, 11-08) — this plan owns disjoint files.

Output: 2 modified shell/route files + 1 modified Print-Status screen + 4 new spool UI files (holder, card, screen, picker).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-spool-management-spoolman-camera-qr/11-PATTERNS.md
@docs/view_specific_notes/spoolman.md
@docs/ui_design/LAYOUT.md
@docs/ui_design/THEMING.md
@app/src/main/java/works/mees/dinghy/ui/files/FilesScreen.kt
@app/src/main/java/works/mees/dinghy/ui/files/FileBrowserHolder.kt
@app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusScreen.kt
@app/src/main/java/works/mees/dinghy/ui/route/TopRoute.kt
@app/src/main/java/works/mees/dinghy/ui/shell/AppDrawer.kt
@app/src/main/java/works/mees/dinghy/ui/webcam/WebcamScreen.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dest.Spool + capability-greyed drawer tile + SpoolHolder</name>
  <files>app/src/main/java/works/mees/dinghy/ui/route/TopRoute.kt, app/src/main/java/works/mees/dinghy/ui/shell/AppDrawer.kt, app/src/main/java/works/mees/dinghy/ui/spool/SpoolHolder.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/route/TopRoute.kt line 31 (the Dest enum — add Spool between Webcam and Settings)
    - app/src/main/java/works/mees/dinghy/ui/shell/AppDrawer.kt lines 132-158 (DRAWER_TILES + DrawerTileSpec) + lines 69-77, 176 (the webcamEnabled runtime-greying decision to mirror exactly for spoolEnabled)
    - app/src/main/java/works/mees/dinghy/ui/files/FileBrowserHolder.kt lines 32-80 (the MutableStateFlow<State> + asStateFlow + scope.launch upstream reaction + runCatching degrade + suspend mutator idiom for SpoolHolder)
    - app/src/main/java/works/mees/dinghy/spool/SpoolmanClient.kt (11-04 — listSpools/listFilaments/listMaterials/listVendors/listLocations the holder calls) + app/src/main/java/works/mees/dinghy/spool/SpoolmanParsers.kt (the parsers that walk the returned envelopes)
    - .planning/phases/11-spool-management-spoolman-camera-qr/11-PATTERNS.md §"ui/route/TopRoute.kt" + §"ui/shell/AppDrawer.kt" + §"ui/spool/SpoolScreen.kt ... Holder:"
    - docs/view_specific_notes/spoolman.md (picker default list shape: unarchived, sort by material/...,id, limit=50; D-05 material family chips comma-separated; D-06 color-on-swatch-tap two-step)
  </read_first>
  <action>
    In TopRoute.kt add Spool to the Dest enum (between Webcam and Settings — the QR scan surface is NOT a Dest per D-02). In AppDrawer.kt add a DrawerTileSpec(label="Spool", symbol=<a glyph NOT already in DRAWER_TILES — e.g. "inventory_2" or "category">, dest=Dest.Spool); thread a spoolEnabled: Boolean = false param (fed AppContainer.spoolmanPresent at the call site) and fold it into the `live` decision EXACTLY as webcamEnabled is (tile.dest != Dest.Spool || spoolEnabled) so the tile greys when the spoolman component is absent (D-02). Do NOT set beta=true (no amber dev flag unless desired). Create SpoolHolder.kt mirroring FileBrowserHolder: a MutableStateFlow<SpoolPickerState> exposed via asStateFlow() carrying the list, applied filters (material family chips per D-05 comma-separated, color via D-06 swatch-tap two-step, vendor, location), the active sort (recent / low-remaining, default unarchived sort=filament.material:asc,...,id:asc limit=50 per Discretion), and the selected spool; scope.launch reactions to upstream activeSpool flow; runCatching reads via SpoolmanClient that degrade to empty/null; suspend mutators selectSpool/applyMaterialFilter/applyColorSwatch/applySort. Derive picker prefs each session (Discretion — do not persist).
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:compileDebugKotlin --no-daemon" 2>&1 | tr -d '\r' | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - Dest.Spool exists; the Spool drawer tile uses a glyph not already in DRAWER_TILES (icon-no-repeat law).
    - The tile greys via spoolEnabled folded into `live` exactly as webcamEnabled (D-02 capability gate).
    - SpoolHolder mirrors FileBrowserHolder: StateFlow state, runCatching SpoolmanClient reads, material(comma)/color(swatch-tap)/vendor/location filters + recent/low sorts, session-derived prefs.
    - :app:compileDebugKotlin succeeds.
  </acceptance_criteria>
  <done>Dest.Spool + greyed tile + SpoolHolder compile and follow the Webcam-gate + Files-holder precedents.</done>
</task>

<task type="auto">
  <name>Task 2: Active-spool card on Print Status (D-03 state variants + quick actions)</name>
  <files>app/src/main/java/works/mees/dinghy/ui/spool/ActiveSpoolCard.kt, app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusScreen.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusScreen.kt lines 449-531 (LastJobCard — the EXACT card precedent: filament type·name row + color swatch + icon-led stat rows via LocalTokens) + lines 538-548 (LastJobStatRow / scroll-row) + the existing parseHexColor helper + where LastJobCard is placed in the screen body (to slot the active-spool card alongside)
    - .planning/phases/11-spool-management-spoolman-camera-qr/11-PATTERNS.md §"ui/spool/ActiveSpoolCard.kt" + §"Button intent colors"
    - docs/view_specific_notes/spoolman.md (D-03 card fields: material / color swatch / vendor·name / remaining / state — confidence indicator NOT a DB row) + the full state-variant list (unavailable/disconnected/no-active/loading/loaded/stale/pending/changed-externally)
    - app/src/main/java/works/mees/dinghy/spool/SpoolmanModels.kt (11-02 — SpoolmanStatus + SpoolmanSpool + the normalizedColorHex/colorSwatches computed vals the swatch renders from; D-08)
    - app/src/main/java/works/mees/dinghy/designsystem/control/OutlinedControl.kt + the Intent type (the Scan/Change/Clear quick-action controls)
  </read_first>
  <action>
    Create ActiveSpoolCard.kt copying LastJobCard's structure: a compact card rendering material / color swatch / vendor·name / remaining / state from the active SpoolmanStatus + resolved SpoolmanSpool. Drive the visible content with a `when` over the state inputs covering EVERY D-03 variant: unavailable (spoolman component absent — capability flow), disconnected (no connection), no-active (status.spool_id null), loading (detail fetch in flight), loaded (the normal filled card), stale (non-empty pending_reports — D-11 "usage queued / remaining may be stale"), pending, and changed-externally (D-10 reconciled to a new id). Render the swatch from SpoolmanFilament.colorSwatches (D-08 normalized; multi-color = split swatch) — NEVER a raw untrusted hex; archived spools show the D-09 archived warning badge. Quick actions Scan / Change / Clear are OutlinedControls with Intent colors via LocalTokens (Scan/Change = accent physical, Clear = red Danger per D-13 → post_spool_id {}) — NO raw color literal (THEME-01). Wire the card into PrintStatusScreen near LastJobCard, fed by the activeSpool + spoolmanPresent flows; tapping Scan/Change launches the Spool screen / scan sub-surface (route via the existing nav callbacks — the scan surface itself lands in 11-07, so expose an onScan/onChange/onClear callback hook the screen wires).
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:compileDebugKotlin --no-daemon" 2>&1 | tr -d '\r' | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - ActiveSpoolCard renders material/color swatch/vendor·name/remaining/state and has a `when` branch for every D-03 variant (unavailable/disconnected/no-active/loading/loaded/stale/pending/changed-externally).
    - The swatch comes from the D-08 normalized colorSwatches (multi-color split), never a raw hex; archived shows the D-09 badge.
    - Scan/Change/Clear are OutlinedControls with LocalTokens Intent colors (Clear = red, D-13); no raw color literal.
    - The card is wired into PrintStatusScreen; :app:compileDebugKotlin succeeds.
  </acceptance_criteria>
  <done>ActiveSpoolCard compiles with all state variants + quick actions; wired into Print Status.</done>
</task>

<task type="auto">
  <name>Task 3: Dest.Spool screen + Files-style picker (Focus/Field/Gutter, portrait collapse)</name>
  <files>app/src/main/java/works/mees/dinghy/ui/spool/SpoolScreen.kt, app/src/main/java/works/mees/dinghy/ui/spool/SpoolPicker.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/files/FilesScreen.kt lines 91-146 (the ScreenScaffold Focus/Field/Gutter + showFocus portrait-collapse idiom) + lines 202-211 (the ⚠ Views-in-Compose pinned-EXACT-height scroll lesson — apply VERBATIM if the picker reuses a RecyclerView; moot if pure Compose LazyColumn) + lines 425-431 (intentColor)
    - app/src/main/java/works/mees/dinghy/ui/webcam/WebcamScreen.kt lines 169-221 (CamPicker — the pure-Compose dense-list-with-expanded-selected-row precedent if going Compose) + lines 80-84 (orientation collapse via BoxWithConstraints)
    - app/src/main/java/works/mees/dinghy/ui/spool/SpoolHolder.kt (Task 1 — the SpoolPickerState the screen renders + the mutators the chips/sorts/rows call)
    - .planning/phases/11-spool-management-spoolman-camera-qr/11-PATTERNS.md §"ui/spool/SpoolScreen.kt + SpoolPicker.kt" + §"Button intent colors" + the MEMORY note "Files Delete-blocks-all-during-print is a known deferred defect — do NOT copy a print-state-gating bug into the picker"
    - docs/ui_design/LAYOUT.md (Focus/Field/Gutter grammar, portrait stacks / landscape 50/50, ratio-only sizing — no hardcoded px)
  </read_first>
  <action>
    Create SpoolScreen.kt as the Dest.Spool host using a BoxWithConstraints + ScreenScaffold mirroring FilesScreen: showFocus = maxWidth > maxHeight || selectedSpool != null; Focus = the selected-spool detail (material/color/vendor/remaining/used linked, location, archived badge — D-09); Field = SpoolPicker (the dense scrollable list + the filter chip row + the recent/low-remaining sort control); Gutter = the intent controls (red Back/Cancel + green Set-active/Load + accent Scan-launch). Portrait collapses to the stacked Field-first layout per the UI LAW (ratio-only sizing, no hardcoded px). Create SpoolPicker.kt as the Field content: render the SpoolHolder list rows (each row shows the spool's ACTUAL swatch even on a color-filter result — D-06 endpoint can match unexpectedly), the material family chips (comma-separated multi-family per D-05), the color palette swatches (trigger the D-06 two-step ONLY on swatch tap), vendor + location chips ("At this printer" + "No location" per D-04), and the recent/low-remaining sort toggle. If the list reuses a RecyclerView via AndroidView, apply the pinned-EXACT-height lesson VERBATIM; if pure Compose LazyColumn, the lesson is moot but the Focus/Field/Gutter collapse still applies. Do NOT gate row actions on print-state (avoid the Files Delete-blocks-all-during-print defect). All colors via LocalTokens (THEME-01).
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:compileDebugKotlin --no-daemon" 2>&1 | tr -d '\r' | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - SpoolScreen hosts Dest.Spool with ScreenScaffold Focus(detail)/Field(picker)/Gutter, portrait collapse per the UI LAW (ratio-only sizing).
    - SpoolPicker renders the dense list + material(comma D-05)/color(swatch-tap D-06)/vendor/location("At this printer"/"No location" D-04) chips + recent/low sorts; every row shows the spool's actual swatch.
    - If a RecyclerView is hosted, the pinned-EXACT-height scroll lesson is applied; no print-state row gating.
    - All colors via LocalTokens; :app:compileDebugKotlin succeeds.
  </acceptance_criteria>
  <done>SpoolScreen + SpoolPicker compile; Files-style Focus/Field/Gutter picker with D-04/05/06 filters; scroll-lesson honored.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Spoolman color_hex → swatch render | Untrusted/loose hex; rendered only via the D-08 normalized computed val, never raw. |
| spoolman component absence → drawer/card | Missing capability must grey the tile + show the card "unavailable" state, not crash. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-06-01 | DoS (UI crash) | A bad color_hex breaks a card/row render | mitigate | D-08: render only normalizedColorHex/colorSwatches; a non-normalizable hex → neutral marker, never a parse crash |
| T-11-06-02 | DoS (UI crash) | spoolman component absent at render | mitigate | D-02: capability flow greys the tile + drives the card "unavailable" variant |
| T-11-06-03 | Tampering (state) | Stale local active-spool overwrites a pushed change | mitigate | D-10: card reads AppContainer.activeSpool (facade-reconciled), shows the changed-externally variant |
| T-11-06-SC | Tampering | npm/pip/cargo installs | accept | No package installs (UI only; camera/zxing deps land in 11-05) |
</threat_model>

<verification>
- :app:compileDebugKotlin succeeds.
- Dest.Spool present; the drawer Spool tile greys via spoolEnabled.
- ActiveSpoolCard has a `when` covering all D-03 variants; renders the swatch from colorSwatches (grep raw `Color(0x` in spool UI → none beyond token usage).
- SpoolPicker chips cover material/color/vendor/location + recent/low sorts.
</verification>

<success_criteria>
The browse/glance half of the view is built: Dest.Spool + a capability-greyed drawer tile, a Print-Status active-spool card spanning every D-03 state variant with Scan/Change/Clear, and a Files-style Focus/Field/Gutter picker with D-04/05/06 filters + sorts — all color-safe (D-08) and theme-tokened, scroll-lesson honored.
</success_criteria>

<output>
Create `.planning/phases/11-spool-management-spoolman-camera-qr/11-06-SUMMARY.md` when done
</output>
</output>
