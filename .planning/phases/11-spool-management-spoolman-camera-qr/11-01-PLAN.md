---
phase: 11-spool-management-spoolman-camera-qr
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - app/src/test/resources/golden/spoolman-live-ender5-proxy-pla.json
  - app/src/test/resources/golden/spoolman-live-ender5-proxy-spool3.json
  - app/src/test/resources/golden/spoolman-live-direct-spool3-before.json
  - app/src/test/resources/golden/spoolman-live-ender5-notify.json
  - app/src/test/resources/golden/spoolman-live-ender5-status-before-set.json
  - app/src/test/resources/golden/spoolman-live-ender5-proxy-materials.json
  - app/src/test/resources/golden/spoolman-live-ender5-proxy-vendors.json
  - app/src/test/resources/golden/spoolman-live-ender5-proxy-locations.json
  - app/src/test/resources/golden/spoolman-live-ender5-proxy-abs-asa.json
  - app/src/test/resources/golden/spoolman-live-ender5-proxy-color-red-filaments.json
  - app/src/test/java/works/mees/dinghy/spool/FakeSpoolmanClient.kt
  - app/src/test/java/works/mees/dinghy/spool/FakeMoonrakerSpoolmanSession.kt
  - app/src/test/java/works/mees/dinghy/spool/SpoolmanProxyParserTest.kt
  - app/src/test/java/works/mees/dinghy/spool/SpoolmanModelParserTest.kt
  - app/src/test/java/works/mees/dinghy/spool/SpoolmanNotifyRouterTest.kt
  - app/src/test/java/works/mees/dinghy/spool/QrPayloadParserTest.kt
  - app/src/test/java/works/mees/dinghy/spool/PrintStartGateTest.kt
  - app/src/test/java/works/mees/dinghy/spool/ScanStateMachineTest.kt
  - app/src/test/java/works/mees/dinghy/state/FilePreviewMetadataTest.kt
  - app/src/androidTest/java/works/mees/dinghy/spool/ScanSurfaceLifecycleTest.kt
  - docs/commands/moonraker-api.md
autonomous: true
requirements: [SPOOL-02, SPOOL-03, SPOOL-04, SPOOL-05, SPOOL-06, SPOOL-07, SPOOL-08]
must_haves:
  truths:
    - "Every Wave-0 RED test compiles and runs (fails with a clear message, never a compile error)"
    - "The live Spoolman fixtures are available on the test classpath as goldens"
    - "The Moonraker catalog documents notify_active_spool_set and notify_spoolman_status_changed"
    - "The instrumented ScanSurfaceLifecycleTest (SPOOL-03/06 Wave-0 ❌ W0) is scaffolded compile-clean as a RED stub"
  artifacts:
    - path: "app/src/test/java/works/mees/dinghy/spool/FakeSpoolmanClient.kt"
      provides: "Hardened proxy-v2-envelope fake matching the live fixtures"
    - path: "app/src/test/java/works/mees/dinghy/spool/SpoolmanProxyParserTest.kt"
      provides: "RED scaffold locking X-Total-Count==7 against the live pla fixture"
    - path: "app/src/androidTest/java/works/mees/dinghy/spool/ScanSurfaceLifecycleTest.kt"
      provides: "Instrumented Wave-0 RED stub for camera-release-on-dispose + no-camera→picker degrade (SPOOL-03/06)"
    - path: "docs/commands/moonraker-api.md"
      provides: "Catalog entries for the two Spoolman notifications"
  key_links:
    - from: "Wave-0 tests"
      to: "app/src/test/resources/golden/spoolman-live-*.json"
      via: "GoldenFixtures.resolve / raw"
      pattern: "GoldenFixtures\\.(resolve|raw)\\(\"spoolman-live"
---

<objective>
Lay the Wave-0 foundation for the entire phase: copy the verbatim live Spoolman fixtures into the test golden corpus, write hardened fakes (`FakeSpoolmanClient` / `FakeMoonrakerSpoolmanSession`) that replicate the EXACT proxy-v2 envelope and 1-element notify-array contracts, scaffold every VALIDATION.md RED test (JVM unit + the instrumented `ScanSurfaceLifecycleTest`) as a compile-clean failing stub, and close the documentation catalog gap (the two Spoolman notifications).

Purpose: This is the project's 7th-mock-vs-reality-strike prevention. The fakes are hardened to the real server contract from day one so green unit suites cannot hide a wire-shape bug. Wave-0 RED stubs MUST compile (a bad scaffold bricks the entire test source set — Gradle compiles the whole test sourceset before applying `--tests`); the same compile-safety rule applies to the androidTest sourceset for the instrumented stub.

Output: 10 golden fixtures, 2 fakes, 7 JVM RED test scaffolds, 1 extended-metadata test scaffold, 1 instrumented RED scaffold, and the catalog doc fix.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-spool-management-spoolman-camera-qr/11-RESEARCH.md
@.planning/phases/11-spool-management-spoolman-camera-qr/11-VALIDATION.md
@.planning/phases/11-spool-management-spoolman-camera-qr/11-PATTERNS.md
@docs/view_specific_notes/spoolman.md
@docs/view_specific_notes/spoolman_live_validation.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Copy live fixtures to goldens + close the catalog gap</name>
  <files>app/src/test/resources/golden/spoolman-live-*.json, docs/commands/moonraker-api.md</files>
  <read_first>
    - docs/commands/spoolman-live-ender5-proxy-pla.json + spoolman-live-ender5-proxy-spool3.json + spoolman-live-direct-spool3-before.json + spoolman-live-ender5-notify.json + spoolman-live-ender5-status-before-set.json + spoolman-live-ender5-proxy-{materials,vendors,locations,abs-asa,color-red-filaments}.json (the source-of-truth captures to copy verbatim)
    - app/src/test/java/works/mees/dinghy/net/GoldenFixtures.kt (the resolve/raw loader reads /golden/<name> from the test classpath — fixtures must land in app/src/test/resources/golden/)
    - docs/commands/moonraker-api.md (the catalog; find the existing server.spoolman.* method block + the existing notify_* documentation style to mirror)
    - docs/view_specific_notes/spoolman.md lines 140-175 (the live notify shape {"method":"notify_active_spool_set","params":[{"spool_id":1}]})
  </read_first>
  <action>
    Copy the 10 named docs/commands/spoolman-live-*.json fixtures VERBATIM (byte-identical, no edits) into app/src/test/resources/golden/ keeping the same filenames. These become the Wave-0 goldens loaded via GoldenFixtures.raw("spoolman-live-ender5-proxy-pla.json"). Then edit docs/commands/moonraker-api.md to add catalog entries for notify_active_spool_set and notify_spoolman_status_changed (Open-Q2 / D-10): document each as a server-push notification whose `params` is a 1-element array carrying `{spool_id: Int}` (per the live spoolman-live-ender5-notify.json shape), mirroring the existing notify_* doc style. Keep CommandCatalogDriftTest green if it asserts over this doc — match the existing row/heading format exactly.
  </action>
  <verify>
    <automated>ls app/src/test/resources/golden/spoolman-live-ender5-proxy-pla.json app/src/test/resources/golden/spoolman-live-ender5-notify.json && grep -c "notify_active_spool_set" docs/commands/moonraker-api.md</automated>
  </verify>
  <acceptance_criteria>
    - All 10 named fixtures exist under app/src/test/resources/golden/ and are byte-identical to their docs/commands/ source (diff is empty).
    - docs/commands/moonraker-api.md contains entries for both notify_active_spool_set and notify_spoolman_status_changed, each noting the 1-element params array carrying spool_id.
    - If CommandCatalogDriftTest exists and reads this doc, it stays GREEN.
  </acceptance_criteria>
  <done>Goldens copied; catalog gap closed; no existing doc-drift test broken.</done>
</task>

<task type="auto">
  <name>Task 2: Hardened fakes replicating the proxy-v2 + notify contracts</name>
  <files>app/src/test/java/works/mees/dinghy/spool/FakeSpoolmanClient.kt, app/src/test/java/works/mees/dinghy/spool/FakeMoonrakerSpoolmanSession.kt</files>
  <read_first>
    - app/src/test/java/works/mees/dinghy/net/SessionTestHarness.kt (the harness idiom — injectable JSON fields seeded from GoldenFixtures.raw; the contract-tightening discipline that caught the Phase-2 identify-url bug)
    - app/src/test/java/works/mees/dinghy/net/GoldenFixtures.kt (resolve/raw)
    - app/src/main/java/works/mees/dinghy/net/JsonRpc.kt lines 83-87 (the shared MoonrakerJson instance — fakes parse with the SAME Json, never a fresh Json {})
    - docs/view_specific_notes/spoolman_live_validation.md (proxy v2 envelope {response,error,response_headers}; X-Total-Count in response_headers; 1-element notify array; E5 active=5)
    - .planning/phases/11-spool-management-spoolman-camera-qr/11-PATTERNS.md §"Tolerant JSON" + §SpoolmanParsers (envelope walk contract)
  </read_first>
  <action>
    Create FakeSpoolmanClient: a test double that returns the proxy-v2 envelope EXACTLY as the live server does — `{response: <rows-or-object>, error: null, response_headers: {"X-Total-Count": "<n>"}}` — seeded from the golden fixtures (e.g. proxy-pla returns X-Total-Count "7"). It must expose the same suspend read surface the real SpoolmanClient will (getSpool(id), listSpools(query), listFilaments(query), listMaterials(), listVendors(), listLocations(), measureSpool(id, grossGrams)) returning JsonElement? envelopes. Harden it like SessionTestHarness: a query that does NOT match a seeded fixture returns a well-formed empty-envelope (X-Total-Count "0"), never a lenient catch-all — so a parser that ignores the query string fails the test. Create FakeMoonrakerSpoolmanSession: replicates the JSON-RPC active-spool surface — status (returns the status-before-set shape: spoolman_connected true, spool_id 5, pending_reports []), get_spool_id, post_spool_id (records the set id; `{}` clears to null), and a way to inject notify_active_spool_set / notify_spoolman_status_changed frames with `params` as a 1-element array. Parse all JSON with the shared MoonrakerJson. Do NOT reference any production Spoolman symbol that does not yet exist — these fakes define their own test-facing interfaces (or use JsonElement) so they compile standalone in Wave 0; Wave 1/2 will conform the real types to them.
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:compileDebugUnitTestKotlin --no-daemon" 2>&1 | tr -d '\r' | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - FakeSpoolmanClient.proxy reads return the exact `{response,error,response_headers}` envelope with X-Total-Count drawn from the seeded fixture; an unmatched query returns the empty-envelope, not a default row set.
    - FakeMoonrakerSpoolmanSession.status returns spool_id 5 / pending_reports [] (from status-before-set golden); post_spool_id with `{}` clears to null; notify injection produces a 1-element params array.
    - Both files compile (`compileDebugUnitTestKotlin` succeeds) with NO reference to unbuilt production symbols.
  </acceptance_criteria>
  <done>Both fakes compile and replicate the live envelope + notify-array contract.</done>
</task>

<task type="auto">
  <name>Task 3: Compile-clean RED scaffolds for every VALIDATION test (JVM unit + instrumented ScanSurfaceLifecycleTest)</name>
  <files>app/src/test/java/works/mees/dinghy/spool/SpoolmanProxyParserTest.kt, app/src/test/java/works/mees/dinghy/spool/SpoolmanModelParserTest.kt, app/src/test/java/works/mees/dinghy/spool/SpoolmanNotifyRouterTest.kt, app/src/test/java/works/mees/dinghy/spool/QrPayloadParserTest.kt, app/src/test/java/works/mees/dinghy/spool/PrintStartGateTest.kt, app/src/test/java/works/mees/dinghy/spool/ScanStateMachineTest.kt, app/src/test/java/works/mees/dinghy/state/FilePreviewMetadataTest.kt, app/src/androidTest/java/works/mees/dinghy/spool/ScanSurfaceLifecycleTest.kt</files>
  <read_first>
    - .planning/phases/11-spool-management-spoolman-camera-qr/11-VALIDATION.md §Wave 0 Requirements (the JVM-unit + instrumented tests + their golden sources) + line 52 (the instrumented `ScanSurfaceLifecycleTest` row — ❌ W0, SPOOL-03/06, `connectedDebugAndroidTest *ScanSurfaceLifecycleTest`)
    - .agents/memory or MEMORY note "Wave-0 RED scaffolds must compile" (fail() bodies, no refs to unbuilt symbols — Gradle compiles the whole test sourceset before --tests filter; this rule applies to BOTH the unit and androidTest sourcesets)
    - app/src/test/java/works/mees/dinghy/state/KlippyLifecycleTest.kt (an existing GoldenFixtures-driven test for the assertion/import idiom)
    - an existing instrumented test under app/src/androidTest/ (the @RunWith(AndroidJUnit4::class) + @Test idiom + the androidTest package layout) to mirror for ScanSurfaceLifecycleTest
    - .planning/phases/11-spool-management-spoolman-camera-qr/11-RESEARCH.md lines 343-349 (QR parser 4 accept/reject classes) + lines 419-426 (the per-req behavior list)
  </read_first>
  <action>
    Write the JVM-unit RED test scaffolds, one per VALIDATION.md Wave-0 row, PLUS the instrumented ScanSurfaceLifecycleTest scaffold. Each test method body asserts the intended behavior by loading the relevant golden (where applicable) and calling org.junit.Assert.fail("RED: <behavior> not yet implemented") — NO references to production Spoolman symbols that do not exist yet (they are built in Waves 1-2). Each must COMPILE on its own.

    JVM-unit scaffolds (app/src/test/...): SpoolmanProxyParserTest (loads spoolman-live-ender5-proxy-pla golden, documents the assertion X-Total-Count=="7" + error==null in a comment, fail() body); SpoolmanModelParserTest (proxy-spool3 + direct-spool3-before goldens — null-safety, color normalize, multi-color split documented, fail()); SpoolmanNotifyRouterTest (ender5-notify golden — 1-element params array, ignore notify_proc_stat_update, fail()); QrPayloadParserTest (the 4 classes: accept web+spoolman:s-<id> + uppercase WEB+SPOOLMAN:S- + http(s) /spool/show/<id>; reject f-/UPC/non-numeric/hostile-host — fail()); PrintStartGateTest (D-01 decision table, every condition warn-only no-block — fail()); ScanStateMachineTest (granted/denied/no-camera/busy/unreadable/no-QR/unsupported — fail()); and EXTEND/create FilePreviewMetadataTest in state/ asserting filament_type[]/name[]/colors[]/weights[] lift (fail() body now; if a FilePreviewMetadataTest already exists, add the new RED cases without breaking existing green ones). Use the FakeSpoolmanClient/FakeMoonrakerSpoolmanSession from Task 2 where a seam needs a collaborator so the scaffold types resolve.

    Instrumented scaffold (app/src/androidTest/java/works/mees/dinghy/spool/ScanSurfaceLifecycleTest.kt): a `@RunWith(AndroidJUnit4::class)` class with two `@Test` methods scaffolded as compile-clean RED stubs (each body `org.junit.Assert.fail("RED: ... not yet implemented")`) documenting, in a comment, the two target assertions: (1) the scan surface RELEASES the camera on dispose (no held camera after leaving the surface), (2) a no-camera device DEGRADES to the manual picker. This is the SPOOL-03/06 Wave-0 instrumented row (VALIDATION line 52). CRITICAL Wave-0 compile-safety: it must NOT reference unbuilt `ScanSurface.kt` symbols (ScanSurface lands in 11-07); it may reference Wave-0 `ScanState` constants from 11-03 ONLY if needed for compile-cleanliness, but the safest form is a pure fail() stub with zero production references. It is instrumented, so the per-task quick-run for the OTHER scaffolds remains the JVM unit compile (`:app:compileDebugUnitTestKotlin`); this scaffold's guard is the androidTest sourceset COMPILE (`:app:compileDebugAndroidTestKotlin`) — it is not RUN in Wave 0 (it runs via `connectedDebugAndroidTest` once 11-07's ScanSurface exists).
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:compileDebugUnitTestKotlin :app:compileDebugAndroidTestKotlin --no-daemon" 2>&1 | tr -d '\r' | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - All 7 JVM-unit scaffold files exist and `:app:compileDebugUnitTestKotlin` succeeds (the whole test sourceset compiles — no bricking).
    - ScanSurfaceLifecycleTest exists under app/src/androidTest/java/works/mees/dinghy/spool/ and `:app:compileDebugAndroidTestKotlin` succeeds; it references NO unbuilt ScanSurface symbol.
    - Running `--tests *SpoolmanProxyParserTest` (etc.) FAILS with the RED fail() message, not a compile error.
    - No scaffold references a production symbol that does not yet exist; each documents its target assertion + golden source in a comment.
    - Any pre-existing FilePreviewMetadataTest green cases still pass.
  </acceptance_criteria>
  <done>7 JVM-unit RED scaffolds + the instrumented ScanSurfaceLifecycleTest stub compile, run/stay red with clear messages, and reference no unbuilt production symbols.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live fixture → golden corpus | Verbatim copy; the fixture is the trusted contract source. No transformation = no chance to weaken it. |
| test fake → parser-under-test | The fake is the adversary that must be STRICTER than convenient (mock-vs-reality discipline). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01-01 | Tampering | Lenient fake hides wire-shape bug | mitigate | FakeSpoolmanClient returns the EXACT envelope; unmatched query → empty-envelope not catch-all (per CONTEXT mock-vs-reality, 7 prior strikes) |
| T-11-01-02 | Tampering | Bad RED scaffold bricks whole test sourceset (unit OR androidTest) | mitigate | fail() bodies, zero refs to unbuilt symbols, dual compile gate (`compileDebugUnitTestKotlin` + `compileDebugAndroidTestKotlin`) is the verify step |
| T-11-01-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan (test-only Kotlin + JSON copies) — package legitimacy gate handled in 11-05 |
</threat_model>

<verification>
- `:app:compileDebugUnitTestKotlin` succeeds (the whole unit test sourceset compiles).
- `:app:compileDebugAndroidTestKotlin` succeeds (the androidTest sourceset compiles — the instrumented ScanSurfaceLifecycleTest stub does not brick it).
- Each Wave-0 JVM test runs RED with its fail() message (not a compile error).
- 10 goldens present; both fakes return live-accurate envelopes; catalog gap closed.
</verification>

<success_criteria>
All 10 fixtures live in golden/, both hardened fakes compile and match the live envelope + 1-element notify contract, all 7 JVM-unit RED scaffolds compile and run red, the metadata test scaffold exists, the instrumented ScanSurfaceLifecycleTest stub compiles clean (no unbuilt ScanSurface ref), and the two Spoolman notifications are documented in moonraker-api.md.
</success_criteria>

<output>
Create `.planning/phases/11-spool-management-spoolman-camera-qr/11-01-SUMMARY.md` when done
</output>
