---
phase: 11-spool-management-spoolman-camera-qr
plan: 02
type: execute
wave: 1
depends_on: ["11-01"]
files_modified:
  - app/src/main/java/works/mees/dinghy/spool/SpoolmanModels.kt
  - app/src/main/java/works/mees/dinghy/spool/SpoolmanParsers.kt
  - app/src/main/java/works/mees/dinghy/state/PrintMetadata.kt
autonomous: true
requirements: [SPOOL-02, SPOOL-07, SPOOL-08]
must_haves:
  truths:
    - "The proxy v2 envelope parses to rows + X-Total-Count, treating error:null as success"
    - "Every Spoolman field is null-safe; one bad row drops without throwing"
    - "Color is display-normalized (with/without #, 6/8 hex else neutral); multi_color_hexes splits"
    - "FilePreviewMetadata lifts filament_type[]/name[]/colors[]/weights[]"
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/spool/SpoolmanModels.kt"
      provides: "Null-safe @Serializable Spoolman data model + color-normalize computed vals"
      contains: "data class SpoolmanSpool"
    - path: "app/src/main/java/works/mees/dinghy/spool/SpoolmanParsers.kt"
      provides: "Proxy v2 envelope + spool/filament/vendor/status parsers (fixture-golden)"
    - path: "app/src/main/java/works/mees/dinghy/state/PrintMetadata.kt"
      provides: "Extended FilePreviewMetadata with filament arrays"
  key_links:
    - from: "SpoolmanParsers"
      to: "net/JsonRpc.kt MoonrakerJson"
      via: "shared Json instance"
      pattern: "MoonrakerJson\\.(decodeFromJsonElement|parse)"
---

<objective>
Build the pure, headless Spoolman data layer: null-safe models, the proxy-v2 envelope parser, the spool/filament/vendor/status parsers, and extend `FilePreviewMetadata` to lift the filament arrays the gate and prefilter need. All test-GREEN against the Wave-0 goldens.

Purpose: Spoolman owns inventory; Dinghy's job here is null-safety + the v2 envelope walk (D-08/D-07). No I/O, no Android — fully host-testable. This turns the Wave-0 RED parser/model scaffolds GREEN.

Output: 3 files (2 new headless model files + 1 extended existing parser).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-spool-management-spoolman-camera-qr/11-PATTERNS.md
@.planning/phases/11-spool-management-spoolman-camera-qr/11-RESEARCH.md
@docs/view_specific_notes/spoolman.md
@app/src/main/java/works/mees/dinghy/state/WebcamModels.kt
@app/src/main/java/works/mees/dinghy/state/PrintMetadata.kt
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Null-safe Spoolman models + color normalize</name>
  <files>app/src/main/java/works/mees/dinghy/spool/SpoolmanModels.kt</files>
  <behavior>
    - SpoolmanSpool decodes from a row with omitted optional fields without throwing (filament null, remaining_weight null, location null all tolerated).
    - colorHex normalization: "ff0000" → "#FF0000"; "#ff0000" → "#FF0000"; "ff0000ff" (8 hex) accepted; "xyz"/"" → neutral-unknown marker (null).
    - multiColorHexes "ff0000,00ff00" splits to ["#FF0000","#00FF00"] via a computed val.
    - SpoolmanStatus carries spoolman_connected, spool_id (nullable), pending_reports (empty-default list); a non-empty pending_reports is observable.
    - extra defaults to empty Map<String,String>; an invalid-JSON value never breaks construction.
  </behavior>
  <read_first>
    - app/src/main/java/works/mees/dinghy/state/WebcamModels.kt lines 25-69 (the @Serializable + @SerialName null-or-defaulted idiom; safeRotation/hasSnapshot computed-val precedent to mirror for colorHex normalize + multiColorHexes split)
    - docs/view_specific_notes/spoolman.md lines 592-640 (Suggested Data Model — the exact target field shape: SpoolmanStatus/PendingSpoolmanReport/SpoolmanSpool/SpoolmanFilament/SpoolmanVendor)
    - docs/view_specific_notes/spoolman.md lines 221-225 (D-08 color defensive rule: accept with/without #, 6/8 hex post-normalize else neutral)
    - .planning/phases/11-spool-management-spoolman-camera-qr/11-PATTERNS.md §SpoolmanModels.kt
  </read_first>
  <action>
    Create SpoolmanModels.kt mirroring WebcamModels' conventions exactly: every wire field nullable-or-defaulted, snake_case via @SerialName, the shared serialization. Define @Serializable SpoolmanSpool (id, filament: SpoolmanFilament?, remainingWeight/remainingLength/usedWeight/usedLength: Double?, location/lotNumber: String?, archived: Boolean=false, extra: Map<String,String>=emptyMap()), SpoolmanFilament (id/name/material/colorHex/multiColorHexes/multiColorDirection nullable, vendor: SpoolmanVendor?, settingsExtruderTemp/settingsBedTemp: Int? via @SerialName), SpoolmanVendor (id/name nullable), SpoolmanStatus (@SerialName spoolman_connected, spool_id nullable, pending_reports defaulted empty), PendingSpoolmanReport (spoolId, filamentUsedMm). Add D-08 computed vals on SpoolmanFilament: `normalizedColorHex: String?` (uppercase, prepend #, require 6 or 8 hex post-normalize else null) and `colorSwatches: List<String>` (split multiColorHexes comma-separated, each normalized, fall back to [normalizedColorHex] when present). Keep extra as Map<String,String> — provide a safe accessor that tolerates invalid JSON per value (never throw). Conform the field shapes to whatever FakeMoonrakerSpoolmanSession (11-01) returns for status so SpoolmanModelParserTest is satisfiable.
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --tests *SpoolmanModelParserTest --no-daemon" 2>&1 | tr -d '\r' | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - SpoolmanModelParserTest GREEN: decodes proxy-spool3 + direct-spool3-before goldens with omitted fields, color normalize, multi-color split.
    - colorHex normalization handles with/without #, 6/8 hex, and rejects to null for invalid input.
    - An invalid `extra` JSON value does not break SpoolmanSpool construction.
  </acceptance_criteria>
  <done>SpoolmanModels compiles; SpoolmanModelParserTest is GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Proxy v2 envelope + status parsers</name>
  <files>app/src/main/java/works/mees/dinghy/spool/SpoolmanParsers.kt</files>
  <behavior>
    - parseProxyEnvelope on spoolman-live-ender5-proxy-pla returns 7 rows AND X-Total-Count "7" AND treats error:null as success (NOT empty).
    - A malformed envelope yields an empty result, never throws.
    - One bad spool row drops via mapNotNull; the remaining rows survive.
    - parseSpoolmanStatus hand-walks the server.spoolman.status reply → SpoolmanStatus (spool_id, pending_reports) tolerating missing fields.
  </behavior>
  <read_first>
    - app/src/main/java/works/mees/dinghy/state/WebcamModels.kt lines 113-123 (parseWebcamsList tolerant decode-per-entry drop-the-bad-one idiom)
    - app/src/main/java/works/mees/dinghy/state/PrintMetadata.kt lines 58-69 (field-by-field runCatching{...jsonPrimitive...}.getOrNull() walk for the hand-walked status)
    - app/src/main/java/works/mees/dinghy/net/JsonRpc.kt lines 83-87 (the ONE MoonrakerJson — never build a fresh Json {})
    - .planning/phases/11-spool-management-spoolman-camera-qr/11-PATTERNS.md §SpoolmanParsers.kt (envelope walk; error:null=success anti-pattern; X-Total-Count source)
    - app/src/test/resources/golden/spoolman-live-ender5-proxy-pla.json (the envelope shape to satisfy)
  </read_first>
  <action>
    Create SpoolmanParsers.kt. parseProxyEnvelope(result: JsonElement?): a result type carrying rows (List<JsonElement> or decoded List<SpoolmanSpool>) + totalCount (Int? from response_headers["X-Total-Count"]) + a success flag, by walking {response, error, response_headers}: error:null = success (the payload is in response; treat error:null as data-present, NOT "no data" — RESEARCH anti-pattern); pull X-Total-Count from response_headers; mapNotNull each row via MoonrakerJson.decodeFromJsonElement(SpoolmanSpool.serializer(), entry) so one bad row drops. A malformed envelope returns empty/failure, never throws. Add parseSpoolmanFilaments / parseSpoolmanVendors / parseSpoolmanLocations as the same envelope walk over the materials/vendors/locations/abs-asa/color-red goldens. Add parseSpoolmanStatus(result: JsonElement?): SpoolmanStatus by field-by-field runCatching walk (spoolman_connected, spool_id nullable, pending_reports). Use the shared MoonrakerJson. All pure functions.
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --tests *SpoolmanProxyParserTest --no-daemon" 2>&1 | tr -d '\r' | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - SpoolmanProxyParserTest GREEN: proxy-pla golden → 7 rows + X-Total-Count "7" + error:null treated as success.
    - A malformed envelope returns empty without throwing; one bad row drops, the rest survive.
    - parseSpoolmanStatus tolerates missing fields and yields the spool_id + pending_reports.
  </acceptance_criteria>
  <done>SpoolmanParsers compiles; SpoolmanProxyParserTest GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend FilePreviewMetadata with filament arrays</name>
  <files>app/src/main/java/works/mees/dinghy/state/PrintMetadata.kt</files>
  <behavior>
    - parseFilePreviewMetadata lifts filament_type[] → filamentType: List<String>, filament_name[] → filamentName, filament_colors[] → filamentColors, filament_weights[] → filamentWeights: List<Double>.
    - A missing array yields an empty list, never throws (existing file contract).
    - Existing filament_total / filament_weight_total parsing is unchanged.
  </behavior>
  <read_first>
    - app/src/main/java/works/mees/dinghy/state/PrintMetadata.kt lines 36-49 (FilePreviewMetadata data class) + lines 71-107 (parseFilePreviewMetadata null-safe walk + largestThumbRelPath array iteration to mirror)
    - .planning/phases/11-spool-management-spoolman-camera-qr/11-PATTERNS.md §"state/PrintMetadata.kt (extend FilePreviewMetadata)" (the exact runCatching JsonArray pull idiom)
  </read_first>
  <action>
    Extend FilePreviewMetadata with filamentType: List<String> = emptyList(), filamentName: List<String> = emptyList(), filamentColors: List<String> = emptyList(), filamentWeights: List<Double> = emptyList(). In parseFilePreviewMetadata pull each via the existing null-safe walk: `runCatching { (result["filament_type"] as? JsonArray)?.mapNotNull { it.jsonPrimitive.contentOrNull } }.getOrNull().orEmpty()` (and the Double variant for weights). Keep every walk runCatching{...}.getOrNull() so a missing array yields empty, never throws. Do NOT touch the existing filament_total / filament_weight_total behavior. Turn the Wave-0 FilePreviewMetadataTest RED cases GREEN.
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --tests *FilePreviewMetadataTest --no-daemon" 2>&1 | tr -d '\r' | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - FilePreviewMetadataTest GREEN: the four filament arrays lift; missing arrays yield empty.
    - Existing FilePreviewMetadata tests (filament_total/weight_total) still GREEN.
  </acceptance_criteria>
  <done>FilePreviewMetadata extended; FilePreviewMetadataTest GREEN; no regression.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Spoolman wire JSON → models | Loose, partial, omitted-null payloads cross here; null-safety is the control (D-08). |
| gcode metadata arrays → FilePreviewMetadata | Untrusted file metadata; defensive parse (V12). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-02-01 | DoS (UI crash) | A Spoolman field with invalid JSON in `extra` | mitigate | D-08: tolerate invalid JSON per value; never let one field break construction |
| T-11-02-02 | DoS (UI crash) | Malformed proxy envelope / bad spool row | mitigate | mapNotNull drops one bad row; malformed envelope → empty, never throws |
| T-11-02-03 | Tampering | error:null misread as "no data" | mitigate | Parser treats error:null as success (payload in response) per RESEARCH anti-pattern |
| T-11-02-SC | Tampering | npm/pip/cargo installs | accept | No package installs (pure Kotlin, reuses pinned kotlinx.serialization) |
</threat_model>

<verification>
- `--tests *SpoolmanModelParserTest *SpoolmanProxyParserTest *FilePreviewMetadataTest` all GREEN.
- No new Json instance created (grep for `Json {` in spool/ returns nothing).
</verification>

<success_criteria>
The headless Spoolman model + parser layer is complete and green against the live goldens; FilePreviewMetadata lifts the filament arrays; all null-safe, no throws on malformed input.
</success_criteria>

<output>
Create `.planning/phases/11-spool-management-spoolman-camera-qr/11-02-SUMMARY.md` when done
</output>
