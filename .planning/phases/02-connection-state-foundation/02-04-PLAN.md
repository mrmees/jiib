---
phase: 02-connection-state-foundation
plan: 04
type: execute
wave: 3
depends_on: ["02-01", "02-02", "02-03"]
files_modified:
  - app/src/main/java/works/mees/dinghy/auth/MoonrakerAuth.kt
  - app/src/main/java/works/mees/dinghy/net/Backoff.kt
  - app/src/main/java/works/mees/dinghy/net/MoonrakerSession.kt
  - app/src/main/java/works/mees/dinghy/state/PrinterStateStore.kt
  - app/src/test/java/works/mees/dinghy/net/BackoffTest.kt
  - app/src/test/java/works/mees/dinghy/net/ReconnectSupervisorTest.kt
  - app/src/test/java/works/mees/dinghy/net/HandshakeTest.kt
  - app/src/test/java/works/mees/dinghy/net/ConnectionStateTest.kt
  - app/src/test/java/works/mees/dinghy/auth/AuthHandshakeTest.kt
  - app/src/test/java/works/mees/dinghy/state/ConflationTest.kt
  - app/src/androidTest/java/works/mees/dinghy/connection/LiveSocketReconnectTest.kt
autonomous: false
requirements: [CONN-02, CONN-03, CONN-04, CONN-06, STATE-02, STATE-03]
user_setup: []

must_haves:
  truths:
    - "A reconnect supervisor maintains the connection with uncapped exponential backoff + jitter and retries forever; the backoff arithmetic is overflow-safe (clamped exponent / safe Duration math) so a large attempt count never overflows; requestReconnectNow() cancels the pending backoff delay and fires an immediate attempt"
    - "On every (re)connect the supervisor runs identify -> printer.objects.list -> deriveCapabilities -> printer.objects.query(subset) -> printer.objects.subscribe(subset) in that order, and exposes BOTH the PrinterState AND a Capabilities re-derived each reconnect; the fresh objects.query snapshot overwrites stale retained state"
    - "ConnectionState (Connecting/Syncing/Connected/Disconnected/Error) is always observable as a StateFlow, and Connected is NOT emitted until the resync handshake (query + subscribe seed) completes — Syncing covers socket-open-but-not-yet-resynced"
    - "When an api key is configured, the supervisor fetches a oneshot-token via an injectable REST Call.Factory and appends ?token= to the ws URL and sends X-Api-Key on REST; a REST 401 surfaces as ConnectionState.Error(AuthRequired) and an identify error is classified (AuthRequired for unauthorized shapes, ProtocolError/ServerError otherwise) without crashing"
    - "On AuthRequired the supervisor enters a stable/quiescent error state (no retry-forever churn against a bad key) that only leaves on config change or requestReconnectNow(); uncapped backoff remains the policy for network-offline only (D-01)"
    - "The assembled PrinterState StateFlow conflates ONLY high-rate numeric status (temps/position) to ~2-4 Hz; ConnectionState, KlippyState, stale/auth markers, and print-state transitions propagate immediately (no sampling delay); the gcode_response line stream is NOT conflated; on drop the state retains last-known values + a stale marker"
    - "On the real Ender 5 Plus: a live ws receives notify_status_update; a Wi-Fi yank triggers backoff reconnect + resync and restores correct (non-stale) state"
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/net/Backoff.kt"
      provides: "Pure overflow-safe backoff: jitter(base * 2^attempt) with clamped exponent / safe Duration math, no ceiling (D-01)"
      contains: "backoffDelay"
    - path: "app/src/main/java/works/mees/dinghy/net/MoonrakerSession.kt"
      provides: "Reconnect supervisor + identify->objects.list->deriveCapabilities->query->subscribe handshake + ConnectionState StateFlow (with Syncing) + capabilities exposure + requestReconnectNow() + gentle auth-failure quiescence"
      contains: "requestReconnectNow"
    - path: "app/src/main/java/works/mees/dinghy/auth/MoonrakerAuth.kt"
      provides: "oneshot-token fetch via injectable Call.Factory, ?token= URL build, X-Api-Key, typed 401/identify-error classification -> AuthRequired/ProtocolError/ServerError"
      contains: "oneshot"
    - path: "app/src/main/java/works/mees/dinghy/state/PrinterStateStore.kt"
      provides: "Assembled PrinterState StateFlow (high-rate fields conflated; control-plane immediate) + separate un-throttled gcode SharedFlow + stale marker + Capabilities StateFlow"
      contains: "StateFlow"
    - path: "app/src/androidTest/java/works/mees/dinghy/connection/LiveSocketReconnectTest.kt"
      provides: "On-device live ws + Wi-Fi-yank reconnect/resync proof (NEW class, not the throwaway smoke test)"
  key_links:
    - from: "app/src/main/java/works/mees/dinghy/net/MoonrakerSession.kt"
      to: "app/src/main/java/works/mees/dinghy/state/PrinterStateReducer.kt"
      via: "feeds snapshot+diffs through the reducer into the StateFlow"
      pattern: "reduceDiff|reduceSnapshot"
    - from: "app/src/main/java/works/mees/dinghy/net/MoonrakerSession.kt"
      to: "app/src/main/java/works/mees/dinghy/state/DeriveCapabilities.kt"
      via: "objects.list -> deriveCapabilities + deriveSubscribeSet on every reconnect"
      pattern: "deriveCapabilities|deriveSubscribeSet"
    - from: "app/src/main/java/works/mees/dinghy/net/MoonrakerSession.kt"
      to: "app/src/main/java/works/mees/dinghy/auth/MoonrakerAuth.kt"
      via: "fetches oneshot-token before connect when key configured; classifies identify error"
      pattern: "oneshot|token|classifyIdentifyError"
    - from: "app/src/main/java/works/mees/dinghy/state/PrinterStateStore.kt"
      to: "app/src/main/java/works/mees/dinghy/net/JsonRpcClient.kt"
      via: "high-rate status conflated (sample); control-plane + gcode flows immediate/un-throttled"
      pattern: "sample|conflate"
---

<objective>
Integrate the spine: `MoonrakerSession` (the reconnect supervisor — overflow-safe uncapped backoff+jitter
D-01, `requestReconnectNow()` D-02, the identify→objects.list→deriveCapabilities→query→subscribe handshake
CONN-04, the five-state `ConnectionState` StateFlow with `Syncing` CONN-06, capabilities re-derived each
reconnect STATE-02, gentle auth-failure quiescence), `MoonrakerAuth` (oneshot-token via an injectable REST
`Call.Factory` + `X-Api-Key` + typed 401/identify-error classification → AuthRequired/ProtocolError/ServerError,
CONN-02, mock-tested per D-06), and `PrinterStateStore` (the assembled `PrinterState` StateFlow with
HIGH-RATE-ONLY conflation STATE-03 + an immediate control-plane + the separate un-throttled gcode
`SharedFlow`, with retain-last-known + stale marker D-03, plus a `Capabilities` StateFlow). Then prove the
live path on the real Ender 5 Plus.

Purpose: This is where the four seams become a working connection that satisfies success criteria #1
(live ws + Wi-Fi-yank reconnect + resync), #2 (conflated retained StateFlow), #4 (id-correlation already
proven in Plan 03, exercised here in the live handshake), and #5 (auth end-to-end, mock-tested). This pass
lands the review HIGH fixes that live at the session layer: the correct handshake ORDER with `objects.list`
before query/subscribe and capabilities exposed/re-derived each reconnect (#2); `Connected` meaning
"resynced + subscribed" via an explicit `Syncing` state (#3); plus the MEDIUM fixes — typed auth
classification, gentle auth-failure quiescence, overflow-safe backoff, conflate-only-high-rate-fields, and
an injectable REST `Call.Factory`. The on-device test is the phase gate — the only thing that proves the
real wire format and reconnect on genuine hardware (D-06 no-auth path).

Output: `auth/MoonrakerAuth.kt`, `net/Backoff.kt`, `net/MoonrakerSession.kt`, `state/PrinterStateStore.kt`;
six JVM unit tests (Backoff, ReconnectSupervisor, Handshake, ConnectionState, AuthHandshake, Conflation —
all virtual-time / FakeWebSocket); and the NEW instrumented `LiveSocketReconnectTest`. A human-verify
checkpoint runs the on-device live + Wi-Fi-yank proof.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

**LOCAL BUILD:** Gradle runs Windows-side via `/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat <args>"`
(`./gradlew` does NOT run from WSL); pipe through `tr -d '\r'`; exit code is authoritative. Instrumented
tests run via `:app:connectedDebugAndroidTest` against the USB-attached flox device; host/port come from
instrumentation args (mirror the smoke harness — but this is a NEW class). Commit/push only when asked.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-connection-state-foundation/02-CONTEXT.md
@.planning/phases/02-connection-state-foundation/02-RESEARCH.md
@.planning/phases/02-connection-state-foundation/02-PATTERNS.md
@.planning/phases/02-connection-state-foundation/02-01-PLAN.md
@.planning/phases/02-connection-state-foundation/02-02-PLAN.md
@.planning/phases/02-connection-state-foundation/02-03-PLAN.md
@app/src/androidTest/java/works/mees/dinghy/smoke/CleartextMoonrakerSmokeTest.kt
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MoonrakerAuth — oneshot-token via injectable Call.Factory + X-Api-Key + typed 401/identify-error classification (CONN-02, mock-tested)</name>
  <files>app/src/main/java/works/mees/dinghy/auth/MoonrakerAuth.kt, app/src/test/java/works/mees/dinghy/auth/AuthHandshakeTest.kt</files>
  <read_first>
    - .planning/phases/02-connection-state-foundation/02-RESEARCH.md § "Auth handshake" (access.oneshot_token base32/5s/single-use → ?token=; X-Api-Key REST header; failure shapes: HTTP 401 + identify {code:-32602,"Unauthorized"}; A5 defensive default), § "Security Domain" V7 (never log key/token/full URL)
    - .planning/phases/02-connection-state-foundation/02-CONTEXT.md D-06 (open test bed → mock-only), D-05 (apiKey from BuildConfig)
    - .planning/phases/02-connection-state-foundation/02-PATTERNS.md § "auth/MoonrakerAuth.kt" (REST shape from smoke assertRestInfo)
    - app/src/main/java/works/mees/dinghy/config/DevConfig.kt (apiKey source), app/src/main/java/works/mees/dinghy/state/PrinterState.kt (ConnectionState.Error reason), app/src/main/java/works/mees/dinghy/net/RpcError.kt (the typed ConnectionError model + classifyIdentifyError from Plan 01)
    - app/src/test/resources/golden/adversarial_auth_identify_error.json, adversarial_rest_401.json; app/src/test/java/works/mees/dinghy/net/FakeWebSocket.kt
  </read_first>
  <behavior>
    - When apiKey is configured, fetchOneshotToken() (using the INJECTED Call.Factory) returns the base32 token and buildWsUrl(base, token) appends ?token=<token>.
    - REST calls carry an X-Api-Key header when a key is configured; none when not (D-06 default open path).
    - A REST 401 maps to ConnectionState.Error(AuthRequired) (no exception escapes).
    - An identify response error is CLASSIFIED via classifyIdentifyError (Plan 01): unauthorized shapes ({-32602}/"Unauthorized") -> AuthRequired; clearly non-auth method errors -> ProtocolError/ServerError; an unknown/unrecognized identify error still falls back to AuthRequired defensively (A5).
    - The api key and the full ?token= URL are never logged.
    - fetchOneshotToken is unit-testable with a fake Call.Factory returning a canned token / a canned 401 — no real network.
  </behavior>
  <action>
    Create `auth/MoonrakerAuth.kt` taking an INJECTABLE `okhttp3.Call.Factory` (the OkHttp client is a
    `Call.Factory`; in tests pass a fake that returns canned `Response`s — review MEDIUM, so the 401/token
    path is unit-testable with no network). `fetchOneshotToken()` issues the REST `GET /access/oneshot_token`
    (or RPC `access.oneshot_token`) via that factory, returning the base32 token — call it immediately before
    each connect when a key is set (5 s TTL, single-use); `buildAuthedWsUrl(baseWsUrl, token)` appends
    `?token=`; an `xApiKeyHeader()` helper applied to REST requests when `DevConfig.apiKey` is non-null; and a
    `mapAuthFailure(...)` that turns a REST HTTP 401 into `ConnectionState.Error(AuthRequired)` and DELEGATES
    identify JSON-RPC errors to `classifyIdentifyError(code, message)` from `RpcError.kt` (Plan 01) — so
    unauthorized shapes become `AuthRequired`, non-auth method errors become `ProtocolError`/`ServerError`,
    and an unrecognized identify error still defaults to `AuthRequired` (A5) — never crash. SECURITY: never
    log the key or the full `?token=` URL (redact). This whole branch is exercised in MOCK tests only (D-06).
    Create `AuthHandshakeTest.kt` (fake `Call.Factory` + FakeWebSocket + adversarial fixtures): assert
    `?token=` is appended from a fake-returned token, `X-Api-Key` is set when keyed, a fake REST-401 →
    `AuthRequired`, identify-`-32602` → `AuthRequired`, a DIFFERENT clearly-non-auth identify error →
    `ProtocolError`/`ServerError` (not silently swallowed as auth), and that no log path emits the key/token.
  </action>
  <acceptance_criteria>
    - `gw.bat :app:testDebugUnitTest --tests *AuthHandshakeTest*` exits 0.
    - Behavior assertion: with a key set, the built ws URL contains `?token=<token>` (token from the injected fake Call.Factory) and REST requests carry `X-Api-Key`.
    - Behavior assertion: a fake REST 401 yields `ConnectionState.Error(AuthRequired)`; identify `{-32602,"Unauthorized"}` yields `AuthRequired`; a clearly non-auth identify error yields `ProtocolError`/`ServerError` (typed classification, not blanket AuthRequired) — no exception escapes.
    - Source assertion: `MoonrakerAuth` takes an injectable `Call.Factory` (no hard-coded client); no `Log`/`println` call receives the api key or the full `?token=` URL (redaction by construction); identify-error mapping delegates to `classifyIdentifyError`.
  </acceptance_criteria>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --tests *AuthHandshakeTest* --no-daemon" | tr -d '\r'</automated>
  </verify>
  <done>Auth path is fully mock-testable via an injectable Call.Factory (D-06): oneshot-token → ?token=, X-Api-Key on REST, and a REST 401 → AuthRequired while identify errors are typed-classified (AuthRequired for unauthorized shapes, ProtocolError/ServerError otherwise, defensive A5 fallback for unknowns) — key/token never logged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Backoff (overflow-safe) + MoonrakerSession + PrinterStateStore — supervisor, ordered handshake, ConnectionState w/ Syncing, gentle auth quiescence, high-rate-only conflation (CONN-03/04/06, STATE-02/03, D-01/02/03/04)</name>
  <files>app/src/main/java/works/mees/dinghy/net/Backoff.kt, app/src/main/java/works/mees/dinghy/net/MoonrakerSession.kt, app/src/main/java/works/mees/dinghy/state/PrinterStateStore.kt, app/src/test/java/works/mees/dinghy/net/BackoffTest.kt, app/src/test/java/works/mees/dinghy/net/ReconnectSupervisorTest.kt, app/src/test/java/works/mees/dinghy/net/HandshakeTest.kt, app/src/test/java/works/mees/dinghy/net/ConnectionStateTest.kt, app/src/test/java/works/mees/dinghy/state/ConflationTest.kt</files>
  <read_first>
    - .planning/phases/02-connection-state-foundation/02-RESEARCH.md § "Architecture Patterns" Pattern 3 (supervisorScope loop, jitter(base*2^attempt) NO ceiling, Channel(CONFLATED) reconnectNow, select{onTimeout;onReceive}) + Pattern 4 (sample(250.ms) status StateFlow; un-throttled gcode SharedFlow), § "Resync handshake" (identify→query→subscribe exact shapes; A3 derive subscribe set from objects.list), § "Common Pitfalls" P4/P5/P6 (don't blank on drop; identify once; reconnectNow cancels delay)
    - .planning/phases/02-connection-state-foundation/02-CONTEXT.md D-01, D-02, D-03, D-04, STATE-03, STATE-03 discretion (gcode NOT conflated)
    - .planning/phases/02-connection-state-foundation/02-PATTERNS.md § "net/MoonrakerSession.kt", § "PrinterStateReducer / StateFlow wiring"
    - app/src/main/java/works/mees/dinghy/net/MoonrakerSocket.kt, RpcConnection.kt, JsonRpcClient.kt (Plan 03 — the seams it orchestrates; JsonRpcClient.close(cause) + per-request timeout it relies on so the handshake never deadlocks)
    - app/src/main/java/works/mees/dinghy/state/PrinterStateReducer.kt, DeriveCapabilities.kt (Plan 02 — reduceSnapshot/reduceDiff + deriveCapabilities + deriveSubscribeSet)
    - app/src/main/java/works/mees/dinghy/state/PrinterState.kt (ConnectionState incl. Syncing, from Plan 01), app/src/main/java/works/mees/dinghy/auth/MoonrakerAuth.kt (Task 1 — pre-connect token + typed failure mapping)
    - app/src/test/resources/golden/objects_list.json, objects_query_snapshot.json, notify_status_update_stream.json (+ fallback_* siblings); app/src/test/java/works/mees/dinghy/net/FakeWebSocket.kt
  </read_first>
  <behavior>
    - backoffDelay(attempt, base) is pure and OVERFLOW-SAFE: the exponent is clamped and Duration math is safe so a large attempt count (e.g. 1000) yields a large-but-finite Duration, never an overflow/negative/zero — "no ceiling" (D-01) still produces bounded arithmetic.
    - The supervisor loops: Connecting -> (socket open) Syncing -> (resync complete) Connected; on socket death -> Disconnected -> backoff(jitter, uncapped, overflow-safe) -> retry; attempt resets to 0 on a successful connect (D-01).
    - requestReconnectNow() cancels the pending backoff delay and fires an immediate attempt (D-02) — provable in virtual time.
    - On each (re)connect the supervisor runs identify -> printer.objects.list -> deriveCapabilities -> printer.objects.query(subset) -> printer.objects.subscribe(subset), in that EXACT order, exactly once each; the objects.query snapshot overwrites retained state (D-04); Capabilities is re-derived and exposed each reconnect (STATE-02).
    - ConnectionState.Connected is NOT emitted until query + subscribe seed land — Syncing covers the socket-open-but-not-resynced window; a consumer never sees Connected with stale/unsubscribed state (review HIGH #3).
    - On socket drop, PrinterState retains last-known values and flips a stale/disconnected marker (D-03 — NOT blanked); only the post-reconnect objects.query overwrites.
    - On AuthRequired (Task 1 mapping), the supervisor enters a stable/quiescent error state — it does NOT churn the retry-forever loop fetching tokens against a bad key; it only leaves on config change or requestReconnectNow(). Uncapped backoff stays the policy for network-offline (D-01), distinct from a rejected credential.
    - The assembled PrinterState StateFlow conflates ONLY high-rate numeric status (temps/position) to ~2-4 Hz; ConnectionState, KlippyState, stale/auth markers, and print_stats.state transitions propagate IMMEDIATELY (not delayed/dropped by sampling). The gcode_response SharedFlow is NOT conflated.
  </behavior>
  <action>
    Create `net/Backoff.kt`: a pure `backoffDelay(attempt: Int, base: Duration, rng): Duration` computing
    `base * 2^attempt` with jitter but CLAMPING the exponent (e.g. cap the shift / use `Long` ms with a safe
    upper bound below overflow, or clamp attempt to a max-shift while keeping NO semantic ceiling that makes
    the loop give up) so a huge attempt count returns a large finite Duration, never overflow/negative — D-01
    "no ceiling" via bounded arithmetic (review MEDIUM). Create `net/MoonrakerSession.kt` per RESEARCH
    Pattern 3: a `supervisorScope` reconnect loop emitting a `ConnectionState` `StateFlow` (CONN-06) through
    the FIVE states incl. `Syncing`; uncapped backoff via `backoffDelay` (D-01), attempt counter reset on
    success; a `Channel<Unit>(CONFLATED)` `reconnectNow` consumed in
    `select { onTimeout(backoff){}; reconnectNow.onReceive{} }` exposed as `requestReconnectNow()` (D-02).
    Per connect attempt: if a key is configured, fetch the oneshot-token via `MoonrakerAuth` (Task 1) and
    build the `?token=` URL; open the socket via the injected factory (Plan 03), emit `Syncing`; run the
    handshake EXACTLY ONCE each, IN ORDER (review HIGH #2): `server.connection.identify` (type `"display"`)
    → `printer.objects.list` → `deriveCapabilities`(list)+`deriveSubscribeSet`(list) (Plan 02) →
    `printer.objects.query(subset)` (full snapshot — seeds the reducer and OVERWRITES stale state per D-04)
    → `printer.objects.subscribe(subset)`; ONLY THEN emit `Connected` (review HIGH #3). Expose the
    re-derived `Capabilities` (STATE-02) alongside `PrinterState`. Map identify error / REST 401 to a typed
    `ConnectionState.Error(reason)` (Task 1); on `AuthRequired`, transition to a stable quiescent error state
    and SUSPEND the reconnect loop until a config change or `requestReconnectNow()` (review MEDIUM — gentle
    auth retry, distinct from D-01 network backoff). Rely on `JsonRpcClient`'s per-request timeout +
    `close(cause)` (Plan 03) so a handshake RPC can never deadlock. Create `state/PrinterStateStore.kt`:
    assemble the public `PrinterState` `StateFlow` — split emission into an IMMEDIATE control plane and a
    SAMPLED high-rate plane (review MEDIUM): `scan` the status-diff flow through `reduceDiff` (seeded by the
    snapshot), apply `sample(~250.ms)` conflation ONLY to the high-rate numeric fields (temps/position),
    while `ConnectionState`, `KlippyState`, the stale/auth markers, and `print_stats.state` transitions are
    merged in WITHOUT sampling delay (e.g. combine an un-sampled control-plane flow with the sampled
    high-rate flow so a klippy-shutdown/stale/auth change is emitted immediately). Retain last-known + flip
    the stale marker on disconnect (D-03); expose `klippyState` first-class (STATE-04 already in the model);
    expose the SEPARATE un-throttled `gcode_response` `SharedFlow` (bounded buffer per Plan 03); expose the
    `Capabilities` `StateFlow`. Write the JVM tests (all `runTest`/virtual-time / FakeWebSocket):
    `BackoffTest` (large attempt count yields a finite positive Duration — overflow-safe; growth across
    attempts), `ReconnectSupervisorTest` (uncapped backoff grows, retries forever for network failures,
    `requestReconnectNow()` cancels the delay mid-backoff; AuthRequired enters quiescence and does NOT churn —
    only requestReconnectNow/config-change resumes), `HandshakeTest` (identify→objects.list→deriveCapabilities
    →query→subscribe runs in THAT order exactly once each; subscribe set derived from objects.list — A3;
    query snapshot overwrites a stale seed — D-04; Capabilities exposed/re-derived on a second connect),
    `ConnectionStateTest` (Connecting→Syncing→Connected sequence — Connected NOT emitted before the subscribe
    seed lands, review HIGH #3; Disconnected→Error transitions observable; drop retains state + sets stale
    marker, not blank — D-03), `ConflationTest` (high-rate temp/position burst sampled to ~2-4 Hz while a
    klippy-shutdown/stale transition AND every gcode line are emitted WITHOUT sampling delay — STATE-03 +
    control-plane-immediate, review MEDIUM).
  </action>
  <acceptance_criteria>
    - `gw.bat :app:testDebugUnitTest --tests *BackoffTest* --tests *ReconnectSupervisorTest* --tests *HandshakeTest* --tests *ConnectionStateTest* --tests *ConflationTest*` exits 0.
    - Behavior assertion (overflow-safe backoff): `backoffDelay(1000, base, ...)` returns a finite POSITIVE Duration (no overflow/negative/zero); intervals grow across consecutive attempts with NO give-up.
    - Behavior assertion (D-02): triggering `requestReconnectNow()` mid-backoff issues an immediate connect attempt without waiting out the remaining delay.
    - Behavior assertion (CONN-04/D-04, review HIGH #2): the handshake runs identify→objects.list→deriveCapabilities→query→subscribe in that order exactly once per connect; subscribe set is derived from objects.list; a fresh query snapshot overwrites a deliberately-stale seeded state; Capabilities is re-derived on a second connect.
    - Behavior assertion (review HIGH #3): `ConnectionState.Connected` is emitted only AFTER the subscribe seed lands; `Syncing` is observed between socket-open and resync-complete (Connected is never observed with stale/unsubscribed state).
    - Behavior assertion (gentle auth, review MEDIUM): an AuthRequired result puts the supervisor into a quiescent state that does NOT keep retrying/token-fetching; it resumes only on `requestReconnectNow()` (or config change).
    - Behavior assertion (D-03): a socket drop retains last-known temps and sets the stale marker (state NOT reset to empty); only the post-reconnect query overwrites.
    - Behavior assertion (STATE-03 + control-plane-immediate): under virtual time a high-rate status burst is conflated to ~2-4 Hz, while a klippy-shutdown/stale transition AND every `notify_gcode_response` line are delivered immediately/un-conflated.
    - Source assertion: backoff has no max-ceiling that triggers give-up and uses overflow-safe arithmetic; identify is invoked exactly once per connection; sampling is applied only to the high-rate plane, not to ConnectionState/KlippyState/stale/auth/print-state.
  </acceptance_criteria>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --tests *BackoffTest* --tests *ReconnectSupervisorTest* --tests *HandshakeTest* --tests *ConnectionStateTest* --tests *ConflationTest* --no-daemon" | tr -d '\r'</automated>
  </verify>
  <done>The supervisor maintains the connection with overflow-safe uncapped backoff+jitter + immediate-retry hook (D-01/D-02), runs the resync handshake identify→objects.list→deriveCapabilities→query→subscribe in order once per connect overwriting stale state and exposing re-derived Capabilities (CONN-04/D-04/STATE-02, review HIGH #2), emits an always-observable five-state ConnectionState that reaches Connected only after the subscribe seed lands (CONN-06, review HIGH #3), enters gentle quiescence on AuthRequired rather than churning (review MEDIUM), retains-last-known+stale on drop (D-03), and the assembled StateFlow conflates ONLY high-rate status to ~2-4 Hz while keeping control-plane + gcode immediate (STATE-03, review MEDIUM) — all virtual-time proven.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: On-device live ws + Wi-Fi-yank reconnect/resync proof on the real Ender 5 Plus (phase gate)</name>
  <files>app/src/androidTest/java/works/mees/dinghy/connection/LiveSocketReconnectTest.kt</files>
  <read_first>
    - app/src/androidTest/.../CleartextMoonrakerSmokeTest.kt (HARNESS pattern to MIRROR: AndroidJUnit4, @RunWith, instrumentation-arg host/port, placeholder defaults — but a NEW class exercising the REAL spine, NOT a reuse/edit of the throwaway)
    - app/src/main/java/works/mees/dinghy/net/MoonrakerSession.kt, state/PrinterStateStore.kt (the spine under test — incl. the Syncing→Connected timing and the identify→objects.list→query→subscribe order)
    - .planning/phases/02-connection-state-foundation/02-CONTEXT.md D-06 (on-device = no-auth path only), .planning/phases/02-connection-state-foundation/02-VALIDATION.md § "Manual-Only Verifications"
    - .planning/phases/02-connection-state-foundation/02-RESEARCH.md § "Validation Architecture" → "Sampling Rate" (phase gate = full JVM suite + on-device live-socket proof), § "Environment Availability"
    - CLAUDE.md (repo root) "Local Build Environment" + "Device reality" (flox = LineageOS 18.1 / API 30; gw.bat; adb via interop)
  </read_first>
  <action>
    First (autonomous build step before the human verification): create a NEW instrumented test class
    `app/src/androidTest/java/works/mees/dinghy/connection/LiveSocketReconnectTest.kt` exercising the REAL
    `MoonrakerSession` + `PrinterStateStore` spine — NOT a reuse/edit of `CleartextMoonrakerSmokeTest.kt`
    (mirror only its harness: `AndroidJUnit4`, `@RunWith`, instrumentation-arg host/port with placeholder
    defaults). The test connects on the no-auth path (D-06), asserts the resync handshake completes (ws live,
    objects.query snapshot seeded, subscribe registered) and a live `notify_status_update` updates
    `PrinterState`, and asserts `ConnectionState` reaches `Connected` ONLY after Syncing (the resync completed,
    review HIGH #3). Then PAUSE for the human to run the on-device suite + manual Wi-Fi-yank verification below.
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:compileDebugAndroidTestKotlin --no-daemon" | tr -d '\r'</automated>
  </verify>
  <done>LiveSocketReconnectTest compiles as a NEW instrumented class against the real spine; the on-device live connect + Wi-Fi-yank reconnect/resync is confirmed by the human-verify steps below.</done>
  <what-built>
    A NEW instrumented test `LiveSocketReconnectTest.kt` (NOT the throwaway smoke test) that drives the
    REAL `MoonrakerSession`/`PrinterStateStore` spine against the live Ender 5 Plus over cleartext on the
    no-auth path (D-06): connects via instrumentation-arg host/port, asserts the resync handshake completes
    (identify→objects.list→query→subscribe) and a live `notify_status_update` updates `PrinterState`, asserts
    `ConnectionState` reaches `Connected` only after `Syncing`, and (where automatable) that a forced socket
    interruption triggers backoff reconnect + a fresh objects.query resync restoring non-stale state. Plus
    the full JVM unit suite (Plans 01-04) green.
  </what-built>
  <how-to-verify>
    1. Ensure the flox device is USB-attached and Moonraker is reachable on the LAN. Run the full JVM unit
       suite first: `/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --no-daemon"`
       — confirm ALL Phase-2 unit tests pass (fixtures, reducer, capabilities, klippy, socket, rpcconnection,
       jsonrpc, backoff, reconnect, handshake, connstate, conflation, auth).
    2. Run the instrumented live test against the real printer (host/port via args, mirroring the smoke harness):
       `/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:connectedDebugAndroidTest -Pandroid.testInstrumentationRunnerArguments.moonrakerHost=<live-ip> -Pandroid.testInstrumentationRunnerArguments.moonrakerPort=7125 --no-daemon"`
       — confirm `LiveSocketReconnectTest` connects, completes the handshake, and observes a live
       `notify_status_update` updating PrinterState (ConnectionState reaches Connected only after Syncing).
    3. Manually exercise the Wi-Fi-yank reconnect path: with the dev build connected to the live printer,
       interrupt the network (disable Wi-Fi / pull the AP briefly), confirm ConnectionState goes
       Disconnected and the screen-state is RETAINED (stale marker, not blanked), then restore the network
       and confirm backoff reconnect + resync restores correct (non-stale) temps/position.
  </how-to-verify>
  <resume-signal>Type "approved" once the JVM suite is green AND the on-device live connect + Wi-Fi-yank reconnect/resync is observed on the real Ender 5 Plus; or describe what failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev build → cleartext ws/http on LAN | plaintext traffic on a shared LAN (pre-accepted project decision CONN-05) |
| apiKey/oneshot-token → logs/URLs | credentials must never reach a log or a logged URL |
| Klippy↔Moonraker link state | klippy_disconnected can occur while the ws is still up (distinct axis) |
| bad credential → reconnect loop | a rejected key must not drive a token-fetch/reconnect storm |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06 | Information Disclosure | Oneshot-token in the ws `?token=` URL leaked via logging | mitigate | Never log the full ws URL with `?token=`; redact (token is 5 s/single-use by design) (RESEARCH V7) |
| T-02-07 | Information Disclosure | api key surfaced in an error/state on 401 | mitigate | Surface auth failure as `ConnectionState.Error(AuthRequired)` only — no key/stack-trace in the state (RESEARCH V7) |
| T-02-08 | Information Disclosure | Cleartext ws/http sniffed on shared LAN | accept | Pre-accepted project decision (CONN-05, LAN-only); documented residual risk, no v1 mitigation |
| T-02-09 | Denial of Service (self) | Reconnect storm hammering the LAN | mitigate | Overflow-safe uncapped backoff + jitter (D-01) for network-offline; AuthRequired enters quiescence (no churn against a bad key) — gentle-to-network by design |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan (no new deps); nothing to gate. block_on=high; no HIGH findings (cleartext pre-accepted per CONN-05). |
</threat_model>

<verification>
- Full JVM unit suite green: `gw.bat :app:testDebugUnitTest --no-daemon` (all Plan 01-04 tests).
- Instrumented live proof green: `gw.bat :app:connectedDebugAndroidTest` (LiveSocketReconnectTest) against the real printer.
- On-device Wi-Fi-yank reconnect/resync observed (human-verify checkpoint) — D-03 retain-on-drop + D-04 resync-overwrite confirmed on hardware.
</verification>

<success_criteria>
- CONN-03/D-01/D-02: overflow-safe uncapped backoff+jitter, retry forever (network), requestReconnectNow() cancels the pending delay (virtual-time proven).
- CONN-04/D-04/STATE-02: identify→objects.list→deriveCapabilities→query→subscribe once per (re)connect in order; fresh query overwrites stale state; Capabilities re-derived/exposed each reconnect.
- CONN-06: five-state ConnectionState always observable; Connected only after resync+subscribe (Syncing covers the window).
- CONN-02 (mock-only per D-06): oneshot-token via injectable Call.Factory + X-Api-Key + typed 401/identify-error classification (AuthRequired/ProtocolError/ServerError); gentle auth quiescence; secrets never logged.
- STATE-03: high-rate status conflated to ~2-4 Hz; control-plane (ConnectionState/KlippyState/stale/auth/print-state) + gcode line stream immediate/un-conflated; state retained+stale on drop (D-03).
- Live success criterion #1 proven on the real Ender 5 Plus (no-auth path, D-06): live ws + Wi-Fi-yank reconnect + resync.
</success_criteria>

<output>
Create `.planning/phases/02-connection-state-foundation/02-04-SUMMARY.md` when done
</output>
</content>
