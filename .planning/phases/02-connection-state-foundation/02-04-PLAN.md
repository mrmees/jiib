---
phase: 02-connection-state-foundation
plan: 04
type: execute
wave: 3
depends_on: ["02-01", "02-02", "02-03"]
files_modified:
  - app/src/main/java/works/mees/dinghy/auth/MoonrakerAuth.kt
  - app/src/main/java/works/mees/dinghy/net/MoonrakerSession.kt
  - app/src/main/java/works/mees/dinghy/state/PrinterStateStore.kt
  - app/src/test/java/works/mees/dinghy/net/ReconnectSupervisorTest.kt
  - app/src/test/java/works/mees/dinghy/net/HandshakeTest.kt
  - app/src/test/java/works/mees/dinghy/net/ConnectionStateTest.kt
  - app/src/test/java/works/mees/dinghy/auth/AuthHandshakeTest.kt
  - app/src/test/java/works/mees/dinghy/state/ConflationTest.kt
  - app/src/androidTest/java/works/mees/dinghy/connection/LiveSocketReconnectTest.kt
autonomous: false
requirements: [CONN-02, CONN-03, CONN-04, CONN-06, STATE-03]
user_setup: []

must_haves:
  truths:
    - "A reconnect supervisor maintains the connection with uncapped exponential backoff + jitter and retries forever; requestReconnectNow() cancels the pending backoff delay and fires an immediate attempt"
    - "On every (re)connect the supervisor runs identify -> objects.query -> objects.subscribe exactly once each, and the fresh objects.query snapshot overwrites stale retained state"
    - "ConnectionState (Connecting/Connected/Disconnected/Error) is always observable as a StateFlow"
    - "When an api key is configured, the supervisor fetches a oneshot-token and appends ?token= to the ws URL and sends X-Api-Key on REST; a REST 401 or an identify error surfaces as ConnectionState.Error(AuthRequired) without crashing"
    - "The assembled PrinterState StateFlow conflates status to ~2-4 Hz while the gcode_response line stream is NOT conflated; on drop the state retains last-known values + a stale marker"
    - "On the real Ender 5 Plus: a live ws receives notify_status_update; a Wi-Fi yank triggers backoff reconnect + resync and restores correct (non-stale) state"
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/net/MoonrakerSession.kt"
      provides: "Reconnect supervisor + handshake + ConnectionState StateFlow + requestReconnectNow()"
      contains: "requestReconnectNow"
    - path: "app/src/main/java/works/mees/dinghy/auth/MoonrakerAuth.kt"
      provides: "oneshot-token fetch, ?token= URL build, X-Api-Key, 401/identify-error -> AuthRequired"
      contains: "oneshot"
    - path: "app/src/main/java/works/mees/dinghy/state/PrinterStateStore.kt"
      provides: "Assembled PrinterState StateFlow (conflated) + separate un-throttled gcode SharedFlow + stale marker"
      contains: "StateFlow"
    - path: "app/src/androidTest/java/works/mees/dinghy/connection/LiveSocketReconnectTest.kt"
      provides: "On-device live ws + Wi-Fi-yank reconnect/resync proof (NEW class, not the throwaway smoke test)"
  key_links:
    - from: "app/src/main/java/works/mees/dinghy/net/MoonrakerSession.kt"
      to: "app/src/main/java/works/mees/dinghy/state/PrinterStateReducer.kt"
      via: "feeds snapshot+diffs through the reducer into the StateFlow"
      pattern: "reduceDiff|reduceSnapshot"
    - from: "app/src/main/java/works/mees/dinghy/net/MoonrakerSession.kt"
      to: "app/src/main/java/works/mees/dinghy/auth/MoonrakerAuth.kt"
      via: "fetches oneshot-token before connect when key configured"
      pattern: "oneshot|token"
    - from: "app/src/main/java/works/mees/dinghy/state/PrinterStateStore.kt"
      to: "app/src/main/java/works/mees/dinghy/net/JsonRpcClient.kt"
      via: "status flow conflated (sample), gcode flow un-throttled"
      pattern: "sample|conflate"
---

<objective>
Integrate the spine: `MoonrakerSession` (the reconnect supervisor — uncapped backoff+jitter D-01,
`requestReconnectNow()` D-02, the identify→query→subscribe handshake CONN-04, the `ConnectionState`
StateFlow CONN-06), `MoonrakerAuth` (oneshot-token + `X-Api-Key` + graceful 401/identify-error → AuthRequired,
CONN-02, mock-tested per D-06), and `PrinterStateStore` (the assembled conflated `PrinterState` StateFlow
STATE-03 + the separate un-throttled gcode `SharedFlow`, with retain-last-known + stale marker D-03). Then
prove the live path on the real Ender 5 Plus.

Purpose: This is where the four seams become a working connection that satisfies success criteria #1
(live ws + Wi-Fi-yank reconnect + resync), #2 (conflated retained StateFlow), #4 (id-correlation already
proven in Plan 03, exercised here in the live handshake), and #5 (auth end-to-end, mock-tested). The
on-device test is the phase gate — the only thing that proves the real wire format and reconnect on
genuine hardware (D-06 no-auth path).

Output: `auth/MoonrakerAuth.kt`, `net/MoonrakerSession.kt`, `state/PrinterStateStore.kt`; five JVM unit
tests (ReconnectSupervisor, Handshake, ConnectionState, AuthHandshake, Conflation — all virtual-time /
FakeWebSocket); and the NEW instrumented `LiveSocketReconnectTest`. A human-verify checkpoint runs the
on-device live + Wi-Fi-yank proof.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

**LOCAL BUILD:** Gradle runs Windows-side via `/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat <args>"`
(`./gradlew` does NOT run from WSL); pipe through `tr -d '\r'`; exit code is authoritative. Instrumented
tests run via `:app:connectedDebugAndroidTest` against the USB-attached flox device; host/port come from
instrumentation args (mirror the smoke harness — but this is a NEW class). Commit/push only when asked.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-connection-state-foundation/02-CONTEXT.md
@.planning/phases/02-connection-state-foundation/02-RESEARCH.md
@.planning/phases/02-connection-state-foundation/02-PATTERNS.md
@.planning/phases/02-connection-state-foundation/02-01-PLAN.md
@.planning/phases/02-connection-state-foundation/02-02-PLAN.md
@.planning/phases/02-connection-state-foundation/02-03-PLAN.md
@app/src/androidTest/java/works/mees/dinghy/smoke/CleartextMoonrakerSmokeTest.kt
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MoonrakerAuth — oneshot-token + X-Api-Key + graceful 401/identify-error (CONN-02, mock-tested)</name>
  <files>app/src/main/java/works/mees/dinghy/auth/MoonrakerAuth.kt, app/src/test/java/works/mees/dinghy/auth/AuthHandshakeTest.kt</files>
  <read_first>
    - .planning/phases/02-connection-state-foundation/02-RESEARCH.md § "Auth handshake" (access.oneshot_token base32/5s/single-use → ?token=; X-Api-Key REST header; failure shapes: HTTP 401 + identify {code:-32602,"Unauthorized"}; A5 defensive: treat ANY identify error as AuthRequired), § "Security Domain" V7 (never log key/token/full URL)
    - .planning/phases/02-connection-state-foundation/02-CONTEXT.md D-06 (open test bed → mock-only), D-05 (apiKey from BuildConfig)
    - .planning/phases/02-connection-state-foundation/02-PATTERNS.md § "auth/MoonrakerAuth.kt" (REST shape from smoke assertRestInfo)
    - app/src/main/java/works/mees/dinghy/config/DevConfig.kt (apiKey source), app/src/main/java/works/mees/dinghy/state/PrinterState.kt (ConnectionState.Error AuthRequired reason)
    - app/src/test/resources/golden/adversarial_auth_identify_error.json, adversarial_rest_401.json; app/src/test/java/works/mees/dinghy/net/FakeWebSocket.kt
  </read_first>
  <behavior>
    - When apiKey is configured, fetchOneshotToken() returns the base32 token and buildWsUrl(base, token) appends ?token=<token>.
    - REST calls carry an X-Api-Key header when a key is configured; none when not (D-06 default open path).
    - A REST 401 maps to ConnectionState.Error(AuthRequired) (no exception escapes).
    - An identify response error (any error, esp. {-32602,"Unauthorized"}) maps to ConnectionState.Error(AuthRequired) defensively (A5).
    - The api key and the full ?token= URL are never logged.
  </behavior>
  <action>
    Create `auth/MoonrakerAuth.kt`: `fetchOneshotToken()` (RPC `access.oneshot_token` or REST
    `GET /access/oneshot_token`) returning the base32 token — call it immediately before each connect when
    a key is set (5 s TTL, single-use); `buildAuthedWsUrl(baseWsUrl, token)` appending `?token=`; an
    `xApiKeyHeader()` helper applied to REST requests when `DevConfig.apiKey` is non-null; and a
    `mapAuthFailure(...)` that turns a REST HTTP 401 and an identify JSON-RPC error into
    `ConnectionState.Error(AuthRequired)` — treat ANY identify error as auth-fail defensively (A5), never
    crash. Reuse the smoke test's REST `newCall(...).execute().use {}` shape. SECURITY: never log the key
    or the full `?token=` URL (redact). This whole branch is exercised in MOCK tests only (D-06). Create
    `AuthHandshakeTest.kt` (FakeWebSocket + adversarial fixtures): assert `?token=` is appended, `X-Api-Key`
    is set when keyed, REST-401 → `AuthRequired`, identify-`-32602` → `AuthRequired`, and that no log path
    emits the key/token (assert via an injected logger or by structuring the API so secrets never reach a log call).
  </action>
  <acceptance_criteria>
    - `gw.bat :app:testDebugUnitTest --tests *AuthHandshakeTest*` exits 0.
    - Behavior assertion: with a key set, the built ws URL contains `?token=<token>` and REST requests carry `X-Api-Key`.
    - Behavior assertion: a REST 401 yields `ConnectionState.Error(AuthRequired)`; an identify `{-32602,"Unauthorized"}` (and a different arbitrary identify error) both yield `AuthRequired` — no exception escapes.
    - Source assertion: no `Log`/`println` call receives the api key or the full `?token=` URL (redaction by construction).
  </acceptance_criteria>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --tests *AuthHandshakeTest* --no-daemon" | tr -d '\r'</automated>
  </verify>
  <done>Auth path is fully mock-testable (D-06): oneshot-token → ?token=, X-Api-Key on REST, and any REST 401 / identify error → graceful ConnectionState.Error(AuthRequired) with the key/token never logged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: MoonrakerSession + PrinterStateStore — reconnect supervisor, handshake, ConnectionState, conflation (CONN-03/04/06, STATE-03, D-01/02/03/04)</name>
  <files>app/src/main/java/works/mees/dinghy/net/MoonrakerSession.kt, app/src/main/java/works/mees/dinghy/state/PrinterStateStore.kt, app/src/test/java/works/mees/dinghy/net/ReconnectSupervisorTest.kt, app/src/test/java/works/mees/dinghy/net/HandshakeTest.kt, app/src/test/java/works/mees/dinghy/net/ConnectionStateTest.kt, app/src/test/java/works/mees/dinghy/state/ConflationTest.kt</files>
  <read_first>
    - .planning/phases/02-connection-state-foundation/02-RESEARCH.md § "Architecture Patterns" Pattern 3 (supervisorScope loop, uncapped jitter(base*2^attempt) NO ceiling, Channel(CONFLATED) reconnectNow, select{onTimeout;onReceive}) + Pattern 4 (sample(250.ms) status StateFlow; un-throttled gcode SharedFlow), § "Resync handshake" (identify→query→subscribe exact shapes; A3 derive subscribe set from objects.list), § "Common Pitfalls" P4/P5/P6 (don't blank on drop; identify once; reconnectNow cancels delay)
    - .planning/phases/02-connection-state-foundation/02-CONTEXT.md D-01, D-02, D-03, D-04, STATE-03, STATE-03 discretion (gcode NOT conflated)
    - .planning/phases/02-connection-state-foundation/02-PATTERNS.md § "net/MoonrakerSession.kt", § "PrinterStateReducer / StateFlow wiring"
    - app/src/main/java/works/mees/dinghy/net/MoonrakerSocket.kt, JsonRpcClient.kt (Plan 03 — the seams it orchestrates)
    - app/src/main/java/works/mees/dinghy/state/PrinterStateReducer.kt, DeriveCapabilities.kt (Plan 02 — the pure reducers it feeds)
    - app/src/main/java/works/mees/dinghy/auth/MoonrakerAuth.kt (Task 1 — pre-connect token + failure mapping)
    - app/src/test/resources/golden/objects_query_snapshot.json, notify_status_update_stream.json; app/src/test/java/works/mees/dinghy/net/FakeWebSocket.kt
  </read_first>
  <behavior>
    - The supervisor loops: Connecting -> (connect+serve) -> Connected; on socket death -> Disconnected -> backoff(jitter, uncapped 2^attempt, NO ceiling) -> retry; attempt resets to 0 on a successful connect (D-01).
    - requestReconnectNow() cancels the pending backoff delay and fires an immediate attempt (D-02) — provable in virtual time: trigger mid-backoff, attempt issues without waiting out the delay.
    - On each (re)connect the supervisor runs identify -> objects.query -> objects.subscribe exactly once each, in order; the objects.query snapshot overwrites retained state (D-04, non-stale after recovery).
    - On socket drop, PrinterState retains last-known values and flips a stale/disconnected marker (D-03 — NOT blanked); only the post-reconnect objects.query overwrites.
    - ConnectionState StateFlow emits Connecting->Connected->Disconnected->Error transitions observably (CONN-06).
    - The assembled PrinterState StateFlow conflates status to ~2-4 Hz (sample ~250ms) under virtual time; the gcode_response SharedFlow is NOT conflated (every injected line observed). (STATE-03)
    - When apiKey configured, a oneshot-token is fetched and applied before opening the socket (Task 1).
  </behavior>
  <action>
    Create `net/MoonrakerSession.kt` per RESEARCH Pattern 3: a `supervisorScope` reconnect loop emitting a
    `ConnectionState` `StateFlow` (CONN-06); uncapped `jitter(base * 2^attempt)` backoff (D-01 — NO ceiling,
    no give-up), attempt counter reset on success; a `Channel<Unit>(CONFLATED)` `reconnectNow` consumed in
    `select { onTimeout(backoff){}; reconnectNow.onReceive{} }` exposed as `requestReconnectNow()` (D-02).
    Per connect attempt: if a key is configured, fetch the oneshot-token and build the `?token=` URL via
    `MoonrakerAuth` (Task 1); open the socket via the injected factory (Plan 03); run the handshake
    EXACTLY ONCE each in order — `server.connection.identify` (type `"display"`), `printer.objects.query`
    (full snapshot, seeds the reducer and OVERWRITES stale state per D-04), `printer.objects.subscribe`
    (derive the subscribe set by intersecting the v1 superset with the `objects.list` result per A3 — also
    fetch `objects.list` and run `deriveCapabilities`). Map identify error / REST 401 to
    `ConnectionState.Error(AuthRequired)` (Task 1). Create `state/PrinterStateStore.kt`: assemble the public
    `PrinterState` `StateFlow` by `scan`-ing the status-diff flow through `reduceDiff` (seeded by the snapshot),
    applying `sample(~250.ms)` conflation (STATE-03), retaining last-known + flipping the stale marker on
    disconnect (D-03), exposing `klippyState` first-class (STATE-04 already in the model), and exposing the
    SEPARATE un-throttled `gcode_response` `SharedFlow` (bounded buffer per Plan 03). Write the four JVM
    tests (all `runTest`/virtual-time / FakeWebSocket): `ReconnectSupervisorTest` (uncapped backoff grows,
    retries forever, `requestReconnectNow()` cancels the delay mid-backoff), `HandshakeTest` (identify→query→
    subscribe runs once each in order; query snapshot overwrites a stale seed — D-04; subscribe set derived
    from objects.list — A3), `ConnectionStateTest` (Connecting→Connected→Disconnected→Error transitions
    observable; drop retains state + sets stale marker, not blank — D-03), `ConflationTest` (status sampled
    to ~2-4 Hz while a burst of gcode lines is ALL delivered un-conflated — STATE-03 + discretion).
  </action>
  <acceptance_criteria>
    - `gw.bat :app:testDebugUnitTest --tests *ReconnectSupervisorTest* --tests *HandshakeTest* --tests *ConnectionStateTest* --tests *ConflationTest*` exits 0.
    - Behavior assertion (CONN-03/D-01): backoff interval grows across consecutive failures with NO ceiling and the loop never gives up (virtual time).
    - Behavior assertion (D-02): triggering `requestReconnectNow()` mid-backoff issues an immediate connect attempt without waiting out the remaining delay.
    - Behavior assertion (CONN-04/D-04): identify→query→subscribe each run exactly once per connect, in order, and a fresh query snapshot overwrites a deliberately-stale seeded state.
    - Behavior assertion (D-03): a socket drop retains last-known temps and sets the stale marker (state NOT reset to empty); only the post-reconnect query overwrites.
    - Behavior assertion (STATE-03): under virtual time a high-rate status burst is conflated to ~2-4 Hz on the StateFlow, while every line of a `notify_gcode_response` burst is delivered on the separate flow (no conflation).
    - Source assertion: backoff has no max-ceiling clamp and no give-up budget; identify is invoked exactly once per connection.
  </acceptance_criteria>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --tests *ReconnectSupervisorTest* --tests *HandshakeTest* --tests *ConnectionStateTest* --tests *ConflationTest* --no-daemon" | tr -d '\r'</automated>
  </verify>
  <done>The supervisor maintains the connection with uncapped backoff+jitter + immediate-retry hook (D-01/D-02), runs the resync handshake once per connect overwriting stale state (CONN-04/D-04), emits an always-observable ConnectionState (CONN-06), retains-last-known+stale on drop (D-03), and the assembled StateFlow conflates status to ~2-4 Hz while keeping the gcode stream un-throttled (STATE-03) — all virtual-time proven.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: On-device live ws + Wi-Fi-yank reconnect/resync proof on the real Ender 5 Plus (phase gate)</name>
  <files>app/src/androidTest/java/works/mees/dinghy/connection/LiveSocketReconnectTest.kt</files>
  <read_first>
    - app/src/androidTest/.../CleartextMoonrakerSmokeTest.kt (HARNESS pattern to MIRROR: AndroidJUnit4, @RunWith, instrumentation-arg host/port, placeholder defaults — but a NEW class exercising the REAL spine, NOT a reuse/edit of the throwaway)
    - app/src/main/java/works/mees/dinghy/net/MoonrakerSession.kt, state/PrinterStateStore.kt (the spine under test)
    - .planning/phases/02-connection-state-foundation/02-CONTEXT.md D-06 (on-device = no-auth path only), .planning/phases/02-connection-state-foundation/02-VALIDATION.md § "Manual-Only Verifications"
    - .planning/phases/02-connection-state-foundation/02-RESEARCH.md § "Validation Architecture" → "Sampling Rate" (phase gate = full JVM suite + on-device live-socket proof), § "Environment Availability"
    - CLAUDE.md (repo root) "Local Build Environment" + "Device reality" (flox = LineageOS 18.1 / API 30; gw.bat; adb via interop)
  </read_first>
  <action>
    First (autonomous build step before the human verification): create a NEW instrumented test class
    `app/src/androidTest/java/works/mees/dinghy/connection/LiveSocketReconnectTest.kt` exercising the REAL
    `MoonrakerSession` + `PrinterStateStore` spine — NOT a reuse/edit of `CleartextMoonrakerSmokeTest.kt`
    (mirror only its harness: `AndroidJUnit4`, `@RunWith`, instrumentation-arg host/port with placeholder
    defaults). The test connects on the no-auth path (D-06), asserts the resync handshake completes and a
    live `notify_status_update` updates `PrinterState`, and asserts `ConnectionState` reaches `Connected`.
    Then PAUSE for the human to run the on-device suite + manual Wi-Fi-yank verification below.
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:compileDebugAndroidTestKotlin --no-daemon" | tr -d '\r'</automated>
  </verify>
  <done>LiveSocketReconnectTest compiles as a NEW instrumented class against the real spine; the on-device live connect + Wi-Fi-yank reconnect/resync is confirmed by the human-verify steps below.</done>
  <what-built>
    A NEW instrumented test `LiveSocketReconnectTest.kt` (NOT the throwaway smoke test) that drives the
    REAL `MoonrakerSession`/`PrinterStateStore` spine against the live Ender 5 Plus over cleartext on the
    no-auth path (D-06): connects via instrumentation-arg host/port, asserts the resync handshake completes
    and a live `notify_status_update` updates `PrinterState`, asserts `ConnectionState` reaches `Connected`,
    and (where automatable) that a forced socket interruption triggers backoff reconnect + a fresh
    objects.query resync restoring non-stale state. Plus the full JVM unit suite (Plans 01-04) green.
  </what-built>
  <how-to-verify>
    1. Ensure the flox device is USB-attached and Moonraker is reachable on the LAN. Run the full JVM unit
       suite first: `/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --no-daemon"`
       — confirm ALL Phase-2 unit tests pass (reducer, capabilities, klippy, socket, jsonrpc, reconnect,
       handshake, connstate, conflation, auth, fixtures).
    2. Run the instrumented live test against the real printer (host/port via args, mirroring the smoke harness):
       `/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:connectedDebugAndroidTest -Pandroid.testInstrumentationRunnerArguments.moonrakerHost=<live-ip> -Pandroid.testInstrumentationRunnerArguments.moonrakerPort=7125 --no-daemon"`
       — confirm `LiveSocketReconnectTest` connects, completes the handshake, and observes a live
       `notify_status_update` updating PrinterState (ConnectionState=Connected).
    3. Manually exercise the Wi-Fi-yank reconnect path: with the dev build connected to the live printer,
       interrupt the network (disable Wi-Fi / pull the AP briefly), confirm ConnectionState goes
       Disconnected and the screen-state is RETAINED (stale marker, not blanked), then restore the network
       and confirm backoff reconnect + resync restores correct (non-stale) temps/position.
  </how-to-verify>
  <resume-signal>Type "approved" once the JVM suite is green AND the on-device live connect + Wi-Fi-yank reconnect/resync is observed on the real Ender 5 Plus; or describe what failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev build → cleartext ws/http on LAN | plaintext traffic on a shared LAN (pre-accepted project decision CONN-05) |
| apiKey/oneshot-token → logs/URLs | credentials must never reach a log or a logged URL |
| Klippy↔Moonraker link state | klippy_disconnected can occur while the ws is still up (distinct axis) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06 | Information Disclosure | Oneshot-token in the ws `?token=` URL leaked via logging | mitigate | Never log the full ws URL with `?token=`; redact (token is 5 s/single-use by design) (RESEARCH V7) |
| T-02-07 | Information Disclosure | api key surfaced in an error/state on 401 | mitigate | Surface auth failure as `ConnectionState.Error(AuthRequired)` only — no key/stack-trace in the state (RESEARCH V7) |
| T-02-08 | Information Disclosure | Cleartext ws/http sniffed on shared LAN | accept | Pre-accepted project decision (CONN-05, LAN-only); documented residual risk, no v1 mitigation |
| T-02-09 | Denial of Service (self) | Reconnect storm hammering the LAN | mitigate | Uncapped backoff + jitter (D-01) IS the mitigation — gentle-to-network by design; no give-up needed |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan (no new deps); nothing to gate. block_on=high; no HIGH findings (cleartext pre-accepted per CONN-05). |
</threat_model>

<verification>
- Full JVM unit suite green: `gw.bat :app:testDebugUnitTest --no-daemon` (all Plan 01-04 tests).
- Instrumented live proof green: `gw.bat :app:connectedDebugAndroidTest` (LiveSocketReconnectTest) against the real printer.
- On-device Wi-Fi-yank reconnect/resync observed (human-verify checkpoint) — D-03 retain-on-drop + D-04 resync-overwrite confirmed on hardware.
</verification>

<success_criteria>
- CONN-03/D-01/D-02: uncapped backoff+jitter, retry forever, requestReconnectNow() cancels the pending delay (virtual-time proven).
- CONN-04/D-04: identify→query→subscribe once per (re)connect; fresh query overwrites stale state.
- CONN-06: ConnectionState always observable as a StateFlow.
- CONN-02 (mock-only per D-06): oneshot-token + X-Api-Key + graceful 401/identify-error → AuthRequired, secrets never logged.
- STATE-03: status conflated to ~2-4 Hz; gcode line stream NOT conflated; state retained+stale on drop (D-03).
- Live success criterion #1 proven on the real Ender 5 Plus (no-auth path, D-06): live ws + Wi-Fi-yank reconnect + resync.
</success_criteria>

<output>
Create `.planning/phases/02-connection-state-foundation/02-04-SUMMARY.md` when done
</output>
