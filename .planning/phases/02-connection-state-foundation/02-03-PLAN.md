---
phase: 02-connection-state-foundation
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - app/src/main/java/works/mees/dinghy/net/MoonrakerSocket.kt
  - app/src/main/java/works/mees/dinghy/net/JsonRpcClient.kt
  - app/src/test/java/works/mees/dinghy/net/JsonRpcClientTest.kt
  - app/src/test/java/works/mees/dinghy/net/MoonrakerSocketTest.kt
autonomous: true
requirements: [STATE-05]
user_setup: []

must_haves:
  truths:
    - "A single OkHttp WebSocket is bridged into a cold Flow of raw text frames via callbackFlow, cancellable through awaitClose, with an injectable socket factory so a FakeWebSocket can be substituted"
    - "A suspend request() returns the response matched to its request id even when notify_* events interleave between the request and its reply — the client never assumes the next frame is its answer (STATE-05)"
    - "Notifications (method present, no id) are routed to SharedFlows by method name and never resolve a pending request"
    - "The gcode_response line stream is a separable raw SharedFlow, kept distinct from status notifications (no conflation at this layer)"
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/net/MoonrakerSocket.kt"
      provides: "OkHttp WebSocket -> callbackFlow<SocketEvent> bridge + send; injectable socket factory"
      contains: "callbackFlow"
    - path: "app/src/main/java/works/mees/dinghy/net/JsonRpcClient.kt"
      provides: "id-correlation Map<Long, CompletableDeferred>, suspend request(), notify routing by method"
      contains: "CompletableDeferred"
    - path: "app/src/test/java/works/mees/dinghy/net/JsonRpcClientTest.kt"
      provides: "interleaved-notification correlation proof (STATE-05)"
  key_links:
    - from: "app/src/main/java/works/mees/dinghy/net/JsonRpcClient.kt"
      to: "app/src/main/java/works/mees/dinghy/net/MoonrakerSocket.kt"
      via: "consumes frame Flow, sends via socket"
      pattern: "send"
    - from: "app/src/test/java/works/mees/dinghy/net/JsonRpcClientTest.kt"
      to: "app/src/test/java/works/mees/dinghy/net/FakeWebSocket.kt"
      via: "injects interleaved adversarial frames"
      pattern: "FakeWebSocket"
---

<objective>
Build the transport + protocol seams: `MoonrakerSocket` (OkHttp `WebSocket` → `callbackFlow` raw-frame
bridge with an injectable socket factory) and `JsonRpcClient` (the `Map<id, CompletableDeferred>`
correlation layer + notification routing by method) — the STATE-05 correctness surface.

Purpose: STATE-05 ("response matched by id, never by arrival order, under interleaved notify_*") is a
named success criterion and a first-class adversarial-tested seam. Isolating socket and protocol from the
session lets the interleaving proof run against the `FakeWebSocket` with zero reconnect/handshake noise.
The injectable factory is the testability seam the whole phase leans on.

Output: `net/MoonrakerSocket.kt`, `net/JsonRpcClient.kt`, and two JVM unit tests
(`JsonRpcClientTest` proving id-correlation under interleaving + notify routing + gcode-stream separation;
`MoonrakerSocketTest` proving the callbackFlow bridge + cancellation via the FakeWebSocket) — all green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

**LOCAL BUILD:** Gradle runs Windows-side via `/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat <args>"`
(`./gradlew` does NOT run from WSL); pipe through `tr -d '\r'`; exit code is authoritative. Commit only when asked.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-connection-state-foundation/02-CONTEXT.md
@.planning/phases/02-connection-state-foundation/02-RESEARCH.md
@.planning/phases/02-connection-state-foundation/02-PATTERNS.md
@.planning/phases/02-connection-state-foundation/02-01-PLAN.md
@app/src/androidTest/java/works/mees/dinghy/smoke/CleartextMoonrakerSmokeTest.kt
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MoonrakerSocket — OkHttp WebSocket -> callbackFlow bridge with injectable factory</name>
  <files>app/src/main/java/works/mees/dinghy/net/MoonrakerSocket.kt, app/src/test/java/works/mees/dinghy/net/MoonrakerSocketTest.kt</files>
  <read_first>
    - .planning/phases/02-connection-state-foundation/02-RESEARCH.md § "Architecture Patterns" Pattern 1 (callbackFlow bridge, SocketEvent, awaitClose, readTimeout(0)), § "Don't Hand-Roll" (push→Flow bridge)
    - .planning/phases/02-connection-state-foundation/02-PATTERNS.md § "net/MoonrakerSocket.kt" (carry the smoke test's OkHttp timeout posture; inject the socket-opening lambda — do NOT hard-code newWebSocket)
    - app/src/androidTest/.../CleartextMoonrakerSmokeTest.kt (OkHttp client construction lines 88-92; listener→frame surface lines 102-135; ws URL shape line 84)
    - app/src/test/java/works/mees/dinghy/net/FakeWebSocket.kt (the fake to inject in tests, from Plan 01)
    - app/src/main/java/works/mees/dinghy/config/DevConfig.kt (wsUrl source, from Plan 01)
  </read_first>
  <behavior>
    - Opening the flow drives onOpen -> emits a SocketEvent.Open; each onMessage -> SocketEvent.Frame(text); onClosing/onFailure -> flow completes (close / close(t)).
    - Cancelling the collecting scope triggers awaitClose -> the underlying socket is cancelled (no leak).
    - The socket factory is injectable: a FakeWebSocket replays golden frames into the same emission path the real OkHttp listener would.
    - send(text) forwards to the underlying WebSocket.send.
  </behavior>
  <action>
    Create `net/MoonrakerSocket.kt`: a `callbackFlow`-based bridge that, given an injected socket-opening
    lambda/factory (NOT a hard-coded `client.newWebSocket(...)` — the one deliberate difference from the
    smoke test), wires a `WebSocketListener` whose `onOpen`/`onMessage`/`onClosing`/`onFailure` map to
    `trySend(SocketEvent.Open/Frame)` / `close()` / `close(t)`, with `awaitClose { ws.cancel() }` for
    structured-concurrency cancellation. Define a sealed `SocketEvent { Open(send fn or ws handle), Frame(text) }`
    and expose a `send(text)` path. Carry forward the smoke test's OkHttp posture — one client for ws+REST,
    `connectTimeout`, `readTimeout(0)` (websockets have no read timeout). Use `DevConfig.wsUrl` for the
    default URL but allow the URL to be supplied (so the `?token=` auth variant in Plan 04 can pass a
    token-bearing URL). Create `MoonrakerSocketTest.kt`: inject the `FakeWebSocket`, collect the flow under
    `runTest`, assert Open then Frame emissions for replayed golden frames, assert `send` is captured, and
    assert cancelling the collection cancels the fake socket (awaitClose ran).
  </action>
  <acceptance_criteria>
    - `gw.bat :app:testDebugUnitTest --tests *MoonrakerSocketTest*` exits 0.
    - Behavior assertion: replaying a golden frame through the injected FakeWebSocket emits a `SocketEvent.Frame` with the exact text.
    - Behavior assertion: cancelling the collecting scope invokes `awaitClose` and cancels the fake socket (no-leak proof).
    - Source assertion: the socket-opening function is a constructor/param-injected lambda (no hard-coded `newWebSocket` in the class body); `readTimeout(0)` posture present for the real client path.
  </acceptance_criteria>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --tests *MoonrakerSocketTest* --no-daemon" | tr -d '\r'</automated>
  </verify>
  <done>OkHttp WebSocket bridged to a cancellable callbackFlow of SocketEvents with an injectable factory (FakeWebSocket-substitutable), send() forwarding, and the websocket timeout posture carried forward — green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: JsonRpcClient — id-correlation under interleaving (STATE-05) + notify routing</name>
  <files>app/src/main/java/works/mees/dinghy/net/JsonRpcClient.kt, app/src/test/java/works/mees/dinghy/net/JsonRpcClientTest.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/net/JsonRpc.kt (envelope types from Plan 01)
    - app/src/main/java/works/mees/dinghy/net/MoonrakerSocket.kt (the frame Flow + send it consumes, from Task 1)
    - .planning/phases/02-connection-state-foundation/02-RESEARCH.md § "Architecture Patterns" Pattern 2 (Map<id,CompletableDeferred>, dispatch split), § "Common Pitfalls" Pitfall 2 (out-of-order correlation), § "Anti-Patterns" (never treat next frame as answer; never conflate gcode), § "Notifications" table (notify_status_update array[2], notify_gcode_response array[1], notify_klippy_* no params)
    - .planning/phases/02-connection-state-foundation/02-CONTEXT.md STATE-05, STATE-03 discretion (gcode stream MUST stay separable/un-throttled)
    - .planning/phases/02-connection-state-foundation/02-PATTERNS.md § "net/JsonRpcClient.kt" (smoke test's id-match + method-route reference; replace CountDownLatch with Map<id,Deferred>+Mutex+AtomicLong)
    - app/src/test/resources/golden/adversarial_interleaved.json, app/src/test/java/works/mees/dinghy/net/FakeWebSocket.kt (the mid-flight injection harness)
  </read_first>
  <behavior>
    - request(method, params) suspends and resolves with the {result} whose id echoes the request id, regardless of how many notify_* frames arrive in between (STATE-05).
    - A notify_status_update injected BETWEEN an objects.query request and its reply does NOT resolve the request — it routes to the status notification SharedFlow.
    - An {error,id} response fails the matching deferred with a typed RpcError (carrying code/message) — e.g. identify {-32602,"Unauthorized"}.
    - Frames with method + no id route to SharedFlows by method; notify_gcode_response routes to a SEPARATE gcode-line SharedFlow (kept distinct from status; un-throttled here).
    - A malformed/unparseable frame is dropped, not fatal.
  </behavior>
  <action>
    Create `net/JsonRpcClient.kt`: owns `pending = Map<Long, CompletableDeferred<JsonElement>>` (guarded by
    a `Mutex`) and an `AtomicLong` id counter. `suspend fun request(method, params): JsonElement` registers a
    deferred under a fresh id, sends `{jsonrpc:"2.0",method,params,id}` via the socket, and awaits the
    deferred (resolved out-of-band by the dispatcher). A `dispatch(text)` (fed by the `MoonrakerSocket`
    frame Flow) splits inbound frames per Pattern 2: `id != null && result` → resolve deferred[id];
    `id != null && error` → fail deferred[id] with a typed `RpcError(code,message)`; `method present, no id`
    → route to notification `SharedFlow`s by method name. Expose at least: a status-notification flow
    (`notify_status_update` `params[0]` diff), a klippy-lifecycle flow (`notify_klippy_ready/shutdown/disconnected`),
    and a SEPARATE un-throttled `notify_gcode_response` line `SharedFlow` (`params[0]` string) — keep the
    gcode stream distinct from status (STATE-03 discretion; no conflation at this layer). Defensive parse
    (lenient shared `Json`, drop bad frames). Create `JsonRpcClientTest.kt` (under `runTest`): drive the
    client off the `FakeWebSocket`, issue an `objects.query`-style `request`, INJECT an interleaved
    `notify_status_update` (from `adversarial_interleaved.json`) BEFORE the matching `{result,id}` reply,
    and assert (a) the request resolves to the correct id-matched result, (b) the interleaved notification
    routed to the status flow and did NOT satisfy the request (STATE-05), (c) an `{error:{-32602}}` fails
    the deferred with the typed error, and (d) a `notify_gcode_response` reaches the separate gcode flow.
  </action>
  <acceptance_criteria>
    - `gw.bat :app:testDebugUnitTest --tests *JsonRpcClientTest*` exits 0.
    - Behavior assertion (STATE-05): with a `notify_status_update` injected between a request and its `{result,id}` reply, `request()` resolves to the id-matched result and the notification is observed on the status flow — proving response matched by id, NOT arrival order.
    - Behavior assertion: an `{error:{code:-32602,message:"Unauthorized"},id}` response fails the matching deferred with a typed RpcError exposing code/message.
    - Behavior assertion: `notify_gcode_response` routes to a SEPARATE flow from `notify_status_update` (gcode stream separable, un-throttled here).
    - Source assertion: correlation uses `Map<Long, CompletableDeferred>` guarded by a Mutex + AtomicLong id; no "await next frame" sequential matching.
  </acceptance_criteria>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --tests *JsonRpcClientTest* --no-daemon" | tr -d '\r'</automated>
  </verify>
  <done>JsonRpcClient correlates responses by id under interleaved notify_* (STATE-05, adversarially proven), routes notifications by method, keeps the gcode line stream separable/un-throttled, and surfaces {error} as typed RpcError without crashing on malformed frames.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Moonraker ws frames → JsonRpcClient | every inbound frame is loose/untrusted-ish JSON crossing into the protocol layer |
| unbounded gcode stream → memory | notify_gcode_response is a log that could grow without bound on 2GB hardware |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Tampering / DoS | Malformed/hostile JSON frame in dispatch() | mitigate | `runCatching { json.parseToJsonElement(text).jsonObject }` drop-on-failure; null-safe walks; never `!!` (ASVS V5) — a bad frame is dropped, not fatal |
| T-02-05 | Denial of Service | Unbounded gcode_response SharedFlow buffer on 2GB | mitigate | Bound the gcode `SharedFlow` replay/backing buffer (extraBufferCapacity / replay cap); Console (Phase 7) caps scrollback (RESEARCH Security Domain) |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan (no new deps); nothing to gate |
</threat_model>

<verification>
- `gw.bat :app:testDebugUnitTest --tests *MoonrakerSocketTest* --tests *JsonRpcClientTest* --no-daemon` exits 0.
- STATE-05 adversarial interleaving proof passes; gcode stream is a separate bounded flow.
</verification>

<success_criteria>
- callbackFlow socket bridge with injectable factory; cancellable; FakeWebSocket-substitutable.
- STATE-05: id-correlation correct under interleaved notify_* (adversarially proven), never next-frame matching.
- Notifications routed by method; gcode_response separable + un-throttled + bounded buffer.
- Both JVM unit tests green.
</success_criteria>

<output>
Create `.planning/phases/02-connection-state-foundation/02-03-SUMMARY.md` when done
</output>
