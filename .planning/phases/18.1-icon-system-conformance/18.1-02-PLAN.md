---
phase: 18.1-icon-system-conformance
plan: 02
type: execute
wave: 1
depends_on: ["18.1-01"]
files_modified:
  - app/src/main/java/works/mees/dinghy/designsystem/icons/DinghyIcons.kt
  - app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusScreen.kt
  - app/src/main/java/works/mees/dinghy/ui/console/ConsoleRowsAdapter.kt
autonomous: true
requirements: [SC-2, SC-3, D-02, D-05, D-06, D-07, D-08, D-09, D-14, D-15]
must_haves:
  truths:
    - "All six registry IconRef.Drawable entries flip to IconRef.Ligature; the only remaining IconRef.Drawable are Nozzle + HeatBed (D-15 amends D-09; D-09 list of four was non-exhaustive)"
    - "StatusOctagon is renamed StatusStop (token + alternate) with a KDoc note citing D-06, because the glyph is now disabled_by_default (square+✕), not an octagon (D-14 amends D-05/D-09); the external call site PrintStatusScreen.kt:971 (DinghyIcons.StatusOctagon → DinghyIcons.StatusStop) is updated in the same atomic rename so the build does not break"
    - "PressureAdvance flips to IconRef.Ligature with the _word suffix (text_select_move_forward_word), not the truncated bucket bookmark (D-08 name-discrepancy resolution)"
    - "The single classic-Views status site (ConsoleRowsAdapter) swaps its two resIds to the official vectors disabled_by_default / warning; render path untouched, THEME-01 tint preserved (D-05, D-07, SC-2 relaxed per D-06)"
    - "The DinghyIconsTest drift-guard (Plan 01) now passes GREEN (D-15, SC-3b); icon-never-twice (iconRef_isUnique) still holds (D-08)"
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/designsystem/icons/DinghyIcons.kt"
      provides: "six drawable→ligature registry flips + StatusOctagon→StatusStop rename"
      contains: "StatusStop"
    - path: "app/src/main/java/works/mees/dinghy/ui/console/ConsoleRowsAdapter.kt"
      provides: "Views console status resId swap to official vectors"
      contains: "disabled_by_default"
  key_links:
    - from: "app/src/main/java/works/mees/dinghy/ui/console/ConsoleRowsAdapter.kt"
      to: "R.drawable.disabled_by_default / R.drawable.warning"
      via: "applySeverityGlyph resId swap + DrawableCompat.setTint (unchanged)"
      pattern: "R\\.drawable\\.(disabled_by_default|warning)"
---

<objective>
Perform the tokenized source flips — the registry-backed (D-09/D-15) and the single classic-Views
(D-07) status surfaces — now that Plan 01 has proven the ligatures resolve and authored the official
vectors. Two surfaces, both touched here because they are the *registry/Views* half of the conversion
(the raw Compose call sites are Plan 03, a disjoint file set, running in parallel):

1. `DinghyIcons.kt` — flip ALL SIX `IconRef.Drawable` entries to `IconRef.Ligature` (D-15: the D-09
   list of "four" was non-exhaustive — Increase/Decrease are also drawable-backed and MUST flip):
   - `BabystepCompress` → `IconRef.Ligature("compress")`
   - `BabystepExpand`   → `IconRef.Ligature("expand")`
   - `StatusOctagon`    → RENAME to `StatusStop`, `IconRef.Ligature("disabled_by_default")`, alternate
     `"status_stop"`, KDoc note citing D-06 (the glyph is now square+✕, no longer an octagon) — D-14.
   - `PressureAdvance`  → `IconRef.Ligature("text_select_move_forward_word")` (KEEP the `_word` suffix).
   - `Decrease`         → `IconRef.Ligature("remove")`
   - `Increase`         → `IconRef.Ligature("add")`
   After the flips the ONLY `IconRef.Drawable` entries are `Nozzle` + `HeatBed` (D-10 keepers) — which
   makes the Plan-01 drift-guard go GREEN.

2. `ConsoleRowsAdapter.kt` — the SINGLE classic-Views status-glyph site. A 2-line resId swap inside
   `applySeverityGlyph`: ERROR `R.drawable.ic_status_octagon` → `R.drawable.disabled_by_default`,
   WARNING `R.drawable.ic_status_triangle` → `R.drawable.warning`. The whole DrawableCompat.setTint +
   setBounds + setCompoundDrawables machinery stays UNTOUCHED (THEME-01 tint preserved; D-07). The new
   official vectors (Plan 01) are KEEPERS the row depends on.

Do NOT delete any drawable in this plan (deletions are Plan 04, gated behind ALL call sites converting,
incl. Plan 03). Do NOT promote raw call sites into the registry (that is Phase 22 — D-09).
Output: a tokenized, ligature-backed registry + a conformant Views console status row.

PRE-FLIGHT: re-run `python tools/verify_ligatures.py` (must be `missing: []`) before flipping — never
flip a registry entry to a ligature the gate hasn't proven resolves.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18.1-icon-system-conformance/18.1-CONTEXT.md
@.planning/phases/18.1-icon-system-conformance/18.1-RESEARCH.md
@.planning/phases/18.1-icon-system-conformance/18.1-PATTERNS.md
@app/src/main/java/works/mees/dinghy/designsystem/icons/DinghyIcons.kt
@app/src/test/java/works/mees/dinghy/designsystem/icons/DinghyIconsTest.kt
</context>

<build_constraint>
DO NOT run `./gradlew` from WSL. Build/test ONLY via the Windows helper:
`/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat <args> --no-daemon"` — pipe CR through `tr -d '\r'`;
exit code is authoritative. Ligature gate: `python tools/verify_ligatures.py` (WSL python). minSdk 23 —
the Views console status glyph CANNOT use a font ligature (`setFontVariationSettings` is API-26+); it
keeps the official-vector + DrawableCompat.setTint path (D-07).
</build_constraint>

<tasks>

<task type="auto">
  <name>Task 1: Flip all six DinghyIcons registry entries to ligatures + rename StatusOctagon→StatusStop (D-14, D-15)</name>
  <read_first>
    - app/src/main/java/works/mees/dinghy/designsystem/icons/DinghyIcons.kt (the six current `IconRef.Drawable(...)` entries at lines ~63-68, 90-91, 93-94; the `all` list at ~102-111; the existing `IconRef.Ligature(...)` shape as the target)
    - .planning/phases/18.1-icon-system-conformance/18.1-PATTERNS.md (§ 2 — exact before/after for all six flips + the D-14 rename rules + "only remaining IconRef.Drawable are Nozzle/HeatBed")
    - .planning/phases/18.1-icon-system-conformance/18.1-RESEARCH.md (§ A4 registry-flip block + A5 icon-never-twice confirmation that compress/expand/add/remove/disabled_by_default/text_select_move_forward_word are each unique in the registry)
    - app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusScreen.kt (the ONLY external consumer of the renamed token: `DinghyIcons.StatusOctagon` at line ~971 inside a DinghyIconView call, with an "octagon stop-status silhouette" comment at ~967 — both must update. Codex-found 2026-06-07; missed by the original call-site grep)
  </read_first>
  <action>
    First re-run `python tools/verify_ligatures.py` and confirm `missing: []`. Then in `DinghyIcons.kt`
    flip these six entries `IconRef.Drawable(...)` → `IconRef.Ligature(...)`, preserving each entry's
    existing `alternate` EXCEPT StatusOctagon:
    - `BabystepCompress` → `IconRef.Ligature("compress")` (alternate "babystep_compress" unchanged)
    - `BabystepExpand`   → `IconRef.Ligature("expand")` (alternate "babystep_expand" unchanged)
    - `PressureAdvance`  → `IconRef.Ligature("text_select_move_forward_word")` — KEEP the `_word` suffix
      (the bucket bookmark `text_select_move_forward` is the stale/wrong name; the font carries `_word`)
    - `Decrease` → `IconRef.Ligature("remove")` (alternate "decrease" unchanged)
    - `Increase` → `IconRef.Ligature("add")` (alternate "increase" unchanged)
    - `StatusOctagon` → RENAME the `val` to `StatusStop`, set `IconRef.Ligature("disabled_by_default")`,
      change `alternate = "status_octagon"` → `alternate = "status_stop"`, and add a KDoc note: the glyph
      is now `disabled_by_default` (square+✕), no longer an octagon — cite D-06/D-14. Update the
      reference to it in the `all` list (StatusOctagon → StatusStop).
    Then update the ONE external consumer (Codex-found 2026-06-07 — the original grep missed it):
    `app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusScreen.kt` line ~971 inside the
    `DinghyIconView(...)` call — `DinghyIcons.StatusOctagon` → `DinghyIcons.StatusStop`; and update the
    stale "octagon stop-status silhouette" comment at ~967 to reflect the square+✕ `disabled_by_default`
    glyph (cite D-06). After this, `grep -rn "StatusOctagon" app/src` MUST return nothing — the Kotlin
    compiler (assembleDebug runs as part of testDebugUnitTest) will red-build on any remaining reference.
    After the flips, the ONLY `IconRef.Drawable` entries remaining are `Nozzle` and `HeatBed` (D-10).
    Remove the now-unused `import works.mees.dinghy.R` ONLY IF no `IconRef.Drawable(R.drawable.*)`
    reference remains (Nozzle/HeatBed still use R.drawable.* — so the import STAYS). Do not delete any
    drawable file (Plan 04).
  </action>
  <verify>
    <automated>cd /mnt/e/claude/personal/github/dinghy-display && python tools/verify_ligatures.py && /mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --tests *DinghyIconsTest --no-daemon" 2>&1 | tr -d '\r' | tee /tmp/gw_18_1_p02t1.log | tail -8 && grep -q "BUILD SUCCESSFUL" /tmp/gw_18_1_p02t1.log</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'IconRef.Ligature("compress")' DinghyIcons.kt` == 1; same for `"expand"`, `"add"`, `"remove"`, `"disabled_by_default"`, `"text_select_move_forward_word"`.
    - `grep -c "StatusStop" DinghyIcons.kt` >= 2 (the `val` + the `all` list entry); `grep -rc "StatusOctagon" app/src` == 0 (NO references anywhere, incl. PrintStatusScreen.kt).
    - `grep -c "DinghyIcons.StatusStop" app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusScreen.kt` == 1 (external call site swapped).
    - `grep -c 'alternate = "status_stop"' DinghyIcons.kt` == 1; `grep -c "status_octagon" DinghyIcons.kt` == 0.
    - `grep -c 'IconRef.Drawable' DinghyIcons.kt` == 2 (only Nozzle + HeatBed).
    - `grep -cE "text_select_move_forward[^_w]" DinghyIcons.kt` == 0 (no truncated name; `_word` retained).
    - `:app:testDebugUnitTest --tests *DinghyIconsTest` passes — including the Plan-01 drift-guard `registryDrawableEntries_areOnlyTheCustomKeepers` (now GREEN), `alternate_isUnique`, and `iconRef_isUnique_acrossAllEntries`.
  </acceptance_criteria>
  <done>Six registry entries are IconRef.Ligature; StatusStop replaces StatusOctagon everywhere (incl. PrintStatusScreen.kt:971 call site + comment); only Nozzle/HeatBed remain drawable-backed; full DinghyIconsTest suite green.</done>
</task>

<task type="auto">
  <name>Task 2: Swap ConsoleRowsAdapter status resIds to the official vectors (D-05, D-07)</name>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/console/ConsoleRowsAdapter.kt (the `applySeverityGlyph` block ~lines 195-221: ERROR `resId = R.drawable.ic_status_octagon` :200, WARNING `resId = R.drawable.ic_status_triangle` :204, and the DrawableCompat.setTint + setBounds + setCompoundDrawables machinery to leave UNTOUCHED)
    - .planning/phases/18.1-icon-system-conformance/18.1-PATTERNS.md (§ 3 — "resId swap ONLY, zero render-path change; the new white-fill vectors honor the existing tint; do NOT attempt ligature-in-TextView")
    - app/src/main/res/drawable/disabled_by_default.xml + warning.xml (the Plan-01 official vectors being referenced)
  </read_first>
  <action>
    In `ConsoleRowsAdapter.applySeverityGlyph`, change ONLY the two `resId` assignments:
    ERROR branch `R.drawable.ic_status_octagon` → `R.drawable.disabled_by_default`; WARNING branch
    `R.drawable.ic_status_triangle` → `R.drawable.warning`. Leave the surrounding `tint = palette?.error
    ?: …` / `palette?.warning ?: …` logic and the entire `DrawableCompat.setTint(d, tint)` +
    `d.setBounds(...)` + `setCompoundDrawables(d, null, null, null)` machinery EXACTLY as-is — the new
    white-fill official vectors recolor identically (THEME-01 preserved). Do NOT convert this site to a
    font ligature (`setFontVariationSettings` is API-26+, below the minSdk-23 floor — D-07 anti-pattern).
    If a header/KDoc comment references `ic_status_octagon`/`ic_status_triangle` by name, update the prose
    to the new vector names. Do NOT delete the old drawables here (Plan 04).
  </action>
  <verify>
    <automated>cd /mnt/e/claude/personal/github/dinghy-display && grep -cE "R\.drawable\.(disabled_by_default|warning)" app/src/main/java/works/mees/dinghy/ui/console/ConsoleRowsAdapter.kt | grep -q '^2$' && grep -cE "R\.drawable\.ic_status_(octagon|triangle)" app/src/main/java/works/mees/dinghy/ui/console/ConsoleRowsAdapter.kt | grep -q '^0$'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "R.drawable.disabled_by_default" ConsoleRowsAdapter.kt` == 1 (ERROR branch).
    - `grep -c "R.drawable.warning" ConsoleRowsAdapter.kt` == 1 (WARNING branch).
    - `grep -cE "R\.drawable\.ic_status_(octagon|triangle)" ConsoleRowsAdapter.kt` == 0.
    - `grep -c "DrawableCompat.setTint" ConsoleRowsAdapter.kt` unchanged from before (render path untouched).
    - `grep -c "setFontVariationSettings" ConsoleRowsAdapter.kt` == 0 (no ligature-in-TextView attempt).
  </acceptance_criteria>
  <done>Console ERROR→disabled_by_default, WARNING→warning resId swap done; tint/setBounds/setCompoundDrawables machinery unchanged; no ligature-in-TextView.</done>
</task>

<task type="auto">
  <name>Task 3: Plan-02 build + unit gate</name>
  <read_first>
    - .planning/phases/18.1-icon-system-conformance/18.1-VALIDATION.md (sampling-rate + build-gate commands)
  </read_first>
  <action>
    Run the Windows-helper build + the DinghyIconsTest to confirm the registry flips and the Console
    resId swap compile and pass together: `:app:assembleDebug` then `:app:testDebugUnitTest --tests
    *DinghyIconsTest`. (Do NOT delete drawables — the old `ic_status_*` / `add` / `remove` etc. still
    exist and are referenced by Plan-03 raw call sites, so a full project delete is NOT done until Plan
    04.) Confirm both green. Record results in the SUMMARY.
  </action>
  <verify>
    <automated>cd /mnt/e/claude/personal/github/dinghy-display && /mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:assembleDebug :app:testDebugUnitTest --tests *DinghyIconsTest --no-daemon" 2>&1 | tr -d '\r' | tee /tmp/gw_18_1_p02t3.log | tail -10 && grep -q "BUILD SUCCESSFUL" /tmp/gw_18_1_p02t3.log</automated>
  </verify>
  <acceptance_criteria>
    - `:app:assembleDebug` exit 0.
    - `:app:testDebugUnitTest --tests *DinghyIconsTest` exit 0 with all four DinghyIconsTest methods green (incl. the now-GREEN drift-guard).
    - SUMMARY records both gate outcomes.
  </acceptance_criteria>
  <done>assembleDebug + DinghyIconsTest green after the registry flips and the Console resId swap.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Source-only change to a static icon registry and one Views adapter's resId assignments. No new network,
auth, input, or data-handling surface.

| Boundary | Description |
|----------|-------------|
| (none new) | No trust boundary is added or moved; icon source selection is a compile-time/static concern |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18.1-03 | Spoofing/Information-disclosure | a11y label leak via ligature name in TalkBack | accept | The registry ligature path already overrides semantics in DinghyIconView (cd-or-decorative); these flips reuse that proven path. No raw ligature name is exposed at the registry-backed call sites. |
| T-18.1-04 | Tampering | Console status glyph swap | accept | resId-only swap into pre-authored, reviewed official vectors; tint/bounds machinery unchanged; no untrusted input reaches the render path. |

No new attack surface: no new I/O, no permissions, no untrusted input, no network, no secrets, no
package installs. No high-severity threats. Block-on-high gate: PASS.
</threat_model>

<verification>
- `python tools/verify_ligatures.py` → `missing: []` (pre-flight before flips).
- DinghyIcons.kt: six entries flipped to ligature; only Nozzle/HeatBed drawable-backed; StatusStop rename complete.
- ConsoleRowsAdapter.kt: two resIds swapped to official vectors; render path untouched.
- `:app:assembleDebug` + `:app:testDebugUnitTest --tests *DinghyIconsTest` green (drift-guard now GREEN).
</verification>

<success_criteria>
- All six registry IconRef.Drawable entries are IconRef.Ligature; remaining drawable-backed = {Nozzle, HeatBed}.
- StatusOctagon renamed StatusStop (token + alternate + KDoc D-06 note); no `StatusOctagon`/`status_octagon` strings remain.
- PressureAdvance ligature keeps the `_word` suffix.
- Console ERROR/WARNING resIds → disabled_by_default / warning; tint machinery unchanged.
- DinghyIconsTest fully green (drift-guard + uniqueness).
</success_criteria>

<output>
Create `.planning/phases/18.1-icon-system-conformance/18.1-02-SUMMARY.md` when done.
</output>
