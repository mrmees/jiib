# Phase 18.1: Icon System Conformance - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-06-07
**Phase:** 18.1-icon-system-conformance
**Areas discussed:** Status shape fidelity, Convert-vs-keep boundary, Font cut & subsetting, Pressure Advance glyph

---

## Area selection

All four offered gray areas were selected for discussion. Three (status shape,
convert-vs-keep, pressure advance) were largely resolved up-front when the owner pointed to
`img/material-icon-bucket.json` — an owner-curated Google Fonts bucket with per-icon usage notes
and locked axes that pre-decides the target glyph for every conversion. The discussion then
collapsed to the genuinely-open decision (font subsetting) plus one consequence-check.

---

## Status shape fidelity

| Option | Description | Selected |
|--------|-------------|----------|
| dangerous + warning | Material Symbols Outlined `dangerous` (octagon+✕) / `warning` (triangle+!) | |
| Bare shape, no inner mark | Bare octagon/triangle, shape+color only | |
| Keep these two hand-traced | Leave octagon/triangle as documented custom exceptions | |
| **(Owner override)** | **`disabled_by_default` for stop, `warning` for caution** (per his JSON icon bucket notes) | ✓ |

**User's choice:** Pointed to his JSON icon file: stop = `disabled_by_default`, caution = `warning`.
**Notes:** `disabled_by_default` is a square+✕, NOT an octagon — see the consequence-check below.

## Convert-vs-keep boundary

**User's choice:** Resolved by `material-icon-bucket.json`. babystep → `compress`/`expand`,
detector → `detector`, locks → `lock`/`lock_open_right`, add/remove → `add`/`remove`, arrows →
`arrow_upward`/`arrow_downward`. Keep custom: nozzle, heat_bed, bed_tilt, benchy (no bucket entry).
**Notes:** The bucket's per-icon `notes` are treated as the spec for which glyph means what.

## Font cut & subsetting

| Option | Description | Selected |
|--------|-------------|----------|
| **Ship full, subset in P22** | Drop in refreshed full variable font now; defer subsetting to Phase 22 ship-hardening | ✓ |
| Subset now from bucket | Build subset tooling this phase, sourced from the bucket superset | |
| Subset now from registry | Subset from `DinghyIcons.all` only (risky — breaks on Phase-22 backfill) | |

**User's choice:** Ship full refreshed font now; subset in Phase 22.
**Notes:** Axes locked by the bucket at FILL 0 / wght 400 / GRAD 0. Current full ttf is ~10.6 MB;
no subset tooling exists yet (the `tools/subset-symbols` in the KDoc is aspirational).

## Pressure Advance glyph

**User's choice:** Resolved by the bucket — `text_select_move_forward` ("pressure advance"). Keep
the glyph; it becomes a font ligature after the refresh.
**Notes:** Drawable is named `text_select_move_forward_word`; bucket bookmark is
`text_select_move_forward`. Execution must confirm the exact ligature name in the refreshed font.

## Consequence-check: status-stop shape

| Option | Description | Selected |
|--------|-------------|----------|
| **Yes, disabled_by_default** | Use square+✕ for stop; relax roadmap SC-2 from "octagon" to "shape-distinct" | ✓ |
| Keep octagon hand-traced | Treat the stop octagon as a custom safety glyph; only convert the triangle | |

**User's choice:** Proceed with `disabled_by_default`; SC-2 wording to be relaxed.
**Notes:** Owner knowingly traded the octagon/stop-sign silhouette for Material-Symbol consistency.
square+✕ vs triangle+! stay shape-distinct in grayscale/CVD, so D-01/D-02 safety coding survives.

## Claude's Discretion

- Exact font provenance/version pinning and the official-vector vs font-ligature split per
  classic-Views call site (status glyphs in the console row); the mechanical KDoc-link fixes.

## Deferred Ideas

- Font subsetting → Phase 22 (build `tools/subset-symbols`, source from the bucket).
- Per-screen icon-call-site tokenization (registry + strings) → Phase 22 deferred backfill.
- Phase-18 UAT Test 6 (en-XA pseudolocale) → out of scope (roadmap).
- Temperature "No heaters" observation → bug, route to `/gsd-debug` or a todo.
- 18 keyword-matched todos reviewed; none concern icon-source conformance — not folded.
