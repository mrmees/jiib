---
phase: 18.1-icon-system-conformance
plan: 02
subsystem: design-system / icons
tags: [icons, material-symbols, ligatures, registry, views-console, wave-1]
requires:
  - tools/verify_ligatures.py (Plan 01 — D-04 ligature gate; must print missing: [] pre-flight)
  - app/src/main/res/drawable/disabled_by_default.xml (Plan 01 — official MS vector, Views ERROR keeper)
  - app/src/main/res/drawable/warning.xml (Plan 01 — official MS vector, Views WARNING keeper)
  - DinghyIconsTest.registryDrawableEntries_areOnlyTheCustomKeepers (Plan 01 — drift-guard, RED until this plan)
provides:
  - "DinghyIcons registry: six IconRef.Drawable entries flipped to IconRef.Ligature (only Nozzle/HeatBed remain drawable-backed)"
  - "StatusStop token (renamed from StatusOctagon; disabled_by_default ligature)"
  - "ConsoleRowsAdapter status rows on the official disabled_by_default / warning vectors"
  - "DinghyIconsTest drift-guard now GREEN"
affects:
  - Plan 18.1-03 (raw Compose call-site swaps — disjoint file set, runs in parallel)
  - Plan 18.1-04 (drawable deletions — gated behind ALL call sites converting, incl. Plan 03)
tech-stack:
  added: []
  patterns:
    - "registry-backed icon source flip (Drawable→Ligature) proven by the device-free ligature gate"
    - "resId-only swap into pre-authored official vectors for the one minSdk-23 Views status site (no ligature-in-TextView)"
key-files:
  created: []
  modified:
    - app/src/main/java/works/mees/dinghy/designsystem/icons/DinghyIcons.kt
    - app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusScreen.kt
    - app/src/main/java/works/mees/dinghy/ui/console/ConsoleRowsAdapter.kt
decisions:
  - "D-14: StatusOctagon renamed StatusStop — the disabled_by_default glyph is a square+✕, not a literal octagon; safety carried by shape distinctness (D-06)"
  - "D-15: the six-entry flip set INCLUDES Increase/Decrease (the D-09 'four' was non-exhaustive); only Nozzle + HeatBed remain IconRef.Drawable"
  - "D-08: PressureAdvance ligature retains the _word suffix (text_select_move_forward_word); the truncated bucket bookmark name is stale"
  - "D-07: the Views console status site keeps the official-vector + DrawableCompat.setTint path (setFontVariationSettings is API-26+, below minSdk 23) — no ligature-in-TextView"
metrics:
  duration: ~6 min
  completed: 2026-06-07
---

# Phase 18.1 Plan 02: Registry & Views Source Flips Summary

Flipped all six remaining `IconRef.Drawable` registry entries to `IconRef.Ligature`, renamed the stop-status token `StatusOctagon`→`StatusStop`, and swapped the single classic-Views console status site onto the official Plan-01 vectors — turning the Plan-01 drift-guard GREEN without deleting any drawable. `assembleDebug` + the full `DinghyIconsTest` suite are green.

## What Was Built

**Task 1 — Six registry flips + StatusOctagon→StatusStop rename (D-09/D-14/D-15).** Pre-flight `python tools/verify_ligatures.py` re-confirmed `48 needed, 3953 ligatures in font, missing: []` (never flip a registry entry to an unproven ligature). In `DinghyIcons.kt` the six drawable-backed entries flipped to ligatures:
- `BabystepCompress` → `IconRef.Ligature("compress")`
- `BabystepExpand`   → `IconRef.Ligature("expand")`
- `PressureAdvance`  → `IconRef.Ligature("text_select_move_forward_word")` (the `_word` suffix retained; D-08)
- `Decrease`         → `IconRef.Ligature("remove")`
- `Increase`         → `IconRef.Ligature("add")`
- `StatusOctagon`    → RENAMED `StatusStop`, `IconRef.Ligature("disabled_by_default")`, `alternate = "status_stop"`, KDoc note citing D-06/D-14 (the glyph is a square+✕, not an octagon — safety is carried by shape distinctness). The `all` list entry updated `StatusOctagon`→`StatusStop`.

After the flips the ONLY `IconRef.Drawable` entries are `Nozzle` + `HeatBed` (D-10 keepers), so the Plan-01 drift-guard `registryDrawableEntries_areOnlyTheCustomKeepers` goes GREEN. The single external consumer — `PrintStatusScreen.kt:971` inside the `DinghyIconView(...)` E-stop tile — was updated `DinghyIcons.StatusOctagon`→`DinghyIcons.StatusStop`, and the stale "octagon stop-status silhouette" comment at ~967 was reworded to the square+✕ `disabled_by_default` glyph (cite D-06). The `import works.mees.dinghy.R` STAYS (Nozzle/HeatBed still use `R.drawable.*`). No drawable file deleted (Plan 04).

`grep -rc "StatusOctagon" app/src` returns NONE; the only residual mention is reworded prose ("the former octagon token, renamed in 18.1") — the literal `StatusOctagon` token appears nowhere.

**Task 2 — ConsoleRowsAdapter resId swap (D-05/D-07).** The single classic-Views status-glyph site `applySeverityGlyph`: ERROR `R.drawable.ic_status_octagon` → `R.drawable.disabled_by_default`; WARNING `R.drawable.ic_status_triangle` → `R.drawable.warning`. The entire `tint = palette?.error/warning ?: …` logic + `DrawableCompat.setTint` + `setBounds` + `setCompoundDrawables` machinery is UNTOUCHED (THEME-01 tint preserved; the new white-fill official vectors recolor identically). No `setFontVariationSettings` / ligature-in-TextView (API-26+ is below the minSdk-23 floor, D-07). No KDoc/prose referenced the old vector names, so no prose update was needed.

**Task 3 — Plan-02 build + unit gate.** `:app:assembleDebug` + `:app:testDebugUnitTest --tests *DinghyIconsTest` (Windows helper) → `BUILD SUCCESSFUL`, all four DinghyIconsTest methods green (incl. the now-GREEN drift-guard, plus `alternate_isUnique` and `iconRef_isUnique`). No drawables deleted — the old `ic_status_*` / `add` / `remove` / `text_select_move_forward_word` vectors still exist and are referenced by Plan-03 raw call sites, so a project-wide delete is deferred to Plan 04.

## Gate Outcomes

- `python tools/verify_ligatures.py` → `48 needed, 3953 ligatures in font, missing: []`, exit 0. ✅ (pre-flight, before any flip)
- Task 1: `:app:testDebugUnitTest --tests *DinghyIconsTest` → `BUILD SUCCESSFUL in 58s`. ✅
- Task 3: `:app:assembleDebug :app:testDebugUnitTest --tests *DinghyIconsTest` → `BUILD SUCCESSFUL in 58s`. ✅

## Acceptance Greps

- `grep -c 'IconRef.Ligature("compress")'` … each of compress/expand/add/remove/disabled_by_default/text_select_move_forward_word == 1 ✅
- `grep -c "StatusStop" DinghyIcons.kt` == 3 (val + KDoc heading + all-list) ≥ 2 ✅; `grep -rc "StatusOctagon" app/src` == 0 ✅
- `grep -c "DinghyIcons.StatusStop" PrintStatusScreen.kt` == 1 ✅
- `grep -c 'alternate = "status_stop"' DinghyIcons.kt` == 1 ✅; `grep -c "status_octagon" DinghyIcons.kt` == 0 ✅
- `grep -c 'IconRef.Drawable' DinghyIcons.kt` == 2 (Nozzle + HeatBed) ✅
- `grep -cE "text_select_move_forward[^_]" DinghyIcons.kt` == 0 (`_word` retained) ✅
- ConsoleRowsAdapter: `disabled_by_default|warning` count == 2 ✅; `ic_status_(octagon|triangle)` == 0 ✅; `setFontVariationSettings` == 0 ✅; `DrawableCompat.setTint` count unchanged (1) ✅

## Deviations from Plan

**None affecting behavior.** One micro-wording choice worth recording (not a plan departure):

- **KDoc historical reference.** The plan's acceptance criterion is `grep -rc "StatusOctagon" app/src == 0` (strict zero). The new StatusStop KDoc originally read "(formerly `StatusOctagon`)"; that literal token would have left a single doc-only hit. Reworded to "the former octagon token, renamed in 18.1" so the criterion holds exactly while preserving the historical-rename note. Code semantics unchanged.

## Threat Flags

None. Source-only change to a static icon registry + one Views adapter's resId assignments — no new network, auth, input, or data surface (matches the plan threat_model: T-18.1-03/04 both `accept`). No package installs.

## Known Stubs

None. No empty/placeholder data introduced; both surfaces render real glyphs.

## Self-Check: PASSED

- FOUND: app/src/main/java/works/mees/dinghy/designsystem/icons/DinghyIcons.kt (StatusStop, six ligature flips, IconRef.Drawable count 2)
- FOUND: app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusScreen.kt (DinghyIcons.StatusStop call site)
- FOUND: app/src/main/java/works/mees/dinghy/ui/console/ConsoleRowsAdapter.kt (disabled_by_default + warning resIds)
- FOUND commit b37b3bd (Task 1 — registry flips + rename)
- FOUND commit 793e391 (Task 2 — console resId swap)
- DinghyIconsTest drift-guard GREEN; assembleDebug GREEN
