---
phase: 18.1-icon-system-conformance
plan: 04
subsystem: design-system / icons
tags: [icons, material-symbols, drawable-deletion, kdoc, on-device-uat, wave-2]
requires:
  - phase: 18.1-02
    provides: "registry ligature flips + Console Views official vectors — converted the registry-routed call sites for the now-deleted drawables"
  - phase: 18.1-03
    provides: "raw Compose call-site swaps (ProbeCalibrate/Move/ThemeEditor) — converted the last non-registry call sites, unblocking these deletions"
provides:
  - "13 superseded hand-traced/redundant drawables DELETED (D-08, SC-3); compiler (assembleDebug) proves zero dangling R.drawable.* references remain"
  - "7 keepers survive: nozzle, heat_bed, bed_tilt, benchy, ic_launcher_foreground (D-10 customs) + disabled_by_default, warning (Plan-01 official vectors)"
  - "5 broken KDoc doc-links fixed across the 3 preview files (D-11 as relaxed/amended by D-16; the Codex-found PrintStatusPreviews.kt:35 [AppContainer] de-bracket)"
  - "On-device icon spot check PASSED on flox — every converted ligature renders (no tofu), status shapes shape-distinct (D-04 / SC-2 relaxed per D-06)"
affects:
  - "Phase 22 (Ship) — icon system is now source-conformant app-wide; the registry/string-extraction backfill remains Phase-22 scope (D-09)"
  - "18.2 (jiib Rebrand) / 18.3 (Color-Reactive Spool Icon) — clean drawable surface to build on"
tech-stack:
  added: []
  patterns:
    - "compiler-as-completeness-gate: every deleted drawable is referenced via a typed R.drawable.<name>, so a missed call site fails :app:assembleDebug loudly — the delete is provably safe only when the build stays green"
key-files:
  created: []
  modified:
    - app/src/main/java/works/mees/dinghy/preview/PrintStatusPreviews.kt
    - app/src/main/java/works/mees/dinghy/preview/FineTunePreviews.kt
    - app/src/main/java/works/mees/dinghy/preview/SpoolPreviews.kt
  deleted:
    - app/src/main/res/drawable/ic_status_octagon.xml
    - app/src/main/res/drawable/ic_status_triangle.xml
    - app/src/main/res/drawable/ic_babystep_compress.xml
    - app/src/main/res/drawable/ic_babystep_expand.xml
    - app/src/main/res/drawable/detector.xml
    - app/src/main/res/drawable/ic_lock_closed.xml
    - app/src/main/res/drawable/ic_lock_open.xml
    - app/src/main/res/drawable/add.xml
    - app/src/main/res/drawable/remove.xml
    - app/src/main/res/drawable/arrow_upward.xml
    - app/src/main/res/drawable/arrow_downward.xml
    - app/src/main/res/drawable/expand.xml
    - app/src/main/res/drawable/text_select_move_forward_word.xml
key-decisions:
  - "Deleted exactly the 13 superseded drawables; explicitly KEPT the 7 keepers (5 D-10 customs + 2 Plan-01 official vectors) — no keeper touched (T-18.1-08 mitigated)"
  - "Fixed FIVE KDoc links, not four: the four token renames ([DinghyPreviews]->[Nexus7Previews] x3, [PrintStatusThemeMatrix]->[PrintStatusStateMatrix] x1) plus the Codex-found PrintStatusPreviews.kt:35 [AppContainer] de-bracket (D-16 amendment — that one file has no AppContainer import so the link was genuinely unresolved; other [AppContainer] links resolve and were left untouched)"
  - "Font v2.944 retained (D-13) — no font refresh; the bundled binary already carries every needed glyph (verified by verify_ligatures.py: missing [])"
patterns-established:
  - "Wave-2 close-the-phase ordering: delete a drawable ONLY after every call site is converted (depends_on 18.1-02 + 18.1-03); the green assembleDebug is the proof, not a manual grep alone"

requirements-completed: [SC-3, SC-4, SC-5, D-08, D-10, D-11, D-12, D-16]

# Metrics
duration: continuation close-out (autonomous tasks ~prior session; on-device gate owner-approved 2026-06-07)
completed: 2026-06-07
---

# Phase 18.1 Plan 04: Drawable Deletions + KDoc Fixes + On-Device Spot Check Summary

**Closed the icon-system-conformance phase: deleted the 13 superseded hand-traced drawables (assembleDebug proves zero dangling refs), fixed 5 broken KDoc doc-links across the 3 preview files, kept font v2.944, ran the full no-regression gate green, and the owner approved the on-device icon render spot check on flox (no tofu, status shapes distinct).**

## Performance

- **Duration:** continuation close-out (the two autonomous tasks were executed + committed in the prior session; this pass writes the SUMMARY after the owner approved the blocking on-device gate)
- **Completed:** 2026-06-07
- **Tasks:** 3 (2 autonomous + 1 on-device human-verify gate — all complete/PASSED)
- **Files:** 16 modified/deleted (13 drawables deleted + 3 preview files edited)

## Accomplishments

- **13 superseded drawables deleted (D-08, SC-3)** — `ic_status_octagon`, `ic_status_triangle`, `ic_babystep_compress`, `ic_babystep_expand`, `detector`, `ic_lock_closed`, `ic_lock_open`, `add`, `remove`, `arrow_upward`, `arrow_downward`, `expand`, `text_select_move_forward_word`. The compiler is the completeness lever: every deleted drawable was referenced via a typed `R.drawable.<name>`, so `:app:assembleDebug` staying green proves no call site was missed (T-18.1-07 mitigated).
- **7 keepers preserved (D-10)** — `nozzle`, `heat_bed`, `bed_tilt`, `benchy`, `ic_launcher_foreground` (the printer-domain customs + OS-registered launcher) and `disabled_by_default`, `warning` (the Plan-01 official Material vectors). Verified still present on disk; the launcher keeper's loss would have failed install (T-18.1-08 mitigated).
- **5 KDoc doc-links fixed (D-11 / D-16, SC-4)** — `[DinghyPreviews]->[Nexus7Previews]` x3 (PrintStatusPreviews/FineTunePreviews/SpoolPreviews), `[PrintStatusThemeMatrix]->[PrintStatusStateMatrix]` x1, plus the Codex-found `PrintStatusPreviews.kt:35 [AppContainer]` de-bracket (that file has no `AppContainer` import → the link was genuinely unresolved; the prose is descriptive "no AppContainer, no socket", so dropping the brackets clears the warning without an unused import). Other resolving `[AppContainer]` links left untouched.
- **Full no-regression gate GREEN (D-12, SC-5)** — `tools/verify_ligatures.py` → `missing: []`, exit 0; `:app:assembleDebug` + full `:app:testDebugUnitTest` → BUILD SUCCESSFUL via the Windows helper. Font v2.944 retained (D-13, no refresh needed).
- **On-device spot check PASSED (D-04 / SC-2 relaxed per D-06)** — the post-deletion debug APK was built, installed on flox (LineageOS 18.1 / API 30, genuine Adreno 320, device `0a64b42e`), and launched. The owner walked the checks (babystep/pressure-advance/increase-decrease glyphs, ProbeCalibrate detector/expand/jog glyphs, Move caution-triangle + force-move lock, Console ERROR square+✕ vs WARNING triangle+! grayscale-distinct, ThemeEditor stop/caution swatches) and reported **"Looks good"** — all glyphs render with no tofu and the status shapes are shape-distinct. This is a PASS, not a defer.

## Task Commits

1. **Task 1: Delete the 13 superseded drawables + compiler completeness gate (D-08, SC-3)** — `5f69f29` (chore)
2. **Task 2: Fix the broken KDoc doc-links (5 edits) + full no-regression gate (D-11/D-16, D-12, SC-4, SC-5)** — `dffd7ef` (docs)
3. **Task 3: On-device icon spot check (D-04, SC-2 relaxed per D-06)** — no commit (blocking `checkpoint:human-verify`); **owner-approved on flox 2026-06-07 ("Looks good")**

**Plan metadata:** this SUMMARY + STATE/ROADMAP (docs: complete plan)

## Files Created/Modified

**Deleted (13 drawables):**
- `app/src/main/res/drawable/{ic_status_octagon,ic_status_triangle,ic_babystep_compress,ic_babystep_expand,detector,ic_lock_closed,ic_lock_open,add,remove,arrow_upward,arrow_downward,expand,text_select_move_forward_word}.xml` — superseded hand-traced/redundant drawables whose call sites were all converted to Material Symbols ligatures (Plans 02 + 03) or official vectors (Plan 01).

**Modified (3 preview files — KDoc only):**
- `app/src/main/java/works/mees/dinghy/preview/PrintStatusPreviews.kt` — 3 fixes: `[DinghyPreviews]->[Nexus7Previews]`, `[PrintStatusThemeMatrix]->[PrintStatusStateMatrix]`, `[AppContainer]->AppContainer` (de-bracket).
- `app/src/main/java/works/mees/dinghy/preview/FineTunePreviews.kt` — 1 fix: `[DinghyPreviews]->[Nexus7Previews]`.
- `app/src/main/java/works/mees/dinghy/preview/SpoolPreviews.kt` — 1 fix: `[DinghyPreviews]->[Nexus7Previews]`.

**Kept (7 keepers, unchanged):** `nozzle.xml`, `heat_bed.xml`, `bed_tilt.xml`, `benchy.xml`, `ic_launcher_foreground.xml`, `disabled_by_default.xml`, `warning.xml`.

## Gate Outcomes

- `python tools/verify_ligatures.py` → `missing: []`, exit 0 ✅
- `:app:assembleDebug` (Windows helper) → BUILD SUCCESSFUL — compiler completeness check: no dangling `R.drawable.*` after the 13 deletes ✅
- `:app:testDebugUnitTest` (full suite, Windows helper) → BUILD SUCCESSFUL ✅
- Grep gate: no live `R.drawable.{deleted names}` references; no `[DinghyPreviews]`/`[PrintStatusThemeMatrix]` remain; `PrintStatusPreviews.kt` has 0 `[AppContainer]` ✅
- On-device flox spot check → owner "Looks good" (glyphs render, status shapes distinct) ✅

## Font Provenance (D-13)

The bundled Material Symbols font binary is **v2.944**, RETAINED (no refresh). Research inspected the actual font with fonttools and confirmed it already carries every glyph the conversion needs; `verify_ligatures.py` re-proves this each run (`missing: []`). The original D-01 "refresh the font" intent was relaxed to retain-v2.944-and-verify per the post-research owner amendments.

## Decisions Made

- **Five KDoc fixes, not four.** The plan's original D-16 amendment exempted `[AppContainer]` links as "not broken," but Codex's final review found PrintStatusPreviews.kt:35 is the exception (no import in that file). De-bracketing (rather than adding an unused import) clears the unresolved-link warning while keeping the descriptive prose intact.
- **Font v2.944 kept (D-13)** — no binary churn; the gate proves sufficiency.

## Deviations from Plan

None affecting behavior. The "5th" KDoc fix (PrintStatusPreviews.kt:35 `[AppContainer]` de-bracket) is the plan-specified Codex/D-16 amendment, not an unplanned deviation — it is enumerated in the plan's Task 2 action.

## Issues Encountered

None. Both autonomous tasks executed clean; all gates green on first run. The blocking on-device gate was the expected pause point (autonomous: false) and was owner-approved on flox.

## User Setup Required

None — no external service configuration required. (Asset deletion + KDoc text edits only; no new network/auth/input/data surface — matches the plan threat_model, both threats `mitigate` and addressed.)

## Threat Flags

None. The plan introduces no new attack surface: it only removes static assets and edits doc comments.
- **T-18.1-07 (missing-glyph tofu DoS):** typed `R.drawable.*` references make any missed site a compile error; `:app:assembleDebug` is the completeness gate; the on-device spot check is the final confirmation no glyph renders as tofu — all passed. ✅
- **T-18.1-08 (accidental keeper deletion / tampering):** the 7 keepers verified present on disk; the OS-registered launcher icon survives (install would have failed otherwise). ✅

## Known Stubs

None. Every former drawable call site now renders a real Material Symbol (ligature or official vector) or a retained D-10 custom; no placeholder/empty data paths introduced.

## Self-Check: PASSED

- FOUND commit `5f69f29` (Task 1 — 13 drawables deleted)
- FOUND commit `dffd7ef` (Task 2 — 5 KDoc fixes)
- VERIFIED: all 13 deletion targets absent from `app/src/main/res/drawable/`
- VERIFIED: all 7 keepers (nozzle, heat_bed, bed_tilt, benchy, ic_launcher_foreground, disabled_by_default, warning) present on disk
- VERIFIED: 3 preview files modified in `dffd7ef`
- :app:assembleDebug + :app:testDebugUnitTest + verify_ligatures GREEN (recorded above)
- On-device flox spot check owner-approved ("Looks good")

## Next Phase Readiness

- **Phase 18.1 (icon-system-conformance) is execution-complete (4/4 plans).** The icon system is source-conformant app-wide: every icon is a real Material Symbol (ligature or official vector) except the D-10 printer-domain customs (nozzle/heat_bed/bed_tilt/benchy) + the launcher. No superseded drawable remains, no dangling reference, no broken KDoc link.
- Registry promotion / string-extraction backfill remains **Phase 22 (Ship)** scope (D-09) — intentionally deferred, not a gap.
- Clean drawable surface is ready for **18.2 (jiib Rebrand)** and **18.3 (Color-Reactive Spool Icon)**.
- Orchestrator runs phase verification next (do NOT mark the phase complete here).

---
*Phase: 18.1-icon-system-conformance*
*Completed: 2026-06-07*
