---
phase: 18.1-icon-system-conformance
plan: 04
type: execute
wave: 2
depends_on: ["18.1-02", "18.1-03"]
files_modified:
  - app/src/main/res/drawable/ic_status_octagon.xml
  - app/src/main/res/drawable/ic_status_triangle.xml
  - app/src/main/res/drawable/ic_babystep_compress.xml
  - app/src/main/res/drawable/ic_babystep_expand.xml
  - app/src/main/res/drawable/detector.xml
  - app/src/main/res/drawable/ic_lock_closed.xml
  - app/src/main/res/drawable/ic_lock_open.xml
  - app/src/main/res/drawable/add.xml
  - app/src/main/res/drawable/remove.xml
  - app/src/main/res/drawable/arrow_upward.xml
  - app/src/main/res/drawable/arrow_downward.xml
  - app/src/main/res/drawable/expand.xml
  - app/src/main/res/drawable/text_select_move_forward_word.xml
  - app/src/main/java/works/mees/dinghy/preview/PrintStatusPreviews.kt
  - app/src/main/java/works/mees/dinghy/preview/FineTunePreviews.kt
  - app/src/main/java/works/mees/dinghy/preview/SpoolPreviews.kt
autonomous: false
requirements: [SC-3, SC-4, SC-5, D-08, D-10, D-11, D-12, D-16]
must_haves:
  truths:
    - "The 13 superseded hand-traced/redundant drawables are deleted; the compiler (assembleDebug) proves no dangling R.drawable.* reference remains (SC-3, D-08)"
    - "The D-10 custom keepers (nozzle, heat_bed, bed_tilt, benchy, ic_launcher_foreground) and the two new official vectors (disabled_by_default, warning) survive (D-10)"
    - "The two broken KDoc doc-links are fixed: [DinghyPreviews]->[Nexus7Previews] (x3) and [PrintStatusThemeMatrix]->[PrintStatusStateMatrix] (x1); [AppContainer] left untouched (D-16 amends D-11, D-11 relaxed by D-16)"
    - "No regression: full :app:assembleDebug + :app:testDebugUnitTest green; icon-source policy enforced app-wide (D-12)"
    - "On-device spot check confirms shape-distinct status glyphs and the converted ligatures render without tofu (SC-2 relaxed per D-06, D-04)"
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/preview/PrintStatusPreviews.kt"
      provides: "fixed KDoc doc-links"
      contains: "Nexus7Previews"
  key_links:
    - from: ":app:assembleDebug"
      to: "deleted R.drawable.* names"
      via: "compiler completeness check (a missed call site = red build)"
      pattern: "R.drawable"
---

<objective>
Close the phase: delete every superseded drawable now that ALL its call sites have been converted
(Plans 02 + 03), fix the two broken KDoc doc-links, and run the full no-regression gate plus the on-device
spot check. This plan MUST run AFTER 02 and 03 (it depends on both) — deleting a drawable before its call
sites are converted would be a red build. The compiler is the completeness lever: every deleted drawable
is referenced via a typed R.drawable.<name>, so a missed call site fails assembleDebug loudly (RESEARCH
"Completeness guarantee").

Work:
1. Delete the 13 superseded drawables (D-08): ic_status_octagon, ic_status_triangle, ic_babystep_compress,
   ic_babystep_expand, detector, ic_lock_closed, ic_lock_open, add, remove, arrow_upward, arrow_downward,
   expand, text_select_move_forward_word.
   KEEP (do NOT delete — D-10 customs + Plan-01 official vectors): nozzle, heat_bed, bed_tilt, benchy,
   ic_launcher_foreground, disabled_by_default, warning.
2. Fix the two broken KDoc doc-links (D-11 as relaxed by D-16 — [AppContainer] is NOT broken, leave it):
   PrintStatusPreviews.kt:20 [DinghyPreviews]->[Nexus7Previews]; PrintStatusPreviews.kt:26
   [PrintStatusThemeMatrix]->[PrintStatusStateMatrix]; FineTunePreviews.kt:24 [DinghyPreviews]->[Nexus7Previews];
   SpoolPreviews.kt:24 [DinghyPreviews]->[Nexus7Previews]. (4 edits across 3 files.)
3. Full no-regression gate (D-12): :app:assembleDebug + :app:testDebugUnitTest green; verify_ligatures.py
   green; grep proves no dangling deleted-drawable refs and no remaining broken KDoc links.
4. On-device spot check (D-04 / SC-2 relaxed per D-06) — a blocking human-verify checkpoint.

Output: a conformant icon system, clean KDoc, green build/test/ligature gates, on-device confirmation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18.1-icon-system-conformance/18.1-CONTEXT.md
@.planning/phases/18.1-icon-system-conformance/18.1-RESEARCH.md
@.planning/phases/18.1-icon-system-conformance/18.1-PATTERNS.md
@.planning/phases/18.1-icon-system-conformance/18.1-VALIDATION.md
</context>

<build_constraint>
DO NOT run ./gradlew from WSL. Build/test ONLY via the Windows helper:
/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat <args> --no-daemon" — pipe CR through tr -d '\r';
exit code is authoritative. Ligature gate: python tools/verify_ligatures.py. On-device install/spot-check:
the release/debug APK on flox (LineageOS 18.1 / API 30) or against an Ender Moonraker; force a console
ERROR/WARNING row via the Moonraker RESPOND broadcast trick (memory dinghy-stream-uat-via-moonraker).
minSdk 23 / Compose BOM 2026.05.
</build_constraint>

<tasks>

<task type="auto">
  <name>Task 1: Delete the 13 superseded drawables + compiler completeness gate (D-08, SC-3)</name>
  <read_first>
    - .planning/phases/18.1-icon-system-conformance/18.1-PATTERNS.md (the Deletions list + the KEEPERS list — do NOT delete nozzle/heat_bed/bed_tilt/benchy/ic_launcher_foreground/disabled_by_default/warning)
    - .planning/phases/18.1-icon-system-conformance/18.1-RESEARCH.md (Runtime State Inventory — "a leftover reference = red build"; the per-row drawable disposition table)
  </read_first>
  <action>
    Confirm Plans 02 and 03 are complete (their SUMMARYs exist and their gates were green). Then grep the
    whole app/src for each of the 13 deletion-target names to confirm NO remaining R.drawable.<name>
    reference exists (the only allowed survivors are the NEW official vectors disabled_by_default/warning,
    which are different names). For each of the 13 confirmed-unreferenced drawables, delete the file:
    ic_status_octagon.xml, ic_status_triangle.xml, ic_babystep_compress.xml, ic_babystep_expand.xml,
    detector.xml, ic_lock_closed.xml, ic_lock_open.xml, add.xml, remove.xml, arrow_upward.xml,
    arrow_downward.xml, expand.xml, text_select_move_forward_word.xml. Do NOT delete nozzle.xml, heat_bed.xml,
    bed_tilt.xml, benchy.xml, ic_launcher_foreground.xml, disabled_by_default.xml, warning.xml. If a grep
    finds a lingering reference, STOP and convert that call site first (do not delete with a live reference).
    Then run :app:assembleDebug via the Windows helper — the compiler proves completeness (any missed call
    site is now a red build). assembleDebug MUST be green.
  </action>
  <verify>
    <automated>cd /mnt/e/claude/personal/github/dinghy-display && grep -rEc "R[.]drawable[.](ic_status_octagon|ic_status_triangle|ic_babystep_compress|ic_babystep_expand|detector|ic_lock_closed|ic_lock_open|add|remove|arrow_upward|arrow_downward|expand|text_select_move_forward_word)\b" app/src | grep -vE ':0$' | grep -q . && echo FOUND_DANGLING && exit 1; /mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:assembleDebug --no-daemon" 2>&1 | tr -d '\r' | tee /tmp/gw_18_1_p04t1.log | tail -6 && grep -q "BUILD SUCCESSFUL" /tmp/gw_18_1_p04t1.log
  </verify>
  <acceptance_criteria>
    - None of the 13 deleted drawable files exist under app/src/main/res/drawable/.
    - The 7 keepers (nozzle, heat_bed, bed_tilt, benchy, ic_launcher_foreground, disabled_by_default, warning) still exist.
    - grep -rE "R[.]drawable[.](ic_status_octagon|...|text_select_move_forward_word)" app/src returns no live references (only the new disabled_by_default/warning names remain, in ConsoleRowsAdapter + ThemeEditor + MoveScreen as ligature/vector targets).
    - :app:assembleDebug via the Windows helper exits 0 (compiler completeness check passes).
  </acceptance_criteria>
  <done>13 drawables deleted; 7 keepers survive; no dangling R.drawable.* references; assembleDebug green.</done>
</task>

<task type="auto">
  <name>Task 2: Fix the broken KDoc doc-links (5 edits incl. the Codex-found AppContainer) + full no-regression gate (D-11/D-16, D-12, SC-4, SC-5)</name>
  <read_first>
    - app/src/main/java/works/mees/dinghy/preview/PrintStatusPreviews.kt (:20 [DinghyPreviews], :26 [PrintStatusThemeMatrix]; PrintStatusStateMatrix fun at :57; AND :35 the genuinely-unresolved [AppContainer] in prose — this file has NO `import ...di.AppContainer`)
    - app/src/main/java/works/mees/dinghy/preview/FineTunePreviews.kt (:24 [DinghyPreviews])
    - app/src/main/java/works/mees/dinghy/preview/SpoolPreviews.kt (:24 [DinghyPreviews])
    - .planning/phases/18.1-icon-system-conformance/18.1-PATTERNS.md (section 7 KDoc edits table; Nexus7Previews is the annotation at preview/DinghyPreviews.kt:32)
    - .planning/phases/18.1-icon-system-conformance/18.1-RESEARCH.md (A6 — [AppContainer] resolves where imported; the PrintStatusPreviews.kt:35 instance is the EXCEPTION, Codex-found 2026-06-07: no import in that file → unresolved. D-16 amended.)
  </read_first>
  <action>
    Make FIVE KDoc fixes. D-16 originally exempted all [AppContainer] links as "not broken", but Codex
    found ONE genuine exception (PrintStatusPreviews.kt:35 has no AppContainer import → the link is
    unresolved). The four token-link renames plus that one:
    - PrintStatusPreviews.kt:20  [DinghyPreviews] -> [Nexus7Previews]
    - PrintStatusPreviews.kt:26  [PrintStatusThemeMatrix] -> [PrintStatusStateMatrix]
    - PrintStatusPreviews.kt:35  [AppContainer] -> AppContainer (remove the link brackets — it is
      descriptive prose "no AppContainer, no socket", NOT a real cross-reference; dropping the brackets
      clears the unresolved-link warning without adding an unused import). Other [AppContainer] links
      elsewhere DO resolve (imports present per A6) — leave those untouched.
    - FineTunePreviews.kt:24     [DinghyPreviews] -> [Nexus7Previews]
    - SpoolPreviews.kt:24        [DinghyPreviews] -> [Nexus7Previews]
    Re-grep app/src to confirm no [DinghyPreviews] or [PrintStatusThemeMatrix] remain, and that
    PrintStatusPreviews.kt no longer contains [AppContainer]. Then run the full
    no-regression gate: python tools/verify_ligatures.py (missing: []); :app:assembleDebug AND
    :app:testDebugUnitTest (full unit suite, not just DinghyIconsTest) via the Windows helper — all green.
    Record all gate outcomes + the font provenance carry-forward in the SUMMARY.
  </action>
  <verify>
    <automated>cd /mnt/e/claude/personal/github/dinghy-display && grep -rEc "\[DinghyPreviews\]|\[PrintStatusThemeMatrix\]" app/src | grep -vE ':0$' | grep -q . && echo STALE_KDOC && exit 1; python tools/verify_ligatures.py && /mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:assembleDebug :app:testDebugUnitTest --no-daemon" 2>&1 | tr -d '\r' | tee /tmp/gw_18_1_p04t2.log | tail -10 && grep -q "BUILD SUCCESSFUL" /tmp/gw_18_1_p04t2.log
  </verify>
  <acceptance_criteria>
    - grep -rE "\[DinghyPreviews\]|\[PrintStatusThemeMatrix\]" app/src returns nothing (SC-4).
    - grep -c "Nexus7Previews" across the 3 preview files >= 3; grep -c "PrintStatusStateMatrix" PrintStatusPreviews.kt >= 1 (the doc-link plus the fun definition).
    - `grep -c "\[AppContainer\]" PrintStatusPreviews.kt` == 0 (the one unresolved prose link de-bracketed); [AppContainer] links in OTHER files unchanged (those resolve — A6).
    - python tools/verify_ligatures.py exits 0 (missing: []).
    - :app:assembleDebug + :app:testDebugUnitTest (full suite) via the Windows helper exit 0 (D-12 / SC-5).
    - SUMMARY records the gate outcomes and the v2.944 font provenance.
  </acceptance_criteria>
  <done>Five KDoc fixes done (4 token renames + the PrintStatusPreviews.kt:35 [AppContainer] de-bracket); no stale links remain; full assembleDebug + testDebugUnitTest + verify_ligatures green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: On-device icon spot check (D-04, SC-2 relaxed per D-06)</name>
  <what-built>
    The whole icon-source conversion: registry ligature flips (Plan 02), raw Compose call-site swaps
    (Plan 03), the Views console official vectors (Plan 02), and the drawable deletions (this plan). The
    automated gates (assembleDebug, full unit suite, verify_ligatures) are green; this checkpoint confirms
    the glyphs actually RENDER on real hardware (no tofu boxes) and the shape-distinct status coding reads.
  </what-built>
  <how-to-verify>
    1. Install the debug build on flox (or a device pointed at an Ender Moonraker). Build/install via the
       Windows helper + adb (do NOT run ./gradlew from WSL).
    2. PrintStatus / FineTune: confirm the babystep compress/expand, pressure-advance (text_select_move_forward_word),
       and increase/decrease (add/remove) glyphs render as real Material Symbols (no empty boxes).
    3. ProbeCalibrate: confirm detector, expand, add, remove, arrow_upward, arrow_downward render; nozzle
       (custom) still renders.
    4. MoveScreen: confirm the unhomed-caution triangle (warning) renders, and the force-move lock toggles
       between open (lock_open_right) and closed (lock).
    5. Console row: force an ERROR and a WARNING line via the Moonraker RESPOND broadcast trick
       (RESPOND TYPE=command MSG="action:..." per memory dinghy-stream-uat-via-moonraker) — confirm the
       ERROR shows square+x (disabled_by_default) and WARNING shows triangle+! (warning), and that the two
       are DISTINCT in grayscale (shape-coded safety, D-06).
    6. ThemeEditor preview swatches: confirm the stop swatch shows square+x and the caution swatch shows
       triangle+!.
  </how-to-verify>
  <resume-signal>Type "approved" if all glyphs render and the status shapes are distinct, or describe any tofu/missing/mis-rendered glyph.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Deletion of static assets + KDoc text edits + verification. No new network, auth, input, or
data-handling surface.

| Boundary | Description |
|----------|-------------|
| (none new) | Only removes assets and edits doc comments; no runtime surface added |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18.1-07 | Denial-of-service (self-inflicted: missing-glyph tofu) | deleted drawable with a missed call site | mitigate | Typed R.drawable.* references make any missed site a compile error; :app:assembleDebug is the completeness gate; the on-device spot check is the final confirmation no glyph renders as tofu. |
| T-18.1-08 | Tampering | accidental deletion of a D-10 custom keeper | mitigate | Explicit KEEP list (nozzle/heat_bed/bed_tilt/benchy/ic_launcher_foreground/disabled_by_default/warning); acceptance criteria assert the keepers still exist; the launcher icon is OS-registered and its loss would fail install. |

No new attack surface: no new I/O, no permissions, no untrusted input, no network, no secrets, no package
installs. No high-severity threats. Block-on-high gate: PASS.
</threat_model>

<verification>
- 13 drawables deleted; 7 keepers survive; no dangling R.drawable.* references.
- Four KDoc links fixed; no [DinghyPreviews]/[PrintStatusThemeMatrix] remain; [AppContainer] untouched.
- python tools/verify_ligatures.py -> missing: [].
- Full :app:assembleDebug + :app:testDebugUnitTest green (D-12 / SC-5).
- On-device spot check approved (glyphs render, status shapes distinct — D-04 / SC-2 per D-06).
</verification>

<success_criteria>
- The icon system is source-conformant app-wide: every icon is a real Material Symbol (ligature or
  official vector) except the D-10 printer-domain customs.
- No superseded hand-traced drawable remains; no dangling reference; no broken KDoc link.
- Build + full unit suite + ligature gate green; on-device spot check approved.
</success_criteria>

<output>
Create `.planning/phases/18.1-icon-system-conformance/18.1-04-SUMMARY.md` when done.
</output>
