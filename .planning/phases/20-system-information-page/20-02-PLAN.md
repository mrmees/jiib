---
phase: 20-system-information-page
plan: 02
type: execute
wave: 2
depends_on: ["20-01"]
files_modified:
  - app/src/main/java/works/mees/dinghy/systeminfo/SystemInfoModels.kt
  - app/src/main/java/works/mees/dinghy/systeminfo/SystemInfoParse.kt
  - app/src/main/java/works/mees/dinghy/systeminfo/HealthChip.kt
  - app/src/main/java/works/mees/dinghy/systeminfo/SysInfoFormat.kt
  - app/src/test/java/works/mees/dinghy/systeminfo/SystemInfoParseTest.kt
  - app/src/test/java/works/mees/dinghy/systeminfo/ProcStatPushTest.kt
  - app/src/test/java/works/mees/dinghy/systeminfo/HealthChipTest.kt
  - app/src/test/java/works/mees/dinghy/systeminfo/SysInfoFormatTest.kt
  - app/src/test/java/works/mees/dinghy/systeminfo/DegradeTest.kt
autonomous: true
requirements: [SYS-01, SYS-02, SYS-03, SYS-04]
must_haves:
  truths:
    - "machine.system_info parses to a SystemInfo identity model: empty model -> degrade, kernel read from distribution"
    - "The 1 Hz push frame parses to ProcStatLive (cpu%/mem/temp) WITHOUT requiring throttled_state or system_uptime"
    - "The machine.proc_stats query parses to ProcStatQuery carrying throttled_state + system_uptime"
    - "healthState() is a pure 3-state fn: throttle-authoritative when throttled_state non-null, temp-fallback (70 warn / 80 caution) when null"
    - "Formatters: kB->GB/MB auto-scale and compact uptime (2d 3h 14m, dropping leading-zero units)"
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/systeminfo/HealthChip.kt"
      provides: "pure healthState(throttledState, cpuTemp) -> HealthState + ThrottledState model"
      contains: "fun healthState"
    - path: "app/src/main/java/works/mees/dinghy/systeminfo/SystemInfoParse.kt"
      provides: "SystemInfo.from / ProcStatLive.fromPush / ProcStatQuery.from parsers (null/blank tolerant)"
      contains: "fromPush"
    - path: "app/src/main/java/works/mees/dinghy/systeminfo/SysInfoFormat.kt"
      provides: "kB->GB/MB + compact-uptime formatters"
      min_lines: 20
  key_links:
    - from: "app/src/main/java/works/mees/dinghy/systeminfo/SystemInfoParse.kt"
      to: "kotlinx.serialization.json.JsonObject"
      via: "tolerant JsonObject walk (no schema-strict @Serializable on loose Moonraker payloads)"
      pattern: "JsonObject|jsonObject"
---

<objective>
Wave 2 pure logic for the System Information page (SYS-01..04, host-unit-testable, no Compose, no socket): the identity/live/query models + their tolerant parsers, the pure health-chip decision function, and the value formatters. Turns the Plan-01 RED scaffolds GREEN against the real fixtures.

Purpose: All correctness lives here as pure functions tested against both-SBC captures — the three traps (push omits throttle+uptime, RockPro64 throttled_state:null, empty cpu_info.model) are decided in testable code, NOT in the screen. This is the Nyquist core: Wave-3 plumbing and UI just wire these.
Output: SystemInfoModels.kt, SystemInfoParse.kt, HealthChip.kt, SysInfoFormat.kt + the five tests converted from pending-RED to live assertions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-system-information-page/20-RESEARCH.md
@.planning/phases/20-system-information-page/20-PATTERNS.md
@app/src/test/java/works/mees/dinghy/calibration/BedMeshModelTest.kt
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Identity + live + query models and their tolerant parsers</name>
  <files>app/src/main/java/works/mees/dinghy/systeminfo/SystemInfoModels.kt, app/src/main/java/works/mees/dinghy/systeminfo/SystemInfoParse.kt, app/src/test/java/works/mees/dinghy/systeminfo/SystemInfoParseTest.kt, app/src/test/java/works/mees/dinghy/systeminfo/ProcStatPushTest.kt, app/src/test/java/works/mees/dinghy/systeminfo/DegradeTest.kt</files>
  <read_first>
    - .planning/phases/20-system-information-page/20-RESEARCH.md sections "Field-Mapping Table", "Per-Host Captured JSON Ground Truth", and "Common Pitfalls" (1-4) — exact source paths, units, null/empty cases
    - app/src/test/java/works/mees/dinghy/calibration/BedMeshModelTest.kt + the existing BedMeshModel.from(JsonObject) parser it tests — the tolerant-JsonObject-walk analog (objectOrNull/floatOrNullAt helpers)
    - app/src/test/resources/fixtures/system_info_e5.json, system_info_e3.json, proc_stats_e5.json, proc_stats_e3.json, notify_proc_stat_push_e5.json (from Plan 01)
    - the existing JsonObject extension helpers used by calibration parsers (objectOrNull, floatOrNullAt, etc.) — reuse, do not re-roll
  </read_first>
  <behavior>
    - SystemInfo.from(system_info_e5) -> model "Raspberry Pi 4 Model B Rev 1.4", cpuCount 4, totalMemoryKb 8007452, distroName "Debian GNU/Linux 12 (bookworm)", kernel "6.12.87+rpt-rpi-v8" (read from distribution.kernel_version, NOT top-level — Pitfall 3).
    - SystemInfo.from(system_info_e3) -> model is null/blank-as-missing (empty string "" treated as missing — Pitfall 4), cpuCount 6, distroName "Armbian 25.11.2 noble", kernel "6.18.10-current-rockchip64".
    - ProcStatLive.fromPush(notify_proc_stat_push_e5 params[0]) -> cpuTemp, cpuLoadPercent (system_cpu_usage.cpu aggregate), memUsedKb/memTotalKb (system_memory) — and MUST NOT throw or require throttled_state / system_uptime (they are absent in the push — Pitfall 1).
    - ProcStatQuery.from(proc_stats_e5) -> throttledState non-null {bits:0, flags:[]}, cpuTemp 64.757, systemUptimeSeconds ~312773.75.
    - ProcStatQuery.from(proc_stats_e3) -> throttledState null (JsonNull tolerated as "no throttle data", NOT a parse error — Pitfall 2), cpuTemp 53.333, systemUptimeSeconds ~1178981.14.
    - Degrade: missing keys, empty-string model, and null throttled_state never crash; absent values surface as null on the model (the UI maps null -> "—").
  </behavior>
  <action>
    Define in SystemInfoModels.kt the data classes: SystemInfo(model: String?, cpuDesc: String?, processor: String?, cpuCount: Int?, totalMemoryKb: Long?, distroName: String?, distroVersion: String?, kernel: String?); ProcStatLive(cpuTemp: Float?, cpuLoadPercent: Float?, memUsedKb: Long?, memTotalKb: Long?, memAvailableKb: Long?); ProcStatQuery(throttledState: ThrottledState?, cpuTemp: Float?, systemUptimeSeconds: Double?). ThrottledState(bits: Int, flags: List<String>) lives in HealthChip.kt (Task 2) — import it here, or define it in models and let Task 2 consume it (pick one home; models is fine).
    In SystemInfoParse.kt, write pure top-level/companion parsers that walk JsonObject tolerantly (NOT schema-strict @Serializable — Moonraker payloads are loose; mirror BedMeshModel.from). SystemInfo.from reads cpu_info.{model,cpu_desc,processor,cpu_count,total_memory} and distribution.{name,version,kernel_version}; treat "" as missing (blankToNull). ProcStatLive.fromPush reads cpu_temp + system_cpu_usage.cpu + system_memory.{used,total,available}; it must tolerate the absence of throttled_state/system_uptime. ProcStatQuery.from reads throttled_state (object -> ThrottledState, JsonNull/absent -> null), cpu_temp, system_uptime. Reuse the existing objectOrNull/floatOrNullAt JsonObject helpers; add a longOrNullAt / blankToNull helper if absent.
    Convert SystemInfoParseTest, ProcStatPushTest, and DegradeTest from the Plan-01 Assume-guarded RED stubs to live assertions against the fixtures (remove the assumeTrue guards). NO fenced code in production — directive only.
  </action>
  <verify>
    <automated>cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --tests '*SystemInfoParse*' --tests '*ProcStatPush*' --tests '*Degrade*' --no-daemon" 2>&1 | tr -d '\r' | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - SystemInfoParseTest, ProcStatPushTest, DegradeTest all GREEN (assumeTrue guards removed).
    - SystemInfoParse.kt: ProcStatLive.fromPush parses the push fixture and a test asserts it succeeds despite no throttled_state/system_uptime keys.
    - A test asserts SystemInfo.from(system_info_e3) yields a null/blank model (empty "" treated as missing).
    - A test asserts ProcStatQuery.from(proc_stats_e3) yields throttledState == null without throwing.
    - kernel is read from distribution.kernel_version (a test asserts the E5 kernel value).
  </acceptance_criteria>
  <done>Identity/live/query models + tolerant parsers exist; the three parse/degrade tests are GREEN against both-SBC fixtures.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pure health-chip decision fn + value formatters</name>
  <files>app/src/main/java/works/mees/dinghy/systeminfo/HealthChip.kt, app/src/main/java/works/mees/dinghy/systeminfo/SysInfoFormat.kt, app/src/test/java/works/mees/dinghy/systeminfo/HealthChipTest.kt, app/src/test/java/works/mees/dinghy/systeminfo/SysInfoFormatTest.kt</files>
  <read_first>
    - .planning/phases/20-system-information-page/20-RESEARCH.md section "Health-Chip Logic Spec" — precedence, the vcgencmd bit table (low nibble = active -> caution; bits 16-19 = occurred -> warn), the D-12 temp cutoffs, and Assumptions A1/A2 (also map off the flags array as a supplement)
    - .planning/phases/20-system-information-page/20-CONTEXT.md D-12 — LOCKED cutoffs: <70 healthy, >=70 warn, >=80 caution
    - .planning/phases/20-system-information-page/20-RESEARCH.md section "Value Formatting" + "Field-Mapping Table" unit note — kB->MB ÷1024, kB->GB ÷1024^2; uptime compact 2d 3h 14m dropping leading-zero units
    - app/src/main/java/works/mees/dinghy/outputs/OutputsHolder.kt (reached()) — the pure when-decision style analog
  </read_first>
  <behavior>
    - healthState(throttledState = {bits:0, flags:[]}, cpuTemp = 64.7) -> HealthState.Healthy (E5 clean: throttle path, no bits set).
    - healthState(throttledState = {bits:0x1, ...}, cpuTemp = anything) -> HealthState.Caution (active under-voltage; low nibble set).
    - healthState(throttledState = {bits:0x40000, ...}, cpuTemp = anything) -> HealthState.Warn (throttling has-occurred; bit 18).
    - healthState(throttledState = null, cpuTemp = 53.3) -> HealthState.Healthy (E3 temp-fallback, <70).
    - healthState(throttledState = null, cpuTemp = 72.0) -> HealthState.Warn (>=70).
    - healthState(throttledState = null, cpuTemp = 81.0) -> HealthState.Caution (>=80).
    - healthState(throttledState = null, cpuTemp = null) -> HealthState.Healthy (or a documented no-crash default; must not throw).
    - formatMemory(usedKb, totalKb) -> "used / total" auto-scaled (e.g. 761312 kB / 8007452 kB -> "744 MB / 7.6 GB" style); formatGb(kb) for Total RAM; formatUptime(seconds) -> "2d 3h 14m" dropping leading-zero units (under a day -> "3h 14m", under an hour -> "14m").
  </behavior>
  <action>
    In HealthChip.kt define enum HealthState { Healthy, Warn, Caution }, the ThrottledState(bits, flags) model (if not already in SystemInfoModels.kt — keep ONE definition), and the pure fun healthState(throttledState: ThrottledState?, cpuTemp: Float?): HealthState. Precedence (RESEARCH): if throttledState != null use the THROTTLE path — (bits & 0xF) != 0 -> Caution; else (bits & 0xF0000) != 0 -> Warn; else Healthy. Else (throttledState == null) use the TEMP-FALLBACK path with the D-12 LOCKED cutoffs: cpuTemp >= 80 -> Caution; >= 70 -> Warn; else Healthy; cpuTemp == null -> Healthy (no crash). Keep the function total and side-effect-free. (Per Assumption A1/A2 you MAY also OR-in a flags-string check, but the raw bits mask is the primary source and the cutoff numbers are LOCKED — do not change them.)
    In SysInfoFormat.kt define pure formatters: formatGb(totalKb: Long?): String (e.g. "7.6 GB"); formatMemoryUsedOver Total(usedKb, totalKb): String auto-scaling kB->MB->GB; formatUptime(seconds: Double?): String compact "2d 3h 14m" dropping leading-zero units from the left; formatCpuLoad(pct: Float?): String integer percent; formatTemp(c: Float?): String whole "48°C". Each returns "—" (em dash) on null/missing input (the SYS-04 degrade contract). Use Locale.US for any number formatting (project convention — see PrinterCommands builders).
    Convert HealthChipTest and SysInfoFormatTest from Assume-guarded RED to live assertions covering every behavior above (include the E5-clean, synthetic-active-bit, synthetic-occurred-bit, and E3 temp-fallback @70/@80 cases, and the uptime under-a-day case).
  </action>
  <verify>
    <automated>cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --tests '*HealthChip*' --tests '*SysInfoFormat*' --no-daemon" 2>&1 | tr -d '\r' | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - HealthChipTest GREEN, including a case for each of: E5 clean -> Healthy, synthetic active bit -> Caution, synthetic occurred bit -> Warn, E3 null-throttle @72°C -> Warn, @81°C -> Caution, @53°C -> Healthy.
    - SysInfoFormatTest GREEN: kB->GB, kB->MB, and uptime "2d 3h 14m" + under-a-day "3h 14m".
    - HealthChip.kt contains `fun healthState(` with the documented precedence and the literal D-12 cutoffs 70 and 80.
    - Every formatter returns "—" on null input (a test asserts at least one).
  </acceptance_criteria>
  <done>The pure health-chip fn (throttle-authoritative + temp-fallback) and all formatters exist and are GREEN against the locked cutoffs/formats.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Moonraker payload -> parser | Loose/heterogeneous JSON from the host crosses into the parsers; a sparse or malicious payload must degrade, never crash |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-02-D | Denial of service | SystemInfoParse / ProcStatLive parsers on a sparse/malformed payload | mitigate | Tolerant JsonObject walk (objectOrNull/floatOrNullAt, blank-as-missing, JsonNull-tolerant); DegradeTest asserts missing keys + null throttled_state + empty model do not throw (SYS-04). |
| T-20-02-T | Tampering | A hostile Moonraker sends absurd bits/temp values | accept | healthState is total over all Int/Float inputs (bit-mask + numeric compare); no array indexing, no parse-to-throw; worst case is a misleading-but-bounded chip state on a screen with no control surface. |

No HIGH threats: pure logic, no I/O, no dependency, no control surface. The only attack surface is a malformed payload, mitigated by tolerant parsing + the DegradeTest gate.
</threat_model>

<verification>
- `--tests '*SystemInfoParse*' '*ProcStatPush*' '*HealthChip*' '*SysInfoFormat*' '*Degrade*'` all GREEN.
- Full `:app:testDebugUnitTest` stays GREEN (no regression).
</verification>

<success_criteria>
- All five Wave-0 RED scaffolds are now GREEN live assertions.
- The three correctness traps are encoded in passing tests (push omission tolerated, null throttle -> temp fallback, empty model -> degrade).
- D-12 cutoffs (70/80) and the 2d 3h 14m uptime format are locked by tests.
</success_criteria>

<output>
Create `.planning/phases/20-system-information-page/20-02-SUMMARY.md` when done.
</output>
