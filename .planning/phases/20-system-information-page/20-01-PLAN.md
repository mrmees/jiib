---
phase: 20-system-information-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/test/resources/fixtures/system_info_e5.json
  - app/src/test/resources/fixtures/system_info_e3.json
  - app/src/test/resources/fixtures/proc_stats_e5.json
  - app/src/test/resources/fixtures/proc_stats_e3.json
  - app/src/test/resources/fixtures/notify_proc_stat_push_e5.json
  - app/src/test/java/works/mees/dinghy/systeminfo/SystemInfoParseTest.kt
  - app/src/test/java/works/mees/dinghy/systeminfo/ProcStatPushTest.kt
  - app/src/test/java/works/mees/dinghy/systeminfo/HealthChipTest.kt
  - app/src/test/java/works/mees/dinghy/systeminfo/SysInfoFormatTest.kt
  - app/src/test/java/works/mees/dinghy/systeminfo/DegradeTest.kt
  - tools/verify_ligatures.py
autonomous: true
requirements: [SYS-01, SYS-02, SYS-03, SYS-04]
must_haves:
  truths:
    - "Five captured-live fixture JSONs are committed as test resources (both SBCs, both planes)"
    - "Five RED test scaffolds compile day-one and run (pending/fail, never a compile error)"
    - "verify_ligatures.py exits 0 with all 10 owner-locked D-01..D-10 ligatures in its NEEDED set"
  artifacts:
    - path: "app/src/test/resources/fixtures/proc_stats_e3.json"
      provides: "RockPro64 proc_stats with throttled_state:null (the temp-fallback discriminator)"
      contains: "throttled_state"
    - path: "app/src/test/resources/fixtures/notify_proc_stat_push_e5.json"
      provides: "captured 1 Hz push frame proving throttled_state + system_uptime are OMITTED"
      contains: "system_cpu_usage"
    - path: "app/src/test/java/works/mees/dinghy/systeminfo/HealthChipTest.kt"
      provides: "RED scaffold for the pure health-chip decision fn"
      min_lines: 15
  key_links:
    - from: "app/src/test/java/works/mees/dinghy/systeminfo/SystemInfoParseTest.kt"
      to: "app/src/test/resources/fixtures/system_info_e5.json"
      via: "javaClass.getResource(/fixtures/system_info_e5.json)"
      pattern: "getResource.*fixtures/system_info"
---

<objective>
Wave 0 for the System Information page (SYS-01..04): commit the five live-captured fixture JSONs (the anti-mock-vs-reality discipline), stand up five RED test scaffolds that compile day-one, and extend the icon-ligature drift guard with the 10 owner-locked Phase-20 glyphs (D-01..D-10, verified resolvable in v2.944 per the RESEARCH Icon Gate).

Purpose: Lock the empirical ground truth (both SBCs, both data planes) and the test surface BEFORE any production code, so Waves 1-3 turn RED scaffolds GREEN against real captures rather than synthetic mocks. The fixtures encode the three correctness traps the research found: the push omits throttle+uptime, the RockPro64 sends `throttled_state: null`, and `cpu_info.model` is empty on the RockPro64.
Output: 5 fixtures + 5 compiling RED test files + the verify_ligatures.py NEEDED-set update.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-system-information-page/20-RESEARCH.md
@.planning/phases/20-system-information-page/20-PATTERNS.md
@.planning/phases/20-system-information-page/20-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Commit the five live-captured fixture JSONs</name>
  <files>app/src/test/resources/fixtures/system_info_e5.json, app/src/test/resources/fixtures/system_info_e3.json, app/src/test/resources/fixtures/proc_stats_e5.json, app/src/test/resources/fixtures/proc_stats_e3.json, app/src/test/resources/fixtures/notify_proc_stat_push_e5.json</files>
  <read_first>
    - .planning/phases/20-system-information-page/20-RESEARCH.md section "Per-Host Captured JSON Ground Truth" (lines ~95-178) — the verbatim captured blobs for both hosts and the push frame
    - .planning/phases/20-system-information-page/20-VALIDATION.md section "Wave 0 Requirements" — the exact 5 fixture filenames
    - .planning/phases/20-system-information-page/20-PATTERNS.md section "Capture-driven tests + fixtures" — the analog (calibration/BedMeshModelTest.kt resource-load) and the existing fixtures/*.json directory
    - app/src/test/resources/fixtures/ (an existing fixture, e.g. bed_mesh_e5.json) for the on-disk shape/placement convention
  </read_first>
  <action>
    Create the five fixture files under app/src/test/resources/fixtures/ from the RESEARCH "Per-Host Captured JSON Ground Truth" captures. Each must be the realistic Moonraker REST `result` object the parser will receive — mirror the wrapping that bed_mesh_e5.json uses.
    system_info_e5.json: the E5/RPi4 machine.system_info result — cpu_info{cpu_count:4, bits:"64bit", processor:"aarch64", cpu_desc:"", model:"Raspberry Pi 4 Model B Rev 1.4", total_memory:8007452, memory_units:"kB"} plus distribution{name:"Debian GNU/Linux 12 (bookworm)", id:"debian", version:"12", kernel_version:"6.12.87+rpt-rpi-v8"}. kernel_version is UNDER distribution.
    system_info_e3.json: the E3/RockPro64 result — cpu_info{cpu_count:6, bits:"64bit", processor:"aarch64", cpu_desc:"", model:"", total_memory:3945568, memory_units:"kB"} plus distribution{name:"Armbian 25.11.2 noble", id:"ubuntu", version:"24.04", kernel_version:"6.18.10-current-rockchip64"}. model is the EMPTY STRING (not absent, not null).
    proc_stats_e5.json: the E5 machine.proc_stats result with throttled_state:{bits:0, flags:[]} (clean Pi OBJECT), cpu_temp:64.757, system_uptime:312773.755726872, system_memory:{total:8007452, available:7246140, used:761312}, system_cpu_usage:{cpu:29.31, cpu0:33.68, cpu1:28.12, cpu2:23.23, cpu3:33.0}.
    proc_stats_e3.json: the E3 result with throttled_state:null (EXPLICIT NULL — the temp-fallback discriminator), cpu_temp:53.333, system_uptime:1178981.142290853, system_memory:{total:3945568, available:2814280, used:1131288}, system_cpu_usage:{cpu:33.33, cpu0:37.89}.
    notify_proc_stat_push_e5.json: a single captured notify_proc_stat_update frame's params[0] object — keys cpu_temp, moonraker_stats, network, system_cpu_usage, system_memory, websocket_connections and CRITICALLY NO throttled_state and NO system_uptime (this fixture is the proof the push omits both — RESEARCH Pitfall 1). Keep it as the raw params[0] object so ProcStatPushTest can feed it straight to the push parser.
    All values are LOCKED from the live capture — do not round, invent, or simplify. Memory units are kB on every field.
  </action>
  <verify>
    <automated>test -f app/src/test/resources/fixtures/proc_stats_e3.json && grep -q '"throttled_state": *null' app/src/test/resources/fixtures/proc_stats_e3.json && ! grep -q 'throttled_state' app/src/test/resources/fixtures/notify_proc_stat_push_e5.json && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - All 5 files exist under app/src/test/resources/fixtures/.
    - proc_stats_e3.json contains `throttled_state` with a literal `null` value.
    - notify_proc_stat_push_e5.json contains NEITHER `throttled_state` NOR `system_uptime` (proves the omission).
    - proc_stats_e5.json contains `throttled_state` as an object with `bits` and `flags`.
    - system_info_e3.json has `cpu_info.model` as an empty string `""`.
  </acceptance_criteria>
  <done>Five fixtures committed, each encoding the captured ground truth incl. the three correctness traps (push omission, null throttle, empty model).</done>
</task>

<task type="auto">
  <name>Task 2: Five RED test scaffolds that compile day-one</name>
  <files>app/src/test/java/works/mees/dinghy/systeminfo/SystemInfoParseTest.kt, app/src/test/java/works/mees/dinghy/systeminfo/ProcStatPushTest.kt, app/src/test/java/works/mees/dinghy/systeminfo/HealthChipTest.kt, app/src/test/java/works/mees/dinghy/systeminfo/SysInfoFormatTest.kt, app/src/test/java/works/mees/dinghy/systeminfo/DegradeTest.kt</files>
  <read_first>
    - app/src/test/java/works/mees/dinghy/calibration/BedMeshModelTest.kt — the fixture-load + assert analog (javaClass.getResource("/fixtures/..."), MoonrakerJson.parseToJsonElement)
    - .planning/phases/20-system-information-page/20-RESEARCH.md section "Validation Architecture" then "Wave 0 Gaps" — the 5 scaffold names plus what each asserts
    - MEMORY note [[dinghy-wave0-red-scaffold-compile]] — Gradle compiles the WHOLE test sourceset before the --tests filter, so a scaffold referencing unbuilt production symbols bricks ALL per-wave test runs
  </read_first>
  <action>
    Create the five test files in package works.mees.dinghy.systeminfo. They MUST compile against only what exists today — production parsers/fns do NOT exist yet (Wave 1 builds them). Per [[dinghy-wave0-red-scaffold-compile]], scaffold bodies must NOT reference unbuilt symbols. Use one of two compiling-RED idioms per method: (a) a load-the-fixture assertion that proves the fixture shape but stops short of the unbuilt parser (assert the fixture JSON has the expected keys), or (b) an org.junit.Assume.assumeTrue("Wave 1 (Plan 02) implements <Symbol>", false) guard naming the symbol the building plan will introduce. Prefer (b) for parser/fn behavior tests so they read as explicit pending-RED.
    SystemInfoParseTest: methods e5_parsesModelAndCpuCount, e3_emptyModelDegrades, kernelComesFromDistributionNotTopLevel — each loads the matching system_info fixture; behavior assertions guarded pending works.mees.dinghy.systeminfo.SystemInfo.from(...).
    ProcStatPushTest: pushParsesCpuTempLoadMem and pushParserDoesNotRequireThrottleOrUptime — load notify_proc_stat_push_e5.json; the second method asserts (idiom (a)) the push fixture has no throttled_state/system_uptime so the parser MUST tolerate their absence; behavior guarded pending ProcStatLive.fromPush(...).
    HealthChipTest: e5CleanThrottle_healthy, syntheticActiveBit_caution, syntheticOccurredBit_warn, e3NullThrottle_tempFallback_70warn_80caution — guarded pending the pure healthState(throttledState, cpuTemp) fn.
    SysInfoFormatTest: kbToGb, kbToMb, uptimeCompactDropsLeadingZeroUnits (2d 3h 14m; under a day -> 3h 14m) — guarded pending the formatter fns.
    DegradeTest: emptyModelString_rendersDash, nullThrottledState_noCrash, missingKeys_rendersDash — guarded pending the parser/degrade logic.
    Every class uses JUnit4 (@Test, org.junit.Assert.*, org.junit.Assume). Reference the D-12 cutoffs (70 warn / 80 caution) and the 2d 3h 14m uptime format in comments so Wave 1 has the contract inline. Run the full host suite at the end to confirm the WHOLE sourceset still compiles.
  </action>
  <verify>
    <automated>cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --no-daemon" 2>&1 | tr -d '\r' | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - Full `:app:testDebugUnitTest` COMPILES and runs to completion (BUILD SUCCESSFUL or only the expected RED/ignored systeminfo methods failing — never a compileTestKotlin error).
    - All 5 test files exist in app/src/test/java/works/mees/dinghy/systeminfo/.
    - HealthChipTest contains a method asserting the E3 null-throttle temp-fallback at the 70/80 cutoffs.
    - No scaffold references a symbol under works.mees.dinghy.systeminfo that does not yet exist as a compiled production symbol (uses Assume guards instead).
  </acceptance_criteria>
  <done>Five RED scaffolds compile day-one; the full test sourceset builds; behavior assertions are explicitly pending Wave 1.</done>
</task>

<task type="auto">
  <name>Task 3: Add the 10 owner-locked Phase-20 ligatures to the verify_ligatures.py NEEDED set</name>
  <files>tools/verify_ligatures.py</files>
  <read_first>
    - tools/verify_ligatures.py — the existing NEEDED set + how prior phases (18.1, 19) appended their blocks
    - .planning/phases/20-system-information-page/20-RESEARCH.md section "Icon Ligature Gate Results (D-11)" — all 10 confirmed resolvable in v2.944, including pulse_alert
    - .planning/phases/20-system-information-page/20-CONTEXT.md D-01..D-11 — the owner-locked slate (do not substitute)
  </read_first>
  <action>
    Add a Phase-20 block to the NEEDED set in tools/verify_ligatures.py with the owner-locked ligatures: pulse_alert (D-01), dns (D-02), schedule (D-04), developer_board (D-05), memory (D-06), deployed_code (D-07), code_blocks (D-08), data_usage (D-10). Do NOT add thermostat (D-03) or speed (D-09) as new NEEDED entries IF they are already present from prior phases (LauncherTemperature/Speed already register them) — if they are already in NEEDED, leave the single entry; the gate de-dups on the ligature string, not the token name. The gate must still exit 0: all 10 D-glyphs resolve in the bundled material_symbols_outlined.ttf v2.944 (RESEARCH Icon Gate confirmed). This is the drift guard that keeps the owner slate protected.
  </action>
  <verify>
    <automated>cd /mnt/e/claude/personal/github/dinghy-display && python3 tools/verify_ligatures.py; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `python3 tools/verify_ligatures.py` exits 0 (missing: []).
    - The NEEDED set contains pulse_alert, dns, schedule, developer_board, deployed_code, code_blocks, data_usage (the new-to-NEEDED names; memory may already be present).
    - thermostat and speed appear at most once in NEEDED (no duplicate re-add).
  </acceptance_criteria>
  <done>verify_ligatures.py protects all 10 owner-locked Phase-20 glyphs and exits 0 against the bundled v2.944 font.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Moonraker host -> app (test fixtures) | Captured JSON committed as fixtures; no live untrusted input crosses here at runtime (this plan is test-only) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01-I | Information disclosure | Committed fixture JSONs (hostname / CPU model / kernel) | accept | Low-sensitivity LAN host metadata captured from the developer's own printers; no secrets, no API keys in the fixtures (proc_stats/system_info carry none). Verify no auth tokens were captured. |
| T-20-01-T | Tampering | tools/verify_ligatures.py / fixtures | accept | Test-only artifacts; no production code path. No package installs in this plan (no supply-chain surface). |

No HIGH threats: this plan touches only test resources, test scaffolds, and a Python drift-guard. No untrusted runtime input, no new dependency, no control surface.
</threat_model>

<verification>
- Full `:app:testDebugUnitTest` compiles and runs (RED scaffolds pending, not compile errors).
- `python3 tools/verify_ligatures.py` exits 0.
- All 5 fixtures present with the encoded correctness traps (null throttle, push omission, empty model).
</verification>

<success_criteria>
- 5 fixtures committed from live captures.
- 5 RED scaffolds compile day-one; whole test sourceset builds.
- 10 owner-locked ligatures protected by the gate; gate green.
</success_criteria>

<output>
Create `.planning/phases/20-system-information-page/20-01-SUMMARY.md` when done.
</output>
