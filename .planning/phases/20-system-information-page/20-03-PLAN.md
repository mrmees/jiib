---
phase: 20-system-information-page
plan: 03
type: execute
wave: 3
depends_on: ["20-02"]
files_modified:
  - app/src/main/java/works/mees/dinghy/net/JsonRpc.kt
  - app/src/main/java/works/mees/dinghy/net/JsonRpcClient.kt
  - app/src/test/java/works/mees/dinghy/spool/SpoolmanNotifyRouterTest.kt
  - app/src/main/java/works/mees/dinghy/command/CommandRegistry.kt
  - docs/commands/catalog.json
  - docs/commands/printer-matrix.json
  - app/src/main/java/works/mees/dinghy/net/MoonrakerSession.kt
  - app/src/main/java/works/mees/dinghy/data/PrinterStateStore.kt
  - app/src/main/java/works/mees/dinghy/di/SpineHandle.kt
  - app/src/main/java/works/mees/dinghy/systeminfo/SystemInfoHolder.kt
  - app/src/main/java/works/mees/dinghy/di/AppContainer.kt
  - app/src/test/java/works/mees/dinghy/systeminfo/ProcStatRouteTest.kt
autonomous: true
requirements: [SYS-01, SYS-02, SYS-03]
must_haves:
  truths:
    - "notify_proc_stat_update is routed to a new _procStatUpdates flow (no longer dropped at else)"
    - "The T-11-04 golden test is updated in the SAME wave: proc-stat frames now reach _procStatUpdates AND still never reach activeSpoolSet"
    - "machine.system_info + machine.proc_stats are registered CommandSpecs; catalog.json rows flipped to registered; printer-matrix.json has a command_availability row for each (predicate always)"
    - "Both one-shot queries seed per-handshake (edge-driven, re-run on reconnect/klippy_ready) — NO poll loop"
    - "SystemInfoHolder exposes identity + procStats(query) + live(push) StateFlows off the SpineHandle/JsonRpcClient"
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/systeminfo/SystemInfoHolder.kt"
      provides: "dedicated host-telemetry holder (identity/procStats/live StateFlows) off the printer hot path"
      contains: "StateFlow"
    - path: "docs/commands/printer-matrix.json"
      provides: "command_availability rows for MR-machine.system_info + MR-machine.proc_stats"
      contains: "MR-machine.proc_stats"
  key_links:
    - from: "app/src/main/java/works/mees/dinghy/net/JsonRpcClient.kt"
      to: "_procStatUpdates SharedFlow"
      via: "NOTIFY_PROC_STAT_UPDATE case in handleNotification when(method)"
      pattern: "NOTIFY_PROC_STAT_UPDATE"
    - from: "app/src/main/java/works/mees/dinghy/net/MoonrakerSession.kt"
      to: "CommandRegistry.machineSystemInfo / machineProcStats"
      via: "rpc.request(...) one-shot seeds in runHandshake step 7"
      pattern: "machineSystemInfo|machineProcStats"
---

<objective>
Wave 3 spine plumbing for the System Information page (SYS-01/02/03): route the free 1 Hz proc-stat push (currently dropped), register the two Moonraker query specs through the command-catalog drift gate, seed the two one-shot queries per handshake (throttle+uptime+identity), and expose everything through a dedicated SystemInfoHolder off the printer hot path. UI (Plan 04) collects from the holder.

Purpose: Wire the two data planes the research mandates — the live plane (1 Hz push: cpu%/mem/temp) and the static/query plane (identity from machine.system_info + throttle/uptime from machine.proc_stats, both OMITTED from the push). This plan handles BOTH coupled-edit landmines in a single wave: (1) notify route + the T-11-04 golden test, and (2) CommandSpec registration + catalog flip + matrix rows. Splitting either across waves reds the build.
Output: notify route + updated golden, two registered specs + catalog/matrix rows, two handshake one-shot seeds, SystemInfoHolder + SpineHandle/AppContainer wiring, a ProcStatRouteTest.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-system-information-page/20-RESEARCH.md
@.planning/phases/20-system-information-page/20-PATTERNS.md
@docs/request-cadence-contract.md
@.planning/phases/20-system-information-page/20-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Route notify_proc_stat_update + update the T-11-04 golden (coupled-edit landmine #1)</name>
  <files>app/src/main/java/works/mees/dinghy/net/JsonRpc.kt, app/src/main/java/works/mees/dinghy/net/JsonRpcClient.kt, app/src/test/java/works/mees/dinghy/spool/SpoolmanNotifyRouterTest.kt, app/src/test/java/works/mees/dinghy/systeminfo/ProcStatRouteTest.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/net/JsonRpcClient.kt — the existing notify_active_spool_set / notify_spoolman_status_changed routes (SharedFlow + accessor + when-case + the params[0] helper spoolNotifyParam), and the current `else -> Unit` that drops proc-stat frames
    - app/src/main/java/works/mees/dinghy/net/JsonRpc.kt JsonRpcMethods — the NOTIFY_* constants block
    - app/src/test/java/works/mees/dinghy/spool/SpoolmanNotifyRouterTest.kt — the golden test; `ignoresUnrelatedProcStatNotifications` (asserts the TEN proc-stat frames never reach activeSpoolSet via else->Unit) and `routesActiveSpoolSetFromOneElementParamsArray`
    - .planning/phases/20-system-information-page/20-PATTERNS.md section "net/JsonRpcClient.kt (modify...)" and RESEARCH Pitfall 5 — the golden breaks if the route is added without updating the test in the same wave
    - app/src/main/java/works/mees/dinghy/systeminfo/SystemInfoParse.kt (Plan 02) — ProcStatLive.fromPush, the parser this route feeds
  </read_first>
  <action>
    Add `const val NOTIFY_PROC_STAT_UPDATE = "notify_proc_stat_update"` to JsonRpcMethods in JsonRpc.kt, next to NOTIFY_ACTIVE_SPOOL_SET.
    In JsonRpcClient.kt add a bounded `_procStatUpdates: MutableSharedFlow<JsonObject>` (mirror SPOOLMAN_BUFFER = 16 extraBufferCapacity) + a `procStatUpdates: SharedFlow<JsonObject>` accessor, and add a `JsonRpcMethods.NOTIFY_PROC_STAT_UPDATE -> { procStatParam(obj)?.let { _procStatUpdates.tryEmit(it) } }` case in the handleNotification when(method) — REPLACING the path that currently lets these frames fall through `else -> Unit`. Reuse/clone the spoolNotifyParam params[0] extractor as procStatParam (params[0].jsonObject, null-safe). Emit the raw params[0] JsonObject (the holder in Task 3 runs ProcStatLive.fromPush on it) — keeping the client a thin router.
    COUPLED EDIT (same task): update SpoolmanNotifyRouterTest.ignoresUnrelatedProcStatNotifications — it currently asserts proc-stat frames are ignored "via else -> Unit". Re-target it to the new reality: dispatching the proc-stat frames now (a) emits them on client.procStatUpdates (assert the count == the ten golden proc-stat frames) AND (b) STILL never emits on client.activeSpoolSet (the spool flow stays empty). Update the stale "else -> Unit" comment. Do NOT weaken routesActiveSpoolSetFromOneElementParamsArray (spool routing must still yield [3, 5]).
    Add ProcStatRouteTest.kt (package systeminfo): dispatch the captured notify frame (build it from notify_proc_stat_push_e5.json wrapped as a notify envelope, or reuse the golden's proc-stat frames) through a JsonRpcClient and assert procStatUpdates emits a JsonObject carrying cpu_temp + system_cpu_usage + system_memory, and that ProcStatLive.fromPush on the emitted object yields non-null cpuTemp/load/mem.
  </action>
  <verify>
    <automated>cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --tests '*SpoolmanNotifyRouter*' --tests '*ProcStatRoute*' --no-daemon" 2>&1 | tr -d '\r' | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - JsonRpcClient.kt contains a `NOTIFY_PROC_STAT_UPDATE ->` case emitting on `_procStatUpdates` (grep: NOTIFY_PROC_STAT_UPDATE present).
    - SpoolmanNotifyRouterTest GREEN with the updated assertion: proc-stat frames reach procStatUpdates (count matches) AND never reach activeSpoolSet.
    - routesActiveSpoolSetFromOneElementParamsArray still asserts [3, 5] (no regression).
    - ProcStatRouteTest GREEN: a dispatched proc-stat frame surfaces on procStatUpdates and ProcStatLive.fromPush parses it.
  </acceptance_criteria>
  <done>The 1 Hz push is routed to a new flow; the T-11-04 golden is updated in the same task; no spool-routing regression.</done>
</task>

<task type="auto">
  <name>Task 2: Register the two query specs through the drift gate (coupled-edit landmine #2)</name>
  <files>app/src/main/java/works/mees/dinghy/command/CommandRegistry.kt, docs/commands/catalog.json, docs/commands/printer-matrix.json</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/command/CommandRegistry.kt — the `serverInfo` CommandSpec (no-args query, availability default Always) and the `all` list the drift test reads
    - docs/commands/catalog.json lines ~6056-6083 (MR-machine.proc_stats) and ~6288-6314 (MR-machine.system_info) — the existing reference_only rows to flip
    - docs/commands/printer-matrix.json — the command_availability array (starts ~line 1303) and the MR-server.gcode_store row shape (predicate always)
    - .planning/phases/20-system-information-page/20-RESEARCH.md section "Command-Catalog / Matrix Drift Rows" — the exact JSON to add and WHY no predicate-evidence rows are needed (both Always)
    - the CommandCatalogDriftTest assertions: registryCatalogIdsExistInCatalogJson, registryCommandsHaveMatrixAvailabilityRows, catalogEntriesHavePlanRequiredShape (id == catalog_id), predicateReferencesAreBackedByMatrixEvidence (only non-Always predicates)
    - MEMORY note [[dinghy-command-catalog-drift]] — registering a spec without the catalog+matrix rows reds the build
  </read_first>
  <action>
    All three edits in ONE task (the drift gate fails if any is missing).
    (a) CommandRegistry.kt: add `machineSystemInfo: CommandSpec<Unit>` (catalogId "MR-machine.system_info", method "machine.system_info", key "machine_system_info", params null) and `machineProcStats: CommandSpec<Unit>` (catalogId "MR-machine.proc_stats", method "machine.proc_stats", key "machine_proc_stats", params null), both via the jsonRpc(...) builder like serverInfo (availability default Always — no ComponentPresent gate; both hosts always expose them). Append BOTH to the `all` list.
    (b) catalog.json: flip the runtime_registry block of the two existing rows from status reference_only to {status:"registered", registered:true, notes:"Registered runtime CommandSpec in CommandRegistry (Phase 20 System Information)."}. Do NOT change `id`/`catalog_id` (must stay equal — catalogEntriesHavePlanRequiredShape) or the `availability:always`/`predicate:always`.
    (c) printer-matrix.json: append two command_availability rows (MR-machine.system_info, MR-machine.proc_stats) copying the exact shape in RESEARCH "Drift Rows" §2 — source_api moonraker, transport json_rpc, registry_status registered, predicate {type:"always"}, predicate_key "always", and printer_status rows for ender5plus + ender3 both "present" with the probed-2026-06-08 evidence strings from RESEARCH. Because both predicates are Always, NO objects/components evidence rows are needed (predicateReferencesAreBackedByMatrixEvidence iterates only non-Always leaf predicates).
  </action>
  <verify>
    <automated>cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --tests '*CommandCatalogDrift*' --no-daemon" 2>&1 | tr -d '\r' | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - CommandRegistry.kt declares machineSystemInfo + machineProcStats and both appear in `all`.
    - catalog.json: both rows now have runtime_registry.status "registered" and id == catalog_id unchanged.
    - printer-matrix.json: a command_availability entry exists for BOTH MR-machine.system_info and MR-machine.proc_stats with predicate always and present rows for ender5plus + ender3.
    - CommandCatalogDriftTest GREEN (registryCommandsHaveMatrixAvailabilityRows + registryCatalogIdsExistInCatalogJson pass).
  </acceptance_criteria>
  <done>Both query specs registered; catalog flipped; matrix rows added; the D-10 drift gate is GREEN — all in one wave.</done>
</task>

<task type="auto">
  <name>Task 3: One-shot handshake seeds + SystemInfoHolder + SpineHandle/AppContainer wiring</name>
  <files>app/src/main/java/works/mees/dinghy/net/MoonrakerSession.kt, app/src/main/java/works/mees/dinghy/data/PrinterStateStore.kt, app/src/main/java/works/mees/dinghy/di/SpineHandle.kt, app/src/main/java/works/mees/dinghy/systeminfo/SystemInfoHolder.kt, app/src/main/java/works/mees/dinghy/di/AppContainer.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/net/MoonrakerSession.kt runHandshake step 7 (the one-shot history/config reads — temperature_store, gcode_store, configfile — each in its own best-effort runCatching, written to the store as capability-like StateFlows; lines ~460-520) — the EXACT pattern the two new one-shots follow
    - app/src/main/java/works/mees/dinghy/di/SpineHandle.kt — the one-shot StateFlow seam fields (minExtrudeTemp, temperatureBackfill etc.) forwarded off the store; how the service builds it
    - app/src/main/java/works/mees/dinghy/outputs/OutputsHolder.kt — the dedicated-holder analog (plain Kotlin, scope.launch collect, MutableStateFlow + asStateFlow, no Compose, no pending/busy machinery)
    - app/src/main/java/works/mees/dinghy/di/AppContainer.kt — how SpineHandle is published/consumed + where a per-session holder is constructed (the 08-07 MacroHolder / Spool holder wiring precedent), the JsonRpcClient accessor for procStatUpdates
    - .planning/phases/20-system-information-page/20-RESEARCH.md sections "Cadence Resolution" + "Open Questions" (Q1 host-label fallback, Q2 holder home) — RESOLVE both here: (Q1) when cpu_info.model is blank, fall back the host label to distro name; (Q2) dedicated SystemInfoHolder, NOT PrinterStateStore
    - docs/request-cadence-contract.md Rule 3 — edge-driven one-shot seeds, NO refresh-on-page-open poll
  </read_first>
  <action>
    Resolve the two open planner questions explicitly (state them in the SUMMARY): Q1 = host-label fallback to distribution.name when cpu_info.model is blank (never empty on either host); Q2 = a dedicated SystemInfoHolder off the printer hot path (host CPU telemetry is not Klipper printer-state).
    MoonrakerSession.kt: in runHandshake step 7, add TWO new best-effort one-shot reads (each in its own runCatching so a printer lacking the endpoint never breaks the handshake, exactly like temperature_store/gcode_store): rpc.request(CommandRegistry.machineSystemInfo, Unit) -> SystemInfo.from(result) -> store.setSystemInfo(...); rpc.request(CommandRegistry.machineProcStats, Unit) -> ProcStatQuery.from(result) -> store.setProcStatQuery(...). These run inside runHandshake so they inherit the reconnect AND notify_klippy_ready reruns (edge-driven, Phase-13 Rule 3 — do NOT add any page-open query).
    PrinterStateStore.kt: add two capability-like seam StateFlows + setters (setSystemInfo / setProcStatQuery + systemInfo: StateFlow<SystemInfo?> / procStatQuery: StateFlow<ProcStatQuery?>), mirroring setTemperatureBackfill/temperatureBackfill. These are seeded ONCE per handshake (not the throttled hot path).
    SpineHandle.kt: forward systemInfo + procStatQuery straight off the store (like minExtrudeTemp/temperatureBackfill).
    SystemInfoHolder.kt: a dedicated plain-Kotlin holder (OutputsHolder shape) constructed per session with the active SpineHandle + the JsonRpcClient's procStatUpdates flow. Expose val identity: StateFlow<SystemInfo?> (from SpineHandle.systemInfo), val procStats: StateFlow<ProcStatQuery?> (from SpineHandle.procStatQuery — carries throttle+uptime), and val live: StateFlow<ProcStatLive?> (scope.launch collect on procStatUpdates -> ProcStatLive.fromPush -> _live.value). NO markPending/reached/timeout (read-only, no dispatch). The 1 Hz push is already >= the store 250ms floor so no extra throttle is needed.
    AppContainer.kt: construct/expose the SystemInfoHolder per session (mirror how MacroHolder/Spool holder are wired from the SpineHandle + client), so the screen (Plan 04) can obtain it.
  </action>
  <verify>
    <automated>cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --no-daemon" 2>&1 | tr -d '\r' | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - MoonrakerSession.runHandshake step 7 contains two new best-effort runCatching one-shots calling rpc.request(CommandRegistry.machineSystemInfo, ...) and (CommandRegistry.machineProcStats, ...) writing to the store; NO new poll loop, NO page-open query anywhere.
    - PrinterStateStore exposes systemInfo + procStatQuery StateFlows with setters; SpineHandle forwards both.
    - SystemInfoHolder.kt exposes identity + procStats + live StateFlows; live collects JsonRpcClient.procStatUpdates via ProcStatLive.fromPush; no pending/busy machinery.
    - AppContainer constructs the SystemInfoHolder per session.
    - Full `:app:testDebugUnitTest` GREEN (no regression). `:app:assembleDebug` compiles.
  </acceptance_criteria>
  <done>Two edge-driven one-shot seeds wired into the handshake; a dedicated SystemInfoHolder exposes the three flows; Q1/Q2 resolved and recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Moonraker websocket/REST -> session/holder | Push frames + query results from the host cross here; the page is read-only (no command dispatch back) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-03-D | Denial of service | runHandshake one-shot reads on a host lacking the endpoint | mitigate | Each new read is its own best-effort runCatching AFTER subscribe (handshake already reached Connected) — a failed/absent read leaves the seam StateFlow at null and the page degrades to "—" (SYS-04); never breaks Connected. |
| T-20-03-D2 | Denial of service | 1 Hz push flooding _procStatUpdates | accept | Bounded MutableSharedFlow (extraBufferCapacity 16, tryEmit drop-on-overflow); 1 Hz is far below any pressure threshold and rides the existing socket — zero added wire traffic. |
| T-20-03-I | Information disclosure | host hostname/CPU model/kernel surfaced via the holder | accept | Low-sensitivity LAN host metadata on a wall-mounted screen; no secrets traverse these endpoints; no new auth/network path (rides the existing trusted-LAN session). |
| T-20-03-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan — no supply-chain surface introduced. |

No HIGH threats: additive read-only plumbing on the existing trusted-LAN session. No new dependency, no control surface, no new auth path.
</threat_model>

<verification>
- `--tests '*SpoolmanNotifyRouter*' '*ProcStatRoute*' '*CommandCatalogDrift*'` all GREEN.
- Full `:app:testDebugUnitTest` GREEN + `:app:assembleDebug` compiles.
- Both coupled-edit landmines handled within this wave (notify route + golden in Task 1; spec + catalog + matrix in Task 2).
</verification>

<success_criteria>
- The push is routed (not dropped); golden updated same-wave.
- Both specs registered + drift gate GREEN same-wave.
- Two edge-driven one-shot seeds + a dedicated holder exposing identity/procStats/live; no poll loop.
</success_criteria>

<output>
Create `.planning/phases/20-system-information-page/20-03-SUMMARY.md` when done.
</output>
