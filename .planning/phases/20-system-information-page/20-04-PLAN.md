---
phase: 20-system-information-page
plan: 04
type: execute
wave: 4
depends_on: ["20-03"]
files_modified:
  - app/src/main/java/works/mees/dinghy/designsystem/icons/DinghyIcons.kt
  - app/src/test/java/works/mees/dinghy/designsystem/icons/DinghyIconsTest.kt
  - app/src/main/java/works/mees/dinghy/ui/route/TopRoute.kt
  - app/src/main/java/works/mees/dinghy/ui/shell/AppDrawer.kt
  - app/src/main/java/works/mees/dinghy/ui/shell/AppShell.kt
  - app/src/main/java/works/mees/dinghy/ui/systeminfo/SystemInformationScreen.kt
  - app/src/main/res/values/strings.xml
  - app/src/main/java/works/mees/dinghy/ui/systeminfo/SystemInfoPreviews.kt
  - .planning/phases/20-system-information-page/20-UAT.md
autonomous: false
requirements: [SYS-01, SYS-02, SYS-03, SYS-04, SYS-05]
user_setup: []
must_haves:
  truths:
    - "The 10 owner-locked D-01..D-10 ligatures are bound in DinghyIcons (reusing LauncherTemperature/Speed for D-03/D-09, never re-adding)"
    - "The drawer System Info stub is flipped to a live Dest.SystemInfo tile with the pulse_alert glyph"
    - "The screen renders Focus health summary (hostname/model, CPU temp, health chip, uptime) + Field grouped Host + Live load sections via icon-led InfoRows"
    - "The health chip renders the Phase-15.1 shape-coded 3 states; missing fields degrade to '—' with the row present"
    - "On-device UAT on flox against BOTH printers (RPi4 + RockPro64) confirms identity/live/health + identical chip semantics (SC-4)"
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/ui/systeminfo/SystemInformationScreen.kt"
      provides: "the read-only host-health screen (Focus summary + Field grouped detail + Back gutter)"
      min_lines: 80
    - path: "app/src/main/java/works/mees/dinghy/ui/route/TopRoute.kt"
      provides: "Dest.SystemInfo route enum value"
      contains: "SystemInfo"
  key_links:
    - from: "app/src/main/java/works/mees/dinghy/ui/shell/AppDrawer.kt"
      to: "Dest.SystemInfo"
      via: "DrawerTileSpec(label = System Info, symbol = pulse_alert, dest = Dest.SystemInfo)"
      pattern: "Dest.SystemInfo"
    - from: "app/src/main/java/works/mees/dinghy/ui/systeminfo/SystemInformationScreen.kt"
      to: "SystemInfoHolder.live / identity / procStats"
      via: "collectAsStateWithLifecycle"
      pattern: "collectAsState"
---

<objective>
Wave 4 UI + on-device gate for the System Information page (SYS-01..05): bind the owner-locked icon slate, flip the drawer stub to a live tile, build the read-only screen (icon-led InfoRows + the shape-coded health chip), wire the Dest route + AppShell branch, and verify on both real SBCs.

Purpose: Surface the Wave-3 holder data as the staging-doc-locked layout — Focus health summary + Field grouped Host/Live-load detail + Back-only gutter, drawer suppressed on-screen — every detail row icon-led with the owner's EXACT D-02..D-10 glyphs, the chip carrying safety by shape. The phase is not done until the cross-SBC on-device UAT confirms the chip means the same thing on the RPi 4 and the RockPro64 (SC-4).
Output: DinghyIcons bindings + drift-test update, Dest route, live drawer tile, the screen + previews + strings, AppShell branch, and the on-device UAT record.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-system-information-page/20-system-info-staging.md
@.planning/phases/20-system-information-page/20-RESEARCH.md
@.planning/phases/20-system-information-page/20-PATTERNS.md
@docs/ui_design/LAYOUT.md
@docs/ui_design/THEMING.md
@docs/ui_design/CLAUDE.md
@docs/ui_design/PREVIEW_AND_TOKENS.md
@app/src/main/java/works/mees/dinghy/ui/screen/AboutScreen.kt
@.planning/phases/20-system-information-page/20-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bind the owner-locked icon slate + flip the drawer stub + Dest route</name>
  <files>app/src/main/java/works/mees/dinghy/designsystem/icons/DinghyIcons.kt, app/src/test/java/works/mees/dinghy/designsystem/icons/DinghyIconsTest.kt, app/src/main/java/works/mees/dinghy/ui/route/TopRoute.kt, app/src/main/java/works/mees/dinghy/ui/shell/AppDrawer.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/designsystem/icons/DinghyIcons.kt — the Phase-19 OutputHeater..OutputSection owner-locked block, the existing LauncherTemperature (thermostat, line 49) + Speed (speed, line 84) tokens, the CheckCircle (line 44), StatusStop (lines 71-72), and the hand-rolled `all` list (lines 126-136)
    - app/src/test/java/works/mees/dinghy/designsystem/icons/DinghyIconsTest.kt — the uniqueness/drift guard that derives checks from `all`
    - .planning/phases/20-system-information-page/20-CONTEXT.md D-01..D-11 + 20-RESEARCH.md "Icon Ligature Gate Results" — the LOCKED slate (all 10 resolve in v2.944) and the REUSE-don't-re-add rule for thermostat/speed
    - app/src/main/java/works/mees/dinghy/ui/route/TopRoute.kt — the Dest enum (line 37)
    - app/src/main/java/works/mees/dinghy/ui/shell/AppDrawer.kt — the greyed System Info stub (line 239: `DrawerTileSpec(label = "System Info", symbol = "memory", dest = null)`) + the live-tile pattern (Macros/Console lines 188-191) + the OUTPUT_SYMBOL sourced-from-token precedent (line 161)
    - MEMORY [[dinghy-never-pick-icons-ask]] — NEVER substitute a glyph; the slate is owner-locked
  </read_first>
  <action>
    DinghyIcons.kt: add the owner-locked bindings, each DinghyIcon(IconRef.Ligature("<lig>"), alternate = "<unique_handle>"), and append each new val to the `all` list: SysInfoTile -> pulse_alert (D-01), SysInfoHost -> dns (D-02), SysInfoUptime -> schedule (D-04), SysInfoCpu -> developer_board (D-05), SysInfoRam -> memory (D-06), SysInfoDistro -> deployed_code (D-07), SysInfoKernel -> code_blocks (D-08), SysInfoMemUsage -> data_usage (D-10). For D-03 (CPU temp) REUSE the existing LauncherTemperature (thermostat) token; for D-09 (CPU load) REUSE the existing Speed token — do NOT re-register thermostat/speed under a new name (the uniqueness guard checks ligature reuse). The health chip uses the existing StatusStop (caution) + IconRef.Ligature("warning") (warn) + a shapeless/CheckCircle go-state — no new chip glyph.
    DinghyIconsTest.kt: it derives uniqueness/drift dynamically from `all`, so it should pass once the new vals are appended — run it; if it asserts an explicit expected-count or expected-name list, update that to include the 8 new tokens.
    TopRoute.kt: add `SystemInfo` to the Dest enum (line 37).
    AppDrawer.kt: flip the stub at line 239 to a live tile: `DrawerTileSpec(label = "System Info", symbol = "pulse_alert", dest = Dest.SystemInfo)` (always shown — host always exists, no runtime gate). Per the OUTPUT_SYMBOL precedent, prefer sourcing the symbol from DinghyIcons.SysInfoTile if a SYSINFO_SYMBOL const fits the existing pattern; otherwise the literal "pulse_alert" matches the registered ligature. Verify pulse_alert is unique among the DRAWER_TILES glyph set.
  </action>
  <verify>
    <automated>cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --tests '*DinghyIcons*' --no-daemon" 2>&1 | tr -d '\r' | tail -12; grep -q 'Dest.SystemInfo' app/src/main/java/works/mees/dinghy/ui/shell/AppDrawer.kt && grep -q 'pulse_alert' app/src/main/java/works/mees/dinghy/ui/shell/AppDrawer.kt && echo TILE_OK</automated>
  </verify>
  <acceptance_criteria>
    - DinghyIcons.kt binds pulse_alert/dns/schedule/developer_board/memory/deployed_code/code_blocks/data_usage; each appears in `all`; thermostat/speed are NOT re-added.
    - DinghyIconsTest GREEN (uniqueness/drift).
    - TopRoute.kt Dest enum contains `SystemInfo`.
    - AppDrawer.kt line for System Info reads dest = Dest.SystemInfo with the pulse_alert glyph (no longer dest = null).
  </acceptance_criteria>
  <done>The owner-locked slate is bound (reusing the two existing tokens), the drift guard is green, the Dest route exists, and the drawer tile is live with pulse_alert.</done>
</task>

<task type="auto">
  <name>Task 2: Build the System Information screen + previews + AppShell wiring</name>
  <files>app/src/main/java/works/mees/dinghy/ui/systeminfo/SystemInformationScreen.kt, app/src/main/res/values/strings.xml, app/src/main/java/works/mees/dinghy/ui/systeminfo/SystemInfoPreviews.kt, app/src/main/java/works/mees/dinghy/ui/shell/AppShell.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/screen/AboutScreen.kt — the ScreenScaffold Field+Gutter shell, the InfoRow primitive (private, lines 218-240), the fsSp(baseSp, t.fs) scale, the Back-only Neutral gutter (lines 78-131); COPY the InfoRow structure into a NEW icon-led row (do NOT widen About's private InfoRow)
    - .planning/phases/20-system-information-page/20-system-info-staging.md sections "Screen Layout", "Health Chip", "Value Formatting", "Graceful Degradation" — the authoritative layout/behavior contract (read every "Phase 19" as Phase 20)
    - .planning/phases/20-system-information-page/20-PATTERNS.md sections "SystemInformationScreen.kt" + "Health chip" — the scaffold/InfoRow/chip render excerpts and the DinghyIconView + fsSp sizing idiom
    - docs/ui_design/PREVIEW_AND_TOKENS.md — the PreviewBox matrix idiom (6 theme combos + fs=L), stringResource convention, DinghyIcon tokens; the preview-first/tokenized-first LAW
    - app/src/main/java/works/mees/dinghy/ui/shell/AppShell.kt — the when(dest) branch list where Dest.SystemInfo is armed (mirror Dest.About / Dest.Console arming + Back -> drawer/PrintStatus)
    - app/src/main/java/works/mees/dinghy/systeminfo/{SystemInfoHolder,HealthChip,SysInfoFormat,SystemInfoModels}.kt (Waves 2-3) — the holder flows + healthState fn + formatters the screen consumes
    - MEMORY [[dinghy-font-sizes-too-small]] — 15sp metadata floor / 17-18 body / 20-22 titles via fsSp
  </read_first>
  <action>
    SystemInformationScreen.kt (package ui.systeminfo): a stateless SystemInformationScreen(state/holder-flows, onBack) using ScreenScaffold with a Field column (verticalScroll) and a Back-only Neutral gutter (OutlinedControl label "Back", symbol "arrow_back"), drawer suppressed on-screen (match AboutScreen/Phase-18 behavior). Collect identity/procStats/live from the SystemInfoHolder via collectAsStateWithLifecycle. Add a NEW icon-led row composable (copy About's InfoRow structure + a leading DinghyIconView slot before the label) — do NOT modify About's private InfoRow.
    Layout per the staging doc (fixed logical order, NOT alphabetical):
    - Focus health summary: hostname/host model (DinghyIcons.SysInfoHost = dns, D-02; label = model or distro-name fallback per Wave-3 Q1), CPU temp (DinghyIcons.LauncherTemperature = thermostat, D-03; formatTemp), the health chip (shape-coded — see below), uptime (DinghyIcons.SysInfoUptime = schedule, D-04; formatUptime from procStats.systemUptimeSeconds).
    - Field Host section (SectionHeader): CPU model + cores (DinghyIcons.SysInfoCpu = developer_board, D-05), Total RAM (DinghyIcons.SysInfoRam = memory, D-06; formatGb), Distro name+version (DinghyIcons.SysInfoDistro = deployed_code, D-07), Kernel (DinghyIcons.SysInfoKernel = code_blocks, D-08).
    - Field Live load section: CPU load % (DinghyIcons.Speed = speed, D-09; formatCpuLoad from live.cpuLoadPercent), Memory used/available (DinghyIcons.SysInfoMemUsage = data_usage, D-10; formatMemory from live).
    Health chip: render healthState(procStats.throttledState, live.cpuTemp ?? procStats.cpuTemp) -> Caution = DinghyIconView(DinghyIcons.StatusStop, tint = t.stop); Warn = DinghyIconView(IconRef.Ligature("warning"), tint = t.heat); Healthy = shapeless/CheckCircle go-state. fsSp-scaled size; never rely on intrinsic glyph size. Shape carries safety (color redundant — THEMING).
    Degrade (SYS-04): every value goes through the Wave-2 formatters which already return "—" on null; the labeled row + its icon STAY present (stable layout across both SBCs). Treat empty-string identity fields as missing (already handled in the parser/formatter).
    strings.xml: add all labels + cd_* content descriptions as string resources (no hardcoded UI strings — PREVIEW_AND_TOKENS LAW).
    SystemInfoPreviews.kt: a @Preview matrix per PREVIEW_AND_TOKENS — at least a healthy-RPi4 state, a temp-fallback RockPro64 state (blank model -> distro-name label), a degraded/sparse state (many "—"), and a caution state; using a fake state provider (NO live Moonraker), the 6 theme combos + fs=L, PreviewBox idiom. No @Stable/@Immutable churn beyond what the data classes already carry.
    AppShell.kt: arm Dest.SystemInfo in when(dest), obtaining the SystemInfoHolder from AppContainer and passing onBack (mirror Dest.About / Dest.Console). All font sizes via fsSp.
  </action>
  <verify>
    <automated>cmd.exe /c "E:\Android\gw.bat :app:assembleDebug --no-daemon" 2>&1 | tr -d '\r' | tail -12; grep -q 'Dest.SystemInfo' app/src/main/java/works/mees/dinghy/ui/shell/AppShell.kt && echo SHELL_OK</automated>
  </verify>
  <acceptance_criteria>
    - `:app:assembleDebug` BUILD SUCCESSFUL.
    - SystemInformationScreen.kt renders the Focus summary (host/temp/chip/uptime) + Field Host + Live-load sections with the EXACT owner-locked D-02..D-10 glyph tokens (grep the screen for DinghyIcons.SysInfoHost/SysInfoCpu/SysInfoRam/SysInfoDistro/SysInfoKernel/SysInfoMemUsage + LauncherTemperature + Speed).
    - The health chip renders via healthState(...) using StatusStop/warning/go shapes — no new chip drawable.
    - AppShell.kt arms Dest.SystemInfo; the gutter is Back-only and the drawer is suppressed on-screen.
    - All UI strings are stringResource (no hardcoded literals in the new screen); a @Preview matrix exists covering healthy/temp-fallback/degraded/caution.
  </acceptance_criteria>
  <done>The read-only host-health screen renders the staging-doc layout with the owner-locked icons + shape-coded chip + graceful degradation, builds, and is armed in the shell with previews.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: On-device cross-SBC UAT on flox (SYS-05 / SC-4)</name>
  <files>.planning/phases/20-system-information-page/20-UAT.md</files>
  <action>
    This is a blocking human-verify checkpoint (not autonomous work). Claude FIRST builds + installs the debug APK on flox (cmd.exe /c "E:\\Android\\gw.bat :app:installDebug --no-daemon") and scaffolds 20-UAT.md with one row per check (1-6) below, then HANDS OFF to the owner for the on-device cross-SBC walk. Do NOT mark the phase complete on the checkpoint; record PASS/FAIL per check and, on any FAIL, spawn a gap-closure plan (per SYS-05 / SC-4).
  </action>
  <read_first>
    - .planning/phases/20-system-information-page/20-VALIDATION.md section "Manual-Only Verifications" — the cross-SBC instructions + why the instrumented drawer swipe is unreliable ([[dinghy-instrumented-swipe-threshold]])
    - MEMORY [[dinghy-display-printer-addresses]] — E5/RPi4 = 192.168.1.120:7125, E3/RockPro64 = 192.168.1.121:7125
    - MEMORY [[dinghy-display-ondevice-iteration]] + [[dinghy-display-build-env]] — Claude builds+installs (E:\Android\gw.bat / installDebug + adb), Matthew navigates/eyeballs on flox
  </read_first>
  <what-built>
    The read-only System Information page: drawer tile (pulse_alert) -> Focus health summary (hostname/model, CPU temp, shape-coded health chip, uptime) + Field Host (CPU/RAM/distro/kernel) and Live load (CPU %, memory) sections, fed by the 1 Hz proc-stat push (live plane) + the two one-shot handshake queries (identity + throttle/uptime), degrading missing fields to "—".
  </what-built>
  <how-to-verify>
    Claude: build + installDebug the debug APK on flox (E:\Android\gw.bat :app:installDebug). Scaffold 20-UAT.md with the checks below before handing off.
    Matthew, on flox:
    1. Point the app at the E5 / RPi 4 (192.168.1.120:7125). Open the swipe-up drawer; confirm the System Info tile shows the pulse_alert glyph; tap it.
    2. Focus: hostname/model reads "Raspberry Pi 4 Model B Rev 1.4"; CPU temp shows a sane whole-°C value that UPDATES (~1 Hz); uptime shows a compact days/hours value; the health chip is the healthy (go/shapeless) shape (the E5 is clean).
    3. Field Host: CPU shows model + 4 cores; Total RAM ~7.6 GB; Distro "Debian GNU/Linux 12 (bookworm)"; Kernel "6.12.87+rpt-rpi-v8". Live load: CPU % + memory used/total update ~1 Hz.
    4. Switch the app to the E3 / RockPro64 (192.168.1.121:7125). The page re-resolves to the new host.
    5. Focus on E3: host model row degrades gracefully (model is empty -> "—" or the distro-name label "Armbian 25.11.2 noble"); CPU temp updates; the health chip uses the TEMP-FALLBACK and reads the SAME three shape-coded states as the Pi (at idle it should be healthy/go since temp < 70). Field: 6 cores, ~3.8 GB RAM, Armbian distro, rockchip64 kernel.
    6. Confirm the chip MEANS THE SAME on both hosts (same shapes, same go/warn/caution semantics), nothing crashes, Back returns cleanly, and the page never shows a janky/empty crash on either SBC.
    Record PASS/FAIL per check in 20-UAT.md. A FAIL spawns a gap-closure plan, not a phase-complete mark.
  </how-to-verify>
  <acceptance_criteria>
    - 20-UAT.md exists with one row per check (1-6) and a recorded PASS/FAIL + owner sign-off.
    - Both printers exercised; the health chip semantics confirmed identical across RPi 4 + RockPro64 (SC-4).
    - No crash / no frozen empty page on either host; live values update ~1 Hz; missing fields show "—".
  </acceptance_criteria>
  <resume-signal>Type "approved" (or list the failing checks to spawn gap closure)</resume-signal>
  <verify>
    <automated>test -f .planning/phases/20-system-information-page/20-UAT.md && grep -qi 'pass\|fail' .planning/phases/20-system-information-page/20-UAT.md && echo UAT_RECORDED</automated>
  </verify>
  <done>20-UAT.md records a PASS/FAIL per check (1-6) for BOTH printers with owner sign-off; the health-chip semantics are confirmed identical across RPi 4 + RockPro64; no crash on either SBC (SYS-05 / SC-4).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Moonraker host -> screen render | Host identity/telemetry rendered on a wall-mounted screen; read-only, no control surface, no text input accepted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-04-I | Information disclosure | hostname / CPU model / kernel / distro shown on-screen | accept | Low-sensitivity LAN host metadata on a dedicated printer screen; no secrets; the page surfaces nothing not already on the host's own dashboards. No PII. |
| T-20-04-D | Denial of service | screen render on a sparse/malicious payload | mitigate | All values flow through the Wave-2 formatters that return "—" on null; rows stay present; the screen reads holder StateFlows that default null when a read fails (SYS-04). UAT check 5/6 exercises the sparse RockPro64 case on real hardware. |
| T-20-04-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan — no supply-chain surface. |

No HIGH threats: a read-only diagnostics screen with no control surface, no new auth/network path (rides the existing trusted-LAN session), and no untrusted input accepted. Degradation on sparse/malicious payloads is gate-tested (Wave-2 DegradeTest) and on-device verified (UAT).
</threat_model>

<verification>
- DinghyIconsTest GREEN; Dest.SystemInfo + live drawer tile present.
- `:app:assembleDebug` BUILD SUCCESSFUL; screen armed in AppShell with the owner-locked glyphs + shape chip + previews.
- On-device cross-SBC UAT recorded in 20-UAT.md, owner-approved (SC-4).
</verification>

<success_criteria>
- Owner-locked slate bound (reusing the two existing tokens); drift guard green.
- Read-only screen renders staging-doc layout (Focus summary + Field detail), degrades to "—", chip shape-coded.
- Both real SBCs verified; chip reads identically; no crash (SYS-05 / SC-4).
</success_criteria>

<output>
Create `.planning/phases/20-system-information-page/20-04-SUMMARY.md` when done.
</output>
