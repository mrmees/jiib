---
phase: 23-design-language-foundation
plan: 05
type: execute
wave: 3
depends_on: ["23-01", "23-03", "23-04"]
files_modified:
  - app/src/main/java/works/mees/dinghy/designsystem/components/ListRow.kt
  - app/src/main/java/works/mees/dinghy/designsystem/components/DetailCard.kt
  - app/src/main/java/works/mees/dinghy/designsystem/components/FillMeter.kt
  - app/src/main/java/works/mees/dinghy/designsystem/components/FootButtonBar.kt
  - app/src/main/java/works/mees/dinghy/designsystem/components/FloatingEStop.kt
  - app/src/main/java/works/mees/dinghy/designsystem/components/SortFilterControlRow.kt
  - app/src/test/java/works/mees/dinghy/designsystem/components/ListRowTest.kt
  - app/src/test/java/works/mees/dinghy/designsystem/components/FillMeterTest.kt
  - app/src/main/java/works/mees/dinghy/preview/DesignKitComponentPreviews.kt
autonomous: true
requirements: []
must_haves:
  truths:
    - "ListRow renders transparent fill + outline when unselected, accentSoft fill + accentLine 2dp border when selected; touch floor = uDp"
    - "DetailCard is a filled t.surface card with a 3dp color-reactive ring (ringColor data carve-out or accentLine fallback), rCard radius"
    - "FillMeter clamps fraction to 0..1, fills left-anchored in the data fillColor over a surface3 track, with a Geist Mono label, NO glow"
    - "FootButtonBar is a 1U OutlinedControl row that lives inside the field lambda (NOT the gutter slot)"
    - "FloatingEStop is an Intent.Danger overlay button (~0.7U) shown only when visible, positioned by the caller as a Box sibling"
    - "SortFilterControlRow is a COMPOUND component: a leading recessed TYPE tile (DinghyIcons.Sort for SortRow / DinghyIcons.FilterList for FilterRow, non-interactive, t.bg2) precedes the option tiles; NO group-label words anywhere (separation by type-tile + fill + accent only)"
    - "SortRow shows a direction indicator on the active option; FilterRow tiles use accent when active; all glyphs render via OutlinedControl's DinghyIcon overload from the DinghyIcons registry — never a raw ligature string"
    - "All kit components read LocalTokens.current (no raw chrome colors), use fsSp for text, and ship a @Preview matrix"
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/designsystem/components/ListRow.kt"
      provides: "Translucent/selected list row primitive (uDp touch floor)"
      contains: "accentSoft"
    - path: "app/src/main/java/works/mees/dinghy/designsystem/components/DetailCard.kt"
      provides: "Filled color-reactive detail card + cardSurface() modifier"
      contains: "rCard"
    - path: "app/src/main/java/works/mees/dinghy/designsystem/components/FillMeter.kt"
      provides: "Read-only clamped fill meter"
      contains: "coerceIn"
    - path: "app/src/main/java/works/mees/dinghy/designsystem/components/FootButtonBar.kt"
      provides: "Foot-of-list action bar (1U, inside field)"
    - path: "app/src/main/java/works/mees/dinghy/designsystem/components/FloatingEStop.kt"
      provides: "Printing-only floating e-stop overlay"
    - path: "app/src/main/java/works/mees/dinghy/designsystem/components/SortFilterControlRow.kt"
      provides: "SortRow + FilterRow + SortOption/FilterOption"
  key_links:
    - from: "app/src/main/java/works/mees/dinghy/designsystem/components/SortFilterControlRow.kt"
      to: "app/src/main/java/works/mees/dinghy/designsystem/control/OutlinedControl.kt"
      via: "renders glyphs via the DinghyIcon-aware OutlinedControl overload (icon = DinghyIcons.Sort/FilterList/...), never a raw ligature string"
      pattern: "icon = DinghyIcons|OutlinedControl\\("
    - from: "app/src/test/java/works/mees/dinghy/designsystem/components/ListRowTest.kt"
      to: "app/src/main/java/works/mees/dinghy/designsystem/components/ListRow.kt"
      via: "fill/border state-selection assertions"
      pattern: "accentSoft|selected"
---

<objective>
Build the six component-class composables on top of the named analogs and the unit-grid primitive,
turning the two RED component scaffolds GREEN.

Purpose: SC-2 (the kit) and SC-4 (the locked rules). These are the reusable classes every redesign
screen consumes: ListRow (translucent content), DetailCard (filled color-reactive card), FillMeter
(read-only fraction), FootButtonBar (foot-of-list actions), FloatingEStop (printing-only overlay), and
SortFilterControlRow (sort-toggle + filter-opens-picker). Each is an EXTRACTION from a real existing
analog (mostly SpoolScreen/SpoolPicker), not a from-scratch design — the analogs and their exact line
references are in 23-PATTERNS.md. The kit enforces the fill convention, token-only chrome, fsSp text,
and icon-registry-only glyphs. Building the kit before the pilot lets the SpoolScreen rebuild (23-06)
just consume it.

Output: 6 component files, GREEN ListRowTest + FillMeterTest, and a @Preview matrix per component.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/23-design-language-foundation/23-RESEARCH.md
@.planning/phases/23-design-language-foundation/23-PATTERNS.md
@.claude/skills/sketch-findings-dinghy-display/SKILL.md
@docs/ui_design/PREVIEW_AND_TOKENS.md
@app/src/main/java/works/mees/dinghy/designsystem/control/OutlinedControl.kt
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the display/list classes — ListRow, DetailCard (+cardSurface), FillMeter — and turn ListRowTest + FillMeterTest GREEN</name>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/spool/SpoolPicker.kt (analog for ListRow — SpoolRow lines ~494-561, OptionButton lines ~448-491: the fill/border 3-way + heightIn + clickable + padding)
    - app/src/main/java/works/mees/dinghy/ui/spool/SpoolScreen.kt (analog for DetailCard — SpoolDetailFocus lines ~302-447: clip(shape).border(3dp,ringColor).background(t.surface), the spoolColor ?: accentLine : hair logic)
    - app/src/main/java/works/mees/dinghy/designsystem/ScrubberPage.kt (analog for FillMeter — the fill-bar Box lines ~239-245, left-anchored fillMaxWidth(fraction); FillMeter is READ-ONLY so OMIT the pointerInput entirely)
    - app/src/test/java/works/mees/dinghy/designsystem/components/ListRowTest.kt + FillMeterTest.kt (the RED scaffolds from 23-01 — the contract)
    - .planning/phases/23-design-language-foundation/23-PATTERNS.md §ListRow, §DetailCard, §FillMeter, §"Shared Patterns" (token access, fsSp scale floors, rCard/rCtrl, Geist/GeistMono pairing)
    - .planning/phases/23-design-language-foundation/23-RESEARCH.md §"FillMeter" (the no-glow-on-Adreno-320 rule), §Pitfall 5 (do NOT rebuild scrubber gestures)
  </read_first>
  <behavior>
    - ListRow: unselected → bgColor == Color.Transparent, borderColor == t.outline, borderWidth == 1.5dp;
      selected → bgColor == t.accentSoft, borderColor == t.accentLine, borderWidth == 2dp; touch floor heightIn(min = uDp)
    - FillMeter: fraction below 0 clamps to 0, above 1 clamps to 1, in-range unchanged (the fill fraction passed to fillMaxWidth)
  </behavior>
  <action>
    Create three files under `designsystem/components/`:
    - `ListRow.kt`: `@Composable fun ListRow(selected, onClick, uDp: Dp, modifier, leadingContent: (@Composable ()->Unit)?=null,
      trailingContent: (@Composable ()->Unit)?=null, content: @Composable RowScope.()->Unit)`. Body per PATTERNS §ListRow:
      `val t = LocalTokens.current`; `shape = RoundedCornerShape(t.rCtrl)`; bg/border/borderWidth from the selected flag
      (translucent content convention); `Modifier.fillMaxWidth().heightIn(min = uDp).clip(shape).background(bg)
      .border(BorderStroke(w, border), shape).clickable(onClick).padding(horizontal = 12.dp)`, CenterVertically; render
      leading → content → trailing. Expose the fill/border selection as a small pure internal helper (or testable values)
      so `ListRowTest` can assert the state-selection without a full Compose harness (turn the RED stubs GREEN).
    - `DetailCard.kt`: `@Composable fun DetailCard(modifier, ringColor: Color? = null, content: @Composable ColumnScope.()->Unit)`
      per PATTERNS §DetailCard — filled `t.surface`, `RoundedCornerShape(t.rCard)`, `BorderStroke(3.dp, ringColor ?: t.accentLine)`,
      padding 16.dp. ALSO add the `fun Modifier.cardSurface(t: ThemeTokens): Modifier` extension (clip rCard + background surface
      + 1dp hair border) in this file. KDoc the ringColor THEME-01 data carve-out: it is the filament's true hex, NEVER
      passed through brandTint.
    - `FillMeter.kt`: `@Composable fun FillMeter(fraction: Float, fillColor: Color, modifier, label: String = "")` per PATTERNS
      §FillMeter — `val clamped = fraction.coerceIn(0f, 1f)`; pill track `RoundedCornerShape(999.dp)` height 6dp; track =
      `t.surface3`, fill layer = left-anchored `fillMaxWidth(clamped).background(fillColor)`; optional label `GeistMono`,
      `fsSp(15f, t.fs).sp`, `t.text2`. NO glow/blurMaskFilter (Adreno-320 fill-rate). Expose the clamp via a pure helper for
      FillMeterTest. fillColor is a data carve-out (caller supplies filament hex or t.accent) — never a theme role internally.
    Turn `ListRowTest` and `FillMeterTest` RED scaffolds GREEN against the pure helpers.
    Token-only chrome (LocalTokens.current); no raw `Color(0x...)`; all text via `fsSp`.
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --tests works.mees.dinghy.designsystem.components.ListRowTest --tests works.mees.dinghy.designsystem.components.FillMeterTest --no-daemon" 2>&1 | tr -d '\r' | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - ListRow.kt / DetailCard.kt / FillMeter.kt exist with the signatures above
    - `ListRowTest` + `FillMeterTest` GREEN
    - DetailCard.kt exports `Modifier.cardSurface`; ringColor carve-out KDoc'd (no brandTint)
    - FillMeter clamps via `coerceIn(0f, 1f)` and has NO blurMaskFilter/glow
    - No raw `Color(0x` for chrome in any of the three (`grep -c 'Color(0x' ...` only on data-carve-out paths if any); no bare `.sp` (all `fsSp`)
  </acceptance_criteria>
  <done>The three display classes are extracted from their analogs, enforce the fill convention + token-only chrome + fsSp, and the two RED scaffolds are GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: Build the action/control classes — FootButtonBar, FloatingEStop, SortFilterControlRow</name>
  <read_first>
    - app/src/main/java/works/mees/dinghy/designsystem/control/OutlinedControl.kt (the Intent primitive — Accent/Neutral/Go/Warn/Danger; USE THE DinghyIcon-aware overload added in 23-03 Task 4: `OutlinedControl(label, onClick, ..., icon: DinghyIcon)`, NOT the raw `symbol: String?` overload — all three components here pass registered DinghyIcons by token)
    - app/src/main/java/works/mees/dinghy/ui/spool/SpoolScreen.kt (analog for FootButtonBar — the gutter Row lines ~261-294: OutlinedControl with Modifier.weight(1f), Arrangement.spacedBy(8.dp))
    - app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusScreen.kt (analog for FloatingEStop overlay-sibling positioning) + app/src/main/java/works/mees/dinghy/designsystem/ConfirmGuard.kt (overlay Box pattern)
    - app/src/main/java/works/mees/dinghy/ui/spool/SpoolPicker.kt (analog for SortFilterControlRow — SpoolFilterControls lines ~134-203, the sort-row direction indicator lines ~146-167, CategoryButton lines ~193-203)
    - .planning/phases/23-design-language-foundation/23-PATTERNS.md §FootButtonBar, §FloatingEStop, §SortFilterControlRow (the exact API shapes + tile sizing uDp-12dp + the Box-sibling positioning rule)
    - .planning/phases/23-design-language-foundation/23-RESEARCH.md §6 SortFilterControlRow (fill convention: type-tile bg2 recessed, option tile surface/accentSoft), §Pitfall 1 (FootButtonBar NOT in gutter slot), §Pitfall 7 (FloatingEStop is an overlay sibling, not a Focus child)
    - .claude/skills/sketch-findings-dinghy-display/SKILL.md (intent colors: accent=physical command, go=accept, caution/amber=peril, stop=destructive, neutral=plain setting; icons registry-only, never auto-pick)
  </read_first>
  <action>
    Create three files under `designsystem/components/`:
    - `FootButtonBar.kt`: `@Composable fun FootButtonBar(uDp: Dp, modifier, content: @Composable RowScope.()->Unit)` per
      PATTERNS §FootButtonBar — a `Row` with `fillMaxWidth().heightIn(min = uDp).padding(horizontal=8.dp, vertical=4.dp)`,
      `Arrangement.spacedBy(8.dp)`, CenterVertically; caller places `OutlinedControl`s with `Modifier.weight(1f)`. KDoc the
      structural rule: this lives as the LAST element INSIDE the `field` lambda Column, NOT in the ScreenScaffold `gutter`
      slot (Pitfall 1). It does NOT render the gutter itself.
    - `FloatingEStop.kt`: `@Composable fun FloatingEStop(visible: Boolean, onClick, uDp: Dp, modifier = Modifier)` per
      PATTERNS §FloatingEStop — `if (!visible) return`; an `OutlinedControl` `intent = Intent.Danger`, sized `uDp * 0.7f`
      (icon-tile ~70% of U). For the e-stop GLYPH: use the ALREADY-ASSIGNED e-stop ligature `disabled_by_default`
      (per the icon-assignments note's already-registered list: "disabled_by_default (e-stop)") via the registry — do NOT
      auto-pick `emergency_stop` (the PATTERNS placeholder); confirm `DinghyIcons` exposes the e-stop token and use it. The
      caller positions it (`Modifier.align(Alignment.TopStart).padding(14.dp)` as a Box sibling, Pitfall 7) — this component
      does NOT impose absolute offsets itself beyond what the modifier carries.
    - `SortFilterControlRow.kt` — MODEL THE LOCKED TYPE-TILE COMPOUND PATTERN (component-classes-catalog +
      redesign-direction §"Sort vs Filter"): each row is a `Row` that LEADS with a recessed, NON-INTERACTIVE
      TYPE tile and then the option tiles — NO group-label words anywhere (the type tile + fill + accent carry the
      grouping; owner: "context + styling should be all we need to group things naturally"). It must NOT ship as a
      bare option-list row. Define:
      `data class SortOption<K>(key, icon: DinghyIcon, contentDescriptionRes: Int, directionUp: Boolean? = null)`,
      `data class FilterOption<K>(key, icon: DinghyIcon, contentDescriptionRes: Int, isActive: Boolean)`,
      `@Composable fun <K> SortRow(options: ImmutableList<SortOption<K>>, activeKey: K?, onSelect: (K)->Unit, uDp: Dp, modifier)`,
      `@Composable fun <K> FilterRow(options: ImmutableList<FilterOption<K>>, onSelect: (K)->Unit, uDp: Dp, modifier)`.
      - LEADING TYPE TILE (mandatory): `SortRow` leads with a recessed tile rendering `DinghyIcons.Sort`; `FilterRow`
        leads with a recessed tile rendering `DinghyIcons.FilterList`. The type tile is `t.bg2`, non-interactive (no
        clickable), sized `heightIn(min = uDp - 12.dp)` with a fixed width (~uDp - 12.dp, square-ish); it is a label-less
        type indicator, not a button. Render its glyph via `DinghyIconView(DinghyIcons.Sort, ...)` (decorative — the
        TalkBack label, if any, comes from the option tiles, not the type tile).
      - OPTION TILES: each = the DinghyIcon-aware `OutlinedControl(label = …, onClick = …, icon = option.icon,
        intent = if (active) Intent.Accent else Intent.Neutral, modifier = Modifier.weight(1f).heightIn(min = uDp - 12.dp))`.
        Pass the option's REGISTERED `DinghyIcon` via the `icon =` overload — NEVER a raw ligature string. Fallback floor
        48dp if `uDp - 12.dp < 48.dp`.
      - SORT DIRECTION INDICATOR: the active sort tile overlays a registry `arrow_upward`/`arrow_downward` glyph
        (via `DinghyIconView`, `t.accent2`, `Box` + `Alignment.TopEnd`, positioned so it does NOT offset/displace the
        primary option glyph — reviewer note). The arrows are already-registered or owner-assigned ligatures; if
        `arrow_upward`/`arrow_downward` are not yet in DinghyIcons, STOP and escalate per icon law (do NOT auto-add).
      - FILL CONVENTION: option tiles are FILLED controls; the leading TYPE tile is recessed `t.bg2`; selection/active
        layers accent (`Intent.Accent`) on top. No header words.
    All three: token-only chrome, fsSp text, registry-only icons. `:app:compileDebugKotlin` GREEN.
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:compileDebugKotlin --no-daemon" 2>&1 | tr -d '\r' | tail -12</automated>
  </verify>
  <acceptance_criteria>
    - FootButtonBar.kt / FloatingEStop.kt / SortFilterControlRow.kt exist with the signatures above
    - FootButtonBar KDoc states it lives in the field lambda, not the gutter slot
    - FloatingEStop uses `Intent.Danger`, sizes `uDp * 0.7f`, and renders the registered e-stop glyph (`disabled_by_default`), NOT an auto-picked `emergency_stop`
    - SortRow and FilterRow each LEAD with a recessed, non-interactive TYPE tile (`DinghyIcons.Sort` / `DinghyIcons.FilterList`, `t.bg2`, no `clickable`) — verified: `grep -E 'DinghyIcons.Sort|DinghyIcons.FilterList' SortFilterControlRow.kt` finds both; the leading tile has NO group-label `Text` word
    - Option tiles render via the DinghyIcon-aware `OutlinedControl(..., icon = option.icon, ...)` overload — `grep -c 'icon = ' SortFilterControlRow.kt` >= 1 and `grep -c 'symbol = "' SortFilterControlRow.kt` == 0 (no raw ligature strings)
    - SortRow renders a direction indicator on the active option (TopEnd, not offsetting the primary glyph); option tiles use `Intent.Accent` when active
    - All glyphs rendered via the DinghyIcons registry (`grep -c 'DinghyIcon' SortFilterControlRow.kt` > 0); no auto-picked literals; no group-label words (`grep -iE 'Text\\(\\s*"?(Sort by|Filter by|Material|Color|Brand)' SortFilterControlRow.kt` finds no hardcoded group header)
    - `:app:compileDebugKotlin` BUILD SUCCESSFUL; no raw chrome `Color(0x`; no bare `.sp`
  </acceptance_criteria>
  <done>The three action/control classes are extracted from their analogs, use Intent colors + the DinghyIcon-aware OutlinedControl overload (registered glyphs, never raw strings) + the fill convention; SortFilterControlRow is a compound row with leading recessed Sort/FilterList type tiles and no group-label words; the e-stop uses the already-assigned glyph (no auto-pick).</done>
</task>

<task type="auto">
  <name>Task 3: @Preview matrices for all six component classes</name>
  <read_first>
    - app/src/main/java/works/mees/dinghy/preview/SpoolPreviews.kt (the matrix template)
    - app/src/main/java/works/mees/dinghy/preview/PreviewTheming.kt (PreviewBox, fsLargeSeed, 6 combos, @Nexus7Previews)
    - docs/ui_design/PREVIEW_AND_TOKENS.md §4 (fontScale no-op → fs=L via PreviewBox(fsLargeSeed)) + §8 (≥48dp + cd_* rider)
    - .planning/phases/23-design-language-foundation/23-PATTERNS.md §"Preview matrix pattern"
  </read_first>
  <action>
    Create `preview/DesignKitComponentPreviews.kt` exercising all six classes (ListRow selected+unselected,
    DetailCard with a ringColor + a FillMeter inside it, FootButtonBar of OutlinedControls, FloatingEStop visible,
    SortRow + FilterRow with sample options driven by the new DinghyIcons). Ship the full matrix per
    PREVIEW_AND_TOKENS.md: the 6 theme-combo `@Nexus7Previews` sibling fns via `PreviewBox(<seed>)`, a
    `*FsLargeOverflow` via `PreviewBox(fsLargeSeed)` (NOT `@Preview(fontScale=…)`), a `*RtlSpotCheck`, and a standalone
    `*PseudolocaleSpotCheck` (`@Preview(device = NEXUS7, locale = "en-XA")`). Provide sample `ImmutableList` option
    fixtures locally (or reuse SampleFixtures). Any string is a `stringResource`; any icon `contentDescription` via
    `cd_*`. This is the SC-2 @Preview-matrix evidence for the kit.
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:compileDebugKotlin --no-daemon" 2>&1 | tr -d '\r' | tail -12</automated>
  </verify>
  <acceptance_criteria>
    - `DesignKitComponentPreviews.kt` exists and previews all 6 classes
    - 6 theme-combo fns + `*FsLargeOverflow` (via `PreviewBox(fsLargeSeed)`) + `*RtlSpotCheck` + standalone `*PseudolocaleSpotCheck`
    - No bare `.sp`; no raw chrome `Color(0x`; icon `contentDescription`s via `cd_*` string resources
    - `:app:compileDebugKotlin` BUILD SUCCESSFUL (the matrix builds clean — SC-2/SC-5 compile gate)
  </acceptance_criteria>
  <done>Every component class has a 6-combo + fs=L + RTL + pseudolocale @Preview matrix that compiles clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Spoolman/gcode filament color/string → DetailCard.ringColor / FillMeter.fillColor / ListRow content | Filament color hex and labels originate from Spoolman/gcode (trusted-LAN Moonraker) and are rendered as a Color/Text. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-05-01 | Injection | filament color/string rendered in kit components | mitigate | Color is parsed to a `Color` (numeric), not interpolated into any markup/HTML/SQL/shell — no text-interpolation sink exists; labels render as plain Compose `Text` (no rich-text/markup path in the kit). Document that any future text-interpolation consumer must not pass these through an eval/format sink. |
| T-23-05-02 | Tampering | n/a | accept | Presentation-only; no storage/network/auth surface introduced. |

No new attack surface introduced — the kit is pure presentation over existing trusted-LAN Moonraker/Spoolman data; the only consideration (filament color/string as data) renders as a numeric Color and plain Text with no injection sink.
</threat_model>

<verification>
- `ListRowTest` + `FillMeterTest` GREEN (state-selection + clamp contracts).
- `:app:compileDebugKotlin` BUILD SUCCESSFUL (all 6 components + preview matrices compile clean).
- Token-only chrome, fsSp text, registry-only icons verified by grep.
</verification>

<success_criteria>
- SC-2: the component-class kit (ListRow, DetailCard, FillMeter, FootButtonBar, FloatingEStop, SortFilterControlRow) built, host-tested where pure, and @Preview-matrixed.
- SC-4 (foundation): the locked rules (translucent-list/filled-controls, intent colors, registry glyphs, sort-vs-filter) are baked into the classes.
</success_criteria>

<output>
Create `.planning/phases/23-design-language-foundation/23-05-SUMMARY.md` when done.
</output>
