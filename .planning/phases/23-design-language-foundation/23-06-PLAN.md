---
phase: 23-design-language-foundation
plan: 06
type: execute
wave: 4
depends_on: ["23-02", "23-03", "23-04", "23-05"]
files_modified:
  - app/src/main/java/works/mees/dinghy/ui/spool/SpoolScreen.kt
  - app/src/main/java/works/mees/dinghy/ui/spool/SpoolPicker.kt
  - app/src/main/java/works/mees/dinghy/preview/SpoolPreviews.kt
  - app/src/main/java/works/mees/dinghy/preview/SampleFixtures.kt
  - app/src/test/java/works/mees/dinghy/ui/spool/SpoolPickerStateTest.kt
  - app/src/main/res/values/strings.xml
autonomous: false
requirements: []
must_haves:
  truths:
    - "SpoolScreen renders on the new kit: DetailCard (with FillMeter) in Focus, ListBlock of ListRows + FootButtonBar in Field, gutter = null"
    - "The filter picker is a Field-takeover (FieldMode in SpoolPickerState) IN PLACE — the full-screen SpoolFilterPickerOverlay is gone"
    - "Foot buttons are Home (accent) · Scan (accent) · conditional Load (expand_circle_up, accent) when not loaded / Unload (expand_circle_down, neutral) when loaded; no Set-Active button"
    - "FloatingEStop overlays the Focus, visible only when printing — `isPrinting` is derived from the LIVE `container.printerState` StateFlow (`state.printState == PrintState.Printing || PrintState.Paused`, the existing LastJobHolder convention), NOT a preview-only flag; its onClick dispatches `CommandRegistry.emergencyStop` via the existing `dispatcher` (behind the ConfirmGuard, the same path PrintStatus uses)"
    - "SortRow toggles in-place with a direction indicator; FilterRow opens the Field-takeover picker; no group-label words"
    - "U is derived once at the SpoolScreen root via rememberUnitGrid(minOf(maxWidth,maxHeight)) and passed down; constant through rotation"
    - "The stateless SpoolScreen(state, ...) preview seam is preserved; existing SpoolHolder/SpoolPickerState host tests stay GREEN"
    - "The pilot is owner-approved on flox in BOTH portrait and landscape"
    - "Stepper/scrubber restyle is OUT OF SCOPE this phase (deferred to Phase 26 per ROADMAP SC-2 / RESEARCH Open-Q §2): SpoolScreen uses neither; the existing ScrubberPage/stepper pages are not touched here"
    - "U is verifiable as constant across rotation: a debug uDp readout/log is present so the owner can confirm uDp is equal in portrait and landscape on flox"
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/ui/spool/SpoolScreen.kt"
      provides: "SpoolScreen rebuilt on the kit (no gutter, Field-takeover filter, FootButtonBar, FloatingEStop, FillMeter)"
      contains: "FootButtonBar"
    - path: "app/src/main/java/works/mees/dinghy/preview/SpoolPreviews.kt"
      provides: "Updated preview matrix incl. a filter-open (FieldMode) state"
  key_links:
    - from: "app/src/main/java/works/mees/dinghy/ui/spool/SpoolScreen.kt"
      to: "app/src/main/java/works/mees/dinghy/designsystem/components/*"
      via: "consumes ListRow/DetailCard/FillMeter/FootButtonBar/FloatingEStop/SortRow/FilterRow"
      pattern: "DetailCard|FootButtonBar|FillMeter|FloatingEStop"
    - from: "app/src/main/java/works/mees/dinghy/ui/spool/SpoolScreen.kt"
      to: "app/src/main/java/works/mees/dinghy/designsystem/layout/UnitGrid.kt"
      via: "rememberUnitGrid at the screen root"
      pattern: "rememberUnitGrid"
---

<objective>
Rebuild SpoolScreen onto the new component-class kit — the pilot that proves the kit and the locked
redesign rules on a real owner-approved screen, in both orientations.

Purpose: SC-3 and SC-4. Spoolman is the north star and the most complete single exercise of the new
grammar (list + filter + detail card + fill meter + foot buttons). This rebuild is a STRUCTURAL refactor
of the existing SpoolScreen/SpoolPicker onto the kit — not a from-scratch rewrite. It removes the gutter
slot, moves actions to a FootButtonBar inside the Field, replaces the full-screen filter overlay with an
in-place Field-takeover picker, wraps the detail pane in a DetailCard with a FillMeter, and adds the
printing-only FloatingEStop. The screen's existing pure state/holder host tests must stay GREEN, and the
final gate is owner eyeball on flox in portrait AND landscape (per [[dinghy-display-ondevice-iteration]]).

Output: rebuilt SpoolScreen.kt + SpoolPicker.kt, FieldMode state, updated preview matrix + fixtures,
GREEN host suite, and the on-device owner approval.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/23-design-language-foundation/23-RESEARCH.md
@.planning/phases/23-design-language-foundation/23-PATTERNS.md
@.claude/skills/sketch-findings-dinghy-display/SKILL.md
@docs/ui_design/COMPONENTS.md
@docs/ui_design/LAYOUT.md
@docs/ui_design/PREVIEW_AND_TOKENS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add FieldMode to SpoolPickerState + rebuild SpoolScreen/SpoolPicker onto the kit (no gutter, Field-takeover, FootButtonBar, DetailCard+FillMeter, FloatingEStop)</name>
  <files>app/src/main/java/works/mees/dinghy/ui/spool/SpoolScreen.kt, app/src/main/java/works/mees/dinghy/ui/spool/SpoolPicker.kt, app/src/main/res/values/strings.xml</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/spool/SpoolScreen.kt (the file being rebuilt — SpoolContent structure, the gutter Row ~261-294, SpoolDetailFocus ~302-447, the stateless SpoolScreen(state,...) seam ~178-214, the openFilter overlay state ~94)
    - app/src/main/java/works/mees/dinghy/ui/spool/SpoolPicker.kt (the file being rebuilt — SpoolFilterControls ~134-203, the LazyColumn ~91-125, SpoolRow ~494-561, OptionButton ~448-491)
    - .planning/phases/23-design-language-foundation/23-PATTERNS.md §"SpoolScreen.kt (REBUILD on kit)" (the full ScreenScaffold structure, the FieldMode when-branch, the foot-button logic, the callback renames)
    - .planning/phases/23-design-language-foundation/23-RESEARCH.md §"SpoolScreen Pilot Rebuild" (the change table, foot-button logic LOCKED, the SpoolDetailFocus rewrite, the stateless seam, Open Q §3 onLoad/onUnload rename)
    - .claude/skills/sketch-findings-dinghy-display/references (the Spoolman anatomy — via the skill SKILL.md index) and the redesign-direction note §"Spoolman tweaks" + §"Sort vs Filter" (Field-takeover, shaded filter background, remove redundant Spoolman icon, edit/delete top-right, fill meter)
    - all six new components in designsystem/components/ + UnitGrid.kt + ListBlock.kt (the kit being consumed)
    - app/src/main/java/works/mees/dinghy/ui/printstatus/LastJobHolder.kt (~line 66 — the canonical isPrinting derivation: `printState == PrintState.Printing || printState == PrintState.Paused`; reuse, do NOT reinvent)
    - app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusScreen.kt (~line 284/299 — the e-stop dispatch path `dispatcher?.dispatch(CommandRegistry.emergencyStop, Unit)` behind a ConfirmGuard; the print-state source `container.printerState.collectAsStateWithLifecycle()` at ~line 82)
    - app/src/main/java/works/mees/dinghy/command/CommandRegistry.kt (~line 396 — `emergencyStop` CommandSpec; the existing catalog entry, no new MR-* spec needed → no command-catalog-drift)
  </read_first>
  <action>
    Rebuild SpoolScreen/SpoolPicker onto the kit, per PATTERNS §"SpoolScreen.kt (REBUILD on kit)" — a structural
    refactor, NOT a rewrite:
    - Add `fieldMode: FieldMode` to `SpoolPickerState` with `sealed class FieldMode { object Spools; data class
      FilterPicker(val category: SpoolFilterCategory) }`. Tapping a filter tile sets `FilterPicker`; picking an
      option or "Show all"/Back reverts to `Spools`. NO back-stack / screen push (Field-takeover in place). Delete
      the full-screen `SpoolFilterPickerOverlay` path.
    - At the SpoolScreen root: `BoxWithConstraints { val grid = rememberUnitGrid(minOf(maxWidth, maxHeight)); … }`
      and pass `grid.uDp` down to ListRow/FootButtonBar/SortRow/FilterRow/FloatingEStop.
    - Add a DEBUG-ONLY uDp readout so the owner can confirm U is constant across rotation at the on-device gate
      (guards the minOf-vs-insets concern): `if (BuildConfig.DEBUG)` log `Log.d("UnitGrid", "uDp=${grid.uDp} count=${grid.count} min=${minOf(maxWidth, maxHeight)}")` AND optionally render a small `t.text2`/GeistMono
      `uDp` text badge in a corner (debug builds only). It must be removable/guarded so it does NOT ship in release.
    - `ScreenScaffold(focus = { … }, field = { … }, gutter = null)` — gutter NULLED (Pitfall 1).
      Focus = `DetailCard(ringColor = resolvedSpoolColor) { detail content + FillMeter(remaining fraction) }` with a
      `FloatingEStop(visible = isPrinting, onClick = onEmergencyStop, uDp = grid.uDp, ...)` as a Box-sibling overlay
      (TopStart, Pitfall 7). WIRE IT FOR REAL (not a preview-only flag): the live `SpoolScreen` derives
      `isPrinting` from the LIVE printer state — collect `container.printerState` (the same StateFlow PrintStatus
      uses) and compute `isPrinting = printState == PrintState.Printing || printState == PrintState.Paused` (reuse
      the LastJobHolder convention; do NOT add a new isPrinting field). The e-stop `onClick` routes through the
      existing `dispatcher` to `CommandRegistry.emergencyStop` — REUSE the PrintStatus path: gate it behind the
      existing full-screen `ConfirmGuard` (red-destructive), then `dispatcher?.dispatch(CommandRegistry.emergencyStop, Unit)`.
      The stateless `SpoolScreen(state, ...)` preview seam takes `isPrinting: Boolean` + `onEmergencyStop: () -> Unit`
      params (so previews can drive the printing state) but the LIVE overload sources them from container/dispatcher.
      No new CommandSpec is added (emergencyStop already exists → no command-catalog-drift). Plus `SortRow` +
      `FilterRow` below (shaded filter background per the Spoolman tweak). Remove the redundant Spoolman icon from the
      detail pane and keep small edit/delete affordances top-right (per the redesign tweak).
      Field = `when (state.fieldMode)`: `Spools` → `ListBlock { items(spools, key = { it.id }) { ListRow(...) } }` +
      `FootButtonBar`; `FilterPicker` → `ListBlock { option ListRows }` + a `FootButtonBar` with clear/done.
    - Foot buttons (LOCKED): Home (`home`, Intent.Accent) · Scan (`qr_code`, Intent.Accent) · conditional
      Load (`expand_circle_up`, Intent.Accent) when the selected spool is NOT loaded / Unload (`expand_circle_down`,
      Intent.Neutral) when it IS loaded. NO "Set Active" button. Render glyphs via the registry DinghyIcons tokens.
    - Rename the public callbacks `onSetActive`→`onLoad`, `onClearActive`→`onUnload` (Open Q §3); the internal
      holder calls (`setActiveSpool`/`clearActiveSpool`) are UNCHANGED.
    - PRESERVE the stateless `SpoolScreen(state, …)` preview seam (add the new params + isPrinting). Add a
      `cd_*`/string for any new label. Keep all chrome token-routed, text via fsSp, icons registry-only.
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --tests works.mees.dinghy.ui.spool.* --no-daemon" 2>&1 | tr -d '\r' | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - SpoolScreen.kt consumes DetailCard, FillMeter, FootButtonBar, FloatingEStop, SortRow, FilterRow, ListRow, ListBlock (grep finds each)
    - `ScreenScaffold(... gutter = null ...)` (`grep -c 'gutter = null' SpoolScreen.kt` >= 1); `SpoolFilterPickerOverlay` removed (`grep -c 'SpoolFilterPickerOverlay' app/src/main` == 0)
    - `FieldMode` sealed class exists in SpoolPickerState; `rememberUnitGrid(minOf(maxWidth, maxHeight))` at the root
    - Foot buttons match the locked logic (Home/Scan + conditional Load expand_circle_up accent / Unload expand_circle_down neutral; no Set-Active)
    - FloatingEStop is wired to LIVE state, not a preview flag: the live SpoolScreen derives `isPrinting` from `container.printerState` (`grep -E 'printerState|PrintState.Printing' SpoolScreen.kt` finds the derivation) and the e-stop onClick dispatches `CommandRegistry.emergencyStop` via `dispatcher` behind a ConfirmGuard (`grep -c 'emergencyStop' SpoolScreen.kt` >= 1); NO new CommandSpec added (`grep -c 'CommandSpec' SpoolScreen.kt` unchanged from baseline)
    - A DEBUG-guarded uDp readout/log exists (`grep -E 'BuildConfig.DEBUG.*uDp|Log.d\\("UnitGrid"' SpoolScreen.kt` finds it); it is release-stripped
    - Foot buttons / control glyphs render via the DinghyIcon-aware `OutlinedControl(..., icon = ...)` overload (registered tokens), never raw ligature strings
    - Callbacks renamed onLoad/onUnload; existing `works.mees.dinghy.ui.spool.*` host tests GREEN (SpoolHolderTest, SpoolPickerStateTest)
  </acceptance_criteria>
  <done>SpoolScreen is rebuilt on the kit — no gutter, Field-takeover filter, FootButtonBar with the locked Load/Unload logic, DetailCard+FillMeter, FloatingEStop wired to the live printerState + emergencyStop dispatch (not a preview flag), U at the root with a debug uDp readout — host suite GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: Update SpoolPreviews matrix + SampleFixtures for the FieldMode/filter-open state; full host suite + assembleDebug GREEN</name>
  <files>app/src/main/java/works/mees/dinghy/preview/SpoolPreviews.kt, app/src/main/java/works/mees/dinghy/preview/SampleFixtures.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/preview/SpoolPreviews.kt (the file being updated — SpoolSelectionProvider, the 6 theme fns, fs=L, RTL, pseudolocale)
    - app/src/main/java/works/mees/dinghy/preview/SampleFixtures.kt (add a filter-open fixture per RESEARCH §"Stateless preview seam" — spoolWithFilterOpen)
    - app/src/main/java/works/mees/dinghy/preview/PreviewTheming.kt (PreviewBox, fsLargeSeed, combos)
    - docs/ui_design/PREVIEW_AND_TOKENS.md (the matrix convention + fs=L gotcha)
  </read_first>
  <action>
    Update `SpoolPreviews.kt` so the matrix covers the new states: at least (a) no-selection, (b) selected spool with
    FillMeter, (c) FieldMode.FilterPicker open (Field showing the option list), and (d) a printing state
    (`isPrinting = true`) so the FloatingEStop overlay is exercised in the preview matrix (drive the stateless
    `SpoolScreen(state, ..., isPrinting = true, ...)` seam). Add a `spoolWithFilterOpen` fixture to `SampleFixtures.kt`. Keep the full matrix shape (6 theme combos via PreviewBox, `*FsLargeOverflow` via
    `PreviewBox(fsLargeSeed)`, `*RtlSpotCheck`, standalone `*PseudolocaleSpotCheck`). Then run the FULL host unit suite
    and `assembleDebug` to confirm no regression across the app from the kit consumption + callback renames.
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest :app:assembleDebug --no-daemon" 2>&1 | tr -d '\r' | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - SpoolPreviews.kt includes a FieldMode.FilterPicker-open preview AND a printing-state preview (`isPrinting = true`) exercising the FloatingEStop; `spoolWithFilterOpen` fixture exists
    - The matrix shape is preserved (6 combos + fs=L via fsLargeSeed + RTL + pseudolocale)
    - FULL `:app:testDebugUnitTest` GREEN and `:app:assembleDebug` BUILD SUCCESSFUL (no app-wide regression)
  </acceptance_criteria>
  <done>The Spool preview matrix covers the rebuilt states incl. the filter-open Field-takeover; full host suite + assembleDebug GREEN.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: On-device flox owner approval — SpoolScreen pilot in BOTH orientations</name>
  <files>(none — owner eyeball gate; debug APK built + installed on flox)</files>
  <action>
    Build + installDebug the rebuilt SpoolScreen on flox, then present the verification walk below to the owner and
    WAIT for approval. This is an owner-eyeball gate (no autonomous resolution); do not write the SUMMARY or advance
    the plan until the owner replies. If issues are reported, fix them, rebuild/reinstall, and re-present.
  </action>
  <what-built>
    SpoolScreen rebuilt on the new component-class kit: translucent ListRows in a ListBlock, a filled DetailCard
    with a horizontal FillMeter, a FootButtonBar (Home · Scan · conditional Load/Unload) pinned to the foot of the
    list, an in-place Field-takeover filter picker (no full-screen overlay), a sort row that toggles in place with a
    direction indicator, a printing-only FloatingEStop overlay, and U-derived row/button heights — no gutter. The
    debug APK is built and installed on flox.
  </what-built>
  <how-to-verify>
    On flox (LineageOS 18.1 / API 30 / genuine Adreno 320), against a printer running Spoolman (E5+ 192.168.1.120:7125
    or E3 192.168.1.121:7125):
    1. Open the Spool screen. Confirm the list rows read as translucent content and the foot buttons / filter tiles
       read as filled controls (the content-vs-controls fill convention is visible).
    2. Scroll the spool list — smooth on the Adreno-320 floor, no frozen frames, edge-fade hint appears.
    3. Tap a spool — the DetailCard shows the color-reactive ring (filament color) + the FillMeter remaining fraction.
    4. Tap a filter tile (Material/Color/Brand) — the Field swaps IN PLACE to the option list (no full-screen overlay);
       pick one → Field filters and swaps back; a clear/"Show all" reverts.
    5. Tap a sort tile — the list reorders in place with a direction indicator on the active tile.
    6. Confirm the foot buttons: Home + Scan always present; the third button is Load (up) when the selected spool is
       not loaded, Unload (down) when it is; Load actually sets the active spool.
    7. ROTATE the device portrait↔landscape on the Spool screen — the layout reflows (portrait stacks Focus/Field,
       landscape is side-by-side) and ROW HEIGHTS DO NOT CHANGE (U constant through rotation). Read the DEBUG uDp
       readout/log (`adb logcat -s UnitGrid` or the corner badge) in BOTH orientations and confirm the uDp value is
       EQUAL portrait vs landscape (guards the minOf-vs-insets concern — if insets shift the short edge, uDp would
       differ and this catches it). Verify in BOTH orientations.
    8. (If a print is active — start one on the printer, or pause one) confirm the FloatingEStop appears top-left of
       the Focus; with NO active print it is ABSENT (proving it is wired to the LIVE printerState, not always-on).
       Tapping it raises the red ConfirmGuard; confirming dispatches the real emergency-stop (only test the guard
       appearance unless the owner wants a live e-stop fired).
  </how-to-verify>
  <verify>
    <human-check>Owner confirms on flox, in BOTH portrait and landscape: fill convention visible (translucent list / filled controls), smooth scroll + edge-fade (list scrolls UNDER the fade — fade does not block touch), DetailCard color ring + FillMeter, in-place Field-takeover filter, in-place sort with direction indicator (no group-label words), conditional Load/Unload foot buttons (Load sets active), U constant through rotation (debug uDp readout EQUAL portrait vs landscape), FloatingEStop printing-only (absent with no active print, present when printing/paused, raises the red ConfirmGuard on tap).</human-check>
  </verify>
  <resume-signal>Reply "approved" (both orientations look right), or describe what's off so it can be fixed before close-out.</resume-signal>
  <done>Owner replies "approved" after verifying the SpoolScreen pilot on flox in both orientations; all locked rules render correctly on the real hardware floor, uDp is confirmed equal across rotation via the debug readout, and FloatingEStop is confirmed printing-only against live state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Spoolman filament color/name → DetailCard.ringColor / FillMeter.fillColor / ListRow content | Filament data from the trusted-LAN Moonraker Spoolman component, rendered as Color/Text. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-06-01 | Injection | filament color/name rendered in the rebuilt SpoolScreen | mitigate | Color parsed to a numeric `Color`; names render as plain Compose `Text` — no markup/HTML/format/eval sink on this path (the only rich-text path in the app is PromptMarkup, not touched here). |
| T-23-06-02 | Tampering | n/a | accept | Structural UI refactor; no new storage/network/auth. The internal setActiveSpool/clearActiveSpool calls are unchanged (already-existing trusted command path). |

No new attack surface introduced — a presentation refactor of an existing screen over existing trusted-LAN Spoolman data; the active-spool command path is unchanged.
</threat_model>

<verification>
- `:app:testDebugUnitTest --tests works.mees.dinghy.ui.spool.*` GREEN (Task 1).
- FULL `:app:testDebugUnitTest` + `:app:assembleDebug` GREEN (Task 2 — no app-wide regression).
- On-device flox owner approval in BOTH orientations (Task 3 — the SC-3 gate).
</verification>

<success_criteria>
- SC-3: SpoolScreen rebuilt on the classes and owner-approved on flox in BOTH orientations (the pilot).
- SC-4: the pilot demonstrates the locked rules — unit grid, translucent-list/filled-controls, no-label grouping, icon-registry glyphs, sort-vs-filter + Field-takeover picker.
</success_criteria>

<output>
Create `.planning/phases/23-design-language-foundation/23-06-SUMMARY.md` when done.
</output>
