---
phase: 23-design-language-foundation
plan: 03
type: execute
wave: 1
depends_on: ["23-01"]
files_modified:
  - app/src/main/java/works/mees/dinghy/designsystem/icons/DinghyIcons.kt
  - img/material-icon-bucket.json
  - docs/ui_design/reference/hifi.css
autonomous: true
requirements: []
must_haves:
  truths:
    - "Six new owner-assigned icons (Sort, FilterList, ExpandCircleUp, ExpandCircleDown, ResetWrench, ResetSettings) are registered in DinghyIcons and DinghyIcons.all with unique alternates"
    - "DinghyIconsTest stays GREEN with the 6 new ligature-backed entries"
    - "verify_ligatures.py gate exits 0 (the 6 new ligatures resolve in v2.944)"
    - "img/material-icon-bucket.json play_circle/stop_circle notes reconciled (load/unload moved to expand_circle_up/down)"
    - "hifi.css .ctl.warn no longer uses color-mix(in oklch, --heat, --outline) — uses --heat directly so caution reads amber not red"
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/designsystem/icons/DinghyIcons.kt"
      provides: "6 new redesign glyph registrations"
      contains: "ExpandCircleUp"
    - path: "docs/ui_design/reference/hifi.css"
      provides: "Fixed .ctl.warn caution-reads-amber rule"
      contains: "--heat"
  key_links:
    - from: "app/src/main/java/works/mees/dinghy/designsystem/icons/DinghyIcons.kt"
      to: "DinghyIcons.all"
      via: "all 6 new vals added to the drift-guard list"
      pattern: "Sort, FilterList|ExpandCircleUp"
    - from: "img/material-icon-bucket.json"
      to: "DinghyIcons.kt"
      via: "play_circle/stop_circle notes reconciled to point at expand_circle_up/down"
      pattern: "expand_circle"
---

<objective>
Register the 6 new owner-assigned redesign glyphs, reconcile the stale play_circle/stop_circle bucket
notes, and fix the oklch caution-reads-red bug in hifi.css.

Purpose: SC-5. The redesign introduced 6 owner-assigned Material Symbols (sort, filter_list,
expand_circle_up = Load spool, expand_circle_down = Unload spool, reset_wrench, reset_settings). Per the
HARD owner icon law, these glyphs are ALREADY chosen — this plan only REGISTERS them by ligature (never
auto-picks). The old bucket notes tag play_circle/stop_circle as load/unload, which the owner reassigned —
clean that up so two glyphs don't claim one function. SC-5 also fixes the `.ctl.warn` `color-mix(in oklch,…)`
bug that makes amber caution borders render orange-red (the Kotlin side already uses `t.heat` directly — this
is CSS-only). Low-coupling, unblocks the kit's icon references.

Output: 6 registered icons (drift-guard GREEN, ligature gate GREEN), reconciled bucket notes, fixed CSS.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/23-design-language-foundation/23-RESEARCH.md
@.planning/phases/23-design-language-foundation/23-PATTERNS.md
@.planning/notes/2026-06-09-icon-assignments-redesign.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register the 6 new redesign glyphs in DinghyIcons.kt + DinghyIcons.all</name>
  <read_first>
    - app/src/main/java/works/mees/dinghy/designsystem/icons/DinghyIcons.kt (the file being modified — the val-entry registration pattern + the hand-rolled `all` list)
    - .planning/phases/23-design-language-foundation/23-PATTERNS.md §"DinghyIcons.kt (ADD 6 entries)" (the exact val names, ligatures, and alternate strings)
    - .planning/notes/2026-06-09-icon-assignments-redesign.md (owner source of truth for the 6 glyph→function bindings)
  </read_first>
  <action>
    Add 6 new `val` entries to the `DinghyIcons` object, each `DinghyIcon(IconRef.Ligature("<lig>"), alternate = "<lig>")`
    with a UNIQUE alternate (mirror the per-entry pattern Phase 19 used for the Output* tokens):
    `Sort`="sort", `FilterList`="filter_list", `ExpandCircleUp`="expand_circle_up",
    `ExpandCircleDown`="expand_circle_down", `ResetWrench`="reset_wrench", `ResetSettings`="reset_settings".
    Add all 6 to the `DinghyIcons.all` list. These ligatures were proven present by plan 23-01 Task 1
    (verify_ligatures.py exit 0). Do NOT auto-pick anything — these are the owner-locked assignments.
    `:app:compileDebugKotlin` must stay GREEN.
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --tests works.mees.dinghy.designsystem.icons.DinghyIconsTest --no-daemon" 2>&1 | tr -d '\r' | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'ExpandCircleUp\|ExpandCircleDown\|ResetWrench\|ResetSettings\|val Sort\|val FilterList' DinghyIcons.kt` shows all 6 vals present
    - All 6 are in `DinghyIcons.all`
    - `DinghyIconsTest` GREEN (unique alternates, all sources resolvable — derives dynamically from `all`)
    - `python tools/verify_ligatures.py` still exits 0
  </acceptance_criteria>
  <done>The 6 owner-assigned redesign glyphs are registered with unique alternates, in `all`, drift-guard and ligature gate GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile play_circle/stop_circle notes in img/material-icon-bucket.json</name>
  <read_first>
    - img/material-icon-bucket.json (the file being modified — the current play_circle/stop_circle notes claiming load/swap and unload)
    - .planning/notes/2026-06-09-icon-assignments-redesign.md §"Registry conflict to clean up" (the exact reconciliation instruction)
    - .planning/phases/23-design-language-foundation/23-RESEARCH.md §"play_circle / stop_circle conflict cleanup"
  </read_first>
  <action>
    Update the `notes` fields for `play_circle` and `stop_circle` in `img/material-icon-bucket.json` to record
    that load/unload moved to `expand_circle_up`/`expand_circle_down` — e.g. "available for reassignment —
    load/unload moved to expand_circle_up/expand_circle_down (jiib redesign 2026-06-09)". Keep both glyphs in the
    bucket (they may be wanted for a separate "swap" concept per the note). Do NOT add play_circle/stop_circle to
    `DinghyIcons.kt`. Keep the JSON well-formed.
  </action>
  <verify>
    <automated>cd /mnt/e/claude/personal/github/dinghy-display && python -c "import json; d=json.load(open('img/material-icon-bucket.json')); print('valid json')" && grep -q 'expand_circle' img/material-icon-bucket.json && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `img/material-icon-bucket.json` is valid JSON (python json.load succeeds)
    - The play_circle/stop_circle notes reference the reassignment to expand_circle_up/down
    - play_circle/stop_circle are NOT added to DinghyIcons.kt (`grep -c 'PlayCircle\|StopCircle' DinghyIcons.kt` unchanged)
  </acceptance_criteria>
  <done>The load/unload glyph conflict is reconciled — only expand_circle_up/down own load/unload; play_circle/stop_circle notes updated.</done>
</task>

<task type="auto">
  <name>Task 3: Fix the .ctl.warn oklch color-mix caution-reads-red bug in hifi.css</name>
  <read_first>
    - docs/ui_design/reference/hifi.css (the file being modified — the `.ctl.warn` rule at ~line 225 with the color-mix)
    - .planning/phases/23-design-language-foundation/23-RESEARCH.md §"hifi.css .ctl.warn Bug Fix" (the exact before/after)
    - .planning/notes/2026-06-09-jiib-redesign-direction.md §"oklch color-mix hazard" (the rationale)
    - app/src/main/java/works/mees/dinghy/designsystem/control/OutlinedControl.kt (confirms the Kotlin side already uses t.heat directly for Intent.Warn — NO Kotlin change needed)
  </read_first>
  <action>
    Replace the `.ctl.warn` rule's `border-color: color-mix(in oklch, var(--heat) 62%, var(--outline));` with
    `border-color: var(--heat);` plus `color: var(--heat);` and `--ctl-glow: var(--heat-glow);`, and set
    `.ctl.warn:hover { background: var(--heat-soft); }` (per RESEARCH §"The fix"). The amber-to-blue-gray oklch
    interpolation takes the short hue path through red — using `--heat` directly is the fix. This is CSS-ONLY:
    do NOT touch `OutlinedControl.kt` (it already maps `Intent.Warn -> t.heat` with no mix — verified). Make sure
    no other `.ctl.warn`-related `color-mix(in oklch, ... --heat ... --outline ...)` survives.
  </action>
  <verify>
    <automated>cd /mnt/e/claude/personal/github/dinghy-display && C=$(grep -c 'color-mix(in oklch[^)]*--heat[^)]*--outline' docs/ui_design/reference/hifi.css); echo "heat-outline-mix count=$C"; test "$C" = "0" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'color-mix(in oklch[^)]*--heat[^)]*--outline' docs/ui_design/reference/hifi.css` returns 0
    - `.ctl.warn` border-color is `var(--heat)` (direct), not a color-mix
    - `OutlinedControl.kt` is unmodified (CSS-only fix; `git diff --quiet -- app/.../OutlinedControl.kt`)
  </acceptance_criteria>
  <done>The `.ctl.warn` caution border uses `--heat` directly; the oklch-mix-reads-red bug is gone; no Kotlin touched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Icon registry config + CSS reference + a bundled-font lookup. No runtime input, network, auth, or storage surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-03-01 | Tampering | img/material-icon-bucket.json | accept | Committed build/config asset, not user/network input; JSON validity asserted in verify. |
| T-23-03-SC | Tampering | npm/pip/cargo installs | n/a | No package installs in this plan. |

No new attack surface introduced — icon registration + a CSS reference fix over committed assets.
</threat_model>

<verification>
- `DinghyIconsTest` GREEN with 6 new entries; `verify_ligatures.py` exit 0.
- `material-icon-bucket.json` valid JSON, notes reconciled.
- `hifi.css` `.ctl.warn` heat-outline color-mix count = 0; OutlinedControl.kt untouched.
</verification>

<success_criteria>
- SC-5: 6 new icons registered (DinghyIcons.kt + bucket) with stale play_circle/stop_circle notes reconciled; the oklch caution-reads-red bug fixed in hifi.css.
</success_criteria>

<output>
Create `.planning/phases/23-design-language-foundation/23-03-SUMMARY.md` when done.
</output>
