---
phase: 23-design-language-foundation
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - tools/verify_ligatures.py
  - app/src/test/java/works/mees/dinghy/designsystem/icons/DinghyIconsTest.kt
  - app/src/test/java/works/mees/dinghy/designsystem/layout/UnitGridTest.kt
  - app/src/test/java/works/mees/dinghy/designsystem/components/ListRowTest.kt
  - app/src/test/java/works/mees/dinghy/designsystem/components/FillMeterTest.kt
autonomous: true
requirements: []
must_haves:
  truths:
    - "verify_ligatures.py NEEDED set includes the 6 new redesign ligatures and the gate exits 0 against the bundled v2.944 font"
    - "Wave-0 RED test stubs compile day-one (whole test sourceset compiles) with fail() bodies and no references to unbuilt symbols"
    - "DinghyIconsTest stays GREEN and continues to derive its uniqueness/drift checks dynamically from DinghyIcons.all"
  artifacts:
    - path: "tools/verify_ligatures.py"
      provides: "Wave-0 ligature presence gate for the 6 new redesign glyphs"
      contains: "expand_circle_up"
    - path: "app/src/test/java/works/mees/dinghy/designsystem/layout/UnitGridTest.kt"
      provides: "RED scaffold for rememberUnitGrid pure formula (N in [5,7], uDp >= 64dp)"
    - path: "app/src/test/java/works/mees/dinghy/designsystem/components/ListRowTest.kt"
      provides: "RED scaffold for ListRow fill/border state selection"
    - path: "app/src/test/java/works/mees/dinghy/designsystem/components/FillMeterTest.kt"
      provides: "RED scaffold for FillMeter fraction clamping"
  key_links:
    - from: "tools/verify_ligatures.py"
      to: "fonts/material_symbols_outlined.ttf (v2.944)"
      via: "fonttools cmap/ligature lookup"
      pattern: "expand_circle_up|filter_list|reset_wrench"
---

<objective>
Lay the Wave-0 test/tooling foundation for Phase 23 so the rest of the phase has an automated
feedback loop and the new glyphs are proven present BEFORE any registration or component build.

Purpose: The phase introduces 6 new owner-assigned Material Symbols ligatures (presence is ASSUMED
per research A1) and three new kit primitives (UnitGrid, ListRow, FillMeter) that need pure-logic
host tests. Per [[dinghy-wave0-red-scaffold-compile]], the RED scaffolds must COMPILE day-one
because Gradle compiles the entire test sourceset before applying any `--tests` filter — a broken
scaffold bricks every per-wave test run.

Output: an extended `verify_ligatures.py` NEEDED set, three RED test files with `fail()` bodies
(no references to symbols built in later waves), and a confirmed-GREEN `DinghyIconsTest`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-design-language-foundation/23-RESEARCH.md
@.planning/phases/23-design-language-foundation/23-PATTERNS.md
@.planning/notes/2026-06-09-icon-assignments-redesign.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend verify_ligatures.py with the 6 new redesign ligatures and run the gate</name>
  <read_first>
    - tools/verify_ligatures.py (the file being modified — read its NEEDED-set structure and how Phase 19/20 added glyphs)
    - .planning/notes/2026-06-09-icon-assignments-redesign.md (the owner-assigned glyph list — the source of truth for the 6 ligatures)
    - .planning/phases/23-design-language-foundation/23-RESEARCH.md §"Icon Registry Changes" (the assumption A1 that these resolve in v2.944)
  </read_first>
  <action>
    Add the 6 new owner-assigned ligatures to the NEEDED set in `tools/verify_ligatures.py`:
    `sort`, `filter_list`, `expand_circle_up`, `expand_circle_down`, `reset_wrench`, `reset_settings`
    (exact ligatures from the icon-assignments note — these are the only 6; do NOT add play_circle/stop_circle).
    Follow the same NEEDED-list-append pattern Phase 19 used (STATE.md plan 19-01 Task 2 added 8 glyphs the same way).
    Do NOT remove any existing entries. Run the gate: it must exit 0 with `missing: []`. If any of the
    6 do NOT resolve in the bundled v2.944 `material_symbols_outlined.ttf`, STOP and report which ones —
    per research A1 the fallback policy is owner notification, not silent substitution (icon law).
  </action>
  <verify>
    <automated>cd /mnt/e/claude/personal/github/dinghy-display && python tools/verify_ligatures.py; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `tools/verify_ligatures.py` NEEDED set contains all 6 ligatures (`grep -E 'sort|filter_list|expand_circle_up|expand_circle_down|reset_wrench|reset_settings' tools/verify_ligatures.py` finds each)
    - `python tools/verify_ligatures.py` exits 0 with `missing: []`
  </acceptance_criteria>
  <done>The gate confirms all 6 new ligatures are present in the bundled v2.944 font (A1 discharged), or reports the missing set for owner escalation.</done>
</task>

<task type="auto">
  <name>Task 2: Confirm DinghyIconsTest drift guard stays GREEN (no edit expected)</name>
  <read_first>
    - app/src/test/java/works/mees/dinghy/designsystem/icons/DinghyIconsTest.kt (the existing drift guard — confirm it derives uniqueness/drift dynamically from DinghyIcons.all)
    - app/src/main/java/works/mees/dinghy/designsystem/icons/DinghyIcons.kt (the registry it guards)
  </read_first>
  <action>
    Read `DinghyIconsTest.kt` and confirm it derives its uniqueness/source-resolvable checks DYNAMICALLY
    from `DinghyIcons.all` (per STATE.md 19-01: "DinghyIconsTest needed NO edit — it derives uniqueness/drift
    checks dynamically from DinghyIcons.all"). Run it now against the UNCHANGED registry to establish the
    GREEN baseline before Wave-1 registrations. Do NOT modify the registry in this plan (that is plan 23-03).
    If the test does NOT derive dynamically and would need a hardcoded edit for the 6 new entries, NOTE that
    in the SUMMARY so plan 23-03 can extend it — but the expected outcome is no edit needed.
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --tests works.mees.dinghy.designsystem.icons.DinghyIconsTest --no-daemon" 2>&1 | tr -d '\r' | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `:app:testDebugUnitTest --tests ...DinghyIconsTest` reports BUILD SUCCESSFUL / all tests pass
    - SUMMARY records whether the test derives dynamically (expected: yes, no edit needed)
  </acceptance_criteria>
  <done>DinghyIconsTest GREEN baseline established; the dynamic-derivation property is confirmed (or flagged for 23-03).</done>
</task>

<task type="auto">
  <name>Task 3: Write three RED pure-logic test scaffolds (UnitGrid, ListRow, FillMeter) that compile day-one</name>
  <read_first>
    - .planning/phases/23-design-language-foundation/23-RESEARCH.md §"Unit Grid", §"Validation Architecture / Wave 0 Gaps" (the exact formula + the three test files named)
    - .planning/phases/23-design-language-foundation/23-PATTERNS.md §UnitGrid, §ListRow, §FillMeter (the formula + fill/border + clamp logic to assert)
    - app/src/test/java/works/mees/dinghy/designsystem/ScrubberMappingTest.kt (existing host-test style/idiom in this package — JUnit4, pure-logic)
    - the memory note [[dinghy-wave0-red-scaffold-compile]] (RED stubs MUST compile: fail() bodies, no refs to unbuilt symbols)
  </read_first>
  <action>
    Create three RED scaffolds. They MUST COMPILE day-one: every import resolvable NOW, NO references to
    `rememberUnitGrid`/`UnitGrid`/`ListRow`/`FillMeter` (those symbols don't exist until Waves 2-3) — bodies
    are `org.junit.Assert.fail("RED: implemented in 23-04/23-05")` and the test names document intent.
    Use JUnit4 + Kotlin (the existing host-test idiom; see ScrubberMappingTest.kt). Files:
    - `app/src/test/.../designsystem/layout/UnitGridTest.kt` — named tests documenting the formula contract:
      `n_clamps_to_5_floor`, `n_clamps_to_7_ceiling`, `udp_never_below_64dp`, `udp_equals_dim_over_n`.
      These assert the PURE arithmetic (`N = round(dim/41).coerceIn(5,7)`, `uDp = (dim/N).coerceAtLeast(64dp)`)
      which is testable without Compose — but leave bodies as fail() in this RED plan; 23-04 turns them GREEN
      by extracting the formula into a pure helper the test can call.
    - `app/src/test/.../designsystem/components/ListRowTest.kt` — `selected_uses_accentSoft_fill`,
      `unselected_uses_transparent_fill`, `selected_uses_2dp_border` (fail() bodies).
    - `app/src/test/.../designsystem/components/FillMeterTest.kt` — `fraction_clamps_below_zero_to_zero`,
      `fraction_clamps_above_one_to_one`, `fraction_in_range_unchanged` (fail() bodies).
    Create the `designsystem/layout` and `designsystem/components` test packages if absent. Confirm the WHOLE
    test sourceset still compiles (Gradle compiles all of it before --tests filtering).
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:compileDebugUnitTestKotlin --no-daemon" 2>&1 | tr -d '\r' | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - All three files exist under the named test packages
    - `:app:compileDebugUnitTestKotlin` reports BUILD SUCCESSFUL (whole test sourceset compiles — no unresolved refs)
    - Running the three filters shows the fail() RED state (expected red, proving the scaffolds execute)
    - `grep -L 'rememberUnitGrid\|ListRow(\|FillMeter(' UnitGridTest.kt ListRowTest.kt FillMeterTest.kt` confirms no refs to unbuilt symbols
  </acceptance_criteria>
  <done>Three RED scaffolds compile day-one with fail() bodies and zero references to Wave-2/3 symbols; the per-wave test gate is unbrickable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan is test scaffolds + a Python font-presence gate. No runtime, network, auth, or storage surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-01-01 | Tampering | bundled Material Symbols font | accept | Font is a committed build asset, not user/network input; `verify_ligatures.py` only READS glyph presence. No injection path. |

No new attack surface introduced — this is presentation-tooling and host test scaffolds over a committed font asset.
</threat_model>

<verification>
- `python tools/verify_ligatures.py` exits 0 (all 6 new ligatures resolve in v2.944).
- `:app:compileDebugUnitTestKotlin` BUILD SUCCESSFUL (RED scaffolds compile day-one).
- `:app:testDebugUnitTest --tests ...DinghyIconsTest` GREEN.
</verification>

<success_criteria>
- The 6 new ligatures are proven present (A1 discharged) and the gate is wired for the rest of the phase.
- Three RED scaffolds exist, compile, and reference no unbuilt symbols.
- DinghyIconsTest GREEN baseline recorded.
</success_criteria>

<output>
Create `.planning/phases/23-design-language-foundation/23-01-SUMMARY.md` when done.
</output>
