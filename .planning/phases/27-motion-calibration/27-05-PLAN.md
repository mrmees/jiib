---
phase: 27-motion-calibration
plan: 05
type: execute
wave: 3
depends_on: ["27-02", "27-04"]
files_modified:
  - app/src/main/java/works/mees/dinghy/ui/calibration/BedMeshScreen.kt
  - app/src/main/res/values/strings.xml
  - app/src/main/java/works/mees/dinghy/preview/SampleFixtures.kt
  - app/src/main/java/works/mees/dinghy/preview/CalibrationPreviews.kt
autonomous: true
requirements: [SC-2, SC-3, SC-4, SC-5]
user_setup: []
must_haves:
  truths:
    - "Field = saved-profile ListRow list (active profile marked); the full-screen Load selector dialog AND the Save-name dialog are deleted entirely (D-11)"
    - "Row tap selects a profile (no immediate dispatch); an Apply foot loads it; Remove acts on selection behind the red cannot-be-undone ConfirmGuard (D-12)"
    - "Save-name is a Field-takeover (MeshFieldMode.SaveName) with the alphanumeric keyboard, pre-filled YY.MM.DD_HH.MM, gated by PrinterCommands.isValidProfileName before dispatch (D-13)"
    - "Foot is state-adaptive: unhomed -> Home All; homed+no-selection -> Calibrate+Save+Back; profile-selected -> Apply+Remove+Back; the amber SAVE_CONFIG restart gate survives (D-14)"
    - "Focus = BedMeshHeatmapHost (Views) with the Phase-22 applyTokens equality guards preserved (no AndroidView factory recreation on recomposition)"
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/ui/calibration/BedMeshScreen.kt"
      provides: "Field profile-list + MeshFieldMode SaveName takeover + heatmap Focus, gutter=null"
      contains: "MeshFieldMode"
  key_links:
    - from: "BedMesh SaveName takeover confirm"
      to: "PrinterCommands.isValidProfileName"
      via: "validation gate disables confirm on invalid name"
      pattern: "isValidProfileName"
    - from: "BedMesh Focus"
      to: "BedMeshHeatmapHost (Views)"
      via: "AndroidView with P22 equality guard, not recreated per recomposition"
      pattern: "BedMeshHeatmapHost"
---

<objective>
Rebuild BedMeshScreen — the most structurally-changed screen in Phase 27. The full-screen Load
selector dialog and Save-name dialog die entirely; the profile flow becomes a Field list + a
`MeshFieldMode` Field-takeover (the P26 FieldMode pattern, mirroring TemperatureScreen's `TempFieldMode`).

D-11: Field = the saved-profile `ListRow` list (active profile marked); the `LoadSelectorDialog`
overlay disappears. Focus stays the Views `BedMeshHeatmapHost` heatmap (+ scale-mode toggle,
empty-state copy) with the Phase-22 `applyTokens` equality guards preserved.
D-12: Row tap selects (no immediate dispatch); an Apply foot loads (`BED_MESH_PROFILE LOAD`); Remove
acts on selection behind the red "cannot be undone" ConfirmGuard.
D-13: Save-name = `MeshFieldMode.SaveName` Field-takeover with the system ALPHANUMERIC keyboard (the
sanctioned carve-out), pre-filled with the auto timestamp `YY.MM.DD_HH.MM` default, gated by
`PrinterCommands.isValidProfileName` (invalid name never reaches dispatch).
D-14: State-adaptive foot — unhomed -> Home All; homed+no-selection -> Calibrate+Save+Back;
profile-selected -> Apply+Remove+Back; the amber SAVE_CONFIG restart gate after profile-save survives.

This plan extends CalibrationPreviews.kt (created by 27-04) with the BedMesh section, so it depends on
27-04. It does NOT add a `LaunchedEffect(Unit){ holder.reset() }` (BedMeshHolder has no reset() and the
profile list survives navigation — Pitfall 3).

Purpose: Deliver the bed-mesh profile management surface on the redesign with no overlay dialogs and a
validated save-name takeover.
Output: Rebuilt BedMeshScreen + BedMesh preview section.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/27-motion-calibration/27-CONTEXT.md
@.planning/phases/27-motion-calibration/27-UI-SPEC.md
@.planning/phases/27-motion-calibration/27-PATTERNS.md
@docs/ui_design/COMPONENTS.md
@docs/ui_design/THEMING.md
@docs/adr/0001-ui-toolkit-decision.md
@app/src/main/java/works/mees/dinghy/ui/calibration/BedMeshScreen.kt
@app/src/main/java/works/mees/dinghy/ui/temperature/TemperatureScreen.kt
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Rebuild BedMeshScreen — Field profile list + MeshFieldMode SaveName takeover + heatmap Focus (D-11..D-14)</name>
  <files>app/src/main/java/works/mees/dinghy/ui/calibration/BedMeshScreen.kt, app/src/main/res/values/strings.xml</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/calibration/BedMeshScreen.kt (full file — the SaveNameDialog + LoadSelectorDialog overlays + MeshDialog enum to DELETE, the gutter rows, defaultProfileName(), the heatmap host wiring, scale-mode toggle, empty-state copy)
    - app/src/main/java/works/mees/dinghy/ui/temperature/TemperatureScreen.kt lines ~91-94 (TempFieldMode sealed class) and ~529-645 (the when(fieldMode) field-takeover structure to mirror)
    - app/src/main/java/works/mees/dinghy/ui/extrude/ExtrudeScreen.kt ~209 (thin-wrapper -> stateless `ExtrudeContent` seam to copy so @Preview renders without a VM — WARNING-5)
    - app/src/main/java/works/mees/dinghy/calibration/BedMeshHolder.kt + BedMeshModel.kt (vm.profileNames, vm.activeProfileName, vm.model, scale mode, homedGate — consume unchanged; NO reset() exists, do not add)
    - app/src/main/java/works/mees/dinghy/render/BedMeshHeatmapHost.kt + BedMeshHeatmapView.kt (the P22 applyTokens equality guard — must survive re-parenting; do NOT recreate the AndroidView factory per recomposition)
    - app/src/main/java/works/mees/dinghy/.../PrinterCommands.kt (isValidProfileName + defaultProfileName — the validation gate + timestamp default)
    - app/src/main/java/works/mees/dinghy/designsystem/components/ListRow.kt + FootButtonBar.kt + ConfirmGuard
    - .planning/phases/27-motion-calibration/27-PATTERNS.md §"ui/calibration/BedMeshScreen.kt" (MeshFieldMode sealed class, when(fieldMode) field, the heatmap preview placeholder idiom, the writeScope note) and 27-UI-SPEC.md §"BedMeshScreen" (the D-14 foot-state table + SaveName takeover spec + empty/error/destructive copy)
  </read_first>
  <action>
    DELETE the `SaveNameDialog` + `LoadSelectorDialog` composables and the `MeshDialog` enum entirely — no full-screen
    overlay survives. Add `private sealed class MeshFieldMode { data object ProfileList; data class SaveName(val prefill: String) }`
    mirroring TempFieldMode. Root: BoxWithConstraints + rememberUnitGrid + ScreenScaffold(gutter=null) + FloatingEStop top-left.
    Focus = the existing `BedMeshHeatmapHost` (Views) — keep the P22 `applyTokens` equality guard intact (do NOT recreate the
    AndroidView factory on recomposition); add the `LocalInspectionMode` -> `PreviewPlaceholderBox(label = cd_mesh_heatmap)`
    branch for preview safety; keep the scale-mode toggle + empty-state copy. Field = `when(fieldMode)`:
    (a) `ProfileList` -> `ListBlock` of `ListRow`s over `vm.profileNames` (key=name): `selected = name == selectedProfile`,
    `onClick = { selectedProfile = name }` (D-12 select-no-dispatch), trailing "Active" badge (`t.accent2`) when
    `name == vm.activeProfileName`; below it a state-adaptive FootButtonBar per the D-14 table — unhomed -> [Home All Accent | Back];
    homed+no-selection -> [Calibrate Accent | Save Neutral (-> fieldMode = SaveName(defaultProfileName())) | Back]; profile-selected ->
    [Apply Accent (BED_MESH_PROFILE LOAD selectedProfile) | Remove Danger (-> Remove ConfirmGuard, red cannot-be-undone) | Back].
    (b) `SaveName(prefill)` -> a Column with a token text field (alphanumeric keyboard — D-13 sanctioned carve-out), pre-filled
    with `prefill` (the YY.MM.DD_HH.MM `defaultProfileName()`), real-time `PrinterCommands.isValidProfileName` validation that
    DISABLES the confirm button + shows the invalid-name inline copy; FootButtonBar [Save Accent (disabled until valid; dispatches
    the save command then returns to ProfileList) | Cancel Danger (C7 cancel-with-loss -> fieldMode = ProfileList)]. KEEP the
    amber SAVE_CONFIG restart ConfirmGuard after a profile save (D-14). All profile ops dispatch through the existing
    CommandRegistry specs — NO `rememberCoroutineScope` for any write ([[dinghy-compose-write-scope-cancellation]]); selectedProfile
    + fieldMode are local `remember` (use `rememberSaveable` so they survive rotation per 27-UI-SPEC orientation rule). Every font
    via fsSp. Add new strings (mesh_calibrate/apply/save/remove/remove_confirm/save_name_hint/save_name_label/save_confirm/
    profile_active/empty_state, cd_mesh_heatmap) per 27-UI-SPEC §Copywriting — audit existing first. Zero raw MaterialSymbol.
    **STATELESS PREVIEW SEAM (Codex WARNING-5):** keep the public `BedMeshScreen(...)` thin — it collects the VM +
    owns `selectedProfile`/`fieldMode`/guard state, forwarding a plain data snapshot (profileNames, activeProfileName,
    selectedProfile, fieldMode, model-or-empty, scale mode, homed) + no-op-able callbacks to a STATELESS
    `@Composable BedMeshContent(...)` holding the BoxWithConstraints/ListBlock/SaveName-takeover/FootButtonBar layout
    (the heatmap Focus stays the `LocalInspectionMode` placeholder branch in preview). The Task-2 @Preview targets
    `BedMeshContent` with stub data + no-op callbacks — no VM (ExtrudeScreen.kt:209 / TemperatureScreen pattern).
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --tests works.mees.dinghy.calibration.BedMeshHolderTest --no-daemon" 2>&1 | tr -d '\r' | tail -10; /mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:assembleDebug --no-daemon" 2>&1 | tr -d '\r' | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - BedMeshScreen is split into a thin VM-reading `BedMeshScreen(...)` + a STATELESS `BedMeshContent(...)` holding the layout (WARNING-5 seam; grep both).
    - `SaveNameDialog`, `LoadSelectorDialog`, and the `MeshDialog` enum no longer exist (grep returns zero matches for each — D-11/anti-pattern).
    - A `MeshFieldMode` sealed class with `ProfileList` + `SaveName(prefill)` exists; the Field renders via `when(fieldMode)`.
    - The SaveName takeover calls `PrinterCommands.isValidProfileName` and disables confirm on invalid input (grep `isValidProfileName`); the field pre-fills with the `defaultProfileName()` / YY.MM.DD_HH.MM default.
    - The profile-selected foot has Apply + Remove(behind ConfirmGuard, destructive/red) + Back; the no-selection foot has Calibrate + Save + Back (D-14).
    - `BedMeshHeatmapHost` is still the Focus with the P22 equality guard (grep `applyTokens` guard usage preserved; AndroidView factory not recreated in a recomposing scope); a `LocalInspectionMode` placeholder branch exists.
    - No `rememberCoroutineScope` for any write in the diff; `selectedProfile`/`fieldMode` use `rememberSaveable`.
    - `BedMeshHolderTest` GREEN; `:app:assembleDebug` BUILD SUCCESSFUL.
  </acceptance_criteria>
  <done>BedMesh has a Field profile list + validated SaveName takeover, no overlay dialogs, Views heatmap Focus with P22 guards, state-adaptive foot with the SAVE_CONFIG gate intact.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Extend CalibrationPreviews with the BedMesh section + SampleFixtures.bedMeshVm</name>
  <files>app/src/main/java/works/mees/dinghy/preview/CalibrationPreviews.kt, app/src/main/java/works/mees/dinghy/preview/SampleFixtures.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/preview/CalibrationPreviews.kt (the Hub+Probe sections created by 27-04 — APPEND a BedMesh section, do not overwrite)
    - app/src/main/java/works/mees/dinghy/preview/SampleFixtures.kt (add bedMeshContent(profiles, activeProfile, fieldMode))
    - .planning/phases/27-motion-calibration/27-UI-SPEC.md §"Preview Contract" (BedMesh matrix: ProfileList-noSelection / ProfileList-selected / SaveName-takeover / empty-profiles; 6 themes on ProfileList-selected; fs=L on SaveName; pseudolocale) and §9 heatmap placeholder
  </read_first>
  <action>
    APPEND a BedMesh section to CalibrationPreviews.kt (keep the existing Hub+Probe sections from 27-04 untouched),
    previewing the STATELESS `BedMeshContent` (not the VM-bound screen — WARNING-5). Add
    `SampleFixtures.bedMeshContent(profiles: List<String>, activeProfile: String?, fieldMode)` returning a plain data
    snapshot (not a live VM). Previews: a `*Matrix` (ProfileList-noSelection /
    ProfileList-selected / SaveName-takeover / empty-profiles), 6 theme combos on ProfileList-selected, `*FsLargeOverflow` on
    the SaveName takeover (keyboard + field), pseudolocale en-XA. The heatmap Focus renders via the `LocalInspectionMode`
    PreviewPlaceholderBox (do NOT instantiate the Views host in a preview). Stateless, no live Moonraker.
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:assembleDebug --no-daemon" 2>&1 | tr -d '\r' | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - CalibrationPreviews.kt retains the Hub + Probe sections (grep their preview fun names from 27-04) AND adds a BedMesh `*Matrix` + 6 themes + `*FsLargeOverflow` + pseudolocale (BedMesh `@Preview` count >= 9).
    - `SampleFixtures.bedMeshContent(...)` exists; the BedMesh previews target the stateless `BedMeshContent`.
    - The BedMesh previews use the `LocalInspectionMode` heatmap placeholder (no Views host in preview).
    - `:app:assembleDebug` BUILD SUCCESSFUL (previews in MAIN source set = assembleDebug is the preview compile gate, SC-4); `:app:compileDebugAndroidTestKotlin` still green.
  </acceptance_criteria>
  <done>BedMesh ships its @Preview matrix appended to CalibrationPreviews; the compile gate is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| IME alphanumeric input -> G-code profile name | The save-name field is the one alphanumeric carve-out; the name is interpolated into a BED_MESH_PROFILE SAVE G-code string |
| user touch -> profile Remove / SAVE_CONFIG | Remove is irreversible; SAVE_CONFIG restarts Klipper |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-05-01 | Tampering/Injection | profile-name IME input -> G-code string (V5 input validation) | mitigate | `PrinterCommands.isValidProfileName` gates the SaveName confirm — an invalid (e.g. injection-shaped) name disables confirm and never reaches dispatch; gate preserved verbatim from D-13. |
| T-27-05-02 | Tampering | Remove profile (irreversible) | mitigate | Remove sits behind the red "cannot be undone" ConfirmGuard; acts only on the explicit selection. |
| T-27-05-03 | Tampering | SAVE_CONFIG restart after profile save | mitigate | Amber SAVE_CONFIG restart ConfirmGuard (proceed-at-peril) carried over unchanged. |

Block-on severity is `high`; the V5 input-validation path (T-27-05-01) is the one real item and is mitigated by the preserved isValidProfileName gate; no high residual.
</threat_model>

<verification>
- `BedMeshHolderTest` GREEN; `:app:assembleDebug` + `:app:compileDebugAndroidTestKotlin` BUILD SUCCESSFUL.
- grep: zero SaveNameDialog/LoadSelectorDialog/MeshDialog, MeshFieldMode present, isValidProfileName gate present, P22 applyTokens guard preserved, no rememberCoroutineScope writes.
</verification>

<success_criteria>
SC-2/SC-3/SC-4/SC-5 (BedMesh slice): the profile list + validated SaveName takeover replace the overlay
dialogs; the Views heatmap Focus keeps its P22 guards; the state-adaptive foot + SAVE_CONFIG gate hold;
conformance + previews fold in; holder green. Owner approves on flox at 27-07.
</success_criteria>

<output>
Create `.planning/phases/27-motion-calibration/27-05-SUMMARY.md` when done.
</output>
