---
phase: 27-motion-calibration
plan: 02
type: execute
wave: 0
depends_on: []
files_modified:
  - app/src/main/java/works/mees/dinghy/ui/route/NavDest.kt
  - app/src/main/java/works/mees/dinghy/ui/shell/ShellNavState.kt
  - app/src/main/java/works/mees/dinghy/ui/shell/AppShell.kt
  - app/src/main/java/works/mees/dinghy/ui/shell/RootController.kt
  - app/src/test/java/works/mees/dinghy/ui/route/NavDestRoundTripTest.kt
  - app/src/test/java/works/mees/dinghy/ui/route/FootGunDestsTest.kt
autonomous: true
requirements: [SC-2]
user_setup: []
must_haves:
  truths:
    - "The 5 calibration routines are real NavDest routes (CalibrationProbe / CalibrationBedMesh / CalibrationScrewsTilt / CalibrationZTilt / CalibrationQgl); NavDest.Calibration is renamed to CalibrationHub (D-07)"
    - "ShellNavState.calibrationRoutine is removed; the NavHost back-stack is the single source of truth for the active calibration screen (no double source of truth)"
    - "FOOT_GUN_DESTS contains all 6 CalibrationXxx members + Move + Extrude so pop-to-root on print start covers the whole calibration sub-tree (D-17)"
    - "System Back is swallowed while a probe session is Active or starting — at the NavHost composable<NavDest.CalibrationProbe> level, not just by hiding a button (D-09)"
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/ui/route/NavDest.kt"
      provides: "6 calibration sub-NavDest members + expanded FOOT_GUN_DESTS + knownNavDests"
      contains: "CalibrationHub"
    - path: "app/src/main/java/works/mees/dinghy/ui/shell/AppShell.kt"
      provides: "6 composable<NavDest.CalibrationXxx> blocks + D-09 BackHandler on the probe route"
      contains: "composable<NavDest.CalibrationProbe>"
  key_links:
    - from: "AppShell composable<NavDest.CalibrationHub>.onOpen"
      to: "navController.navigate(NavDest.CalibrationXxx)"
      via: "CalibrationRoutine -> NavDest mapping helper"
      pattern: "navController.navigate"
    - from: "composable<NavDest.CalibrationProbe> BackHandler"
      to: "probeCalibrateHolder.vm.state"
      via: "enabled = Active || starting"
      pattern: "BackHandler"
---

<objective>
Execute the D-07 navigation structural conversion: replace the in-screen `when(calibrationRoutine)`
sub-nav (controlled by `ShellNavState.calibrationRoutine`) with real NavHost routes, per P24 D-01.
This is the single biggest structural change in Phase 27 and a hard prerequisite for every screen
rebuild — it must land in Wave 0.

Per RESEARCH Option A (recommended): add 6 `@Serializable data object` NavDest sub-members, rename
`NavDest.Calibration` → `CalibrationHub`, expand `knownNavDests` + `FOOT_GUN_DESTS`, remove the
`ShellNavState.calibrationRoutine` field, and replace AppShell's single `composable<NavDest.Calibration>`
block (lines 687-730) with 6 `composable<NavDest.CalibrationXxx>` blocks. The D-09 back-suppression
moves from "the Active gutter simply has no Back button" (which does NOT block system Back) to an
explicit `BackHandler(enabled = Active || starting)` at the `composable<NavDest.CalibrationProbe>` level.

This plan keeps the EXISTING screen composables (CalibrationHubScreen with onNavigate, etc.) calling
into the new routes via a thin adapter — it does NOT rebuild the screens. The hub's `onNavigate` is
renamed/rewired to `onOpen` that calls `navController.navigate(...)`. The Wave-1 plans rebuild each
screen's interior; this plan only rewires the nav spine and proves it green.

Purpose: Convert calibration sub-nav to real routes so the screen rebuilds (27-03..27-06) attach to a
clean NavHost spine, and pop-to-root + back-suppression hold at the nav layer.
Output: 6 calibration NavDest routes, removed calibrationRoutine field, D-09 nav-layer BackHandler,
green nav round-trip + FOOT_GUN tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/27-motion-calibration/27-CONTEXT.md
@.planning/phases/27-motion-calibration/27-RESEARCH.md
@.planning/phases/27-motion-calibration/27-PATTERNS.md
@.planning/phases/27-motion-calibration/27-UI-SPEC.md
@app/src/main/java/works/mees/dinghy/ui/route/NavDest.kt
@app/src/main/java/works/mees/dinghy/ui/shell/ShellNavState.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 6 calibration NavDest members + expand FOOT_GUN_DESTS / knownNavDests + round-trip tests</name>
  <files>app/src/main/java/works/mees/dinghy/ui/route/NavDest.kt, app/src/test/java/works/mees/dinghy/ui/route/NavDestRoundTripTest.kt, app/src/test/java/works/mees/dinghy/ui/route/FootGunDestsTest.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/route/NavDest.kt (full file — the sealed interface at lines 24-40, knownNavDests at 49-67, FOOT_GUN_DESTS at 84-88, shouldPopToRoot at 105-108)
    - app/src/test/java/works/mees/dinghy/ui/route/NavDestRoundTripTest.kt (existing round-trip test to extend)
    - .planning/phases/27-motion-calibration/27-PATTERNS.md §"ui/route/NavDest.kt" (the exact target sealed-interface block + FOOT_GUN_DESTS target)
  </read_first>
  <action>
    In NavDest.kt: REMOVE `@Serializable data object Calibration : NavDest` and ADD six members —
    `CalibrationHub`, `CalibrationProbe`, `CalibrationBedMesh`, `CalibrationScrewsTilt`, `CalibrationZTilt`,
    `CalibrationQgl`, all `@Serializable data object ... : NavDest`. Update `knownNavDests` to replace the single
    `NavDest.Calibration` entry with all 6 new members. Update `FOOT_GUN_DESTS` to the exact target set: Move,
    Extrude, CalibrationHub, CalibrationProbe, CalibrationBedMesh, CalibrationScrewsTilt, CalibrationZTilt,
    CalibrationQgl (per 27-PATTERNS.md target). `shouldPopToRoot` needs no change (it checks `current in
    FOOT_GUN_DESTS`). Add a `CalibrationRoutine -> NavDest` mapping helper (e.g. `fun CalibrationRoutine.toNavDest(): NavDest`)
    next to the NavDest definitions so AppShell's hub onOpen can map routine -> route. Extend NavDestRoundTripTest
    so every new member round-trips through serialization (the existing test asserts each Dest serializes/parses).
    Add FootGunDestsTest asserting all 6 CalibrationXxx members are in FOOT_GUN_DESTS (Pitfall 2). Do NOT touch
    AppShell or ShellNavState in this task.
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --tests works.mees.dinghy.ui.route.* --no-daemon" 2>&1 | tr -d '\r' | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - NavDest.kt declares all 6 `CalibrationXxx` data objects; `NavDest.Calibration` no longer exists (grep returns zero `data object Calibration :`).
    - `FOOT_GUN_DESTS` literally contains all 6 calibration members + Move + Extrude (grep each member inside the set).
    - `knownNavDests` contains all 6 calibration members and no `NavDest.Calibration`.
    - A `CalibrationRoutine.toNavDest()` (or equivalently-named) mapping helper exists covering all 5 routines.
    - `NavDestRoundTripTest` and `FootGunDestsTest` GREEN; the FOOT_GUN test asserts all 6 calibration dests present.
  </acceptance_criteria>
  <done>The 6 calibration routes exist, are serialization-round-trip-safe, and are all in FOOT_GUN_DESTS; pop-to-root will cover the whole sub-tree.</done>
</task>

<task type="auto">
  <name>Task 2: Replace ShellNavState.calibrationRoutine + AppShell when()-block with 6 NavHost routes + D-09 BackHandler</name>
  <files>app/src/main/java/works/mees/dinghy/ui/shell/ShellNavState.kt, app/src/main/java/works/mees/dinghy/ui/shell/AppShell.kt, app/src/main/java/works/mees/dinghy/ui/shell/RootController.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/shell/AppShell.kt lines 180-190 (calibrationRoutine read), 505-516 (old in-screen Calibration BackHandler), 685-730 (the when(calibrationRoutine) block to replace), and the existing composable<NavDest.FineTune> block at ~731 (the canonical composable<> pattern to mirror)
    - app/src/main/java/works/mees/dinghy/ui/shell/ShellNavState.kt lines 54-58 (the calibrationRoutine field), 82-92 (applyEntryReset), 95-100 (the preserve-on-reset KDoc)
    - app/src/main/java/works/mees/dinghy/ui/shell/RootController.kt line ~109 (the calibrationRoutine preserve comment — update wording, no field to preserve after this)
    - .planning/phases/27-motion-calibration/27-PATTERNS.md §"ui/shell/AppShell.kt" (the exact 6 composable<> target blocks + the D-09 BackHandler + starting-state definition) and §"ui/shell/ShellNavState.kt" (the field removal + applyEntryReset update)
  </read_first>
  <action>
    In ShellNavState.kt: REMOVE the `var calibrationRoutine by mutableStateOf<CalibrationRoutine?>(null)` field
    (line 56). In `applyEntryReset(target)` change the `NavDest.Calibration` branch to `NavDest.CalibrationHub`
    and REMOVE its `calibrationRoutine = null` body (the branch may now be empty or dropped — keep applyEntryReset
    callable for CalibrationHub but with no calibrationRoutine mutation). Update the preserve-on-reset KDoc to drop
    the calibrationRoutine reference. In RootController.kt update the line-109 comment to drop calibrationRoutine.
    In AppShell.kt: DELETE the old in-screen calibration BackHandler (lines ~511-515 — the one gated on
    `isRoute<NavDest.Calibration>() && calibrationRoutine != null`) and the `val calibrationRoutine = nav.calibrationRoutine`
    read (~184). REPLACE the entire `composable<NavDest.Calibration> { ... when(calibrationRoutine) ... }` block
    (685-730) with SIX `composable<NavDest.CalibrationXxx>` blocks per 27-PATTERNS.md: (a) CalibrationHub — keep
    `LaunchedEffect(Unit){ nav.applyEntryReset(NavDest.CalibrationHub) }`, call CalibrationHubScreen with
    `onOpen = { routine -> navController.navigate(routine.toNavDest()) }` and `onBack = { navController.popBackStack() }`;
    (b) CalibrationProbe — collect `probeCalibrateHolder.vm`, compute the `starting` boolean VERBATIM
    (`vm.state == ProbePageState.Idle && ("probe_calibrate" in inFlight || "z_endstop_calibrate" in inFlight)`,
    Pitfall 6), add `BackHandler(enabled = vm.state == ProbePageState.Active || starting) { /* swallow */ }` (D-09),
    then ProbeCalibrateScreen(onBack = popBackStack); (c) CalibrationBedMesh -> BedMeshScreen; (d) CalibrationScrewsTilt
    -> ScrewsTiltScreen; (e) CalibrationZTilt -> TiltScreen(variant = TiltVariant.ZTilt, holder = zTiltHolder);
    (f) CalibrationQgl -> TiltScreen(variant = TiltVariant.Qgl, holder = qglHolder). Each onBack = navController.popBackStack().
    The CalibrationHubScreen `onNavigate` parameter is RENAMED to `onOpen` (a signature change the existing screen
    must accept — update CalibrationHubScreen's parameter name + the internal call site minimally; the FULL hub
    rebuild is 27-04, this is the rename-only seam). Add imports for all 6 NavDest members + ProbePageState +
    androidx.activity.compose.BackHandler. GREP `calibrationRoutine` across the whole codebase and confirm ZERO
    remaining references in main source (Pitfall 5) — expected residual only in test fixtures if any, which must
    also be cleaned. Do NOT alter the screen interiors beyond the hub onNavigate->onOpen rename.
  </action>
  <verify>
    <automated>grep -rn "calibrationRoutine" app/src/main/java/ | tee /dev/stderr | wc -l; /mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:assembleDebug --no-daemon" 2>&1 | tr -d '\r' | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn calibrationRoutine app/src/main/java/` returns ZERO matches (field + all read sites removed — Pitfall 5).
    - AppShell.kt declares exactly 6 `composable<NavDest.Calibration*>` blocks (grep count == 6) and no `when (val routine = calibrationRoutine)`.
    - The `composable<NavDest.CalibrationProbe>` block contains a `BackHandler(enabled = ...Active... || starting)` (grep `BackHandler` inside that block; the `starting` boolean matches the verbatim definition).
    - The CalibrationHub block calls `navController.navigate(routine.toNavDest())` in its onOpen lambda.
    - `:app:assembleDebug` BUILD SUCCESSFUL (all 3 source sets compile; the hub onNavigate->onOpen rename is consistent).
  </acceptance_criteria>
  <done>The calibration sub-nav is real NavHost routes; calibrationRoutine is gone; system Back is swallowed during an Active/starting probe session at the nav layer; the app compiles.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user gesture -> nav back-stack | System Back during a live manual-probe session is an untrusted control input that could abandon a probing nozzle mid-descent |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-02-01 | Tampering | system Back during Active/starting probe session | mitigate | `BackHandler(enabled = Active || starting)` at the composable<NavDest.CalibrationProbe> level swallows system Back so the user cannot navigate away mid-probe (D-09 nav-layer upgrade of T-09-06-02). |
| T-27-02-02 | Denial of Service | pop-to-root coverage of calibration sub-tree | mitigate | FOOT_GUN_DESTS includes all 6 calibration dests so a print starting while the user is in any routine pops them to the WaterfallHome root (D-17) — prevents a foot-gun motion command landing during a print. |

Block-on severity is `high`; T-27-02-01 (mid-probe abandonment) is mitigated at the nav layer; no high residual.
</threat_model>

<verification>
- `grep calibrationRoutine` over main source returns zero matches.
- `:app:assembleDebug` BUILD SUCCESSFUL.
- NavDest round-trip + FOOT_GUN tests GREEN (from Task 1).
- The probe-route BackHandler is present and gated on Active || starting.
</verification>

<success_criteria>
SC-2 (foundation): calibration routines are coherent real NavHost routes with the back-stack as
single source of truth; back-suppression holds at the nav layer during an Active probe session; the
whole calibration sub-tree is pop-to-root-covered on print start. The screen rebuilds (27-03..27-06)
attach to this clean spine.
</success_criteria>

<output>
Create `.planning/phases/27-motion-calibration/27-02-SUMMARY.md` when done. Record the final grep
proof that calibrationRoutine is fully removed and the 6 composable blocks are in place.
</output>
