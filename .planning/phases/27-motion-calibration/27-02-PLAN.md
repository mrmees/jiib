---
phase: 27-motion-calibration
plan: 02
type: execute
wave: 0
depends_on: []
files_modified:
  - app/src/main/java/works/mees/dinghy/ui/route/NavDest.kt
  - app/src/main/java/works/mees/dinghy/ui/route/HomeAction.kt
  - app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusField.kt
  - app/src/main/java/works/mees/dinghy/ui/shell/AppDrawer.kt
  - app/src/main/java/works/mees/dinghy/ui/shell/ShellNavState.kt
  - app/src/main/java/works/mees/dinghy/ui/shell/AppShell.kt
  - app/src/main/java/works/mees/dinghy/ui/shell/RootController.kt
  - app/src/test/java/works/mees/dinghy/ui/route/NavDestRoundTripTest.kt
  - app/src/test/java/works/mees/dinghy/ui/route/FootGunDestsTest.kt
  - app/src/test/java/works/mees/dinghy/ui/route/PopToRootTest.kt
  - app/src/test/java/works/mees/dinghy/ui/route/HomeActionTest.kt
  - app/src/test/java/works/mees/dinghy/shell/ShellNavStateTest.kt
autonomous: true
requirements: [SC-2]
user_setup: []
must_haves:
  truths:
    - "The 5 calibration routines are real NavDest routes (CalibrationProbe / CalibrationBedMesh / CalibrationScrewsTilt / CalibrationZTilt / CalibrationQgl); NavDest.Calibration is renamed to CalibrationHub (D-07)"
    - "EVERY NavDest.Calibration consumer is migrated to CalibrationHub atomically in Task 2 (HomeAction launcher row, PrintStatusField LauncherDest mapping, AppDrawer tile, AppShell currentNavDest + swipe-suppress + composable block) — no half-renamed compile-broken intermediate state (P24 Dest->NavDest lesson)"
    - "ShellNavState.calibrationRoutine is removed; the NavHost back-stack is the single source of truth for the active calibration screen (no double source of truth)"
    - "FOOT_GUN_DESTS contains all 6 CalibrationXxx members + Move + Extrude so pop-to-root on print start covers the whole calibration sub-tree (D-17); AppShell's currentNavDest mapping at line ~822 maps ALL SIX calibration routes (not just CalibrationHub) so a print starting in any routine pops to root"
    - "The swipe-suppress route set at AppShell line ~553 lists ALL SIX calibration routes (a scrollable routine Field would otherwise fight the swipe-up drawer on every routine screen)"
    - "System Back is swallowed while a probe session is Active or starting — at the NavHost composable<NavDest.CalibrationProbe> level, gated to ALSO require no overlay visible (drawer/scan/prompt) so it does not out-prioritize overlay dismissal (D-09, WARNING-6)"
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/ui/route/NavDest.kt"
      provides: "6 calibration sub-NavDest members + expanded FOOT_GUN_DESTS + knownNavDests"
      contains: "CalibrationHub"
    - path: "app/src/main/java/works/mees/dinghy/ui/shell/AppShell.kt"
      provides: "6 composable<NavDest.CalibrationXxx> blocks + D-09 overlay-gated BackHandler on the probe route + all-six currentNavDest mapping + all-six swipe-suppress"
      contains: "composable<NavDest.CalibrationProbe>"
  key_links:
    - from: "AppShell composable<NavDest.CalibrationHub>.onOpen"
      to: "navController.navigate(NavDest.CalibrationXxx)"
      via: "CalibrationRoutine -> NavDest mapping helper"
      pattern: "navController.navigate"
    - from: "composable<NavDest.CalibrationProbe> BackHandler"
      to: "probeCalibrateHolder.vm.state"
      via: "enabled = (Active || starting) && !drawerOpen && !nav.scanActive && !promptView.visible"
      pattern: "BackHandler"
---

<objective>
Execute the D-07 navigation structural conversion: replace the in-screen `when(calibrationRoutine)`
sub-nav (controlled by `ShellNavState.calibrationRoutine`) with real NavHost routes, per P24 D-01.
This is the single biggest structural change in Phase 27 and a hard prerequisite for every screen
rebuild — it must land in Wave 0.

Per RESEARCH Option A (recommended): add 6 `@Serializable data object` NavDest sub-members, rename
`NavDest.Calibration` → `CalibrationHub`, expand `knownNavDests` + `FOOT_GUN_DESTS`, remove the
`ShellNavState.calibrationRoutine` field, and replace AppShell's single `composable<NavDest.Calibration>`
block (lines 687-730) with 6 `composable<NavDest.CalibrationXxx>` blocks. The D-09 back-suppression
moves from "the Active gutter simply has no Back button" (which does NOT block system Back) to an
explicit overlay-gated `BackHandler` at the `composable<NavDest.CalibrationProbe>` level.

**CRITICAL — atomic rename (P24 Dest->NavDest lesson, Codex SHOW-STOPPER-1):** `NavDest.Calibration`
has FIVE consumers OUTSIDE the original files_modified list — `HomeAction.kt:125` (launcher idle-list
row), `PrintStatusField.kt:481` (`LauncherDest.Calibration -> NavDest.Calibration` mapping),
`AppDrawer.kt:210` (the Calibration drawer tile), plus the AppShell `currentNavDest` mapping (~826) and
swipe-suppress set (~553). ALL of them must migrate to `NavDest.CalibrationHub` in the SAME Task-2 edit
BEFORE any compile gate runs — a Kotlin sealed-type rename leaves the tree uncompilable until every
consumer is updated. The executor MUST `grep -rn 'NavDest\.Calibration\b' app/src/main app/src/test`
and confirm zero stale references (all rewritten to CalibrationHub or a CalibrationXxx route) before
running assembleDebug.

This plan keeps the EXISTING screen composables (CalibrationHubScreen with onNavigate, etc.) calling
into the new routes via a thin adapter — it does NOT rebuild the screens. The hub's `onNavigate` is
renamed/rewired to `onOpen` that calls `navController.navigate(...)`. The Wave-1/2 plans rebuild each
screen's interior; this plan only rewires the nav spine and proves it green.

Purpose: Convert calibration sub-nav to real routes so the screen rebuilds (27-03..27-06) attach to a
clean NavHost spine, and pop-to-root + back-suppression hold at the nav layer.
Output: 6 calibration NavDest routes, removed calibrationRoutine field, all consumers migrated, D-09
overlay-gated nav-layer BackHandler, green nav round-trip + FOOT_GUN + rewritten PopToRoot/HomeAction/
ShellNavState tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/27-motion-calibration/27-CONTEXT.md
@.planning/phases/27-motion-calibration/27-RESEARCH.md
@.planning/phases/27-motion-calibration/27-PATTERNS.md
@.planning/phases/27-motion-calibration/27-UI-SPEC.md
@app/src/main/java/works/mees/dinghy/ui/route/NavDest.kt
@app/src/main/java/works/mees/dinghy/ui/shell/ShellNavState.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 6 calibration NavDest members + expand FOOT_GUN_DESTS / knownNavDests + round-trip + FOOT_GUN tests</name>
  <files>app/src/main/java/works/mees/dinghy/ui/route/NavDest.kt, app/src/test/java/works/mees/dinghy/ui/route/NavDestRoundTripTest.kt, app/src/test/java/works/mees/dinghy/ui/route/FootGunDestsTest.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/route/NavDest.kt (full file — the sealed interface at lines 24-40, knownNavDests at 49-67, FOOT_GUN_DESTS at 84-88, shouldPopToRoot at 105-108)
    - app/src/test/java/works/mees/dinghy/ui/route/NavDestRoundTripTest.kt (existing round-trip test to extend)
    - .planning/phases/27-motion-calibration/27-PATTERNS.md §"ui/route/NavDest.kt" (the exact target sealed-interface block + FOOT_GUN_DESTS target)
  </read_first>
  <action>
    In NavDest.kt: REMOVE `@Serializable data object Calibration : NavDest` and ADD six members —
    `CalibrationHub`, `CalibrationProbe`, `CalibrationBedMesh`, `CalibrationScrewsTilt`, `CalibrationZTilt`,
    `CalibrationQgl`, all `@Serializable data object ... : NavDest`. Update `knownNavDests` to replace the single
    `NavDest.Calibration` entry with all 6 new members. Update `FOOT_GUN_DESTS` to the exact target set: Move,
    Extrude, CalibrationHub, CalibrationProbe, CalibrationBedMesh, CalibrationScrewsTilt, CalibrationZTilt,
    CalibrationQgl (per 27-PATTERNS.md target). `shouldPopToRoot` needs no change (it checks `current in
    FOOT_GUN_DESTS`). Add a `CalibrationRoutine -> NavDest` mapping helper (e.g. `fun CalibrationRoutine.toNavDest(): NavDest`)
    next to the NavDest definitions so AppShell's hub onOpen can map routine -> route. Extend NavDestRoundTripTest
    so every new member round-trips through serialization (the existing test asserts each Dest serializes/parses).
    Add FootGunDestsTest asserting all 6 CalibrationXxx members are in FOOT_GUN_DESTS (Pitfall 2). Do NOT touch
    AppShell or ShellNavState in this task. NOTE: the existing PopToRootTest `footGunDests_containsExactlyThreeMembers`
    test asserts `FOOT_GUN_DESTS.size == 3` and references `NavDest.Calibration` — that whole test sourceset will
    NOT compile after this task's NavDest edit, so Task 2 (which fixes PopToRootTest) is a hard same-plan dependency;
    expect this task's `--tests works.mees.dinghy.ui.route.*` run to be GREEN only for the route tests it owns once
    Task 2 has also landed. (Both tasks are in this one plan; run the route-test gate after Task 2.)
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:compileDebugUnitTestKotlin --no-daemon" 2>&1 | tr -d '\r' | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - NavDest.kt declares all 6 `CalibrationXxx` data objects; `NavDest.Calibration` no longer exists (grep returns zero `data object Calibration :`).
    - `FOOT_GUN_DESTS` literally contains all 6 calibration members + Move + Extrude (grep each member inside the set).
    - `knownNavDests` contains all 6 calibration members and no `NavDest.Calibration`.
    - A `CalibrationRoutine.toNavDest()` (or equivalently-named) mapping helper exists covering all 5 routines.
    - `NavDestRoundTripTest` and `FootGunDestsTest` cover all 6 calibration dests (the green run lands after Task 2 fixes the sibling tests).
  </acceptance_criteria>
  <done>The 6 calibration routes exist, are serialization-round-trip-safe, and are all in FOOT_GUN_DESTS; pop-to-root will cover the whole sub-tree.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate ALL NavDest.Calibration consumers + replace ShellNavState.calibrationRoutine + AppShell when()-block with 6 NavHost routes + overlay-gated D-09 BackHandler + rewrite the 3 brittle tests (ALL ATOMIC)</name>
  <files>app/src/main/java/works/mees/dinghy/ui/route/HomeAction.kt, app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusField.kt, app/src/main/java/works/mees/dinghy/ui/shell/AppDrawer.kt, app/src/main/java/works/mees/dinghy/ui/shell/ShellNavState.kt, app/src/main/java/works/mees/dinghy/ui/shell/AppShell.kt, app/src/main/java/works/mees/dinghy/ui/shell/RootController.kt, app/src/test/java/works/mees/dinghy/ui/route/PopToRootTest.kt, app/src/test/java/works/mees/dinghy/ui/route/HomeActionTest.kt, app/src/test/java/works/mees/dinghy/shell/ShellNavStateTest.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/route/HomeAction.kt line ~125 (the `dest = NavDest.Calibration` launcher idle-list row → CalibrationHub)
    - app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusField.kt line ~481 (`LauncherDest.Calibration -> NavDest.Calibration` mapping → CalibrationHub)
    - app/src/main/java/works/mees/dinghy/ui/shell/AppDrawer.kt line ~210 (the Calibration DrawerTileSpec `dest = NavDest.Calibration` → CalibrationHub)
    - app/src/main/java/works/mees/dinghy/ui/shell/AppShell.kt lines 183-184 (calibrationRoutine read), 511-515 (old in-screen Calibration BackHandler — DELETE), 518-530 (the scan/prompt overlay BackHandlers — the WARNING-6 priority context), 549-565 (the swipe-suppress route set — ADD all six), 685-730 (the when(calibrationRoutine) block to replace), 820-829 (the currentNavDest pop-to-root mapping — ADD all six), and the existing composable<NavDest.FineTune> block (the canonical composable<> pattern to mirror)
    - app/src/main/java/works/mees/dinghy/ui/shell/ShellNavState.kt lines 54-58 (the calibrationRoutine field), 82-92 (applyEntryReset), 95-100 (the preserve-on-reset KDoc)
    - app/src/main/java/works/mees/dinghy/ui/shell/RootController.kt line ~109 (the calibrationRoutine preserve comment — update wording, no field to preserve after this)
    - app/src/test/java/works/mees/dinghy/ui/route/PopToRootTest.kt lines 38-42 + 98-104 (the `calibration_isFootGun` test refs NavDest.Calibration; `footGunDests_containsExactlyThreeMembers` asserts size==3 — BOTH must be rewritten)
    - app/src/test/java/works/mees/dinghy/ui/route/HomeActionTest.kt lines 148, 167, 203 (NavDest.Calibration in the idle-list assertions → CalibrationHub)
    - app/src/test/java/works/mees/dinghy/shell/ShellNavStateTest.kt lines 64-105 (three tests that set/read `nav.calibrationRoutine` and `applyEntryReset(NavDest.Calibration)` — all reference deleted symbols)
    - .planning/phases/27-motion-calibration/27-PATTERNS.md §"ui/shell/AppShell.kt" (the exact 6 composable<> target blocks + the D-09 BackHandler + starting-state definition) and §"ui/shell/ShellNavState.kt" (the field removal + applyEntryReset update)
  </read_first>
  <action>
    **(A) Migrate the three out-of-files consumers FIRST (atomic rename — Codex SHOW-STOPPER-1):** in
    HomeAction.kt change the launcher-row `dest = NavDest.Calibration` → `NavDest.CalibrationHub`; in
    PrintStatusField.kt change `LauncherDest.Calibration -> NavDest.Calibration` → `... -> NavDest.CalibrationHub`;
    in AppDrawer.kt change the Calibration `DrawerTileSpec(dest = NavDest.Calibration)` → `NavDest.CalibrationHub`.
    **(B) ShellNavState.kt:** REMOVE the `var calibrationRoutine by mutableStateOf<CalibrationRoutine?>(null)` field
    (line 56). In `applyEntryReset(target)` change the `NavDest.Calibration` branch to `NavDest.CalibrationHub` and
    REMOVE its `calibrationRoutine = null` body. Update the preserve-on-reset KDoc to drop the calibrationRoutine
    reference. In RootController.kt update the line-109 comment to drop calibrationRoutine.
    **(C) AppShell.kt — five edit sites:**
      (1) DELETE the `val calibrationRoutine = nav.calibrationRoutine` read (~184).
      (2) DELETE the old in-screen calibration BackHandler (lines ~511-515, gated on
          `isRoute<NavDest.Calibration>() && calibrationRoutine != null`).
      (3) In the swipe-suppress set (~549-565) REPLACE the single `d.isRoute<NavDest.Calibration>()` with all SIX:
          `d.isRoute<NavDest.CalibrationHub>() || d.isRoute<NavDest.CalibrationProbe>() ||
          d.isRoute<NavDest.CalibrationBedMesh>() || d.isRoute<NavDest.CalibrationScrewsTilt>() ||
          d.isRoute<NavDest.CalibrationZTilt>() || d.isRoute<NavDest.CalibrationQgl>()` (every routine Field is a
          scrollable list that must suppress the swipe-up drawer — Codex SHOW-STOPPER-2).
      (4) In the `currentNavDest` pop-to-root mapping (~820-829) REPLACE the single
          `d.isRoute<NavDest.Calibration>() -> NavDest.Calibration` branch with six branches mapping each
          `d.isRoute<NavDest.CalibrationXxx>() -> NavDest.CalibrationXxx` so shouldPopToRoot fires from ANY routine,
          not just the hub (Codex SHOW-STOPPER-2).
      (5) REPLACE the entire `composable<NavDest.Calibration> { ... when(calibrationRoutine) ... }` block (685-730)
          with SIX `composable<NavDest.CalibrationXxx>` blocks per 27-PATTERNS.md:
          (a) CalibrationHub — keep `LaunchedEffect(Unit){ nav.applyEntryReset(NavDest.CalibrationHub) }`, call
              CalibrationHubScreen with `onOpen = { routine -> navController.navigate(routine.toNavDest()) }` and
              `onBack = { navController.popBackStack() }`;
          (b) CalibrationProbe — collect `probeCalibrateHolder.vm`, compute the `starting` boolean VERBATIM
              (`vm.state == ProbePageState.Idle && ("probe_calibrate" in inFlight || "z_endstop_calibrate" in inFlight)`,
              Pitfall 6 — wire `inFlight` from `dispatcher?.inFlight` collected at this composable scope, mirroring the
              prompt-overlay `inFlightKeys` pattern at AppShell ~863), then add the D-09 BackHandler **gated to also
              require no overlay visible (Codex WARNING-6):**
              `BackHandler(enabled = (vm.state == ProbePageState.Active || starting) && !drawerOpen && !nav.scanActive && !promptView.visible) { /* swallow — abandoning a live probe descent is unsafe */ }`.
              The overlay-gating is required because in-screen composable BackHandlers register INNERMOST (highest
              priority) — an un-gated probe handler would out-prioritize the scan/prompt overlay dismissal handlers at
              AppShell ~518/~524 while a probe is active. `drawerOpen`, `nav.scanActive`, `promptView.visible` are all
              in scope in the NavHost composable lambda. Then ProbeCalibrateScreen(onBack = popBackStack);
          (c) CalibrationBedMesh -> BedMeshScreen;
          (d) CalibrationScrewsTilt -> ScrewsTiltScreen;
          (e) CalibrationZTilt -> TiltScreen(variant = TiltVariant.ZTilt, holder = zTiltHolder);
          (f) CalibrationQgl -> TiltScreen(variant = TiltVariant.Qgl, holder = qglHolder).
          Each onBack = navController.popBackStack(). Add imports for all 6 NavDest members + ProbePageState +
          androidx.activity.compose.BackHandler.
    The CalibrationHubScreen `onNavigate` parameter is RENAMED to `onOpen` (a signature change the existing screen
    must accept — update CalibrationHubScreen's parameter name + the internal call site minimally; the FULL hub
    rebuild is 27-04, this is the rename-only seam).
    **(D) Rewrite the three brittle tests in this SAME task (Codex SHOW-STOPPER-3 — the whole test sourceset compiles
    before --tests filtering, so leaving these referencing deleted symbols bricks EVERY per-wave test run):**
      - PopToRootTest.kt: `calibration_isFootGun_returnsTrue` → assert each of the six `NavDest.CalibrationXxx`
        shouldPopToRoot true; `footGunDests_containsExactlyThreeMembers` → rename/retarget to assert
        `FOOT_GUN_DESTS.size == 8` and membership of Move + Extrude + all six Calibration* dests.
      - HomeActionTest.kt: lines 148/167/203 `NavDest.Calibration` → `NavDest.CalibrationHub` (the idle-list/launcher
        row is the HUB destination, not a routine).
      - ShellNavStateTest.kt: the three tests setting/reading `nav.calibrationRoutine` and
        `applyEntryReset(NavDest.Calibration)` — DELETE the calibrationRoutine-clear assertions (the field is gone)
        and retarget `applyEntryReset(NavDest.Calibration)` → `applyEntryReset(NavDest.CalibrationHub)`; assert the
        reset is still callable and preserves the non-calibrationRoutine fields it used to. If a test's entire reason
        for existing was the deleted field, remove that test and note the removal in the SUMMARY.
    GREP `calibrationRoutine` AND `NavDest.Calibration\b` across `app/src/main` and `app/src/test` and confirm ZERO
    remaining references (Pitfall 5 / SHOW-STOPPER-1/3) before the assembleDebug + unit-test gate.
  </action>
  <verify>
    <automated>grep -rn "calibrationRoutine" app/src/main/java/ app/src/test/java/ | tee /dev/stderr | wc -l; grep -rn "NavDest\.Calibration\b" app/src/main/java/ app/src/test/java/ | tee /dev/stderr | wc -l; /mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:assembleDebug :app:testDebugUnitTest --tests works.mees.dinghy.ui.route.* --tests works.mees.dinghy.shell.* --no-daemon" 2>&1 | tr -d '\r' | tail -18</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn calibrationRoutine app/src/main/java/ app/src/test/java/` returns ZERO matches (field + all read/test sites removed — Pitfall 5 / SHOW-STOPPER-3).
    - `grep -rn 'NavDest\.Calibration\b' app/src/main/java/ app/src/test/java/` returns ZERO matches — every consumer (HomeAction, PrintStatusField, AppDrawer, AppShell currentNavDest + swipe-suppress + composable, and all three tests) migrated to CalibrationHub or a CalibrationXxx route (SHOW-STOPPER-1).
    - AppShell.kt declares exactly 6 `composable<NavDest.Calibration*>` blocks (grep count == 6) and no `when (val routine = calibrationRoutine)`.
    - The swipe-suppress set lists all six calibration routes; the currentNavDest mapping branches on all six calibration routes (grep all six in both sites — SHOW-STOPPER-2).
    - The `composable<NavDest.CalibrationProbe>` block contains the D-09 BackHandler gated `(Active || starting) && !drawerOpen && !nav.scanActive && !promptView.visible` (WARNING-6 — overlay dismissal not starved).
    - The CalibrationHub block calls `navController.navigate(routine.toNavDest())` in its onOpen lambda.
    - PopToRootTest asserts `FOOT_GUN_DESTS.size == 8` + all six calibration dests; HomeActionTest asserts the idle-list/launcher row is `NavDest.CalibrationHub`; ShellNavStateTest no longer references the deleted field.
    - `:app:assembleDebug` BUILD SUCCESSFUL and `:app:testDebugUnitTest --tests ...route.* --tests ...shell.*` GREEN.
  </acceptance_criteria>
  <done>The calibration sub-nav is real NavHost routes; calibrationRoutine is gone; every consumer compiles against CalibrationHub; pop-to-root + swipe-suppress cover all six routes; system Back is swallowed during an Active/starting probe session WITHOUT starving overlay dismissal; the three brittle tests are rewritten green; the app compiles.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user gesture -> nav back-stack | System Back during a live manual-probe session is an untrusted control input that could abandon a probing nozzle mid-descent |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-02-01 | Tampering | system Back during Active/starting probe session | mitigate | overlay-gated `BackHandler(enabled = (Active || starting) && no overlay visible)` at the composable<NavDest.CalibrationProbe> level swallows system Back so the user cannot navigate away mid-probe (D-09 nav-layer upgrade of T-09-06-02), while still allowing the scan/prompt overlay dismissal handlers to win when an overlay is up (WARNING-6). |
| T-27-02-02 | Denial of Service | pop-to-root coverage of calibration sub-tree | mitigate | FOOT_GUN_DESTS includes all 6 calibration dests AND AppShell's currentNavDest mapping branches on all six, so a print starting while the user is in ANY routine pops them to the WaterfallHome root (D-17) — prevents a foot-gun motion command landing during a print. |

Block-on severity is `high`; T-27-02-01 (mid-probe abandonment) is mitigated at the nav layer; no high residual.
</threat_model>

<verification>
- `grep calibrationRoutine` + `grep 'NavDest\.Calibration\b'` over main+test source return zero matches.
- `:app:assembleDebug` BUILD SUCCESSFUL (all consumers migrated atomically).
- NavDest round-trip + FOOT_GUN + rewritten PopToRoot/HomeAction/ShellNavState tests GREEN.
- The probe-route BackHandler is present, gated on (Active || starting) AND no overlay visible.
- The swipe-suppress set and currentNavDest mapping each list all six calibration routes.
</verification>

<success_criteria>
SC-2 (foundation): calibration routines are coherent real NavHost routes with the back-stack as
single source of truth; back-suppression holds at the nav layer during an Active probe session
without starving overlay dismissal; the whole calibration sub-tree is pop-to-root-covered + swipe-
suppressed on print start. The screen rebuilds (27-03..27-06) attach to this clean spine.
</success_criteria>

<output>
Create `.planning/phases/27-motion-calibration/27-02-SUMMARY.md` when done. Record the final grep
proof that calibrationRoutine + NavDest.Calibration are fully removed, the 6 composable blocks are in
place, the swipe-suppress + currentNavDest mappings list all six, and the three rewritten tests are green.
</output>
