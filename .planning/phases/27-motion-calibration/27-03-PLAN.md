---
phase: 27-motion-calibration
plan: 03
type: execute
wave: 1
depends_on: ["27-01", "27-02"]
files_modified:
  - app/src/main/java/works/mees/dinghy/ui/move/MoveScreen.kt
  - app/src/main/res/values/strings.xml
  - app/src/test/java/works/mees/dinghy/preview/MovePreviews.kt
autonomous: true
requirements: [SC-1, SC-3, SC-4, SC-5]
user_setup: []
must_haves:
  truths:
    - "The XY jog pad stays a sacred-square 3x3 grid; portrait caps it at ~60% of HEIGHT so the Z/distance columns are never crowded out (D-03)"
    - "The Z control is a vertical 3-cell column (Z-up / live-Z readout+home / Z-down) with all three cells weight(1f) equal (D-01)"
    - "The distance selector is a vertical 3-cell stepper (+ / active-step readout / -) cycling the fixed DISTANCES set, no free entry (D-02)"
    - "Pad internals carry over unchanged: home-XY center, axis-corner readouts + tap-to-home, red force-move toggle, Disable ConfirmGuard (D-04)"
    - "gutter is null; Home All / Disable / Back live in a FootButtonBar inside the field; MovePreviews ships a portrait+landscape+theme+fsL matrix (D-15, SC-4)"
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/ui/move/MoveScreen.kt"
      provides: "rebuilt Move screen — BoxWithConstraints sizing, vertical Z col + distance stepper, FootButtonBar"
      contains: "BoxWithConstraints"
    - path: "app/src/test/java/works/mees/dinghy/preview/MovePreviews.kt"
      provides: "@Preview matrix: portrait/landscape + 6 theme combos + fs=L + pseudolocale"
      contains: "@Preview"
  key_links:
    - from: "MoveScreen Z column center cell"
      to: "MoveVm.z / MoveVm.zHomed"
      via: "live readout + tap-to-home (same vm.z as the JogPad Z corner, Pitfall 7)"
      pattern: "vm.z"
    - from: "distance stepper +/- cells"
      to: "DISTANCES fixed set"
      via: "index cycling with coerceIn bounds"
      pattern: "DISTANCES"
---

<objective>
Rebuild MoveScreen onto the jiib grammar with the specialized-layout exemption (like Extrude's P26
D-15): the sacred-square 3x3 XY jog pad STAYS a grid (spatial arrangement carries meaning), but the
horizontal ZRow becomes a vertical 3-cell Z column (D-01) and the row-of-preset-buttons
DistanceSelector becomes a vertical 3-cell distance stepper (D-02) — both following the verbatim
owner steer "3 equally spaced rows, just like the vertical Z column."

The load-bearing layout rule (D-03): the square pad must NEVER crowd out the columns when it fights
to maintain its 1:1 ratio. Portrait caps the pad at ~60% of HEIGHT (ratio rule then derives the
width); landscape lets the pad fill full height of its half with the columns beside it. MoveScreen
uses `BoxWithConstraints` for manual sizing, NOT `portraitFocusAspect`.

Pad internals carry over unchanged (D-04): home-XY center, axis-corner live readouts + tap-to-home,
red force-move toggle (C4: filled stop-red when armed), Disable ConfirmGuard — restyled onto tokens
+ registry icons only. The gutter goes null; Home All / Disable / Back move into a FootButtonBar.

This plan is independent of the calibration screen plans (touches only MoveScreen + its preview +
shared strings) — it depends on Wave 0 (27-01 for any owner-confirmed stepper glyph; 27-02 only for
the spine compiling) and can run in parallel with the calibration rebuilds.

Purpose: Deliver the spatial Move surface on the redesign while preserving the spatial-grid carve-out
and the long-deferred C3 vertical-Z rule.
Output: Rebuilt MoveScreen + MovePreviews matrix.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/27-motion-calibration/27-CONTEXT.md
@.planning/phases/27-motion-calibration/27-UI-SPEC.md
@.planning/phases/27-motion-calibration/27-PATTERNS.md
@docs/ui_design/LAYOUT.md
@docs/ui_design/THEMING.md
@docs/ui_design/PREVIEW_AND_TOKENS.md
@app/src/main/java/works/mees/dinghy/ui/move/MoveScreen.kt
@app/src/main/java/works/mees/dinghy/ui/calibration/ProbeCalibrateScreen.kt
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Rebuild MoveScreen — BoxWithConstraints layout, vertical Z column + distance stepper, FootButtonBar</name>
  <files>app/src/main/java/works/mees/dinghy/ui/move/MoveScreen.kt, app/src/main/res/values/strings.xml</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/move/MoveScreen.kt (full file — the current JogPad lines ~254-307, ZRow, DistanceSelector, gutter rows ~204-233, Disable ConfirmGuard, JogCell/AxisCorner private composables)
    - app/src/main/java/works/mees/dinghy/ui/calibration/ProbeCalibrateScreen.kt lines 419-462 (the EXACT vertical 3-cell column motif to copy — weight(1f) cells, StepDisplay center)
    - app/src/main/java/works/mees/dinghy/ui/move/MoveHolder.kt (MoveVm fields: x/y/z, xHomed/yHomed/zHomed, forceMove — read but do NOT modify)
    - app/src/main/java/works/mees/dinghy/designsystem/components/FootButtonBar.kt (the gutter=null + FootButtonBar-in-field LAW + uDp param)
    - app/src/main/java/works/mees/dinghy/designsystem/layout/UnitGrid.kt (rememberUnitGrid contract)
    - .planning/phases/27-motion-calibration/27-PATTERNS.md §"ui/move/MoveScreen.kt" (the full target layout: portrait padSize=(maxHeight*0.60f).coerceAtMost(maxWidth), landscape Row split, ZColumn, DistanceStepperColumn, FootButtonBar) and 27-UI-SPEC.md §"MoveScreen — specialized-layout exemption"
    - .planning/phases/27-motion-calibration/27-01-SUMMARY.md (owner's distance-stepper +/- glyph decision from Wave 0)
  </read_first>
  <action>
    Replace MoveScreen's root with `BoxWithConstraints(modifier.fillMaxSize())` + `rememberUnitGrid(minOf(maxWidth, maxHeight))`
    + a `landscape = maxWidth > maxHeight` branch (27-PATTERNS.md target). PORTRAIT: `padSize = (maxHeight * 0.60f).coerceAtMost(maxWidth)`;
    render the JogPad centered at padSize x padSize in the top region, then a Row with the Z column + distance stepper
    column (each `Modifier.weight(1f).fillMaxHeight()`) in the remaining height, then a full-width FootButtonBar.
    LANDSCAPE: Row split — JogPad fills `weight(1f).fillMaxHeight()` left half; the right half is a Column holding the
    Z + distance columns Row (weight 1f) then the FootButtonBar. Build TWO new private composables following the
    ProbeCalibrateScreen 3-cell motif (all cells `Modifier.weight(1f).fillMaxWidth()`, `Arrangement.spacedBy(8.dp)`,
    NEVER weight(2f) for the center — Pitfall 4): (a) `ZColumn` — top Z+ JogCell (intent/outline `t.directional.z`),
    center HomeZCell showing `vm.z` live readout in Geist Mono + tap-to-home (reads the SAME vm.z as the JogPad Z
    corner, Pitfall 7), bottom Z- JogCell; jog disabled per existing homed/inFlight/forceMove gates; (b)
    `DistanceStepperColumn` — top `+` OutlinedControl cycling `DISTANCES[(idx+1).coerceAtMost(lastIndex)]`, center
    StepDisplay of the active distance (Geist Mono), bottom `−` OutlinedControl cycling down; enabled at bounds;
    intent Neutral; the +/- glyphs are the owner-confirmed tokens from 27-01 (reuse BabystepExpand/Compress OR the
    distinct StepIncrease/StepDecrease per the 27-01 SUMMARY — do NOT pick). Move the Home All / Disable / Back
    actions from the old gutter into a `FootButtonBar(uDp = grid.uDp)` inside the field: Home All (Intent.Accent),
    Disable (Intent.Danger -> showDisableGuard=true), Back (Intent.Neutral, icon DinghyIcons.Back). Set
    `gutter = null` (the screen no longer uses ScreenScaffold's gutter; if MoveScreen still wraps ScreenScaffold,
    replace with the BoxWithConstraints layout entirely). KEEP the Disable ConfirmGuard composable + force-move
    toggle + JogPad internals unchanged in behavior — token-restyle only (D-04). Every text size via `fsSp(baseSp, t.fs).sp`
    ([[dinghy-font-sizes-too-small]] floors). Add the new strings to strings.xml per 27-UI-SPEC §Copywriting
    (move_z_up/down/home, move_distance_increase/decrease, move_home_all, move_disable_steppers, move_disable_confirm,
    cd_move_jog_pad) — audit existing move_* strings first to avoid duplicates. Reserve the Focus top-left corner
    for FloatingEStop (UAT-4) even though Move is idle-only. NO rememberCoroutineScope for any persistence
    (route through existing dispatch/AppContainer.writeScope if any write is touched — none expected).
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --tests works.mees.dinghy.ui.move.* --no-daemon" 2>&1 | tr -d '\r' | tail -12; /mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:assembleDebug --no-daemon" 2>&1 | tr -d '\r' | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - MoveScreen.kt uses `BoxWithConstraints` with `padSize = (maxHeight * 0.60f).coerceAtMost(maxWidth)` in the portrait branch (grep both literals).
    - A `ZColumn` and a `DistanceStepperColumn` private composable exist; every cell in each uses `Modifier.weight(1f)` (grep shows no `weight(2f)` in either column).
    - The DistanceStepperColumn cycles the existing `DISTANCES` set with index coercion (no free numeric entry).
    - The Z column center cell reads `vm.z` (live readout) AND the JogPad Z corner still reads `vm.z` (both present — Pitfall 7).
    - `gutter = null` (or ScreenScaffold removed); Home All / Disable / Back are inside a `FootButtonBar`; zero raw `MaterialSymbol(name=` in MoveScreen.
    - The Disable ConfirmGuard and force-move toggle survive (grep `ConfirmGuard` + force-move references).
    - `:app:testDebugUnitTest --tests *move*` GREEN (existing MoveHolder tests unregressed) and `:app:assembleDebug` BUILD SUCCESSFUL.
  </acceptance_criteria>
  <done>MoveScreen renders the sacred square pad + vertical Z column + distance stepper + FootButtonBar; portrait caps the pad at 60% height; pad internals and Disable guard carry over; host tests green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: MovePreviews matrix (portrait/landscape + 6 themes + fs=L + pseudolocale)</name>
  <files>app/src/test/java/works/mees/dinghy/preview/MovePreviews.kt</files>
  <read_first>
    - app/src/test/java/works/mees/dinghy/preview/ (any existing *Previews.kt — the PreviewBox idiom, @Nexus7Previews multipreview, DinghyTheme(palette, dark) wrapper, SampleFixtures usage)
    - docs/ui_design/PREVIEW_AND_TOKENS.md §3 (minimized matrix shape) and §9 (D-04/D-05 preview-unsafe placeholder idiom)
    - .planning/phases/27-motion-calibration/27-UI-SPEC.md §"Preview Contract" (MoveScreen matrix: portrait vs landscape, 6 theme combos, fs=L overflow, pseudolocale en-XA)
    - app/src/main/java/works/mees/dinghy/preview/SampleFixtures.kt (existing fixtures; add a Move vm fixture if none exists)
  </read_first>
  <action>
    Create `MovePreviews.kt` with the minimized matrix from 27-UI-SPEC §"Preview Contract" for MoveScreen: a
    `*Matrix` preview pair showing portrait vs landscape (the 60%-height portrait cap MUST be visible in the
    portrait preview — verify the pad is not full-bleed), a `*ThemeColorfulDark..HighContrastLight` set (6 combos
    via DinghyTheme(palette, dark) on portrait at the default distance), a `*FsLargeOverflow` portrait preview at
    fs=L (verifies the Z/step center-cell readouts don't clip), and a `*PseudolocaleSpotCheck` en-XA. Use a
    SampleFixtures Move vm (add `SampleFixtures.moveVm(...)` if absent — homed with a representative z value).
    No live Moonraker; all previews stateless. Follow the existing project preview file's PreviewBox/@Nexus7Previews
    conventions exactly.
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:compileDebugAndroidTestKotlin :app:assembleDebug --no-daemon" 2>&1 | tr -d '\r' | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `MovePreviews.kt` exists with at least: a portrait+landscape `*Matrix`, 6 theme-combo previews, an `*FsLargeOverflow` fs=L preview, and a pseudolocale preview (grep `@Preview` count >= 9).
    - The portrait preview demonstrates the pad capped (not full-height) — the layout compiles with the BoxWithConstraints sizing.
    - `:app:assembleDebug` BUILD SUCCESSFUL (preview compile gate green — SC-4).
  </acceptance_criteria>
  <done>MoveScreen ships its @Preview matrix on day one; the preview compile gate is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user touch -> motion dispatch | Jog / Disable-steppers are physical-motion commands; an accidental tap moves the toolhead or drops homing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-03-01 | Tampering | Disable Steppers foot button (un-homes all axes) | mitigate | ConfirmGuard (destructive, red) carries over unchanged — Disable never dispatches without an explicit confirm tap. |
| T-27-03-02 | Tampering | force-move toggle (bypasses homed-gating) | mitigate | Force-move stays an explicit armed toggle rendered C4 filled stop-red while active (visible catastrophic-mode signal); jog-disabled gates carry over per axis. |
| T-27-03-03 | Tampering | mid-print accidental jog | mitigate | Move is in FOOT_GUN_DESTS (27-02) — a print starting pops the user to root; jog gates honor homed/inFlight state. |

Block-on severity is `high`; the two destructive controls (Disable, force-move) are guarded; no high residual.
</threat_model>

<verification>
- `:app:testDebugUnitTest --tests *move*` GREEN; `:app:assembleDebug` BUILD SUCCESSFUL.
- grep confirms portrait 60%-height cap literals, equal-weight column cells, DISTANCES cycling, ConfirmGuard + force-move survival, zero raw MaterialSymbol.
- MovePreviews compiles (SC-4).
</verification>

<success_criteria>
SC-1/SC-3/SC-4/SC-5 (Move slice): the spatial jog controls stay a grid, the vertical Z column + distance
stepper land, conformance (≥64px via uDp, fsSp, rotation via BoxWithConstraints) folds in, the @Preview
matrix ships, and no Move functional regression (host tests green). Owner approves on flox at 27-07.
</success_criteria>

<output>
Create `.planning/phases/27-motion-calibration/27-03-SUMMARY.md` when done.
</output>
