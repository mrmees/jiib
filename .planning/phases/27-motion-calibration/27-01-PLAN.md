---
phase: 27-motion-calibration
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - app/src/main/java/works/mees/dinghy/designsystem/icons/DinghyIcons.kt
  - app/src/main/res/values/strings.xml
  - app/src/test/java/works/mees/dinghy/designsystem/icons/DinghyIconsTest.kt
  - tools/verify_ligatures.py
autonomous: false
requirements: [SC-4]
user_setup: []
must_haves:
  truths:
    - "The owner has confirmed the glyph for all 5 calibration hub routine icons (Probe Calibrate, Bed Mesh, Screws Tilt, Z Tilt, QGL)"
    - "The owner has confirmed whether the Move/TESTZ distance-stepper +/- cells reuse expand/compress or use distinct glyphs (D-02 / UI-SPEC Icon Contract)"
    - "Each confirmed glyph is a registered DinghyIcons token (no raw MaterialSymbol(name=...) reaches any rebuilt P27 screen)"
    - "verify_ligatures.py confirms every new ligature resolves in the bundled v2.944 font; DinghyIconsTest stays GREEN"
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/designsystem/icons/DinghyIcons.kt"
      provides: "5 owner-confirmed routine glyph tokens + confirmed distance-stepper +/- tokens"
      contains: "RoutineProbeCalibrate"
  key_links:
    - from: "DinghyIcons.all"
      to: "the new routine + stepper tokens"
      via: "explicit registration in the drift-guard list"
      pattern: "RoutineProbeCalibrate"
---

<objective>
Resolve every glyph the Phase-27 screen rebuilds need but the registry does not yet have, and
register them as `DinghyIcons` tokens. Per the HARD OWNER LAW ([[dinghy-never-pick-icons-ask]],
D-16), Claude must NEVER pick a glyph independently — this plan ASKS the owner for the unmapped
surfaces up front, then registers exactly what is confirmed, BEFORE any screen task consumes them.

The unmapped surfaces (confirmed absent / raw-ligature in the registry during planning):
1. The 5 calibration hub routine icons — currently used as RAW `MaterialSymbol(name = entry.routine.glyph)`
   in `CalibrationHubScreen.kt` (`straighten`/`grid_on`/`architecture`/`vertical_align_center`/`crop_square`),
   NOT registered tokens. The rebuilt list+Focus hub (27-04) must route them through `DinghyIconView`.
2. The Move/TESTZ vertical distance-stepper `+`/`−` cells (D-02/D-08) — UI-SPEC Icon Contract flags
   an open question: do these reuse `BabystepExpand`/`BabystepCompress` (`expand`/`compress`) or get
   distinct increment/decrement glyphs? They operate on DISTANCE selection, not spatial Z motion.

All 5 routine ligatures are confirmed present in the Geist v2.944 bundled font (Phase 18.1
`verify_ligatures.py`) — the font is NOT the blocker; the icon-registry law is.

This plan runs in Wave 0 (touches only DinghyIcons.kt + strings.xml + the icon tests + verify
script — zero overlap with the route/screen files), so the owner-confirmed tokens exist before any
Wave-1 screen plan wires them.

Purpose: Keep the icon-registry-only law intact and unblock the screen rebuilds without Claude guessing a glyph.
Output: Owner-confirmed routine + stepper tokens registered and font-verified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/27-motion-calibration/27-CONTEXT.md
@.planning/phases/27-motion-calibration/27-UI-SPEC.md
@.planning/phases/27-motion-calibration/27-PATTERNS.md
@docs/ui_design/CLAUDE.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: ASK OWNER — confirm the 5 routine glyphs + the distance-stepper +/- glyphs (D-16)</name>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/calibration/CalibrationHubScreen.kt (the current RoutineTile + CalibrationRoutine.glyph raw ligature usage — straighten/grid_on/architecture/vertical_align_center/crop_square)
    - app/src/main/java/works/mees/dinghy/designsystem/icons/DinghyIcons.kt (the Launcher*/Babystep* token style; BabystepExpand=expand, BabystepCompress=compress, LauncherCalibration=tune already registered)
    - .planning/phases/27-motion-calibration/27-UI-SPEC.md §"Icon Contract" (the OWNER DECISION GATE table + the distance-stepper +/- open question)
    - docs/ui_design/CLAUDE.md (HARD RULE: never invent/choose an icon — ask the owner)
  </read_first>
  <action>Present the unmapped P27 glyph surfaces to the owner and capture the owner's exact choice for each. Do NOT pick any glyph — surface the candidates, record the owner's decision, and stop. Task 2 registers exactly what is chosen. The two decision groups: (A) the 5 calibration hub routine icons — confirm whether the current raw ligatures (Probe Calibrate=straighten, Bed Mesh=grid_on, Screws Tilt=architecture, Z Tilt=vertical_align_center, QGL=crop_square) are blessed as registered tokens or replaced; (B) the Move + TESTZ distance-stepper +/- cells — confirm whether they reuse the existing BabystepExpand/BabystepCompress (expand/compress) tokens or get distinct increment/decrement glyphs (e.g. add/remove). Per D-16 Claude proposes nothing beyond surfacing the candidates already named in the source artifacts.</action>
  <decision>For each unmapped P27 surface, which registered glyph (existing token, blessed current ligature, or new ligature) should it use?</decision>
  <context>
    The rebuilt calibration hub (27-04) renders routine rows + a Focus icon through DinghyIconView,
    so the 5 routine glyphs MUST be registry tokens — the current raw MaterialSymbol(name=...) usage
    is forbidden post-migration. All 5 current ligatures resolve in the bundled v2.944 font (verified
    P18.1), so blessing them as-is is valid; the owner may also pick alternatives. Separately, the
    Move Z-distance stepper and the Probe TESTZ step column (D-02/D-08) need a +/- glyph pair: they
    select a numeric jog/probe step, NOT spatial Z motion, so the owner may want them visually
    distinct from the Z-up/Z-down spatial arrows (which already use expand/compress). Any new ligature
    is font-verified in Task 2.
  </context>
  <options>
    <option id="routine-icons">
      <name>5 calibration hub routine icons</name>
      <pros>Current ligatures (straighten/grid_on/architecture/vertical_align_center/crop_square) already resolve in v2.944 and match the existing hub look — blessing them is zero visual churn.</pros>
      <cons>Owner may prefer clearer printer-domain glyphs for any of the five.</cons>
    </option>
    <option id="stepper-glyphs">
      <name>Move + TESTZ distance-stepper +/- cells</name>
      <pros>Reusing BabystepExpand/BabystepCompress (expand/compress) needs zero new registration.</pros>
      <cons>expand/compress already mean spatial Z-up/Z-down in the adjacent Z column — reusing them for the step selector may read ambiguously; a distinct add/remove pair disambiguates.</cons>
    </option>
  </options>
  <resume-signal>Owner names a specific glyph for each of the 5 routines and states the +/- stepper glyph choice (reuse expand/compress OR a named distinct pair).</resume-signal>
  <verify><human-check>Owner has named a specific registered glyph for each of the 5 routines AND for the distance-stepper +/- cells.</human-check></verify>
  <done>The owner has chosen a glyph for all 5 routines and for the distance-stepper +/- pair; no glyph was picked by Claude.</done>
  <acceptance_criteria>
    - The owner has named a specific glyph (Material Symbols ligature or existing DinghyIcons token) for EACH of: Probe Calibrate, Bed Mesh, Screws Tilt, Z Tilt, QGL routine rows.
    - The owner has stated the +/- distance-stepper choice (reuse expand/compress, or a named distinct increment/decrement pair).
    - No glyph was chosen by Claude; the decisions are recorded in the SUMMARY with the owner's exact words.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Register the owner-confirmed tokens + font-verify + drift-guard</name>
  <files>app/src/main/java/works/mees/dinghy/designsystem/icons/DinghyIcons.kt, app/src/main/res/values/strings.xml, tools/verify_ligatures.py, app/src/test/java/works/mees/dinghy/designsystem/icons/DinghyIconsTest.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/designsystem/icons/DinghyIcons.kt (the Launcher*/Output*/Babystep* token blocks AND the `val all: List<DinghyIcon>` drift-guard list — every new token MUST be added there)
    - tools/verify_ligatures.py (the NEEDED ligature list the font-resolution gate checks)
    - app/src/test/java/works/mees/dinghy/designsystem/icons/DinghyIconsTest.kt (dynamic uniqueness/drift checks derived from DinghyIcons.all)
    - app/src/main/res/values/strings.xml (the cd_* content-description convention)
  </read_first>
  <action>
    Register exactly the owner-confirmed glyphs from Task 1 as new `DinghyIcons` tokens: `RoutineProbeCalibrate`,
    `RoutineBedMesh`, `RoutineScrewsTilt`, `RoutineZTilt`, `RoutineQgl` (descriptive val names matching the
    `Routine*` convention named in 27-PATTERNS.md §"Icon Registration Gate"), each as
    `DinghyIcon(IconRef.Ligature("<owner-ligature>"), alternate = "<snake_name>")`. If the owner chose distinct
    distance-stepper +/- glyphs (rather than reusing BabystepExpand/BabystepCompress), register those too with
    descriptive val names (e.g. `StepIncrease`/`StepDecrease`); if the owner chose to reuse expand/compress, add
    NO new stepper token and record that decision. Add every new token to the `DinghyIcons.all` drift-guard list.
    Add each new ligature to `tools/verify_ligatures.py`'s NEEDED list. Add any new content-description strings
    needed (cd_* convention) to strings.xml only if a token requires one not already present. Do NOT pick any
    glyph not confirmed in Task 1. Do NOT touch CalibrationHubScreen.kt or any screen file — token registration
    only; the call-site swaps are owned by the Wave-1 screen plans.
  </action>
  <verify>
    <automated>python tools/verify_ligatures.py 2>&1 | tail -5; /mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --tests works.mees.dinghy.designsystem.icons.DinghyIconsTest --no-daemon" 2>&1 | tr -d '\r' | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `DinghyIcons.kt` defines `RoutineProbeCalibrate`, `RoutineBedMesh`, `RoutineScrewsTilt`, `RoutineZTilt`, `RoutineQgl`, all 5 present in `DinghyIcons.all` (grep each val name in the `all` list).
    - If distinct stepper glyphs were chosen, those tokens exist and are in `DinghyIcons.all`; if reuse was chosen, no new stepper token is added and the SUMMARY records the reuse decision.
    - `verify_ligatures.py` exits 0 with `missing: []` — every new ligature resolves in the bundled v2.944 font.
    - `DinghyIconsTest` GREEN (uniqueness + drift checks pass with the new tokens).
    - No glyph in the diff was chosen by Claude — each matches the owner's Task-1 decision (cross-check against the SUMMARY record).
    - Zero edits to any file under `ui/calibration/` or `ui/move/` (token registration only).
  </acceptance_criteria>
  <done>The owner-confirmed routine + stepper glyphs are registered, font-verified, and drift-guarded; the Wave-1 screen plans have registry tokens to consume.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Icon-registry + string-resource edits only; no input, network, or data boundary crossed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-01-01 | (N/A) | DinghyIcons token registration | accept | No new attack surface — registry/strings edits only; local-LAN UI asset wiring. No network, auth, input, or data handling introduced. |

This plan registers font glyph tokens and strings. Block-on severity is `high`; there are none.
</threat_model>

<verification>
- `verify_ligatures.py` exits 0 (`missing: []`); all new ligatures resolve in v2.944.
- `DinghyIconsTest` GREEN.
- The owner confirmed each glyph (recorded in SUMMARY); zero Claude-chosen glyphs.
</verification>

<success_criteria>
SC-4 (foundation): the rebuilt calibration hub + the Move/TESTZ steppers have owner-confirmed,
registered, font-verified `DinghyIcons` tokens to consume in the Wave-1 screen plans — preserving the
icon-registry-only law with no Claude-picked glyphs.
</success_criteria>

<output>
Create `.planning/phases/27-motion-calibration/27-01-SUMMARY.md` when done. Record the owner's exact
glyph choices for all 5 routines and the distance-stepper +/- pair.
</output>
