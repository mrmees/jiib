---
phase: 27-motion-calibration
plan: 06
type: execute
wave: 4
depends_on: ["27-02", "27-05"]
files_modified:
  - app/src/main/java/works/mees/dinghy/ui/calibration/ScrewsTiltScreen.kt
  - app/src/main/java/works/mees/dinghy/ui/calibration/TiltScreen.kt
  - app/src/main/res/values/strings.xml
  - app/src/main/java/works/mees/dinghy/preview/SampleFixtures.kt
  - app/src/main/java/works/mees/dinghy/preview/CalibrationPreviews.kt
autonomous: true
requirements: [SC-2, SC-3, SC-4, SC-5]
user_setup: []
must_haves:
  truths:
    - "ScrewsTilt screw rows become ListRows (not hand-rolled); the bed-scale spatial visualization keeps its spatial carve-out (a graphic, not a list) (D-10)"
    - "TiltScreen stays ONE parameterized screen for both ZTilt + QGL variants; run/result states render via FootButtonBar + intent colors (D-10)"
    - "Both screens: gutter=null, FootButtonBar inside field, state-adaptive (unhomed->Home All; homed->Run; result->Run Again+Back)"
    - "CalibrationPreviews ships ScrewsTilt + Tilt matrices; all 6 P27 screens now have @Preview coverage (SC-4)"
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/ui/calibration/ScrewsTiltScreen.kt"
      provides: "ListRow screw list + spatial carve-out Focus + state-adaptive FootButtonBar"
      contains: "ListBlock"
    - path: "app/src/main/java/works/mees/dinghy/ui/calibration/TiltScreen.kt"
      provides: "one parameterized ZTilt/QGL screen with FootButtonBar, gutter=null"
      contains: "TiltVariant"
  key_links:
    - from: "ScrewsTilt screw ListRow trailing"
      to: "ScrewsTiltVm.loop.points turn instruction"
      via: "turn instruction in trailing slot (Geist Mono)"
      pattern: "turnInstruction"
    - from: "TiltScreen run action"
      to: "variant-selected run command"
      via: "TiltVariant parameterizes the dispatch"
      pattern: "TiltVariant"
---

<objective>
Restyle the two pure-restyle calibration screens (D-10) onto the jiib grammar and finish the
CalibrationPreviews matrix so all 6 P27 screens have @Preview coverage.

ScrewsTiltScreen (D-10): same flow and data. The bed-scale visualization keeps its spatial carve-out
(a graphic, not a list) — Focus unchanged in shape, token-restyled. The hand-rolled screw point rows
become `ListRow`s (label + turn instruction in the trailing slot, Geist Mono). Run/result states render
via FootButtonBar + intent colors. The scrollable screw Field auto-suppresses the swipe-up drawer (Pitfall 1).

TiltScreen (D-10): the ZTilt + QGL variants STAY one parameterized screen (no reason to split — owner
discretion confirmed). Focus = bed-tilt icon + state headline (unchanged content, token-restyle). Field
= status body + adjustment recommendations (compact, no scrollable list). Gutter -> null + FootButtonBar
inside field with state-adaptive run/result semantics.

This plan extends CalibrationPreviews.kt (now holding Hub+Probe+BedMesh) with the final ScrewsTilt +
Tilt sections — so it depends on 27-05. After this, all 6 P27 screens ship @Preview matrices (SC-4 met).

Purpose: Complete the calibration-cluster migration with the two restyle screens and close the preview matrix.
Output: Rebuilt ScrewsTiltScreen + TiltScreen + final CalibrationPreviews sections.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/27-motion-calibration/27-CONTEXT.md
@.planning/phases/27-motion-calibration/27-UI-SPEC.md
@.planning/phases/27-motion-calibration/27-PATTERNS.md
@docs/ui_design/COMPONENTS.md
@docs/ui_design/THEMING.md
@app/src/main/java/works/mees/dinghy/ui/calibration/ScrewsTiltScreen.kt
@app/src/main/java/works/mees/dinghy/ui/calibration/TiltScreen.kt
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Restyle ScrewsTiltScreen — ListRow screw list + spatial-carve-out Focus + FootButtonBar (D-10)</name>
  <files>app/src/main/java/works/mees/dinghy/ui/calibration/ScrewsTiltScreen.kt, app/src/main/res/values/strings.xml</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/calibration/ScrewsTiltScreen.kt (full file — current Focus bed-scale visualization, hand-rolled screw point rows, gutter Run/Back, ScrewsTiltVm.hasCoords / loop.points / turn instructions)
    - app/src/main/java/works/mees/dinghy/calibration/ScrewsTiltHolder.kt (vm.loop.points, homedGate, run state — consume unchanged)
    - app/src/main/java/works/mees/dinghy/designsystem/components/ListRow.kt + FootButtonBar.kt
    - app/src/main/java/works/mees/dinghy/ui/extrude/ExtrudeScreen.kt ~209 (thin-wrapper -> stateless `ExtrudeContent` seam to copy so @Preview renders without a VM — WARNING-5)
    - .planning/phases/27-motion-calibration/27-PATTERNS.md §"ui/calibration/ScrewsTiltScreen.kt" (the ListBlock+ListRow screw rows, the FootButtonBar state branches, the Focus-stays-spatial note) and 27-UI-SPEC.md §"ScrewsTiltScreen" (foot-state table + empty/result states)
    - .planning/phases/27-motion-calibration/27-01-SUMMARY.md (any owner glyph decision for a screw-point state indicator, if one was raised — otherwise no leading glyph)
  </read_first>
  <action>
    Restyle ScrewsTiltScreen: root BoxWithConstraints + rememberUnitGrid + ScreenScaffold(gutter=null) + FloatingEStop
    top-left. Focus = the EXISTING bed-scale spatial visualization (spatial carve-out — keep its custom-drawn shape;
    token-restyle only: `t.surface` background, `t.outline` borders — do NOT convert to a list). Field = `ListBlock` of
    `ListRow`s over `vm.loop?.points ?: emptyList()` (key = point.name): primary label = point.name (16sp), trailing =
    the turn instruction (e.g. "1.25 CW", Geist Mono, `t.text2`); idle/awaiting-run rows show "—" placeholder; rows are
    informational (`onClick = {}`, no selection). IF the owner confirmed a screw-point state glyph in 27-01, use it as a
    leading `DinghyIconView`; otherwise no leading icon (do NOT pick one — D-16). FootButtonBar state-adaptive per
    27-UI-SPEC: idle-unhomed -> [Home All Accent | Back]; idle-homed -> [Run Accent (SCREWS_TILT_CALCULATE) | Back];
    running -> [Back] (non-interactive, no abort in current impl); result -> [Run Again Neutral | Back]. `gutter = null`.
    The scrollable screw Field auto-suppresses the swipe-up drawer (Pitfall 1) — no manual gesture code. Every font via
    fsSp. Add new strings (calibration_run, the run-again label, screw labels if needed) — audit existing first. Zero
    raw MaterialSymbol.
    **STATELESS PREVIEW SEAM (Codex WARNING-5):** keep the public `ScrewsTiltScreen(...)` thin (collects the VM),
    forwarding a plain snapshot (loop points / turn instructions, run state, homed, hasCoords) + no-op callbacks to a
    STATELESS `@Composable ScrewsTiltContent(...)` holding the spatial Focus + ListBlock + FootButtonBar layout. The
    Task-3 @Preview targets `ScrewsTiltContent` with stub Idle/Result data — no VM (ExtrudeScreen.kt:209 pattern).
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --tests works.mees.dinghy.calibration.ScrewsTiltHolderTest --no-daemon" 2>&1 | tr -d '\r' | tail -10; /mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:assembleDebug --no-daemon" 2>&1 | tr -d '\r' | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - ScrewsTiltScreen is split into a thin VM-reading `ScrewsTiltScreen(...)` + a STATELESS `ScrewsTiltContent(...)` holding the layout (WARNING-5 seam; grep both).
    - Screw rows use `ListBlock` + `ListRow` (grep — no hand-rolled per-point Row remaining); turn instruction in the trailing slot (Geist Mono, `t.text2`).
    - The bed-scale Focus visualization is preserved as a custom-drawn spatial surface (NOT converted to a list — grep the existing visualization composable still present).
    - `gutter = null`; FootButtonBar state-adaptive (unhomed/homed/running/result branches present); zero raw `MaterialSymbol(name=`.
    - No leading screw glyph unless 27-01 confirmed one (D-16 — no Claude-picked icon).
    - `ScrewsTiltHolderTest` GREEN; `:app:assembleDebug` BUILD SUCCESSFUL.
  </acceptance_criteria>
  <done>ScrewsTilt has a ListRow screw list, a preserved spatial Focus, and a state-adaptive FootButtonBar; holder green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Restyle TiltScreen (ZTilt + QGL, one parameterized screen) — FootButtonBar, gutter=null (D-10)</name>
  <files>app/src/main/java/works/mees/dinghy/ui/calibration/TiltScreen.kt, app/src/main/res/values/strings.xml</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/calibration/TiltScreen.kt (full file — TiltVariant enum, the signature (holder, variant, onBack), gutter run/back, Focus bed-tilt icon + state headline, TiltState.Running)
    - app/src/main/java/works/mees/dinghy/calibration/TiltHolder.kt (vm state, homedGate, run command per variant — consume unchanged)
    - .planning/phases/27-motion-calibration/27-PATTERNS.md §"ui/calibration/TiltScreen.kt" (the FootButtonBar replacement + variant-stays-one-screen note) and 27-UI-SPEC.md §"TiltScreen" (foot-state table)
    - app/src/main/java/works/mees/dinghy/ui/extrude/ExtrudeScreen.kt ~209 (thin-wrapper -> stateless Content seam to copy — WARNING-5)
  </read_first>
  <action>
    Restyle TiltScreen keeping `TiltVariant { ZTilt, Qgl }` and the signature `(container, holder, variant, onBack, modifier)`
    unchanged — the two variants stay ONE parameterized screen. Root BoxWithConstraints + rememberUnitGrid +
    ScreenScaffold(gutter=null) + FloatingEStop top-left. Focus = bed-tilt icon (from DinghyIcons.LauncherCalibration or
    a routine token — do NOT introduce a new per-variant icon unless 27-01 confirmed one; D-16) + state headline (idle/
    running/result), token-restyled. Field = status body text + adjustment recommendations (result state) — compact, no
    scrollable list. Move the gutter run/back onto a `FootButtonBar(uDp=grid.uDp)` inside the field, state-adaptive per
    27-UI-SPEC: idle-unhomed -> [Home All Accent | Back]; idle-homed -> [Run Accent (variant-selected run command;
    enabled = dispatcher != null && state != Running) | Back]; running -> [Back]; result -> [Run Again Neutral | Back].
    `gutter = null`. Every font via fsSp. Reuse existing calibration_* strings; add only what's missing (audit first).
    Zero raw MaterialSymbol.
    **STATELESS PREVIEW SEAM (Codex WARNING-5):** keep the public `TiltScreen(...)` thin (collects the VM), forwarding
    a plain snapshot (variant, state, result body/recommendations, homed) + no-op callbacks to a STATELESS
    `@Composable TiltContent(...)` holding the Focus + Field + FootButtonBar layout. The Task-3 @Preview targets
    `TiltContent` with stub Idle/Running/Result data for both variants — no VM (ExtrudeScreen.kt:209 pattern).
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --tests works.mees.dinghy.calibration.TiltHolderTest --no-daemon" 2>&1 | tr -d '\r' | tail -10; /mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:assembleDebug --no-daemon" 2>&1 | tr -d '\r' | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - TiltScreen is split into a thin VM-reading `TiltScreen(...)` + a STATELESS `TiltContent(...)` holding the layout (WARNING-5 seam; grep both).
    - `TiltVariant { ZTilt, Qgl }` and the parameterized signature are unchanged (one screen, two variants — grep both enum members + the variant param).
    - `gutter = null`; the run/back actions are in a FootButtonBar; state-adaptive branches (unhomed/homed/running/result) present; the Run button is variant-selected and gated on `state != Running`.
    - Zero raw `MaterialSymbol(name=`; no new per-variant icon unless 27-01 confirmed one.
    - `TiltHolderTest` GREEN; `:app:assembleDebug` BUILD SUCCESSFUL.
  </acceptance_criteria>
  <done>TiltScreen renders both variants from one parameterized screen with a state-adaptive FootButtonBar; holder green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Finish CalibrationPreviews — ScrewsTilt + Tilt sections + fixtures (SC-4 complete) + full host suite</name>
  <files>app/src/main/java/works/mees/dinghy/preview/CalibrationPreviews.kt, app/src/main/java/works/mees/dinghy/preview/SampleFixtures.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/preview/CalibrationPreviews.kt (the Hub+Probe+BedMesh sections from 27-04/27-05 — APPEND ScrewsTilt + Tilt, do not overwrite)
    - app/src/main/java/works/mees/dinghy/preview/SampleFixtures.kt (add screwsTiltContent + tiltContent(variant, state))
    - .planning/phases/27-motion-calibration/27-UI-SPEC.md §"Preview Contract" (ScrewsTilt: Idle/Result; Tilt: Idle/Running/Result for both variants; 6 themes; fs=L; pseudolocale)
  </read_first>
  <action>
    APPEND ScrewsTilt + Tilt sections to CalibrationPreviews.kt (keep Hub+Probe+BedMesh untouched), previewing the
    STATELESS `ScrewsTiltContent` and `TiltContent` composables (not the VM-bound screens — WARNING-5). Add
    `SampleFixtures.screwsTiltContent(...)` (Idle + screw-turn Result snapshots) and `SampleFixtures.tiltContent(variant: TiltVariant, state)`.
    ScrewsTilt previews: `*Matrix` (Idle / Result with screw-turn data in ListRows), 6 themes on Idle, `*FsLargeOverflow`
    (turn instructions in trailing slot). Tilt previews: `*Matrix` (Idle / Running / Result, both ZTilt + QGL variants),
    6 themes on Idle, `*FsLargeOverflow` Result. Stateless, no live Moonraker. After this, all 6 P27 screens have @Preview
    coverage. Run the FULL host unit suite (this is the last screen-rebuild plan — confirm no calibration/move regression
    across all holders + state machines before the on-device gate at 27-07).
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:assembleDebug --no-daemon" 2>&1 | tr -d '\r' | tail -10; /mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --no-daemon" 2>&1 | tr -d '\r' | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - CalibrationPreviews.kt retains Hub+Probe+BedMesh sections AND adds ScrewsTilt + Tilt `*Matrix` + 6 themes + `*FsLargeOverflow` (grep all 5 screen section markers present).
    - `SampleFixtures.screwsTiltContent(...)` and `SampleFixtures.tiltContent(variant, state)` exist; previews target the stateless `ScrewsTiltContent`/`TiltContent`.
    - `:app:assembleDebug` BUILD SUCCESSFUL (previews in MAIN = assembleDebug is the preview compile gate; SC-4 — all 6 P27 screens have previews); `:app:compileDebugAndroidTestKotlin` still green.
    - FULL `:app:testDebugUnitTest` GREEN (no Move/calibration regression — SC-5 host tier).
  </acceptance_criteria>
  <done>All 6 P27 screens ship @Preview matrices; the full host suite is green; the migration is ready for the on-device gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user touch -> tilt/screws run | Z-tilt/QGL/screws-tilt run commands move multiple Z motors / report bed-screw turns; wrong-time invocation is a motion foot-gun |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-06-01 | Tampering | Run (ZTilt/QGL/ScrewsTilt) while unhomed or mid-print | mitigate | Run is homed-gated (unhomed foot shows Home All instead) and the screens are in FOOT_GUN_DESTS (27-02) so a print starting pops to root; Run disabled while a routine is already Running. |

This phase adds no new network/input/data surface (pure restyle, same dispatch paths). Block-on severity is `high`; no high residual.
</threat_model>

<verification>
- `ScrewsTiltHolderTest` + `TiltHolderTest` GREEN; FULL `:app:testDebugUnitTest` GREEN.
- `:app:assembleDebug` + `:app:compileDebugAndroidTestKotlin` BUILD SUCCESSFUL.
- grep: ListRow screw rows, spatial Focus preserved, TiltVariant one-screen, gutter=null, zero raw MaterialSymbol, all 6 preview sections present.
</verification>

<success_criteria>
SC-2/SC-3/SC-4/SC-5 (ScrewsTilt + Tilt slice + preview close): both restyle screens land on the kit with
preserved spatial carve-out + one parameterized Tilt screen; all 6 P27 screens have @Preview coverage;
the full host suite is green. Owner approves on flox at 27-07.
</success_criteria>

<output>
Create `.planning/phases/27-motion-calibration/27-06-SUMMARY.md` when done.
</output>
