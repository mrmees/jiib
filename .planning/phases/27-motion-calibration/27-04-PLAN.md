---
phase: 27-motion-calibration
plan: 04
type: execute
wave: 2
depends_on: ["27-01", "27-02"]
files_modified:
  - app/src/main/java/works/mees/dinghy/ui/calibration/CalibrationHubScreen.kt
  - app/src/main/java/works/mees/dinghy/ui/calibration/ProbeCalibrateScreen.kt
  - app/src/main/res/values/strings.xml
  - app/src/main/java/works/mees/dinghy/preview/SampleFixtures.kt
  - app/src/test/java/works/mees/dinghy/preview/CalibrationPreviews.kt
autonomous: true
requirements: [SC-2, SC-3, SC-4, SC-5]
user_setup: []
must_haves:
  truths:
    - "The calibration hub is a list+Focus screen: Field = routine ListRows (supported-first), Focus = selected routine's icon + title + description + accent Open button; first entry pre-selected so Focus is never empty (D-05)"
    - "All 5 routines always render; unsupported ones are greyed (t.text3) but still listed and selectable/openable (D-06 owner override)"
    - "Hub routine icons + Focus icon route through the owner-confirmed DinghyIcons Routine* tokens — zero raw MaterialSymbol(name=...) (D-16)"
    - "ProbeCalibrate TESTZ controls are paired vertical 3-cell columns (Z-nudge with live-Z readout center + step column) matching the Move motif (D-08)"
    - "ProbeCalibrate foot is state-adaptive on FootButtonBar (Idle-unhomed/Idle-homed/starting/Active/Accepted) with Back suppressed when Active; the starting state carries over verbatim (D-09, Pitfall 6)"
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/ui/calibration/CalibrationHubScreen.kt"
      provides: "list+Focus hub with DetailCard Focus + ListBlock routine list + onOpen nav"
      contains: "DetailCard"
    - path: "app/src/main/java/works/mees/dinghy/ui/calibration/ProbeCalibrateScreen.kt"
      provides: "vertical TESTZ columns + state-adaptive FootButtonBar, gutter=null"
      contains: "FootButtonBar"
    - path: "app/src/test/java/works/mees/dinghy/preview/CalibrationPreviews.kt"
      provides: "@Preview matrices for Hub + ProbeCalibrate"
      contains: "@Preview"
  key_links:
    - from: "CalibrationHubScreen onOpen"
      to: "navController.navigate (via AppShell)"
      via: "onOpen(routine) callback wired in 27-02"
      pattern: "onOpen"
    - from: "ProbeCalibrate FootButtonBar Active branch"
      to: "probeCalibrateHolder state machine (Accept/Abort)"
      via: "state-adaptive when(vm.state), Back suppressed"
      pattern: "ProbePageState.Active"
---

<objective>
Rebuild the two highest-value calibration screens onto the jiib grammar:

CalibrationHubScreen (D-05/D-06) — the tile grid dies; becomes a list+Focus screen following the
SpoolScreen pilot. Field = routine `ListRow`s (supported-first, consuming `CalibrationHubHolder.routines`
unchanged); Focus = a `DetailCard` with the selected routine's icon (owner-confirmed Routine* token,
UAT-1 ~75% of U) + title + author-written description + an accent Open button that pushes the routine
route. First entry pre-selected so Focus is never empty (D-05). All 5 routines always render;
unsupported ones greyed `t.text3` but still listed and selectable/openable (D-06 owner override).

ProbeCalibrateScreen (D-08/D-09) — the TESTZ controls become paired vertical 3-cell columns matching
the Move motif: a Z-nudge column (Z-up / live-Z readout center cell / Z-down firing TESTZ ±step) beside
a step column (+ / step readout / −). The state-adaptive gutter `when(vm.state)` moves onto FootButtonBar
verbatim (Idle-unhomed -> Home All; Idle-homed -> Start; `starting` -> disabled Starting…; Active ->
Accept(go)/Abort(danger) with Back suppressed; Accepted -> amber Save behind the SAVE_CONFIG ConfirmGuard).
The `starting` state and `LaunchedEffect(Unit){ holder.reset() }` carry over verbatim (Pitfall 6, Pitfall 3).

This plan creates CalibrationPreviews.kt (Hub + Probe sections); 27-05 and 27-06 extend it. It depends
on Wave 0 (27-01 routine glyphs, 27-02 nav spine + the onNavigate->onOpen rename seam already applied).

Purpose: Deliver the hub navigation surface + the probe wizard on the redesign with verbatim state
semantics and the nav-layer back-block.
Output: Rebuilt CalibrationHubScreen + ProbeCalibrateScreen + CalibrationPreviews (Hub+Probe).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/27-motion-calibration/27-CONTEXT.md
@.planning/phases/27-motion-calibration/27-UI-SPEC.md
@.planning/phases/27-motion-calibration/27-PATTERNS.md
@docs/ui_design/COMPONENTS.md
@docs/ui_design/THEMING.md
@docs/ui_design/PREVIEW_AND_TOKENS.md
@app/src/main/java/works/mees/dinghy/ui/calibration/CalibrationHubScreen.kt
@app/src/main/java/works/mees/dinghy/ui/calibration/ProbeCalibrateScreen.kt
@app/src/main/java/works/mees/dinghy/ui/spool/SpoolScreen.kt
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Rebuild CalibrationHubScreen as list+Focus (D-05/D-06)</name>
  <files>app/src/main/java/works/mees/dinghy/ui/calibration/CalibrationHubScreen.kt, app/src/main/res/values/strings.xml</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/calibration/CalibrationHubScreen.kt (full file — the current RoutineTile grid, CalibrationRoutine.glyph raw ligature usage, the onOpen param renamed from onNavigate in 27-02, the supported/unsupported color logic ~116-148)
    - app/src/main/java/works/mees/dinghy/ui/spool/SpoolScreen.kt (the list+Focus+DetailCard pilot — ListBlock + ListRow + DetailCard + ScreenScaffold gutter=null + FloatingEStop + rememberUnitGrid)
    - app/src/main/java/works/mees/dinghy/calibration/CalibrationHubHolder.kt + CalibrationGate.kt (RoutineEntry(routine,isSupported) supported-first flow — consume unchanged)
    - app/src/main/java/works/mees/dinghy/designsystem/components/DetailCard.kt + ListRow.kt + FootButtonBar.kt (kit call signatures + selection fill convention)
    - .planning/phases/27-motion-calibration/27-PATTERNS.md §"ui/calibration/CalibrationHubScreen.kt" (full target: signature, pre-select LaunchedEffect, ListBlock items, HubRoutineFocus DetailCard) and 27-UI-SPEC.md §"CalibrationHubScreen" + §Copywriting (descriptions table)
    - .planning/phases/27-motion-calibration/27-01-SUMMARY.md (the owner-confirmed Routine* glyph tokens)
  </read_first>
  <action>
    Replace the tile-grid hub with the list+Focus structure from 27-PATTERNS.md §CalibrationHubScreen. Signature:
    `CalibrationHubScreen(holder, onOpen: (CalibrationRoutine)->Unit, onBack, modifier)`. Collect `holder.routines`;
    `var selected by remember { mutableStateOf<CalibrationRoutine?>(null) }`; `LaunchedEffect(routines){ if (selected==null) selected = routines.firstOrNull()?.routine }`
    (D-05 pre-select). Root: `BoxWithConstraints` + `rememberUnitGrid(minOf(maxWidth,maxHeight))` + `ScreenScaffold(gutter=null)`
    + `FloatingEStop` overlay (UAT-4 top-left reserve). Focus = `DetailCard` rendering `HubRoutineFocus(selected)`:
    routine icon via `DinghyIconView(routineIconToken(routine), sizeDp = grid.uDp * 0.75f)` using the owner-confirmed
    Routine* tokens (NO raw MaterialSymbol — D-16), title (20sp semibold), author-written description (15sp, from the
    27-UI-SPEC §Copywriting table), and an accent `OutlinedControl(Intent.Accent)` Open button calling `onOpen(routine)`.
    Field = `ListBlock` of `ListRow`s over `routines` (key = routine.name): each row `selected = entry.routine == selected`,
    `onClick = { selected = entry.routine }`, leading icon = the routine token tinted `t.accent2` if supported else
    `t.text3` (D-06 greyed), label `t.text` if supported else `t.text3` — ALL 5 routines render, unsupported dimmed but
    still selectable AND openable (D-06). FootButtonBar with a single Back (Intent.Neutral, DinghyIcons.Back). Every
    font via `fsSp(baseSp,t.fs).sp`. Add the new strings (calibration_open_routine, the 5 routine title/desc keys,
    cd_calibration_hub) per 27-UI-SPEC §Copywriting — audit existing calibration_* strings before adding. The
    ListBlock is a scrollable Field so it auto-suppresses the swipe-up drawer (Pitfall 1) — do not add manual gesture code.
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --tests works.mees.dinghy.calibration.* --no-daemon" 2>&1 | tr -d '\r' | tail -12; /mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:assembleDebug --no-daemon" 2>&1 | tr -d '\r' | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - CalibrationHubScreen uses `ListBlock` + `ListRow` (no `LazyVerticalGrid`/RoutineTile remaining — grep returns zero RoutineTile) and a `DetailCard` Focus.
    - A pre-select `LaunchedEffect(routines)` sets `selected` to the first routine when null (D-05 — Focus never empty).
    - All 5 routines render; unsupported uses `t.text3` tint for both label and icon (grep the `if (entry.isSupported) ... else t.text3` branch); unsupported rows are still `onClick`-selectable and the Open button is reachable (D-06).
    - Zero raw `MaterialSymbol(name=` in CalibrationHubScreen; routine icons use `DinghyIconView(DinghyIcons.Routine*)`.
    - `gutter = null`; Back in a FootButtonBar; FloatingEStop overlay present (UAT-4).
    - `CalibrationHubHolderTest` GREEN (holder unchanged) and `:app:assembleDebug` BUILD SUCCESSFUL.
  </acceptance_criteria>
  <done>The hub is a list+Focus screen with pre-selected Focus, greyed-but-listed unsupported routines, registry icons, and Open-pushes-route wiring.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Rebuild ProbeCalibrateScreen — vertical TESTZ columns + state-adaptive FootButtonBar (D-08/D-09)</name>
  <files>app/src/main/java/works/mees/dinghy/ui/calibration/ProbeCalibrateScreen.kt, app/src/main/res/values/strings.xml</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/calibration/ProbeCalibrateScreen.kt (full file — the starting-state def ~122-126, gutter when(vm.state) branches ~162-255, ProbeJogPad columns ~419-462, ProbeFocus, LaunchedEffect(Unit){holder.reset()} ~112, TESTZ_STEPS, ProbeIconButton/StepDisplay)
    - app/src/main/java/works/mees/dinghy/calibration/ProbeCalibrateHolder.kt + ManualProbeState.kt (Idle/Active/Accepted state machine + markAborted — consume unchanged)
    - app/src/main/java/works/mees/dinghy/designsystem/components/FootButtonBar.kt + control/OutlinedControl.kt (Intent values: Accent/Go/Danger/Warn/Neutral)
    - .planning/phases/27-motion-calibration/27-PATTERNS.md §"ui/calibration/ProbeCalibrateScreen.kt" (the D-08 3-cell Z-nudge column with ZReadoutDisplay center + the verbatim state-adaptive FootButtonBar branches) and 27-UI-SPEC.md §"ProbeCalibrateScreen" (the foot-button state table + Back-suppression note + starting def)
  </read_first>
  <action>
    Rebuild ProbeCalibrateScreen keeping the signature `(container, holder, onBack, modifier)` unchanged. COPY the
    `starting` boolean VERBATIM (`vm.state == ProbePageState.Idle && ("probe_calibrate" in inFlight || "z_endstop_calibrate" in inFlight)`,
    Pitfall 6) and KEEP `LaunchedEffect(Unit){ holder.reset() }` at entry (Pitfall 3 — Probe DOES reset; do not remove).
    Root: BoxWithConstraints + rememberUnitGrid + ScreenScaffold(gutter=null) + FloatingEStop top-left (UAT-4). Focus =
    the existing ProbeFocus content (probe offset + live-Z, token-restyled; offset number `t.accent`). Field = two vertical
    3-cell columns side by side (the Move motif): Column 1 Z-nudge = Z-up ProbeIconButton (fires TESTZ +step) / a NEW
    center `ZReadoutDisplay(vm.z)` cell (Geist Mono, `t.directional.z` outline — D-08 adds this center cell to what was a
    2-cell column) / Z-down ProbeIconButton (fires TESTZ -step); Column 2 step selector = + / StepDisplay(step) / − cycling
    `TESTZ_STEPS` (existing pattern, Intent.Neutral, `t.outline`). All cells `Modifier.weight(1f).fillMaxWidth()` — no
    weight(2f) (Pitfall 4). Move the `when(vm.state)` gutter branches onto a `FootButtonBar(uDp=grid.uDp)` inside the
    field, VERBATIM semantics per the 27-UI-SPEC foot-button table: Idle+starting -> [Starting… disabled Neutral | Back];
    Idle+unhomed -> [Home All Accent | Back]; Idle+homed ready -> [Start Accent | Back]; Active -> [Accept Go | Abort
    Danger] with NO Back button (the nav-layer BackHandler from 27-02 owns system-Back suppression); Accepted -> [Save
    Warn/heat behind the SAVE_CONFIG ConfirmGuard | Back]. KEEP the SAVE_CONFIG ConfirmGuard (amber/warn) + the markAborted
    dispatch on Abort unchanged. `gutter = null`. Every font via fsSp. Add new strings (calibration_start/accept/abort/
    save_config/save_config_confirm/home_all, probe_starting) per 27-UI-SPEC §Copywriting — audit existing first. Zero raw
    MaterialSymbol.
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --tests works.mees.dinghy.calibration.ProbeCalibrateHolderTest --no-daemon" 2>&1 | tr -d '\r' | tail -10; /mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:assembleDebug --no-daemon" 2>&1 | tr -d '\r' | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - The `starting` boolean matches the verbatim definition (grep both `probe_calibrate` and `z_endstop_calibrate` in the condition).
    - `LaunchedEffect(Unit)` with `holder.reset()` is present (Pitfall 3 — not removed).
    - Two vertical 3-cell columns exist; the Z-nudge column has a center `ZReadoutDisplay`/live-Z cell (D-08); all cells `weight(1f)` (no `weight(2f)`).
    - FootButtonBar branches cover Idle-unhomed / Idle-homed / starting / Active / Accepted; the Active branch has NO Back button (Accept Go + Abort Danger only).
    - `gutter = null`; zero raw `MaterialSymbol(name=`; SAVE_CONFIG ConfirmGuard (warn/amber) preserved.
    - `ProbeCalibrateHolderTest` GREEN (state machine unregressed); `:app:assembleDebug` BUILD SUCCESSFUL.
  </acceptance_criteria>
  <done>ProbeCalibrate uses the paired vertical columns + verbatim state-adaptive FootButtonBar; Back is suppressed when Active; the state machine and SAVE_CONFIG guard are intact.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: CalibrationPreviews (Hub + Probe sections) + SampleFixtures additions</name>
  <files>app/src/test/java/works/mees/dinghy/preview/CalibrationPreviews.kt, app/src/main/java/works/mees/dinghy/preview/SampleFixtures.kt</files>
  <read_first>
    - app/src/test/java/works/mees/dinghy/preview/ (existing *Previews.kt idioms + @Nexus7Previews + DinghyTheme wrapper)
    - app/src/main/java/works/mees/dinghy/preview/SampleFixtures.kt (existing fixtures — add calibrationRoutineList + probeVm(state))
    - .planning/phases/27-motion-calibration/27-UI-SPEC.md §"Preview Contract" (Hub matrix: ProbeCalibrate-selected vs BedMesh-selected vs unsupported-greyed visible; Probe matrix: Idle-unhomed/Idle-homed/Active/Accepted; 6 themes; fs=L; pseudolocale) and §"Sample fixtures"
    - docs/ui_design/PREVIEW_AND_TOKENS.md §3 + §9
  </read_first>
  <action>
    Create `CalibrationPreviews.kt` with the Hub + ProbeCalibrate matrices from 27-UI-SPEC §"Preview Contract". Add
    `SampleFixtures.calibrationRoutineList` (all 5 entries, 3 supported + 2 greyed) and `SampleFixtures.probeVm(state: ProbePageState)`
    state variants. Hub previews: a `*Matrix` (ProbeCalibrate-selected / BedMesh-selected / an unsupported-greyed entry
    visible), 6 theme combos (ProbeCalibrate selected), `*FsLargeOverflow` (verifies description text doesn't clip the
    Focus card), pseudolocale en-XA. Probe previews: a `*Matrix` (Idle-unhomed / Idle-homed / Active / Accepted states),
    6 theme combos on Active (most complex), `*FsLargeOverflow` Active, pseudolocale. Stateless, no live Moonraker.
    This file is EXTENDED (not overwritten) by 27-05 (BedMesh) and 27-06 (ScrewsTilt/Tilt) — structure it with clear
    per-screen sections so later plans append cleanly.
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:compileDebugAndroidTestKotlin :app:assembleDebug --no-daemon" 2>&1 | tr -d '\r' | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `CalibrationPreviews.kt` exists with Hub + Probe sections; `@Preview` count >= 16 (Hub matrix+6themes+fsL+pseudo; Probe matrix+6themes+fsL+pseudo).
    - `SampleFixtures.calibrationRoutineList` (5 entries: 3 supported, 2 greyed) and `SampleFixtures.probeVm(state)` exist.
    - `:app:assembleDebug` + `:app:compileDebugAndroidTestKotlin` BUILD SUCCESSFUL (preview compile gate — SC-4).
  </acceptance_criteria>
  <done>Hub + ProbeCalibrate ship their @Preview matrices on day one; the compile gate is green; the previews file is structured for 27-05/27-06 to extend.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user touch -> probe motion / SAVE_CONFIG | TESTZ nudges drive the nozzle toward the bed; SAVE_CONFIG restarts Klipper and persists offsets |
| system Back -> live probe session | abandoning an Active probe mid-descent (handled at nav layer in 27-02) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-04-01 | Tampering | system Back during Active/starting probe | mitigate | Nav-layer BackHandler (27-02) swallows Back; this screen's Active foot omits a Back button so the only exits are Accept/Abort (D-09). |
| T-27-04-02 | Tampering | SAVE_CONFIG (restart + persist) | mitigate | Save sits behind the amber SAVE_CONFIG ConfirmGuard (proceed-at-peril) carried over unchanged. |
| T-27-04-03 | Information Disclosure | hub renders unsupported routines | accept | D-06 owner override — greyed-but-listed is an intentional visual reminder; routines gate on Capabilities.hasObject at dispatch, so opening an unsupported one cannot run an unavailable macro. |

Block-on severity is `high`; the two destructive/irreversible flows (mid-probe abandon, SAVE_CONFIG) are mitigated; no high residual.
</threat_model>

<verification>
- `CalibrationHubHolderTest` + `ProbeCalibrateHolderTest` GREEN (holders/state machine unregressed).
- `:app:assembleDebug` + `:app:compileDebugAndroidTestKotlin` BUILD SUCCESSFUL.
- grep: zero RoutineTile, zero raw MaterialSymbol, D-06 greyed branch present, starting verbatim, Active foot has no Back.
</verification>

<success_criteria>
SC-2/SC-3/SC-4/SC-5 (Hub + Probe slice): the hub is a coherent list+Focus nav surface with greyed-but-listed
routines and registry icons; ProbeCalibrate uses the paired vertical columns with verbatim state-adaptive foot
+ nav-layer back-block; conformance + previews fold in; holders/state machine green. Owner approves on flox at 27-07.
</success_criteria>

<output>
Create `.planning/phases/27-motion-calibration/27-04-SUMMARY.md` when done.
</output>
