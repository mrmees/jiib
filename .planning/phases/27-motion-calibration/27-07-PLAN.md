---
phase: 27-motion-calibration
plan: 07
type: execute
wave: 5
depends_on: ["27-03", "27-04", "27-05", "27-06"]
files_modified:
  - .planning/phases/27-motion-calibration/27-UAT.md
autonomous: false
requirements: [SC-1, SC-2, SC-3, SC-5]
user_setup: []
must_haves:
  truths:
    - "A signed/debug-signed RELEASE APK with all 6 rebuilt P27 screens is force-rebuilt (mtime after the last screen commit) and installed on flox before the owner tests"
    - "The owner verifies Move on flox in BOTH portrait and landscape: spatial jog grid intact, vertical Z column + distance stepper work, the pad never crowds out the columns (SC-1)"
    - "The owner walks the calibration hub + all 5 routine screens on a live printer: hub list+Focus, probe wizard nav/back-stack/back-suppression, bed-mesh profile apply/save/remove, screws-tilt run, tilt run — coherent with the redesigned nav (SC-2)"
    - "On-device smoke confirms no functional regression: homing/jog, probe/mesh/screws routines intact (SC-5)"
    - "Results recorded in 27-UAT.md; the C3 vertical-Z todo is closed iff the owner approves the vertical Z column (CONTEXT D-01)"
  artifacts:
    - path: ".planning/phases/27-motion-calibration/27-UAT.md"
      provides: "on-device UAT scaffold + recorded owner results for SC-1/SC-2/SC-5"
      contains: "SC-1"
  key_links:
    - from: "force-rebuilt release APK"
      to: "flox (Nexus 7 2013 / Adreno 320 / LineageOS 18.1)"
      via: "adb install of an APK whose mtime is after the last 27 screen commit"
      pattern: "adb install"
---

<objective>
The load-bearing on-device UAT phase gate (the pattern of 24-05 / 25-07). After all six P27 screens are
rebuilt and the full host suite is green, build a force-rebuilt APK, install it on flox, and have the
owner drive Move + the full calibration cluster on a live printer in both orientations. This is the
authoritative gate for SC-1 (Move both orientations), SC-2 (calibration nav/back-stack coherence), and
SC-5 (no functional regression — homing/jog/probe/mesh/screws intact). Host tests + previews (SC-3/SC-4)
were closed in the screen plans; this plan proves the on-device reality.

CRITICAL stale-APK guard ([[dinghy-stale-apk-uat-gate]]): Gradle may mark assembleDebug/Release UP-TO-DATE
and install a STALE APK. Force a rebuild (`--rerun-tasks`) and VERIFY the installed APK's mtime is AFTER
the last 27 screen commit before the owner touches it — otherwise the owner tests an old build and "the
fix doesn't work." Suspect the build before re-diagnosing code.

The owner drives the printer (Ender 5 Plus = 192.168.1.120:7125 or Ender 3 Pro = 192.168.1.121:7125,
both klicky probes). Claude builds + installs + scaffolds the checklist; the owner conducts and reports.

Purpose: Prove the migration works on the real Adreno-320 hardware floor with a live printer — the only
authoritative SC-1/SC-2/SC-5 gate.
Output: 27-UAT.md with recorded owner results; C3 todo closed iff vertical Z approved.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/27-motion-calibration/27-CONTEXT.md
@.planning/phases/27-motion-calibration/27-UI-SPEC.md
@.planning/phases/27-motion-calibration/27-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Force-rebuild + install on flox + scaffold 27-UAT.md</name>
  <files>.planning/phases/27-motion-calibration/27-UAT.md</files>
  <read_first>
    - CLAUDE.md §"Local Build Environment" (the gw.bat invocation + sign-release.bat for release installs + adb interop) and the dinghy-display CLAUDE.md build notes
    - .planning/phases/27-motion-calibration/27-VALIDATION.md §"Manual-Only Verifications" (SC-1/SC-2/SC-5 UAT instructions)
    - .planning/phases/27-motion-calibration/27-UI-SPEC.md §"Conformance Checklist" (the on-device items: ≥64px, rotation, scrollable-Field-suppresses-drawer, FloatingEStop reserve, D-09 back-suppression)
    - .planning/phases/24-navigation-spine/24-UAT.md (the prior on-device UAT scaffold shape to mirror)
  </read_first>
  <action>
    Build a fresh APK with `--rerun-tasks` to defeat the stale-APK trap: run `:app:assembleDebug --rerun-tasks`
    (or `:app:assembleRelease --rerun-tasks` + sign-release.bat if a release-parity check is wanted) via gw.bat,
    pipe through `tr -d '\r'`, trust the exit code. VERIFY the built APK's mtime is AFTER the last 27 screen commit
    (`git log -1 --format=%cd` vs the APK file mtime) — if older, the build was a no-op; re-run with --rerun-tasks
    and re-check. Install on flox via adb interop (confirm "Success"). Scaffold `27-UAT.md` with a checklist covering:
    SC-1 — Move portrait + landscape (spatial jog grid intact; vertical Z column up/down + live readout + tap-to-home;
    distance stepper +/- cycles; pad does NOT crowd the columns in portrait at 60% cap; force-move toggle; Disable
    ConfirmGuard); SC-2 — calibration hub list+Focus (pre-selected Focus, greyed-but-listed unsupported routines,
    Open pushes route, Back pops to hub), Probe wizard (TESTZ vertical columns, state-adaptive foot, system-Back
    swallowed while Active — try the gesture, Accept/Abort, SAVE_CONFIG amber gate), BedMesh (profile list, tap-select,
    Apply loads, Save-name alphanumeric takeover + validation, Remove ConfirmGuard), ScrewsTilt run + screw rows,
    Tilt ZTilt + QGL run; SC-5 — no regression in homing/jog/probe/mesh/screws; plus conformance spot-checks
    (scrollable Field swipe-up does NOT open the App Drawer; ≥64px targets; rotation does not reset selected
    profile/routine). Each item starts `pending`. Do NOT mark anything passed — Task 2 records the owner's verdict.
  </action>
  <verify>
    <automated>ls -la app/build/outputs/apk/debug/*.apk 2>&1 | tail -3; git log -1 --format='last-commit: %cd' 2>&1; /mnt/c/Windows/System32/cmd.exe /c "E:\Android\Sdk\platform-tools\adb.exe devices" 2>&1 | tr -d '\r' | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - A fresh APK exists with mtime AFTER the last 27 screen commit (the ls mtime vs git last-commit comparison proves not-stale — [[dinghy-stale-apk-uat-gate]]).
    - The APK is installed on flox (adb install returned Success; `adb devices` shows flox).
    - `27-UAT.md` exists with SC-1 / SC-2 / SC-5 checklist items + conformance spot-checks, all `pending`.
  </acceptance_criteria>
  <done>A non-stale APK with all 6 rebuilt screens is on flox and the UAT checklist is scaffolded.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: OWNER on-device UAT — Move (both orientations) + full calibration cluster on a live printer</name>
  <files>.planning/phases/27-motion-calibration/27-UAT.md</files>
  <action>
    Pause for the owner to conduct the on-device UAT on flox with a live printer. Present the what-built summary
    and the step-by-step how-to-verify script below, wait for the owner's verdict, and record the per-item
    SC-1/SC-2/SC-5 results in 27-UAT.md. Do NOT mark any item passed without the owner's word; capture any
    failures concretely for gap-closure. Close the C3 vertical-Z todo only if the owner approves the vertical Z column.
  </action>
  <what-built>
    All six Phase-27 screens rebuilt onto the jiib grammar: MoveScreen (spatial grid + vertical Z column +
    distance stepper, 60%-portrait cap), CalibrationHub (list+Focus), ProbeCalibrate (vertical TESTZ columns +
    state-adaptive foot + nav-layer back-suppression), BedMesh (profile list + SaveName takeover), ScrewsTilt
    (ListRow screws), Tilt (ZTilt/QGL). Installed on flox (force-rebuilt, non-stale).
  </what-built>
  <how-to-verify>
    1. Open Move. PORTRAIT: confirm the XY pad is a square grid that does NOT crowd out the Z/distance columns
       (capped ~60% height); jog X/Y; jog Z up/down via the vertical Z column; confirm the live Z readout +
       tap-to-home in the column center; cycle the distance stepper +/-; arm the red force-move toggle; tap
       Disable Steppers and confirm the red ConfirmGuard. Rotate to LANDSCAPE and repeat — pad fills its half,
       columns beside it, U constant.
    2. Swipe up into the App Drawer -> Calibration. Confirm the hub is a list with a pre-selected Focus
       (icon + title + description + Open button); confirm unsupported routines are greyed but still listed.
       Tap a row to select; tap Open to push the routine; Back pops to the hub.
    3. Probe Calibrate (homed): Start; during the Active session TRY the system Back gesture — it must be
       swallowed (no exit); use the TESTZ vertical columns (Z up/down + step +/-) to paper-drag; Accept;
       confirm the amber Save & Restart ConfirmGuard.
    4. Bed Mesh (homed): tap a profile to select (no immediate load); Apply loads it; Save opens the
       alphanumeric name takeover pre-filled with the timestamp — type an invalid name (confirm disabled),
       then a valid one (confirm enabled); Remove a profile behind the red ConfirmGuard.
    5. Screws Tilt: Run; confirm screw rows show turn instructions. Z Tilt and QGL: Run each (if the printer
       supports it; greyed-unsupported is expected otherwise).
    6. Confirm: inside any scrollable list (hub, profiles, screws), swiping UP does NOT open the App Drawer;
       touch targets feel ≥finger-sized; rotating mid-screen does not reset the selected profile/routine.
    7. Smoke: homing/jog and the probe/mesh/screws routines behave as before — no functional regression.
  </how-to-verify>
  <resume-signal>Type "approved" (optionally noting the vertical-Z verdict for the C3 todo), or describe issues per screen.</resume-signal>
  <verify><human-check>Owner has driven Move in both orientations + the full calibration cluster on a live printer and reported pass/fail per SC-1/SC-2/SC-5.</human-check></verify>
  <acceptance_criteria>
    - The owner has reported a verdict for SC-1 (Move both orientations), SC-2 (calibration nav/back-stack coherence across hub + 5 routines), and SC-5 (no functional regression).
    - 27-UAT.md is updated with the owner's per-item results (passed/failed + notes); any failures are captured concretely for gap-closure.
    - The C3 vertical-Z todo (`2026-06-05-move-z-vertical-layout-rework.md`) is closed IFF the owner approved the vertical Z column (D-01); otherwise it stays open with the owner's note.
  </acceptance_criteria>
  <done>The owner has conducted the on-device UAT on flox + a live printer and recorded SC-1/SC-2/SC-5 results in 27-UAT.md.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stale APK -> false UAT verdict | Installing an UP-TO-DATE (stale) APK makes the owner test an old build and mis-judge the migration |
| live printer motion under owner control | The owner drives real homing/jog/probe/screws/tilt motion during UAT |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-07-01 | Spoofing (build identity) | stale APK installed for UAT | mitigate | Force `--rerun-tasks` + verify APK mtime > last 27 screen commit before install ([[dinghy-stale-apk-uat-gate]]). |
| T-27-07-02 | Tampering | live motion during UAT | accept | Owner-conducted on the owner's own printer; all the in-app destructive guards (ConfirmGuard, back-suppression, homed-gating) shipped in 27-03..27-06 are the mitigations being verified here. |

This is a verification plan (no production code changes — only 27-UAT.md). Block-on severity is `high`; the build-identity risk is mitigated; no high residual.
</threat_model>

<verification>
- APK mtime is after the last 27 screen commit (non-stale proof).
- flox shows in `adb devices`; install Success.
- Owner-reported SC-1/SC-2/SC-5 results recorded in 27-UAT.md.
</verification>

<success_criteria>
SC-1 + SC-2 + SC-5 (authoritative on-device gate): Move works in both orientations with the spatial grid
kept; the calibration hub + 5 routines are coherent with the redesigned nav/back-stack; no functional
regression in homing/jog/probe/mesh/screws — all confirmed by the owner on the real Adreno-320 floor with
a live printer. (SC-3/SC-4 closed in the screen plans.)
</success_criteria>

<output>
Create `.planning/phases/27-motion-calibration/27-07-SUMMARY.md` when done. Record the owner's per-screen
verdict, any gap items, and the C3-todo disposition.
</output>
