---
phase: 22-performance-architecture-refactor
plan: 03
type: execute
wave: 1
depends_on: ["22-01"]
files_modified:
  - app/src/main/java/works/mees/dinghy/designsystem/icons/SpoolGlyph.kt
  - app/src/test/java/works/mees/dinghy/designsystem/icons/SpoolGlyphTest.kt
autonomous: true
requirements: []
must_haves:
  truths:
    - "D-03/P2: SpoolGlyph no longer allocates a new Brush.linearGradient on every recomposition — the gradient Brush is wrapped in remember(render) keyed on the SpiralRender instance (whose data-class equality is its two Color fields), mirroring ColorWheel.kt rememberHueSweep() — so a repeated same-color emission returns the cached Brush and a filament-color change rebuilds it"
    - "The SpoolGlyph render logic (spiralRenderFor solid/gradient/empty) is behavior-unchanged: same Brush is produced, only its allocation is now cached — verified by the existing SpoolGlyphTest staying green and an added assertion that an unchanged render reuses the brush instance"
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/designsystem/icons/SpoolGlyph.kt"
      provides: "Cached gradient Brush via remember(render)"
      contains: "remember(render)"
  key_links:
    - from: "app/src/main/java/works/mees/dinghy/designsystem/icons/SpoolGlyph.kt"
      to: "app/src/main/java/works/mees/dinghy/designsystem/ColorWheel.kt"
      via: "the rememberHueSweep() cached-Brush template"
      pattern: "remember\\(render\\)"
---

<objective>
Fix the `SpoolGlyph` per-draw `Brush.linearGradient` allocation (CONCERNS P2) using the established `ColorWheel.kt` cached-Brush pattern. `SpoolGlyph` appears on 5 surfaces (launcher tile, drawer tile, mid-print shortcut, SpoolScreen empty-state, SpoolScreen detail); pre-fix, each inside a 4 Hz scope allocates a fresh `Brush` 4×/sec, so this is up to ~20 wasted allocations/sec across the app.

Purpose: D-03 opportunistic P2 allocation fix; runs in parallel with the @Immutable migration (Plan 02) — disjoint files.
Output: A `SpoolGlyph` whose gradient Brush is `remember`ed on the `SpiralRender` key.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/22-performance-architecture-refactor/22-CONTEXT.md
@.planning/phases/22-performance-architecture-refactor/22-RESEARCH.md
@.planning/codebase/CONCERNS.md
@app/src/main/java/works/mees/dinghy/designsystem/icons/SpoolGlyph.kt
@app/src/main/java/works/mees/dinghy/designsystem/ColorWheel.kt
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Cache the SpoolGlyph gradient Brush via remember(render)</name>
  <read_first>
    - app/src/main/java/works/mees/dinghy/designsystem/icons/SpoolGlyph.kt (the Canvas body around line 90 — the inline `Brush.linearGradient(...)` in the SpiralRender.Gradient branch; the already-remembered spiralPath/spiralBounds; the SpiralRender sealed interface incl. data class Gradient(val start: Color, val end: Color))
    - app/src/main/java/works/mees/dinghy/designsystem/ColorWheel.kt (rememberHueSweep() ~line 184 — the canonical `remember { Brush.* }` cached-gradient template)
    - app/src/test/java/works/mees/dinghy/designsystem/icons/SpoolGlyphTest.kt (the existing spiralRenderFor coverage to keep green and extend)
    - .planning/phases/22-performance-architecture-refactor/22-RESEARCH.md (§6 Cached-Brush Pattern — the exact remember(render) shape + fallback)
  </read_first>
  <behavior>
    - When `spiralRenderFor(swatches)` returns the same `SpiralRender.Gradient` (same two Colors) across recompositions, the gradient Brush instance is reused (not reallocated).
    - When the filament color changes (new Gradient with different Colors), a new Brush is built (remember key invalidates).
    - SpiralRender.Solid still maps to SolidColor(render.color); SpiralRender.Empty path is unchanged.
    - The rendered output is visually identical (D-10 reads-the-same) — this is allocation hygiene, not a visual change.
  </behavior>
  <action>
    Hoist the gradient Brush construction out of the inline Canvas draw into `val gradientBrush = remember(render) { if (render is SpiralRender.Gradient) Brush.linearGradient(0f to render.start, 1f to render.end, start = Offset(spiralBounds.left, spiralBounds.center.y), end = Offset(spiralBounds.right, spiralBounds.center.y)) else null }`, then select `val brush = when (render) { is SpiralRender.Solid -> SolidColor(render.color); is SpiralRender.Gradient -> gradientBrush ?: SolidColor(render.start); SpiralRender.Empty -> ... }`. `remember(render)` keys on the SpiralRender data-class instance whose structural equality is its Color fields. Keep the existing `spiralPath`/`spiralBounds` remembers. Do not alter the spiral geometry, the disc/flange/keyline chrome, the evenOdd window holes, or any token-routed color — only the gradient-Brush allocation site changes.
  </action>
  <verify>
    <automated>cmd /c "E:\Android\gw.bat :app:testReleaseUnitTest --tests works.mees.dinghy.designsystem.icons.SpoolGlyphTest --no-daemon"</automated>
  </verify>
  <acceptance_criteria>
    `SpoolGlyphTest` is green (existing spiralRenderFor logic unchanged). `grep -n "remember(render)" app/src/main/java/works/mees/dinghy/designsystem/icons/SpoolGlyph.kt` returns a hit. The `Brush.linearGradient` no longer appears inside the raw `Canvas`/draw lambda (it lives in the `remember(render)` block). Extend SpoolGlyphTest if feasible to assert spiralRenderFor returns an equal Gradient for the same swatches (the equality that makes remember(render) cache) — if the brush instance itself is not host-testable, document that and rely on the equality assertion.
  </acceptance_criteria>
  <done>SpoolGlyph caches its gradient Brush; render logic tests green; visual output unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Pure Compose draw-allocation hygiene in the design system. No auth/network/persistence-crypto/input-validation surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-03 | (none) | SpoolGlyph cached Brush | accept | No new threat — caching a gradient Brush introduces no boundary; the THEME-01 carve-out (filament color is data, not chrome) is preserved verbatim. |
</threat_model>

<verification>
- `SpoolGlyphTest` green; gradient Brush construction is inside `remember(render)`.
</verification>

<success_criteria>
- SpoolGlyph allocates the gradient Brush only when the filament color changes; visual output reads-the-same.
</success_criteria>

<output>
Create `.planning/phases/22-performance-architecture-refactor/22-03-SUMMARY.md` when done.
</output>
