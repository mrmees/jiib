---
phase: 22-performance-architecture-refactor
plan: 06
type: execute
wave: 3
depends_on: ["22-05"]
files_modified:
  - app/src/main/java/works/mees/dinghy/render/GraphView.kt
autonomous: false
requirements: []
must_haves:
  truths:
    - "D-09/SC3: GraphView per-trace gradient-fill overdraw is reduced on the Adreno-320 fill-rate floor — Option A (the leaner default per RESEARCH): secondary traces (index 1+, e.g. tool1/bed) get reduced fill opacity or line-only fills while the primary active heater keeps its area fill; this halves the translucent full-width fill passes for the common 2-3 trace temperature case"
    - "D-08 honored: NO control-glow flattening is attempted (there is no glow drawn — OutlinedControl is a 2px border on transparent fill; *Glow tokens are read by zero draw code); the ONLY overdraw target touched is GraphView's gradient fills (and the kept *Glow tokens are NOT pruned per D-11)"
    - "D-10: the fill-opacity change is owner-approved side-by-side on flox (it could alter a blend/falloff) — the gfxinfo delta is measured on-device; if Option A still misses the zero-frozen-frames gate, Option B (pre-rasterize static setpoint/axis to an off-screen bitmap) is the documented follow-up, measured in isolation (never combined with A in one capture)"
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/render/GraphView.kt"
      provides: "Reduced secondary-trace fill overdraw"
  key_links:
    - from: "app/src/main/java/works/mees/dinghy/render/GraphView.kt"
      to: "the Adreno-320 fill-rate budget"
      via: "fewer translucent full-width fill passes per onDraw"
      pattern: "FILL_ALPHA|drawArea|primaryTrace"
---

<objective>
Genuine overdraw relief (D-09, SC3) on the one place overdraw actually exists: `GraphView`'s per-trace gradient fills (the line-283 comment already flags the Adreno-320 fill-rate cost). Apply Option A (RESEARCH's leaner default): keep the primary active heater's area fill, reduce/drop secondary-trace fills, halving the translucent full-width fill passes for the common multi-trace case. This is a visual blend change, so it is owner-gated side-by-side on flox (D-10).

Purpose: D-09 retargeted overdraw work (NOT control glow — D-08 confirms none is drawn). Depends on Plan 05 (same GraphView.kt; the D-12 guard already cut redraw frequency, so measure that first).
Output: A lower-overdraw GraphView, owner-approved, with the gfxinfo delta recorded.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/22-performance-architecture-refactor/22-CONTEXT.md
@.planning/phases/22-performance-architecture-refactor/22-RESEARCH.md
@.planning/phases/22-performance-architecture-refactor/22-GFXINFO-BASELINE.md
@.planning/codebase/CONCERNS.md
@docs/adr/0001-ui-toolkit-decision.md
@app/src/main/java/works/mees/dinghy/render/GraphView.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reduce GraphView secondary-trace fill overdraw (Option A, D-09)</name>
  <read_first>
    - app/src/main/java/works/mees/dinghy/render/GraphView.kt (line 283 fill-rate comment; the per-trace fill loop 307-326; setpoint path draw 330-340; the FILL_ALPHA constant ~40/255)
    - .planning/phases/22-performance-architecture-refactor/22-RESEARCH.md (§5 — Option A vs Option B tradeoff, the "primary trace keeps area fill, secondary traces line-only" recipe, and the "measure the D-12 guard alone first" note)
    - .planning/phases/22-performance-architecture-refactor/22-GFXINFO-BASELINE.md (the Temperature-graph baseline row from Plan 01 — the before-anchor for this change)
    - docs/ui_design/THEMING.md (so the line color still identifies each trace after fills are reduced — trace identity must survive)
  </read_first>
  <action>
    Apply Option A: in the per-trace fill loop, restrict the gradient area fill to the primary active heater trace (e.g. `if (drawArea && t == primaryTraceIndex)`) and either drop the area fill for secondary traces (index 1+) or reduce their `FILL_ALPHA` markedly (e.g. ~20-25 from ~40). Secondary traces remain identified by their line color (line stroke stays). Do NOT touch OutlinedControl, ProgressRing, or any `*Glow` token (D-08: no glow is drawn; D-11: keep the dead tokens). Do NOT implement Option B (off-screen bitmap) in this task — it is the documented isolated follow-up only if Option A still misses the gate. Keep the setpoint/axis draw and the live trace lines exactly as-is. This is the only overdraw target in scope.
  </action>
  <verify>
    <automated>cmd /c "E:\Android\gw.bat :app:testReleaseUnitTest --no-daemon"</automated>
  </verify>
  <acceptance_criteria>
    Host suite green; `assembleRelease` builds. Only the secondary-trace fill logic in GraphView changed (`git diff` touches the fill loop / FILL_ALPHA only — no OutlinedControl/ProgressRing/*Glow edits). Primary-trace area fill is retained; secondary traces are line-only or reduced-alpha. No Option B bitmap allocation introduced.
  </acceptance_criteria>
  <done>Secondary-trace fill overdraw is reduced; the change is isolated to GraphView's fill path.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Owner side-by-side flox approval + gfxinfo delta on the Temperature graph (D-10/SC3)</name>
  <read_first>
    - .planning/phases/22-performance-architecture-refactor/22-CONTEXT.md (D-10 — any blend/falloff change requires owner side-by-side approval on-device; owner is the gate)
    - .planning/phases/22-performance-architecture-refactor/22-RESEARCH.md (§7 gfxinfo protocol for the Temperature screen + §5 "measure first; layer Option B only if it still misses")
    - .planning/phases/22-performance-architecture-refactor/22-GFXINFO-BASELINE.md (the Plan-01 Temperature baseline to compare against)
  </read_first>
  <action>
    Build + install the RELEASE APK on flox. Surface the live Temperature multi-trace graph to the owner for a side-by-side judgment against the prior fill (D-10 bar: trace identity preserved, reduced secondary fills acceptable); do not adb-drive the UI. Capture `dumpsys gfxinfo works.mees.dinghy framestats` on the Temperature screen (reset → exercise live graph 6-10s → dump → parse_framestats.py) and record the p50/p90/p95 + frozen-frame count vs the Plan-01 Temperature baseline. If frozen frames remain or responsiveness still misses the ADR-0001 Addendum-2 gate, record Option B (pre-rasterize setpoint/axis to an off-screen bitmap) as the isolated follow-up. Close on owner approval; on rejection, revert/retune the alpha.
  </action>
  <what-built>
    Claude builds + installs the RELEASE APK on flox; the owner views the Temperature multi-trace graph during an active heat (or with backfilled history) side-by-side against the recollection/screenshot of the prior fill, and judges it reads-the-same per the D-10 bar (line colors still identify each trace; the reduced secondary fills are acceptable). Claude captures gfxinfo framestats on the Temperature screen, parses with parse_framestats.py, and records the before(Plan-01 baseline)/after delta + frozen-frame count.
  </what-built>
  <how-to-verify>
    1. Release build installed on flox; navigate to Temperature with multiple traces active.
    2. Owner judges the graph against the D-10 bar — secondary-trace fill reduction acceptable, trace identity preserved, no jarring blend change.
    3. Claude runs `dumpsys gfxinfo works.mees.dinghy reset` → exercise the live graph 6-10s → framestats dump → parse_framestats.py; record p50/p90/p95 + frozen-frame count and compare to the Plan-01 Temperature baseline.
    4. If frozen frames remain > 0 or responsiveness still misses the ADR-0001 Addendum-2 gate, record the recommendation to layer Option B (pre-rasterize setpoint/axis to an off-screen bitmap) in a follow-up, measured in isolation.
    5. Owner replies "approved" (or rejects the visual change → revert/retune alpha).
  </how-to-verify>
  <verify>
    <automated>MISSING — perceptual visual + on-device fill-rate gate; owner-approved per D-10, measured via parse_framestats.py (not a host assertion).</automated>
    <human-check>Owner approves the Temperature graph reads-the-same with reduced fills; gfxinfo shows zero frozen frames vs the Plan-01 baseline (or Option B is recorded as the follow-up).</human-check>
  </verify>
  <acceptance_criteria>
    Owner approves the GraphView fill change side-by-side on flox (D-10). The Temperature-screen gfxinfo delta vs the Plan-01 baseline is recorded; zero frozen frames on the graph OR Option B is documented as the isolated follow-up if the gate is still missed.
  </acceptance_criteria>
  <resume-signal>Owner types "approved" or rejects the visual change (revert/retune) or asks for Option B.</resume-signal>
  <done>GraphView overdraw relief is owner-approved on flox with a recorded gfxinfo delta.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Canvas fill-opacity change in the render layer. No auth/network/persistence-crypto/input-validation surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-06 | (none) | GraphView secondary-trace fill reduction | accept | No new threat — reducing translucent fill passes preserves all boundaries; the change is purely a draw-cost reduction, owner-gated for visual acceptability (D-10). |
</threat_model>

<verification>
- Host suite green; the diff is isolated to GraphView's fill path (no glow/OutlinedControl/ProgressRing edits — D-08/D-11 honored).
- Owner approves on flox; gfxinfo Temperature delta recorded.
</verification>

<success_criteria>
- D-09/SC3 satisfied: real overdraw (GraphView fills) reduced, owner-approved, reads-the-same, gfxinfo delta recorded; D-08 (no glow hunt) and D-11 (keep tokens) honored.
</success_criteria>

<output>
Create `.planning/phases/22-performance-architecture-refactor/22-06-SUMMARY.md` when done.
</output>
