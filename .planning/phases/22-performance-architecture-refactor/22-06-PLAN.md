---
phase: 22-performance-architecture-refactor
plan: 06
type: execute
wave: 3
depends_on: ["22-05"]
files_modified:
  - app/src/main/java/works/mees/dinghy/render/GraphView.kt
  - .planning/phases/22-performance-architecture-refactor/22-GFXINFO-GRAPH-POST05.md
autonomous: false
requirements: []
must_haves:
  truths:
    - "REVIEW-MEDIUM (measure after Plan 05 first): Task 0 captures a RELEASE-mode gfxinfo on the Temperature multi-trace graph AFTER Plan 05's D-12 redraw guard landed but BEFORE any fill change — recorded in 22-GFXINFO-GRAPH-POST05.md — because the D-12 guard alone may already cut the graph's redraw frequency enough to clear the gate, in which case the visual fill change may be unnecessary or smaller"
    - "D-09/SC3: GraphView per-trace gradient-fill overdraw is reduced on the Adreno-320 fill-rate floor — Option A (the leaner default per RESEARCH): the PRIMARY trace keeps its area fill; secondary traces (all non-primary) get reduced fill opacity or line-only fills; this halves the translucent full-width fill passes for the common 2-3 trace temperature case"
    - "REVIEW-MEDIUM (deterministic primary trace): 'primary trace' is defined deterministically — the primary is the active/extruder (heater_bed-excluded) heater the screen already emphasizes (e.g. index 0 of the trace list / the currently-selected heater), with an EXPLICIT FALLBACK when the series ordering changes or is empty (if no active heater is resolvable, the first stable-sorted trace by name is primary) so the fill never flickers between traces frame-to-frame"
    - "D-08 honored: NO control-glow flattening is attempted (there is no glow drawn — OutlinedControl is a 2px border on transparent fill; *Glow tokens are read by zero draw code); the ONLY overdraw target touched is GraphView's gradient fills (and the kept *Glow tokens are NOT pruned per D-11)"
    - "REVIEW-LOW (no doc-only SC closure): D-10 — the fill-opacity change is owner-approved side-by-side on flox; the gfxinfo delta is measured on-device. If Option A still misses the zero-frozen-frames gate, Option B (pre-rasterize static setpoint/axis to an off-screen bitmap) is the IMPLEMENTED follow-up (measured in isolation, never combined with A in one capture) — SC1/SC3 are NOT marked satisfied by documenting Option B alone; the gate must be met by a measured change"
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/render/GraphView.kt"
      provides: "Reduced secondary-trace fill overdraw"
    - path: ".planning/phases/22-performance-architecture-refactor/22-GFXINFO-GRAPH-POST05.md"
      provides: "Temperature-graph gfxinfo captured AFTER the Plan-05 D-12 guard, BEFORE the fill change (the attribution anchor for the fill delta)"
  key_links:
    - from: "app/src/main/java/works/mees/dinghy/render/GraphView.kt"
      to: "the Adreno-320 fill-rate budget"
      via: "fewer translucent full-width fill passes per onDraw"
      pattern: "FILL_ALPHA|drawArea|primaryTrace"
---

<objective>
Genuine overdraw relief (D-09, SC3) on the one place overdraw actually exists: `GraphView`'s per-trace gradient fills (the line-283 comment already flags the Adreno-320 fill-rate cost). FIRST measure the graph after Plan 05's D-12 guard (it may already have cut redraws), THEN apply Option A (RESEARCH's leaner default): keep the primary active heater's area fill, reduce/drop secondary-trace fills, halving the translucent full-width fill passes for the common multi-trace case. This is a visual blend change, so it is owner-gated side-by-side on flox (D-10).

Purpose: D-09 retargeted overdraw work (NOT control glow — D-08 confirms none is drawn). Depends on Plan 05 (same GraphView.kt; the D-12 guard already cut redraw frequency, so measure that first).
Output: A post-D-12 graph measurement, then a lower-overdraw GraphView, owner-approved, with the gfxinfo delta recorded.

REVIEW NOTE (Codex MEDIUM/LOW, 22-06): three corrections. (1) Added Task 0 — measure the Temperature graph AFTER Plan 05's guard and BEFORE the fill change (Plan 05 may already reduce redraws; this isolates the fill delta). (2) "Primary trace" now has a deterministic definition + a fallback for changed/empty series ordering. (3) SC1/SC3 are NOT satisfied by documenting Option B alone — if Option A misses the gate, Option B is implemented and measured, not just written down.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/22-performance-architecture-refactor/22-CONTEXT.md
@.planning/phases/22-performance-architecture-refactor/22-RESEARCH.md
@.planning/phases/22-performance-architecture-refactor/22-GFXINFO-BASELINE.md
@.planning/codebase/CONCERNS.md
@docs/adr/0001-ui-toolkit-decision.md
@app/src/main/java/works/mees/dinghy/render/GraphView.kt
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 0: Measure the Temperature graph AFTER the Plan-05 D-12 guard, BEFORE any fill change</name>
  <read_first>
    - .planning/phases/22-performance-architecture-refactor/22-GFXINFO-BASELINE.md (the Plan-01 Temperature baseline — the original before-anchor)
    - .planning/phases/22-performance-architecture-refactor/22-RESEARCH.md (§7 gfxinfo protocol for the Temperature screen + §5 "measure the D-12 guard alone first")
    - docs/adr/0001-ui-toolkit-decision.md (Addendum 2 — zero frozen frames + responsiveness gate)
  </read_first>
  <action>
    Build + install the RELEASE APK with the Plan-05 D-12 guard already merged (this plan depends on 22-05). Create `22-GFXINFO-GRAPH-POST05.md`. While the owner drives the live Temperature multi-trace graph on flox, capture `dumpsys gfxinfo works.mees.dinghy framestats` (reset → exercise 3-6s → dump immediately → parse_framestats.py) as 2-3 short windows and record p50/p90/p95 + frozen-frame count vs the Plan-01 Temperature baseline. If this post-D-12 measurement ALREADY clears the zero-frozen-frames gate, record that explicitly — the Task-1 fill change may then be minimized or skipped (owner decides). This isolates the fill change's contribution from the D-12 guard's. Do not adb-drive the UI.
  </action>
  <what-built>
    Claude builds + installs the RELEASE APK at the post-Plan-05 commit (D-12 guard in, no fill change yet); the owner drives the Temperature graph; Claude captures gfxinfo and records the post-D-12 graph numbers vs the Plan-01 baseline in 22-GFXINFO-GRAPH-POST05.md.
  </what-built>
  <how-to-verify>
    1. Release build (D-12 guard present, no fill change) installed on flox.
    2. Owner drives the live Temperature multi-trace graph; Claude captures 2-3 short gfxinfo windows and parses them.
    3. Record p50/p90/p95 + frozen-frame count vs the Plan-01 Temperature baseline. Note whether the gate is ALREADY met by the D-12 guard alone.
    4. Owner replies "approved — proceed with fill change" (or "gate already met — skip/minimize the fill change").
  </how-to-verify>
  <verify>
    <automated>MISSING — on-device gfxinfo measurement; artifact check automatable: test -f .planning/phases/22-performance-architecture-refactor/22-GFXINFO-GRAPH-POST05.md</automated>
    <human-check>Owner confirms the post-D-12 Temperature-graph numbers are recorded vs baseline; directs whether the fill change proceeds.</human-check>
  </verify>
  <acceptance_criteria>
    `22-GFXINFO-GRAPH-POST05.md` records the Temperature-graph gfxinfo (2-3 windows: p50/p90/p95 + frozen-frame count) at the post-Plan-05 / pre-fill-change commit, vs the Plan-01 baseline. The "is the gate already met by D-12 alone?" question is answered explicitly.
  </acceptance_criteria>
  <resume-signal>Owner types "approved — proceed" or "gate already met — minimize/skip fill change".</resume-signal>
  <done>The fill change's before-anchor is the post-D-12 measurement (not the Plan-01 baseline), isolating its contribution.</done>
</task>

<task type="auto">
  <name>Task 1: Reduce GraphView secondary-trace fill overdraw (Option A, D-09) with a deterministic primary trace</name>
  <read_first>
    - app/src/main/java/works/mees/dinghy/render/GraphView.kt (line 283 fill-rate comment; the per-trace fill loop 307-326; setpoint path draw 330-340; the FILL_ALPHA constant ~40/255; how the trace list is ordered/sourced — to define the deterministic primary)
    - .planning/phases/22-performance-architecture-refactor/22-RESEARCH.md (§5 — Option A vs Option B tradeoff, the "primary trace keeps area fill, secondary traces line-only" recipe, and the deterministic primary-trace selection)
    - .planning/phases/22-performance-architecture-refactor/22-GFXINFO-GRAPH-POST05.md (Task 0's post-D-12 before-anchor for this change)
    - docs/ui_design/THEMING.md (so the line color still identifies each trace after fills are reduced — trace identity must survive)
  </read_first>
  <action>
    Apply Option A. First define the PRIMARY trace deterministically: the primary is the active/emphasized extruder heater the screen already highlights (the currently-selected/active heater, or index 0 of the trace list if that is how the screen orders it). Add an EXPLICIT FALLBACK: if no active heater is resolvable (empty/changed series), pick the first trace by a stable sort (e.g. by name) so the area-filled trace never flickers between traces frame-to-frame as the series reorders. In the per-trace fill loop, restrict the gradient area fill to that primary trace (e.g. `if (drawArea && t == primaryTraceIndex)`) and either drop the area fill for secondary traces or reduce their `FILL_ALPHA` markedly (e.g. ~20-25 from ~40). Secondary traces remain identified by their line color (line stroke stays). Do NOT touch OutlinedControl, ProgressRing, or any `*Glow` token (D-08: no glow is drawn; D-11: keep the dead tokens). Do NOT implement Option B (off-screen bitmap) in this task — it is the isolated follow-up only if Option A still misses the gate (Task 2). Keep the setpoint/axis draw and the live trace lines exactly as-is. This is the only overdraw target in scope.
  </action>
  <verify>
    <automated>cmd /c "E:\Android\gw.bat :app:testReleaseUnitTest --no-daemon"</automated>
  </verify>
  <acceptance_criteria>
    Host suite green; `assembleRelease` builds. Only the secondary-trace fill logic + the deterministic primary-trace selection in GraphView changed (`git diff` touches the fill loop / FILL_ALPHA / a primaryTrace selector only — no OutlinedControl/ProgressRing/*Glow edits). Primary-trace area fill is retained via a deterministic selector with an explicit empty/reorder fallback; secondary traces are line-only or reduced-alpha. No Option B bitmap allocation introduced.
  </acceptance_criteria>
  <done>Secondary-trace fill overdraw is reduced; the primary trace is deterministically chosen; the change is isolated to GraphView's fill path.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Owner side-by-side flox approval + gfxinfo delta on the Temperature graph (D-10/SC3)</name>
  <read_first>
    - .planning/phases/22-performance-architecture-refactor/22-CONTEXT.md (D-10 — any blend/falloff change requires owner side-by-side approval on-device; owner is the gate)
    - .planning/phases/22-performance-architecture-refactor/22-RESEARCH.md (§7 gfxinfo protocol for the Temperature screen + §5 "measure first; layer Option B only if it still misses")
    - .planning/phases/22-performance-architecture-refactor/22-GFXINFO-GRAPH-POST05.md (Task 0's post-D-12 anchor — compare the fill change against THIS, not just the Plan-01 baseline, to attribute the fill delta)
  </read_first>
  <action>
    Build + install the RELEASE APK on flox. Surface the live Temperature multi-trace graph to the owner for a side-by-side judgment against the prior fill (D-10 bar: trace identity preserved, reduced secondary fills acceptable); do not adb-drive the UI. Capture `dumpsys gfxinfo works.mees.dinghy framestats` on the Temperature screen (reset → exercise live graph 3-6s → dump immediately → parse_framestats.py) and record the p50/p90/p95 + frozen-frame count vs BOTH the Task-0 post-D-12 anchor (fill-change attribution) and the Plan-01 baseline (total delta). If frozen frames remain or responsiveness still misses the ADR-0001 Addendum-2 gate, IMPLEMENT Option B (pre-rasterize setpoint/axis to an off-screen bitmap) and re-measure it in isolation — do NOT close SC1/SC3 by merely documenting Option B. Close on owner approval; on rejection, revert/retune the alpha.
  </action>
  <what-built>
    Claude builds + installs the RELEASE APK on flox; the owner views the Temperature multi-trace graph during an active heat (or backfilled history) side-by-side against the prior fill and judges it reads-the-same per the D-10 bar (line colors still identify each trace; reduced secondary fills acceptable). Claude captures gfxinfo, parses with parse_framestats.py, and records the delta vs the Task-0 post-D-12 anchor AND the Plan-01 baseline + frozen-frame count.
  </what-built>
  <how-to-verify>
    1. Release build installed on flox; navigate to Temperature with multiple traces active.
    2. Owner judges the graph against the D-10 bar — secondary-trace fill reduction acceptable, trace identity preserved, no jarring blend change.
    3. Claude runs `dumpsys gfxinfo works.mees.dinghy reset` → exercise the live graph 3-6s → framestats dump immediately → parse_framestats.py; record p50/p90/p95 + frozen-frame count and compare to BOTH the Task-0 post-D-12 anchor and the Plan-01 Temperature baseline.
    4. If frozen frames remain > 0 or responsiveness still misses the ADR-0001 Addendum-2 gate, IMPLEMENT Option B (pre-rasterize setpoint/axis to an off-screen bitmap), measured in isolation, and re-capture — SC1/SC3 close only on a measured pass, never on documentation of Option B alone.
    5. Owner replies "approved" (or rejects the visual change → revert/retune alpha).
  </how-to-verify>
  <verify>
    <automated>MISSING — perceptual visual + on-device fill-rate gate; owner-approved per D-10, measured via parse_framestats.py (not a host assertion).</automated>
    <human-check>Owner approves the Temperature graph reads-the-same with reduced fills; gfxinfo shows zero frozen frames vs the Task-0/Plan-01 anchors (or Option B is implemented + re-measured to a pass — not documented-only).</human-check>
  </verify>
  <acceptance_criteria>
    Owner approves the GraphView fill change side-by-side on flox (D-10). The Temperature-screen gfxinfo delta is recorded vs BOTH the Task-0 post-D-12 anchor and the Plan-01 baseline; zero frozen frames on the graph OR Option B is IMPLEMENTED and re-measured to a pass (SC1/SC3 are not closed by documenting Option B alone).
  </acceptance_criteria>
  <resume-signal>Owner types "approved" or rejects the visual change (revert/retune) or asks for Option B.</resume-signal>
  <done>GraphView overdraw relief is owner-approved on flox with a recorded gfxinfo delta vs the post-D-12 anchor; if needed Option B is implemented + measured, not just documented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Canvas fill-opacity change in the render layer. No auth/network/persistence-crypto/input-validation surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-06 | (none) | GraphView secondary-trace fill reduction | accept | No new threat — reducing translucent fill passes preserves all boundaries; the change is purely a draw-cost reduction, owner-gated for visual acceptability (D-10). |
</threat_model>

<verification>
- Task 0 records the post-D-12 graph measurement BEFORE the fill change (isolating the fill delta).
- Host suite green; the diff is isolated to GraphView's fill path + deterministic primary-trace selection (no glow/OutlinedControl/ProgressRing edits — D-08/D-11 honored).
- Owner approves on flox; gfxinfo Temperature delta recorded vs the post-D-12 anchor; Option B (if needed) implemented + measured, not documented-only.
</verification>

<success_criteria>
- D-09/SC3 satisfied: real overdraw (GraphView fills) reduced after isolating the D-12 contribution, primary trace deterministic, owner-approved, reads-the-same, gfxinfo delta measured (Option B implemented if the gate is still missed); D-08 (no glow hunt) and D-11 (keep tokens) honored.
</success_criteria>

<output>
Create `.planning/phases/22-performance-architecture-refactor/22-06-SUMMARY.md` when done.
</output>
