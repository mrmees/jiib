---
phase: 22-performance-architecture-refactor
plan: 05
type: execute
wave: 2
depends_on: ["22-01"]
files_modified:
  - app/src/main/java/works/mees/dinghy/render/GraphView.kt
  - app/src/main/java/works/mees/dinghy/render/GraphViewHost.kt
  - app/src/main/java/works/mees/dinghy/render/WebcamView.kt
  - app/src/main/java/works/mees/dinghy/render/WebcamViewHost.kt
  - app/src/main/java/works/mees/dinghy/ui/files/FileRowsAdapter.kt
autonomous: false
requirements: []
must_haves:
  truths:
    - "D-12: GraphView and WebcamView cache the last-applied ThemeTokens and short-circuit applyTokens on structural equality (tokens == lastTokens → return) BEFORE invalidate(), so the AndroidView update block no longer forces a full onDraw on every 4 Hz PrinterState emission when tokens are unchanged (ThemeTokens is already an @Immutable data class so == is correct structural equality)"
    - "D-12: WebcamView.setTransform and setChrome are equality-guarded the same way; setFrame(Bitmap?) and GraphView.setData/setSetpoints stay UNCONDITIONAL (a new frame/dataset is genuinely new and must invalidate) — the dark→light theme swap still recolors graph + webcam chrome (the guard short-circuits only on equal tokens, never on a real change)"
    - "SC4/A1: WebcamView is read for registered callbacks (Handler/Runnable/observers surviving onDetachedFromWindow); the three Views surfaces (GraphView, WebcamView, and the Console RecyclerView path) and the MJPEG/H.264/thumbnail decode paths are confirmed leak-free and not GC-churning under sustained use (no callback survives detach; invalidate-driven draw only)"
    - "D-03/P2: WebcamView.measureText results are cached per draw helper (remeasure only when the overlay text string changes); P3 opportunistic — FileRowsAdapter hoists SimpleDateFormat to a companion val (done because the Views-hygiene file set is open)"
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/render/GraphView.kt"
      provides: "ThemeTokens equality-guarded applyTokens"
      contains: "lastTokens"
    - path: "app/src/main/java/works/mees/dinghy/render/WebcamView.kt"
      provides: "Equality-guarded applyTokens/setTransform/setChrome + cached measureText"
  key_links:
    - from: "app/src/main/java/works/mees/dinghy/render/GraphViewHost.kt"
      to: "app/src/main/java/works/mees/dinghy/render/GraphView.kt"
      via: "update block calls applyTokens(tokens) — now internally guarded"
      pattern: "applyTokens"
---

<objective>
AndroidView interop hygiene (D-12, SC4): the `GraphViewHost`/`WebcamViewHost` `update` blocks call `view.applyTokens(tokens)` (→ `invalidate()` → full `onDraw`) unconditionally on every recomposition, forcing graph + webcam redraws on every 250 ms emission even when tokens are unchanged. Cache the last-applied tokens inside the View and short-circuit on structural equality. Confirm the three Views surfaces + decode paths leak-free and not GC-churning (SC4). Land the co-located P2/P3 hygiene fixes (WebcamView.measureText cache, FileRowsAdapter SimpleDateFormat hoist) while these files are open (D-03).

Purpose: D-12 AndroidView guards + SC4 leak/GC confirmation. Independent of the state/split work (disjoint render/ files) — runs parallel to Plan 04.
Output: Equality-guarded View update paths; a leak-free/GC-clean confirmation for SC4.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/22-performance-architecture-refactor/22-CONTEXT.md
@.planning/phases/22-performance-architecture-refactor/22-RESEARCH.md
@.planning/codebase/CONCERNS.md
@app/src/main/java/works/mees/dinghy/render/GraphView.kt
@app/src/main/java/works/mees/dinghy/render/GraphViewHost.kt
@app/src/main/java/works/mees/dinghy/render/WebcamView.kt
@app/src/main/java/works/mees/dinghy/render/WebcamViewHost.kt
@app/src/main/java/works/mees/dinghy/theme/ThemeTokens.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Equality-guard applyTokens/setTransform/setChrome in GraphView + WebcamView (D-12)</name>
  <read_first>
    - app/src/main/java/works/mees/dinghy/render/GraphView.kt (applyTokens body + paint assignments + invalidate; line-283 fill comment)
    - app/src/main/java/works/mees/dinghy/render/GraphViewHost.kt (line 46 single-trace + 89-100 multi-trace update blocks calling applyTokens/setData/setSetpoints)
    - app/src/main/java/works/mees/dinghy/render/WebcamView.kt (applyTokens, setTransform, setChrome, setFrame, and the draw helpers calling measureText: drawCornerBadge ~316, drawCenteredOverlay ~333, drawDeadEndCard ~360, drawCycleOverlay ~385)
    - app/src/main/java/works/mees/dinghy/render/WebcamViewHost.kt (line 61 update block + setTransform/setFrame/setChrome calls)
    - app/src/main/java/works/mees/dinghy/theme/ThemeTokens.kt (confirm it is an @Immutable data class — structural == is correct)
    - .planning/phases/22-performance-architecture-refactor/22-RESEARCH.md (§4 — the canonical guard pattern, the explicit "setFrame/setData stay unconditional" rule, and Pitfall 6)
  </read_first>
  <action>
    In `GraphView`: add `private var lastTokens: ThemeTokens? = null`; at the top of `applyTokens(tokens)` add `if (tokens == lastTokens) return` then `lastTokens = tokens` before the existing paint assignments + `invalidate()`. In `WebcamView`: same `lastTokens` guard on `applyTokens`; add equality guards on `setTransform(flipH, flipV, rotation)` (cache lastFlipH/lastFlipV/lastRot) and `setChrome(mode, camName, serviceName, multiCam)` (cache the last tuple) — return early when unchanged before `invalidate()`. Leave `setFrame(frame: Bitmap?)` UNCONDITIONAL (a new bitmap is genuinely new). Leave `GraphView.setData`/`setSetpoints` UNCONDITIONAL (new data = new frame). Do NOT add guards in the Host update blocks themselves — the guard lives in the View so the structural equality is checked once per call. ThemeTokens is `@Immutable data class` so `==` is correct structural equality (Pitfall 6: a real dark→light swap creates a new tokens instance with different Colors → not equal → recolors correctly).
  </action>
  <verify>
    <automated>cmd /c "E:\Android\gw.bat :app:testReleaseUnitTest --no-daemon"</automated>
  </verify>
  <acceptance_criteria>
    Host suite green. `grep -n "lastTokens" app/src/main/java/works/mees/dinghy/render/GraphView.kt app/src/main/java/works/mees/dinghy/render/WebcamView.kt` shows the guard in both. `applyTokens` returns early on `tokens == lastTokens`. `setFrame` and `setData`/`setSetpoints` remain unconditional (no early-return added). `assembleRelease` builds.
  </acceptance_criteria>
  <done>Token application is equality-guarded in both Views; frame/data application stays unconditional.</done>
</task>

<task type="auto">
  <name>Task 2: Cache WebcamView.measureText + hoist FileRowsAdapter SimpleDateFormat (D-03 opportunistic)</name>
  <read_first>
    - app/src/main/java/works/mees/dinghy/render/WebcamView.kt (the four draw helpers that call paint.measureText on each frame — drawCornerBadge/drawCenteredOverlay/drawDeadEndCard/drawCycleOverlay)
    - app/src/main/java/works/mees/dinghy/ui/files/FileRowsAdapter.kt (line ~259 — the per-bind `SimpleDateFormat("MMM d, HH:mm", Locale.US)` in onBindViewHolder)
    - .planning/phases/22-performance-architecture-refactor/22-RESEARCH.md (§8 — the measureText cache pair pattern + the FileRowsAdapter companion-val note incl. the thread-safety caveat)
    - .planning/codebase/CONCERNS.md (P2 WebcamView.measureText, P3 FileRowsAdapter SimpleDateFormat)
  </read_first>
  <action>
    In each WebcamView draw helper that calls `measureText`, cache a `(lastText, lastWidth)` pair per helper and only remeasure when the overlay text changes (these strings update infrequently). In `FileRowsAdapter`, hoist the formatter to `companion object { private val DATE_FORMAT = SimpleDateFormat("MMM d, HH:mm", Locale.US) }` and use it in `onBindViewHolder` (binding is on the main thread, so the single shared instance is safe — `SimpleDateFormat` is not thread-safe; keep it main-thread-only, do not move binding off-thread). These are opportunistic P2/P3 fixes done only because the render/Views file set is already open (D-03). No behavior change.
  </action>
  <verify>
    <automated>cmd /c "E:\Android\gw.bat :app:testReleaseUnitTest --no-daemon"</automated>
  </verify>
  <acceptance_criteria>
    Host suite green. WebcamView draw helpers cache measured text width (remeasure only on text change). `grep -n "companion object" app/src/main/java/works/mees/dinghy/ui/files/FileRowsAdapter.kt` shows a hoisted `DATE_FORMAT`; no `SimpleDateFormat(` remains inside `onBindViewHolder`. Date formatting output unchanged.
  </acceptance_criteria>
  <done>measureText is cached per overlay; SimpleDateFormat is allocated once.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: SC4 leak-free / GC-clean confirmation on flox (three Views + decode paths)</name>
  <read_first>
    - app/src/main/java/works/mees/dinghy/render/WebcamView.kt (Assumption A1 — read fully for any registered Handler/Runnable/observer that must be cleaned up in onDetachedFromWindow; confirm draw is invalidate-driven only)
    - .planning/phases/22-performance-architecture-refactor/22-RESEARCH.md (§4 "Confirming leak-free (no LeakCanary)" — the DisposableEffect holder-cancel pattern + the manual confirmation steps)
    - .planning/phases/22-performance-architecture-refactor/22-VALIDATION.md (the SC4 manual-only row: sustained on-device session, watch logcat GC churn / memory, confirm guards short-circuit)
  </read_first>
  <action>
    Read WebcamView and GraphView fully and record (in the SUMMARY) whether either registers a Handler/Runnable/observer that survives onDetachedFromWindow — resolving Assumption A1; if any is found, add the matching cleanup in onDetachedFromWindow. Build + install the RELEASE APK on flox. While the owner enters/exits the temp-graph, Console, and webcam surfaces repeatedly, sample `adb shell dumpsys meminfo works.mees.dinghy` TOTAL PSS before/after the cycles and watch `adb logcat` for GC churn; have the owner toggle dark↔light on the graph + webcam to confirm chrome still recolors. Do not adb-drive the UI. Record the PSS trend + A1 resolution; close on owner approval.
  </action>
  <what-built>
    Claude reads WebcamView for registered callbacks (resolving Assumption A1) and records the finding; builds + installs on flox; the owner runs a sustained session entering/exiting the temp-graph, Console, and webcam surfaces repeatedly. Claude watches `adb logcat` for GC churn and tracks `adb shell dumpsys meminfo works.mees.dinghy` TOTAL PSS across enter/exit cycles to confirm flat memory (leak-free) and confirms the dark→light theme swap still recolors graph + webcam chrome (the guard didn't break theming). Claude does NOT adb-drive the UI; the owner navigates.
  </what-built>
  <how-to-verify>
    1. Confirm in code that WebcamView and GraphView register no callback surviving onDetachedFromWindow (invalidate-driven draw only); record A1's resolution in the SUMMARY.
    2. Owner enters/exits Temperature (graph), Console (RecyclerView scrollback), and Webcam ~5x each on flox.
    3. Claude samples `dumpsys meminfo works.mees.dinghy` TOTAL PSS before and after the cycles — confirm it is flat (no monotonic climb = leak-free), like the Phase-21 SC4 method.
    4. Owner toggles dark↔light while on the graph and on the webcam — chrome must still recolor (proves the equality guard short-circuits only on equal tokens).
    5. Owner replies "approved" or reports a leak/GC/theming regression.
  </how-to-verify>
  <verify>
    <automated>MISSING — sustained-use leak/GC behavior is on-device; gated by owner approval + meminfo PSS trend. Code-side A1 resolution is reviewable in the SUMMARY.</automated>
    <human-check>Owner confirms flat memory across enter/exit cycles, no GC churn, and theme recolor still works on graph + webcam; replies "approved".</human-check>
  </verify>
  <acceptance_criteria>
    A1 resolved in the SUMMARY (WebcamView/GraphView register no leaking callback). TOTAL PSS is flat across enter/exit cycles on flox (leak-free, SC4). dark↔light still recolors graph + webcam chrome (guard correct, Pitfall 6). Owner approves.
  </acceptance_criteria>
  <resume-signal>Owner types "approved" or reports a leak / GC churn / lost theme recolor.</resume-signal>
  <done>The three Views surfaces + decode paths are confirmed leak-free and GC-clean on flox; SC4 satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | View-side draw-guard + measureText cache + date-format hoist. No auth/network/persistence-crypto/input-validation surface; the MJPEG/H.264 decode trust boundary (untrusted camera bytes) is unchanged — frame application stays unconditional and is not altered. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-05 | (none) | AndroidView update-block guards + SC4 confirmation | accept | No new threat — equality-guarding token application preserves all boundaries; setFrame (the decode-bytes path) is deliberately left unconditional so no frame is dropped or stale. |
</threat_model>

<verification>
- Host suite green; `lastTokens` guard present in both Views; setFrame/setData unconditional.
- Owner approves SC4: flat PSS, no GC churn, theme recolor intact on flox.
</verification>

<success_criteria>
- D-12 guards landed; SC4 leak-free/GC-clean confirmed on-device; measureText + SimpleDateFormat hygiene done.
</success_criteria>

<output>
Create `.planning/phases/22-performance-architecture-refactor/22-05-SUMMARY.md` when done.
</output>
