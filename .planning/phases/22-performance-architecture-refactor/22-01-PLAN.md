---
phase: 22-performance-architecture-refactor
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - gradle/libs.versions.toml
  - app/build.gradle.kts
  - app/src/main/java/works/mees/dinghy/di/AppContainer.kt
  - .planning/phases/22-performance-architecture-refactor/22-GFXINFO-BASELINE.md
autonomous: false
requirements: []
must_haves:
  truths:
    - "D-05/D-06: a RELEASE-mode gfxinfo framestats baseline is captured on flox across the D-06 sweep set (PrintStatus steady-state idle + mid-print, App Drawer open, Files scroll, Console, Temperature graph, Webcam, a scrubber/control screen) BEFORE any code change, recorded with parse_framestats.py output (the SC1 before/after anchor — cannot be reconstructed later)"
    - "D-02: kotlinx-collections-immutable 0.3.8 is added to libs.versions.toml [versions] + [libraries] and wired into app/build.gradle.kts dependencies (NOT 0.5.0-beta01 which needs Kotlin 2.3.0; this project pins Kotlin 2.1.21), and assembleRelease resolves it"
    - "A3/SC2: di/AppContainer.kt is read and annotated @Stable if it lacked the annotation, so screens receiving it as a composable parameter can skip"
    - "D-04 (non-goal guardrail): the 6 DataStores are left exactly as-is — DataStore consolidation is a cold-start concern, NOT nav-lag, and is explicitly OUT of this phase. No executor should touch store wiring."
    - "D-07 (non-goal guardrail): the Macrobenchmark module stays PARKED — do NOT wire the pending macrobenchmark-module-wiring todo this phase. Manual gfxinfo (above) is the only measurement gate; FrameTimingMetric may not work on API 23 and Baseline Profile is a no-op on the API-23 floor."
  artifacts:
    - path: ".planning/phases/22-performance-architecture-refactor/22-GFXINFO-BASELINE.md"
      provides: "Release-mode gfxinfo baseline (p50/p90/p95 + frozen-frame count per sweep screen) on flox"
    - path: "gradle/libs.versions.toml"
      provides: "kotlinxCollectionsImmutable=0.3.8 version + kotlinx-collections-immutable library coordinate"
    - path: "app/build.gradle.kts"
      provides: "implementation(libs.kotlinx.collections.immutable)"
  key_links:
    - from: "app/build.gradle.kts"
      to: "gradle/libs.versions.toml"
      via: "libs.kotlinx.collections.immutable version-catalog accessor"
      pattern: "kotlinx.collections.immutable"
---

<objective>
Wave-0 setup that gates every code wave: capture the release-mode gfxinfo baseline on flox (the SC1 before/after anchor that cannot be reconstructed after code changes), add the `kotlinx-collections-immutable` dependency Wave 1 compiles against, and read/annotate `AppContainer` `@Stable` so the push-down (Plan 07) actually lets screens skip.

Purpose: D-05/D-06 require baseline-first measurement; D-02 requires the immutable-collections library before any `ImmutableMap`/`ImmutableList` code compiles; A3 (research open question) requires confirming `AppContainer` stability.
Output: `22-GFXINFO-BASELINE.md`, an updated version catalog + module dependency, and a `@Stable`-annotated `AppContainer` (if it was absent).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-performance-architecture-refactor/22-CONTEXT.md
@.planning/phases/22-performance-architecture-refactor/22-RESEARCH.md
@.planning/phases/22-performance-architecture-refactor/22-VALIDATION.md
@docs/adr/0001-ui-toolkit-decision.md
@tools/gfxinfo-parser/parse_framestats.py
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Capture the release-mode gfxinfo baseline on flox (D-05/D-06)</name>
  <read_first>
    - .planning/phases/22-performance-architecture-refactor/22-RESEARCH.md (§7 gfxinfo Framestats Measurement Protocol — the exact per-screen capture sequence)
    - tools/gfxinfo-parser/parse_framestats.py (the parser + its header comments on ring-buffer dedup / API-23 support)
    - docs/adr/0001-ui-toolkit-decision.md (Addendum 2 — the reframed gate: zero frozen frames + responsiveness, NOT a millisecond FPS target)
  </read_first>
  <what-built>
    Claude builds + installs the RELEASE APK (`cmd /c "E:\Android\gw.bat :app:assembleRelease --no-daemon"`, then zipalign+debug-sign via `E:\Android\sign-release.bat`, then `adb install -r`), creates `22-GFXINFO-BASELINE.md` with one section per D-06 sweep screen, and prepares the exact adb capture commands. The OWNER drives flox through each screen; Claude runs the gfxinfo reset → exercise → `dumpsys gfxinfo works.mees.dinghy framestats` → `parse_framestats.py` sequence and records p50/p90/p95 + the count of frames >700ms (frozen) per screen into the doc.
  </what-built>
  <how-to-verify>
    1. Confirm the installed build is RELEASE (R8-minified, debug-signed) — not assembleDebug (debug Compose lies 5-10x about jank).
    2. Enable Developer Options → Profile GPU rendering → "In adb shell dumpsys gfxinfo" on flox.
    3. For EACH D-06 sweep screen — PrintStatus steady-state (idle), PrintStatus mid-print, App Drawer open/close, Files scroll, Console (live gcode responses), Temperature multi-trace graph, Webcam, and a scrubber/control screen (Move) — navigate to it, stabilize 3s, `adb shell dumpsys gfxinfo works.mees.dinghy reset`, exercise 6-10s, dump framestats, run through `parse_framestats.py`.
    4. Confirm every sweep screen has a recorded row in `22-GFXINFO-BASELINE.md` (p50/p90/p95 + frozen-frame count). Owner replies "approved" or notes a missing screen.
  </how-to-verify>
  <action>
    Build and install the RELEASE APK on flox per the CLAUDE.md build-env helpers. Create `22-GFXINFO-BASELINE.md` with a heading per D-06 sweep screen (PrintStatus-idle, PrintStatus-printing, AppDrawer, Files, Console, Temperature, Webcam, Move). For each, follow the RESEARCH §7 protocol: `adb shell dumpsys gfxinfo works.mees.dinghy reset`, exercise the screen, `adb shell dumpsys gfxinfo works.mees.dinghy framestats > capture_<screen>.txt`, then `python3 tools/gfxinfo-parser/parse_framestats.py capture_<screen>.txt` and paste p50/p90/p95 + the frozen-frame (>700ms) count. This is the ONLY before-anchor; capture it before Wave 1 touches any code. Owner drives flox navigation; do NOT attempt to adb-drive the app UI.
  </action>
  <verify>
    <automated>MISSING — perceptual on-device baseline; gated by owner approval. The artifact existence check is automatable: test -f .planning/phases/22-performance-architecture-refactor/22-GFXINFO-BASELINE.md && grep -qi "frozen" .planning/phases/22-performance-architecture-refactor/22-GFXINFO-BASELINE.md</automated>
    <human-check>Owner confirms every D-06 sweep screen has a recorded baseline row; replies "approved".</human-check>
  </verify>
  <acceptance_criteria>
    `22-GFXINFO-BASELINE.md` exists and contains a baseline row (p50/p90/p95 + frozen-frame count from `parse_framestats.py`) for all 8 D-06 sweep screens, captured against the RELEASE build on flox. Owner approves.
  </acceptance_criteria>
  <resume-signal>Owner types "approved" or names a sweep screen still missing a baseline.</resume-signal>
  <done>All 8 D-06 sweep screens have a recorded release-mode gfxinfo baseline on flox; owner approved.</done>
</task>

<task type="auto">
  <name>Task 2: Add kotlinx-collections-immutable 0.3.8 to the version catalog + module (D-02)</name>
  <read_first>
    - gradle/libs.versions.toml (the [versions] and [libraries] tables — confirm the existing kotlinx.* entries' formatting, e.g. kotlinx-serialization-json)
    - app/build.gradle.kts (the dependencies block — match the existing `implementation(libs.kotlinx.*)` style)
    - .planning/phases/22-performance-architecture-refactor/22-RESEARCH.md (§Standard Stack — exact coordinates + the 0.5.0-beta01 incompatibility rationale)
  </read_first>
  <action>
    In `gradle/libs.versions.toml` add to `[versions]`: `kotlinxCollectionsImmutable = "0.3.8"`. Add to `[libraries]`: `kotlinx-collections-immutable = { group = "org.jetbrains.kotlinx", name = "kotlinx-collections-immutable", version.ref = "kotlinxCollectionsImmutable" }`. In `app/build.gradle.kts` add `implementation(libs.kotlinx.collections.immutable)` to the dependencies block. Pin EXACTLY 0.3.8 (0.4.0 also works; 0.5.0-beta01 requires Kotlin 2.3.0 and is INCOMPATIBLE with this project's pinned Kotlin 2.1.21). No R8 keep rules are needed — the library has no reflective surface.
  </action>
  <verify>
    <automated>cmd /c "E:\Android\gw.bat :app:assembleRelease --no-daemon"</automated>
  </verify>
  <acceptance_criteria>
    `assembleRelease` resolves the new dependency and the build succeeds. `libs.versions.toml` contains `kotlinxCollectionsImmutable = "0.3.8"` and the `kotlinx-collections-immutable` library entry; `app/build.gradle.kts` references `libs.kotlinx.collections.immutable`. Version is 0.3.8, not 0.5.0-beta01.
  </acceptance_criteria>
  <done>The immutable-collections library resolves in a release build; Wave 1 can compile `ImmutableMap`/`ImmutableList` code.</done>
</task>

<task type="auto">
  <name>Task 3: Read AppContainer and annotate @Stable if absent (A3/SC2)</name>
  <read_first>
    - app/src/main/java/works/mees/dinghy/di/AppContainer.kt (the class declaration — confirmed a plain `class AppContainer(` with no `@Stable` at the time of planning)
    - .planning/codebase/ARCHITECTURE.md (the AppContainer / flow-ownership role; "process-scoped service-locator, no DI framework")
    - .planning/phases/22-performance-architecture-refactor/22-RESEARCH.md (Open Question 1 + Assumption A3 — why an unstable AppContainer parameter defeats screen skipping)
  </read_first>
  <action>
    Read `di/AppContainer.kt`. It is a process-scoped service-locator passed to screen composables as a parameter; for those screens to skip on 4 Hz state emission (after the Plan 07 push-down), `AppContainer` must be a stable parameter type. Add `import androidx.compose.runtime.Stable` and annotate the class `@Stable` (the instance is a single process-lifetime singleton whose identity never changes — the `@Stable` contract holds). Do NOT change any of its members or flow wiring; this is an annotation-only change. If, on reading, it is already `@Stable`, record that fact in the SUMMARY and make no edit.
  </action>
  <verify>
    <automated>cmd /c "E:\Android\gw.bat :app:testReleaseUnitTest --no-daemon"</automated>
  </verify>
  <acceptance_criteria>
    `AppContainer` carries `@Stable` (or the SUMMARY records it already did). The host unit suite is green (annotation-only change introduces no behavior change). grep confirms: `grep -n "@Stable" app/src/main/java/works/mees/dinghy/di/AppContainer.kt` returns a hit on the class.
  </acceptance_criteria>
  <done>AppContainer is a stable composable parameter, unblocking the Plan 07 push-down's skip benefit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This phase introduces no new trust boundary. The one new dependency (`kotlinx-collections-immutable`) has no network, crypto, persistence, or I/O surface; gfxinfo capture is a local adb developer operation on the owner's own device. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-01 | (none) | Compose stability refactor + measurement setup | accept | Refactor preserves all existing trust boundaries; no auth/network/persistence-crypto/input-validation surface is touched. No new threats introduced. |
| T-22-SC | Tampering | gradle install of `kotlinx-collections-immutable` | mitigate | Package Legitimacy Audit in 22-RESEARCH.md verdict = Approved (official JetBrains library, `org.jetbrains.kotlinx`, 5+ yr Maven Central history, public GitHub source). No `[ASSUMED]`/`[SUS]`/`[SLOP]` packages — no blocking install checkpoint required. |
</threat_model>

<verification>
- `assembleRelease` resolves `kotlinx-collections-immutable:0.3.8` and builds.
- `testReleaseUnitTest` green after the `@Stable` annotation.
- `22-GFXINFO-BASELINE.md` has all 8 sweep-screen baseline rows, owner-approved.
</verification>

<success_criteria>
- Release-mode gfxinfo baseline captured on flox across the D-06 sweep set, owner-approved (the SC1 anchor).
- `kotlinx-collections-immutable = 0.3.8` resolves in a release build.
- `AppContainer` is `@Stable`.
</success_criteria>

<output>
Create `.planning/phases/22-performance-architecture-refactor/22-01-SUMMARY.md` when done.
</output>
