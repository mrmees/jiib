---
phase: 22-performance-architecture-refactor
plan: 04
type: execute
wave: 2
depends_on: ["22-02", "22-03"]
files_modified:
  - app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusScreen.kt
  - app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusFocus.kt
  - app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusField.kt
  - app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusGutter.kt
  - app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusPreviews.kt
autonomous: false
requirements: []
must_haves:
  truths:
    - "D-01 move #1: PrintStatusScreen.kt (1584-line god-component) is split at natural seams into PrintStatusFocus / PrintStatusField / PrintStatusGutter / PrintStatusPreviews — each a non-inline top-level @Composable fun (its own independently-restartable recomposition scope), with PrintStatusScreen.kt retained as the orchestration + state-collection shell"
    - "D-02 P0 lambda-slot fix: the two inline @Composable lambda slots passed to ScreenScaffold (gutterContent ~line 490, activeFieldContent ~line 522) become named top-level @Composable fun (PrintStatusGutter / the field content) so ScreenScaffold receives stable references and can skip"
    - "P1: BoxWithConstraints is removed from IconTwoRowCell (~857) and IconValueCell (~888) — icon size is passed as an explicit Dp derived from the grid column (the ScreenScaffold root BoxWithConstraints stays; no new per-cell subcomposition)"
    - "P1/P2: spoolSwatches becomes ImmutableList<Color> via remember(spoolDetail, metadata); ImageRequest.Builder is wrapped in remember(thumbnailUrl, context); LauncherGrid uses LazyVerticalGrid(GridCells.Fixed) with key={it.id}; fmtDuration is hoisted to a pure host-testable function (and/or derivedStateOf keyed on the integer duration) — opportunistic P2/P3 done because the file is already open (D-03)"
    - "D-10/Pitfall-4: the existing 6-theme-combo x portrait/landscape @Preview matrix moves to PrintStatusPreviews.kt and every slice still renders it without visual regression; fsSp(baseSp, t.fs) font scale is preserved through every extraction (no raw .sp introduced)"
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusFocus.kt"
      provides: "Independently-restartable Focus-region composable"
    - path: "app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusField.kt"
      provides: "Independently-restartable Field-region composable"
    - path: "app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusGutter.kt"
      provides: "Named gutter composable replacing the inline lambda slot"
    - path: "app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusPreviews.kt"
      provides: "The 6-combo x portrait/landscape @Preview matrix"
  key_links:
    - from: "app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusScreen.kt"
      to: "app/src/main/java/works/mees/dinghy/designsystem/layout/ScreenScaffold.kt"
      via: "named @Composable slot references (not inline lambdas)"
      pattern: "PrintStatusGutter|PrintStatusFocus|PrintStatusField"
---

<objective>
The first big D-01 restructure: split the 1584-line `PrintStatusScreen.kt` god-component into independently-restartable scopes (`PrintStatusFocus` / `PrintStatusField` / `PrintStatusGutter` / `PrintStatusPreviews`), which simultaneously fixes the P0 inline-lambda-slot problem (named composables are stable references `ScreenScaffold` can skip). While the file is open, land the co-located P1/P2 hot-path fixes the audit flagged on this exact file (BoxWithConstraints cells, spoolSwatches ImmutableList, ImageRequest remember, LauncherGrid → LazyVerticalGrid, fmtDuration hoist) per D-03 "opportunistic when the file is already open".

Purpose: D-01 (god-component split → restartable scopes) + D-02 (lambda slots) + D-03 (opportunistic P1/P2 on this file). Depends on Plan 02 (new immutable field types this screen reads) and Plan 03 (SpoolGlyph signature).
Output: A four-file PrintStatus split with the @Preview matrix preserved and visual behavior unchanged.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/22-performance-architecture-refactor/22-CONTEXT.md
@.planning/phases/22-performance-architecture-refactor/22-RESEARCH.md
@.planning/codebase/CONCERNS.md
@docs/ui_design/LAYOUT.md
@app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusScreen.kt
@app/src/main/java/works/mees/dinghy/designsystem/layout/ScreenScaffold.kt
@app/src/main/java/works/mees/dinghy/designsystem/icons/SpoolGlyph.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Split PrintStatusScreen into Focus/Field/Gutter/Previews + fix the lambda slots</name>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusScreen.kt (the whole file: PrintStatusScreen entry that collects state ~line 135; PrintStatusContent; gutterContent inline lambda ~490; activeFieldContent inline lambda ~522; PrintStatusFocus root ~640; the four-state PrintStatusMode layout; the existing @Preview functions)
    - app/src/main/java/works/mees/dinghy/designsystem/layout/ScreenScaffold.kt (the focus/field/gutter @Composable () -> Unit slot contract ~line 64)
    - docs/ui_design/LAYOUT.md (Focus/Field/Gutter grammar — the contract the split must preserve in both orientations)
    - .planning/phases/22-performance-architecture-refactor/22-RESEARCH.md (§1 — restartability mechanics, the inline-function pitfall, "pass only what each slice needs", and Pitfall 4 preview-regression guard)
  </read_first>
  <action>
    Create three new files — `PrintStatusFocus.kt`, `PrintStatusField.kt`, `PrintStatusGutter.kt` — each containing a non-inline top-level `@Composable fun` for that region, receiving ONLY the resolved values it reads (stable primitives, enums, @Immutable types, ImmutableMap/ImmutableList from Plan 02 — not the full PrinterState if a slice reads only a few fields). Replace the two inline `@Composable () -> Unit` slots passed to ScreenScaffold (gutterContent ~490, activeFieldContent ~522) with calls to these named composables (named references are stable; ScreenScaffold can then skip). Move ALL `@Preview` declarations (the 6-combo x portrait/landscape matrix + any PreviewBox helpers used only by previews) into `PrintStatusPreviews.kt`. Keep `PrintStatusScreen.kt` as the orchestration shell that does `container.printerState.collectAsStateWithLifecycle(...)`, resolves PrintStatusMode, and wires the slices into ScreenScaffold. Preserve `fsSp(baseSp, t.fs)` everywhere — introduce NO raw `.sp`. Do not change layout, the four-state model, accent-temperature identity, or any visible behavior — this is a structural split (D-10 reads-the-same). Split incrementally and rebuild after each slice (Pitfall 4: do not batch all splits before checking previews).
  </action>
  <verify>
    <automated>cmd /c "E:\Android\gw.bat :app:assembleDebug --no-daemon"</automated>
  </verify>
  <acceptance_criteria>
    `PrintStatusFocus.kt`, `PrintStatusField.kt`, `PrintStatusGutter.kt`, `PrintStatusPreviews.kt` exist, each with a non-inline top-level `@Composable fun`. The two ScreenScaffold slots are named composable references, not inline `{ ... }` lambdas (`grep -n "gutterContent.*=.*{" app/.../PrintStatusScreen.kt` returns no inline-lambda slot definition). `assembleDebug` builds (debug needed for @Preview). No raw `.sp` introduced (`grep -rn "[0-9]\.sp\|[0-9]sp" app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatus*.kt` shows only fsSp usages, no bare `.sp` literals outside fsSp). The @Preview matrix lives in PrintStatusPreviews.kt.
  </acceptance_criteria>
  <done>The god-component is four restartable files; lambda slots are named composables; previews preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Land the co-located P1/P2 hot-path fixes on the split files (D-03 opportunistic)</name>
  <read_first>
    - .planning/codebase/CONCERNS.md (P1: BoxWithConstraints cells ~857/~888, spoolSwatches ~187; P2: ImageRequest ~680, LauncherGrid ~1073, fmtDuration ~1537)
    - .planning/phases/22-performance-architecture-refactor/22-RESEARCH.md (§8 — exact remember/ImmutableList/LazyVerticalGrid shapes + the spoolSwatches remember key)
    - app/src/main/java/works/mees/dinghy/designsystem/ColorWheel.kt (and the deleteAllowed/pure-helper pattern) — the established "pull logic into a pure host-tested function" template for fmtDuration
    - the just-created PrintStatusFocus.kt / PrintStatusField.kt (where these cells/grids/images now live after Task 1)
  </read_first>
  <action>
    On the split files: (1) Remove `BoxWithConstraints` from `IconTwoRowCell` and `IconValueCell` — add an `iconSize: Dp` parameter the caller derives from the grid column width; size the icon via `Modifier.size(iconSize)`. (2) Make `spoolSwatches` an `ImmutableList<Color>` computed in `remember(spoolDetail, metadata) { ... }` (per RESEARCH §8: Spoolman colorSwatches → else gcode filamentColors[0] → else empty, all `.toImmutableList()`/`persistentListOf()`); propagate `ImmutableList<Color>` as the param type into the SpoolGlyph call. (3) Wrap the `ImageRequest.Builder(context).data(thumbnailUrl).build()` in `remember(thumbnailUrl, context) { ... }`. (4) Replace `LauncherGrid`'s `chunked(columns).forEach { Row { ... } }` with `LazyVerticalGrid(columns = GridCells.Fixed(columns))` + `items(tiles, key = { it.id })`. (5) Hoist `fmtDuration` to a pure top-level function (host-testable, mirroring the deleteAllowed pattern) and either call it inside `derivedStateOf { fmtDuration(durationSeconds.toInt()) }` keyed on the integer seconds or compute-once. Do NOT change any visible output (D-10). These are the P3-adjacent opportunistic fixes permitted only because this file is open (D-03).
  </action>
  <verify>
    <automated>cmd /c "E:\Android\gw.bat :app:testReleaseUnitTest --no-daemon"</automated>
  </verify>
  <acceptance_criteria>
    Host unit suite green (including any new fmtDuration test). `grep -n "BoxWithConstraints" app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatus*.kt` shows it removed from the icon cells. spoolSwatches is `ImmutableList<Color>` inside a `remember(...)`. ImageRequest is inside `remember(thumbnailUrl`. LauncherGrid uses `LazyVerticalGrid`. fmtDuration is a top-level pure function. `assembleDebug` builds for the @Preview matrix.
  </acceptance_criteria>
  <done>The co-located P1/P2 allocations on the home screen are fixed; behavior unchanged; tests green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: On-device flox preview-parity + layout check of the split (D-01/D-10)</name>
  <read_first>
    - .planning/phases/22-performance-architecture-refactor/22-CONTEXT.md (D-10 visual bar = reads-the-same on flox; Claude's Discretion note that any ScreenScaffold/layout-touching change requires the full @Preview matrix + on-device flox check)
    - .planning/codebase/CONCERNS.md (Fragile Areas — ScreenScaffold/BoxWithConstraints affects ALL screens)
  </read_first>
  <action>
    Build the APK and install on flox (`installDebug` or signed release per CLAUDE.md). Surface the running build to the owner for a side-by-side parity check of the Print Status home against the pre-split version, in both orientations across the four print states; do not adb-drive the UI. Record the owner's verdict in the SUMMARY; if a visual diff is reported, fix it before closing.
  </action>
  <what-built>
    Claude builds + installs the APK on flox; the owner verifies the Print Status home renders identically to before the split in BOTH orientations across the four print states (Standby / Printing / Paused / Terminal) — the split is structural and must read-the-same (D-10). Claude does NOT adb-drive the UI; the owner navigates/eyeballs.
  </what-built>
  <how-to-verify>
    1. Build + install (`installDebug` or release per CLAUDE.md) on flox.
    2. Owner opens Print Status in portrait and landscape and confirms: Focus/Field/Gutter layout, stat grid cells, launcher/shortcut tiles, spool glyph, thumbnail, and duration text all render as before — no clipping, no relayout, no missing element.
    3. Owner exercises the four print states if reachable (idle, mid-print, paused, complete/error) and confirms each reads-the-same.
    4. Owner replies "approved" or describes the visual diff.
  </how-to-verify>
  <verify>
    <automated>MISSING — perceptual on-device parity; gated by owner approval (the @Preview matrix is the host-side net; the on-device flox check is the D-10 authority).</automated>
    <human-check>Owner confirms Print Status reads-the-same in both orientations across all four states; replies "approved".</human-check>
  </verify>
  <acceptance_criteria>
    Owner approves that the split Print Status home is visually identical to pre-split in portrait and landscape across the four print states. No layout regression on flox.
  </acceptance_criteria>
  <resume-signal>Owner types "approved" or describes the visual difference to fix.</resume-signal>
  <done>The god-component split is on-device-confirmed visually unchanged on flox.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | UI-layer composable split + allocation hygiene. No auth/network/persistence-crypto/input-validation surface; the screen renders the same trusted state it already received. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-04 | (none) | PrintStatusScreen split + co-located P1/P2 fixes | accept | No new threat — splitting a composable into restartable scopes and caching allocations preserves all existing trust boundaries; visual behavior is owner-verified unchanged (D-10). |
</threat_model>

<verification>
- `assembleDebug` builds (preview matrix); `testReleaseUnitTest` green (incl. fmtDuration).
- Owner approves on-device flox parity in both orientations across the four states.
</verification>

<success_criteria>
- D-01 move #1 complete: four restartable PrintStatus files, lambda slots named, P1/P2 co-located fixes landed, @Preview matrix preserved, on-device reads-the-same.
</success_criteria>

<output>
Create `.planning/phases/22-performance-architecture-refactor/22-04-SUMMARY.md` when done.
</output>
