---
phase: 22-performance-architecture-refactor
plan: 04
type: execute
wave: 2
depends_on: ["22-02", "22-03"]
files_modified:
  - app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusScreen.kt
  - app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusFocus.kt
  - app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusField.kt
  - app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusGutter.kt
  - app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusPreviews.kt
autonomous: false
requirements: []
must_haves:
  truths:
    - "D-01 move #1: PrintStatusScreen.kt (1584-line god-component) is split at natural seams into PrintStatusFocus / PrintStatusField / PrintStatusGutter / PrintStatusPreviews — each a non-inline top-level @Composable fun (its own independently-restartable recomposition scope), with PrintStatusScreen.kt retained as the orchestration + state-collection shell"
    - "REVIEW-HIGH (correct lambda-slot criterion): the two ScreenScaffold slots (gutterContent ~490, activeFieldContent ~522) still RECEIVE lambdas (slot params are `@Composable () -> Unit` — values are captured, so a `{ ... }` body is unavoidable and correct); the real fix is that each slot lambda body contains ONLY a call to a named top-level @Composable (PrintStatusGutter(...) / the named field composable) with stable/minimal params, creating a restartable child scope — NOT 'no inline lambdas'"
    - "REVIEW-HIGH (per-slice verifiability): the split is performed incrementally and a Windows-side build (`E:\\Android\\gw.bat :app:assembleDebug`) is run after EACH extracted child composable file is created (PrintStatusFocus.kt, then PrintStatusField.kt, then PrintStatusGutter.kt, then PrintStatusPreviews.kt) and confirmed to compile BEFORE extracting the next slice — so a mid-split preview/compile regression is caught at the slice that introduced it, not at the end (Pitfall 4)"
    - "REVIEW-HIGH (attributable commits): the structural split is its OWN commit/checkpoint (visual parity verified) BEFORE any opportunistic P1/P2 fix; each opportunistic fix (BoxWithConstraints removal, spoolSwatches ImmutableList, ImageRequest remember, LauncherGrid grid, fmtDuration hoist) lands as a SEPARATE commit so a perf delta or visual regression is attributable to a single change"
    - "P1 (concrete sizing source): BoxWithConstraints is removed from IconTwoRowCell (~857) and IconValueCell (~888); icon size is passed as an explicit Dp the CALLER derives from the already-known ScreenScaffold root BoxWithConstraints column width (a value already in scope at the grid level) — NOT a new per-cell or grid-level BoxWithConstraints (the subcomposition problem must not move upward; the column width is computed once at the grid root and threaded down as a Dp)"
    - "P1/P2: spoolSwatches becomes ImmutableList<Color> via remember(spoolDetail, metadata); ImageRequest.Builder is wrapped in remember(thumbnailUrl, context); fmtDuration is hoisted to a pure host-testable function (and/or derivedStateOf keyed on the integer duration) — opportunistic P2/P3 done because the file is already open (D-03), each its own commit"
    - "P2 (LazyVerticalGrid justified): LauncherGrid moves to LazyVerticalGrid(GridCells.Fixed) with key={it.id} ONLY IF the tile set is non-trivial/scrollable; for a tiny FIXED tile set inside ScreenScaffold's BoxWithConstraints, a LazyVerticalGrid can change measurement/scroll — if the set is small+fixed, KEEP the chunked Row layout and instead just stabilize its inputs (ImmutableList tiles + keyed items). The executor measures/justifies the choice on flox and records which path was taken in the SUMMARY"
    - "D-10/Pitfall-4: the existing 6-theme-combo x portrait/landscape @Preview matrix moves to PrintStatusPreviews.kt and every slice still renders it without visual regression; fsSp(baseSp, t.fs) font scale is preserved through every extraction (no raw .sp introduced)"
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusFocus.kt"
      provides: "Independently-restartable Focus-region composable"
    - path: "app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusField.kt"
      provides: "Independently-restartable Field-region composable"
    - path: "app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusGutter.kt"
      provides: "Named gutter composable called from the slot lambda (replacing the inline-bodied slot)"
    - path: "app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusPreviews.kt"
      provides: "The 6-combo x portrait/landscape @Preview matrix"
  key_links:
    - from: "app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusScreen.kt"
      to: "app/src/main/java/works/mees/dinghy/designsystem/layout/ScreenScaffold.kt"
      via: "slot lambdas whose bodies call only named top-level @Composables"
      pattern: "PrintStatusGutter|PrintStatusFocus|PrintStatusField"
---

<objective>
The first big D-01 restructure: split the 1584-line `PrintStatusScreen.kt` god-component into independently-restartable scopes (`PrintStatusFocus` / `PrintStatusField` / `PrintStatusGutter` / `PrintStatusPreviews`), which simultaneously fixes the P0 inline-lambda-slot problem (the slot lambda bodies now call named composables → restartable child scopes ScreenScaffold can skip). While the file is open, land the co-located P1/P2 hot-path fixes the audit flagged (BoxWithConstraints cells, spoolSwatches ImmutableList, ImageRequest remember, LauncherGrid, fmtDuration hoist) per D-03 — but as SEPARATE, attributable commits AFTER the structural split is visually verified.

Purpose: D-01 (god-component split → restartable scopes) + D-02 (lambda slots call named composables) + D-03 (opportunistic P1/P2 on this file). Depends on Plan 02 (new immutable field types this screen reads) and Plan 03 (SpoolGlyph signature).
Output: A four-file PrintStatus split with the @Preview matrix preserved and visual behavior unchanged, with the structural change and each opportunistic fix in distinct commits.

REVIEW NOTE (Codex HIGH, 22-04): two corrections. (1) The old "eliminate inline lambda slots" criterion was technically wrong — `ScreenScaffold` slot params are `@Composable () -> Unit`, so they stay lambdas; the real fix is that each slot lambda body contains ONLY a call to a named top-level composable. Reworded throughout. (2) Splitting a 1584-line screen PLUS grid + image-request + swatch typing + duration formatting + launcher grid in one plan makes regressions un-attributable — the structural split is now its own commit/checkpoint, and each opportunistic fix is a separate commit. Also: `LazyVerticalGrid` for a tiny fixed tile set may change measurement/scroll — it is now justify-or-keep, and the `BoxWithConstraints` replacement names a concrete sizing source (the grid-root column width threaded down as a Dp) so the subcomposition problem doesn't move upward.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/22-performance-architecture-refactor/22-CONTEXT.md
@.planning/phases/22-performance-architecture-refactor/22-RESEARCH.md
@.planning/codebase/CONCERNS.md
@docs/ui_design/LAYOUT.md
@app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusScreen.kt
@app/src/main/java/works/mees/dinghy/designsystem/layout/ScreenScaffold.kt
@app/src/main/java/works/mees/dinghy/designsystem/icons/SpoolGlyph.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: STRUCTURAL SPLIT ONLY — Focus/Field/Gutter/Previews + slot lambdas call named composables (own commit, rebuild per slice)</name>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusScreen.kt (the whole file: PrintStatusScreen entry that collects state ~line 135; PrintStatusContent; gutterContent inline lambda ~490; activeFieldContent inline lambda ~522; PrintStatusFocus root ~640; the four-state PrintStatusMode layout; the existing @Preview functions)
    - app/src/main/java/works/mees/dinghy/designsystem/layout/ScreenScaffold.kt (the focus/field/gutter `@Composable () -> Unit` slot contract ~line 64 — the slots ARE lambdas; this confirms the criterion is "body calls a named composable", not "no lambda")
    - docs/ui_design/LAYOUT.md (Focus/Field/Gutter grammar — the contract the split must preserve in both orientations)
    - .planning/phases/22-performance-architecture-refactor/22-RESEARCH.md (§1 — restartability mechanics, the named-child-composable fix, "pass only what each slice needs", Pitfall 4 preview-regression guard)
  </read_first>
  <action>
    STRUCTURAL SPLIT ONLY — no allocation/grid/format changes in this task (those are Task 3, separate commits). Create three new files — `PrintStatusFocus.kt`, `PrintStatusField.kt`, `PrintStatusGutter.kt` — each a non-inline top-level `@Composable fun` for that region, receiving ONLY the resolved values it reads (stable primitives, enums, @Immutable types, ImmutableMap/ImmutableList from Plan 02 — not the full PrinterState if a slice reads only a few fields). For the two ScreenScaffold slots (gutterContent ~490, activeFieldContent ~522): the slot param stays a `@Composable () -> Unit` lambda, but rewrite each lambda BODY so it contains ONLY a call to the named composable — e.g. `gutterContent = { PrintStatusGutter(state = ..., tokens = ...) }` — creating a restartable child scope. Move ALL `@Preview` declarations (the 6-combo x portrait/landscape matrix + any PreviewBox helpers used only by previews) into `PrintStatusPreviews.kt`. Keep `PrintStatusScreen.kt` as the orchestration shell (`container.printerState.collectAsStateWithLifecycle(...)`, resolves PrintStatusMode, wires the slices). Preserve `fsSp(baseSp, t.fs)` everywhere — NO raw `.sp`. Do not change layout, the four-state model, accent-temperature identity, BoxWithConstraints cells, the LauncherGrid, fmtDuration, or any allocation — this task is purely the structural split (D-10 reads-the-same).

    PER-SLICE REBUILD (Pitfall 4, MANDATORY — do NOT batch the whole split then build once): extract ONE child composable file at a time, and after EACH file is created (after PrintStatusFocus.kt, then again after PrintStatusField.kt, then PrintStatusGutter.kt, then PrintStatusPreviews.kt) run the Windows-side build `cmd /c "E:\Android\gw.bat :app:assembleDebug --no-daemon"` and CONFIRM IT COMPILES before extracting the next slice. If a slice breaks the build (compile or @Preview regression), fix it before moving on — this localizes any regression to the slice that introduced it instead of surfacing only at the end. COMMIT this structural split on its own before Task 2.
  </action>
  <verify>
    <automated>cmd /c "E:\Android\gw.bat :app:assembleDebug --no-daemon"</automated>
  </verify>
  <acceptance_criteria>
    Each of the four slices was rebuilt and confirmed compiling individually before the next was extracted (per-slice `assembleDebug` green after PrintStatusFocus.kt, PrintStatusField.kt, PrintStatusGutter.kt, PrintStatusPreviews.kt — recorded in the SUMMARY), so any mid-split regression was caught at its slice. `PrintStatusFocus.kt`, `PrintStatusField.kt`, `PrintStatusGutter.kt`, `PrintStatusPreviews.kt` exist, each with a non-inline top-level `@Composable fun`. Each ScreenScaffold slot lambda body contains ONLY a call to a named top-level composable (`grep -n "gutterContent" PrintStatusScreen.kt` shows `gutterContent = { PrintStatusGutter(` style — a single named call, not an inline block of layout). Final `assembleDebug` builds. No raw `.sp` introduced (`grep -rn "[0-9]\.sp\|[0-9]sp" app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatus*.kt` shows only fsSp usages). The @Preview matrix lives in PrintStatusPreviews.kt. NO allocation/grid/format change is present in this commit's diff (BoxWithConstraints/LauncherGrid/fmtDuration untouched here).
  </acceptance_criteria>
  <done>The god-component is four restartable files, each rebuilt-and-verified as it was extracted; slot lambdas call named composables; previews preserved; committed standalone.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: On-device flox parity check of the STRUCTURAL SPLIT (before any opportunistic fix) (D-01/D-10)</name>
  <read_first>
    - .planning/phases/22-performance-architecture-refactor/22-CONTEXT.md (D-10 visual bar = reads-the-same on flox; any ScreenScaffold/layout-touching change requires the full @Preview matrix + on-device flox check)
    - .planning/codebase/CONCERNS.md (Fragile Areas — ScreenScaffold/BoxWithConstraints affects ALL screens)
  </read_first>
  <action>
    Build the APK (the post-Task-1 structural-split commit) and install on flox (`installDebug` or signed release per CLAUDE.md). Surface the running build to the owner for a side-by-side parity check of the Print Status home against the pre-split version, in both orientations across the four print states; do not adb-drive the UI. Record the owner's verdict in the SUMMARY. Because this is the STRUCTURAL split in isolation, any visual diff here is attributable to the split itself (the opportunistic fixes have not landed yet). Fix any diff before proceeding to Task 3.
  </action>
  <what-built>
    Claude builds + installs the structural-split APK on flox; the owner verifies the Print Status home renders identically to before the split in BOTH orientations across the four print states (Standby / Printing / Paused / Terminal). Claude does NOT adb-drive the UI; the owner navigates/eyeballs.
  </what-built>
  <how-to-verify>
    1. Build + install (`installDebug` or release per CLAUDE.md) on flox.
    2. Owner opens Print Status in portrait and landscape and confirms Focus/Field/Gutter layout, stat grid cells, launcher/shortcut tiles, spool glyph, thumbnail, and duration text all render as before — no clipping, no relayout, no missing element.
    3. Owner exercises the four print states if reachable and confirms each reads-the-same.
    4. Owner replies "approved" or describes the visual diff.
  </how-to-verify>
  <verify>
    <automated>MISSING — perceptual on-device parity; gated by owner approval (the @Preview matrix is the host-side net; the on-device flox check is the D-10 authority).</automated>
    <human-check>Owner confirms the split Print Status reads-the-same in both orientations across all four states; replies "approved".</human-check>
  </verify>
  <acceptance_criteria>
    Owner approves that the split (structural only) Print Status home is visually identical to pre-split in portrait and landscape across the four print states. No layout regression on flox attributable to the split.
  </acceptance_criteria>
  <resume-signal>Owner types "approved" or describes the visual difference to fix.</resume-signal>
  <done>The god-component split is on-device-confirmed visually unchanged on flox, in isolation from the opportunistic fixes.</done>
</task>

<task type="auto">
  <name>Task 3: Land the co-located P1/P2 hot-path fixes as SEPARATE attributable commits (D-03 opportunistic)</name>
  <read_first>
    - .planning/codebase/CONCERNS.md (P1: BoxWithConstraints cells ~857/~888, spoolSwatches ~187; P2: ImageRequest ~680, LauncherGrid ~1073, fmtDuration ~1537)
    - .planning/phases/22-performance-architecture-refactor/22-RESEARCH.md (§8 — exact remember/ImmutableList/grid shapes + the spoolSwatches remember key + the column-width sizing source for the BoxWithConstraints removal)
    - app/src/main/java/works/mees/dinghy/designsystem/ColorWheel.kt (and the deleteAllowed/pure-helper pattern) — the "pull logic into a pure host-tested function" template for fmtDuration
    - the post-split PrintStatusFocus.kt / PrintStatusField.kt (where these cells/grids/images now live after Task 1)
    - app/src/main/java/works/mees/dinghy/designsystem/layout/ScreenScaffold.kt (the root BoxWithConstraints whose column width is the concrete sizing source threaded down to the icon cells)
  </read_first>
  <action>
    Land each as a SEPARATE commit so each is independently attributable (per the review). Commit 3a — BoxWithConstraints removal: remove `BoxWithConstraints` from `IconTwoRowCell` and `IconValueCell`; add an `iconSize: Dp` parameter; the CALLER derives it ONCE from the grid-root column width (already available from the ScreenScaffold root BoxWithConstraints / the grid layout's known column count and width) and threads it down — do NOT introduce a new BoxWithConstraints at the cell or grid level (the subcomposition cost must not move upward). Size via `Modifier.size(iconSize)`. Commit 3b — spoolSwatches: make it `ImmutableList<Color>` computed in `remember(spoolDetail, metadata) { ... }` (Spoolman colorSwatches → else gcode filamentColors[0] → else empty, `.toImmutableList()`/`persistentListOf()`); propagate `ImmutableList<Color>` into the SpoolGlyph call. Commit 3c — ImageRequest: wrap `ImageRequest.Builder(context).data(thumbnailUrl).build()` in `remember(thumbnailUrl, context) { ... }`. Commit 3d — fmtDuration: hoist to a pure top-level host-testable function (mirroring deleteAllowed) and call inside `derivedStateOf { fmtDuration(durationSeconds.toInt()) }` keyed on integer seconds. Commit 3e — LauncherGrid: MEASURE/JUSTIFY first — if the tile set is small + fixed, KEEP the chunked Row layout but stabilize inputs (`ImmutableList` tiles + keyed iteration); only move to `LazyVerticalGrid(GridCells.Fixed(columns))` + `items(tiles, key={it.id})` if the set is genuinely scrollable/large (a Lazy grid inside ScreenScaffold's BoxWithConstraints can alter measurement/scroll for a tiny fixed set). Record which path was taken in the SUMMARY. Do NOT change any visible output (D-10).
  </action>
  <verify>
    <automated>cmd /c "E:\Android\gw.bat :app:testReleaseUnitTest --no-daemon"</automated>
  </verify>
  <acceptance_criteria>
    Host unit suite green (including the new fmtDuration test). `grep -n "BoxWithConstraints" app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatus*.kt` shows it removed from the icon cells (and NOT re-introduced at the grid/cell level). spoolSwatches is `ImmutableList<Color>` inside a `remember(...)`. ImageRequest is inside `remember(thumbnailUrl`. fmtDuration is a top-level pure function. The LauncherGrid choice (kept chunked-with-stable-inputs vs LazyVerticalGrid) is recorded with its justification in the SUMMARY. Each fix is a distinct commit. `assembleDebug` builds for the @Preview matrix.
  </acceptance_criteria>
  <done>The co-located P1/P2 allocations on the home screen are fixed as separate attributable commits; behavior unchanged; tests green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: On-device flox parity check of the OPPORTUNISTIC FIXES (D-01/D-10)</name>
  <read_first>
    - .planning/phases/22-performance-architecture-refactor/22-CONTEXT.md (D-10 visual bar; any layout-touching change requires on-device flox check)
    - the Task-2 SUMMARY note (the split-only parity baseline to compare against — any diff now is attributable to a Task-3 fix, especially the icon-cell sizing or the LauncherGrid choice)
  </read_first>
  <action>
    Build + install the post-Task-3 APK on flox. Surface it to the owner for a parity check focused on the surfaces the opportunistic fixes touched — the stat grid icon cells (BoxWithConstraints removal: icon sizing), the launcher tiles (grid choice), the spool glyph (swatch typing), the thumbnail (ImageRequest), and the duration text (fmtDuration) — in both orientations. Because Task 2 already approved the split, any diff here is attributable to a specific Task-3 fix; identify and fix it. Record the verdict in the SUMMARY.
  </action>
  <what-built>
    Claude builds + installs the post-opportunistic-fix APK on flox; the owner confirms the icon cells, launcher tiles, spool glyph, thumbnail, and duration text still read-the-same in both orientations across the print states. Claude does NOT adb-drive the UI.
  </what-built>
  <how-to-verify>
    1. Build + install on flox.
    2. Owner checks the icon stat cells (size/position unchanged after BoxWithConstraints removal), the launcher grid (no relayout/scroll change), the spool glyph color, the thumbnail, and the duration text.
    3. Owner replies "approved" or names the specific surface that regressed (attributable to a Task-3 commit).
  </how-to-verify>
  <verify>
    <automated>MISSING — perceptual on-device parity; gated by owner approval.</automated>
    <human-check>Owner confirms the opportunistic-fix surfaces (icon cells / launcher grid / spool glyph / thumbnail / duration) read-the-same; replies "approved".</human-check>
  </verify>
  <acceptance_criteria>
    Owner approves that the opportunistic fixes left Print Status visually unchanged in both orientations. Any reported diff is traced to its Task-3 commit and fixed before closing.
  </acceptance_criteria>
  <resume-signal>Owner types "approved" or names the regressed surface.</resume-signal>
  <done>The opportunistic P1/P2 fixes are on-device-confirmed visually unchanged on flox.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | UI-layer composable split + allocation hygiene. No auth/network/persistence-crypto/input-validation surface; the screen renders the same trusted state it already received. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-04 | (none) | PrintStatusScreen split + co-located P1/P2 fixes | accept | No new threat — splitting a composable into restartable scopes and caching allocations preserves all existing trust boundaries; visual behavior is owner-verified unchanged (D-10), now in two separated checkpoints (split vs fixes). |
</threat_model>

<verification>
- `assembleDebug` builds (preview matrix) — rebuilt per-slice during the split, then once more at task end; `testReleaseUnitTest` green (incl. fmtDuration).
- Owner approves on-device flox parity TWICE: once for the structural split alone (Task 2), once for the opportunistic fixes (Task 4) — keeping any regression attributable.
</verification>

<success_criteria>
- D-01 move #1 complete: four restartable PrintStatus files (each rebuilt-verified as extracted), slot lambdas call named composables, P1/P2 co-located fixes landed as separate attributable commits, @Preview matrix preserved, on-device reads-the-same (split and fixes verified independently).
</success_criteria>

<output>
Create `.planning/phases/22-performance-architecture-refactor/22-04-SUMMARY.md` when done.
</output>
