---
phase: 22-performance-architecture-refactor
plan: 07
type: execute
wave: 3
depends_on: ["22-02", "22-04"]
files_modified:
  - app/src/main/java/works/mees/dinghy/ui/shell/AppShell.kt
  - app/src/main/java/works/mees/dinghy/ui/shell/AppDrawer.kt
  - app/src/main/java/works/mees/dinghy/di/AppContainer.kt
  - .planning/phases/22-performance-architecture-refactor/22-GFXINFO-AFTER.md
autonomous: false
requirements: []
must_haves:
  truths:
    - "D-01 move #2: the AppShell flow collections are pushed DOWN — the shell keeps ONLY the flows it directly renders (connection status, active screen id / nav.dest, drawer open state, and the drawer-tile-visibility flows: webcam/spool/outputs enabled, drawerSpoolSwatches, activeName/activeProfileId, spine for holder re-keying); collections used ONLY to pass into a when(dest) screen call (e.g. the calibration zTiltVm/qglVm/bedMeshVm/probeCalibrateVm at ~386-389) are removed from AppShell and collected inside the consuming screen, which already receives container"
    - "Pitfall-3 honored: before moving any collection, the holder builds keyed via remember(store){...} are traced — holders keyed only on store/StateFlow stay in AppShell and are re-keyed on the StateFlow directly (not a collected value) where needed; nothing references a removed collected value"
    - "P2/P3 opportunistic (file open, D-03): the inline .map{} flows in AppShell (activeProfile.map{id} ~224, activeProfile.map{displayName} ~227, errorLines ~421-429) are hoisted to named StateFlow properties on AppContainer via stateIn(...) so they are created once, not per-recomposition; drawerSpoolSwatches becomes ImmutableList<Color> via remember(activeSpoolDetail) and AppDrawer/DrawerTile receive ImmutableList<Color>"
    - "SC1: a RELEASE-mode gfxinfo AFTER capture is taken on flox across the full D-06 sweep set and compared to the Plan-01 baseline — zero frozen frames + responsiveness per ADR-0001 Addendum 2; recorded in 22-GFXINFO-AFTER.md"
    - "SC2/SC5: recomposition hygiene is confirmed (AppShell recomposition count near-zero between nav events; hot screens skip on 4 Hz emission per Layout-Inspector or Compose compiler reports) AND the connect→monitor→control-a-print loop still works on flox with no functional regression; all screens still navigate"
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/ui/shell/AppShell.kt"
      provides: "Shell collecting only shell-rendered flows; screen-local collections pushed down"
    - path: "app/src/main/java/works/mees/dinghy/di/AppContainer.kt"
      provides: "Hoisted named StateFlows (activeProfileId/activeName/errorLines) created once via stateIn"
    - path: ".planning/phases/22-performance-architecture-refactor/22-GFXINFO-AFTER.md"
      provides: "Post-fix release-mode gfxinfo sweep vs baseline (SC1 evidence)"
  key_links:
    - from: "app/src/main/java/works/mees/dinghy/ui/shell/AppShell.kt"
      to: "the consuming when(dest) screens"
      via: "screens receive container and collect their own flows (no shell-side collection passed in)"
      pattern: "container ="
---

<objective>
The second big D-01 restructure: push the 28 `AppShell` `collectAsStateWithLifecycle` collections DOWN so the shell collects only what it directly renders (connection status, nav.dest, drawer-open, drawer-tile visibility). This kills the wide-recomposition root cause where any 4 Hz emission re-evaluates all 28 collected flows in the shell scope. While the file is open, hoist the inline `.map{}` flows to named `AppContainer` StateFlows (created once) and make drawerSpoolSwatches an ImmutableList (D-03 P2/P3). Then take the final SC1 gfxinfo AFTER sweep and run the SC2/SC5 owner UAT — this is the phase's closing measurement + regression gate.

Purpose: D-01 move #2 + the SC1/SC2/SC5 close-out. Depends on Plan 02 (immutable state the screens collect) and Plan 04 (PrintStatus already split + self-collecting).
Output: A lean shell, hoisted container flows, the post-fix gfxinfo sweep, and an owner-confirmed print-loop regression check.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/22-performance-architecture-refactor/22-CONTEXT.md
@.planning/phases/22-performance-architecture-refactor/22-RESEARCH.md
@.planning/phases/22-performance-architecture-refactor/22-GFXINFO-BASELINE.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/ARCHITECTURE.md
@docs/adr/0001-ui-toolkit-decision.md
@app/src/main/java/works/mees/dinghy/ui/shell/AppShell.kt
@app/src/main/java/works/mees/dinghy/ui/shell/AppDrawer.kt
@app/src/main/java/works/mees/dinghy/di/AppContainer.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit + push down the AppShell collections (D-01 move #2)</name>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/shell/AppShell.kt (every collectAsStateWithLifecycle: spine 185, capabilities 186, httpBase 187, printerState 203, webcamCount 212, webcamEnabled 213, cfg 218, activeProfileId 224, activeName 227, spoolEnabled 297, activeSpoolStatus 308, activeSpoolDetail 313, outputsEnabled 323, outputRows 333, systemInfoHolder 352, zTiltVm 386, qglVm 387, bedMeshVm 388, probeCalibrateVm 389, errorLines 429, dispatcher 462, promptView 477, consoleBackfillFailed 481, + the nested ones 929/981/983/988; the holder builds via remember(store){...}; the when(dest) router)
    - .planning/phases/22-performance-architecture-refactor/22-RESEARCH.md (§2 — the safe mechanical recipe, the "stays vs moves vs split" tagging of each collection, the explicit list of shell-level keepers, and Pitfall 3 holder-build trap)
    - .planning/codebase/ARCHITECTURE.md (flow-ownership map — which flow each screen actually consumes)
    - the consuming screens (e.g. ui/calibration/* for zTiltVm/qglVm/bedMeshVm/probeCalibrateVm) to confirm each already receives `container` and can collect its own holder.vm
  </read_first>
  <action>
    Tag each of the 28 collections per RESEARCH §2: (a) STAYS — shell renders it directly (connectionState/connection-overlay, nav.dest, drawerOpen, webcamEnabled/spoolEnabled/outputsEnabled drawer-tile gates, drawerSpoolSwatches, activeName/activeProfileId drawer subtitle, spine for holder re-keying, dispatcher/promptView/consoleBackfillFailed if shell-rendered); (b) MOVES — collected ONLY to pass into a when(dest) screen call (the calibration *Vm at 386-389 are the clearest: pass `container` to the calibration screens and let each collect its own holder.vm); (c) SPLIT — a flow that gates a drawer tile AND feeds a screen: keep the gate in AppShell, move the screen-level data collection into the screen. For each MOVES collection, delete it from AppShell and have the consuming screen collect it (the screen already receives `container`). Before deleting any collection, trace the holder builds keyed on it (Pitfall 3): holders keyed only on `store`/a StateFlow stay in AppShell; re-key on the StateFlow directly if a holder used a collected value. Do NOT change navigation, holder lifetimes, or any screen's rendered output — visual unchanged (D-10).
  </action>
  <verify>
    <automated>cmd /c "E:\Android\gw.bat :app:testReleaseUnitTest --no-daemon"</automated>
  </verify>
  <acceptance_criteria>
    Host suite green (incl. any ShellPresence/nav tests); `assembleRelease` builds. `grep -c "collectAsStateWithLifecycle" app/src/main/java/works/mees/dinghy/ui/shell/AppShell.kt` is materially lower than 28 (the MOVES collections are gone from the shell). The calibration *Vm collections (386-389) no longer appear in AppShell. No reference to a removed collected value remains (compiler-clean). Holder builds are intact.
  </acceptance_criteria>
  <done>AppShell collects only shell-rendered flows; screen-local collections live in their screens.</done>
</task>

<task type="auto">
  <name>Task 2: Hoist the inline .map{} flows to AppContainer StateFlows + drawerSpoolSwatches ImmutableList (D-03 P2/P3)</name>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/shell/AppShell.kt (the inline .map flows: activeProfile.map{it?.id} ~224, activeProfile.map{it?.displayName()} ~227, the errorLines .map sequence ~421-429; drawerSpoolSwatches ~315)
    - app/src/main/java/works/mees/dinghy/di/AppContainer.kt (its scope + existing StateFlow stateIn(...) patterns — match the SharingStarted/scope convention already used; note AppContainer is @Stable from Plan 01)
    - app/src/main/java/works/mees/dinghy/ui/shell/AppDrawer.kt (the DrawerTile receiving List<Color> swatches — change to ImmutableList<Color>)
    - .planning/phases/22-performance-architecture-refactor/22-RESEARCH.md (§8 — the errorLines/activeProfile stateIn hoist + drawerSpoolSwatches remember/ImmutableList shapes)
    - .planning/codebase/CONCERNS.md (P2 ~156-168 mapped-flow garbage; P1 ~99-114 drawerSpoolSwatches; P3 ~276-285 errorLines)
  </read_first>
  <action>
    On AppContainer, add named StateFlow properties created once via `stateIn(scope, SharingStarted...)` for: activeProfileId (`activeProfile.map { it?.id }`), activeName (`activeProfile.map { it?.displayName() }`), and errorLines (the console-scrollback ERROR filter → takeLast(3)). In AppShell, replace the inline `.map{}.collectAsStateWithLifecycle(...)` calls with `collectAsStateWithLifecycle` on these cold StateFlow references (no per-recomposition Flow wrapper allocation). Make `drawerSpoolSwatches` an `ImmutableList<Color>` via `remember(activeSpoolDetail) { ...mapNotNull(::parseNormalizedHex).toImmutableList() }` and change `AppDrawer`/`DrawerTile` to receive `ImmutableList<Color>`. Match AppContainer's existing stateIn scope/SharingStarted convention. No behavior change — same values, just allocated once and stable-typed.
  </action>
  <verify>
    <automated>cmd /c "E:\Android\gw.bat :app:testReleaseUnitTest --no-daemon"</automated>
  </verify>
  <acceptance_criteria>
    Host suite green; `assembleRelease` builds. `AppContainer` exposes `activeProfileId`/`activeName`/`errorLines` as `StateFlow` (`grep -n "val errorLines\|val activeProfileId\|val activeName" app/.../di/AppContainer.kt`). AppShell no longer constructs inline `.map{}` before collect for these. drawerSpoolSwatches is `ImmutableList<Color>` and AppDrawer/DrawerTile params are `ImmutableList<Color>`.
  </acceptance_criteria>
  <done>The mapped flows are created once on AppContainer; drawer swatches are a stable ImmutableList.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Final gfxinfo AFTER sweep + SC1/SC2/SC5 owner UAT on flox</name>
  <read_first>
    - .planning/phases/22-performance-architecture-refactor/22-GFXINFO-BASELINE.md (the Plan-01 before-anchor to compare every sweep screen against)
    - .planning/phases/22-performance-architecture-refactor/22-RESEARCH.md (§7 gfxinfo protocol + §2 "Verification: Layout Inspector recomposition counts" for SC2)
    - .planning/phases/22-performance-architecture-refactor/22-VALIDATION.md (the SC1/SC2/SC5 manual-only rows)
    - docs/adr/0001-ui-toolkit-decision.md (Addendum 2 — zero frozen frames + responsiveness gate)
  </read_first>
  <action>
    Build + install the RELEASE APK on flox and create `22-GFXINFO-AFTER.md`. While the owner drives flox through the full D-06 sweep set, capture `dumpsys gfxinfo works.mees.dinghy framestats` per screen (reset → exercise → dump → parse_framestats.py) and record the AFTER row beside the Plan-01 baseline (SC1). Record SC2 recomposition-hygiene evidence (AppShell recomposition near-zero between nav via Layout Inspector, or Compose compiler reports showing the hot screens skip). Have the owner drive the connect→monitor→control-a-print loop on flox + live E5 Plus for SC5. Do not adb-drive the UI. Close on owner approval; if a regression or a frozen-frame screen is reported, fix before closing.
  </action>
  <what-built>
    Claude builds + installs the RELEASE APK on flox and creates `22-GFXINFO-AFTER.md`. The owner drives flox through the full D-06 sweep set; Claude captures gfxinfo framestats per screen, parses with parse_framestats.py, and records the AFTER row beside the Plan-01 baseline (SC1: zero frozen frames + responsiveness). For SC2, Claude records recomposition-hygiene evidence (Layout Inspector recomposition counts near-zero on AppShell between nav events, or Compose compiler reports showing the hot screens skip). For SC5, the owner drives the connect→monitor→control-a-print loop on flox (E5 Plus) and confirms no functional regression. Claude does NOT adb-drive the UI.
  </what-built>
  <how-to-verify>
    1. Release build installed on flox; `22-GFXINFO-AFTER.md` created.
    2. Owner walks the full D-06 sweep (PrintStatus idle + mid-print, App Drawer, Files, Console, Temperature, Webcam, Move); Claude captures + parses gfxinfo per screen and records AFTER vs baseline. SC1 passes when every screen shows zero frozen frames and responsiveness is maintained/improved.
    3. SC2 evidence: AppShell recomposition count near-zero between nav events (Layout Inspector) or Compose reports show socket-driven types skip — recorded.
    4. SC5: owner drives connect → monitor → control a print (start/pause/resume/cancel as reachable) on flox + live E5 Plus → no functional regression; all screens navigate.
    5. Owner replies "approved" or reports a regression / a screen still showing frozen frames.
  </how-to-verify>
  <verify>
    <automated>MISSING — SC1 perceptual perf + SC5 end-to-end print loop are on-device, owner-gated; SC2 needs Layout-Inspector/Compose-report evidence. parse_framestats.py produces the SC1 numbers; not a host assertion.</automated>
    <human-check>Owner approves: every D-06 sweep screen shows zero frozen frames vs baseline (SC1); AppShell recomposition near-zero between nav (SC2); connect→monitor→control-a-print loop works with no regression (SC5).</human-check>
  </verify>
  <acceptance_criteria>
    `22-GFXINFO-AFTER.md` records an AFTER row for every D-06 sweep screen vs the Plan-01 baseline with zero frozen frames + maintained responsiveness (SC1). SC2 recomposition-hygiene evidence recorded (AppShell near-zero between nav; hot screens skip). SC5 print loop confirmed on flox with no regression; all screens navigate. Owner approves.
  </acceptance_criteria>
  <resume-signal>Owner types "approved" or reports a frozen-frame screen / navigation or print-loop regression.</resume-signal>
  <done>Phase close-out: SC1 (perf delta), SC2 (recomposition hygiene), SC5 (print loop intact) all owner-confirmed on flox.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Flow-collection relocation + StateFlow hoist in the shell/container. No auth/network/persistence-crypto/input-validation surface; the same flows carry the same data, only the collection site and allocation lifetime change. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-07 | (none) | AppShell push-down + AppContainer flow hoist | accept | No new threat — relocating where a flow is collected and hoisting a .map to a stateIn'd StateFlow preserves every trust boundary; the FGS-owned session spine and its data flow are unchanged. |
</threat_model>

<verification>
- Host suite green; AppShell collection count materially below 28; AppContainer exposes the hoisted StateFlows.
- Owner approves SC1 (gfxinfo AFTER vs baseline, zero frozen frames), SC2 (recomposition hygiene), SC5 (print loop intact) on flox.
</verification>

<success_criteria>
- D-01 move #2 complete: shell collects only shell-rendered flows; mapped flows hoisted; SC1/SC2/SC5 owner-confirmed on flox.
</success_criteria>

<output>
Create `.planning/phases/22-performance-architecture-refactor/22-07-SUMMARY.md` when done.
</output>
