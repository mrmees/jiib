---
phase: 15.2
slug: theme-conformance-sweep-settings-ia
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-06-05
---

# Phase 15.2 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> See `15.2-RESEARCH.md` § "Validation Architecture" for the full rationale.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | JUnit4 (host-pure unit) + Robolectric where a Context is needed; existing Gradle test source set |
| **Config file** | `app/build.gradle.kts` (testOptions); no new framework install |
| **Quick run command** | `cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --tests works.mees.dinghy.theme.* --no-daemon"` |
| **Full suite command** | `cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --no-daemon"` |
| **Estimated runtime** | ~60–120 s (host JVM; no device) |

> ⚠ Build/test runs Windows-side via `E:\Android\gw.bat` (`./gradlew` will not run from WSL).
> Pipe CR progress through `tr -d '\r'`; the process exit code is authoritative.
> ⚠ AGP glob gotcha: `--tests 'works.mees.dinghy.theme.*'` may false-fail "No tests found" —
> list test classes explicitly if it does.

---

## Sampling Rate

- **After every task commit:** Run the quick command for the touched package.
- **After every plan wave:** Run the full unit suite.
- **Before `/gsd-verify-work`:** Full unit suite green AND the on-device eyeball gates (below) signed off by the owner.
- **Max feedback latency:** ~120 s (host JVM).

---

## Per-Task Verification Map

> The phase has two validation halves (RESEARCH § Validation Architecture):
> **(1) logic that is host-pure-testable** — the transient theme override resolver, the IA
> persistence/per-profile toggles, the Devices→Printers rename invariants; and
> **(2) the conformance sweep itself**, which is fundamentally an on-device visual judgment
> (the `fsSp` / button-intent / FFG / ≥64px dimensions cannot be asserted by a JVM test the way
> a colour-token equality can). Half (1) gets automated verify; half (2) gets the on-device
> eyeball gate + the AUDIT.md scorecard as the durable artifact of record.

| Task area | Wave | Decision | Test Type | Automated Command | Status |
|-----------|------|----------|-----------|-------------------|--------|
| Transient override resolver — `effectiveTokens` falls back to persisted tuple when override null | W1 | D-06..D-08 | unit | `gw.bat :app:testDebugUnitTest --tests works.mees.dinghy.theme.ThemeOverrideTest` | ⬜ pending |
| Override never writes the persisted per-profile tuple (no `dataStore.edit` on apply/dismiss) | W1 | D-08 | unit | `gw.bat :app:testDebugUnitTest --tests works.mees.dinghy.theme.ThemeOverrideNoPersistTest` | ⬜ pending |
| Style cycler advances exactly the 6 `{dark,light}×{Colorful,Simple,High-Contrast}` combos using the current accent/seed | W1 | D-06 | unit | `gw.bat ... --tests works.mees.dinghy.theme.StyleCyclerTest` | ⬜ pending |
| Text-size cycler steps S/M/L `--fs` independently of style | W1 | D-07 | unit | `gw.bat ... --tests works.mees.dinghy.theme.TextSizeCyclerTest` | ⬜ pending |
| Dev-widget enable = persisted app-global boolean (NOT BuildConfig.DEBUG), readable in release | W1 | D-08 | unit | `gw.bat ... --tests works.mees.dinghy.theme.DevWidgetEnableTest` | ⬜ pending |
| IA: per-profile Settings toggles persist via `writeScope` + `mutateActive{}`, survive restart | W2 | D-04 | unit/instrumented | `gw.bat ... --tests works.mees.dinghy.config.ProfileToggleTest` | ⬜ pending |
| IA: Devices→Printers rename — no dangling nav refs; 4 drawer destinations route distinctly | W2 | D-01,D-02 | unit | `gw.bat ... --tests works.mees.dinghy.shell.ShellNavStateTest` | ⬜ pending |
| Conformance fixes (token/intent/FFG/≥64px/fsSp) per surface | W3 | D-11..D-13 | manual eyeball + AUDIT.md | on-device review (see Manual-Only) | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `app/src/test/java/works/mees/dinghy/theme/ThemeOverrideTest.kt` — RED stub for the override resolver (must compile day-one: typed `fail()` body, no refs to unbuilt symbols beyond the interface the building plan introduces). See `[[dinghy-wave0-red-scaffold-compile]]`.
- [ ] Existing test infra (JUnit4/Robolectric) already present — no framework install.

*Wave-0 RED scaffolds MUST compile against the whole test source set (Gradle compiles it before the `--tests` filter); a bad stub bricks ALL per-wave runs.*

---

## Manual-Only Verifications

| Behavior | Decision | Why Manual | Test Instructions |
|----------|----------|------------|-------------------|
| Cycler flips the WHOLE live app (both Compose + classic-Views surfaces) across all 6 styles, then restores the real saved theme on dismiss | D-06,D-08 | "looks right across every screen" is a visual judgment; the override reaching GraphView/BedMeshHeatmapView via `applyTokens` is only provable by eye | On flox (release APK): enable dev widgets in About → walk Home/Move/Temp/Files/Console → tap style cycler through 6 combos, confirm every surface (incl. graph/heatmap) flips → tap text-size cycler S/M/L → dismiss → confirm saved per-profile theme is back |
| Guided D-10 batch surfaces latent criteria | D-10 | Purpose is to DISCOVER unwritten criteria before the app-wide audit | Owner-led screen-by-screen review of the ~5–10 core screens (edit→build→install→eyeball, `[[dinghy-display-ondevice-iteration]]`); fold findings into `docs/ui_design/THEMING.md` + AUDIT checklist |
| Back-button intent sweep — Back is Neutral + consistent gutter position app-wide | 15.1 D-10 | The single largest fix bucket (RESEARCH: Back currently rendered red/green/neutral across screens); correctness is per-screen visual | After fixes, walk every screen with a Back affordance; confirm Neutral intent + LAYOUT.md gutter position everywhere |
| App-wide AUDIT.md scorecard complete | D-11,D-12 | Coverage-of-all-~30-surfaces is the durable artifact, scored by eye against 6+1 dimensions | Every surface row in AUDIT.md scored; all mechanical violations fixed (D-13), judgment-call conflicts flagged to owner |
| `fsSp` font-scale dimension | — | `[[dinghy-font-sizes-too-small]]` — Claude under-sizes; only catchable on-device | Eyeball floor 15sp / body 17–18 / titles 20–22 / tabular 26 / focus 30+ on flox |
| Adreno-320 perf: overlay widget adds NO continuous animation | — | Perf floor; static-glow-only LAW | Confirm the floating widget is static (no breathing/looping); no new jank on the sweep |

---

## Validation Sign-Off

- [ ] All host-pure-testable tasks (override resolver, cycler logic, IA persistence, nav rename) have automated verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive logic tasks without automated verify
- [ ] Conformance-sweep tasks have an explicit on-device eyeball gate + an AUDIT.md row (the manual-but-tracked path)
- [ ] Wave 0 RED scaffold compiles against the full test source set
- [ ] No watch-mode flags
- [ ] Feedback latency < 120s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
