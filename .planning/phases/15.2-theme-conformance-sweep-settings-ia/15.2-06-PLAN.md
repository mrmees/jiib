---
phase: 15.2-theme-conformance-sweep-settings-ia
plan: 06
type: execute
wave: 6
depends_on: ["15.2-05"]
files_modified:
  - .planning/phases/15.2-theme-conformance-sweep-settings-ia/15.2-AUDIT.md
  - app/src/main/java/works/mees/dinghy/ui/calibration/CalibrationHubScreen.kt
  - app/src/main/java/works/mees/dinghy/ui/calibration/TiltScreen.kt
  - app/src/main/java/works/mees/dinghy/ui/calibration/BedMeshScreen.kt
  - app/src/main/java/works/mees/dinghy/ui/calibration/ProbeCalibrateScreen.kt
  - app/src/main/java/works/mees/dinghy/ui/spool/SpoolScreen.kt
  - app/src/main/java/works/mees/dinghy/ui/macros/SystemMacrosScreen.kt
autonomous: false
requirements: [UI-01, UI-02, THEME-01, THEME-02]
must_haves:
  truths:
    - "Every one of the ~30 existing surfaces (core + long tail: calibration, spool, webcam, macros, prompt, render Views) is scored in AUDIT.md against the 6+1 LAW dimensions — coverage is PROVABLY complete via the generated surface inventory, EXPANDED to surface granularity (multi-surface files split into their distinct surface rows), nothing slips (D-11/D-12/MEDIUM-2/LOW)"
    - "All mechanical violations — both the long-tail findings AND the core-screen mechanical findings recorded by plan 05 — are fixed silently (D-13/MEDIUM-1)"
    - "Genuine design-judgment conflicts (new LAW vs a deliberate existing choice) are flagged to the owner, not silently changed (D-13)"
    - "The on-device checkpoint forms a CLOSED loop: any NEW mechanical gap the owner finds on-device is fixed -> rebuilt -> reinstalled -> re-verified, looping until the owner reports no remaining mechanical gaps before AUDIT.md is finalized (HIGH-6)"
    - "A lint-style conformance check passes: no raw Color(0x...) outside the sanctioned whitelist, zero MaterialTheme.colorScheme usage, no raw .sp outside fsSp (MEDIUM-2)"
    - "Sanctioned raw-color literals (PromptMarkup author-hex, SpoolPicker/Spoolman filament swatches, theme swatches) are recorded as conformant — NOT 'fixed' (THEMING.md carve-out)"
  artifacts:
    - path: ".planning/phases/15.2-theme-conformance-sweep-settings-ia/15.2-AUDIT.md"
      provides: "The completed D-12 scorecard — every surface row scored, every finding resolved or flagged, coverage proven against the surface inventory + lint check"
      contains: "calibration"
  key_links:
    - from: ".planning/phases/15.2-theme-conformance-sweep-settings-ia/15.2-AUDIT.md"
      to: "the long-tail surface files"
      via: "every surface row scored + the mechanical fixes applied"
      pattern: "score|check"
---

<objective>
Run the app-wide conformance audit on top of the criteria hardened in plan 05: score ALL ~30 surfaces
(core + the long tail — calibration, spool, webcam, macros, prompt, and the render Views) in the AUDIT.md
scorecard, then fix every mechanical violation silently and flag genuine judgment-call conflicts to the
owner (D-11/D-12/D-13). The cycler widgets make the per-screen long-tail checks cheap enough that nothing
is deferred.

Per the cross-AI review: coverage is PROVEN against the generated surface inventory + a lint-style check
(MEDIUM-2), at SURFACE granularity (multi-surface files expanded into distinct surface rows — LOW); the
fix pass includes the core-screen mechanical findings plan 05 recorded (MEDIUM-1); and the final on-device
checkpoint is a CLOSED loop — new on-device mechanical gaps are fixed -> rebuilt -> reinstalled ->
re-verified until the owner reports none remaining (HIGH-6).

Purpose: Make every existing surface obey the reconciled theme LAW — the heavy sweep of everything that
exists today, with a durable tracked scorecard and a closed verification loop.
Output: a completed AUDIT.md, the long-tail + core mechanical fixes, the lint-check pass, and an
owner-signed conformance gate reached through a closed fix/verify loop.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15.2-theme-conformance-sweep-settings-ia/15.2-CONTEXT.md
@.planning/phases/15.2-theme-conformance-sweep-settings-ia/15.2-RESEARCH.md
@.planning/phases/15.2-theme-conformance-sweep-settings-ia/15.2-PATTERNS.md
@.planning/phases/15.2-theme-conformance-sweep-settings-ia/15.2-REVIEWS.md
@.planning/phases/15.2-theme-conformance-sweep-settings-ia/15.2-AUDIT.md
@docs/ui_design/THEMING.md
@docs/ui_design/CLAUDE.md
@docs/ui_design/LAYOUT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Score every remaining surface in AUDIT.md against the 6+1 LAW dimensions; prove coverage via the surface-granular inventory + a lint-style check</name>
  <files>.planning/phases/15.2-theme-conformance-sweep-settings-ia/15.2-AUDIT.md</files>
  <read_first>
    - .planning/phases/15.2-theme-conformance-sweep-settings-ia/15.2-REVIEWS.md (MEDIUM-2 — generated surface inventory + lint-style check so coverage is provably complete)
    - .planning/phases/15.2-theme-conformance-sweep-settings-ia/15.2-AUDIT.md (the scorecard — the inventory command recorded by plan 01; core rows scored in plan 05; fill the long-tail rows here)
    - .planning/phases/15.2-theme-conformance-sweep-settings-ia/15.2-01-PLAN.md (Task 1 — the surface-granular inventory note: multi-surface FILES are expanded into distinct SURFACE rows, FIX 5)
    - .planning/phases/15.2-theme-conformance-sweep-settings-ia/15.2-RESEARCH.md (Summary — token purity healthy [4 sanctioned literals in 3 files], the heavy dimensions are button-intent + fsSp + FFG; Pitfall 5 — fsSp false-negatives [trace val ...Sp = fsSp(...) defs, not the fontSize= line])
    - .planning/phases/15.2-theme-conformance-sweep-settings-ia/15.2-PATTERNS.md (Shared Patterns: token purity / fsSp discipline / button-intent — the audit standards + the sanctioned-literal carve-out list)
    - docs/ui_design/THEMING.md (the hardened 6+1 dimension standard from plan 05) + docs/ui_design/CLAUDE.md (button-intent grammar) + docs/ui_design/LAYOUT.md (FFG grammar + Back position)
    - The long-tail surface files: ui/calibration/* (CalibrationHubScreen, TiltScreen, BedMeshScreen, ProbeCalibrateScreen), ui/spool/SpoolScreen + SpoolPicker, ui/webcam/WebcamScreen, ui/macros/{SystemMacrosScreen,BookmarkedMacrosScreen}, ui/prompt/PromptDialog + PromptMarkupText, render/{GraphView,BedMeshHeatmapView,ProgressRing,WebcamView}
  </read_first>
  <action>
    First, PROVE the AUDIT.md row set is complete (MEDIUM-2): re-run the surface-inventory command recorded by
    plan 01 (the grep -rEl over ui/** + render/**) and diff its output against the AUDIT.md rows — EVERY
    enumerated surface MUST have a row (incl. the overlays/dialogs/drawer-chrome/scan-surface/prompt components
    the hand-list would miss). CRITICAL (LOW — surface granularity, FIX 5): the `grep -rEl` inventory is
    FILE-level, but a single file can host MULTIPLE distinct surfaces (a screen plus its dialogs/overlays/popups).
    The AUDIT row set EXPANDS multi-surface files into their distinct SURFACE rows — e.g. AppShell.kt expands
    into its overlay surfaces (MacroExecutionPopup, ScanSurface, PromptDialog, DevThemeCyclerOverlay), and the
    prompt file expands into PromptDialog vs PromptMarkupText (and any prompt image surface) — so coverage is
    proven at SURFACE granularity, not just file granularity. Add any missing surface row (file-level OR
    expanded-surface-level) before scoring.
    Then fill the remaining (long-tail) rows of 15.2-AUDIT.md: for each surface NOT scored in plan 05, score
    the 6+1 dimensions (Token-purity / Button-intent / FFG-grammar / >=64px / fsSp / dark-light-custom / 15.1-
    conformance) as check / ! / X with a one-line finding. Use the cycler widgets on-device for the visual
    dimensions where possible. CRITICAL carve-outs (do NOT flag as violations — record conformant):
    PromptMarkupText author-hex, SpoolPicker/Spoolman filament swatches, theme swatches are DATA per THEMING.md.
    When auditing fsSp, trace `val ...Sp = fsSp(...)` local definitions (Pitfall 5) — don't pattern-match the
    `fontSize=` line alone. Tag every non-conformant cell mechanical (Task 2 fixes) vs judgment-call (Task 3 flags).
    Finally run a LINT-STYLE CONFORMANCE CHECK (MEDIUM-2) over app/src/main/java/works/mees/dinghy/ui +
    .../render and record the results in AUDIT.md as a coverage-proof section:
      - grep -rn "Color(0x" over ui+render -> every hit must be on the sanctioned whitelist (PromptMarkupText
        author-hex, SpoolPicker/Spoolman filament swatches, ThemeEditor/Settings theme swatches) — any other
        hit is a token-purity violation tagged mechanical.
      - grep -rn "MaterialTheme.colorScheme" over ui+render -> MUST be zero (the app routes through LocalTokens, not M3 colorScheme).
      - grep -rn "\.sp\b" over ui+render cross-checked against fsSp( usage -> any raw .sp font size NOT flowing
        through fsSp is an fsSp-dimension violation tagged mechanical (mind Pitfall 5 false-negatives).
    The completed scorecard + the lint-check record are the durable D-12 artifact of record.
  </action>
  <verify>
    <automated>grep -c "calibration\|spool\|webcam\|macros\|prompt\|GraphView" .planning/phases/15.2-theme-conformance-sweep-settings-ia/15.2-AUDIT.md | head -1; grep -rn "MaterialTheme.colorScheme" app/src/main/java/works/mees/dinghy/ui app/src/main/java/works/mees/dinghy/render | grep -v '^#' | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - Every long-tail surface (calibration x4, spool, webcam, macros, prompt, render Views x4) has a scored row in AUDIT.md
    - The AUDIT row set matches the plan-01 surface-inventory command output EXPANDED to surface granularity (no enumerated surface missing; multi-surface files like AppShell + the prompt file are split into their distinct surface rows, e.g. AppShell overlays, PromptDialog vs PromptMarkupText) — recorded as a coverage-proof note
    - The lint-style check is recorded: every `Color(0x` hit is whitelisted-or-tagged-mechanical, `MaterialTheme.colorScheme` count is zero, and raw `.sp` outside `fsSp` is tagged mechanical
    - Each scored cell has a one-line finding + a mechanical/judgment tag; sanctioned literals recorded conformant (not flagged)
  </acceptance_criteria>
  <done>Every existing surface is scored at surface granularity, coverage is proven against the expanded inventory + lint check, findings tagged mechanical vs judgment.</done>
</task>

<task type="auto">
  <name>Task 2: Apply the mechanical conformance fixes — long-tail findings AND the core-screen mechanical findings from plan 05 (D-13 fix-silently)</name>
  <files>app/src/main/java/works/mees/dinghy/ui/calibration/CalibrationHubScreen.kt, app/src/main/java/works/mees/dinghy/ui/calibration/TiltScreen.kt, app/src/main/java/works/mees/dinghy/ui/calibration/BedMeshScreen.kt, app/src/main/java/works/mees/dinghy/ui/calibration/ProbeCalibrateScreen.kt, app/src/main/java/works/mees/dinghy/ui/spool/SpoolScreen.kt, app/src/main/java/works/mees/dinghy/ui/macros/SystemMacrosScreen.kt</files>
  <read_first>
    - .planning/phases/15.2-theme-conformance-sweep-settings-ia/15.2-REVIEWS.md (MEDIUM-1 — the fix pass includes the core-screen mechanical findings plan 05 tagged `mechanical (plan-06)`, not just Task-1's long-tail)
    - .planning/phases/15.2-theme-conformance-sweep-settings-ia/15.2-AUDIT.md (the mechanical-tagged findings — BOTH the Task-1 long-tail rows AND the plan-05 core-screen rows tagged `mechanical (plan-06)`)
    - .planning/phases/15.2-theme-conformance-sweep-settings-ia/15.2-PATTERNS.md (Shared Patterns — the token-purity / fsSp / button-intent reference values + the MoveScreen Neutral-Back + OutlinedControl Intent->token map)
    - docs/ui_design/THEMING.md + docs/ui_design/CLAUDE.md + docs/ui_design/LAYOUT.md (the standard each fix conforms to)
    - the specific surface files flagged mechanical in Task 1 + plan 05 (read the exact lines before editing)
  </read_first>
  <action>
    Apply EVERY mechanical-tagged finding recorded in AUDIT.md (D-13 fix-silently — clear-cut violations need no
    owner gate). The fix set is the UNION of: (a) the long-tail findings from Task 1, AND (b) the CORE-SCREEN
    findings plan 05 tagged `mechanical (plan-06)` (MEDIUM-1 — the core screens are NOT exempt; pick them up
    here alongside the long tail). Concretely, per finding: a stray raw `Color(0x...)` chrome literal -> the
    matching role token from THEMING.md; a wrong button-intent -> the correct Intent (the OutlinedControl map);
    a sub-15sp font -> `fsSp(<floor>f, t.fs)` (15 metadata / 17-18 body / 20-22 titles / 26 stats / 30+ focus
    per [[dinghy-font-sizes-too-small]]); a sub-64px target -> >=64dp; a non-conforming pool->role / shape-status
    usage -> the 15.1 canonical assignment. Do NOT touch the sanctioned-literal carve-outs. Update each fixed
    AUDIT.md cell to check. The files listed are the EXPECTED long-tail mechanical-fix set; the ACTUAL set is
    whatever Task 1 + plan 05 flagged mechanical (which may include core-screen files like ConsoleScreen,
    TemperatureScreen, etc.) — edit those too, and add any such file to files_modified at SUMMARY time. If a
    listed file had no findings, leave it untouched (drop it from files_modified at SUMMARY time).
  </action>
  <verify>
    <automated>cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --no-daemon" 2>&1 | tr -d '\r' | tail -6; cmd.exe /c "E:\Android\gw.bat :app:assembleDebug --no-daemon" 2>&1 | tr -d '\r' | tail -4</automated>
  </verify>
  <acceptance_criteria>
    - Every mechanical-tagged AUDIT.md finding — long-tail AND core-screen `mechanical (plan-06)` — is fixed (cell flipped to check) or explicitly re-tagged judgment-call
    - Full host unit suite GREEN (`:app:testDebugUnitTest`) — no regression
    - `:app:assembleDebug` BUILD SUCCESSFUL
    - No sanctioned-literal carve-out was altered (grep: PromptMarkupText author-hex, SpoolPicker swatches intact)
  </acceptance_criteria>
  <done>All mechanical conformance violations (long-tail + core) are fixed silently; the suite stays green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: On-device long-tail sweep + judgment-call decisions (CLOSED fix/verify loop)</name>
  <what-built>The completed app-wide conformance sweep: every surface scored in AUDIT.md, all mechanical violations (long-tail + core) fixed, and the long tail walked with the cycler widgets. This gate handles BOTH the D-13 JUDGMENT-CALL conflicts (new LAW vs a deliberate existing choice) AND a closed loop for any NEW mechanical gap the owner spots on-device (HIGH-6).</what-built>
  <how-to-verify>
    On flox (build + install; cyclers enabled):
    1. Walk the long-tail surfaces with the style + size cyclers across all combos: Calibration (hub + tilt + bed-mesh + probe-calibrate), Spool (list/picker/detail), Webcam, Macros, Prompt dialog, and confirm the render Views (GraphView/BedMeshHeatmap/ProgressRing/WebcamView) flip correctly.
    2. Confirm the mechanical fixes landed: no off-theme chrome, correct button intents, >=64px targets, fsSp floors (HARD eyeball — fonts not too small), dark/light/custom all correct, shape-status + pool->role conforming.
    3. Report TWO buckets:
       (a) Remaining MECHANICAL gaps (a clear token/intent/font/target violation you still see) — these are NOT judgment calls; the executor will fix them (Task 4's closed loop).
       (b) JUDGMENT-CALL decisions: for each judgment-call the AUDIT.md flagged (the executor will list them), decide keep-existing vs change-to-new-LAW.
    4. Confirm the sanctioned data swatches (filament colors, prompt author-hex, theme swatches) are untouched.
  </how-to-verify>
  <resume-signal>Type "approved" with decisions on each judgment-call (keep / change) AND the list of any remaining mechanical gaps (or "no remaining mechanical gaps"). If mechanical gaps remain, the executor fixes them and returns here to re-verify (HIGH-6 loop).</resume-signal>
</task>

<task type="auto">
  <name>Task 4: CLOSED loop — fix any owner-reported mechanical gaps (rebuild/reinstall/re-verify until none remain) + resolve judgment-calls + finalize AUDIT.md</name>
  <files>.planning/phases/15.2-theme-conformance-sweep-settings-ia/15.2-AUDIT.md</files>
  <read_first>
    - .planning/phases/15.2-theme-conformance-sweep-settings-ia/15.2-REVIEWS.md (HIGH-6 — the audit loop must CLOSE: new on-device mechanical gaps are fixed -> rebuilt -> reinstalled -> re-verified until the owner reports none)
    - .planning/phases/15.2-theme-conformance-sweep-settings-ia/15.2-AUDIT.md (the judgment-call rows awaiting the owner decision + any cells the owner re-opened on-device)
    - the resume-signal from Task 3 (the two buckets: remaining mechanical gaps + judgment-call decisions)
    - docs/ui_design/THEMING.md (if a judgment-call decision establishes a new criterion, record it here)
    - the surface files named in the owner's remaining-mechanical-gap list (read the exact lines before editing)
  </read_first>
  <action>
    HIGH-6 CLOSED LOOP — handle the Task-3 resume-signal in TWO parts:
    PART A (remaining mechanical gaps): for EACH mechanical gap the owner reported on-device, apply the fix
    (same mechanics as Task 2 — token/intent/fsSp/>=64px/15.1-assignment), then REBUILD + REINSTALL on flox
    (`:app:assembleDebug` or the signed release per the build env, `adb install -r`), and ask the owner to
    re-verify. REPEAT this fix -> build -> reinstall -> re-verify cycle until the owner reports
    "no remaining mechanical gaps." Do NOT finalize AUDIT.md while any mechanical gap is open — the loop is
    the gate (this is the closure the review (HIGH-6) requires; the checkpoint's resume-signal returns here
    each iteration).
    PART B (judgment-calls): apply the owner's Task-3 decisions — for each judgment-call the owner said
    "change," make the edit to the matching surface file and flip the AUDIT.md cell to check; for each "keep,"
    record the deliberate exception in AUDIT.md (and, if it establishes a standing rule, note it in THEMING.md).
    Finalize AUDIT.md only AFTER Part A reports zero remaining mechanical gaps and Part B is resolved: every
    surface row resolved (check / deliberate-exception), no open cells. The completed scorecard is the durable
    artifact of record for the phase.
  </action>
  <verify>
    <automated>cmd.exe /c "E:\Android\gw.bat :app:assembleDebug --no-daemon" 2>&1 | tr -d '\r' | tail -4</automated>
  </verify>
  <acceptance_criteria>
    - The owner reported "no remaining mechanical gaps" before AUDIT.md was finalized — the SUMMARY records how many fix/verify iterations the closed loop took (HIGH-6)
    - Every AUDIT.md row is resolved (check or a recorded deliberate-exception) — no open/unscored cells
    - Owner "change" decisions are implemented; "keep" decisions are recorded as exceptions
    - `:app:assembleDebug` BUILD SUCCESSFUL
  </acceptance_criteria>
  <done>The closed loop reached zero owner-reported mechanical gaps; judgment-calls resolved; AUDIT.md complete and final.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Conformance edits to UI chrome tokens/fonts/sizes + a tracked doc; no network/auth/data boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15.2-06-01 | Tampering | A conformance "fix" alters a sanctioned data swatch (filament color / author-hex) | mitigate | The carve-out list is explicit; Task 1 records them conformant; the lint check + a grep gate confirm PromptMarkupText/SpoolPicker swatches are untouched |
| T-15.2-06-02 | Tampering | A silent fix changes a deliberate design choice without owner consent | mitigate | D-13 conflict policy — judgment-calls are flagged to the owner (Task 3 gate), only mechanical violations are auto-fixed |
| T-15.2-06-03 | Tampering | An on-device mechanical gap ships unfixed because the audit loop never re-verified | mitigate | HIGH-6 closed loop — Task 4 fixes -> rebuilds -> reinstalls -> re-verifies until the owner reports none; AUDIT.md is not finalized while a gap is open |
| T-15.2-06-SC | Tampering | package installs | n/a | No package installs — internal-codebase only |

No high-severity threats — cosmetic conformance edits + a documentation scorecard; no attack surface.
</threat_model>

<verification>
- Full host unit suite `:app:testDebugUnitTest` GREEN; `:app:assembleDebug` BUILD SUCCESSFUL
- Every ~30-surface row in AUDIT.md scored + resolved; coverage proven against the surface-granular inventory + the lint check (MEDIUM-2/LOW)
- Sanctioned data swatches untouched
- Core-screen mechanical findings from plan 05 fixed in Task 2 (MEDIUM-1)
- On-device long-tail cycler walk + judgment-call decisions signed off; the closed fix/verify loop reached zero remaining mechanical gaps (HIGH-6)
</verification>

<success_criteria>
- All ~30 surfaces scored in the AUDIT.md scorecard — coverage provably complete via the surface-granular inventory + lint check (D-11/D-12/MEDIUM-2/LOW)
- Mechanical violations (long-tail + core) fixed silently; judgment-calls flagged + resolved with the owner (D-13/MEDIUM-1)
- The on-device audit closes — new mechanical gaps loop through fix/build/reinstall/re-verify until none remain (HIGH-6)
- Sanctioned data literals recorded conformant, not "fixed" (THEMING.md carve-out)
</success_criteria>

<output>
Create `.planning/phases/15.2-theme-conformance-sweep-settings-ia/15.2-06-SUMMARY.md` when done
</output>
