---
phase: 15.2-theme-conformance-sweep-settings-ia
plan: 03
type: execute
wave: 3
depends_on: ["15.2-01"]
files_modified:
  - app/src/main/java/works/mees/dinghy/config/Profile.kt
  - app/src/main/java/works/mees/dinghy/config/ProfileStore.kt
  - app/src/main/java/works/mees/dinghy/di/AppContainer.kt
  - app/src/main/java/works/mees/dinghy/ui/screen/PrintersScreen.kt
  - app/src/main/java/works/mees/dinghy/ui/screen/SettingsScreen.kt
  - app/src/test/java/works/mees/dinghy/config/ProfileToggleTest.kt
autonomous: true
requirements: [SET-01, CONN-01, CONN-02]
must_haves:
  truths:
    - "Printers (= today's Devices, renamed user-facing) owns printer add/remove/switch AND connection (host/port/key) — Connection moves OUT of Settings into Printers (D-02)"
    - "Per-profile feature toggles persist on the active Profile via writeScope + mutateActiveProfile and survive restart — NOT one app-wide set (D-04)"
    - "The absorbed Connection save preserves the existing host/port validation and the apiKey redaction (Profile.toString masks to ***) (V5/V7)"
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/config/Profile.kt"
      provides: "New per-profile feature-toggle field(s) on Profile + PersistedProfile + the to/fromPersisted round-trip"
      contains: "webcamEnabled"
    - path: "app/src/main/java/works/mees/dinghy/ui/screen/PrintersScreen.kt"
      provides: "The renamed/extended Devices screen that absorbs Connection editing"
      contains: "PrintersScreen"
    - path: "app/src/test/java/works/mees/dinghy/config/ProfileToggleTest.kt"
      provides: "Host-pure proof the per-profile toggle round-trips through ProfileStore.mutateActive"
  key_links:
    - from: "app/src/main/java/works/mees/dinghy/ui/screen/PrintersScreen.kt"
      to: "AppContainer.saveProfile / mutateActiveProfile"
      via: "Connection save routed through writeScope (NOT a composition scope)"
      pattern: "mutateActiveProfile|saveProfile|setActiveProfile"
    - from: "app/src/main/java/works/mees/dinghy/config/Profile.kt"
      to: "PersistedProfile"
      via: "toPersisted/fromPersisted carries the new toggle field"
      pattern: "toPersisted"
---

<objective>
Begin the Settings IA dissolve: rename Devices → Printers (user-facing), absorb Connection (host/port/key)
editing into it, and move per-profile feature toggles onto the active Profile (D-02/D-04). This is the
per-printer half of the four-tile split; plan 04 does the routing + the Theme/Settings/About split.

Purpose: Kill the Connection-redundant-with-Devices problem the owner flagged in Phase-15 UAT — Printers
becomes the single per-printer management surface.
Output: a new per-profile toggle field on Profile, the PrintersScreen (extended DevicesScreen), the
Connection-edit absorption with preserved validation/redaction, and host-pure toggle round-trip tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15.2-theme-conformance-sweep-settings-ia/15.2-CONTEXT.md
@.planning/phases/15.2-theme-conformance-sweep-settings-ia/15.2-RESEARCH.md
@.planning/phases/15.2-theme-conformance-sweep-settings-ia/15.2-PATTERNS.md
@.planning/phases/14-multi-printer-switching/14-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Per-profile feature-toggle field on Profile + ProfileStore round-trip + AppContainer setter</name>
  <files>app/src/main/java/works/mees/dinghy/config/Profile.kt, app/src/main/java/works/mees/dinghy/di/AppContainer.kt, app/src/test/java/works/mees/dinghy/config/ProfileToggleTest.kt</files>
  <read_first>
    - .planning/phases/15.2-theme-conformance-sweep-settings-ia/15.2-PATTERNS.md (the per-profile toggle persist pattern: copy the setActive* intent-helper + the theme-tuple field shape across ALL THREE — Profile, PersistedProfile, to/fromPersisted)
    - app/src/main/java/works/mees/dinghy/config/Profile.kt (PersistedProfile ~28-55; Profile data class ~64-78; toPersisted ~107-121; fromPersisted ~134-148; the apiKey-redacting toString at 51 and 123)
    - app/src/main/java/works/mees/dinghy/config/ProfileStore.kt (mutateActive ~109-118)
    - app/src/main/java/works/mees/dinghy/di/AppContainer.kt (mutateActiveProfile ~130; setActiveDark ~372-375 as the per-profile setter template; writeScope ~107)
    - app/src/test/java/works/mees/dinghy/config/ProfileStoreTest.kt (existing host-pure ProfileStore test shape to extend/mirror)
  </read_first>
  <behavior>
    - Test toggle_round_trips_through_mutate_active: a new per-profile toggle (webcamEnabled) defaults to its existing sensible default on a fresh Profile, persists via mutateActive { it.copy(webcamEnabled = …) }, and survives toPersisted→fromPersisted (reload) with the value intact
    - Test toggle_is_per_profile_not_global: two profiles can hold different toggle values simultaneously (toggling profile A does not change profile B)
  </behavior>
  <action>
    Add a per-profile feature-toggle field to `Profile` (D-04). Use the webcam toggle as the concrete first
    toggle (matches the Phase-14 webcam-pref re-keying by profileId; the owner's example: "webcam on the
    printer that has a camera, off on the other") — add `val webcamEnabled: Boolean = <existing default>`,
    confirming the current default by checking how webcam enablement is read today and preserving it. Add the
    SAME field to `PersistedProfile` and thread it through BOTH `toPersisted()` and `fromPersisted()` so it
    round-trips. In `AppContainer`, add a per-profile setter mirroring `setActiveDark`:
    `fun setActiveWebcamEnabled(on: Boolean) { mutateActiveProfile { it.copy(webcamEnabled = on) } }` —
    durable + lost-update-safe via the read-modify-write inside mutateActive
    ([[dinghy-compose-write-scope-cancellation]]; NEVER a composition scope). If webcam enablement is
    currently read from a separate per-profileId pref, reconcile to the single Profile field (do not leave
    two sources of truth) while preserving the existing default. Flesh out `ProfileToggleTest` to the
    behaviors above.
  </action>
  <verify>
    <automated>cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --tests works.mees.dinghy.config.ProfileToggleTest --tests works.mees.dinghy.config.ProfileStoreTest --no-daemon" 2>&1 | tr -d '\r' | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `ProfileToggleTest` GREEN (round-trips through mutateActive, per-profile not global)
    - `ProfileStoreTest` still GREEN (no regression)
    - `Profile.kt` carries the new toggle field on Profile + PersistedProfile + both to/fromPersisted
    - `AppContainer.kt` has `setActiveWebcamEnabled` routing through `mutateActiveProfile` (no composition scope)
  </acceptance_criteria>
  <done>The per-profile feature toggle persists durably, round-trips on reload, and is per-profile not app-wide.</done>
</task>

<task type="auto">
  <name>Task 2: PrintersScreen (extended DevicesScreen) absorbs Connection editing; SettingsScreen sheds Connection</name>
  <files>app/src/main/java/works/mees/dinghy/ui/screen/PrintersScreen.kt, app/src/main/java/works/mees/dinghy/ui/screen/SettingsScreen.kt, app/src/main/java/works/mees/dinghy/config/ProfileStore.kt</files>
  <read_first>
    - .planning/phases/15.2-theme-conformance-sweep-settings-ia/15.2-PATTERNS.md (PrintersScreen section: keep DevicesScreen verbatim as the base, absorb Connection from SettingsScreen; the switch-persist copy-verbatim block; the SECURITY V7 apiKey redaction + V5 validator reuse; the keep-Dest.Devices-enum rename recommendation)
    - app/src/main/java/works/mees/dinghy/ui/screen/DevicesScreen.kt (the base: add/delete/switch grid ~73-134; the gutter Back ~123-131; setActiveProfile + onSwitched at ~101-115)
    - app/src/main/java/works/mees/dinghy/ui/screen/SettingsScreen.kt (the Connection section — host/port/apiKey edit fields + the save handler + the host/port validators; copy these OUT, then delete them from Settings)
    - app/src/main/java/works/mees/dinghy/config/Profile.kt (apiKey redaction at 51/123 — preserve; host/port/apiKey fields)
    - app/src/main/java/works/mees/dinghy/di/AppContainer.kt (saveProfile ~116, mutateActiveProfile ~130, setActiveProfile ~110 — the writeScope persist helpers)
  </read_first>
  <action>
    Create `PrintersScreen.kt` by copying `DevicesScreen.kt`'s add/delete/switch grid VERBATIM as the base
    (keep the `Dest.Devices` enum constant per RESEARCH Open-Q2 — minimize churn; only the user-facing
    LABEL changes to "Printers" in plan 04). Then ABSORB the Connection (host/port/key) editing currently in
    `SettingsScreen.kt`: move the host/port/apiKey edit fields + their save handler into the per-printer edit
    flow on PrintersScreen, persisting via `AppContainer.saveProfile`/`mutateActiveProfile` (the writeScope
    path) — NEVER `rememberCoroutineScope().launch { profileStore… }` ([[dinghy-compose-write-scope-cancellation]]).
    CRITICAL (RESEARCH V5/V7): carry over the EXISTING host/port validators verbatim (do not drop them in
    the move) and preserve the `apiKey` redaction (it stays masked in toString/logs; never surface the raw
    key in the Printers UI). Then DELETE the Connection section from `SettingsScreen.kt` (it now lives in
    Printers). Leave the Theme entry + the feature-toggles in SettingsScreen for plan 04 to split. Keep the
    switch-persist block verbatim: `container.setActiveProfile(profile.id); onSwitched()`.
    If you want to rename the `Dest.Devices` enum identifier to `Dest.Printers` for cleanliness, that is a
    D-13 JUDGMENT CALL — flag it to the owner rather than doing it silently (the recommendation is to keep
    the enum and rename only the label in plan 04).
  </action>
  <verify>
    <automated>cmd.exe /c "E:\Android\gw.bat :app:assembleDebug --no-daemon" 2>&1 | tr -d '\r' | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `PrintersScreen.kt` exists with the Devices add/delete/switch grid + the absorbed host/port/apiKey edit + save
    - The Connection save routes through `saveProfile`/`mutateActiveProfile` (grep: no `rememberCoroutineScope` persisting profile data in PrintersScreen)
    - The host/port validators + apiKey redaction are preserved (the same validator identifiers from SettingsScreen present in PrintersScreen; apiKey never rendered raw)
    - `SettingsScreen.kt` no longer contains the host/port/apiKey Connection fields
    - `:app:assembleDebug` BUILD SUCCESSFUL
  </acceptance_criteria>
  <done>Printers owns connection editing with validation + redaction preserved; Settings no longer carries Connection.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user text entry (host/port/key) → Profile persist | The one input-validation surface this phase touches; moves from Settings to Printers |
| Profile → logs/UI | apiKey must stay redacted when Connection editing relocates |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15.2-03-01 | Tampering | Host/port validation dropped during the Settings→Printers Connection move | mitigate | Carry over the EXISTING validators verbatim (V5); a build-time check the host/port parse path is present in PrintersScreen |
| T-15.2-03-02 | Information Disclosure | API key leaked when Connection editing moves into Printers | mitigate | Preserve `apiKey` redaction (Profile.toString masks to ***, Profile.kt:51/123); never surface the raw key in Printers UI/logs (V7) |
| T-15.2-03-03 | Tampering | Per-profile toggle lost-update on the multithreaded writeScope | mitigate | Toggle write is a read-modify-write inside `mutateActiveProfile` (atomic mutateActive), per the Phase-14 WR-01 lesson |
| T-15.2-03-SC | Tampering | package installs | n/a | No package installs — internal-codebase only |

No high-severity threats — no new network/auth surface; the move preserves existing validation + redaction.
</threat_model>

<verification>
- `:app:testDebugUnitTest --tests works.mees.dinghy.config.ProfileToggleTest --tests works.mees.dinghy.config.ProfileStoreTest` GREEN
- `:app:assembleDebug` BUILD SUCCESSFUL
- PrintersScreen carries the Connection edit with validators + redaction; SettingsScreen no longer carries Connection
- No composition-scope persist of profile data in PrintersScreen
</verification>

<success_criteria>
- Printers owns add/remove/switch + Connection (D-02); Connection is gone from Settings
- Per-profile feature toggles persist durably and per-profile (D-04)
- Validation + apiKey redaction preserved through the move (V5/V7)
</success_criteria>

<output>
Create `.planning/phases/15.2-theme-conformance-sweep-settings-ia/15.2-03-SUMMARY.md` when done
</output>
