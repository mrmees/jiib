---
phase: 15.2-theme-conformance-sweep-settings-ia
plan: 03
type: execute
wave: 3
depends_on: ["15.2-01"]
files_modified:
  - app/src/main/java/works/mees/dinghy/config/Profile.kt
  - app/src/main/java/works/mees/dinghy/config/ProfileStore.kt
  - app/src/main/java/works/mees/dinghy/di/AppContainer.kt
  - app/src/main/java/works/mees/dinghy/ui/screen/PrintersScreen.kt
  - app/src/main/java/works/mees/dinghy/ui/screen/SettingsScreen.kt
  - app/src/test/java/works/mees/dinghy/config/ProfileToggleTest.kt
autonomous: true
requirements: [SET-01, CONN-01, CONN-02]
must_haves:
  truths:
    - "Printers (= today's Devices, renamed user-facing) owns printer add/remove/switch AND connection (host/port/key) — Connection moves OUT of Settings into Printers (D-02)"
    - "Per-profile feature toggles persist on the active Profile via writeScope + mutateActiveProfile and survive restart — NOT one app-wide set (D-04)"
    - "The `webcamEnabled` per-profile toggle GATES the Webcam drawer tile/surface (the tile is live only when the printer has cams AND the toggle is on) — it is wired to behavior, not just persisted (MEDIUM-4)"
    - "The absorbed Connection save preserves the existing host/port validation and the apiKey redaction (Profile.toString masks to ***) (V5/V7)"
    - "Connection API-key edit semantics are explicit: a BLANK field PRESERVES the existing key; an explicit Clear removes it (writes null); a non-blank entry REPLACES it and is masked in the field — never round-trips the raw stored key into the UI (MEDIUM-5/V7)"
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/config/Profile.kt"
      provides: "New per-profile feature-toggle field(s) on Profile + PersistedProfile + the to/fromPersisted round-trip"
      contains: "webcamEnabled"
    - path: "app/src/main/java/works/mees/dinghy/di/AppContainer.kt"
      provides: "setActiveWebcamEnabled + the webcamTileEnabled gating flow (webcamCount>0 AND active profile webcamEnabled)"
      contains: "setActiveWebcamEnabled"
    - path: "app/src/main/java/works/mees/dinghy/ui/screen/PrintersScreen.kt"
      provides: "The renamed/extended Devices screen that absorbs Connection editing"
      contains: "PrintersScreen"
    - path: "app/src/test/java/works/mees/dinghy/config/ProfileToggleTest.kt"
      provides: "Host-pure proof the per-profile toggle round-trips through ProfileStore.mutateActive + the apiKey blank-preserves/clear/replace semantics"
  key_links:
    - from: "app/src/main/java/works/mees/dinghy/ui/screen/PrintersScreen.kt"
      to: "AppContainer.saveProfile / mutateActiveProfile"
      via: "Connection save routed through writeScope (NOT a composition scope)"
      pattern: "mutateActiveProfile|saveProfile|setActiveProfile"
    - from: "app/src/main/java/works/mees/dinghy/di/AppContainer.kt"
      to: "webcamTileEnabled gating flow"
      via: "combine(webcamCount, activeProfile) — count>0 AND profile.webcamEnabled"
      pattern: "webcamTileEnabled"
    - from: "app/src/main/java/works/mees/dinghy/config/Profile.kt"
      to: "PersistedProfile"
      via: "toPersisted/fromPersisted carries the new toggle field"
      pattern: "toPersisted"
---

<objective>
Begin the Settings IA dissolve: rename Devices → Printers (user-facing), absorb Connection (host/port/key)
editing into it, and move per-profile feature toggles onto the active Profile (D-02/D-04). This is the
per-printer half of the four-tile split; plan 04 does the routing + the Theme/Settings/About split.

Per the cross-AI review: the `webcamEnabled` toggle is WIRED to actually gate the Webcam tile/surface
(MEDIUM-4 — not just persisted), the Connection API-key edit has explicit blank-preserves / clear /
masked-replace semantics (MEDIUM-5), and PrintersScreen avoids a dead verbatim DevicesScreen duplicate
(LOW-1 — rename-in-place / no orphaned divergent screen body).

Purpose: Kill the Connection-redundant-with-Devices problem the owner flagged in Phase-15 UAT — Printers
becomes the single per-printer management surface.
Output: a new per-profile toggle field on Profile, the webcam gating flow, the PrintersScreen (extended
DevicesScreen), the Connection-edit absorption with preserved validation/redaction + explicit key
semantics, and host-pure toggle round-trip tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15.2-theme-conformance-sweep-settings-ia/15.2-CONTEXT.md
@.planning/phases/15.2-theme-conformance-sweep-settings-ia/15.2-RESEARCH.md
@.planning/phases/15.2-theme-conformance-sweep-settings-ia/15.2-PATTERNS.md
@.planning/phases/15.2-theme-conformance-sweep-settings-ia/15.2-REVIEWS.md
@.planning/phases/14-multi-printer-switching/14-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Per-profile webcamEnabled field on Profile + ProfileStore round-trip + setActiveWebcamEnabled + the webcamTileEnabled gating flow + apiKey-semantics helper</name>
  <files>app/src/main/java/works/mees/dinghy/config/Profile.kt, app/src/main/java/works/mees/dinghy/di/AppContainer.kt, app/src/test/java/works/mees/dinghy/config/ProfileToggleTest.kt</files>
  <read_first>
    - .planning/phases/15.2-theme-conformance-sweep-settings-ia/15.2-REVIEWS.md (MEDIUM-4 webcamEnabled gates behavior; MEDIUM-5 apiKey blank-preserves/clear/masked-replace)
    - .planning/phases/15.2-theme-conformance-sweep-settings-ia/15.2-PATTERNS.md (the per-profile toggle persist pattern: copy the setActive* intent-helper + the theme-tuple field shape across ALL THREE — Profile, PersistedProfile, to/fromPersisted)
    - app/src/main/java/works/mees/dinghy/config/Profile.kt (PersistedProfile :25-56; Profile data class :64-78; toPersisted :107-121; fromPersisted :134-148; the apiKey-redacting toString at :51 and :123-127; the apiKey field :28/:69)
    - app/src/main/java/works/mees/dinghy/config/ProfileStore.kt (mutateActive ~109-118)
    - app/src/main/java/works/mees/dinghy/di/AppContainer.kt (mutateActiveProfile :130; setActiveDark :372-375 as the per-profile setter template; writeScope :107; activeProfile :140; webcamCount :236 + webcams :228 — the CURRENT capability-only gate; the AppShell `webcamEnabled = webcamCount > 0` consumer at AppShell.kt:183)
    - app/src/test/java/works/mees/dinghy/config/ProfileStoreTest.kt (existing host-pure ProfileStore test shape to extend/mirror)
  </read_first>
  <behavior>
    - Test toggle_round_trips_through_mutate_active: `webcamEnabled` defaults to its existing sensible default (true — preserve current always-on behavior) on a fresh Profile, persists via mutateActive { it.copy(webcamEnabled = …) }, and survives toPersisted→fromPersisted (reload) with the value intact
    - Test toggle_is_per_profile_not_global: two profiles can hold different `webcamEnabled` values simultaneously (toggling profile A does not change profile B)
    - Test webcam_tile_gating_requires_both_count_and_toggle (MEDIUM-4): the pure gating predicate is `count > 0 && profile.webcamEnabled` — assert it is false when count==0 (regardless of toggle), false when toggle off (regardless of count), true only when both hold
    - Test apikey_blank_preserves_existing (MEDIUM-5): the pure apiKey-resolve helper, given (existingKey="abc", fieldInput=blank, cleared=false), returns "abc" (unchanged); given (existing="abc", fieldInput="xyz", cleared=false) returns "xyz" (replace); given (existing="abc", cleared=true) returns null (clear)
  </behavior>
  <action>
    Add a per-profile feature-toggle field to `Profile` (D-04): `val webcamEnabled: Boolean = true` (default
    TRUE to preserve today's always-available behavior — confirm by reading how webcam enablement is read
    today; the current gate is capability-only `webcamCount > 0`, so defaulting the new toggle to true keeps
    existing printers unchanged). Add the SAME field to `PersistedProfile` and thread it through BOTH
    `toPersisted()` and `fromPersisted()` so it round-trips. In `AppContainer`, add a per-profile setter
    mirroring `setActiveDark`:
    `fun setActiveWebcamEnabled(on: Boolean) { mutateActiveProfile { it.copy(webcamEnabled = on) } }` —
    durable + lost-update-safe via the read-modify-write inside mutateActive
    ([[dinghy-compose-write-scope-cancellation]]; NEVER a composition scope).
    WIRE THE TOGGLE TO BEHAVIOR (MEDIUM-4): add a gating flow
    `val webcamTileEnabled: Flow<Boolean> = combine(webcamCount, activeProfile) { count, p -> count > 0 && (p?.webcamEnabled ?: true) }`
    — this REPLACES the bare `webcamCount > 0` as the Webcam-tile/surface gate. (Plan 04 repoints the
    AppShell `webcamEnabled = webcamCount > 0` consumer at AppShell.kt:183 to collect `container.webcamTileEnabled`
    so the toggle actually greys the tile; this task provides the flow + the pure predicate the test asserts.)
    Extract the gating predicate as a PURE testable function (e.g. `fun webcamTileGate(count: Int, webcamEnabled: Boolean): Boolean = count > 0 && webcamEnabled`) so the host test can assert it.
    DEFINE THE API-KEY EDIT SEMANTICS (MEDIUM-5): add a PURE helper
    `fun resolveApiKeyEdit(existing: String?, fieldInput: String, cleared: Boolean): String?` returning:
    `null` when `cleared` (explicit Clear), `existing` when `fieldInput.isBlank() && !cleared` (blank PRESERVES
    the stored key), else `fieldInput` (non-blank REPLACES). Task 2's Connection-save uses this so a user who
    opens the editor and saves without retyping the key does NOT wipe it, and a deliberate Clear is distinct.
    If webcam enablement is currently read from a separate per-profileId pref, reconcile to the single Profile
    field (do not leave two sources of truth) while preserving the existing default. Flesh out
    `ProfileToggleTest` to the behaviors above.
  </action>
  <verify>
    <automated>cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --tests works.mees.dinghy.config.ProfileToggleTest --tests works.mees.dinghy.config.ProfileStoreTest --no-daemon" 2>&1 | tr -d '\r' | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `ProfileToggleTest` GREEN (round-trips through mutateActive, per-profile not global, the count×toggle gate predicate, the apiKey blank-preserves/clear/replace helper)
    - `ProfileStoreTest` still GREEN (no regression)
    - `Profile.kt` carries `webcamEnabled` on Profile + PersistedProfile + both to/fromPersisted (default true)
    - `AppContainer.kt` has `setActiveWebcamEnabled` (via `mutateActiveProfile`), a `webcamTileEnabled` flow combining count×toggle, and the pure `webcamTileGate` + `resolveApiKeyEdit` helpers
  </acceptance_criteria>
  <done>The per-profile webcam toggle persists durably + per-profile, gates the tile via a real flow, and the apiKey edit semantics are defined as a pure helper.</done>
</task>

<task type="auto">
  <name>Task 2: PrintersScreen (renamed/extended Devices, no dead duplicate) absorbs Connection editing; SettingsScreen sheds Connection</name>
  <files>app/src/main/java/works/mees/dinghy/ui/screen/PrintersScreen.kt, app/src/main/java/works/mees/dinghy/ui/screen/SettingsScreen.kt, app/src/main/java/works/mees/dinghy/config/ProfileStore.kt</files>
  <read_first>
    - .planning/phases/15.2-theme-conformance-sweep-settings-ia/15.2-REVIEWS.md (LOW-1 — prefer rename-in-place / extract-shared over a verbatim copy; no orphaned DevicesScreen; keep Dest.Devices enum. MEDIUM-5 — apiKey semantics. V5/V7 — validation + redaction)
    - .planning/phases/15.2-theme-conformance-sweep-settings-ia/15.2-PATTERNS.md (PrintersScreen section: the Connection absorb from SettingsScreen; the switch-persist copy-verbatim block; the SECURITY V7 apiKey redaction + V5 validator reuse; the keep-Dest.Devices-enum rename recommendation)
    - app/src/main/java/works/mees/dinghy/ui/screen/DevicesScreen.kt (the base: add/delete/switch grid ~73-134; the gutter Back ~123-131; setActiveProfile + onSwitched at ~101-115)
    - app/src/main/java/works/mees/dinghy/ui/screen/SettingsScreen.kt (the Connection section — host/port/apiKey edit fields + the save handler + the host/port validators; extract these, then delete them from Settings)
    - app/src/main/java/works/mees/dinghy/config/Profile.kt (apiKey redaction at :51/:123 — preserve; host/port/apiKey fields; the new resolveApiKeyEdit helper from Task 1)
    - app/src/main/java/works/mees/dinghy/di/AppContainer.kt (saveProfile :115, mutateActiveProfile :130, setActiveProfile :110 — the writeScope persist helpers)
  </read_first>
  <action>
    Create `PrintersScreen.kt` as the RENAMED Devices screen (LOW-1): do NOT leave a second divergent screen
    body. Preferred path — RENAME the existing `DevicesScreen` composable to `PrintersScreen` in place (the
    function and file) so there is ONE screen, and update the single AppShell call site in plan 04. If the
    executor instead keeps `DevicesScreen.kt` as a thin shared base, it MUST be referenced (not orphaned) and
    must not diverge — verify at SUMMARY time that no unreferenced `DevicesScreen` composable remains
    (`grep -rn "fun DevicesScreen"` resolves to either the renamed function or a still-called shared piece,
    never a dead copy). Keep the `Dest.Devices` enum constant (per RESEARCH Open-Q2 — minimize churn; only
    the user-facing LABEL changes to "Printers" in plan 04).
    Then ABSORB the Connection (host/port/key) editing currently in `SettingsScreen.kt`: move the host/port/
    apiKey edit fields + their save handler into the per-printer edit flow on PrintersScreen, persisting via
    `AppContainer.saveProfile`/`mutateActiveProfile` (the writeScope path) — NEVER
    `rememberCoroutineScope().launch { profileStore… }` ([[dinghy-compose-write-scope-cancellation]]).
    CRITICAL (RESEARCH V5/V7): carry over the EXISTING host/port validators verbatim (do not drop them in the
    move). API-KEY semantics (MEDIUM-5/V7): the apiKey edit field starts EMPTY (or shows a masked placeholder
    like "•••• set" when a key exists) — NEVER pre-fill the raw stored key into the TextField. On save, resolve
    the key via `resolveApiKeyEdit(existing = profile.apiKey, fieldInput = field, cleared = clearPressed)` from
    Task 1: blank field + no Clear → keep the existing key; an explicit Clear button → write null; a non-blank
    entry → replace. The Profile.toString redaction (masks to ***) is preserved unchanged. Then DELETE the
    Connection section from `SettingsScreen.kt` (it now lives in Printers). Leave the Theme entry + the feature
    toggles in SettingsScreen for plan 04 to split. Keep the switch-persist block verbatim:
    `container.setActiveProfile(profile.id); onSwitched()`.
    If you want to rename the `Dest.Devices` enum identifier to `Dest.Printers` for cleanliness, that is a
    D-13 JUDGMENT CALL — flag it to the owner rather than doing it silently (the recommendation is to keep the
    enum and rename only the label in plan 04).
  </action>
  <verify>
    <automated>cmd.exe /c "E:\Android\gw.bat :app:assembleDebug --no-daemon" 2>&1 | tr -d '\r' | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `PrintersScreen.kt` exists (renamed-in-place from Devices) with the add/delete/switch grid + the absorbed host/port/apiKey edit + save; NO orphaned divergent `DevicesScreen` composable remains (grep)
    - The Connection save routes through `saveProfile`/`mutateActiveProfile` (grep: no `rememberCoroutineScope` persisting profile data in PrintersScreen)
    - The host/port validators are preserved (the same validator identifiers from SettingsScreen present in PrintersScreen)
    - The apiKey field is never pre-filled with the raw stored key; save resolves via `resolveApiKeyEdit` (blank preserves / Clear nulls / non-blank replaces); apiKey stays masked in toString (MEDIUM-5/V7)
    - `SettingsScreen.kt` no longer contains the host/port/apiKey Connection fields
    - `:app:assembleDebug` BUILD SUCCESSFUL
  </acceptance_criteria>
  <done>Printers owns connection editing with validation + redaction + explicit key semantics preserved; Settings no longer carries Connection; no dead Devices duplicate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user text entry (host/port/key) → Profile persist | The one input-validation surface this phase touches; moves from Settings to Printers |
| Profile → logs/UI | apiKey must stay redacted when Connection editing relocates; the edit field must never round-trip the raw key |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15.2-03-01 | Tampering | Host/port validation dropped during the Settings→Printers Connection move | mitigate | Carry over the EXISTING validators verbatim (V5); a build-time check the host/port parse path is present in PrintersScreen |
| T-15.2-03-02 | Information Disclosure | API key leaked when Connection editing moves into Printers — raw key surfaced in the edit field or a log | mitigate | Preserve `apiKey` redaction (Profile.toString masks to ***, Profile.kt:51/123); the edit field NEVER pre-fills the raw stored key (shows empty/masked-placeholder); blank-preserves via `resolveApiKeyEdit` so a save without retyping does not need the raw key in the UI (V7/MEDIUM-5) |
| T-15.2-03-03 | Tampering | API key silently WIPED when the user opens the editor and saves without retyping it | mitigate | `resolveApiKeyEdit` blank-field path returns the EXISTING key (preserve); only an explicit Clear writes null — host-tested (MEDIUM-5) |
| T-15.2-03-04 | Tampering | Per-profile toggle lost-update on the multithreaded writeScope | mitigate | Toggle write is a read-modify-write inside `mutateActiveProfile` (atomic mutateActive), per the Phase-14 WR-01 lesson |
| T-15.2-03-SC | Tampering | package installs | n/a | No package installs — internal-codebase only |

No high-severity threats — no new network/auth surface; the move preserves existing validation + redaction and tightens key-edit semantics.
</threat_model>

<verification>
- `:app:testDebugUnitTest --tests works.mees.dinghy.config.ProfileToggleTest --tests works.mees.dinghy.config.ProfileStoreTest` GREEN
- `:app:assembleDebug` BUILD SUCCESSFUL
- PrintersScreen carries the Connection edit with validators + redaction + explicit key semantics; SettingsScreen no longer carries Connection
- No composition-scope persist of profile data in PrintersScreen; no orphaned DevicesScreen duplicate
- `webcamTileEnabled` flow exists (count×toggle); the pure gate + apiKey-resolve helpers are host-tested
</verification>

<success_criteria>
- Printers owns add/remove/switch + Connection (D-02); Connection is gone from Settings
- Per-profile feature toggles persist durably and per-profile (D-04) and the webcam toggle GATES the tile (MEDIUM-4)
- Validation + apiKey redaction preserved through the move, with explicit blank-preserves/clear/replace semantics (V5/V7/MEDIUM-5)
- No dead Devices duplicate (LOW-1)
</success_criteria>

<output>
Create `.planning/phases/15.2-theme-conformance-sweep-settings-ia/15.2-03-SUMMARY.md` when done
</output>
