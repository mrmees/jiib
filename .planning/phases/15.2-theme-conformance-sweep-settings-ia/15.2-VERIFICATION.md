---
phase: 15.2-theme-conformance-sweep-settings-ia
verified: 2026-06-05T00:00:00Z
status: passed
score: 28/28
overrides_applied: 0
---

# Phase 15.2: Theme Conformance Sweep & Settings IA — Verification Report

**Phase Goal:** Heavy audit of every existing surface against the now-reconciled theme LAW — token
purity, button-intent color, Focus/Field/Gutter grammar, ≥64px touch targets, the fsSp font scale,
dark/light/custom correctness; plus resolve the Settings-vs-Devices boundary. Also delivered: the
transient theme-override layer + dev cyclers, the four-tile Settings IA dissolve (Printers · Theme ·
Settings · About), an app-wide Back-intent sweep, a D-10 guided core-screen review that hardened
the LAW (criteria C1-C7 into THEMING.md/LAYOUT.md), and the AUDIT.md scorecard with mechanical
fixes applied and judgment calls/redesigns deferred.

**Verified:** 2026-06-05
**Status:** PASSED
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | The whole live app re-themes when a transient override is set; snaps back when cleared; writes nothing to DataStore (D-06/D-08) | VERIFIED | `ThemeOverride.kt` + `AppContainer.effectiveTokens` — pure in-memory override; `ThemeOverrideTest` 9/9 including `override_writes_no_persistence` + `dismiss_restores_persisted_no_drift`. No DataStore.edit in the override code path (grep-clean confirmed in SUMMARY-01). |
| 2 | `effectiveTokens` is a PURE derivation that bakes in BOTH branches, never reads `themeResolver.tokens`, re-emits whenever `activeThemeTuple` changes (HIGH-1) | VERIFIED | `AppContainer.kt:410-416`: `combine(activeThemeTuple, _themeOverride, devCyclerEnabled) { base, ov, devOn -> ... themeResolver.bake(tuple) }`. The CRITICAL comment at line 400 documents it "NEVER reads themeResolver.tokens". `ThemeOverrideTest.persisted_change_propagates_through_effectivetokens` GREEN. |
| 3 | Override bakes from a single canonical `activeThemeTuple` flow (same as seedTheme) — no profile skew (HIGH-1) | VERIFIED | `AppContainer.kt:385-399`: `val activeThemeTuple: Flow<ThemePrefs.ThemeTuple>` is the single source; both `seedTheme()` and `effectiveTokens` derive from it. `ThemeOverrideTest.override_idle_no_profile_bakes_from_themeprefs_tuple` GREEN. |
| 4 | Override baking is PURE — `ThemeResolver.bake(tuple)` returns a snapshot `ThemeTokens` and mutates no shared field (HIGH-2) | VERIFIED | `ThemeResolver.kt:162-179`: `fun bake(tuple)` calls `computeFrom(...)` extracted private helper; reads no `var` field, never assigns `_tokens`. `ThemeResolverBakeTest.bake_is_pure_leaves_resolver_tokens_untouched` + `bake_matches_apply_for_same_tuple` GREEN. |
| 5 | Override carries the FULL active ThemeTuple with ONLY the cycled axes substituted — no field dropped (HIGH-3) | VERIFIED | `ThemeOverride.kt:24-28`: `mergeOnto` uses `base.copy(dark=..., paletteMode=..., fs=...)` — `base.copy` passes all other fields verbatim. `ThemeOverrideTest.override_carries_full_tuple_status_override_survives_round_trip` GREEN. |
| 6 | An active override steps ATOMICALLY via `updateThemeOverride(transform)` via `MutableStateFlow.update` — rapid taps cannot lose an axis (MEDIUM) | VERIFIED | `AppContainer.kt:228-231`: `fun updateThemeOverride(transform) { _themeOverride.update(transform) }`. AppShell wires cycler taps as `container.updateThemeOverride { nextStyleOverride(it) }` (line 742). `ThemeOverrideTest.update_theme_override_steps_atomically` GREEN. |
| 7 | When dev-enable goes false, the active override is cleared so the app cannot be stranded in an overridden look (HIGH-5) | VERIFIED | `AppContainer.kt:238`: `setDevCyclerEnabled(false)` calls `setThemeOverride(null)` + the `!devOn` gate in `effectiveTokens`. `ThemeOverrideTest.disable_dev_clears_or_ignores_override` GREEN. |
| 8 | The dev-widget enable is a persisted app-global boolean defaulting false, readable in a RELEASE build — NOT `BuildConfig.DEBUG` (D-08) | VERIFIED | `ThemePrefs.kt:117-126`: `val devEnableFlow` mapped from `KEY_DEV_ENABLE` defaulting `?: false`. No `BuildConfig.DEBUG` gate (comments at lines 107, 178 in ThemePrefs + AppContainer:195 confirm this is intentional). `DevWidgetEnableTest.dev_enable_defaults_false_persists_app_global` GREEN. |
| 9 | The plan-02 TEMP `devEnableFlow`-default-true flip is fully reverted — no `// TEMP(15.2-02)` marker anywhere in `app/src/` (MEDIUM-3) | VERIFIED | `grep -rn "TEMP(15.2-02)" app/src/` returns empty. Commit `401c4db` is the revert. `ThemePrefs.devEnableFlow` default is `?: false`. |
| 10 | Style cycler steps exactly 6 {dark,light}×{Colorful,Simple,High-Contrast} combos with the user's real seed; size cycler toggles S/M/L; both axes are runtime-independent via merge-onto-current (D-06/D-07/HIGH-4) | VERIFIED | `DevThemeCyclerOverlay.kt:98-125`: `nextStyleOverride` / `nextSizeOverride` each use `(current ?: ThemeOverride()).copy(...)` advancing only their axis, carrying the other axis forward. `StyleCyclerTest` (incl. `style_advance_preserves_active_size_override`) + `TextSizeCyclerTest` (incl. `size_advance_preserves_active_style_override`) GREEN. |
| 11 | The AppShell overlay wires cycler taps through ATOMIC `updateThemeOverride` — NOT `setThemeOverride(nextStyleOverride(capturedSnapshot))` | VERIFIED | `AppShell.kt:742-743`: `onCycleStyle = { container.updateThemeOverride { nextStyleOverride(it) } }` / `onCycleSize = { container.updateThemeOverride { nextSizeOverride(it) } }`. The captured-snapshot anti-pattern is absent (grep confirms no `setThemeOverride(nextStyle`). |
| 12 | Printers (= renamed DevicesScreen) owns printer add/remove/switch AND connection (host/port/key) — Connection is OUT of Settings (D-02) | VERIFIED | `PrintersScreen.kt` exists with Connection editing absorbed; `SettingsScreen.kt` grep for `host`/`port`/`apiKey` returns empty (no connection fields). `DevicesScreen.kt` does not exist (file-not-found confirmed). No orphaned `fun DevicesScreen` composable in the tree. |
| 13 | Per-profile `webcamEnabled` feature toggle persists via writeScope + mutateActiveProfile, per-profile not global, survives restart (D-04) | VERIFIED | `Profile.kt:46,83`: `webcamEnabled: Boolean = true` on both `Profile` and `PersistedProfile` data classes, threaded through `toPersisted()` and `fromPersisted()`. `AppContainer.setActiveWebcamEnabled` uses `mutateActiveProfile`. `ProfileToggleTest.toggle_round_trips_through_mutate_active` + `toggle_is_per_profile_not_global` GREEN. |
| 14 | `webcamEnabled` toggle GATES the Webcam drawer tile via `webcamTileEnabled` (count > 0 AND profile.webcamEnabled) — wired to behavior, not just persisted (MEDIUM-4) | VERIFIED | `AppContainer.kt:301-306`: `val webcamTileEnabled = combine(webcamCount, activeProfile) { count, p -> webcamTileGate(count, p?.webcamEnabled ?: true) }`. `AppShell.kt:189`: `val webcamEnabled by container.webcamTileEnabled.collectAsStateWithLifecycle(...)`. `ProfileToggleTest.webcam_tile_gating_requires_both_count_and_toggle` GREEN. |
| 15 | Connection API-key edit semantics: blank preserves, explicit Clear nulls, non-blank replaces; field never pre-filled with raw stored key (MEDIUM-5/V7) | VERIFIED | `PrintersScreen.kt:78,316-384`: `resolveApiKeyEdit` used on save. Comments at lines 73/317/383 confirm the write-scope discipline and blank-preserves semantics. `ProfileToggleTest.apikey_blank_preserves_existing` GREEN. |
| 16 | Four App-Drawer destinations exist — Printers · Theme · Settings · About — each its own tile routing distinctly; no longer one monolithic Settings page (D-01) | VERIFIED | `TopRoute.kt:37`: `enum class Dest { ..., Devices, Theme, Settings, About }`. `AppShell.kt:605-629`: four distinct `when(dest)` arms. `AppDrawer.kt` has four IA tiles. `ShellNavStateTest.four_ia_destinations_route_distinctly` GREEN. |
| 17 | Theme is its own per-printer drawer destination; ThemeEditor content promoted here (D-03) | VERIFIED | `ThemeScreen.kt` exists (confirmed via `ls`). `AppShell.kt:619`: `Dest.Theme -> ThemeScreen(...)`. |
| 18 | Settings is its own per-printer drawer destination for feature toggles; the webcam toggle persists AND gates the Webcam tile (D-04/MEDIUM-4) | VERIFIED | `SettingsScreen.kt` shows per-profile webcam toggle wired to `setActiveWebcamEnabled` (lines 91-102). Connection is absent. |
| 19 | About is the 4th drawer destination for app-global items + the hidden dev-widget enable toggle; turning it OFF clears any active override so the app is never stranded (D-05/HIGH-5) | VERIFIED | `AboutScreen.kt:65-103`: `fun AboutScreen` with `container.setDevCyclerEnabled(it)` at line 103. Comment at line 45-47 documents the HIGH-5 clear-on-off behavior. No `BuildConfig.DEBUG`. |
| 20 | Back is Intent.Neutral on every screen with a Back affordance — the enumerated offenders (Extrude=Go, Temperature=Go, Spool=Danger, Webcam=Danger, Console=green-custom, Devices=Go) are all flipped to Neutral | VERIFIED | `TemperatureScreen.kt:180`: `intent = Intent.Neutral // D-10`. `SpoolScreen.kt:144`: `intent = Intent.Neutral`. `ExtrudeScreen.kt` comment at line 106 confirms Neutral. AUDIT.md back-sweep column shows OK for all core+long-tail screens. `a921cc8` is the sweep commit. |
| 21 | D-10 guided core-screen review surfaced criteria C1-C7, folded into THEMING.md (and C3/C6 into LAYOUT.md) before the app-wide audit ran | VERIFIED | `THEMING.md:151-190`: C1 through C7 present in "Conformance criteria — the C-series" section. `LAYOUT.md:74-83`: C3 and C6 present. `AUDIT.md` "Hardened criteria — the C-series" section documents the 7 criteria. |
| 22 | Every mechanical violation (core M1-M4 + long-tail fsSp font floors) is fixed silently (D-13/MEDIUM-1) | VERIFIED | `AUDIT.md` shows `OK✓(M1)`, `OK✓(M2)` on MoveScreen, `OK✓(M3)` on TemperatureScreen, `OK✓(M4)` on FilesScreen, `OK✓(fs)` across Extrude/Console/Webcam/BedMesh/ProbeCalibrate/SystemMacros. Commit `c2f16a8` is the mechanical-fix commit. |
| 23 | Every ~30-surface row in AUDIT.md is scored against the 6+1 LAW dimensions — coverage proven via the surface-granular inventory + lint check (D-11/D-12/MEDIUM-2/LOW) | VERIFIED | `AUDIT.md`: 32 surface rows (#1-#32 incl. sub-rows #23a/b/c, #26a). Coverage proof section records the inventory diff reconciliation. Lint checks: `MaterialTheme.colorScheme` = 0, `Color(0x` = 4 hits all whitelisted, raw `.sp` outside `fsSp` = 0 font violations. |
| 24 | Sanctioned raw-color literals (PromptMarkup author-hex, SpoolPicker filament swatches, theme swatches) are recorded as conformant — NOT "fixed" | VERIFIED | `AUDIT.md` footnote 1: "SpoolPicker.kt filament swatches and PromptMarkupText.kt author-hex are DATA per the THEMING.md carve-out — recorded CONFORMANT." `grep -rn "Color(0x"` returns 4 hits, all in those sanctioned files. |
| 25 | The on-device closed fix/verify loop (HIGH-6) reached zero remaining owner-reported mechanical gaps before AUDIT.md was finalized | VERIFIED | `AUDIT.md` finalization section: "the HIGH-6 closed fix/verify loop reached zero remaining owner-reported mechanical gaps"; owner reported "Looks great." Commit `0686b07` finalizes the scorecard. |
| 26 | The bed-mesh heatmap ramp is theme-derived (low=pool, high=accent), re-baked in `applyTokens` (the one real gap the closed loop found) | VERIFIED | `BedMeshHeatmapView.kt:86-112`: `rampStops = OklchRamp.themedRampStops(...)` in `applyTokens`. `OklchRamp.kt:125`: `fun themedRampStops(lowArgb, highArgb)`. Commit `12d894c`. |
| 27 | Core human verification (all `checkpoint:human-verify` gates across all 6 plans) was owner-verified on-device on flox + real printers — UAT burden is already discharged | VERIFIED | Plans 02/04/05/06 each had `checkpoint:human-verify gate="blocking"` tasks. Summaries document owner-approved signals for: cycler walk (plan 02), four-tile IA (plan 04), D-10 guided review + Back sweep (plan 05), long-tail on-device sweep (plan 06). AUDIT.md records "approved" + "Looks great." |
| 28 | AUDIT.md is finalized: every surface row resolved (OK / deliberate-exception / deferred-with-todo), no open cells | VERIFIED | `AUDIT.md` final section: "STATUS: COMPLETE. Every non-exempt surface is scored and resolved. No open or unscored cells remain." EX(dev)/EX(set) exemptions + R1-R4 deferred todos recorded. |

**Score:** 28/28 truths verified

---

## Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `app/src/main/java/works/mees/dinghy/theme/ThemeOverride.kt` | Sparse nullable override model + mergeOnto | VERIFIED | `data class ThemeOverride(dark,paletteMode,fs)` + `mergeOnto` via `base.copy(...)` |
| `app/src/main/java/works/mees/dinghy/theme/ThemeResolver.kt` | Pure `bake(tuple)` snapshot function | VERIFIED | `fun bake(tuple): ThemeTokens` delegates to extracted `computeFrom(...)`, reads no `var` field |
| `app/src/main/java/works/mees/dinghy/di/AppContainer.kt` | `activeThemeTuple` + pure `effectiveTokens` + override plumbing | VERIFIED | All present: `activeThemeTuple`, `effectiveTokens`, `setThemeOverride`, `updateThemeOverride`, `devCyclerEnabled`, `setDevCyclerEnabled`, `webcamTileEnabled`, `setActiveWebcamEnabled` |
| `app/src/main/java/works/mees/dinghy/theme/compose/DinghyTheme.kt` | Collects override-aware `effectiveTokens` flow | VERIFIED | `MainActivity` passes `container.effectiveTokens`; resolver-based overload retained for BenchActivity/instrumented tests |
| `app/src/main/java/works/mees/dinghy/ui/shell/DevThemeCyclerOverlay.kt` | Two floating cycler widgets + pure merge-onto-current stepping | VERIFIED | `nextStyleOverride` + `nextSizeOverride` + `fun DevThemeCyclerOverlay(...)` |
| `app/src/main/java/works/mees/dinghy/ui/shell/AppShell.kt` | Cycler overlay wired atomically + webcam gate updated | VERIFIED | `DevThemeCyclerOverlay` block at line 731; `container.webcamTileEnabled` at line 189 |
| `app/src/main/java/works/mees/dinghy/ui/screen/PrintersScreen.kt` | Renamed DevicesScreen absorbing Connection editing | VERIFIED | Exists; no orphaned `fun DevicesScreen`; Connection absorbed with validators + `resolveApiKeyEdit` |
| `app/src/main/java/works/mees/dinghy/ui/screen/SettingsScreen.kt` | Shrunk to per-profile feature toggles only | VERIFIED | Webcam toggle present; no host/port/apiKey fields |
| `app/src/main/java/works/mees/dinghy/ui/screen/ThemeScreen.kt` | Promoted ThemeEditor as own Dest | VERIFIED | Exists; `Dest.Theme -> ThemeScreen(...)` in AppShell |
| `app/src/main/java/works/mees/dinghy/ui/screen/AboutScreen.kt` | App-global + dev-enable toggle (off clears override) | VERIFIED | Exists; `container.setDevCyclerEnabled(it)` at line 103; no `BuildConfig.DEBUG` |
| `app/src/main/java/works/mees/dinghy/ui/route/TopRoute.kt` | Dest.Theme + Dest.About added | VERIFIED | `enum class Dest { ..., Devices, Theme, Settings, About }` at line 37 |
| `app/src/main/java/works/mees/dinghy/config/Profile.kt` | Per-profile `webcamEnabled` field on Profile + PersistedProfile + round-trip | VERIFIED | Field present on both data classes at lines 46 and 83; threaded through `toPersisted`/`fromPersisted` |
| `app/src/main/java/works/mees/dinghy/render/BedMeshHeatmapView.kt` | Theme-derived ramp re-baked in `applyTokens` | VERIFIED | `OklchRamp.themedRampStops(...)` called in `applyTokens` at line 112 |
| `app/src/main/java/works/mees/dinghy/theme/OklchRamp.kt` | `themedRampStops(low,high)` baker | VERIFIED | `fun themedRampStops(lowArgb, highArgb): IntArray` at line 125 |
| `docs/ui_design/THEMING.md` | C1-C7 conformance criteria appended (D-10) | VERIFIED | C1-C7 present at lines 151-190 in "Conformance criteria — the C-series" section |
| `docs/ui_design/LAYOUT.md` | C3/C6 also in LAYOUT.md | VERIFIED | C3 at line 74; C6 at line 78 |
| `.planning/phases/15.2-theme-conformance-sweep-settings-ia/15.2-AUDIT.md` | Finalized D-12 scorecard — 32 surfaces, coverage proof, lint check | VERIFIED | All 32 surface rows scored+resolved; inventory diff reconciliation; lint-check table; closed-loop finalization section |

---

## Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `AppContainer.effectiveTokens` | `ThemeResolver.bake(tuple)` | `combine(activeThemeTuple, ...)` bakes in both branches | VERIFIED | Lines 410-416 in AppContainer; `.bake(` appears in both override and non-override paths |
| `AppContainer.effectiveTokens` | `activeThemeTuple` | Single canonical flow feeds both `seedTheme` + `effectiveTokens` | VERIFIED | `activeThemeTuple` at line 389; `seedTheme` collects it at line 425 |
| `AppContainer._themeOverride` | `MutableStateFlow.update` | `updateThemeOverride(transform)` | VERIFIED | `_themeOverride.update(transform)` at line 231 |
| `MainActivity` | `AppContainer.effectiveTokens` | `DinghyTheme(tokensFlow = container.effectiveTokens)` | VERIFIED | Confirmed in SUMMARY-01; retained resolver overload for bench/instrumented tests |
| `AppShell` | `DevThemeCyclerOverlay` | `if (devCyclerEnabled) { DevThemeCyclerOverlay(...) }` at line 732 | VERIFIED | Lines 731-745 in AppShell |
| `AppShell` | `container.updateThemeOverride` | `onCycleStyle = { container.updateThemeOverride { nextStyleOverride(it) } }` | VERIFIED | Line 742 — atomic path confirmed |
| `AppShell` | `container.webcamTileEnabled` | `val webcamEnabled by container.webcamTileEnabled.collectAsStateWithLifecycle(...)` | VERIFIED | Line 189 in AppShell replaces the bare `webcamCount > 0` |
| `PrintersScreen` | `AppContainer.saveProfile / mutateActiveProfile` | Connection save via writeScope — no composition-scope persist | VERIFIED | Lines 317-384; `rememberCoroutineScope` limited to mDNS scan (line 184 comment) |
| `AppContainer` | `webcamTileEnabled` flow | `combine(webcamCount, activeProfile)` with `webcamTileGate(count, p?.webcamEnabled ?: true)` | VERIFIED | Lines 301-306 in AppContainer |
| `AboutScreen` | `AppContainer.setDevCyclerEnabled` | Dev-widget enable toggle | VERIFIED | Line 103 in AboutScreen |
| `Dest.Theme/About` | `AppShell.when(dest)` | `Dest.Theme -> ThemeScreen(...)` / `Dest.About -> AboutScreen(...)` | VERIFIED | Lines 619, 629 in AppShell |

---

## Behavioral Spot-Checks

| Behavior | Check | Result | Status |
|----------|-------|--------|--------|
| `ThemeOverrideTest` — 9/9 GREEN including HIGH-1/3/5 + atomic stepping | SUMMARY-01 reports + test file exists | Documented GREEN in SUMMARY-01 | PASS |
| `ThemeResolverBakeTest` — 2/2 GREEN (pure bake) | Test file exists at expected path | Documented GREEN in SUMMARY-01 | PASS |
| `DevWidgetEnableTest` — 1/1 GREEN (default false, round-trips) | Test file exists at expected path | Documented GREEN in SUMMARY-01 | PASS |
| `StyleCyclerTest` + `TextSizeCyclerTest` — incl. HIGH-4 preserve-other-axis tests | Test files exist at expected paths | Documented GREEN in SUMMARY-02 | PASS |
| `ProfileToggleTest` — round-trip + per-profile + gate predicate + apiKey semantics | Test file exists at expected path | Documented GREEN in SUMMARY-03 | PASS |
| `ShellNavStateTest` — 4 IA dests route distinctly, no dangling ref | Test file exists at expected path | Documented GREEN in SUMMARY-04 | PASS |
| `MaterialTheme.colorScheme` count = 0 (lint check) | `grep -rn "MaterialTheme.colorScheme" ui/ render/` | 0 hits confirmed | PASS |
| Unsanctioned `Color(0x` literals = 0 (lint check) | `grep -rn "Color(0x"` filtered against whitelist | 0 unsanctioned hits | PASS |
| `TEMP(15.2-02)` marker fully removed | `grep -rn "TEMP(15.2-02)" app/src/` | Empty — revert confirmed | PASS |

---

## Requirements Coverage

| Requirement | Plans | Description | Status | Evidence |
|-------------|-------|-------------|--------|---------|
| THEME-01 | 01, 02, 06 | Semantic-token theme — every component via role tokens, never raw color | SATISFIED | Lint check: 0 unsanctioned `Color(0x`; 0 `MaterialTheme.colorScheme`; `effectiveTokens` pure token flow; AUDIT all surfaces scored token-clean |
| THEME-02 | 01, 02 | User text-size S/M/L persisted | SATISFIED | `DevThemeCyclerOverlay.nextSizeOverride` cycles S/M/L; persisted `fs` via `devEnableFlow`; `TextSizeCyclerTest` GREEN |
| UI-01 | 05, 06 | Focus/Field/Gutter grammar | SATISFIED | AUDIT.md FFG column: all applicable surfaces OK; core screens reviewed with cyclers on-device |
| UI-02 | 05, 06 | Outline-led ≥64px targets, button-intent colors | SATISFIED | Back sweep all Neutral; M1-M4 fixed (accent/stop/fill correctness); long-tail fsSp floors fixed; AUDIT ≥64 column OK |
| SET-01 | 03, 04 | Settings for connection, theme, text-size, feature toggles | SATISFIED | Settings IA dissolved: Printers owns connection, Theme owns look, Settings owns toggles, About owns app-global. All four surfaces exist and route distinctly |
| CONN-01 | 03 | Enter/save Moonraker connection | SATISFIED | Connection editing absorbed into PrintersScreen with preserved validation + apiKey semantics |
| CONN-02 | 03 | Optional API key auth | SATISFIED | `resolveApiKeyEdit` blank-preserves/clear/replace; apiKey stays masked; `ProfileToggleTest.apikey_blank_preserves_existing` GREEN |

**Note on requirement traceability:** REQUIREMENTS.md documents Phase 15 (the parent phase grouping 15, 15.1, 15.2) as "refines SET-*/THEME-*/UI-* — UX + conformance rework, no new functional REQ-IDs." The plans reference these existing IDs as the requirements being tightened. No orphaned requirement IDs were found — all IDs claimed in plan frontmatter (THEME-01, THEME-02, UI-01, UI-02, SET-01, CONN-01, CONN-02) exist in REQUIREMENTS.md.

---

## Anti-Patterns Found

| File | Pattern | Severity | Decision |
|------|---------|----------|---------|
| `DevThemeCyclerOverlay.kt` (WR-01) | Dev overlay panel-drag does not accumulate — uses captured `pos` snapshot, not live `offset` state | WARNING | Owner-deferred to Phase 21 (dev tool, not user-facing shipping chrome). Todo recorded. |
| `AppShell.kt` + `AppContainer.kt` (WR-02) | `connectionStore` is write-dead; webcam URL resolution reads it, producing an empty host after Phase-14 migration | WARNING | Owner-deferred to Phase 20 (pre-existing Phase-14 migration gap; webcam works via the live profile on both printers anyway). Todo recorded. |
| `ThemePrefs.kt` (IN-01) | `SHIFT_RANGE` KDoc says `0..360`; code is `0..359` | INFO | Cosmetic doc drift; deferred to ship cleanup |
| `AppContainer.kt` (IN-02) | `connectionStore` dead weight (tied to WR-02) | INFO | Will be deleted once WR-02 is fixed in Phase 20 |
| `BedMeshHeatmapView.kt` (IN-03) | `OPAQUE_BLACK` KDoc slightly misleading about its role | INFO | Pure nit; no behavior impact |

No TBD/FIXME/XXX markers found in phase-modified files. All `// TEMP(15.2-02)` markers are gone. WR-01/WR-02 are pre-existing or dev-tool defects explicitly triaged by the owner to later phases with tracked todos — they are not blockers for this phase's goal.

---

## Human Verification Required

All human verification for this phase was completed during execution. Every `checkpoint:human-verify gate="blocking"` task across all six plans was owner-verified on-device (flox + live printers):

- **Plan 02 checkpoint:** Cycler walk on-device — whole app re-themes through 6 style combos + S/M/L, including Views surfaces; axes independent; restores on dismiss; static widget. Owner signed off.
- **Plan 04 checkpoint:** Four-tile IA on-device — Printers/Theme/Settings/About all present; connection editing works; API key not pre-filled; Theme per-printer; webcam toggle gates the tile; cyclers gated by About toggle; Back neutral+consistent. Owner signed off.
- **Plan 05 checkpoint (D-10):** Guided core-screen review on-device with cyclers — surfaced C1-C7 criteria; all findings recorded; mechanical findings (M1-M4) tagged for plan 06. Owner signed off.
- **Plan 06 checkpoint:** Long-tail on-device sweep — all calibration/spool/webcam/macros/prompt/render-View surfaces walked; mechanical fixes confirmed; bed-mesh ramp gap found and fixed in the closed loop. Owner: "Looks great." AUDIT finalized.

The UAT burden is fully discharged. No additional human verification is outstanding.

---

## Deferred Items

Items not yet met but explicitly addressed in later milestone phases (tracked as todos, not gaps):

| # | Item | Addressed In | Evidence |
|---|------|-------------|---------|
| 1 | R1: increment-picker 3-cell component (C2) | Phase 17 | Todo `2026-06-05-increment-picker-3cell-component.md`; Phase 17 = Fine-Tune/Live-Adjust |
| 2 | R2: Move Z vertical-layout rework (C3) | Phase 17 | Todo `2026-06-05-move-z-vertical-layout-rework.md` |
| 3 | R3: Settings/Theme densify-restyle (C6) | Phase 20/21 | Todo `2026-06-05-settings-densify-one-page-restyle.md` |
| 4 | R4: Printers edit/delete-mode toggle buttons | Phase 16 | Todo `2026-06-05-printers-edit-delete-mode-buttons.md` |
| 5 | Webcam screen crash (undiagnosed decode layer) | Phase 20 | Todo `2026-06-05-webcam-screen-crash.md`; owner-accepted triage |
| 6 | Bookmarked-macros density | Phase 21 | Todo `2026-06-05-bookmarked-macros-density.md` |
| 7 | Print-Status (Home) conformance sweep | Phase 16 | Phase 16 = Home/Status redesign (reworks it wholesale; auditing now is wasted effort) |
| 8 | WR-01: Dev overlay panel-drag accumulation bug | Phase 21 | Dev-tool only; not user-facing; owner-accepted defer |
| 9 | WR-02: `connectionStore` dead-read + webcam URL | Phase 20 | Pre-existing Phase-14 gap; Phase 20 (WebRTC) is where the webcam plumbing is rebuilt |

---

## Gaps Summary

No gaps. All 28 must-haves are VERIFIED. All human verification was discharged on-device during execution. Deferred items are tracked todos with clear owner-assigned target phases — they are not gaps in this phase's goal.

---

_Verified: 2026-06-05_
_Verifier: Claude (gsd-verifier)_
