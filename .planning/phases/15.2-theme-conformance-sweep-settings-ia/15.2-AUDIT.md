# 15.2 Theme Conformance Scorecard (D-12)

The app-wide UI-conformance scorecard the sweep (plans 03/05/06) fills, one row per DISTINCT SURFACE.
Each cell is scored as the sweep walks the device per theme combo using the dev cyclers (the override
layer built in 15.2-01). Placeholder `-` = not yet audited; fill with `OK`, `FIX` (mechanical, fixed
silently per D-13), or `FLAG` (judgment call surfaced to the owner).

## Inventory provenance (reproducible — MEDIUM-2 / LOW FIX 5)

Surface rows were GENERATED from a programmatic inventory of `ui/**` + `render/**` (NOT a hand-built
list), so no overlay/dialog/drawer-chrome/scan-surface/prompt/render-View is silently omitted. The
file-level inventory command:

```
grep -rEl "fun .*Screen\(|fun .*Dialog\(|fun .*Overlay\(|fun .*Popup\(|fun .*Sheet\(|: View\(|class .*View " \
  app/src/main/java/works/mees/dinghy/ui app/src/main/java/works/mees/dinghy/render
# + a chrome pass for the shell surfaces the screen/dialog pattern misses:
grep -rEl "AppDrawer|ScanSurface|ProgressRing|RootController|fun AppShell|SplashScreen" \
  app/src/main/java/works/mees/dinghy/ui app/src/main/java/works/mees/dinghy/render
```

The `grep -rEl` inventory is FILE-level. A single file can host MULTIPLE distinct surfaces (a screen
plus its dialogs/overlays/popups), so every multi-surface file is EXPANDED into its distinct SURFACE
rows below — coverage is proven at SURFACE granularity, not file granularity. Expansions made:

- `AppShell.kt` → its hoisted overlay surfaces: MacroExecutionPopup, ScanSurface, PromptDialog,
  DevThemeCyclerOverlay (the last is built in a later 15.2 plan — row reserved now so the sweep can't
  skip it).
- The prompt surfaces (`prompt/**`) → PromptDialog (the overlay frame + close control) vs the content
  renderers PromptMarkupText and PromptImageItem as separate surface rows.

## Hardened criteria — the C-series (15.2-05 D-10 guided core-screen review)

The D-10 guided on-device review of the core daily-driver screens (15.2-05 Task 2) surfaced seven new
LAW criteria, now folded into `docs/ui_design/THEMING.md` ("Conformance criteria — the C-series"; C3/C6
also in `LAYOUT.md`). **Plan 06's app-wide sweep audits EVERY surface against these in addition to the
7 dimension columns below.** They sharpen the intent/status/density rules into testable checks:

| C | Criterion (testable) | Dimension it sharpens | Doc home |
|---|---|---|---|
| **C1** | A button performing the screen's EXPECTED physical action uses ACCENT, not caution/danger. | Intent | THEMING.md |
| **C2** | The increment picker is a 3-cell `[−][value][+]` pattern with shared-increment vs single-measurement modes. | Intent / FFG | THEMING.md |
| **C3** | Vertical adjustments (Z) use a vertical arrangement when the orientation layout allows it (not a horizontal row). | FFG | LAYOUT.md + THEMING.md |
| **C4** | A control that ENABLES a catastrophic state (force-move unlock) is FILLED with the STOP color while active. | Intent / 15.1 | THEMING.md |
| **C5** | The screen's NATURAL PRIMARY ACTION takes ACCENT, and may be context-dependent (Temp: Presets↔Cooldown; Files: Print file). | Intent | THEMING.md |
| **C6** | Config/settings-type surfaces are EXEMPT from ≥64px and should be DENSIFIED (print-control surfaces stay bound). | ≥64 / FFG | LAYOUT.md + THEMING.md |
| **C7** | BACK is Neutral ONLY for plain nav; a Back that DISCARDS input / REJECTS a pending result stays RED. | Intent / 15.1 | THEMING.md + OutlinedControl docstring |

### Finding tags (the shared vocabulary plan 06 picks up — MEDIUM-1)

- **`mechanical (plan-06)`** — a concrete violation plan 06 Task 2 fixes silently (D-13). The core-screen
  mechanical findings (M1–M4 below) use the SAME tag vocabulary as plan 06's own long-tail findings, so
  they flow into the SAME fix pass (the core screens are NOT exempt from the long-tail fix loop).
- **`redesign (deferred)`** — exceeds plan 06's mechanical charter (new component / layout rework /
  interaction-model change); captured as a pending todo, NOT plan-06 work (R1–R4 below).
- **`judgment (owner-flagged)`** — a layout/role/semantic question surfaced to the owner, not silently
  changed.

### Scope exemptions (plan 06 SKIPS these)

- **Print-Status (Home) is EXEMPT** from this sweep and the plan-06 audit — **Phase 16 (Home/Status
  redesign)** reworks it wholesale; auditing it now would be wasted effort. (Row 1 marked EXEMPT.)
- **Icons / glyphs are EXEMPT from plan-06 conformance everywhere** — a future phase redefines all icon
  usage + the glyph library. **Plan 06 audits layout / color / intent / size / FFG / 15.1 but NOT
  icon/glyph CHOICE.** (Shape-coded *status* glyphs — octagon/triangle — remain in scope as a 15.1
  safety mechanism; what's exempt is decorative/affordance icon selection.)

### Core-screen mechanical findings (M-series → `mechanical (plan-06)`)

These concrete violations on the CORE screens are tagged so plan 06 Task 2 fixes them:

- **M1** — *Move*: X/Y/Z direction buttons should be **ACCENT when active** (currently caution-styled) [C1].
- **M2** — *Move*: force-move "unlock" should be **FILLED with the STOP color when active** (not just outlined) [C4].
- **M3** — *Temperature*: **Presets = accent when not heating; Cooldown = active/accent when heating** [C5].
- **M4** — *Files*: **"Print file" should be ACCENT** [C5]; **"Cancel picker" should be Neutral/Back**.

### Core-screen redesign findings (R-series → `redesign (deferred)`, todos)

| R | Finding | Criterion | Todo | Target phase |
|---|---|---|---|---|
| **R1** | Build the 3-cell increment-picker component | C2 | `.planning/todos/pending/2026-06-05-increment-picker-3cell-component.md` | 17 |
| **R2** | *Move* — Z-button vertical-layout rework | C3 | `.planning/todos/pending/2026-06-05-move-z-vertical-layout-rework.md` | 17 |
| **R3** | Theme + settings-type pages densify-to-one-page restyle | C6 | `.planning/todos/pending/2026-06-05-settings-densify-one-page-restyle.md` | 20 (settings pass; cross-links settings-vs-devices-boundary) |
| **R4** | *Printers* — replace corner-icon with Edit-mode / Delete-mode toggle buttons | — | `.planning/todos/pending/2026-06-05-printers-edit-delete-mode-buttons.md` | 16 (or dedicated Printers pass) |

## Conflict policy (D-13) + sanctioned literals

- **Mechanical conflicts** (a raw color where a token exists, a sub-64dp target, a missing `fsSp`
  scale, a hardcoded px): FIX SILENTLY during the sweep, note in the row.
- **Judgment calls** (a layout that violates Focus/Field/Gutter, a color-role reassignment, a
  pool→role semantic question): FLAG to the owner, do NOT silently change.
- **Sanctioned raw-color literals are CONFORMANT per THEMING.md — do NOT flag them:**
  - `PromptMarkupText` author-hex color spans (D-03 carve-out — the macro author chooses the color).
  - `SpoolPicker` filament swatches (the filament's real color, not a theme role).
  - Theme swatches in `ThemeEditorScreen`/`ColorWheel` (they DISPLAY raw colors by definition).

## Scorecard

Columns = the 6 LAW dimensions + a 7th 15.1-conformance dimension:

1. **Token** — token-purity (no raw color where a role token exists; THEME-01)
2. **Intent** — button-intent color (red stop/back · green accept · amber peril · accent command · white setting)
3. **FFG** — Focus/Field/Gutter grammar (portrait stack · landscape 50/50 · ratio-only sizing)
4. **≥64** — ≥64dp touch targets
5. **fsSp** — `fsSp(baseSp, t.fs)` text scale (no hardcoded sp; floor 15sp metadata)
6. **D/L/C** — dark / light / custom render correctly across all three palette modes
7. **15.1** — shape-coded status (octagon stop / triangle caution / shapeless go) · pool→role color · Back neutral+position

> **Core-screen rows (D-10 guided review, 15.2-05) are SCORED below.** `OK` = conformant; `FIX:Mn` =
> mechanical finding tagged `mechanical (plan-06)` (plan 06 fixes); `R:Rn` = `redesign (deferred)` todo;
> `EXEMPT` = out of scope (see exemptions above); `-` = not yet audited (plan-06 long-tail). The
> back-sweep (Task 1) already flipped every Back → Neutral, so the **15.1** column Back-half is `OK` on
> the cores unless C7 (discarding Back) applies.

> **15.2-06 sweep status (Tasks 1+2, pre-checkpoint):** all core-screen mechanical findings (M1–M4) are
> now **FIXED** (cells flipped `FIX:Mn` → `OK✓(Mn)`); the long-tail rows are scored; mechanical fsSp
> findings (`FIX:fs`) are fixed inline. `OK` = was already conformant; `OK✓(Mn)` / `OK✓(fs)` = fixed this
> plan; `R:Rn` = deferred redesign (todo, NOT plan-06 work); `EXEMPT` = out of scope; `EX(dev)` / `EX(set)`
> = exempt (dev tool / settings-class close-interaction). No long-tail JUDGMENT-call conflicts were found —
> every long-tail non-conformance was a clear-cut mechanical fix (fixed silently per D-13).

| # | Surface | File | Token | Intent | FFG | ≥64 | fsSp | D/L/C | 15.1 |
|---|---------|------|-------|--------|-----|-----|------|-------|------|
| 1 | PrintStatusScreen | ui/printstatus/PrintStatusScreen.kt | EXEMPT | EXEMPT | EXEMPT | EXEMPT | EXEMPT | EXEMPT | EXEMPT |
| 2 | MoveScreen | ui/move/MoveScreen.kt | OK | OK✓(M1) | R:R2 | OK | OK | OK | OK✓(M2) |
| 3 | TemperatureScreen | ui/temperature/TemperatureScreen.kt | OK | OK✓(M3) | OK | OK | OK | OK | OK |
| 4 | ExtrudeScreen | ui/extrude/ExtrudeScreen.kt | OK | OK | OK | OK | OK✓(fs) | OK | OK |
| 5 | FilesScreen | ui/files/FilesScreen.kt | OK | OK✓(M4) | OK | OK | OK | OK | OK |
| 6 | ConsoleScreen | ui/console/ConsoleScreen.kt | OK | OK | OK | OK | OK✓(fs) | OK | OK |
| 7 | SettingsScreen | ui/screen/SettingsScreen.kt | OK | OK | n/a | R:R3 | OK | OK | OK |
| 8 | PrintersScreen (was DevicesScreen) | ui/screen/PrintersScreen.kt | OK | OK | n/a | OK | OK | OK | OK / R:R4 |
| 9 | ThemeScreen (Dest) + ThemeEditorScreen (body) | ui/screen/ThemeScreen.kt + ThemeEditorScreen.kt | OK | OK | n/a | R:R3 | OK | OK | OK |
| 10 | SplashScreen / Connect prompt | ui/screen/SplashScreen.kt | OK | OK | n/a | OK | OK | OK | OK |
| 11 | SpoolScreen | ui/spool/SpoolScreen.kt | OK | OK | OK | OK | OK | OK | OK |
| 12 | SpoolPicker | ui/spool/SpoolPicker.kt | OK¹ | OK | OK | OK | OK | OK | OK |
| 13 | WebcamScreen | ui/webcam/WebcamScreen.kt | OK | OK | OK | OK | OK✓(fs) | OK | OK |
| 14 | CalibrationHubScreen | ui/calibration/CalibrationHubScreen.kt | OK | OK | OK | OK | OK | OK | OK |
| 15 | TiltScreen | ui/calibration/TiltScreen.kt | OK | OK | OK | OK | OK | OK | OK |
| 16 | ScrewsTiltScreen | ui/calibration/ScrewsTiltScreen.kt | OK | OK | OK | OK | OK | OK | OK |
| 17 | BedMeshScreen | ui/calibration/BedMeshScreen.kt | OK | OK | OK | OK | OK✓(fs) | OK | OK |
| 18 | ProbeCalibrateScreen | ui/calibration/ProbeCalibrateScreen.kt | OK | OK | OK | OK | OK✓(fs) | OK | OK |
| 19 | SystemMacrosScreen | ui/macros/SystemMacrosScreen.kt | OK | OK | OK | OK | OK✓(fs) | OK | OK |
| 20 | BookmarkedMacrosScreen | ui/macros/BookmarkedMacrosScreen.kt | OK | OK | OK | OK | OK | OK | OK |
| 21 | AppDrawer chrome | ui/shell/AppDrawer.kt | OK | OK | n/a² | OK | OK | OK | OK |
| 22 | MacroExecutionPopup (AppShell overlay) | ui/macros/MacroExecutionPopup.kt | OK | OK | n/a² | OK | OK | OK | OK |
| 23 | ScanSurface (AppShell overlay) | ui/spool/scan/ScanSurface.kt | OK | OK | OK | OK | OK | OK | OK |
| 23a | ScanConfirmCard (scan overlay sub-surface) | ui/spool/scan/ScanConfirmCard.kt | OK | OK³ | OK | OK | OK | OK | OK³ |
| 23b | MeasuredWeightPage (spool overlay sub-surface) | ui/spool/MeasuredWeightPage.kt | OK | OK³ | OK | OK | OK | OK | OK³ |
| 23c | ActiveSpoolCard (spool sub-surface) | ui/spool/ActiveSpoolCard.kt | OK | OK | OK | OK | OK | OK | OK |
| 24 | PromptDialog (AppShell overlay frame + close) | ui/prompt/PromptDialog.kt | OK | OK | n/a² | OK | OK | OK | OK |
| 25 | PromptMarkupText (content renderer) | ui/prompt/PromptMarkupText.kt | OK¹ | OK | n/a | OK | OK | OK | OK |
| 26 | PromptImageItem (content image surface) | ui/prompt/PromptImageItem.kt | OK | OK | n/a | OK | OK | OK | OK |
| 26a | PromptContentItems (content renderers) | ui/prompt/PromptContentItems.kt | OK | OK | n/a | OK | OK | OK | OK |
| 27 | DevThemeCyclerOverlay (dev tool) | ui/shell/DevThemeCyclerOverlay.kt | OK | OK | n/a² | OK | EX(dev)⁴ | OK | OK |
| 28 | AboutScreen (built in 15.2-04) | ui/screen/AboutScreen.kt | OK | OK | n/a | OK | OK | OK | OK |
| 29 | GraphView (render) | render/GraphView.kt | OK | n/a⁵ | n/a⁵ | n/a⁵ | OK | OK | OK |
| 30 | BedMeshHeatmapView (render) | render/BedMeshHeatmapView.kt | OK | n/a⁵ | n/a⁵ | n/a⁵ | OK | OK | OK |
| 31 | ProgressRing (render) | render/ProgressRing.kt | OK | n/a⁵ | n/a⁵ | n/a⁵ | OK | OK | OK |
| 32 | WebcamView (render) | render/WebcamView.kt | OK | n/a⁵ | n/a⁵ | n/a⁵ | OK | OK | OK |

**Footnotes:**
1. **OK¹ (sanctioned data literals — NOT a token-purity violation).** `SpoolPicker.kt` filament swatches
   (`Color(0xFFFF0000)`…, the 7-color fill grid) and `PromptMarkupText.kt` author-hex are DATA per the
   THEMING.md carve-out — recorded CONFORMANT, deliberately not "fixed." `ThemeEditorScreen.kt`'s
   luminance-contrast ink (`Color(0xFF101010)`/`0xFFF5F5F5`) is a swatch readability helper on the theme
   swatch surface — same carve-out class.
2. **n/a² (overlay/drawer chrome, FFG-exempt).** Overlays, popups, the drawer, and the dev cycler are not
   Focus/Field/Gutter screens; the FFG grammar governs full screens, so FFG is n/a for these surfaces
   (LAYOUT.md: the app drawer / confirm guard omit the gutter because their tiles ARE the navigation).
3. **OK³ (C7 destructive-Back stays red — correct, not a violation).** `ScanConfirmCard` Back
   (`Intent.Danger`) rejects a pending scan result and `MeasuredWeightPage` Back discards a pending
   measurement — both are cancel-with-loss, so they correctly stay RED per C7 (the Task-1 back-sweep
   deliberately left them red; plan 06 must NOT neutralize them).
4. **EX(dev)⁴ (dev tool, exempt from the user-facing font floor).** `DevThemeCyclerOverlay` uses 11–12sp
   on its compact floating cycler widgets — it is a DEV-only accelerant (default-OFF in release, gated by
   the About dev-enable toggle), not user-facing shipping chrome, so the 15sp metadata floor does not bind it.
5. **n/a⁵ (classic-Views render surface).** GraphView / BedMeshHeatmapView / ProgressRing / WebcamView are
   allocation-free `android.graphics.Canvas` drawing surfaces with no buttons/intents/touch-targets/text —
   they have no Intent/FFG/≥64 dimension. Token-purity (they read `LocalTokens` via `applyTokens`, FACT 2)
   and dark/light/custom (they re-tint on `applyTokens`, proven by the 15.2-02 cycler walk) DO apply and pass.

`OK(set)` note: SettingsScreen / Printers / Theme / TokenTextField label fonts (13sp Material floating
labels) are **settings-class close-interaction** surfaces — C6-exempt from ≥64px AND the small label is a
deliberate dense pattern, recorded conformant (the R3 densify-restyle is the deferred redesign, NOT a font fix).

## Coverage proof — inventory diff (MEDIUM-2 / LOW FIX 5, regenerated 15.2-06 Task 1)

The plan-01 surface-inventory commands were re-run this plan and diffed against the scorecard rows. The
actual file inventory and the AUDIT rows reconcile fully (multi-surface files expanded to surface rows):

```
# file-level inventory (28 files) + chrome pass — ALL accounted for in the scorecard:
ui/printstatus/PrintStatusScreen.kt ............ #1  (EXEMPT)
ui/move/MoveScreen.kt .......................... #2
ui/temperature/TemperatureScreen.kt ........... #3
ui/extrude/ExtrudeScreen.kt ................... #4
ui/files/FilesScreen.kt ....................... #5
ui/console/ConsoleScreen.kt ................... #6
ui/screen/SettingsScreen.kt ................... #7
ui/screen/PrintersScreen.kt ................... #8   (was DevicesScreen — renamed in 15.2-04)
ui/screen/ThemeScreen.kt + ThemeEditorScreen.kt #9  (Dest wrapper + promoted body)
ui/screen/SplashScreen.kt ..................... #10
ui/spool/SpoolScreen.kt ....................... #11
ui/spool/SpoolPicker.kt ....................... #12
ui/webcam/WebcamScreen.kt ..................... #13
ui/calibration/CalibrationHubScreen.kt ........ #14
ui/calibration/TiltScreen.kt .................. #15
ui/calibration/ScrewsTiltScreen.kt ............ #16
ui/calibration/BedMeshScreen.kt ............... #17
ui/calibration/ProbeCalibrateScreen.kt ........ #18
ui/macros/SystemMacrosScreen.kt ............... #19
ui/macros/BookmarkedMacrosScreen.kt ........... #20
ui/shell/AppDrawer.kt ......................... #21
ui/macros/MacroExecutionPopup.kt .............. #22
ui/spool/scan/ScanSurface.kt .................. #23  (+ #23a ScanConfirmCard, #23b MeasuredWeightPage, #23c ActiveSpoolCard)
ui/prompt/PromptDialog.kt ..................... #24
ui/prompt/PromptMarkupText.kt ................. #25
ui/prompt/PromptImageItem.kt .................. #26  (+ #26a PromptContentItems)
ui/shell/DevThemeCyclerOverlay.kt ............. #27  (path was TBD in the reservation; now resolved)
ui/screen/AboutScreen.kt ...................... #28
render/GraphView.kt ........................... #29
render/BedMeshHeatmapView.kt .................. #30
render/ProgressRing.kt ........................ #31
render/WebcamView.kt .......................... #32
```

**Reconciliation deltas vs the plan-01 draft (all benign — code evolved between plan 01 and plan 06):**
- `DevicesScreen.kt` → `PrintersScreen.kt` (renamed in 15.2-04); row 8 path corrected.
- `ThemeEditorScreen.kt` is now hosted by a `ThemeScreen.kt` Dest wrapper (15.2-04 promotion); both scored under row 9.
- `DevThemeCyclerOverlay.kt` lives at `ui/shell/` (the reservation's "TBD"); row 27 path resolved.
- Added surface rows that the file-level draft folded away: ScanConfirmCard (#23a), MeasuredWeightPage
  (#23b), ActiveSpoolCard (#23c), PromptContentItems (#26a) — distinct surfaces inside the scan / spool /
  prompt files. Non-surface helpers (PromptButtonFlatten, PromptStyleColors, TokenTextField, RootController,
  ShellNavState, TopRoute) are plumbing/style maps with no rendered chrome of their own — NOT scored as surfaces.

Every enumerated surface has a scored row. Coverage is complete at surface granularity.

## Coverage proof — lint-style conformance check (MEDIUM-2, run 15.2-06 Task 1, re-run post-Task-2)

Run over `app/src/main/java/works/mees/dinghy/ui` + `…/render`:

| Check | Result | Verdict |
|---|---|---|
| `grep -rn "Color(0x"` | 4 hits — all WHITELISTED: `PromptMarkupText.kt` (author-hex doc-comment), `SpoolPicker.kt` ×2 (filament swatch grid), `ThemeEditorScreen.kt` (luminance-contrast swatch ink) | **PASS** — every hit is a sanctioned data/swatch literal per THEMING.md; zero token-purity violations |
| `grep -rn "MaterialTheme.colorScheme"` | **0** | **PASS** — the app routes 100% through `LocalTokens`, never the M3 colorScheme |
| raw `.sp` not via `fsSp` | 0 font violations — the non-`fsSp` `.sp` hits are (a) the `import …unit.sp` lines, (b) `val …Sp = fsSp(…)` then `…Sp.sp` (Pitfall-5 false-negatives, traced and confirmed conformant in SpoolScreen), and (c) `TemperatureScreen.kt:371 stepSize = 1.sp` (an autosize STEP granularity, not a font size — its min/max bounds use `fsSp`) | **PASS** — no raw font `.sp` outside `fsSp` |

The token-purity + colorScheme + sp lint is clean. The only mechanical FONT findings were **sub-15sp
metadata floors** (not raw `.sp`, but below the `[[dinghy-font-sizes-too-small]]` 15sp metadata floor) —
fixed in Task 2 (see the `FIX:fs` flips). Sub-15sp fonts remaining are the EX(dev) dev-cycler (11–12sp)
and the EX(set) Material floating labels (13sp) — both deliberately exempt.

## Long-tail scoring notes (15.2-06 Task 1)

The long-tail surfaces were built recently to the LAW (post-15.1) and the 15.2-05 Task-1 back-sweep
already neutralized their Backs, so the heavy intent/FFG/≥64/token dimensions were largely conformant.
The findings:

- **Calibration (#14–18)** — Intent grammar fully conformant: Home/Run/Start/Activate/Save-mesh = Accent
  (the screen's expected physical action, C1/C5); ProbeCalibrate Accept = Go, Abort = Danger (discards the
  run), SAVE_CONFIG Save = Warn (host-restart peril); every Back = Neutral. All control cells
  `heightIn(min=64.dp)` or aspectRatio squares. **`FIX:fs`** (fixed): sub-15sp metadata — ProbeCalibrate
  "mm" unit (12→15sp), BedMesh active-mesh line + save-name validation (14→15sp).
- **Spool (#11, #12, #23a–c)** — Back = Neutral, Scan = Accent, Set-active = Go; SpoolPicker
  active/inactive = Accent/Neutral; ScanConfirmCard + MeasuredWeightPage destructive Backs correctly RED
  (C7). Filament swatches are sanctioned data (footnote 1). All conformant.
- **Webcam (#13)** — Back = Neutral, all conformant. **`FIX:fs`** (fixed): cam service + aspect captions
  (13/12→15sp).
- **Macros (#19, #20, #22)** — Back = Neutral, Run/Execute = Accent, Show-hidden = Accent-when-active;
  64dp rows. **`FIX:fs`** (fixed): SystemMacros hidden-helpers caption (14→15sp).
- **Console (#6)** — intent + glyphs PASS (glyphs exempt). **`FIX:fs`** (fixed): filter-pill + Back labels
  + history-unavailable caption (13/14→15sp).
- **Extrude (#4)** — fully conformant. **`FIX:fs`** (fixed): selector section label (13→15sp).
- **Prompt (#24, #25, #26, #26a)** — all chrome token-routed; author-hex is sanctioned content (footnote 1);
  semantic button styles map to tokens (`promptStyleColor`). Conformant.
- **Drawer / overlays / dev cycler (#21, #27)** — token-routed, themes WITH the app (proven by the 15.2-02
  cycler walk). Dev cycler font is EX(dev) (footnote 4).
- **Render Views (#29–32)** — token-routed via `applyTokens(LocalTokens.current)` (FACT 2); no
  intent/FFG/touch/text dimension (footnote 5). Conformant.

**No judgment-call conflicts surfaced in the long tail** — every long-tail non-conformance was a clear
mechanical font-floor fix. (The only owner-flagged judgment items remain the deferred R1–R4 redesigns,
which are NOT plan-06 work.)

## Core-screen scoring notes (D-10 guided review, 15.2-05 Task 2)

Per-screen result of the on-device guided walk (the source of M1–M4 + R1–R4). The owner reported these
findings; this records them so plan 06 picks up the mechanical ones and the deferred redesigns are
tracked as todos:

- **Move (#2)** — `M1` **FIXED** (Intent/C1): `jogIconTint` normal-jog `t.heat` → `t.accent2` — the
  X/Y/Z direction arrows now wear ACCENT (the screen's expected physical action), reserving the stop color
  for force-move-armed. `M2` **FIXED** (15.1/C4): `ForceMoveCell` now FILLS the cell with `t.stopSoft`
  when armed (new `fill` param on `PadCell`), not just the red outline — the unmistakable "dangerous mode"
  signal complementing the open-padlock shape. `R:R2` (FFG): Z buttons in a horizontal row — vertical
  rework deferred to Phase 17 [C3]. Token / fsSp / D-L-C / Back-neutral all OK.
- **Temperature (#3)** — `M3` **FIXED** (Intent/C5): Presets = `Intent.Accent` when NOT heating /
  `Intent.Neutral` when heating; Cooldown = `Intent.Accent` when heating / `Intent.Warn` when not —
  context-dependent natural-primary-action, driven by `legend.any { it.target != null }`. Trace colors
  PASS. Everything else OK.
- **Files (#5)** — `M4` **FIXED** (Intent/C5): "Print file" `Intent.Go` → `Intent.Accent` (the natural
  primary action); "Cancel picker" was already `Intent.Neutral` (correct). Everything else OK.
- **Console (#6)** — **PASS.** (Icon/glyph work explicitly deferred — do NOT flag glyphs per the
  icons/glyphs exemption.)
- **Settings (#7)** — **PASS ("good enough")** on theme/intent. C6 densification applies → `R:R3`
  (deferred, Phase 20 settings pass). FFG is n/a (settings-class, FFG-exempt).
- **Theme (#9)** — **colors PASS.** C6 densification applies → `R:R3` (deferred, same Phase-20 pass).
  FFG n/a (settings-class).
- **Printers / Devices (#8)** — **PASS on theme.** `R:R4` (deferred): replace corner-icon affordance with
  explicit Edit-mode / Delete-mode toggle buttons (Phase 16 or dedicated Printers pass). FFG n/a.
- **About (#28)** — **PASS.**
- **Print-Status / Home (#1)** — **EXEMPT** (Phase 16 Home/Status redesign reworks it; auditing now is
  wasted).

**C7 (discarding/rejecting Back stays red) — already conformant, no code change.** The Task-1 back-sweep
deliberately LEFT `MeasuredWeightPage` and `ScanConfirmCard` Backs **red** (they discard a pending
measurement / reject a pending scan result — cancel-with-loss, NOT plain navigation). Per the owner's
ruling this is correct under the new C7 criterion; the rule is now documented in THEMING.md + the
`OutlinedControl`/`Intent` docstring. Plan 06 must NOT "fix" these two reds to Neutral.
