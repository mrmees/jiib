---
phase: 15.2-theme-conformance-sweep-settings-ia
plan: 06
subsystem: ui
tags: [theme, conformance, audit, fsSp, button-intent, bed-mesh, oklch, tokens]

# Dependency graph
requires:
  - phase: 15.2-05
    provides: hardened C-series criteria (folded into THEMING.md/LAYOUT.md) + M1-M4/R1-R4 core-screen findings
  - phase: 15.2-01
    provides: dev theme/size cycler override layer (the per-combo sweep accelerant)
provides:
  - app-wide conformance scorecard (15.2-AUDIT.md) — all ~32 surfaces scored at surface granularity, finalized
  - mechanical conformance fixes (M1-M4 core intent/status + long-tail fsSp font floors)
  - bed-mesh heatmap ramp now theme-derived (re-tints on theme/palette-mode change)
  - owner-signed conformance gate via a closed on-device fix/verify loop (zero remaining mechanical gaps)
affects: [16-home, 17-fine-tune, 20-webrtc, 21-release-ship, theming]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "OklchRamp.themedRampStops(low,high): run-time-endpoint OKLCH ramp baker; bake-once in applyTokens, allocation-free onDraw"
    - "Surface-granular conformance scorecard as the durable D-12 audit artifact (multi-surface files expanded to surface rows)"
    - "Closed on-device fix/verify loop (HIGH-6) as the conformance gate — AUDIT not finalized while any mechanical gap is open"

key-files:
  created: []
  modified:
    - .planning/phases/15.2-theme-conformance-sweep-settings-ia/15.2-AUDIT.md
    - app/src/main/java/works/mees/dinghy/render/BedMeshHeatmapView.kt
    - app/src/main/java/works/mees/dinghy/theme/OklchRamp.kt
    - (Tasks 1-2, earlier commits) ui/calibration/*, ui/spool/SpoolScreen.kt, ui/macros/SystemMacrosScreen.kt, ui/console/ConsoleScreen.kt, ui/extrude/ExtrudeScreen.kt, ui/webcam/WebcamScreen.kt, ui/move/MoveScreen.kt, ui/temperature/TemperatureScreen.kt, ui/files/FilesScreen.kt

key-decisions:
  - "ZERO genuine judgment-call design conflicts surfaced — every non-conformance was a clear-cut mechanical fix applied silently (D-13)"
  - "Bed-mesh heatmap ramp made theme-derived (LOW=pool/seriesColor(1), HIGH=accent/seriesColor(0)), re-baked in applyTokens"
  - "Z-calibrate (ProbeCalibrateScreen) closed-loop fix was a misdirect — reverted, net-unchanged"
  - "Owner-accepted exemptions: dev cycler fonts (EX-dev) + settings-class Material labels (EX-set) recorded deliberate, not gaps"
  - "Prompt screen left as-is (owner developing the prompt standard externally); Print-Status + icons/glyphs out of scope"

patterns-established:
  - "Surface-granular conformance scorecard with reproducible inventory + lint-style coverage proof (MEDIUM-2/LOW)"
  - "Theme-derived render-View ramps baked once per applyTokens, indexed cheaply per draw (Adreno-320 fill-rate floor)"

requirements-completed: [UI-01, UI-02, THEME-01, THEME-02]

# Metrics
duration: ~multi-session
completed: 2026-06-05
---

# Phase 15.2 Plan 06: App-Wide Theme Conformance Sweep Summary

**Every existing surface (~32, core + long tail) scored against the 6+1 LAW dimensions in a finalized D-12 scorecard, all mechanical violations fixed silently, and an owner-signed conformance gate reached through a closed on-device fix/verify loop — capped by making the bed-mesh heatmap ramp theme-derived.**

## Performance

- **Duration:** multi-session (continuation close-out)
- **Completed:** 2026-06-05
- **Tasks:** 4 (Task 1 score, Task 2 mechanical fixes, Task 3 on-device checkpoint, Task 4 closed loop + finalize)
- **Files modified:** AUDIT.md + ~12 surface/render files across the task commits

## Accomplishments

- **Finalized the app-wide conformance scorecard** (15.2-AUDIT.md): all ~32 surfaces scored at SURFACE granularity (multi-surface files expanded into distinct surface rows), coverage proven against a regenerated inventory diff + a lint-style check (zero `MaterialTheme.colorScheme`, every `Color(0x` whitelisted, no raw font `.sp` outside `fsSp`).
- **Fixed all mechanical violations silently** (D-13): core-screen M1–M4 (Move accent jog + force-move stop-fill, Temperature context-dependent presets/cooldown, Files Print-file accent) and the long-tail sub-15sp font floors (`FIX:fs` across calibration / spool / webcam / macros / console / extrude).
- **Closed the HIGH-6 on-device fix/verify loop:** the device walk surfaced exactly ONE real mechanical gap (the bed-mesh ramp) → fixed → rebuilt → re-verified → owner reported no remaining gaps. **"Looks great."**
- **Made the bed-mesh heatmap ramp theme-derived** (`12d894c`): was a hardcoded theme-independent viridis ramp; now `LOW=pool/seriesColor(1)`, `HIGH=accent/seriesColor(0)`, re-baked in `applyTokens` so it re-tints on theme/palette-mode change, allocation-free hot loop preserved.
- **Recorded deliberate exemptions and deferrals** so nothing is silently dropped: EX(dev)/EX(set) exemptions, Print-Status / icons-glyphs / Prompt-screen out-of-scope, and the R1–R4 + webcam-crash + macros-density deferred todos.

## Task Commits

1. **Task 1: Score every surface + prove coverage (inventory + lint)** — `95f23a7` (docs: complete the app-wide conformance scorecard)
2. **Task 2: Apply mechanical conformance fixes (M1–M4 + long-tail fsSp)** — `c2f16a8` (fix)
   - Deferred-finding capture (webcam crash, macros density): `b949ac2` (docs)
3. **Task 3: On-device long-tail sweep checkpoint (closed loop)** — owner returned **"approved"** (checkpoint, no code commit)
4. **Task 4: Closed loop fixes + finalize AUDIT**
   - Z-calibrate misdirect: `c82bd2c` (fix) → reverted `6fad432` (net-unchanged)
   - Bed-mesh theme-derived ramp: `12d894c` (fix)
   - AUDIT finalization: `0686b07` (docs: finalize conformance AUDIT — closed loop closed, owner-signed)

**Plan close-out (this commit):** SUMMARY + STATE + ROADMAP.

## Files Created/Modified

- `.planning/phases/15.2-theme-conformance-sweep-settings-ia/15.2-AUDIT.md` — the finalized D-12 scorecard: every surface scored + resolved, coverage proof, closed-loop record, exemptions, deferrals.
- `app/src/main/java/works/mees/dinghy/render/BedMeshHeatmapView.kt` — ramp now theme-derived, re-baked in `applyTokens`.
- `app/src/main/java/works/mees/dinghy/theme/OklchRamp.kt` — new `themedRampStops(low,high)` baker (locked viridis `bedMeshRampStops` retained for the golden test).
- Tasks 1–2 (earlier commits): core screens (Move, Temperature, Files) for M1–M4; long-tail (calibration, spool, macros, console, extrude, webcam) for `FIX:fs` font floors.

## Decisions Made

- **Zero judgment-call conflicts** — the entire app's non-conformance reduced to clear-cut mechanical fixes (font floor / intent / the bed-mesh ramp), all applied silently per D-13. No design choices required owner adjudication.
- **Bed-mesh ramp theme-derivation** chosen over keeping the locked viridis — the heatmap now obeys THEME-01 and re-tints with the theme, while the golden-tested locked ramp stays available for the test.
- **Z-calibrate left untouched** — the owner clarified the screen was fine; the speculative fix was reverted.
- **Prompt screen deferred** — owner is building the prompt standard externally; restyle waits on that.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Reverted a misdirected closed-loop fix (Z-calibrate)**
- **Found during:** Task 4 (closed fix/verify loop)
- **Issue:** A first closed-loop fix targeted `ProbeCalibrateScreen` height colors based on the on-device read; the owner clarified that screen was already conformant — the change was a misdirect.
- **Fix:** Reverted `c82bd2c` via `6fad432`; `ProbeCalibrateScreen` is net-unchanged (empty `git diff c82bd2c^ 6fad432 -- ProbeCalibrateScreen.kt`).
- **Files modified:** none net (revert cancels)
- **Verification:** empty diff confirmed; row #18 stays `OK✓(fs)` from the Task-2 font fix only.
- **Committed in:** `c82bd2c` then `6fad432`

**2. [Rule 1 - Bug] Bed-mesh heatmap ramp ignored the theme**
- **Found during:** Task 4 (on-device closed loop — the one real owner-reported gap)
- **Issue:** `BedMeshHeatmapView` used a hardcoded, theme-independent viridis ramp; it did not re-tint with theme/palette-mode, violating THEME-01.
- **Fix:** New `OklchRamp.themedRampStops(low,high)` baker; `applyTokens` re-bakes the ramp from current tokens (LOW=pool/seriesColor(1), HIGH=accent/seriesColor(0)); `onDraw` stays allocation-free (index + lerp). Docstrings corrected; tests GREEN.
- **Files modified:** `render/BedMeshHeatmapView.kt`, `theme/OklchRamp.kt`
- **Verification:** OklchRamp tests green; owner re-verified on-device ("Looks great").
- **Committed in:** `12d894c`

---

**Total deviations:** 2 auto-fixed (1 reverted misdirect, 1 theme-purity bug). Closed-loop iterations: **1** real fix.
**Impact on plan:** Both were the closed loop working as designed (HIGH-6). The bed-mesh fix is a genuine correctness/theme-purity improvement; the revert restored intended state. No scope creep.

## Issues Encountered

- The host unit suite (green) did not catch the bed-mesh ramp's theme-independence — only the on-device walk did. (Another mock-vs-reality data point; the closed device loop is exactly the gate that caught it.)

## User Setup Required

None — internal UI conformance edits + a documentation scorecard.

## Next Phase Readiness

- **Phase 15.2 is COMPLETE** (6/6 plans). The theme conformance foundation is locked: every existing surface obeys the reconciled theme LAW or is a recorded deliberate exemption/deferral.
- The orchestrator runs phase verification next (do NOT mark the phase complete here).
- Carried forward as tracked todos: webcam-screen-crash (Phase 20), bookmarked-macros-density (Phase 21), R1/R2 (Phase 17), R3 (Phase 20), R4 (Phase 16). Prompt-screen restyle waits on the owner's external prompt standard. Print-Status redesign is Phase 16; icon/glyph choice is a future glyph-library phase.

## Self-Check: PASSED

- AUDIT.md present; SUMMARY.md present.
- Commits verified present: `95f23a7`, `c2f16a8`, `6fad432` (revert), `12d894c`, `0686b07`.
- `ProbeCalibrateScreen` net-unchanged (empty `git diff c82bd2c^ 6fad432`).

---
*Phase: 15.2-theme-conformance-sweep-settings-ia*
*Completed: 2026-06-05*
