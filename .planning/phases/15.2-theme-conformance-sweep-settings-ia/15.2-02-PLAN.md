---
phase: 15.2-theme-conformance-sweep-settings-ia
plan: 02
type: execute
wave: 2
depends_on: ["15.2-01"]
files_modified:
  - app/src/main/java/works/mees/dinghy/ui/shell/DevThemeCyclerOverlay.kt
  - app/src/main/java/works/mees/dinghy/ui/shell/AppShell.kt
  - app/src/test/java/works/mees/dinghy/theme/StyleCyclerTest.kt
  - app/src/test/java/works/mees/dinghy/theme/TextSizeCyclerTest.kt
autonomous: false
requirements: [THEME-01, THEME-02]
must_haves:
  truths:
    - "A floating Style cycler advances through exactly the 6 {dark,light}x{Colorful,Simple,High-Contrast} combos using the user's current accent/seed (D-06)"
    - "A SEPARATE floating Text-size cycler toggles S/M/L (--fs) independently of the style cycler (D-07)"
    - "A tap applies the combo to the WHOLE live app (navigate anywhere, every screen + the Views surfaces render it); dismiss restores the real saved per-profile theme (D-08)"
    - "Both widgets render only when the dev-enable boolean is on; they are STATIC (no continuous/looping animation) per the Adreno-320 motion LAW (D-08 + CLAUDE.md)"
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/ui/shell/DevThemeCyclerOverlay.kt"
      provides: "The two floating cycler widgets + the pure combo-stepping logic"
      contains: "DevThemeCyclerOverlay"
    - path: "app/src/test/java/works/mees/dinghy/theme/StyleCyclerTest.kt"
      provides: "Host-pure proof the style cycler steps exactly 6 combos with the real seed"
  key_links:
    - from: "app/src/main/java/works/mees/dinghy/ui/shell/AppShell.kt"
      to: "DevThemeCyclerOverlay"
      via: "gated overlay Box after when(dest), driven by devCyclerEnabled"
      pattern: "DevThemeCyclerOverlay"
    - from: "app/src/main/java/works/mees/dinghy/ui/shell/DevThemeCyclerOverlay.kt"
      to: "AppContainer.setThemeOverride"
      via: "onCycleStyle / onCycleSize / onDismiss"
      pattern: "setThemeOverride"
---

<objective>
Build the two floating cycler widgets (D-06 style cycler + D-07 size cycler) and wire them as an
AppShell overlay above all routes, gated on the dev-enable boolean from plan 01. Each tap advances the
appropriate axis and pushes a transient `ThemeOverride` into the layer built in 15.2-01, flipping the
whole live app; dismiss clears it back to the real theme. This is the conformance accelerant the rest of
the phase rides on (D-09).

Purpose: One device walk per combo instead of six rebuilds — make the app-wide sweep (D-10/D-11) cheap.
Output: `DevThemeCyclerOverlay.kt`, the AppShell overlay wiring, host-pure cycler-stepping tests, and an
on-device cycler-walk sign-off.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15.2-theme-conformance-sweep-settings-ia/15.2-CONTEXT.md
@.planning/phases/15.2-theme-conformance-sweep-settings-ia/15.2-RESEARCH.md
@.planning/phases/15.2-theme-conformance-sweep-settings-ia/15.2-PATTERNS.md
@.planning/phases/15.2-theme-conformance-sweep-settings-ia/15.2-01-PLAN.md
@docs/ui_design/CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DevThemeCyclerOverlay widgets + pure combo-stepping logic + cycler tests</name>
  <files>app/src/main/java/works/mees/dinghy/ui/shell/DevThemeCyclerOverlay.kt, app/src/test/java/works/mees/dinghy/theme/StyleCyclerTest.kt, app/src/test/java/works/mees/dinghy/theme/TextSizeCyclerTest.kt</files>
  <read_first>
    - .planning/phases/15.2-theme-conformance-sweep-settings-ia/15.2-PATTERNS.md (DevThemeCyclerOverlay section: the gated-Box-after-when(dest) overlay grammar, the Adreno-320 static-only motion rule, the draggable Modifier.offset+detectDragGestures precedent)
    - .planning/phases/15.2-theme-conformance-sweep-settings-ia/15.2-RESEARCH.md (Pattern 2: cycler combos use the user's REAL seed; Pattern 3: floating overlay above all routes — NOT a WindowManager TYPE_APPLICATION_OVERLAY)
    - app/src/main/java/works/mees/dinghy/theme/ThemeOverride.kt (the ThemeOverride model from 15.2-01)
    - app/src/main/java/works/mees/dinghy/theme/ThemeTokens.kt (enum class FontScale at 188 — S/M/L multipliers; the paletteMode string values "Colorful"/"Simple"/"HighContrast" — confirm exact spelling against ThemePrefs DEFAULT_MODE)
    - app/src/main/java/works/mees/dinghy/designsystem/control/OutlinedControl.kt (the "NO looping/infinite Compose transition" doc-comment ~62-66 — the motion LAW)
    - app/src/main/java/works/mees/dinghy/theme/compose/LocalTokens.kt (the widget themes WITH the app: every color via LocalTokens.current)
  </read_first>
  <behavior>
    - Test (StyleCyclerTest) style_cycler_steps_six_combos_in_order: starting from "no override", repeated style-advance produces exactly the 6 combos {dark,light}x{Colorful,Simple,HighContrast} as ThemeOverride(dark=…, paletteMode=…) values, never touching `fs`, and wraps after 6
    - Test (StyleCyclerTest) style_cycler_leaves_seed_untouched: a style-advance ThemeOverride carries only dark+paletteMode (seedHex is NOT in ThemeOverride — it falls through to the active profile, D-06)
    - Test (TextSizeCyclerTest) size_cycler_steps_S_M_L_independently: repeated size-advance produces ThemeOverride(fs=…) cycling FontScale.S/M/L multipliers, never touching dark/paletteMode, and is independent of the style cycler's state
  </behavior>
  <action>
    Create `ui/shell/DevThemeCyclerOverlay.kt`. Extract the combo-stepping as PURE host-testable functions
    (so StyleCyclerTest/TextSizeCyclerTest can assert them without Compose): e.g.
    `fun nextStyleOverride(current: ThemeOverride?): ThemeOverride` returning the next of the 6
    `{dark,light}x{Colorful,Simple,HighContrast}` combos (each as `ThemeOverride(dark=…, paletteMode=…)`,
    fs left null per D-07 independence), wrapping after 6; and
    `fun nextSizeOverride(current: ThemeOverride?): ThemeOverride` cycling `fs` through FontScale.S/M/L
    multipliers (dark/paletteMode left null). Confirm the exact paletteMode strings against
    ThemePrefs.DEFAULT_MODE — do NOT invent new mode names.
    The Composable `DevThemeCyclerOverlay(onCycleStyle, onCycleSize, onDismiss)` renders TWO small floating
    tap targets (>=64px) — a Style cycler and a separate Size cycler (D-07) — each showing a label of its
    current combo/size (Claude's discretion: fixed corner via Modifier.align OR draggable via
    Modifier.offset+detectDragGestures, precedent in ScrubberPage/ColorWheel) plus a dismiss/hide
    affordance that calls onDismiss → setThemeOverride(null). Every color via `LocalTokens.current` (the
    widget themes WITH the app). CRITICAL motion LAW (D-08/CLAUDE.md, Adreno-320 floor): STATIC ONLY — NO
    continuous/looping/breathing animation; a one-shot tap ripple is fine, a pulsing glow is NOT. Do NOT
    use a WindowManager overlay (RESEARCH anti-pattern — avoids SYSTEM_ALERT_WINDOW). Flesh out the two
    cycler tests to the behaviors above.
  </action>
  <verify>
    <automated>cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --tests works.mees.dinghy.theme.StyleCyclerTest --tests works.mees.dinghy.theme.TextSizeCyclerTest --no-daemon" 2>&1 | tr -d '\r' | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `StyleCyclerTest` + `TextSizeCyclerTest` GREEN (6-combo order, seed untouched, S/M/L independent)
    - `DevThemeCyclerOverlay.kt` contains the pure `nextStyleOverride`/`nextSizeOverride` steppers + the Composable with two >=64px tap targets
    - No `WindowManager` / `TYPE_APPLICATION_OVERLAY` / `SYSTEM_ALERT_WINDOW` reference (grep-clean)
    - No looping/infinite Compose transition in the widget (grep-clean of `rememberInfiniteTransition`/`infiniteRepeatable`)
    - Every widget color reads `LocalTokens.current` (no raw `Color(0x…)`)
  </acceptance_criteria>
  <done>The two cycler widgets exist, step their axes purely-testably, theme with the app, and stay static.</done>
</task>

<task type="auto">
  <name>Task 2: Wire DevThemeCyclerOverlay into AppShell as a dev-enable-gated overlay above all routes</name>
  <files>app/src/main/java/works/mees/dinghy/ui/shell/AppShell.kt</files>
  <read_first>
    - .planning/phases/15.2-theme-conformance-sweep-settings-ia/15.2-PATTERNS.md (Overlay grammar: AppShell.kt:608-694 MacroExecutionPopup/ScanSurface/PromptDialog gated Boxes after when(dest); the onCycleStyle/onCycleSize/onDismiss wiring shape)
    - app/src/main/java/works/mees/dinghy/ui/shell/AppShell.kt (the BoxWithConstraints + when(dest) ~440-606; the existing gated overlay Boxes ~608-694; how container/AppContainer is reached; the LocalTokens.current `val t` at ~126)
    - app/src/main/java/works/mees/dinghy/di/AppContainer.kt (devCyclerEnabled flow + setThemeOverride from 15.2-01)
  </read_first>
  <action>
    In `AppShell.kt`, after the existing overlay Boxes (the MacroExecutionPopup/ScanSurface/PromptDialog
    pattern, ~608-694), add a gated `DevThemeCyclerOverlay` Box: collect `container.devCyclerEnabled`
    (collectAsStateWithLifecycle, default false) and render the overlay ONLY when true:
    `onCycleStyle = { container.setThemeOverride(nextStyleOverride(currentOverride)) }`,
    `onCycleSize = { container.setThemeOverride(nextSizeOverride(currentOverride)) }`,
    `onDismiss = { container.setThemeOverride(null) }`. Track the current override locally by collecting
    `container.themeOverride` so the steppers advance from the live value. It must sit ABOVE the
    `when(dest)` content (drawn after it) so it floats over EVERY Dest — this is what makes it a true
    walk-the-app tool (D-08). Do not add it to any swipe-suppress logic (it's chrome, not a route).
  </action>
  <verify>
    <automated>cmd.exe /c "E:\Android\gw.bat :app:assembleDebug --no-daemon" 2>&1 | tr -d '\r' | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `AppShell.kt` renders `DevThemeCyclerOverlay` inside a Box gated on `container.devCyclerEnabled`, drawn AFTER `when(dest)`
    - The overlay wires `onCycleStyle`/`onCycleSize`/`onDismiss` to `setThemeOverride(nextStyleOverride/nextSizeOverride/null)`
    - `:app:assembleDebug` BUILD SUCCESSFUL
  </acceptance_criteria>
  <done>The cycler overlays float over every screen when dev-enable is on, off otherwise; the app builds.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: On-device cycler-walk verification (whole-app theme flip + restore)</name>
  <what-built>The two floating cycler widgets (Style + Size), wired into AppShell as a dev-enable-gated overlay above all routes, pushing transient ThemeOverrides into the override layer so the whole live app re-themes without persisting.</what-built>
  <how-to-verify>
    On flox (build + sign + install a RELEASE APK so we prove it works in release, NOT debug):
    1. Go to About (or wherever the dev-enable toggle lands — if About isn't built yet, temporarily flip the persisted boolean; the durable toggle lands in 15.2-04). Enable the dev widgets.
    2. Walk Home/Print-Status → Move → Temperature → Files → Console.
    3. Tap the STYLE cycler 6 times — confirm every surface flips through dark/light x Colorful/Simple/High-Contrast, INCLUDING the classic-Views surfaces (the temperature GraphView, the bed-mesh heatmap, the webcam frame border). Confirm the SEED/accent stays the user's real one (not a demo palette).
    4. Tap the TEXT-SIZE cycler S/M/L — confirm font scale flips app-wide, independently of the style cycler.
    5. Dismiss/disable — confirm the app snaps back to the real saved per-profile theme with NO drift.
    6. Confirm the widget is STATIC (no breathing/looping animation) and adds no visible jank on the Adreno-320.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (which surface didn't flip / seed changed / didn't restore / animation present / jank)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| widget tap → in-memory ThemeOverride | Cosmetic; no network/printer/auth effect |
| dev-enable gate → overlay visibility | A local boolean controls whether the chrome renders |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15.2-02-01 | Elevation | Cycler reachable in release by an end user | accept | Re-themes transiently only, persists nothing, no printer command; gated behind the hidden dev-enable (default false) — RESEARCH Security Domain accepts |
| T-15.2-02-02 | Denial of Service | Continuous animation burns the Adreno-320 budget | mitigate | LAW-enforced STATIC widget — no looping transitions (grep-clean of infiniteRepeatable); verified on-device for jank |
| T-15.2-02-SC | Tampering | package installs | n/a | No package installs — internal-codebase only |

No high-severity threats — local cosmetic dev tool, no new network/auth/attack surface.
</threat_model>

<verification>
- `:app:testDebugUnitTest --tests works.mees.dinghy.theme.StyleCyclerTest --tests works.mees.dinghy.theme.TextSizeCyclerTest` GREEN
- `:app:assembleDebug` BUILD SUCCESSFUL
- On-device cycler-walk: whole app flips across 6 styles incl. Views surfaces, S/M/L independent, restores on dismiss, static widget
</verification>

<success_criteria>
- Style cycler steps exactly the 6 combos with the real seed (D-06); size cycler steps S/M/L independently (D-07)
- Whole live app re-themes on tap and restores on dismiss, persisting nothing (D-08)
- Widget is static (Adreno-320 motion LAW)
</success_criteria>

<output>
Create `.planning/phases/15.2-theme-conformance-sweep-settings-ia/15.2-02-SUMMARY.md` when done
</output>
