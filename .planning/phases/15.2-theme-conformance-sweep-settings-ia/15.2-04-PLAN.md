---
phase: 15.2-theme-conformance-sweep-settings-ia
plan: 04
type: execute
wave: 4
depends_on: ["15.2-01", "15.2-02", "15.2-03"]
files_modified:
  - app/src/main/java/works/mees/dinghy/ui/route/TopRoute.kt
  - app/src/main/java/works/mees/dinghy/ui/shell/AppDrawer.kt
  - app/src/main/java/works/mees/dinghy/ui/shell/AppShell.kt
  - app/src/main/java/works/mees/dinghy/ui/screen/ThemeScreen.kt
  - app/src/main/java/works/mees/dinghy/ui/screen/SettingsScreen.kt
  - app/src/main/java/works/mees/dinghy/ui/screen/AboutScreen.kt
  - app/src/test/java/works/mees/dinghy/shell/ShellNavStateTest.kt
autonomous: false
requirements: [SET-01, THEME-01, THEME-02]
must_haves:
  truths:
    - "Four App-Drawer destinations exist — Printers · Theme · Settings · About — each its own tile routing distinctly; there is no longer one monolithic Settings page (D-01)"
    - "Theme is its own per-printer drawer destination controlling look (theme/accent/text-size); the existing ThemeEditor content is promoted here (D-03)"
    - "Settings is its own per-printer drawer destination controlling functionality (per-profile feature toggles); the webcam toggle persists AND gates the Webcam tile via webcamTileEnabled (D-04/MEDIUM-4)"
    - "About is the 4th drawer destination for app-global items (version/about) plus the hidden dev-widget enable toggle from D-08; turning it OFF clears any active override so the app is never stranded (D-05/HIGH-5)"
    - "The Devices→Printers rename leaves no dangling Dest/nav ref; the swipe-suppress set + the drawer subtitle still resolve (D-02)"
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/ui/screen/ThemeScreen.kt"
      provides: "The promoted ThemeEditor content as its own Dest"
      contains: "ThemeScreen"
    - path: "app/src/main/java/works/mees/dinghy/ui/screen/AboutScreen.kt"
      provides: "App-global About surface + the dev-enable toggle (off clears override)"
      contains: "AboutScreen"
    - path: "app/src/main/java/works/mees/dinghy/ui/route/TopRoute.kt"
      provides: "Dest.Theme + Dest.About added; Dest.Settings kept; Devices→Printers label"
      contains: "Theme"
    - path: "app/src/test/java/works/mees/dinghy/shell/ShellNavStateTest.kt"
      provides: "Host-pure proof the 4 IA destinations route distinctly with no dangling ref"
  key_links:
    - from: "app/src/main/java/works/mees/dinghy/ui/shell/AppDrawer.kt"
      to: "Dest.Theme / Dest.About / Dest.Settings / Dest.Devices(Printers)"
      via: "DRAWER_TILES entries (4 IA tiles, unique symbols)"
      pattern: "DrawerTileSpec"
    - from: "app/src/main/java/works/mees/dinghy/ui/shell/AppShell.kt"
      to: "AppContainer.webcamTileEnabled"
      via: "the drawer webcamEnabled gate now collects webcamTileEnabled (count × toggle)"
      pattern: "webcamTileEnabled"
    - from: "app/src/main/java/works/mees/dinghy/ui/screen/AboutScreen.kt"
      to: "AppContainer.setDevCyclerEnabled"
      via: "the hidden dev-widget enable toggle"
      pattern: "setDevCyclerEnabled"
---

<objective>
Finish the Settings IA dissolve: split the monolithic Settings into the four drawer destinations
(Printers · Theme · Settings · About, D-01..D-05), promote the ThemeEditor content to its own Theme tile,
shrink Settings to per-profile feature toggles, build the new About surface (app-global + the dev-enable
toggle that lights up the cyclers from plan 02), and wire all four through the lean Dest/DRAWER_TILES/
when(dest) router. Rename the Devices tile label to Printers. Wire the webcam toggle to actually gate the
Webcam tile (MEDIUM-4).

Purpose: Clean page identity — Printers/Theme/Settings are per-printer, About is the app-global remainder;
the dev-enable toggle gets its durable home (D-05/D-08).
Output: ThemeScreen, AboutScreen, the shrunk SettingsScreen, the 4-tile routing, the webcam-tile gating
rewire, and host-pure nav tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15.2-theme-conformance-sweep-settings-ia/15.2-CONTEXT.md
@.planning/phases/15.2-theme-conformance-sweep-settings-ia/15.2-RESEARCH.md
@.planning/phases/15.2-theme-conformance-sweep-settings-ia/15.2-PATTERNS.md
@.planning/phases/15.2-theme-conformance-sweep-settings-ia/15.2-REVIEWS.md
@.planning/phases/15.2-theme-conformance-sweep-settings-ia/15.2-01-PLAN.md
@.planning/phases/15.2-theme-conformance-sweep-settings-ia/15.2-03-PLAN.md
@docs/ui_design/LAYOUT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Promote ThemeEditor → ThemeScreen (own Dest); shrink SettingsScreen to per-profile toggles (webcam toggle wired to gating); build AboutScreen + dev-enable toggle</name>
  <files>app/src/main/java/works/mees/dinghy/ui/screen/ThemeScreen.kt, app/src/main/java/works/mees/dinghy/ui/screen/SettingsScreen.kt, app/src/main/java/works/mees/dinghy/ui/screen/AboutScreen.kt</files>
  <read_first>
    - .planning/phases/15.2-theme-conformance-sweep-settings-ia/15.2-REVIEWS.md (MEDIUM-4 webcam toggle gates behavior; HIGH-5 dev-enable off clears override)
    - .planning/phases/15.2-theme-conformance-sweep-settings-ia/15.2-PATTERNS.md (ThemeScreen + SettingsScreen-shrunk section; AboutScreen section: the dev-enable boolean persist via ThemePrefs/AppContainer.setDevCyclerEnabled; the Settings/Theme FFG-exempt note)
    - app/src/main/java/works/mees/dinghy/ui/screen/ThemeEditorScreen.kt (the existing theme content — color wheel + preset swatches + per-slot pool-override grid + Randomize/Reset/Done — promote to its own Dest screen)
    - app/src/main/java/works/mees/dinghy/ui/screen/SettingsScreen.kt (post-plan-03: Connection already removed; the remaining Theme entry + feature toggles — keep the toggles, route the Theme entry to the new Dest.Theme nav, remove inline theme content)
    - app/src/main/java/works/mees/dinghy/ui/screen/DevicesScreen.kt (ScreenScaffold Field-only + gutter-Back skeleton ~85-133 — the AboutScreen layout template)
    - app/src/main/java/works/mees/dinghy/di/AppContainer.kt (devCyclerEnabled + setDevCyclerEnabled from 15.2-01 — note setDevCyclerEnabled(false) already clears the override, HIGH-5; setActiveWebcamEnabled + webcamTileEnabled from 15.2-03)
    - docs/ui_design/THEMING.md + docs/ui_design/LAYOUT.md (Settings/Theme/About are FFG-exempt keyboard surfaces; the gutter-Back rule for scrollable Fields)
  </read_first>
  <action>
    Create `ThemeScreen.kt` by promoting the `ThemeEditorScreen.kt` content to its own Dest-level screen
    (color wheel + preset swatches + per-slot pool-override grid + Randomize/Reset/Done). Theme is per-printer
    (D-03) — its writes route through the existing per-profile setActive* helpers (writeScope). Keep
    ThemeEditorScreen as the inner content if reused, or fold it in — but the navigable target is `ThemeScreen`.
    Shrink `SettingsScreen.kt` to per-profile FEATURE TOGGLES only (D-04): the webcam toggle (and any other
    feature flags). The webcam toggle reads the active profile's `webcamEnabled` and persists via
    `setActiveWebcamEnabled` (the plan-03 per-profile setter). MEDIUM-4: this toggle is the SAME field
    `webcamTileEnabled` gates on, so flipping it here greys/lights the Webcam drawer tile (the visible
    behavior the on-device checkpoint verifies — not just a stored boolean). Remove the inline theme content
    (now in ThemeScreen).
    Create `AboutScreen.kt` (D-05) using the DevicesScreen ScreenScaffold Field-only + gutter-Back skeleton:
    show the app-global items (version + build, "about"), and a "Developer" subsection with the hidden
    dev-widget enable toggle calling `container.setDevCyclerEnabled(on)` (reads `container.devCyclerEnabled`).
    HIGH-5: `setDevCyclerEnabled(false)` already clears any active override (implemented in 15.2-01), so
    turning the toggle off here both hides the cyclers AND restores the real theme — no stranded override.
    Per RESEARCH Open-Q1 recommendation: a plain About toggle in a Developer subsection (discoverable but out
    of the way) — NOT a tap-count easter egg, NOT BuildConfig.DEBUG. Every color via LocalTokens; fsSp font
    scale per [[dinghy-font-sizes-too-small]] (15sp metadata floor, 17–18 body, 20–22 titles).
  </action>
  <verify>
    <automated>cmd.exe /c "E:\Android\gw.bat :app:assembleDebug --no-daemon" 2>&1 | tr -d '\r' | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `ThemeScreen.kt` exists hosting the promoted theme content; its writes route through per-profile setActive* (writeScope)
    - `SettingsScreen.kt` shows ONLY per-profile feature toggles (no theme content, no connection); the webcam toggle persists via `setActiveWebcamEnabled` and is the same field `webcamTileEnabled` gates on
    - `AboutScreen.kt` exists with version/about + a Developer dev-enable toggle calling `setDevCyclerEnabled`; no `BuildConfig.DEBUG` gate
    - All three read `LocalTokens.current` (no raw `Color(0x…)` except sanctioned swatches) and use fsSp
    - `:app:assembleDebug` BUILD SUCCESSFUL
  </acceptance_criteria>
  <done>Theme/Settings/About surfaces exist with clean per-printer-vs-app-global identity; the webcam toggle gates the tile; the dev-enable toggle has its home and clears the override on off.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 4-tile routing — Dest.Theme + Dest.About + Devices→Printers label + DRAWER_TILES + AppShell when(dest) + webcam-gate rewire + swipe-suppress + nav test</name>
  <files>app/src/main/java/works/mees/dinghy/ui/route/TopRoute.kt, app/src/main/java/works/mees/dinghy/ui/shell/AppDrawer.kt, app/src/main/java/works/mees/dinghy/ui/shell/AppShell.kt, app/src/test/java/works/mees/dinghy/shell/ShellNavStateTest.kt</files>
  <read_first>
    - .planning/phases/15.2-theme-conformance-sweep-settings-ia/15.2-REVIEWS.md (MEDIUM-4 — the AppShell webcam gate must collect webcamTileEnabled; LOW-1 — repoint the Dest.Devices arm to the renamed PrintersScreen)
    - .planning/phases/15.2-theme-conformance-sweep-settings-ia/15.2-PATTERNS.md (IA routing section: Dest enum additions; DRAWER_TILES copy-the-Settings-entry + unique-symbol law; AppShell when(dest) +arms; swipe-suppress set; Back-stack normal pushes)
    - app/src/main/java/works/mees/dinghy/ui/route/TopRoute.kt (enum class Dest at :31 — add Theme + About, keep Settings + Devices)
    - app/src/main/java/works/mees/dinghy/ui/shell/AppDrawer.kt (DRAWER_TILES at :144-180; the Settings entry at :178; the Devices label+subtitle at :177/:107; the icon-no-repeat law)
    - app/src/main/java/works/mees/dinghy/ui/shell/AppShell.kt (when(dest) :440-606; the Dest.Settings arm ~602-605 + Dest.Devices arm ~596-601 to copy; the swipe-suppress set at ~431; the `webcamEnabled = webcamCount > 0` at :183 — repoint to collect container.webcamTileEnabled)
    - app/src/main/java/works/mees/dinghy/ui/shell/ShellNavState.kt (navigateTo at :74-87 — only PrintStatus clears the stack; the 4 IA dests are normal pushes)
    - app/src/test/java/works/mees/dinghy/shell/ShellNavStateTest.kt (existing nav test to extend — if absent, mirror the nearest shell test)
  </read_first>
  <behavior>
    - Test four_ia_destinations_route_distinctly: navigating to each of Printers(Dest.Devices)/Theme/Settings/About pushes a distinct dest and back-pops correctly (normal pushes, only PrintStatus clears)
    - Test devices_rename_no_dangling_ref: the Dest enum + DRAWER_TILES + when(dest) all reference the same kept Dest.Devices constant (the label change is lexical; no enum value is orphaned)
  </behavior>
  <action>
    Extend the lean router (NOT Navigation-Compose — RESEARCH Pattern 4): add `Theme` + `About` to the `Dest`
    enum (keep `Settings`, keep `Devices`). Add `DrawerTileSpec` entries for Theme + About to `DRAWER_TILES`
    (copy the Settings entry at :178), each with a UNIQUE Material Symbols `symbol` not used by another tile
    (e.g. Theme="palette", About="info" — verify uniqueness against the existing list, icon-no-repeat law).
    Change the Devices tile LABEL "Devices"→"Printers" at AppDrawer.kt:177 (keep the subtitle check at :107
    pointed at Dest.Devices). Add `Dest.Theme ->` and `Dest.About ->` arms to the AppShell `when(dest)` (copy
    the Dest.Settings arm); repoint the `Dest.Devices ->` arm to `PrintersScreen` (the renamed screen from
    plan 03, LOW-1).
    MEDIUM-4 webcam gate rewire: change AppShell.kt:183 from `val webcamEnabled = webcamCount > 0` to collect
    the new gating flow — `val webcamEnabled by container.webcamTileEnabled.collectAsStateWithLifecycle(initialValue = false)`
    — so the per-profile webcam toggle (Settings) actually greys/lights the drawer Webcam tile. The downstream
    `AppDrawer(webcamEnabled = …)` threading is unchanged; only the SOURCE of the boolean changes from
    capability-only to capability×toggle.
    Add `Dest.Theme`, `Dest.Settings`, `Dest.About` to the swipe-suppress set at AppShell.kt:431 IF they
    scroll (they are FFG-exempt keyboard surfaces likely to scroll, RESEARCH A2) — each suppressed surface
    needs an explicit gutter Back (the DevicesScreen rule). The 4 IA dests are normal ShellNavState pushes —
    no special handling. Flesh out `ShellNavStateTest` to the behaviors above.
  </action>
  <verify>
    <automated>cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --tests works.mees.dinghy.shell.ShellNavStateTest --no-daemon" 2>&1 | tr -d '\r' | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `ShellNavStateTest` GREEN (4 dests route distinctly; no dangling ref)
    - `Dest` enum contains Theme + About + Settings + Devices; DRAWER_TILES has Theme + About tiles with unique symbols + the Devices tile relabeled "Printers"
    - AppShell `when(dest)` has Dest.Theme/Dest.About arms + Dest.Devices→PrintersScreen; the three keyboard surfaces are swipe-suppressed with explicit gutter Backs
    - AppShell's webcam tile gate now collects `container.webcamTileEnabled` (not the bare `webcamCount > 0`) — grep: `webcamTileEnabled` present at the gate site
    - `grep -rn "Dest.Devices" app/src` resolves to the kept constant (no orphaned enum value); `:app:assembleDebug` BUILD SUCCESSFUL
  </acceptance_criteria>
  <done>Four drawer destinations route distinctly; the Devices→Printers rename is dangling-ref-free; the webcam tile gates on the per-profile toggle; the app builds.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: On-device four-tile IA verification (Printers/Theme/Settings/About)</name>
  <what-built>The four-tile Settings IA: Printers (absorbs Connection), Theme (promoted editor, per-printer), Settings (per-profile toggles), About (app-global + the dev-enable toggle), all routed through the App Drawer.</what-built>
  <how-to-verify>
    On flox (build + install):
    1. Swipe up the App Drawer — confirm four distinct tiles: Printers, Theme, Settings, About (each unique icon; Printers carries the active-printer-name subtitle).
    2. Printers — add/remove/switch a printer works; editing host/port/key works and saves; the API key field is NOT pre-filled with the raw key; saving WITHOUT retyping the key preserves it (reconnect still works); an explicit Clear empties it.
    3. Theme — the color wheel / presets / Done work; changing theme changes ONLY the active printer's look (per-printer).
    4. Settings — the webcam (feature) toggle flips per-printer (toggle on printer A, switch to B, confirm independent) AND the Webcam DRAWER TILE greys when the toggle is OFF / lights when ON (MEDIUM-4 — the toggle gates the tile, not just persists).
    5. About — shows version/build; the Developer dev-enable toggle turns the cycler widgets on/off (cross-check with the plan-02 cyclers appearing); turning it OFF while an override is active also restores the real theme (HIGH-5).
    6. Confirm Back is present + Neutral on each scrollable IA surface, in a consistent gutter slot.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (tile missing/wrong icon / connection edit broken / key wiped on blank save / theme not per-printer / toggle global / webcam toggle doesn't gate tile / dev toggle doesn't gate cyclers or doesn't restore on off / Back wrong)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev-enable toggle (About) → DataStore | App-global boolean controlling the cycler chrome; off clears the override |
| Connection editing (now in Printers, plan 03) | Validation/redaction/key-semantics preserved there; About/Theme/Settings touch no secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15.2-04-01 | Elevation | The dev-enable toggle is reachable by an end user in release | accept | It only lights up the transient cosmetic cyclers (which persist nothing); default false; no printer command — RESEARCH Security Domain accepts |
| T-15.2-04-02 | Spoofing | A dangling Dest/nav ref after the rename routes to a dead/wrong screen | mitigate | Keep the `Dest.Devices` enum constant; host-pure `ShellNavStateTest` asserts the 4 dests route distinctly with no orphaned value; grep gate for dangling refs |
| T-15.2-04-03 | Denial of Service | Override stranded after disabling dev widgets in About | mitigate | `setDevCyclerEnabled(false)` clears the override (HIGH-5, implemented in 15.2-01); on-device checkpoint confirms the real theme returns on toggle-off |
| T-15.2-04-SC | Tampering | package installs | n/a | No package installs — internal-codebase only |

No high-severity threats — UI/IA reshuffle; the only persisted new state is a cosmetic dev-enable boolean.
</threat_model>

<verification>
- `:app:testDebugUnitTest --tests works.mees.dinghy.shell.ShellNavStateTest` GREEN
- `:app:assembleDebug` BUILD SUCCESSFUL
- Four drawer tiles route distinctly; Devices→Printers rename dangling-ref-free
- AppShell webcam gate collects `webcamTileEnabled` (MEDIUM-4)
- On-device: each tile works, Theme/Settings are per-printer, webcam toggle gates the tile, About gates the cyclers and clears the override on off
</verification>

<success_criteria>
- Four App-Drawer destinations (Printers · Theme · Settings · About) with clean identity (D-01..D-05)
- Theme + Settings are per-printer; About is app-global and homes the dev-enable toggle (off clears override, HIGH-5)
- The webcam toggle gates the Webcam tile via webcamTileEnabled (MEDIUM-4)
- Devices→Printers rename leaves no dangling reference (D-02)
</success_criteria>

<output>
Create `.planning/phases/15.2-theme-conformance-sweep-settings-ia/15.2-04-SUMMARY.md` when done
</output>
