---
phase: 15.2-theme-conformance-sweep-settings-ia
plan: 05
type: execute
wave: 5
depends_on: ["15.2-02", "15.2-04"]
files_modified:
  - app/src/main/java/works/mees/dinghy/ui/extrude/ExtrudeScreen.kt
  - app/src/main/java/works/mees/dinghy/ui/temperature/TemperatureScreen.kt
  - app/src/main/java/works/mees/dinghy/ui/spool/SpoolScreen.kt
  - app/src/main/java/works/mees/dinghy/ui/webcam/WebcamScreen.kt
  - app/src/main/java/works/mees/dinghy/ui/console/ConsoleScreen.kt
  - app/src/main/java/works/mees/dinghy/ui/screen/DevicesScreen.kt
  - docs/ui_design/THEMING.md
  - .planning/phases/15.2-theme-conformance-sweep-settings-ia/15.2-AUDIT.md
autonomous: false
requirements: [UI-02, THEME-01]
must_haves:
  truths:
    - "Back is Intent.Neutral (token = outline) on every screen with a Back affordance, in a consistent gutter position — matching the MoveScreen reference (15.1 D-10)"
    - "The enumerated Back-intent offenders (Extrude=Go, Temperature=Go, Spool=Danger, Webcam=Danger, Console=green-custom, Devices=Go) are all flipped to Neutral"
    - "The D-10 guided on-device review of the ~5–10 core screens surfaces latent criteria that are folded into docs/ui_design/THEMING.md + the AUDIT.md checklist before the app-wide audit runs"
    - "Every MECHANICAL conformance finding on the CORE screens (not just new LAW criteria) is recorded in AUDIT.md and tagged mechanical so it explicitly flows into plan 06 Task 2's fix pass (MEDIUM-1)"
  artifacts:
    - path: "docs/ui_design/THEMING.md"
      provides: "Any newly-surfaced conformance criteria appended (D-10) so they propagate to the rest of the sweep + future phases"
      contains: "conformance"
    - path: ".planning/phases/15.2-theme-conformance-sweep-settings-ia/15.2-AUDIT.md"
      provides: "Core-screen rows scored; the dimension checklist hardened with any new criteria; core-screen mechanical findings tagged for plan-06 fix"
      contains: "Back"
  key_links:
    - from: "app/src/main/java/works/mees/dinghy/ui/extrude/ExtrudeScreen.kt"
      to: "designsystem/control/OutlinedControl Intent.Neutral"
      via: "the Back OutlinedControl intent flip"
      pattern: "Intent.Neutral"
    - from: "app/src/main/java/works/mees/dinghy/ui/console/ConsoleScreen.kt"
      to: "t.outline"
      via: "the custom BackControl token flip from t.go to t.outline"
      pattern: "outline"
---

<objective>
Do the single largest conformance fix — the app-wide Back-intent sweep (flip every Back from its ad-hoc
red/green to Neutral, matching the MoveScreen reference per 15.1 D-10) — AND run the D-10 GUIDED on-device
review of the ~5–10 daily-driver core screens. The guided batch's PURPOSE is to surface any latent
design/UI criteria not yet captured in the LAW, folding findings into THEMING.md + the AUDIT checklist
BEFORE the app-wide audit (plan 06) runs on top of the hardened criteria.

Per the cross-AI review (MEDIUM-1): the guided batch ALSO records every MECHANICAL conformance finding on
the core screens (not just new LAW criteria) and tags it mechanical so it explicitly flows into plan 06's
fix pass — the core screens are not exempt from the long-tail fix loop.

Purpose: Lock the conformance criteria and clear the biggest mechanical bucket before the long-tail sweep.
Output: every Back = Neutral, a hardened THEMING.md + AUDIT.md checklist, core-screen audit rows scored,
and the core-screen mechanical findings tagged for plan 06.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15.2-theme-conformance-sweep-settings-ia/15.2-CONTEXT.md
@.planning/phases/15.2-theme-conformance-sweep-settings-ia/15.2-RESEARCH.md
@.planning/phases/15.2-theme-conformance-sweep-settings-ia/15.2-PATTERNS.md
@.planning/phases/15.2-theme-conformance-sweep-settings-ia/15.2-REVIEWS.md
@.planning/phases/15.2-theme-conformance-sweep-settings-ia/15.2-AUDIT.md
@docs/ui_design/THEMING.md
@docs/ui_design/CLAUDE.md
@docs/ui_design/LAYOUT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: App-wide Back-intent sweep — flip every offender to Intent.Neutral (token = outline)</name>
  <files>app/src/main/java/works/mees/dinghy/ui/extrude/ExtrudeScreen.kt, app/src/main/java/works/mees/dinghy/ui/temperature/TemperatureScreen.kt, app/src/main/java/works/mees/dinghy/ui/spool/SpoolScreen.kt, app/src/main/java/works/mees/dinghy/ui/webcam/WebcamScreen.kt, app/src/main/java/works/mees/dinghy/ui/console/ConsoleScreen.kt, app/src/main/java/works/mees/dinghy/ui/screen/DevicesScreen.kt</files>
  <read_first>
    - .planning/phases/15.2-theme-conformance-sweep-settings-ia/15.2-PATTERNS.md (FACT 3 — the enumerated Back-intent offender table with file:line + the one-token fix; the OutlinedControl Intent→token map at 48-54; the MoveScreen.kt:228 Neutral reference; the ConsoleScreen custom-BackControl exception)
    - .planning/phases/15.2-theme-conformance-sweep-settings-ia/15.2-RESEARCH.md (Pitfall 3 — Back-intent is the real sweep weight; Pitfall 4 — Back POSITION not just color)
    - app/src/main/java/works/mees/dinghy/ui/move/MoveScreen.kt (the canonical Intent.Neutral Back at ~223-231 with the D-10 doc-comment — the reference)
    - app/src/main/java/works/mees/dinghy/designsystem/control/OutlinedControl.kt (the Intent enum + outlineColor map 39-54: Neutral→t.outline, Go→t.go, Danger→t.stop)
    - docs/ui_design/CLAUDE.md (the button-intent grammar — Back = neutral per 15.1 D-10) + docs/ui_design/LAYOUT.md (Back gutter-position rule)
    - Each offender file at the line in the FACT-3 table: ExtrudeScreen ~287, TemperatureScreen ~180, SpoolScreen ~144, WebcamScreen ~128, ConsoleScreen ~178-200, DevicesScreen ~128
  </read_first>
  <action>
    Flip every Back affordance to `Intent.Neutral` (resolves to `t.outline`), matching the MoveScreen
    reference. Concrete edits (verify each line by grep at execute-time — 15.1 fixes + the plan-03 Devices→
    Printers rename may have shifted them):
    `ExtrudeScreen.kt` Back `Intent.Go`→`Intent.Neutral`; `TemperatureScreen.kt` Back `Intent.Go`→`Intent.Neutral`;
    `SpoolScreen.kt` Back `Intent.Danger`→`Intent.Neutral`; `WebcamScreen.kt` Back `Intent.Danger`→`Intent.Neutral`;
    `DevicesScreen.kt` Back `Intent.Go`→`Intent.Neutral` (note: plan-03 renamed Devices→PrintersScreen — apply to
    whichever file/function now owns the Back; check `PrintersScreen.kt`). `ConsoleScreen.kt` is the EXCEPTION —
    its custom `BackControl` (~178-200) hand-rolls a green Back OUTSIDE OutlinedControl; flip its token from
    `t.go` to `t.outline` (or replace with `OutlinedControl(intent = Intent.Neutral)`). Do NOT touch
    Move/Files (already Neutral). Also score the Back POSITION (Pitfall 4) per screen against LAYOUT.md's
    gutter-position rule — note any position outliers in AUDIT.md (the on-device task confirms position; this
    task records it). This is a MECHANICAL fix bucket (D-13 fix-silently) — no owner gate needed for the color flips.
  </action>
  <verify>
    <automated>cmd.exe /c "E:\Android\gw.bat :app:assembleDebug --no-daemon" 2>&1 | tr -d '\r' | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "Intent.Danger\|Intent.Go" app/src/main/java/works/mees/dinghy/ui/extrude/ExtrudeScreen.kt app/src/main/java/works/mees/dinghy/ui/temperature/TemperatureScreen.kt app/src/main/java/works/mees/dinghy/ui/spool/SpoolScreen.kt app/src/main/java/works/mees/dinghy/ui/webcam/WebcamScreen.kt` shows NONE of these on a Back/onBack control (only on legitimately destructive/positive controls)
    - ConsoleScreen's BackControl resolves to `t.outline` (not `t.go`)
    - DevicesScreen/PrintersScreen Back is `Intent.Neutral`
    - MoveScreen + FilesScreen Back untouched (still Neutral)
    - `:app:assembleDebug` BUILD SUCCESSFUL
  </acceptance_criteria>
  <done>Every Back affordance is Neutral/outline app-wide; Back positions noted against LAYOUT.md.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: D-10 guided core-screen review (surface latent criteria + record core-screen mechanical findings)</name>
  <what-built>The D-10 GUIDED on-device review batch: the ~5–10 daily-driver core screens (Home/Print-Status, Move, Temperature, Files, Print controls, Console, plus the new IA surfaces Settings/Theme/Printers/About), walked screen-by-screen with the cycler widgets, to surface any latent design/UI criteria the LAW hasn't captured yet — Back-intent + position now fixed, so this is about catching the UNWRITTEN rules before the app-wide audit AND logging any concrete mechanical violations on these core screens.</what-built>
  <how-to-verify>
    On flox (build + install; enable the dev cyclers in About):
    1. Walk each core screen: Print-Status → Move → Temperature → Files → Console → Settings → Theme → Printers → About.
    2. On each, tap the style cycler through the 6 combos + the size cycler S/M/L and eyeball: token purity (no off-theme chrome), button-intent correctness, FFG grammar (printer-control screens only — Settings/Theme/About are FFG-exempt), ≥64px targets, the fsSp scale (HARD eyeball per [[dinghy-font-sizes-too-small]] — 15sp metadata floor, 17–18 body, 20–22 titles, 26 stats, 30+ focus), dark/light/custom correctness, and 15.1 conformance (shape-status, pool→role, Back=Neutral+consistent position).
    3. Call out any NEW criterion not already in THEMING.md (e.g. "icon-only buttons need a min label" / "this gutter slot should be reserved" / "stat numbers must be tabular at this size"). These are the latent criteria the batch exists to find.
    4. ALSO call out any concrete MECHANICAL violation you see on these CORE screens (a too-small font, an off-theme color, a wrong intent, a sub-64px target) — these get recorded + tagged mechanical so plan 06 fixes them (MEDIUM-1; the core screens are NOT exempt from the fix loop).
    5. For each finding, decide: mechanical fix (note it for plan 06) vs new LAW criterion (fold into THEMING.md).
  </how-to-verify>
  <resume-signal>Type "approved" with the list of latent criteria to fold + any per-screen mechanical findings (or "no new criteria / no mechanical findings")</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Fold the D-10 findings into THEMING.md + the AUDIT.md checklist; score the core-screen rows; tag core-screen mechanical findings for plan 06</name>
  <files>docs/ui_design/THEMING.md, .planning/phases/15.2-theme-conformance-sweep-settings-ia/15.2-AUDIT.md</files>
  <read_first>
    - .planning/phases/15.2-theme-conformance-sweep-settings-ia/15.2-REVIEWS.md (MEDIUM-1 — core-screen mechanical findings must flow into plan 06 Task 2)
    - docs/ui_design/THEMING.md (the canonical 6-dimension token-law from 15.1-08 — APPEND newly-surfaced criteria per D-10; do NOT re-litigate locked semantics)
    - .planning/phases/15.2-theme-conformance-sweep-settings-ia/15.2-AUDIT.md (the scorecard scaffold from plan 01 — harden the dimension columns, score the core-screen rows)
    - the resume-signal findings from Task 2 (the latent criteria + per-screen results + the mechanical findings the owner reported)
  </read_first>
  <action>
    Append any new conformance criteria surfaced in the Task-2 guided review to `docs/ui_design/THEMING.md`
    (D-10 — so they propagate to the rest of the sweep and to future phases 16–21), keeping the file's
    existing structure and NOT re-litigating the locked 15.1 semantics (pool→role / shape-status / N-series
    / Back-neutral). Mirror each new criterion into the `15.2-AUDIT.md` dimension checklist so plan 06's
    app-wide scorecard tests against the hardened list. Score the core-screen rows in AUDIT.md from the
    guided review (check / ! / X + one-line finding per dimension), and tag each finding mechanical
    (plan-06 fix) vs judgment-call (owner-flagged, D-13).
    MEDIUM-1 — make the seam explicit: record the CORE-SCREEN mechanical findings (the concrete violations
    from checkpoint step 4) in AUDIT.md under their core-screen rows, each tagged `mechanical (plan-06)`, in
    the SAME tag vocabulary the long-tail rows use — so plan 06 Task 2 picks them up alongside its own
    Task-1 long-tail findings (not just the long-tail). If the owner reported "no new criteria / no mechanical
    findings," record that explicitly (the LAW is already complete) and just score the rows clean.
  </action>
  <verify>
    <automated>grep -c "mechanical (plan-06)\|check\|!\|X" .planning/phases/15.2-theme-conformance-sweep-settings-ia/15.2-AUDIT.md | head -1; echo "AUDIT core rows scored + core mechanical tagged"</automated>
  </verify>
  <acceptance_criteria>
    - THEMING.md carries any new D-10 criteria (or a note that none were surfaced), with locked 15.1 semantics untouched
    - 15.2-AUDIT.md's core-screen rows are scored with per-dimension findings + mechanical/judgment tags
    - Core-screen MECHANICAL findings are tagged `mechanical (plan-06)` so they explicitly flow into plan 06 Task 2 (MEDIUM-1) — or an explicit "no core mechanical findings" note is recorded
    - The AUDIT dimension checklist reflects the hardened criteria for plan 06 to use
  </acceptance_criteria>
  <done>The conformance criteria are hardened in THEMING.md + AUDIT.md; core-screen rows scored; core-screen mechanical findings tagged for plan 06.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan edits UI intent tokens + docs; no network/auth/data boundary crossed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15.2-05-01 | Tampering | A Back-intent flip accidentally changes a genuinely-destructive control's color | mitigate | Edits target only Back/onBack controls (enumerated by file:line); grep gate confirms remaining Danger/Go intents are on legitimately destructive/positive controls; on-device review confirms |
| T-15.2-05-SC | Tampering | package installs | n/a | No package installs — internal-codebase only |

No high-severity threats — cosmetic intent tokens + documentation; no attack surface.
</threat_model>

<verification>
- `:app:assembleDebug` BUILD SUCCESSFUL after the Back sweep
- No Back/onBack control resolves to Danger/Go (grep gate)
- THEMING.md + AUDIT.md carry the hardened criteria; core-screen rows scored
- Core-screen mechanical findings tagged `mechanical (plan-06)` (MEDIUM-1)
- On-device guided review signed off by the owner with the latent-criteria + mechanical-findings list
</verification>

<success_criteria>
- Back is Neutral + consistent position app-wide (15.1 D-10 enforced — the largest fix bucket)
- The D-10 guided batch surfaced latent criteria, folded into THEMING.md + AUDIT.md before the app-wide audit (D-10)
- Core-screen audit rows scored (D-12); core-screen mechanical findings explicitly routed to plan 06 (MEDIUM-1)
</success_criteria>

<output>
Create `.planning/phases/15.2-theme-conformance-sweep-settings-ia/15.2-05-SUMMARY.md` when done
</output>
