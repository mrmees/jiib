---
phase: 15.2-theme-conformance-sweep-settings-ia
plan: 04
subsystem: ui
tags: [settings, ia, navigation, theme, compose, app-drawer, datastore]

# Dependency graph
requires:
  - phase: 15.2-01
    provides: canonical activeThemeTuple + PURE effectiveTokens override layer; setDevCyclerEnabled(false) clears the active override (HIGH-5)
  - phase: 15.2-02
    provides: DevThemeCyclerOverlay + devEnableFlow (the TEMP default-true flip this plan reverts)
  - phase: 15.2-03
    provides: PrintersScreen (absorbs Connection); per-profile setActiveWebcamEnabled + webcamTileEnabled gate; apiKey-edit helper
provides:
  - Four-tile App-Drawer IA — Printers · Theme · Settings · About — each a distinct Dest routed through the lean when(dest) router (D-01..D-05)
  - ThemeScreen (per-printer): promoted ThemeEditor content + the relocated Appearance section (dark/light + S/M/L + palette-mode chips + live preview)
  - SettingsScreen shrunk to per-profile feature toggles; the webcam toggle gates the Webcam drawer tile via webcamTileEnabled (MEDIUM-4)
  - AboutScreen (app-global): version/build + the durable dev-enable toggle; off clears any active override (HIGH-5/D-05/D-08)
  - Reverted the plan-02 TEMP devEnableFlow-default-true flip to false — the About toggle is now the durable enable path (MEDIUM-3)
  - Printer-switcher dev cycler in the dev overlay (dev-enable-gated, wrap-around)
affects: [16-home-redesign, 21-release-and-ship]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Lean Dest/DRAWER_TILES/when(dest) router extended (NOT Navigation-Compose) — 4 IA destinations as normal ShellNavState pushes; only PrintStatus clears the stack"
    - "Per-printer vs app-global page identity: Printers/Theme/Settings are per-profile; About is the app-global remainder + dev-enable home"
    - "Feature toggle gates a drawer tile (capability × per-profile toggle) via a collected StateFlow, not a bare capability count"

key-files:
  created:
    - app/src/main/java/works/mees/dinghy/ui/screen/ThemeScreen.kt
    - app/src/main/java/works/mees/dinghy/ui/screen/AboutScreen.kt
  modified:
    - app/src/main/java/works/mees/dinghy/ui/route/TopRoute.kt
    - app/src/main/java/works/mees/dinghy/ui/shell/AppDrawer.kt
    - app/src/main/java/works/mees/dinghy/ui/shell/AppShell.kt
    - app/src/main/java/works/mees/dinghy/ui/screen/SettingsScreen.kt
    - app/src/main/java/works/mees/dinghy/theme/ThemePrefs.kt
    - app/src/test/java/works/mees/dinghy/shell/ShellNavStateTest.kt

key-decisions:
  - "Appearance section (dark/light + S/M/L + palette-mode + live preview) relocated onto the Theme tile — Task 1's Settings shrink had deleted it without rehoming, orphaning setActiveDark/setActiveFs/setActiveMode (checkpoint finding 3)"
  - "Printer-switcher dev cycler added to the dev overlay (approved new scope from the checkpoint) — dev-enable-gated, wrap-around, single-profile no-op, reuses setActiveProfile"
  - "Webcam-tile gating verified-on-device DEFERRED to Phase 21 (owner-accepted; tile gates on the same field the toggle persists, code-confirmed)"
  - "Dev-overlay panel no longer drag-relocates after the chip-tap consume fix — owner-accepted (dev-only tool); Phase 21 todo"

patterns-established:
  - "Checkpoint-driven fix loop on an autonomous:false gate: relocate orphaned setters, add approved scope, fix a tap/drag gesture-bleed regression, re-verify on device"

requirements-completed: [SET-01, THEME-01, THEME-02]

# Metrics
duration: ~2h (incl. two on-device re-verify rounds)
completed: 2026-06-06
---

# Phase 15.2 Plan 04: Settings IA Dissolve (Four-Tile App Drawer) Summary

**The monolithic Settings page is dissolved into four distinct App-Drawer destinations — Printers · Theme · Settings · About — with per-printer Theme/Settings, an app-global About that homes the durable dev-enable toggle (off clears the override), the webcam toggle now gating the Webcam tile, and the plan-02 TEMP cyclers-on-by-default flip reverted to false.**

## Performance

- **Duration:** ~2h (includes two on-device re-verify rounds and three checkpoint-driven fix commits)
- **Completed:** 2026-06-06
- **Tasks:** 3 (2 auto + 1 checkpoint, PASSED)
- **Files modified:** 8 (2 created, 6 modified)

## Accomplishments
- Four App-Drawer destinations route distinctly (Printers · Theme · Settings · About) through the extended lean `when(dest)` router — no monolithic Settings page remains (D-01).
- `ThemeScreen` promoted as a per-printer Dest (color wheel + presets + per-slot pool grid), and after the checkpoint, the full Appearance section (dark/light + S/M/L + palette-mode + live preview) was relocated onto it.
- `SettingsScreen` shrunk to per-profile feature toggles; the webcam toggle gates the Webcam drawer tile via `webcamTileEnabled` (MEDIUM-4).
- `AboutScreen` built as the app-global surface with the durable dev-enable toggle; turning it OFF clears any active override so the app is never stranded (HIGH-5).
- Reverted the plan-02 TEMP `devEnableFlow`-default-true flip to `false` — the cyclers no longer ship on-by-default; the About toggle is the durable control (MEDIUM-3; grep-clean of `// TEMP(15.2-02)`).
- Devices→Printers rename left no dangling Dest/nav ref (D-02); host-pure `ShellNavStateTest` proves the 4 dests route distinctly.

## Task Commits

1. **Task 1: Promote ThemeScreen + shrink Settings to toggles + AboutScreen + revert TEMP devEnable default** - `401c4db` (feat)
2. **Task 2: 4-tile IA routing — Dest.Theme/About + Printers label + webcam-tile gate + nav test** - `11c2ebf` (feat, tdd)
3. **Task 3: On-device four-tile IA verification (checkpoint:human-verify)** - PASSED by human ("approved" across two re-verify rounds)

**Checkpoint-driven fix commits (part of this plan):**
- `e9e59b3` (feat) - Finding 3: relocate the Appearance section onto the Theme tile (rehome the orphaned setActiveDark/setActiveFs/setActiveMode setters)
- `8af0111` (feat) - Finding 2 (approved new scope): printer-switcher dev cycler in the dev overlay + host-pure `PrinterCyclerTest`
- `0067514` (fix) - Finding 2 regression: chip taps no longer bleed into the panel-drag handler (`change.consume()`)
- `41e72b2` (docs) - capture deferred webcam-tile-gating verification as a Phase 21 todo
- `7a7d67b` (docs) - note dev-overlay panel-drag disabled (owner-accepted, Phase 21 todo)

## Files Created/Modified
- `app/src/main/java/works/mees/dinghy/ui/screen/ThemeScreen.kt` - Per-printer Theme Dest; promoted theme editor + relocated Appearance section.
- `app/src/main/java/works/mees/dinghy/ui/screen/AboutScreen.kt` - App-global About surface + durable dev-enable toggle (off clears override).
- `app/src/main/java/works/mees/dinghy/ui/route/TopRoute.kt` - Added Dest.Theme + Dest.About; kept Dest.Settings + Dest.Devices.
- `app/src/main/java/works/mees/dinghy/ui/shell/AppDrawer.kt` - Theme + About DRAWER_TILES (unique symbols); Devices tile relabeled "Printers".
- `app/src/main/java/works/mees/dinghy/ui/shell/AppShell.kt` - Dest.Theme/Dest.About arms; Dest.Devices→PrintersScreen; webcam gate collects `webcamTileEnabled`; swipe-suppress + the dev-overlay printer-switcher cycler.
- `app/src/main/java/works/mees/dinghy/ui/screen/SettingsScreen.kt` - Shrunk to per-profile feature toggles; inline theme content removed.
- `app/src/main/java/works/mees/dinghy/theme/ThemePrefs.kt` - `devEnableFlow` default reverted true→false; `// TEMP(15.2-02)` marker deleted.
- `app/src/test/java/works/mees/dinghy/shell/ShellNavStateTest.kt` - Host-pure proof the 4 IA dests route distinctly with no dangling ref.

## Decisions Made
- **Appearance relocation (finding 3):** Task 1's Settings shrink deleted the Appearance section without rehoming it, orphaning all three setters. Relocated to ThemeEditorScreen (the Theme tile) — its natural per-printer home.
- **Printer-switcher dev cycler (finding 2, approved):** Added to the dev overlay as an approved scope expansion during the checkpoint — dev-enable-gated, wrap-around, single-profile no-op, reuses `setActiveProfile`. Backed by a host-pure `PrinterCyclerTest`.
- **Chip-tap gesture-bleed (finding 2 regression):** Cycler chip taps were bleeding into the panel-drag handler; fixed with `change.consume()`.

## Deviations from Plan

The two auto-tasks executed as written. The deviations below arose from the on-device checkpoint and were handled as checkpoint-driven fixes (Rule 1/Rule 2), all approved by the owner.

### Auto-fixed Issues

**1. [Rule 1 - Bug] Appearance section orphaned by the Settings shrink**
- **Found during:** Task 3 (on-device checkpoint, finding 3)
- **Issue:** Task 1's Settings shrink deleted the Appearance section (dark/light + S/M/L + palette-mode + live preview) without relocating it; setActiveDark/setActiveFs/setActiveMode were orphaned (unreachable).
- **Fix:** Relocated the full Appearance section onto the Theme tile (ThemeEditorScreen).
- **Files modified:** ThemeEditorScreen / ThemeScreen path
- **Verification:** On-device re-verify — Theme tile shows the restored dark/light + S/M/L + palette-mode + preview; PASS.
- **Committed in:** `e9e59b3`

**2. [Rule 2 - Approved scope] Printer-switcher dev cycler**
- **Found during:** Task 3 (on-device checkpoint, finding 2)
- **Issue:** Owner requested a dev-overlay printer cycler to accompany the theme cyclers (approved new scope).
- **Fix:** Added a dev-enable-gated, wrap-around printer-switcher cycler reusing setActiveProfile; single-profile no-op; shows the active printer. Added host-pure `PrinterCyclerTest`.
- **Files modified:** AppShell.kt + PrinterCyclerTest
- **Verification:** On-device — cycler steps the active printer; PASS.
- **Committed in:** `8af0111`

**3. [Rule 1 - Bug] Chip-tap bleeds into panel drag**
- **Found during:** Task 3 (regression from fix #2)
- **Issue:** Cycler chip taps triggered the dev-overlay panel-drag handler.
- **Fix:** Added `change.consume()` so chip taps no longer reach the drag handler.
- **Files modified:** AppShell.kt (dev overlay)
- **Verification:** On-device — chip taps no longer trigger panel drag; PASS.
- **Committed in:** `0067514`

---

**Total deviations:** 3 (2 bugs auto-fixed, 1 approved scope addition) — all surfaced by the on-device checkpoint and owner-approved.
**Impact on plan:** All within the checkpoint's verify loop; no scope creep beyond the owner-approved printer cycler.

## Owner-Accepted Deferrals

Two items were owner-accepted as out-of-scope for now and captured as pending todos (not re-created here):
- **Webcam-tile-gating on-device verification** → `.planning/todos/pending/2026-06-05-webcam-tile-gating-verification.md` (deferred to Phase 21; the tile gates on the same field the toggle persists, code-confirmed).
- **Dev-overlay panel no longer drag-relocates** after the chip-tap consume fix → `.planning/todos/pending/2026-06-05-dev-overlay-panel-drag.md` (dev-only tool; deferred to Phase 21).

## Checkpoint Result

The Task 3 `checkpoint:human-verify` gate (on-device four-tile IA walk on flox) was **PASSED** by the owner ("approved" across two re-verify rounds):
- Drawer four tiles (Printers/Theme/Settings/About) — PASS.
- Printers connection edit + key-preserve semantics — PASS.
- Theme tile per-printer appearance incl. restored dark/light + S/M/L + palette-mode + preview — PASS.
- About dev toggle gates the cyclers + restores the real theme on off — PASS.
- Printer-switcher dev cycler steps the active printer; chip taps no longer trigger panel drag — PASS.
- Cyclers NOT visible by default (TEMP revert holds) — PASS.

## Issues Encountered
None beyond the three checkpoint-surfaced findings documented above (all fixed and re-verified live).

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- Settings IA is fully dissolved into four clean destinations; the foundation for Phase 16 (Home redesign) and the remaining feature-surface phases is in place.
- Two owner-accepted Phase 21 todos carried forward (webcam-tile gating live-verify; dev-overlay panel drag).

## Self-Check: PASSED

- Created files verified present: ThemeScreen.kt, AboutScreen.kt, 15.2-04-SUMMARY.md.
- Commits verified present: 401c4db, 11c2ebf, e9e59b3, 8af0111, 0067514.
- TEMP(15.2-02) marker grep returns empty (MEDIUM-3 revert holds).

---
*Phase: 15.2-theme-conformance-sweep-settings-ia*
*Completed: 2026-06-06*
