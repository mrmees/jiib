---
phase: 15.2-theme-conformance-sweep-settings-ia
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/main/java/works/mees/dinghy/theme/ThemeOverride.kt
  - app/src/main/java/works/mees/dinghy/di/AppContainer.kt
  - app/src/main/java/works/mees/dinghy/theme/compose/DinghyTheme.kt
  - app/src/main/java/works/mees/dinghy/MainActivity.kt
  - app/src/main/java/works/mees/dinghy/theme/ThemePrefs.kt
  - app/src/test/java/works/mees/dinghy/theme/ThemeOverrideTest.kt
  - app/src/test/java/works/mees/dinghy/theme/DevWidgetEnableTest.kt
  - .planning/phases/15.2-theme-conformance-sweep-settings-ia/15.2-AUDIT.md
autonomous: true
requirements: [THEME-01, THEME-02]
must_haves:
  truths:
    - "The whole live app (Compose + classic-Views surfaces) re-themes when a transient override is set, and snaps back to the real persisted per-profile theme when the override is cleared (D-06/D-08)"
    - "Setting or clearing the override writes NOTHING to DataStore — the persisted per-profile theme tuple is never touched (D-08)"
    - "The override uses the active profile's real seed/accent — only the cycled axes (dark, paletteMode, fs) are substituted (D-06/D-07)"
    - "The dev-widget enable is a persisted app-global boolean defaulting false, readable in a RELEASE build (NOT BuildConfig.DEBUG) (D-08)"
    - "The AUDIT.md scorecard artifact exists and lists every surface row to be filled by the sweep (D-12)"
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/theme/ThemeOverride.kt"
      provides: "Sparse nullable override tuple model + the override-aware effective-tokens derivation"
      contains: "data class ThemeOverride"
    - path: "app/src/main/java/works/mees/dinghy/di/AppContainer.kt"
      provides: "_themeOverride StateFlow + setThemeOverride + effectiveTokens derived flow + overrideResolver + devEnableFlow"
      contains: "effectiveTokens"
    - path: "app/src/main/java/works/mees/dinghy/theme/compose/DinghyTheme.kt"
      provides: "DinghyTheme collects the override-aware effective-tokens flow"
    - path: "app/src/test/java/works/mees/dinghy/theme/ThemeOverrideTest.kt"
      provides: "Host-pure override-null-passthrough / override-applies / dismiss-restores / no-persist coverage"
    - path: ".planning/phases/15.2-theme-conformance-sweep-settings-ia/15.2-AUDIT.md"
      provides: "The D-12 conformance scorecard artifact (surface x 6+1 LAW dimensions)"
  key_links:
    - from: "app/src/main/java/works/mees/dinghy/MainActivity.kt"
      to: "AppContainer.effectiveTokens"
      via: "DinghyTheme(... effective-tokens flow ...)"
      pattern: "effectiveTokens"
    - from: "app/src/main/java/works/mees/dinghy/di/AppContainer.kt"
      to: "overrideResolver (secondary ThemeResolver)"
      via: "combine(themeResolver.tokens, _themeOverride, activeProfile)"
      pattern: "overrideResolver"
---

<objective>
Build the transient theme-override LAYER — the engine that lets a single in-memory override flip the
whole live app (both Compose and classic-Views surfaces) without ever persisting, plus the app-global
dev-widget enable boolean and the Wave-0 RED scaffold + AUDIT.md artifact. This is the phase-early
foundation (D-09): both the guided review (D-10) and the app-wide audit (D-11) ride on the cycler,
and the cycler rides on this override layer.

Purpose: Make per-screen theme conformance checks cheap — one device walk per combo instead of six
rebuilds — without corrupting the persisted per-profile theme tuple.
Output: `ThemeOverride.kt`, the `AppContainer` override plumbing + `effectiveTokens`, the `DinghyTheme`
collect swap, the app-global dev-enable boolean, host-pure tests, and the AUDIT.md scaffold.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15.2-theme-conformance-sweep-settings-ia/15.2-CONTEXT.md
@.planning/phases/15.2-theme-conformance-sweep-settings-ia/15.2-RESEARCH.md
@.planning/phases/15.2-theme-conformance-sweep-settings-ia/15.2-PATTERNS.md
@.planning/phases/15.2-theme-conformance-sweep-settings-ia/15.2-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave-0 RED scaffold (ThemeOverrideTest + DevWidgetEnableTest) + AUDIT.md scorecard scaffold</name>
  <files>app/src/test/java/works/mees/dinghy/theme/ThemeOverrideTest.kt, app/src/test/java/works/mees/dinghy/theme/DevWidgetEnableTest.kt, .planning/phases/15.2-theme-conformance-sweep-settings-ia/15.2-AUDIT.md</files>
  <read_first>
    - .planning/phases/15.2-theme-conformance-sweep-settings-ia/15.2-VALIDATION.md (the Wave-0 requirements + per-task verification map)
    - app/src/test/java/works/mees/dinghy/theme/ThemeResolverTest.kt (existing host-pure theme test shape — imports, package, runner)
    - $HOME/.claude memory note [[dinghy-wave0-red-scaffold-compile]] context: Gradle compiles the WHOLE test source set before applying the --tests filter, so a stub that references unbuilt symbols bricks ALL per-wave runs
  </read_first>
  <action>
    Create `ThemeOverrideTest.kt` and `DevWidgetEnableTest.kt` in package `works.mees.dinghy.theme` as
    COMPILE-CLEAN RED scaffolds (per [[dinghy-wave0-red-scaffold-compile]]). Each test method body is a
    typed `fail("not yet implemented")` (org.junit.Assert.fail) — do NOT reference `ThemeOverride`,
    `effectiveTokens`, `setThemeOverride`, or `devEnableFlow` yet (those symbols are introduced in Tasks
    2-3 of THIS plan and the test bodies get fleshed out there). Name the test methods after the
    VALIDATION rows: `override_null_passes_through_persisted`, `override_nonnull_applies_baked_tokens`,
    `dismiss_restores_persisted_no_drift`, `override_writes_no_persistence`,
    `dev_enable_defaults_false_persists_app_global`. This makes the whole test source set compile so the
    per-wave `--tests` filter works.
    Then create `15.2-AUDIT.md` as the D-12 scorecard scaffold: a markdown table with one ROW per
    existing surface (enumerate from `ui/**` + `render/**`: PrintStatusScreen, MoveScreen,
    TemperatureScreen, ExtrudeScreen, FilesScreen, ConsoleScreen, SpoolScreen, WebcamScreen, the
    calibration screens [CalibrationHubScreen, TiltScreen, BedMeshScreen, ProbeCalibrateScreen,
    ScrewsTiltScreen], macros/PromptDialog, SettingsScreen, DevicesScreen/PrintersScreen,
    ThemeEditorScreen/ThemeScreen, AboutScreen, and the render Views: GraphView, BedMeshHeatmapView,
    ProgressRing, WebcamView). COLUMNS = the 6 LAW dimensions (Token-purity / Button-intent / FFG-grammar
    / >=64px / fsSp / dark-light-custom) PLUS a 7th "15.1-conformance" (shape-status / pool->role /
    Back-neutral+position), each cell scored placeholder `-` (to be filled with the check / ! / X +
    one-line finding during Waves 3-4). Add a short header noting the conflict policy D-13 (fix mechanical
    silently, flag judgment calls) and that sanctioned raw-color literals (PromptMarkupText author-hex,
    SpoolPicker filament swatches, theme swatches) are conformant per THEMING.md — do NOT flag them.
  </action>
  <verify>
    <automated>cmd.exe /c "E:\Android\gw.bat :app:compileDebugUnitTestKotlin --no-daemon" 2>&1 | tr -d '\r' | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `:app:compileDebugUnitTestKotlin` BUILD SUCCESSFUL (the whole test source set compiles with the new stubs)
    - `ThemeOverrideTest.kt` + `DevWidgetEnableTest.kt` exist with typed `fail()` bodies, zero references to symbols not yet built
    - `15.2-AUDIT.md` exists with a row per surface (~22 rows) and the 6+1 dimension columns, plus the D-13 + sanctioned-literals header note
  </acceptance_criteria>
  <done>Test source set compiles; both RED scaffolds and the AUDIT.md scorecard exist.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ThemeOverride model + AppContainer override layer + effectiveTokens + DinghyTheme/MainActivity wiring</name>
  <files>app/src/main/java/works/mees/dinghy/theme/ThemeOverride.kt, app/src/main/java/works/mees/dinghy/di/AppContainer.kt, app/src/main/java/works/mees/dinghy/theme/compose/DinghyTheme.kt, app/src/main/java/works/mees/dinghy/MainActivity.kt, app/src/test/java/works/mees/dinghy/theme/ThemeOverrideTest.kt</files>
  <read_first>
    - .planning/phases/15.2-theme-conformance-sweep-settings-ia/15.2-PATTERNS.md (Pattern Assignments: theme/ThemeOverride.kt + di/AppContainer.effectiveTokens — the copy-from-here shapes and the WHY-a-second-resolver rationale)
    - .planning/phases/15.2-theme-conformance-sweep-settings-ia/15.2-RESEARCH.md (Pattern 1: Override-ahead-of-persisted; Pitfall 1: persistence collector fights the cycler; Pitfall 2: Views surfaces don't flip — already mitigated per PATTERNS FACT 2)
    - app/src/main/java/works/mees/dinghy/di/AppContainer.kt (the _spine/asStateFlow publish idiom ~line 188-200; writeScope ~107; seedTheme collector ~320-345; setActiveDark/setActiveFs/setActivePaletteMode ~355-385; activeProfile ~140; themeResolver ~188)
    - app/src/main/java/works/mees/dinghy/theme/ThemeResolver.kt (apply() at line 114; tokens StateFlow at 47; the compute() fail-safe try/catch)
    - app/src/main/java/works/mees/dinghy/theme/ThemePrefs.kt (the ThemeTuple data class ~127-139: seedHex/dark/paletteMode/poolShift/maxItems/poolOverrides/fs)
    - app/src/main/java/works/mees/dinghy/theme/compose/DinghyTheme.kt (the resolver param + collectAsStateWithLifecycle + the fontScale=1f LocalDensity override line — KEEP unchanged)
    - app/src/main/java/works/mees/dinghy/MainActivity.kt (the single DinghyTheme(container.themeResolver) boundary at line 42)
    - app/src/main/java/works/mees/dinghy/theme/ThemeTokens.kt (enum class FontScale at 188 — the S/M/L multipliers)
  </read_first>
  <behavior>
    - Test override_null_passes_through_persisted: with `_themeOverride == null`, the value emitted by `effectiveTokens` is exactly `themeResolver.tokens.value` (same instance / equal tokens)
    - Test override_nonnull_applies_baked_tokens: with a non-null `ThemeOverride(dark=false, paletteMode="Simple")`, `effectiveTokens` emits tokens baked from the active profile's REAL seed but with dark=false + Simple mode (assert a token that differs between dark/light, e.g. bg, flips)
    - Test override_writes_no_persistence: setThemeOverride(non-null) then setThemeOverride(null) performs ZERO `dataStore.edit` — the persisted ThemeTuple / ProfileStore flow value is byte-identical before and after (use a fake/inspectable store or assert the persisted resolver's tokens.value is unchanged)
    - Test dismiss_restores_persisted_no_drift: after override then clear, `effectiveTokens` == the original persisted tokens with no field drift
  </behavior>
  <action>
    Create `theme/ThemeOverride.kt`: a sparse nullable `data class ThemeOverride(val dark: Boolean? = null,
    val paletteMode: String? = null, val fs: Float? = null)` — null fields fall through to the active
    profile's persisted value (D-06 keeps the user's real seed; only the cycled axes are overridden).
    In `AppContainer`, add (mirroring the `_spine`/`asStateFlow` publish idiom): a
    `private val _themeOverride = MutableStateFlow<ThemeOverride?>(null)`, the public
    `val themeOverride: StateFlow<ThemeOverride?>`, and `fun setThemeOverride(o: ThemeOverride?)` that just
    sets the value (in-memory only — NEVER routes through writeScope/mutateActiveProfile; this is the
    deliberate non-persisting exception to [[dinghy-compose-write-scope-cancellation]]). Add a
    `private val overrideResolver = ThemeResolver()` that is NEVER fed by `seedTheme` and NEVER persisted —
    it exists only to BAKE override tuples via its `apply(...)` (reusing the proven compute() fail-safe).
    Add `val effectiveTokens: Flow<ThemeTokens> = combine(themeResolver.tokens, _themeOverride,
    activeProfile) { persisted, ov, profile -> if (ov == null) persisted else { bake via overrideResolver
    .apply(seedHex = base.seedHex, dark = ov.dark ?: base.dark, paletteMode = ov.paletteMode ?:
    base.paletteMode, poolShift = base.poolShift, maxItems = base.maxItems, overrides = base.poolOverrides
    mapped, fs = ov.fs ?: base.fs); overrideResolver.tokens.value } }` — where `base` is the active
    profile's tuple (profile?.toThemeTuple()) or the idle ThemePrefs tuple. CRITICAL (RESEARCH Pitfall 1):
    do NOT call `themeResolver.setMode/setDark` — the `seedTheme` collector would fight it and snap back.
    Swap `DinghyTheme` to collect the override-aware flow: change its signature to accept the
    effective-tokens flow (either `DinghyTheme(tokensFlow = container.effectiveTokens)` or add an override
    param) — KEEP the `fontScale=1f` LocalDensity line unchanged. Update `MainActivity.kt:42` to pass
    `container.effectiveTokens`. Per PATTERNS FACT 2, the classic-Views hosts already root in
    `LocalTokens.current`, so NO `*Host` change is needed — flipping `LocalTokens` (via this one collect
    swap) carries Views automatically; verify by grep that no Views host collects `themeResolver.tokens`
    directly. Finally, flesh out the four `ThemeOverrideTest` methods (replace the `fail()` stubs) to the
    behaviors above.
  </action>
  <verify>
    <automated>cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --tests works.mees.dinghy.theme.ThemeOverrideTest --no-daemon" 2>&1 | tr -d '\r' | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `ThemeOverrideTest` 4 methods GREEN (override-null-passthrough, override-applies-baked, no-persist, dismiss-restores)
    - `AppContainer.kt` contains `effectiveTokens`, `_themeOverride`, `setThemeOverride`, and a SECONDARY `overrideResolver`
    - The cycler apply path contains NO `dataStore.edit` / `writeScope` / `mutateActiveProfile` (grep-clean in the override code path)
    - `grep -rn "themeResolver.tokens" render/` returns no host collecting it directly (Views root in LocalTokens — FACT 2)
    - `MainActivity.kt` passes `container.effectiveTokens` (or the override-aware flow) into `DinghyTheme`
  </acceptance_criteria>
  <done>Override layer flips the whole app via one collect swap, persists nothing, and the host-pure tests are green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: App-global dev-widget enable boolean (persisted, release-readable, default false)</name>
  <files>app/src/main/java/works/mees/dinghy/theme/ThemePrefs.kt, app/src/main/java/works/mees/dinghy/di/AppContainer.kt, app/src/test/java/works/mees/dinghy/theme/DevWidgetEnableTest.kt</files>
  <read_first>
    - .planning/phases/15.2-theme-conformance-sweep-settings-ia/15.2-PATTERNS.md (AboutScreen section: the dev-enable boolean persist — copy the ThemePrefs setDark/KEY_DARK booleanPreferencesKey pattern; the BuildConfig.DEBUG anti-pattern)
    - .planning/phases/15.2-theme-conformance-sweep-settings-ia/15.2-RESEARCH.md (Runtime State Inventory A1 — app-global in ThemePrefs is simplest, no new DataStore file; Anti-Pattern: NOT BuildConfig.DEBUG)
    - app/src/main/java/works/mees/dinghy/theme/ThemePrefs.kt (KEY_DARK booleanPreferencesKey at 144; setDark at 64; the data.catch{}.map{} flow shape)
    - app/src/main/java/works/mees/dinghy/di/AppContainer.kt (writeScope at 107; how themePrefs is held + exposed)
  </read_first>
  <behavior>
    - Test dev_enable_defaults_false_persists_app_global: a fresh ThemePrefs `devEnableFlow` emits `false` by default; after `setDevEnable(true)` it emits `true`; the value survives a re-read (round-trip), and it is APP-GLOBAL (not keyed by profile.id) — NOT gated on BuildConfig.DEBUG
  </behavior>
  <action>
    In `ThemePrefs.kt` add an app-global boolean key mirroring `setDark`/`KEY_DARK`:
    `private val KEY_DEV_ENABLE = booleanPreferencesKey("dev_cycler_enabled")`,
    `val devEnableFlow: Flow<Boolean> = dataStore.data.catch{ emit(emptyPreferences()) }.map { it[KEY_DEV_ENABLE] ?: false }` (default FALSE), and
    `suspend fun setDevEnable(on: Boolean) { dataStore.edit { it[KEY_DEV_ENABLE] = on } }`.
    Expose it on `AppContainer`: a `val devCyclerEnabled: Flow<Boolean> = themePrefs.devEnableFlow` and a
    `fun setDevCyclerEnabled(on: Boolean) { writeScope.launch { themePrefs.setDevEnable(on) } }` (the toggle
    write routes through the process-lifetime writeScope per [[dinghy-compose-write-scope-cancellation]] —
    this boolean IS persisted, unlike the transient override). Per RESEARCH, do NOT gate the cycler on
    `BuildConfig.DEBUG` — the owner sideloads RELEASE APKs to flox; the gate must be this runtime boolean
    so the tool is reachable in release. Flesh out `DevWidgetEnableTest` to the behavior above.
  </action>
  <verify>
    <automated>cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --tests works.mees.dinghy.theme.DevWidgetEnableTest --no-daemon" 2>&1 | tr -d '\r' | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `DevWidgetEnableTest` GREEN (default false, round-trips true, app-global)
    - `ThemePrefs.kt` contains `KEY_DEV_ENABLE` + `devEnableFlow` + `setDevEnable`
    - `AppContainer.kt` exposes `devCyclerEnabled` + `setDevCyclerEnabled` (write via writeScope)
    - No `BuildConfig.DEBUG` gate anywhere in the dev-enable path (grep-clean)
  </acceptance_criteria>
  <done>The dev-enable boolean persists app-global, defaults false, is release-readable, and the test is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user tap → in-memory override StateFlow | Cosmetic theme cycling; no network, no printer command, no auth surface |
| dev-enable toggle → DataStore (app-global boolean) | A local persisted flag; no secret, no remote effect |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15.2-01-01 | Tampering | The transient override corrupts the persisted per-profile theme tuple | mitigate | The override is a SEPARATE in-memory `MutableStateFlow` baked by a SECONDARY `overrideResolver`; the persisted `themeResolver`/ProfileStore are never written on apply/dismiss — asserted by `override_writes_no_persistence` (host test) |
| T-15.2-01-02 | Elevation | Dev cycler shipped in release reachable by an end user | accept | It only re-themes transiently and persists nothing destructive; gated behind a hidden app-global enable (default false); no printer commands, no auth change — RESEARCH Security Domain accepts this |
| T-15.2-01-SC | Tampering | npm/pip/cargo installs | n/a | No package installs this plan — internal-codebase only; RESEARCH Package Legitimacy Audit = N/A |

No high-severity threats — local cosmetic feature, no new network/auth/attack surface.
</threat_model>

<verification>
- `:app:testDebugUnitTest --tests works.mees.dinghy.theme.ThemeOverrideTest` GREEN
- `:app:testDebugUnitTest --tests works.mees.dinghy.theme.DevWidgetEnableTest` GREEN
- `:app:compileDebugUnitTestKotlin` BUILD SUCCESSFUL (Wave-0 scaffolds compile)
- The override apply/dismiss path is grep-clean of any persistence call
- `15.2-AUDIT.md` scaffold exists with all surface rows
</verification>

<success_criteria>
- A transient override flips the whole live app (proven by host test that effectiveTokens bakes from the override) and snaps back deterministically on clear, persisting nothing (D-06/D-08)
- The dev-enable boolean persists app-global, defaults false, release-readable (D-08)
- The AUDIT.md scorecard exists for the sweep to fill (D-12)
- Wave-0 RED scaffold compiles against the full test source set
</success_criteria>

<output>
Create `.planning/phases/15.2-theme-conformance-sweep-settings-ia/15.2-01-SUMMARY.md` when done
</output>
