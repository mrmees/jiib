---
phase: 15.1-shape-coded-status-semantic-color
plan: 08
type: execute
wave: 4
depends_on: ["15.1-02", "15.1-03", "15.1-04", "15.1-05", "15.1-06", "15.1-07"]
files_modified:
  - docs/ui_design/THEMING.md
  - docs/ui_design/CLAUDE.md
autonomous: true
requirements: [THEME-shape, THEME-Nseries, THEME-bedmesh, THEME-accentlead, UI-backbtn, THEME-statusedit]
must_haves:
  truths:
    - "THEMING.md is the canonical token-law: status-by-shape (not fixed RYG), pure-neutral surfaces, the pool, the N-series rule, the doctrine resolutions, with a pointer to ../theme_theory/COLOR-SYSTEM.md"
    - "CLAUDE.md's 'red = back' line is reconciled to D-10 (Back = neutral)"
    - "Where CONTEXT (D-05/D-06/D-07) and COLOR-SYSTEM.md disagree on accent-leads, THEMING.md encodes CONTEXT's rule"
  artifacts:
    - path: "docs/ui_design/THEMING.md"
      provides: "Rewritten canonical token-law absorbing the 15.1 model (D-13)"
      contains: "N-series"
  key_links:
    - from: "docs/ui_design/THEMING.md"
      to: "../theme_theory/COLOR-SYSTEM.md"
      via: "pointer to deep rationale (not vendored in)"
      pattern: "COLOR-SYSTEM"
---

<objective>
Rewrite `docs/ui_design/THEMING.md` (D-13) as the canonical in-repo token-law absorbing the 15.1 model,
and reconcile the `docs/ui_design/CLAUDE.md` "red = back" line to D-10. Downstream agents read
`docs/ui_design/` reliably; they won't read the sibling repo — so the doc must carry the new model, with
a pointer to `../theme_theory/COLOR-SYSTEM.md` for deep rationale (do NOT vendor that prose in).

Purpose: The current THEMING.md is partially superseded (fixed RYG status, cool-tinted surfaces, no
pool). It must become the authority every later phase (16–21) builds against.
Output: rewritten THEMING.md + reconciled CLAUDE.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15.1-shape-coded-status-semantic-color/15.1-CONTEXT.md
@.planning/phases/15.1-shape-coded-status-semantic-color/15.1-RESEARCH.md
@.planning/phases/15.1-shape-coded-status-semantic-color/15.1-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite THEMING.md as the canonical token-law (D-13)</name>
  <files>docs/ui_design/THEMING.md</files>
  <read_first>
    - docs/ui_design/THEMING.md (the current 119-line doc — the section-by-section disposition is in RESEARCH "THEMING.md Rewrite Scope")
    - .planning/phases/15.1-shape-coded-status-semantic-color/15.1-RESEARCH.md (the "THEMING.md Rewrite Scope (D-13)" table — keep/reframe/rewrite/NEW per section + the "Deprecated/outdated (must change)" list)
    - app/src/main/java/works/mees/dinghy/theme/ThemeTokens.kt (the AUTHORITATIVE current token names/semantics: `heat`=caution :62, `pool` :77, `directional` :85; reconcile the doc to the code, not vice versa)
    - ../theme_theory/COLOR-SYSTEM.md (§7 status-by-shape, §9 modes, §11 sequential ramp — the deep rationale to POINT to, not vendor)
  </read_first>
  <action>
    Rewrite THEMING.md per the RESEARCH "THEMING.md Rewrite Scope" table. Concrete sections to fix:
    - "Role tokens (exact values)" table: `--heat` = CAUTION (not nozzle/bed amber); surfaces are
      PURE-NEUTRAL (drop the cool `0.012 255` chroma, D-16); ADD the `pool[]` and
      `directional{temperature/xy/z}` model; note status colors are USER-OVERRIDABLE (D-03).
    - "How themes are applied": reframe from CSS-attribute-flips to the Kotlin generate-and-cache +
      token-remap model.
    - "Button intent = color": Back = NEUTRAL (D-10), not green; ADD the Back-position-consistency rule;
      reconcile force-move to D-12 (green-lock/red-open + lock open/closed shape).
    - Worked examples (Move/Extrude): Move Back→neutral; jog-pad = directional.xy; force-move = D-12.
    - "Shape & type tokens": ADD a first-class STATUS SHAPE VOCABULARY section — octagon = stop, triangle
      = caution, NO circle for go in dinghy (D-02); shape is the safety mechanism, color is redundant.
    - NEW section — the N-SERIES RULE (D-05): the canonical mode-aware distinguishable sequence
      (accent-leads, wraps infinitely), reproduce the CONTEXT D-05 table (Colorful/Simple/High-Contrast).
    - NEW section — the SEQUENTIAL-DATA SUB-SYSTEM (§11/D-11): the bed-mesh OKLCH ramp is its own thing,
      off red/green, NOT the categorical pool.
    - NEW section — PALETTE MODES (D-04): Colorful/Simple/High-Contrast renderings of status + pool +
      directional (High-Contrast force-overrides status to RYG regardless of user overrides).
    - NEW — a POINTER to `../theme_theory/COLOR-SYSTEM.md` as deep rationale (do NOT vendor it in — it's
      design-theory prose in a sibling repo, will drift).
    Keep verbatim: the PromptMarkup author-hex carve-out and the control-language outline rule. Where
    CONTEXT (D-05/D-06/D-07 accent-leads) and COLOR-SYSTEM.md disagree, ENCODE CONTEXT's rule (owner
    override — nozzle=accent, data≠status-in-Colorful).
  </action>
  <verify>
    <automated>grep -v '^#' docs/ui_design/THEMING.md | grep -c -i 'N-series\|pool\|octagon\|pure-neutral\|COLOR-SYSTEM'</automated>
  </verify>
  <acceptance_criteria>
    - THEMING.md contains: the status shape vocabulary (octagon/triangle, NO circle), the N-series rule table, the bed-mesh OKLCH sub-system, palette modes, the pool + directional model, pure-neutral surfaces, and a pointer to `../theme_theory/COLOR-SYSTEM.md` (grep confirms the key terms appear in non-comment body).
    - `--heat` is documented as CAUTION (not amber nozzle/bed); status is USER-OVERRIDABLE.
    - Back = NEUTRAL with a position-consistency rule; force-move = D-12.
    - The PromptMarkup author-hex carve-out is preserved verbatim.
    - COLOR-SYSTEM.md prose is POINTED to, not vendored in.
  </acceptance_criteria>
  <done>THEMING.md is the canonical token-law encoding the 15.1 model.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile docs/ui_design/CLAUDE.md "red = back" to D-10</name>
  <files>docs/ui_design/CLAUDE.md</files>
  <read_first>
    - docs/ui_design/CLAUDE.md (the button-intent grammar line: `red = stop/cancel/back, green = accept, amber = proceed-at-peril, accent = physical command` — the "back" assignment is superseded by D-10)
    - docs/ui_design/THEMING.md (the rewritten authority from Task 1 — keep the two docs consistent)
  </read_first>
  <action>
    Update the button-intent grammar line so `back` is NO LONGER red — Back = NEUTRAL/outline (D-10), and
    plain navigation carries no role-meaning. Keep red = stop/cancel (destructive/safety), green = accept,
    amber = proceed-at-peril, accent = physical command. Add a one-line note that Back's gutter POSITION
    must be consistent app-wide (D-10) and point to THEMING.md for the full rule. Do not otherwise rewrite
    CLAUDE.md — this is a targeted reconciliation.
  </action>
  <verify>
    <automated>grep -i 'back' docs/ui_design/CLAUDE.md | grep -v '^#'</automated>
  </verify>
  <acceptance_criteria>
    - The button-intent line no longer assigns `back` to red; Back = neutral/outline (D-10).
    - A note records the Back-position-consistency rule and points to THEMING.md.
    - Red still = stop/cancel; the rest of the intent grammar is intact.
  </acceptance_criteria>
  <done>CLAUDE.md's back-color is reconciled to D-10 and consistent with THEMING.md.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Documentation-only change; no runtime surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15.1-08-01 | Information disclosure | docs | accept | Public design doc, no secrets; pointer to sibling repo only (no vendored content) |
| T-15.1-08-SC | Tampering | npm/pip/cargo installs | accept | No packages installed (docs only) |

No security surface — documentation reconciliation.
</threat_model>

<verification>
- THEMING.md contains the new-model key terms (grep gate).
- CLAUDE.md back-color reconciled.
</verification>

<success_criteria>
- THEMING.md is the canonical token-law (D-13).
- CLAUDE.md "red = back" reconciled to D-10.
</success_criteria>

<output>
Create `.planning/phases/15.1-shape-coded-status-semantic-color/15.1-08-SUMMARY.md` when done.
</output>
