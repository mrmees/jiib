---
phase: 15.1-shape-coded-status-semantic-color
plan: 06
type: execute
wave: 3
depends_on: ["15.1-01", "15.1-02"]
files_modified:
  - app/src/main/java/works/mees/dinghy/ui/move/MoveScreen.kt
  - app/src/main/java/works/mees/dinghy/ui/console/ConsoleRowsAdapter.kt
  - app/src/main/java/works/mees/dinghy/ui/console/ConsoleSeverity.kt
  - app/src/main/res/drawable/ic_lock_closed.xml
  - app/src/main/res/drawable/ic_lock_open.xml
autonomous: false
requirements: [THEME-shape, UI-backbtn]
must_haves:
  truths:
    - "Console ERROR rows show the octagon glyph and WARNING rows the triangle glyph; benign rows show none (D-01/D-02)"
    - "The console compound drawable has EXPLICIT fsSp-scaled pixel bounds (not the 96dp vector intrinsic) so row height stays correct"
    - "An unhomed axis letter shows the triangle caution glyph; homed/go stays color-only (D-02)"
    - "Force-move shows green-lock (safe) / red-open (armed) with a lock open/closed shape as the redundant signal (D-12)"
    - "The Back button is neutral/outline (not green) and sits in a consistent gutter position ON THE MOVE SCREEN (D-10 rule established; app-wide sweep deferred to 15.2)"
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/ui/console/ConsoleRowsAdapter.kt"
      provides: "Compound shape-glyph drawable on ERROR/WARNING rows with explicit fsSp-scaled bounds"
      contains: "setBounds"
    - path: "app/src/main/java/works/mees/dinghy/ui/move/MoveScreen.kt"
      provides: "Unhomed caution glyph + force-move lock shape + neutral Back"
    - path: "app/src/main/res/drawable/ic_lock_closed.xml"
      provides: "Closed-padlock silhouette for force-move SAFE state (D-12 redundant non-color signal)"
      contains: "vector"
    - path: "app/src/main/res/drawable/ic_lock_open.xml"
      provides: "Open-padlock silhouette for force-move ARMED state (D-12)"
      contains: "vector"
  key_links:
    - from: "ui/console/ConsoleRowsAdapter.kt bind"
      to: "ic_status_octagon / ic_status_triangle tinted from severity color, explicit bounds"
      via: "DrawableCompat.setTint + setBounds + setCompoundDrawables"
      pattern: "ic_status_(octagon|triangle)"
    - from: "ui/move/MoveScreen.kt Back tile"
      to: "neutral/outline intent (supersedes Intent.Go)"
      via: "D-10 back=neutral, consistent gutter position (Move only this phase)"
      pattern: "Back"
---

<objective>
Attach the shape-glyph safety layer to the two classic status sites that aren't covered by plan 05:
the console scrollback (ERROR=octagon, WARNING=triangle) and the Move screen (unhomed-axis caution
glyph, force-move lock open/closed shape). Plus apply D-10 ON THE MOVE SCREEN: Back = neutral/outline
with a consistent gutter position. (The app-wide Back sweep is DEFERRED to Phase 15.2 — see plan 08.)

Purpose: D-02 coverage = safety-critical only, EVERYWHERE status appears — including the classic-Views
console row (RESEARCH Pitfall 3: it's a TextView, not Compose) and Move's axis/force-move state.
Output: console shape glyphs (with explicit bounds) + Move shape/back changes + lock drawables.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15.1-shape-coded-status-semantic-color/15.1-CONTEXT.md
@.planning/phases/15.1-shape-coded-status-semantic-color/15.1-RESEARCH.md
@.planning/phases/15.1-shape-coded-status-semantic-color/15.1-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Console ERROR/WARNING shape glyphs via compound drawable with explicit fsSp bounds (D-01/D-02)</name>
  <files>app/src/main/java/works/mees/dinghy/ui/console/ConsoleRowsAdapter.kt, app/src/main/java/works/mees/dinghy/ui/console/ConsoleSeverity.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/console/ConsoleRowsAdapter.kt (`ConsoleRowView.bind` :153-168 — a bare TextView, NOT Compose; the severity switch :159-166; `ConsoleRowPalette` :22 passes resolved ARGB ints down because the ViewHolder can't read LocalTokens; check the row's text size / fsSp source for the bound calc)
    - app/src/main/java/works/mees/dinghy/ui/console/ConsoleSeverity.kt (ERROR→stop, WARNING→caution mapping — already status-aligned, no new taxonomy)
    - app/src/main/res/drawable/ic_status_octagon.xml + ic_status_triangle.xml (plan-01 assets — 96dp intrinsic; MUST be bounded explicitly here)
    - .planning/phases/15.1-shape-coded-status-semantic-color/15.1-RESEARCH.md (Code Example — DrawableCompat tint on a compound drawable, minSdk-23-safe; Pitfall 3)
  </read_first>
  <action>
    In `ConsoleRowView.bind`, keyed off the existing `line.severity` switch (:159-166), attach a leading
    compound drawable: ERROR → `ic_status_octagon` tinted with the palette's error color; WARNING →
    `ic_status_triangle` tinted with the warning color; all other severities (NORMAL/ACTION/DEBUG/success)
    → NO glyph (D-02). Use `AppCompatResources.getDrawable(context, resId)!!.mutate()` +
    `DrawableCompat.setTint(d, argbInt)`.

    CRITICAL (Item 6 — explicit bounds, the row-height fix): the plan-01 vectors carry a 96dp intrinsic
    size; `setCompoundDrawablesRelativeWithIntrinsicBounds(...)` would inject a 96dp glyph and blow up row
    height. Instead set EXPLICIT fsSp-scaled pixel bounds on the drawable BEFORE attaching it: compute a
    pixel size from the row's fsSp text size (e.g. an `fsSp`-scaled value matching the line height, dp→px
    via `resources.displayMetrics.density`), call `d.setBounds(0, 0, px, px)`, then attach with
    `setCompoundDrawables(d, null, null, null)` (the variant that RESPECTS the bounds you set — NOT the
    `...WithIntrinsicBounds` variant). Also set `compoundDrawablePadding` to a small fsSp-scaled gap so the
    glyph doesn't crowd the text. Clear it (`setCompoundDrawables(null, null, null, null)`) on the no-glyph
    branch so recycled rows don't keep a stale glyph. The tint colors come from `ConsoleRowPalette`
    (resolved status ARGB ints, never raw hex). If `ConsoleRowPalette` lacks separate error/warning fields,
    add them sourced from the resolved `stop`/`heat` tokens at the Compose `setPalette` call site.
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:assembleDebug --no-daemon" 2>&1 | tr -d '\r' | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - ERROR rows attach `ic_status_octagon`, WARNING rows `ic_status_triangle`, both tinted from the palette (grep confirms `ic_status_octagon`/`ic_status_triangle`).
    - EXPLICIT bounds (Item 6): the drawable has `setBounds(...)` to an fsSp-scaled pixel size and is attached via `setCompoundDrawables(...)` (NOT `...WithIntrinsicBounds`); grep confirms `setBounds` present and no `setCompoundDrawablesRelativeWithIntrinsicBounds`/`setCompoundDrawablesWithIntrinsicBounds` used for these glyphs. `compoundDrawablePadding` is set.
    - Non-ERROR/WARNING rows clear the compound drawable (no stale glyph on recycled rows).
    - `:app:assembleDebug` succeeds.
    - On-device UAT (flox, autonomous:false gate): scroll console with error+warning lines; octagon/triangle silhouettes render at a sane size (row height NOT blown up) and are distinct (incl. desaturated).
  </acceptance_criteria>
  <done>Console ERROR/WARNING rows carry distinct tinted shape glyphs at explicit fsSp-scaled bounds; benign rows none; row height stays correct.</done>
</task>

<task type="auto">
  <name>Task 2: Move unhomed caution glyph + force-move lock shape + neutral Back (D-01/D-02/D-12/D-10)</name>
  <files>app/src/main/java/works/mees/dinghy/ui/move/MoveScreen.kt, app/src/main/res/drawable/ic_lock_closed.xml, app/src/main/res/drawable/ic_lock_open.xml</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/move/MoveScreen.kt (`jogIconTint` ~:307; `ForceMoveCell` ~:284; `AxisCorner`/`HomeCell` ~:282-296; the homed/unhomed KDoc :78 green=go / amber=heat; `forceMove` state :122; the Back tile :220-223 currently `Intent.Go` "overrides LAW's red=back"; the jog-pad/Z directional reads)
    - app/src/main/res/drawable/nozzle.xml (the white-stroke-tinted-at-call-site authoring convention for the new lock drawables)
    - app/src/main/res/drawable/ic_status_triangle.xml + ic_status_octagon.xml (plan-01 assets)
    - .planning/phases/15.1-shape-coded-status-semantic-color/15.1-PATTERNS.md (MoveScreen section — D-01/D-02/D-08/D-12; jog-pad reads directional.xy already, covered by plan 02's re-derivation with NO MoveScreen change)
    - docs/ui_design/reference/hifi.css (`.jbtn.ovr` force-move styling resolved by D-12)
  </read_first>
  <action>
    0. Lock drawables (Item 6 — assets, since none exist in res): author `ic_lock_closed.xml` (closed
       padlock — shackle DOWN/engaged) and `ic_lock_open.xml` (open padlock — shackle UP/disengaged)
       following the `nozzle.xml` convention (white stroke, tinted at call site, 96dp box, geometry only).
       The OPEN vs CLOSED silhouettes must differ at a glance (the redundant non-color signal of D-12).
    1. Unhomed axis (D-01/D-02): add the `ic_status_triangle` (caution) glyph to the UNHOMED axis-letter
       state ONLY — homed/go stays color-only, no glyph (D-02). Tint from the existing caution color
       (`t.heat`). Set an EXPLICIT fsSp-scaled size on the glyph (do NOT rely on the 96dp intrinsic).
    2. Force-move (D-12): in `ForceMoveCell` render green-`ic_lock_closed` when SAFE/off and
       red-`ic_lock_open` when ARMED/on, so the LOCK OPEN/CLOSED shape is the redundant non-color signal
       (the shape doctrine). `jogIconTint` (~:307) already flips to `t.stop` when `forceMove` — pair it
       with the open/closed lock shape. Size the lock glyph via `fsSp`.
    3. Back = neutral (D-10): change the Back tile (:220-223) from `Intent.Go` to NEUTRAL/outline (spend
       no safety color on plain nav). Enforce a CONSISTENT gutter LOCATION (right-aligned, or centered —
       pick ONE and apply it; the position-consistency rule is canonicalized as doctrine in THEMING.md by
       plan 08). Drop the "overrides LAW's red=back" comment; replace with a D-10 reference. SCOPE NOTE:
       this phase changes Back ONLY on Move; the app-wide Back inventory/sweep is DEFERRED to Phase 15.2's
       conformance audit (plan 08 documents the deferral) — do NOT sweep other screens here.
    Jog-pad/Z directional (D-08) needs NO change here — plan 02's directional re-derivation covers it
    (verify the jog-pad outline reads `directional.xy`, not a raw pool index).
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:assembleDebug --no-daemon" 2>&1 | tr -d '\r' | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `ic_lock_closed.xml` and `ic_lock_open.xml` exist under `res/drawable/`, are valid `<vector>` (white stroke, no baked status hex), and the open/closed silhouettes differ.
    - Unhomed axis state renders `ic_status_triangle` tinted from `t.heat` at an explicit fsSp size; homed state has no glyph (grep + source review).
    - Force-move SAFE=green-`ic_lock_closed` / ARMED=red-`ic_lock_open` (color + lock shape both present; grep confirms `ic_lock_closed`/`ic_lock_open`).
    - The Back tile is neutral/outline (no `Intent.Go`/`Intent.Danger` for back) with a consistent gutter position; the "red=back" comment is gone.
    - NEGATIVE GREP (Item 7): `grep -nE 'Intent\.Go.*[Bb]ack|red.?=.?back' app/src/main/java/works/mees/dinghy/ui/move/MoveScreen.kt` returns nothing (no stale red/green-back doctrine on Move).
    - Jog-pad outline reads `directional.xy` (not a raw pool index).
    - `:app:assembleDebug` succeeds.
    - On-device UAT (flox, autonomous:false gate): home/unhome axes (caution glyph appears unhomed), toggle force-move (green-lock-closed ↔ red-lock-open + lock shape flips), Back is neutral.
  </acceptance_criteria>
  <done>Move shows the unhomed caution glyph, force-move lock open/closed shape, and a neutral consistent Back (Move only; app-wide deferred to 15.2).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Local status-site rendering; no input crosses a boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15.1-06-01 | Repudiation/Safety | force-move armed state | mitigate | Redundant non-color signal (lock open/closed shape) so the armed/safe state is legible without color — the explicit D-12 safety intent |
| T-15.1-06-SC | Tampering | npm/pip/cargo installs | accept | No packages installed (zero new dependencies per RESEARCH §Standard Stack) |

Negligible security surface — local glyph rendering + a button-intent color change.
</threat_model>

<verification>
- `:app:assembleDebug` succeeds.
- Console compound drawable has explicit `setBounds` (no `...WithIntrinsicBounds`); lock drawables exist.
- Negative grep: no stale red/green-back doctrine on Move.
- On-device flox UAT: console glyphs (sane row height), Move unhomed glyph + force-move lock shape + neutral Back (manual gate, autonomous:false).
</verification>

<success_criteria>
- Console ERROR/WARNING shape glyphs at explicit fsSp bounds; benign none.
- Move unhomed caution glyph + force-move lock open/closed shape; Back neutral + consistent position (Move only).
</success_criteria>

<output>
Create `.planning/phases/15.1-shape-coded-status-semantic-color/15.1-06-SUMMARY.md` when done.
</output>
