---
phase: 15.1-shape-coded-status-semantic-color
plan: 04
type: execute
wave: 2
depends_on: ["15.1-01", "15.1-02"]
files_modified:
  - app/src/main/java/works/mees/dinghy/theme/ThemePrefs.kt
  - app/src/main/java/works/mees/dinghy/config/Profile.kt
  - app/src/main/java/works/mees/dinghy/di/AppContainer.kt
  - app/src/main/java/works/mees/dinghy/theme/TokenBridge.kt
  - app/src/main/java/works/mees/dinghy/theme/ThemeResolver.kt
  - app/src/test/java/works/mees/dinghy/theme/StatusOverrideTokenBridgeTest.kt
  - app/src/test/java/works/mees/dinghy/theme/ThemePrefsFallbackTest.kt
autonomous: true
requirements: [THEME-statusedit]
must_haves:
  truths:
    - "A user-set status-color override (stop/caution/go) is applied in TokenBridge over the generated status color and clears back to the generated color when removed (D-03)"
    - "A junk/malicious persisted status-override value fails safe to the generated color (never black-screens) via the existing sanitizeTuple per-entry drop"
    - "Status-override writes route through AppContainer.writeScope + one atomic dataStore.edit/mutateActive — never a composition scope"
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/di/AppContainer.kt"
      provides: "setActiveStatusOverride durable write on writeScope"
      contains: "writeScope"
    - path: "app/src/main/java/works/mees/dinghy/theme/TokenBridge.kt"
      provides: "Status-key override application split out before the integer-pool mapIndexed"
  key_links:
    - from: "di/AppContainer.kt setActiveStatusOverride"
      to: "ProfileStore.mutateActive / ThemePrefs.mutateOverrides on writeScope"
      via: "process-lifetime durable read-modify-write (never composition scope)"
      pattern: "writeScope"
    - from: "theme/TokenBridge.kt"
      to: "ThemeTokens stop/heat/go"
      via: "overrides[\"stop\"|\"caution\"|\"go\"] applied after pool overrides"
      pattern: "\"stop\"|\"caution\"|\"go\""
---

<objective>
Extend the existing per-profile durable override path to the 3 status slots (D-03): users can edit
`stop`/`caution`/`go` colors with no guard, persisted per-profile, fail-safe-sanitized, and applied in
`TokenBridge`. Reuses the Phase-14/15 `writeScope` + `mutateActive` path and the existing per-entry
sanitizer — no new persistence path, no new sanitizer.

Purpose: Safety is carried 100% by shape + icon + position (Area A), so status colors are freely
editable. This wires the persistence + application; plan 07 wires the editor UI.
Output: status-override persistence + application + fail-safe, with GREEN unit + fail-safe tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15.1-shape-coded-status-semantic-color/15.1-CONTEXT.md
@.planning/phases/15.1-shape-coded-status-semantic-color/15.1-RESEARCH.md
@.planning/phases/15.1-shape-coded-status-semantic-color/15.1-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Carry status overrides through the persisted tuple + apply in TokenBridge (D-03)</name>
  <files>app/src/main/java/works/mees/dinghy/theme/ThemePrefs.kt, app/src/main/java/works/mees/dinghy/config/Profile.kt, app/src/main/java/works/mees/dinghy/theme/TokenBridge.kt, app/src/main/java/works/mees/dinghy/theme/ThemeResolver.kt, app/src/test/java/works/mees/dinghy/theme/StatusOverrideTokenBridgeTest.kt, app/src/test/java/works/mees/dinghy/theme/ThemePrefsFallbackTest.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/theme/ThemePrefs.kt (`ThemeTuple` ~:127, `sanitizeTuple` ~:207 with per-entry `isValidArgb` drop ~:223-244, `readOverrides`/`writeOverrides` ~:176-194, `mutateOverrides` ~:94)
    - app/src/main/java/works/mees/dinghy/config/Profile.kt (`poolOverrides: Map<String,Long>` ~:36 + KDoc on the per-entry-tolerance rationale; `toThemeTuple()` ~:95)
    - app/src/main/java/works/mees/dinghy/theme/TokenBridge.kt (pool-override block ~:75-82 — out-of-range keys never match)
    - app/src/main/java/works/mees/dinghy/theme/ThemeResolver.kt (`setOverride` ~:77, `apply` ~:97, the try/catch fail-safe ~:133-151)
    - app/src/test/java/works/mees/dinghy/theme/StatusOverrideTokenBridgeTest.kt (the RED scaffold from plan 01)
    - app/src/test/java/works/mees/dinghy/theme/ThemePrefsFallbackTest.kt (extend for the junk-status-value case)
    - 15.1-01-SUMMARY.md (the recorded typing decision: separate `statusOverrides: Map<String,Long>` on the tuple, ONE persisted wire map)
  </read_first>
  <behavior>
    - TokenBridge: overrides["stop"] applied over generated stop; same for "caution" and "go"; absent key → generated color
    - Clear (remove key) → reverts to generated color
    - sanitizeTuple: a junk/out-of-range "stop" value drops that entry → generated color (never throws/black-screens)
    - Status keys ("stop"/"caution"/"go") never reach key.toInt() (must be split out before the integer-pool mapIndexed)
  </behavior>
  <action>
    Implement the plan-01 typing decision: add a separate `statusOverrides: Map<String,Long>` field to
    `ThemeTuple` (the Int-keyed `poolOverrides` cannot hold `"stop"`), keeping ONE persisted DataStore
    wire map (the String→Long form already holds arbitrary keys — `Profile.poolOverrides` is already
    String-keyed, so reserved keys `"stop"/"caution"/"go"` ride the same map, additive and
    forward-compatible). In `Profile.toThemeTuple()` split the String-keyed map: integer-string keys →
    `poolOverrides`, the three reserved status keys → `statusOverrides`. In `TokenBridge.build`, apply
    status overrides AFTER pool overrides: `stop = statusOverrides["stop"]?.let{Color(it)} ?: bake(st.stop)`
    and the same for `caution`(→`heat`) and `go`. CRITICAL: split the status keys out BEFORE the
    integer-pool `mapIndexed` so they never hit `key.toInt()` (RESEARCH Runtime State). Extend
    `sanitizeTuple` so status-key values pass the SAME `isValidArgb` per-entry guard — a junk value drops
    just that entry, fail-safe to the generated color (V5/T-15.1-04-01). Thread the status-override input
    through `ThemeResolver.apply`. Turn the `StatusOverrideTokenBridgeTest` RED scaffold GREEN (applied +
    clear-to-generated) and extend `ThemePrefsFallbackTest` for the junk-`"stop"` fail-safe case.
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --tests '*StatusOverrideTokenBridgeTest' --tests '*ThemePrefsFallbackTest' --no-daemon" 2>&1 | tr -d '\r' | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `StatusOverrideTokenBridgeTest` GREEN: `overrides["stop"]` applied over generated stop; removing it reverts to generated.
    - `ThemePrefsFallbackTest` GREEN: a junk/out-of-range `"stop"` value drops to the generated color (no throw).
    - Grep confirms status keys are split out before any `key.toInt()` in TokenBridge.
    - `:app:compileDebugKotlin` succeeds.
  </acceptance_criteria>
  <done>Status overrides persist through the tuple, apply in TokenBridge, and fail safe; tests GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: Durable status-override write on writeScope (D-03 + write-scope trap)</name>
  <files>app/src/main/java/works/mees/dinghy/di/AppContainer.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/di/AppContainer.kt (`setActiveOverride` ~:387-406 — the EXACT durable path to reuse; `writeScope` ~:106 process-lifetime SupervisorJob+Dispatchers.IO; `mutateActiveProfile` ~:129; `seedTheme` ~:319 with tuple→resolver `apply` at ~:329-337; the CR-01 KDoc note ~:342-376)
    - app/src/main/java/works/mees/dinghy/theme/ThemePrefs.kt (`mutateOverrides` ~:94 for the idle global path)
    - .planning/phases/15.1-shape-coded-status-semantic-color/15.1-PATTERNS.md (the durable-theme-write shared pattern)
  </read_first>
  <action>
    Add `setActiveStatusOverride(active: Boolean, slot: String, argb: Long?)` mirroring
    `setActiveOverride` EXACTLY: for the active branch, `mutateActiveProfile { p -> ... }` doing an atomic
    read-modify-write of the shared String-keyed override map (set `p.poolOverrides[slot] = argb and
    0xFFFFFFFFL` or remove on null); for the idle-global branch, `writeScope.launch { themePrefs.mutateOverrides
    { ... } }`. MANDATORY [[dinghy-compose-write-scope-cancellation]]: route through the process-lifetime
    `writeScope` (~:106) ONLY — NEVER a composition `rememberCoroutineScope()` (a same-frame nav cancels
    the write on slow flash → silently dropped, the Phase-14 switch-revert bug). Do the read-modify-write
    inside ONE `dataStore.edit`/`mutateActive`. Confirm the new status field flows through `seedTheme`'s
    single `apply` (~:329-337) so an edit re-emits the resolved tokens.
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:assembleDebug --no-daemon" 2>&1 | tr -d '\r' | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `setActiveStatusOverride` exists and routes through `writeScope` + `mutateActiveProfile`/`mutateOverrides` (grep confirms — no `rememberCoroutineScope` in the path).
    - The status override flows through `seedTheme`'s single `apply` (one re-emit, not a sequence of `set*()` calls).
    - `:app:assembleDebug` succeeds.
    - (Wave-2+ instrumented gate, deferred to phase verification) status override survives restart per-profile: `:app:connectedDebugAndroidTest --tests '*Override*Restart*'` — extend the `ProfileSurvivesRestartTest` pattern.
  </acceptance_criteria>
  <done>Status-override writes are durable on writeScope and re-emit through seedTheme; never on a composition scope.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DataStore-persisted override map → resolver | Persisted status-override values re-enter at startup; untrusted-on-disk, must fail safe |
| Slow-flash write race | A composition-scope write can be cancelled mid-flush by same-frame nav (data-integrity boundary) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15.1-04-01 | Denial of Service | corrupt/junk persisted status-override value | mitigate | Existing `sanitizeTuple` per-entry `isValidArgb` drop, extended to status keys → junk fails safe to generated color (never black-screens) |
| T-15.1-04-02 | Tampering (data integrity) | dropped write on slow flash | mitigate | `writeScope` (process-lifetime) + one atomic `dataStore.edit`/`mutateActive` — [[dinghy-compose-write-scope-cancellation]] reused; never a composition scope |
| T-15.1-04-SC | Tampering | npm/pip/cargo installs | accept | No packages installed (zero new dependencies per RESEARCH §Standard Stack) |

Only realistic surface is the already-sanitized persisted-value path above. No network/auth/IPC added.
</threat_model>

<verification>
- `StatusOverrideTokenBridgeTest` + `ThemePrefsFallbackTest` GREEN.
- `:app:assembleDebug` succeeds.
- Instrumented restart-durability gate deferred to phase verification (extend `ProfileSurvivesRestartTest`).
</verification>

<success_criteria>
- Status overrides persist per-profile, apply in TokenBridge, and fail safe.
- Writes are durable (writeScope), never composition-scoped.
</success_criteria>

<output>
Create `.planning/phases/15.1-shape-coded-status-semantic-color/15.1-04-SUMMARY.md` when done.
</output>
