---
phase: 15.1-shape-coded-status-semantic-color
plan: 03
type: execute
wave: 1
depends_on: ["15.1-01"]
files_modified:
  - app/src/main/java/works/mees/dinghy/theme/OklchRamp.kt
  - app/src/main/java/works/mees/dinghy/render/BedMeshHeatmapView.kt
  - app/src/test/java/works/mees/dinghy/theme/OklchRampTest.kt
autonomous: false
requirements: [THEME-bedmesh]
must_haves:
  truths:
    - "The bed-mesh ramp is a perceptually-uniform OKLCH sequential ramp off pure red/green (a tall spot no longer reads as a FAILED spot)"
    - "Ramp stops are baked ONCE in applyTokens; onDraw only indexes/lerps baked ARGB ints (no OKLCH math per cell per frame)"
    - "The Kotlin ramp output matches the committed golden fixture bit-for-bit"
    - "BedMeshHeatmapView no longer uses t.stop / t.surface2 / t.accent as ramp endpoints (the sRGB lerp is gone)"
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/theme/OklchRamp.kt"
      provides: "OKLCH ramp baker reusing Palette.hexToOklch/oklchToHex"
      contains: "Palette.oklchToHex"
    - path: "app/src/main/java/works/mees/dinghy/render/BedMeshHeatmapView.kt"
      provides: "Bed-mesh heatmap using the OKLCH ramp instead of the accent→surface2→stop sRGB lerp"
  key_links:
    - from: "theme/OklchRamp.kt"
      to: "theme/Palette.hexToOklch / Palette.oklchToHex"
      via: "delegate to the existing internal OKLCH helpers (never re-port matrices)"
      pattern: "Palette\\.(hexToOklch|oklchToHex)"
    - from: "render/BedMeshHeatmapView.kt applyTokens"
      to: "theme/OklchRamp baked stops"
      via: "bake-once lookup, lerpArgb between stops in onDraw"
      pattern: "lerpArgb"
---

<objective>
Replace the bed-mesh view's `accent→surface2→stop` sRGB lerp with a dedicated perceptually-uniform
OKLCH sequential ramp kept OFF pure red/green (D-11), so a tall spot reads as "tall," not "FAILED."
The OKLCH math already exists in `Palette.kt` — this plan adds interpolation + stop selection only.

Purpose: Sequential height data is its own sub-system, distinct from the categorical pool. A red/green
ramp falsely implies pass/fail on a mesh.
Output: `OklchRamp.kt`, a rewired `BedMeshHeatmapView`, and a GREEN golden test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15.1-shape-coded-status-semantic-color/15.1-CONTEXT.md
@.planning/phases/15.1-shape-coded-status-semantic-color/15.1-RESEARCH.md
@.planning/phases/15.1-shape-coded-status-semantic-color/15.1-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the OKLCH ramp baker reusing Palette's helpers (D-11)</name>
  <files>app/src/main/java/works/mees/dinghy/theme/OklchRamp.kt, app/src/test/java/works/mees/dinghy/theme/OklchRampTest.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/theme/Palette.kt (`hexToOklch` ~:121, `oklchToHex` ~:138 with the 20-iter gamut clamp, `OklchValue(L,C,H)` ~:61, the OKLab matrices `linToLab`/`labToLin` ~:91/:106 "copied VERBATIM from color.js")
    - app/src/main/java/works/mees/dinghy/theme/TokenBridge.kt (`lShift` ~:51 — the canonical delegate-to-Palette-helpers pattern to mirror)
    - app/src/test/java/works/mees/dinghy/theme/OklchRampTest.kt (the RED scaffold from plan 01)
    - app/src/test/resources/oklch-ramp-golden.json (the committed STRICT-JSON reference fixture from plan 01: `_meta` block + 32-element `ramp` array; the `_meta` block holds the LOCKED parameterization — control points, mid-at-stop-15, piecewise-linear, endpoints-inclusive, lowercase #rrggbb)
    - tools/oklch-ramp-oracle.mjs (the committed independent oracle from plan 01 — the ONLY way to regenerate the fixture; never from Kotlin)
    - app/src/test/java/works/mees/dinghy/theme/PaletteGoldenTest.kt (golden-assert structure)
  </read_first>
  <behavior>
    - The baker takes the THREE OKLCH control points and a stop count, and returns `stops` ARGB ints interpolated PIECEWISE-LINEARLY in OKLCH (lerp L, C, H along the shorter hue arc), each converted via Palette.oklchToHex
    - Piecewise interpolation is EXACTLY as locked in `_meta` (plan 01): two segments split at the mid control point, mid landing at stop index 15; segment A = stops 0..15 low→mid, segment B = stops 15..31 mid→high; endpoints inclusive (stop 0 == low, stop 31 == high)
    - Output matches oklch-ramp-golden.json `ramp[]` bit-for-bit on the quantized 8-bit lowercase #rrggbb (no epsilon needed)
    - Endpoints are off pure red/green (viridis/cividis-style blue→teal→yellow)
  </behavior>
  <action>
    Create `theme/OklchRamp.kt` mirroring the `TokenBridge.lShift` pattern: convert each control point via
    `Palette.hexToOklch` (or accept OKLCH triples directly), lerp L/C/H (H along the shorter arc) PIECEWISE
    across the two segments, and convert back via `Palette.oklchToHex` (which already clamps out-of-gamut by
    reducing chroma) to produce a baked `IntArray`/`List<Int>` of 32 ARGB stops. Do NOT re-derive the OKLab
    matrices — they are golden-tested in `Palette.kt` (re-porting risks golden drift on the Nexus-7 floor).

    RECONCILE THE API SIGNATURE (Item 4): the earlier `bakeRamp(low, high, stops)` two-endpoint signature
    is INCONSISTENT with the locked three-control-point spec — DO NOT use it. Define the API to take the
    three control points explicitly. Either:
      - `bedMeshRampStops(): List<Int>` — a zero-arg helper that bakes the LOCKED viridis ramp (control
        points + stop count + segmentation baked in as `private val`s named to match `_meta`), OR
      - `bakeRamp(controlPoints: List<OklchValue>, stops: Int): List<Int>` — a general piecewise baker the
        bed-mesh helper calls with the three locked points.
    Pick the form that reads cleanest at the BedMeshHeatmapView call site; whichever you choose, the bed-mesh
    consumer must end up with the locked 32-stop viridis ramp, and the helper must implement the EXACT
    piecewise rule from `_meta` (mid at stop 15, two segments, endpoints inclusive, shorter-arc hue).

    The endpoints are NOT open here — they are LOCKED by plan 01's `oklch-ramp-golden.json` `_meta` block:
    low `oklch(0.45 0.12 255)` → mid `oklch(0.65 0.13 150)` → high `oklch(0.85 0.15 95)`. Implement *to*
    that spec. Turn the `OklchRampTest` RED scaffold GREEN: parse `oklch-ramp-golden.json` as strict JSON
    via `javaClass.getResourceAsStream(...)`, read the `ramp[]` array, and assert each baked stop equals the
    reference hex bit-for-bit (quantized lowercase #rrggbb, no epsilon, like `PaletteGoldenTest`). **Do NOT
    regenerate the fixture from the Kotlin output** — that collapses the golden test into a tautology. If a
    stop mismatches, fix the Kotlin baker, not the fixture. If the locked control points themselves must
    change, re-run `tools/oklch-ramp-oracle.mjs` (the committed independent oracle) — never derive from
    `OklchRamp.kt` — exactly how `PaletteGoldenTest` derives `color-golden.json` from the sibling `color.js`.
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --tests '*OklchRampTest' --no-daemon" 2>&1 | tr -d '\r' | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `OklchRampTest` GREEN: every baked stop equals the `oklch-ramp-golden.json` `ramp[]` reference hex bit-for-bit.
    - The baker API takes the THREE control points (not a two-endpoint `bakeRamp(low, high, stops)`); grep confirms no `bakeRamp(low, high` two-endpoint signature remains.
    - The piecewise interpolation matches `_meta` (mid at stop 15, two segments, endpoints inclusive) — the bit-for-bit golden pass proves it.
    - `OklchRamp.kt` calls `Palette.hexToOklch`/`Palette.oklchToHex` (grep confirms) and contains NO hand-rolled OKLab matrix.
    - Ramp endpoints are off pure red and pure green (documented in source).
  </acceptance_criteria>
  <done>The OKLCH ramp baker reuses Palette's helpers, takes three control points, and matches the golden fixture.</done>
</task>

<task type="auto">
  <name>Task 2: Rewire BedMeshHeatmapView to the OKLCH ramp (bake-once, read-cheap)</name>
  <files>app/src/main/java/works/mees/dinghy/render/BedMeshHeatmapView.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/render/BedMeshHeatmapView.kt (`applyTokens` ~:92-100 with the current `rampHigh=t.stop / rampLow=t.accent / rampMid=t.surface2` at ~:93-95; `rampColor` ~:201; `lerpArgb` ~:235; the per-cell `cellPaint.color = rampColor(frac)` in `onDraw` ~:144; `deviationToRamp`/`endpoints` scale-mode math ~:175/:229)
    - app/src/main/java/works/mees/dinghy/theme/OklchRamp.kt (the new baker from Task 1)
    - .planning/phases/15.1-shape-coded-status-semantic-color/15.1-RESEARCH.md (Pitfall 5 — never OKLCH math per cell per frame)
  </read_first>
  <action>
    In `applyTokens`, replace the `accent→surface2→stop` endpoint capture (~:93-95) with a call to the
    `OklchRamp` baker to fill a baked ARGB lookup (32 stops). Replace `rampColor(frac)` (~:201) with an
    index into the baked array + `lerpArgb` between adjacent stops, so `onDraw`'s per-cell color is a
    LOOKUP READ, never an OKLCH computation (Adreno-320 floor — Pitfall 5: a 50×50 grid × 20-iter gamut
    search per cell per frame would tank fill rate). The `deviationToRamp`/`endpoints` scale-mode math
    (RELATIVE/PLATE/±range) and the probe-dot overlay are UNCHANGED — only the color source changes. Keep
    the allocation-free `onDraw` contract. The `t.stop`/`t.surface2`/`t.accent` ramp-endpoint references at
    ~:93-95 MUST be removed entirely (the ramp no longer derives from any status/surface/accent token).
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:assembleDebug --no-daemon" 2>&1 | tr -d '\r' | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - NEGATIVE GREP (Item 4): `BedMeshHeatmapView.kt` no longer uses `t.stop`, `t.surface2`, or `t.accent` as ramp endpoints — `grep -nE 't\.(stop|surface2|accent)' app/src/main/java/works/mees/dinghy/render/BedMeshHeatmapView.kt` returns NO ramp-endpoint matches (the `accent→surface2→stop` lerp is gone). Any surviving `t.*` reference must be unrelated to the ramp (e.g. probe-dot/grid chrome) and called out in the SUMMARY.
    - `onDraw` does NO OKLCH math — only baked-array lookup + `lerpArgb` (visual/source review).
    - `:app:assembleDebug` succeeds.
    - On-device UAT (flox): load a bed mesh; confirm the ramp reads "tall spot" not "FAILED spot," off pure red/green (the manual gate — autonomous:false on this plan covers it).
  </acceptance_criteria>
  <done>Bed-mesh heatmap uses the OKLCH ramp; the sRGB red/green lerp is gone (negative grep proves no t.stop/t.surface2/t.accent ramp endpoints); onDraw stays math-free.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Local render-only color transform; no input crosses a boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15.1-03-01 | Denial of Service | BedMeshHeatmapView onDraw | mitigate | Bake-once ramp; onDraw is allocation-free lookup+lerp (Pitfall 5) — preserves the Adreno-320 fill budget |
| T-15.1-03-SC | Tampering | npm/pip/cargo installs | accept | No packages installed (zero new dependencies per RESEARCH §Standard Stack) |

Negligible security surface — local heatmap rendering, no external input.
</threat_model>

<verification>
- `OklchRampTest` GREEN against the golden fixture (three-control-point baker).
- Negative grep: no `t.stop`/`t.surface2`/`t.accent` ramp endpoints in BedMeshHeatmapView.
- `:app:assembleDebug` succeeds.
- On-device flox UAT: ramp reads off red/green (manual gate, autonomous:false).
</verification>

<success_criteria>
- OKLCH ramp reuses Palette helpers, takes three control points, and matches the golden fixture.
- Bed-mesh view bakes once, reads cheap, off red/green; no stale ramp-endpoint token references.
</success_criteria>

<output>
Create `.planning/phases/15.1-shape-coded-status-semantic-color/15.1-03-SUMMARY.md` when done.
</output>
