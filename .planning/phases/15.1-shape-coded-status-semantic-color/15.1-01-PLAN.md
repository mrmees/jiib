---
phase: 15.1-shape-coded-status-semantic-color
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - app/src/main/res/drawable/ic_status_octagon.xml
  - app/src/main/res/drawable/ic_status_triangle.xml
  - app/src/test/java/works/mees/dinghy/theme/SeriesColorTest.kt
  - app/src/test/java/works/mees/dinghy/theme/OklchRampTest.kt
  - app/src/test/resources/oklch-ramp-golden.json
  - app/src/test/java/works/mees/dinghy/theme/StatusOverrideTokenBridgeTest.kt
autonomous: true
requirements: [THEME-shape, THEME-Nseries, THEME-bedmesh, THEME-statusedit]
must_haves:
  truths:
    - "Two distinct shape-glyph silhouette assets (octagon, triangle) exist and compile into the APK"
    - "RED test scaffolds for seriesColor, OKLCH ramp, and status-override TokenBridge exist and the whole test source set still COMPILES"
    - "The ThemeTuple status-override typing decision is recorded so wiring plans can depend on it"
  artifacts:
    - path: "app/src/main/res/drawable/ic_status_octagon.xml"
      provides: "Octagon stop-status silhouette (white stroke, tinted at call site)"
      contains: "vector"
    - path: "app/src/main/res/drawable/ic_status_triangle.xml"
      provides: "Triangle caution-status silhouette"
      contains: "vector"
    - path: "app/src/test/java/works/mees/dinghy/theme/SeriesColorTest.kt"
      provides: "RED scaffold for D-05 N-series rule"
    - path: "app/src/test/resources/oklch-ramp-golden.json"
      provides: "Committed reference-stops fixture for the OKLCH ramp golden test"
  key_links:
    - from: "ic_status_octagon.xml / ic_status_triangle.xml"
      to: "res/drawable/nozzle.xml"
      via: "same white-stroke-tinted-at-call-site authoring convention"
      pattern: "strokeColor=\"#FFFFFFFF\""
---

<objective>
Lay the Wave-0 foundation every later wiring plan depends on: lock the `ThemeTuple` status-override
typing decision (RESEARCH Open-Question-1), create the two shape-glyph drawable assets (D-01/D-02),
and stand up the RED test scaffolds (`SeriesColorTest`, `OklchRampTest` + golden fixture,
`StatusOverrideTokenBridgeTest`) so per-wave `--tests` runs are unblocked.

Purpose: Wiring against an unresolved type or a non-compiling test source set bricks every later
plan. This plan resolves the one real typing drift, ships the safety-critical silhouette assets, and
seeds compile-clean RED tests.
Output: 2 drawable assets, 3 RED test files + 1 golden fixture, and a recorded type decision.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15.1-shape-coded-status-semantic-color/15.1-CONTEXT.md
@.planning/phases/15.1-shape-coded-status-semantic-color/15.1-RESEARCH.md
@.planning/phases/15.1-shape-coded-status-semantic-color/15.1-PATTERNS.md
@.planning/phases/15.1-shape-coded-status-semantic-color/15.1-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Record the status-override typing decision + author the two shape-glyph assets</name>
  <files>app/src/main/res/drawable/ic_status_octagon.xml, app/src/main/res/drawable/ic_status_triangle.xml</files>
  <read_first>
    - app/src/main/res/drawable/nozzle.xml (the EXACT authoring convention to copy — white stroke, geometry only, tinted at call site)
    - app/src/main/res/drawable/heat_bed.xml (second analog)
    - app/src/main/java/works/mees/dinghy/theme/ThemePrefs.kt (the `ThemeTuple` at ~:127, `poolOverrides: Map<Int,Long>` — the type that cannot hold "stop")
    - app/src/main/java/works/mees/dinghy/config/Profile.kt (`poolOverrides: Map<String,Long>` at ~:36 — already String-keyed)
    - .planning/phases/15.1-shape-coded-status-semantic-color/15.1-RESEARCH.md (Open Question 1)
  </read_first>
  <action>
    Author `ic_status_octagon.xml` and `ic_status_triangle.xml` following the `nozzle.xml` pattern
    exactly: a `<vector>` with `android:width/height="96dp"`, `viewportWidth/Height="96"`, paths drawn
    with `android:strokeColor="#FFFFFFFF"` (recolored at the call site via tint — NEVER a status hex
    baked in, THEME-01). Octagon = a true 8-gon OUTER CONTOUR (regular octagon path) plus an inner `✕`
    mark as additional `<path>` elements. Triangle = a triangle OUTER CONTOUR plus an inner `!` mark.
    D-01 load-bearing: the two must differ at the OUTER SILHOUETTE (octagon vs triangle), not just the
    inner mark, so the shape survives grayscale/CVD — the silhouette IS the safety mechanism. D-02: only
    these two shapes; NO circle for `go`. Do NOT use Unicode glyphs (`⯃`/`▲`) — those are HTML-preview-only.

    Record the Wave-0 type decision in this plan's SUMMARY (and honor it in plan 02/04): per RESEARCH
    Open-Question-1, add a SEPARATE `statusOverrides: Map<String,Long>` field to `ThemeTuple` (the
    Int-keyed `poolOverrides` cannot hold `"stop"`/`"caution"`/`"go"`), while keeping ONE persisted
    DataStore wire map (the String→Long form already holds arbitrary keys). This decision is consumed by
    plans 02 (TokenBridge split) and 04 (persistence). No code change to ThemePrefs here — just the
    drawable assets and the recorded decision.
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:processDebugResources --no-daemon" 2>&1 | tr -d '\r' | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Both XML files exist under `app/src/main/res/drawable/` and parse as valid `<vector>` resources (resource processing succeeds).
    - Each uses `android:strokeColor="#FFFFFFFF"` (no status-color hex baked in).
    - Octagon outer path is an 8-gon; triangle outer path is a 3-gon — silhouettes are distinct (visual confirmation deferred to the Wave-3 on-device UAT, but the path vertex counts differ in source).
    - The SUMMARY records the `statusOverrides: Map<String,Long>` tuple-typing decision for plans 02/04.
  </acceptance_criteria>
  <done>Both drawable assets compile; the status-override typing decision is recorded for downstream plans.</done>
</task>

<task type="auto">
  <name>Task 2: Create compile-clean RED test scaffolds + the OKLCH ramp golden fixture</name>
  <files>app/src/test/java/works/mees/dinghy/theme/SeriesColorTest.kt, app/src/test/java/works/mees/dinghy/theme/OklchRampTest.kt, app/src/test/resources/oklch-ramp-golden.json, app/src/test/java/works/mees/dinghy/theme/StatusOverrideTokenBridgeTest.kt</files>
  <read_first>
    - app/src/test/java/works/mees/dinghy/theme/TokenBridgeTest.kt (host-pure structure analog for SeriesColorTest + StatusOverrideTokenBridgeTest)
    - app/src/test/java/works/mees/dinghy/theme/PaletteGoldenTest.kt (golden-oracle structure for OklchRampTest)
    - app/src/test/resources/color-golden.json (golden fixture format analog)
    - .planning/phases/15.1-shape-coded-status-semantic-color/15.1-VALIDATION.md (Wave 0 Requirements list)
  </read_first>
  <action>
    Create three RED test files whose bodies are `fail("not yet implemented — wave N")` ONLY. CRITICAL
    ([[dinghy-wave0-red-scaffold-compile]]): the bodies MUST NOT reference any symbol that does not yet
    exist (`seriesColor`, `PaletteMode`, the OKLCH ramp baker, `statusOverrides`) — Gradle compiles the
    WHOLE test source set before the `--tests` filter, so a single unresolved reference bricks every
    per-wave run. Each test method = a named `@Test fun` documenting the behavior it will assert (3 modes
    × wrap + empty-pool guard for SeriesColorTest; per-stop golden equality for OklchRampTest; status-key
    applied + clear-to-generated + junk-fails-safe for StatusOverrideTokenBridgeTest) with a `fail(...)`
    body and a `// TODO(15.1-0N): assert ...` comment naming the future symbol in prose, not code.

    Also commit `oklch-ramp-golden.json`: compute the reference ramp stops ONCE (off-line, e.g. a short
    JS/notebook OKLCH→sRGB run, viridis/cividis-style blue→teal→yellow off pure red/green, ~16–32 stops,
    L ramping ~0.45→0.85) and store them as an array of hex strings keyed to stop index. Plan 03 asserts
    the Kotlin ramp output bit-for-bit against this fixture. Document in a top-of-file comment how the
    fixture was generated so it is reproducible.
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:compileDebugUnitTestKotlin --no-daemon" 2>&1 | tr -d '\r' | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `:app:compileDebugUnitTestKotlin` SUCCEEDS — the whole test source set compiles with the three new scaffolds present (the load-bearing [[dinghy-wave0-red-scaffold-compile]] gate).
    - No scaffold references `seriesColor`, `PaletteMode`, the ramp baker, or `statusOverrides` in compiled code (only in prose comments).
    - `oklch-ramp-golden.json` exists under `app/src/test/resources/` with an indexed array of ramp hex stops and a generation note.
    - Running `:app:testDebugUnitTest --tests '*SeriesColor*'` produces RED (failing) results, not compile errors.
  </acceptance_criteria>
  <done>All three RED scaffolds compile as part of the full test source set; the golden fixture is committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DataStore-persisted theme blob → resolver | Persisted override values (incl. future status keys) re-enter the app at startup; must fail safe |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15.1-01-01 | Denial of Service | drawable asset render | accept | Static vector geometry, no input, no math at paint time — Adreno-320 safe by construction |
| T-15.1-01-SC | Tampering | npm/pip/cargo installs | accept | This plan installs NO packages (assets + tests only); the wider phase adds zero dependencies (RESEARCH §Standard Stack) |

Note: this phase has essentially nil security surface — local color/theme rendering + DataStore theme
overrides only. The one relevant note (a junk/malicious persisted status-override value must fail-safe
to the generated color via the existing `sanitizeTuple` path) is realized in plan 04, not here.
</threat_model>

<verification>
- `:app:processDebugResources` succeeds (drawable assets valid).
- `:app:compileDebugUnitTestKotlin` succeeds (RED scaffolds compile — the per-wave gate).
- The status-override typing decision is recorded for plans 02/04.
</verification>

<success_criteria>
- Two distinct-silhouette shape assets exist and compile.
- Three RED test scaffolds compile as part of the full test source set.
- The OKLCH ramp golden fixture is committed.
- The `ThemeTuple` status-override typing decision is locked and recorded.
</success_criteria>

<output>
Create `.planning/phases/15.1-shape-coded-status-semantic-color/15.1-01-SUMMARY.md` when done.
</output>
