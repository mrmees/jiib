---
phase: 15.1-shape-coded-status-semantic-color
plan: 02
type: execute
wave: 1
depends_on: ["15.1-01"]
files_modified:
  - app/src/main/java/works/mees/dinghy/theme/ThemeTokens.kt
  - app/src/main/java/works/mees/dinghy/theme/SeriesColor.kt
  - app/src/main/java/works/mees/dinghy/theme/TokenBridge.kt
  - app/src/main/java/works/mees/dinghy/theme/ThemeResolver.kt
  - app/src/test/java/works/mees/dinghy/theme/SeriesColorTest.kt
autonomous: true
requirements: [THEME-Nseries, THEME-accentlead]
must_haves:
  truths:
    - "seriesColor(i) returns accent for i=0 in all three palette modes (the lead series keeps identity across mode switch)"
    - "Colorful wraps accent + pool infinitely with no status colors in data; Simple alternates accent/text; High-Contrast cycles accent/stop/caution/go/text"
    - "Series wrap infinitely with no user-facing cap (D-09); maxItems is an internal generator boundary only"
    - "directional.temperature resolves to accent, directional.xy to pool[0], directional.z to pool[1] (D-07)"
    - "ThemeTokens carries the active PaletteMode so seriesColor is a pure read"
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/theme/SeriesColor.kt"
      provides: "Toolkit-agnostic ThemeTokens.seriesColor(i) N-series helper"
      contains: "fun ThemeTokens.seriesColor"
    - path: "app/src/main/java/works/mees/dinghy/theme/ThemeTokens.kt"
      provides: "PaletteMode enum + mode field + D-07 re-tagged Directional"
      contains: "enum class PaletteMode"
  key_links:
    - from: "theme/TokenBridge.kt"
      to: "theme/ThemeTokens.kt directional"
      via: "directional re-derivation: temperature=accent, xy=pool[0], z=pool[1]"
      pattern: "directional"
    - from: "theme/SeriesColor.kt"
      to: "ThemeTokens.mode + accent + pool"
      via: "mode-aware sequence read"
      pattern: "seriesColor"
---

<objective>
Build the semantic core of the token model: the D-05 N-series helper (`seriesColor`), the
`PaletteMode` field that makes it a pure read, the D-09 infinite-wrap behavior (no user-facing cap),
and the D-07 directional reconception (`temperature=accent`, `xy=pool[0]`, `z=pool[1]`). This is the
shared foundation the data-surface rewires (plan 05) and the status sites (plan 06) consume.

Purpose: D-05/D-06/D-07 are a SUPERSESSION of Phase-15's nozzle=pool[0]/directional.temperature
wiring, not an addition. Centralize the re-derivation here in one place so Move/PrintStatus consumers
shift values without changing shape.
Output: `SeriesColor.kt`, an extended `ThemeTokens`/`TokenBridge`/`ThemeResolver`, and the now-GREEN
`SeriesColorTest`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15.1-shape-coded-status-semantic-color/15.1-CONTEXT.md
@.planning/phases/15.1-shape-coded-status-semantic-color/15.1-RESEARCH.md
@.planning/phases/15.1-shape-coded-status-semantic-color/15.1-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add PaletteMode + the seriesColor N-series helper (D-05 / D-09)</name>
  <files>app/src/main/java/works/mees/dinghy/theme/ThemeTokens.kt, app/src/main/java/works/mees/dinghy/theme/SeriesColor.kt, app/src/main/java/works/mees/dinghy/theme/TokenBridge.kt, app/src/main/java/works/mees/dinghy/theme/ThemeResolver.kt, app/src/test/java/works/mees/dinghy/theme/SeriesColorTest.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/theme/ThemeTokens.kt (the `@Immutable` value type; `fsSp` at ~:154 is the toolkit-agnostic-pure-fn placement precedent; `heat` IS caution per ~:62-67)
    - app/src/main/java/works/mees/dinghy/theme/ThemeResolver.kt (MODE_COLORFUL/MODE_SIMPLE/MODE_HIGH_CONTRAST at :155-157; `simple`/`highContrast` derived at :135-136)
    - app/src/main/java/works/mees/dinghy/theme/TokenBridge.kt (`build(...)` signature + the pool-override block at ~:75-82)
    - app/src/test/java/works/mees/dinghy/theme/SeriesColorTest.kt (the RED scaffold from plan 01 to turn GREEN)
    - app/src/test/java/works/mees/dinghy/theme/TokenBridgeTest.kt (host-pure test pattern)
  </read_first>
  <behavior>
    - Colorful: seriesColor(0)=accent, seriesColor(1)=pool[0], seriesColor(N+1)=pool[N % pool.size] (D-09 wrap, NO user-facing cap)
    - Simple: seriesColor(even)=accent, seriesColor(odd)=text (alternating)
    - High-Contrast: seriesColor(i) cycles [accent, stop, caution(=heat), go, text][i % 5]
    - Empty-pool guard: seriesColor never divides by zero on an empty pool (fall back to accent, like GraphView :190)
    - Line-1 (i=0) = accent in ALL three modes (lead series keeps identity across mode switch)
  </behavior>
  <action>
    Add `enum class PaletteMode { Colorful, Simple, HighContrast }` to `ThemeTokens.kt` and a new
    `mode: PaletteMode` field on the `@Immutable` `ThemeTokens` data class (RESEARCH Pattern 2 option 1
    — explicit, cheap; the resolver already knows it). Map the existing `ThemeResolver.MODE_*` string
    constants to the enum and thread the resolved mode through `TokenBridge.build(...)` so the built
    tokens carry it. Create `theme/SeriesColor.kt` with `fun ThemeTokens.seriesColor(i: Int): Color`
    implementing the D-05 sequence per the `<behavior>` block — a top-level extension reading ONLY
    `ThemeTokens` fields, no Android view imports, host-testable. The sequence wraps infinitely with NO
    user-facing cap (D-09 — `maxItems` is an internal generator boundary only; `seriesColor` defines what
    happens past the pool via the modulo wrap). Guard the empty pool exactly as `GraphView` does. Turn the
    `SeriesColorTest` RED scaffold GREEN: real `@Test` assertions for each mode, the wrap past pool length
    (D-09), and the empty-pool guard. Do NOT use status colors for data in Colorful (data wraps within
    accent+pool only, per D-05).
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --tests '*SeriesColorTest' --no-daemon" 2>&1 | tr -d '\r' | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `SeriesColorTest` GREEN: all three modes, infinite wrap (D-09), and the empty-pool guard assert correctly.
    - `seriesColor(0)` == `accent` in Colorful, Simple, AND HighContrast (assert in all three).
    - Colorful never emits `stop`/`go`/`caution` for any index (data stays within accent+pool).
    - A wrap test asserts an index well past pool length resolves correctly (no cap, D-09).
    - `:app:compileDebugKotlin` succeeds (mode field threaded through TokenBridge/ThemeResolver).
  </acceptance_criteria>
  <done>seriesColor implements D-05/D-09 correctly across all modes and wraps; SeriesColorTest is GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: Re-derive Directional per D-07 (temperature=accent, xy=pool[0], z=pool[1])</name>
  <files>app/src/main/java/works/mees/dinghy/theme/TokenBridge.kt, app/src/main/java/works/mees/dinghy/theme/ThemeTokens.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/theme/TokenBridge.kt (where `directional` is built from `gen.directional`)
    - app/src/main/java/works/mees/dinghy/theme/ThemeTokens.kt (the `Directional` value type at ~:122 + its KDoc at ~:62-85)
    - .planning/phases/15.1-shape-coded-status-semantic-color/15.1-RESEARCH.md (Pitfall 2 — keep directional a STORED field, redefine its derivation)
  </read_first>
  <action>
    Per D-07 (RESEARCH Pitfall 2): keep `directional` a STORED field but REDEFINE its derivation in
    `TokenBridge.build` so the VALUES become `temperature = accent`, `xy = pool[0]`, `z = pool[1]`.
    Bed shares xy's `pool[0]`, chamber shares z's `pool[1]` (dual-tagging preserved, shifted down one —
    they never co-occur on screen). This single re-derivation point covers Move's `directional.xy`/`.z`
    reads (D-08) and PrintStatus's `directional.temperature` read with NO change to those consumers'
    shape — only the values shift (the D-08 intent exactly). In Simple/HighContrast confirm
    `temperature` still resolves to accent (accent survives all modes per D-05), not collapsed to text.
    Update the `Directional` KDoc to document the accent-leads reconception (supersedes Phase-15 D-2/D-13).
    Guard the empty pool when reading pool[0]/pool[1] (fall back to accent like GraphView).
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --tests '*TokenBridgeTest' --no-daemon" 2>&1 | tr -d '\r' | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `TokenBridge` builds `directional.temperature == accent`, `directional.xy == pool[0]`, `directional.z == pool[1]` (assert in `TokenBridgeTest` — extend it).
    - In Simple/HighContrast, `directional.temperature` still resolves to `accent` (not text-collapsed).
    - Empty-pool path does not crash (xy/z fall back to accent).
    - The `Directional` KDoc documents the D-07 accent-leads reconception.
  </acceptance_criteria>
  <done>Directional is re-derived per D-07 in one place; consumers reading directional.* shift values without code change.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Pure in-memory token-model transform; no input crosses a boundary in this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15.1-02-01 | Denial of Service | seriesColor empty-pool read | mitigate | Empty-pool guard (fall back to accent) mirrors the GraphView CR-02 fix — no div-by-zero |
| T-15.1-02-SC | Tampering | npm/pip/cargo installs | accept | No packages installed (RESEARCH §Standard Stack: zero new dependencies) |

Negligible security surface — pure color-model code, no network/auth/external input.
</threat_model>

<verification>
- `SeriesColorTest` GREEN; `TokenBridgeTest` GREEN with the new directional assertions.
- `:app:compileDebugKotlin` succeeds with the new `mode` field threaded through.
</verification>

<success_criteria>
- D-05 N-series helper correct across all modes + D-09 wrap + empty-pool guard.
- D-07 directional re-derivation in one place; accent leads in all modes.
- ThemeTokens carries PaletteMode.
</success_criteria>

<output>
Create `.planning/phases/15.1-shape-coded-status-semantic-color/15.1-02-SUMMARY.md` when done.
</output>
