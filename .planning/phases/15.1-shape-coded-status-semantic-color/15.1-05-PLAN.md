---
phase: 15.1-shape-coded-status-semantic-color
plan: 05
type: execute
wave: 2
depends_on: ["15.1-01", "15.1-02"]
files_modified:
  - app/src/main/java/works/mees/dinghy/render/GraphView.kt
  - app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusScreen.kt
autonomous: false
requirements: [THEME-accentlead, THEME-shape]
must_haves:
  truths:
    - "The GraphView nozzle trace (line 0) resolves to accent in all modes; bed = pool[0], chamber = pool[1] (D-05/D-06)"
    - "The PrintStatus nozzle readout resolves to the SAME accent value as the trace (cross-screen identity — same channel, same color)"
    - "The PrintStatus Stop button carries the octagon stop-status silhouette (D-01/D-02)"
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/render/GraphView.kt"
      provides: "N-series accent-leads trace binding (supersedes pool[i % size])"
      contains: "seriesColor"
    - path: "app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusScreen.kt"
      provides: "Nozzle readout = accent + Stop octagon glyph"
      contains: "seriesColor"
  key_links:
    - from: "render/GraphView.kt applyTokens"
      to: "ThemeTokens.seriesColor"
      via: "trace i = seriesColor(i); fill = seriesColor(0)"
      pattern: "seriesColor"
    - from: "ui/printstatus/PrintStatusScreen.kt nozzle readout"
      to: "ThemeTokens.seriesColor(0)"
      via: "nozzle readout = accent, matching the trace"
      pattern: "seriesColor\\(0\\)"
---

<objective>
Turn on the N-series rule at its first real consumer and close the D-06 supersession: rewire GraphView
traces to `seriesColor(i)` (accent-leads) and re-point the PrintStatus nozzle readout to the SAME
accent value, so the nozzle trace and readout share one hue across both screens. Also attach the
octagon stop-status glyph to the PrintStatus Stop button (D-01/D-02).

Purpose: This is the Pitfall-1 supersession — Phase-15 wired nozzle=pool[0]. D-06 makes it accent and
demands cross-screen identity. Adding the helper without re-binding leaves the contradiction.
Output: rewired GraphView + PrintStatus, with the Stop silhouette.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15.1-shape-coded-status-semantic-color/15.1-CONTEXT.md
@.planning/phases/15.1-shape-coded-status-semantic-color/15.1-RESEARCH.md
@.planning/phases/15.1-shape-coded-status-semantic-color/15.1-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewire GraphView traces to seriesColor (accent-leads, D-05/D-06)</name>
  <files>app/src/main/java/works/mees/dinghy/render/GraphView.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/render/GraphView.kt (`applyTokens` :186-200; the lines to REPLACE :191-196 `linePaints[i].color = pool[i % pool.size]` + `fillPaint.color = pool[0]`; the KDoc :178-184 that currently says "trace i = pool[i % size]" — now WRONG; the empty-pool guard :190)
    - app/src/main/java/works/mees/dinghy/theme/SeriesColor.kt (the helper from plan 02)
    - .planning/phases/15.1-shape-coded-status-semantic-color/15.1-RESEARCH.md (Pitfall 1 — the supersession)
  </read_first>
  <action>
    Replace the trace binding at GraphView :191-196: `linePaints[i].color = t.seriesColor(i).toArgb()`
    so trace 0 = accent (nozzle), trace 1 = pool[0] (bed), trace 2 = pool[1] (chamber) in all modes; the
    under-fill takes trace 0's color = `t.seriesColor(0).toArgb()` (accent) at low alpha (NOT `pool[0]`).
    This is the D-06 supersession of the Phase-15 `pool[i % size]` binding. Update the :178-184 KDoc to
    describe the accent-leads N-series binding (cite D-05/D-06, note it supersedes Phase-15 D-2/D-13).
    Keep the allocation-free contract and the empty-pool guard. No color math in `onDraw`.
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:assembleDebug --no-daemon" 2>&1 | tr -d '\r' | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - GraphView `applyTokens` binds each trace via `t.seriesColor(i)` (grep confirms `seriesColor`; the bare `pool[i % pool.size]` trace binding is gone).
    - The under-fill uses `seriesColor(0)` (accent), not `pool[0]`.
    - The :178-184 KDoc reflects the accent-leads binding.
    - `:app:assembleDebug` succeeds.
  </acceptance_criteria>
  <done>GraphView trace 0 = accent in all modes; fill = accent; the Phase-15 pool[0] binding is superseded.</done>
</task>

<task type="auto">
  <name>Task 2: Nozzle readout = accent (cross-screen identity) + Stop octagon glyph (D-06/D-01/D-02)</name>
  <files>app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusScreen.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusScreen.kt (`nozzleColor` :469 currently `t.pool[0 % t.pool.size]`; the KDoc :463 "nozzle = pool[0]"; the heater readout block :463-477; `StopButton` :828 + its call site :275; `DrawableIcon` helper :683)
    - app/src/main/java/works/mees/dinghy/theme/SeriesColor.kt (the helper from plan 02)
    - app/src/main/res/drawable/ic_status_octagon.xml (the asset from plan 01)
    - .planning/phases/15.1-shape-coded-status-semantic-color/15.1-RESEARCH.md (Code Examples — Compose tint pattern; [[dinghy-font-sizes-too-small]])
  </read_first>
  <action>
    Re-point the nozzle readout color at :469 from `t.pool[0 % t.pool.size]` to `t.seriesColor(0)` (=
    accent) and the bed readout to `t.seriesColor(1)` (= pool[0]) so a sensor reads the SAME color across
    GraphView trace ↔ PrintStatus readout ↔ Temperature (stable identity — same channel same color, the
    Integration Points note). Update the :463 KDoc ("nozzle = pool[0]" → "nozzle = accent via
    seriesColor(0)"). Add the octagon stop-status silhouette to the gutter `StopButton` (:828): render
    `Icon(painter = painterResource(R.drawable.ic_status_octagon), contentDescription = "stop", tint =
    t.stop, modifier = Modifier.size(fsSp(20f, t.fs).dp))` via the `DrawableIcon` helper pattern (:683).
    [[dinghy-font-sizes-too-small]]: the glyph size tracks adjacent text via `fsSp(baseSp, t.fs)`. D-02:
    Stop (octagon) gets the glyph; benign/go states stay color-only.
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:assembleDebug --no-daemon" 2>&1 | tr -d '\r' | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `nozzleColor` resolves to `t.seriesColor(0)` (accent), bed to `t.seriesColor(1)`; the :463 KDoc updated (grep confirms `seriesColor`).
    - The `StopButton` renders `ic_status_octagon` tinted from `t.stop` at an `fsSp`-scaled size.
    - `:app:assembleDebug` succeeds.
    - On-device UAT (flox + live printer, the autonomous:false gate): nozzle trace (GraphView) and nozzle readout resolve to the SAME accent value; reseed and confirm both move together; the Stop octagon silhouette reads distinct.
  </acceptance_criteria>
  <done>Nozzle readout = accent matching the trace; Stop carries the octagon silhouette.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Local render/readout color binding; no input crosses a boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15.1-05-01 | Denial of Service | GraphView onDraw | mitigate | seriesColor read happens in applyTokens (token change), not onDraw; allocation-free hot loop preserved |
| T-15.1-05-SC | Tampering | npm/pip/cargo installs | accept | No packages installed (zero new dependencies per RESEARCH §Standard Stack) |

Negligible security surface — local trace/readout color + a static glyph.
</threat_model>

<verification>
- `:app:assembleDebug` succeeds.
- On-device flox UAT: nozzle trace == readout (accent), reseed moves both; Stop silhouette distinct (manual gate, autonomous:false).
</verification>

<success_criteria>
- GraphView trace 0 = accent; PrintStatus nozzle readout = same accent (cross-screen identity).
- Stop button carries the octagon silhouette.
</success_criteria>

<output>
Create `.planning/phases/15.1-shape-coded-status-semantic-color/15.1-05-SUMMARY.md` when done.
</output>
