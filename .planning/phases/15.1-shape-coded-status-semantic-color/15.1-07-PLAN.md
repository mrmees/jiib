---
phase: 15.1-shape-coded-status-semantic-color
plan: 07
type: execute
wave: 3
depends_on: ["15.1-01", "15.1-04"]
files_modified:
  - app/src/main/java/works/mees/dinghy/ui/screen/ThemeEditorScreen.kt
autonomous: false
requirements: [THEME-statusedit, THEME-shape]
must_haves:
  truths:
    - "The Theme Editor exposes 3 status-color swatches (stop/caution/go) that open the same picker and write via setActiveStatusOverride(StatusSlot, ...) (D-03)"
    - "The editor preview strip renders the status colors WITH their shapes (octagon on stop, triangle on caution) (D-03)"
    - "In Simple/High-Contrast the editor distinguishes the STORED override from the EFFECTIVE rendered status color (overrides only take visual effect in Colorful, per plan 04's mode-gating)"
    - "The status-slot picker write routes through the AppContainer intent helper, never a composition scope"
    - "The S/M/L preview sample colors draw from seriesColor(0)+pool slots ONLY, never seriesColor(1+)/stop/go/caution (which are status colors in High-Contrast) (Open-Question-2)"
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/ui/screen/ThemeEditorScreen.kt"
      provides: "Status-slot editing + shape-decorated preview strip + mode-aware effective-color display"
      contains: "setActiveStatusOverride"
  key_links:
    - from: "ui/screen/ThemeEditorScreen.kt status swatch picker"
      to: "AppContainer.setActiveStatusOverride"
      via: "intent helper on writeScope (never rememberCoroutineScope)"
      pattern: "setActiveStatusOverride"
    - from: "ui/screen/ThemeEditorScreen.kt preview strip"
      to: "ic_status_octagon / ic_status_triangle"
      via: "shape glyph overlaid on the stop/caution preview swatches"
      pattern: "ic_status_(octagon|triangle)"
---

<objective>
Wire the Theme Editor UI for D-03: add the 3 status-color swatches (stop/caution/go) using the existing
per-slot override pattern, write through the new `setActiveStatusOverride` durable path (plan 04), and
render the status colors WITH their shapes in the preview strip. Make the editor mode-aware so it
distinguishes the STORED override from the EFFECTIVE rendered color (Simple/High-Contrast ignore the
override per plan 04). Confirm the S/M/L preview samples draw from accent+pool, never status
(Open-Question-2).

Purpose: D-03 makes status colors fully user-editable; this is the user-facing surface. The preview
must show shapes so the user sees that color is redundant to shape — and must not mislead the user into
thinking an override "took" in a mode where it is gated off.
Output: status-slot editing in the Theme Editor.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15.1-shape-coded-status-semantic-color/15.1-CONTEXT.md
@.planning/phases/15.1-shape-coded-status-semantic-color/15.1-RESEARCH.md
@.planning/phases/15.1-shape-coded-status-semantic-color/15.1-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Status-slot swatches + writeScope-routed picker (D-03)</name>
  <files>app/src/main/java/works/mees/dinghy/ui/screen/ThemeEditorScreen.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/screen/ThemeEditorScreen.kt (`PoolSlotSwatch` :310-343; the per-slot picker sub-page :114-159 with `editingSlot` :96 + the `setActiveOverride` write at :135/:143; `ScreenTitle`/`SectionLabel`/`SubLabel` helpers :345-379; the KDoc :61-64 on routing writes through intent helpers, NEVER rememberCoroutineScope; the ColorWheel import :36)
    - app/src/main/java/works/mees/dinghy/theme/StatusSlot.kt (the enum from plan 04 — the slot arg type)
    - app/src/main/java/works/mees/dinghy/di/AppContainer.kt (`setActiveStatusOverride(active, slot: StatusSlot, argb)` from plan 04)
    - .planning/phases/15.1-shape-coded-status-semantic-color/15.1-PATTERNS.md (ThemeEditorScreen section — D-03; [[dinghy-compose-write-scope-cancellation]]; [[dinghy-font-sizes-too-small]])
  </read_first>
  <action>
    Add 3 status swatches (`StatusSlot.Stop`/`Caution`/`Go`) alongside the pool-override grid (~:212-225),
    each rendering its STORED/EFFECTIVE resolved status color and opening the SAME `ColorWheel` picker
    sub-page as the pool slots (mirror the `editingSlot` pattern — use a parallel
    `editingStatusSlot: StatusSlot?` state). The picker's settle write MUST call
    `container.setActiveStatusOverride(active, slot, argb)` — [[dinghy-compose-write-scope-cancellation]],
    NEVER `rememberCoroutineScope()` (the KDoc :61-64 documents why). No guard on the status colors (D-03 —
    a user CAN set stop=green; shape carries safety).

    MODE-AWARENESS (Item 1 dependency on plan 04): since Simple/High-Contrast IGNORE status overrides at
    render time, the editor must not lie. When the active mode is Simple or High-Contrast, surface that the
    edit is stored but does NOT change the rendered status color in this mode — e.g. show the STORED
    override swatch with a SubLabel like "applies in Colorful; this mode renders <effective>" (or show both
    the stored override and the effective rendered color). In Colorful, the swatch shows the live applied
    color. Read the effective color from the resolved tokens (`t.stop`/`t.heat`/`t.go`) and the stored
    override from the profile/tuple so the two can be compared.

    New labels use the `fsSp` helpers (`ScreenTitle`/`SectionLabel`/`SubLabel` already do — 22/20/15sp;
    [[dinghy-font-sizes-too-small]]).
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:assembleDebug --no-daemon" 2>&1 | tr -d '\r' | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - 3 status swatches (stop/caution/go) render and open the picker; the write calls `container.setActiveStatusOverride` with a `StatusSlot` arg (grep confirms — no `rememberCoroutineScope` in the status write path).
    - No editability guard on the status slots (D-03).
    - In Simple/High-Contrast the editor distinguishes stored override vs effective rendered color (does not imply the override changes the rendered status in those modes); in Colorful the swatch shows the applied color.
    - New labels use the `fsSp`-scaled helpers.
    - `:app:assembleDebug` succeeds.
  </acceptance_criteria>
  <done>Status-slot swatches edit + persist via the durable intent helper; the editor is mode-aware about override effectiveness.</done>
</task>

<task type="auto">
  <name>Task 2: Shape-decorated preview strip + S/M/L sample-color confirmation (D-03 / Open-Q2)</name>
  <files>app/src/main/java/works/mees/dinghy/ui/screen/ThemeEditorScreen.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/screen/ThemeEditorScreen.kt (the preview strip :201-210 rendering `DataSwatch`; the 15-06 S/M/L preview indicator)
    - app/src/main/res/drawable/ic_status_octagon.xml + ic_status_triangle.xml (plan-01 assets)
    - app/src/main/java/works/mees/dinghy/theme/SeriesColor.kt (for the S/M/L sample-color source)
    - .planning/phases/15.1-shape-coded-status-semantic-color/15.1-RESEARCH.md (Open Question 2)
  </read_first>
  <action>
    1. Shapes in the preview strip (D-03): the preview strip (:201-210) renders `t.stop`/`t.heat`/`t.go`
       as swatches — OVERLAY `ic_status_octagon` on the stop swatch and `ic_status_triangle` on the
       caution swatch (tinted to contrast) so the editor previews the status colors WITH their shapes.
       `go` stays shapeless (D-02). Glyph size tracks the swatch via `fsSp` (explicit size, not the 96dp
       intrinsic).
    2. S/M/L sample colors (Open-Question-2 + Item 1): confirm the 15-06 S/M/L preview indicator draws its
       sample colors from `seriesColor(0)` (accent) plus PLAIN `pool` slots ONLY — NEVER `stop`/`go`/
       `caution`, and NEVER `seriesColor(1+)` (in High-Contrast `seriesColor(1..3)` ARE the status RYG
       colors, which would imply status meaning). If it currently samples a status token OR `seriesColor(1+)`,
       re-point it to `seriesColor(0)` + `pool[0]`/`pool[1]` directly.
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:assembleDebug --no-daemon" 2>&1 | tr -d '\r' | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - The preview strip overlays the octagon on the stop swatch and the triangle on the caution swatch (grep confirms `ic_status_octagon`/`ic_status_triangle` in the editor); `go` has no glyph.
    - The S/M/L preview samples draw from `seriesColor(0)`/plain `pool` slots, never `stop`/`go`/`caution` and never `seriesColor(1+)` (source review; grep shows no status token nor `seriesColor(1`/`seriesColor(2`/`seriesColor(3` feeding the S/M/L sample).
    - `:app:assembleDebug` succeeds.
    - On-device UAT (flox, autonomous:false gate): in Colorful edit a status color, confirm the swatch + shape update and persist across an editor close/reopen; switch to High-Contrast and confirm the S/M/L samples are NOT the RYG status colors.
  </acceptance_criteria>
  <done>The editor previews status colors WITH shapes; S/M/L samples are accent+pool-sourced (never status / seriesColor(1+)).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User color-picker input → persisted override | A picked ARGB value enters the durable override map; already clamped/sanitized (plan 04) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15.1-07-01 | Tampering (data integrity) | status-override write from the editor | mitigate | Routes through `setActiveStatusOverride` (writeScope + atomic edit) — [[dinghy-compose-write-scope-cancellation]]; the value is fail-safe-sanitized on read (plan 04) |
| T-15.1-07-SC | Tampering | npm/pip/cargo installs | accept | No packages installed (zero new dependencies per RESEARCH §Standard Stack) |

Negligible security surface — the picker value is already validated; no new external input.
</threat_model>

<verification>
- `:app:assembleDebug` succeeds.
- On-device flox UAT: status-color edit persists across editor close/reopen; preview shows shapes; High-Contrast S/M/L samples are not RYG (manual gate, autonomous:false).
</verification>

<success_criteria>
- Status-slot editing via the durable intent helper; editor mode-aware about override effectiveness.
- Preview strip shows status colors WITH shapes; S/M/L samples are accent+pool-sourced (never status / seriesColor(1+)).
</success_criteria>

<output>
Create `.planning/phases/15.1-shape-coded-status-semantic-color/15.1-07-SUMMARY.md` when done.
</output>
