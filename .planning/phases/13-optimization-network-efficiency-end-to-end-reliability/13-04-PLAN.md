---
phase: 13-optimization-network-efficiency-end-to-end-reliability
plan: 04
type: execute
wave: 4
depends_on: ["13-02", "13-03"]
files_modified:
  - .planning/phases/13-optimization-network-efficiency-end-to-end-reliability/13-UAT.md
autonomous: false
requirements: []
user_setup: []

must_haves:
  truths:
    - "On the E5 (Pi 4) AND the E3 (RockPro64): SAVE_CONFIG → start a print → the live feed stays live and the new print registers (printState→Printing) WITHOUT an app restart (D-09a, SC-3)"
    - "On the E5 AND the E3: a mid-print network drop → reconnect → the print state resyncs correctly (D-09b, SC-3, D-07b)"
    - "Recovery shows a brief Syncing splash every time (D-03 verified visually on-device)"
    - "Probe-Calibrate still shows a correct z_offset after SAVE_CONFIG despite refreshProbeZOffset removal (Pitfall 3 live backstop, SC-1/SC-3)"
    - "The optimization is behavior-preserving — every screen still shows correct live data on both real printers (SC-3)"
  artifacts:
    - path: ".planning/phases/13-optimization-network-efficiency-end-to-end-reliability/13-UAT.md"
      provides: "Recorded dual-printer dual-scenario UAT results (the binding phase gate)"
  key_links:
    - from: "release APK on flox"
      to: "live E5 + E3 Moonraker"
      via: "connect → SAVE_CONFIG → print; and print → network drop → reconnect"
      pattern: "printState"
---

<objective>
Wave 3 — the BINDING phase gate (D-09). Dual-printer, dual-scenario, on-device UAT on the real flox tablet
against BOTH live printers. This is manual-only and justified: a real Klipper FIRMWARE_RESTART and a real
mid-print LAN drop cannot be exercised in unit tests, and this project's whole reliability class
(mock-vs-reality, 3 strikes) is exactly what green units miss. The E5 is unproven, not assumed-good (D-02)
— both printers run both scenarios.

Purpose: prove on hardware that the headline freeze is dead and the optimization is behavior-preserving.
Full unit suite GREEN is necessary but NOT sufficient — this live gate is the backstop the project's
lessons mandate.

Output: 13-UAT.md recording results for all four combinations (E5/E3 × scenario-a/scenario-b) plus the
D-03 Syncing-splash visual check and the Probe-Calibrate stale-config backstop.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-optimization-network-efficiency-end-to-end-reliability/13-CONTEXT.md
@.planning/phases/13-optimization-network-efficiency-end-to-end-reliability/13-RESEARCH.md
@.planning/phases/13-optimization-network-efficiency-end-to-end-reliability/13-VALIDATION.md
@.planning/phases/13-optimization-network-efficiency-end-to-end-reliability/13-02-SUMMARY.md
@.planning/phases/13-optimization-network-efficiency-end-to-end-reliability/13-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build + sign + install the release APK on flox; confirm full unit suite green (pre-UAT gate)</name>
  <files>.planning/phases/13-optimization-network-efficiency-end-to-end-reliability/13-UAT.md</files>
  <read_first>
    - CLAUDE.md (Local Build Environment: E:\Android\gw.bat builds Windows-side; release APK is unsigned until Phase 8 → zipalign+debug-sign via E:\Android\sign-release.bat; adb at E:\Android\Sdk\platform-tools\adb.exe; flox = real Nexus 7 2013 / LineageOS 18.1 / API 30)
    - .planning/phases/13-optimization-network-efficiency-end-to-end-reliability/13-VALIDATION.md (the full-suite command + the manual-only verification table)
  </read_first>
  <action>
    Claude (automatable, no checkpoint): run the full unit suite
    `:app:testReleaseUnitTest --no-daemon` and confirm GREEN (the three Wave-0 fix-driver tests + the
    whole suite). Build the release APK `:app:assembleRelease`, zipalign+debug-sign it via
    E:\Android\sign-release.bat, and `adb install -r` onto flox. Create 13-UAT.md scaffolding ALL
    the checks Task 2 runs (E5×a, E5×b, E3×a, E3×b, the D-03 Syncing-splash visual, the Probe-Calibrate
    stale-config backstop, AND the Temp-graph/Move/Files live-data spot-checks) with PASS/FAIL/notes slots,
    ready for the human run in Task 2 — the scaffold rows must match Task 2's acceptance criteria one-to-one.
    Do NOT fabricate results.
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testReleaseUnitTest --no-daemon" | tr -d '\r'</automated>
  </verify>
  <acceptance_criteria>
    - Full `:app:testReleaseUnitTest` is GREEN (exit code 0).
    - `:app:assembleRelease` SUCCESSFUL; signed APK installed on flox (adb install returns Success).
    - 13-UAT.md exists with every check slot scaffolded and empty (the four scenario combos, the Syncing-splash visual, the Probe-Calibrate backstop, and the Temp/Move/Files spot-checks — matching Task 2's acceptance rows one-to-one), no fabricated PASS.
  </acceptance_criteria>
  <done>Green unit suite + a signed release APK on flox + a UAT result template ready; the human gate can run.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Dual-printer dual-scenario on-device UAT — the binding phase gate (D-09)</name>
  <files>.planning/phases/13-optimization-network-efficiency-end-to-end-reliability/13-UAT.md</files>
  <action>This is a human verification checkpoint — Claude has already built/signed/installed in Task 1. Matthew runs the steps in how-to-verify on BOTH live printers and records PASS/FAIL in 13-UAT.md. Claude does not advance the plan or write 13-04-SUMMARY.md until the human reports results.</action>
  <what-built>
    The visible, disconnect-driven, self-healing klippy-restart recovery (13-02) + the cadence audit's
    refreshProbeZOffset removal (13-03), installed as a signed release APK on flox. This gate proves the
    headline freeze is dead on real hardware and the optimization is behavior-preserving — on BOTH printers
    (the E5 is unproven, not assumed-good, D-02).
  </what-built>
  <how-to-verify>
    Run EACH check on BOTH printers — E5 = 192.168.1.120:7125, E3 = 192.168.1.121:7125 (the repro printer).
    Record PASS/FAIL + notes in 13-UAT.md.

    Scenario A (D-09a — the headline) — repeat on E5, then E3:
    1. Point the app at the printer; confirm live temps/position updating.
    2. Issue SAVE_CONFIG (or FIRMWARE_RESTART) from Mainsail or the printer console.
    3. Observe: a brief Syncing splash appears (D-03), then the feed RESUMES live — without force-stopping
       the app. Temps/positions update again.
    4. Start a print. The Home Print-Status registers it (printState→Printing, progress ring + stats live),
       NO app restart needed. (Pre-fix symptom: stuck on the last finished print until force-restart.)

    Scenario B (D-09b — mid-print drop) — repeat on E5, then E3:
    5. Start a print (or continue the one from A).
    6. Drop the network (toggle the tablet WiFi off, or pull the printer's network briefly).
    7. Restore the network. Observe a Syncing splash, then the print state resyncs correctly (printState
       back to Printing, live progress/temps resume) — no frozen feed, no stale print.

    Backstop checks:
    8. Open Probe-Calibrate after a SAVE_CONFIG and confirm the displayed z_offset is correct/fresh
       (Pitfall 3 — proves removing refreshProbeZOffset did not regress config freshness).
    9. Quick spot-check that other live screens (Temp graph, Move, Files) still show correct live data
       (SC-3 behavior-preserving).
  </how-to-verify>
  <acceptance_criteria>
    - Scenario A PASSES on BOTH E5 and E3: Syncing splash → feed resumes → new print registers, no app restart.
    - Scenario B PASSES on BOTH E5 and E3: drop → reconnect → print state resyncs.
    - The D-03 Syncing splash is observed every recovery.
    - Probe-Calibrate z_offset is correct after SAVE_CONFIG (no stale-config regression).
    - The spot-check screens scaffolded in Task 1 — Temp graph, Move, Files — each still show correct live data on-device (SC-3 behavior-preserving); recorded as explicit PASS rows in 13-UAT.md (matching the Task-1 scaffold, so scaffold and acceptance agree).
    - All results recorded in 13-UAT.md; any FAIL spawns a gap-closure plan (do NOT mark the phase complete on a FAIL).
  </acceptance_criteria>
  <verify>
    <human-check>Matthew runs the six checks on E5 + E3 and records PASS/FAIL in 13-UAT.md; the plan does not complete until all six pass.</human-check>
  </verify>
  <done>All 13-UAT items pass on flox + live E5 AND E3: SAVE_CONFIG→print registers (both printers), mid-print drop→resync (both printers), Syncing splash visible, Probe-Calibrate z_offset fresh, and the Temp/Move/Files spot-check screens show correct live data.</done>
  <resume-signal>Report the 6-item results (E5×a, E5×b, E3×a, E3×b, Syncing-splash, Probe z_offset). Type "approved" if all pass, or describe which combination froze / failed to resync (note which printer) so a gap-closure plan can be cut.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| flox ↔ live printers (LAN) | The real reconnect/re-handshake path under genuine klippy restart + LAN drop. No new surface — this gate EXERCISES the existing one. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-09 | Denial of service | reconnect storm under repeated drops on the weak SBC | mitigate | The self-heal escalation reuses the jittered backoff (13-02 T-13-04); this gate's scenario B exercises it on real hardware — a frozen-feed or churn observation FAILS the gate and blocks completion. |
| T-13-10 | Tampering | a real mid-restart subscribe rejection | mitigate | Verified live: the recovery must resume the feed, not die forever — the literal pass condition of scenario A. |
</threat_model>

<verification>
- Full `:app:testReleaseUnitTest` GREEN (Task 1).
- Signed release APK on flox.
- The D-09 dual-printer dual-scenario human gate PASSES (Task 2) — the binding phase gate.
</verification>

<success_criteria>
- SC-3: behavior-preserving + the headline freeze dead, proven on the real E5 AND E3.
- D-09a + D-09b: both scenarios pass on both printers.
- D-03: Syncing splash visible every recovery.
- SC-1 backstop: Probe-Calibrate z_offset fresh after SAVE_CONFIG despite the cadence-fix removal.
</success_criteria>

<output>
Create `.planning/phases/13-optimization-network-efficiency-end-to-end-reliability/13-04-SUMMARY.md` and
finalize `13-UAT.md` when the gate passes. On any FAIL, do NOT mark the phase complete — return for a
`--gaps` closure plan.
</output>
