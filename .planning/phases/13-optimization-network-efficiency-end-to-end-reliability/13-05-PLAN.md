---
phase: 13-optimization-network-efficiency-end-to-end-reliability
plan: 05
type: execute
wave: 5
gap_closure: true
depends_on: ["13-02", "13-03", "13-04"]
files_modified:
  - app/src/main/java/works/mees/dinghy/net/MoonrakerSocket.kt
  - app/src/test/java/works/mees/dinghy/net/MoonrakerSocketClientTest.kt
  - app/src/main/java/works/mees/dinghy/ui/shell/AppShell.kt
  - app/src/main/java/works/mees/dinghy/ui/shell/RootController.kt
  - app/src/main/java/works/mees/dinghy/ui/route/TopRoute.kt
  - app/src/test/java/works/mees/dinghy/ui/route/TopRouteTest.kt
  - docs/ui_design/CLAUDE.md
  - .planning/phases/13-optimization-network-efficiency-end-to-end-reliability/13-UAT.md
autonomous: false
requirements: []
user_setup: []

must_haves:
  truths:
    - "G-B1a: MoonrakerSocket.defaultClient() sets an OkHttp pingInterval (~10s) so a half-open socket (tablet WiFi off / printer network pulled mid-print) is detected within ~one interval — OkHttp's missing-pong fails the connection → onFailure → SocketEvent.Closed → the reconnect supervisor runs. Proven by a host test asserting client.pingIntervalMillis > 0 (the unit gap that let the 4th mock-vs-reality strike through: FakeWebSocket synthesizes Closed; real OkHttp never does without keepalive)."
    - "G-A1: the AppShell nav state (the visible `dest`, the `backStack`, AND the in-progress `calibrationRoutine`) is HOISTED above the RootController Splash/Shell switch (owned by the root, passed into AppShell), so a transient recovery that flips the route to Splash (klippy-not-Ready OR socket-reconnect) and decomposes AppShell does NOT reset the user to Dest.PrintStatus — the user returns to the screen (and calibration routine page) they were on; the transient `macroPopupFor` is reset on return, `macroShowSystem` preserved."
    - "G-B1b: a socket reconnect (ConnectionState Connecting/Syncing/Disconnected while config is present and not first-run) shows the FULL Syncing splash (Matthew's call 2026-06-03 — overrides the prior D-05 'socket state never routes' rule). Because nav is hoisted (G-A1), the splash does NOT bounce the user. derive()/RootController routes the recovery splash on socket ConnectionState in addition to klippyState!=Ready."
    - "G-B1b perceptibility: the Syncing/recovery splash has a MINIMUM visible dwell (~500-800ms) so a fast recovery is still seen — recovery is never silent (D-03 on BOTH the klippy-restart AND the socket-reconnect paths). A recovery that completes faster than the floor still shows the splash for the floor duration."
    - "The change is behavior-preserving otherwise: first-run Connect routing, the Splash 'open Settings' escape, and the klippy-not-Ready Splash all still work; a brief idle-store render while no spine is attached still composes."
    - "docs/ui_design is updated to record that socket reconnect now shows the recovery splash (the D-05 departure) so the design LAW and the code stay in sync."
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/net/MoonrakerSocket.kt"
      provides: "pingInterval keepalive on the shared OkHttp client"
      contains: "pingInterval"
    - path: "app/src/test/java/works/mees/dinghy/net/MoonrakerSocketClientTest.kt"
      provides: "host test asserting defaultClient() has pingIntervalMillis > 0"
    - path: "app/src/main/java/works/mees/dinghy/ui/route/TopRoute.kt"
      provides: "derive() routes the recovery Splash on socket ConnectionState (reconnect) as well as klippyState!=Ready"
    - path: "app/src/main/java/works/mees/dinghy/ui/shell/AppShell.kt"
      provides: "nav state (dest+backStack) accepted from the root, not owned by a remember that dies with the Splash override"
    - path: ".planning/phases/13-optimization-network-efficiency-end-to-end-reliability/13-UAT.md"
      provides: "re-run dual-printer dual-scenario UAT results (the binding gate, second run)"
  key_links:
    - from: "MoonrakerSocket.defaultClient()"
      to: "SocketEvent.Closed on a half-open drop"
      via: "pingInterval → OkHttp missing-pong → onFailure → trySend(SocketEvent.Closed)"
      pattern: "pingInterval"
    - from: "socket ConnectionState (reconnecting)"
      to: "full Syncing splash without a Home bounce"
      via: "derive() routes Splash on reconnect + hoisted AppShell nav survives the decompose"
      pattern: "Splash"
---

<objective>
Gap-closure for the 13-04 binding-gate FAIL. The on-device UAT proved Scenario A (klippy-restart recovery)
works but bounces the user to Home (G-A1), and Scenario B (mid-print network drop) fails entirely (G-B1):
a silent WiFi drop is never detected, so the feed just freezes with no Syncing splash and no resync. Two
code-confirmed root causes: (1) the shared OkHttp client has no pingInterval, so a half-open socket is
never detected; (2) the recovery Splash routes ONLY off klippyState (a socket drop leaves klippy Ready, so
no splash) AND the AppShell nav state is lost whenever the Splash decomposes the shell.

Fix all three (Matthew approved scope 2026-06-03: fix G-B1 + G-A1, and show the FULL Syncing splash on
socket reconnect too), then re-run the dual-printer dual-scenario on-device UAT — the binding gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/13-optimization-network-efficiency-end-to-end-reliability/13-CONTEXT.md
@.planning/phases/13-optimization-network-efficiency-end-to-end-reliability/13-RESEARCH.md
@.planning/phases/13-optimization-network-efficiency-end-to-end-reliability/13-UAT.md
@.planning/phases/13-optimization-network-efficiency-end-to-end-reliability/13-02-SUMMARY.md
@docs/ui_design/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: G-B1a — add an OkHttp pingInterval (websocket keepalive) so half-open drops are detected</name>
  <files>app/src/main/java/works/mees/dinghy/net/MoonrakerSocket.kt, app/src/test/java/works/mees/dinghy/net/MoonrakerSocketClientTest.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/net/MoonrakerSocket.kt (defaultClient() ~130-135: connectTimeout + readTimeout(0), NO pingInterval; onFailure ~88-92 → SocketEvent.Closed(NetworkUnavailable))
    - 13-UAT.md ## Gaps G-B1 (the on-device symptom + root cause)
  </read_first>
  <action>
    Add `.pingInterval(PING_INTERVAL_MS, TimeUnit.MILLISECONDS)` to MoonrakerSocket.defaultClient() with a
    ~10s interval (define a named constant, e.g. PING_INTERVAL_MS = 10_000L, with a comment: a websocket
    over WiFi can go half-open silently; OkHttp's pingInterval sends WS PINGs and fails the connection on a
    missing PONG within the interval → onFailure → SocketEvent.Closed → the reconnect supervisor runs. This
    is the load-bearing fix for the silent-drop freeze the live UAT caught; the unit suite missed it because
    FakeWebSocket synthesizes Closed while real OkHttp never does without keepalive — the project's 4th
    mock-vs-reality strike). Keep readTimeout(0) (a ws must not die on a read timeout) — pingInterval is the
    correct half-open-detection mechanism, not readTimeout.
    Add a host test MoonrakerSocketClientTest asserting `MoonrakerSocket.defaultClient().pingIntervalMillis > 0`
    (and == the constant). This is a pure config assertion — no socket needed.
  </action>
  <verify>
    <automated>timeout --signal=KILL 600 /mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testReleaseUnitTest --tests works.mees.dinghy.net.MoonrakerSocketClientTest --no-daemon" 2>&1 | tr -d '\r' | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - defaultClient() sets a non-zero pingInterval (grep shows `pingInterval` in MoonrakerSocket.kt).
    - MoonrakerSocketClientTest asserts pingIntervalMillis > 0 and passes.
  </acceptance_criteria>
  <done>The shared OkHttp client has a ~10s websocket keepalive; a host test pins it. Half-open drops now surface as SocketEvent.Closed.</done>
</task>

<task type="auto">
  <name>Task 2: G-A1 — hoist the AppShell nav state above the Splash/Shell switch so recovery never resets to Home</name>
  <files>app/src/main/java/works/mees/dinghy/ui/shell/AppShell.kt, app/src/main/java/works/mees/dinghy/ui/shell/RootController.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/shell/RootController.kt (the when{} that hard-overrides to SplashScreen when route is Splash → AppShell NOT composed; ~46-72)
    - app/src/main/java/works/mees/dinghy/ui/shell/AppShell.kt (`var dest by remember { mutableStateOf(Dest.PrintStatus) }` ~113, `val backStack = remember { mutableStateListOf<Dest>() }` ~114, navigateTo/back ~129-145, plus the macro/calibration sub-nav state ~117-126 — decide which sub-nav state must also survive)
    - app/src/main/java/works/mees/dinghy/ui/route/TopRoute.kt (derive — Shell(Dest.PrintStatus); note the Shell.dest field is currently ignored by RootController)
  </read_first>
  <action>
    Make the shell's visible `dest` and `backStack` survive a transient Splash override (today, when klippy
    drops or — after Task 3 — the socket reconnects, RootController swaps AppShell out for SplashScreen,
    decomposing AppShell and losing its remember-ed nav state; on return it re-inits to Dest.PrintStatus =
    Home). HOIST this nav state so it lives ABOVE the RootController route switch (own it in RootController
    or a small root-scoped holder) and pass it into AppShell. Hoisting plain `remember` state up is cleaner
    than a SaveableStateHolder here (Codex-reviewed) — do that.

    EXPLICIT sub-nav disposition (Codex-required — do NOT leave to judgment; getting this wrong returns the
    user into a stale page after recovery):
      - HOIST and PRESERVE across the Splash blip: the top-level `dest`, the `backStack`, AND the Calibration
        LOCAL routine state `calibrationRoutine` (~123-126) — so a user mid-calibration-routine returns to it,
        not to a blank hub or Home.
      - RESET on return from a recovery Splash: the macro execution popup `macroPopupFor` — a transient popup
        should not survive a reconnect (re-opening a half-state macro popup is wrong); clear it.
      - DOCUMENT the call for `macroShowSystem` (the System-vs-Bookmarked macro toggle): preserve it (it is a
        view preference, not transient state) and note that in the SUMMARY.
    Keep navigateTo/back semantics identical (PrintStatus clears the backStack, etc.). Do NOT switch to
    Navigation-Compose (D-05 stands — stay with the lean when(dest) holder, just hoist its state).
  </action>
  <verify>
    <automated>timeout --signal=KILL 600 /mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:assembleRelease --no-daemon" 2>&1 | tr -d '\r' | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - The shell's `dest`, `backStack`, AND `calibrationRoutine` are owned above the Splash/Shell switch (not in an AppShell-local remember that dies when the Splash override decomposes AppShell), and passed in / restored on return; `macroPopupFor` is reset on return; `macroShowSystem` is preserved (documented in SUMMARY).
    - assembleRelease succeeds; nav semantics unchanged (PrintStatus clears backStack; drawer nav + back still work).
    - On a transient route→Splash→route-back cycle the visible dest (and an in-progress calibration routine) is the SAME screen the user was on (verified on-device in Task 4; structurally guaranteed here by the hoist).
  </acceptance_criteria>
  <done>Shell nav state survives a transient recovery Splash — the user returns to their screen, not Home.</done>
</task>

<task type="auto">
  <name>Task 3: G-B1b — route the full Syncing splash on socket reconnect (+ minimum perceptible dwell) and sync the design doc</name>
  <files>app/src/main/java/works/mees/dinghy/ui/route/TopRoute.kt, app/src/test/java/works/mees/dinghy/ui/route/TopRouteTest.kt, app/src/main/java/works/mees/dinghy/ui/shell/RootController.kt, docs/ui_design/CLAUDE.md</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/route/TopRoute.kt (derive(cfgPresent, s): currently `!cfgPresent → Connect; klippyState!=Ready → Splash; else Shell(PrintStatus)` — and the explicit comment that it NEVER reads s.connection; that is the rule Matthew is overriding)
    - app/src/main/java/works/mees/dinghy/ui/shell/RootController.kt (collects PrinterState + hasConfig, computes route, owns the splash override)
    - app/src/main/java/works/mees/dinghy/state/PrinterState.kt (ConnectionState: Connecting/Syncing/Connected/Disconnected/Error; where the socket state lives on PrinterState — confirm the field derive() would read)
    - app/src/main/java/works/mees/dinghy/ui/screen/SplashScreen.kt (for the minimum-dwell treatment)
    - docs/ui_design/CLAUDE.md + docs/ui_design/LAYOUT.md (the routing/status rules to update)
  </read_first>
  <action>
    (A) Make derive() show TopRoute.Splash ALSO when the socket is mid-reconnect with a config present and
    not first-run. Use this EXACT arm order (Codex-reviewed — order is load-bearing so first-run and the
    klippy gate still win, and an auth failure is NOT trapped behind a bare Syncing splash):
        !cfgPresent                  -> Connect
        klippyState != Ready         -> Splash
        connection !is Connected     -> Splash      // NEW: reconnecting (Connecting/Syncing/Disconnected/Error)
        else                         -> Shell
    derive() currently takes only PrinterState and reads s.klippyState; it must also read the socket
    ConnectionState — confirm whether `connection` already lives on PrinterState (read PrinterState.kt); if so
    derive stays single-arg and pure (just read s.connection); if not, add the ConnectionState as a 2nd pure
    param. Keep derive a PURE function (TopRouteTest drives it directly).
    NOTE (Codex-confirmed, no extra work): SplashScreen already maps Disconnected/Error → an "Unreachable"
    surface with Retry + "Edit connection", so a printer that is simply OFF (Disconnected-forever) shows a
    reachable Unreachable/Edit-connection screen, NOT an eternal dead "Syncing" — the Settings escape stays
    reachable on the reconnect-splash path. Verify this holds; do not regress the escape.
    This is the deliberate D-05 departure (socket state now routes the recovery splash); it is SAFE only
    because Task 2 hoisted the nav state.

    (B) Minimum perceptible dwell — implement as a RootController-OWNED UI latch (Codex pattern), NOT in the
    socket/session layer (must never block actual recovery — only delay HIDING the splash):
      - compute the raw `route` as today;
      - track `splashVisible` with `LaunchedEffect(rawRoute is Splash) { /* on entering Splash, mark visible;
        on leaving, delay(remaining-of-~600ms) then hide */ }`, cancelling/recomputing on re-entry;
      - the Connect/Settings routes (first-run + settingsEscape) BYPASS the dwell entirely (never floored);
      - net effect: a recovery that completes in <600ms still shows the splash for ~600ms so it is perceptible
        on BOTH the klippy-restart and socket-reconnect paths (Matthew couldn't catch it last run).
    (C) Sync docs/ui_design/CLAUDE.md (and LAYOUT.md if it states the routing rule) to record: socket reconnect
    now shows the full recovery Splash (supersedes the earlier 'socket ConnectionState is chrome, never routes'
    rule) — Matthew 2026-06-03; with nav state hoisted so it never bounces the user.
    Extend TopRouteTest with cases: cfgPresent + Connected + klippy Ready → Shell; cfgPresent + reconnecting
    (Disconnected/Connecting/Syncing) → Splash; !cfgPresent → Connect; klippy!=Ready → Splash. Keep derive pure.
  </action>
  <verify>
    <automated>timeout --signal=KILL 600 /mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testReleaseUnitTest --tests works.mees.dinghy.ui.route.TopRouteTest --no-daemon" 2>&1 | tr -d '\r' | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - derive() (pure) returns Splash for cfgPresent + socket-not-Connected (reconnecting), in addition to klippy!=Ready; first-run Connect still wins; Connected+Ready → Shell. TopRouteTest covers all arms and passes.
    - The recovery splash has a minimum visible dwell (~500-800ms) so it is perceptible on a fast recovery (verified on-device Task 4).
    - docs/ui_design/CLAUDE.md records the socket-reconnect-now-routes-Splash departure (LAW kept in sync).
    - Full :app:testReleaseUnitTest GREEN + :app:assembleRelease SUCCESSFUL (guarded).
  </acceptance_criteria>
  <done>A socket reconnect shows the full Syncing splash (perceptibly), without bouncing the user, and the design LAW is updated to match.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Re-run the dual-printer dual-scenario on-device UAT — the binding gate (second run)</name>
  <files>.planning/phases/13-optimization-network-efficiency-end-to-end-reliability/13-UAT.md</files>
  <action>
    Claude (automatable): run the full unit suite guarded → GREEN; build :app:assembleRelease guarded →
    SUCCESSFUL; debug-sign via E:\Android\sign-release.bat and adb install -r the new APK on flox; reset the
    13-UAT.md result rows to pending (fresh run, second attempt) keeping the Gaps history. THEN STOP at the
    human-verify checkpoint.
    Matthew (human): re-run BOTH scenarios on BOTH printers (E5 192.168.1.120, E3 192.168.1.121):
      - Scenario A: SAVE_CONFIG → CONFIRM a brief Syncing splash is visible (watch for it explicitly this
        time) → feed resumes → start print registers → AND the app stays on / returns to the screen you were
        on (G-A1 fixed), not forced to Home.
      - Scenario B: mid-print → toggle tablet WiFi off → within ~10-20s a full Syncing splash appears (drop
        detected via the new keepalive) → restore WiFi → feed + print state resync.
      - Backstops: Probe-Calibrate z_offset fresh after SAVE_CONFIG; Temp graph / Move / Files still show
        correct live data.
    Record PASS/FAIL in 13-UAT.md. The orchestrator may fire SAVE_CONFIG/FIRMWARE_RESTART via the Moonraker
    API on cue; the WiFi toggle is the human's. Do NOT write 13-05-SUMMARY.md or mark the phase complete until
    all checks PASS. Any FAIL → another gap-closure pass (do NOT mark complete on a FAIL).
  </action>
  <verify>
    <human-check>Matthew runs both scenarios on E5 + E3 and records PASS/FAIL in 13-UAT.md; the plan does not complete until Scenario A + B pass on both printers, the Syncing splash is confirmed visible on every recovery, the app does NOT reset to Home, probe z_offset is fresh, and the Temp/Move/Files spot-checks pass.</human-check>
  </verify>
  <acceptance_criteria>
    - Scenario A PASSES on E5 + E3: Syncing splash visible, feed resumes, print registers, NO restart, and the app stays on the current screen (G-A1 verified fixed).
    - Scenario B PASSES on E5 + E3: WiFi drop detected (full Syncing splash within ~one keepalive interval) → reconnect → print state resyncs (G-B1 verified fixed).
    - D-03 Syncing splash confirmed visible on EVERY recovery (both paths).
    - Probe-Calibrate z_offset fresh after SAVE_CONFIG; Temp/Move/Files show correct live data.
    - Results recorded in 13-UAT.md.
  </acceptance_criteria>
  <done>All 13-UAT items pass on flox + live E5 AND E3: SAVE_CONFIG→splash→print registers→stays on screen; mid-print WiFi drop→splash→resync; splash visible every recovery; probe z_offset fresh; Temp/Move/Files live.</done>
  <resume-signal>Report the results — E5×A, E5×B, E3×A, E3×B, Syncing-splash-visible (y/n), stays-on-screen (y/n), probe z_offset fresh (y/n). Type "approved" if all pass, or describe what failed (and which printer) so another gap pass can be cut.</resume-signal>
</task>

</tasks>

<verification>
- G-B1a: MoonrakerSocketClientTest GREEN (pingIntervalMillis > 0).
- G-A1: nav state hoisted; assembleRelease green; verified on-device (no Home bounce).
- G-B1b: TopRouteTest GREEN (reconnect → Splash); design doc synced; splash perceptible.
- Full :app:testReleaseUnitTest GREEN + :app:assembleRelease SUCCESSFUL (all guarded).
- The D-09 dual-printer dual-scenario human gate PASSES on the re-run (Task 4).
</verification>

<success_criteria>
- The mid-print network-drop freeze is dead on real hardware (both printers): a silent WiFi drop is detected and shows a full Syncing splash, then resyncs.
- Recovery (klippy-restart AND socket-reconnect) is visibly non-silent (perceptible Syncing splash) and never resets the user to Home.
- Behavior-preserving otherwise; unit suite + assembleRelease green; design LAW synced to the new routing.
</success_criteria>

<output>
Create `.planning/phases/13-optimization-network-efficiency-end-to-end-reliability/13-05-SUMMARY.md` and
finalize `13-UAT.md` when the gate passes. On any FAIL, do NOT mark the phase complete — return for another
gap pass.
</output>
