---
phase: 13-optimization-network-efficiency-end-to-end-reliability
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - app/src/main/java/works/mees/dinghy/net/MoonrakerSession.kt
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "On klippy-restart recovery the in-session re-handshake emits ConnectionState.Syncing at start and ConnectionState.Connected when the reseed lands — recovery is visible, never silent (D-03)"
    - "Recovery is driven off the klippy-DROP signal the capture identified (notify_klippy_disconnected/shutdown and/or the webhooks.state transition), not only notify_klippy_ready — the screen dims via markStale the instant klippy drops (SC-2)"
    - "A re-handshake whose objects.subscribe is rejected/timed-out/withheld in the klippy-down window escalates to a full socket reconnect (the proven supervisor path) instead of being swallowed by runCatching{} — the feed self-heals instead of freezing forever (D-01 fallback reached from root cause, Pitfall 4)"
    - "The fix is a REUSE of existing plumbing — emit()/markStale/seed, runHandshake(), the run() supervisor's reconnect — no new recovery machinery, no new backoff loop, no timer poll (Don't-Hand-Roll)"
    - "KlippyReadyResyncTest (resumed-diff keystone), KlippyRecoveryStateTest (Syncing→Connected), and the self-heal test all go GREEN; the full unit suite stays green"
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/net/MoonrakerSession.kt"
      provides: "Visible, disconnect-driven, self-healing klippy-restart recovery"
      contains: "emit(ConnectionState.Syncing)"
  key_links:
    - from: "klippyEvents collect block (~234)"
      to: "emit(Syncing)/emit(Connected)"
      via: "wrapping the re-handshake with the existing emit() seam"
      pattern: "emit\\(ConnectionState"
    - from: "re-handshake failure"
      to: "run() supervisor reconnect"
      via: "force socket close on subscribe rejection/timeout in the down-window"
      pattern: "rpc\\.close|driveClosing|reconnect"
---

<objective>
Wave 1 — the headline fix. Make the in-session klippy-restart re-handshake VISIBLE (emit Syncing/Connected,
D-03), DISCONNECT-DRIVEN (recover off the drop signal the capture identified, not only ready, SC-2), and
SELF-HEALING (on re-handshake failure in the klippy-down window, force a full reconnect via the existing
supervisor instead of swallowing it forever — the D-01 fallback, now reached FROM the root cause).

Purpose: This is the bug that froze the live feed until app force-restart (the reason Phase 13 was
promoted). RESEARCH H1 pins it to the silent fire-and-forget `runCatching { runHandshake() }` that swallows
subscribe failure with no retry/escalation, plus recovery gated only on notify_klippy_ready. Every change
here is a REUSE of proven plumbing — the same emit()/markStale/seed the socket-death path uses and the
run() supervisor that demonstrably works on a fresh socket. Inventing new machinery is exactly how the
silent re-handshake became a dead-forever trap.

Output: a modified MoonrakerSession.kt where klippy-restart recovery looks identical to a reconnect
(Syncing splash → fresh seed → Connected) and self-heals on failure. The Wave-0 RED tests turn GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-optimization-network-efficiency-end-to-end-reliability/13-CONTEXT.md
@.planning/phases/13-optimization-network-efficiency-end-to-end-reliability/13-RESEARCH.md
@.planning/phases/13-optimization-network-efficiency-end-to-end-reliability/13-PATTERNS.md
@.planning/phases/13-optimization-network-efficiency-end-to-end-reliability/13-01-SUMMARY.md
@docs/commands/e3-saveconfig-capture.jsonl
@docs/commands/e5-saveconfig-capture.jsonl
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make recovery visible + disconnect-driven (D-03 emit Syncing/Connected; recover off the captured drop signal)</name>
  <files>app/src/main/java/works/mees/dinghy/net/MoonrakerSession.kt</files>
  <behavior>
    - KlippyRecoveryStateTest (Wave 0): on the re-handshake trigger the session emits ConnectionState.Syncing, then ConnectionState.Connected when the reseed lands — currently emits neither. GREEN after this task.
    - The screen dims (markStale) the instant the captured drop signal arrives, not only on ready.
    - KlippyReadyResyncTest keystone: an injected post-restart notify_status_update reaches store.printerState.printState == Printing (the subscription is live again). GREEN after this task.
  </behavior>
  <read_first>
    - app/src/main/java/works/mees/dinghy/net/MoonrakerSession.kt (EDIT SITE 1 the silent re-handshake ~234–248; EDIT SITE 2 store.onKlippyMethod ~236; the emit() helper ~501–504; connectAndServe's own emit(Syncing) ~285 / emit(Connected) ~311; runHandshake ~322–425; the per-attempt handshakeComplete/rehandshakeMutex gate ~99–101/190/309)
    - .planning/phases/13-optimization-network-efficiency-end-to-end-reliability/13-PATTERNS.md (EDIT SITE 1 lines 80–98; EDIT SITE 2 lines 100–106; the emit() seam lines 43–55; store plumbing lines 65–70)
    - .planning/phases/13-optimization-network-efficiency-end-to-end-reliability/13-01-SUMMARY.md (which drop signal each printer emits — THE branch decider; do not code before reading this)
    - app/src/main/java/works/mees/dinghy/state/PrinterStateStore.kt (seed ~147–152 clears stale; markStale ~187–191 retains values; setConnectionState ~177–181; onKlippyMethod ~170–174)
    - app/src/main/java/works/mees/dinghy/state/PrinterStateReducer.kt (klippyFromWebhook ~69–82 — webhooks.state→KlippyState, the signal source if the capture shows webhooks.state not bare notify_klippy_*)
  </read_first>
  <action>
    At EDIT SITE 1 (the klippyEvents collect block ~234–248), wrap the re-handshake with the existing
    emit() seam exactly as connectAndServe does: emit(ConnectionState.Syncing) BEFORE runHandshake() and
    emit(ConnectionState.Connected) on success. Reuse the existing rehandshakeMutex serialization and the
    handshakeComplete gate unchanged — do NOT touch the seed ordering inside runHandshake (query-seed then
    subscribe-seed ~347–356 is already correct). The emit() helper already pushes to both _connectionState
    (notification + UI shell) and store.setConnectionState (control-plane immediate); MoonrakerService
    already maps Syncing → "Syncing…" — no new surface.

    At EDIT SITE 2: route the klippy-DROP signal the Wave-0 capture identified to markStale so the screen
    dims the instant klippy drops, matching the stale model rather than freezing on stale values until
    ready. If the capture shows the real signal is notify_klippy_disconnected/shutdown, key off those in
    this collect block (they currently only fold into KlippyState via onKlippyMethod). If the capture shows
    the transition rides webhooks.state inside a notify_status_update, the reducer's klippyFromWebhook
    already maps it — drive markStale off that KlippyState edge instead. Pick the branch from the SUMMARY
    evidence (Pitfall 5), not assumption.

    NO timer, NO poll, NO new ConnectionState — reuse Syncing + markStale + seed (Don't-Hand-Roll).
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testReleaseUnitTest --tests KlippyRecoveryStateTest --tests KlippyReadyResyncTest --no-daemon" | tr -d '\r'</automated>
  </verify>
  <acceptance_criteria>
    - MoonrakerSession.kt contains `emit(ConnectionState.Syncing)` and `emit(ConnectionState.Connected)` wrapping the re-handshake (grep confirms; the calls are inside the notify_klippy_ready / drop-signal recovery path, not only connectAndServe).
    - KlippyRecoveryStateTest is GREEN (Syncing→Connected emitted).
    - KlippyReadyResyncTest keystone is GREEN (injected post-restart notify_status_update reaches store.printerState.printState == Printing).
    - markStale is driven off the drop signal the SUMMARY identified (grep shows markStale in the klippy-drop recovery path or off the klippyFromWebhook edge); SUMMARY documents which branch the capture dictated.
    - No new ConnectionState added; no timer/poll introduced (grep for `delay(` in the recovery path returns only pre-existing toast timers, none in net/).
  </acceptance_criteria>
  <done>Klippy-restart recovery is visible (Syncing→Connected) and disconnect-driven; the keystone resumed-diff test and the D-03 emit test are GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Self-heal on re-handshake failure — escalate to a full reconnect instead of swallowing (Pitfall 4, D-01 fallback)</name>
  <files>app/src/main/java/works/mees/dinghy/net/MoonrakerSession.kt</files>
  <behavior>
    - Self-heal test (Wave 0): with the harness in klippy-down mode rejecting/withholding the re-handshake's objects.subscribe, the session escalates to a full socket reconnect — harness.opens INCREMENTS (a new socket) — instead of opens staying flat (the current dead-forever bug). GREEN after this task.
    - A re-subscribe Timeout / RpcError in the down-window is treated as "escalate," NOT "give up" (the G4 ConnectionError.Timeout distinction).
  </behavior>
  <read_first>
    - app/src/main/java/works/mees/dinghy/net/MoonrakerSession.kt (EDIT SITE 1 ~234–248 now emitting Syncing/Connected from Task 1; the served-then-died teardown path ~315–318 with rpc.close(reason); the run() supervisor backoff/reconnect loop ~129–177)
    - app/src/main/java/works/mees/dinghy/net/RpcError.kt (ConnectionError sealed interface ~13–42: AuthRequired/NetworkUnavailable/Timeout/ProtocolError/ServerError/ParseError — a slow re-subscribe surfaces as Timeout, the G4 distinction lines ~24–32)
    - app/src/test/java/works/mees/dinghy/net/SessionTestHarness.kt (the klippyDown window + harness.opens added/used in Wave 0; driveClosing ~226)
    - .planning/phases/13-optimization-network-efficiency-end-to-end-reliability/13-PATTERNS.md (EDIT SITE 1 direction lines 93–98; the proven reconnect-reuse mandate § "Reuse the proven paths" lines 35–42; Pitfall 4)
    - .planning/phases/13-optimization-network-efficiency-end-to-end-reliability/13-RESEARCH.md (§ Don't Hand-Roll lines 207–216 — force a socket close → let run() reconnect; do NOT write a new retry loop)
  </read_first>
  <action>
    Replace the silent `runCatching { runHandshake() }` failure path: on a re-handshake that throws
    (RpcError or ConnectionError.Timeout from a rejected/withheld objects.subscribe in the klippy-down
    window), do NOT swallow it. Force a full socket reconnect by reusing the EXISTING teardown — close the
    socket (the same rpc.close(reason) / collector-completion the served-then-died path uses ~315–318) so
    the run() supervisor's backoff loop reconnects on a fresh socket (the path proven by force-restart).
    Treat Timeout/RpcError during the down-window as "escalate to reconnect," matching the existing G4
    distinction, NOT "give up." Do NOT add a new backoff loop, a new retry counter with its own timing, or
    a "fast poll while syncing" — the supervisor's jitter/backoff/seed path already exists and is proven on
    socket death. A configurable small failure tolerance before escalation is acceptable ONLY if it reuses
    existing state; default behavior is escalate-on-failure. Keep the rehandshakeMutex serialization.
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testReleaseUnitTest --no-daemon" | tr -d '\r'</automated>
  </verify>
  <acceptance_criteria>
    - The self-heal test is GREEN: harness.opens increments (full reconnect) when the down-window rejects/withholds the re-handshake subscribe.
    - No `runCatching { runHandshake() }` swallow remains on the recovery failure path (grep shows the failure branch forces a close/reconnect, not a bare swallow).
    - No new backoff/retry loop or timer poll added (grep for new `delay(`/`while(true)` in net/ recovery; reuse only).
    - Full `:app:testReleaseUnitTest` suite is GREEN (no regressions in HandshakeTest/ReconnectSupervisorTest/ConnectionStateTest/etc.).
  </acceptance_criteria>
  <done>A failed re-handshake self-heals via a full reconnect through the existing supervisor; the self-heal test and the full unit suite are GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Moonraker (LAN) → session | Untrusted frames; the recovery decides on klippy-state signals + subscribe replies. No NEW input surface — same frames as before. |
| session → reconnect supervisor | Recovery escalation crosses here; reuses the proven socket-death path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-04 | Denial of service | self-heal reconnect loop | mitigate | Escalation reuses the existing uncapped-but-jittered backoff; it does NOT add a tight reconnect loop. A re-handshake failure forces ONE close → the supervisor's jittered backoff governs cadence — no churn. Verified by the self-heal test asserting bounded opens, not a storm. |
| T-13-05 | Spoofing | a malicious LAN peer forging notify_klippy_ready/disconnected | accept | Local-trusted LAN; Moonraker is the only peer (CLAUDE.md connectivity model). A forged drop signal at worst triggers a benign reconnect (self-healing), no privilege gain, no new secret exposure. |
| T-13-06 | Tampering | re-subscribe rejected mid-restart | mitigate | The fix treats a rejected/timed-out subscribe as escalate-to-reconnect (G4 Timeout distinction), so a transient mid-restart rejection cannot leave the feed dead-forever. |
</threat_model>

<verification>
- Quick (per commit): `:app:testReleaseUnitTest --tests KlippyReadyResyncTest`.
- Per wave merge: full `:app:testReleaseUnitTest` GREEN.
- The three Wave-0 fix-driver tests (keystone resumed-diff, KlippyRecoveryStateTest, self-heal) are GREEN.
</verification>

<success_criteria>
- D-03: recovery emits Syncing→Connected (visible, never silent).
- SC-2/Pitfall 5: recovery is disconnect-driven off the captured signal.
- Pitfall 4 / D-01 fallback: re-handshake failure self-heals via the proven reconnect; no dead-forever socket.
- Don't-Hand-Roll honored: every change is a reuse of emit/markStale/seed/runHandshake/run() — no new machinery, no poll.
- Full unit suite GREEN.
</success_criteria>

<output>
Create `.planning/phases/13-optimization-network-efficiency-end-to-end-reliability/13-02-SUMMARY.md` when done.
Record which drop signal the fix keyed off (per the capture), and confirm config now stays fresh after a
re-handshake (this UNBLOCKS the 13-03 refreshProbeZOffset removal — Pitfall 3 sequencing).
</output>
