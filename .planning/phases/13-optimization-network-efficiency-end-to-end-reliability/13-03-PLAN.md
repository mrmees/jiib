---
phase: 13-optimization-network-efficiency-end-to-end-reliability
plan: 03
type: execute
wave: 3
depends_on: ["13-02"]
files_modified:
  - docs/request-cadence-contract.md
  - app/src/main/java/works/mees/dinghy/net/MoonrakerSession.kt
  - app/src/main/java/works/mees/dinghy/service/MoonrakerService.kt
  - app/src/main/java/works/mees/dinghy/di/SpineHandle.kt
  - app/src/main/java/works/mees/dinghy/ui/shell/AppShell.kt
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "A committed request-cadence contract doc exists under docs/ inventorying every Moonraker call (one-shot vs subscribe, trigger, frequency) sourced from CommandRegistry + DeriveCapabilities + PrinterStateStore, with a RULES-FOR-FUTURE-PHASES guardrail block (SC-1, D-04/05/06)"
    - "The doc DISTINGUISHES registry-declared specs from ACTUAL production call sites — declared-but-unused entries (e.g. oneshotToken, declared in CommandRegistry yet fetched in production via REST GET /access/oneshot_token in MoonrakerAuth.fetchOneshotToken(), with NO websocket call site) are labelled declared-not-live, NOT listed as live cadence (SC-1, accuracy)"
    - "The doc records the subscribe-set object→consumer justification (every subscribed object justified) and the two-plane throttle model (250ms DEFAULT_SAMPLE_MS), and confirms NO per-screen polling (the delay(4_000) loops are toast timers, verified) (SC-1)"
    - "refreshProbeZOffset()'s redundant configfile re-query is removed — but ONLY after the GREEN ProbeZOffsetFreshnessTest (authored in 13-01, turned/kept GREEN by 13-02) proves the re-handshake keeps probe.z_offset fresh (Pitfall 3 sequencing, behavior-preservation SC-3) — and its wiring is removed across MoonrakerSession + MoonrakerService + SpineHandle + AppShell together (SC-1)"
    - "The committed live SAVE_CONFIG .jsonl captures are referenced from the contract as wire-truth (D-06)"
  artifacts:
    - path: "docs/request-cadence-contract.md"
      provides: "The cadence guardrail doc phases 10–12 must honor"
      contains: "RULES FOR FUTURE PHASES"
      min_lines: 40
  key_links:
    - from: "docs/request-cadence-contract.md"
      to: "CommandRegistry.all"
      via: "call inventory table sourced from the registry specs, cross-checked against real call sites"
      pattern: "objects.subscribe|objects.query|temperature_store"
    - from: "refreshProbeZOffset removal"
      to: "ProbeZOffsetFreshnessTest (GREEN gate)"
      via: "removal proceeds only when the green test proves re-handshake refreshes probe.z_offset"
      pattern: "refreshProbeZOffset"
---

<objective>
Wave 2 — the cadence audit deliverable (SC-1) + the ONE applied cadence fix, sequenced after the
re-handshake fix. Produce the committed request-cadence contract doc (the guardrail phases 10–12 must
honor), then remove the redundant refreshProbeZOffset configfile re-query — but ONLY now that the GREEN
ProbeZOffsetFreshnessTest (authored in 13-01, kept GREEN by 13-02) proves the re-handshake keeps
probe.z_offset fresh after SAVE_CONFIG (Pitfall 3: removing it earlier would expose stale z_offset).

Purpose: D-04 makes this preventative — there is no observed chattiness; the goal is to PROVE the
subscribe-driven model is clean and DOCUMENT the contract before phases 10–12 stack on it (protecting the
weak SBC host CPU as much as the LAN — D-05). The audit found exactly one real fix: refreshProbeZOffset's
redundant configfile query, which is causally coupled to the Thread-1 fix (it was masking the dead-config
symptom of the headline bug).

Output: docs/request-cadence-contract.md (format twin of docs/moonraker-capabilities.md) that
distinguishes declared specs from real call sites + refreshProbeZOffset removed end-to-end with no
stale-config regression, gated on a real green test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-optimization-network-efficiency-end-to-end-reliability/13-CONTEXT.md
@.planning/phases/13-optimization-network-efficiency-end-to-end-reliability/13-RESEARCH.md
@.planning/phases/13-optimization-network-efficiency-end-to-end-reliability/13-PATTERNS.md
@.planning/phases/13-optimization-network-efficiency-end-to-end-reliability/13-02-SUMMARY.md
@docs/moonraker-capabilities.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author docs/request-cadence-contract.md — the call inventory (declared vs live call sites) + subscribe-set justification + future-phase guardrail (SC-1, D-06)</name>
  <files>docs/request-cadence-contract.md</files>
  <read_first>
    - docs/moonraker-capabilities.md (the format twin — match its Purpose-block + table structure)
    - app/src/main/java/works/mees/dinghy/command/CommandRegistry.kt (the call inventory source: objectsSubscribe the ONE persistent call; one-shot seeds identify/serverInfo/objectsList/objectsQuery/temperatureStore/gcodeStore; edge-driven filesMetadata/filesGetDirectory/filesThumbnails/historyList; per-tap gcode specs; the `all` enumeration ~438–486; NOTE oneshotToken is DECLARED ~82–87 and listed in `all` ~440 but has NO production websocket call site)
    - app/src/main/java/works/mees/dinghy/auth/MoonrakerAuth.kt (fetchOneshotToken ~53–58 — production fetches the one-shot token via REST `GET /access/oneshot_token`, NOT via the CommandRegistry.oneshotToken websocket spec; called from MoonrakerSession.kt ~195 before connect — this is the declared-but-unused-on-the-socket case the doc must call out)
    - app/src/main/java/works/mees/dinghy/state/DeriveCapabilities.kt (V1_SUBSCRIBE_CORE ~50–68 + deriveSubscribeSet ~76–98 — the subscribe subset to justify; the intersect-with-detected loop ~81–84 = the A3 "never subscribe to an undefined object" rule)
    - app/src/main/java/works/mees/dinghy/state/PrinterStateStore.kt (DEFAULT_SAMPLE_MS=250L ~273 + the sampled-flush init loop ~130–140 — the two-plane throttle to document)
    - .planning/phases/13-optimization-network-efficiency-end-to-end-reliability/13-RESEARCH.md (§ Cadence Audit lines 160–192 — the full inventory table + recommended doc format + the 4 RULES)
    - .planning/phases/13-optimization-network-efficiency-end-to-end-reliability/13-PATTERNS.md (§ docs/request-cadence-contract.md lines 264–276 — structure spec)
    - docs/commands/e5-saveconfig-capture.jsonl, docs/commands/e3-saveconfig-capture.jsonl (the committed wire-truth to reference)
  </read_first>
  <action>
    Create docs/request-cadence-contract.md mirroring docs/moonraker-capabilities.md's structure. Include:
    (1) a top Purpose block stating it is the wire-truth guardrail that kills the recurring
    chattiness / mock-vs-reality bug class and protects both the LAN and the weak SBC host CPU (D-05);
    (2) the full call-inventory TABLE (every CommandRegistry call: method, transport, one-shot vs
    persistent, trigger, frequency, cadence verdict). CRITICAL — the table must distinguish
    "registry-DECLARED specs" from "ACTUAL production call sites." Walk CommandRegistry.all, but for each
    entry verify whether it actually has a websocket call site in production; specifically CALL OUT
    declared-but-unused entries — `oneshotToken` is declared in CommandRegistry (~82–87, listed in `all`
    ~440) yet production fetches the one-shot token via REST `GET /access/oneshot_token`
    (MoonrakerAuth.fetchOneshotToken ~53–58, invoked from MoonrakerSession ~195), so it has NO live
    websocket cadence — label it "declared, not live on the socket (REST path)" rather than listing it as a
    live call. Do the same check for any other registry entry with no real call site. The point of the
    contract is wire-truth, not a verbatim transcription of the registry. List the live calls: the single
    objects.subscribe, the per-handshake one-shot seeds, the edge-driven one-shots, the per-tap actions;
    (3) the subscribe-set object→consumer justification from DeriveCapabilities (note quad_gantry_level is
    gated off on both test printers but kept forward-compat, and the calibration objects only diff during
    calibration); (4) the two-plane throttle note (high-rate numeric conflated to DEFAULT_SAMPLE_MS=250ms,
    control plane immediate, gcode stream separate); (5) a "RULES FOR FUTURE PHASES" block: no
    objects.subscribe outside the central handshake; no per-screen polling (derive from the central
    subscribe — the delay(4_000) loops are toast timers, NOT a polling precedent); one-shot reads go
    through the SpineHandle off the throttled hot path, edge-driven (filename change / idle transition),
    never timer-driven; high-rate data is never surfaced to the UI faster than 250ms. Reference the
    committed e5/e3 SAVE_CONFIG .jsonl captures as the klippy-restart wire-truth.
    This is a documentation task — no production code change.
  </action>
  <verify>
    <automated>test -s docs/request-cadence-contract.md && grep -q "RULES FOR FUTURE PHASES" docs/request-cadence-contract.md && grep -q "objects.subscribe" docs/request-cadence-contract.md && grep -q "250" docs/request-cadence-contract.md && echo "contract present"</automated>
  </verify>
  <acceptance_criteria>
    - docs/request-cadence-contract.md exists, ≥40 lines, with a Purpose block + the call-inventory table + the subscribe-set justification + the "RULES FOR FUTURE PHASES" block.
    - The table covers the single persistent objects.subscribe, the per-handshake one-shot seeds, the edge-driven one-shots (filesMetadata/historyList), and per-tap actions — matching the ACTUAL production call sites, not a blind copy of CommandRegistry.all.
    - The doc explicitly flags at least the `oneshotToken` registry entry as declared-but-not-live-on-the-socket (production uses the REST GET /access/oneshot_token path), distinguishing declared specs from real call sites (grep for "oneshot" returns the declared-not-live note).
    - The doc records DEFAULT_SAMPLE_MS=250ms conflation and states "no per-screen polling" with the delay(4_000)=toast-timer clarification.
    - The committed e5/e3 SAVE_CONFIG captures are referenced.
  </acceptance_criteria>
  <done>The cadence contract is committed as the phases-10–12 guardrail, sourced from the real code inventory cross-checked against actual call sites (declared-but-unused entries like oneshotToken flagged, not listed as live cadence) and the live captures.</done>
</task>

<task type="auto">
  <name>Task 2: Remove the redundant refreshProbeZOffset configfile re-query end-to-end (SC-1, D-06) — gated on the GREEN ProbeZOffsetFreshnessTest (Pitfall 3)</name>
  <files>app/src/main/java/works/mees/dinghy/net/MoonrakerSession.kt, app/src/main/java/works/mees/dinghy/service/MoonrakerService.kt, app/src/main/java/works/mees/dinghy/di/SpineHandle.kt, app/src/main/java/works/mees/dinghy/ui/shell/AppShell.kt</files>
  <read_first>
    - app/src/test/java/works/mees/dinghy/net/ProbeZOffsetFreshnessTest.kt (the GREEN GATE — authored in 13-01, turned/kept GREEN by 13-02; it asserts a post-restart re-handshake whose configfile reply carries a CHANGED probe.z_offset updates store.probeZOffset to the NEW value. This test — not a SUMMARY sentence — is the proof that the re-handshake keeps probe.z_offset fresh; the removal below is GATED on it being GREEN)
    - .planning/phases/13-optimization-network-efficiency-end-to-end-reliability/13-02-SUMMARY.md (corroborating: which drop branch the fix took; secondary to the green test)
    - app/src/main/java/works/mees/dinghy/net/MoonrakerSession.kt (refreshProbeZOffset ~114–122 — the redundant objects.query{configfile} that duplicates the handshake's configfile read ~387–411 which already calls store.setProbeZOffset ~406)
    - app/src/main/java/works/mees/dinghy/service/MoonrakerService.kt (the wiring ~179: refreshProbeZOffset = { serviceScope.launch { session.refreshProbeZOffset() } })
    - app/src/main/java/works/mees/dinghy/di/SpineHandle.kt (the field ~84: val refreshProbeZOffset: () -> Unit = {})
    - app/src/main/java/works/mees/dinghy/ui/shell/AppShell.kt (the caller ~406: spine?.refreshProbeZOffset?.invoke() at the Probe-Calibrate page entry)
    - .planning/phases/13-optimization-network-efficiency-end-to-end-reliability/13-PATTERNS.md (EDIT SITE 3 lines 107–119; the MoonrakerService teardown lines 131–144 — the edge-driven one-shot pattern to PRESERVE)
  </read_first>
  <action>
    GATE (test-backed, not prose): FIRST run ProbeZOffsetFreshnessTest (13-01) and confirm it is GREEN —
    i.e. a post-restart re-handshake now refreshes probe.z_offset to a changed value via runHandshake's
    configfile read (MoonrakerSession ~395–406). ONLY proceed if that test passes. If it is RED, STOP and
    flag — the re-query is the only thing masking the dead-config symptom (Pitfall 3); do NOT remove it on
    the strength of a SUMMARY sentence.

    Remove the four coupled pieces together: (1) delete MoonrakerSession.refreshProbeZOffset() ~114–122 —
    the handshake's existing configfile read already publishes probe.z_offset via store.setProbeZOffset, so
    a re-handshake on SAVE_CONFIG now refreshes it (proven by the green test); (2) remove the
    MoonrakerService wiring ~179; (3) remove the SpineHandle.refreshProbeZOffset field ~84; (4) remove the
    AppShell caller ~406 (spine?.refreshProbeZOffset?.invoke() at the Probe-Calibrate entry). Do NOT replace
    it with any timer or page-open query — that would violate the contract this very phase documents.
    Preserve the edge-driven one-shot pattern (PrintMetadataHolder per-filename, LastJobHolder on-idle) as
    the canonical compliant shape. Ensure the build compiles with all four references gone, and
    ProbeZOffsetFreshnessTest STILL passes after the removal (it does not reference refreshProbeZOffset; it
    exercises the runHandshake path, so it must stay green and is the regression guard for the removal).
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:assembleRelease :app:testReleaseUnitTest --tests ProbeZOffsetFreshnessTest --no-daemon" | tr -d '\r' && /mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testReleaseUnitTest --no-daemon" | tr -d '\r'</automated>
  </verify>
  <acceptance_criteria>
    - ProbeZOffsetFreshnessTest is GREEN both BEFORE the removal (the gate) and AFTER (the regression guard) — it exercises runHandshake's configfile read, not refreshProbeZOffset, so removing the redundant query must not change its result.
    - `grep -rn "refreshProbeZOffset" app/src/main/java/` returns ZERO matches (method, field, wiring, and caller all removed).
    - No timer/page-open configfile re-query replaces it (grep for a new objects.query{configfile} outside runHandshake returns nothing).
    - `:app:assembleRelease` is SUCCESSFUL and the full `:app:testReleaseUnitTest` suite is GREEN.
    - SUMMARY confirms the Pitfall-3 gate was satisfied by the GREEN ProbeZOffsetFreshnessTest (not merely a 13-02 SUMMARY sentence) before removal.
  </acceptance_criteria>
  <done>The one redundant cadence call is removed end-to-end with no stale-config regression, gated and guarded by the GREEN ProbeZOffsetFreshnessTest; the audit's single applied fix is landed and the build is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app → Moonraker | Removing a redundant query REDUCES the surface; no new boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-07 | Information disclosure | the cadence contract doc | accept | Documents call shapes already public in the codebase + the live captures (no secrets). |
| T-13-08 | Tampering | removing refreshProbeZOffset could expose stale z_offset | mitigate | Removal is GATED behind the GREEN ProbeZOffsetFreshnessTest (13-01) proving the re-handshake keeps probe.z_offset fresh (Pitfall 3) — a real test, not a SUMMARY sentence; the same test is the post-removal regression guard; assembleRelease + the full suite confirm no compile/logic regression; the D-09 on-device UAT (plan 13-04) is the live backstop. |
</threat_model>

<verification>
- docs/request-cadence-contract.md committed with the guardrail rules + the declared-vs-live call-site distinction (oneshotToken flagged declared-not-live).
- ProbeZOffsetFreshnessTest GREEN before AND after removal.
- `grep -rn refreshProbeZOffset app/src/main/java/` → zero matches.
- `:app:assembleRelease` SUCCESSFUL + full unit suite GREEN.
</verification>

<success_criteria>
- SC-1: cadence audit documented (declared vs live call sites, oneshotToken flagged) + applied (the one real fix removed); no per-screen polling reintroduced.
- D-06: the contract doc is the committed guardrail for phases 10–12; live captures referenced.
- Pitfall 3 honored: removal sequenced strictly after — and GATED on — the GREEN ProbeZOffsetFreshnessTest proving config freshness, not a prose sentence.
</success_criteria>

<output>
Create `.planning/phases/13-optimization-network-efficiency-end-to-end-reliability/13-03-SUMMARY.md` when done.
Record that the Pitfall-3 gate was the GREEN ProbeZOffsetFreshnessTest (cite its result), and which
registry entries the contract flagged as declared-but-not-live-on-the-socket (oneshotToken + any others).
</output>
