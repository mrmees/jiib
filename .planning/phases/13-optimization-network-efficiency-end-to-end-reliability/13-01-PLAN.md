---
phase: 13-optimization-network-efficiency-end-to-end-reliability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/13-optimization-network-efficiency-end-to-end-reliability/captures/e5-saveconfig-capture.jsonl
  - .planning/phases/13-optimization-network-efficiency-end-to-end-reliability/captures/e3-saveconfig-capture.jsonl
  - docs/commands/e5-saveconfig-capture.jsonl
  - docs/commands/e3-saveconfig-capture.jsonl
  - tools/ws-capture.py
  - app/src/test/java/works/mees/dinghy/net/SessionTestHarness.kt
  - app/src/test/java/works/mees/dinghy/net/KlippyReadyResyncTest.kt
  - app/src/test/java/works/mees/dinghy/net/ReconnectSupervisorTest.kt
  - app/src/test/java/works/mees/dinghy/net/KlippyRecoveryStateTest.kt
autonomous: false
requirements: []
user_setup: []

must_haves:
  truths:
    - "The exact klippy-restart wire sequence (notify_klippy_disconnected/shutdown/ready ordering + webhooks.state transitions + the FULL post-restart re-handshake replies — server.info, objects.query, objects.subscribe — in the down-window) is captured live on BOTH printers and committed as .jsonl fixtures (D-10, D-02)"
    - "FakeWebSocket/SessionTestHarness can model a klippy-down window via a subscriptionActive flag: it is cleared on the captured klippy-drop signal and set TRUE only after a successful objects.subscribe reply is produced; while unsubscribed, post-restart status frames are REFUSED/DROPPED so an injected diff cannot reach the store unless a re-subscribe actually happened (D-10, closes the inject()-bypass mock-lie)"
    - "KlippyReadyResyncTest asserts RESUMED DIFFS (a post-restart notify_status_update with print_stats.state=printing reaches store.printerState.printState == Printing ONLY when subscriptionActive is true again) — NOT sent-frame counts (D-10)"
    - "A RED KlippyRecoveryStateTest exists asserting the re-handshake emits Syncing then Connected (D-03), with the initial Connected transition awaited-then-reset before the restart signal so the assertion cannot be satisfied by the initial connect — fails against the current silent path"
    - "A RED self-heal test exists asserting a re-handshake whose objects.subscribe is rejected/withheld in the down-window escalates to a full socket reconnect (harness.opens increments AND a SECOND full handshake — identify/objects.subscribe on a fresh socket — is observed), proving connectAndServe was actually re-entered, not merely that close() was called — fails against the current runCatching{}-swallow path"
    - "ReconnectSupervisorTest is extended with a mid-print socket-Closed → reconnect → printState resyncs to Printing case (D-07b, SC-2)"
  artifacts:
    - path: "docs/commands/e5-saveconfig-capture.jsonl"
      provides: "Live E5 (Pi 4) SAVE_CONFIG wire-truth fixture (full handshake + post-restart re-handshake replies)"
    - path: "docs/commands/e3-saveconfig-capture.jsonl"
      provides: "Live E3 (RockPro64) SAVE_CONFIG wire-truth fixture (the repro printer)"
    - path: "tools/ws-capture.py"
      provides: "Scriptable websocket capture client (replays the FULL runHandshake sequence + timestamped frame log)"
    - path: "app/src/test/java/works/mees/dinghy/net/SessionTestHarness.kt"
      provides: "klippy-down window mode + subscriptionActive gate on RespondingFakeWebSocket"
      contains: "subscriptionActive"
    - path: "app/src/test/java/works/mees/dinghy/net/KlippyRecoveryStateTest.kt"
      provides: "D-03 Syncing→Connected emission regression"
  key_links:
    - from: "KlippyReadyResyncTest"
      to: "store.printerState"
      via: "post-restart notify_status_update gated by subscriptionActive → store assertion"
      pattern: "printState"
    - from: "SessionTestHarness klippy-down window"
      to: "captured .jsonl sequence"
      via: "subscriptionActive cleared on drop, set only on a successful re-subscribe reply"
      pattern: "subscriptionActive"
---

<objective>
Wave 0 — capture-first + harden the mock-that-lied. This plan BLOCKS every fix task (D-10): no
re-handshake fix code may be written until the real klippy-restart wire sequence is captured live on
BOTH printers and the test harness replays it faithfully. It also lands the RED regression tests the
Wave-1 fix must turn GREEN.

Purpose: The fix shape literally branches on what the capture shows (does `notify_klippy_ready`
arrive on the same socket? is the signal `webhooks.state` instead? is the failure printer-dependent?).
The current `KlippyReadyResyncTest` is GREEN while reality fails on-device — it asserts sent-frame
counts against a mock that auto-replies instantly and unconditionally (the third strike of the
project's mock-vs-reality class). Worse, `FakeWebSocket.inject(frame)` delivers a frame straight to
`listener.onMessage` regardless of any subscription state, so an injected post-restart status frame
arrives whether or not a re-subscribe happened — a "resumed diffs" test built on raw inject() passes
even when the fix is absent. We capture the truth, harden the mock to it (subscription-gated frame
delivery), and rewrite the test to assert resumed diffs before touching production code.

Output: two committed `.jsonl` capture fixtures (E5 + E3) each replaying the FULL runHandshake +
post-restart re-handshake replies, a reusable capture script, a klippy-down window + subscriptionActive
gate on `SessionTestHarness`, a rewritten resumed-diff `KlippyReadyResyncTest`, a new RED
`KlippyRecoveryStateTest` (D-03), a new RED self-heal test, and an extended `ReconnectSupervisorTest`
(D-07b).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-optimization-network-efficiency-end-to-end-reliability/13-CONTEXT.md
@.planning/phases/13-optimization-network-efficiency-end-to-end-reliability/13-RESEARCH.md
@.planning/phases/13-optimization-network-efficiency-end-to-end-reliability/13-PATTERNS.md
@.planning/phases/13-optimization-network-efficiency-end-to-end-reliability/13-VALIDATION.md
@.planning/todos/pending/save-config-rehandshake-not-refreshing-config.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Live wire-capture of the SAVE_CONFIG / klippy-restart sequence on BOTH printers (D-10, BLOCKING)</name>
  <files>tools/ws-capture.py, docs/commands/e5-saveconfig-capture.jsonl, docs/commands/e3-saveconfig-capture.jsonl</files>
  <read_first>
    - .planning/phases/13-optimization-network-efficiency-end-to-end-reliability/13-RESEARCH.md (§ Wire-Capture Discipline, lines 128–158 — what to capture, how to capture, what to harden)
    - .planning/todos/pending/save-config-rehandshake-not-refreshing-config.md (the repro this capture must reproduce on the wire)
    - app/src/main/java/works/mees/dinghy/net/MoonrakerSession.kt (runHandshake ~322–356 — the EXACT step order the capture script must mirror: identify → server.info → objects.list → objects.query → objects.subscribe)
    - docs/moonraker-capabilities.md (the wire-truth fixture analog + the live printer versions: E5/E3 both Klipper v0.13, Moonraker v0.10, API 1.5.0)
    - docs/commands/printer-matrix.json (committed-wire-truth fixture format precedent — same discipline as Phase 8/9 gcode_store_e5.json)
  </read_first>
  <action>
    Claude does the automatable half autonomously; Matthew triggers the live SAVE_CONFIG.

    AUTOMATED (Claude): Install a scriptable websocket client WSL-native — `pip install websockets`
    (WSL python 3.13, NOT python.exe). Write `tools/ws-capture.py`: connect to
    `ws://<host>:7125/websocket`, send the EXACT SAME handshake the app's `runHandshake()` sends, in the
    SAME order (MoonrakerSession.kt:322–356) — do NOT shortcut it, the D-10 capture is worthless if it
    does not replay what the app actually sends:
      1. `server.connection.identify` (MUST include a non-empty `url` arg — the Phase-2 identify bug),
      2. `server.info`  ← the app sends this; the original capture script OMITTED it,
      3. `printer.objects.list`,
      4. `printer.objects.query` of the v1 core subset (heaters, print_stats, toolhead, gcode_move,
         webhooks, configfile)  ← the app sends a full query-seed BEFORE subscribe; the original script
         OMITTED it,
      5. `printer.objects.subscribe` of that same subset.
    Then log EVERY inbound frame with a monotonic timestamp to the target `.jsonl`, one JSON frame per
    line. After the SAVE_CONFIG window, RE-RUN that whole handshake again on the SAME socket — re-send
    `server.info` → `printer.objects.query` → `printer.objects.subscribe` in order (exactly what the
    in-session re-handshake does) and LOG THE REPLY TO EACH post-restart step (result / error / or
    documented silence), then keep logging diffs for ~30s. The post-restart `objects.query` and
    `objects.subscribe` replies (or their absence/errors) — and whether `notify_klippy_ready` even
    arrives on the same socket — are the load-bearing evidence the Wave-1 fix branches on.
    Capture must enumerate the four RESEARCH § "What to capture" items: (1) ordering/presence of
    notify_klippy_disconnected / notify_klippy_shutdown / notify_klippy_ready — does `ready` arrive on
    the SAME socket? (2) any notify_status_update carrying webhooks.state transitions
    (ready→shutdown/startup→ready) — Pitfall 5 says this may be the real signal; (3) whether the
    post-restart re-query AND re-subscribe each get a normal {result:{status:...}} reply, an error, or
    silence; (4) whether notify_status_update diffs resume after the re-subscribe.

    HUMAN (Matthew, why this is a checkpoint): a real Klipper FIRMWARE_RESTART on physical hardware
    cannot be issued by Claude. Run the capture against E5 (192.168.1.120:7125) first, then E3
    (192.168.1.121:7125); for each, while the script logs, trigger SAVE_CONFIG (or FIRMWARE_RESTART)
    from Mainsail or a second shell, and let it run through klippy down→up.

    Commit BOTH raw captures under docs/commands/ (wire-truth fixtures, sibling to printer-matrix.json)
    AND copy into the phase captures/ dir for plan-local reference. Record in the SUMMARY which
    klippy-state signal each printer actually emitted, the post-restart objects.query/objects.subscribe
    reply for each, and whether the failure is printer-dependent (D-02) — this evidence is what the
    Wave-1 fix branches on (D-01).
  </action>
  <verify>
    <automated>test -s docs/commands/e5-saveconfig-capture.jsonl && test -s docs/commands/e3-saveconfig-capture.jsonl && python3 -c "import json,sys; [json.loads(l) for l in open('docs/commands/e5-saveconfig-capture.jsonl') if l.strip()]; print('e5 parses')"</automated>
  </verify>
  <acceptance_criteria>
    - Both `.jsonl` fixtures exist, are non-empty, and every line is valid JSON.
    - The capture script replays the FULL runHandshake sequence (identify → server.info → objects.list → objects.query → objects.subscribe) — grep/read confirms `server.info` AND `objects.query` are sent both pre-restart and on the post-restart re-handshake, not just identify/list/subscribe.
    - Each capture contains at least one of notify_klippy_disconnected / notify_klippy_shutdown / notify_klippy_ready OR a notify_status_update with a webhooks.state transition across the restart window (the SUMMARY states which signal each printer emitted).
    - The capture includes BOTH post-restart re-handshake replies — the re-`objects.query` reply AND the re-`objects.subscribe` reply (result / error / or documented silence) — and whether diffs resumed; the four RESEARCH § "What to capture" items are answered for each printer.
    - SUMMARY records whether the failure reproduced on the E5 (D-02 — E5 is unproven, not assumed-good).
  </acceptance_criteria>
  <done>Both .jsonl SAVE_CONFIG captures committed (E5 + E3), JSON-valid, replaying the full handshake + post-restart re-handshake (server.info/objects.query/objects.subscribe) replies; SUMMARY records the per-printer signal, the post-restart query/subscribe replies, and whether the E5 reproduced the freeze (D-02).</done>
  <resume-signal>Report which klippy-state signal each printer emitted (disconnected/shutdown/ready on the same socket vs webhooks.state), what the post-restart objects.query and objects.subscribe replies were (result/error/silence), and whether the freeze reproduced on the E5. Type "captured" with that summary, or describe what the capture script needs fixed.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Harden SessionTestHarness with a klippy-down window + subscriptionActive gate + rewrite KlippyReadyResyncTest to assert resumed diffs (D-10 keystone)</name>
  <files>app/src/test/java/works/mees/dinghy/net/SessionTestHarness.kt, app/src/test/java/works/mees/dinghy/net/KlippyReadyResyncTest.kt</files>
  <read_first>
    - app/src/test/java/works/mees/dinghy/net/SessionTestHarness.kt (the mock-that-lied; the @Volatile toggle pattern at failOnOpen ~94 / identifyErrorFrame ~90; replyFor ~112; the OBJECTS_SUBSCRIBE branch ~146–149; the error-frame builder errorFrameWithId DEFINED ~208–210; RespondingFakeWebSocket.send ~228–234 that injects replyFor unconditionally)
    - app/src/test/java/works/mees/dinghy/net/KlippyReadyResyncTest.kt (current sent-frame-count assertions ~90–117; the runTest+harness+launch{session.run()} setup ~76–88 to reuse)
    - app/src/test/java/works/mees/dinghy/net/FakeWebSocket.kt (inject ~55 — delivers straight to listener.onMessage, BYPASSING any subscription state; this is exactly the bypass the gate must close; simulateClosing / simulateFailure / sentFrames the harness builds on)
    - docs/commands/e3-saveconfig-capture.jsonl (the captured down-window sequence to replay — E3 is the repro printer)
    - .planning/phases/13-optimization-network-efficiency-end-to-end-reliability/13-PATTERNS.md (§ SessionTestHarness hardening, lines 173–203; § KlippyReadyResyncTest, lines 206–227)
  </read_first>
  <action>
    In SessionTestHarness add a klippy-down window mode mirroring the EXISTING `failOnOpen` /
    `identifyErrorFrame` toggle pattern (a `@Volatile var klippyDown: Boolean` plus a way to feed the
    captured notification sequence). While `klippyDown`, `replyFor` for OBJECTS_SUBSCRIBE / OBJECTS_QUERY
    must NOT auto-inject a success — instead either return the real mid-restart error frame Moonraker
    sends in the capture (reuse the existing error-frame builder `errorFrameWithId`, DEFINED at
    SessionTestHarness.kt:208–210) or withhold the reply until an explicit "klippy ready" step the test
    drives. Add a helper to inject the captured notification ordering verbatim (disconnected → [gap] →
    ready, or whatever the E3 capture shows) via FakeWebSocket.inject.

    CRITICAL — close the inject() bypass (the mock-that-still-lies trap): add a
    `@Volatile var subscriptionActive: Boolean` flag to the harness/RespondingFakeWebSocket. Wire it so:
      - it starts FALSE (no live subscription),
      - it is set TRUE only when `replyFor`/`send` actually PRODUCES a successful OBJECTS_SUBSCRIBE reply
        (i.e. the success branch ~146–149, not the down-window error/withhold branch),
      - it is cleared FALSE the instant the klippy-drop signal is injected (the captured
        notify_klippy_disconnected/shutdown, or the webhooks.state down transition).
    Then gate post-restart STATUS-frame delivery on it: a `notify_status_update` injected while
    `subscriptionActive == false` must be REFUSED/DROPPED (do not forward to listener.onMessage), so an
    injected post-restart diff can only reach the store if a successful re-subscribe actually flipped the
    flag back to TRUE. This makes a "resumed diffs" assertion impossible to pass without a real
    re-subscribe — closing the FakeWebSocket.inject()-delivers-unconditionally hole that let the old test
    pass with the fix absent. Do NOT make the fake more lenient than the real server — extend the
    existing missingIdentifyArg/invalidObjectsSubset validator philosophy to the restart window.

    Rewrite KlippyReadyResyncTest's assertions: KEEP the runTest(UnconfinedTestDispatcher) + harness +
    PrinterStateStore(scope=backgroundScope, sampleMillis=250L) + launch{session.run()} +
    connectionState.first{Connected} setup. DELETE the sent-frame-count assertions
    (subscribeBefore/tempStoreBefore/configfileBefore comparisons ~90–117). Replace with: drive the
    harness through the captured klippy-down window (inject the drop signal → subscriptionActive clears),
    let the re-handshake run, then inject a post-restart notify_status_update carrying
    print_stats.state=printing via fake.inject, advanceUntilIdle, and assert
    store.printerState.value.printState == PrintState.Printing — which (because of the subscriptionActive
    gate) can ONLY be true if the session genuinely re-subscribed, proving the subscription is LIVE
    again, not merely that a subscribe frame left the client. This is the keystone regression.
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testReleaseUnitTest --tests KlippyReadyResyncTest --no-daemon" | tr -d '\r'</automated>
  </verify>
  <acceptance_criteria>
    - SessionTestHarness.kt contains a `klippyDown` toggle AND a `subscriptionActive` flag; while klippyDown, OBJECTS_SUBSCRIBE/OBJECTS_QUERY do NOT receive an auto-injected success reply (grep confirms the down-window branch in replyFor).
    - subscriptionActive is set TRUE only on a successful OBJECTS_SUBSCRIBE reply and cleared FALSE on the injected klippy-drop signal; a notify_status_update injected while subscriptionActive==false is DROPPED, not delivered to listener.onMessage (grep shows the gate in the inject/delivery path).
    - KlippyReadyResyncTest no longer asserts any `count(after, ...) > ...Before` sent-frame comparison (grep -v '^#' the test file shows zero sent-frame-count assertions).
    - KlippyReadyResyncTest asserts `store.printerState.value.printState` reaches Printing from a post-restart notify_status_update injected AFTER the drop+re-subscribe — and the assertion fails if subscriptionActive was not restored (i.e. it genuinely depends on a re-subscribe).
    - The test compiles and RUNS RED against the unmodified production session (documented in SUMMARY — it is the regression guard Wave 1 turns green). If the build host requires it, the test may be left RED at this plan's commit; the keystone going GREEN is gated on plan 13-02.
  </acceptance_criteria>
  <done>The hardened harness models a real klippy-down window with subscription-gated frame delivery (inject() bypass closed), and the keystone test asserts resumed diffs reach the store ONLY via a real re-subscribe, not sent-frame counts. Test is RED against current production (correct for Wave 0).</done>
</task>

<task type="auto">
  <name>Task 3: New RED tests — KlippyRecoveryStateTest (D-03 Syncing emit) + self-heal escalation + ReconnectSupervisor mid-print resync (D-07b)</name>
  <files>app/src/test/java/works/mees/dinghy/net/KlippyRecoveryStateTest.kt, app/src/test/java/works/mees/dinghy/net/KlippyReadyResyncTest.kt, app/src/test/java/works/mees/dinghy/net/ReconnectSupervisorTest.kt</files>
  <read_first>
    - app/src/test/java/works/mees/dinghy/net/ReconnectSupervisorTest.kt (the connectionState.first{...} idiom ~104–119; networkFailure_retriesForever_withBackoff ~41–61 using harness.failOnOpen + harness.opens; driveClosing via harness.current.get())
    - app/src/test/java/works/mees/dinghy/net/KlippyReadyResyncTest.kt (structure clone source for the new state test)
    - app/src/test/java/works/mees/dinghy/net/SessionTestHarness.kt (the klippy-down window + subscriptionActive flag added in Task 2; current AtomicReference + driveClosing ~226; opens counter ~26–28)
    - app/src/main/java/works/mees/dinghy/state/PrinterState.kt (ConnectionState sealed interface lines 227–232: Connecting/Syncing/Connected/Disconnected/Error)
    - .planning/phases/13-optimization-network-efficiency-end-to-end-reliability/13-PATTERNS.md (§ KlippyRecoveryStateTest lines 243–252; § Self-heal test lines 254–260; § ReconnectSupervisorTest lines 231–239)
  </read_first>
  <action>
    Create KlippyRecoveryStateTest.kt cloning the KlippyReadyResyncTest runTest+harness+launch{session.run()}
    setup. AVOID the false-pass where the initial connect's own Connecting→Syncing→Connected satisfies the
    assertion: FIRST await the initial `connectionState.first { it is Connected }` (the connect completed),
    THEN start a FRESH connection-state collector (or reset the collected list to empty / drop everything
    up to and including that initial Connected). ONLY THEN inject notify_klippy_ready (using the captured
    sequence) on the live socket and assert the RECOVERY emits Syncing THEN Connected on that fresh
    collector. This is RED — the current re-handshake emits NEITHER (D-03 defect) and (critically) the
    assertion can no longer be satisfied by the initial connect transition.

    Add the self-heal test (in KlippyReadyResyncTest or a new file): put the harness in klippyDown mode so
    the re-handshake's objects.subscribe is rejected/withheld. Assert the session escalates to a FULL
    socket reconnect by checking BOTH: (a) harness.opens INCREMENTS (a new socket was opened), AND (b) a
    SECOND full handshake is actually observed on that fresh socket — e.g. a second `identify` /
    `objects.subscribe` appears in the new socket's sentFrames (harness.current.get() after opens
    increments), proving connectAndServe was genuinely RE-ENTERED and the supervisor restarted, not merely
    that close() was called. (Asserting only opens++ is insufficient — confirm the re-handshake replays.)
    Model on ReconnectSupervisorTest.networkFailure_retriesForever. RED against current production (the
    runCatching{} swallow neither closes the socket nor re-enters connectAndServe).

    Extend ReconnectSupervisorTest: add a test driving a mid-print socket Closed
    (harness.current.get()!!.driveClosing()) → reconnect (harness.opens increments, state returns to
    Connected) → inject the running-print status → assert store.printerState resyncs to Printing. The
    socket-death path is already correct (RESEARCH § Current reconnect behavior) so this LOCKS D-07b; it
    may already pass — that is fine, it is the regression lock, not a RED fix-driver.
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testReleaseUnitTest --tests KlippyRecoveryStateTest --tests ReconnectSupervisorTest --no-daemon" | tr -d '\r'</automated>
  </verify>
  <acceptance_criteria>
    - KlippyRecoveryStateTest.kt exists, compiles, and asserts a Syncing-then-Connected connectionState sequence on re-handshake, observed on a collector that was reset/started AFTER the initial Connected (so the initial connect cannot satisfy it); RED against current production (documented in SUMMARY).
    - A self-heal test exists asserting BOTH harness.opens increments AND a second full handshake (identify/objects.subscribe) appears on the new socket when a re-handshake subscribe is rejected/withheld in the klippy-down window — proving connectAndServe re-entry, not just close() called; RED against current production.
    - ReconnectSupervisorTest contains a new mid-print socket-Closed → reconnect → printState==Printing case (D-07b); passes if the socket-death resync is already correct.
    - All three are wired through the real session.run()/connectionState path (no synthetic shortcut), consistent with the project's faithful-mock discipline.
  </acceptance_criteria>
  <done>Three new/extended tests pin D-03 (Syncing emit, collector reset past the initial Connected), the self-heal escalation (opens++ AND observed re-handshake), and D-07b mid-print resync. The two fix-driver tests are RED against current production; the resync lock passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LAN → app websocket | Untrusted Moonraker frames cross here (already parsed by the existing JSON-RPC layer; no new surface) |
| capture script → printer | Read-mostly; the script sends identify+server.info+list+query+subscribe (same frames the app sends) + logs — no writes beyond the existing handshake |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Tampering | `pip install websockets` (WSL python) | mitigate | `websockets` is a long-established PyPI package; install in WSL python only, never bundled into the APK. No new app dependency. |
| T-13-02 | Information disclosure | committed `.jsonl` captures | accept | Local-LAN Moonraker frames contain no secrets (no API key in the capture — identify uses a placeholder url; printer config values only). Matthew confirms no token leaks before commit. |
| T-13-03 | Denial of service | capture script hammering the weak SBC | accept | One websocket, the same handshake the app uses (identify/server.info/list/query/subscribe) — no extra cadence. Read-only logging. |
</threat_model>

<verification>
- Quick: `:app:testReleaseUnitTest --tests KlippyReadyResyncTest` (the keystone — RED at Wave 0).
- Both `.jsonl` fixtures committed, JSON-valid, and replaying the full handshake + post-restart re-handshake replies.
- New RED tests compile and run (RED is correct here — they drive the Wave-1 fix).
</verification>

<success_criteria>
- Capture-first satisfied (D-10): real wire sequence committed for BOTH printers (D-02), replaying the FULL runHandshake (incl. server.info + objects.query) and the post-restart re-handshake replies.
- Mock hardened to a klippy-down window WITH a subscriptionActive gate (inject() bypass closed); KlippyReadyResyncTest asserts resumed diffs that depend on a real re-subscribe.
- KlippyRecoveryStateTest (collector reset past initial Connected) + self-heal test (opens++ AND observed re-handshake) RED against current production; ReconnectSupervisor D-07b case added.
- Wave 1 is UNBLOCKED: the fix has its evidence (capture) and its regression targets (RED tests).
</success_criteria>

<output>
Create `.planning/phases/13-optimization-network-efficiency-end-to-end-reliability/13-01-SUMMARY.md` when done.
Record: which klippy-state signal each printer emits, the post-restart objects.query/objects.subscribe
replies, whether the freeze reproduces on the E5 (D-02), and the exact down-window failure mode the
harness now models (incl. the subscriptionActive gate) — the Wave-1 fix branches on this.
</output>
</content>
</invoke>
