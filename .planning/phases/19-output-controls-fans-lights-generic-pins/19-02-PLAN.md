---
phase: 19-output-controls-fans-lights-generic-pins
plan: 02
type: execute
wave: 0
depends_on: []
files_modified:
  - app/src/test/resources/outputs/configfile_settings_e5p.json
  - app/src/test/resources/outputs/objects_list_e5p.json
  - app/src/test/resources/outputs/configfile_settings_e3p.json
  - app/src/test/resources/outputs/objects_list_e3p.json
  - app/src/test/java/works/mees/dinghy/outputs/OutputsGateTest.kt
  - app/src/test/java/works/mees/dinghy/outputs/OutputsHolderTest.kt
  - app/src/test/java/works/mees/dinghy/command/PrinterCommandsOutputsTest.kt
autonomous: true
requirements: [SC-1, SC-2, SC-3]
must_haves:
  truths:
    - "Real Moonraker configfile.settings + objects.list captures from BOTH printers exist as test fixtures"
    - "RED test scaffolds for the parser, holder, and command builders compile day-one and fail (not error)"
  artifacts:
    - path: "app/src/test/resources/outputs/configfile_settings_e5p.json"
      provides: "E5P live capture — 9 whitelisted outputs in real Moonraker shape"
    - path: "app/src/test/java/works/mees/dinghy/outputs/OutputsGateTest.kt"
      provides: "RED scaffolds: whitelist filter, case-recovery, empty-hides-tile, pwm detection"
    - path: "app/src/test/java/works/mees/dinghy/command/PrinterCommandsOutputsTest.kt"
      provides: "RED scaffolds: builder gcode strings + 0-100→0-1 scaling + clamp"
  key_links:
    - from: "OutputsGateTest.kt"
      to: "app/src/test/resources/outputs/*.json"
      via: "loads real capture fixtures, not idealized mocks"
      pattern: "configfile_settings_e5p"
---

<objective>
Lay the Nyquist validation foundation for Phase 19: capture the REAL Moonraker `configfile.settings`
and `objects.list` JSON from both dev printers as committed test fixtures, and create compiling RED
test scaffolds for the three pure layers built in Wave 1 (the parser/gate, the holder degrade logic,
and the command builders). Wave-0 scaffolds gate every downstream task's `<automated>` verify.

Purpose: The mock-vs-reality trap has bitten this project repeatedly — `OutputsGateTest` must parse
the ACTUAL Moonraker shape (lowercased settings keys, case-preserved objects.list, servo→value not
angle, color_data arrays), not an idealized hand-written mock. Wave-0 RED stubs must COMPILE day-one
(typed `fail()` bodies, no refs to unbuilt symbols) because Gradle compiles the whole test sourceset
before `--tests` filters — a bad scaffold bricks every per-wave test run.
Output: 4 fixture JSON files + 3 compiling RED test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-output-controls-fans-lights-generic-pins/19-RESEARCH.md
@.planning/phases/19-output-controls-fans-lights-generic-pins/19-VALIDATION.md
@app/src/main/java/works/mees/dinghy/command/PrinterCommands.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture real Moonraker fixtures from both printers (mock-vs-reality)</name>
  <files>app/src/test/resources/outputs/configfile_settings_e5p.json, app/src/test/resources/outputs/objects_list_e5p.json, app/src/test/resources/outputs/configfile_settings_e3p.json, app/src/test/resources/outputs/objects_list_e3p.json</files>
  <read_first>
    - .planning/phases/19-output-controls-fans-lights-generic-pins/19-RESEARCH.md § "Discovery Algorithm" + § "Fixtures from live captures" (E5P = 9 whitelisted outputs incl fan_generic/led/neopixel/servo×2/output_pin×4; E3P = 1 virtual output_pin) + § "How each family appears (VERIFIED live)"
    - $HOME/.claude memory [[dinghy-display-printer-addresses]] (E5P 192.168.1.120:7125, E3P 192.168.1.121:7125)
    - $HOME/.claude memory [[dinghy-display-mock-vs-reality]] (why real fixtures matter)
  </read_first>
  <action>
    Query both printers live and save the raw JSON responses as fixtures (these are read-only GETs — safe).
    For E5P: GET `http://192.168.1.120:7125/printer/objects/query?configfile` and save the
    `result.status.configfile.settings` object (the parsed/defaulted view — `pwm` is always a real boolean,
    names are LOWERCASED) to `configfile_settings_e5p.json`; GET `http://192.168.1.120:7125/printer/objects/list`
    and save the `result.objects` array (case-PRESERVED names — the command-name source) to `objects_list_e5p.json`.
    Repeat for E3P at `192.168.1.121:7125` → `configfile_settings_e3p.json` / `objects_list_e3p.json`.
    Save the EXACT server shape (do not hand-edit or idealize). If a printer is unreachable, STOP and report —
    do not synthesize a fixture. The E5P capture must contain fan_generic, led, neopixel, servo, and output_pin
    sections (whitelisted), plus excluded sections (extruder/heater_bed/fan/temperature_sensor/stepper_*) so the
    whitelist-filter test has real negatives to exclude. The E3P capture is the sparse case (1 virtual output_pin).
    Place all four under `app/src/test/resources/outputs/` so test code can load them via classpath resources.
  </action>
  <acceptance_criteria>
    - All 4 JSON files exist under `app/src/test/resources/outputs/` and parse as valid JSON
    - E5P configfile_settings contains keys starting with `fan_generic `, `led `, `neopixel `, `servo `, `output_pin ` (lowercased) AND excluded keys like `extruder`/`heater_bed` (real negatives)
    - E5P objects_list contains the same outputs with case PRESERVED (e.g. `fan_generic FILTER_fan`, not lowercased)
    - E3P configfile_settings is the sparse capture (the single virtual output_pin present)
    - Files are byte-for-byte server output (no hand-editing) — note the capture URLs in a header comment or commit message
  </acceptance_criteria>
  <verify>
    <automated>cd /mnt/e/claude/personal/github/dinghy-display && for f in app/src/test/resources/outputs/*.json; do python -c "import json,sys; json.load(open('$f')); print('OK $f')" || exit 1; done</automated>
  </verify>
  <done>Four real-capture fixtures committed; E5P has the 9-output rich shape + excluded negatives, E3P the sparse shape; all valid JSON, case-correctness confirmed (settings lowercased, objects.list preserved).</done>
</task>

<task type="auto">
  <name>Task 2: Compiling RED scaffolds for gate, holder, and command builders</name>
  <files>app/src/test/java/works/mees/dinghy/outputs/OutputsGateTest.kt, app/src/test/java/works/mees/dinghy/outputs/OutputsHolderTest.kt, app/src/test/java/works/mees/dinghy/command/PrinterCommandsOutputsTest.kt</files>
  <read_first>
    - .planning/phases/19-output-controls-fans-lights-generic-pins/19-VALIDATION.md § "Per-Task Verification Map" + § "Wave 0 Requirements" (the SC→test names: OutputsGateTest#emptyHidesTile, OutputsGateTest#pwmDetection, OutputsHolderTest#servoValueHidden, PrinterCommandsOutputsTest)
    - .planning/phases/19-output-controls-fans-lights-generic-pins/19-RESEARCH.md § "Code Examples" (the exact builder gcode strings + scaling) + § "Live status field map"
    - $HOME/.claude memory [[dinghy-wave0-red-scaffold-compile]] (typed fail() bodies, no refs to unbuilt symbols — the whole test sourceset compiles before --tests filters)
    - app/src/test/java/works/mees/dinghy/calibration/CalibrationGateTest.kt (host JUnit pure-gate test precedent — read for structure)
  </read_first>
  <action>
    Create three JUnit4 test files with RED scaffolds that COMPILE but FAIL (use `org.junit.Assert.fail("not
    implemented — Wave 1")` bodies; do NOT reference symbols that Wave-1 will create — keep each test body a bare
    `fail()` plus a comment naming the exact intended assertion, so the sourceset compiles without OutputsGate /
    OutputsHolder / new PrinterCommands builders existing yet).
    `OutputsGateTest.kt` (package `works.mees.dinghy.outputs`): methods `whitelistFiltersStdOutputs`,
    `recoversCaseFromObjectsList`, `emptyHidesTile`, `pwmDetection` (digital vs PWM output_pin branches on the
    settings `pwm` boolean), `servoAngleMaxFromConfig`, `excludedSectionsNeverAppear`. Add a helper that loads the
    Task-1 fixtures via `this::class.java.classLoader.getResourceAsStream("outputs/configfile_settings_e5p.json")`
    and comment that Wave-1 turns these into real assertions against parseOutputs.
    `OutputsHolderTest.kt` (package `works.mees.dinghy.outputs`): methods `absentValueHidesRowValueButRowTappable`,
    `servoValueHidden` (servo `value` is PWM not angle → degrade), `staticPinReadOnly`, `displayScalingSpeedToPct`
    (raw 0..1 → 0..100% in the holder, not the reducer).
    `PrinterCommandsOutputsTest.kt` (package `works.mees.dinghy.command`): methods
    `setGenericFanScalesPctToWire` (50% → SPEED=0.5), `setGenericFanClampsOutOfRange` (150% → SPEED=1),
    `setLedClampsChannels`, `setLedOffEmitsAllZero` (D-12), `setServoClampsToMaxDeg`, `setPinDigitalEmits0or1`,
    `setPinPwmScalesAndClamps`, `pwmToolUsesSetPin` (no SET_PWM_TOOL — uses SET_PIN). Comment the exact expected
    gcode string for each (from RESEARCH § Code Examples) so Wave-1 fills them in.
    Every file must compile against the CURRENT sourceset (only imports that exist today: JUnit, kotlinx
    serialization for the fixture-load helper, standard lib). Run the build to PROVE they compile and fail.
  </action>
  <acceptance_criteria>
    - The whole test sourceset COMPILES with the three new files present: `:app:compileDebugUnitTestKotlin` succeeds
    - Each test method body is `fail(...)` (or asserts a deliberately-unmet condition) — running them is RED, not ERROR
    - No reference to OutputsGate, OutputDescriptor, OutputsHolder, or new PrinterCommands builders (those don't exist yet)
    - The fixture-load helper successfully reads `outputs/*.json` from test resources (the resource path resolves)
    - Method names match the VALIDATION map (emptyHidesTile, pwmDetection, servoValueHidden, etc.)
  </acceptance_criteria>
  <verify>
    <automated>cmd.exe /c "E:\Android\gw.bat :app:compileDebugUnitTestKotlin --no-daemon" 2>&1 | tr -d '\r' | tail -8</automated>
  </verify>
  <done>Three RED test files compile as part of the full test sourceset, fail (not error), reference no unbuilt symbols, and load the real fixtures; the per-wave `--tests outputs.*` filter now runs without a compile brick.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live Moonraker REST (read-only) → test fixture | captured JSON is the source of truth for parser tests; an idealized mock would hide real-shape bugs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-02-01 | Tampering | fixture diverges from real server shape | mitigate | capture byte-for-byte from live printers; both E5P+E3P; no hand-editing |
| T-19-02-02 | Information Disclosure | captured config may contain host/network details | accept | local-LAN printer config; no secrets/PII in Klipper output sections; fixtures are output-section JSON only |
</threat_model>

<verification>
- All 4 fixtures valid JSON, real server shape (settings lowercased, objects.list case-preserved)
- `:app:compileDebugUnitTestKotlin` succeeds with the 3 RED files present
- RED tests fail (not error) when run; the `outputs.*` filter does not brick the sourceset
</verification>

<success_criteria>
Real captures from both printers committed as fixtures; compiling RED scaffolds for parser/holder/builders mapped to the VALIDATION SC→test names; downstream waves have a non-bricking per-wave test filter.
</success_criteria>

<output>
Create `.planning/phases/19-output-controls-fans-lights-generic-pins/19-02-SUMMARY.md` when done.
</output>
