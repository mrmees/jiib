---
phase: 19-output-controls-fans-lights-generic-pins
plan: 04
type: execute
wave: 1
depends_on: ["19-02"]
files_modified:
  - app/src/main/java/works/mees/dinghy/outputs/OutputDescriptor.kt
  - app/src/main/java/works/mees/dinghy/outputs/OutputsGate.kt
  - app/src/main/java/works/mees/dinghy/net/MoonrakerSession.kt
  - app/src/main/java/works/mees/dinghy/state/PrinterState.kt
  - app/src/main/java/works/mees/dinghy/state/PrinterStateReducer.kt
  - app/src/main/java/works/mees/dinghy/di/AppContainer.kt
  - app/src/test/java/works/mees/dinghy/outputs/OutputsGateTest.kt
autonomous: true
requirements: [SC-1, SC-3]
must_haves:
  truths:
    - "Outputs are discovered from configfile.settings ∩ whitelist ∩ objects.list, case-recovered for commands"
    - "Live values (speed/temperature/target/value/color_data) flow through the central subscribe into a per-rawName map"
    - "AppContainer exposes outputsPresent: Flow<Boolean> (non-empty descriptors) for the D-10 tile gate"
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/outputs/OutputsGate.kt"
      provides: "pure parseOutputs(settings, liveObjects) → List<OutputDescriptor>, whitelist-filtered, case-recovered, alpha-sorted"
      exports: ["parseOutputs"]
    - path: "app/src/main/java/works/mees/dinghy/outputs/OutputDescriptor.kt"
      provides: "model: rawName, family, bare, prettyName, pwm, servoAngleMax, readOnly"
      contains: "data class OutputDescriptor"
    - path: "app/src/main/java/works/mees/dinghy/di/AppContainer.kt"
      provides: "outputsPresent flow for D-10 gating"
      contains: "outputsPresent"
  key_links:
    - from: "net/MoonrakerSession.kt"
      to: "store.setOutputDescriptors"
      via: "5th consumer of the SAME one-shot configfile.settings (no new query)"
      pattern: "setOutputDescriptors"
    - from: "state/PrinterStateReducer.kt"
      to: "PrinterState.outputs"
      via: "reduce fan_generic/heater_generic/led/servo/output_pin/pwm_tool live fields keyed by rawName"
      pattern: "outputs"
---

<objective>
Build the discovery + live-state spine: a pure `parseOutputs` gate (configfile.settings ∩ whitelist ∩
objects.list, case-recovered, prettified, alpha-sorted), the `OutputDescriptor` model, a 5th consumer
on the EXISTING one-shot configfile query, central-subscribe reduction of live output values into a
per-rawName map, and the `outputsPresent` flow that gates the drawer tile (D-10). Turn the Wave-0
`OutputsGateTest` scaffolds green against the real fixtures.

Purpose: SC-1 ("see every controllable output, and nothing it doesn't") and SC-3 ("absent/read-only
degrade gracefully") are both data-layer truths. The discovery algorithm is settled (research):
settings gives metadata (pwm bool always present, servo angle range) but LOWERCASES names, so commands
must use the case-preserved name from objects.list — the `hasMacroIgnoreCase` lesson. No second
configfile query (Pitfall 3).
Output: the outputs package data layer + state wiring + the D-10 gate flow, host-tested on live fixtures.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-output-controls-fans-lights-generic-pins/19-RESEARCH.md
@.planning/phases/19-output-controls-fans-lights-generic-pins/19-PATTERNS.md
@app/src/main/java/works/mees/dinghy/calibration/CalibrationGate.kt
@app/src/main/java/works/mees/dinghy/net/MoonrakerSession.kt
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: OutputDescriptor model + pure parseOutputs gate (SC-1, case-recovery)</name>
  <files>app/src/main/java/works/mees/dinghy/outputs/OutputDescriptor.kt, app/src/main/java/works/mees/dinghy/outputs/OutputsGate.kt, app/src/test/java/works/mees/dinghy/outputs/OutputsGateTest.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/calibration/CalibrationGate.kt (the pure-gate shape: data class + sorted support list, lines 30-43 — the exact precedent)
    - app/src/main/java/works/mees/dinghy/net/MoonrakerSession.kt (configfile.settings parse at lines 499-534 — `objectOrNull`/`booleanOrNull`/`floatOrNull` JsonObject walk helpers)
    - app/src/main/java/works/mees/dinghy/state/Capabilities.kt (`objects` set line 13, `hasObject` line 52, `hasMacroIgnoreCase` line 49 — the case-recovery precedent)
    - .planning/phases/19-output-controls-fans-lights-generic-pins/19-RESEARCH.md § "Discovery Algorithm" (the 7-step algorithm) + § "Pattern 1" (the verified parse) + § "Name Prettifying" (exact transform) + § "Read-only detection" + Pitfall 1/2/4
    - app/src/test/.../outputs/OutputsGateTest.kt (Wave-0 RED scaffold) + app/src/test/resources/outputs/*.json (real fixtures from 19-02)
  </read_first>
  <behavior>
    - parseOutputs filters settings entries whose key's first token ∈ WHITELIST (heater_generic, fan_generic, led, neopixel, dotstar, pca9533, pca9632, servo, output_pin, pwm_tool); excluded sections (extruder/heater_bed/fan/temperature_sensor/stepper_*) NEVER appear
    - case-recovery: rawName resolved by case-insensitive match against liveObjects (objects.list); a settings entry with no matching live object is DROPPED (configured-but-not-loaded)
    - pwm flag read from settings boolean (always present in .settings; digital pin → pwm=false, branch correctly)
    - servoAngleMax read from settings `maximum_servo_angle`, default 180
    - readOnly = true when a `static_value` key is present (static pin); otherwise false
    - prettyName: bare = rawName.substringAfter(' '); replace _ and - with space; title-case each word; collapse spaces. Do NOT camelCase-split (expanderPixel → "Expanderpixel" accepted — RESEARCH Open Q3)
    - result sorted alphabetically by prettyName
    - empty result on E3P-minus-the-pin / no whitelisted outputs → empty list (drives D-10)
  </behavior>
  <action>
    Create the `outputs/` package. `OutputDescriptor.kt`: a `data class OutputDescriptor(val rawName: String,
    val family: String, val bare: String, val prettyName: String, val pwm: Boolean, val servoAngleMax: Float,
    val readOnly: Boolean)`. `OutputsGate.kt`: a `WHITELIST` set of the 10 families (per CONTEXT/RESEARCH) and a
    pure `fun parseOutputs(settings: JsonObject, liveObjects: Set<String>): List<OutputDescriptor>` implementing the
    RESEARCH 7-step algorithm — filter by `it.key.substringBefore(' ') in WHITELIST`, recover the case-correct
    rawName via `liveObjects.firstOrNull { it.equals(lcKey, ignoreCase = true) }` (drop if none), read pwm/
    maximum_servo_angle/static_value from the settings JsonObject with the existing JsonObject helpers, compute
    prettyName via the exact transform (split on `_`/`-`/space only — NO camelCase split), set readOnly per the
    static_value rule, and return the list sorted by prettyName. Pure, no I/O, no Compose (host-testable like
    CalibrationGate). Also add a trivial helper the gate flow uses, e.g. `fun hasAnyOutput(descriptors): Boolean
    = descriptors.isNotEmpty()` (D-10 input).
    Fill the Wave-0 `OutputsGateTest` scaffolds with REAL assertions loading the 19-02 fixtures: whitelist-filters-
    std-outputs (E5P fixture → the 9 outputs, NO extruder/heater_bed), recovers-case (rawName preserves uppercase
    e.g. `FILTER_fan`), empty-hides-tile (a fixture with zero whitelisted → empty list), pwm-detection (digital vs
    pwm output_pin), servo-angle-max-from-config, excluded-sections-never-appear. Use the E5P + E3P fixtures so the
    test parses the REAL Moonraker shape.
  </action>
  <acceptance_criteria>
    - `OutputsGateTest` GREEN against the real fixtures (all Wave-0 `fail()` bodies replaced)
    - E5P fixture parses to exactly the whitelisted outputs (fan_generic/led/neopixel/servo×2/output_pin×N), excluding extruder/heater_bed/fan/sensors
    - rawName is case-PRESERVED (`FILTER_fan`, not `filter_fan`) — proven by an assertion
    - a settings entry with no matching objects.list name is DROPPED
    - pwm boolean drives the digital-vs-pwm distinction; servoAngleMax defaults 180 when absent
    - prettyName transform: `output_pin bed_safety_switch` → "Bed Safety Switch"; `neopixel expanderPixel` → "Expanderpixel" (no camelCase split)
    - list is alpha-sorted by prettyName
  </acceptance_criteria>
  <verify>
    <automated>cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --tests *OutputsGateTest --no-daemon" 2>&1 | tr -d '\r' | tail -6</automated>
  </verify>
  <done>OutputDescriptor + pure parseOutputs implement the settled discovery algorithm with case-recovery, pwm/servo/readOnly metadata, and the exact prettify transform; OutputsGateTest green on real fixtures.</done>
</task>

<task type="auto">
  <name>Task 2: 5th configfile consumer + central-subscribe live values + outputsPresent flow</name>
  <files>app/src/main/java/works/mees/dinghy/net/MoonrakerSession.kt, app/src/main/java/works/mees/dinghy/state/PrinterState.kt, app/src/main/java/works/mees/dinghy/state/PrinterStateReducer.kt, app/src/main/java/works/mees/dinghy/di/AppContainer.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/net/MoonrakerSession.kt (the ONE-SHOT configfile block at lines 490-547: `val settings = parseStatus(cfgResult)?.objectOrNull("configfile")?.objectOrNull("settings")` at 499-501, the four existing consumers at 503-534 — ADD the 5th here, NO new query [Pitfall 3]; the subscribe-subset build for `objects.subscribe` near line 454)
    - app/src/main/java/works/mees/dinghy/state/PrinterState.kt (`partFanSpeed: Double?` at lines 166-167 — the structured-field precedent for the new `outputs` map)
    - app/src/main/java/works/mees/dinghy/state/PrinterStateReducer.kt (the `fan` reduce at line 134, the `temperature_sensor` UPDATE-ON-PRESENT loop at 253-259, the heater field-merge at 232-237 — the per-object reduce precedents)
    - app/src/main/java/works/mees/dinghy/di/AppContainer.kt (`spoolmanPresent: Flow<Boolean>` at line 367, `webcamTileEnabled` combine at 343-344 — the gate-flow precedent)
    - .planning/phases/19-output-controls-fans-lights-generic-pins/19-RESEARCH.md § "Live status field map" (fan→.speed, heater→.temperature/.target, led→.color_data, servo/output_pin/pwm_tool→.value) + Pitfall 1 (reducer stores RAW 0..1, no scaling) + § "Discovery Algorithm" step 6/7
    - .planning/phases/19-output-controls-fans-lights-generic-pins/19-PATTERNS.md § "Live values from central subscribe" + § "Discovery via the existing one-shot configfile consumer"
  </read_first>
  <action>
    (a) MoonrakerSession 5th consumer: in the SAME one-shot configfile block (~line 503-534, do NOT add another
    `objects.query` for configfile — Pitfall 3), call `outputs.OutputsGate.parseOutputs(settings, capabilities
    objects-set)` using the already-derived live objects from objects.list (the `Capabilities.objects` already
    computed in this handshake) and push the result via a new `store.setOutputDescriptors(List<OutputDescriptor>)`
    one-shot StateFlow (model it on `store.setScrewsTiltConfig` / `store.setProbeZOffset` seam — a StateFlow on
    PrinterStateStore re-emitted each handshake/reconnect). Add the whitelisted output object names to the
    `objects.subscribe` subset so their live status diffs flow (where the subscribe subset is built ~line 454 —
    intersect with detected objects, same as the calibration objects were added in 09-02).
    (b) PrinterState + reducer: add `outputs: Map<String, OutputLiveValue>` to PrinterState (define a small
    `OutputLiveValue` holding nullable speed/temperature/target/value/colorData) keyed by rawName, defaulting empty.
    Reduce each whitelisted family's live fields into it WITHOUT scaling (RAW 0..1 — Pitfall 1; the holder scales
    to %): fan_generic→.speed, heater_generic→.temperature/.target, led/neopixel/dotstar/pca*→.color_data (the
    [[r,g,b,w],...] array; keep index 0 = whole-strip), servo/output_pin/pwm_tool→.value. Use the UPDATE-ON-PRESENT
    merge pattern (lines 253-259) so an absent field is retained/null (SC-3 — row hides value, stays tappable),
    never clobbered to a default.
    (c) AppContainer D-10 gate: expose `val outputsPresent: Flow<Boolean>` derived from the descriptors StateFlow
    (non-empty → true), mirroring `spoolmanPresent` at line 367. This is the input the Wave-3 drawer tile gates on.
  </action>
  <acceptance_criteria>
    - The configfile consumer is added INSIDE the existing one-shot block: `grep -c 'ObjectSubsetArgs(setOf("configfile"))' MoonrakerSession.kt` is unchanged (still one configfile query — Pitfall 3)
    - `setOutputDescriptors` exists on the store and is called from the handshake; descriptors re-emit on reconnect
    - whitelisted output object names are added to the subscribe subset (grep shows them intersected with detected objects)
    - PrinterState has an `outputs` map; the reducer stores RAW values (no /100 scaling in the reducer) and uses update-on-present merge (absent field → retained/null, not clobbered)
    - `AppContainer.outputsPresent: Flow<Boolean>` exists and emits false when descriptors empty (D-10 input)
    - full host suite still green (no regression): `:app:testDebugUnitTest`
  </acceptance_criteria>
  <verify>
    <automated>cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --no-daemon" 2>&1 | tr -d '\r' | tail -8</automated>
  </verify>
  <done>5th configfile consumer wired (no new query); output objects in the subscribe subset; PrinterState.outputs reduced RAW with update-on-present; outputsPresent flow exposed for D-10; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Moonraker configfile.settings + objects.list → descriptor | printer-supplied config; lowercased settings names must not become command names |
| central subscribe push → PrinterState | heterogeneous/partial status diffs; absent fields must degrade, not crash |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-04-01 | Tampering | command built from lowercased settings name fails/misfires | mitigate | case-recover rawName from objects.list (case-insensitive match); drop entries with no live object (the hasMacroIgnoreCase lesson) |
| T-19-04-02 | Denial of Service | malformed/partial status diff clobbers state | mitigate | update-on-present merge; nullable fields; reducer never throws on absent keys (mirrors temperature_sensor loop) |
| T-19-04-03 | Spoofing | non-whitelisted/static section surfaced as controllable | mitigate | strict whitelist filter + readOnly flag on static_value pins (SC-3: no command the printer can't accept) |
</threat_model>

<verification>
- `:app:testDebugUnitTest --tests *OutputsGateTest` green on real fixtures
- full `:app:testDebugUnitTest` green (reducer/state regression-free)
- one configfile query only (Pitfall 3); output objects in subscribe subset; outputsPresent emits false on empty
</verification>

<success_criteria>
Discovery via configfile.settings ∩ whitelist ∩ objects.list with case-recovery (SC-1); live values reduced RAW with graceful absent-field handling (SC-3); D-10 outputsPresent flow ready; OutputsGateTest green on real captures.
</success_criteria>

<output>
Create `.planning/phases/19-output-controls-fans-lights-generic-pins/19-04-SUMMARY.md` when done.
</output>
