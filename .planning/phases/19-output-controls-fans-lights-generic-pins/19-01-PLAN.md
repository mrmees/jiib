---
phase: 19-output-controls-fans-lights-generic-pins
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - app/src/main/java/works/mees/dinghy/designsystem/icons/DinghyIcons.kt
  - app/src/main/java/works/mees/dinghy/ui/finetune/ExtrusionScreen.kt
  - tools/verify_ligatures.py
  - app/src/test/java/works/mees/dinghy/designsystem/icons/DinghyIconsTest.kt
autonomous: true
requirements: [SC-1, SC-2]
must_haves:
  truths:
    - "Every Phase-19 output family resolves to its owner-locked glyph (D-01..D-07)"
    - "The part-cooling fan icon is reassigned mode_fan→air (D-08) and ExtrusionScreen still renders a fan glyph"
    - "verify_ligatures.py exits 0 with all 8 D-09 glyphs in the NEEDED set"
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/designsystem/icons/DinghyIcons.kt"
      provides: "7 new output ligature registry entries + D-08 FanMode reassignment"
      contains: "mode_fan_2"
    - path: "tools/verify_ligatures.py"
      provides: "NEEDED set extended with the 8 D-09 glyphs"
      contains: "vital_signs"
  key_links:
    - from: "ui/finetune/ExtrusionScreen.kt"
      to: "DinghyIcons.FanMode"
      via: "single consumer of the reassigned glyph"
      pattern: "FanMode"
---

<objective>
Register the owner-locked Phase-19 output-type icons (D-01..D-07), perform the cross-phase
part-cooling fan glyph reassignment (D-08: `mode_fan`→`air`), and gate every glyph through
`verify_ligatures.py` (D-09). This is the icon foundation Wave-2 detail rows consume.

Purpose: The icon law forbids inventing/substituting glyphs — these are owner-locked (D-01..D-09).
Wiring them once, in the registry, behind the ligature-resolution gate, lets later waves reference
`DinghyIcons.Output*` tokens (never raw strings) with zero glyph risk.
Output: 7 new registry entries, the D-08 reassignment + its single consumer verified, the extended
ligature gate, and the registry drift-guard test kept green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-output-controls-fans-lights-generic-pins/19-CONTEXT.md
@.planning/phases/19-output-controls-fans-lights-generic-pins/19-RESEARCH.md
@docs/ui_design/CLAUDE.md
@app/src/main/java/works/mees/dinghy/designsystem/icons/DinghyIcons.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register the 7 output ligatures + reassign FanMode (D-01..D-08)</name>
  <files>app/src/main/java/works/mees/dinghy/designsystem/icons/DinghyIcons.kt, app/src/main/java/works/mees/dinghy/ui/finetune/ExtrusionScreen.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/designsystem/icons/DinghyIcons.kt (registry entries pattern at lines 26-102; `FanMode = DinghyIcon(IconRef.Ligature("mode_fan"), alternate = "fan_mode")` at line 80; the explicit `all` list at lines 109-118)
    - app/src/main/java/works/mees/dinghy/ui/finetune/ExtrusionScreen.kt (line 271 — the ONE consumer of DinghyIcons.FanMode)
    - .planning/phases/19-output-controls-fans-lights-generic-pins/19-CONTEXT.md (D-01..D-08, OWNER-LOCKED, do not substitute)
    - .planning/phases/19-output-controls-fans-lights-generic-pins/19-PATTERNS.md § "Icon registry (D-01..D-08)" (registry + all-list precedent)
  </read_first>
  <action>
    Add seven new `val` entries to the `DinghyIcons` object, each as
    `DinghyIcon(IconRef.Ligature("<name>"), alternate = "<alt>")`, using EXACTLY these owner-locked ligatures
    (do NOT invent or swap — icon law): OutputHeater→`mode_heat` (D-01), OutputFan→`mode_fan_2` (D-02),
    OutputLed→`lightbulb_2` (D-03, shared by led/neopixel/dotstar/pca9533/pca9632), OutputServo→`cyclone` (D-04),
    OutputPin→`check_box` (D-05, both digital and pwm output_pin variants), OutputPwmTool→`vital_signs` (D-06),
    OutputSection→`output` (D-07, the Outputs section / drawer-tile glyph). Name each `val` clearly
    (`OutputHeater`, `OutputFan`, `OutputLed`, `OutputServo`, `OutputPin`, `OutputPwmTool`, `OutputSection`) and
    give each a unique `alternate` string (icon-no-repeat law — alternates and ligatures must be unique across the
    registry). Add all seven to the explicit `all` list at lines 109-118.
    Then perform the D-08 cross-phase reassignment: change `FanMode` at line 80 from `IconRef.Ligature("mode_fan")`
    to `IconRef.Ligature("air")` (keep the `FanMode` val name and `alternate = "fan_mode"`; this is the part-cooling
    fan glyph — owner-directed). Update the KDoc above it (currently says the font carries `mode_fan`) to note it
    now renders `air`. The single consumer `ExtrusionScreen.kt:271` references `DinghyIcons.FanMode` symbolically,
    so no call-site edit is needed — but grep to PROVE the raw string `mode_fan` and `DinghyIcons.FanMode` have no
    OTHER references that would regress, and confirm ExtrusionScreen still renders the part-fan control via FanMode.
  </action>
  <acceptance_criteria>
    - The 7 new entries exist: `grep -c 'IconRef.Ligature("mode_fan_2")\|IconRef.Ligature("mode_heat")\|IconRef.Ligature("lightbulb_2")\|IconRef.Ligature("cyclone")\|IconRef.Ligature("check_box")\|IconRef.Ligature("vital_signs")\|IconRef.Ligature("output")' DinghyIcons.kt` returns 7
    - `grep 'val FanMode' DinghyIcons.kt` shows `IconRef.Ligature("air")` (NOT `mode_fan`)
    - `grep -rn 'IconRef.Ligature("mode_fan")' app/src/main` returns no hits (the raw string is gone)
    - `grep -rn 'DinghyIcons.FanMode' app/src/main` returns exactly one hit (ExtrusionScreen.kt) — no new regression site
    - The 7 new vals all appear in the `all` list (registry drift-guard)
  </acceptance_criteria>
  <verify>
    <automated>cmd.exe /c "E:\Android\gw.bat :app:compileDebugKotlin --no-daemon" 2>&1 | tr -d '\r' | tail -5</automated>
  </verify>
  <done>All 7 output ligatures registered with unique alternates and present in `all`; FanMode reassigned to `air`; ExtrusionScreen unchanged and still uses FanMode; module compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Extend verify_ligatures.py NEEDED set + keep DinghyIconsTest green (D-09 gate)</name>
  <files>tools/verify_ligatures.py, app/src/test/java/works/mees/dinghy/designsystem/icons/DinghyIconsTest.kt</files>
  <read_first>
    - tools/verify_ligatures.py (the `NEEDED` set at lines 63-78; `main()` at line 79 prints missing and exits 1 if any missing)
    - app/src/test/java/works/mees/dinghy/designsystem/icons/DinghyIconsTest.kt (the Phase-18.1 registry uniqueness/drift-guard test — confirm shape before editing)
    - .planning/phases/19-output-controls-fans-lights-generic-pins/19-RESEARCH.md § "D-09 ligature gate: ALL 8 glyphs PASS" (all 8 already verified resolvable in the v2.944 font)
  </read_first>
  <action>
    Add the 8 D-09 glyph names to the `NEEDED` set in `tools/verify_ligatures.py`: `mode_heat`, `mode_fan_2`,
    `lightbulb_2`, `cyclone`, `check_box`, `vital_signs`, `output`, and `air` (the D-08 target). Research already
    verified all 8 resolve in the bundled v2.944 ttf, so the gate stays green. After Task 1, grep DinghyIcons for
    `IconRef.Ligature("mode_fan")` — if no entry still references it, also remove `mode_fan` from NEEDED (it was
    only there for the now-reassigned FanMode). If any of the 8 unexpectedly reports missing when the gate runs,
    STOP and ask the owner — do NOT silently fall back to a different glyph (icon law).
    Update `DinghyIconsTest.kt` only if its drift-guard hardcodes expected ligature strings or a count — extend it
    to cover the 7 new entries and the FanMode→air change. If the test derives from `DinghyIcons.all` dynamically
    (no hardcoded list), no edit is needed beyond confirming it passes.
  </action>
  <acceptance_criteria>
    - `python tools/verify_ligatures.py` prints `missing: []` and exits 0
    - NEEDED set contains all 8 D-09 names (grep proves `mode_fan_2`, `air`, `cyclone`, `vital_signs`, `lightbulb_2`, `mode_heat`, `check_box`, `output`)
    - `DinghyIconsTest` passes (alternate + ligature uniqueness, all-list coverage)
  </acceptance_criteria>
  <verify>
    <automated>cd /mnt/e/claude/personal/github/dinghy-display && python tools/verify_ligatures.py; echo "lig_exit=$?"; cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --tests *DinghyIconsTest --no-daemon" 2>&1 | tr -d '\r' | tail -5</automated>
  </verify>
  <done>verify_ligatures.py exits 0 with all 8 D-09 glyphs in NEEDED; DinghyIconsTest green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time font asset → render | ligature names must resolve in the bundled ttf; a missing glyph renders as tofu/empty |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01-01 | Tampering | icon glyph substitution | mitigate | `verify_ligatures.py` is a hard gate (exit 1 on any missing); owner-locked D-01..D-09 ligatures, never substituted; registry uniqueness test prevents silent collisions |
| T-19-01-02 | Denial of Service | D-08 reassignment regresses ExtrusionScreen | mitigate | grep proves exactly one FanMode consumer; compile gate + DinghyIconsTest catch breakage |
</threat_model>

<verification>
- `python tools/verify_ligatures.py` exit 0, `missing: []`
- `:app:compileDebugKotlin` succeeds
- `:app:testDebugUnitTest --tests *DinghyIconsTest` green
</verification>

<success_criteria>
All 8 owner-locked glyphs resolve (D-09 gate green); 7 output tokens registered (D-01..D-07); FanMode reassigned to `air` (D-08) with its sole consumer verified; no glyph substituted.
</success_criteria>

<output>
Create `.planning/phases/19-output-controls-fans-lights-generic-pins/19-01-SUMMARY.md` when done.
</output>
