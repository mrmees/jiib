---
phase: 19-output-controls-fans-lights-generic-pins
plan: 19-09
type: execute
wave: 1
gap_closure: true
depends_on: []
autonomous: true
requirements: [SC-2]
files_modified:
  - app/src/main/java/works/mees/dinghy/designsystem/ScrubberPage.kt
  - app/src/main/java/works/mees/dinghy/ui/outputs/OutputScrubberDetail.kt
  - app/src/main/java/works/mees/dinghy/ui/outputs/OutputLedDetail.kt
  - app/src/test/java/works/mees/dinghy/ui/outputs/OutputScrubberSettleTest.kt
  - app/src/main/java/works/mees/dinghy/preview/OutputDetailPreviews.kt

must_haves:
  truths:
    - "On every scrubber detail page (fan, servo, PWM output_pin, pwm_tool, heater) the Off button sits in the gutter beside Back — it does NOT overlay the scrubber value/track (SC-2)."
    - "The LED brightness scrubber no longer nests a second full ScreenScaffold/gutter inside the LED field; the LED page has exactly ONE scaffold (SC-2)."
    - "Existing ApplyCancel scrubber call sites (Temperature / Fine-Tune / Gallery) render unchanged — the new Off slot is OnSettle-only and optional."
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/designsystem/ScrubberPage.kt"
      provides: "ScrubberActions.OnSettle with optional onOff + offLabel; gutter renders [Off | Back] when onOff != null"
      contains: "onOff"
    - path: "app/src/main/java/works/mees/dinghy/ui/outputs/OutputScrubberDetail.kt"
      provides: "OutputScrubberContent with the overlay Off Column removed; Off routed through the gutter slot"
    - path: "app/src/main/java/works/mees/dinghy/ui/outputs/OutputLedDetail.kt"
      provides: "LED brightness control re-homed so it no longer nests a ScreenScaffold inside the LED field"
  key_links:
    - from: "OutputScrubberContent"
      to: "ScrubberActions.OnSettle.onOff"
      via: "Off button passed as a gutter action, not an overlay Column"
      pattern: "onOff\\s*="
---

<objective>
GAP-A (MAJOR, layout): the Off button overlays the scrubber on every scrubber detail page. Give
`ScrubberActions.OnSettle` an OPTIONAL Off slot rendered IN THE GUTTER beside Back, remove the
floating overlay `Column` from `OutputScrubberContent`, and re-home the LED brightness control so it
stops nesting a second `ScreenScaffold`/gutter inside the LED field.

Purpose: the Off action must have its own space on the shared Focus/Field/Gutter grid (button-intent
law: red Danger Off | neutral Back, ≥64px) instead of floating over the scrubber value/track. This
is the shared layout primitive that 19-10 (GAP-B white-only LED brightness page) reuses.

Output: a fixed `ScrubberPage` Off-slot contract + clean (non-overlay) fan/servo/pin/pwm/heater
scrubber layout + a single-scaffold LED page, with updated tests and previews.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-output-controls-fans-lights-generic-pins/19-UAT.md
@.planning/phases/19-output-controls-fans-lights-generic-pins/19-CONTEXT.md
@docs/ui_design/LAYOUT.md
@docs/ui_design/THEMING.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add an optional gutter Off slot to ScrubberActions.OnSettle</name>
  <files>app/src/main/java/works/mees/dinghy/designsystem/ScrubberPage.kt, app/src/test/java/works/mees/dinghy/ui/outputs/OutputScrubberSettleTest.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/designsystem/ScrubberPage.kt (the file being modified — note the `sealed interface ScrubberActions`, the `OnSettle` data class lines 122-125, and the gutter `when (actions)` block lines 280-312 where `OnSettle` currently renders a single full-width Back)
    - app/src/test/java/works/mees/dinghy/ui/outputs/OutputScrubberSettleTest.kt (the file being modified — proves the settle-count contract; must still pass)
    - app/src/main/java/works/mees/dinghy/designsystem/control/OutlinedControl.kt (the `OutlinedControl(label, onClick, modifier, intent)` signature + `Intent.Danger`/`Intent.Neutral`)
    - docs/ui_design/LAYOUT.md (Focus/Field/Gutter grammar — the gutter holds actions on the shared grid; ≥64px targets)
  </read_first>
  <action>
    Extend `ScrubberActions.OnSettle` (in ScrubberPage.kt) with TWO new OPTIONAL params: `onOff: (() -> Unit)? = null`
    and `offLabel: String? = null`, both defaulting null so existing `OnSettle` call sites are source-compatible.
    Add a KDoc line noting the Off slot is the GAP-A fix: when `onOff != null` the gutter renders the Off button
    on the shared grid beside Back, NOT floating over the field.
    In the gutter `when (actions)` / `is ScrubberActions.OnSettle ->` branch, change the single full-width Back Row
    so that when `actions.onOff != null` it renders a TWO-button Row `[Off | Back]`: Off = `OutlinedControl` with
    `intent = Intent.Danger` (button-intent law: red = stop/cancel), label = `actions.offLabel` (fall back to a
    sensible default only if null — but callers will always pass it), `Modifier.weight(1f)`; Back = `Intent.Neutral`,
    `Modifier.weight(1f)`. When `onOff == null` keep the EXISTING single full-width Back (no behavior change for
    callers that don't want an Off). Do NOT touch the `ApplyCancel` branch — that grammar is unchanged (Temperature /
    Fine-Tune / Gallery rely on it).
    Update `OutputScrubberSettleTest` ONLY if the OnSettle constructor change breaks compilation; the settle-count
    assertions (`settleDispatchCount`) are unaffected by the new optional params — keep them green.
  </action>
  <acceptance_criteria>
    - `ScrubberActions.OnSettle` has `onOff: (() -> Unit)? = null` and `offLabel: String? = null` params (grep `onOff` in ScrubberPage.kt returns a match in the data class).
    - With `onOff` non-null, the OnSettle gutter renders TWO buttons (Off `Intent.Danger` + Back `Intent.Neutral`), each `Modifier.weight(1f)`; with `onOff == null` it renders the single full-width Back unchanged.
    - The `ApplyCancel` branch is byte-for-byte unchanged (no new params, no layout change).
    - `OutputScrubberSettleTest` compiles and its 4 assertions pass.
  </acceptance_criteria>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --tests works.mees.dinghy.ui.outputs.OutputScrubberSettleTest --no-daemon" 2>&1 | tr -d '\r' | tail -20</automated>
  </verify>
  <done>OnSettle carries an optional gutter Off slot; ApplyCancel unchanged; settle test green.</done>
</task>

<task type="auto">
  <name>Task 2: Remove the overlay Off Column from OutputScrubberContent; route Off through the gutter slot</name>
  <files>app/src/main/java/works/mees/dinghy/ui/outputs/OutputScrubberDetail.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/outputs/OutputScrubberDetail.kt (the file being modified — `OutputScrubberContent` lines 189-292: the `ScrubberPage(...)` call lines 257-267 currently in OnSettle WITHOUT onOff, and the floating overlay `Column` lines 269-290 that holds the heater-temp readout + Off button + failure toast "over the field")
    - app/src/main/java/works/mees/dinghy/designsystem/ScrubberPage.kt (Task-1 result — the new `OnSettle(onOff, offLabel)` slot)
    - app/src/main/java/works/mees/dinghy/R.string usages already present: `R.string.output_off`, `R.string.output_heater_current` (keep using the SAME string resources)
    - docs/ui_design/LAYOUT.md (one shared grid; the gutter owns actions, the field owns the value)
  </read_first>
  <action>
    In `OutputScrubberContent`, pass the Off action INTO the `ScrubberPage` via the new gutter slot:
    `actions = ScrubberActions.OnSettle(onSettle = { if (enabled) onSettle(it) }, onBack = onBack, onOff = { if (enabled) onOff() }, offLabel = stringResource(R.string.output_off))`.
    DELETE the floating overlay `Column` (the `Modifier.fillMaxWidth().padding(...)` Box-sibling Column that the
    comment calls "float over the field") that currently renders the Off button. The Off button now lives in the gutter.
    Re-home the two remaining overlay-Column elements sanely (NOT floating over the scrubber value):
    (1) the heater current-temp readout (`R.string.output_heater_current`) — render it as a small caption ABOVE the
    scrubber or in a non-overlapping position that does not sit on top of the fill-bar value; do NOT drop the readout.
    (2) the failure `SeverityToast` — keep it but place it so it does not overlap the scrubber value/track (e.g. a
    top-aligned toast within the Box that is not the same vertical center as the value). Keep the `readOnly` branch
    untouched. The outer `Box(modifier.fillMaxSize())` can remain, but it must no longer contain a second Column that
    paints on top of the ScrubberPage field — the ScrubberPage now owns the full screen including its own gutter.
    Keep `onOff`/`heaterTemp`/`failureText` params on `OutputScrubberContent` (the live overload + previews pass them).
  </action>
  <acceptance_criteria>
    - `OutputScrubberContent` passes `onOff` + `offLabel` to `ScrubberActions.OnSettle` (grep `onOff =` in OutputScrubberDetail.kt returns a match).
    - The floating overlay `Column` that rendered the Off button "over the field" is removed — no `OutlinedControl` with `intent = Intent.Danger` remains as a Box-sibling of `ScrubberPage` in `OutputScrubberContent`.
    - The heater current-temp readout (`R.string.output_heater_current`) is still rendered for `type == HEATER` (grep confirms the string ref survives).
    - The failure toast (`SeverityToast`) still appears on a dispatch failure and does not overlay the scrubber value.
    - `:app:assembleDebug` compiles.
  </acceptance_criteria>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:compileDebugKotlin --no-daemon" 2>&1 | tr -d '\r' | tail -20</automated>
  </verify>
  <done>Off lives in the gutter on fan/servo/PWM/heater scrubbers; heater-temp + toast re-homed without overlay; compiles.</done>
</task>

<task type="auto">
  <name>Task 3: Re-home the LED brightness control out of the nested ScrubberPage scaffold + update previews</name>
  <files>app/src/main/java/works/mees/dinghy/ui/outputs/OutputLedDetail.kt, app/src/main/java/works/mees/dinghy/preview/OutputDetailPreviews.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/outputs/OutputLedDetail.kt (the file being modified — `OutputLedContent` lines 154-262: the LED page's own `ScreenScaffold` (line 175) whose field at lines 220-236 NESTS a whole `ScrubberPage` (which brings its OWN ScreenScaffold + gutter) inside a `Box(...).weight(1f)`. This double-scaffold is the GAP-A compounding bug.)
    - app/src/main/java/works/mees/dinghy/designsystem/ScrubberPage.kt (note: `ScrubberPage` is a full-screen page with its own `ScreenScaffold` + gutter — it is NOT a drop-in inline widget; nesting it inside another scaffold is the bug)
    - app/src/main/java/works/mees/dinghy/designsystem/control/OutlinedControl.kt (for an inline ± stepper row if you build a non-scaffold brightness control)
    - app/src/main/java/works/mees/dinghy/preview/OutputDetailPreviews.kt (the file being modified — `LedCell()` + the 6-theme `@Nexus7Previews` LED matrix + `LedFsLargeOverflow`; must still compile/render)
    - app/src/main/java/works/mees/dinghy/theme/fsSp usage already in the file (fsSp scale — see [[dinghy-font-sizes-too-small]])
  </read_first>
  <action>
    In `OutputLedContent`, re-home the brightness control so the LED page has exactly ONE `ScreenScaffold` (the LED
    page's own). Do NOT nest `ScrubberPage` (which carries its own scaffold + gutter) inside the LED field. Replace the
    nested `ScrubberPage` (lines 220-236) with an INLINE brightness control that lives in the LED field's Column: a
    self-contained fill-bar/slider + a `[− +]` stepper Row built from `OutlinedControl`, dispatching the SAME
    `onColorSettle(hue, settled)` on settle (gesture-end / stepper tap) — preserve the existing OnSettle semantics
    (one dispatch per settle, never per scrub frame; WHITE=0 color policy unchanged in the dispatch path). Keep the
    hue wheel, the live swatch, the brightness label string (`R.string.output_led_brightness`), the Off (Danger) +
    Back (Neutral) gutter Row exactly as-is. Keep `onColorSettle`/`onOff`/`onBack`/`enabled`/`failureText` params and
    the `initialHue`/`initialBrightness` seeding intact.
    NOTE for 19-10: do NOT yet add white-only branching here — that is GAP-B's job (this plan only fixes layout). The
    inline brightness control you build here is the SAME control 19-10 will reuse for the white-only LED page, so make
    it a reusable composable (e.g. an internal `LedBrightnessControl(value, onSettle, enabled)`), not buried logic.
    Update `OutputDetailPreviews.kt`: `LedCell()` already drives `OutputLedContent` — verify the 6-theme matrix +
    `LedFsLargeOverflow` still render with the re-homed control (no nested-scaffold clipping). Adjust the preview cell
    only if the param list changed.
  </action>
  <acceptance_criteria>
    - `OutputLedContent` contains exactly ONE `ScreenScaffold` invocation (grep `ScreenScaffold(` in OutputLedDetail.kt returns 1).
    - `ScrubberPage(` no longer appears inside `OutputLedContent` (grep confirms the nested page is gone).
    - The brightness control still dispatches via `onColorSettle(hue, brightness)` on settle and steps via a `[− +]` row.
    - The hue wheel, live swatch, Off (Danger) + Back (Neutral) gutter, and `R.string.output_led_brightness` label all remain.
    - The reusable inline brightness composable is `internal`/named so 19-10 can reuse it for the white-only page.
    - `OutputDetailPreviews.kt` compiles; the LED `@Nexus7Previews` matrix renders.
  </acceptance_criteria>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --tests works.mees.dinghy.ui.outputs.* --no-daemon" 2>&1 | tr -d '\r' | tail -25</automated>
  </verify>
  <done>LED page is single-scaffold; brightness is an inline reusable control; all ui.outputs host tests + previews green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user touch → ScrubberPage gesture | tap/drag drives the value; Off is a destructive-ish disable action |
| Off button placement | a mis-placed/overlapping Off can be fat-fingered while scrubbing (the GAP-A symptom) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-09-01 | Tampering (UI) | overlay Off Column over the scrubber track | mitigate | move Off into the gutter on the shared grid (≥64px, Danger intent) so it can't sit on/over the value/track — the GAP-A fix itself |
| T-19-09-02 | Denial (perf) | nested double-scaffold in LED page | mitigate | collapse to ONE ScreenScaffold; inline brightness control avoids a second full-page recomposition tree on Adreno-320 |
| T-19-09-03 | Repudiation | accidental Off vs Back mis-tap | accept | Off (red Danger) and Back (neutral) are visually distinct per button-intent law; Off only sends a zero/disable command, fully reversible by re-scrubbing |
| T-19-09-SC | Tampering | npm/pip/cargo installs | mitigate | NONE — this plan adds no dependencies; no package-manager install tasks |
</threat_model>

<verification>
- `:app:testDebugUnitTest` for `works.mees.dinghy.ui.outputs.*` + `OutputScrubberSettleTest` green (run via the Windows helper, NOT ./gradlew).
- `:app:assembleDebug` / `:app:compileDebugKotlin` compiles (no nested-scaffold, no overlay Column).
- Visual confirmation is the existing 19-08 on-device re-UAT gate (orchestrator-owned) — checks 2/4/5 (fan/PWM-pin/servo Off no longer overlays) on flox. Do NOT add a new checkpoint plan here.
</verification>

<success_criteria>
- SC-2: on every scrubber detail page the Off button sits in the gutter beside Back, not overlaying the scrubber; the LED page has a single scaffold.
- Existing ApplyCancel call sites (Temperature/Fine-Tune/Gallery) are unaffected (new Off slot is optional + OnSettle-only).
- All host unit tests + the @Preview matrix remain green.
</success_criteria>

<output>
Create `.planning/phases/19-output-controls-fans-lights-generic-pins/19-09-SUMMARY.md` when done.
</output>
