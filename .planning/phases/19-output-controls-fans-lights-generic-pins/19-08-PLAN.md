---
phase: 19-output-controls-fans-lights-generic-pins
plan: 08
type: execute
wave: 5
depends_on: ["19-07"]
files_modified:
  - .planning/phases/19-output-controls-fans-lights-generic-pins/19-UAT.md
autonomous: false
requirements: [SC-4]
must_haves:
  truths:
    - "The full host suite is green and verify_ligatures.py exits 0 before the on-device gate"
    - "The Outputs feature is proven live against the real printers (E5P rich, E3P sparse) with state-flip confirmation"
    - "REQUIRED /server/gcode_store command-evidence for fan, LED, output_pin, and servo (objective wire proof, not optional)"
  artifacts:
    - path: ".planning/phases/19-output-controls-fans-lights-generic-pins/19-UAT.md"
      provides: "the recorded on-device UAT checklist + owner sign-off + required gcode_store evidence"
  key_links:
    - from: "on-device flox + E5P"
      to: "object state flip + /server/gcode_store cross-check"
      via: "set fan/LED/pin/servo → confirm via subscribe state AND required server-side gcode evidence"
      pattern: "gcode_store"
---

<objective>
Close SC-4: prove the Outputs feature live against the real printers. Run the full host suite + the D-09
ligature gate green, build/install on flox, then drive the actual hardware (E5P rich config, E3P sparse)
and confirm each control via the object-state flip AND the REQUIRED server-side `/server/gcode_store`
command evidence (for fan/LED/pin/servo). Owner is the authoritative gate.

Purpose: SC-4 is "proven live against the real printers' actual fan/pin/LED configuration." The project's
recurring mock-vs-reality lesson means green host tests are necessary but NOT sufficient — the on-device
run catches what the FakeWebSocket can't (real Moonraker shape, real state flips, real Adreno-320 perf).
This wave is ALSO the only place the true FULL `:app:testDebugUnitTest` gate runs (HIGH-4: per-wave gates
were scoped to specific classes because Wave-0 RED scaffolds make an intermediate full-suite gate fail; by
this final wave every scaffold is filled, so the full suite is the legitimate pre-UAT phase gate).
Output: the recorded 19-UAT.md with owner sign-off and required gcode_store evidence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-output-controls-fans-lights-generic-pins/19-VALIDATION.md
@.planning/phases/19-output-controls-fans-lights-generic-pins/19-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-gate — full host suite (the phase gate) + D-09 ligature gate green, build + install on flox</name>
  <files>.planning/phases/19-output-controls-fans-lights-generic-pins/19-UAT.md</files>
  <read_first>
    - .planning/phases/19-output-controls-fans-lights-generic-pins/19-VALIDATION.md (the SC→test map + Manual-Only Verifications row + the sampling/phase-gate rules — note the full suite is the PHASE gate, per-wave is scoped)
    - .planning/phases/19-output-controls-fans-lights-generic-pins/19-RESEARCH.md § "SC-4 'proven live' plan" (the exact on-device walk: open Outputs → Filter Fan, Chamber Light, Expanderpixel, Camera Servo, Mosfet2/3, virtual pins; set each; confirm via state flip + /server/gcode_store)
    - $HOME/.claude memory [[dinghy-stream-uat-via-moonraker]] (server-side gcode_store cross-check method) + [[dinghy-display-ondevice-iteration]] (Claude builds/installs, Matthew navigates/eyeballs) + [[dinghy-display-build-env]] (build via E:\Android\gw.bat; flox on USB)
    - CLAUDE.md "Installing a RELEASE build" / installDebug notes
  </read_first>
  <action>
    Run the phase gate locally: full `:app:testDebugUnitTest` GREEN (this is the legitimate full-suite gate — by this
    final wave every Wave-0 RED scaffold has been filled by its owning plan, HIGH-4) and `python tools/verify_ligatures.py`
    exit 0 (the D-09 gate). Then build the debug APK and install on flox via the documented helper
    (`cmd.exe /c "E:\Android\gw.bat :app:installDebug --no-daemon"`; flox is the genuine Adreno-320 / LineageOS 18.1
    device on USB). Scaffold `19-UAT.md` with the verification checklist below (status pending each). Note the two
    printer targets: E5P (192.168.1.120:7125 — the rich config: Filter Fan, Chamber Light/Expanderpixel LEDs, Camera
    Servo, Mosfet output pins, virtual pins) and E3P (192.168.1.121:7125 — the sparse single virtual output_pin,
    proves the tile is NOT hidden when ≥1). Do NOT mark any UAT item resolved — Task 2 is the owner gate.
    NOTE in `19-UAT.md`: neither dev printer exposes a `heater_generic` — its detail page is covered by unit tests
    ONLY (existing `setHeater` builder + 19-03 tests + the CommandRegistry drift test); live on-device verification of
    `heater_generic` is EXPLICITLY ACCEPTED as automated-only / not-live-covered, deferred to a printer that exposes
    one. This does NOT block SC-4 (scope = "fan/pin/LED configuration"). Record it as an accepted exclusion, not a gap.
  </action>
  <acceptance_criteria>
    - full `:app:testDebugUnitTest` GREEN (no failures) — the phase gate (HIGH-4: full suite legitimately runs here)
    - `python tools/verify_ligatures.py` exits 0 (`missing: []`)
    - debug APK installed on flox (BUILD SUCCESSFUL + install confirmed)
    - `19-UAT.md` scaffolded with the checklist (all pending), both printer targets noted, heater_generic recorded as accepted automated-only exclusion
  </acceptance_criteria>
  <verify>
    <automated>cd /mnt/e/claude/personal/github/dinghy-display && python tools/verify_ligatures.py; echo "lig=$?"; cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --no-daemon" 2>&1 | tr -d '\r' | tail -6</automated>
  </verify>
  <done>Full host suite (phase gate) + D-09 gate green; debug APK on flox; 19-UAT.md scaffolded with the on-device checklist for both printers and the heater_generic accepted exclusion.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: On-device UAT on flox + live E5P/E3P (SC-4) — owner gate</name>
  <files>.planning/phases/19-output-controls-fans-lights-generic-pins/19-UAT.md</files>
  <action>
    This is a BLOCKING human-verify checkpoint — the owner (Matthew) drives the installed app on flox + live
    printers and records each result in 19-UAT.md. Claude does not auto-advance. See `<what-built>` /
    `<how-to-verify>` / `<resume-signal>` for the gate contract; the executor pauses here until the owner reports.
  </action>
  <what-built>
    The full Outputs feature: a drawer tile (hidden when no outputs, D-10) → a flat alpha Outputs list →
    per-type immediate-dispatch detail pages (fan %, LED hue+brightness+Off, output_pin toggle/PWM, servo angle,
    heater setpoint, pwm_tool %). Built, host-tested green, D-09 ligatures verified, installed on flox.
  </what-built>
  <how-to-verify>
    On flox connected to E5P (192.168.1.120:7125):
    1. Swipe up → confirm the **Output** tile is present (icon = the `output` glyph from DinghyIcons.OutputSection) and tap it.
    2. Confirm the list shows ONLY whitelisted outputs (Filter Fan, Chamber Light, Expanderpixel, Camera Servo,
       Mosfet pins, virtual pins) — NO nozzle/bed/part-fan/sensors (SC-1).
    3. **Filter Fan:** open → scrub to ~50% → release → fan responds; the row value updates to ~50% (state flip, SC-2).
    4. **Chamber Light (LED):** open → pick a hue + brightness → release → strip changes color; tap **Off** → strip
       goes dark (D-12). Row shows swatch + brightness.
    5. **A Mosfet (output_pin):** open → toggle On/Off (digital) or scrub % (PWM) → pin responds; row reflects it (SC-2).
    6. **Camera Servo:** open → set an angle → servo moves; the row hides the value (servo value isn't angle, SC-3 degrade);
       tap **Off** → servo disables (WIDTH=0).
    7. Trigger a failure (e.g. while disconnected, or an intentionally bad case) → confirm a toast shows and you STAY
       on the page (no nav away).
    8. Confirm controls work WHILE the printer is printing (no print-state lockout) — if a print is convenient.
    9. Perf: the list scrolls and the detail pages respond without frozen frames on the Adreno-320 (responsiveness gate).
    Then on E3P (192.168.1.121:7125): confirm the **Output** tile IS present (≥1 output) and the list shows the single
    sparse output (Ignore M600) — proving the tile is NOT hidden when exactly one output exists.
    REQUIRED command-evidence cross-check (NOT optional — review fix): for at least the **fan, LED, output_pin, and
    servo** dispatches above, capture server-side `GET /server/gcode_store` on the printer and paste the matching
    `SET_FAN_SPEED FAN=... / SET_LED LED=... WHITE=0 / SET_PIN PIN=... / SET_SERVO SERVO=...` lines into 19-UAT.md as
    objective wire evidence (confirms the bare section name reached the wire — HIGH-1 — and the command went through
    the catalog). Each of fan/LED/pin/servo MUST have a recorded gcode_store line; missing evidence = that item is not
    PASS.
    Record each item PASS/FAIL in 19-UAT.md.
  </how-to-verify>
  <verify>
    <human-check>Owner runs the on-device walk on flox + E5P/E3P, captures the REQUIRED /server/gcode_store evidence for fan/LED/pin/servo, records PASS/FAIL per item in 19-UAT.md; replies "approved" only when all items pass with the required gcode evidence attached.</human-check>
  </verify>
  <done>All UAT items recorded PASS in 19-UAT.md with owner sign-off and the required fan/LED/pin/servo gcode_store evidence attached; any FAIL spawns a gap-closure plan (orchestrator-owned), not a phase-complete mark.</done>
  <resume-signal>Type "approved" if all items pass (with the required fan/LED/pin/servo gcode_store evidence recorded), or describe failures (each FAIL spawns a gap-closure plan, not a phase-complete mark).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app command → real printer hardware | live actuation of fans/LEDs/pins/servos/heaters on physical hardware |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-08-01 | Tampering | a command the printer can't accept reaches hardware (SC-3) | mitigate | on-device UAT exercises read-only/servo-degrade paths; clamp authority (19-03) bounds every value; owner gate catches any reject-toast path |
| T-19-08-02 | Repudiation | "works on host, fails on device" (mock-vs-reality) | mitigate | host green is necessary not sufficient — on-device state-flip + REQUIRED /server/gcode_store evidence for fan/LED/pin/servo is the authoritative SC-4 gate |
| T-19-08-03 | Tampering | wrong wire string (family prefix / wrong command) silently accepted | mitigate | required gcode_store evidence shows the EXACT bare-name command on the wire (HIGH-1 proof on real hardware) |
</threat_model>

<verification>
- full `:app:testDebugUnitTest` green + `verify_ligatures.py` exit 0 (pre-gate / phase gate)
- on-device: every control confirmed via object-state flip on flox + E5P; REQUIRED gcode_store evidence for fan/LED/pin/servo; sparse E3P renders (tile not hidden); owner signs off
</verification>

<success_criteria>
SC-4 met: the Outputs feature is proven live on the real printers (rich E5P + sparse E3P) with state-flip confirmation, REQUIRED fan/LED/pin/servo gcode_store evidence, and owner sign-off recorded in 19-UAT.md; heater_generic accepted automated-only; any FAIL spawns gap closure (orchestrator-owned), not a phase-complete mark.
</success_criteria>

<output>
Create `.planning/phases/19-output-controls-fans-lights-generic-pins/19-08-SUMMARY.md` when done (after the owner reports the UAT result).
</output>
