---
phase: 19-output-controls-fans-lights-generic-pins
plan: 05
type: execute
wave: 2
depends_on: ["19-01", "19-03", "19-04"]
files_modified:
  - app/src/main/java/works/mees/dinghy/outputs/OutputsHolder.kt
  - app/src/main/java/works/mees/dinghy/ui/outputs/OutputsScreen.kt
  - app/src/main/java/works/mees/dinghy/preview/OutputsPreviews.kt
  - app/src/test/java/works/mees/dinghy/outputs/OutputsHolderTest.kt
autonomous: true
requirements: [SC-1, SC-2, SC-3]
must_haves:
  truths:
    - "The holder exposes alpha-sorted descriptors with display-scaled live values and per-detail busy state"
    - "The Outputs list shows icon + prettyName + live value per row; absent value hides only the value, row stays tappable"
    - "Each output family row uses its owner-locked DinghyIcons token (D-01..D-06), never a raw ligature string"
    - "The Outputs list + a representative detail ship a @Preview matrix (6 theme combos + fs=L, no live Moonraker)"
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/outputs/OutputsHolder.kt"
      provides: "StateFlow of descriptors + live values (display-scaled) + per-detail busy/markPending"
      min_lines: 40
    - path: "app/src/main/java/works/mees/dinghy/ui/outputs/OutputsScreen.kt"
      provides: "flat alpha list, drawer-suppressed, Back-only gutter, stateless content seam"
      contains: "LazyColumn"
    - path: "app/src/main/java/works/mees/dinghy/preview/OutputsPreviews.kt"
      provides: "preview matrix for the Outputs list"
  key_links:
    - from: "ui/outputs/OutputsScreen.kt"
      to: "DinghyIcons.Output*"
      via: "per-family row glyph from the registry (D-01..D-06)"
      pattern: "DinghyIcons.Output"
    - from: "outputs/OutputsHolder.kt"
      to: "PrinterStateStore + PrinterCommands.outputPctToWire"
      via: "combine descriptors+live values; clamped markPending target (17-07)"
      pattern: "markPending"
---

<objective>
Build the Outputs list surface: the `OutputsHolder` (descriptors + display-scaled live values + per-detail
busy state) and the `OutputsScreen` flat alpha list (icon + prettyName + live value, drawer-suppressed,
Back-only gutter), plus its preview matrix. Turn the Wave-0 `OutputsHolderTest` scaffolds green.

Purpose: SC-1 (see every output), SC-3 (absent value hides only the value, row stays tappable). The list
mirrors `CalibrationHubScreen` (a hub of tappable rows → detail pages) but is a flat `LazyColumn` of rows.
Display scaling (0..1 → %) lives in the holder, not the reducer (FineTune precedent). PREVIEW_AND_TOKENS
is mandatory day-one: a @Preview matrix + stringResource strings + DinghyIcon tokens from the start.
Output: holder + list screen + previews, host-tested.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-output-controls-fans-lights-generic-pins/19-RESEARCH.md
@.planning/phases/19-output-controls-fans-lights-generic-pins/19-PATTERNS.md
@docs/ui_design/LAYOUT.md
@docs/ui_design/PREVIEW_AND_TOKENS.md
@app/src/main/java/works/mees/dinghy/ui/calibration/CalibrationHubScreen.kt
@app/src/main/java/works/mees/dinghy/ui/finetune/FineTuneHolder.kt
@app/src/main/java/works/mees/dinghy/preview/SpoolPreviews.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: OutputsHolder — descriptors + display-scaled live values + per-detail busy (SC-1/SC-3)</name>
  <files>app/src/main/java/works/mees/dinghy/outputs/OutputsHolder.kt, app/src/test/java/works/mees/dinghy/outputs/OutputsHolderTest.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/finetune/FineTuneHolder.kt (the combine shape at lines 128-148; display scaling in the holder not reducer at 47-49 + buildVm 286-325; markPending/clearPending/reached/toleranceFor at 154-203 — and the 17-07 lesson: pending target MUST be the clamped wire value)
    - app/src/main/java/works/mees/dinghy/calibration/CalibrationHubHolder.kt (the simpler descriptor-list holder template, lines 24-39)
    - app/src/main/java/works/mees/dinghy/command/PrinterCommands.kt (the `outputPctToWire`/clamp helpers added in 19-03 — feed markPending the SAME clamped value)
    - .planning/phases/19-output-controls-fans-lights-generic-pins/19-RESEARCH.md § "Live status field map" (raw 0..1 fields per family) + § "Read-only detection" (servo value not angle → hide; static pin read-only) + § "Per-Type Control-Page Shape Mapping"
    - app/src/test/.../outputs/OutputsHolderTest.kt (Wave-0 RED scaffold)
  </read_first>
  <behavior>
    - holder combines store.outputDescriptors + store.printerState.outputs → a list of row view-models, alpha-sorted by prettyName
    - per row, the holder converts RAW live values to display: fan_generic .speed → round(speed*100)%; output_pin pwm / pwm_tool .value → round(value*100)%; output_pin digital → On/Off from value; heater_generic → current temp °C; led → brightness% + swatch from color_data[0]
    - servo .value is PWM not angle → the holder hides the row value (degrade, SC-3) — does NOT back-compute angle
    - absent live value → row value is null/hidden but the row stays present and tappable (SC-3)
    - static-value/readOnly descriptor → row shown value-only, marked read-only (no settable control)
    - per-detail busy: a markPending(rawName, clampedWireValue) arms an optimistic target; clearPending on failure/timeout; busy scoped to that detail's key (NOT the whole list). The pending target is computed via PrinterCommands' clamp helper so it equals the wire value (17-07)
  </behavior>
  <action>
    Create `outputs/OutputsHolder.kt` mirroring `FineTuneHolder`'s combine + per-spine busy structure but over the
    descriptor list. `combine(store.outputDescriptors, store.printerState) { descriptors, state -> ... }` → a
    `StateFlow<List<OutputRowVm>>` where `OutputRowVm` carries the descriptor + a display value (the scaled/derived
    presentation: `"45%"`, `"On"`, `"210°C"`, brightness%+color, or null when absent/servo). Do the 0..1→% scaling
    HERE (Pitfall 1 — reducer stores raw). For servo, leave displayValue null (hide — SC-3). For a readOnly
    descriptor, set a readOnly flag the detail page honors. Implement per-detail busy with markPending/clearPending/
    reached, feeding markPending the CLAMPED wire value from `PrinterCommands.outputPctToWire(...)` (or the matching
    clamp helper) so the optimistic target equals the dispatched command (17-07 — unclamped wedged the lock). Scope
    busy to a per-rawName dispatch key. Capabilities/descriptors folded as a FLOW (re-emit on reconnect).
    Fill the Wave-0 `OutputsHolderTest` scaffolds with real assertions: absent-value-hides-row-value-but-row-tappable,
    servo-value-hidden (servo descriptor → displayValue null), static-pin-read-only, display-scaling-speed-to-pct
    (raw 0.45 → "45%"). Feed it real descriptors built from the 19-02 fixtures (or a small handcrafted set matching
    the fixture shape) and synthetic PrinterState.outputs values.
  </action>
  <acceptance_criteria>
    - `OutputsHolderTest` GREEN (all Wave-0 `fail()` bodies replaced)
    - speed 0.45 → "45%" (display scaling in the holder); a missing live value → row displayValue null, row still emitted
    - servo descriptor → displayValue null (value-not-angle degrade)
    - static_value descriptor → readOnly true, no settable control
    - markPending target is computed via the PrinterCommands clamp helper (same value the wire sends — 17-07); busy is per-rawName key, not global
    - rows alpha-sorted by prettyName
  </acceptance_criteria>
  <verify>
    <automated>cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --tests *OutputsHolderTest --no-daemon" 2>&1 | tr -d '\r' | tail -6</automated>
  </verify>
  <done>OutputsHolder combines descriptors + live values with holder-side scaling, servo/absent/readOnly degrade (SC-3), and per-detail clamped markPending busy (17-07); OutputsHolderTest green.</done>
</task>

<task type="auto">
  <name>Task 2: OutputsScreen flat list (drawer-suppressed, Back-only gutter) + preview matrix</name>
  <files>app/src/main/java/works/mees/dinghy/ui/outputs/OutputsScreen.kt, app/src/main/java/works/mees/dinghy/preview/OutputsPreviews.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/calibration/CalibrationHubScreen.kt (the EXACT scaffold precedent: ScreenScaffold field+gutter at lines 58-107, Back-only gutter via HubActionControl(intent=Intent.Neutral) at 95-104, RoutineTile row at 114-148, the glyph extension idiom at 155-162)
    - app/src/main/java/works/mees/dinghy/preview/SpoolPreviews.kt (the canonical PREVIEW_AND_TOKENS exemplar: @Preview matrix over 6 theme combos + fs=L via seed, PreviewBox wrappers, stateless screen overload fed SampleFixtures — lines 14-58)
    - app/src/main/java/works/mees/dinghy/designsystem/icons/DinghyIcons.kt (the new Output* tokens from 19-01 — D-01..D-06)
    - docs/ui_design/LAYOUT.md (Focus/Field/Gutter; the Outputs Field is the scrollable list, gutter = Back only, swipe-up drawer suppressed on this screen)
    - docs/ui_design/PREVIEW_AND_TOKENS.md (the day-one preview/tokenized convention; fs via seed not @Preview(fontScale))
    - $HOME/.claude memory [[dinghy-font-sizes-too-small]] (use fsSp scale: floor 15sp metadata, 17-18sp body, 24sp titles)
  </read_first>
  <action>
    Create `ui/outputs/OutputsScreen.kt`. Build a stateless content seam (`OutputsContent(rows, onRowTap, onBack)`)
    + a live overload that collects the holder's row StateFlow (mirror the Spool stateless/live split so previews
    drive the stateless seam with NO Moonraker/holder). Use `ScreenScaffold` with `field = { title Text +
    LazyColumn of rows, items(key = { it.rawName }) }` and `gutter = { Box(fillMaxWidth().padding(8.dp)) {
    HubActionControl(label = stringResource(R.string.common_back), onClick = onBack, intent = Intent.Neutral) } }`
    (plain nav = neutral — never red). Each row is a horizontal Row: the family glyph on the left via a
    `MaterialSymbol`/`DinghyIconView` using the OWNER-LOCKED `DinghyIcons.Output*` token chosen by family
    (heater_generic→OutputHeater D-01, fan_generic→OutputFan D-02, all LED families→OutputLed D-03, servo→OutputServo
    D-04, output_pin→OutputPin D-05, pwm_tool→OutputPwmTool D-06) — NEVER a raw ligature string, NEVER invented;
    prettyName in the middle; the display value on the right, rendered only when non-null (absent → omit the value
    Composable entirely, row stays `clickable`). Outline/fill/shape + fonts per the RoutineTile precedent and the
    `fsSp(baseSp, t.fs)` scale (≥15sp metadata, 17-18sp body, 24sp title). All strings via `stringResource` (add the
    needed `outputs_*` / `cd_*` keys), all chrome via `LocalTokens` (no raw colors). No looping/breathing animation
    (Adreno-320 budget — static only).
    Create `preview/OutputsPreviews.kt` mirroring SpoolPreviews: a `PreviewParameterProvider` of sample row lists
    (mixed families: a fan with a %, an LED with swatch, a servo with NO value, a read-only pin, an absent-value row),
    driving `OutputsContent` inside `PreviewBox` across the 6 theme combos (dark/light × Colorful/Simple/HighContrast)
    + an fs=L overflow variant (fs injected via the seed, NOT @Preview(fontScale=) which is a no-op). No live
    Moonraker, no holder — stateless content only.
  </action>
  <acceptance_criteria>
    - OutputsScreen compiles; the stateless `OutputsContent` seam takes a plain row list (preview-drivable)
    - Each family maps to its owner-locked token: `grep -c 'DinghyIcons.Output' OutputsScreen.kt` ≥ 6 (one per family); `grep -c 'IconRef.Ligature' OutputsScreen.kt` == 0 (no raw strings)
    - Absent display value omits the value Composable but the row stays `clickable` (SC-3) — verifiable by a preview row with null value still rendering a tappable row
    - Gutter has Back only, Intent.Neutral (no red); LazyColumn keyed by rawName
    - All user strings via `stringResource` (no hardcoded `Text("...")` literals for output labels); chrome via LocalTokens (no `Color(0x` except the LED swatch data-color carve-out, which lives in the detail not the list)
    - `OutputsPreviews.kt` renders the 6-combo + fs=L matrix via PreviewBox, no live Moonraker
  </acceptance_criteria>
  <verify>
    <automated>cmd.exe /c "E:\Android\gw.bat :app:compileDebugKotlin --no-daemon" 2>&1 | tr -d '\r' | tail -6</automated>
  </verify>
  <done>Flat alpha Outputs list with owner-locked per-family glyphs, absent-value-degrades-but-row-tappable (SC-3), Back-only neutral gutter, stringResource strings, token chrome, and a day-one @Preview matrix; compiles.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live PrinterState → row display | partial/absent live values must render a tappable row, never crash |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-05-01 | Denial of Service | absent/null live value crashes the list | mitigate | holder emits null displayValue; screen omits the value Composable but keeps the row clickable (SC-3) |
| T-19-05-02 | Tampering | wrong/substituted row glyph (icon law) | mitigate | per-family DinghyIcons.Output* tokens only (D-01..D-06); grep gate proves no raw ligature strings |
| T-19-05-03 | Tampering | busy-lock wedge from unclamped markPending (17-07) | mitigate | holder feeds markPending the clamped wire value from PrinterCommands |
</threat_model>

<verification>
- `:app:testDebugUnitTest --tests *OutputsHolderTest` green
- `:app:compileDebugKotlin` green (screen + previews compile)
- grep: ≥6 `DinghyIcons.Output` refs, 0 raw `IconRef.Ligature` in OutputsScreen
</verification>

<success_criteria>
Outputs list shows icon+name+value per row with owner-locked glyphs (SC-1); absent value hides only the value, row stays tappable (SC-3); display scaling + clamped per-detail busy in the holder (SC-2/17-07); day-one preview matrix (PREVIEW_AND_TOKENS); OutputsHolderTest green.
</success_criteria>

<output>
Create `.planning/phases/19-output-controls-fans-lights-generic-pins/19-05-SUMMARY.md` when done.
</output>
