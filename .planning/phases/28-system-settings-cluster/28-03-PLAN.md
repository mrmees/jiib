---
phase: 28-system-settings-cluster
plan: 03
type: execute
wave: 2
depends_on: ["28-01"]
files_modified:
  - app/src/main/java/works/mees/dinghy/ui/route/HomeAction.kt
  - app/src/test/java/works/mees/dinghy/ui/route/HomeActionTest.kt
autonomous: true
requirements: []
must_haves:
  truths:
    - "buildIdleActions emits Temperature, Console, and Fine-Tune rows on the WaterfallHome idle list (D-05)"
    - "The three new rows are unconditionally present (no capability gate) and use the D-21 / existing launcher glyph tokens"
    - "HomeActionTest count + order assertions are updated to the new full list (8 -> 11 destination rows)"
    - "The HomeAction.OpenDrawer variant still exists at this plan's close (its removal is owned atomically by 28-05)"
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/ui/route/HomeAction.kt"
      provides: "extended buildIdleActions with three rehomed orphan rows"
      contains: "NavDest.FineTune"
    - path: "app/src/test/java/works/mees/dinghy/ui/route/HomeActionTest.kt"
      provides: "updated count/order assertions for the 11-row full list"
      contains: "11"
  key_links:
    - from: "buildIdleActions new rows"
      to: "DinghyIcons.LauncherTemperature / LauncherConsole / LauncherFineTune"
      via: "HomeAction.Destination(icon = ...)"
      pattern: "LauncherFineTune"
    - from: "HomeActionTest full-list assertion"
      to: "buildIdleActions output size"
      via: "assertEquals(11, dests.size)"
      pattern: "11"
---

<objective>
Rehome the three orphaned drawer-only destinations — Temperature, Console, Fine-Tune — onto the
WaterfallHome idle action list by extending `buildIdleActions` (D-05). This REVISES the P24 D-07
"printing-only" posture: these three become always-present idle-list rows.

Purpose: Before the AppDrawer can be deleted (28-05), every former drawer-only destination must be
reachable elsewhere. The System page (28-02) absorbs Printers/Settings/Theme/SysInfo/About/Power;
this plan absorbs the remaining three orphans onto the home idle list. Together they complete the
DRAWER_TILES reachability checklist that 28-05 deletes against.
Output: `buildIdleActions` emits three new always-present rows in the proposed D-05 order; the pure
`HomeActionTest` updated to the new 11-row full list and order. `HomeAction.OpenDrawer` is left
intact — its removal is coupled to AppShell + PrintStatusField and lands atomically in 28-05.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-system-settings-cluster/28-CONTEXT.md
@.planning/phases/28-system-settings-cluster/28-PATTERNS.md
@.planning/phases/28-system-settings-cluster/28-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend buildIdleActions with Temperature / Console / Fine-Tune rows (D-05)</name>
  <files>app/src/main/java/works/mees/dinghy/ui/route/HomeAction.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/route/HomeAction.kt (the sealed interface ~line 28, Destination ~line 37, OpenDrawer ~line 47, buildIdleActions ~line 82 and its current row order + capability gates)
    - app/src/main/java/works/mees/dinghy/designsystem/icons/DinghyIcons.kt (LauncherTemperature `thermostat` ~line 49, LauncherConsole `terminal` ~line 62, LauncherFineTune `line_style` registered in 28-01)
    - app/src/main/res/values/strings.xml (cd_launcher_temperature, cd_launcher_console exist; cd_launcher_fine_tune added in 28-01)
    - .planning/phases/28-system-settings-cluster/28-PATTERNS.md (HomeAction.kt section — exact add() blocks, the D-05 insert position)
    - .planning/phases/28-system-settings-cluster/28-UI-SPEC.md (WaterfallHome idle list table — proposed order + glyph tokens)
  </read_first>
  <action>
    In `buildIdleActions`, add three `HomeAction.Destination` rows, all UNCONDITIONALLY present (no
    capability gate — Temperature, Console, and Fine-Tune are always available). Insert them per the
    UI-SPEC proposed order: after the Calibration row and before the Outputs/Webcam capability-gated
    tail, in the order Temperature -> Console -> Fine-Tune. Use the registered tokens and string keys:
    Temperature = `NavDest.Temperature` / `R.string.cd_launcher_temperature` / `DinghyIcons.LauncherTemperature`;
    Console = `NavDest.Console` / `R.string.cd_launcher_console` / `DinghyIcons.LauncherConsole`;
    Fine-Tune = `NavDest.FineTune` / `R.string.cd_launcher_fine_tune` / `DinghyIcons.LauncherFineTune`.
    Do NOT remove or alter `HomeAction.OpenDrawer` in this plan — its deletion is owned atomically by
    28-05 (it is still referenced by AppShell + PrintStatusField until then; removing it here would
    break the build). Do NOT change the existing capability gates on Spool/Macros/Outputs/Webcam.
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:compileDebugKotlin --no-daemon" | tr -d '\r'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'NavDest.Temperature\|NavDest.Console\|NavDest.FineTune' app/src/main/java/works/mees/dinghy/ui/route/HomeAction.kt` returns at least 3 (the three new rows)
    - `grep -c 'LauncherFineTune' app/src/main/java/works/mees/dinghy/ui/route/HomeAction.kt` returns 1
    - `grep -c 'OpenDrawer' app/src/main/java/works/mees/dinghy/ui/route/HomeAction.kt` is still >= 1 (variant intact for 28-05)
    - `:app:compileDebugKotlin` BUILD SUCCESSFUL
  </acceptance_criteria>
  <done>buildIdleActions emits three new always-present idle rows; OpenDrawer untouched; debug compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Update HomeActionTest count + order for the 11-row full list</name>
  <files>app/src/test/java/works/mees/dinghy/ui/route/HomeActionTest.kt</files>
  <read_first>
    - app/src/test/java/works/mees/dinghy/ui/route/HomeActionTest.kt (the full-list assertion "Full list must have 8 destination rows" ~line 142, the minimal-list assertion ~line 163, the order test ~line 195, any OpenDrawer reference)
    - app/src/main/java/works/mees/dinghy/ui/route/HomeAction.kt (the just-edited buildIdleActions — the authoritative new order)
    - .planning/phases/28-system-settings-cluster/28-PATTERNS.md (HomeActionTest update note: count becomes 11, order test gains the 3 new dests)
  </read_first>
  <action>
    Update `HomeActionTest` to match the extended list. The all-capabilities full-list size assertion
    "Full list must have 8 destination rows" becomes 11 (the 3 new always-present rows add to every
    list, not just the all-capable case). Update the minimal-list size assertion accordingly: the
    minimal list (no Spool/Macros/Outputs/Webcam) gains the 3 unconditional rows too, so it rises from
    4 to 7 — verify against the actual buildIdleActions output rather than assuming. Update the order
    test to include Temperature -> Console -> Fine-Tune at their inserted positions (after Calibration,
    before the gated tail). If the test references `HomeAction.OpenDrawer`, leave that case intact (the
    variant still exists this wave; its test is deleted in 28-05). Do NOT weaken assertions to "size >=
    N" — keep exact-count assertions so a future regression is caught.
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --tests *HomeActionTest --no-daemon" | tr -d '\r'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '11' app/src/test/java/works/mees/dinghy/ui/route/HomeActionTest.kt` returns at least 1 (the new full-list count)
    - `grep -c 'NavDest.Temperature\|NavDest.Console\|NavDest.FineTune' app/src/test/java/works/mees/dinghy/ui/route/HomeActionTest.kt` returns at least 3 (order coverage)
    - `:app:testDebugUnitTest --tests *HomeActionTest` GREEN with exact-count assertions
    - The whole test sourceset still compiles (`:app:compileDebugUnitTestKotlin` implied by the test run)
  </acceptance_criteria>
  <done>HomeActionTest asserts the 11-row full list + new order; test green; OpenDrawer case intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Pure route-model extension; no network surface, no untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-03-01 | Tampering (reachability regression) | idle-list coverage | mitigate | HomeActionTest exact-count + order assertions prove the three orphans are emitted before the drawer is deleted (28-05 reachability precondition) |
| T-28-03-SC | Tampering | npm/pip/cargo installs | n/a | No package installs — pure Kotlin |
</threat_model>

<verification>
- `:app:compileDebugKotlin` BUILD SUCCESSFUL (buildIdleActions extension)
- `:app:testDebugUnitTest --tests *HomeActionTest` green (11-row count + order)
- `HomeAction.OpenDrawer` still present (28-05 owns its removal)
</verification>

<success_criteria>
The WaterfallHome idle list gains always-present Temperature, Console, and Fine-Tune rows via
buildIdleActions in the proposed D-05 order; HomeActionTest asserts the new 11-row full list and
order with exact counts; OpenDrawer remains intact for atomic removal in 28-05.
</success_criteria>

<output>
Create `.planning/phases/28-system-settings-cluster/28-03-SUMMARY.md` when done.
</output>
