---
phase: 28-system-settings-cluster
plan: 07
type: execute
wave: 4
depends_on: ["28-01"]
files_modified:
  - app/src/main/java/works/mees/dinghy/ui/screen/SettingsScreen.kt
  - app/src/main/java/works/mees/dinghy/ui/systeminfo/SystemInformationScreen.kt
  - app/src/main/java/works/mees/dinghy/ui/screen/AboutScreen.kt
  - app/src/main/java/works/mees/dinghy/preview/SettingsPreviews.kt
  - app/src/main/java/works/mees/dinghy/preview/SysInfoPreviews.kt
  - app/src/main/java/works/mees/dinghy/preview/AboutPreviews.kt
autonomous: true
must_haves:
  truths:
    - "SettingsScreen densifies to fit one page at M text size (D-11): dense toggle/numeric-field rows, single verticalScroll Column, Back foot"
    - "SystemInformationScreen restyles to dense icon-label-value rows and scrolls freely (D-11)"
    - "AboutScreen densifies to fit one page at M (D-11), keeps the dev-enable toggle and brandTint wordmark"
    - "Every text node uses fsSp with base >= 15f; the old EX(set) 13sp Material-label exemption is gone (D-10)"
    - "All persistence routes through container intent helpers (writeScope) — never rememberCoroutineScope()"
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/ui/screen/SettingsScreen.kt"
      provides: "dense one-page Settings"
      contains: "verticalScroll"
    - path: "app/src/main/java/works/mees/dinghy/ui/screen/AboutScreen.kt"
      provides: "dense one-page About with dev-enable toggle"
      contains: "verticalScroll"
  key_links:
    - from: "Settings toggle/field rows"
      to: "container.set* writeScope intent helpers"
      via: "onToggle / onValue -> container.*"
      pattern: "container.set"
    - from: "rebuilt screens text"
      to: "fsSp scale"
      via: "fsSp(baseSp, t.fs).sp with base >= 15f"
      pattern: "fsSp"
---

<objective>
Dense-restyle the three remaining config screens onto the jiib kit: `SettingsScreen` (D-11/D-12 —
fits one page at M, dense toggle + numeric-field rows, keyboard fields inline), `SystemInformationScreen`
(dense icon-label-value rows, scrolls freely), and `AboutScreen` (D-11 — fits one page at M, keeps the
dev-enable toggle + brandTint wordmark). The 15sp text floor holds (D-10); the old 13sp Material-label
exemption dies with the restyle.

Purpose: These are the lighter C6 dense restyles (no new state machines, no new pickers — just the
dense kit applied to existing content). They share the dense `ListRow`/`TokenTextField` from 28-01 and
own no files that 28-06 (Printers) or 28-08 (Theme editor) touch, so all three rebuild plans run in
parallel.
Output: three restyled screens at the 15sp floor with writeScope-only persistence, each shipping its
`@Preview` matrix.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-system-settings-cluster/28-CONTEXT.md
@.planning/phases/28-system-settings-cluster/28-PATTERNS.md
@.planning/phases/28-system-settings-cluster/28-UI-SPEC.md
@docs/ui_design/PREVIEW_AND_TOKENS.md
@.claude/skills/sketch-findings-dinghy-display/SKILL.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Dense-restyle SettingsScreen to one page (D-11/D-12)</name>
  <files>app/src/main/java/works/mees/dinghy/ui/screen/SettingsScreen.kt</files>
  <behavior>
    - Single verticalScroll Column of dense rows; fits one page at M text size, portrait + landscape
    - Toggle row: [icon] label + Spacer + Switch (t.accent checked tint)
    - Numeric-field row: [icon] label + Spacer + TokenTextField(dense = true, numeric keyboard)
    - No section-header words; grouping by outline + fill shade only
    - Back foot button (Neutral), right-aligned at the bottom of the column
    - Every existing feature toggle survives restyled (D-14 boundary final — nothing moves to another screen)
    - All text >= 15sp base via fsSp; the 13sp EX(set) exemption is removed
  </behavior>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/screen/SettingsScreen.kt (current toggle inventory + write-scope calls ~line 150/173/190/215 — setActiveWebcamEnabled / setBabystepEnabled / setBabystepLayers / setKeepScreenOn etc.; the babystep-layers numeric validation `digits.toIntOrNull()?.let`)
    - app/src/main/java/works/mees/dinghy/designsystem/components/ListRow.kt + app/src/main/java/works/mees/dinghy/ui/screen/TokenTextField.kt (the dense = true flags from 28-01)
    - app/src/main/java/works/mees/dinghy/designsystem/layout/ScreenScaffold.kt (gutter=null usage) — or follow CalibrationHubScreen's scaffold idiom
    - .planning/phases/28-system-settings-cluster/28-PATTERNS.md (SettingsScreen.kt section — dense layout shape, write-scope preserve, 15sp floor)
    - .planning/phases/28-system-settings-cluster/28-UI-SPEC.md (Settings Screen layout: row types, no section headers, fit-one-page, Back foot)
  </read_first>
  <action>
    Replace the SettingsScreen layout with a single `Column(Modifier.fillMaxSize().verticalScroll(
    rememberScrollState()).padding(16.dp), verticalArrangement = Arrangement.spacedBy(8.dp))` of dense
    rows. Each existing toggle becomes a dense toggle row `[icon] label + Spacer(weight) + Switch`
    (Switch token-tinted with `t.accent` for checked). Each numeric setting (e.g. babystep layers)
    becomes a dense numeric-field row using `TokenTextField(dense = true)` with the numeric keyboard;
    preserve the existing validation exactly (`digits.toIntOrNull()?.let { container.setBabystepLayers(it) }`
    — invalid input silently not persisted). Keep EVERY existing feature toggle — do NOT move any setting
    to another screen (D-14 is final). All persistence stays on the existing `container.set*` writeScope
    intent helpers — NEVER `rememberCoroutineScope()`. No section-header words — separate groups by
    outline/fill only. Back = a single `OutlinedControl(Intent.Neutral)` at the bottom of the Column. All
    text via `fsSp(baseSp, t.fs).sp` with base >= 15f — delete any 13sp label remnants. The page must fit
    one screenful at M (verify visually at UAT in 28-09); keep rows tight (10dp dense padding).
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:compileDebugKotlin --no-daemon" | tr -d '\r'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'verticalScroll' app/src/main/java/works/mees/dinghy/ui/screen/SettingsScreen.kt` returns at least 1
    - `grep -c 'rememberCoroutineScope' app/src/main/java/works/mees/dinghy/ui/screen/SettingsScreen.kt` returns 0
    - `grep -c 'container.set' app/src/main/java/works/mees/dinghy/ui/screen/SettingsScreen.kt` returns at least 3 (the existing toggle writes preserved)
    - `grep -Ec '= 13f|13\.sp|fsSp\(1[0-4]f' app/src/main/java/works/mees/dinghy/ui/screen/SettingsScreen.kt` returns 0 (no sub-15sp base)
    - `:app:compileDebugKotlin` BUILD SUCCESSFUL
  </acceptance_criteria>
  <done>SettingsScreen is a dense one-page scroll; all toggles preserved; writeScope-only; 15sp floor held; compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Dense-restyle SystemInformationScreen + AboutScreen</name>
  <files>app/src/main/java/works/mees/dinghy/ui/systeminfo/SystemInformationScreen.kt, app/src/main/java/works/mees/dinghy/ui/screen/AboutScreen.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/systeminfo/SystemInformationScreen.kt (current SYS-01..SYS-05 content + the health chip; the existing row rendering)
    - app/src/main/java/works/mees/dinghy/ui/screen/AboutScreen.kt (current content: wordmark + version + tagline + dev-enable toggle + link rows; brandTint usage)
    - app/src/main/java/works/mees/dinghy/ui/calibration/CalibrationHubScreen.kt (the ListBlock + dense ListRow icon-label-value idiom; ScreenScaffold gutter=null)
    - app/src/main/java/works/mees/dinghy/designsystem/components/ListRow.kt (dense = true from 28-01)
    - .planning/phases/28-system-settings-cluster/28-PATTERNS.md (SystemInformationScreen.kt + AboutScreen.kt sections — dense row shape, health chip shape-coded status, About one-page column)
    - .planning/phases/28-system-settings-cluster/28-UI-SPEC.md (SystemInformationScreen + AboutScreen layout contracts; shape-coded health chip; About fits-one-page + dev-enable)
  </read_first>
  <action>
    SystemInformationScreen: restyle to dense `ListRow(dense = true)` rows with `[icon] label +
    Spacer(weight) + value(Geist Mono)` anatomy inside a `ListBlock`; `ScreenScaffold(gutter = null)`;
    scrolls freely (D-11 — no fit-one-page requirement). Keep the SYS-01..SYS-05 content unchanged —
    restyle only. Keep the health chip shape-coded per the Phase-15.1 status shapes (octagon = stop,
    triangle = caution, go = color-only). Back foot = `OutlinedControl(Intent.Neutral)`.

    AboutScreen: restyle to a single `Column(verticalScroll)` that fits one page at M (D-11). Keep the
    jiib wordmark (brandTint accent-tinted, >= WCAG-3:1), version string (Geist Mono 15sp), tagline (Geist
    17sp t.text2), the EXISTING dev-enable toggle implementation (densified, persistence unchanged), and
    the existing link rows. Back foot = `OutlinedControl(Intent.Neutral)`. Both screens: all text via
    `fsSp(baseSp, t.fs).sp` base >= 15f; no `rememberCoroutineScope()` for any persistence (dev-enable
    toggle keeps its existing writeScope route); do NOT render FloatingEStop; do NOT change the dev-enable
    semantics.
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:compileDebugKotlin --no-daemon" | tr -d '\r'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'dense = true' app/src/main/java/works/mees/dinghy/ui/systeminfo/SystemInformationScreen.kt` returns at least 1
    - `grep -c 'verticalScroll' app/src/main/java/works/mees/dinghy/ui/screen/AboutScreen.kt` returns at least 1
    - `grep -c 'brandTint' app/src/main/java/works/mees/dinghy/ui/screen/AboutScreen.kt` returns at least 1 (wordmark tinting preserved)
    - `grep -c 'rememberCoroutineScope' app/src/main/java/works/mees/dinghy/ui/systeminfo/SystemInformationScreen.kt app/src/main/java/works/mees/dinghy/ui/screen/AboutScreen.kt` returns 0
    - `:app:compileDebugKotlin` BUILD SUCCESSFUL
  </acceptance_criteria>
  <done>SysInfo dense icon-label-value rows (scrolls freely); About dense one-page with dev-enable + brandTint wordmark; compiles.</done>
</task>

<task type="auto">
  <name>Task 3: Preview matrices for Settings / SysInfo / About</name>
  <files>app/src/main/java/works/mees/dinghy/preview/SettingsPreviews.kt, app/src/main/java/works/mees/dinghy/preview/SysInfoPreviews.kt, app/src/main/java/works/mees/dinghy/preview/AboutPreviews.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/preview/CalibrationPreviews.kt (PreviewBox + 6-combo + fsLarge + RTL idiom)
    - docs/ui_design/PREVIEW_AND_TOKENS.md (minimized matrix shape)
    - .planning/phases/28-system-settings-cluster/28-UI-SPEC.md (Preview Contract — Settings axis = toggles all-on/all-off; SysInfo axis = populated/degraded; About axis = dev-enable on/off)
  </read_first>
  <action>
    Add three preview files driving stateless content seams (extract a stateless `*Content` seam from each
    rebuilt screen if not already present): `SettingsPreviews.kt` (interesting axis = feature toggles all
    on / all off), `SysInfoPreviews.kt` (populated / degraded "—" fields), `AboutPreviews.kt` (dev-enable
    on / off). Each ships the 6 theme combos via `PreviewBox`, a `*FsLargeOverflow` panel, an
    `*RtlSpotCheck` panel, and a pseudolocale panel. All `stringResource` labels; no Moonraker/coroutines.
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:assembleDebug --no-daemon" | tr -d '\r'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'PreviewBox' app/src/main/java/works/mees/dinghy/preview/SettingsPreviews.kt` returns at least 6
    - `grep -c 'PreviewBox' app/src/main/java/works/mees/dinghy/preview/SysInfoPreviews.kt` returns at least 6
    - `grep -c 'PreviewBox' app/src/main/java/works/mees/dinghy/preview/AboutPreviews.kt` returns at least 6
    - each file has at least one `fsLargeSeed`/`FsLarge` panel and one RTL panel
    - `:app:assembleDebug` BUILD SUCCESSFUL
  </acceptance_criteria>
  <done>Settings/SysInfo/About each ship a 6-combo + fsL + RTL preview matrix; assembleDebug green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Settings numeric-field input -> stored prefs | Babystep-layers etc. cross keyboard input into persisted settings |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-07-01 | Input Validation | Settings numeric fields | mitigate | Preserve the existing `toIntOrNull()?.let { ... }` guard — invalid input is silently not persisted (no crash, no bad write) |
| T-28-07-02 | Tampering (dropped write on slow flash) | dev-enable + setting toggles | mitigate | All persistence stays on container writeScope intent helpers, never a composition scope ([[dinghy-compose-write-scope-cancellation]]) |
| T-28-07-SC | Tampering | npm/pip/cargo installs | n/a | No package installs — pure Kotlin/Compose |
</threat_model>

<verification>
- `:app:compileDebugKotlin` BUILD SUCCESSFUL (three screen restyles)
- `:app:assembleDebug` BUILD SUCCESSFUL (previews)
- Grep gates: verticalScroll on Settings/About; no rememberCoroutineScope; brandTint on About; no sub-15sp base
</verification>

<success_criteria>
Settings densifies to one page at M (D-11/D-12) with all toggles preserved and writeScope-only;
SystemInformation restyles to dense scroll-free rows; About densifies to one page keeping the
dev-enable toggle + brandTint wordmark; the 15sp floor holds everywhere (D-10); all three ship preview
matrices and assembleDebug is green.
</success_criteria>

<output>
Create `.planning/phases/28-system-settings-cluster/28-07-SUMMARY.md` when done.
</output>
