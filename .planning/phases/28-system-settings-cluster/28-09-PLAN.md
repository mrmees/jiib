---
phase: 28-system-settings-cluster
plan: 09
type: execute
wave: 5
depends_on: ["28-05", "28-06", "28-07", "28-08"]
files_modified:
  - .planning/phases/28-system-settings-cluster/28-UAT.md
autonomous: false
must_haves:
  truths:
    - "A release build is produced from a forced rebuild and its APK mtime is verified to post-date the last source commit (no stale-APK gate)"
    - "The full host unit suite + assembleRelease are green before the on-device gate"
    - "The owner verifies on flox, both orientations, at S/M/L text size: the restyled cluster, C6 density, fsSp/rotation, and the full no-regression smoke (connection edit, theme apply, printer add/remove/switch, sysinfo read)"
    - "The owner confirms the swipe-up gesture is fully dead and every former DRAWER_TILES destination is reachable via the home idle list / System page / printing shortcut grid"
    - "UAT results are persisted to 28-UAT.md with per-item pass/fail/deferred status"
  artifacts:
    - path: ".planning/phases/28-system-settings-cluster/28-UAT.md"
      provides: "the persisted on-device UAT record"
      contains: "SC-1"
  key_links:
    - from: "forced release rebuild"
      to: "flox install"
      via: "verified-fresh APK mtime then adb install"
      pattern: "assembleRelease"
---

<objective>
Run the load-bearing on-device UAT phase gate on flox (Nexus 7 2013 / Adreno 320 / LineageOS 18.1 /
API 30): build a verified-fresh release APK, install it, and have the owner walk the Manual-Only
Verifications — the restyled cluster + System page in both orientations and all three text sizes, the
full no-regression smoke against a live Moonraker printer, and the swipe-up-is-dead + full-reachability
checklist. Persist results to 28-UAT.md.

Purpose: SC-1 and SC-5 are owner-judgment + live-Moonraker criteria that no host test can close. This
is the final gate. The forced-rebuild + APK-mtime check exists because Gradle UP-TO-DATE has installed
stale APKs before an on-device UAT in this project ([[dinghy-stale-apk-uat-gate]]).
Output: a green pre-gate (full host suite + assembleRelease), a verified-fresh installed build, and a
persisted 28-UAT.md with per-item status.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-system-settings-cluster/28-CONTEXT.md
@.planning/phases/28-system-settings-cluster/28-VALIDATION.md
@.planning/phases/28-system-settings-cluster/28-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verified-fresh release build + green pre-gate + UAT scaffold</name>
  <files>.planning/phases/28-system-settings-cluster/28-UAT.md</files>
  <read_first>
    - .planning/phases/28-system-settings-cluster/28-VALIDATION.md (the Manual-Only Verifications table — the exact items the owner walks)
    - CLAUDE.md (the WSL build invocation: `/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat <args>"`; pipe through `tr -d '\r'`; force-rebuild + verify APK mtime; `E:\Android\sign-release.bat` for debug-signing the release APK; flox install)
    - .planning/phases/28-system-settings-cluster/28-UI-SPEC.md (the Screen Inventory Summary — the surfaces to walk)
  </read_first>
  <action>
    Run the full host unit suite and `assembleRelease` as the pre-gate (both must be green). FORCE a fresh
    build to defeat Gradle UP-TO-DATE staleness: use `--rerun-tasks` (or clean the release outputs) on the
    release assemble, then verify the produced APK's mtime POST-DATES the last source commit of this phase
    before installing ([[dinghy-stale-apk-uat-gate]] — suspect the build before re-diagnosing code).
    Debug-sign the release APK via `E:\Android\sign-release.bat` and `adb install` it on flox (printer
    targets: Ender 5 Plus 192.168.1.120:7125 / Ender 3 Pro 192.168.1.121:7125). Scaffold
    `28-UAT.md` with one row per Manual-Only Verification item from 28-VALIDATION.md (SC-1 restyle +
    System page both orientations; SC-2/SC-3 C6 density + Settings/About fit-one-page at S/M/L; SC-4
    fsSp/rotation; SC-5 live smoke = connection edit + theme apply + printer add/remove/switch + sysinfo
    read; D-04/D-05 swipe-up-dead + DRAWER_TILES reachability walk), each marked `pending` with explicit
    test instructions.
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest :app:assembleRelease --rerun-tasks --no-daemon" | tr -d '\r'</automated>
  </verify>
  <acceptance_criteria>
    - `:app:testDebugUnitTest` FULL suite GREEN and `:app:assembleRelease` BUILD SUCCESSFUL (exit 0)
    - The installed release APK's mtime is after the last phase-28 source commit (staleness check recorded in 28-UAT.md)
    - `28-UAT.md` exists with one `pending` row per Manual-Only Verification item, each with concrete test instructions
  </acceptance_criteria>
  <done>Verified-fresh release build installed on flox; pre-gate green; 28-UAT.md scaffolded with pending items.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Owner on-device UAT walk on flox (SC-1 / SC-5 — the phase gate)</name>
  <files>.planning/phases/28-system-settings-cluster/28-UAT.md</files>
  <action>This is the on-device UAT phase gate. The owner walks the Manual-Only Verification items on flox against a live printer and the verified-fresh release build from Task 1; record PASS/FAIL/defer per item into 28-UAT.md (Task 3 persists). A FAIL spawns gap closure, NOT a phase-complete mark; on-device UAT may be owner-deferred per project posture (the orchestrator handles deferral).</action>
  <what-built>
    The full Phase-28 migration is installed on flox as a verified-fresh release build: the restyled
    System-cluster (Settings, Theme/Theme-Editor with the new S/V square, Printers with the Edit/Delete
    mode-toggle foot bar, System Information, About) + the new System page; the swipe-up gesture +
    AppDrawer are deleted; Temperature/Console/Fine-Tune are rehomed onto the WaterfallHome idle list; the
    printing shortcut grid has a System entry.
  </what-built>
  <how-to-verify>
    1. Restyled cluster + System page (SC-1): open each of Settings, Theme/Theme-Editor, Printers, System
       Information, About, and the System page. In BOTH portrait and landscape, confirm each reads as the
       jiib design (dense rows, token colors, correct intent colors), the System page Focus shows the jiib
       lockup + version + active printer capped at the 20%-height / 40%-width ratio, and rows direct-tap to
       their destination. Approve or list issues per screen.
    2. C6 density + fit-one-page (SC-2/SC-3): at S, M, and L text size, confirm Settings and About each fit
       one page at M, density reads well in-hand, and nothing clips at L.
    3. fsSp + rotation (SC-4): rotate each screen; confirm layout reflows correctly and no text drops below
       the 15sp floor.
    4. No-regression smoke (SC-5) against a live printer (E5+ or E3): edit a connection (host/port/key) and
       reconnect; apply a theme change (incl. a custom pool color picked via the new S/V square, then
       re-open the slot and confirm the S/V is restored); add a printer, switch the active printer, then
       delete a printer (Edit/Delete mode toggles + ConfirmGuard); read System Information. All must work.
    5. Swipe-up dead + reachability (D-04/D-05): attempt a swipe-up on several screens — the drawer must
       NOT appear anywhere. Then confirm every former drawer destination is reachable: Temperature /
       Console / Fine-Tune on the home idle list; Printers / Settings / Theme / System Info / About / Power
       on the System page; the System entry on the printing shortcut grid while a print runs.
    6. Mid-print e-stop (D-06): while a print runs, navigate into the System cluster and confirm the
       floating e-stop is still reachable on those screens.
    Printer Moonraker addresses: Ender 5 Plus 192.168.1.120:7125 / Ender 3 Pro 192.168.1.121:7125.
  </how-to-verify>
  <resume-signal>Type "approved" if all items pass (or pass-with-deferrals you name), or describe the issues to fix.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Persist UAT results to 28-UAT.md</name>
  <files>.planning/phases/28-system-settings-cluster/28-UAT.md</files>
  <read_first>
    - .planning/phases/28-system-settings-cluster/28-UAT.md (the scaffold from Task 1)
    - The owner's Task-2 verdict (approved / approved-with-deferrals / issues)
  </read_first>
  <action>
    Record the owner's verdict into `28-UAT.md`: mark each item `resolved` / `deferred` / `failed` with the
    owner's notes verbatim where given. If the owner approved with deferrals, capture each deferral as a
    named follow-up (and note whether it belongs to Phase 29). If any item failed, do NOT mark the phase
    verified — surface the failures so the orchestrator can spawn gap closure. Set the 28-UAT.md status
    line to reflect the overall outcome (`passed` / `partial` / `failed`).
  </action>
  <verify>
    <automated>grep -c 'resolved\|deferred\|failed' .planning/phases/28-system-settings-cluster/28-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - `28-UAT.md` has a status for every Manual-Only Verification item (no `pending` rows remain unless the owner deferred them, explicitly marked `deferred`)
    - The overall status line is set (`passed` / `partial` / `failed`)
    - Any deferrals are captured as named follow-ups; failures are surfaced (not silently closed)
  </acceptance_criteria>
  <done>28-UAT.md records the owner's per-item verdict + overall status; deferrals/failures captured explicitly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Release APK -> flox install | A stale APK could be installed, hiding the real build state from the owner |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-09-01 | Tampering (stale build masks state) | Gradle UP-TO-DATE | mitigate | Force `--rerun-tasks` + verify APK mtime post-dates the last source commit before install ([[dinghy-stale-apk-uat-gate]]) |
| T-28-09-02 | Denial of Service (e-stop hidden mid-print) | System cluster while printing | mitigate | UAT step 6 explicitly checks the floating e-stop is reachable on the System cluster during a live print (D-06 / Pitfall 6) |
| T-28-09-SC | Tampering | npm/pip/cargo installs | n/a | No package installs — pure Kotlin/Compose |
</threat_model>

<verification>
- `:app:testDebugUnitTest` FULL suite green + `:app:assembleRelease` green (pre-gate, forced rebuild)
- Installed APK mtime verified fresh
- Owner on-device verdict recorded in 28-UAT.md with per-item status
</verification>

<success_criteria>
A verified-fresh release build runs on flox; the owner approves (or names deferrals for) the restyled
cluster + System page in both orientations at S/M/L, the live no-regression smoke (connection edit,
theme apply incl. S/V restore, printer add/remove/switch, sysinfo read), the swipe-up-is-dead +
full-reachability checklist, and mid-print e-stop reachability; results are persisted to 28-UAT.md.
</success_criteria>

<output>
Create `.planning/phases/28-system-settings-cluster/28-09-SUMMARY.md` when done.
</output>
