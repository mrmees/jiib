---
phase: 28-system-settings-cluster
plan: 06
type: execute
wave: 4
depends_on: ["28-01"]
files_modified:
  - app/src/main/java/works/mees/dinghy/ui/screen/PrintersScreen.kt
  - app/src/main/java/works/mees/dinghy/preview/PrintersPreviews.kt
  - app/src/main/java/works/mees/dinghy/preview/SampleFixtures.kt
  - app/src/test/java/works/mees/dinghy/ui/screen/PrintersModeToggleTest.kt
autonomous: true
must_haves:
  truths:
    - "PrintersScreen rebuilds on the jiib kit: Focus = active-printer DetailCard, Field = dense profile rows, Foot = Add / Edit / Delete / Back (D-15)"
    - "Edit and Delete are FootButtonBar mode toggles (D-13): arming Edit lets a row tap open the connection editor; arming Delete lets a row tap raise the ConfirmGuard; tapping the toggle again or Back disarms; normal-mode row tap switches the active printer"
    - "Delete-armed shows the C4 filled-stop intent; Edit-armed shows filled-neutral; ConfirmGuard gates the destructive delete"
    - "All persistence routes through container intent helpers (writeScope) — never rememberCoroutineScope()"
    - "A pure host test covers the mode-toggle state machine (arm Edit, arm Delete, disarm, ConfirmGuard trigger)"
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/ui/screen/PrintersScreen.kt"
      provides: "rebuilt Printers screen with mode-toggle foot bar"
      contains: "PrinterMode"
    - path: "app/src/test/java/works/mees/dinghy/ui/screen/PrintersModeToggleTest.kt"
      provides: "pure mode-toggle state-machine test"
      contains: "PrinterMode"
  key_links:
    - from: "Delete mode row tap"
      to: "ConfirmGuard -> container.deleteProfile"
      via: "destructive confirm then writeScope delete"
      pattern: "ConfirmGuard"
    - from: "Edit mode row tap"
      to: "inline densified connection editor"
      via: "TokenTextField(dense = true) host/port/key"
      pattern: "TokenTextField"
---

<objective>
Rebuild `PrintersScreen` on the jiib dense kit with the R4 Edit/Delete mode-toggle foot bar (D-13)
and the D-15 Focus/Field/Foot layout: Focus is the active printer's `DetailCard` (name, host:port,
color-coded connection state, Klippy state); Field is the dense profile-row list; Foot is
Add(accent) / Edit(neutral) / Delete(stop) / Back(neutral). Closes the
`2026-06-05-printers-edit-delete-mode-buttons.md` todo.

Purpose: PrintersScreen is the connection-management surface (D-14 boundary: connection/add/remove/
switch live here). It rebuilds on the dense kit (28-01) like the other C6 screens, but it is NOT
C6-exempt itself for the foot bar (FootButtonBar stays 1U — it is the primary action row). Pulling
the mode-toggle state machine into a pure host test makes the arm/disarm/confirm logic verifiable
off-device.
Output: rebuilt PrintersScreen with the mode-toggle foot bar + inline densified editor; a pure
`PrintersModeToggleTest`; the full `@Preview` matrix (Normal / Edit armed / Delete armed / Empty).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-system-settings-cluster/28-CONTEXT.md
@.planning/phases/28-system-settings-cluster/28-PATTERNS.md
@.planning/phases/28-system-settings-cluster/28-UI-SPEC.md
@docs/ui_design/PREVIEW_AND_TOKENS.md
@.claude/skills/sketch-findings-dinghy-display/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract the Printers mode-toggle state machine + pure host test (write the test first)</name>
  <files>app/src/test/java/works/mees/dinghy/ui/screen/PrintersModeToggleTest.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/screen/PrintersScreen.kt (current `editingTarget` sealed pattern ~line 110-122, EditorTarget ~line 167, ConfirmGuard usage ~line 215-230)
    - .planning/phases/28-system-settings-cluster/28-PATTERNS.md (PrintersScreen.kt section — `enum class PrinterMode { Normal, EditArmed, DeleteArmed }`, the BackHandler disarm, the row-tap dispatch by mode)
    - .planning/phases/28-system-settings-cluster/28-UI-SPEC.md (Printers mode-toggle state machine diagram — Normal/EditArmed/DeleteArmed transitions)
    - app/src/test/java/works/mees/dinghy/ui/route/HomeActionTest.kt (the pure-host-test idiom for this codebase — JUnit4, no Robolectric)
  </read_first>
  <action>
    Define the mode-toggle as a pure, testable unit so the arm/disarm/confirm logic is not trapped in a
    composable. Create a small pure model: a `PrinterMode` enum (`Normal`, `EditArmed`, `DeleteArmed`)
    plus pure transition functions — e.g. `armEdit(current)`, `armDelete(current)`, `disarm()`, and a
    `rowTapEffect(mode, profile)` that maps a row tap under a given mode to one of three outcomes
    (SwitchActive / OpenEditor / RequestDelete). Place these alongside PrintersScreen (same package /
    file or a small `PrintersMode.kt` — Claude's discretion, but they must be importable by the host
    test). Write `PrintersModeToggleTest` FIRST asserting: arming Edit from Normal -> EditArmed; arming
    Delete from Normal -> DeleteArmed; tapping the armed toggle again or Back -> Normal; a row tap in
    Normal -> SwitchActive; in EditArmed -> OpenEditor; in DeleteArmed -> RequestDelete; arming the other
    mode from an armed mode switches modes (Edit->Delete) rather than stacking. Run it RED first to prove
    it tests something, then implement the pure functions to GREEN. The test must compile against the
    whole sourceset day-one ([[dinghy-wave0-red-scaffold-compile]]) — no references to unbuilt composable
    symbols.
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --tests *PrintersModeToggleTest --no-daemon" | tr -d '\r'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'enum class PrinterMode' app/src/main/java/works/mees/dinghy/ui/screen/*.kt` returns 1 (the pure enum exists in main, importable by tests)
    - `PrintersModeToggleTest` covers all transitions (arm Edit, arm Delete, disarm via toggle, disarm via Back, the three row-tap outcomes, mode-switch from armed) — at least 6 assertions
    - `:app:testDebugUnitTest --tests *PrintersModeToggleTest` GREEN
    - No reference to a composable/Compose-runtime symbol in the test (pure-unit)
  </acceptance_criteria>
  <done>Pure PrinterMode state machine + green PrintersModeToggleTest covering arm/disarm/confirm transitions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rebuild PrintersScreen on the jiib kit with the mode-toggle foot bar (D-13/D-15)</name>
  <files>app/src/main/java/works/mees/dinghy/ui/screen/PrintersScreen.kt</files>
  <behavior>
    - Focus = active printer DetailCard: name (Geist 20sp semibold), host:port (Geist Mono 15sp), color-coded connection-state chip, Klippy state label
    - Field = dense ListBlock of profile rows; active row = accentSoft fill + accentLine border; inactive = transparent + t.outline
    - Foot = FootButtonBar (1U, NOT dense): Add(Accent) / Edit(Neutral; armed = filled neutral) / Delete(Stop; armed = filled stop, C4) / Back(Neutral; disarms)
    - Normal-mode row tap switches the active printer immediately (container.setActiveProfile)
    - Edit-mode row tap opens the inline densified connection editor (TokenTextField dense: Host text / Port numeric-keyboard / API key masked)
    - Delete-mode row tap raises ConfirmGuard (destructive); confirm -> container.deleteProfile, back to Normal
    - Empty state: centered "No printers configured" + "Tap Add to set up your first printer"; Foot = Add + Back
    - The screen does NOT render its own FloatingEStop (shell owns it); gutter = null
  </behavior>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/screen/PrintersScreen.kt (current screen, the EditorTarget editor seam, the existing ConfirmGuard + AddPrinterTile + connection editor form, the write-scope calls)
    - app/src/main/java/works/mees/dinghy/ui/calibration/CalibrationHubScreen.kt (ScreenScaffold Focus|Field|FootButtonBar gutter=null + BoxWithConstraints + rememberUnitGrid idiom)
    - app/src/main/java/works/mees/dinghy/designsystem/components/DetailCard.kt (the Focus active-printer card with ringColor)
    - app/src/main/java/works/mees/dinghy/designsystem/components/ListRow.kt (the dense = true flag from 28-01)
    - app/src/main/java/works/mees/dinghy/ui/screen/TokenTextField.kt (the dense = true flag from 28-01)
    - app/src/main/java/works/mees/dinghy/designsystem/control/OutlinedControl.kt (Intent.Accent/Neutral/Danger; filled-when-armed convention)
    - app/src/main/java/works/mees/dinghy/di/AppContainer.kt (setActiveProfile / saveProfile / deleteProfile / mutateActiveProfile write-scope intent helpers ~line 168-189, 680+)
    - .planning/phases/28-system-settings-cluster/28-PATTERNS.md (PrintersScreen.kt section — ScreenScaffold + DetailCard + ListRow + FootButtonBar + ConfirmGuard + write-scope patterns verbatim)
    - .planning/phases/28-system-settings-cluster/28-UI-SPEC.md (Printers Screen layout + button intent map + copywriting keys + empty state)
  </read_first>
  <action>
    Rebuild PrintersScreen as `ScreenScaffold(gutter = null)` with `BoxWithConstraints` +
    `rememberUnitGrid(minOf(maxWidth, maxHeight))`. Drive mode from the `PrinterMode` state machine (Task
    1) held in a `remember { mutableStateOf(PrinterMode.Normal) }`; `BackHandler(printerMode != Normal)`
    disarms. Focus = active-printer `DetailCard` with `ringColor` from the connection state (accent =
    connected, heat = connecting, stop = error) — name Geist 20sp semibold, host:port Geist Mono 15sp,
    connection-state chip, Klippy state Geist 15sp t.text2. Field = `ListBlock` of `ListRow(dense = true,
    selected = profile.id == activeId, onClick = { rowTapEffect(printerMode, profile) -> dispatch })`;
    active row uses accentSoft/accentLine. Foot = `FootButtonBar` (1U) with four equal-weight
    `OutlinedControl`s: Add(`Intent.Accent`, text-only — D-21 assigns no Add glyph, do NOT invent one),
    Edit(`Intent.Neutral`, filled/highlighted when EditArmed), Delete(`Intent.Danger` filled when
    DeleteArmed else Neutral, C4 rule), Back(`Intent.Neutral`, disarms if a mode is armed). Edit-mode row
    tap opens the inline densified connection editor (reuse the existing editor form; switch its fields to
    `TokenTextField(dense = true)`; Port keeps the numeric keyboard; API key stays masked via the existing
    `resolveApiKeyEdit` — blank preserves stored key, explicit clear writes null). Delete-mode row tap
    raises the existing `ConfirmGuard` (destructive); confirm calls `container.deleteProfile(id)` then
    resets to Normal. ALL persistence routes through `container.*` writeScope intent helpers — NEVER
    `rememberCoroutineScope()`. Empty state per the UI-SPEC. Every text node via `fsSp(baseSp, t.fs).sp`
    with base >= 15f. Do NOT render FloatingEStop and do NOT add the screen to screenOwnsEstop. If any
    Add/Edit/Delete foot glyph is desired but unregistered, keep the button text-only — STOP-AND-ASK
    rather than inventing a glyph.
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:compileDebugKotlin --no-daemon" | tr -d '\r'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'ScreenScaffold' app/src/main/java/works/mees/dinghy/ui/screen/PrintersScreen.kt` returns at least 1 and `grep -c 'gutter = null' ...` returns at least 1
    - `grep -c 'DetailCard' app/src/main/java/works/mees/dinghy/ui/screen/PrintersScreen.kt` returns at least 1 (Focus active-printer card)
    - `grep -c 'PrinterMode' app/src/main/java/works/mees/dinghy/ui/screen/PrintersScreen.kt` returns at least 1 (mode-toggle wired)
    - `grep -c 'rememberCoroutineScope' app/src/main/java/works/mees/dinghy/ui/screen/PrintersScreen.kt` returns 0 (writeScope only)
    - `grep -c 'ConfirmGuard' app/src/main/java/works/mees/dinghy/ui/screen/PrintersScreen.kt` returns at least 1 (delete confirm)
    - `grep -c 'dense = true' app/src/main/java/works/mees/dinghy/ui/screen/PrintersScreen.kt` returns at least 1 (dense rows / fields)
    - No bare `.sp` literal introduced (all text via fsSp, base >= 15f)
    - `:app:compileDebugKotlin` BUILD SUCCESSFUL
  </acceptance_criteria>
  <done>PrintersScreen rebuilt: DetailCard Focus + dense rows + mode-toggle foot bar + ConfirmGuard delete + inline dense editor; writeScope-only; compiles.</done>
</task>

<task type="auto">
  <name>Task 3: Printers preview matrix + SampleFixtures</name>
  <files>app/src/main/java/works/mees/dinghy/preview/PrintersPreviews.kt, app/src/main/java/works/mees/dinghy/preview/SampleFixtures.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/preview/CalibrationPreviews.kt (PreviewBox + 6-combo + fsLarge + RTL idiom)
    - app/src/main/java/works/mees/dinghy/preview/SampleFixtures.kt (existing fixture style — pure immutable Profile data)
    - app/src/main/java/works/mees/dinghy/config/Profile.kt (Profile shape AFTER the 28-04 maxItems removal — build fixtures without maxItems)
    - docs/ui_design/PREVIEW_AND_TOKENS.md (minimized matrix shape)
    - .planning/phases/28-system-settings-cluster/28-UI-SPEC.md (Preview Contract — Printers interesting axis = Normal / Edit armed / Delete armed / Empty)
  </read_first>
  <action>
    Add `PrintersPreviews.kt` driving a stateless Printers content seam (extract one if the rebuilt screen
    does not already expose it) across the interesting axis: Normal, Edit-armed, Delete-armed, and Empty
    (no printers). Render the 6 theme combos via `PreviewBox(<combo>)`, a `*FsLargeOverflow` panel, an
    `*RtlSpotCheck` panel, and a pseudolocale panel. Add `SampleFixtures.printerProfileList` (2-3 Profile
    fixtures, one marked active) built against the post-28-04 Profile shape (no maxItems). All
    `stringResource` labels; no Moonraker, no coroutines.
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:assembleDebug --no-daemon" | tr -d '\r'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'PreviewBox' app/src/main/java/works/mees/dinghy/preview/PrintersPreviews.kt` returns at least 6
    - `grep -c 'fsLargeSeed\|FsLarge' app/src/main/java/works/mees/dinghy/preview/PrintersPreviews.kt` returns at least 1
    - `grep -c 'printerProfileList' app/src/main/java/works/mees/dinghy/preview/SampleFixtures.kt` returns at least 1
    - `:app:assembleDebug` BUILD SUCCESSFUL
  </acceptance_criteria>
  <done>Printers 6-combo + fsL + RTL preview matrix (Normal/Edit/Delete/Empty axis); SampleFixtures.printerProfileList added; assembleDebug green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Inline connection editor -> stored Profile | Host / Port / API key cross from keyboard input into the persisted connection profile |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-06-01 | Information Disclosure | API key field | mitigate | Reuse the existing `resolveApiKeyEdit` masking contract — never pre-fill the raw key; blank preserves stored key, explicit clear writes null; Profile.toString masks to *** (RESEARCH V5) |
| T-28-06-02 | Input Validation | host/port edit | mitigate | Port keeps the numeric keyboard; host is passed to the OkHttp URL builder which URL-encodes — no shell-injection surface (existing validation preserved) |
| T-28-06-03 | Tampering (accidental profile loss) | Delete mode | mitigate | Delete is a two-step arm + ConfirmGuard (destructive); no single-tap delete path |
| T-28-06-SC | Tampering | npm/pip/cargo installs | n/a | No package installs — pure Kotlin/Compose |
</threat_model>

<verification>
- `:app:testDebugUnitTest --tests *PrintersModeToggleTest` green (state machine)
- `:app:compileDebugKotlin` BUILD SUCCESSFUL (screen rebuild)
- `:app:assembleDebug` BUILD SUCCESSFUL (previews)
- Grep gates: no rememberCoroutineScope; ConfirmGuard + DetailCard + PrinterMode + dense rows present
</verification>

<success_criteria>
PrintersScreen is rebuilt on the jiib kit with the D-15 Focus/Field/Foot layout and the D-13
Edit/Delete mode-toggle foot bar (arm/disarm, ConfirmGuard-gated delete, inline dense editor); the
mode-toggle state machine is covered by a pure green host test; all persistence is writeScope-only;
the full preview matrix ships and assembleDebug is green.
</success_criteria>

<output>
Create `.planning/phases/28-system-settings-cluster/28-06-SUMMARY.md` when done.
</output>
