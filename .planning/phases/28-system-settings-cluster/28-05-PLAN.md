---
phase: 28-system-settings-cluster
plan: 05
type: execute
wave: 3
depends_on: ["28-02", "28-03"]
files_modified:
  - app/src/main/java/works/mees/dinghy/ui/shell/AppDrawer.kt
  - app/src/main/java/works/mees/dinghy/ui/shell/SwipeUpAccumulator.kt
  - app/src/main/java/works/mees/dinghy/ui/shell/AppShell.kt
  - app/src/main/java/works/mees/dinghy/ui/route/HomeAction.kt
  - app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusField.kt
  - app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusScreen.kt
  - app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusUiModel.kt
  - app/src/test/java/works/mees/dinghy/ui/shell/AppDrawerOutputsGateTest.kt
  - app/src/test/java/works/mees/dinghy/ui/shell/SwipeUpAccumulatorTest.kt
  - app/src/test/java/works/mees/dinghy/ui/printstatus/PrintStatusUiModelTest.kt
  - app/src/androidTest/java/works/mees/dinghy/ui/ShellPresenceTest.kt
  - app/src/androidTest/java/works/mees/dinghy/webcam/DrawerWebcamGatingTest.kt
  - app/src/androidTest/java/works/mees/dinghy/ui/finetune/FineTuneNavTest.kt
autonomous: true
must_haves:
  truths:
    - "AppDrawer.kt and SwipeUpAccumulator.kt are deleted along with their two test files in the same change"
    - "The swipe-up gesture, drawer overlay, drawer BackHandler, bottom-edge handle, and suppressSwipe set are all removed from AppShell"
    - "The System foot button on WaterfallHome and the printing shortcut grid both navigate to NavDest.System; the standby Drawer launcher tile retargets to NavDest.System"
    - "HomeAction.OpenDrawer is removed and the exhaustive when over the sealed interface still compiles (no dead branch)"
    - "NavDest.System is NOT in FOOT_GUN_DESTS and NOT in screenOwnsEstop (shell-level e-stop applies mid-print, D-06)"
    - "onOpenDrawer is retired at ALL PrintStatusScreen.kt callsites and threaded to onNavigate(NavDest.System) — grep -r \"onOpenDrawer\" app/src/ returns zero hits"
    - "The LauncherDest.Drawer enum variant + its PrintStatusUiModelTest assertions are removed; the standby launcher Drawer tile retargets to NavDest.System"
    - "The three drawer/swipe androidTest files (ShellPresenceTest, DrawerWebcamGatingTest, FineTuneNavTest) are deleted so the androidTest sourceset compiles and asserts no deleted behavior — grep -ril \"drawer|swipeup\" app/src/androidTest/ returns zero hits"
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/ui/shell/AppShell.kt"
      provides: "drawer-free shell with NavDest.System composable + System foot routing"
      contains: "composable<NavDest.System>"
  key_links:
    - from: "WaterfallHome System foot + printing shortcut grid + standby Drawer tile"
      to: "NavDest.System"
      via: "onNavigate(NavDest.System)"
      pattern: "NavDest.System"
    - from: "AppShell NavHost"
      to: "SystemPageScreen"
      via: "composable<NavDest.System> block"
      pattern: "SystemPageScreen"
---

<objective>
Delete the swipe-up gesture + AppDrawer entirely (D-04) and rewire the System entry points: the
WaterfallHome System foot button, the printing-mode shortcut grid (D-06), and the standby Drawer
launcher tile all route to `NavDest.System`. Remove `HomeAction.OpenDrawer` and wire the new
`SystemPageScreen` (from 28-02) into the NavHost.

Purpose: This is the atomic destination-retirement wave. It runs ONLY after the System page (28-02)
and the rehomed idle rows (28-03) make every former DRAWER_TILES destination reachable elsewhere — so
deleting the drawer strands nothing. The deletion of source + test files + the OpenDrawer variant must
land together; Kotlin's exhaustive-`when` and import hygiene turn any stale reference into a compile
error, which is the intended guard.
Output: AppDrawer.kt + SwipeUpAccumulator.kt + their two test files deleted; AppShell drawer/gesture
plumbing removed and the `composable<NavDest.System>` block added; OpenDrawer gone; PrintStatusField
retargeted to System; the app builds and the full host suite is green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-system-settings-cluster/28-CONTEXT.md
@.planning/phases/28-system-settings-cluster/28-PATTERNS.md
@.planning/phases/28-system-settings-cluster/28-RESEARCH.md
@.planning/todos/pending/2026-06-11-retire-swipe-up-nav.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete AppDrawer + SwipeUpAccumulator + drawer/swipe androidTests (source + tests); remove HomeAction.OpenDrawer + LauncherDest.Drawer</name>
  <files>app/src/main/java/works/mees/dinghy/ui/shell/AppDrawer.kt, app/src/main/java/works/mees/dinghy/ui/shell/SwipeUpAccumulator.kt, app/src/test/java/works/mees/dinghy/ui/shell/AppDrawerOutputsGateTest.kt, app/src/test/java/works/mees/dinghy/ui/shell/SwipeUpAccumulatorTest.kt, app/src/androidTest/java/works/mees/dinghy/ui/ShellPresenceTest.kt, app/src/androidTest/java/works/mees/dinghy/webcam/DrawerWebcamGatingTest.kt, app/src/androidTest/java/works/mees/dinghy/ui/finetune/FineTuneNavTest.kt, app/src/main/java/works/mees/dinghy/ui/route/HomeAction.kt, app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusUiModel.kt, app/src/test/java/works/mees/dinghy/ui/printstatus/PrintStatusUiModelTest.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/shell/AppDrawer.kt (DRAWER_TILES inventory ~line 198-262 — the reachability checklist; confirm every dest now lives on the System page or idle list before deleting)
    - app/src/main/java/works/mees/dinghy/ui/shell/SwipeUpAccumulator.kt (the helper being deleted)
    - app/src/test/java/works/mees/dinghy/ui/shell/AppDrawerOutputsGateTest.kt + SwipeUpAccumulatorTest.kt (host test files to delete)
    - app/src/androidTest/java/works/mees/dinghy/ui/ShellPresenceTest.kt (~line 119-224 — every test method swipes up + asserts drawer tiles; whole file is drawer-dependent, delete it)
    - app/src/androidTest/java/works/mees/dinghy/webcam/DrawerWebcamGatingTest.kt (~line 123-151 — opens the Webcam tile via the drawer; whole file is drawer-dependent, delete it)
    - app/src/androidTest/java/works/mees/dinghy/ui/finetune/FineTuneNavTest.kt (~line 110-158 — enters Fine-Tune via `openFineTuneViaDrawer()`; already known-broken on real hardware per [[dinghy-instrumented-swipe-threshold]]; delete it — the nav-reset logic it asserted is covered by host tests)
    - app/src/main/java/works/mees/dinghy/ui/route/HomeAction.kt (the `data object OpenDrawer : HomeAction` variant ~line 47)
    - app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusUiModel.kt (the `LauncherDest.Drawer` enum variant ~line 39-40 + `add(LauncherDest.Drawer)` ~line 133 + the Drawer KDoc ~line 28/39/118-119)
    - app/src/test/java/works/mees/dinghy/ui/printstatus/PrintStatusUiModelTest.kt (the `LauncherDest.Drawer` assertions ~line 45/49-50/71)
    - .planning/phases/28-system-settings-cluster/28-RESEARCH.md (Pattern 7 — exact files to delete; Pitfall 3 — the three-file OpenDrawer coupling; Pitfall 4 — let the compiler flag stale refs)
  </read_first>
  <action>
    Confirm the DRAWER_TILES reachability checklist is satisfied: Temperature/Console/Fine-Tune now on
    the home idle list (28-03); Printers/Theme/Settings/SysInfo/About/Power on the System page (28-02);
    all other tiles already on the idle list. Then DELETE the four core drawer/swipe files: `AppDrawer.kt`,
    `SwipeUpAccumulator.kt`, `AppDrawerOutputsGateTest.kt`, `SwipeUpAccumulatorTest.kt`. ALSO DELETE the
    three drawer/swipe-dependent androidTest files — `ShellPresenceTest.kt`, `DrawerWebcamGatingTest.kt`,
    `FineTuneNavTest.kt` — each opens the drawer via `swipeUp()` and asserts drawer-tile behavior that no
    longer exists after D-04; the androidTest sourceset must still COMPILE and not assert deleted behavior
    (FineTuneNavTest is already known-broken on hardware per the instrumented-swipe-threshold lesson, so
    deletion is the right call, not migration). Remove the `data object OpenDrawer : HomeAction` variant
    from `HomeAction.kt`. Remove the `LauncherDest.Drawer` enum variant from `PrintStatusUiModel.kt`
    (delete the `Drawer,` member, the `add(LauncherDest.Drawer)` line in `standbyLauncherDests`, and reword
    the Drawer-referencing KDoc) AND delete the corresponding `LauncherDest.Drawer` assertions in
    `PrintStatusUiModelTest.kt` IN THE SAME CHANGE (host test sourceset must compile at every commit —
    blessing-of-drawer-semantics dead code dies with the variant). This deletion produces compile errors at
    every remaining reference (AppShell, PrintStatusScreen, PrintStatusField) — that is intentional and the
    next two tasks resolve them. Do this deletion FIRST so the compiler flags every stale reference (import
    hygiene + exhaustive-when guard, RESEARCH Pitfall 4). Also check whether the `LauncherDrawer` DinghyIcons
    token is still referenced anywhere after the drawer is gone; if it is fully unreferenced, remove it from
    DinghyIcons and its `all` membership (keep the drift guard green); if anything still references it,
    leave it untouched and note it as a follow-up — do NOT break DinghyIconsTest.
  </action>
  <verify>
    <automated>test ! -f app/src/main/java/works/mees/dinghy/ui/shell/AppDrawer.kt && test ! -f app/src/main/java/works/mees/dinghy/ui/shell/SwipeUpAccumulator.kt && test ! -f app/src/test/java/works/mees/dinghy/ui/shell/AppDrawerOutputsGateTest.kt && test ! -f app/src/test/java/works/mees/dinghy/ui/shell/SwipeUpAccumulatorTest.kt && test ! -f app/src/androidTest/java/works/mees/dinghy/ui/ShellPresenceTest.kt && test ! -f app/src/androidTest/java/works/mees/dinghy/webcam/DrawerWebcamGatingTest.kt && test ! -f app/src/androidTest/java/works/mees/dinghy/ui/finetune/FineTuneNavTest.kt && echo DELETED_OK</automated>
  </verify>
  <acceptance_criteria>
    - The seven files no longer exist (the `DELETED_OK` echo prints)
    - `grep -c 'OpenDrawer' app/src/main/java/works/mees/dinghy/ui/route/HomeAction.kt` returns 0
    - `grep -rc 'LauncherDest.Drawer' app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusUiModel.kt app/src/test/java/works/mees/dinghy/ui/printstatus/PrintStatusUiModelTest.kt` returns 0 in both (variant + assertions gone)
    - `grep -rilE 'drawer|swipeup' app/src/androidTest/` (case-insensitive) returns zero hits (no drawer/swipe androidTest survives)
    - `git status --porcelain` shows the seven files as deleted (`D`) — source and tests removed together
    - If `LauncherDrawer` was removed, `:app:testDebugUnitTest --tests *DinghyIconsTest` stays green; if kept, no DinghyIcons edit was made
  </acceptance_criteria>
  <done>AppDrawer + SwipeUpAccumulator + their host tests + the three drawer/swipe androidTests deleted; OpenDrawer + LauncherDest.Drawer variants (and their test assertions) removed; drift guard intact.</done>
</task>

<task type="auto">
  <name>Task 2: Strip drawer/swipe plumbing from AppShell + add the NavDest.System composable block</name>
  <files>app/src/main/java/works/mees/dinghy/ui/shell/AppShell.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/shell/AppShell.kt (drawerOpen ~line 165; drawer BackHandler ~line 510; swipe pointerInput + suppressSwipe block ~line 526-584; the AppDrawer overlay ~line 961-973; bottom-edge handle box ~line 975-982; SWIPE_UP_THRESHOLD_PX ~line 1026; the WaterfallHome block's onOpenDrawer ~line 593-605; the composable<NavDest.About> block ~line 806-829; screenOwnsEstop ~line 994-999; shouldPopToRoot LaunchedEffect ~line 838-860; the imports of AppDrawer + SwipeUpAccumulator)
    - app/src/main/java/works/mees/dinghy/ui/screen/SystemPageScreen.kt (the 28-02 screen signature: SystemPageScreen(container, onNavigate, onBack))
    - .planning/phases/28-system-settings-cluster/28-PATTERNS.md (AppShell.kt section — every hunk to delete + the composable<NavDest.System> insert pattern + the screenOwnsEstop / FOOT_GUN_DESTS non-inclusion rules)
    - .planning/phases/28-system-settings-cluster/28-RESEARCH.md (Pattern 2 + Pitfall 6 — do NOT add NavDest.System to screenOwnsEstop)
  </read_first>
  <action>
    Remove all drawer + swipe plumbing from AppShell: delete `var drawerOpen`; delete the
    `BackHandler(enabled = drawerOpen)`; delete the entire swipe-detect `pointerInput(...)` block and the
    `suppressSwipe` val; delete the `if (drawerOpen) { AppDrawer(...) }` overlay; delete the bottom-edge
    swipe-handle `Box`; delete the `SWIPE_UP_THRESHOLD_PX` const and the `AppDrawer`/`SwipeUpAccumulator`
    imports; remove every `drawerOpen` read elsewhere (e.g. the MacroPopup/scan/prompt visibility guard
    that ANDed `!drawerOpen` — drop that term since drawerOpen no longer exists). In the WaterfallHome
    composable block, replace `onOpenDrawer = { drawerOpen = true }` so the System foot routes through the
    existing navigate lambda to `NavDest.System` (if WaterfallHome/PrintStatusField still expects an
    `onOpenDrawer` param this wave, pass `{ navController.navigate(NavDest.System) }` until Task 3 changes
    its signature — keep the build coherent at each step). Add a `composable<NavDest.System> { SystemPageScreen(container = container, onNavigate = { navController.navigate(it) }, onBack = { navController.popBackStack() }) }` block after the `composable<NavDest.About>` block, following the exact pattern of the other leaf-screen blocks. Do NOT add `NavDest.System` to `screenOwnsEstop` and do NOT add it to `FOOT_GUN_DESTS` — the shell-level FloatingEStop must fire on the System cluster while printing (D-06, Pitfall 6). Add a one-line comment at the shouldPopToRoot mapping noting System is intentionally non-foot-gun (covered by the existing `else` path).
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:compileDebugKotlin --no-daemon" | tr -d '\r'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'drawerOpen\|SwipeUpAccumulator\|SWIPE_UP_THRESHOLD_PX\|suppressSwipe' app/src/main/java/works/mees/dinghy/ui/shell/AppShell.kt` returns 0
    - `grep -c 'import works.mees.dinghy.ui.shell.AppDrawer' app/src/main/java/works/mees/dinghy/ui/shell/AppShell.kt` returns 0
    - `grep -c 'composable<NavDest.System>' app/src/main/java/works/mees/dinghy/ui/shell/AppShell.kt` returns 1
    - `grep -A6 'screenOwnsEstop' app/src/main/java/works/mees/dinghy/ui/shell/AppShell.kt` does NOT contain `NavDest.System` (shell e-stop applies)
    - `:app:compileDebugKotlin` BUILD SUCCESSFUL (no stale AppDrawer/OpenDrawer references remain)
  </acceptance_criteria>
  <done>AppShell is drawer/swipe-free; NavDest.System wired into the NavHost; System cluster keeps the shell e-stop; debug compiles.</done>
</task>

<task type="auto">
  <name>Task 3: Retarget PrintStatusField + PrintStatusScreen System entry points to NavDest.System; full suite green</name>
  <files>app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusField.kt, app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusScreen.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusField.kt (the idle foot `onOpenDrawer` param + System foot button ~line 78/136; the Standby launcher grid `onOpenDrawer` + Drawer tile ~line 435/458; the printing shortcut row TuneShortcutTile + grid ~line 588-623; the `HomeAction.OpenDrawer` when-branch in the action dispatch)
    - app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusScreen.kt (the `onOpenDrawer: () -> Unit` param + its 6 callsites — KDoc ~line 66, param ~line 74, pass-down ~line 318, inner param ~line 392, pass-down ~line 429, inner param ~line 468, pass-down ~line 494; ALL must be retired)
    - app/src/main/java/works/mees/dinghy/designsystem/icons/DinghyIcons.kt (FootSystem `bottom_panel_open` ~line 69 — the printing shortcut System tile glyph; already registered)
    - app/src/main/res/values/strings.xml (home_foot_system / system_row_label for the shortcut tile label)
    - .planning/phases/28-system-settings-cluster/28-PATTERNS.md (PrintStatusField.kt section — onOpenDrawer -> onNavigate(NavDest.System), add System tile to printing grid)
    - .planning/phases/28-system-settings-cluster/28-UI-SPEC.md (printing shortcut grid System entry, D-06; Tune tile survives)
  </read_first>
  <action>
    Remove the `onOpenDrawer: () -> Unit` parameter from `PrintStatusField` and the standby launcher
    grid composable. Everywhere `onOpenDrawer` was invoked, route to System instead: the idle System
    foot button calls `onNavigate(NavDest.System)`; the standby launcher grid's flexible/growing Drawer
    tile retargets to `onClick = { onNavigate(NavDest.System) }` (keep it as the System entry tile — do
    not leave a no-op tile). Remove the `is HomeAction.OpenDrawer -> onOpenDrawer()` branch from the
    action `when` (the variant no longer exists; the `when` over the sealed `HomeAction` is exhaustive
    without it). In the PRINTING-mode shortcut grid, ADD a System shortcut tile alongside the surviving
    Tune tile: `DinghyIcons.FootSystem` glyph, label from `R.string.home_foot_system` (or `system_row_label`),
    `onClick = { onNavigate(NavDest.System) }` (D-06 mid-print System access). Update the KDoc that says
    "the System foot button calls onOpenDrawer" to reflect the NavDest.System routing. Update any AppShell
    call site of PrintStatusField that still passes `onOpenDrawer` (drop the arg) so the build stays
    coherent. THREAD the same retirement through `PrintStatusScreen.kt`: remove the `onOpenDrawer: () -> Unit`
    parameter from EVERY composable that declares it (the public `PrintStatusScreen`, the inner content
    composables ~line 392 and ~line 468) and delete every pass-down (~line 318/429/494) — these screens
    forward `onOpenDrawer` to `PrintStatusField`, so once the param is gone from the Field signature each
    forward must be dropped, NOT left as a stale arg. Where the public `PrintStatusScreen` needs a System
    entry point at all, route it through the existing `onNavigate`/navigate lambda to `NavDest.System`
    (D-01/D-06); update the line-66 KDoc that describes `onOpenDrawer` as "opens the swipe-up App Drawer".
    The end state is ZERO `onOpenDrawer` references anywhere under `app/src/`. After edits, run the FULL
    host unit suite so the whole sourceset is proven green post-deletion.
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --no-daemon" | tr -d '\r'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'onOpenDrawer' app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusField.kt` returns 0
    - `grep -c 'onOpenDrawer' app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusScreen.kt` returns 0 (param + all 6 callsites retired)
    - `grep -r 'onOpenDrawer' app/src/` returns ZERO hits across the whole tree (no surviving reference anywhere)
    - `grep -c 'NavDest.System' app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusField.kt` returns at least 2 (idle foot + standby Drawer tile + printing shortcut tile)
    - `grep -c 'FootSystem' app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusField.kt` returns at least 1 (printing shortcut System tile)
    - `grep -c 'OpenDrawer' app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusField.kt` returns 0
    - `:app:testDebugUnitTest` (FULL suite) BUILD SUCCESSFUL / all green — proves the whole sourceset compiles + passes after the drawer deletion
  </acceptance_criteria>
  <done>PrintStatusField + PrintStatusScreen route to NavDest.System; onOpenDrawer fully retired everywhere (zero app/src hits); OpenDrawer gone; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User navigation -> System cluster while printing | The cluster becomes mid-print reachable (D-06); the shell e-stop must remain reachable on it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-05-01 | Denial of Service (stranded destination) | drawer deletion | mitigate | Reachability checklist enforced in Task 1: System page (28-02) + idle rows (28-03) cover every DRAWER_TILES dest before the drawer is removed |
| T-28-05-02 | Denial of Service (e-stop hidden mid-print) | screenOwnsEstop | mitigate | NavDest.System deliberately excluded from screenOwnsEstop + FOOT_GUN_DESTS; shell-level FloatingEStop fires on the cluster while printing (grep gate proves exclusion) |
| T-28-05-SC | Tampering | npm/pip/cargo installs | n/a | No package installs — pure Kotlin |
</threat_model>

<verification>
- The seven deleted files are gone (4 core drawer/swipe + 3 drawer/swipe androidTests); OpenDrawer + LauncherDest.Drawer removed; deletions are atomic with their tests
- `:app:compileDebugKotlin` BUILD SUCCESSFUL after AppShell strip + System wiring
- `:app:compileDebugAndroidTestKotlin` (or `:app:assembleDebugAndroidTest`) BUILD SUCCESSFUL — androidTest sourceset compiles with no drawer/swipe assertions surviving
- `:app:testDebugUnitTest` FULL suite green after the deletion
- Grep gates: no drawerOpen/SwipeUp/suppressSwipe/onOpenDrawer/OpenDrawer/LauncherDest.Drawer survive; `grep -r onOpenDrawer app/src/` and `grep -rilE 'drawer|swipeup' app/src/androidTest/` both return zero; NavDest.System absent from screenOwnsEstop + FOOT_GUN_DESTS
</verification>

<success_criteria>
The swipe-up gesture and AppDrawer (source + tests) are fully deleted (D-04); the System page is wired
into the NavHost and reached from the WaterfallHome foot, the standby launcher tile, and the printing
shortcut grid (D-06); HomeAction.OpenDrawer is removed with no dead branches; NavDest.System stays
print-reachable with the shell e-stop; the full host suite is green.
</success_criteria>

<output>
Create `.planning/phases/28-system-settings-cluster/28-05-SUMMARY.md` when done.
</output>
