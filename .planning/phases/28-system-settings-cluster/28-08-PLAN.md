---
phase: 28-system-settings-cluster
plan: 08
type: execute
wave: 4
depends_on: ["28-01", "28-04"]
files_modified:
  - app/src/main/java/works/mees/dinghy/ui/screen/ThemeEditorScreen.kt
  - app/src/main/java/works/mees/dinghy/ui/shell/DevThemeCyclerOverlay.kt
  - app/src/main/java/works/mees/dinghy/preview/ThemeEditorPreviews.kt
autonomous: true
must_haves:
  truths:
    - "ThemeEditorScreen gains a 2D saturation/value square below the hue ColorWheel (D-16): pool + status slot pickers persist full S/V, and re-opening a slot restores its stored S/V from the persisted ARGB"
    - "The editor is one dense vertical scroll with inline picker expansions and no sub-pages (D-18); ThemeScreen stays a thin wrapper"
    - "The idle (no-active-profile) seed read is reactive (D-19): the firstOrNull() one-shot at ThemeEditorScreen line 117 is replaced with a reactive collect"
    - "The dev cycler overlay drag-relocates again (D-20) via a dedicated bare-surface drag region; chip taps still consume their own events"
    - "All theme writes route through container writeScope intent helpers — never rememberCoroutineScope()"
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/ui/screen/ThemeEditorScreen.kt"
      provides: "S/V square + dense scroll + reactive idle seed"
      contains: "Color.hsv(hue, sat, value)"
  key_links:
    - from: "S/V square onSettle"
      to: "persisted ARGB via existing poolOverrides/statusOverrides wire"
      via: "Color.hsv(hue, sat, value) write + HSV decode on re-open"
      pattern: "colorToHSV"
    - from: "idle seed read"
      to: "themePrefs.tupleFlow reactive collect"
      via: "collectAsStateWithLifecycle (no firstOrNull)"
      pattern: "collectAsStateWithLifecycle"
---

<objective>
Rebuild the theme editor's color-picking and fix its two lingering functional bugs: add a 2D
saturation/value square below the hue wheel so pool + status custom colors get full color freedom and
their S/V survives a re-open (D-16 / WR-02); collapse the editor into one dense vertical scroll with
inline picker expansions (D-18); make the idle (no-active-profile) seed read reactive (D-19, the
ThemeEditorScreen line-117 `firstOrNull()` one-shot); and restore the dev cycler overlay's drag
relocation (D-20).

Purpose: This is the heaviest single-screen rebuild of the phase — a new Canvas S/V control with its
own gesture loop + ARGB<->HSV round-trip + a reactivity fix. Isolating it (it owns only
ThemeEditorScreen + DevThemeCyclerOverlay, files nothing else touches) lets it run in parallel with the
Printers (28-06) and Settings/SysInfo/About (28-07) rebuilds. It depends on 28-04 having trimmed the
ThemeTuple/Profile shape so the editor rebuilds against the final types.
Output: ThemeEditorScreen with an inline S/V square + dense scroll + reactive idle seed; the dev cycler
drag restored; the editor's `@Preview` matrix; writeScope-only persistence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-system-settings-cluster/28-CONTEXT.md
@.planning/phases/28-system-settings-cluster/28-PATTERNS.md
@.planning/phases/28-system-settings-cluster/28-RESEARCH.md
@.planning/phases/28-system-settings-cluster/28-UI-SPEC.md
@docs/ui_design/PREVIEW_AND_TOKENS.md
@docs/ui_design/THEMING.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix the idle seedTheme reactivity (D-19) at ThemeEditorScreen line 117</name>
  <files>app/src/main/java/works/mees/dinghy/ui/screen/ThemeEditorScreen.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/screen/ThemeEditorScreen.kt (the `LaunchedEffect(activeProfile?.id)` at ~line 116 and the `?: container.themePrefs.tupleFlow.firstOrNull()` one-shot at line 117; the `import kotlinx.coroutines.flow.firstOrNull` ~line 54)
    - app/src/main/java/works/mees/dinghy/di/AppContainer.kt (seedTheme is ALREADY reactive ~line 650-671 — do NOT change it; the bug is the editor's local one-shot, RESEARCH Pitfall 5)
    - app/src/main/java/works/mees/dinghy/theme/ThemePrefs.kt (tupleFlow + the post-28-04 ThemeTuple shape; the default tuple constant for the collect's initial value)
    - .planning/phases/28-system-settings-cluster/28-PATTERNS.md (ThemeEditorScreen.kt seedTheme reactive fix — collectAsStateWithLifecycle pattern)
    - .planning/phases/28-system-settings-cluster/28-RESEARCH.md (Pitfall 5 + Pattern 6 — the fix is in the editor, NOT AppContainer)
  </read_first>
  <action>
    Replace the one-shot idle seed read. Currently the `LaunchedEffect` seeds the editor's mirror state
    vars from `activeProfile?.toThemeTuple() ?: container.themePrefs.tupleFlow.firstOrNull()` — the
    `firstOrNull()` reads the global idle theme ONCE and never re-runs if the global theme changes while
    idle. Collect `container.themePrefs.tupleFlow` reactively (e.g.
    `val globalTuple by container.themePrefs.tupleFlow.collectAsStateWithLifecycle(initialValue = <the
    default ThemeTuple constant>)`) and key the seeding `LaunchedEffect` on both `activeProfile?.id` AND
    `globalTuple`, choosing `activeProfile?.toThemeTuple() ?: globalTuple`. Remove the now-unused
    `firstOrNull` import. Do NOT touch `AppContainer.seedTheme` (already reactive). Keep the active-profile
    branch behavior identical.
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:compileDebugKotlin --no-daemon" | tr -d '\r'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'tupleFlow.firstOrNull' app/src/main/java/works/mees/dinghy/ui/screen/ThemeEditorScreen.kt` returns 0 (one-shot gone)
    - `grep -c 'collectAsStateWithLifecycle' app/src/main/java/works/mees/dinghy/ui/screen/ThemeEditorScreen.kt` returns at least 1 (reactive collect)
    - `grep -c 'import kotlinx.coroutines.flow.firstOrNull' app/src/main/java/works/mees/dinghy/ui/screen/ThemeEditorScreen.kt` returns 0 (unused import removed)
    - `:app:compileDebugKotlin` BUILD SUCCESSFUL
  </acceptance_criteria>
  <done>The editor's idle seed read is reactive; firstOrNull one-shot removed; AppContainer untouched; compiles.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the 2D S/V square + dense single-scroll editor (D-16 / D-18)</name>
  <files>app/src/main/java/works/mees/dinghy/ui/screen/ThemeEditorScreen.kt</files>
  <behavior>
    - A 2D saturation/value square Canvas renders below the hue ColorWheel: X = saturation 0..1 (white -> pure hue), Y = value 1..0 (bright -> black), crosshair at (sat, 1-value) in thin t.outline lines
    - Drag and tap on the square update sat/value; onSettle writes the final color
    - Pool slot pickers AND status slot pickers persist the FULL chosen color (Color.hsv(hue, sat, value)), not the old hardcoded Color.hsv(hue, 1f, 1f)
    - Re-opening a slot restores sat + value by decoding the stored ARGB to HSV (android.graphics.Color.colorToHSV), not resetting S/V to 1
    - The editor is one Column.verticalScroll: ColorWheel, S/V square, pool slots, status slots, FootButtonBar (Apply/Reset/Back) — no sub-pages, picker expansions are inline (D-18)
    - ThemeScreen remains the thin wrapper hosting the editor (no fork)
  </behavior>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/screen/ThemeEditorScreen.kt (the existing slot pickers reusing ColorWheel ~line 165/228/341; the hardcoded `Color.hsv(((hue%360)+360)%360, 1f, 1f)` write sites ~line 726/734; the poolOverrides/statusOverrides read paths ~line 203/384/403; the onSettle handlers)
    - app/src/main/java/works/mees/dinghy/designsystem/ColorWheel.kt (the awaitEachGesture / onHandleMove / onSettle gesture template ~line 58-111 — mirror it for the S/V square)
    - app/src/main/java/works/mees/dinghy/ui/screen/ThemeScreen.kt (the thin wrapper contract — leave unchanged)
    - app/src/main/java/works/mees/dinghy/di/AppContainer.kt (setActive* theme-edit intent helpers ~line 679+ — the writeScope persistence routes)
    - .planning/phases/28-system-settings-cluster/28-PATTERNS.md (ThemeEditorScreen.kt section — S/V square Canvas + gesture verbatim, the var sat/value state seed-from-ARGB pattern, the onSettle write, dense scroll shape)
    - .planning/phases/28-system-settings-cluster/28-UI-SPEC.md (Theme editor layout: seed section, pool/status slots, S/V square contract, foot Apply/Reset/Back, UAT-5 wheel+square >1U exception)
    - docs/ui_design/THEMING.md (the THEME-01 data carve-out — pool/status slot colors are user-input raw hex, never brandTint-clamped)
  </read_first>
  <action>
    Build a `SaturationValueSquare` composable (Canvas, `fillMaxWidth().aspectRatio(1f)`) using the
    ColorWheel gesture template: an `awaitEachGesture` that on down + each move computes `sat =
    (x/width).coerceIn(0,1)` and `value = 1f - (y/height).coerceIn(0,1)`, calls an `onHandleMove(sat,
    value)` (cheap, repaint-only), consumes the change, and fires `onSettle(sat, value)` on pointer-UP.
    Draw two overlaid gradients — a horizontal white -> `Color.hsv(hue,1f,1f)` (saturation) and a vertical
    transparent -> black (value) — plus a thin `t.outline` crosshair at `(sat*width, (1-value)*height)`.
    Add `var sat` / `var value` state (mutableFloatStateOf) alongside the existing `hue`. On opening a
    slot picker, seed hue/sat/value by decoding the stored ARGB via
    `android.graphics.Color.colorToHSV(storedArgb.toInt(), FloatArray(3))` (default to 1f/1f when no
    stored override). Change BOTH the pool-slot and status-slot write sites (the `Color.hsv(hue, 1f, 1f)`
    at ~line 726/734) to `Color.hsv(hue, sat, value)`. Persist via the EXISTING `poolOverrides`/
    `statusOverrides` ARGB-Long wire through the existing `container.setActive*` writeScope intent helpers
    — NO new DataStore keys (the ARGB already carries full color; the bug was only the read decode + the
    hardcoded S/V write). Restructure the editor body into one `Column.verticalScroll(rememberScrollState())`:
    ColorWheel -> S/V square -> Divider -> pool slots -> Divider -> status slots -> FootButtonBar
    (Apply=Go, Reset to Default=Warn behind a ConfirmGuard, Back=Neutral). Inline picker expansions only —
    no sub-pages (D-18). Leave ThemeScreen as the thin wrapper. Preserve the THEME-01 carve-out (slot
    colors are raw user hex, never brandTint-clamped). All text via fsSp base >= 15f; no
    rememberCoroutineScope.
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --no-daemon" | tr -d '\r'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Color.hsv(hue, sat, value)\|Color.hsv(\s*hue, sat, value' app/src/main/java/works/mees/dinghy/ui/screen/ThemeEditorScreen.kt` returns at least 1 (full S/V write)
    - `grep -c 'Color.hsv(((hue' app/src/main/java/works/mees/dinghy/ui/screen/ThemeEditorScreen.kt` returns 0 (the hardcoded S=V=1 writes are gone)
    - `grep -c 'colorToHSV' app/src/main/java/works/mees/dinghy/ui/screen/ThemeEditorScreen.kt` returns at least 1 (ARGB->HSV decode on re-open)
    - `grep -c 'verticalScroll' app/src/main/java/works/mees/dinghy/ui/screen/ThemeEditorScreen.kt` returns at least 1 (dense single scroll)
    - `grep -c 'rememberCoroutineScope' app/src/main/java/works/mees/dinghy/ui/screen/ThemeEditorScreen.kt` returns 0
    - `:app:testDebugUnitTest` FULL suite green (no theme-test regression after the editor rebuild)
  </acceptance_criteria>
  <done>S/V square added with full-color persistence + ARGB->HSV restore; editor is one dense scroll; THEME-01 carve-out intact; suite green.</done>
</task>

<task type="auto">
  <name>Task 3: Restore the dev cycler overlay drag (D-20) + add the editor preview matrix</name>
  <files>app/src/main/java/works/mees/dinghy/ui/shell/DevThemeCyclerOverlay.kt, app/src/main/java/works/mees/dinghy/preview/ThemeEditorPreviews.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/shell/DevThemeCyclerOverlay.kt (the Column `pointerInput(boxSize)` drag ~line 187-211; the chip consume-on-down handlers; the offset state ~line 178)
    - .planning/phases/28-system-settings-cluster/28-PATTERNS.md (DevThemeCyclerOverlay.kt section — the dedicated bare-surface drag-strip pattern; chips keep consuming their own events)
    - app/src/main/java/works/mees/dinghy/preview/CalibrationPreviews.kt (PreviewBox + 6-combo + fsLarge + RTL idiom)
    - docs/ui_design/PREVIEW_AND_TOKENS.md (minimized matrix shape)
    - .planning/phases/28-system-settings-cluster/28-UI-SPEC.md (Preview Contract — ThemeEditor axis = pool/status slot picker open vs closed; dev cycler drag fix is dev-only, no preview required for it)
  </read_first>
  <action>
    D-20 dev cycler drag: the current drag lives on the whole Column's `pointerInput`, but chip taps
    consume the DOWN before the Column's `awaitFirstDown` sees it, so the panel stops relocating. Add a
    DEDICATED bare-surface drag region (e.g. a thin drag-handle `Box` strip in the Column, separate from
    the chips) that owns the `awaitFirstDown` + `drag` + offset update; keep the existing chip
    consume-on-down handlers so chip taps still work and do not bleed to the parent. The panel must
    drag-relocate again while chip taps continue cycling. This is a dev-only tool — mechanical fix, no
    user-visible change. Then add `ThemeEditorPreviews.kt` driving a stateless editor content seam across
    the interesting axis (a pool/status slot picker OPEN vs CLOSED): 6 theme combos via `PreviewBox`, a
    `*FsLargeOverflow` panel, an `*RtlSpotCheck` panel, a pseudolocale panel. Build the pool + status slot fixture data INLINE in `ThemeEditorPreviews.kt`
    (do NOT edit `SampleFixtures.kt` — 28-06 owns it this wave; keep file ownership disjoint). `stringResource`
    labels; no Moonraker/coroutines in previews.
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:assembleDebug --no-daemon" | tr -d '\r'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'awaitFirstDown\|drag(' app/src/main/java/works/mees/dinghy/ui/shell/DevThemeCyclerOverlay.kt` returns at least 1 (drag region present) AND a dedicated drag surface separate from the chips exists
    - `grep -c 'PreviewBox' app/src/main/java/works/mees/dinghy/preview/ThemeEditorPreviews.kt` returns at least 6
    - `grep -c 'fsLargeSeed\|FsLarge' app/src/main/java/works/mees/dinghy/preview/ThemeEditorPreviews.kt` returns at least 1
    - `:app:assembleDebug` BUILD SUCCESSFUL
  </acceptance_criteria>
  <done>Dev cycler drag-relocates via a dedicated bare-surface region; ThemeEditor 6-combo + fsL + RTL preview matrix ships; assembleDebug green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| S/V picker input -> persisted theme override | User-chosen color crosses into stored pool/status overrides (raw hex, THEME-01 carve-out) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-08-01 | Tampering (corrupt theme state) | S/V write path | mitigate | Writes route through the existing sanitize-backed poolOverrides/statusOverrides wire + ThemeResolver compute() try/catch fail-safe; coerceIn(0,1) bounds sat/value at the gesture |
| T-28-08-02 | Tampering (dropped theme write) | slot persistence | mitigate | All persistence on container writeScope intent helpers, never a composition scope ([[dinghy-compose-write-scope-cancellation]]) |
| T-28-08-03 | Error Handling (theme fail-open) | idle seed reactivity | accept | Reactive collect uses a default ThemeTuple initial value; a transient empty emission falls back to defaults rather than crashing (V7 graceful-degrade) |
| T-28-08-SC | Tampering | npm/pip/cargo installs | n/a | No package installs — pure Kotlin/Compose |
</threat_model>

<verification>
- `:app:compileDebugKotlin` BUILD SUCCESSFUL (seedTheme reactive fix)
- `:app:testDebugUnitTest` FULL suite green (editor rebuild + S/V)
- `:app:assembleDebug` BUILD SUCCESSFUL (dev cycler + previews)
- Grep gates: firstOrNull gone; Color.hsv(hue,sat,value) present; colorToHSV decode; verticalScroll; no rememberCoroutineScope
</verification>

<success_criteria>
The theme editor gains a 2D S/V square giving full color freedom with S/V surviving a re-open (D-16);
it is one dense scroll with inline expansions (D-18); the idle seed read is reactive (D-19); the dev
cycler drag-relocates again (D-20); ThemeScreen stays a thin wrapper; all persistence is writeScope-only;
the editor preview matrix ships and the suite + assembleDebug are green.
</success_criteria>

<output>
Create `.planning/phases/28-system-settings-cluster/28-08-SUMMARY.md` when done.
</output>
