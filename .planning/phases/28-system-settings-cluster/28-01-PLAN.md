---
phase: 28-system-settings-cluster
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/main/java/works/mees/dinghy/designsystem/icons/DinghyIcons.kt
  - tools/verify_ligatures.py
  - app/src/main/java/works/mees/dinghy/designsystem/components/ListRow.kt
  - app/src/main/java/works/mees/dinghy/ui/screen/TokenTextField.kt
  - app/src/main/res/values/strings.xml
autonomous: true
requirements: []
must_haves:
  truths:
    - "Six new D-21 owner-locked glyph tokens are registered in DinghyIcons and resolve in the bundled v2.944 font"
    - "ListRow accepts a dense flag that suspends the U-derived min-height for C6 surfaces"
    - "TokenTextField accepts a dense flag that reduces internal vertical padding"
    - "The icon drift guard (DinghyIconsTest) and ligature gate stay green"
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/designsystem/icons/DinghyIcons.kt"
      provides: "LauncherFineTune + 5 SystemRow* tokens"
      contains: "LauncherFineTune"
    - path: "app/src/main/java/works/mees/dinghy/designsystem/components/ListRow.kt"
      provides: "dense ListRow variant"
      contains: "dense"
  key_links:
    - from: "DinghyIcons.kt new tokens"
      to: "DinghyIcons.all drift-guard list"
      via: "explicit list membership"
      pattern: "LauncherFineTune"
    - from: "tools/verify_ligatures.py NEEDED set"
      to: "bundled material symbols ttf"
      via: "ligature resolution gate"
      pattern: "line_style"
---

<objective>
Lay the icon-registry and dense-component foundation every downstream Phase 28 screen consumes:
register the six new D-21 owner-locked glyph tokens, extend the ligature drift guard, and add the
C6 `dense` flag to `ListRow` and `TokenTextField`.

Purpose: Wave 1 is the foundation wave — the new System page (28-02), the rehomed home-list rows
(28-03), and every dense screen rebuild (28-05/06/07) reference these tokens and the dense
component variants. Registering icons and components first removes the "scavenger hunt" and lets
later plans run in parallel against a stable contract.
Output: Six registered glyph tokens, a green ligature gate, dense `ListRow`/`TokenTextField`
overloads (default-false, zero impact on existing call sites), and the new System-page row strings.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-system-settings-cluster/28-CONTEXT.md
@.planning/phases/28-system-settings-cluster/28-PATTERNS.md
@.planning/phases/28-system-settings-cluster/28-UI-SPEC.md
@docs/ui_design/PREVIEW_AND_TOKENS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register the six D-21 glyph tokens + extend the ligature drift guard</name>
  <files>app/src/main/java/works/mees/dinghy/designsystem/icons/DinghyIcons.kt, tools/verify_ligatures.py</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/designsystem/icons/DinghyIcons.kt (current registry, the `all` list ~line 226-233, existing token style)
    - tools/verify_ligatures.py (the NEEDED set ~line 63 and the de-dup note ~line 80)
    - .planning/phases/28-system-settings-cluster/28-PATTERNS.md (DinghyIcons.kt section — exact token block and the `all`-list + alternate-uniqueness rules)
    - .planning/phases/28-system-settings-cluster/28-CONTEXT.md (D-21 — the complete owner-locked glyph list; NO glyph outside it may be added)
    - docs/ui_design/CLAUDE.md (icon never-auto-pick law)
  </read_first>
  <action>
    Add exactly six new `val` tokens to `DinghyIcons` using the D-21 owner-locked ligatures, each
    with a globally-unique `alternate` string (the drift guard checks uniqueness): `LauncherFineTune`
    = ligature `line_style` (alternate `launcher_fine_tune`); `SystemRowPrinters` = `android_wifi_3_bar_plus`
    (alternate `system_row_printers`); `SystemRowSettings` = `settings` (alternate `system_row_settings`);
    `SystemRowTheme` = `palette` (alternate `system_row_theme`); `SystemRowAbout` = `info` (alternate
    `system_row_about`); `SystemRowPower` = `power_settings_new` (alternate `system_row_power`). Add all
    six to the `DinghyIcons.all` list (the drift guard fails at test time if any is missing). Do NOT add
    any glyph not in this exact list — anything else is STOP-AND-ASK. Extend `tools/verify_ligatures.py`
    NEEDED set with the six new ligature strings (`line_style`, `android_wifi_3_bar_plus`, `settings`,
    `palette`, `info`, `power_settings_new`); note `palette`/`settings`/`info` may already be in NEEDED
    via existing drawer/other tokens — the gate de-dups on the ligature string, so only add strings not
    already present. Do NOT register an Add-printer foot glyph — D-21 assigns none; Printers foot buttons
    stay text-only (28-06 enforces this).
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --tests *DinghyIconsTest --no-daemon" | tr -d '\r'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'LauncherFineTune\|SystemRowPrinters\|SystemRowSettings\|SystemRowTheme\|SystemRowAbout\|SystemRowPower' app/src/main/java/works/mees/dinghy/designsystem/icons/DinghyIcons.kt` returns at least 12 (declaration + `all` membership for each of the 6 tokens)
    - `python3 tools/verify_ligatures.py` (or the project's invocation) prints `missing: []` and exits 0
    - `:app:testDebugUnitTest --tests *DinghyIconsTest` is GREEN (alternate-uniqueness + all-membership drift guard pass)
    - No glyph string outside the D-21 list appears among the new tokens
  </acceptance_criteria>
  <done>Six D-21 tokens registered + in `all`; ligature gate prints `missing: []`; DinghyIconsTest green.</done>
</task>

<task type="auto">
  <name>Task 2: Add the C6 dense flag to ListRow and TokenTextField</name>
  <files>app/src/main/java/works/mees/dinghy/designsystem/components/ListRow.kt, app/src/main/java/works/mees/dinghy/ui/screen/TokenTextField.kt, app/src/main/res/values/strings.xml</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/designsystem/components/ListRow.kt (current signature ~line 91-128 and the `heightIn(min = uDp)` line)
    - app/src/main/java/works/mees/dinghy/ui/screen/TokenTextField.kt (current signature ~line 47 and internal padding)
    - app/src/main/res/values/strings.xml (existing `cd_launcher_*` and `system_row_*` key conventions)
    - .planning/phases/28-system-settings-cluster/28-PATTERNS.md (ListRow.kt + TokenTextField.kt sections — exact dense-flag shape)
    - .planning/phases/28-system-settings-cluster/28-UI-SPEC.md (Spacing Scale: C6 dense row = 10dp vertical padding; Typography 15sp floor)
  </read_first>
  <action>
    Add a `dense: Boolean = false` parameter to `ListRow` (D-09 — the reusable dense jiib ListRow variant;
    default false → existing call sites unchanged). When `dense` is true, suspend `heightIn(min = uDp)` and instead apply fixed
    `padding(vertical = 10.dp)` per the C6 dense-row spec; all other ListRow conventions (leading icon,
    border, fill, selected accent, start-aligned label, end-aligned trailing via weight spacer) stay
    identical. Add a `dense: Boolean = false` parameter to `TokenTextField` (default false → unchanged)
    that reduces the field's internal vertical padding (≈6dp) when true; the numeric-keyboard option and
    all validation behavior are untouched. Add the new System-page row + idle-list strings to
    `strings.xml`: `cd_launcher_fine_tune` ("Fine-Tune"), `system_row_printers` ("Printers"),
    `system_row_settings` ("Settings"), `system_row_theme` ("Theme"), `system_row_sysinfo` ("System Info"),
    `system_row_about` ("About"), `system_row_power` ("Power"), `system_row_power_sub` ("Coming soon"),
    `system_row_label` ("System") — verbatim from the UI-SPEC Copywriting Contract; reuse the existing
    `cd_launcher_temperature`/`cd_launcher_console` keys if present, else add them.
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:compileDebugKotlin --no-daemon" | tr -d '\r'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'dense: Boolean = false' app/src/main/java/works/mees/dinghy/designsystem/components/ListRow.kt` returns 1
    - `grep -c 'dense: Boolean = false' app/src/main/java/works/mees/dinghy/ui/screen/TokenTextField.kt` returns 1
    - `grep -c 'system_row_printers\|system_row_power\|cd_launcher_fine_tune' app/src/main/res/values/strings.xml` returns at least 3
    - `:app:compileDebugKotlin` BUILD SUCCESSFUL (existing ListRow/TokenTextField call sites compile unchanged — default param)
    - No bare `.sp` literal added; no row text below a 15sp base introduced
  </acceptance_criteria>
  <done>ListRow + TokenTextField each carry a default-false `dense` flag; new strings present; debug compiles.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan adds no network surface; icon/component registration is internal static code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-01-01 | Tampering | DinghyIcons drift | mitigate | DinghyIconsTest enforces unique alternates + all-membership; ligature gate proves font resolution — both run in this plan's verify |
| T-28-01-SC | Tampering | npm/pip/cargo installs | n/a | No package installs — pure Kotlin/Compose, zero new dependencies (RESEARCH Package Legitimacy Audit: N/A) |
</threat_model>

<verification>
- `:app:testDebugUnitTest --tests *DinghyIconsTest` green
- `tools/verify_ligatures.py` prints `missing: []`
- `:app:compileDebugKotlin` BUILD SUCCESSFUL
</verification>

<success_criteria>
Six D-21 glyph tokens registered and drift-guarded; ligature gate green; ListRow + TokenTextField
carry a default-false `dense` flag with zero impact on existing call sites; new System/idle strings
added.
</success_criteria>

<output>
Create `.planning/phases/28-system-settings-cluster/28-01-SUMMARY.md` when done.
</output>
