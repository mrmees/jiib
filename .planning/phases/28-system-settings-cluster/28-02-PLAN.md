---
phase: 28-system-settings-cluster
plan: 02
type: execute
wave: 2
depends_on: ["28-01"]
files_modified:
  - app/src/main/java/works/mees/dinghy/ui/route/NavDest.kt
  - app/src/main/java/works/mees/dinghy/ui/screen/SystemPageScreen.kt
  - app/src/main/java/works/mees/dinghy/preview/SystemPagePreviews.kt
  - app/src/main/java/works/mees/dinghy/preview/SampleFixtures.kt
  - app/src/test/java/works/mees/dinghy/ui/route/NavDestRoundTripTest.kt
autonomous: true
requirements: []
must_haves:
  truths:
    - "NavDest.System exists, is in knownNavDests, round-trips through serialization, and is NOT in FOOT_GUN_DESTS"
    - "SystemPageScreen renders a static brand-identity Focus capped at 20% height (portrait) / 40% width (landscape) and direct-tap dense nav rows in D-03 order"
    - "The Power row is a stop-tinted, greyed, inert stub with no onClick"
    - "SystemPageScreen ships its full @Preview matrix (6 theme combos + fsLarge + RTL) on a stateless content seam"
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/ui/screen/SystemPageScreen.kt"
      provides: "the System page screen + stateless SystemPageContent seam"
      contains: "SystemPageContent"
    - path: "app/src/main/java/works/mees/dinghy/preview/SystemPagePreviews.kt"
      provides: "6-combo + fsL preview matrix"
      contains: "PreviewBox"
  key_links:
    - from: "SystemPageScreen rows"
      to: "DinghyIcons SystemRow* tokens"
      via: "DinghyIconView(icon = ...)"
      pattern: "SystemRow"
    - from: "NavDest.System"
      to: "knownNavDests"
      via: "list membership + round-trip test"
      pattern: "NavDest.System"
---

<objective>
Create the new `NavDest.System` route and the `SystemPageScreen` — the real System page that
replaces the retired drawer-as-system-hub (D-01). The page is a Focus|Field (no gutter) screen:
a STATIC brand-identity Focus strip (jiib lockup + version + active printer) capped at the
owner-specified ratios (D-02), and direct-tap dense nav rows in Printer-first order (D-03) with
an inert Power stub (D-08).

Purpose: This is the destination the System foot button and the printing shortcut grid both route
to (wired in 28-03). Building the route + screen here, with the new icon tokens from 28-01, gives
28-03 a stable target to wire against.
Output: `NavDest.System` (serialization round-trip tested), `SystemPageScreen` with a stateless
`SystemPageContent` seam, its full `@Preview` matrix, and SampleFixtures additions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-system-settings-cluster/28-CONTEXT.md
@.planning/phases/28-system-settings-cluster/28-PATTERNS.md
@.planning/phases/28-system-settings-cluster/28-UI-SPEC.md
@docs/ui_design/PREVIEW_AND_TOKENS.md
@.claude/skills/sketch-findings-dinghy-display/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add NavDest.System route + extend the round-trip test</name>
  <files>app/src/main/java/works/mees/dinghy/ui/route/NavDest.kt, app/src/test/java/works/mees/dinghy/ui/route/NavDestRoundTripTest.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/route/NavDest.kt (sealed interface members ~line 29-84, knownNavDests, FOOT_GUN_DESTS ~line 123-132, shouldPopToRoot)
    - app/src/test/java/works/mees/dinghy/ui/route/NavDestRoundTripTest.kt (existing round-trip coverage shape)
    - .planning/phases/28-system-settings-cluster/28-PATTERNS.md (NavDest.kt section — exact insert pattern + the FOOT_GUN_DESTS / screenOwnsEstop non-inclusion rules)
  </read_first>
  <action>
    Add `@Serializable data object System : NavDest` to the sealed interface, inserted after
    `NavDest.About`. Add `NavDest.System` to `knownNavDests` (count rises to 23). Do NOT add
    `NavDest.System` to `FOOT_GUN_DESTS` — the System cluster is intentionally mid-print reachable
    (D-06); the `else -> null` fallback in `shouldPopToRoot` already covers it (add a one-line comment
    noting System is intentionally non-foot-gun). Extend `NavDestRoundTripTest` to assert
    `NavDest.System` is in `knownNavDests` and survives serialization round-trip (mirror the existing
    per-member assertions).
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --tests *NavDestRoundTripTest --tests *FootGunDestsTest --no-daemon" | tr -d '\r'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'data object System' app/src/main/java/works/mees/dinghy/ui/route/NavDest.kt` returns 1
    - `grep -v '^#' app/src/main/java/works/mees/dinghy/ui/route/NavDest.kt | grep -c 'NavDest.System'` shows membership in knownNavDests but NOT inside the FOOT_GUN_DESTS list body
    - `:app:testDebugUnitTest --tests *NavDestRoundTripTest --tests *FootGunDestsTest` GREEN (System round-trips; System absent from foot-gun set)
  </acceptance_criteria>
  <done>NavDest.System added + in knownNavDests, absent from FOOT_GUN_DESTS, round-trip test green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build SystemPageScreen (static Focus + direct-tap dense rows + inert Power stub)</name>
  <files>app/src/main/java/works/mees/dinghy/ui/screen/SystemPageScreen.kt</files>
  <behavior>
    - Focus is STATIC: jiib lockup (brandTint accent-tinted), version string (BuildConfig.VERSION_NAME, Geist Mono 15sp), active printer name (Geist 17sp) — never live telemetry/connection state
    - Focus is ratio-capped: 20% of HEIGHT in portrait, 40% of WIDTH in landscape, computed from BoxWithConstraints orientation
    - Field rows in D-03 order: Printers → Settings → Theme → System Info → About → Power(stub)
    - Rows 1-5 are direct-tap: onClick navigates immediately to the destination (no selected state — nav, not a picker)
    - Power row (#6): no onClick, stop-tinted at alpha 0.38, greyed, with a "Coming soon" sublabel — visually present but inert (D-08)
    - gutter = null; FootButtonBar carries Back only (Neutral); SystemPageScreen does NOT render its own FloatingEStop (shell owns it)
  </behavior>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/calibration/CalibrationHubScreen.kt (the closest analog — BoxWithConstraints + rememberUnitGrid, ScreenScaffold gutter=null, ListBlock+ListRow, FootButtonBar Back-only, the import set)
    - app/src/main/java/works/mees/dinghy/designsystem/icons/DinghyIcons.kt (the SystemRow* + SysInfoTile tokens registered in 28-01)
    - .planning/phases/28-system-settings-cluster/28-PATTERNS.md (SystemPageScreen.kt section — Focus sizing, direct-tap row, Power stub patterns verbatim)
    - .planning/phases/28-system-settings-cluster/28-UI-SPEC.md (System Page layout contract: Focus content, field row table with glyph tokens + intents, Back foot)
    - app/src/main/java/works/mees/dinghy/ui/screen/AboutScreen.kt (brandTint lockup drawable usage — reuse the 18.2 brand asset for the Focus)
  </read_first>
  <action>
    Create `SystemPageScreen(container, onNavigate, onBack)` plus a stateless `SystemPageContent`
    seam (parameters: activePrinterName, versionName, onNavigate, onBack) so previews drive it with
    no Moonraker. Use `BoxWithConstraints` + `rememberUnitGrid(minOf(maxWidth, maxHeight))`; compute
    `landscape = maxWidth > maxHeight` and set the Focus grow ratio to 0.4 (landscape WIDTH) / 0.2
    (portrait HEIGHT) per D-02. Focus = the static brand strip (jiib lockup via brandTint, version via
    `BuildConfig.VERSION_NAME` in Geist Mono 15sp, active printer in Geist 17sp) — NO connection state,
    NO live data. Field = a `ListBlock` of dense `ListRow(dense = true, selected = false, onClick = ...)`
    rows in D-03 order using the registered tokens: Printers→SystemRowPrinters (→ onNavigate(NavDest.Devices)),
    Settings→SystemRowSettings (→ NavDest.Settings), Theme→SystemRowTheme (→ NavDest.Theme), System
    Info→SysInfoTile (→ NavDest.SystemInfo route),
    About→SystemRowAbout (→ NavDest.About). The Power row (#6) is NOT a clickable ListRow: render it as
    a plain Row with no onClick, `SystemRowPower` icon + label both `t.stop.copy(alpha = 0.38f)` and a
    `system_row_power_sub` "Coming soon" trailing sublabel in `t.text2.copy(alpha = 0.38f)`. All text via
    `fsSp(baseSp, t.fs).sp` with base ≥15f. `ScreenScaffold(gutter = null)`; FootButtonBar holds a single
    Back `OutlinedControl(Intent.Neutral)`. Do NOT render FloatingEStop and do NOT add the screen to
    `screenOwnsEstop` (shell-level e-stop applies — D-06). Resolve the exact `NavDest.*` route names for
    Settings / System Info from NavDest.kt rather than assuming.
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:compileDebugKotlin --no-daemon" | tr -d '\r'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'fun SystemPageContent' app/src/main/java/works/mees/dinghy/ui/screen/SystemPageScreen.kt` returns 1 (stateless seam exists)
    - `grep -c 'gutter = null' app/src/main/java/works/mees/dinghy/ui/screen/SystemPageScreen.kt` returns 1
    - `grep -c 'BuildConfig.VERSION_NAME' app/src/main/java/works/mees/dinghy/ui/screen/SystemPageScreen.kt` returns 1 (static version, not live data)
    - `grep -c 'FloatingEStop' app/src/main/java/works/mees/dinghy/ui/screen/SystemPageScreen.kt` returns 0 (shell owns e-stop)
    - `grep -c 'rememberCoroutineScope' app/src/main/java/works/mees/dinghy/ui/screen/SystemPageScreen.kt` returns 0
    - Power row has no `onClick`/`clickable`; `grep '0.38' SystemPageScreen.kt` shows the greyed stub
    - `:app:compileDebugKotlin` BUILD SUCCESSFUL
  </acceptance_criteria>
  <done>SystemPageScreen renders static ratio-capped Focus + D-03 direct-tap rows + inert Power stub; compiles.</done>
</task>

<task type="auto">
  <name>Task 3: SystemPage preview matrix + SampleFixtures</name>
  <files>app/src/main/java/works/mees/dinghy/preview/SystemPagePreviews.kt, app/src/main/java/works/mees/dinghy/preview/SampleFixtures.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/preview/CalibrationPreviews.kt (the PreviewBox + 6-combo + fsLarge + RTL idiom, NEXUS7 device qualifiers)
    - app/src/main/java/works/mees/dinghy/preview/SampleFixtures.kt (existing fixture style — pure immutable data)
    - docs/ui_design/PREVIEW_AND_TOKENS.md (the minimized-matrix shape: *Matrix + 6 theme wrappers + *FsLargeOverflow + *RtlSpotCheck + pseudolocale)
    - .planning/phases/28-system-settings-cluster/28-UI-SPEC.md (Preview Contract — SystemPage interesting axis = active printer present/absent)
  </read_first>
  <action>
    Add `SystemPagePreviews.kt` driving the stateless `SystemPageContent` seam: the minimized matrix
    (one panel per the 6 theme combos colorfulDark/Light, simpleDark/Light, highContrastDark/Light via
    `PreviewBox(<combo>)`), a `*FsLargeOverflow` panel via `PreviewBox(fsLargeSeed)` proving no clipping
    at L, a `*RtlSpotCheck` panel, and a pseudolocale panel (`locale = "en-XA"`), portrait + a landscape
    spot panel to prove the 40%-width Focus. Use `stringResource` for all labels (no hardcoded strings).
    Add `SampleFixtures.systemPageState` (version = "0.1.0-debug", activePrinterName = "Ender 5 Plus")
    if a fixture is convenient, or pass literals directly. No Moonraker, no coroutines in any preview.
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:assembleDebug --no-daemon" | tr -d '\r'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'PreviewBox' app/src/main/java/works/mees/dinghy/preview/SystemPagePreviews.kt` returns at least 6 (the 6 theme combos)
    - `grep -c 'fsLargeSeed\|FsLarge' app/src/main/java/works/mees/dinghy/preview/SystemPagePreviews.kt` returns at least 1
    - `grep -c 'Rtl\|LayoutDirection' app/src/main/java/works/mees/dinghy/preview/SystemPagePreviews.kt` returns at least 1
    - `:app:assembleDebug` BUILD SUCCESSFUL (preview composables compile)
  </acceptance_criteria>
  <done>SystemPage 6-combo + fsL + RTL + pseudolocale preview matrix present; assembleDebug green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| System page Focus → display | Read-only static identity (BuildConfig version + activeName); no untrusted input rendered |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-02-01 | Information Disclosure | System page Focus | accept | Focus shows only app version + printer display name — no API key, no host secret; activeName is a user-set display label |
| T-28-02-02 | Denial of Service (e-stop hidden) | FloatingEStop suppression | mitigate | SystemPageScreen does NOT add itself to screenOwnsEstop and does NOT render its own e-stop — shell-level e-stop stays reachable mid-print (Pitfall 6 guard, verified by grep gate) |
| T-28-02-SC | Tampering | npm/pip/cargo installs | n/a | No package installs — pure Kotlin/Compose |
</threat_model>

<verification>
- `:app:testDebugUnitTest --tests *NavDestRoundTripTest --tests *FootGunDestsTest` green
- `:app:assembleDebug` BUILD SUCCESSFUL (screen + previews compile)
- Grep gates: no FloatingEStop in SystemPageScreen, no rememberCoroutineScope, static version source
</verification>

<success_criteria>
NavDest.System exists, round-trips, and is foot-gun-safe; SystemPageScreen renders the static
ratio-capped brand Focus + D-03 direct-tap dense rows + inert greyed Power stub, with the shell
owning e-stop; full preview matrix ships and assembleDebug is green.
</success_criteria>

<output>
Create `.planning/phases/28-system-settings-cluster/28-02-SUMMARY.md` when done.
</output>
