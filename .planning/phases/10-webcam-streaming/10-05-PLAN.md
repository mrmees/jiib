---
phase: 10-webcam-streaming
plan: 05
type: execute
wave: 3
depends_on: ["10-02"]
files_modified:
  - app/src/main/java/works/mees/dinghy/render/WebcamView.kt
  - app/src/main/java/works/mees/dinghy/render/WebcamViewHost.kt
autonomous: true
requirements: [CAM-01]

must_haves:
  truths:
    - "The live frame surface is a classic View (ThemeableView) hosted via AndroidView — the GraphView/GraphViewHost ADR-0001 hybrid pattern for a high-churn raster"
    - "Frames draw pixel-square / never-stretched in BOTH portrait and landscape (Matrix scale-to-fit, not fill), with the cam's flip_*/rotation applied (camera_feed note)"
    - "The feed is clipped to a rounded framing cutout (edge loss acceptable for visual continuity — camera_feed note)"
    - "A dark/light theme flip recolors the frame chrome/overlays via applyTokens with NO view recreation"
    - "All Paint/Path/Matrix are pre-allocated in init — never allocated in onDraw (Adreno-320 GC-churn guard)"
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/render/WebcamView.kt"
      provides: "ThemeableView raster surface: pixel-square frame + badge/overlay/dead-end chrome"
      contains: "ThemeableView"
    - path: "app/src/main/java/works/mees/dinghy/render/WebcamViewHost.kt"
      provides: "AndroidView factory-once/update-push host"
      contains: "AndroidView"
  key_links:
    - from: "WebcamViewHost (AndroidView update)"
      to: "WebcamView.applyTokens + setFrame"
      via: "factory-once / update-push (GraphViewHost pattern)"
      pattern: "factory =|update ="
    - from: "WebcamView.onDraw"
      to: "frame Bitmap via Matrix"
      via: "pixel-square scale + flip/rotation, clip rounded cutout"
      pattern: "Matrix|clipPath|RoundRect"
---

<objective>
The live raster surface and its Compose host — the exact ADR-0001 hybrid pattern (`GraphView`/`GraphViewHost`/`ThemeableView`) for a high-churn webcam frame. `WebcamView` draws the current frame pixel-square / never-stretched in both orientations with the cam's flip/rotation, clipped to a rounded framing cutout, plus the token-colored chrome: the snapshot badge (D-03), the dimmed last-frame + "Reconnecting…" overlay (D-11), the cycle overlay (camera_feed: burst icon + cam name), and the rung-3 dead-end card (D-04). `WebcamViewHost` hosts it via `AndroidView` factory-once/update-push.

Purpose: This is the only new render surface this phase. Following GraphView exactly (pre-allocate ALL Paint/Path/Matrix in init, never in onDraw) keeps the Adreno-320 fill-rate budget; the `ThemeableView` push-tokens seam means a theme flip recolors chrome without recreating the View. The camera_feed note is the verbatim design contract (no hi-fi mockup) — never-stretch/pixel-square and the rounded cutout are load-bearing owner requirements.
Output: `render/WebcamView.kt`, `render/WebcamViewHost.kt`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-webcam-streaming/10-RESEARCH.md
@.planning/phases/10-webcam-streaming/10-PATTERNS.md
@docs/view_specific_notes/camera_feed
@docs/ui_design/LAYOUT.md
@docs/ui_design/THEMING.md
@app/src/main/java/works/mees/dinghy/render/GraphViewHost.kt
@app/src/main/java/works/mees/dinghy/theme/views/ThemeableView.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: WebcamView — pixel-square frame + token chrome (badge / reconnect / cycle / dead-end)</name>
  <files>app/src/main/java/works/mees/dinghy/render/WebcamView.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/render/GraphView.kt (the `class … : View(context), ThemeableView` contract; ALL Paint/Path/Matrix pre-allocated in init, NEVER in onDraw; setData→invalidate repaint trigger — the discipline to copy)
    - app/src/main/java/works/mees/dinghy/theme/views/ThemeableView.kt:21-24 (the applyTokens contract to implement)
    - docs/view_specific_notes/camera_feed (the LOCKED design contract: never-stretch/pixel-square both orientations; rounded framing cutout, edge loss OK; in-feed cycle overlay = burst icon + cam name; dead-end card)
    - docs/ui_design/THEMING.md (semantic tokens — frame/badge/overlay colors; back-intent = red; never raw color)
    - .planning/phases/10-webcam-streaming/10-RESEARCH.md (§ D-03 badge, § D-04 dead-end card naming the service + NO browser button, § D-11 dimmed last-frame + "Reconnecting…")
    - .planning/phases/10-webcam-streaming/10-PATTERNS.md (§ render/WebcamView.kt — the GraphView analog + setFrame/postInvalidate)
    - app/src/main/java/works/mees/dinghy/state/WebcamModels.kt (the selected-cam VM/Rung the view renders chrome from)
  </read_first>
  <action>
    Implement `class WebcamView(context: Context) : View(context), ThemeableView`. Pre-allocate in init: a `Matrix` for the frame transform, a `Path`/`RectF` for the rounded cutout clip, and all `Paint`s (badge bg/text, reconnect dim+text, cycle-overlay icon/text, dead-end card bg/text) — NEVER allocate in `onDraw` (Pitfall 4 GC-churn trap, the GraphView law). Expose `setFrame(bitmap: Bitmap?)` + `postInvalidate()` (called from the decoder/poller thread), and setters for the chrome state: `mode` (Live / SnapshotFallback / Reconnecting / DeadEnd), `camName`, `serviceName` (for the dead-end card text), `multiCam` (whether to show the cycle overlay). In `onDraw`: compute a pixel-square scale-to-FIT (never fill/stretch — `min(viewW/frameW, viewH/frameH)` via the Matrix, centered) applying the cam's `flip_horizontal`/`flip_vertical`/`rotation`; clip to the rounded cutout (`clipPath`/`RoundRect`, edge loss acceptable per the note); draw the frame; then the active chrome — SnapshotFallback → a small persistent corner badge "Snapshot ~2fps" (D-03, NOT a toast); Reconnecting → keep the last frame DIMMED + a subtle "Reconnecting…" overlay (D-11, do not black out); DeadEnd → a card INSIDE the cutout naming the detected service ("This camera uses WebRTC — not supported yet"), NO open-in-browser button (D-04); multiCam in full-focus → the in-feed cycle overlay (burst icon + cam name, camera_feed note). Implement `applyTokens(t)` to stash the role colors + invalidate (theme flip recolors chrome, no recreation). ALL colors via tokens (THEME-01 — no raw literal); back-intent red where a back affordance appears (the gutter Back lives on the screen, plan 10-06).
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:compileReleaseKotlin --no-daemon" 2>&1 | tr -d '\r' | tail -12</automated>
  </verify>
  <done>`WebcamView` compiles as a `ThemeableView`; all Paint/Path/Matrix pre-allocated in init; onDraw does pixel-square scale-to-fit + flip/rotation + rounded clip; badge/reconnect/cycle/dead-end chrome routed through tokens; no raw color literal.</done>
</task>

<task type="auto">
  <name>Task 2: WebcamViewHost — AndroidView factory-once/update-push</name>
  <files>app/src/main/java/works/mees/dinghy/render/WebcamViewHost.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/render/GraphViewHost.kt:27-43 (the AndroidView factory-once/update-push host to copy verbatim in shape)
    - app/src/main/java/works/mees/dinghy/theme/views/ThemeableView.kt
    - .planning/phases/10-webcam-streaming/10-PATTERNS.md (§ render/WebcamViewHost.kt — the GraphViewHost analog)
  </read_first>
  <action>
    Add `@Composable fun WebcamViewHost(tokens, frame: Bitmap?, mode, camName, serviceName, multiCam, modifier)` mirroring `GraphViewHost`: `factory = { ctx -> WebcamView(ctx) }` (created ONCE), `update = { view -> view.applyTokens(tokens); view.setFrame(frame); view.setChrome(mode, camName, serviceName, multiCam) }` (push-tokens + latest frame + chrome state → invalidate; a theme flip recolors chrome without recreating the View). Keep the host thin — no decode/poll logic here (that is the holder/services); the host only pushes state into the View. Frame hand-off itself comes via a flow the screen collects (plan 10-06) and passes in as `frame`, OR the View pulls from the decoder's drop-behind seam — keep the host's contract a simple `frame: Bitmap?` parameter so it stays the GraphViewHost shape.
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:compileReleaseKotlin --no-daemon" 2>&1 | tr -d '\r' | tail -12</automated>
  </verify>
  <done>`WebcamViewHost` compiles; factory runs once, update pushes tokens+frame+chrome; mirrors GraphViewHost; no decode logic in the host.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| decoded Bitmap → View | The frame is already-decoded (the decoder bounds it); the View only rasters it. The only risk is fill-rate/alloc, not input — pre-allocate to protect the Adreno-320 floor. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-11 | Denial of Service (fill-rate) | WebcamView.onDraw | mitigate | All Paint/Path/Matrix pre-allocated in init (GraphView law); pixel-square scale-to-fit, no per-frame alloc — the on-device perf gate (plan 10-08) pins it. |
| T-10-12 | Info Disclosure | dead-end card text | accept | The card names the detected `service` (e.g. "webrtc-mediamtx") — a non-secret config string; the tokened URL is NEVER drawn (only the service name). No mitigation needed beyond not drawing the URL. |
| T-10-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — no installs (RESEARCH § Package Legitimacy Audit: N/A); `libs.versions.toml` unchanged. |
</threat_model>

<verification>
- `:app:compileReleaseKotlin` SUCCESSFUL.
- Token-purity: no raw color literal in `render/WebcamView.kt` (all via tokens — THEME-01).
- All Paint/Path/Matrix allocated in init, none in onDraw.
</verification>

<success_criteria>
- `WebcamView` is a `ThemeableView` drawing pixel-square never-stretched frames + flip/rotation + rounded cutout in both orientations, with token chrome (badge/reconnect/cycle/dead-end).
- `WebcamViewHost` hosts it factory-once/update-push (GraphViewHost shape).
</success_criteria>

<output>
Create `.planning/phases/10-webcam-streaming/10-05-SUMMARY.md` when done.
</output>
