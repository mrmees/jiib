---
phase: 10-webcam-streaming
plan: 06
type: execute
wave: 4
depends_on: ["10-02", "10-03", "10-04", "10-05"]
files_modified:
  - app/src/main/java/works/mees/dinghy/ui/webcam/WebcamPrefs.kt
  - app/src/main/java/works/mees/dinghy/ui/webcam/WebcamHolder.kt
  - app/src/main/java/works/mees/dinghy/ui/webcam/WebcamScreen.kt
  - app/src/main/java/works/mees/dinghy/di/AppContainer.kt
  - app/src/main/java/works/mees/dinghy/DinghyApp.kt
  - app/src/test/java/works/mees/dinghy/webcam/WebcamReconnectStateTest.kt
autonomous: true
requirements: [CAM-01]

must_haves:
  truths:
    - "The holder resolves each cam's URLs (D-09), probes for the rung (D-02), and drives the active decoder/poller for the selected cam — implementing the three-rung fallback ladder (D-01): rung-1 MJPEG stream → rung-2 ~2fps snapshot poll → rung-3 unsupported card"
    - "No user-facing tuning knobs this phase — fixed sane defaults only; the Webcam screen exposes no fps/resolution settings UI (D-05)"
    - "Selected cam = the per-printer last-viewed preference (D-10), else the first cam in list order"
    - "On a transient stall the holder enters Reconnecting (keep last frame), retries with 1s→10s backoff while foreground; 401/403 is terminal (rung-3 card) — D-11/D-12"
    - "The holder exposes cancel(); its decoder/poller loops stop on cancel (WR-01 leak-cancel)"
    - "The Webcam screen is single-focus default, aspect-aware Field show/hide, Back-only red gutter, with the cycle overlay in full-focus when multiple cams (camera_feed note)"
    - "Preferred-cam persists per printer in its own webcam.preferences_pb DataStore (D-10)"
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/ui/webcam/WebcamHolder.kt"
      provides: "Orchestration: rung select + decoder/poller lifecycle + reconnect state machine + selected-cam + cancel()"
      contains: "fun cancel"
    - path: "app/src/main/java/works/mees/dinghy/ui/webcam/WebcamScreen.kt"
      provides: "Focus/Field/Gutter webcam screen hosting WebcamViewHost"
    - path: "app/src/main/java/works/mees/dinghy/ui/webcam/WebcamPrefs.kt"
      provides: "Per-printer preferred-cam DataStore (fail-safe)"
      contains: "preferred_cam"
  key_links:
    - from: "WebcamHolder"
      to: "WebcamProbe / MjpegStreamDecoder / SnapshotPoller / WebcamUrl"
      via: "rung select → active loop"
      pattern: "WebcamProbe|MjpegStreamDecoder|SnapshotPoller|resolveWebcamUrl"
    - from: "WebcamHolder.selectedCam"
      to: "WebcamPrefs preferred-cam | first-in-list"
      via: "per-printer DataStore default-pick (D-10)"
      pattern: "preferredCam|first"
---

<objective>
The orchestration layer that turns the enumeration + services + render surface into a working screen: `WebcamHolder` (rung selection via `WebcamProbe`, the active decoder/poller lifecycle, the D-11 reconnect state machine, foreground-only backoff, the D-10 selected-cam, and the WR-01 `cancel()`), `WebcamScreen` (single-focus default + aspect-aware Field + Back-only gutter, hosting `WebcamViewHost`), and `WebcamPrefs` (per-printer preferred-cam DataStore). This plan BUILDS `WebcamHolder` and then REPLACES the COMPILING runtime-RED `WebcamReconnectStateTest` `fail()` scaffold body (from plan 10-01) with the real typed assertions against the now-existing holder state machine, taking it GREEN.

Purpose: This is where the camera_feed design contract becomes a screen and where the holder-owns-long-lived-loops leak hazard (WR-01) is contained. The holder must expose `cancel()` and the shell (plan 10-07) must `DisposableEffect(holder){ onDispose{ holder.cancel() } }` it — made an explicit acceptance criterion here. Selected-cam defaults to the per-printer last-viewed (D-10), else first-in-list; the page-visible lifecycle binding itself is wired at the shell (plan 10-07), but the holder exposes the start/stop surface for it.
Output: `ui/webcam/WebcamPrefs.kt`, `ui/webcam/WebcamHolder.kt`, `ui/webcam/WebcamScreen.kt`, `AppContainer`/`DinghyApp` prefs wiring (+ the replaced-and-GREEN `WebcamReconnectStateTest`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-webcam-streaming/10-RESEARCH.md
@.planning/phases/10-webcam-streaming/10-PATTERNS.md
@docs/view_specific_notes/camera_feed
@docs/ui_design/LAYOUT.md
@app/src/main/java/works/mees/dinghy/ui/move/MoveHolder.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Per-printer preferred-cam DataStore (WebcamPrefs.kt) + AppContainer/DinghyApp wiring</name>
  <files>app/src/main/java/works/mees/dinghy/ui/webcam/WebcamPrefs.kt, app/src/main/java/works/mees/dinghy/di/AppContainer.kt, app/src/main/java/works/mees/dinghy/DinghyApp.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/macros/MacroPrefs.kt:20-79 (the injected-DataStore + fail-safe-on-IOException shape to copy verbatim; the AppContainer construction precedent)
    - app/src/main/java/works/mees/dinghy/theme/ThemePrefs.kt (the dynamic-string-keyed idiom for a per-printer key)
    - app/src/main/java/works/mees/dinghy/di/AppContainer.kt:49-76 (where macroPrefs/connectionStore are constructed — add webcamPrefs the same way), :126 (the derived-flow region)
    - app/src/main/java/works/mees/dinghy/DinghyApp.kt (where the macros.preferences_pb DataStore is created — add a SEPARATE webcam.preferences_pb the same way)
    - .planning/phases/10-webcam-streaming/10-PATTERNS.md (§ ui/webcam/WebcamPrefs.kt — MacroPrefs + ThemePrefs dynamic-key; OWN webcam.preferences_pb)
  </read_first>
  <action>
    Add `class WebcamPrefs(private val dataStore: DataStore<Preferences>)` mirroring `MacroPrefs`: a fail-safe `data` read (`.catch { if (it is IOException) emit(emptyPreferences()) else throw it }`). Key the preferred cam PER PRINTER via the `ThemePrefs` dynamic-key idiom — `stringPreferencesKey("preferred_cam_$host")` from `ConnectionConfig.host` (D-10 "keyed by printer/connection"). Expose `preferredCam(host): Flow<String?>` (the cam uid/name) and `suspend fun setPreferredCam(host, camId)`. Create a SEPARATE `webcam.preferences_pb` DataStore in `DinghyApp` (NOT shared with theme/macros — the MacroPrefs precedent) and construct `val webcamPrefs = WebcamPrefs(webcamDataStore)` in `AppContainer` (it is PROCESS-scoped + connection-independent like macroPrefs — survives reconnects). No "set as default" action — the preference is written as a side-effect of viewing a cam (the holder calls setPreferredCam on selection).
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --tests *WebcamPrefs* --no-daemon" 2>&1 | tr -d '\r' | tail -20; /mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:compileReleaseKotlin --no-daemon" 2>&1 | tr -d '\r' | tail -8</automated>
  </verify>
  <done>`WebcamPrefs` fail-safe per-printer keyed store exists; SEPARATE webcam.preferences_pb wired in DinghyApp; `AppContainer.webcamPrefs` constructed; compiles.</done>
</task>

<task type="auto">
  <name>Task 2: WebcamHolder — rung select + decoder/poller lifecycle + reconnect state machine + cancel() + replace the reconnect scaffold with typed assertions</name>
  <files>app/src/main/java/works/mees/dinghy/ui/webcam/WebcamHolder.kt, app/src/test/java/works/mees/dinghy/webcam/WebcamReconnectStateTest.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/move/MoveHolder.kt:33-73 (the toolkit-agnostic MutableStateFlow→StateFlow holder skeleton; no Compose annotations, host-testable)
    - app/src/main/java/works/mees/dinghy/ui/shell/AppShell.kt:200-210 (the DisposableEffect(holder){ onDispose{ holder.cancel() } } leak-cancel pattern the holder must support)
    - app/src/main/java/works/mees/dinghy/net/Backoff.kt:22-50 (backoffDelay for the 1s→10s foreground-only retry)
    - .planning/phases/10-webcam-streaming/10-RESEARCH.md (§ D-11 reconnect state machine, § D-12 backoff/terminal-401, § Pattern 4 lifecycle, § D-10 default-pick)
    - .planning/phases/10-webcam-streaming/10-PATTERNS.md (§ ui/webcam/WebcamHolder.kt — the MoveHolder skeleton + the holder's owned responsibilities + the WR-01 cancel)
    - app/src/main/java/works/mees/dinghy/state/WebcamModels.kt, app/src/main/java/works/mees/dinghy/net/WebcamUrl.kt, app/src/main/java/works/mees/dinghy/net/WebcamProbe.kt, app/src/main/java/works/mees/dinghy/net/MjpegStreamDecoder.kt, app/src/main/java/works/mees/dinghy/net/SnapshotPoller.kt (the pieces it orchestrates)
    - app/src/test/java/works/mees/dinghy/webcam/WebcamReconnectStateTest.kt (the COMPILING runtime-RED `fail()` scaffold from plan 10-01 — this task REPLACES its body with the real typed assertions against the now-existing `WebcamHolder` reconnect state machine)
  </read_first>
  <action>
    Implement `WebcamHolder(scope, webcams: StateFlow<List<Webcam>>, webcamPrefs, host, sharedClient, …)` as a toolkit-agnostic StateFlow holder (MoveHolder skeleton — `_vm: MutableStateFlow<WebcamVm>`). It owns: (1) SELECTED-CAM resolution — on a cam list change, pick `webcamPrefs.preferredCam(host)` if present-and-still-listed, else the first cam in list order (D-10); on a user cycle/select it updates the selection AND `setPreferredCam` (D-10 last-viewed). (2) URL RESOLUTION — `resolveWebcamUrl(stream/snapshot, cfg)` (D-09). (3) RUNG SELECT + active loop — call `WebcamProbe.probe(...)`; Rung.Mjpeg → run `MjpegStreamDecoder`; Rung.Snapshot → run `SnapshotPoller`; Rung.Unsupported → emit the dead-end VM (D-04) naming the service. (4) The D-11 RECONNECT STATE MACHINE — on a transient stall/drop, set Reconnecting (keep the last good frame), retry via `backoffDelay` (1s→~10s cap) FOREGROUND-ONLY; a 401/403 is TERMINAL → dead-end card (no spin, A4). (5) FRAME hand-off — collect the decoder/poller's drop-behind frames into the VM (latest-wins). (6) A `start(camSelection)` / `stop()` surface for the page-visible lifecycle (the shell binds it, plan 10-07) AND a `fun cancel()` that cancels ALL owned decode/poll/retry coroutines — the WR-01 leak-cancel the shell's `DisposableEffect(holder){ onDispose{ holder.cancel() } }` calls on spine rebuild. Expose `WebcamVm` (selected cam, frame, mode = Live/SnapshotFallback/Reconnecting/DeadEnd, serviceName, multiCam, cam list for the Field). Plain Kotlin (no Compose) so the reconnect/backoff/terminal logic is host-unit-testable.
    Then REPLACE the `fail()` body of the `WebcamReconnectStateTest` plan-10-01 scaffold with the real typed assertions against the now-existing `WebcamHolder` reconnect state machine (transient stall → Reconnecting keeping last frame, 1s→10s foreground-only backoff, 401/403 → terminal dead-end no-spin, cancel() stops all loops) and confirm GREEN. (`WebcamBackoffTest` is already GREEN from plan 10-04's poller side; this plan exercises the holder-state-machine side via `WebcamReconnectStateTest`.)
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --tests *WebcamReconnectStateTest* --tests *WebcamBackoff* --tests *WebcamHolder* --no-daemon" 2>&1 | tr -d '\r' | tail -25</automated>
  </verify>
  <done>The `WebcamReconnectStateTest` scaffold body is REPLACED with real typed assertions and is GREEN (with `WebcamBackoffTest` from 10-04); holder selects per-printer preferred-or-first cam, probes+routes the rung, drives the active loop, enters Reconnecting on transient stall with 1s→10s backoff, treats 401/403 terminal, exposes `cancel()` that stops all loops.</done>
</task>

<task type="auto">
  <name>Task 3: WebcamScreen — single-focus + aspect-aware Field + Back-only gutter</name>
  <files>app/src/main/java/works/mees/dinghy/ui/webcam/WebcamScreen.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/move/MoveScreen.kt (a panel + back-gutter wired with onBack, hosted in AppShell — the ScreenScaffold Focus/Field/Gutter usage to mirror)
    - app/src/main/java/works/mees/dinghy/ui/shell/AppShell.kt:300-304 (how MoveScreen is wired with onBack — the exact gutter contract)
    - docs/view_specific_notes/camera_feed (the LOCKED contract: single-focus default; aspect-aware Field show/hide vs device orientation — landscape device+landscape feed → no Field, landscape device+portrait feed → show Field; Back-only gutter; in-feed cycle overlay in full-focus when multiple cams; selected cam expanded in the Field to show moonraker info)
    - docs/ui_design/LAYOUT.md (Focus/Field/Gutter grammar — portrait stacks, landscape Focus|Field 50/50 + full-width gutter; ratio-only sizing)
    - .planning/phases/10-webcam-streaming/10-PATTERNS.md (§ ui/webcam/WebcamScreen.kt — MoveScreen + ScreenScaffold + camera_feed contract)
    - app/src/main/java/works/mees/dinghy/render/WebcamViewHost.kt (the host this screen embeds in the Focus region)
  </read_first>
  <action>
    Implement `@Composable fun WebcamScreen(holder, tokens, onBack, …)` on `ScreenScaffold` (Focus/Field/Gutter, LAYOUT.md). FOCUS = the `WebcamViewHost` (the live frame, pixel-square, rounded cutout). FIELD = the cam-list picker, shown ONLY when it makes sense vs device orientation AND feed aspect (camera_feed rule: detect the feed ratio from the selected cam's `aspect_ratio`/decoded frame; in landscape-device+landscape-feed hide the Field, in landscape-device+portrait-feed show it; in portrait stack per LAYOUT.md) — when shown, list the cams with the selected one expanded to show its moonraker info (name/service/resolution). GUTTER = Back ONLY (red intent, THEME-04 back=red), wired `onBack = onBack` exactly as MoveScreen. In FULL-FOCUS mode (no Field) with multiple cams, the cycle overlay (burst icon + cam name) is drawn IN the feed (WebcamView chrome) and tapping it cycles `holder.cycleCam()`. The dead-end card (D-04) and snapshot badge (D-03) are WebcamView chrome inside the cutout — the screen just feeds the VM mode through `WebcamViewHost`. Collect the holder VM via `collectAsStateWithLifecycle`. Do NOT add any keyboard (no alphanumeric input on this control surface).
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:compileReleaseKotlin --no-daemon" 2>&1 | tr -d '\r' | tail -10</automated>
  </verify>
  <done>`WebcamScreen` compiles on ScreenScaffold: single-focus default, aspect-aware Field show/hide, Back-only red gutter (onBack wired like MoveScreen), full-focus cycle overlay, hosts WebcamViewHost; no keyboard.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| holder loops → FGS | The holder owns long-lived decode/poll/retry coroutines — they must stop on background/nav/spine-rebuild or they hold the FGS awake + leak (WR-01). |
| preferred-cam pref → DataStore | Untrusted/odd cam name persisted; a corrupt store must fail safe. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-09 | DoS / battery | WebcamHolder owned loops | mitigate | `cancel()` stops all decode/poll/retry; the shell `DisposableEffect(holder){onDispose{cancel()}}` (plan 10-07) fires on spine rebuild — no wedged loop holds the FGS (WR-01 leak lesson). |
| T-10-10 | DoS (self-DoS SBC) | reconnect state machine | mitigate | 401/403 terminal → dead-end card (no spin); transient stall backoff 1s→10s foreground-only (D-12) — `WebcamReconnectStateTest`/`WebcamBackoffTest` prove it. |
| T-10-13 | Tampering | WebcamPrefs corrupt store | mitigate | Fail-safe `.catch { IOException → emptyPreferences() }` (MacroPrefs precedent) — a corrupt store yields no preference (falls to first-in-list), never crashes. |
| T-10-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — no installs (RESEARCH § Package Legitimacy Audit: N/A); `libs.versions.toml` unchanged. |
</threat_model>

<verification>
- `WebcamReconnectStateTest`, `WebcamBackoffTest`, `WebcamPrefs` (+ holder) GREEN; `:app:compileReleaseKotlin` SUCCESSFUL. (The `WebcamReconnectStateTest` scaffold body is replaced with typed assertions in this plan.)
- Holder exposes `cancel()` (grep `fun cancel`); the WR-01 wiring is consumed in plan 10-07.
- No keyboard on WebcamScreen; Back-only red gutter.
</verification>

<success_criteria>
- Holder: per-printer preferred-or-first cam, rung select, active loop, D-11 reconnect, D-12 backoff/terminal-401, `cancel()`.
- Screen: single-focus + aspect-aware Field + Back-only gutter + cycle overlay.
- Per-printer preferred-cam persists in its own DataStore.
- The `WebcamReconnectStateTest` plan-10-01 `fail()` scaffold is replaced with typed assertions and GREEN.
</success_criteria>

<output>
Create `.planning/phases/10-webcam-streaming/10-06-SUMMARY.md` when done.
</output>
