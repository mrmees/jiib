---
phase: 10-webcam-streaming
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - app/src/main/java/works/mees/dinghy/state/WebcamModels.kt
  - app/src/main/java/works/mees/dinghy/net/WebcamUrl.kt
autonomous: true
requirements: [CAM-01]

must_haves:
  truths:
    - "Both E5+E3 goldens parse into tolerant Webcam models (all 16 fields; blank service/missing snapshot/odd-unicode name never crash)"
    - "Relative stream/snapshot URLs resolve against the configured Moonraker host (D-09)"
    - "localhost/127.0.0.1/0.0.0.0/::1 hosts are rewritten to the configured Moonraker host, preserving port/path/?token= (D-09)"
    - "Rung selection maps Content-Type (multipart=1, image/jpeg or snapshot-present=2, else=3) — service is a hint only (D-02)"
    - "A resolved webcam URL is never logged unredacted — redact(?token=) before any log/crash surface (Security V7)"
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/state/WebcamModels.kt"
      provides: "Tolerant @Serializable webcam model + rung enum + resolved-cam VM"
      contains: "ignoreUnknownKeys"
    - path: "app/src/main/java/works/mees/dinghy/net/WebcamUrl.kt"
      provides: "D-09 URL resolver/rewriter + token redaction"
      contains: "redact"
  key_links:
    - from: "WebcamUrl.resolve"
      to: "ConnectionConfig.httpBase / host"
      via: "HttpUrl.resolve + host rewrite"
      pattern: "toHttpUrl\\(\\)\\.resolve|newBuilder\\(\\)\\.host"
---

<objective>
The pure, host-testable core of the webcam feature: the tolerant `/server/webcams/list` models, the D-09 URL resolver/rewriter (the one genuinely hand-rolled helper), and the D-02 rung-selection logic. No I/O, no Android, no coroutines — all unit-provable, turning the WebcamListParseTest / WebcamUrlResolverTest / WebcamRungSelectTest scaffolds from plan 10-01 GREEN.

Purpose: Every untrusted `/server/webcams/list` field is tolerated (Security V5); the #1 "works in Mainsail, blank here" gotcha (relative + localhost URLs) is killed at the resolver (D-09); the tokened E3 URL survives resolution AND is redacted before logging (Security V7). The rung selector encodes D-02's "Content-Type is source of truth, service is only a hint" so the WebRTC `service` field can never mislead the decoder.
Output: `state/WebcamModels.kt`, `net/WebcamUrl.kt`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-webcam-streaming/10-RESEARCH.md
@.planning/phases/10-webcam-streaming/10-PATTERNS.md
@app/src/main/java/works/mees/dinghy/auth/MoonrakerAuth.kt
@app/src/main/java/works/mees/dinghy/config/ConnectionConfig.kt
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tolerant webcam models + rung-selection logic (WebcamModels.kt)</name>
  <files>app/src/main/java/works/mees/dinghy/state/WebcamModels.kt</files>
  <behavior>
    - Parsing webcams_list_e5.json → a list of 2 Webcam models; webcams_list_e3.json → its multi-cam list; every documented field is read; unknown keys ignored.
    - A webcam with blank `service`, missing `snapshot_url`, empty `extra_data`, or the odd-unicode E3 name decodes without throwing.
    - A truncated/garbage `/server/webcams/list` payload → empty list (fail-safe), never an exception.
    - rungFor(contentType, snapshotPresent): `multipart/x-mixed-replace` (any case/params) → Rung.Mjpeg(1); a 2xx `image/jpeg` OR (stream not multipart but snapshot_url present) → Rung.Snapshot(2); 401/403/404/neither-usable → Rung.Unsupported(3). `service` is NOT consulted for the decode decision (hint only, D-02).
  </behavior>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/move/MoveHolder.kt:75-88 (the all-nullable, never-fabricated data-class + VM idiom to copy)
    - app/src/main/java/works/mees/dinghy/auth/MoonrakerAuth.kt:78-80 (the spine's tolerant `MoonrakerJson.parseToJsonElement` decode discipline)
    - .planning/phases/10-webcam-streaming/10-RESEARCH.md (§ "Full Moonraker field set" — model defensively; § Pattern 1 — the rung/Content-Type branch)
    - .planning/phases/10-webcam-streaming/10-PATTERNS.md (§ state/WebcamModels.kt — the MoveVm + MoonrakerJson assignment)
    - app/src/test/java/works/mees/dinghy/webcam/WebcamListParseTest.kt, WebcamRungSelectTest.kt (the RED scaffolds this turns green)
  </read_first>
  <action>
    Define a `@Serializable data class Webcam` with ALL 16 documented fields, every field nullable-or-defaulted and never fabricated: `name`, `location`, `service`, `enabled`, `icon`, `targetFps`/`target_fps` (use `@SerialName` for snake_case), `targetFpsIdle`, `streamUrl`, `snapshotUrl`, `flipHorizontal`, `flipVertical`, `rotation` (Int ∈ {0,90,180,270}, tolerate others → 0), `aspectRatio` (a `"W:H"` String), `extraData`, `source`, `uid`. Decode `result.webcams[]` using the spine's existing tolerant JSON config (the same instance `MoonrakerAuth` uses — `ignoreUnknownKeys`, lenient, fail to empty list on a malformed payload, NEVER throw — Security V5). Provide a `parseWebcamsList(JsonElement): List<Webcam>` that walks `result.webcams` defensively. Add a `Rung` sealed type / enum (`Mjpeg`, `Snapshot`, `Unsupported`) and a pure `rungFor(contentType: String?, httpCode: Int, snapshotUrlPresent: Boolean): Rung` implementing D-02 exactly (Content-Type is truth; 401/403/404 → Unsupported; `service` never consulted). Also model the resolved/selected-cam VM state here (mirror MoveVm's "resolved VM for the screen" role) — the selected `Webcam` + its chosen `Rung` + resolved URLs — so the holder (plan 10-06) has a ready shape. Do NOT inline okhttp here (URL resolution lives in WebcamUrl.kt).
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --tests *WebcamListParseTest* --tests *WebcamRungSelectTest* --no-daemon" 2>&1 | tr -d '\r' | tail -25</automated>
  </verify>
  <done>`WebcamListParseTest` + `WebcamRungSelectTest` GREEN; both goldens parse to correct models; blank-service/missing-snapshot/odd-unicode tolerated; rung selection driven by Content-Type only.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: D-09 URL resolver/rewriter + token redaction (WebcamUrl.kt)</name>
  <files>app/src/main/java/works/mees/dinghy/net/WebcamUrl.kt</files>
  <behavior>
    - A relative `stream_url`/`snapshot_url` (`/webcam/?action=stream`) resolves against `ConnectionConfig.httpBase` to an absolute URL on the configured host.
    - An absolute URL whose host is `localhost`/`127.0.0.1`/`0.0.0.0`/`::1` is rewritten to `ConnectionConfig.host`, preserving scheme/port/path AND the `?token=...` query (E3 needs it).
    - An already-correct absolute URL on the real host passes through unchanged (token preserved).
    - redact(url) replaces `?token=`/`&token=` value with `<redacted>` — the resolved URL is never logged in the clear.
  </behavior>
  <read_first>
    - app/src/main/java/works/mees/dinghy/auth/MoonrakerAuth.kt:88-105 (buildAuthedWsUrl — the HttpUrl `newBuilder()` manipulation idiom), :130-135 (redactWsUrl — the regex token strip to mirror)
    - app/src/main/java/works/mees/dinghy/config/ConnectionConfig.kt:14-28 (httpBase + host — the resolution base)
    - .planning/phases/10-webcam-streaming/10-RESEARCH.md (§ Pitfall 1 — the exact resolve(a)/rewrite(b)/preserve-token contract; § Don't Hand-Roll — HttpUrl.resolve does the join, the localhost rewrite is the one bit you write)
    - .planning/phases/10-webcam-streaming/10-PATTERNS.md (§ net/WebcamUrl.kt — the resolver contract + redact mirror)
    - app/src/test/java/works/mees/dinghy/webcam/WebcamUrlResolverTest.kt (the RED scaffold this turns green)
  </read_first>
  <action>
    Add `resolveWebcamUrl(raw: String, cfg: ConnectionConfig): String?` — (a) join a relative `raw` via `cfg.httpBase.toHttpUrl().resolve(raw)` (okhttp does the relative join correctly — do NOT string-concat); (b) on the resolved `HttpUrl`, if `host` ∈ {`localhost`,`127.0.0.1`,`0.0.0.0`,`::1`} rebuild with `.newBuilder().host(cfg.host).build()`, PRESERVING port/path/query so the `?token=...` survives; (c) return the absolute string, or null on an unparseable input (caller treats null → rung-3). Add `redactWebcamUrl(url: String): String` mirroring `MoonrakerAuth.redactWsUrl`'s regex (`Regex("([?&]token=)[^&]*")` → `$1<redacted>`) — this is the ONLY form any log/crash surface may show (Security V7). Keep it pure (no Android, no logging side-effects inside the resolver).
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --tests *WebcamUrlResolverTest* --no-daemon" 2>&1 | tr -d '\r' | tail -25</automated>
  </verify>
  <done>`WebcamUrlResolverTest` GREEN: relative→host resolved; 127.0.0.1/localhost rewritten to host with port/path/?token= preserved; already-correct URL unchanged; redact masks the token.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `/server/webcams/list` JSON → app | Every field untrusted (service/name may be blank or odd-unicode; URLs may be relative/localhost/malformed). |
| resolved URL → log/crash surface | The E3 URL embeds a `?token=` secret. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04 | Tampering | parseWebcamsList | mitigate | Tolerant decode (`ignoreUnknownKeys`, nullable fields, fail-to-empty-list); `WebcamListParseTest` proves no-crash on blank/odd/missing fields (ASVS V5). |
| T-10-05 | Info Disclosure | redactWebcamUrl | mitigate | `redact(?token=…)` mirrors `MoonrakerAuth.redactWsUrl`; the resolved URL is never surfaced unredacted (ASVS V7); `WebcamUrlResolverTest` asserts the mask. |
| T-10-06 | Spoofing/Tampering | rungFor (service hint) | mitigate | Content-Type is the sole decode authority (D-02); a hostile/blank `service` cannot route bytes into the wrong decoder. |
</threat_model>

<verification>
- `WebcamListParseTest`, `WebcamRungSelectTest`, `WebcamUrlResolverTest` GREEN.
- No new dependency (PKG-02; `libs.versions.toml` unchanged).
</verification>

<success_criteria>
- Both goldens parse to tolerant models; malformed payload → empty list.
- D-09 resolve+rewrite+token-preserve proven; redact masks the token.
- Rung selection is Content-Type-driven (D-02), service ignored.
</success_criteria>

<output>
Create `.planning/phases/10-webcam-streaming/10-02-SUMMARY.md` when done.
</output>
