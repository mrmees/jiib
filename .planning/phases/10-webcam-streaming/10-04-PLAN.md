---
phase: 10-webcam-streaming
plan: 04
type: execute
wave: 3
depends_on: ["10-01", "10-02"]
files_modified:
  - app/src/main/java/works/mees/dinghy/net/WebcamProbe.kt
  - app/src/main/java/works/mees/dinghy/net/MjpegStreamDecoder.kt
  - app/src/main/java/works/mees/dinghy/net/SnapshotPoller.kt
autonomous: true
requirements: [CAM-01]

must_haves:
  truths:
    - "The probe GETs the stream URL, reads Content-Type, and routes to rung 1/2/3 (multipart→1, snapshot-present→2, 401/403/404/else→3) — D-02"
    - "401/403 is terminal-for-this-cam (no spin-retry) — D-12/A4/Pitfall 3"
    - "The MJPEG decoder yields N frames from both Content-Length fixtures and the split-JPEG fixture; one reused bitmap, no per-frame allocation"
    - "An absurd Content-Length / oversized part is rejected (OOM-by-hostile-frame guard) — Security DoS"
    - "Frames are drop-behind (conflated latest-wins) — a slow consumer never backs up the decoder — D-06/SC-1"
    - "The snapshot poller uses a FINITE read timeout (~5s); the stream uses readTimeout(0) — Pitfall 4; both derived off the ONE shared OkHttp client"
    - "A resolved webcam URL is never logged unredacted (redactWebcamUrl) — Security V7"
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/net/MjpegStreamDecoder.kt"
      provides: "Lean Okio multipart MJPEG decoder, drop-behind, inSampleSize+inBitmap reuse"
      contains: "inBitmap"
    - path: "app/src/main/java/works/mees/dinghy/net/WebcamProbe.kt"
      provides: "D-02 Content-Type sniff → rung select, typed terminal-on-401/403"
    - path: "app/src/main/java/works/mees/dinghy/net/SnapshotPoller.kt"
      provides: "~2fps finite-timeout snapshot GET loop, 401/403 terminal"
  key_links:
    - from: "WebcamProbe / MjpegStreamDecoder / SnapshotPoller"
      to: "MoonrakerSocket.defaultClient().newBuilder()"
      via: "two derived read-timeout postures off the one shared pool"
      pattern: "newBuilder\\(\\)\\.readTimeout"
    - from: "MjpegStreamDecoder"
      to: "ResponseBody.source() (Okio)"
      via: "boundary scan + readByteArray"
      pattern: "\\.source\\(\\)|BufferedSource"
---

<objective>
The decode I/O layer: the D-02 Content-Type probe/rung-selector, the lean Okio MJPEG multipart decoder (rung 1), and the ~2fps snapshot poller (rung 2). Pure-ish services off the shared OkHttp client, host-testable against the plan 10-01 fixtures — turning `WebcamRungSelectTest` (probe side), `MjpegStreamDecoderTest`, and `FrameDropBehindTest` GREEN.

Purpose: This is the genuinely-new custom code (RESEARCH: "small and bounded — the multipart boundary scanner, the rung selector"). It must survive the real contract: no-Content-Length parts (boundary scan), JPEG split across reads, 401/404-text-plain, an absurd Content-Length (hostile-frame OOM guard). It reuses the ONE OkHttp client with two derived read-timeout postures (Pitfall 4: stream=0, snapshot=finite) — never a second client. Drop-behind only (D-06): on a slow Adreno-320, a backed-up queue is ever-growing latency + memory.
Output: `net/WebcamProbe.kt`, `net/MjpegStreamDecoder.kt`, `net/SnapshotPoller.kt`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-webcam-streaming/10-RESEARCH.md
@.planning/phases/10-webcam-streaming/10-PATTERNS.md
@app/src/main/java/works/mees/dinghy/auth/MoonrakerAuth.kt
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Content-Type probe + rung selection (WebcamProbe.kt)</name>
  <files>app/src/main/java/works/mees/dinghy/net/WebcamProbe.kt</files>
  <behavior>
    - GET a `multipart/x-mixed-replace; boundary=…` stream URL → Rung.Mjpeg, carrying the open body + parsed boundary token.
    - A `404 text/plain` (mediamtx WebRTC reality) or non-multipart 2xx with a snapshot URL present → Rung.Snapshot.
    - A `401`/`403` (E5 Basic Auth reality) → Rung.Unsupported, terminal-for-this-cam (the probe returns a terminal result; it does NOT throw and does NOT signal "retry").
    - Neither a decodable stream nor a usable snapshot → Rung.Unsupported.
    - A resolved URL never appears in a thrown message or log unredacted.
  </behavior>
  <read_first>
    - app/src/main/java/works/mees/dinghy/auth/MoonrakerAuth.kt:36-86 (the injectable `Call.Factory` constructor + `newCall(...).execute().use { resp -> }` typed-branch idiom — never throw raw; 401 → typed terminal)
    - .planning/phases/10-webcam-streaming/10-RESEARCH.md (§ Pattern 1 — the exact GET-not-HEAD sniff + boundary extraction; § Pitfall 3 — 401/403 terminal; § Pitfall 4 — read-timeout posture)
    - .planning/phases/10-webcam-streaming/10-PATTERNS.md (§ net/WebcamProbe.kt — the fetchOneshotToken analog; § Shared OkHttp client — two derived postures)
    - app/src/main/java/works/mees/dinghy/state/WebcamModels.kt (the `Rung` type + `rungFor` from 10-02 — the probe RESULT type)
    - app/src/main/java/works/mees/dinghy/net/WebcamUrl.kt (redactWebcamUrl — use before any URL reaches a message/log)
    - app/src/test/java/works/mees/dinghy/webcam/FakeWebcamHttp.kt (the hardened double the test injects)
  </read_first>
  <action>
    Build `WebcamProbe` with an injectable `okhttp3.Call.Factory` (the `MoonrakerAuth` seam — tests pass `FakeWebcamHttp`, production passes the shared client derived with `readTimeout(0)` for the stream GET). `probe(resolvedStreamUrl, snapshotUrlPresent): ProbeResult` does a GET (NOT HEAD — some MJPEG servers ignore HEAD, RESEARCH Pattern 1), reads `Content-Type`, and folds through `WebcamModels.rungFor(contentType, code, snapshotUrlPresent)`: on `multipart/x-mixed-replace` keep the body open + parse the `boundary=` token and return Rung.Mjpeg(body, boundary); otherwise close the body and return Rung.Snapshot or Rung.Unsupported. Map 401/403 to a TERMINAL Unsupported (mirror how `fetchOneshotToken` maps 401 → a typed terminal error) — the holder must NOT spin-retry it (A4). Any URL in a thrown message or log MUST pass through `redactWebcamUrl` first (Security V7). Catch `IOException` → a typed transient/terminal result, never an escaping exception.
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --tests *WebcamRungSelectTest* --tests *WebcamProbe* --no-daemon" 2>&1 | tr -d '\r' | tail -25</automated>
  </verify>
  <done>Probe routes multipart→Mjpeg, 404-text-plain/snapshot-present→Snapshot, 401/403→terminal Unsupported; GET not HEAD; URLs redacted in any surfaced message; no raw exception escapes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lean MJPEG multipart decoder, drop-behind (MjpegStreamDecoder.kt)</name>
  <files>app/src/main/java/works/mees/dinghy/net/MjpegStreamDecoder.kt</files>
  <behavior>
    - `mjpeg_with_content_length.bin` → the expected frame count, each a decoded bitmap.
    - `mjpeg_no_content_length.bin` → same frame count via the boundary/SOI-EOI scan path.
    - `mjpeg_split_jpeg.bin` fed in chunks that split a JPEG across reads → still the correct frame count (no torn frame).
    - One reused mutable bitmap across frames (inBitmap reuse); NO per-frame allocation in steady state.
    - An absurd Content-Length / oversized accumulated part → the part is dropped (rejected), not decoded — no OOM.
    - A slow consumer never backs up the decoder: frames are conflated latest-wins (FrameDropBehindTest).
    - Scope cancellation tears down the underlying body (no leak).
  </behavior>
  <read_first>
    - app/src/main/java/works/mees/dinghy/net/MoonrakerSocket.kt:67-106 (the callbackFlow + awaitClose cancel-tears-down-the-resource idiom), :137-155 (defaultClient — the one shared client to derive off)
    - app/src/main/java/works/mees/dinghy/render/GraphView.kt (the allocation-free / one-reused-buffer discipline to mirror — pre-allocate, never alloc in the hot loop)
    - .planning/phases/10-webcam-streaming/10-RESEARCH.md (§ Pattern 2 — the boundary/Content-Length/SOI-EOI scan; § Pattern 3 — inSampleSize two-pass; § Pitfall 5 — inBitmap size-mismatch catch; § Pitfall 4 — readTimeout(0) for the stream)
    - .planning/phases/10-webcam-streaming/10-PATTERNS.md (§ net/MjpegStreamDecoder.kt — the data-flow analog + decode policy)
    - app/src/test/java/works/mees/dinghy/webcam/FakeMjpegStream.kt, MjpegStreamDecoderTest.kt, FrameDropBehindTest.kt
  </read_first>
  <action>
    Implement `MjpegStreamDecoder` consuming an Okio `BufferedSource` (`ResponseBody.source()`): scan parts as `--<boundary>\r\nContent-Type: image/jpeg\r\n[Content-Length: N\r\n]\r\n<jpeg>\r\n`. If `Content-Length` present, `require(n)` then `readByteArray(n)`; else scan for the next boundary / JPEG EOI (FF D9). Decode each part via `BitmapFactory.decodeByteArray` with a shared `BitmapFactory.Options{ inMutable=true; inSampleSize=<computed once>; inBitmap=<reused> }`; compute `inSampleSize` ONCE via a `inJustDecodeBounds` first pass so the decoded bitmap ≈ the view px (Pattern 3 — the res is stable). Catch the `inBitmap` size-mismatch `IllegalArgumentException`, drop `inBitmap` for one frame, re-establish (Pitfall 5). Cap an accumulated part at a sane ceiling and DROP an absurd `Content-Length`/oversized part (hostile-frame OOM guard — Security DoS). Hand frames off DROP-BEHIND via a `Channel(CONFLATED)` or `AtomicReference<Bitmap?>` (newest wins — never queue; D-06/SC-1). Use the structured-cancel teardown shape from `MoonrakerSocket` (close the body on cancel — no leak). Production stream client = `MoonrakerSocket.defaultClient().newBuilder().readTimeout(0, MILLISECONDS).build()` (Pitfall 4) — derived off the ONE pool, never a new client. Decode on a background dispatcher; the View consumes the latest frame. Do NOT inline a JPEG parser (use `BitmapFactory`), do NOT build a custom bitmap pool (use `inBitmap`).
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --tests *MjpegStreamDecoderTest* --tests *FrameDropBehindTest* --no-daemon" 2>&1 | tr -d '\r' | tail -30</automated>
  </verify>
  <done>`MjpegStreamDecoderTest` + `FrameDropBehindTest` GREEN: all three fixtures yield the correct frame count; one reused bitmap (no per-frame alloc); absurd Content-Length rejected; drop-behind conflated; cancel closes the body.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: ~2fps snapshot poller, 401/403 terminal (SnapshotPoller.kt)</name>
  <files>app/src/main/java/works/mees/dinghy/net/SnapshotPoller.kt</files>
  <behavior>
    - Polls the snapshot URL at ~2fps (~500ms) emitting decoded frames; the E3 `?token=` 200 image/jpeg fixture yields frames.
    - The E5 `401 Basic Auth` snapshot → terminal Unsupported (no spin-retry) — the poller stops, surfaces rung-3, does not hammer the host.
    - A wedged/slow snapshot GET times out (finite ~5s read timeout) and is retried with backoff (1s→10s cap) — it never hangs forever.
    - The snapshot client read timeout is FINITE (never readTimeout(0)).
  </behavior>
  <read_first>
    - app/src/main/java/works/mees/dinghy/auth/MoonrakerAuth.kt:53-76 (one-shot GET with a finite timeout, typed branch)
    - app/src/main/java/works/mees/dinghy/net/Backoff.kt:22-50 (backoffDelay — overflow-safe jittered; reuse for the retry side, 1s→~10s cap foreground-only)
    - .planning/phases/10-webcam-streaming/10-RESEARCH.md (§ D-07 ~2fps; § Pitfall 3 401/403 terminal; § Pitfall 4 finite read timeout)
    - .planning/phases/10-webcam-streaming/10-PATTERNS.md (§ net/SnapshotPoller.kt — finite-timeout GET + Backoff reuse)
    - app/src/main/java/works/mees/dinghy/net/WebcamUrl.kt (redactWebcamUrl — for any logged URL)
  </read_first>
  <action>
    Implement `SnapshotPoller` as a ~500ms (~2fps, D-07) loop of finite-timeout GETs off `MoonrakerSocket.defaultClient().newBuilder().readTimeout(5, SECONDS).build()` (Pitfall 4 — NEVER the readTimeout(0) stream posture; a wedged snapshot must time out, not hang the poller). Decode each 200 `image/jpeg` body (reuse the off-thread downsample discipline — Coil MAY serve the still per the FileThumbnailLoader precedent, but the loop/cadence is custom). On 401/403 → STOP and surface terminal Unsupported (E5 reality — do NOT spin; A4/Pitfall 3, protects the weak SBC host). On a transient IOException/timeout → retry via `backoffDelay(attempt)` (1s→~10s cap), foreground-only (the caller cancels on background — plan 10-06/10-07). Hand frames off the same drop-behind seam as the decoder. Any URL in a log/message passes through `redactWebcamUrl` (Security V7). Expose `cancel()`/structured cancellation.
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --tests *SnapshotPoller* --tests *WebcamBackoff* --no-daemon" 2>&1 | tr -d '\r' | tail -25</automated>
  </verify>
  <done>Snapshot poller emits ~2fps frames from the E3 token fixture; E5 401 → terminal (no spin); finite read timeout (never 0); transient failures backoff 1s→10s; URLs redacted in logs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| webcam HTTP host → decoder/poller | Untrusted JPEG/stream bytes + headers; a hostile/oversized frame or a wedged connection crosses here. The host is a separate auth domain (Basic Auth / in-URL token). |
| resolved URL → log surface | The E3 URL embeds a `?token=` secret. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-02 | Denial of Service | MjpegStreamDecoder (oversized frame) | mitigate | Cap accumulated part size / reject absurd Content-Length; always `inSampleSize` downscale — `MjpegStreamDecoderTest` proves the reject (OOM-by-hostile-frame). |
| T-10-09 | DoS / battery | wedged snapshot/stream connection | mitigate | Snapshot uses a FINITE 5s read timeout (Pitfall 4); backoff cap (D-12); loops cancel on background (plan 10-07) — no wedged conn holds the FGS awake. |
| T-10-10 | DoS (self-DoS the SBC) | 401/403 spin-retry | mitigate | 401/403 treated as terminal-for-this-cam (A4) — `WebcamBackoffTest`/poller stop, no spin hammering the weak host. |
| T-10-05 | Info Disclosure | logged tokened URL | mitigate | `redactWebcamUrl` applied before any URL reaches a log/message (ASVS V7). |
| T-10-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — no installs (RESEARCH § Package Legitimacy Audit: N/A); platform `BitmapFactory` + pinned OkHttp only; `libs.versions.toml` unchanged. |
</threat_model>

<verification>
- `MjpegStreamDecoderTest`, `FrameDropBehindTest`, `WebcamProbe`/`WebcamRungSelectTest` (probe), `SnapshotPoller`/`WebcamBackoffTest` GREEN.
- Two derived read-timeout postures off the ONE shared client (grep `newBuilder().readTimeout`); no new OkHttp client minted.
- No new dependency (PKG-02).
</verification>

<success_criteria>
- Probe routes by Content-Type (D-02); 401/403 terminal (A4).
- MJPEG decoder: 3 fixtures → correct frames, one reused bitmap, hostile-frame rejected, drop-behind.
- Snapshot poller: ~2fps, finite timeout, 401 terminal, backoff.
</success_criteria>

<output>
Create `.planning/phases/10-webcam-streaming/10-04-SUMMARY.md` when done.
</output>
