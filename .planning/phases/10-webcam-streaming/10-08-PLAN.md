---
phase: 10-webcam-streaming
plan: 08
type: execute
wave: 6
depends_on: ["10-07"]
files_modified:
  - app/src/main/java/works/mees/dinghy/net/MjpegStreamDecoder.kt
  - app/src/main/java/works/mees/dinghy/ui/webcam/WebcamHolder.kt
  - docs/view_specific_notes/camera_feed
  - .planning/phases/10-webcam-streaming/10-UAT.md
autonomous: false
requirements: [CAM-01]

must_haves:
  truths:
    - "The full unit suite + instrumented webcam tests run GREEN, and a signed release APK is installed on flox"
    - "The A1/A2 ASSUMED values (decode fps cap + inSampleSize step + ARGB_8888-vs-565) are PINNED by on-device gfxinfo measurement (0 frozen frames during ~15s decode)"
    - "The snapshot-ladder live UAT passes on flox + E3 (token snapshot → ~2fps feed + persistent 'Snapshot ~2fps' badge)"
    - "An E5 webrtc-mediamtx / 401 cam correctly shows the rung-3 dead-end card on-device (NOT a bug)"
    - "WebRTC is documented as explicitly deferred (SC-4)"
  artifacts:
    - path: ".planning/phases/10-webcam-streaming/10-UAT.md"
      provides: "On-device UAT record (perf pin + snapshot ladder + rung-3 card + WebRTC-deferred note)"
  key_links:
    - from: "gfxinfo capture on flox"
      to: "MjpegStreamDecoder fps cap + inSampleSize (A1/A2)"
      via: "measured-then-pinned decode policy"
      pattern: "inSampleSize|fps"
---

<objective>
The phase gate: the full automated suite + instrumented webcam tests green, a signed release APK on flox, the on-device gfxinfo perf measurement that PINS the A1/A2 ASSUMED decode values (fps cap + inSampleSize step + ARGB_8888-vs-565), and the live snapshot-ladder UAT (E3 token snapshot working; E5/WebRTC rung-3 dead-end card correct, not a bug). WebRTC documented as explicitly deferred (SC-4).

Purpose: The perf gate is the system-of-record per ADR-0001 Addendum 2 (gfxinfo 0-frozen-frames + responsiveness on the real Adreno-320 — emulators lie about old-GPU fill rate). The exact fps/inSampleSize/8888-vs-565 are explicitly Claude's-discretion-PENDING-measurement (A1/A2) — they must be measured and recorded, not left vague. The MJPEG live path has no live subject on these printers (both ravens-perch=webrtc-mediamtx), so the golden-byte-stream unit tests are the authoritative MJPEG proof and the snapshot ladder (E3) drives the live UAT — Task 2 is a BLOCKING human-verify gate returned to the orchestrator.
Output: pinned decode constants (MjpegStreamDecoder/WebcamHolder), `10-UAT.md`, the WebRTC-deferred note.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-webcam-streaming/10-RESEARCH.md
@.planning/phases/10-webcam-streaming/10-VALIDATION.md
@docs/view_specific_notes/camera_feed
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full automated gate + build/sign/install on flox + document WebRTC-deferred</name>
  <files>docs/view_specific_notes/camera_feed</files>
  <read_first>
    - .planning/phases/10-webcam-streaming/10-VALIDATION.md (§ Sampling Rate — the phase-gate command set; § Manual-Only Verifications — what the device gate must cover)
    - CLAUDE.md (the build env: `E:\Android\gw.bat` command form; `E:\Android\sign-release.bat`; flox = LineageOS 18.1 / API 30; printer addresses E5=192.168.1.120, E3=192.168.1.121)
    - .planning/phases/10-webcam-streaming/10-RESEARCH.md (§ Open Questions — the MJPEG-no-live-subject + E5-401 realities to record in UAT; § Deferred — WebRTC)
  </read_first>
  <action>
    Run the full automated gate: `:app:testDebugUnitTest` (all webcam unit classes GREEN) and `:app:connectedDebugAndroidTest` (the three instrumented webcam classes from plan 10-07) via the gw.bat form, guarded with `timeout` + taskkill (build-env memory: hung gw.bat children outlive WSL timeouts). Build + sign + install the release APK on flox: `… "E:\Android\gw.bat :app:assembleRelease --no-daemon"` then `E:\Android\sign-release.bat <in.apk> <out.apk>` then `adb install`. Append a short "WebRTC explicitly deferred (MJPEG + snapshot only this phase; camera-streamer/go2rtc/webrtc-mediamtx streams render the rung-3 dead-end card — SC-4)" note to `docs/view_specific_notes/camera_feed` so the deferral is recorded where the design contract lives. Do NOT run the human UAT here — that is Task 2 (a blocking checkpoint returned to the orchestrator).
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --no-daemon" 2>&1 | tr -d '\r' | tail -12; grep -qi "deferred" docs/view_specific_notes/camera_feed && echo WEBRTC_DEFERRED_NOTED</automated>
  </verify>
  <done>Full `:app:testDebugUnitTest` GREEN; `:app:connectedDebugAndroidTest` webcam classes GREEN; signed release APK installed on flox; WebRTC-deferred note appended to the camera_feed contract.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: On-device flox UAT — perf pin (A1/A2) + snapshot ladder + rung-3 card (BLOCKING)</name>
  <files>.planning/phases/10-webcam-streaming/10-UAT.md, app/src/main/java/works/mees/dinghy/net/MjpegStreamDecoder.kt, app/src/main/java/works/mees/dinghy/ui/webcam/WebcamHolder.kt</files>
  <action>
    BLOCKING human-verify gate — returned to the orchestrator, NOT executed by the plan-runner. The orchestrator drives the on-device UAT on flox + live E5/E3 per the <how-to-verify> steps below; the user navigates/observes/confirms. If the perf pin requires lowering fps / stepping inSampleSize up / switching ARGB_8888→RGB_565 (A1/A2), apply the final pinned values to `MjpegStreamDecoder`/`WebcamHolder` and re-measure. Record every result + the pinned A1/A2 values + the raw gfxinfo numbers in `10-UAT.md`. The plan SUMMARY and the phase-complete mark are WITHHELD until this gate returns "approved" (a FAIL spawns a gap-closure plan, not a phase-complete mark).
  </action>
  <what-built>
    The complete Webcam feature is installed on flox (signed release APK): enumeration off the spine, the D-02 rung ladder (MJPEG decode / snapshot poll / dead-end card), the pixel-square never-stretch frame surface with the rounded cutout, the per-printer preferred-cam, the page-visible lifecycle, and the greyed-when-no-cams drawer tile. The decoder's fps cap + inSampleSize + ARGB_8888-vs-565 are at their ASSUMED A1/A2 starting values, to be PINNED by this measurement.
  </what-built>
  <how-to-verify>
    On the real flox (Adreno-320 / 2GB) with the live printers (E5 192.168.1.120, E3 192.168.1.121):
    1. PERF PIN (system-of-record, ADR-0001 Add. 2): reset gfxinfo (`adb shell dumpsys gfxinfo works.mees.dinghy reset`), open Webcam on the E3 cam, let the snapshot feed run ~15s, then `adb shell dumpsys gfxinfo works.mees.dinghy` — require p95 within budget + **0 frozen frames**. If jank/frozen frames appear, lower the decode fps (~12→8–10) and/or step inSampleSize up (and/or switch ARGB_8888→RGB_565 per A2); RE-MEASURE; RECORD the final pinned values.
    2. SNAPSHOT LADDER (the primary on-device path, E3 has a `?token=` 200 snapshot): confirm the E3 Webcam shows a live ~2fps snapshot feed with the persistent "Snapshot ~2fps" badge (D-03), pixel-square (never stretched) in BOTH portrait and landscape, inside the rounded cutout.
    3. RUNG-3 CARD (correct, NOT a bug): select an E5 cam (webrtc-mediamtx stream + 401 Basic-Auth snapshot — no creds) — confirm the dead-end card names the WebRTC service and has NO open-in-browser button (D-04/SC-4).
    4. GREYED TILE (D-08): on a printer/connection with zero cams (or before enumeration), confirm the Webcam drawer tile is greyed/disabled, and live once cams enumerate.
    5. LIFECYCLE (SC-3/D-13): navigate away / background the app while a feed runs — confirm the feed stops (no continued decode/bandwidth); returning resumes it.
    6. (Optional MJPEG live) If a crowsnest/ustreamer MJPEG cam is stood up, point Dinghy at it and confirm a live moving feed; otherwise the golden-byte-stream unit tests are the authoritative MJPEG proof (no live MJPEG subject on E5/E3 — expected).
    Record all results (incl. the pinned A1/A2 values + the gfxinfo numbers) in `.planning/phases/10-webcam-streaming/10-UAT.md`.
  </how-to-verify>
  <verify>
    <human-check>The 5 (or 6) UAT steps pass on flox + live E5/E3; the A1/A2 decode values are pinned (0 frozen frames in gfxinfo); the snapshot ladder works on E3; the rung-3 card is correct on E5/WebRTC; results recorded in 10-UAT.md.</human-check>
  </verify>
  <done>User reports the UAT results + pinned decode values; "approved" only if perf pins clean + snapshot ladder works + rung-3 card correct. A gap spawns a gap-closure plan; the SUMMARY is withheld until the re-run passes.</done>
  <resume-signal>Report the 5 (or 6) UAT results + the pinned decode values. Type "approved" if perf pins clean + snapshot ladder works + rung-3 card correct, or describe the gaps (a gap spawns a gap-closure plan, NOT a phase-complete mark — the SUMMARY is withheld until the re-run passes).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live printer hosts → flox | The real E5/E3 webcam hosts (401 Basic Auth / token snapshot / WebRTC stream) exercise the real rung ladder on-device — the gate that the unit suite alone has missed twice. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-11 | Denial of Service (fill-rate) | on-device decode | mitigate | gfxinfo 0-frozen-frames gate pins fps/inSampleSize/8888-vs-565 on the real Adreno-320 (system-of-record) — not left to the ASSUMED A1/A2 values. |
| T-10-03 | Info Disclosure | E3 tokened snapshot URL | mitigate | The on-device feed uses the tokened URL but it is never logged (redact verified in plan 10-02/10-04); the dead-end card shows only the service name, never the URL. |
| T-10-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — no installs (RESEARCH § Package Legitimacy Audit: N/A); the release APK is debug-signed via the documented `sign-release.bat`; `libs.versions.toml` unchanged. |
</threat_model>

<verification>
- Full `:app:testDebugUnitTest` + `:app:connectedDebugAndroidTest` (webcam) GREEN; signed release APK on flox.
- gfxinfo 0-frozen-frames during ~15s decode on flox; A1/A2 values PINNED and recorded in 10-UAT.md.
- Snapshot-ladder works on E3; rung-3 dead-end card correct on E5/WebRTC; WebRTC-deferred documented.
- 10-UAT.md captures all results (this plan's SUMMARY + phase-complete mark are withheld until the human gate returns "approved").
</verification>

<success_criteria>
- All 4 phase Success Criteria proven on-device: (1) enumerate + stream/render, (2) off-thread decode + downscale, no OOM/jank (gfxinfo pinned), (3) page-visible stop + graceful degrade, (4) WebRTC deferred.
- A1/A2 ASSUMED values pinned by measurement.
- Snapshot ladder + rung-3 card verified live; UAT recorded.
</success_criteria>

<output>
Create `.planning/phases/10-webcam-streaming/10-08-SUMMARY.md` ONLY after the blocking UAT returns "approved". Record the UAT results in `.planning/phases/10-webcam-streaming/10-UAT.md`.
</output>
