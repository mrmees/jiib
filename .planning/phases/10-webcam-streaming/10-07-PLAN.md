---
phase: 10-webcam-streaming
plan: 07
type: execute
wave: 5
depends_on: ["10-06"]
files_modified:
  - app/src/main/java/works/mees/dinghy/ui/route/TopRoute.kt
  - app/src/main/java/works/mees/dinghy/ui/shell/AppDrawer.kt
  - app/src/main/java/works/mees/dinghy/ui/shell/AppShell.kt
  - app/src/androidTest/java/works/mees/dinghy/webcam/DrawerWebcamGatingTest.kt
  - app/src/androidTest/java/works/mees/dinghy/webcam/WebcamLifecycleTest.kt
  - app/src/androidTest/java/works/mees/dinghy/webcam/WebcamUnsupportedCardTest.kt
autonomous: true
requirements: [CAM-01]

must_haves:
  truths:
    - "A Webcam drawer tile exists and is GREYED when cam-count==0, LIVE when ≥1 — runtime-gated (D-08), the deliberate departure from Phase-9 hide-the-tile"
    - "Selecting the live Webcam tile routes to Dest.Webcam → WebcamScreen with a Back-only gutter"
    - "The decode/poll/retry loops run ONLY while the Webcam page is visible+foreground; they cancel on background/nav-away (SC-3/D-13)"
    - "The WebcamHolder is re-keyed on spine rebuild and its loops are cancelled via DisposableEffect(holder){onDispose{holder.cancel()}} (WR-01)"
    - "A webrtc-mediamtx / 401 cam shows the rung-3 dead-end card naming the service, no browser button (SC-4/D-04)"
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/ui/shell/AppDrawer.kt"
      provides: "Runtime-gated Webcam tile (greyed when 0 cams)"
      contains: "Webcam"
    - path: "app/src/main/java/works/mees/dinghy/ui/shell/AppShell.kt"
      provides: "Dest.Webcam arm + holder re-key + DisposableEffect cancel + page-visible lifecycle binding"
      contains: "WebcamHolder"
  key_links:
    - from: "AppShell"
      to: "AppDrawer(webcamEnabled = webcamCount > 0)"
      via: "runtime enablement signal threaded into the static tile set (D-08)"
      pattern: "webcamEnabled|webcamCount"
    - from: "AppShell Dest.Webcam"
      to: "repeatOnLifecycle(STARTED) start/stop + DisposableEffect cancel"
      via: "page-visible lifecycle + WR-01 leak-cancel"
      pattern: "repeatOnLifecycle|DisposableEffect\\(webcamHolder\\)"
---

<objective>
Wire the Webcam destination into the shell the way the rest of the app routes: a runtime-GREYED-gated drawer tile (D-08 — the deliberate departure from Phase-9's hide-the-tile), the `Dest.Webcam` arm building/re-keying the `WebcamHolder`, the page-visible lifecycle binding (decode/poll runs ONLY while foreground — SC-3/D-13), and the WR-01 `DisposableEffect(holder){onDispose{holder.cancel()}}` leak-cancel. Plus the instrumented gating / lifecycle / dead-end-card tests.

Purpose: D-08 needs the drawer to express a RUNTIME enablement the current static `DRAWER_TILES` cannot — the one real codebase gap (PATTERNS.md): thread a `webcamEnabled` signal (from `AppContainer.webcamCount > 0`) into `AppDrawer` and fold it into the existing `val live = tile.dest != null` decision. The styling is already correct; only the enablement input is new. The lifecycle + leak-cancel are load-bearing (the WR-01 lesson + the FGS-battery concern) — made explicit acceptance criteria.
Output: `ui/route/TopRoute.kt` (+Dest.Webcam), `ui/shell/AppDrawer.kt` (runtime gating), `ui/shell/AppShell.kt` (arm + holder + lifecycle + cancel), 3 instrumented tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-webcam-streaming/10-RESEARCH.md
@.planning/phases/10-webcam-streaming/10-PATTERNS.md
@.planning/phases/10-webcam-streaming/10-VALIDATION.md
@app/src/main/java/works/mees/dinghy/ui/shell/AppDrawer.kt
@app/src/main/java/works/mees/dinghy/ui/shell/AppShell.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dest.Webcam + runtime-gated greyed Webcam drawer tile (D-08)</name>
  <files>app/src/main/java/works/mees/dinghy/ui/route/TopRoute.kt, app/src/main/java/works/mees/dinghy/ui/shell/AppDrawer.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/route/TopRoute.kt:31 (the Dest enum — add Webcam, a one-line addition)
    - app/src/main/java/works/mees/dinghy/ui/shell/AppDrawer.kt:97-195 (DrawerTileSpec, the static DRAWER_TILES list, and the `val live = tile.dest != null` decision + the greyed styling that is ALREADY correct)
    - .planning/phases/10-webcam-streaming/10-PATTERNS.md (§ "Drawer greyed-gating: the one real GAP" — the EXACT surgical extension: add the tile, thread a webcamEnabled param, fold it into `val live`)
    - .planning/phases/10-webcam-streaming/10-RESEARCH.md (§ D-08 — greyed not hidden, the App Drawer "coming soon" convention)
  </read_first>
  <action>
    Add `Webcam` to the `Dest` enum (`TopRoute.kt`). Add `DrawerTileSpec(label = "Webcam", symbol = <unique-glyph>, dest = Dest.Webcam)` to `DRAWER_TILES` — pick a Material Symbol NOT already used (icon-no-repeat law; `terminal`/`tune`/`videocam` are taken — use `photo_camera` or `linked_camera`, verify it is unused in the file). Thread a NEW `webcamEnabled: Boolean` parameter into `AppDrawer(...)` and down to `DrawerTile`, and fold it into the existing live-decision: `val live = tile.dest != null && (tile.dest != Dest.Webcam || webcamEnabled)` so the Webcam tile renders GREYED (hairline outline, dimmed text, inert `semantics{disabled()}`, no clickable) when `!webcamEnabled` and LIVE (accent outline, clickable) when enabled. The greyed STYLING is already correct (`AppDrawer.kt:146-166`) — only the enablement INPUT is new (do NOT assume the drawer already supports runtime gating; it does not). Default `webcamEnabled = false` so an idle/no-session drawer greys it.
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:compileReleaseKotlin --no-daemon" 2>&1 | tr -d '\r' | tail -10; grep -n "Webcam" app/src/main/java/works/mees/dinghy/ui/route/TopRoute.kt app/src/main/java/works/mees/dinghy/ui/shell/AppDrawer.kt</automated>
  </verify>
  <done>`Dest.Webcam` added; a Webcam tile with a unique glyph is in DRAWER_TILES; `webcamEnabled` threaded through AppDrawer→DrawerTile and folded into `val live`; greyed when false / live when true; compiles.</done>
</task>

<task type="auto">
  <name>Task 2: AppShell Dest.Webcam arm + holder re-key + page-visible lifecycle + WR-01 cancel</name>
  <files>app/src/main/java/works/mees/dinghy/ui/shell/AppShell.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/shell/AppShell.kt:148-156 (the holder re-key idiom `remember(store){ Holder(...) }`), :200-210 + :232-236 (the DisposableEffect(holder){onDispose{holder.cancel()}} leak-cancel to copy), :282 (the swipe-suppress set), :300-304 (the Dest.Move arm to mirror), :435-440 (the AppDrawer call site — pass webcamEnabled)
    - app/src/main/java/works/mees/dinghy/di/AppContainer.kt:126 (webcamCount flow from plan 10-03 — the D-08 enablement source + D-10 default-pick)
    - .planning/phases/10-webcam-streaming/10-RESEARCH.md (§ Pattern 4 — repeatOnLifecycle(STARTED) + effect cancellation for the page-visible loops; § D-13)
    - .planning/phases/10-webcam-streaming/10-PATTERNS.md (§ ui/shell/AppShell.kt — the arm + re-key + DisposableEffect + lifecycle + swipe-suppress decision)
    - app/src/main/java/works/mees/dinghy/ui/webcam/WebcamHolder.kt, WebcamScreen.kt (the holder/screen from plan 10-06)
  </read_first>
  <action>
    Collect `webcamCount` (`container.webcamCount`) and the live `webcams`/host; pass `webcamEnabled = webcamCount > 0` to the `AppDrawer(...)` call. Build `val webcamHolder = remember(store) { WebcamHolder(scope = scope, webcams = spine?.webcams ?: …, webcamPrefs = container.webcamPrefs, host = <current cfg host>, sharedClient = …) }` re-keyed on the live per-session store (the MoveHolder precedent), and add `DisposableEffect(webcamHolder) { onDispose { webcamHolder.cancel() } }` (the WR-01 leak-cancel — the holder owns long-lived decode/poll loops; a re-key on reconnect MUST cancel the old loops or they leak + keep streaming after the session swapped — also a cadence/efficiency violation). Add the `Dest.Webcam -> WebcamScreen(holder = webcamHolder, tokens = t, onBack = { goBack() })` arm mirroring `Dest.Move`. BIND THE PAGE-VISIBLE LIFECYCLE (SC-3/D-13): only while `dest == Dest.Webcam` AND the page is foreground do the decode/poll/retry loops run — wire `repeatOnLifecycle(Lifecycle.State.STARTED)` (covers screen-off/home) PLUS the effect's own cancellation on nav-away (the `LaunchedEffect`/arm leaving composition cancels the loop), calling `webcamHolder.start(...)`/`stop()` accordingly (RESEARCH Pattern 4 — use both, they compose cleanly). Decide the swipe-up drawer-suppression: ADD `Dest.Webcam` to the `setOf(Dest.Files, Dest.Console, Dest.Macros, Dest.Calibration)` suppress set (the cam-cycle tap overlay wants the canvas; the explicit Back gutter is the exit) — PATTERNS.md recommends YES.
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --no-daemon" 2>&1 | tr -d '\r' | tail -15; /mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:assembleRelease --no-daemon" 2>&1 | tr -d '\r' | tail -8</automated>
  </verify>
  <done>`Dest.Webcam` arm renders `WebcamScreen` (onBack=goBack); holder re-keyed on store with `DisposableEffect{onDispose{cancel()}}`; page-visible lifecycle binds start/stop (repeatOnLifecycle STARTED + arm cancellation); `webcamEnabled = webcamCount>0` passed to AppDrawer; Webcam in the swipe-suppress set; full unit suite GREEN + `:app:assembleRelease` SUCCESSFUL (guarded).</done>
</task>

<task type="auto">
  <name>Task 3: Instrumented gating / lifecycle / dead-end-card tests</name>
  <files>app/src/androidTest/java/works/mees/dinghy/webcam/DrawerWebcamGatingTest.kt, app/src/androidTest/java/works/mees/dinghy/webcam/WebcamLifecycleTest.kt, app/src/androidTest/java/works/mees/dinghy/webcam/WebcamUnsupportedCardTest.kt</files>
  <read_first>
    - app/src/androidTest/java/works/mees/dinghy/ (the existing instrumented-test conventions — find a representative Compose UI / drawer test to mirror the harness + ComposeTestRule style)
    - .planning/phases/10-webcam-streaming/10-VALIDATION.md (§ Per-Task Verification Map — the three instrumented class names + their assertions; § Manual-Only Verifications for what is DEFERRED to the on-device gate)
    - app/src/main/java/works/mees/dinghy/ui/shell/AppDrawer.kt (the greyed-vs-live semantics the gating test asserts)
  </read_first>
  <action>
    `DrawerWebcamGatingTest` (instrumented): with `webcamEnabled = false` the Webcam tile is present but NOT clickable / marked disabled (greyed); with `webcamEnabled = true` it navigates — assert via the `semantics{disabled()}` / click-action presence (mirror `ShellPresenceTest`'s greyed-tile assertions). `WebcamLifecycleTest` (instrumented, or unit with virtual time if the lifecycle binding is host-observable): assert the holder's decode/poll loops START on STARTED and STOP on STOPPED/nav-away — no work in background (SC-3/D-13); if a real lifecycle is needed, drive it through the ComposeTestRule lifecycle. `WebcamUnsupportedCardTest`: a `webrtc-mediamtx` cam (the E5/E3 reality) → the rung-3 dead-end card renders, NAMES the service, and has NO open-in-browser button (SC-4/D-04) — assert the card text contains the service name and that no browser-button node exists. Use the goldens/fakes from plan 10-01 to construct the cam states; do NOT hit the network. (The no-OOM/no-jank decode and the live snapshot-ladder UAT are DEFERRED to the on-device gate, plan 10-08 — not instrumented here.)
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:compileDebugAndroidTestKotlin --no-daemon" 2>&1 | tr -d '\r' | tail -12</automated>
  </verify>
  <done>The three instrumented test classes exist and compile (`compileDebugAndroidTestKotlin` SUCCESSFUL); they assert greyed-vs-live gating, page-visible start/stop, and the rung-3 dead-end card (names service, no browser button). Execution on flox runs at the plan 10-08 device gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| page lifecycle → decode/poll loops | The loops must be bound to page-visible-foreground; a background loop wastes decode/bandwidth and holds the FGS awake. |
| spine rebuild → holder | A reconnect re-keys the holder; the old holder's loops must be cancelled or they leak + keep streaming. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-09 | DoS / battery | page-visible lifecycle + DisposableEffect cancel | mitigate | Loops run only on `Dest.Webcam` + STARTED; `DisposableEffect(webcamHolder){onDispose{cancel()}}` on spine rebuild — no background decode, no leaked stream (WR-01 + cadence-contract "stop when not visible"). |
| T-10-14 | Spoofing/Tampering | greyed tile navigability | mitigate | A greyed (`!webcamEnabled`) tile is inert (no clickable, `semantics{disabled()}`) — `DrawerWebcamGatingTest` proves it cannot navigate (mirrors `ShellPresenceTest`). |
| T-10-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — no installs (RESEARCH § Package Legitimacy Audit: N/A); `libs.versions.toml` unchanged. |
</threat_model>

<verification>
- Full `:app:testDebugUnitTest` GREEN; `:app:assembleRelease` SUCCESSFUL; `:app:compileDebugAndroidTestKotlin` SUCCESSFUL.
- `Dest.Webcam` routes; `webcamEnabled` gating wired; `DisposableEffect(webcamHolder){onDispose{cancel()}}` present; page-visible lifecycle binding present.
- No new dependency (PKG-02).
</verification>

<success_criteria>
- Webcam tile greyed at 0 cams / live at ≥1 (D-08); routes to WebcamScreen with Back gutter.
- Decode/poll loops page-visible-only (SC-3/D-13); WR-01 leak-cancel on spine rebuild.
- Instrumented gating/lifecycle/dead-end-card tests compile (run at the device gate).
</success_criteria>

<output>
Create `.planning/phases/10-webcam-streaming/10-07-SUMMARY.md` when done.
</output>
