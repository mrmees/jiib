---
phase: 10-webcam-streaming
plan: 03
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - app/src/main/java/works/mees/dinghy/net/JsonRpc.kt
  - app/src/main/java/works/mees/dinghy/command/CommandRegistry.kt
  - app/src/main/java/works/mees/dinghy/di/SpineHandle.kt
  - app/src/main/java/works/mees/dinghy/service/MoonrakerService.kt
  - app/src/main/java/works/mees/dinghy/di/AppContainer.kt
autonomous: true
requirements: [CAM-01]

must_haves:
  truths:
    - "server.webcams.list is a registered paramless one-shot JSON-RPC spec (NOT a subscribe, NOT polled — cadence contract)"
    - "The enumeration is fetched edge-driven once per handshake and forwarded as a StateFlow<List<Webcam>> on the SpineHandle (metadata/lastJob precedent)"
    - "The webcam-count flow is exposed off AppContainer for the drawer greyed-gating (D-08) and the holder default-cam pick (D-10)"
    - "A rejected/absent /server/webcams/list leaves the list empty (greyed tile), never crashes"
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/command/CommandRegistry.kt"
      provides: "webcamsList one-shot CommandSpec"
      contains: "webcamsList"
    - path: "app/src/main/java/works/mees/dinghy/di/SpineHandle.kt"
      provides: "webcams StateFlow forwarded off the session"
      contains: "webcams"
  key_links:
    - from: "MoonrakerService.buildSpineAndLaunch"
      to: "rpc.request(CommandRegistry.webcamsList)"
      via: "one-shot holder forwarded as StateFlow (metadata/lastJob seam)"
      pattern: "webcamsList|WebcamsHolder"
    - from: "AppContainer.webcamCount"
      to: "SpineHandle.webcams"
      via: "spine.flatMapLatest map size"
      pattern: "webcams"
---

<objective>
Wire the `/server/webcams/list` enumeration into the spine the cadence-compliant way: a registered paramless one-shot JSON-RPC spec, fetched edge-driven once per handshake (NOT a subscribe, NOT a per-page poll), and forwarded off the session as a `StateFlow<List<Webcam>>` on the `SpineHandle` — exactly the `metadata`/`lastJob` precedent (PATTERNS.md Decision B, recommended). Plus the `AppContainer` webcam-count convenience flow the drawer greyed-gating (D-08) and the holder's default-cam pick (D-10) consume.

Purpose: This is the integration seam every later webcam plan reads from. Choosing the service-owned-StateFlow shape (B) keeps the `rpc` capture where every other one-shot read already lives (`SpineHandle` exposes `dispatcher`, not `rpc`, so a holder cannot `rpc.request` off the handle as-is). The webcam component is universal on these printers, so there is NO availability predicate — gate the TILE on cam-count instead (D-08).
Output: `JsonRpc.kt` (+const), `CommandRegistry.kt` (+spec), `SpineHandle.kt` (+field), `MoonrakerService.kt` (+one-shot holder + handle wiring), `AppContainer.kt` (+webcam-count flow).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-webcam-streaming/10-RESEARCH.md
@.planning/phases/10-webcam-streaming/10-PATTERNS.md
@docs/request-cadence-contract.md
@app/src/main/java/works/mees/dinghy/di/SpineHandle.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register the server.webcams.list one-shot spec (JsonRpc + CommandRegistry)</name>
  <files>app/src/main/java/works/mees/dinghy/net/JsonRpc.kt, app/src/main/java/works/mees/dinghy/command/CommandRegistry.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/command/CommandRegistry.kt:82-152 (the serverInfo/objectsList paramless one-shot spec shape to copy), :438-486 (the `all` list every spec registers into)
    - app/src/main/java/works/mees/dinghy/net/JsonRpc.kt (the JsonRpcMethods consts — add WEBCAMS_LIST beside FILES_GET_DIRECTORY)
    - .planning/phases/10-webcam-streaming/10-PATTERNS.md (§ CommandRegistry — the exact webcamsList spec to copy; NO availability predicate — universal component, gate the tile instead)
    - docs/request-cadence-contract.md (§ Rule 3 — one-shot reads go through the handle, edge-driven, NEVER timer-driven; § LIVE calls table — where files.get_directory sits as a one-shot precedent)
  </read_first>
  <action>
    Add `const val WEBCAMS_LIST = "server.webcams.list"` to `JsonRpcMethods` beside `FILES_GET_DIRECTORY`. In `CommandRegistry`, add `val webcamsList: CommandSpec<Unit>` mirroring `serverInfo` verbatim: `catalogId = "MR-server.webcams.list"`, `method = JsonRpcMethods.WEBCAMS_LIST`, `key = { "webcams_list" }`, `params = { null }`, and NO `availability` predicate (the `webcams` Moonraker component is universal on E5/E3; D-08 greys the tile on cam-count==0 instead of hiding it). Register `webcamsList` in the `all` list so the catalog-drift guard sees it. This is a one-shot read (cadence contract: NOT a subscribe, NOT polled) — do not add it to `V1_SUBSCRIBE_CORE`.
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --tests *CommandCatalogDrift* --tests *Registry* --no-daemon" 2>&1 | tr -d '\r' | tail -25</automated>
  </verify>
  <done>`webcamsList` spec exists, registered in `all`, paramless, no availability predicate; catalog-drift / registry tests GREEN; `WEBCAMS_LIST` const added.</done>
</task>

<task type="auto">
  <name>Task 2: Forward the enumeration as a SpineHandle StateFlow + AppContainer count flow (service-owned one-shot, D-08/D-10)</name>
  <files>app/src/main/java/works/mees/dinghy/di/SpineHandle.kt, app/src/main/java/works/mees/dinghy/service/MoonrakerService.kt, app/src/main/java/works/mees/dinghy/di/AppContainer.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/di/SpineHandle.kt (the immutable handle — add a `webcams: StateFlow<List<Webcam>>` field mirroring `metadata`/`lastJob`)
    - app/src/main/java/works/mees/dinghy/service/MoonrakerService.kt:140-177 (the PrintMetadataHolder/LastJobHolder one-shot seam + the SpineHandle assembly the new field plugs into)
    - app/src/main/java/works/mees/dinghy/di/AppContainer.kt:93-127 (the `spine.flatMapLatest`/`spine.map` derived-flow convenience pattern — add a webcamCount flow here)
    - .planning/phases/10-webcam-streaming/10-PATTERNS.md (§ "Architecture decision" — Decision B recommended: service-owned one-shot forwarded as a StateFlow; § di/SpineHandle.kt + service/MoonrakerService.kt rows)
    - app/src/main/java/works/mees/dinghy/state/WebcamModels.kt (the `Webcam` model + `parseWebcamsList` from plan 10-02 — the type the StateFlow carries)
  </read_first>
  <action>
    Add `val webcams: StateFlow<List<Webcam>>` to `SpineHandle` (KDoc it as a one-shot-per-handshake read forwarded off the session, the `metadata`/`lastJob` precedent — written once per handshake, NOT the throttled hot path; a late collector still sees the value because StateFlow carries it forward). In `buildSpineAndLaunch`, build a small one-shot holder (modeled on `PrintMetadataHolder`/`LastJobHolder` — a `MutableStateFlow<List<Webcam>>` filled once per handshake by `runCatching { rpc.request(CommandRegistry.webcamsList, Unit) }.getOrNull()` then `parseWebcamsList(...)`); trigger it on the (re)connect/klippy_ready edge the other handshake one-shots use (NOT a wall-clock timer — cadence Rule 3). A rejected/absent read leaves the list empty (the greyed-tile signal), never crashes (Security V5 / Tampering). Pass its flow into the `SpineHandle(... webcams = ...)` assembly. In `AppContainer`, add `val webcamCount: Flow<Int> = spine.flatMapLatest { it?.webcams ?: flowOf(emptyList()) }.map { it.size }` (mirror the existing derived flows) — the D-08 gating signal + the D-10 default-pick source. Do NOT expose raw `rpc` on the handle (nothing else does; keep the capture in the service).
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --no-daemon" 2>&1 | tr -d '\r' | tail -25; /mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:assembleRelease --no-daemon" 2>&1 | tr -d '\r' | tail -8</automated>
  </verify>
  <done>`SpineHandle.webcams` field exists and is populated by a service-owned one-shot keyed on the handshake edge; `AppContainer.webcamCount` exposes the size; rejected read → empty list, no crash; full unit suite GREEN + `:app:assembleRelease` SUCCESSFUL (guarded).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Moonraker JSON-RPC → spine | `/server/webcams/list` result is untrusted; a malformed/rejected read must fail safe. |
| webcam stream/poll → FGS | The enumeration is a one-shot — it must NOT become a poll holding the FGS awake (cadence contract). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-07 | Denial of Service | webcam enumeration cadence | mitigate | `webcamsList` is a one-shot edge-driven read (per-handshake), NOT a subscribe and NOT polled — honors cadence-contract Rules 1–3; no FGS-waking loop. |
| T-10-08 | Tampering | rejected/malformed enumeration | mitigate | `runCatching{}.getOrNull()` + tolerant `parseWebcamsList` → empty list (greyed tile), never crash (ASVS V5). |
| T-10-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — no package installs this phase (RESEARCH § Package Legitimacy Audit: N/A); `libs.versions.toml` unchanged. |
</threat_model>

<verification>
- Full `:app:testDebugUnitTest` GREEN; `:app:assembleRelease` SUCCESSFUL (guarded, exit code authoritative).
- `webcamsList` registered (catalog-drift guard green); no addition to `V1_SUBSCRIBE_CORE` (one-shot, not subscribe).
- `SpineHandle.webcams` + `AppContainer.webcamCount` present.
</verification>

<success_criteria>
- `server.webcams.list` is a registered paramless one-shot spec, fetched edge-driven per handshake.
- Enumeration forwarded as `SpineHandle.webcams` StateFlow (B); `AppContainer.webcamCount` derived.
- Rejected/absent read → empty list, no crash.
</success_criteria>

<output>
Create `.planning/phases/10-webcam-streaming/10-03-SUMMARY.md` when done.
</output>
