---
phase: 17-fine-tune-live-adjust-panel
plan: 03
type: execute
wave: 1
depends_on: ["17-01"]
files_modified:
  - app/src/main/java/works/mees/dinghy/state/PrinterState.kt
  - app/src/main/java/works/mees/dinghy/state/PrinterStateReducer.kt
  - app/src/main/java/works/mees/dinghy/state/DeriveCapabilities.kt
  - app/src/main/java/works/mees/dinghy/state/PrinterStateStore.kt
  - app/src/main/java/works/mees/dinghy/net/MoonrakerSession.kt
  - app/src/test/java/works/mees/dinghy/state/PrinterStateReducerTest.kt
autonomous: true
requirements: [TUNE-02, TUNE-03, TUNE-05]
must_haves:
  truths:
    - "notify_status_update diffs deep-merge new tunable fields (toolhead limits, extruder PA/smooth-time, fan.speed, firmware_retraction) null-safe into the retained PrinterState; an omitted field retains its prior value."
    - "Config baselines for reset are read ONCE at handshake from configfile.settings (smooth-time from pressure_advance_smooth_time, NOT smooth_time) and exposed as one-shot StateFlows — no second configfile query."
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/state/PrinterState.kt"
      provides: "Nullable readback fields + FirmwareRetractionObject data class"
      contains: "pressureAdvance"
    - path: "app/src/main/java/works/mees/dinghy/state/PrinterStateReducer.kt"
      provides: "Null-safe toolhead/extruder/fan/firmware_retraction diff-merge walks"
      contains: "minimum_cruise_ratio"
    - path: "app/src/main/java/works/mees/dinghy/state/PrinterStateStore.kt"
      provides: "Config-baseline one-shot StateFlows for reset"
    - path: "app/src/main/java/works/mees/dinghy/net/MoonrakerSession.kt"
      provides: "store.set* baseline reads inside the existing configfile runCatching block"
  key_links:
    - from: "PrinterStateReducer"
      to: "PrinterState new fields"
      via: "doubleOrNullAt walks copying into s.copy(...)"
      pattern: "doubleOrNullAt\\(\"(max_velocity|minimum_cruise_ratio|pressure_advance|speed)\""
    - from: "MoonrakerSession configfile read"
      to: "PrinterStateStore baselines"
      via: "store.setBaseline*(extruderCfg?.floatOrNullAt(\"pressure_advance_smooth_time\"))"
      pattern: "pressure_advance_smooth_time"
---

<objective>
Build the state layer for Fine-Tune: the new nullable readback fields, the null-safe diff-merge reducer
walks, the subscribe-set addition, and the config-baseline one-shot reads that source the reset action.

Purpose: The reducer deep-merges live diffs so a tile's readout flips only when the printer reports the new
value (state-flip confirm, D-14); baselines are static and read once at handshake (the `minExtrudeTemp` seam),
so reset (D-16) targets the printer's config, not a Dinghy default. Turns the 17-01 reducer RED tests GREEN.

REVIEW NOTE (HIGH #1): 17-01 plants ACTIVE `fail()` RED stubs in the holder/nav tests that stay RED until
Waves 2–3. Therefore this plan verifies ONLY its own targeted classes — the reducer test plus the
store/session tests it directly touches. It does NOT require the full host suite green (that gate moves to
the final gate, 17-06, after all waves convert RED→GREEN).

Output: new `PrinterState` fields + `FirmwareRetractionObject`; reducer walks for toolhead/extruder/fan/
firmware_retraction; `firmware_retraction` added to the subscribe core; baseline StateFlows + setters; the
configfile-block baseline reads; the converted PrinterStateReducerTest.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-fine-tune-live-adjust-panel/17-CONTEXT.md
@.planning/phases/17-fine-tune-live-adjust-panel/17-RESEARCH.md
@.planning/phases/17-fine-tune-live-adjust-panel/17-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: New readback fields + null-safe reducer walks + subscribe-set</name>
  <files>app/src/main/java/works/mees/dinghy/state/PrinterState.kt, app/src/main/java/works/mees/dinghy/state/PrinterStateReducer.kt, app/src/main/java/works/mees/dinghy/state/DeriveCapabilities.kt, app/src/test/java/works/mees/dinghy/state/PrinterStateReducerTest.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/state/PrinterState.kt (the file being modified — `speedFactor`/`gcodeZOffset` nullable fields ~61-72)
    - app/src/main/java/works/mees/dinghy/state/PrinterStateReducer.kt (the file being modified — toolhead/gcode_move walks ~101-114; heater merge loop ~194-224; bed_mesh/manual_probe field-by-field merge-onto-retained ~154-192; `doubleOrNullAt` helper)
    - app/src/main/java/works/mees/dinghy/state/DeriveCapabilities.kt (the file being modified — `V1_SUBSCRIBE_CORE` ~50-68; dynamic `fan` clause ~89-92; absence-guard intersect loop ~81-83)
    - 17-RESEARCH.md § "Reducer additions" (the exact walk additions; extruder PA/smooth in a SEPARATE walk, NOT the heater loop; firmware_retraction field-by-field merge) + § Pitfall 1 (reducer stores RAW, scale only at display boundary)
    - 17-PATTERNS.md § "state/PrinterState.kt + PrinterStateReducer.kt" + § "state/DeriveCapabilities.kt"
  </read_first>
  <behavior>
    - A toolhead diff {max_velocity:300, max_accel:3000, minimum_cruise_ratio:0.5, square_corner_velocity:8} → state.maxVelocity=300.0, maxAccel=3000.0, minimumCruiseRatio=0.5, squareCornerVelocity=8.0.
    - An extruder diff {pressure_advance:0.045, smooth_time:0.04} → state.pressureAdvance=0.045, smoothTime=0.04 (read in a separate extruder walk, not per-heater).
    - A fan diff {speed:0.6} → state.partFanSpeed=0.6 (RAW 0..1, no scaling in reducer).
    - A synthetic firmware_retraction diff {retract_length:0.5, retract_speed:35, unretract_extra_length:0, unretract_speed:35} → state.firmwareRetraction non-null with those fields.
    - A partial diff omitting an already-set tunable field RETAINS the prior value (deep-merge, not replace).
    - firmware_retraction is present in V1_SUBSCRIBE_CORE; absence on a printer is safely skipped by the intersect loop.
  </behavior>
  <action>
    Per RESEARCH § Reducer additions: add nullable fields to `PrinterState` mirroring `gcodeZOffset` —
    `maxVelocity`, `maxAccel`, `minimumCruiseRatio`, `squareCornerVelocity`, `pressureAdvance`, `smoothTime`,
    `partFanSpeed` (all `Double? = null`), plus a `firmwareRetraction: FirmwareRetractionObject? = null` and a
    new `FirmwareRetractionObject` data class (retractLength/retractSpeed/unretractExtraLength/unretractSpeed,
    all nullable). Never fabricate a 0 — null = unreported (D-20 readout honesty).

    Extend the EXISTING `toolhead` walk in the reducer with the four `doubleOrNullAt` reads (per D-04..D-07).
    Add a SEPARATE `extruder`-keyed walk for `pressure_advance` + `smooth_time` (per D-09/D-10) — do NOT put
    them in the per-heater merge loop (they are not per-heater concepts, RESEARCH). Add a `fan` walk for
    `speed` (per D-11). Add a `firmware_retraction` field-by-field merge-onto-retained block mirroring
    bed_mesh/manual_probe so a partial diff retains omitted fields (per D-12). The reducer stores RAW
    ratios/0-1 — scaling happens at the display boundary in the holder, never here (Pitfall 1). NOTE: the
    `minimum_cruise_ratio` stored here is the RAW ratio (0.5); the holder converts to percent for display and
    the +tap target back to ratio for the wire (REVIEW #9) — the reducer never touches percent.

    Add `"firmware_retraction"` to `V1_SUBSCRIBE_CORE` (the intersect loop already guards absence — safe to add
    unconditionally; build-blind on both dev printers).

    Convert the 17-01 PrinterStateReducerTest reducer stubs to real fixture-fed assertions (including the
    synthetic firmware_retraction fixture and the diff-merge-retention case).
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --tests 'works.mees.dinghy.state.PrinterStateReducerTest' --no-daemon"</automated>
  </verify>
  <acceptance_criteria>
    - PrinterStateReducerTest GREEN: all five reducer behaviors (toolhead limits, extruder PA/smooth, fan.speed, synthetic firmware_retraction, diff-merge retention) pass.
    - `grep -n "minimum_cruise_ratio\|pressure_advance\|firmware_retraction" app/src/main/java/works/mees/dinghy/state/PrinterStateReducer.kt` shows the new walks.
    - `grep -c "firmware_retraction" app/src/main/java/works/mees/dinghy/state/DeriveCapabilities.kt` >= 1.
    - No scaling math (`* 100`, `/ 255`) appears in PrinterStateReducer.kt for these fields (raw storage, Pitfall 1).
  </acceptance_criteria>
  <done>New tunable fields parse null-safe and diff-merge-retain; firmware_retraction subscribed build-blind; reducer test GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: Config-baseline one-shot StateFlows + handshake reads (reset source of truth)</name>
  <files>app/src/main/java/works/mees/dinghy/state/PrinterStateStore.kt, app/src/main/java/works/mees/dinghy/net/MoonrakerSession.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/state/PrinterStateStore.kt (the file being modified — `minExtrudeTemp`/`screwsTiltConfig` one-shot StateFlow + setter idiom ~70-76, 116-128, 204-257)
    - app/src/main/java/works/mees/dinghy/net/MoonrakerSession.kt (the file being modified — the configfile `runCatching` block ~490-527; `setMinExtrudeTemp`/`setScrewsTiltConfig` calls; `extruderCfg` already read ~504)
    - 17-RESEARCH.md § "Config-Baseline Reset Paths" (the config-key table; the ⚠ smooth-time `pressure_advance_smooth_time` mismatch; speed/flow reset = protocol 100% no baseline; part-fan = NO baseline) + § Pitfall 2/3
    - 17-PATTERNS.md § "state/PrinterStateStore.kt + net/MoonrakerSession.kt"
  </read_first>
  <action>
    Per RESEARCH § Config-Baseline Reset Paths (D-16): add one-shot StateFlow + setter pairs to
    `PrinterStateStore` mirroring `minExtrudeTemp` — `baselineMaxVelocity`, `baselineMaxAccel`,
    `baselineMinCruise`, `baselineScv`, `baselinePressureAdvance`, `baselineSmoothTime`, and the four
    `baselineRetraction*` (all nullable, default null). Speed % and Flow % get NO baseline StateFlow — their
    reset is protocol-neutral `M220 S100`/`M221 S100` (D-16), handled in the holder/UI. The part-cooling fan
    gets NO baseline either — Klipper `[fan]` has no persistent configured speed (RESEARCH A2 / Open-Q1);
    record in a code comment that the part-fan tile has no reset affordance (owner confirms at UAT). NOTE: when
    a baseline StateFlow is null (e.g. firmware_retraction on dev printers, or any absent config section) the
    holder's reset for that tuner must be a NO-OP (REVIEW #3 — never dispatch a bare/invalid command); this
    plan just provides the nullable baselines, 17-05 enforces the no-op.

    In `MoonrakerSession`, inside the EXISTING configfile `runCatching` block (no second query — Pitfall 3),
    add `store.setBaseline*` calls reading `configfile.settings.printer.{max_velocity,max_accel,
    minimum_cruise_ratio,square_corner_velocity}`, `configfile.settings.extruder.pressure_advance`, and
    `configfile.settings.extruder.pressure_advance_smooth_time` ⚠ (NOT `smooth_time` — config key ≠ status
    field, Pitfall 2), plus `configfile.settings.firmware_retraction.{retract_length,retract_speed,
    unretract_extra_length,unretract_speed}` (build-blind; null on dev printers, safe).
    NO fenced code — implement against the cited analog lines.
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --tests 'works.mees.dinghy.state.PrinterStateStoreTest' --tests 'works.mees.dinghy.net.MoonrakerSessionTest' --no-daemon"</automated>
  </verify>
  <acceptance_criteria>
    - The directly-touched store/session tests GREEN (`PrinterStateStoreTest` / `MoonrakerSessionTest` — substitute the actual class names if they differ; verify by class, NOT a full-suite sweep, since 17-01's holder/nav RED stubs are still active until Waves 2–3, REVIEW #1).
    - `grep -c "pressure_advance_smooth_time" app/src/main/java/works/mees/dinghy/net/MoonrakerSession.kt` == 1 (the smooth-time baseline reads the CONFIG key, not the status field).
    - `grep -n "fun setBaseline" app/src/main/java/works/mees/dinghy/state/PrinterStateStore.kt` shows the new setters; speed/flow/part-fan have NO baseline StateFlow (verified by absence + the documented comment).
    - Only ONE configfile query exists in the handshake (no duplicate added) — confirm the new reads are inside the existing runCatching block.
  </acceptance_criteria>
  <done>Reset baselines read once at handshake from the correct config keys (smooth-time mismatch handled); speed/flow/part-fan correctly have no baseline; the touched store/session tests GREEN (full-suite-green deferred to 17-06).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Moonraker websocket (notify_status_update / configfile) → PrinterStateReducer / MoonrakerSession | Untrusted/loose JSON from the printer is parsed null-safe; a missing or malformed field retains/null, never crashes (deep-merge-not-replace). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-03-01 | Tampering/DoS | malformed status diff field | mitigate | `doubleOrNullAt` null-safe walks; malformed → null/retained (asserted in reducer diff-merge test) — no crash. |
| T-17-03-02 | Information disclosure | reset reads wrong config key (smooth-time) | mitigate | Baseline reads `pressure_advance_smooth_time` (config) vs `smooth_time` (status) — asserted via grep gate; prevents a silent no-op reset. |
| T-17-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase. |
</threat_model>

<verification>
Run Task 1's targeted reducer test, then Task 2's targeted store/session tests. Expected: reducer test
GREEN, the touched store/session tests GREEN, the two grep gates pass. Do NOT require a full-suite-green
sweep here — 17-01's holder/nav RED stubs are intentionally active until Waves 2–3 (REVIEW #1).
</verification>

<success_criteria>
- TUNE-02/03 readback fields parse + diff-merge-retain (incl. synthetic firmware_retraction); reducer test GREEN.
- TUNE-05 reset baselines read once from the correct config keys; the touched store/session tests GREEN.
- firmware_retraction in the subscribe core; speed/flow/part-fan correctly baseline-less; nullable baselines available for the 17-05 no-op-reset rule.
</success_criteria>

<output>
Create `.planning/phases/17-fine-tune-live-adjust-panel/17-03-SUMMARY.md` when done.
</output>
