---
phase: 17-fine-tune-live-adjust-panel
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/main/res/drawable/speed.xml
  - app/src/main/res/drawable/arrow_shape_up_stack_2.xml
  - app/src/main/res/drawable/sprint.xml
  - app/src/main/res/drawable/directions_boat.xml
  - app/src/main/res/drawable/rounded_corner.xml
  - app/src/main/res/drawable/output_circle.xml
  - app/src/main/res/drawable/text_select_move_forward.xml
  - app/src/main/res/drawable/avg_time.xml
  - app/src/main/res/drawable/mode_fan.xml
  - app/src/main/res/drawable/input_circle.xml
  - app/src/main/res/drawable/keyboard_return.xml
autonomous: true
requirements: [TUNE-07]
must_haves:
  truths:
    - "Every Fine-Tune tile/affordance glyph (D-17) exists as a project-local Android vector drawable, tintable at runtime via Icon(tint=token) — NOT the Material Symbols font."
    - "The drawable set matches the chosen glyph names folded into img/material-icon-bucket.json; the debug build with all 11 drawables installs without resource errors."
  artifacts:
    - path: "app/src/main/res/drawable/speed.xml"
      provides: "Speed % tile glyph (and the template for the other 10)"
      contains: "<vector"
  key_links:
    - from: "FineTuneTile / screens (17-05)"
      to: "these drawables"
      via: "painterResource(R.drawable.<glyph>)"
      pattern: "R\\.drawable\\.(speed|sprint|mode_fan|output_circle|avg_time)"
---

<objective>
Export the 11 new per-icon vector drawables for the Fine-Tune glyphs chosen in the staging note (D-17),
sourced from the Material Symbols Outlined set the owner-maintained `img/material-icon-bucket.json` bookmarks.

Purpose: The app renders icons as individual vector drawables via `painterResource` + `Icon(tint=...)` (NOT
the Material Symbols font) — minSdk-23 / Adreno-320 has no font dependency (D-17). This plan provides the
asset substrate the 17-05 UI consumes; it is fully parallel to the code waves (disjoint files).

Output: 11 new `res/drawable/*.xml` vector drawables (`add`/`remove` already exist and are reused).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/17-fine-tune-live-adjust-panel/17-CONTEXT.md
@.planning/phases/17-fine-tune-live-adjust-panel/17-RESEARCH.md
@.planning/phases/17-fine-tune-live-adjust-panel/17-PATTERNS.md
@img/material-icon-bucket.json
@app/src/main/res/drawable/add.xml
@app/src/main/res/drawable/nozzle.xml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Export the 11 Fine-Tune vector drawables</name>
  <files>app/src/main/res/drawable/speed.xml, app/src/main/res/drawable/arrow_shape_up_stack_2.xml, app/src/main/res/drawable/sprint.xml, app/src/main/res/drawable/directions_boat.xml, app/src/main/res/drawable/rounded_corner.xml, app/src/main/res/drawable/output_circle.xml, app/src/main/res/drawable/text_select_move_forward.xml, app/src/main/res/drawable/avg_time.xml, app/src/main/res/drawable/mode_fan.xml, app/src/main/res/drawable/input_circle.xml, app/src/main/res/drawable/keyboard_return.xml</files>
  <read_first>
    - app/src/main/res/drawable/add.xml (the drawable shape template — white fill #FFFFFFFF, 24dp viewport, recolored at runtime via Icon(tint))
    - app/src/main/res/drawable/nozzle.xml (a second exemplar with a real pathData)
    - img/material-icon-bucket.json (the canonical glyph registry — all 13 P17 names verified PRESENT; these define which Material Symbols Outlined glyph each name maps to)
    - 17-RESEARCH.md § "Package Legitimacy / Asset Audit" (the 11-to-export table; `add`/`remove` already exist) and 17-CONTEXT.md § D-17 (the per-control glyph assignments)
    - 17-PATTERNS.md § "Icons: res/drawable/*.xml (asset)"
  </read_first>
  <action>
    Per D-17 + RESEARCH § Asset Audit: export 11 Android vector drawable XMLs, one per chosen glyph name:
    `speed` (Speed %), `arrow_shape_up_stack_2` (Max velocity), `sprint` (Max acceleration), `directions_boat`
    (Minimum cruise ratio), `rounded_corner` (Square-corner velocity), `output_circle` (Flow %),
    `text_select_move_forward` (Pressure advance), `avg_time` (Smooth time), `mode_fan` (Part-cooling fan),
    `input_circle` (Firmware-retraction entry), `keyboard_return` (Back).

    Source each glyph's SVG path from the Material Symbols Outlined set referenced by
    `img/material-icon-bucket.json` (the two unusual ligatures `arrow_shape_up_stack_2` and
    `text_select_move_forward` are owner-confirmed REAL). Convert SVG → Android vector XML matching the
    `add.xml` shape exactly: `android:width/height="24dp"`, `viewportWidth/Height="24"`, a single (or few)
    `<path>` with `android:fillColor="#FFFFFFFF"` (white fill so runtime `Icon(tint = t.<role>)` recolors per
    token — never bake a token color into the asset). Mechanical asset task, NOT a dependency add.

    `add`/`remove` (the generic ± glyphs, D-17) already exist in `res/drawable/` — do NOT re-export them; 17-05
    reuses them. The optional distinct FW-retraction sub-control glyphs are NOT exported here (RESEARCH A5:
    reuse `input_circle`/`output_circle`/`sprint` unless the owner asks for distinct ones).
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:assembleDebug --no-daemon"</automated>
  </verify>
  <acceptance_criteria>
    - `:app:assembleDebug` SUCCEEDS (all 11 vector drawables are valid resources — `aapt`/resource-linking passes).
    - `ls app/src/main/res/drawable/ | grep -E "^(speed|arrow_shape_up_stack_2|sprint|directions_boat|rounded_corner|output_circle|text_select_move_forward|avg_time|mode_fan|input_circle|keyboard_return)\.xml$" | wc -l` == 11.
    - Each new XML contains `<vector` and `android:fillColor="#FFFFFFFF"` (white fill, runtime-tintable; no baked token color) — `grep -L 'fillColor="#FFFFFFFF"' app/src/main/res/drawable/{speed,sprint,...}.xml` returns nothing.
  </acceptance_criteria>
  <done>All 11 chosen Fine-Tune glyphs exist as runtime-tintable vector drawables; debug build assembles clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a (static asset plan) | Project-local vector XML assets only; no runtime input, network, or trust boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-04-01 | Tampering | icon-asset provenance | mitigate | Glyphs sourced ONLY from the owner-maintained `img/material-icon-bucket.json` registry (all 13 names verified present); none invented — the bucket is the provenance record (D-17). |
| T-17-SC | Tampering | npm/pip/cargo installs | accept | No package installs; SVG→XML is a mechanical project-local export, not a dependency (RESEARCH Asset Audit). |
</threat_model>

<verification>
Run `:app:assembleDebug`. Expected: clean resource linking with all 11 drawables present; the count + fill-color
grep gates pass.
</verification>

<success_criteria>
- TUNE-07: 11 new tintable vector drawables exist matching the D-17 glyph names; `add`/`remove` reused.
- Debug build assembles without resource errors.
</success_criteria>

<output>
Create `.planning/phases/17-fine-tune-live-adjust-panel/17-04-SUMMARY.md` when done.
</output>
