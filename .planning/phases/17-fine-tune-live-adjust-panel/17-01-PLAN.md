---
phase: 17-fine-tune-live-adjust-panel
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - app/src/test/java/works/mees/dinghy/command/PrinterCommandsTest.kt
  - app/src/test/java/works/mees/dinghy/command/CommandRegistryGcodeTest.kt
  - app/src/test/java/works/mees/dinghy/state/PrinterStateReducerTest.kt
  - app/src/test/java/works/mees/dinghy/ui/finetune/FineTuneHolderTest.kt
  - app/src/androidTest/java/works/mees/dinghy/ui/finetune/FineTuneNavTest.kt
autonomous: true
requirements: [TUNE-02, TUNE-03, TUNE-04, TUNE-05, TUNE-06, TUNE-07, TUNE-01]
must_haves:
  truths:
    - "The whole test sourceset compiles before any --tests filter runs (no forward-ref to a later-wave symbol bricks the suite)."
    - "Each new test asserts a concrete, falsifiable Fine-Tune behavior (exact gcode incl. scaling, spec membership, reducer field-read, vm scaling/gates/busy-lock, nav route) and is RED until the building wave lands."
  artifacts:
    - path: "app/src/test/java/works/mees/dinghy/ui/finetune/FineTuneHolderTest.kt"
      provides: "Host test for FineTuneHolder vm scaling, capability gates, baseline reset, whole-group busy-lock"
    - path: "app/src/androidTest/java/works/mees/dinghy/ui/finetune/FineTuneNavTest.kt"
      provides: "Instrumented test: PrintStatusControlAction.Tune navigates to Dest.FineTune"
  key_links:
    - from: "PrinterCommandsTest"
      to: "PrinterCommands.speedFactor/setFan/setVelocityLimit/setPressureAdvance/setRetraction"
      via: "assertEquals on exact gcode strings incl. scaling (1.05 ratio→S105, 60%→M106 S153)"
      pattern: "S105|S153|SET_VELOCITY_LIMIT"
---

<objective>
Wave 0 — author the RED test scaffolds that pin every Fine-Tune behavior BEFORE the building waves
land. This plan establishes the executable Nyquist gate for TUNE-01..07.

Purpose: Lock the exact contracts (gcode strings + scaling, registry membership, reducer field-reads,
holder vm derivation, nav route) so later waves implement against a fixed target. Honors the recurring
project trap [[dinghy-wave0-red-scaffold-compile]]: Gradle compiles the WHOLE test sourceset before the
`--tests` filter applies, so a scaffold that forward-references an unbuilt symbol bricks every per-wave
test run.

Output: 3 extended test files + 2 new test files, all compiling, all RED (or `fail()`-stubbed) until
their building plan turns them GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-fine-tune-live-adjust-panel/17-CONTEXT.md
@.planning/phases/17-fine-tune-live-adjust-panel/17-RESEARCH.md
@.planning/phases/17-fine-tune-live-adjust-panel/17-PATTERNS.md
@.planning/phases/17-fine-tune-live-adjust-panel/17-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED command-layer tests — exact gcode + scaling + registry membership</name>
  <files>app/src/test/java/works/mees/dinghy/command/PrinterCommandsTest.kt, app/src/test/java/works/mees/dinghy/command/CommandRegistryGcodeTest.kt</files>
  <read_first>
    - app/src/test/java/works/mees/dinghy/command/PrinterCommandsTest.kt (the file being modified — existing clamp/format assertion style)
    - app/src/test/java/works/mees/dinghy/command/CommandRegistryGcodeTest.kt (the file being modified — `.all` membership + availability assertions)
    - 17-RESEARCH.md § "Command Param + Range Reference" (exact gcode + the suggested MIN/MAX/step clamp table) and § "Scaling notes" (1.05 ratio→S105; 60%→M106 S153)
    - 17-PATTERNS.md § "command/PrinterCommands.kt" and § "command/CommandRegistry.kt" (builder + spec idioms, distinct dispatchKey per motion-limit field)
  </read_first>
  <action>
    CRITICAL: this plan must compile alone. The new builders (`PrinterCommands.speedFactor`, `flowFactor`,
    `setVelocityLimit`, `setPressureAdvance`, `setFan`, `setRetraction`) and the new `CommandRegistry`
    specs do NOT exist yet (they land in 17-02). So write these tests so the test SOURCESET compiles today:
    use `fail("RED — implemented in 17-02")` bodies (or `@Ignore`-free `fail()` assertions) that reference
    ONLY symbols that exist now. Do NOT import or call the unbuilt builders/specs at compile scope — describe
    the expected string in the `fail()` message so 17-02 can convert each stub to a real `assertEquals`.

    Extend `PrinterCommandsTest`: add `fail()`-stubbed test methods named for each builder + scaling case —
    `speedFactor_clampsAndFormats` (M220 S<pct>, clamp 25..300), `flowFactor_*` (M221 S<pct>, clamp 50..150),
    `setVelocityLimit_singleField_*` (one field per call: `SET_VELOCITY_LIMIT VELOCITY=`, `ACCEL=`,
    `MINIMUM_CRUISE_RATIO=` clamped 0.0..1.0, `SQUARE_CORNER_VELOCITY=`), `setPressureAdvance_advance_*`
    (`SET_PRESSURE_ADVANCE ADVANCE=` up to 3dp), `setPressureAdvance_smoothTime_*`
    (`SET_PRESSURE_ADVANCE SMOOTH_TIME=` 2dp), `setFan_pctToPwm` (60% → `M106 S153`, 100% → `M106 S255`,
    0% → `M106 S0`, compute from displayed % each tap — no rounding accumulation),
    `setRetraction_fourFields` (`SET_RETRACTION RETRACT_LENGTH= RETRACT_SPEED= UNRETRACT_EXTRA_LENGTH=
    UNRETRACT_SPEED=`, NO Z_HOP), `localeUS_noCommaInDouble` (assert no `,` reaches gcode for a value like 0.05).
    Each `fail()` message carries the exact expected string for 17-02.

    Extend `CommandRegistryGcodeTest`: add `fail()`-stubbed methods asserting each new spec is in
    `CommandRegistry.all` with the correct `AvailabilityPredicate.ObjectPresent` — `speedFactor`/`flowFactor`
    → `gcode_move`; `setVelocityLimit` (×fields) → `toolhead`; `setPressureAdvance` → `extruder`; `setFan`
    → `fan`; `setRetraction` → `firmware_retraction`. Also a stub asserting motion-limit specs use DISTINCT
    dispatchKeys per field (Pitfall 4: not a shared `set_vel_limit`).
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --tests 'works.mees.dinghy.command.PrinterCommandsTest' --tests 'works.mees.dinghy.command.CommandRegistryGcodeTest' --no-daemon"</automated>
  </verify>
  <acceptance_criteria>
    - `:app:compileDebugUnitTestKotlin` SUCCEEDS (the whole test sourceset compiles — proves no forward-ref to an unbuilt 17-02 symbol).
    - The named test methods exist and FAIL (RED) with messages carrying the exact expected gcode strings (`M220 S105`, `M106 S153`, `SET_RETRACTION ...` with no `Z_HOP`).
    - No import of any not-yet-existing `PrinterCommands` builder or `CommandRegistry` spec at compile scope.
  </acceptance_criteria>
  <done>Command-layer RED scaffolds compile and fail with the exact target strings documented; 17-02 converts them to real assertions.</done>
</task>

<task type="auto">
  <name>Task 2: RED state + holder tests + instrumented nav test</name>
  <files>app/src/test/java/works/mees/dinghy/state/PrinterStateReducerTest.kt, app/src/test/java/works/mees/dinghy/ui/finetune/FineTuneHolderTest.kt, app/src/androidTest/java/works/mees/dinghy/ui/finetune/FineTuneNavTest.kt</files>
  <read_first>
    - app/src/test/java/works/mees/dinghy/state/PrinterStateReducerTest.kt (the file being modified — fixture-fed reduceDiff + diff-merge-retains style)
    - app/src/test/java/works/mees/dinghy/ui/extrude/ExtrudeHolderTest.kt (analog for the NEW FineTuneHolderTest — runTest(UnconfinedTestDispatcher), real PrinterStateStore, setCapabilities + seed, runCurrent, assert vm)
    - 17-RESEARCH.md § "Reducer additions" (new fields, synthetic firmware_retraction fixture, smooth-time config-key ⚠) and § "Validation Architecture" → Wave 0 Gaps
    - 17-PATTERNS.md § "state/PrinterState.kt + PrinterStateReducer.kt" and § "Wave-0 test extensions"
  </read_first>
  <action>
    Same compile-safety rule: new `PrinterState` fields (`maxVelocity`, `maxAccel`, `minimumCruiseRatio`,
    `squareCornerVelocity`, `pressureAdvance`, `smoothTime`, `partFanSpeed`, `firmwareRetraction`) and the
    `FineTuneHolder`/`FineTuneVm` do NOT exist yet (17-03 / 17-05). Use `fail("RED — 17-03")` /
    `fail("RED — 17-05")` bodies that reference ONLY existing symbols.

    Extend `PrinterStateReducerTest`: add `fail()`-stubbed methods — `reduces_toolhead_motion_limits`
    (feed a synthetic `toolhead` diff with max_velocity/max_accel/minimum_cruise_ratio/square_corner_velocity;
    expected → the four new state fields), `reduces_extruder_pa_and_smoothtime` (`extruder.pressure_advance`
    + `smooth_time` from a SEPARATE extruder walk, NOT the heater loop), `reduces_fan_speed` (`fan.speed`
    0.6 → `partFanSpeed=0.6`, RAW ratio — no scaling in reducer), `reduces_firmware_retraction_synthetic`
    (synthetic `firmware_retraction` object → FirmwareRetractionObject, the only way to test build-blind),
    `diffMerge_retains_omitted_finetune_fields` (a partial diff omitting a field retains the prior value).
    Each message names the field + expected value.

    Create `FineTuneHolderTest.kt` (new, mirror ExtrudeHolderTest): `fail()`-stubbed methods —
    `vm_scales_ratio_to_percent` (speedFactor 1.05 → speedPct 105; extrudeFactor 1.0 → flowPct 100),
    `vm_scales_fan_0to1_to_percent` (partFanSpeed 0.6 → partFanPct 60), `vm_gates_on_capabilities`
    (hasFan = hasObject("fan"); hasFwRetraction = hasObject("firmware_retraction")), `vm_folds_baselines`
    (config-baseline StateFlows fold into vm deterministically), `groupBusy_true_when_any_inflight`
    (whole-group busy-lock D-15 derivation), `nullValue_shows_dash_not_zero` (unreported → "—" / null, never fabricated 0).

    Create `FineTuneNavTest.kt` (new instrumented, `app/src/androidTest/...`): a `fail()`-stubbed (or
    @Ignore-free skeleton compiling against existing test infra) instrumented test asserting that invoking
    `PrintStatusControlAction.Tune` routes to `Dest.FineTune`. It must COMPILE against the current shell test
    harness; the real assertion lands in 17-06. Reference only `Dest`/route symbols that already exist (do
    NOT reference `Dest.FineTune` at compile scope yet — assert via the action handler indirection or leave a
    `fail("RED — 17-06: Tune → Dest.FineTune")`).
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --tests 'works.mees.dinghy.state.PrinterStateReducerTest' --tests 'works.mees.dinghy.ui.finetune.FineTuneHolderTest' --no-daemon"</automated>
  </verify>
  <acceptance_criteria>
    - `:app:compileDebugUnitTestKotlin` and `:app:compileDebugAndroidTestKotlin` SUCCEED (no forward-ref bricks the sourceset).
    - `PrinterStateReducerTest` and `FineTuneHolderTest` named methods exist and FAIL (RED), messages naming the field/value/gate they pin.
    - The synthetic `firmware_retraction` reducer case is present (build-blind coverage).
    - `FineTuneNavTest.kt` exists under `androidTest` and compiles.
  </acceptance_criteria>
  <done>State + holder + nav RED scaffolds compile and fail with documented targets; 17-03/17-05/17-06 turn them GREEN in turn.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a (test-only plan) | This plan adds host + instrumented tests only; no runtime trust boundary crosses here. The validated boundary (numeric ±-nudge → gcode string → Moonraker) is exercised by the assertions, not introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01-V5 | Tampering | gcode builder scaling/clamp (asserted here) | mitigate | Tests pin clamp-before-format + Locale.US: `setFan` 0..255 from 0..100% clamp, no comma in a formatted Double — the assertions are the guard rails 17-02 must satisfy. |
| T-17-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase (RESEARCH: zero new deps; only project-local vector drawables). No legitimacy gate needed. |
</threat_model>

<verification>
Run both verify commands above. Expected: clean compile of the full test sourceset; the new methods RED.
Then full suite still compiles: `:app:testDebugUnitTest --no-daemon` (other tests must remain GREEN —
adding RED stubs must not break unrelated suites).
</verification>

<success_criteria>
- All 5 test files compile as part of the whole test sourceset (the [[dinghy-wave0-red-scaffold-compile]] gate).
- Every Wave-0 gap from 17-VALIDATION.md has a named RED test with a documented target string/value.
- No unrelated existing test regresses to red merely from these additions.
</success_criteria>

<output>
Create `.planning/phases/17-fine-tune-live-adjust-panel/17-01-SUMMARY.md` when done.
</output>
