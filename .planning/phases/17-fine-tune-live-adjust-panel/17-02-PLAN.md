---
phase: 17-fine-tune-live-adjust-panel
plan: 02
type: execute
wave: 1
depends_on: ["17-01"]
files_modified:
  - app/src/main/java/works/mees/dinghy/command/PrinterCommands.kt
  - app/src/main/java/works/mees/dinghy/command/CommandRegistry.kt
  - app/src/test/java/works/mees/dinghy/command/PrinterCommandsTest.kt
  - app/src/test/java/works/mees/dinghy/command/CommandRegistryGcodeTest.kt
autonomous: true
requirements: [TUNE-02, TUNE-03, TUNE-04, TUNE-05]
must_haves:
  truths:
    - "Each live-adjust gcode string is built clamp-before-format, Locale.US, with display-vs-wire scaling correct (M220/M221 percent; M106 0..255; MINIMUM_CRUISE_RATIO ratio-on-wire/percent-on-screen; SET_VELOCITY_LIMIT/SET_PRESSURE_ADVANCE/SET_RETRACTION exact params)."
    - "Every new command is registered in CommandRegistry.all, gated on its owning printer object, with a distinct dispatchKey per motion-limit field."
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/command/PrinterCommands.kt"
      provides: "Pure clamp-before-format builders: speedFactor, flowFactor, setVelocityLimit, setPressureAdvance, setFan, setRetraction"
      contains: "fun speedFactor"
    - path: "app/src/main/java/works/mees/dinghy/command/CommandRegistry.kt"
      provides: "gcode() specs for all six command families + arg data classes, registered in .all"
      contains: "setVelocityLimit"
  key_links:
    - from: "CommandRegistry specs"
      to: "PrinterCommands builders"
      via: "gcode = { PrinterCommands.<builder>(...) }"
      pattern: "PrinterCommands\\.(speedFactor|flowFactor|setVelocityLimit|setPressureAdvance|setFan|setRetraction)"
    - from: "CommandRegistry specs"
      to: "Capabilities.hasObject gating"
      via: "AvailabilityPredicate.ObjectPresent(<object>)"
      pattern: "ObjectPresent\\(\"(gcode_move|toolhead|extruder|fan|firmware_retraction)\""
---

<objective>
Build the pure command layer for Fine-Tune: the clamp-before-format gcode builders (D-03..D-12) and the
canonical `CommandRegistry` specs that dispatch them, capability-gated on the owning printer object.

Purpose: All printer mutations funnel through the existing dispatcher; new commands are `gcode()` specs,
never raw rpc from the UI. This plan turns the 17-01 command-layer RED tests GREEN. The command set IS the
two-category failure-mode tuner (D-01: Motion commands M220/SET_VELOCITY_LIMIT vs Extrusion commands
M221/SET_PRESSURE_ADVANCE/M106/SET_RETRACTION). Per-control step increments are FIXED/app-defined this phase
and baked into the builders/UI (D-18: increment customization is DEFERRED — do NOT build any
increment-customization surface here).

REVIEW NOTE (HIGH #1): 17-01 plants ACTIVE `fail()` RED stubs in the holder/nav tests that stay RED until
Waves 2–3. Therefore this plan verifies ONLY its own targeted classes (`PrinterCommandsTest`,
`CommandRegistryGcodeTest`) — it does NOT require the full host suite green (that gate moves to the final
gate, 17-06, after all waves convert RED→GREEN). The ONE exception is the catalog-drift test, which this
plan touches directly (run it by class, not via a full-suite sweep).

Output: ~6 new pure builders in `PrinterCommands.kt`, ~6 `gcode()` specs + arg data classes in
`CommandRegistry.kt` (registered in `.all`), and the converted PrinterCommandsTest / CommandRegistryGcodeTest.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-fine-tune-live-adjust-panel/17-CONTEXT.md
@.planning/phases/17-fine-tune-live-adjust-panel/17-RESEARCH.md
@.planning/phases/17-fine-tune-live-adjust-panel/17-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure gcode builders in PrinterCommands.kt</name>
  <files>app/src/main/java/works/mees/dinghy/command/PrinterCommands.kt, app/src/test/java/works/mees/dinghy/command/PrinterCommandsTest.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/command/PrinterCommands.kt (the file being modified — `jog` clamp idiom ~150-155, `extrude` ~195-199, `setGcodeOffsetZAdjust` ~231-241, `formatZ` Locale.US trailing-zero-strip ~300-304, named bound consts block ~29-59)
    - 17-RESEARCH.md § "Command Param + Range Reference" (exact param spellings; the MIN/MAX/step table) + § "Scaling notes" (the load-bearing display-vs-wire conversions) + § Pattern 3
    - 17-PATTERNS.md § "command/PrinterCommands.kt" (the SET_VELOCITY_LIMIT multi-field buildString idiom + per-precision fmt)
  </read_first>
  <behavior>
    - speedFactor(105) == "M220 S105"; clamps to SPEED_PCT_MIN(25)..SPEED_PCT_MAX(300) (per D-03; range per ROADMAP SC-3 / RESEARCH A1).
    - flowFactor(101) == "M221 S101"; clamps to 50..150 (D-08 WIDE).
    - setVelocityLimit(velocity=250.0) == "SET_VELOCITY_LIMIT VELOCITY=250"; accel=3000.0 → "...ACCEL=3000"; minCruiseRatio=0.55 → "...MINIMUM_CRUISE_RATIO=0.55" (clamp 0.0..1.0); scv=8.0 → "...SQUARE_CORNER_VELOCITY=8". One field per call.
    - REVIEW #9 — minCruiseRatio is the RATIO on the wire: the UI displays it as percent (0.5↔50%) but the builder receives/sends the ratio; setVelocityLimit(minCruiseRatio=0.55) → "...MINIMUM_CRUISE_RATIO=0.55" (a +tap from a live 0.5 = +0.05 ratio).
    - setPressureAdvance(advance=0.045) == "SET_PRESSURE_ADVANCE ADVANCE=0.045" (up to 3dp, trailing-zero strip); setPressureAdvance(smoothTime=0.04) == "SET_PRESSURE_ADVANCE SMOOTH_TIME=0.04" (2dp). No EXTRUDER= (single-extruder v1, D-09).
    - setFan(60) == "M106 S153"; setFan(100) == "M106 S255"; setFan(0) == "M106 S0" — round(pct/100*255), computed from the displayed % (no accumulation). Clamp 0..100.
    - setRetraction(retractLength=0.5, unretractExtraLength=0.0, retractSpeed=35, unretractSpeed=35) == "SET_RETRACTION RETRACT_LENGTH=0.5 RETRACT_SPEED=35 UNRETRACT_EXTRA_LENGTH=0 UNRETRACT_SPEED=35" — no Z_HOP param (D-12).
    - No locale comma ever reaches a formatted Double (0.05 stays "0.05", never "0,05").
  </behavior>
  <action>
    Per D-03..D-12 and RESEARCH § Command Param Reference: add named bound consts to the existing consts block
    (SPEED_PCT_MIN/MAX, FLOW_PCT_MIN/MAX, VEL_MIN/MAX, ACCEL_MIN/MAX, SCV_MIN/MAX, PA_MIN/MAX, SMOOTH_MIN/MAX,
    RETRACT_LEN_MIN/MAX, UNRETRACT_EXTRA_MIN/MAX, RETRACT_SPEED_MIN/MAX) using the RESEARCH MIN/MAX table
    (these are UI-side guard rails — Klipper is the real authority and rejects out-of-range as a non-fatal toast).

    Add `speedFactor(pct: Int)` = M220 (per D-03) and `flowFactor(pct: Int)` = M221 (per D-08), both
    `coerceIn`-clamped. Add `setVelocityLimit(velocity, accel, minCruiseRatio, scv: Double? = null)` as the
    nullable-field buildString (per D-04/D-05/D-06/D-07) — appends ONLY the non-null field, each clamped. The
    `minCruiseRatio` arg is the RATIO (0.0..1.0); display-to-percent conversion happens in the holder/UI, never
    here (REVIEW #9 — wire carries the ratio). Add `setPressureAdvance(advance: Double? = null, smoothTime:
    Double? = null)` (per D-09/D-10) appending only the set field. Add `setFan(pct: Int)` = M106 with the
    0..100% → 0..255 conversion (per D-11). Add `setRetraction(retractLength, unretractExtraLength, retractSpeed,
    unretractSpeed)` (per D-12), no Z_HOP.

    Generalize `formatZ` into a per-precision `fmt(v, decimals)` (Locale.US + trailing-zero strip) reused for
    PA(3dp)/smooth/SCV/min-cruise(2dp)/retraction-length(1dp) — or keep `formatZ` and add precision-specific
    helpers; do NOT inline String.format at call sites (avoid the locale-comma trap, ASVS V5).

    Convert the 17-01 PrinterCommandsTest `fail()` stubs into real `assertEquals` using the exact strings above
    (including the `MINIMUM_CRUISE_RATIO=0.55` percent-display/ratio-wire case, REVIEW #9).
    NO fenced code in this action — implement against the cited analog lines.
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --tests 'works.mees.dinghy.command.PrinterCommandsTest' --no-daemon"</automated>
  </verify>
  <acceptance_criteria>
    - PrinterCommandsTest GREEN: every behavior assertion above passes verbatim (incl. `MINIMUM_CRUISE_RATIO=0.55`, REVIEW #9).
    - `grep -n "fun speedFactor\|fun flowFactor\|fun setVelocityLimit\|fun setPressureAdvance\|fun setFan\|fun setRetraction" app/src/main/java/works/mees/dinghy/command/PrinterCommands.kt` returns all six.
    - No `String.format(` without `Locale.US` in the new builders; every numeric `coerceIn`-clamped before format.
  </acceptance_criteria>
  <done>All six builders produce the exact, clamped, scaled, Locale.US gcode strings (incl. min-cruise ratio-on-wire); PrinterCommandsTest GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CommandRegistry gcode() specs + arg data classes, registered in .all</name>
  <files>app/src/main/java/works/mees/dinghy/command/CommandRegistry.kt, app/src/test/java/works/mees/dinghy/command/CommandRegistryGcodeTest.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/command/CommandRegistry.kt (the file being modified — the `gcode()` factory ~624-637, `babystepZ` spec ~526-531, `extrude` per-arg key ~379-384, `quadGantryLevel` build-blind precedent ~438-444, the `.all` list ~544, arg data classes ~25/62)
    - 17-RESEARCH.md § "Capability-Gate Predicates" (gate on the OWNING object; NEVER GcodeCommandPresent) + § Pattern 3 (distinct key per field)
    - 17-PATTERNS.md § "command/CommandRegistry.kt" (spec idiom, distinct dispatchKey per motion-limit field, register-in-.all)
  </read_first>
  <behavior>
    - `CommandRegistry.all` contains: speedFactor, flowFactor, setVelocityLimit (×4 fields or 1 spec keyed by field), setPressureAdvance (advance + smoothTime), setFan, setRetraction.
    - speedFactor/flowFactor availability == ObjectPresent("gcode_move").
    - setVelocityLimit availability == ObjectPresent("toolhead").
    - setPressureAdvance availability == ObjectPresent("extruder").
    - setFan availability == ObjectPresent("fan").
    - setRetraction availability == ObjectPresent("firmware_retraction").
    - Motion-limit nudges use DISTINCT dispatchKeys per field (e.g. set_vel_velocity vs set_vel_accel) — not a shared key (Pitfall 4).
  </behavior>
  <action>
    Per RESEARCH § Pattern 3 + PATTERNS § CommandRegistry: add arg data classes (e.g. `SpeedFactorArgs(pct)`,
    `FlowFactorArgs(pct)`, `VelocityLimitArgs(field, value)` carrying which field + value so one spec serves all
    four motion limits, `PressureAdvanceArgs(field, value)`, `FanArgs(pct)`, `RetractionArgs(...)`). Add a
    `gcode()` spec per family mirroring `babystepZ` — `catalogId` from the docs/commands catalog
    (KGC-M220/M221/SET_VELOCITY_LIMIT/SET_PRESSURE_ADVANCE/M106/SET_RETRACTION), `key` producing a DISTINCT
    dispatchKey per motion-limit field (`"set_vel_${args.field}"`) and per PA field, `gcode` delegating to the
    matching `PrinterCommands` builder, `availability = AvailabilityPredicate.ObjectPresent(<owning object>)`
    per the gate table above (D-02 capability gating; firmware_retraction is the build-blind gate mirroring
    `quadGantryLevel`).

    Register every new spec in `CommandRegistry.all` (the `CommandRegistryGcodeTest` asserts membership). Gate
    STRICTLY on the owning OBJECT — do NOT add or evaluate a `GcodeCommandPresent` help-query path (RESEARCH:
    these are built-in Klipper commands present whenever their object exists).

    If the docs/commands sidecar (`catalog.json` / `printer-matrix.json`) has a drift test that asserts every
    registered catalogId has a catalog row, add the corresponding rows (these commands are already in
    `docs/commands/klipper-gcode.md` per 17-CONTEXT canonical refs) to keep CommandCatalogDriftTest green.

    Convert the 17-01 CommandRegistryGcodeTest `fail()` stubs to real membership + availability + distinct-key
    assertions.
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --tests 'works.mees.dinghy.command.CommandRegistryGcodeTest' --tests 'works.mees.dinghy.command.CommandCatalogDriftTest' --no-daemon"</automated>
  </verify>
  <acceptance_criteria>
    - CommandRegistryGcodeTest GREEN: all six families present in `.all` with the correct ObjectPresent gate and distinct motion-limit keys.
    - `grep -c "ObjectPresent(\"firmware_retraction\")" app/src/main/java/works/mees/dinghy/command/CommandRegistry.kt` == 1 (the build-blind retraction gate exists).
    - CommandCatalogDriftTest GREEN (this plan touches the catalog directly — verified by class, NOT by a full-suite sweep, since 17-01's holder/nav RED stubs are still active until Waves 2–3, REVIEW #1).
  </acceptance_criteria>
  <done>Six command families registered, object-gated, distinct-keyed; CommandRegistryGcodeTest + the directly-touched catalog-drift test GREEN (full-suite-green deferred to 17-06).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Numeric ±-nudge (UI) → gcode string (this layer) → Moonraker websocket | Untrusted-ish numeric input is bounded by ±-nudge (no keyboard); this layer is the clamp/format chokepoint before the string crosses to the printer. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-02-01 | Tampering | PrinterCommands builders | mitigate | clamp-before-format (`coerceIn` named consts) + `Locale.US` on every Double — no free-text into a gcode string; asserted in PrinterCommandsTest. |
| T-17-02-02 | DoS | out-of-range value reaching Klipper | accept | Conservative clamp ranges are UI guard rails; Klipper rejects beyond config maxima and the dispatcher surfaces it as a non-fatal redacted toast (G1 lesson) — never an uncaught crash. |
| T-17-02-03 | Tampering | locale-comma in formatted Double (`0,05`) | mitigate | `String.format(Locale.US,...)` + trailing-zero strip via the shared `fmt` helper; asserted (`localeUS_noCommaInDouble`). |
| T-17-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase (zero new deps). |
</threat_model>

<verification>
Run the two per-task targeted commands. Expected: 17-01's command-layer RED tests now GREEN; the
directly-touched catalog-drift test GREEN. Do NOT require a full-suite-green sweep here — 17-01's holder/nav
RED stubs are intentionally still active until Waves 2–3 (REVIEW #1); the full-suite gate lives at 17-06.
</verification>

<success_criteria>
- TUNE-02/03/04/05 command-layer behaviors are GREEN-tested (exact gcode + scaling + clamp + gating, incl. min-cruise ratio-on-wire).
- All six command families in `CommandRegistry.all`, gated on the owning object, distinct keys per motion-limit field.
- The directly-touched catalog-drift test GREEN (full-suite-green deferred to the final 17-06 gate).
</success_criteria>

<output>
Create `.planning/phases/17-fine-tune-live-adjust-panel/17-02-SUMMARY.md` when done.
</output>
