---
phase: 17-fine-tune-live-adjust-panel
plan: 05
type: execute
wave: 2
depends_on: ["17-02", "17-03", "17-04"]
files_modified:
  - app/src/main/java/works/mees/dinghy/ui/finetune/FineTuneGroup.kt
  - app/src/main/java/works/mees/dinghy/ui/finetune/FineTuneVm.kt
  - app/src/main/java/works/mees/dinghy/ui/finetune/FineTuneHolder.kt
  - app/src/main/java/works/mees/dinghy/ui/finetune/FineTuneTile.kt
  - app/src/main/java/works/mees/dinghy/ui/finetune/FineTuneHubScreen.kt
  - app/src/main/java/works/mees/dinghy/ui/finetune/MotionScreen.kt
  - app/src/main/java/works/mees/dinghy/ui/finetune/ExtrusionScreen.kt
  - app/src/main/java/works/mees/dinghy/ui/finetune/FwRetractionScreen.kt
  - app/src/test/java/works/mees/dinghy/ui/finetune/FineTuneHolderTest.kt
autonomous: true
requirements: [TUNE-02, TUNE-03, TUNE-04, TUNE-05, TUNE-06]
must_haves:
  truths:
    - "The holder transforms live PrinterState + config baselines + capabilities (as a FLOW, re-emitting on reconnect) into a FineTuneVm with display-scaled values (ratio→%, 0..1→%), gates (hasFan/hasFwRetraction), and a state-flip whole-group busy flag."
    - "Each value tile shows icon·name·live-value·−·+; ±dispatches one command per tap immediately and confirms by the reduced state flip (no optimistic local state); tapping the value is inert; long-press the value resets to baseline ONLY when a baseline exists (D-16)."
    - "The whole-group busy flag stays busy until the printer-object value actually flips (inFlight.isNotEmpty() || pendingStateFlip != null), not on the bare RPC ack (D-15); absent tunables are HIDDEN not disabled (capability-gated, SC-2)."
    - "The FW-Retraction mini-screen entry appears on Extrusion only when firmware_retraction is present, navigated via an explicit onFwRetraction callback; the mini-screen itself is build-blind (fixture-tested, no on-device gate)."
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/ui/finetune/FineTuneGroup.kt"
      provides: "Top-level public enum FineTuneGroup { MOTION, EXTRUSION, FW_RETRACTION } consumed by screens + AppShell (17-06)"
      contains: "enum class FineTuneGroup"
    - path: "app/src/main/java/works/mees/dinghy/ui/finetune/FineTuneHolder.kt"
      provides: "Holder(scope, store) → FineTuneVm via combine(printerState, capabilities, baselines){buildVm}"
      contains: "buildVm"
    - path: "app/src/main/java/works/mees/dinghy/ui/finetune/FineTuneTile.kt"
      provides: "Shared C2-derived value tile (tap-inert value, ± action, nullable long-press=reset)"
      contains: "detectTapGestures"
    - path: "app/src/main/java/works/mees/dinghy/ui/finetune/MotionScreen.kt"
      provides: "5 capability-gated Motion tiles"
    - path: "app/src/main/java/works/mees/dinghy/ui/finetune/ExtrusionScreen.kt"
      provides: "flow/PA/smooth/part-fan tiles + FW-retraction entry-if-present (onFwRetraction)"
  key_links:
    - from: "FineTuneTile ±"
      to: "CommandDispatcher.dispatch(spec, args)"
      via: "dispatchCommand funnel computing target from the live displayed value"
      pattern: "dispatcher\\??\\.dispatch"
    - from: "every tile enabled flag"
      to: "dispatcher.inFlight + pendingStateFlip"
      via: "groupBusy = inFlight.isNotEmpty() || pendingStateFlip != null"
      pattern: "pendingStateFlip"
---

<objective>
Build the Fine-Tune UI: the headless holder + view-model, the top-level group enum, the shared value tile,
and the four screens (Hub, Motion, Extrusion, FW-Retraction). All assembled from the Move/Extrude/Calibration
idioms; the genuinely-novel pieces are the tap-inert/long-press-reset tile semantics (D-14/D-16), the
nullable-reset API (REVIEW #3), and the STATE-FLIP whole-group busy-lock (D-15 / REVIEW #2).

Purpose: Deliver the keyboard-free live-adjust surface — failure-mode organized (Motion vs Extrusion),
capability-gated, state-flip-confirmed, no ConfirmGuard. Turns the 17-01 FineTuneHolderTest GREEN. The two
group screens realize the failure-mode split (D-01: Motion = what moves the head; Extrusion = what affects
the filament). Tap increments are FIXED/app-defined per D-03..D-12 and NOT user-customizable this phase
(D-18: no increment-customization UI).

REVIEW NOTE (HIGH #1): this plan turns the 17-01 holder RED stubs GREEN; it verifies the holder test by class
plus a compile of the new screens. The full-suite-green gate lives at 17-06.

Output: 8 new files under `ui/finetune/` (incl. the top-level `FineTuneGroup.kt`) + the converted holder
test. (Navigation wiring + on-device UAT are 17-06 — this plan does NOT touch the shell; screens are reachable
only after 17-06 wires the route.)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-fine-tune-live-adjust-panel/17-CONTEXT.md
@.planning/phases/17-fine-tune-live-adjust-panel/17-RESEARCH.md
@.planning/phases/17-fine-tune-live-adjust-panel/17-PATTERNS.md
@docs/ui_design/LAYOUT.md
@docs/ui_design/THEMING.md
@app/src/main/java/works/mees/dinghy/ui/extrude/ExtrudeScreen.kt
@app/src/main/java/works/mees/dinghy/ui/extrude/ExtrudeHolder.kt
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: FineTuneGroup enum + FineTuneHolder + FineTuneVm (display-scaling, gates, baselines, state-flip busy derivation)</name>
  <files>app/src/main/java/works/mees/dinghy/ui/finetune/FineTuneGroup.kt, app/src/main/java/works/mees/dinghy/ui/finetune/FineTuneVm.kt, app/src/main/java/works/mees/dinghy/ui/finetune/FineTuneHolder.kt, app/src/test/java/works/mees/dinghy/ui/finetune/FineTuneHolderTest.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/extrude/ExtrudeHolder.kt (the analog — `combine(printerState, oneShotFlows){buildVm}` ~44-114; ExtrudeVm data class ~125-139; caps snapshot via store.capabilities.value ~68 — REVIEW #10: prefer the FLOW form here, see action)
    - app/src/test/java/works/mees/dinghy/ui/extrude/ExtrudeHolderTest.kt (the analog for the converted FineTuneHolderTest — runTest(UnconfinedTestDispatcher), real PrinterStateStore, setCapabilities + seed, runCurrent, assert vm)
    - app/src/main/java/works/mees/dinghy/state/PrinterStateStore.kt (the `capabilities` flow shape — confirm it re-emits on reconnect; if it is a StateFlow, fold it as a flow source in combine, REVIEW #10)
    - 17-RESEARCH.md § "Code Examples" (buildVm display-scaling: ratio→%, 0..1→%, null→"—") + § Pattern 2 (busy-lock derivation) + § Pitfall 1/5
    - 17-PATTERNS.md § "ui/finetune/FineTuneHolder.kt + FineTuneVm.kt" (combine arity pitfall → wrap baselines in a single Baselines data class StateFlow OR list-form combine; buildVm(s, caps, baselines) shape)
  </read_first>
  <behavior>
    - speedFactor 1.05 → vm.speedPct 105; extrudeFactor 1.0 → vm.flowPct 100 (ratio→%, D-03/D-08 scaling, Pitfall 1).
    - partFanSpeed 0.6 → vm.partFanPct 60 (0..1→%, D-11).
    - minimumCruiseRatio 0.5 → vm shows 50% (ratio→percent for display); a +tap produces a 0.55 ratio target (REVIEW #9).
    - null maxAccel/pressureAdvance → vm shows "—"/null, never fabricated 0 (D-20).
    - hasFan == caps.hasObject("fan"); hasFwRetraction == caps.hasObject("firmware_retraction") (D-02 gating); caps fold in as a FLOW so a reconnect that changes caps re-emits the vm (REVIEW #10).
    - config baselines fold into vm deterministically the instant the one-shot reads land (combine, not a later diff); a null baseline → that tuner's reset is a no-op (REVIEW #3, enforced where reset is invoked).
    - groupBusy (REVIEW #2 / D-15) is true while `dispatcher.inFlight` is non-empty OR a `pendingStateFlip` target is still outstanding; it CLEARS only when the reduced vm value reaches the dispatched target (state flip) OR on a dispatch failure/timeout — never on the bare RPC ack alone.
  </behavior>
  <action>
    Create `FineTuneGroup.kt` (REVIEW #7): a TOP-LEVEL `enum class FineTuneGroup { MOTION, EXTRUSION,
    FW_RETRACTION }` with PUBLIC (or `internal`) visibility — NOT private/local to a screen file — so both the
    screens here and `AppShell` (17-06) can reference it without the P16-style private-symbol compile blocker
    (echo of P16 `PresetSelector` being `private`). 17-06 read_firsts this file.

    Per RESEARCH § Code Examples + PATTERNS § FineTuneHolder: create `FineTuneVm` as a plain data class (all
    fields defaulted, NO Compose annotations — host-testable) carrying per-tuner display-scaled current values
    (speedPct, flowPct, maxVelocity, maxAccel, minCruisePct [percent for display, REVIEW #9], scv,
    pressureAdvance, smoothTime, partFanPct — nullable where unreported), capability gates (hasFan,
    hasFwRetraction, plus hasObject gates for gcode_move/toolhead/extruder if a printer can lack them), the
    firmware-retraction sub-values, and a `baselines` bundle for reset (nullable per-tuner).

    Create `FineTuneHolder(scope, store)` mirroring ExtrudeHolder, but with TWO review-driven changes:
    (a) REVIEW #10 — fold `store.capabilities` in as a FLOW source in the `combine` (e.g.
    `combine(store.printerState, store.capabilities, <baselines flow>) { state, caps, baselines -> buildVm(...) }`)
    rather than snapshotting `store.capabilities.value` once, so capability changes on reconnect re-emit the vm.
    (If RESEARCH/PATTERNS shows `capabilities` is a plain immutable value that cannot change post-handshake,
    snapshot is acceptable — but state which in a code comment; default to the flow form.) Because there are
    MANY baselines and `combine` overloads cap arity, wrap the baseline StateFlows in a single `Baselines` data
    class flow (or use the list-form combine) per PATTERNS — prefer the single-`baselines`-arg buildVm shape.
    (b) REVIEW #2 / D-15 — provide a STATE-FLIP busy model. Expose a holder API the screen uses to mark a
    `pendingStateFlip` (the dispatched tuner + target value) on each ±dispatch, and derive `groupBusy =
    dispatcher.inFlight.isNotEmpty() || pendingStateFlip != null`. The pendingStateFlip clears when the reduced
    vm value for that tuner reaches the target (state flip observed) OR on a dispatch failure/timeout event.
    Do display scaling in `buildVm` ONLY (ratio→%, 0..1→%) — never read raw ratios into the tile (Pitfall 1).

    Convert the 17-01 FineTuneHolderTest stubs to real assertions: scaling (1.05→105, 0.6 fan→60, 0.5
    minCruise→50%), gates (hasFan/hasFwRetraction), baseline folding, null→dash, the nullable-baseline reset
    no-op (`reset_isNoOp_whenBaselineNull`, REVIEW #3), and the state-flip busy persistence
    (`groupBusy_persists_until_state_flip`, REVIEW #2 — assert busy stays true after inFlight clears until the
    vm value reaches the target).
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --tests 'works.mees.dinghy.ui.finetune.FineTuneHolderTest' --no-daemon"</automated>
  </verify>
  <acceptance_criteria>
    - FineTuneHolderTest GREEN: scaling (1.05→105, 0.6 fan→60, 0.5 minCruise→50%), gates, baseline folding, null→dash, nullable-baseline reset no-op (REVIEW #3), and state-flip busy persistence (REVIEW #2) all pass.
    - `grep -n "enum class FineTuneGroup" app/src/main/java/works/mees/dinghy/ui/finetune/FineTuneGroup.kt` present and NOT `private` (REVIEW #7).
    - `grep -n "pendingStateFlip" app/src/main/java/works/mees/dinghy/ui/finetune/FineTuneHolder.kt` present (state-flip busy model, REVIEW #2); `grep -n "store.capabilities" FineTuneHolder.kt` shows the flow fold (or a documented snapshot-is-safe comment, REVIEW #10).
    - `grep -n "fun buildVm" FineTuneHolder.kt` present; `grep -c "\\* 100\|/ 255\|roundToInt" FineTuneHolder.kt` >= 1 (scaling lives here, not the reducer).
    - FineTuneVm has NO Compose annotations (host-testable, `grep -L "@Composable\|@Stable" FineTuneVm.kt` confirms or manual check).
  </acceptance_criteria>
  <done>Top-level group enum + holder producing a correctly-scaled, gated, baseline-folded vm with a state-flip busy model and capabilities-as-flow; holder test GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: FineTuneTile (nullable-reset value tile) + FineTuneHubScreen</name>
  <files>app/src/main/java/works/mees/dinghy/ui/finetune/FineTuneTile.kt, app/src/main/java/works/mees/dinghy/ui/finetune/FineTuneHubScreen.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/extrude/ExtrudeScreen.kt (SettingReadout ~430-473, FieldButton ~481-513, BigCommand disabled-dim ~393-423, painterResource icon ~234-241, outline/token border ~438-443, fsSp heroes)
    - app/src/main/java/works/mees/dinghy/ui/calibration/CalibrationHubScreen.kt (the hub-of-tiles → onNavigate pattern; DrawerTile grammar reuse)
    - 17-RESEARCH.md § Pattern 1 (tile: tap-inert value via detectTapGestures(onLongPress=onReset); ± are plain clickable; render the LIVE reduced value, no optimistic state) + § "Anti-Patterns to Avoid"
    - 17-PATTERNS.md § "ui/finetune/FineTuneTile.kt" + § "FineTuneHubScreen.kt"
    - docs/ui_design/THEMING.md (C1/C5: arrows performing the screen's expected action = accent; C2 3-cell increment-picker the tile is DERIVED from — D-19) + docs/ui_design/LAYOUT.md (Focus/Field/Gutter; interactive-grid flexible-tile law)
    - The font-scale lesson: use fsSp(baseSp, t.fs) (15sp metadata floor / 18sp body / 26sp tabular stats / 48sp focus value) — [[dinghy-font-sizes-too-small]]
  </read_first>
  <action>
    Per RESEARCH Pattern 1 + PATTERNS § FineTuneTile (D-13/D-14/D-16/D-19): build `FineTuneTile` as a shared
    `@Composable` taking (iconRes, name, valueText, onDecrement, onIncrement, `onReset: (() -> Unit)? = null`,
    enabled). REVIEW #3 — `onReset` is NULLABLE: install the long-press handler ONLY when `onReset != null`
    (`if (onReset != null) Modifier.pointerInput { detectTapGestures(onLongPress = { onReset() }) }` else no
    long-press modifier); when null, the value cell is purely inert (no reset affordance). The ± cells are plain
    `clickable(onClick = onDecrement/onIncrement)`. The value cell is NEVER `.clickable` for a tap-to-set
    (D-14 tap-inert). Render icons via `painterResource(R.drawable.<glyph>)` + `Icon(tint = t.<role>)` from the
    17-04 drawables (NOT MaterialSymbol font, D-17). The ± arrows are the screen's expected physical action →
    accent intent (`t.accent2`/`t.accentLine`, THEMING C5). Outline/token border via `RoundedCornerShape(t.rCtrl)`
    + `BorderStroke(2.dp, t.outline)` (or `t.hair` when disabled). When `!enabled`, dim the WHOLE tile
    (alpha 0.4f) and strip the click handlers (busy-lock, D-15). Font: `fsSp` scale — value hero ~fsSp(48f)
    GeistMono tabular, name ~fsSp(18f), no sub-15sp. Build it shared-ready (clean API) but it is wired ONLY in
    Fine-Tune this phase — do NOT retrofit Move or build the full two-mode C2 component (D-19).

    Per PATTERNS § FineTuneHubScreen (D-20): build `FineTuneHubScreen(holder, onNavigate: (FineTuneGroup) ->
    Unit, onBack)` (REVIEW #4 — explicit typed onNavigate callback) as TWO large entries only (Motion,
    Extrusion), NO summary values on the hub. Mirror CalibrationHubScreen's outline tile grid (aspectRatio,
    accent, DrawerTile grammar); each entry calls `onNavigate(FineTuneGroup.MOTION/EXTRUSION)`. Gutter Back =
    Intent.Neutral (15.2 C7). Use `keyboard_return` glyph for Back where an icon is shown.
    NO fenced code in this action — implement against the cited analog lines.
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:assembleDebug --no-daemon"</automated>
  </verify>
  <acceptance_criteria>
    - `:app:assembleDebug` SUCCEEDS with FineTuneTile + FineTuneHubScreen compiling.
    - `grep -n "onReset: (() -> Unit)? = null" app/src/main/java/works/mees/dinghy/ui/finetune/FineTuneTile.kt` present (nullable reset, REVIEW #3); the long-press is installed conditionally (only when onReset != null).
    - `grep -n "detectTapGestures" FineTuneTile.kt` present on the value cell (tap-inert + long-press-reset, D-14/D-16); the value cell does NOT use `.clickable` for a tap-to-set.
    - `grep -n "painterResource\|MaterialSymbol" FineTuneTile.kt` shows painterResource and NO MaterialSymbol-font use for the glyphs (D-17).
    - `grep -n "onNavigate: (FineTuneGroup)" FineTuneHubScreen.kt` present (typed nav callback, REVIEW #4).
    - No raw `.sp` font size outside `fsSp(...)` in the two files ([[dinghy-font-sizes-too-small]]).
    - FineTuneHubScreen renders exactly two entries and no live value readout (D-20).
  </acceptance_criteria>
  <done>The shared value tile (tap-inert, NULLABLE long-press-reset, accent ±, fsSp, busy-dim) + the two-entry hub with a typed onNavigate compile and conform to D-13/14/16/19/20.</done>
</task>

<task type="auto">
  <name>Task 3: MotionScreen + ExtrusionScreen + FwRetractionScreen (capability-gated, state-flip busy-locked, onFwRetraction)</name>
  <files>app/src/main/java/works/mees/dinghy/ui/finetune/MotionScreen.kt, app/src/main/java/works/mees/dinghy/ui/finetune/ExtrusionScreen.kt, app/src/main/java/works/mees/dinghy/ui/finetune/FwRetractionScreen.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/extrude/ExtrudeScreen.kt (FULL skeleton: signature ~117-122, dispatcher+inFlight wiring ~123-127, dispatch funnel ~168-171, failure toast collect ~144-158, Back gutter Intent.Neutral ~283-288, per-key in-flight check ~359 which we OVERRIDE)
    - 17-RESEARCH.md § Pattern 2 (whole-group busy-lock, STRONGER than per-key) + § Pattern 1 (compute target from live displayed value at tap, then dispatch) + § Pitfall 3 (FW-retraction build-blind) + § Pitfall 4 (distinct keys) + § "Part-cooling fan reset" (A2: NO reset affordance on the part-fan tile)
    - 17-PATTERNS.md § "MotionScreen.kt / ExtrusionScreen.kt / FwRetractionScreen.kt"
    - 17-CONTEXT.md § D-03..D-12 (the exact tuners + steps per screen) + § D-15/D-16/D-20 + § D-02 (always-available when connected — NO temperature guard on flow/PA)
  </read_first>
  <action>
    Per PATTERNS § group screens + RESEARCH Pattern 1/2 (D-13/D-14/D-15): each screen has the ExtrudeScreen
    signature shape `(container, holder, onBack, modifier)`, collects `dispatcher` + `dispatcher.inFlight` +
    `holder.vm`, and derives the STATE-FLIP whole-group busy flag `val groupBusy = inFlight.isNotEmpty() ||
    holder.pendingStateFlip != null` (REVIEW #2 / D-15 — DIVERGES from ExtrudeScreen's per-key check AND from a
    bare `inFlight.isNotEmpty()`; the group stays busy until the printer-object value flips, not on the ack).
    Use the verbatim `dispatchCommand(spec, args)` funnel. Each tile's onIncrement/onDecrement computes the
    TARGET from the live `vm` value + the per-control step (D-03..D-12), marks the holder's pendingStateFlip
    (tuner + target), then dispatches ONE command (no optimistic local state — the readout flips only when the
    reducer reports back, D-14/Pitfall 5). Every tile `enabled = !groupBusy`. Collect `dispatcher.events` →
    surface `DispatchEvent.Failure.message` as a `SeverityToast(Severity.Error, ...)` auto-dismissing ~4s
    (out-of-range rejections land here, G1 lesson; a failure also clears pendingStateFlip). Gutter Back =
    Intent.Neutral.

    MotionScreen: 5 tiles (per D-03..D-07) — Speed % (step 5, M220, gate gcode_move), Max velocity (step 10,
    SET_VELOCITY_LIMIT VELOCITY, gate toolhead), Max accel (step 100, ACCEL), Min cruise ratio (step 5 pct-pts
    = 0.05 ratio; DISPLAY percent, send ratio — REVIEW #9; MINIMUM_CRUISE_RATIO), Square-corner velocity (step
    0.1, SQUARE_CORNER_VELOCITY). Capability-gate each tile via the vm gates — absent tunables HIDDEN, not
    disabled (SC-2). Long-press value → reset: speed → M220 S100 (protocol 100%, D-16, always available since no
    baseline needed); the four motion limits → `onReset` that dispatches SET_VELOCITY_LIMIT <field>=<baseline>
    from vm.baselines ONLY when that baseline is non-null, else `onReset = null` (no long-press affordance,
    REVIEW #3). Lay out 5 tiles in a Field grid that keeps ≥64px targets in BOTH portrait and landscape
    (RESEARCH Open-Q2: a responsive 1×5 / 2×3 grid, no "advanced" disclosure needed — validate on-device in 17-06).

    ExtrusionScreen: signature `ExtrusionScreen(container, holder, onBack, onFwRetraction: () -> Unit, modifier)`
    (REVIEW #4 — the FW-retraction navigation is an EXPLICIT typed callback, build-blind so UAT can't catch a
    dead wire; 17-06 threads it from the AppShell fineTuneGroup sub-nav). Tiles per D-08..D-11 — Flow % (step 1,
    M221, gate gcode_move; long-press reset → M221 S100), Pressure advance (step 0.001, SET_PRESSURE_ADVANCE
    ADVANCE, gate extruder; reset → baseline if non-null else null), Smooth time (step 0.01, SET_PRESSURE_ADVANCE
    SMOOTH_TIME; reset → baseline from pressure_advance_smooth_time if non-null else null), Part-cooling fan
    (step 5%, M106, gate fan; `onReset = null` — no config baseline exists, A2/Open-Q1, REVIEW #3; document in a
    comment, owner confirms at UAT). REVIEW #5 — DO NOT apply any cold-extruder/temperature guard to flow or
    pressure-advance: flow factor (M221) and pressure advance are non-extruding LIVE overrides and must be
    always-available when connected (D-02). Gate the flow tile ONLY on `gcode_move` presence and the PA/smooth
    tiles ONLY on `extruder` OBJECT presence — capability gating only, never a hot/cold temperature guard.
    Surface the FW-Retraction ENTRY tile ONLY when `vm.hasFwRetraction`; its tap calls `onFwRetraction()`.

    FwRetractionScreen: 4 tiles (per D-12) — Retract length (step 0.1), Unretract extra length (step 0.1),
    Retract speed (step 1), Unretract speed (step 1); all SET_RETRACTION, gate firmware_retraction; reset →
    baselines (if non-null, else null per REVIEW #3 — on dev printers the baselines are null, so resets are
    no-ops). BUILD-BLIND (Pitfall 3): gates OFF on both dev printers — code-reasoned + fixture-tested only,
    NO on-device UAT gate. Reuse input_circle/output_circle/sprint glyphs for sub-controls (A5).
    NO fenced code in this action.
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:assembleDebug --no-daemon"</automated>
  </verify>
  <acceptance_criteria>
    - `:app:assembleDebug` SUCCEEDS with all three screens.
    - `grep -n "pendingStateFlip" app/src/main/java/works/mees/dinghy/ui/finetune/MotionScreen.kt` present (state-flip busy-lock, REVIEW #2 / D-15) in all three group screens.
    - `grep -n "onFwRetraction" app/src/main/java/works/mees/dinghy/ui/finetune/ExtrusionScreen.kt` present in the signature AND called from the FW-retraction entry tile (REVIEW #4); the entry is gated on `vm.hasFwRetraction`.
    - NO temperature/cold guard on flow or PA: `grep -n "canExtrude\|coldGuard\|minExtrudeTemp\|cold" ExtrusionScreen.kt` shows NONE applied to the flow/PA/smooth tiles (REVIEW #5); those tiles gate only on object presence.
    - Part-fan tile passes `onReset = null` (`grep -n "onReset = null" ExtrusionScreen.kt` present); flow/PA/smooth pass real (possibly baseline-conditional) resets.
    - FwRetractionScreen gates on firmware_retraction and is NOT referenced by any on-device UAT task (build-blind).
    - No optimistic local value state: tiles render `vm.<value>` only (no `remember { mutableStateOf(<value>) }` for the displayed tuner value).
  </acceptance_criteria>
  <done>All three group screens render capability-gated tiles, STATE-FLIP whole-group busy-locked, with onFwRetraction wiring, NO cold-guard on flow/PA, correct per-control steps + nullable resets; FW-retraction build-blind; debug build assembles.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User tap (UI) → dispatchCommand → CommandDispatcher → Moonraker | The tile is the input surface; all dispatch funnels through the existing dispatcher (debounce/in-flight/timeout/redacted-failure). No keyboard input on these screens. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-05-01 | Tampering | numeric ±-nudge → gcode | mitigate | All values are ±-stepped numbers (no keyboard); the target is built by the clamped `PrinterCommands` builders (17-02) — nil injection surface (V5). |
| T-17-05-02 | DoS | printer-rejected out-of-range value | mitigate | `dispatcher.events` Failure → non-fatal `SeverityToast`, readout simply doesn't flip (state-flip confirm), pendingStateFlip clears on failure; never an uncaught crash (G1 lesson). |
| T-17-05-03 | DoS | rapid nudging racing in-flight commands | mitigate | STATE-FLIP whole-group busy-lock (D-15 / REVIEW #2) holds busy until the value flips, serializing taps; dispatcher debounce + distinct dispatchKeys prevent double-fire/key-collision (Pitfall 4). |
| T-17-05-04 | Tampering | dead-wired FW-retraction nav (build-blind) | mitigate | Explicit typed `onFwRetraction` callback (REVIEW #4) makes the wire a compile-checked contract; 17-06 threads it + a grep gate confirms it's called. |
| T-17-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase. |
</threat_model>

<verification>
Run the holder test (Task 1), then `:app:assembleDebug` after Tasks 2 + 3. Screens are NOT yet reachable
(17-06 wires the route) — verification here is compile + holder test + the grep gates. Do NOT require a
full-suite-green sweep here (REVIEW #1 — 17-06 owns the final full-suite gate).
</verification>

<success_criteria>
- TUNE-02/03/04/05/06: holder vm correct (scaling/gates/baselines/caps-flow/state-flip-busy), tile semantics
  correct (tap-inert, NULLABLE long-press-reset, accent ±, busy-dim), three group screens capability-gated +
  STATE-FLIP whole-group busy-locked + state-flip-confirmed, onFwRetraction wired, NO cold-guard on flow/PA,
  FW-retraction build-blind.
- FineTuneHolderTest GREEN; debug build assembles.
</success_criteria>

<output>
Create `.planning/phases/17-fine-tune-live-adjust-panel/17-05-SUMMARY.md` when done.
</output>
