---
phase: 17-fine-tune-live-adjust-panel
plan: 05
type: execute
wave: 2
depends_on: ["17-02", "17-03", "17-04"]
files_modified:
  - app/src/main/java/works/mees/dinghy/ui/finetune/FineTuneVm.kt
  - app/src/main/java/works/mees/dinghy/ui/finetune/FineTuneHolder.kt
  - app/src/main/java/works/mees/dinghy/ui/finetune/FineTuneTile.kt
  - app/src/main/java/works/mees/dinghy/ui/finetune/FineTuneHubScreen.kt
  - app/src/main/java/works/mees/dinghy/ui/finetune/MotionScreen.kt
  - app/src/main/java/works/mees/dinghy/ui/finetune/ExtrusionScreen.kt
  - app/src/main/java/works/mees/dinghy/ui/finetune/FwRetractionScreen.kt
  - app/src/test/java/works/mees/dinghy/ui/finetune/FineTuneHolderTest.kt
autonomous: true
requirements: [TUNE-02, TUNE-03, TUNE-04, TUNE-05, TUNE-06]
must_haves:
  truths:
    - "The holder transforms live PrinterState + config baselines + capabilities into a FineTuneVm with display-scaled values (ratio→%, 0..1→%), gates (hasFan/hasFwRetraction), and a whole-group busy flag."
    - "Each value tile shows icon·name·live-value·−·+; ±dispatches one command per tap immediately and confirms by the reduced state flip (no optimistic local state); tapping the value is inert; long-press the value resets to baseline (D-16)."
    - "While any command is in flight the ENTIRE group screen is busy/disabled (D-15); absent tunables are HIDDEN not disabled (capability-gated, SC-2)."
    - "The FW-Retraction mini-screen entry appears on Extrusion only when firmware_retraction is present; the mini-screen itself is build-blind (fixture-tested, no on-device gate)."
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/ui/finetune/FineTuneHolder.kt"
      provides: "Holder(scope, store) → FineTuneVm via combine(printerState, baselines){buildVm}"
      contains: "buildVm"
    - path: "app/src/main/java/works/mees/dinghy/ui/finetune/FineTuneTile.kt"
      provides: "Shared C2-derived value tile (tap-inert value, ± action, long-press=reset)"
      contains: "detectTapGestures"
    - path: "app/src/main/java/works/mees/dinghy/ui/finetune/MotionScreen.kt"
      provides: "5 capability-gated Motion tiles"
    - path: "app/src/main/java/works/mees/dinghy/ui/finetune/ExtrusionScreen.kt"
      provides: "flow/PA/smooth/part-fan tiles + FW-retraction entry-if-present"
  key_links:
    - from: "FineTuneTile ±"
      to: "CommandDispatcher.dispatch(spec, args)"
      via: "dispatchCommand funnel computing target from the live displayed value"
      pattern: "dispatcher\\??\\.dispatch"
    - from: "every tile enabled flag"
      to: "dispatcher.inFlight"
      via: "groupBusy = inFlight.isNotEmpty()"
      pattern: "inFlight\\.isNotEmpty\\(\\)"
---

<objective>
Build the Fine-Tune UI: the headless holder + view-model, the shared value tile, and the four screens
(Hub, Motion, Extrusion, FW-Retraction). All assembled from the Move/Extrude/Calibration idioms; the only
genuinely-novel pieces are the tap-inert/long-press-reset tile semantics (D-14/D-16) and the whole-group
busy-lock (D-15).

Purpose: Deliver the keyboard-free live-adjust surface — failure-mode organized (Motion vs Extrusion),
capability-gated, state-flip-confirmed, no ConfirmGuard. Turns the 17-01 FineTuneHolderTest GREEN. The two
group screens realize the failure-mode split (D-01: Motion = what moves the head; Extrusion = what affects
the filament). Tap increments are FIXED/app-defined per D-03..D-12 and NOT user-customizable this phase
(D-18: no increment-customization UI).

Output: 7 new files under `ui/finetune/` + the converted holder test. (Navigation wiring + on-device UAT
are 17-06 — this plan does NOT touch the shell; screens are reachable only after 17-06 wires the route.)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-fine-tune-live-adjust-panel/17-CONTEXT.md
@.planning/phases/17-fine-tune-live-adjust-panel/17-RESEARCH.md
@.planning/phases/17-fine-tune-live-adjust-panel/17-PATTERNS.md
@docs/ui_design/LAYOUT.md
@docs/ui_design/THEMING.md
@app/src/main/java/works/mees/dinghy/ui/extrude/ExtrudeScreen.kt
@app/src/main/java/works/mees/dinghy/ui/extrude/ExtrudeHolder.kt
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: FineTuneHolder + FineTuneVm (display-scaling, gates, baselines, busy derivation)</name>
  <files>app/src/main/java/works/mees/dinghy/ui/finetune/FineTuneVm.kt, app/src/main/java/works/mees/dinghy/ui/finetune/FineTuneHolder.kt, app/src/test/java/works/mees/dinghy/ui/finetune/FineTuneHolderTest.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/extrude/ExtrudeHolder.kt (the analog — `combine(printerState, oneShotFlows){buildVm}` ~44-114; ExtrudeVm data class ~125-139; caps snapshot via store.capabilities.value ~68)
    - app/src/test/java/works/mees/dinghy/ui/extrude/ExtrudeHolderTest.kt (the analog for the converted FineTuneHolderTest — runTest(UnconfinedTestDispatcher), real PrinterStateStore, setCapabilities + seed, runCurrent, assert vm)
    - 17-RESEARCH.md § "Code Examples" (buildVm display-scaling: ratio→%, 0..1→%, null→"—") + § Pattern 2 (busy-lock derivation) + § Pitfall 1/5
    - 17-PATTERNS.md § "ui/finetune/FineTuneHolder.kt + FineTuneVm.kt" (combine arity pitfall → wrap baselines in a single Baselines data class StateFlow OR list-form combine; buildVm(s, caps, baselines) shape)
  </read_first>
  <behavior>
    - speedFactor 1.05 → vm.speedPct 105; extrudeFactor 1.0 → vm.flowPct 100 (ratio→%, D-03/D-08 scaling, Pitfall 1).
    - partFanSpeed 0.6 → vm.partFanPct 60 (0..1→%, D-11).
    - null maxAccel/pressureAdvance → vm shows "—"/null, never fabricated 0 (D-20).
    - hasFan == caps.hasObject("fan"); hasFwRetraction == caps.hasObject("firmware_retraction") (D-02 gating).
    - config baselines fold into vm deterministically the instant the one-shot reads land (combine, not a later diff).
    - groupBusy derives true when dispatcher.inFlight is non-empty (D-15).
  </behavior>
  <action>
    Per RESEARCH § Code Examples + PATTERNS § FineTuneHolder: create `FineTuneVm` as a plain data class (all
    fields defaulted, NO Compose annotations — host-testable) carrying per-tuner display-scaled current values
    (speedPct, flowPct, maxVelocity, maxAccel, minCruiseRatio, scv, pressureAdvance, smoothTime, partFanPct —
    nullable where unreported), capability gates (hasFan, hasFwRetraction, plus hasObject gates for
    gcode_move/toolhead/extruder if a printer can lack them), the firmware-retraction sub-values, and a
    `baselines` bundle for reset.

    Create `FineTuneHolder(scope, store)` mirroring ExtrudeHolder: `combine(store.printerState, <baseline
    StateFlows>) { state, ... -> buildVm(state, store.capabilities.value, baselines) }.collect { _vm.value = it }`.
    Because there are MANY baselines and `combine` overloads cap arity, wrap the baseline StateFlows in a single
    `Baselines` data class flow (or use the list-form combine) per PATTERNS — prefer the single-`baselines`-arg
    buildVm shape. Do display scaling in `buildVm` ONLY (ratio→%, 0..1→%) — never read raw ratios into the tile
    (Pitfall 1). The whole-group busy flag is NOT held in the vm — it derives in the screen from
    `dispatcher.inFlight` (Pattern 2); the holder just provides values/gates/baselines.

    Convert the 17-01 FineTuneHolderTest stubs to real assertions (scaling, gates, baseline folding,
    null-shows-dash). The busy-lock derivation test can live here as a pure derivation check
    (inFlight.isNotEmpty()).
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --tests 'works.mees.dinghy.ui.finetune.FineTuneHolderTest' --no-daemon"</automated>
  </verify>
  <acceptance_criteria>
    - FineTuneHolderTest GREEN: scaling (1.05→105, 0.6 fan→60), gates (hasFan/hasFwRetraction), baseline folding, null→dash all pass.
    - `grep -n "fun buildVm" app/src/main/java/works/mees/dinghy/ui/finetune/FineTuneHolder.kt` present; `grep -c "\\* 100\|/ 255\|roundToInt" FineTuneHolder.kt` >= 1 (scaling lives here, not the reducer).
    - FineTuneVm has NO Compose annotations (host-testable, `grep -L "@Composable\|@Stable" FineTuneVm.kt` confirms or manual check).
  </acceptance_criteria>
  <done>Holder produces a correctly-scaled, gated, baseline-folded vm; holder test GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: FineTuneTile (shared C2-derived value tile) + FineTuneHubScreen</name>
  <files>app/src/main/java/works/mees/dinghy/ui/finetune/FineTuneTile.kt, app/src/main/java/works/mees/dinghy/ui/finetune/FineTuneHubScreen.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/extrude/ExtrudeScreen.kt (SettingReadout ~430-473, FieldButton ~481-513, BigCommand disabled-dim ~393-423, painterResource icon ~234-241, outline/token border ~438-443, fsSp heroes)
    - app/src/main/java/works/mees/dinghy/ui/calibration/CalibrationHubScreen.kt (the hub-of-tiles → onNavigate pattern; DrawerTile grammar reuse)
    - 17-RESEARCH.md § Pattern 1 (tile: tap-inert value via detectTapGestures(onLongPress=onReset); ± are plain clickable; render the LIVE reduced value, no optimistic state) + § "Anti-Patterns to Avoid"
    - 17-PATTERNS.md § "ui/finetune/FineTuneTile.kt" + § "FineTuneHubScreen.kt"
    - docs/ui_design/THEMING.md (C1/C5: arrows performing the screen's expected action = accent; C2 3-cell increment-picker the tile is DERIVED from — D-19) + docs/ui_design/LAYOUT.md (Focus/Field/Gutter; interactive-grid flexible-tile law)
    - The font-scale lesson: use fsSp(baseSp, t.fs) (15sp metadata floor / 18sp body / 26sp tabular stats / 48sp focus value) — [[dinghy-font-sizes-too-small]]
  </read_first>
  <action>
    Per RESEARCH Pattern 1 + PATTERNS § FineTuneTile (D-13/D-14/D-16/D-19): build `FineTuneTile` as a shared
    `@Composable` taking (iconRes, name, valueText, onDecrement, onIncrement, onReset, enabled). Layout =
    `− [icon · name · value] +` (C2-derived 3-cell). The VALUE cell is tap-inert with a long-press handler:
    `pointerInput { detectTapGestures(onLongPress = onReset) }` (D-16) — NOT `.clickable`. The ± cells are plain
    `clickable(onClick = onDecrement/onIncrement)`. Render icons via `painterResource(R.drawable.<glyph>)` +
    `Icon(tint = t.<role>)` from the 17-04 drawables (NOT MaterialSymbol font, D-17). The ± arrows are the
    screen's expected physical action → accent intent (`t.accent2`/`t.accentLine`, THEMING C5). Outline/token
    border via `RoundedCornerShape(t.rCtrl)` + `BorderStroke(2.dp, t.outline)` (or `t.hair` when disabled). When
    `!enabled`, dim the WHOLE tile (alpha 0.4f) and strip the click handlers (busy-lock, D-15). Font: `fsSp`
    scale — value hero ~fsSp(48f) GeistMono tabular, name ~fsSp(18f), no sub-15sp. Build it shared-ready (clean
    API) but it is wired ONLY in Fine-Tune this phase — do NOT retrofit Move or build the full two-mode C2
    component (D-19).

    Per PATTERNS § FineTuneHubScreen (D-20): build `FineTuneHubScreen(holder, onNavigate, onBack)` as TWO
    large entries only (Motion, Extrusion), NO summary values on the hub. Mirror CalibrationHubScreen's outline
    tile grid (aspectRatio, accent, DrawerTile grammar); each entry calls `onNavigate(FineTuneGroup.MOTION/
    EXTRUSION)`. Gutter Back = Intent.Neutral (15.2 C7). Use `keyboard_return` glyph for Back where an icon is shown.
    NO fenced code in this action — implement against the cited analog lines.
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:assembleDebug --no-daemon"</automated>
  </verify>
  <acceptance_criteria>
    - `:app:assembleDebug` SUCCEEDS with FineTuneTile + FineTuneHubScreen compiling.
    - `grep -n "detectTapGestures" app/src/main/java/works/mees/dinghy/ui/finetune/FineTuneTile.kt` present on the value cell (tap-inert + long-press-reset, D-14/D-16); the value cell does NOT use `.clickable` for a tap-to-set.
    - `grep -n "painterResource\|MaterialSymbol" FineTuneTile.kt` shows painterResource and NO MaterialSymbol-font use for the glyphs (D-17).
    - No raw `.sp` font size outside `fsSp(...)` in the two files ([[dinghy-font-sizes-too-small]]).
    - FineTuneHubScreen renders exactly two entries and no live value readout (D-20).
  </acceptance_criteria>
  <done>The shared value tile (tap-inert, long-press-reset, accent ±, fsSp, busy-dim) + the two-entry hub compile and conform to D-13/14/16/19/20/29.</done>
</task>

<task type="auto">
  <name>Task 3: MotionScreen + ExtrusionScreen + FwRetractionScreen (capability-gated, busy-locked, state-flip)</name>
  <files>app/src/main/java/works/mees/dinghy/ui/finetune/MotionScreen.kt, app/src/main/java/works/mees/dinghy/ui/finetune/ExtrusionScreen.kt, app/src/main/java/works/mees/dinghy/ui/finetune/FwRetractionScreen.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/extrude/ExtrudeScreen.kt (FULL skeleton: signature ~117-122, dispatcher+inFlight wiring ~123-127, dispatch funnel ~168-171, failure toast collect ~144-158, Back gutter Intent.Neutral ~283-288, per-key in-flight check ~359 which we OVERRIDE)
    - 17-RESEARCH.md § Pattern 2 (whole-group busy-lock = inFlight.isNotEmpty(), STRONGER than per-key) + § Pattern 1 (compute target from live displayed value at tap, then dispatch) + § Pitfall 3 (FW-retraction build-blind) + § Pitfall 4 (distinct keys) + § "Part-cooling fan reset" (A2: NO reset affordance on the part-fan tile)
    - 17-PATTERNS.md § "MotionScreen.kt / ExtrusionScreen.kt / FwRetractionScreen.kt"
    - 17-CONTEXT.md § D-03..D-12 (the exact tuners + steps per screen) + § D-15/D-16/D-20
  </read_first>
  <action>
    Per PATTERNS § group screens + RESEARCH Pattern 1/2 (D-13/D-14/D-15): each screen has the ExtrudeScreen
    signature `(container, holder, onBack, modifier)`, collects `dispatcher` + `dispatcher.inFlight` + `holder.vm`,
    derives `val groupBusy = inFlight.isNotEmpty()` (Pattern 2 — DIVERGES from ExtrudeScreen's per-key check;
    D-15 locks the WHOLE group), and uses the verbatim `dispatchCommand(spec, args)` funnel. Each tile's
    onIncrement/onDecrement computes the TARGET from the live `vm` value + the per-control step (D-03..D-12),
    then dispatches ONE command (no optimistic local state — the readout flips only when the reducer reports
    back, D-14/Pitfall 5). Every tile `enabled = !groupBusy`. Collect `dispatcher.events` → surface
    `DispatchEvent.Failure.message` as a `SeverityToast(Severity.Error, ...)` auto-dismissing ~4s (out-of-range
    rejections land here, G1 lesson). Gutter Back = Intent.Neutral.

    MotionScreen: 5 tiles (per D-03..D-07) — Speed % (step 5, M220, gate gcode_move), Max velocity (step 10,
    SET_VELOCITY_LIMIT VELOCITY, gate toolhead), Max accel (step 100, ACCEL), Min cruise ratio (step 5 pct-pts
    = 0.05 ratio, MINIMUM_CRUISE_RATIO), Square-corner velocity (step 0.1, SQUARE_CORNER_VELOCITY). Capability-
    gate each tile via the vm gates — absent tunables HIDDEN, not disabled (SC-2). Long-press value → reset:
    speed → M220 S100 (protocol 100%, D-16); the four motion limits → SET_VELOCITY_LIMIT <field>=<baseline>
    from vm.baselines. Lay out 5 tiles in a Field grid that keeps ≥64px targets in BOTH portrait and landscape
    (RESEARCH Open-Q2: a responsive 1×5 / 2×3 grid, no "advanced" disclosure needed — validate on-device in 17-06).

    ExtrusionScreen: tiles per D-08..D-11 — Flow % (step 1, M221, gate gcode_move; long-press reset → M221 S100),
    Pressure advance (step 0.001, SET_PRESSURE_ADVANCE ADVANCE, gate extruder; reset → baseline), Smooth time
    (step 0.01, SET_PRESSURE_ADVANCE SMOOTH_TIME; reset → baseline from pressure_advance_smooth_time), Part-
    cooling fan (step 5%, M106, gate fan; **NO long-press reset affordance** — no config baseline exists, A2/
    Open-Q1; document in a comment, owner confirms at UAT). Surface the FW-Retraction ENTRY tile ONLY when
    `vm.hasFwRetraction` (navigates to FwRetractionScreen). Apply the cold-extruder guard idiom for PA/flow if
    appropriate (they tie to a hot extruder) — reuse the Extrude guard pattern.

    FwRetractionScreen: 4 tiles (per D-12) — Retract length (step 0.1), Unretract extra length (step 0.1),
    Retract speed (step 1), Unretract speed (step 1); all SET_RETRACTION, gate firmware_retraction; reset →
    baselines. BUILD-BLIND (Pitfall 3): gates OFF on both dev printers — code-reasoned + fixture-tested only,
    NO on-device UAT gate. Reuse input_circle/output_circle/sprint glyphs for sub-controls (A5).
    NO fenced code in this action.
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:assembleDebug --no-daemon"</automated>
  </verify>
  <acceptance_criteria>
    - `:app:assembleDebug` SUCCEEDS with all three screens.
    - `grep -n "inFlight.isNotEmpty()" app/src/main/java/works/mees/dinghy/ui/finetune/MotionScreen.kt` present (whole-group busy-lock, D-15) in all three group screens.
    - MotionScreen references all 5 motion specs; ExtrusionScreen gates the FW-retraction entry on `vm.hasFwRetraction` (`grep -n "hasFwRetraction" ExtrusionScreen.kt`).
    - Part-fan tile has NO long-press-reset call (`grep -n "onReset" ExtrusionScreen.kt` shows resets for flow/PA/smooth but a documented none/comment for part-fan).
    - FwRetractionScreen gates on firmware_retraction and is NOT referenced by any on-device UAT task (build-blind).
    - No optimistic local value state: tiles render `vm.<value>` only (no `remember { mutableStateOf(<value>) }` for the displayed tuner value).
  </acceptance_criteria>
  <done>All three group screens render capability-gated tiles, whole-group busy-locked, state-flip-confirmed, with correct per-control steps + resets; FW-retraction build-blind; debug build assembles.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User tap (UI) → dispatchCommand → CommandDispatcher → Moonraker | The tile is the input surface; all dispatch funnels through the existing dispatcher (debounce/in-flight/timeout/redacted-failure). No keyboard input on these screens. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-05-01 | Tampering | numeric ±-nudge → gcode | mitigate | All values are ±-stepped numbers (no keyboard); the target is built by the clamped `PrinterCommands` builders (17-02) — nil injection surface (V5). |
| T-17-05-02 | DoS | printer-rejected out-of-range value | mitigate | `dispatcher.events` Failure → non-fatal `SeverityToast`, readout simply doesn't flip (state-flip confirm); never an uncaught crash (G1 lesson). |
| T-17-05-03 | DoS | rapid nudging racing in-flight commands | mitigate | Whole-group busy-lock (D-15) serializes taps; dispatcher debounce + distinct dispatchKeys prevent double-fire/key-collision (Pitfall 4). |
| T-17-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase. |
</threat_model>

<verification>
Run the holder test (Task 1), then `:app:assembleDebug` after Tasks 2 + 3. Then the full host suite
(`:app:testDebugUnitTest --no-daemon`) to confirm no regression. Screens are NOT yet reachable (17-06 wires
the route) — verification here is compile + holder test + the grep gates.
</verification>

<success_criteria>
- TUNE-02/03/04/05/06: holder vm correct (scaling/gates/baselines), tile semantics correct (tap-inert,
  long-press-reset, accent ±, busy-dim), three group screens capability-gated + whole-group busy-locked +
  state-flip-confirmed, FW-retraction build-blind.
- FineTuneHolderTest GREEN; debug build assembles; full host suite GREEN.
</success_criteria>

<output>
Create `.planning/phases/17-fine-tune-live-adjust-panel/17-05-SUMMARY.md` when done.
</output>
