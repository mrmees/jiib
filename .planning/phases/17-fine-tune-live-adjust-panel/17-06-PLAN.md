---
phase: 17-fine-tune-live-adjust-panel
plan: 06
type: execute
wave: 3
depends_on: ["17-05"]
files_modified:
  - app/src/main/java/works/mees/dinghy/ui/route/TopRoute.kt
  - app/src/main/java/works/mees/dinghy/ui/shell/AppShell.kt
  - app/src/main/java/works/mees/dinghy/ui/shell/AppDrawer.kt
  - app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusScreen.kt
  - app/src/androidTest/java/works/mees/dinghy/ui/finetune/FineTuneNavTest.kt
  - .planning/phases/17-fine-tune-live-adjust-panel/17-UAT.md
autonomous: false
requirements: [TUNE-01]
must_haves:
  truths:
    - "The stubbed PrintStatusControlAction.Tune navigates to a real Dest.FineTune; the Fine-Tune Hub → Motion/Extrusion/FwRetraction is a LOCAL sub-nav back-stack within Dest.FineTune (mirroring Calibration), not four top-level Dests."
    - "FineTuneHolder is built next to the other panel holders and re-keyed on spine rebuild; the Dest.FineTune arm is registered in AppShell with a BackHandler popping a group page to the hub."
    - "On real flox + live E5/E3: nudging speed%, flow%, a motion-limit, and pressure-advance during a print flips each tile's reported value (state-flip, not bare ack); the Adreno-320 perf check shows no frozen frames."
  artifacts:
    - path: "app/src/androidTest/java/works/mees/dinghy/ui/finetune/FineTuneNavTest.kt"
      provides: "Instrumented proof Tune → Dest.FineTune"
    - path: ".planning/phases/17-fine-tune-live-adjust-panel/17-UAT.md"
      provides: "On-device UAT script + recorded results (Motion + Extrusion + perf; FW-retraction explicitly excluded)"
  key_links:
    - from: "PrintStatusScreen Tune action"
      to: "Dest.FineTune"
      via: "PrintStatusControlAction.Tune -> onNavigate(Dest.FineTune)"
      pattern: "Tune -> onNavigate\\(Dest\\.FineTune\\)"
    - from: "AppShell Dest.FineTune arm"
      to: "FineTuneHubScreen / MotionScreen / ExtrusionScreen / FwRetractionScreen"
      via: "local fineTuneGroup sub-nav state + BackHandler"
      pattern: "Dest\\.FineTune"
---

<objective>
Wire the Fine-Tune entry point + navigation and prove the live-adjust loop on real hardware. This is the
binding on-device gate (autonomous: false) for Phase 17.

Purpose: TUNE-01 (D-21) — the stubbed Tune button becomes a real route; the hub→group sub-nav mirrors the
Calibration shell pattern. Then the human-verify gate proves state-flip confirm + Adreno-320 perf on flox +
live E5/E3 (Motion + Extrusion only — FW-retraction is build-blind and explicitly excluded, Pitfall 3).

Output: route + shell + drawer + entry wiring; the converted instrumented nav test; and 17-UAT.md with the
recorded on-device results.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-fine-tune-live-adjust-panel/17-CONTEXT.md
@.planning/phases/17-fine-tune-live-adjust-panel/17-RESEARCH.md
@.planning/phases/17-fine-tune-live-adjust-panel/17-PATTERNS.md
@.planning/phases/17-fine-tune-live-adjust-panel/17-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Route + shell + drawer + entry wiring (TUNE-01 / D-21) + instrumented nav test</name>
  <files>app/src/main/java/works/mees/dinghy/ui/route/TopRoute.kt, app/src/main/java/works/mees/dinghy/ui/shell/AppShell.kt, app/src/main/java/works/mees/dinghy/ui/shell/AppDrawer.kt, app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusScreen.kt, app/src/androidTest/java/works/mees/dinghy/ui/finetune/FineTuneNavTest.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/route/TopRoute.kt (the file being modified — the `Dest` enum ~37)
    - app/src/main/java/works/mees/dinghy/ui/shell/AppShell.kt (the file being modified — the Dest.Calibration arm + local hub→routine back-stack ~542-585; hoisted `nav.calibrationRoutine` sub-state ~150-155; BackHandler pop ~406; where extrudeHolder/moveHolder/calibrationHubHolder are remember(store)'d ~171+)
    - app/src/main/java/works/mees/dinghy/ui/shell/AppDrawer.kt (the file being modified — `DRAWER_TILES` ~144-156; icon-no-repeat law — `tune` is taken by Calibration)
    - app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusScreen.kt (the file being modified — `PrintStatusControlAction.Tune -> Unit` stub ~260)
    - 17-RESEARCH.md § "Wiring the Tune entry point (D-21)" + § Integration Points
    - 17-PATTERNS.md § "Navigation wiring (route + shell + drawer + entry)" (the exact Dest.FineTune arm + local fineTuneGroup sub-nav + BackHandler shape; AppShell.kt MUST be in files_modified)
  </read_first>
  <action>
    Per RESEARCH § D-21 + PATTERNS § Navigation wiring: add `FineTune` to the `Dest` enum (TopRoute.kt). In
    AppShell, build `FineTuneHolder` next to the other panel holders (`remember(store)`, re-keyed on spine
    rebuild like extrudeHolder/calibrationHubHolder). Add a hoisted `fineTuneGroup` sub-state (a
    `FineTuneGroup?` enum: null=hub, MOTION, EXTRUSION, FW_RETRACTION — mirror `nav.calibrationRoutine`). Add a
    `Dest.FineTune` arm: when group is null render FineTuneHubScreen(onNavigate sets group); MOTION →
    MotionScreen; EXTRUSION → ExtrusionScreen; FW_RETRACTION → FwRetractionScreen — a LOCAL back-stack WITHIN
    Dest.FineTune (NOT four top-level Dests). Add a BackHandler that pops a group page back to the hub (mirror
    the Calibration BackHandler). Add Dest.FineTune to the swipe-drawer-suppression set if any group screen
    has a scrollable Field (match the Calibration treatment).

    Replace the `PrintStatusScreen.kt:260` stub `PrintStatusControlAction.Tune -> Unit` with
    `PrintStatusControlAction.Tune -> onNavigate(Dest.FineTune)` (D-21). Add a Fine-Tune drawer tile to
    `DRAWER_TILES` (D-21 planner's call — include it for parity with the other panels) with a UNIQUE glyph
    (icon-no-repeat law — `tune` is Calibration's; pick a distinct Fine-Tune glyph from the bucket), `dest =
    Dest.FineTune`, conforming to the Phase-16 shell.

    Convert the 17-01 `FineTuneNavTest.kt` instrumented stub to a real assertion: launching the shell and
    invoking the Print-Status Tune action lands on Dest.FineTune (the Fine-Tune Hub renders). Use the existing
    instrumented shell-test harness pattern. NO fenced code in this action.
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:assembleDebug --no-daemon"</automated>
  </verify>
  <acceptance_criteria>
    - `:app:assembleDebug` SUCCEEDS with the route live.
    - `grep -n "Tune -> onNavigate(Dest.FineTune)" app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusScreen.kt` present (stub replaced).
    - `grep -c "Dest.FineTune" app/src/main/java/works/mees/dinghy/ui/shell/AppShell.kt` >= 1; the Fine-Tune arm uses a LOCAL group sub-nav (single Dest, not MOTION/EXTRUSION as top-level Dests — verify the enum has only `FineTune`, no `Motion`/`Extrusion` Dests).
    - The Fine-Tune drawer tile glyph does not duplicate Calibration's `tune` glyph (icon-no-repeat).
    - `FineTuneNavTest.kt` compiles under androidTest and asserts the Tune → FineTune route.
  </acceptance_criteria>
  <done>Tune button opens Fine-Tune; hub→group local sub-nav with BackHandler works; drawer tile present; instrumented nav test real. Full release/debug build assembles.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: On-device UAT — Motion + Extrusion state-flip + Adreno-320 perf (live E5/E3 + flox)</name>
  <files>.planning/phases/17-fine-tune-live-adjust-panel/17-UAT.md</files>
  <read_first>
    - 17-VALIDATION.md § "Manual-Only Verifications" (the state-flip + perf + FW-retraction-excluded contract)
    - 17-RESEARCH.md § "Sampling Rate" → Phase gate + § Pitfall 3 (FW-retraction NOT live-testable) + § Environment Availability (E5 192.168.1.120:7125 / E3 192.168.1.121:7125 / flox API-30 Adreno 320)
    - The on-device iteration lesson [[dinghy-display-ondevice-iteration]] (Claude builds/installs; Matthew navigates/eyeballs) + the perf reframe [[ADR-0001 Add.2]] (judge on no-frozen-frames + responsiveness, not the 95%-janky gfxinfo artifact on sparse event-driven screens)
  </read_first>
  <action>
    Orchestrator builds + installs the debug APK on flox (`E:\Android\gw.bat :app:assembleDebug` → `adb install`),
    scaffolds `17-UAT.md` with the 7 checks below, and the human runs them on flox + a live printer (E5
    192.168.1.120:7125 / E3 192.168.1.121:7125). This is a blocking human-verify checkpoint — the plan-runner
    does NOT auto-execute it; results are recorded in 17-UAT.md and a FAIL spawns gap-closure, not phase-complete.
  </action>
  <verify>
    <human-check>Run the 7-check UAT script on flox + live printer; record PASS/FAIL per check in 17-UAT.md. State-flip on speed%/flow%/a motion-limit/pressure-advance during a live print; FW-retraction entry ABSENT (capability-gated); perf no-frozen-frames.</human-check>
  </verify>
  <what-built>
    The full Fine-Tune surface wired off the Print-Status Tune button: Hub → Motion (5 capability-gated tiles)
    → Extrusion (flow/PA/smooth/part-fan + FW-retraction entry-if-present), keyboard-free ±-nudge value tiles,
    whole-group busy-lock, long-press-reset, state-flip confirm. The orchestrator builds + installs the debug
    APK on flox (`E:\Android\gw.bat :app:assembleDebug` → `adb install`); the live printers are E5
    (192.168.1.120:7125) and E3 (192.168.1.121:7125).
  </what-built>
  <how-to-verify>
    Scaffold `17-UAT.md` with these checks, then run them on flox + a live printer (start a real print on E5
    for the mid-print checks per ROADMAP SC-4):
    1. Tap the Print-Status Tune button → the Fine-Tune Hub opens (TUNE-01). Hub shows Motion + Extrusion, no
       summary values (D-20).
    2. Motion screen: nudge Speed % ±5 → the tile's reported value flips to the commanded value (state-flip,
       not instant-optimistic) and the print visibly speeds/slows (SC-1/SC-4). Nudge Max accel ±100 → tile
       value flips. Confirm 5 tiles show, ≥64px targets, usable in BOTH portrait and landscape (Open-Q2).
    3. Extrusion screen: nudge Flow % ±1 → tile flips + physical effect; nudge Pressure advance ±0.001 → tile
       flips; nudge Part-cooling fan ±5% → `fan.speed` readout flips. Confirm the FW-retraction entry is ABSENT
       (neither printer exposes it — capability-gated, SC-2).
    4. Long-press Speed value → resets to 100%; long-press Max-accel value → resets to config baseline. Confirm
       the part-fan tile has NO reset (or owner decides the desired behavior — A2 open item).
    5. Busy-lock: rapid-nudge → the whole group dims/disables while a command is in flight, re-enables on
       confirm (D-15).
    6. Reject path: nudge a value Klipper rejects (or beyond range) → a non-fatal error toast, readout does NOT
       move, no crash (G1 lesson).
    7. Perf: capture `gfxinfo` framestats on flox while nudging — judge on no-frozen-frames + responsiveness
       (ADR-0001 Add.2); static value-tile screens, LOW perf risk.
    Record PASS/FAIL + any gaps in 17-UAT.md. FW-Retraction is EXPLICITLY EXCLUDED from on-device UAT
    (build-blind — verified by the 17-03 synthetic-fixture reducer test + the 17-05 gate, not on hardware).
  </how-to-verify>
  <acceptance_criteria>
    - 17-UAT.md exists with all 7 checks scripted and the recorded results.
    - State-flip confirmed on at least speed%, flow%, one motion-limit, and pressure-advance during a live print (SC-1/SC-4).
    - Capability-gating confirmed: FW-retraction entry ABSENT on both dev printers (SC-2).
    - Perf check recorded: no frozen frames / responsive (ADR-0001 Add.2).
    - Any failure spawns a gap-closure plan (per project gate convention) — the phase is NOT marked complete on a FAIL.
  </acceptance_criteria>
  <resume-signal>Type "approved" with the UAT results, or describe the gaps to close.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Navigation (shell) | Routing only; no new external trust boundary. The live-adjust dispatch boundary is unchanged from 17-05 (funnels through the existing dispatcher). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-06-01 | DoS | live mid-print nudge causing a bad command | mitigate | UAT explicitly tests the reject path (check 6): a printer-rejected value yields a non-fatal toast + no readout move + no crash (G1); the builders (17-02) clamp before format. |
| T-17-06-02 | Tampering | FW-retraction shipped untested on a printer that DOES have it | accept | Build-blind on dev hardware; covered by the 17-03 synthetic-fixture reducer test + 17-05 gate (mirrors QUAD_GANTRY_LEVEL). Documented as a known limitation in 17-UAT.md. |
| T-17-SC | Tampering | npm/pip/cargo installs | accept | No package installs this phase. |
</threat_model>

<verification>
Task 1: `:app:assembleDebug` + the grep gates + the instrumented nav test. Task 2: the on-device UAT
checklist recorded in 17-UAT.md (human-verify gate; a FAIL spawns gap-closure, NOT phase-complete).
</verification>

<success_criteria>
- TUNE-01: Tune button opens Fine-Tune; hub→group local sub-nav works; instrumented nav test GREEN.
- ROADMAP SC-1/SC-2/SC-3/SC-4 proven on-device (Motion + Extrusion state-flip mid-print, capability-gating,
  bounded keyboard-free reset, reachable from Tune); perf no-frozen-frames.
- FW-retraction explicitly excluded from on-device UAT (build-blind), covered by fixture tests.
</success_criteria>

<output>
Create `.planning/phases/17-fine-tune-live-adjust-panel/17-06-SUMMARY.md` when done (only after the UAT gate
passes; on a FAIL, hold the SUMMARY and spawn a gap-closure plan).
</output>
