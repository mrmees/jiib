---
phase: 21-webrtc-camera-streaming
plan: 03
type: execute
wave: 3
depends_on: ["21-01", "21-02"]
files_modified:
  - app/src/main/java/works/mees/dinghy/state/WebcamModels.kt
  - app/src/main/java/works/mees/dinghy/net/WebcamUrl.kt
  - app/src/test/java/works/mees/dinghy/webcam/RungSelectTest.kt
  - app/src/test/java/works/mees/dinghy/webcam/WebcamUrlDeriveTest.kt
autonomous: true
requirements: [CAM-11, CAM-12, CAM-13]

must_haves:
  truths:
    - "Rung.H264 exists as the new top/preferred rung (tier 0, above Mjpeg=1) — D-09"
    - "selectsH264Rung is a pure host-testable heuristic: service startsWith webrtc OR streamUrl is rtsp:// (a HINT only) — D-10"
    - "rungFor (the Content-Type probe) NEVER returns H264 — the GET probe stays service-blind (T-10-06)"
    - "deriveNativeStreamUrl swaps :8889→:8554 (rtsp) / :8888 (hls) keeping the PATH parsed from stream_url, never the name — the landmine"
    - "An explicit extra_data native-stream tag is preferred over derive when present (prefer-if-present, dormant until ravens-perch ships it) — D-12/D-14"
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/state/WebcamModels.kt"
      provides: "Rung.H264 (tier 0), NativeTransport enum, selectsH264Rung, extra_data tag read helper"
      contains: "H264"
    - path: "app/src/main/java/works/mees/dinghy/net/WebcamUrl.kt"
      provides: "deriveNativeStreamUrl + nativeStreamUrlFor (explicit-tag-else-derive)"
      contains: "deriveNativeStreamUrl"
  key_links:
    - from: "app/src/main/java/works/mees/dinghy/net/WebcamUrl.kt"
      to: "okhttp3.HttpUrl"
      via: "toHttpUrlOrNull + host/encodedPath (never string-concat)"
      pattern: "toHttpUrlOrNull"
    - from: "app/src/main/java/works/mees/dinghy/net/WebcamUrl.kt"
      to: "surfaceWebcamUrl"
      via: "redaction applied to any surfaced derived URL (V7)"
      pattern: "surfaceWebcamUrl"
---

<objective>
Build the PURE, host-testable logic of the H.264 rung: the model addition (`Rung.H264` top rung +
`NativeTransport`), the heuristic selector (`selectsH264Rung`), and the URL resolution
(`deriveNativeStreamUrl` + explicit-tag-else-derive). This is the spike-independent half — it ships
regardless of which transport the spike picked, exposing BOTH RTSP and HLS derive so plan 04 wires
the recorded lead. Converts three of the four Wave-0 RED scaffolds (RungSelect, WebcamUrlDerive) to
live green assertions.

Purpose: this is where D-09 (H.264 = new top rung), D-10 (heuristic-selects/decoder-verifies,
preserving T-10-06), and the D-11/D-12 integration crux land — including the name≠path landmine that
RESEARCH flagged as the highest-leverage correctness item. All of it is pure (no Android, no I/O),
so it is fully provable on the host before the on-device wiring in plan 04.

Output: the model + URL transforms + green CAM-11/12/13 unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-webrtc-camera-streaming/21-CONTEXT.md
@.planning/phases/21-webrtc-camera-streaming/21-RESEARCH.md
@.planning/phases/21-webrtc-camera-streaming/21-PATTERNS.md
@app/src/main/java/works/mees/dinghy/state/WebcamModels.kt
@app/src/main/java/works/mees/dinghy/net/WebcamUrl.kt
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Rung.H264 + NativeTransport + selectsH264Rung to WebcamModels.kt; close RungSelectTest</name>
  <read_first>
    - app/src/main/java/works/mees/dinghy/state/WebcamModels.kt (the `Rung` enum lines 59-68 with numeric `tier`; the tolerant `Webcam` model lines 26-53 incl. `service`/`streamUrl`/`extraData`; `rungFor` lines 86-103 — the Content-Type-only selector that must STAY service-blind; class KDoc lines 11-24 tolerant-never-fabricate discipline; `ResolvedWebcam` lines 133-138)
    - app/src/test/java/works/mees/dinghy/webcam/RungSelectTest.kt (the Wave-0 RED scaffold to convert to live assertions)
    - .planning/phases/21-webrtc-camera-streaming/21-PATTERNS.md § WebcamModels.kt + § selectsH264Rung (the exact signatures + the "do NOT make rungFor return H264" exhaustiveness warning)
    - .planning/phases/21-webrtc-camera-streaming/21-CONTEXT.md (D-09 ladder order; D-10 heuristic-selects/decoder-verifies; T-10-06)
  </read_first>
  <behavior>
    - selectsH264Rung(cam) == true when cam.service startsWith "webrtc" (ignoreCase) — de-facto "webrtc-mediamtx"
    - selectsH264Rung(cam) == true when cam.streamUrl startsWith "rtsp://" (ignoreCase)
    - selectsH264Rung(cam) == false for a plain MJPEG/snapshot cam (no webrtc service, http stream_url)
    - Rung.H264.tier == 0 and is strictly less than Rung.Mjpeg.tier (1) — H.264 is the preferred/top rung
    - rungFor(...) NEVER returns Rung.H264 for any Content-Type/code/snapshot combination (probe stays service-blind, T-10-06)
  </behavior>
  <action>
    In WebcamModels.kt: add `H264(0)` to the `Rung` enum as the new lowest-tier (best) rung with a KDoc noting it is
    decoded natively by Media3 over RTSP/HLS, selected by heuristic + verified by decoder (D-09/D-10). Add a `NativeTransport`
    enum (`Rtsp`, `Hls`) next to `Rung` (the derive transform's parameter — kept in this state file per the existing
    `ResolvedWebcam` co-location precedent). Add a pure `selectsH264Rung(cam: Webcam): Boolean` — a HINT-only heuristic
    (`service.startsWith("webrtc", ignoreCase=true) || streamUrl?.startsWith("rtsp://", ignoreCase=true) == true`), no Android,
    no I/O, host-testable like `rungFor`. Do NOT alter `rungFor` to emit H264 — audit its `when` stays exhaustive and
    service-blind. Convert RungSelectTest.kt from fail() stubs to live assertions covering the `<behavior>` cases.
  </action>
  <acceptance_criteria>
    - `Rung.H264.tier == 0` and `Rung.H264.tier < Rung.Mjpeg.tier` (assert in test).
    - `selectsH264Rung` returns the `<behavior>` truth table; lives in WebcamModels.kt, pure (grep: no `import android.` added for it, no I/O).
    - `rungFor` source unchanged in behavior — grep confirms `rungFor` body contains no `H264` reference.
    - RungSelectTest passes: `/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --tests *RungSelectTest --no-daemon" | tr -d '\r'` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --tests *RungSelectTest --no-daemon" | tr -d '\r'</automated>
  </verify>
  <done>Rung.H264 is the tier-0 top rung, selectsH264Rung is a pure green-tested HINT, rungFor stays service-blind, RungSelectTest is green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add deriveNativeStreamUrl + explicit-tag-else-derive to WebcamUrl.kt; close WebcamUrlDeriveTest</name>
  <read_first>
    - app/src/main/java/works/mees/dinghy/net/WebcamUrl.kt (resolveWebcamUrl lines 39-56 — the `HttpUrl`-parse-never-concat idiom + LOOPBACK_HOSTS rewrite lines 29/47-49; redactWebcamUrl/surfaceWebcamUrl lines 63-76 — the V7 redaction surface to reuse; imports lines 3-4 toHttpUrl/toHttpUrlOrNull already present)
    - app/src/main/java/works/mees/dinghy/state/WebcamModels.kt (the `extraData: JsonObject` field line 40 — the D-12 explicit-tag read is a key lookup on this; NativeTransport added in Task 1)
    - app/src/test/java/works/mees/dinghy/webcam/WebcamUrlDeriveTest.kt (the Wave-0 RED scaffold to convert)
    - .planning/phases/21-webrtc-camera-streaming/21-RESEARCH.md § URL Resolution (the verified port constants 8554/8888/8889 from stream_manager.py; the ⚠ LANDMINE name≠path; the concrete deriveNativeStreamUrl shape; "explicit-tag is prefer-if-present layer over derive, UNBUILT in ravens-perch today, dormant — D-14")
    - .planning/phases/21-webrtc-camera-streaming/21-PATTERNS.md § WebcamUrl.kt (the exact transform + the surfaceWebcamUrl reuse mandate)
  </read_first>
  <behavior>
    - deriveNativeStreamUrl("http://192.168.1.120:8889/0/", Rtsp) == "rtsp://192.168.1.120:8554/0" (port swap, scheme→rtsp, path kept, trailing slash dropped)
    - deriveNativeStreamUrl("http://192.168.1.120:8889/0/", Hls) == "http://192.168.1.120:8888/0/" (port swap, http kept, trailing slash kept)
    - The derived URL uses the PATH segment from stream_url, NOT a friendly `name` — a Webcam whose name differs from its stream_url path derives from the path (the landmine: name="back_camera" but stream_url path="0" → URL contains "0", never "back_camera")
    - null/blank/non-parseable stream_url → null (toHttpUrlOrNull → null → rung falls through, V5 fail-safe)
    - a loopback host in stream_url is rewritten to the connection host (mirror resolveWebcamUrl loopback handling)
    - explicit extra_data native-stream tag (when present) is returned in preference to the derive
  </behavior>
  <action>
    Extend WebcamUrl.kt with a pure `deriveNativeStreamUrl(webrtcStreamUrl: String?, transport: NativeTransport): String?`
    using `toHttpUrlOrNull()` (NEVER string-concat) per RESEARCH § URL Resolution: parse the path via `url.encodedPath.trim('/')`
    (the MediaMTX single path segment OUT of the stream_url — never the webcam name), return null if path blank;
    `Rtsp -> "rtsp://${host}:8554/$path"`, `Hls -> "http://${host}:8888/$path/"`. Apply the existing loopback-host rewrite
    (reuse LOOPBACK_HOSTS + the ConnectionConfig host swap idiom from resolveWebcamUrl). Add a `nativeStreamUrlFor(cam, transport, cfg)`
    that FIRST reads the explicit ravens-perch `extra_data` native-stream tag from `cam.extraData` (prefer-if-present, tolerant —
    missing/garbage → fall through), ELSE derives from the resolved stream_url (D-12). Route any surfaced/logged derived URL through
    the existing `surfaceWebcamUrl` (V7 — even though RTSP/HLS are tokenless, a mixed diagnostic must not leak the snapshot token).
    Convert WebcamUrlDeriveTest.kt to live assertions covering every `<behavior>` case incl. the name≠path landmine and the explicit-tag precedence.
  </action>
  <acceptance_criteria>
    - The `<behavior>` truth table holds exactly (assert each in WebcamUrlDeriveTest, including the name≠path case where the friendly name must NOT appear in the derived URL).
    - The derive uses `toHttpUrlOrNull` and `encodedPath` — grep confirms no `+ "/"` style stream-URL string concatenation in the new function.
    - The explicit-tag path reads `cam.extraData` and is preferred when the tag is present.
    - WebcamUrlDeriveTest passes: `/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --tests *WebcamUrlDeriveTest --no-daemon" | tr -d '\r'` exits 0.
    - Full webcam test slice green (no regression): `/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --tests *Webcam* --no-daemon" | tr -d '\r'` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --tests *WebcamUrlDeriveTest --tests *Webcam* --no-daemon" | tr -d '\r'</automated>
  </verify>
  <done>deriveNativeStreamUrl + explicit-tag-else-derive land in WebcamUrl.kt, parse the path from stream_url (never the name), fail-safe to null, and WebcamUrlDeriveTest is green with the landmine + precedence covered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Moonraker `Webcam` fields → app | all fields untrusted (tolerant model); service/streamUrl/extraData feed selection + derive |
| derived URL → logs/UI | a derived/surfaced URL string crosses into diagnostics |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-03-01 | Tampering | hostile `service`/`stream_url` selecting H.264 | mitigate | selectsH264Rung is a HINT only (D-10); the decoder verifies in plan 04 (T-10-06). A wrong/hostile service can only mis-SELECT, never force a fatal decode — the rung falls through. This plan adds only the hint + derive, never an authority. |
| T-21-03-02 | Denial of Service | malformed `stream_url` crashing the derive | mitigate | `toHttpUrlOrNull()` → null → null return → rung falls through (the existing fail-safe; asserted in WebcamUrlDeriveTest blank/garbage cases). Never throws. |
| T-21-03-03 | Information Disclosure | token leak via a logged derived URL | mitigate | Any surfaced derived URL routes through `surfaceWebcamUrl`/`redactWebcamUrl` (the existing V7 control); RTSP/HLS are tokenless but the snapshot fallback URL is not, so a mixed log must not leak it. |
</threat_model>

<verification>
- RungSelectTest + WebcamUrlDeriveTest green (two of the four Wave-0 scaffolds closed).
- `*Webcam*` test slice green (no regression in the existing ladder).
- grep proofs: rungFor stays H264-free; derive uses HttpUrl not concat.
</verification>

<success_criteria>
- Rung.H264 is the tier-0 top rung; selectsH264Rung is a pure HINT; rungFor stays service-blind (T-10-06).
- deriveNativeStreamUrl swaps port keeping the path from stream_url (never name); explicit-tag preferred when present; fail-safe to null.
- CAM-11/12/13 unit coverage green.
</success_criteria>

<output>
Create `.planning/phases/21-webrtc-camera-streaming/21-03-SUMMARY.md` when done.
</output>
