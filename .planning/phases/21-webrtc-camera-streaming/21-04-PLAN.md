---
phase: 21-webrtc-camera-streaming
plan: 04
type: execute
wave: 4
depends_on: ["21-02", "21-03"]
files_modified:
  - app/src/main/java/works/mees/dinghy/ui/webcam/Media3Feed.kt
  - app/src/main/java/works/mees/dinghy/render/Media3SurfaceHost.kt
  - app/src/main/java/works/mees/dinghy/ui/webcam/WebcamHolder.kt
  - app/src/main/java/works/mees/dinghy/ui/webcam/WebcamScreen.kt
  - app/src/main/java/works/mees/dinghy/spike/SpikeActivity.kt
  - app/src/main/AndroidManifest.xml
  - app/src/test/java/works/mees/dinghy/webcam/Media3FeedOutcomeTest.kt
  - app/src/test/java/works/mees/dinghy/webcam/RtspSourceConfigTest.kt
  - app/src/test/java/works/mees/dinghy/webcam/WebcamReconnectStateTest.kt
autonomous: true
requirements: [CAM-12, CAM-14, CAM-15, CAM-16]

must_haves:
  truths:
    - "Media3Feed is a WebcamFeed plugged into the existing host-tested WebcamHolder reconnect machine — the drive()/cancel()/FeedOutcome state machine is reused VERBATIM"
    - "All ExoPlayer ops (build/prepare/setVideoSurfaceView/release) run on Dispatchers.Main while the holder driver stays IO-dispatched — the flagged divergence"
    - "PlaybackException maps to FeedOutcome: network drop → Transient (backoff), decoder-init/unsupported → Terminal (decoder VERIFIES → falls to MJPEG/Snapshot, D-10/T-10-06)"
    - "The codec/player releases leak-free on screen exit: setVideoSurfaceView(null) THEN release(), idempotent, mirroring WebcamHolder.cancel()"
    - "Media3SurfaceHost hosts a raw SurfaceView via AndroidView with the onReset/onRelease null-surface leak guard; the SurfaceView-cutout chrome follows the owner's recorded spike decision"
    - "The throwaway SpikeActivity is deleted and de-registered from the manifest"
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/ui/webcam/Media3Feed.kt"
      provides: "The H.264 WebcamFeed binding: build player, attach surface (main thread), map PlaybackException→FeedOutcome, leak-free release"
      min_lines: 50
    - path: "app/src/main/java/works/mees/dinghy/render/Media3SurfaceHost.kt"
      provides: "AndroidView<SurfaceView> player host with leak guard + preview short-circuit"
      min_lines: 30
  key_links:
    - from: "app/src/main/java/works/mees/dinghy/ui/webcam/Media3Feed.kt"
      to: "WebcamHolder WebcamFeed seam + FeedOutcome"
      via: "implements WebcamFeed, returns FeedOutcome — reuses drive()/cancel() verbatim"
      pattern: "FeedOutcome"
    - from: "app/src/main/java/works/mees/dinghy/ui/webcam/Media3Feed.kt"
      to: "Dispatchers.Main"
      via: "withContext(Dispatchers.Main) for all ExoPlayer ops"
      pattern: "Dispatchers\\.Main"
    - from: "app/src/main/java/works/mees/dinghy/ui/webcam/WebcamHolder.kt"
      to: "selectsH264Rung + Rung.H264 + Media3Feed"
      via: "resolveSelectedCam routes H.264-selected cams to the Media3 feed branch"
      pattern: "selectsH264Rung"
---

<objective>
Build the on-device player binding: `Media3Feed` (the H.264 `WebcamFeed` that builds an ExoPlayer,
forces RTSP-over-TCP, attaches a SurfaceView, maps `PlaybackException` into the existing `FeedOutcome`,
and releases leak-free), `Media3SurfaceHost` (the `AndroidView<SurfaceView>` host mirroring
`WebcamViewHost`), and the `WebcamHolder` wiring that routes H.264-selected cams through it. The lead
transport (RTSP or HLS) and the SurfaceView-cutout chrome follow the recorded spike DECISION
(`21-SPIKE-RESULT.md`). Closes the last two Wave-0 RED scaffolds (Media3FeedOutcome, RtspSourceConfig)
and extends WebcamReconnectStateTest. Deletes the throwaway spike.

Purpose: this is the genuinely-new shipped work — narrow by design (PATTERNS: "almost everything except
build-an-ExoPlayer-force-TCP-attach-a-SurfaceView-map-its-errors is already built"). The two real
hazards are the main-thread ExoPlayer confinement (a DIVERGENCE from the IO-dispatched bitmapFeed) and
the leak-free release (the project's frozen-feed-after-restart history, WR-01). The reconnect/backoff/
dead-end machinery is REUSED verbatim — the H.264 rung is just a new WebcamFeed.

Output: a wired H.264 rung that renders on-device, falls through to MJPEG/snapshot on decoder error,
releases cleanly, plus green CAM-12/14/16 unit coverage and a removed spike.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-webrtc-camera-streaming/21-CONTEXT.md
@.planning/phases/21-webrtc-camera-streaming/21-RESEARCH.md
@.planning/phases/21-webrtc-camera-streaming/21-PATTERNS.md
@.planning/phases/21-webrtc-camera-streaming/21-SPIKE-RESULT.md
@app/src/main/java/works/mees/dinghy/ui/webcam/WebcamHolder.kt
@app/src/main/java/works/mees/dinghy/render/WebcamViewHost.kt
@app/src/main/java/works/mees/dinghy/ui/webcam/WebcamScreen.kt
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build Media3Feed (main-thread ExoPlayer, forced-TCP RTSP/HLS, FeedOutcome mapping); close Media3FeedOutcomeTest + RtspSourceConfigTest</name>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/webcam/WebcamHolder.kt (the `fun interface WebcamFeed<T>` seam lines 316-318; `bitmapFeed` lines 331-384 — the existing WebcamFeed<Bitmap> to mirror; `FeedOutcome` enum lines 297-307 — Transient/Terminal/Cancelled; the `driverContext = Dispatchers.IO` note lines 75-88/406-408 — the divergence source)
    - .planning/phases/21-webrtc-camera-streaming/21-RESEARCH.md § Pattern 1 (RtspMediaSource.Factory().setForceUseRtpTcp().setTimeoutMs(8000)) + § Code Examples (HlsMediaSource.Factory + the onPlayerError→FeedOutcome mapping) + § ExoPlayer Lifecycle Contract (the create/attach/prepare/error/release table) + Pitfall 2 (main-thread) + Pitfall 3 (leak) + Pitfall 4 (1.10.1)
    - .planning/phases/21-webrtc-camera-streaming/21-PATTERNS.md § Media3Feed.kt (the WebcamFeed binding, the CRITICAL main-thread DIVERGENCE callout, the outcome-mapping example) + § Idempotent leak-free teardown
    - .planning/phases/21-webrtc-camera-streaming/21-SPIKE-RESULT.md (the recorded lead transport — wire it as the primary; the non-leading transport is the robustness fallback per D-04; if WebRTC-escape was Y, STOP — this plan does not apply, re-plan per D-03)
    - app/src/test/java/works/mees/dinghy/webcam/Media3FeedOutcomeTest.kt + RtspSourceConfigTest.kt (the Wave-0 RED scaffolds to convert)
  </read_first>
  <behavior>
    - onPlayerError(ERROR_CODE_IO_NETWORK_CONNECTION_FAILED) maps to FeedOutcome.Transient
    - onPlayerError(ERROR_CODE_IO_NETWORK_CONNECTION_TIMEOUT) maps to FeedOutcome.Transient
    - onPlayerError(decoder-init / unsupported / other) maps to FeedOutcome.Terminal (decoder VERIFIES → holder falls to next rung)
    - clean cancellation maps to FeedOutcome.Cancelled
    - the RTSP MediaSource Factory is constructed with forceUseRtpTcp (MediaMTX is TCP-only) — asserted on the factory/config, not on playback
  </behavior>
  <action>
    Create `app/src/main/java/works/mees/dinghy/ui/webcam/Media3Feed.kt` implementing the existing `WebcamFeed` seam
    (a sibling to `bitmapFeed`). Build `ExoPlayer.Builder(context).build()`, construct the source per the recorded spike
    lead — RTSP via `RtspMediaSource.Factory().setForceUseRtpTcp().setTimeoutMs(8000).createMediaSource(MediaItem.fromUri(rtspUri))`,
    HLS via `HlsMediaSource.Factory(DefaultHttpDataSource.Factory()).createMediaSource(...)` — `setVideoSurfaceView(surfaceView)`
    (raw SurfaceView, NO PlayerView), `playWhenReady = true`, `prepare()`. ⚠ CRITICAL DIVERGENCE: the holder drives this feed on
    `Dispatchers.IO`, but ExoPlayer is single-threaded by contract — marshal ALL player ops (build/prepare/setVideoSurfaceView/
    release) onto `Dispatchers.Main` via `withContext(Dispatchers.Main)` (or a main `Handler`) while the IO driver loop owns
    orchestration. Add a `Player.Listener.onPlayerError(PlaybackException)` that maps to `FeedOutcome` per `<behavior>` (the
    decoder-VERIFIES half of D-10/T-10-06 — a Terminal decoder error falls the holder to MJPEG/Snapshot). On teardown:
    `setVideoSurfaceView(null)` THEN `player.release()`, idempotent (mirror `WebcamHolder.cancel()`; the WR-01 frozen-feed history).
    Extract the URL via `nativeStreamUrlFor` (plan 03) and surface any diagnostic through `surfaceWebcamUrl` (V7). Convert
    Media3FeedOutcomeTest (fake listener → outcome mapping) and RtspSourceConfigTest (factory built with forceUseRtpTcp) to live assertions.
  </action>
  <acceptance_criteria>
    - Media3Feed implements the `WebcamFeed` interface and returns `FeedOutcome`; grep confirms `withContext(Dispatchers.Main)` (or a main Handler) wraps the player ops.
    - The outcome mapping matches `<behavior>` exactly (asserted via a fake listener in Media3FeedOutcomeTest — no real player in the unit test, per the host-testability seam).
    - The RTSP factory is built with `setForceUseRtpTcp` (asserted in RtspSourceConfigTest); grep `grep -c 'setForceUseRtpTcp' app/src/main/java/works/mees/dinghy/ui/webcam/Media3Feed.kt` returns ≥1.
    - Teardown detaches surface before release: grep both `setVideoSurfaceView(null)` and `.release()` present in Media3Feed.kt.
    - Media3FeedOutcomeTest + RtspSourceConfigTest pass: `/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --tests *Media3FeedOutcomeTest --tests *RtspSourceConfigTest --no-daemon" | tr -d '\r'` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --tests *Media3FeedOutcomeTest --tests *RtspSourceConfigTest --no-daemon" | tr -d '\r'</automated>
  </verify>
  <done>Media3Feed plugs into the WebcamFeed seam, confines player ops to the main thread, maps PlaybackException→FeedOutcome (decoder-verifies), releases leak-free, and CAM-14/16 unit coverage is green.</done>
</task>

<task type="auto">
  <name>Task 2: Build Media3SurfaceHost + wire WebcamHolder to route H.264-selected cams to Media3Feed; extend WebcamReconnectStateTest; delete the spike</name>
  <read_first>
    - app/src/main/java/works/mees/dinghy/render/WebcamViewHost.kt (the factory-once/update-push AndroidView shape lines 38-68; the preview short-circuit lines 54-57 `LocalInspectionMode.current → PreviewPlaceholderBox`)
    - app/src/main/java/works/mees/dinghy/ui/webcam/WebcamHolder.kt (drive() lines 205-242 + FeedOutcome lines 297-307 + cancel() lines 172-179 + start()/stop() lines 137-165 — REUSE VERBATIM; resolveSelectedCam lines 245-258 where it hardcodes `Rung.Mjpeg` at line 257 — the H.264 select slots in here; webcamBitmapHolder wiring lines 391-409)
    - app/src/main/java/works/mees/dinghy/ui/webcam/WebcamScreen.kt (the FeedFocus render path lines 71-134; the DisposableEffect onDispose { holder.stop() } lines 81-84 — the screen-exit release contract; swap WebcamViewHost→Media3SurfaceHost only for the H.264 rung, or host both)
    - app/src/test/java/works/mees/dinghy/webcam/WebcamReconnectStateTest.kt (extend: an H.264 feed returning Terminal falls through the reconnect machine to the next rung; a Transient backs off — REUSING the existing host-tested machine)
    - .planning/phases/21-webrtc-camera-streaming/21-PATTERNS.md § Media3SurfaceHost.kt (the AndroidView host + the onReset/onRelease null-surface guard + the SurfaceView clipping limitation) + § WebcamHolder.kt (the H.264 select branch + main-thread marshaling + REUSE-VERBATIM list)
    - .planning/phases/21-webrtc-camera-streaming/21-SPIKE-RESULT.md (the owner's recorded cutout = overlay|square decision — implement EXACTLY that; do NOT pick independently — hard owner law)
  </read_first>
  <action>
    Create `app/src/main/java/works/mees/dinghy/render/Media3SurfaceHost.kt`: an `AndroidView({ ctx -> SurfaceView(ctx) }, update = { … })`
    mirroring WebcamViewHost's factory-once/update-push contract; attach the player surface in factory/update; add the documented
    Compose-AndroidView leak guard (`onReset`/`onRelease` → `player.setVideoSurfaceView(null)`); short-circuit under
    `LocalInspectionMode.current` to `PreviewPlaceholderBox` (a live RTSP feed never renders under @Preview). Implement the
    rounded-cutout chrome per the OWNER'S RECORDED SPIKE DECISION (sibling-overlay rounded mask ABOVE the SurfaceView, OR square
    corners) — do NOT silently choose; if `21-SPIKE-RESULT.md` lacks the call, STOP and surface it. In WebcamHolder.kt: route
    H.264-selected cams (`selectsH264Rung(cam)` → `Rung.H264` in resolveSelectedCam) to `Media3Feed`, else the existing bitmapFeed
    probe path; thread a `Context` for `ExoPlayer.Builder` into the production wiring (webcamBitmapHolder analog); keep the IO
    `driverContext` for orchestration (only player ops marshal to main). REUSE VERBATIM drive()/cancel()/FeedOutcome/start()/stop().
    In WebcamScreen.kt, render the H.264 rung through Media3SurfaceHost (the same DisposableEffect onDispose release contract holds).
    Extend WebcamReconnectStateTest: an H.264 feed Terminal → falls to MJPEG/Snapshot; Transient → backs off (proving the reuse).
    DELETE the throwaway `spike/SpikeActivity.kt` and remove its AndroidManifest registration.
  </action>
  <acceptance_criteria>
    - Media3SurfaceHost.kt exists, uses `AndroidView` with a `SurfaceView` factory, has the `LocalInspectionMode` preview short-circuit, and nulls the player surface in onReset/onRelease (grep `setVideoSurfaceView(null)`).
    - WebcamHolder routes H.264-selected cams to Media3Feed: grep `selectsH264Rung` and `Rung.H264` present in WebcamHolder.kt; drive()/cancel()/FeedOutcome bodies unchanged (the reconnect machine reused, not rewritten).
    - The spike is gone: `test ! -f app/src/main/java/works/mees/dinghy/spike/SpikeActivity.kt` true; no SpikeActivity reference in AndroidManifest.xml.
    - WebcamReconnectStateTest extended + green; full webcam slice green: `/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --tests *Webcam* --no-daemon" | tr -d '\r'` exits 0.
    - Release build + floor guard green: `/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:assembleRelease :app:verifyMinSdkRelease --no-daemon" | tr -d '\r'` exits 0 (R8 keep rules survive; floor held).
  </acceptance_criteria>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --tests *Webcam* :app:assembleRelease :app:verifyMinSdkRelease --no-daemon" | tr -d '\r'</automated>
  </verify>
  <done>The H.264 rung renders through Media3SurfaceHost, the holder routes H.264 cams to Media3Feed reusing the reconnect machine, the cutout follows the owner's recorded call, the spike is deleted, and the full webcam suite + release build + floor guard are green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Moonraker `service`/`stream_url` → rung select | untrusted hint selects the H.264 rung |
| RTSP/HLS media bytes → ExoPlayer MediaCodec | untrusted media crosses into the decoder |
| derived/diagnostic URL → logs | a URL string crosses into logging |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-04-01 (T-10-06) | Tampering | hostile `service`/`stream_url` routing bytes into a fatal mis-decode | mitigate | Decoder-verifies: a bad guess SELECTS H.264 (a hint) but ExoPlayer errors → onPlayerError(Terminal) → holder falls to MJPEG/Snapshot. A hostile/wrong service can NEVER force a fatal mis-decode or render attacker-controlled bytes as a valid frame — it falls through the rung ladder. Asserted in WebcamReconnectStateTest (Terminal → next rung). |
| T-21-04-02 | Denial of Service (self) | held MediaCodec exhausting the 2 GB device | mitigate | Idempotent leak-free release: `setVideoSurfaceView(null)` THEN `release()` on screen exit (DisposableEffect onDispose) + the AndroidView onReset/onRelease null-surface guard. Mirrors WebcamHolder.cancel() (WR-01 frozen-feed history). |
| T-21-04-03 | Information Disclosure | token leak via a logged RTSP/snapshot URL | mitigate | All H.264 diagnostics route through `surfaceWebcamUrl`/`redactWebcamUrl` (the existing V7 control). RTSP/HLS tokenless, but a mixed log must not leak the snapshot `?token=`. |
| T-21-04-04 | Denial of Service | calling ExoPlayer off the main thread (crash) | mitigate | All player ops confined to `Dispatchers.Main` (the flagged divergence) — asserted by grep + structurally enforced in Media3Feed. |
</threat_model>

<verification>
- Media3FeedOutcomeTest + RtspSourceConfigTest + extended WebcamReconnectStateTest green (last two Wave-0 scaffolds closed).
- Full `*Webcam*` slice green; `:app:assembleRelease` + `:app:verifyMinSdkRelease` green (R8 + floor survive media3).
- Spike deleted + de-registered; H.264 rung renders through Media3SurfaceHost with the owner's recorded cutout.
- NOTE: latency, hardware decode, no-leak, and no-crash-regression are NOT closed here — they are the plan-05 on-device UAT gate (the load-bearing criteria; green units are not a passing camera).
</verification>

<success_criteria>
- The shipped H.264 rung is wired (Media3Feed + Media3SurfaceHost + holder routing), reuses the reconnect machine verbatim, confines player ops to the main thread, and releases leak-free.
- The lead transport + cutout chrome follow the recorded spike DECISION.
- CAM-12/14/16 unit coverage green; release build + floor guard green; spike removed.
</success_criteria>

<output>
Create `.planning/phases/21-webrtc-camera-streaming/21-04-SUMMARY.md` when done.
</output>
