---
phase: 21-webrtc-camera-streaming
plan: 02
type: execute
wave: 2
depends_on: ["21-01"]
files_modified:
  - app/src/main/java/works/mees/dinghy/spike/SpikeActivity.kt
  - app/src/main/AndroidManifest.xml
  - .planning/phases/21-webrtc-camera-streaming/21-SPIKE-RESULT.md
autonomous: false
requirements: [CAM-10]
user_setup: []

must_haves:
  truths:
    - "A throwaway SpikeActivity plays Media3 RTSP AND HLS of a printer camera on flox, on the real Adreno 320"
    - "Glass-to-glass latency is measured (median of repeated captures) for RTSP and HLS on BOTH printers"
    - "The decoder name is logged (OMX.qcom hardware vs OMX.google software) and whether the RTSP SDP carries SPS/PPS in fmtp is recorded"
    - "The spike emits a recorded DECISION: RTSP-vs-HLS lead, fallback transport, and whether the D-03 WebRTC escape is triggered"
    - "The owner makes the SurfaceView rounded-cutout-vs-square-corners aesthetic call (not silently picked)"
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/spike/SpikeActivity.kt"
      provides: "Throwaway on-device latency/decode/APK harness (deleted in plan 04)"
      min_lines: 40
    - path: ".planning/phases/21-webrtc-camera-streaming/21-SPIKE-RESULT.md"
      provides: "The recorded D-02 readout + DECISION that plans 03/04 consume"
      contains: "DECISION"
  key_links:
    - from: "app/src/main/java/works/mees/dinghy/spike/SpikeActivity.kt"
      to: "androidx.media3 ExoPlayer + RtspMediaSource/HlsMediaSource"
      via: "ExoPlayer.Builder + setForceUseRtpTcp(true) + setVideoSurfaceView"
      pattern: "ExoPlayer\\.Builder"
    - from: ".planning/phases/21-webrtc-camera-streaming/21-SPIKE-RESULT.md"
      to: "plans 21-03 / 21-04"
      via: "recorded transport-lead DECISION consumed as the shipped lead"
      pattern: "lead"
---

<objective>
THE GATING DELIVERABLE (D-02). Before any shipped rung is built, prove on the real Nexus 7 (flox)
against BOTH printers whether Media3 can hit the ~1–2 s glass-to-glass bar (D-05) and which transport
(RTSP `:8554` TCP vs HLS `:8888`) leads. This is throwaway code — a bare temporary Activity, NOT the
shipped rung, deleted in plan 04 — that bypasses the holder/WebcamFeed entirely. Its recorded readout
+ DECISION settles three things the shipped-rung plans 03/04 CONSUME and the planner deliberately did
NOT predetermine: (1) the RTSP-vs-HLS lead (D-04), (2) whether MediaMTX's RTSP DESCRIBE SDP carries
SPS/PPS in the fmtp attribute (RESEARCH now rates this LOW-risk — MediaMTX provably emits
`sprop-parameter-sets`, and the real RTSP failure mode is 461-Unsupported-Transport which
`setForceUseRtpTcp(true)` defeats — but the spike still confirms it on this silicon; if absent, RTSP
fails decoder-init and HLS legitimately leads), and (3) whether the latency bar is met at all (the
D-03 WebRTC-escape trigger).

Purpose: every verified source is silent on the deciding numbers on this exact silicon; the
feasibility doc proved the footprint win but explicitly REFUTED any latency claim — only on-device
measurement settles it (the mock-vs-reality discipline — green units are not a passing camera).

Output: SpikeActivity + a recorded `21-SPIKE-RESULT.md` carrying the DECISION (lead/fallback/
WebRTC-escape?) and the owner's SurfaceView-cutout aesthetic call.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-webrtc-camera-streaming/21-CONTEXT.md
@.planning/phases/21-webrtc-camera-streaming/21-RESEARCH.md
@.planning/phases/21-webrtc-camera-streaming/21-PATTERNS.md
@.planning/phases/21-webrtc-camera-streaming/research/webrtc-vs-media3-feasibility.md
@app/build.gradle.kts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the throwaway SpikeActivity (Media3 RTSP + HLS, raw SurfaceView, decoder + SDP logging)</name>
  <read_first>
    - .planning/phases/21-webrtc-camera-streaming/21-RESEARCH.md § The Throwaway Spike (the glass-to-glass method, the D-02 readout table, the SPS/PPS-in-fmtp verification requirement) + § Pattern 1 (RtspMediaSource.Factory().setForceUseRtpTcp(true).setTimeoutMs(8000)) + § Code Examples (HlsMediaSource.Factory)
    - .planning/phases/21-webrtc-camera-streaming/21-PATTERNS.md § spike/SpikeActivity.kt (THROWAWAY — bypasses the holder; NOT built against the rung analogs)
    - .planning/phases/21-webrtc-camera-streaming/21-CONTEXT.md (D-02 the spike contract; D-07 the encoder reality; "Spike how-to" + "Encoder reality" under Specific Ideas)
    - .planning/phases/21-webrtc-camera-streaming/research/webrtc-vs-media3-feasibility.md (the REFUTED claims — do NOT predetermine "2-3s" or SPS/PPS presence)
    - app/src/main/AndroidManifest.xml (existing activity registration shape; the spike Activity needs a debug-only/launcher-reachable entry)
    - Printer addresses: Ender 5 Plus Moonraker 192.168.1.120:7125 (MediaMTX :8554/:8888/:8889); Ender 3 Pro 192.168.1.121:7125
  </read_first>
  <action>
    Create `app/src/main/java/works/mees/dinghy/spike/SpikeActivity.kt` — a bare temporary Activity that: builds an
    `ExoPlayer.Builder(this).build()` on the MAIN thread (ExoPlayer is single-threaded by contract); accepts a hardcoded
    or intent-supplied stream URL + a transport toggle; for RTSP builds `RtspMediaSource.Factory().setForceUseRtpTcp(true).setTimeoutMs(8000).createMediaSource(MediaItem.fromUri(uri))`
    — note `setForceUseRtpTcp` takes a BOOLEAN in Media3 1.10.1 (`setForceUseRtpTcp(forceUseRtpTcp: Boolean)`), NOT a no-arg
    call; pass `true` (MediaMTX RTSP is TCP-only). Confirm the exact signature against the resolved 1.10.1 artifact at build
    time — this spike build is the first place it compiles, so a wrong signature surfaces here. For HLS build
    `HlsMediaSource.Factory(DefaultHttpDataSource.Factory()).createMediaSource(...)`;
    attach a raw `SurfaceView` via `player.setVideoSurfaceView(surfaceView)` (NO PlayerView — footprint discipline); set
    `playWhenReady = true`; `prepare()`. On player events, log the SELECTED DECODER NAME by enumerating the chosen MediaCodec
    (must show whether it is `OMX.qcom.*` hardware or `OMX.google.*` software) and log any `PlaybackException` verbatim
    (a source/decoder-init error on RTSP prepare with no frames is the SPS/PPS-in-fmtp-absent signal — RESEARCH Pitfall 1).
    Provide RTSP and HLS URLs for both printers using the convention from ravens-perch `stream_manager.py` (RTSP `rtsp://192.168.1.120:8554/<path>`, HLS `http://192.168.1.120:8888/<path>/` — derive the `<path>` from the real Moonraker
    `/server/webcams/list` stream_url at spike time; do NOT hardcode a guessed path). On exit, `setVideoSurfaceView(null)` THEN
    `player.release()`. Register the Activity in AndroidManifest as a debug-reachable launcher entry (a second launcher icon or
    an explicit `am start` target). This is THROWAWAY — keep it crude; it is deleted in plan 04.
  </action>
  <acceptance_criteria>
    - `app/src/main/java/works/mees/dinghy/spike/SpikeActivity.kt` exists, builds an `ExoPlayer`, and references both
      `RtspMediaSource.Factory().setForceUseRtpTcp(true)` (boolean arg, NOT no-arg) and `HlsMediaSource.Factory`; grep `grep -c 'setForceUseRtpTcp(true)' app/src/main/java/works/mees/dinghy/spike/SpikeActivity.kt` returns ≥1.
    - On exit path the surface is detached before release: grep both `setVideoSurfaceView(null)` and `.release()` present.
    - The debug APK assembles + installs: `/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:installDebug --no-daemon" | tr -d '\r'` exits 0.
    - The Activity is launchable on flox (`adb shell am start` resolves to it) and logs a decoder name to logcat when a stream is played.
  </acceptance_criteria>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:assembleDebug --no-daemon" | tr -d '\r'</automated>
  </verify>
  <done>SpikeActivity builds + installs on flox, plays RTSP (forced TCP via setForceUseRtpTcp(true)) and HLS, and logs the selected decoder name + any PlaybackException for the SPS/PPS-in-fmtp signal.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Run the spike on flox vs BOTH printers; record the DECISION + owner's cutout call</name>
  <what-built>
    SpikeActivity (Media3 RTSP forced-TCP + HLS, raw SurfaceView, decoder-name + SDP-error logging), installed on flox.
    Now MEASURE on the real Adreno 320 against both MediaMTX printers and record the verdict that plans 03/04 consume.
  </what-built>
  <files>.planning/phases/21-webrtc-camera-streaming/21-SPIKE-RESULT.md</files>
  <action>This is an on-device measurement + owner-decision checkpoint. Run SpikeActivity on flox against both printers, measure glass-to-glass latency for RTSP and HLS, log the decoder name and the SPS/PPS-in-fmtp signal, and record the verdict into 21-SPIKE-RESULT.md per the steps below. The owner makes the SurfaceView-cutout aesthetic call. The recorded DECISION (lead/fallback/WebRTC-escape/cutout) is what plans 03/04 consume.</action>
  <how-to-verify>
    On flox (LineageOS 18.1 / API 30, genuine Adreno 320 / 2GB / armeabi-v7a), against the Ender 5 Plus
    (192.168.1.120, primary ravens-perch MediaMTX) AND the Ender 3 (192.168.1.121, crowsnest-class MediaMTX):
    1. Derive each camera's MediaMTX `<path>` from the live Moonraker `/server/webcams/list` stream_url (the `:8889/<path>/`
       segment) — confirm the derive convention holds on crowsnest too (RESEARCH Assumption A2 / Open Question 4).
    2. Display a running millisecond clock where the printer camera can see it; play the live RTSP feed of that clock in the
       spike on flox; photograph the source clock and the flox screen together; subtract → glass-to-glass latency. Median of
       ~10 captures. Repeat for HLS. Repeat on both printers. (RESEARCH § glass-to-glass method.)
    3. From logcat, record: the selected decoder name (OMX.qcom hardware vs OMX.google software) and whether RTSP `prepare()`
       errored with a source/decoder-init exception before any frame (→ SPS/PPS-in-fmtp absent → RTSP can't lead).
    4. Capture steady-state CPU% (`adb shell top`) and the APK-size delta (armeabi-v7a release, RTSP-only vs +HLS, `unzip -l`).
    5. OWNER AESTHETIC CALL (do NOT let Claude pick — hard owner law): the SurfaceView punches through the view hierarchy, so
       the existing rounded-cutout clip won't apply. Decide: sibling-overlay rounded chrome ABOVE the SurfaceView, or accept
       square corners for the H.264 rung. Record the decision.
    Write all of it into `.planning/phases/21-webrtc-camera-streaming/21-SPIKE-RESULT.md` using the RESEARCH "Pass/fail clean
    readout" template, ending in: `DECISION: lead = RTSP|HLS · fallback = the other · WebRTC escape needed? Y/N (D-03) · cutout = overlay|square`.
  </how-to-verify>
  <resume-signal>Reply with the recorded DECISION line (lead transport, fallback, WebRTC-escape Y/N, cutout call), or "spike done — see 21-SPIKE-RESULT.md". If WebRTC-escape = Y, this triggers a re-plan (D-03), NOT a default plan-03/04 build.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Moonraker `stream_url` → spike URL | untrusted server-advertised URL; spike derives RTSP/HLS from it |
| RTSP/HLS pipe → ExoPlayer decoder | untrusted media bytes cross into the MediaCodec |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-02-01 | Tampering | spike stream URL / decoder | accept | Throwaway spike, dev-controlled URLs on the dev's own LAN, not shipped. The shipped rung (plan 04) enforces decoder-verifies (T-10-06); the spike just measures. Low risk. |
| T-21-02-02 | Information Disclosure | logged stream URLs | mitigate | RTSP/HLS pipes are tokenless (verified `stream_manager.py`); the spike logs no `?token=` snapshot URL. Throwaway logging acceptable; the shipped path (plan 03/04) routes through `surfaceWebcamUrl` redaction. |
| T-21-02-03 | Denial of Service (self) | spike ExoPlayer codec | mitigate | Spike releases the player (`setVideoSurfaceView(null)` then `release()`) on exit; it is deleted in plan 04 so no leaked surface ships. |
</threat_model>

<verification>
- `:app:assembleDebug` green; spike installs + launches on flox.
- `21-SPIKE-RESULT.md` exists and contains a DECISION line (lead/fallback/WebRTC-escape/cutout).
- Latency medians recorded for RTSP and HLS on both printers; decoder name + SPS/PPS-in-fmtp signal recorded.
</verification>

<success_criteria>
- The D-02 spike ran on the real Adreno 320 against both printers and produced a recorded, owner-confirmed DECISION.
- The RTSP-vs-HLS lead (D-04), the SPS/PPS-in-fmtp finding, the latency-bar pass/fail (D-05 → D-03 trigger), and the SurfaceView-cutout aesthetic call are all settled and recorded for plans 03/04 to consume.
</success_criteria>

<output>
Create `.planning/phases/21-webrtc-camera-streaming/21-02-SUMMARY.md` when done.
Record the verdict in `.planning/phases/21-webrtc-camera-streaming/21-SPIKE-RESULT.md` (consumed by plans 03/04).
</output>
</content>
