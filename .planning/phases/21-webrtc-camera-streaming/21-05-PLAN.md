---
phase: 21-webrtc-camera-streaming
plan: 05
type: execute
wave: 5
depends_on: ["21-04"]
files_modified:
  - .planning/phases/21-webrtc-camera-streaming/21-UAT.md
autonomous: false
requirements: [CAM-17]

must_haves:
  truths:
    - "Live H.264 renders on flox from BOTH printers' MediaMTX cameras with correct aspect and no frozen frames (the cams Phase-10 MJPEG could not display) — D-08"
    - "Glass-to-glass latency meets the ~1–2 s bar (D-05) on both printers for the leading transport"
    - "The decode stays in hardware on the Adreno 320 (OMX.qcom, not software fallback) at steady CPU"
    - "Codec/player releases cleanly on screen exit — no leaked codec, no rising memory, no frozen feed after re-entry (WR-01 class) — D-08"
    - "Fallback to MJPEG/snapshot rungs is intact for a non-H.264 cam"
    - "D-20 fold-ins verified: no crash on the MediaMTX cams that previously crashed the screen; drawer tile gates correctly; per-printer camera selection holds (WR-03 null-key edge)"
  artifacts:
    - path: ".planning/phases/21-webrtc-camera-streaming/21-UAT.md"
      provides: "The recorded on-device UAT results on both printers (the load-bearing phase gate)"
      contains: "PASS"
  key_links:
    - from: ".planning/phases/21-webrtc-camera-streaming/21-UAT.md"
      to: "the shipped H.264 rung (plan 04) on real Adreno 320 + both MediaMTX printers"
      via: "live on-device verification of every Success Criterion"
      pattern: "Ender"
---

<objective>
THE LOAD-BEARING PHASE GATE. Green host unit tests CANNOT close this phase — latency, hardware decode,
no-leak, and no-crash-regression are inherently on-device (the mock-vs-reality discipline: a too-lenient
mock once hid a real server-contract bug behind green units). This plan proves the shipped H.264 rung
LIVE on flox (real Adreno 320 / 2GB / armeabi-v7a) against BOTH printers — the Ender 5 Plus
(192.168.1.120, primary ravens-perch MediaMTX) and the Ender 3 (192.168.1.121, crowsnest-class
MediaMTX) — the exact cameras the Phase-10 MJPEG path could never render. It also folds in the three
deferred webcam todos as UAT checks (D-20), NOT dedicated fix-tasks (adding real H.264 support IS the
likely fix for the webcam-screen-crash).

Purpose: D-08 defines "done" as the ~1–2 s bar met on BOTH printers, on-device, for the leading
transport, with clean codec/player release on exit — verified on flox against both SBCs. This is that
verification.

Output: a recorded `21-UAT.md` with every Success Criterion PASS/FAIL on both printers. A FAIL spawns
a gap-closure plan, NOT a phase-complete mark.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-webrtc-camera-streaming/21-CONTEXT.md
@.planning/phases/21-webrtc-camera-streaming/21-RESEARCH.md
@.planning/phases/21-webrtc-camera-streaming/21-SPIKE-RESULT.md
@.planning/phases/21-webrtc-camera-streaming/21-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build + install the release-class H.264 build on flox and scaffold 21-UAT.md</name>
  <read_first>
    - .planning/phases/21-webrtc-camera-streaming/21-VALIDATION.md (the Manual-Only Verifications table — the exact on-device criteria to script into the UAT)
    - .planning/phases/21-webrtc-camera-streaming/21-RESEARCH.md § Validation Architecture (the phase-gate posture: the spike readout + on-device UAT on both printers; green units ≠ passing camera)
    - .planning/phases/21-webrtc-camera-streaming/21-CONTEXT.md (D-08 the "done" bar; D-20 the three fold-in checks)
    - CLAUDE.md Local Build Environment (E:\Android\gw.bat; E:\Android\sign-release.bat for installing a release build; adb via interop)
    - Printer addresses: Ender 5 Plus 192.168.1.120:7125, Ender 3 192.168.1.121:7125
  </read_first>
  <action>
    Assemble the release APK via gw.bat, debug-sign it for on-device install (E:\Android\sign-release.bat — the release APK is
    unsigned until PKG-01/Phase 8), and `adb install` it on flox. Scaffold `.planning/phases/21-webrtc-camera-streaming/21-UAT.md`
    with one check row per Success Criterion, structured for BOTH printers (Ender 5 Plus / Ender 3), each row PENDING: (1) live
    H.264 renders with correct aspect + no frozen frames; (2) glass-to-glass latency ≤ ~1–2 s (the spike method, median of a few
    captures); (3) hardware decode confirmed (OMX.qcom in logcat, steady CPU, no software-fallback jank); (4) clean release on exit
    (enter/exit the webcam screen repeatedly — no leaked codec, no rising memory, no frozen feed on re-entry); (5) fallback to
    MJPEG/snapshot intact on a non-H.264 cam; and the D-20 fold-ins — (6) no crash on the MediaMTX cams that previously crashed the
    screen, (7) drawer tile gates correctly, (8) per-printer camera selection holds (WR-03 null-key edge: switch printers, confirm
    each keeps its own preferred cam). Note the crowsnest derive-convention confirmation (RESEARCH A2) as a sub-check on the Ender 3.
  </action>
  <acceptance_criteria>
    - The signed release-class APK installs on flox: `adb install` exits 0 (run via interop; build via gw.bat assembleRelease exits 0).
    - `21-UAT.md` exists with one PENDING row per Success Criterion, columns for both printers, and the three D-20 fold-in checks present.
  </acceptance_criteria>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:assembleRelease --no-daemon" | tr -d '\r'</automated>
  </verify>
  <done>The release-class H.264 build is installed on flox and 21-UAT.md is scaffolded with every Success Criterion + the D-20 fold-ins, both printers, all PENDING.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Run the on-device UAT on BOTH printers; record PASS/FAIL per criterion</name>
  <what-built>
    The shipped Media3 H.264 rung (Media3Feed + Media3SurfaceHost + holder routing), installed on flox. Now prove it LIVE on the
    real Adreno 320 against both MediaMTX printers — the cameras the Phase-10 MJPEG path could never render.
  </what-built>
  <files>.planning/phases/21-webrtc-camera-streaming/21-UAT.md</files>
  <action>This is the on-device UAT phase gate. Drive the shipped H.264 rung live on flox against both printers and record PASS/FAIL for every Success Criterion + the three D-20 fold-ins into 21-UAT.md per the steps below. A FAIL spawns a gap-closure plan, not a phase-complete mark.</action>
  <how-to-verify>
    On flox (LineageOS 18.1 / API 30, genuine Adreno 320 / 2GB / armeabi-v7a), with the live Ender 5 Plus (192.168.1.120, primary
    ravens-perch MediaMTX) AND the Ender 3 (192.168.1.121, crowsnest-class MediaMTX) — for EACH printer:
    1. Open the webcam screen, select the MediaMTX camera → live H.264 renders, correct aspect (no stretch), no frozen frames.
    2. Measure glass-to-glass latency (the spike clock-on-camera method, a few captures, median) → ≤ ~1–2 s (D-05).
    3. Confirm hardware decode in logcat (OMX.qcom.* not OMX.google.*); watch CPU/frame health — no software-fallback jank.
    4. Enter/exit the webcam screen ~5× → no leaked codec, no rising memory, no frozen feed on re-entry (the WR-01 class).
    5. On a cam WITHOUT an H.264 pipe (or by forcing it), confirm fallback to MJPEG/snapshot is intact.
    6. D-20 fold-ins: (a) the MediaMTX cams that previously crashed the screen no longer crash it; (b) the drawer webcam tile gates
       correctly (greyed when unavailable, live when available); (c) per-printer camera selection holds — switch between the two
       printers and confirm each retains its own preferred cam (the WR-03 null-key edge).
    7. Confirm the crowsnest (Ender 3) `stream_url` matched the derive convention (RESEARCH A2) — if it didn't, that's a recorded gap.
    Record each as PASS/FAIL in 21-UAT.md. If any criterion FAILS, that becomes a gap (spawn gap-closure), NOT a phase-complete mark.
  </how-to-verify>
  <resume-signal>Reply "approved" / "all pass" if every criterion passes on both printers, OR describe each FAIL (which printer, which criterion). A FAIL spawns a gap-closure plan per D-08/D-20. (On-device UAT may be owner-deferred per project posture — the orchestrator handles deferral; do NOT mark the phase verified here.)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live MediaMTX cameras → shipped rung | the real untrusted media + server-advertised URLs cross into the shipped decoder on real hardware |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-05-01 (T-10-06) | Tampering | the real cams routing bytes into a mis-decode | mitigate | UAT verifies decoder-verifies on real cams: a cam without a valid H.264 pipe falls through to MJPEG/Snapshot, never renders garbage (D-20 no-crash check + fallback check). This is the on-device confirmation of the plan-04 mitigation. |
| T-21-05-02 | Denial of Service (self) | leaked codec on the 2 GB device | mitigate | UAT check 4 (enter/exit ×5, no rising memory / frozen feed) is the on-device confirmation of the leak-free release — the only place a leak is observable. |
</threat_model>

<verification>
- The release-class build is installed on flox; 21-UAT.md records every Success Criterion + the D-20 fold-ins for BOTH printers.
- Latency ≤ ~1–2 s, hardware decode, no-leak, fallback-intact, and the three D-20 checks are PASS on both printers (or recorded FAILs → gap-closure).
- This — not the green unit suite — is the load-bearing phase gate (D-08).
</verification>

<success_criteria>
- Live H.264 renders on flox from both printers' MediaMTX cams (the cams MJPEG couldn't show), ≤ ~1–2 s glass-to-glass, hardware-decoded, leak-free on exit, with MJPEG/snapshot fallback intact — D-08.
- The three D-20 fold-ins verified (no-crash regression, tile gating, per-printer selection).
- Results recorded in 21-UAT.md; any FAIL routed to gap-closure, not silently closed.
</success_criteria>

<output>
Create `.planning/phases/21-webrtc-camera-streaming/21-05-SUMMARY.md` when done.
Record results in `.planning/phases/21-webrtc-camera-streaming/21-UAT.md`.
</output>
