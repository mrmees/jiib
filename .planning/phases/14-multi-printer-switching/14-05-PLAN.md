---
phase: 14-multi-printer-switching
plan: 05
type: execute
wave: 3
depends_on: ["14-03", "14-04"]
files_modified:
  - app/src/main/java/works/mees/dinghy/ui/screen/DevicesScreen.kt
  - app/src/main/java/works/mees/dinghy/ui/shell/AppDrawer.kt
  - app/src/main/java/works/mees/dinghy/ui/shell/AppShell.kt
autonomous: true
requirements: [MULTI-01]

must_haves:
  truths:
    - "The Devices drawer tile is LIVE (dest = Dest.Devices) and shows the active-printer name as a subtitle (D-01/D-03)"
    - "The Devices screen lists one tile per saved profile (name + host:port, active highlighted) + an Add-printer tile (D-01); tapping a profile persists it active and the existing seam rebinds through the recovery Splash, with no confirm guard"
    - "After a switch tap, the shell EXPLICITLY navigates to Dest.PrintStatus (D-02) so the recovery Splash returns to the new printer's Status — NOT back to the Devices screen (ShellNavState.dest is preserved across the Splash, so without this it would return to Devices)"
    - "The Devices screen suppresses the swipe-up drawer and carries an explicit green gutter Back (scroll-Field discipline)"
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/ui/screen/DevicesScreen.kt"
      provides: "full-screen field-of-printers switcher (ScreenScaffold Field + green gutter Back), tap-to-switch that persists active + signals navigate-to-Status (D-02)"
      contains: "ScreenScaffold"
      min_lines: 40
    - path: "app/src/main/java/works/mees/dinghy/ui/shell/AppDrawer.kt"
      provides: "Devices tile dest=Dest.Devices + active-name subtitle param"
      contains: "Dest.Devices"
    - path: "app/src/main/java/works/mees/dinghy/ui/shell/AppShell.kt"
      provides: "Dest.Devices host arm + swipe-suppress + activeName thread + post-switch navigateTo(Dest.PrintStatus) (D-02)"
      contains: "Dest.Devices"
  key_links:
    - from: "DevicesScreen tile tap"
      to: "profileStore.setActive(id) + onSwitched()"
      via: "instant persist → runConfigLoop rebind → recovery Splash; onSwitched → navigateTo(Dest.PrintStatus) (D-02)"
      pattern: "setActive"
    - from: "AppDrawer Devices tile"
      to: "active profile name subtitle"
      via: "activeName param threaded from AppShell (webcamEnabled/spoolEnabled precedent)"
      pattern: "activeName"
    - from: "AppShell Dest.Devices arm onSwitched"
      to: "navigateTo(Dest.PrintStatus)"
      via: "explicit nav so the preserved ShellNavState.dest lands on Status not Devices after the Splash (D-02)"
      pattern: "Dest.PrintStatus"
---

<objective>
Build the one net-new screen (the Devices switcher) and wire it into the shell: flip the greyed Devices
drawer tile LIVE with the active-printer-name subtitle (D-01/D-03), host `Dest.Devices` in AppShell with
swipe-drawer suppression, and — on a switch tap — EXPLICITLY navigate to `Dest.PrintStatus` so the
recovery Splash returns to the new printer's Status (D-02), not to the Devices screen. The screen reuses
the `AppDrawer`/`DrawerTile` square-tile grammar verbatim inside a Files-style scroll-Field
`ScreenScaffold` with an explicit green gutter Back. Tapping a printer persists it active — the existing
rebind seam does the rest (NO rebind logic added here).

The webcam-holder re-key wiring is NOT in this plan — it was completed end-to-end in plan 03 (wave 2)
to keep that wave compile-clean. Plan 05 touches AppShell only for the Devices host arm, swipe-suppress,
active-name subtitle, and post-switch navigation.

Purpose: Make printer switching reachable and visible, and land the switch on Status (D-02).
Output: `DevicesScreen.kt` (new), `AppDrawer.kt` + `AppShell.kt` (Devices wiring only).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-multi-printer-switching/14-CONTEXT.md
@.planning/phases/14-multi-printer-switching/14-PATTERNS.md
@.planning/phases/14-multi-printer-switching/14-UI-SPEC.md
@.planning/phases/14-multi-printer-switching/14-03-SUMMARY.md
@.planning/phases/14-multi-printer-switching/14-04-SUMMARY.md
@docs/ui_design/LAYOUT.md
@docs/ui_design/THEMING.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: DevicesScreen — full-screen field-of-printers switcher (ScreenScaffold Field + green gutter Back); switch tap persists active + signals navigate-to-Status</name>
  <files>app/src/main/java/works/mees/dinghy/ui/screen/DevicesScreen.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/shell/AppDrawer.kt (lines 87-110 the `LazyVerticalGrid(GridCells.Fixed(4))` + `spacedBy(12.dp)` + `.padding(16.dp)` + `.background(t.bg)`; the `DrawerTile` body lines 193-242 — `aspectRatio(1f)`, `clip(RoundedCornerShape(t.rCtrl))`, `background(t.surface2)`, `border(BorderStroke(2.dp, t.accentLine))`, the icon glyph at sizeSp 40f, the 16sp label — copy this tile grammar VERBATIM for the printer tiles)
    - app/src/main/java/works/mees/dinghy/ui/files/FilesScreen.kt (the scroll-Field + explicit green gutter Back pattern, and the ScreenScaffold field/gutter slot usage)
    - app/src/main/java/works/mees/dinghy/designsystem/layout/ScreenScaffold.kt (the Focus/Field/Gutter slots — use Field + Gutter only, omit Focus)
    - app/src/main/java/works/mees/dinghy/designsystem/OutlinedControl.kt (the green `Intent.Go` gutter button)
    - app/src/main/java/works/mees/dinghy/config/Profile.kt (plan 01 — `displayName()`, `id`)
    - app/src/main/java/works/mees/dinghy/config/ProfileStore.kt (plan 01 — `setActive`)
    - .planning/phases/14-multi-printer-switching/14-UI-SPEC.md (§ New surface — Devices list layout contract: grammar, tile content, active-tile emphasis, Add-printer tile, gutter Back, interaction, NO confirm on switch, NO looping animation; § Typography 20/17sp floors; § Color accent-reserved list)
    - .planning/phases/14-multi-printer-switching/14-RESEARCH.md (§ Code Examples — tile grid + swipe-suppress notes)
    - MEMORY [[dinghy-font-sizes-too-small]] (fsSp scale: 20sp name SemiBold, 17sp host:port GeistMono)
  </read_first>
  <action>
Create `ui/screen/DevicesScreen.kt`: `@Composable fun DevicesScreen(container: AppContainer, onAddPrinter: () -> Unit, onSwitched: () -> Unit, onBack: () -> Unit)`.
- Collect `val profiles by container.profileStore.profiles.collectAsStateWithLifecycle(emptyList())` and `val activeId by container.profileStore.activeId.collectAsStateWithLifecycle(null)`.
- `ScreenScaffold(field = { grid }, gutter = { Back })` — Focus omitted (no single primary item).
- Field: a `LazyVerticalGrid(GridCells.Fixed(4))` (landscape; planner may use a portrait-aware column count) reusing the AppDrawer tile grammar VERBATIM — `aspectRatio(1f)` sacred squares, `spacedBy(12.dp)`, `.padding(16.dp)`, `RoundedCornerShape(t.rCtrl)`, `2.dp` outline, `t.bg` background. Items: one tile per profile + a final "Add printer" tile.
  - Profile tile: a device glyph (`print` or `dns` — MUST NOT repeat the gutter Back glyph or the active-marker glyph; icon-no-repeat law), `profile.displayName()` at `fsSp(20f, t.fs)` SemiBold Geist, and `${profile.host}:${profile.port}` at `fsSp(17f, t.fs)` Regular GeistMono `t.text2`.
  - Active tile (`profile.id == activeId`, D-03): accent outline (`t.accentLine`) + a faint `t.accentSoft` fill tint + an accent active-marker glyph (a `check` or `bolt` MaterialSymbol — unique on-screen). Inactive saved tiles use the ordinary `t.accentLine`/`t.surface2` live styling.
  - "Add printer" tile: an `add` glyph + "Add printer" label → `onClick = onAddPrinter` (navigates to Settings, D-01).
- Tap a PROFILE tile → `scope.launch { container.profileStore.setActive(profile.id) }` THEN call `onSwitched()`. NO confirm guard (switching is non-destructive, D-02). The persisted active-id flip drives `activeConfig` → the existing `runConfigLoop` rebinds → RootController's recovery Splash. `onSwitched()` is the EXPLICIT D-02 navigation hook: AppShell maps it to `navigateTo(Dest.PrintStatus)` so the preserved `ShellNavState.dest` lands on the new printer's STATUS after the Splash, not back on Devices (see Task 2 — this is the FIX-4 gate). Do NOT add any disconnect/publishSpine/manual-rebind code (the seam does it). Delete is NOT on this screen (lives in Settings, D-14). It is fine to call `onSwitched()` even when tapping the already-active tile (a no-op switch still just lands on Status).
- Gutter: a single full-width green `Intent.Go` Back button → `onBack` (the explicit exit required because the swipe-drawer is suppressed for this scroll-Field).
- NO looping/breathing animation (Adreno-320 floor) — static outline + glow only. All color via role tokens (`LocalTokens`/THEMING.md) — never a raw color literal.
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:compileDebugKotlin --no-daemon" 2>&1 | tr -d '\r'</automated>
  </verify>
  <acceptance_criteria>
    - `:app:compileDebugKotlin` SUCCEEDS.
    - Source assertion: `DevicesScreen` uses `ScreenScaffold` with a Field tile grid + a green `Intent.Go` gutter Back; Focus is omitted.
    - Source assertion: a profile-tile tap calls `container.profileStore.setActive(profile.id)` then `onSwitched()`; there is NO `ConfirmGuard` on the switch path and NO `publishSpine`/disconnect call.
    - Source assertion: `DevicesScreen` declares an `onSwitched: () -> Unit` parameter and invokes it after `setActive` (the D-02 navigation hook).
    - Source assertion: the active tile (`id == activeId`) renders accent emphasis (accentLine outline + accentSoft tint + a unique marker glyph) distinct from inactive tiles (D-03).
    - Source assertion: profile name at `fsSp(20f, t.fs)` SemiBold, `host:port` at `fsSp(17f, t.fs)` GeistMono; all colors via `t.*`/`LocalTokens` (no raw `Color(0x…)` literals).
    - Source assertion: no looping/infinite animation API used (no `rememberInfiniteTransition`).
  </acceptance_criteria>
  <done>DevicesScreen renders the field of printers with active emphasis + Add-printer tile, switches via setActive with no confirm, signals onSwitched for the Status landing, and exits via a green gutter Back — token-pure, no looping animation.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the Devices drawer tile LIVE + active-name subtitle (D-01/D-03), host Dest.Devices in AppShell, and navigate to PrintStatus on switch (D-02)</name>
  <files>app/src/main/java/works/mees/dinghy/ui/shell/AppDrawer.kt, app/src/main/java/works/mees/dinghy/ui/shell/AppShell.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/shell/AppDrawer.kt (line 167 the greyed `DrawerTileSpec(label = "Devices", symbol = "cable", dest = null)`; the `AppDrawer` signature lines 75-80 with `webcamEnabled`/`spoolEnabled` params; the `DrawerTile` `Column` body lines 222-241 where a 15sp subtitle is added; the live/greyed styling 190-220 — do NOT touch it, Devices is a compile-time live tile)
    - app/src/main/java/works/mees/dinghy/ui/shell/AppShell.kt (lines 180-181 `webcamCount`/`webcamEnabled` collect; line 186 `cfg` collect; the `navigateTo`/`goBack` helpers at lines 153-154; the `when(dest)` host arms 430-587 (mirror `Dest.Webcam` 562 / `Dest.Files` 452); line 420 the swipe-suppress `setOf(...)`; the `AppDrawer(...)` call 684-690. NOTE: the webcam holder build at 199-205 was ALREADY re-keyed to the active profile id in plan 03 — do NOT touch it here)
    - app/src/main/java/works/mees/dinghy/ui/shell/ShellNavState.kt (the dest is PRESERVED across a recovery Splash — lines 42-44, 99-103: only `macroPopupFor`/`scanActive`/`spoolPrefilter` reset, NOT `dest`. This is WHY an explicit `navigateTo(Dest.PrintStatus)` is needed on switch: without it the preserved `dest` returns to `Dest.Devices` after the Splash, contradicting D-02. `navigateTo(Dest.PrintStatus)` also clears the backStack per its own logic at line 76)
    - app/src/main/java/works/mees/dinghy/ui/route/TopRoute.kt (the Status dest is `Dest.PrintStatus` — line 31/61; confirm the exact enum value name before wiring the nav)
    - app/src/main/java/works/mees/dinghy/di/AppContainer.kt (plan 02 — `activeProfile`; `activeProfile.map { it?.displayName() }`)
    - .planning/phases/14-multi-printer-switching/14-PATTERNS.md (§ AppDrawer.kt + § AppShell.kt — the collect→thread→host→suppress precedent + the active-name subtitle)
    - .planning/phases/14-multi-printer-switching/14-UI-SPEC.md (§ Reused-surface contracts → Drawer Devices tile: 15sp Regular t.text2 subtitle, ellipsized, only this tile gains a subtitle; label stays 16sp)
  </read_first>
  <action>
In `AppDrawer.kt`:
- Flip line 167 to `dest = Dest.Devices` (keep the `cable` glyph — unique, icon-no-repeat law).
- Add an `activeName: String? = null` param to `AppDrawer` (and pass it down to `DrawerTile` for the Devices tile only), threaded the SAME way `webcamEnabled`/`spoolEnabled` are. In `DrawerTile`'s `Column` (222-241), when this tile is Devices and `activeName != null`, render an optional subtitle under the 16sp label: `activeName` at `fsSp(15f, t.fs)` Regular `t.text2`, single-line ellipsized (`maxLines = 1`, `TextOverflow.Ellipsis`). This is the ONLY tile that gains a subtitle. Do NOT change the live/greyed styling (190-220) — Devices is a compile-time `dest != null` live tile (not runtime-gated like Webcam/Spool); the label itself stays the inherited 16sp SemiBold.

In `AppShell.kt`:
- Collect `val activeName by container.activeProfile.map { it?.displayName() }.collectAsStateWithLifecycle(initialValue = null)` (mirror the `webcamCount` collect at 180-181) and pass `activeName = activeName` into the `AppDrawer(...)` call (684-690).
- Add `Dest.Devices` to the swipe-suppress `setOf(...)` at line 420 (the scroll-Field fights the vertical-drag drawer gesture — Files/Console/Webcam/Spool precedent).
- Add a `Dest.Devices ->` arm in `when(dest)` (mirror `Dest.Webcam`/`Dest.Files`):
  `DevicesScreen(container = container, onAddPrinter = { navigateTo(Dest.Settings) }, onSwitched = { navigateTo(Dest.PrintStatus) }, onBack = { goBack() })`.
  The `onSwitched = { navigateTo(Dest.PrintStatus) }` is the FIX-4 gate (D-02): it sets `ShellNavState.dest` to PrintStatus so that AFTER the rebind recovery Splash (which preserves `dest`, see ShellNavState), the shell re-composes on the new printer's Status — NOT on the Devices screen the user tapped from. Do NOT add any rebind/disconnect logic — ONLY this navigation. Use the EXISTING `navigateTo`/`goBack` helpers (lines 153-154); do not invent a new nav mechanism.
- Do NOT touch the webcam holder build (199-205) — the active-profile-id re-key already landed in plan 03.
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:compileDebugKotlin --no-daemon" 2>&1 | tr -d '\r'</automated>
  </verify>
  <acceptance_criteria>
    - `:app:compileDebugKotlin` SUCCEEDS.
    - Source assertion: `AppDrawer.kt` Devices tile has `dest = Dest.Devices`; `AppDrawer`/`DrawerTile` accept an `activeName` param and render a 15sp `t.text2` ellipsized subtitle for the Devices tile only.
    - Source assertion: `AppShell.kt` collects `activeProfile.map { it?.displayName() }` and passes `activeName` to `AppDrawer`; `Dest.Devices` is in the swipe-suppress set (line 420) and has a `when(dest)` host arm rendering `DevicesScreen(...)`.
    - Source assertion (the FIX-4 gate): the `Dest.Devices` host arm passes `onSwitched = { navigateTo(Dest.PrintStatus) }` — so a switch tap lands on PrintStatus after the rebind Splash, not back on Devices.
    - Source assertion: this plan does NOT modify the webcam holder build (no `profileId`/`webcamBitmapHolder` edit in this plan's AppShell diff — that landed in plan 03).
  </acceptance_criteria>
  <done>The Devices drawer tile is live with the active-printer-name subtitle, AppShell hosts the Devices screen with swipe-suppression, and a switch tap explicitly navigates to PrintStatus so the recovery Splash lands on the new printer's Status (D-02).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Devices tile tap → active-profile persist → spine rebind | A UI action triggers a connection teardown/rebind to a different printer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-11 | Tampering | A UI-driven second rebind path (manual disconnect/publishSpine on tap) | mitigate | Tap only calls `profileStore.setActive` + `navigateTo(Dest.PrintStatus)`; the proven `runConfigLoop` seam does the teardown/rebind — code-review asserts no `publishSpine`/disconnect in DevicesScreen/AppShell |
| T-14-12 | Information disclosure | Active-printer name/address rendered on the drawer/Devices tiles | accept | Name + `host:port` are non-secret user-facing identifiers (the API key is never rendered); local single-user device |
| T-14-SC | Tampering | npm/pip/cargo installs | accept | No new external packages this phase |
</threat_model>

<verification>
- `:app:compileDebugKotlin` SUCCESS.
- Full `:app:testDebugUnitTest` GREEN at wave merge.
- On-device verification deferred to plan 06 (the switcher + subtitle + Status-landing are exercised in the live two-printer UAT; the FIX-4 check: tapping a Devices tile lands on PrintStatus, not Devices, after the Splash).
</verification>

<success_criteria>
- Devices tile live + active-name subtitle (D-01/D-03); Devices screen lists profiles, switches via setActive with no confirm, exits via green gutter Back.
- A switch tap explicitly navigates to PrintStatus (D-02) so the recovery Splash lands on the new printer's Status, not Devices.
- AppShell hosts Dest.Devices with swipe-suppression; the webcam re-key wiring is NOT duplicated here (it landed in plan 03).
- Token-pure, no looping animation, font floors honored.
</success_criteria>

<output>
Create `.planning/phases/14-multi-printer-switching/14-05-SUMMARY.md` when done.
</output>
</content>
