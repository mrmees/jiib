---
phase: 14-multi-printer-switching
plan: 04
type: execute
wave: 2
depends_on: ["14-02"]
files_modified:
  - app/src/main/java/works/mees/dinghy/ui/screen/SettingsScreen.kt
autonomous: true
requirements: [MULTI-01]

must_haves:
  truths:
    - "Settings Connection section is a list of saved profiles (name + host:port, active marked); each tappable to edit; an Add-printer entry opens the existing host/port/key + mDNS form 1:1 (D-13)"
    - "Adding the FIRST profile (empty store) makes it active (ProfileStore.upsert auto-selects when no active id, plan 01) so hasConfig flips true and the app routes into the Shell — the user is NOT stranded on Connect/Settings (D-11)"
    - "Each profile has a Delete affordance routed through the full-screen ConfirmGuard (D-14); delete fires profileStore.delete (D-12 auto-pick in the writer)"
    - "The Appearance section's write target becomes the ACTIVE profile's theme (D-09); the live themeResolver retheme stays immediate; controls are unchanged 1:1"
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/ui/screen/SettingsScreen.kt"
      provides: "profile-list CRUD (add/edit/delete-with-ConfirmGuard) + Appearance retargeted to active profile theme"
      contains: "profileStore"
  key_links:
    - from: "SettingsScreen connection save"
      to: "profileStore.upsert"
      via: "validated profile (replaces ConnectionStore.save); first add auto-active (plan 01)"
      pattern: "profileStore.upsert"
    - from: "SettingsScreen delete affordance"
      to: "ConfirmGuard → profileStore.delete"
      via: "destructive confirm (D-14)"
      pattern: "ConfirmGuard"
    - from: "SettingsScreen Appearance controls"
      to: "active profile theme primitives"
      via: "profileStore.upsert(active.copy(themeBase=…)) + live themeResolver.setBase"
      pattern: "themeBase"
---

<objective>
Restructure the Settings screen for profiles: the "Connection" section becomes a list of saved profile
rows (add/edit/delete), reusing the existing host/port/key + mDNS form 1:1; deletion routes through the
full-screen ConfirmGuard (D-14); and the "Appearance" section retargets its persist writes to the
ACTIVE profile's theme (D-09) while keeping the live retheme immediate. No new control types — reuse
`OutlinedControl`/`TokenTextField`/`Intent`/`DiscoveredPrinterRow`/`ConfirmGuard` verbatim.

Purpose: Give the user CRUD over profiles and per-printer theme editing, all from existing components.
Output: rewired `SettingsScreen.kt`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-multi-printer-switching/14-CONTEXT.md
@.planning/phases/14-multi-printer-switching/14-PATTERNS.md
@.planning/phases/14-multi-printer-switching/14-UI-SPEC.md
@.planning/phases/14-multi-printer-switching/14-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Settings Connection section → profile list CRUD (add/edit) + Add-printer entry (D-13); first add becomes active (D-11)</name>
  <files>app/src/main/java/works/mees/dinghy/ui/screen/SettingsScreen.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/screen/SettingsScreen.kt (FULL — the Connection form at lines 137-293 incl. the mDNS scan 184-253, the validate-then-`store.save(config)` block 256-293 (with no-clobber blank-key logic 270-281), `DiscoveredPrinterRow` row grammar 442-472, `onConnectionSaved` callback)
    - app/src/main/java/works/mees/dinghy/config/ProfileStore.kt (plan 01 — `upsert` (auto-selects active when the store has no active id, D-11) / `setActive`; `Profile.newId()`)
    - app/src/main/java/works/mees/dinghy/config/Profile.kt (plan 01 — `displayName()`, `toConnectionConfig()`, the theme fields)
    - app/src/main/java/works/mees/dinghy/di/AppContainer.kt (plan 02 — `profileStore`, `activeProfile`)
    - .planning/phases/14-multi-printer-switching/14-PATTERNS.md (§ SettingsScreen.kt — the connection-save-path generalization + profile-row grammar)
    - .planning/phases/14-multi-printer-switching/14-UI-SPEC.md (§ Reused-surface contracts → Settings profile CRUD; § Typography 20/17/15sp floors; § Copywriting)
    - MEMORY [[dinghy-font-sizes-too-small]] (use fsSp scale; 20sp name SemiBold, 17sp host:port GeistMono, 15sp metadata floor)
  </read_first>
  <action>
Restructure the "Connection" `SectionLabel` block (lines 137-293):
- Collect `val profiles by container.profileStore.profiles.collectAsStateWithLifecycle(emptyList())` and `val activeId by container.profileStore.activeId.collectAsStateWithLifecycle(null)`.
- Render a LIST of profile rows (one per profile) using the `DiscoveredPrinterRow` grammar (lines 442-472) as the template: name at `fsSp(20f, t.fs)` SemiBold (Geist) showing `profile.displayName()`, `host:port` at `fsSp(17f, t.fs)` Regular GeistMono `t.text2`. Mark the active row with the accent marker (per UI-SPEC — accent outline/`t.accentLine`, a unique accent glyph). Each row is `clickable` → opens the EDIT form pre-filled with that profile's host/port/key.
- Add an **"Add printer"** row (UI-SPEC copy) → opens the existing host/port/key + mDNS form BLANK.
- The form (host/port/key fields + the mDNS "Scan" button at 184-253) is REUSED 1:1; only the Save handler changes: replace `store.save(config)` (lines 284) with `scope.launch { container.profileStore.upsert(profile) }` where `profile` is built from the form values — for a NEW profile use `Profile(id = Profile.newId(), name = <optional, null if blank → defaults to host per D-10>, host, port, apiKey, themeBase = <current global ThemePrefs base>, fsChoice = <current global fs>, themeDeltaArgb = <current global deltas>)` so a new printer inherits the user's current look (RESEARCH Pattern 3 new-profile-default); for an EDIT preserve the existing profile's `id` + theme fields via `existing.copy(name=…, host=…, port=…, apiKey=…)`. PRESERVE the no-clobber blank-key logic (270-281): a blank key field on edit keeps the existing key (do not wipe it). After upsert keep calling `onConnectionSaved()`.
- FIRST-ADD ROUTING (D-11): do NOT add any active-selection code here. `ProfileStore.upsert` (plan 01) already auto-selects the new profile as active WHEN the store currently has no active id — so adding the very first printer flips `hasConfig` true (plan 02's `hasConfig = activeConfig.map { it != null }`) and the root controller routes out of Connect into the Shell on its own. The existing `onConnectionSaved()` callback (which navigates to PrintStatus) then lands the user on the running shell rather than back on a dead Connect/Settings screen. This is the FIX for the "first profile leaves the user stranded on Connect" show-stopper — verified by the acceptance criterion below.
- Client-side validation still mirrors `ConnectionStore.sanitize` (reject blank host / out-of-range port) before building the profile (lines 260-269 logic reused).

Do NOT auto-switch active on add/edit beyond the D-11 first-add auto-active above: editing the ACTIVE profile re-saves → the `activeConfig` seam live-rebinds only if host/port/key changed (distinctUntilChanged, plan 02). Adding a NEW profile when one is ALREADY active does not change the active selection (the user switches via Devices, plan 05). Do NOT touch the Appearance section in this task (Task 2).
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:compileDebugKotlin --no-daemon" 2>&1 | tr -d '\r'</automated>
  </verify>
  <acceptance_criteria>
    - `:app:compileDebugKotlin` SUCCEEDS.
    - Source assertion: SettingsScreen collects `profileStore.profiles` + `profileStore.activeId` and renders one row per profile + an "Add printer" row.
    - Source assertion: the connection Save handler calls `container.profileStore.upsert(...)` (not `ConnectionStore.save`); new profiles use `Profile.newId()`; edit preserves `existing.id` + theme fields.
    - Behavior (the FIX-1 routing gate, eyeballed in plan-06 UAT): from a clean/empty store, adding the FIRST printer leaves it active (no explicit setActive call needed here — plan 01's upsert does it), `hasConfig` flips true, and the app routes INTO the Shell (lands on PrintStatus via `onConnectionSaved`), NOT stuck on Connect/Settings. Add this as a plan-06 UAT check ("first-ever add → Shell, not Connect").
    - Source assertion: profile name uses `fsSp(20f, t.fs)` SemiBold and `host:port` uses `fsSp(17f, t.fs)` GeistMono (font floors honored).
    - Behavior: the blank-key-on-edit no-clobber logic is preserved (grep shows the existing-key retention branch).
  </acceptance_criteria>
  <done>Connection section is a profile list with add/edit reusing the form 1:1, writing through profileStore.upsert with stable ids and inherited new-profile theme; the first-ever add becomes active and routes into the Shell (D-11).</done>
</task>

<task type="auto">
  <name>Task 2: Delete-profile ConfirmGuard (D-14) + Appearance section retarget to active profile (D-09)</name>
  <files>app/src/main/java/works/mees/dinghy/ui/screen/SettingsScreen.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/screen/SettingsScreen.kt (the Appearance section at lines 295-368 — the dual-write pattern: `container.themeResolver.setBase(...)` live (307/317) + `scope.launch { container.themePrefs.setBase(...) }` persist (308/318); setFs 334; setDeltas 353/364)
    - app/src/main/java/works/mees/dinghy/designsystem/ConfirmGuard.kt (the guard API — `destructive` defaults true → red Intent.Danger; title/message/confirmLabel/cancelLabel/onConfirm/onCancel)
    - app/src/main/java/works/mees/dinghy/ui/files/FilesScreen.kt (how ConfirmGuard is invoked from a screen — the import + the state-gated overlay pattern, ~line 48)
    - app/src/main/java/works/mees/dinghy/config/Profile.kt (plan 01 — the theme fields `themeBase`/`fsChoice`/`themeDeltaArgb` to write; ThemeBase.name / FontScale.name / Role→ARGB Long shapes)
    - app/src/main/java/works/mees/dinghy/theme/ThemeResolver.kt (the `TokenDelta` data class is defined INSIDE this file at line 20 — `val overrides: Map<TokenDelta.Role, Long>`; there is NO `toArgbMap()` member and NO standalone TokenDelta.kt. The persisted `themeDeltaArgb: Map<String, Long>` is built from `overrides` by stringifying the Role keys)
    - .planning/phases/14-multi-printer-switching/14-UI-SPEC.md (§ Copywriting → Destructive confirmation copy; § Reused-surface contracts → Appearance)
    - .planning/phases/14-multi-printer-switching/14-PATTERNS.md (§ Delete Confirm guard + § SettingsScreen Appearance retarget)
  </read_first>
  <action>
Delete affordance (D-14): give each editable profile row (or the edit form) a Delete button (red `Intent.Danger`). On tap, show the full-screen `ConfirmGuard` reusing its default `destructive = true`:
- title `"Delete printer?"`, message `"This removes ${name} and its saved theme. This can't be undone."`, confirmLabel `"Delete"` (red), cancelLabel `"Keep"` (neutral safe-dismiss) — exact UI-SPEC copy.
- `onConfirm = { scope.launch { container.profileStore.delete(profile.id) } }` — the D-12 auto-pick (select another active / clear if last) fires inside the store writer (plan 01), and if the deleted profile was active the `activeConfig` seam rebinds to the auto-picked one (or routes to Connect when none remain). `onCancel` dismisses.
Manage the guard's visibility with a `remember { mutableStateOf<Profile?>(null) }` (the profile pending delete) — mirror the FilesScreen state-gated overlay.

Appearance retarget (D-09): the dark/light, S/M/L, and accent-picker controls (lines 295-368) keep their LIVE write (`container.themeResolver.setBase(...)` etc. — immediate retheme) UNCHANGED. Re-point only the PERSIST write from the global `themePrefs` to the **active profile's** theme fields: collect `val active by container.activeProfile.collectAsStateWithLifecycle(null)` and on each control change do `active?.let { scope.launch { container.profileStore.upsert(it.copy(themeBase = chosenBase.name)) } }` (and analogously `fsChoice = choice.name`). For the accent/delta control, build the persisted `themeDeltaArgb: Map<String, Long>` from the chosen `TokenDelta` by stringifying its Role keys — `delta.overrides.mapKeys { it.key.name }` (there is NO `delta.toArgbMap()`; `TokenDelta.overrides` is `Map<TokenDelta.Role, Long>`, defined in `ThemeResolver.kt`). If you prefer a named helper, add a tiny local/extension `fun TokenDelta.toPersistedArgb(): Map<String, Long> = overrides.mapKeys { it.key.name }` and use it at the call site — but match the actual `overrides` type either way. When there is NO active profile (idle/first-run), fall back to writing the global `themePrefs` as today (so the idle look is still editable / serves as the new-profile default — RESEARCH Pattern 3). Keep the Appearance mirror-seed (`LaunchedEffect` at 107-110) but seed it from the active profile's theme when present, else `themePrefs` (so the screen opens reflecting the active printer's look).

Keep the controls byte-identical otherwise — this is a write-target change, not a redesign (UI-SPEC: "only the write target changes").
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:compileDebugKotlin --no-daemon" 2>&1 | tr -d '\r'</automated>
  </verify>
  <acceptance_criteria>
    - `:app:compileDebugKotlin` SUCCEEDS.
    - Source assertion: a `ConfirmGuard` with title `"Delete printer?"` and `onConfirm` calling `container.profileStore.delete(profile.id)` exists (D-14 + D-12 in the writer).
    - Source assertion: the Appearance persist writes now target `container.profileStore.upsert(active.copy(themeBase=…))` (and fsChoice/themeDeltaArgb) when an active profile exists; the live `themeResolver.setBase/setFs/setDeltas` calls are retained for immediate retheme.
    - Source assertion: the persisted `themeDeltaArgb` is built via `delta.overrides.mapKeys { it.key.name }` (or an equivalent named helper over `TokenDelta.overrides`) — NOT a non-existent `delta.toArgbMap()` (grep shows no `toArgbMap`).
    - Source assertion: when no active profile, Appearance falls back to writing global `themePrefs` (the idle/new-profile-default path is preserved — `themePrefs` is NOT deleted).
  </acceptance_criteria>
  <done>Profile deletion is guarded and routes through profileStore.delete; Appearance edits the active profile's theme live + persisted (delta persisted from TokenDelta.overrides, no phantom toArgbMap), with the global themePrefs retained as the idle/new-profile default.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Settings text entry → ProfileStore | User-entered host/port/key (untrusted) crosses into the persisted profile blob |
| Delete action → irreversible profile+theme loss | A destructive write that drops a profile's config and theme |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-08 | Tampering | Malformed host/port entered in the add/edit form | mitigate | Client-side validation mirrors `ConnectionStore.sanitize` (reject blank host / port ∉ 1..65535) before `upsert`; the store `sanitize` is the second gate |
| T-14-09 | Denial of service / data loss | Accidental profile deletion (loses config + theme) | mitigate | Full-screen `ConfirmGuard` (D-14) red destructive confirm + explicit "Delete"/"Keep"; D-12 auto-pick prevents a dead no-active state |
| T-14-10 | Information disclosure | API key field echoed/logged from the form | mitigate | Reuse the existing `TokenTextField` + the redacting `ConnectionConfig`/`PersistedProfile.toString`; no new logging of the key |
| T-14-SC | Tampering | npm/pip/cargo installs | accept | No new external packages this phase |
</threat_model>

<verification>
- `:app:compileDebugKotlin` SUCCESS.
- Full `:app:testDebugUnitTest` GREEN at wave merge (no regression).
- Manual (deferred to plan 06 UAT): first-ever add routes into the Shell (D-11); editing the active accent fires NO recovery Splash (distinctUntilChanged, plan 02).
</verification>

<success_criteria>
- Connection section is a profile list (add/edit/delete) reusing the form 1:1 (D-13); first-ever add becomes active and routes into the Shell (D-11).
- Delete is behind the ConfirmGuard (D-14) → profileStore.delete (D-12 auto-pick).
- Appearance edits the active profile's theme (D-09) live + persisted (delta persisted from TokenDelta.overrides); global themePrefs retained as default.
</success_criteria>

<output>
Create `.planning/phases/14-multi-printer-switching/14-04-SUMMARY.md` when done.
</output>
</content>
