---
phase: 14-multi-printer-switching
plan: 03
type: execute
wave: 2
depends_on: ["14-02"]
files_modified:
  - app/src/main/java/works/mees/dinghy/ui/webcam/WebcamPrefs.kt
  - app/src/main/java/works/mees/dinghy/ui/webcam/WebcamHolder.kt
  - app/src/main/java/works/mees/dinghy/ui/route/TopRoute.kt
autonomous: true
requirements: [MULTI-01]

must_haves:
  truths:
    - "The preferred-webcam pref is keyed on the active profile id (D-06), not host — two same-host profiles keep independent preferred cams"
    - "Dest.Devices exists in the route enum so the shell can host the new switcher screen"
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/ui/webcam/WebcamPrefs.kt"
      provides: "preferredCam(profileId)/setPreferredCam(profileId,...)/preferredCamKey(profileId) keyed on profile id"
      contains: "preferred_cam_"
    - path: "app/src/main/java/works/mees/dinghy/ui/webcam/WebcamHolder.kt"
      provides: "holder threads the active profile id into WebcamPrefs (caller re-key)"
      contains: "profileId"
    - path: "app/src/main/java/works/mees/dinghy/ui/route/TopRoute.kt"
      provides: "Dest.Devices enum value"
      contains: "Devices"
  key_links:
    - from: "WebcamHolder"
      to: "WebcamPrefs.preferredCam"
      via: "active profile id (off container.activeProfile.map { it?.id })"
      pattern: "profileId"
---

<objective>
Two mechanical, disjoint changes that other Wave-3 plans depend on: re-key the per-printer preferred
webcam pref from host to profile id (D-06) at BOTH touch points (`WebcamPrefs` signatures AND its
`WebcamHolder` caller), and add `Devices` to the `Dest` route enum. No migration (D-07) — old
`preferred_cam_<host>` entries are simply orphaned.

Purpose: Close the D-06 re-key (two-touch-point) and register the new route so plan 05 can host it.
Output: re-keyed `WebcamPrefs` + `WebcamHolder`, one new enum value.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-multi-printer-switching/14-CONTEXT.md
@.planning/phases/14-multi-printer-switching/14-PATTERNS.md
@.planning/phases/14-multi-printer-switching/14-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-key WebcamPrefs + WebcamHolder from host to profile id (D-06)</name>
  <files>app/src/main/java/works/mees/dinghy/ui/webcam/WebcamPrefs.kt, app/src/main/java/works/mees/dinghy/ui/webcam/WebcamHolder.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/webcam/WebcamPrefs.kt (FULL — the dynamic-key idiom at lines 45-63: `preferredCam(host)` (48-52), `setPreferredCam(host, camId)` (54-58), `preferredCamKey(host)` (62-63). The fail-safe `.catch { IOException → emptyPreferences() }` read stays)
    - app/src/main/java/works/mees/dinghy/ui/webcam/WebcamHolder.kt (lines 240-260 — `webcamPrefs.preferredCam(host).first()` at line 248; line ~182 `setPreferredCam(host, camId)`; the `webcamBitmapHolder(...)` factory at lines 389-401 where `host = cfg.host` is threaded in)
    - app/src/main/java/works/mees/dinghy/di/AppContainer.kt (Task plan-02 output — `activeProfile.map { it?.id }` is the profile-id source)
    - app/src/main/java/works/mees/dinghy/ui/shell/AppShell.kt (line 186 — how `cfg` is collected; the active profile id will be threaded the SAME way the host currently is, in plan 05's AppShell wiring; here just change the holder's INPUT parameter from host to profileId)
    - .planning/phases/14-multi-printer-switching/14-PATTERNS.md (§ WebcamPrefs.kt + § WebcamHolder.kt — the mechanical re-key + Pitfall 5)
    - .planning/phases/14-multi-printer-switching/14-RESEARCH.md (§ Pattern 4 + Pitfall 5)
  </read_first>
  <action>
In `WebcamPrefs.kt`: rename the parameter `host` → `profileId` across all three members and the key prefix: `fun preferredCam(profileId: String): Flow<String?>`, `suspend fun setPreferredCam(profileId: String, camId: String)`, `fun preferredCamKey(profileId: String) = stringPreferencesKey("preferred_cam_$profileId")`. This is a pure parameter/prefix rename — the `.catch { IOException → emptyPreferences() }` read and the `dataStore.edit` write are unchanged. Update the KDoc note (lines 27-28) to say the key derives from the active PROFILE ID (D-06), not the host. D-07: no migration — old `preferred_cam_<host>` entries orphaned (mention in KDoc).

In `WebcamHolder.kt`: thread the active **profile id** instead of `cfg.host`. Change the holder/`webcamBitmapHolder` input from `host: String` to `profileId: String` (the parameter the caller supplies), and update the two pref call sites (`webcamPrefs.preferredCam(profileId).first()` ~line 248, `webcamPrefs.setPreferredCam(profileId, camId)` ~line 182) to pass `profileId`. At the `webcamBitmapHolder(...)` factory (lines 389-401) replace `host = cfg.host` with `profileId = <the supplied profile id>`. The actual collection of `activeProfile.map { it?.id }` into the holder's input happens in plan 05's AppShell wiring; THIS task changes the holder's contract to accept a profile id and stop reading `cfg.host` for the pref key. If a profile id is not yet threaded by the caller at compile time, accept it as a constructor/factory parameter so plan 05 supplies it (do NOT fall back to host — Pitfall 5).
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:compileDebugKotlin --no-daemon" 2>&1 | tr -d '\r'</automated>
  </verify>
  <acceptance_criteria>
    - `:app:compileDebugKotlin` SUCCEEDS.
    - Source assertion: `WebcamPrefs.preferredCamKey` builds `"preferred_cam_$profileId"` (grep shows `profileId`, no `host` parameter remains in the three members).
    - Source assertion: `WebcamHolder` no longer passes `cfg.host` to a pref call; it threads a `profileId` parameter (grep `WebcamHolder.kt` for `preferredCam(` shows a `profileId` argument).
    - No data-migration code added (D-07): no read of `preferred_cam_<host>` to copy forward.
  </acceptance_criteria>
  <done>WebcamPrefs and its WebcamHolder caller both key the preferred-cam pref on the profile id; two same-host profiles stay distinct; old host-keyed entries are orphaned.</done>
</task>

<task type="auto">
  <name>Task 2: Add Dest.Devices to the route enum</name>
  <files>app/src/main/java/works/mees/dinghy/ui/route/TopRoute.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/route/TopRoute.kt (FULL — the `Dest` enum at line 31; the pure `derive()` at lines 57-62 which is UNCHANGED — `!cfgPresent → Connect` already covers the D-11 empty state once hasConfig is "has active profile")
    - .planning/phases/14-multi-printer-switching/14-PATTERNS.md (§ TopRoute.kt — one-token addition)
  </read_first>
  <action>
Add `Devices` to the `Dest` enum (line 31), placed before `Settings` to mirror the other panel
destinations: `enum class Dest { PrintStatus, Temperature, Move, Extrude, Files, Macros, Console, Calibration, Webcam, Spool, Devices, Settings }`. Do NOT touch `derive()` — its `!cfgPresent → Connect`
arm already handles the D-11 empty state (hasConfig became "has active profile" in plan 02). This is a
single-token change.
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:compileDebugKotlin --no-daemon" 2>&1 | tr -d '\r'</automated>
  </verify>
  <acceptance_criteria>
    - `:app:compileDebugKotlin` SUCCEEDS.
    - Source assertion: `Dest` enum contains `Devices` (grep `enum class Dest` line includes `Devices`).
    - `derive()` body is unchanged (grep confirms no edit to lines 57-62).
  </acceptance_criteria>
  <done>Dest.Devices exists; derive() is untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WebcamPrefs DataStore key namespace | Per-profile preferred-cam keys must not collide across same-host profiles |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-07 | Tampering (state confusion) | Webcam pref keyed on host → two same-host profiles cross-read each other's preferred cam | mitigate | Re-key on the stable profile UUID (D-05/D-06) at BOTH WebcamPrefs and its holder caller (Pitfall 5) |
| T-14-SC | Tampering | npm/pip/cargo installs | accept | No new external packages this phase |
</threat_model>

<verification>
- `:app:compileDebugKotlin` SUCCESS.
- Existing webcam unit tests (if any reference the pref key) still GREEN after the rename: full `:app:testDebugUnitTest` at wave merge.
</verification>

<success_criteria>
- Preferred-cam pref keyed on profile id at both touch points (D-06); no migration (D-07).
- `Dest.Devices` registered; `derive()` untouched.
</success_criteria>

<output>
Create `.planning/phases/14-multi-printer-switching/14-03-SUMMARY.md` when done.
</output>
