---
phase: 14-multi-printer-switching
plan: 03
type: execute
wave: 2
depends_on: ["14-02"]
files_modified:
  - app/src/main/java/works/mees/dinghy/ui/webcam/WebcamPrefs.kt
  - app/src/main/java/works/mees/dinghy/ui/webcam/WebcamHolder.kt
  - app/src/main/java/works/mees/dinghy/ui/shell/AppShell.kt
  - app/src/main/java/works/mees/dinghy/ui/route/TopRoute.kt
autonomous: true
requirements: [MULTI-01]

must_haves:
  truths:
    - "The preferred-webcam pref is keyed on the active profile id (D-06), not host — two same-host profiles keep independent preferred cams"
    - "The webcam re-key lands ATOMICALLY in this one plan/wave: WebcamPrefs, WebcamHolder, AND the AppShell caller all flip from host to profile id together, so wave 2 ends compile-clean and correctly wired (no broken intermediate state for wave 3)"
    - "Dest.Devices exists in the route enum so the shell can host the new switcher screen"
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/ui/webcam/WebcamPrefs.kt"
      provides: "preferredCam(profileId)/setPreferredCam(profileId,...)/preferredCamKey(profileId) keyed on profile id"
      contains: "preferred_cam_"
    - path: "app/src/main/java/works/mees/dinghy/ui/webcam/WebcamHolder.kt"
      provides: "holder + webcamBitmapHolder factory thread a profileId (re-key) instead of cfg.host"
      contains: "profileId"
    - path: "app/src/main/java/works/mees/dinghy/ui/shell/AppShell.kt"
      provides: "the webcam holder caller threads the active profile id (off container.activeProfile.map { it?.id }) into webcamBitmapHolder, replacing cfg.host"
      contains: "activeProfile"
    - path: "app/src/main/java/works/mees/dinghy/ui/route/TopRoute.kt"
      provides: "Dest.Devices enum value"
      contains: "Devices"
  key_links:
    - from: "AppShell webcam holder build"
      to: "WebcamHolder/webcamBitmapHolder profileId"
      via: "active profile id (off container.activeProfile.map { it?.id })"
      pattern: "profileId"
---

<objective>
Two disjoint changes other Wave-3 plans depend on, both landing ATOMICALLY in this wave so wave 2 ends
compile-clean: (1) re-key the per-printer preferred webcam pref from host to profile id (D-06) at ALL
THREE production touch points — `WebcamPrefs` signatures, its `WebcamHolder`/`webcamBitmapHolder` caller,
AND the `AppShell` build site that currently passes `cfg.host` — threading the active profile id off
`container.activeProfile.map { it?.id }`; and (2) add `Devices` to the `Dest` route enum. No migration
(D-07) — old `preferred_cam_<host>` entries are simply orphaned.

Purpose: Close the D-06 re-key END-TO-END in one wave (no half-wired intermediate state for wave 3) and
register the new route so plan 05 can host it. Plan 05 keeps ONLY the non-webcam shell wiring.
Output: re-keyed `WebcamPrefs` + `WebcamHolder` + `AppShell` webcam-build site, one new enum value.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-multi-printer-switching/14-CONTEXT.md
@.planning/phases/14-multi-printer-switching/14-PATTERNS.md
@.planning/phases/14-multi-printer-switching/14-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-key WebcamPrefs + WebcamHolder + AppShell webcam build from host to profile id (D-06) — all three touch points in this wave</name>
  <files>app/src/main/java/works/mees/dinghy/ui/webcam/WebcamPrefs.kt, app/src/main/java/works/mees/dinghy/ui/webcam/WebcamHolder.kt, app/src/main/java/works/mees/dinghy/ui/shell/AppShell.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/webcam/WebcamPrefs.kt (FULL — the dynamic-key idiom: `preferredCam(host)` (lines 48-52), `setPreferredCam(host, camId)` (54-59), `preferredCamKey(host)` (62-63). The fail-safe `.catch { IOException → emptyPreferences() }` read stays; the KDoc per-host note is at lines 26-29)
    - app/src/main/java/works/mees/dinghy/ui/webcam/WebcamHolder.kt (the `host: String` ctor param at line 83 + its KDoc at line 70; the pref call sites `webcamPrefs.setPreferredCam(host, camId)` at line 182 and `webcamPrefs.preferredCam(host).first()` at line 248; the `webcamBitmapHolder(...)` factory at lines 389-406 where `host = cfg.host` is threaded in at line 401)
    - app/src/main/java/works/mees/dinghy/ui/shell/AppShell.kt (line 186 `val cfg by container.connectionStore.config.collectAsStateWithLifecycle(...)`; line 187 `val activeCfg = cfg ?: ConnectionConfig(host = "")`; line 199 `remember(store, activeCfg.host, viewWidthPx, viewHeightPx)`; line 200-205 the `webcamBitmapHolder(... cfg = activeCfg ...)` call — this is the THIRD touch point that must thread the profile id)
    - app/src/main/java/works/mees/dinghy/di/AppContainer.kt (plan-02 output — `activeProfile` exists; `container.activeProfile.map { it?.id }` is the profile-id source)
    - .planning/phases/14-multi-printer-switching/14-PATTERNS.md (§ WebcamPrefs.kt + § WebcamHolder.kt + § AppShell.kt — the mechanical re-key + Pitfall 5)
    - .planning/phases/14-multi-printer-switching/14-RESEARCH.md (§ Pattern 4 + Pitfall 5)
  </read_first>
  <action>
This re-key MUST land fully across ALL THREE touch points in THIS task so wave 2 ends compile-clean and
correctly wired (do NOT leave the AppShell caller still passing `cfg.host` — that is the broken
intermediate state this plan exists to avoid). Plan 05 (wave 3) will NOT touch the webcam holder.

(1) `WebcamPrefs.kt`: rename the parameter `host` → `profileId` across all three members and the key prefix: `fun preferredCam(profileId: String): Flow<String?>`, `suspend fun setPreferredCam(profileId: String, camId: String)`, `fun preferredCamKey(profileId: String) = stringPreferencesKey("preferred_cam_$profileId")`. This is a pure parameter/prefix rename — the `.catch { IOException → emptyPreferences() }` read and the `dataStore.edit` write are unchanged. Update the KDoc note (lines 26-29) to say the key derives from the active PROFILE ID (D-06), not the host. D-07: no migration — old `preferred_cam_<host>` entries orphaned (mention in KDoc).

(2) `WebcamHolder.kt`: thread the active **profile id** instead of `cfg.host`. Change the holder's ctor param `private val host: String` (line 83) to `private val profileId: String` and its KDoc (line 70). Update the two pref call sites — `webcamPrefs.setPreferredCam(profileId, camId)` (~line 182) and `webcamPrefs.preferredCam(profileId).first()` (~line 248) — to pass `profileId`. At the `webcamBitmapHolder(...)` factory (lines 389-406): add a `profileId: String` parameter to the factory signature, and replace `host = cfg.host` (line 401) with `profileId = profileId`. The factory still takes `cfg: ConnectionConfig` (the loopback-rewrite host for the feed lives in `bitmapFeed(cfg, …)` — that stays on `cfg`); ONLY the per-printer pref KEY moves to profileId. Do NOT fall back to host for the key (Pitfall 5).

(3) `AppShell.kt`: thread the active profile id into the holder build. Add a collect near the existing webcam collects (~lines 180-187): `val activeProfileId by container.activeProfile.map { it?.id }.collectAsStateWithLifecycle(initialValue = null)`. At the `webcamBitmapHolder(...)` call (lines 200-205) pass `profileId = activeProfileId ?: ""` (empty string when no active profile — harmless, the holder just reads/writes an empty-suffixed key it will rarely hit; the webcam surface is only reachable with an active printer anyway). Add `activeProfileId` to the `remember(...)` keys at line 199 (alongside or replacing `activeCfg.host`) so the holder rebuilds when the active profile changes. `cfg`/`activeCfg` STAY as-is for the feed (`cfg = activeCfg`) — only the pref key moves to profile id. This completes the D-06 re-key end-to-end inside this wave.

If any existing webcam unit/host test constructs `WebcamHolder(...)` or `webcamBitmapHolder(...)` with a named `host =` argument, update it to `profileId =` in the same change so the test sourceset still compiles (grep the test tree for `webcamBitmapHolder(`/`WebcamHolder(` + `host =`).
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:compileDebugKotlin :app:compileDebugUnitTestKotlin --no-daemon" 2>&1 | tr -d '\r'</automated>
  </verify>
  <acceptance_criteria>
    - `:app:compileDebugKotlin` + `:app:compileDebugUnitTestKotlin` SUCCEED (the whole app + test sourceset compiles — no half-wired host/profileId mismatch).
    - Source assertion: `WebcamPrefs.preferredCamKey` builds `"preferred_cam_$profileId"` (grep shows `profileId`; no `host` parameter remains in the three members).
    - Source assertion: `WebcamHolder` ctor takes `profileId: String` (not `host`); both pref call sites pass `profileId`; the `webcamBitmapHolder(...)` factory takes a `profileId` param and no longer passes `host = cfg.host`.
    - Source assertion: `AppShell.kt` collects `container.activeProfile.map { it?.id }` and passes `profileId = activeProfileId ?: ""` into `webcamBitmapHolder(...)`; the holder `remember(...)` keys on `activeProfileId`.
    - Any webcam test that used a named `host =` arg now uses `profileId =` (no stale compile reference).
    - No data-migration code added (D-07): no read of `preferred_cam_<host>` to copy forward.
  </acceptance_criteria>
  <done>WebcamPrefs, WebcamHolder, and the AppShell build site all key the preferred-cam pref on the active profile id; two same-host profiles stay distinct; old host-keyed entries are orphaned; wave 2 ends compile-clean with the re-key fully wired (plan 05 does NOT touch the webcam holder).</done>
</task>

<task type="auto">
  <name>Task 2: Add Dest.Devices to the route enum</name>
  <files>app/src/main/java/works/mees/dinghy/ui/route/TopRoute.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/route/TopRoute.kt (FULL — the `Dest` enum at line 31 (NOTE: `Dest` is defined INSIDE TopRoute.kt — there is NO standalone Dest.kt); the pure `derive()` at lines 57-62 which is UNCHANGED — `!cfgPresent → Connect` already covers the D-11 empty state once hasConfig is "has active profile". The Status dest is `Dest.PrintStatus` — line 31/61)
    - .planning/phases/14-multi-printer-switching/14-PATTERNS.md (§ TopRoute.kt — one-token addition)
  </read_first>
  <action>
Add `Devices` to the `Dest` enum (line 31 of `TopRoute.kt` — there is NO separate Dest.kt file), placed
before `Settings` to mirror the other panel destinations: `enum class Dest { PrintStatus, Temperature,
Move, Extrude, Files, Macros, Console, Calibration, Webcam, Spool, Devices, Settings }`. Do NOT touch
`derive()` — its `!cfgPresent → Connect` arm already handles the D-11 empty state (hasConfig became "has
active profile" in plan 02). This is a single-token change.
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:compileDebugKotlin --no-daemon" 2>&1 | tr -d '\r'</automated>
  </verify>
  <acceptance_criteria>
    - `:app:compileDebugKotlin` SUCCEEDS.
    - Source assertion: `Dest` enum in `TopRoute.kt` contains `Devices` (grep `enum class Dest` line includes `Devices`).
    - `derive()` body is unchanged (grep confirms no edit to lines 57-62).
  </acceptance_criteria>
  <done>Dest.Devices exists in TopRoute.kt; derive() is untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WebcamPrefs DataStore key namespace | Per-profile preferred-cam keys must not collide across same-host profiles |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-07 | Tampering (state confusion) | Webcam pref keyed on host → two same-host profiles cross-read each other's preferred cam | mitigate | Re-key on the stable profile UUID (D-05/D-06) at ALL THREE touch points — WebcamPrefs, WebcamHolder/factory, AND the AppShell caller — atomically in this wave (Pitfall 5) |
| T-14-SC | Tampering | npm/pip/cargo installs | accept | No new external packages this phase |
</threat_model>

<verification>
- `:app:compileDebugKotlin` + `:app:compileDebugUnitTestKotlin` SUCCESS.
- Existing webcam unit tests (if any reference the pref key) still GREEN after the rename: full `:app:testDebugUnitTest` at wave merge.
</verification>

<success_criteria>
- Preferred-cam pref keyed on profile id at ALL THREE touch points (D-06), wired end-to-end in this wave; no migration (D-07).
- `Dest.Devices` registered; `derive()` untouched.
- Wave 2 ends compile-clean (no broken host/profileId intermediate state carried into wave 3).
</success_criteria>

<output>
Create `.planning/phases/14-multi-printer-switching/14-03-SUMMARY.md` when done.
</output>
</content>
