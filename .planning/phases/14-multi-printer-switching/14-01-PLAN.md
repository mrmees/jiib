---
phase: 14-multi-printer-switching
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - app/src/main/java/works/mees/dinghy/config/Profile.kt
  - app/src/main/java/works/mees/dinghy/config/ProfileStore.kt
  - app/src/test/java/works/mees/dinghy/config/ProfileStoreTest.kt
  - app/src/test/java/works/mees/dinghy/di/ActiveConfigDerivationTest.kt
  - app/src/test/java/works/mees/dinghy/theme/ProfileThemeSeedTest.kt
  - app/src/androidTest/java/works/mees/dinghy/service/ProfileSurvivesRestartTest.kt
autonomous: true
requirements: [MULTI-01]

must_haves:
  truths:
    - "ProfileStore.sanitize drops malformed profiles, recovers from a corrupt blob, and an empty store yields an empty list"
    - "upsert into a store with NO active id auto-selects the new profile as active (D-11) — so the first profile added is immediately active and the app routes into the Shell, not stranded on Connect"
    - "delete(id) auto-picks another remaining profile as active (D-12); deleting the last clears active-id (D-11)"
    - "PersistedProfile.toString() redacts apiKey to *** (V7); the profile blob is never logged"
    - "The rebind seam (runConfigLoop cancel-before-rebuild) remains the sole data path — no parallel rebind code is introduced"
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/config/Profile.kt"
      provides: "PersistedProfile @Serializable value (id/name/host/port/apiKey + theme primitives), runtime Profile, toConnectionConfig(), toThemeResolved(), redacting toString"
      contains: "data class PersistedProfile"
    - path: "app/src/main/java/works/mees/dinghy/config/ProfileStore.kt"
      provides: "DataStore-backed profile set + active-id store, fail-safe read flows, pure sanitize, upsert (auto-selects active when none) / delete / setActive writers"
      contains: "fun sanitize"
    - path: "app/src/test/java/works/mees/dinghy/config/ProfileStoreTest.kt"
      provides: "Host unit tests for sanitize + upsert-into-empty-store auto-active (D-11) + delete auto-pick (D-12)"
    - path: "app/src/test/java/works/mees/dinghy/di/ActiveConfigDerivationTest.kt"
      provides: "RED scaffold for the AppContainer activeConfig distinctUntilChanged behavior (turned GREEN in plan 02)"
    - path: "app/src/test/java/works/mees/dinghy/theme/ProfileThemeSeedTest.kt"
      provides: "RED scaffold for active-profile theme triple derivation (turned GREEN in plan 02)"
    - path: "app/src/androidTest/java/works/mees/dinghy/service/ProfileSurvivesRestartTest.kt"
      provides: "Instrumented scaffold for active-id survival across process death (filled in plan 06)"
  key_links:
    - from: "ProfileStore.upsert"
      to: "KEY_ACTIVE_ID set when currently null/empty"
      via: "first-add becomes active (D-11), mirroring the D-12 auto-pick-in-the-writer pattern"
      pattern: "upsert"
    - from: "ProfileStore.delete"
      to: "KEY_ACTIVE_ID rewrite"
      via: "auto-pick first remaining or clear (D-12)"
      pattern: "delete"
    - from: "PersistedProfile.toConnectionConfig"
      to: "ConnectionConfig(host, port, apiKey)"
      via: "projection used for distinctUntilChanged equality"
      pattern: "toConnectionConfig"
---

<objective>
Build the Phase-14 data foundation: the `PersistedProfile`/`Profile` model and the `ProfileStore`
(DataStore-backed profile set + active-id), copying `ConnectionStore`'s shape verbatim, plus the four
Wave-0 RED test scaffolds the rest of the phase turns green. This is a generalize-existing-patterns
plan — `ProfileStore` is `ConnectionStore` with a serialized `List<Profile>` blob instead of three flat
keys, and the model is `ConnectionConfig` plus an id, optional name, and persisted theme primitives.

Purpose: Establish the profile persistence + identity model (D-05) and the validation/redaction
boundary before any derivation, UI, or rewiring depends on it.
Output: `Profile.kt`, `ProfileStore.kt`, three host test files, one instrumented test scaffold.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-multi-printer-switching/14-CONTEXT.md
@.planning/phases/14-multi-printer-switching/14-RESEARCH.md
@.planning/phases/14-multi-printer-switching/14-PATTERNS.md
@.planning/phases/14-multi-printer-switching/14-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Profile model + ProfileStore (mirror ConnectionStore/ConnectionConfig verbatim)</name>
  <files>app/src/main/java/works/mees/dinghy/config/Profile.kt, app/src/main/java/works/mees/dinghy/config/ProfileStore.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/config/ConnectionConfig.kt (FULL — the data class + redacting toString at lines 14-28 to replicate; the httpBase/wsUrl projections are NOT needed on Profile, only toConnectionConfig())
    - app/src/main/java/works/mees/dinghy/config/ConnectionStore.kt (FULL — the injected DataStore, fail-safe `.catch { IOException → emptyPreferences() }` read flow at lines 38-44, suspend writers at lines 47-62, pure sanitize companion at lines 79-88, PORT_RANGE = 1..65535)
    - app/src/main/java/works/mees/dinghy/theme/ThemePrefs.kt (FULL — the PERSISTED theme primitive shape: KEY_BASE/KEY_FS (lines 85-86), deltaArgbKey (line 88), Resolved data class (lines 78-83), DEFAULT (line 91), sanitize (lines 103-124); copy this shape into the profile's theme fields and toThemeResolved())
    - .planning/phases/14-multi-printer-switching/14-PATTERNS.md (§ ProfileStore.kt + § Profile.kt/PersistedProfile sections — the exact change-vs-analog list)
    - .planning/phases/14-multi-printer-switching/14-RESEARCH.md (§ Pattern 1 + Pattern 2 + Security Domain — sanitize parity, D-12-in-writer, redaction)
  </read_first>
  <behavior>
    - sanitize(null) and sanitize("") → emptyList() (no profiles)
    - sanitize of a valid JSON blob of two profiles → both, in order
    - sanitize drops a profile with blank host or out-of-range port (e.g. 0 or 70000), keeps the valid sibling
    - sanitize of a non-JSON / truncated blob → emptyList() (runCatching fail-safe, never throws)
    - upsert into an EMPTY store (active-id currently null) → the new profile is persisted AND its id becomes the active id (D-11 first-add-is-active)
    - upsert into a store that ALREADY has an active id → the active id is left UNCHANGED (a second add does not steal active; the user switches via Devices)
    - upsert that EDITS the already-active profile (same id) → active id unchanged (still that id)
    - PersistedProfile.toString() with a non-null apiKey contains "apiKey=***" and never the raw key
    - PersistedProfile.toString() with null apiKey contains "apiKey=null"
    - toConnectionConfig() yields a ConnectionConfig(host, port, apiKey) — theme/name/id absent (so two profiles differing only in name/theme produce EQUAL ConnectionConfig)
  </behavior>
  <action>
Create `config/Profile.kt` with:
- `@Serializable data class PersistedProfile(val id: String, val name: String? = null, val host: String, val port: Int = 7125, val apiKey: String? = null, val themeBase: String = "Dark", val fsChoice: String = "M", val themeDeltaArgb: Map<String, Long> = emptyMap())`. Mark `@Serializable`. Override `toString()` masking `apiKey` to `***` exactly as `ConnectionConfig.toString()` does (V7 redaction — this is NEW code so it MUST carry the same masking).
- `fun toConnectionConfig(): ConnectionConfig` projecting host/port/apiKey ONLY (this is the value `distinctUntilChanged` keys on in plan 02 — name/theme MUST NOT participate).
- `fun displayName(): String` returning `name ?: host` (D-10).
- `fun toThemeResolved(): ThemePrefs.Resolved` reusing `ThemePrefs.sanitize` parity — feed `themeBase`/`fsChoice`/`themeDeltaArgb` through the same enum-fallback + delta path so corrupt theme primitives fail safe to defaults (do NOT throw, do NOT bake a `ThemeTokens`).
- A `companion object` with `fun newId(): String = java.util.UUID.randomUUID().toString()` (D-05).

Create `config/ProfileStore.kt` copying `ConnectionStore` shape:
- ctor `class ProfileStore(private val dataStore: DataStore<Preferences>)`.
- `val profiles: Flow<List<Profile>>` = `dataStore.data.catch { IOException → emptyPreferences() }.map { sanitize(it[KEY_PROFILES]) }`.
- `val activeId: Flow<String?>` = `dataStore.data.catch { … }.map { it[KEY_ACTIVE_ID] }`.
- `suspend fun upsert(profile: Profile)`: do this in ONE `dataStore.edit { prefs -> … }` block so the blob write and the auto-active write are atomic. Read the current blob from `prefs[KEY_PROFILES]`, decode to list, replace-by-id or append, encode with `Json.encodeToString`, write `prefs[KEY_PROFILES] = blob`. THEN, if `prefs[KEY_ACTIVE_ID]` is null or blank (no profile is currently active), set `prefs[KEY_ACTIVE_ID] = profile.id` — so the FIRST profile added auto-becomes active (D-11; this is what lets `hasConfig` flip true and the root controller route into the Shell instead of stranding on Connect → Settings). When an active id already exists, do NOT touch it (a later add never steals active; switching is the user's Devices tap). This mirrors the D-12 auto-pick-in-the-writer pattern: the writer is the single source of truth for active-id, never a read derivation.
- `suspend fun delete(id: String)`: read list, remove `id`; if `id == it[KEY_ACTIVE_ID]` then set `KEY_ACTIVE_ID` to the first remaining profile's id, or `remove(KEY_ACTIVE_ID)` when none remain (D-12 auto-pick lives HERE, in the writer — NOT in any read derivation).
- `suspend fun setActive(id: String?)`: `edit { if (id == null) it.remove(KEY_ACTIVE_ID) else it[KEY_ACTIVE_ID] = id }`.
- `companion object`: `KEY_PROFILES = stringPreferencesKey("profiles")`, `KEY_ACTIVE_ID = stringPreferencesKey("active_id")`, `PORT_RANGE = 1..65535`, and `fun sanitize(rawBlob: String?): List<Profile>` — PURE, host-testable, no IO: `runCatching { Json.decodeFromString<List<PersistedProfile>>(rawBlob ?: return emptyList()) }.getOrDefault(emptyList())` then drop entries with blank host or port outside PORT_RANGE, mapping survivors to runtime `Profile`. Use a private/internal `Json { ignoreUnknownKeys = true }` instance.

Do NOT add any rebind/disconnect logic here — switching is purely `setActive` flipping the active-id (the service seam rebinds, plan 02 wires `activeConfig`). Do NOT delete `ConnectionStore` (D-07/RESEARCH Open-Q2 — leave it until phase verified).
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --tests works.mees.dinghy.config.ProfileStoreTest --no-daemon" 2>&1 | tr -d '\r'</automated>
  </verify>
  <acceptance_criteria>
    - `ProfileStoreTest` passes (sanitize + upsert-into-empty-store-sets-active + delete auto-pick behaviors above all GREEN).
    - Behavior assertion (the FIX-1 gate): `upsert` of a single profile into a store with no active id leaves that profile's id as the active id; a SECOND `upsert` does not change the active id. Asserted in `ProfileStoreTest` over a fake/in-memory `DataStore<Preferences>` (or by host-testing the active-id write path — see read_first for the ConnectionStore-style harness; if a true in-memory DataStore round-trip is impractical host-side, fall back to asserting the writer logic via a thin testable seam, but the "upsert into empty store sets it active" assertion MUST exist).
    - Source assertion: `Profile.kt` defines `PersistedProfile` with `@Serializable` and a `toString()` that masks `apiKey` to `***`.
    - Source assertion: `ProfileStore.upsert` writes `KEY_ACTIVE_ID = profile.id` ONLY when the current active id is null/blank (grep `upsert` body references `KEY_ACTIVE_ID`).
    - Source assertion: `ProfileStore.delete` rewrites or removes `KEY_ACTIVE_ID` (the D-12 auto-pick is in the writer; grep `delete` body references `KEY_ACTIVE_ID`).
    - Source assertion: `ProfileStore.sanitize` is a pure companion function taking `String?` and returning `List<Profile>`, wrapping the decode in `runCatching` (never throws).
    - No new file under `service/` or `di/` introduces a `publishSpine` or disconnect sequencer (grep the diff — zero new rebind paths).
  </acceptance_criteria>
  <done>Profile model + ProfileStore exist, mirror the ConnectionStore/ConnectionConfig/ThemePrefs shapes, redact the key, auto-select the first-added profile as active (D-11), keep D-12 in the delete writer, and ProfileStoreTest is GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: Wave-0 RED test scaffolds (compile day-one) for derivation, theme seed, survival</name>
  <files>app/src/test/java/works/mees/dinghy/di/ActiveConfigDerivationTest.kt, app/src/test/java/works/mees/dinghy/theme/ProfileThemeSeedTest.kt, app/src/androidTest/java/works/mees/dinghy/service/ProfileSurvivesRestartTest.kt</files>
  <read_first>
    - app/src/test/java/works/mees/dinghy/service/MoonrakerServiceTest.kt (the virtual-time `runTest` seam-test pattern — `kotlinx-coroutines-test`, how flows are driven; mirror its structure for ActiveConfigDerivationTest)
    - app/src/androidTest/java/works/mees/dinghy/service/ServiceSurvivesRotationTest.kt (FULL — the instrumented test analog: real DataStore file, real lifecycle; ProfileSurvivesRestartTest mirrors this)
    - app/src/test/java/works/mees/dinghy/theme/ThemePrefsFallbackTest.kt (if present — the pure-sanitize host test pattern; otherwise mirror ProfileStoreTest)
    - .planning/phases/14-multi-printer-switching/14-VALIDATION.md (§ Wave 0 Requirements + the per-task verification map — exact class names + commands)
    - MEMORY [[dinghy-wave0-red-scaffold-compile]] (Wave-0 RED stubs MUST compile day-one: typed fail()/assertion stubs, no refs to symbols that do not yet exist)
  </read_first>
  <action>
Create three test files that COMPILE day-one (the whole test sourceset compiles before the `--tests` filter runs; a broken scaffold bricks every per-wave run — see MEMORY). They may reference the NEW `Profile`/`ProfileStore`/`PersistedProfile` types from Task 1 (those exist now) but MUST NOT reference the not-yet-built `AppContainer.activeConfig`/`activeProfile`/rewired `seedTheme` (those land in plan 02).

`di/ActiveConfigDerivationTest.kt`: a `@Test` named e.g. `nameOrThemeEditDoesNotChurnConfig` that builds the derivation pipeline LOCALLY in the test from primitives that exist now — `combine(flowOf(profiles), flowOf(activeId))` → pick-by-id → `.map { it?.toConnectionConfig() }` → `.distinctUntilChanged()` — and asserts that emitting two `Profile`s differing ONLY in `name`/theme yields a single distinct `ConnectionConfig`. Because the real `AppContainer.activeConfig` does not exist yet, the test asserts the CONTRACT on the local pipeline using `Profile.toConnectionConfig()` (real, from Task 1). This makes the scaffold meaningfully RED-then-GREEN when plan 02 routes the same pipeline through the container — keep the pipeline shape identical to RESEARCH Pattern 2 so plan 02 can lift it. Use `runTest`.

`theme/ProfileThemeSeedTest.kt`: a `@Test` asserting `Profile.toThemeResolved()` (real, Task 1) maps a profile's `themeBase`/`fsChoice`/`themeDeltaArgb` to the correct `ThemePrefs.Resolved`, AND a corrupt-primitives profile (e.g. `themeBase = "Banana"`) fails safe to `ThemePrefs.DEFAULT.base`. This is fully exercisable now (no container dep).

`androidTest/.../ProfileSurvivesRestartTest.kt`: an instrumented scaffold mirroring `ServiceSurvivesRotationTest`. Provide the class + one `@Test fun activeIdSurvivesProcessDeath()` whose body is a typed `fail("filled in plan 06 — needs the real profiles.preferences_pb DataStore + cold-start path")` so it COMPILES and is explicitly pending. Do NOT reference unbuilt wiring. (Plan 06 fills the real persistence assertions on-device — host DataStore is not persistence-faithful, VALIDATION §mock-vs-reality.)
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:compileDebugUnitTestKotlin :app:compileDebugAndroidTestKotlin --no-daemon" 2>&1 | tr -d '\r'</automated>
  </verify>
  <acceptance_criteria>
    - Both the unit and androidTest Kotlin sourcesets COMPILE (the `compile*TestKotlin` tasks succeed) — proves the RED scaffolds are day-one compilable per MEMORY.
    - `ProfileThemeSeedTest` and `ActiveConfigDerivationTest` PASS now (they exercise only Task-1 real symbols): `gw.bat :app:testDebugUnitTest --tests works.mees.dinghy.theme.ProfileThemeSeedTest --tests works.mees.dinghy.di.ActiveConfigDerivationTest` GREEN.
    - Source assertion: `ProfileSurvivesRestartTest` contains exactly one `@Test` whose body is a typed `fail(...)` pending stub, no reference to unbuilt `AppContainer` members.
    - No test references `container.activeConfig`/`container.activeProfile`/rewired `seedTheme` (those symbols do not exist until plan 02).
  </acceptance_criteria>
  <done>All four Wave-0 test files exist and compile; the two host derivation/theme tests pass against Task-1 symbols; the instrumented survival test is a compile-clean pending stub for plan 06.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DataStore file `profiles.preferences_pb` → app | On-disk profile blob (incl. optional API keys) read back into memory; corrupt/oversized blob is untrusted input |
| Profile → log/notification/crash sinks | A profile carrying an API key must never serialize the key to any sink |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Information disclosure | `PersistedProfile.toString()` / logged blob | mitigate | Redacting `toString()` masks `apiKey` to `***` (mirror `ConnectionConfig.kt:26`); never log the blob — tested in ProfileStoreTest |
| T-14-02 | Denial of service | Corrupt/oversized profile blob on read | mitigate | `.catch { IOException → emptyPreferences() }` + pure `sanitize` wraps decode in `runCatching` → empty list → Connect prompt, never a crash/black screen — tested (corrupt-blob case) |
| T-14-03 | Tampering | Malformed host/port in a decoded profile | mitigate | `sanitize` drops entries with blank host or port ∉ 1..65535 BEFORE they can become an active `ConnectionConfig` (ConnectionStore.sanitize parity) — tested |
| T-14-SC | Tampering | npm/pip/cargo installs | accept | No new external packages introduced this phase (Android/Gradle, all deps already declared — RESEARCH §Standard Stack "no install step") |
</threat_model>

<verification>
- `gw.bat :app:testDebugUnitTest --tests works.mees.dinghy.config.ProfileStoreTest` GREEN (sanitize + upsert-empty-store-sets-active + D-12 delete).
- `gw.bat :app:compileDebugUnitTestKotlin :app:compileDebugAndroidTestKotlin` SUCCESS (RED scaffolds compile day-one).
- `ProfileThemeSeedTest` + `ActiveConfigDerivationTest` GREEN against Task-1 symbols.
</verification>

<success_criteria>
- Profile model + ProfileStore exist, copy the ConnectionStore/ConnectionConfig/ThemePrefs shapes, redact the API key, auto-select the first-added profile as active (D-11), and keep D-12 auto-pick in the delete writer.
- Four Wave-0 test files exist and compile; corrupt-blob and malformed-entry cases are covered.
- No second rebind path is introduced; `ConnectionStore` is left in the tree (D-07).
</success_criteria>

<output>
Create `.planning/phases/14-multi-printer-switching/14-01-SUMMARY.md` when done.
</output>
</content>
</invoke>
