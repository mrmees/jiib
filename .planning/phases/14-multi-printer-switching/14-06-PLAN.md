---
phase: 14-multi-printer-switching
plan: 06
type: execute
wave: 4
depends_on: ["14-05"]
files_modified:
  - app/src/androidTest/java/works/mees/dinghy/service/ProfileSurvivesRestartTest.kt
  - .planning/phases/14-multi-printer-switching/14-UAT.md
autonomous: false
requirements: [MULTI-01]

must_haves:
  truths:
    - "The persisted active-profile id survives process death: re-read on a cold start lands on the right printer (instrumented, on-device — host DataStore is NOT persistence-faithful)"
    - "Live two-printer switch works on real hardware: E5+ ↔ E3, each lands on its own Status and a control action works, with no re-entry of host/port/key (success criterion 4)"
    - "Editing the active printer's accent fires NO recovery Splash (distinctUntilChanged suppression confirmed on-device)"
  artifacts:
    - path: "app/src/androidTest/java/works/mees/dinghy/service/ProfileSurvivesRestartTest.kt"
      provides: "instrumented survival-across-process-death test (real profiles.preferences_pb, real cold start)"
      contains: "activeId"
    - path: ".planning/phases/14-multi-printer-switching/14-UAT.md"
      provides: "the manual live two-printer UAT script + results record"
  key_links:
    - from: "ProfileSurvivesRestartTest"
      to: "real profiles.preferences_pb DataStore"
      via: "write active-id, simulate cold re-read, assert lands on right printer"
      pattern: "active_id"
---

<objective>
Close the two gates a fake cannot prove: the instrumented survival-across-process-death test (host
DataStore is not persistence-faithful) and the mandatory live two-printer hands-on UAT (success
criterion 4 — capabilities are server-derived, so no-stale-state only emerges against live Moonraker).
Fill in the plan-01 `ProfileSurvivesRestartTest` stub against the real DataStore, then run the
blocking on-device UAT on `flox` + the live Ender 5 Plus and Ender 3 Pro.

Purpose: Prove the phase end-to-end where the documented mock-vs-reality history says it must be proven.
Output: a real instrumented survival test + a `14-UAT.md` results record.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-multi-printer-switching/14-CONTEXT.md
@.planning/phases/14-multi-printer-switching/14-VALIDATION.md
@.planning/phases/14-multi-printer-switching/14-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fill in the instrumented ProfileSurvivesRestartTest (real DataStore, cold re-read)</name>
  <files>app/src/androidTest/java/works/mees/dinghy/service/ProfileSurvivesRestartTest.kt</files>
  <read_first>
    - app/src/androidTest/java/works/mees/dinghy/service/ServiceSurvivesRotationTest.kt (FULL — the instrumented harness analog: real Android context, real DataStore file via PreferenceDataStoreFactory, lifecycle handling; mirror its structure)
    - app/src/androidTest/java/works/mees/dinghy/service/ProfileSurvivesRestartTest.kt (the plan-01 pending stub to replace)
    - app/src/main/java/works/mees/dinghy/config/ProfileStore.kt (plan 01 — `profiles`/`activeId`/`upsert`/`setActive`)
    - app/src/main/java/works/mees/dinghy/DinghyApp.kt (plan 02 — the `profiles.preferences_pb` file name + PreferenceDataStoreFactory.create pattern)
    - .planning/phases/14-multi-printer-switching/14-VALIDATION.md (§ Manual-Only Verifications → "Survival across process death" rationale; § Where a Fake would LIE point 1)
    - MEMORY [[dinghy-display-gradle-hang-interop]] (bound coroutine waits; guard hung Gradle with timeout + taskkill; the `connectedDebugAndroidTest` form)
  </read_first>
  <action>
Replace the pending `fail(...)` stub with a real instrumented test using a REAL `DataStore<Preferences>`
on a real `profiles.preferences_pb`-style file (use the instrumentation target context;
`PreferenceDataStoreFactory.create` over a fresh test file, mirroring how `ServiceSurvivesRotationTest`
obtains a real store). The test:
1. Construct a `ProfileStore` over the real DataStore, `upsert` two profiles (E5-ish + E3-ish, any
   host/port — the values are not network-touched here), `setActive(profileB.id)`.
2. Simulate "process death + cold start" by constructing a SECOND `ProfileStore` over the SAME file
   (a fresh DataStore instance reading the persisted bytes — DataStore is single-writer per file, so
   close/recreate appropriately or use a fresh process-scope; mirror the rotation test's recreate path).
3. Assert the re-read `activeId.first()` == `profileB.id` and `profiles.first()` contains both — i.e. the
   active selection landed on the RIGHT printer after the cold re-read (D-02/SC-3).
Keep all coroutine waits bounded (`withTimeout`) per the gradle-hang lesson. This is an on-device test —
run via `connectedDebugAndroidTest` (with a device attached). If a single live `flox` is the only device,
the orchestrator runs it during the UAT session; otherwise it is the standard instrumented gate.
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:compileDebugAndroidTestKotlin --no-daemon" 2>&1 | tr -d '\r'</automated>
  </verify>
  <acceptance_criteria>
    - `:app:compileDebugAndroidTestKotlin` SUCCEEDS (the real test compiles).
    - Source assertion: `ProfileSurvivesRestartTest` constructs a real `DataStore`/`ProfileStore`, writes an active-id, re-reads via a second store instance, and asserts the active-id persisted to the right profile (no `fail(...)` stub remains).
    - On a connected device: `connectedDebugAndroidTest` runs the test GREEN (executed during the UAT session if `flox` is the sole device).
  </acceptance_criteria>
  <done>The survival test is a real instrumented test over a real DataStore file, compiling and (on-device) green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live two-printer hands-on UAT (success criterion 4) — BLOCKING</name>
  <files>.planning/phases/14-multi-printer-switching/14-UAT.md</files>
  <read_first>
    - .planning/phases/14-multi-printer-switching/14-VALIDATION.md (§ Manual-Only Verifications — the three on-device checks + the mock-vs-reality rationale)
    - .planning/phases/14-multi-printer-switching/14-CONTEXT.md (§ Specific Ideas — printer addresses + the SC-4 definition; D-02/D-03/D-08/D-12 behaviors to eyeball)
    - CLAUDE.md (build env: `E:\Android\gw.bat`, `E:\Android\sign-release.bat`, adb interop; printer addresses)
  </read_first>
  <what-built>
    The full multi-printer switching feature: ProfileStore + active-config derivation (distinctUntilChanged), per-printer theme re-seed, the Devices switcher screen + drawer tile, Settings profile CRUD + delete ConfirmGuard, and the webcam-pref re-key. The orchestrator builds the debug (or signed release) APK via `E:\Android\gw.bat` and installs it on `flox`.
  </what-built>
  <how-to-verify>
On `flox`, with the Ender 5 Plus (192.168.1.120:7125) and Ender 3 Pro (192.168.1.121:7125) both powered:
1. **Fresh start (D-07/D-11):** first launch with 0 profiles → the Connect prompt routes to Settings. Add the E5+ (host/port, optional key, give it a name + a distinct theme/accent). Add the E3 (name + a DIFFERENT theme/accent). Confirm each appears as a profile row.
2. **Switch + drive each (SC-4):** open the Devices tile (drawer subtitle shows the active printer's name, D-03). Tap the E5+ → recovery Splash → lands on the E5+ Status; drive a control (e.g. a jog or a temp set). Tap the E3 → Splash → E3 Status; drive a control. Neither switch re-asks for host/port/key. The whole app look changes per printer (D-08).
3. **No-churn on theme edit (Pitfall 1):** with one printer active, change its accent in Settings → confirm NO recovery Splash fires (the connection must not bounce).
4. **Mid-print switch (D-04):** if a print is running on one printer, switch to the other and back — the print continues untouched.
5. **Delete (D-12/D-14):** delete the ACTIVE profile via the Settings ConfirmGuard → confirm the app auto-selects the remaining printer and rebinds (no dead no-active state). Delete the last profile → falls to the Connect prompt (D-11).
6. **Survival (SC-3):** force-stop the app after selecting a printer; relaunch → it reconnects to the SAME printer.
Record PASS/FAIL per item in `14-UAT.md`. Any FAIL spawns a gap-closure plan (do NOT mark the phase complete on a FAIL).
  </how-to-verify>
  <resume-signal>Report the 6-item UAT results (PASS/FAIL each). Type "approved" if all pass, or describe the failures.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Persisted active-id across process death | The on-disk active selection must survive a cold start without leaking or mis-pointing |
| Live spine rebind across two real printers | Capability detection + command registry must rebind cleanly with no stale state from the prior printer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-13 | Tampering (stale state) | Capabilities/command registry leaking from the prior printer after a switch | mitigate | The `SpineHandle` atomic republish guarantees no partial mix; PROVEN only on-device against live Moonraker (UAT item 2) — a fake session would lie (VALIDATION §mock-vs-reality point 2) |
| T-14-14 | Denial of service | Corrupt persisted active-id → wrong/no printer on cold start | mitigate | Dangling active-id resolves to null → Connect prompt (D-11), never a crash; survival test + UAT item 6 confirm the happy path |
| T-14-SC | Tampering | npm/pip/cargo installs | accept | No new external packages this phase |
</threat_model>

<verification>
- `:app:compileDebugAndroidTestKotlin` SUCCESS; `ProfileSurvivesRestartTest` green on-device.
- Full `:app:testDebugUnitTest` GREEN (final phase regression).
- The blocking live two-printer UAT recorded PASS (all 6 items) in `14-UAT.md`.
</verification>

<success_criteria>
- Survival-across-process-death proven by the instrumented test (not a fake).
- The live two-printer switch + drive-each + no-re-entry succeeds on real hardware (SC-4).
- No-churn-on-theme-edit, mid-print switch, delete auto-pick, and empty-state routing all confirmed on-device.
</success_criteria>

<output>
Create `.planning/phases/14-multi-printer-switching/14-06-SUMMARY.md` when done (only after the UAT records PASS).
</output>
