---
phase: 14-multi-printer-switching
plan: 02
type: execute
wave: 1
depends_on: ["14-01"]
files_modified:
  - app/src/main/java/works/mees/dinghy/DinghyApp.kt
  - app/src/main/java/works/mees/dinghy/di/AppContainer.kt
  - app/src/main/java/works/mees/dinghy/service/MoonrakerService.kt
  - app/src/test/java/works/mees/dinghy/di/ActiveConfigDerivationTest.kt
  - app/src/test/java/works/mees/dinghy/theme/ProfileThemeSeedTest.kt
autonomous: true
requirements: [MULTI-01]

must_haves:
  truths:
    - "activeConfig emits the active profile's ConnectionConfig; editing only the active profile's name or theme does NOT re-emit a new ConnectionConfig (distinctUntilChanged suppresses non-connection edits)"
    - "The rebind seam (runConfigLoop cancel-before-rebuild) stays the sole data path into the service — only the configFlow ARG changes, the loop is byte-unchanged"
    - "Switching profiles re-seeds ThemeResolver from the active profile's theme triple in one atomic apply(base,deltas,fs) — no stale theme"
    - "hasConfig is now 'has an active profile' so 0 profiles routes to the existing Connect prompt (D-11)"
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/di/AppContainer.kt"
      provides: "profileStore wiring, activeProfile flow, activeConfig flow (distinctUntilChanged), hasConfig generalization, rewired seedTheme"
      contains: "val activeConfig"
    - path: "app/src/main/java/works/mees/dinghy/service/MoonrakerService.kt"
      provides: "configFlow = container.activeConfig (the ONLY service edit)"
      contains: "container.activeConfig"
    - path: "app/src/main/java/works/mees/dinghy/DinghyApp.kt"
      provides: "profiles.preferences_pb DataStore created once and injected into AppContainer"
      contains: "profiles.preferences_pb"
  key_links:
    - from: "AppContainer.activeConfig"
      to: "MoonrakerService.runConfigLoop"
      via: "configFlow argument (replaces connectionStore.config)"
      pattern: "configFlow = container.activeConfig"
    - from: "AppContainer.seedTheme"
      to: "themeResolver.apply"
      via: "flatMapLatest over activeProfile → toThemeResolved()"
      pattern: "flatMapLatest"
    - from: "AppContainer.activeProfile"
      to: "activeConfig"
      via: "map { toConnectionConfig() }.distinctUntilChanged()"
      pattern: "distinctUntilChanged"
---

<objective>
Compose the active-`ConnectionConfig` derivation in `AppContainer` and feed it to the existing service
rebind seam, rewire theme seeding to the active profile (D-08), generalize `hasConfig`, and create the
`profiles.preferences_pb` DataStore in `DinghyApp`. This is the spine of the phase: switching becomes
"`activeConfig` emits a different value" and the existing `runConfigLoop` does the teardown/rebind +
`SpineHandle` atomic republish for free. The ONLY service edit is one argument.

Purpose: Make the profile model drive the connection + theme without inventing any second rebind path.
Output: rewired `AppContainer`, one-line `MoonrakerService` change, new DataStore in `DinghyApp`;
turns `ActiveConfigDerivationTest` + `ProfileThemeSeedTest` into container-routed GREEN tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-multi-printer-switching/14-CONTEXT.md
@.planning/phases/14-multi-printer-switching/14-RESEARCH.md
@.planning/phases/14-multi-printer-switching/14-PATTERNS.md
@.planning/phases/14-multi-printer-switching/14-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: AppContainer active-config + theme re-seed + hasConfig + DinghyApp datastore wiring</name>
  <files>app/src/main/java/works/mees/dinghy/di/AppContainer.kt, app/src/main/java/works/mees/dinghy/DinghyApp.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/di/AppContainer.kt (FULL — the ctor at lines 51-72, the existing store wiring at lines 66-69, the `spine.flatMapLatest` idiom at lines 121-145, `hasConfig` at line 190, `seedTheme` at lines 224-230. The class already `@OptIn(ExperimentalCoroutinesApi)` and imports flatMapLatest/combine/map)
    - app/src/main/java/works/mees/dinghy/DinghyApp.kt (FULL — the four `PreferenceDataStoreFactory.create` blocks at lines 40-64, the `AppContainer(...)` call at lines 66-83, `seedTheme(appScope)` at line 85)
    - app/src/main/java/works/mees/dinghy/config/ProfileStore.kt (from plan 01 — the flows + writers this container composes)
    - app/src/main/java/works/mees/dinghy/config/Profile.kt (from plan 01 — toConnectionConfig() + toThemeResolved())
    - app/src/main/java/works/mees/dinghy/theme/ThemeResolver.kt (lines 114-120 — the `apply(base, deltas, fs)` one-re-emit primitive; line ~103 `resolve` is pure)
    - app/src/main/java/works/mees/dinghy/theme/ThemePrefs.kt (lines 78-91 — Resolved + DEFAULT for the new-profile-default fallback)
    - .planning/phases/14-multi-printer-switching/14-PATTERNS.md (§ AppContainer.kt + § DinghyApp.kt — the exact add/change lists)
    - .planning/phases/14-multi-printer-switching/14-RESEARCH.md (§ Pattern 2 + Pattern 3 + Pitfall 1/3 — distinctUntilChanged load-bearing, single-writer, global-theme-as-new-profile-default)
  </read_first>
  <action>
In `DinghyApp.onCreate`: add a fifth `val profileDataStore: DataStore<Preferences> = PreferenceDataStoreFactory.create(scope = appScope, produceFile = { applicationContext.preferencesDataStoreFile("profiles.preferences_pb") })`, copying the existing block at lines 40-64 exactly. Pass `profileDataStore = profileDataStore` into the `AppContainer(...)` call (lines 66-83). Create this DataStore ONCE here — never elsewhere (DataStore single-writer invariant, RESEARCH Pitfall 3). `seedTheme(appScope)` at line 85 stays at the call site (its internals change in AppContainer).

In `AppContainer`: add `profileDataStore: DataStore<Preferences>` to the ctor (alongside the existing four store params, before `discovery`). Construct `val profileStore: ProfileStore = ProfileStore(profileDataStore)` next to the existing `connectionStore` at line 69. Then add (per RESEARCH Pattern 2):
- `val activeProfile: Flow<Profile?> = combine(profileStore.profiles, profileStore.activeId) { list, id -> list.firstOrNull { it.id == id } }` — PURE pick, dangling active-id → null (D-12 auto-pick is in the store writer, not here).
- `val activeConfig: Flow<ConnectionConfig?> = activeProfile.map { it?.toConnectionConfig() }.distinctUntilChanged()` — the `distinctUntilChanged` is LOAD-BEARING: a name/theme-only edit on the active profile produces an EQUAL `ConnectionConfig` (structural data-class equality over host/port/apiKey) so it is suppressed and does NOT churn the spine (RESEARCH Pitfall 1).
- Change `hasConfig` (line 190) from `connectionStore.config.map { it != null }` to `activeConfig.map { it != null }` (D-11 "has active profile").

Rewire `seedTheme` (lines 224-230) from `themePrefs.flow.collect { … }` to: `activeProfile.flatMapLatest { p -> flowOf(p?.toThemeResolved() ?: themePrefs.flow.firstOrNull() ?: ThemePrefs.DEFAULT) }.collect { r -> themeResolver.apply(r.base, r.deltas, r.fs) }`. KEEP `themePrefs` in the container — it is now the NEW-PROFILE default + the "no active profile" idle look (RESEARCH Pattern 3 "Global-theme relationship"); do NOT delete it. The `apply(base,deltas,fs)` one-re-emit avoids 3-call flicker.

Do NOT add any disconnect/publishSpine code. Switching is entirely `profileStore.setActive` + the existing `runConfigLoop`. Leave `connectionStore` field in the container untouched (D-07 — dead but kept until phase verified).
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:compileDebugKotlin --no-daemon" 2>&1 | tr -d '\r'</automated>
  </verify>
  <acceptance_criteria>
    - `:app:compileDebugKotlin` SUCCEEDS.
    - Source assertion: `AppContainer` defines `activeProfile` (combine), `activeConfig` (`.map { it?.toConnectionConfig() }.distinctUntilChanged()`), and `hasConfig` now reads `activeConfig.map { it != null }` (grep confirms `hasConfig` no longer references `connectionStore.config`).
    - Source assertion: `seedTheme` collects `activeProfile.flatMapLatest { … toThemeResolved() … }` and calls `themeResolver.apply(...)`; `themePrefs` is still present as the fallback (`?: themePrefs.flow.firstOrNull() ?: ThemePrefs.DEFAULT`).
    - Source assertion: `DinghyApp` creates `profiles.preferences_pb` via `PreferenceDataStoreFactory.create` exactly once and passes `profileDataStore` into `AppContainer(...)`.
    - No `publishSpine` / disconnect sequencer added anywhere in the diff.
  </acceptance_criteria>
  <done>AppContainer derives activeConfig (distinctUntilChanged) + activeProfile, re-seeds theme per active profile, generalizes hasConfig, and DinghyApp owns the new DataStore — compiling.</done>
</task>

<task type="auto">
  <name>Task 2: Rewire MoonrakerService config source + route the Wave-0 derivation/theme tests through the container</name>
  <files>app/src/main/java/works/mees/dinghy/service/MoonrakerService.kt, app/src/test/java/works/mees/dinghy/di/ActiveConfigDerivationTest.kt, app/src/test/java/works/mees/dinghy/theme/ProfileThemeSeedTest.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/service/MoonrakerService.kt (lines 90-100 — the `runConfigLoop(configFlow = container.connectionStore.config, ...)` call at line 94; lines 308-334 — the `runConfigLoop` seam with `cancelAndJoin()` at line 325, which stays UNCHANGED)
    - app/src/test/java/works/mees/dinghy/service/MoonrakerServiceTest.kt (the existing seam test proving cancel-before-rebuild — confirm it still passes after the arg swap; it drives `runConfigLoop` directly with a test flow, so it is unaffected)
    - app/src/main/java/works/mees/dinghy/di/AppContainer.kt (Task-1 output — `activeConfig`/`activeProfile` now exist for the tests to route through)
    - app/src/test/java/works/mees/dinghy/di/ActiveConfigDerivationTest.kt (the plan-01 scaffold to upgrade to the real container-routed assertion)
    - app/src/test/java/works/mees/dinghy/theme/ProfileThemeSeedTest.kt (the plan-01 scaffold to upgrade)
    - .planning/phases/14-multi-printer-switching/14-PATTERNS.md (§ MoonrakerService.kt — the ONE-line edit; § AppContainer — the seedTheme contract)
  </read_first>
  <action>
In `MoonrakerService.onCreate` (line 94) change ONLY the `configFlow` argument: `configFlow = container.activeConfig` (was `container.connectionStore.config`). Nothing else in `MoonrakerService` changes — the `runConfigLoop` body (lines 308-334) with its `cancelAndJoin()`-before-rebuild stays byte-identical (RESEARCH/CONTEXT: do NOT add a second rebind path).

Upgrade the two plan-01 host scaffolds to assert the REAL behavior now that the container exposes the flows. If the derivation pipeline can be exercised by constructing a `ProfileStore`/`AppContainer` over a fake/in-memory `DataStore<Preferences>` (mirror how `MoonrakerServiceTest` builds its harness), route the assertions through `container.activeConfig` and `container.activeProfile` + the seedTheme effect. If constructing the full `AppContainer` in a host test is impractical (Android deps), keep the assertions on the standalone pipeline shape (identical to RESEARCH Pattern 2) and the real `Profile.toConnectionConfig()`/`toThemeResolved()` — the contract is the same. Specifically:
- `ActiveConfigDerivationTest`: assert that emitting profile A then A-with-only-name-changed then A-with-only-theme-changed through the `combine→pick→map→distinctUntilChanged` pipeline yields exactly ONE `ConnectionConfig` emission (no churn); then a host/port edit yields a SECOND distinct emission. Use `runTest` + a `Flow` collector (e.g. collect into a list under `advanceUntilIdle`).
- `ProfileThemeSeedTest`: assert switching the active profile changes the resolved theme triple to the new profile's `(base, deltas, fs)`, and a corrupt-primitives profile falls back to `ThemePrefs.DEFAULT`.
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --tests works.mees.dinghy.di.ActiveConfigDerivationTest --tests works.mees.dinghy.theme.ProfileThemeSeedTest --tests works.mees.dinghy.service.MoonrakerServiceTest --no-daemon" 2>&1 | tr -d '\r'</automated>
  </verify>
  <acceptance_criteria>
    - `ActiveConfigDerivationTest`, `ProfileThemeSeedTest`, and `MoonrakerServiceTest` all PASS.
    - Behavior assertion: a name-only and a theme-only edit on the active profile produce NO new `ConnectionConfig` emission; a host/port edit produces one.
    - Behavior assertion: switching active profile re-applies the new profile's theme triple; corrupt primitives → `ThemePrefs.DEFAULT`.
    - Source assertion: `MoonrakerService.kt` line ~94 reads `configFlow = container.activeConfig`; `runConfigLoop` body is otherwise unchanged (the `cancelAndJoin()` is still the only teardown).
  </acceptance_criteria>
  <done>The service consumes activeConfig via its unchanged seam, MoonrakerServiceTest still proves cancel-before-rebuild, and the two derivation/theme tests are GREEN against the real pipeline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ProfileStore flows → MoonrakerService spine | The active profile's connection (incl. API key) crosses into the live socket build via `activeConfig` |
| Active profile → ThemeResolver | The active profile's persisted theme primitives drive the app-wide token values |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-04 | Denial of service | A non-connection edit (name/theme) churning the spine → repeated reconnect Splash | mitigate | `distinctUntilChanged` after `map { toConnectionConfig() }` suppresses non-connection edits — tested in ActiveConfigDerivationTest + eyeballed on-device (plan 06: no Splash on accent change) |
| T-14-05 | Tampering | A second/parallel rebind path bypassing the proven cancel-before-rebuild seam | mitigate | Only the `configFlow` arg changes; `runConfigLoop` body unchanged; no UI/service `publishSpine` — code-review + MoonrakerServiceTest still green |
| T-14-06 | Information disclosure | API key reaching a log via the derived `ConnectionConfig` | mitigate | `ConnectionConfig.toString()` already redacts (existing); the derived value inherits it — no new sink added |
| T-14-SC | Tampering | npm/pip/cargo installs | accept | No new external packages this phase (Android/Gradle, all deps declared) |
</threat_model>

<verification>
- `:app:compileDebugKotlin` SUCCESS.
- `ActiveConfigDerivationTest` + `ProfileThemeSeedTest` + `MoonrakerServiceTest` GREEN.
- Full `:app:testDebugUnitTest` GREEN after this wave's merge (no regression from the config-source swap).
</verification>

<success_criteria>
- `activeConfig` drives the service through the unchanged seam; name/theme edits do not reconnect.
- Theme re-seeds per active profile in one atomic apply; `themePrefs` retained as new-profile default.
- `hasConfig` is now "has active profile"; `DinghyApp` owns `profiles.preferences_pb` (single writer).
</success_criteria>

<output>
Create `.planning/phases/14-multi-printer-switching/14-02-SUMMARY.md` when done.
</output>
