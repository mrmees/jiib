---
phase: 24-navigation-spine
plan: 04
type: execute
wave: 2
depends_on: [24-01, 24-02, 24-03]
files_modified:
  - app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusScreen.kt
  - app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusField.kt
  - app/src/main/java/works/mees/dinghy/ui/route/HomeAction.kt
  - app/src/main/res/values/strings.xml
  - app/src/test/java/works/mees/dinghy/ui/route/HomeActionTest.kt
autonomous: true
requirements: [SC-1, SC-3, SC-4]
must_haves:
  truths:
    - "The home root is one surface that morphs idle/printing/terminal via a one-shot ~150ms Crossfade (D-13)"
    - "Idle Field is a data-driven List<HomeAction> rendered by Phase-23 ListRow/ListBlock in the D-06 order with D-08 capability HIDE"
    - "The idle foot bar is a FootButtonBar with Preheat (existing runPreheat path) + a NEUTRAL System button that opens the App Drawer (D-09/D-10)"
    - "The gutter is gone from the morphing root; its jobs are rehomed to the foot-of-list bar (SC-3)"
    - "buildIdleActions' Webcam row references the owner-confirmed DinghyIcons.LauncherWebcam (the 24-01 PLACEHOLDER swapped here, after 24-02 registered it) (FIX-5)"
    - "PrintStatusScreen NO LONGER wires its own FloatingEStop or Stop Confirm guard — those were hoisted to AppShell in 24-03 (FIX-1); this plan only does the morph + idle list"
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusScreen.kt"
      provides: "Crossfade morph + onNavigate(NavDest) seam (NO in-screen e-stop — hoisted to AppShell)"
      contains: "Crossfade"
    - path: "app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusField.kt"
      provides: "Data-driven idle HomeAction list (ListRow/ListBlock) + idle FootButtonBar (Preheat + System)"
      contains: "buildIdleActions"
  key_links:
    - from: "PrintStatusField idle list"
      to: "navController (via onNavigate)"
      via: "HomeAction.Destination → onNavigate(action.dest); System foot → onOpenDrawer()"
      pattern: "onNavigate(action.dest)"
    - from: "buildIdleActions Webcam row"
      to: "DinghyIcons.LauncherWebcam"
      via: "PLACEHOLDER(24-04) swap to the 24-02 owner-confirmed token"
      pattern: "LauncherWebcam"
---

<objective>
Reshape `PrintStatusScreen` into the morphing waterfall ROOT per the jiib redesign LAW: one Focus/Field
surface (no gutter) that morphs across idle / printing / terminal via a cheap one-shot ~150ms Crossfade
(D-13), with the idle Field rebuilt as a data-driven `List<HomeAction>` (D-05/D-06) rendered by the
Phase-23 `ListRow`/`ListBlock` classes and capability-gated by D-08 HIDE, and an idle `FootButtonBar` with
Preheat + a NEUTRAL "System" button that opens the App Drawer (D-09/D-10).

⚠ FIX-1: the printing-only `FloatingEStop` + Stop Confirm guard are NOT wired here. 24-03 hoisted them to
the AppShell overlay layer so the e-stop appears on EVERY destination while printing (D-14 "every screen
only when printing"), not just on the root. This plan REMOVES any in-screen e-stop wiring from
PrintStatusScreen and does ONLY the morph + idle-list reshape. (The existing `showEstopGuard` state + its
ConfirmGuard block move out; the AppShell owns them now.)

This consumes 24-01's `buildIdleActions`/`NavDest`, 24-02's owner-confirmed Webcam/Preheat/System glyphs,
and 24-03's NavHost (the `onNavigate` seam is now `(NavDest) -> Unit`). ⚠ FIX-5: this is the FIRST plan
that depends on BOTH 24-01 and 24-02, so it is where the 24-01 Webcam-icon `PLACEHOLDER(24-04)` is swapped
to the real `DinghyIcons.LauncherWebcam` (registered by 24-02) and the Webcam row label set to the
owner-confirmed `cd_launcher_webcam`. The existing four-state `classifyPrintStatus`/`PrintStatusMode`
machinery and the `runPreheat`/`selectPreheatPath` path are REUSED — this is a reshape, not a rewrite of
state handling.

The existing Standby `LauncherDest` tile-grid (Files/Temperature/Move/Extrude/Calibration/Spool/Macros/
Console/Drawer) is REPLACED by the `HomeAction` list — note the membership change: Temperature & Console
drop OFF idle (D-07, they remain reachable while printing), Outputs & Webcam join, Spool leads (D-06).

⚠ Scope guard: do NOT reduce any decision to a "v1/static/placeholder" simplification. The idle list is
the real data-driven `buildIdleActions` output with live capability gating; the System button really
opens the live drawer; the morph is the real Crossfade (with the documented hard-cut fallback only if
flox janks, decided at the 24-05 gate — not pre-emptively).

Purpose: deliver the redesign's home root — the single morphing surface the whole app hangs from.
Output: the morphing PrintStatusScreen (no in-screen e-stop) with the idle action list + idle foot bar.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/24-navigation-spine/24-CONTEXT.md
@.planning/phases/24-navigation-spine/24-RESEARCH.md
@.planning/phases/24-navigation-spine/24-PATTERNS.md
@.claude/skills/sketch-findings-dinghy-display/references/layout-navigation.md
@docs/ui_design/LAYOUT.md
@docs/ui_design/COMPONENTS.md
@docs/ui_design/THEMING.md
@app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusScreen.kt
@app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusField.kt
@app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusUiModel.kt
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Data-driven idle Field list + idle FootButtonBar (Preheat + System) + Webcam-icon swap</name>
  <files>app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusField.kt, app/src/main/java/works/mees/dinghy/ui/route/HomeAction.kt, app/src/main/res/values/strings.xml, app/src/test/java/works/mees/dinghy/ui/route/HomeActionTest.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusField.kt (the current StandbyField launcher-tile grid + LauncherTile mapping at ~357-490; the iconForLauncherDest map; the cd_* string usage)
    - app/src/main/java/works/mees/dinghy/ui/route/HomeAction.kt (the 24-01 buildIdleActions; the `// PLACEHOLDER(24-04)` Webcam-icon stub to swap to DinghyIcons.LauncherWebcam now — FIX-5)
    - app/src/main/java/works/mees/dinghy/designsystem/icons/DinghyIcons.kt (confirm 24-02 registered LauncherWebcam + the Preheat/System foot tokens; read the 24-02 SUMMARY for the owner's exact glyph choices)
    - app/src/main/java/works/mees/dinghy/designsystem/layout/ListBlock.kt + designsystem/components/ListRow.kt (the Phase-23 list classes — ListBlock items{}, ListRow(selected,onClick,uDp){content}, edge-fade)
    - app/src/main/java/works/mees/dinghy/designsystem/components/FootButtonBar.kt + designsystem/control/OutlinedControl.kt + control/Intent.kt (the foot bar + Intent.Neutral)
    - app/src/main/java/works/mees/dinghy/designsystem/layout/UnitGrid.kt (rememberUnitGrid(minOf(maxWidth,maxHeight)) → grid.uDp)
    - 24-PATTERNS.md § ui/printstatus/PrintStatusScreen.kt (the ListBlock/ListRow + FootButtonBar idle code shapes; token-compliance + fsSp rules)
    - .claude/skills/sketch-findings-dinghy-display/references/layout-navigation.md (translucent list rows, foot-of-list bar, list edge-fade, the foot button intents)
  </read_first>
  <behavior>
    - The idle Field renders buildIdleActions(spoolmanPresent, bookmarksExist, outputsPresent, webcamEnabled) in the D-06 order, one ListRow per HomeAction.Destination, with the row's DinghyIcons token + @StringRes label.
    - When a capability flag is false, that row is ABSENT and the list compacts (D-08), never greyed.
    - Tapping a Destination row calls onNavigate(action.dest: NavDest); the System foot button calls onOpenDrawer().
    - The idle foot bar shows exactly two buttons: Preheat (Neutral intent, wired to the existing runPreheat path) and System (Neutral intent, opens the drawer — NOT red, NOT Power).
    - HomeActionTest still GREEN after the Webcam-icon swap (now references DinghyIcons.LauncherWebcam, not the placeholder).
  </behavior>
  <action>
    First (FIX-5): in `HomeAction.kt`, replace the 24-01 `// PLACEHOLDER(24-04)` Webcam-row icon with the
    owner-confirmed `DinghyIcons.LauncherWebcam` (registered by 24-02) and set the Webcam row label to the
    owner-confirmed `R.string.cd_launcher_webcam`. Confirm `HomeActionTest` stays GREEN.

    Then rebuild the Standby Field (in `PrintStatusField.kt`, replacing the `LauncherDest` tile grid) as a
    `ListBlock` over `buildIdleActions(...)`, each item a Phase-23 `ListRow(selected = false, onClick = {...},
    uDp = grid.uDp)` whose content is the row's `DinghyIcons` icon + `stringResource(action.labelRes)` —
    `HomeAction.Destination` → `onNavigate(action.dest)`. Add an idle `FootButtonBar(uDp = grid.uDp)`
    containing two `OutlinedControl`s, both `Intent.Neutral`: Preheat (label `home_foot_preheat`, icon = the
    24-02 Preheat token, onClick = the existing `runPreheat` lambda threaded down from the screen) and System
    (label `home_foot_system`, icon = the 24-02 System token, onClick = `onOpenDrawer`). All colors via
    `LocalTokens.current`; all font sizes via `fsSp(baseSp, t.fs).sp` (≥15sp floor) — no raw `Color(0x` or
    bare `.sp`. Add the `cd_launcher_webcam`/`home_foot_*` strings to `strings.xml` only if 24-02 did not (it
    owns them — verify before adding to avoid a duplicate-resource build error). The old `LauncherDest`
    tile-grid path for Standby is removed; the printing-mode shortcut grid + babystep row are UNTOUCHED (they
    are not the idle list).
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --tests works.mees.dinghy.ui.route.HomeActionTest --no-daemon && E:\Android\gw.bat :app:compileDebugKotlin --no-daemon" 2>&1 | tr -d '\r' | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - The Standby Field renders a `ListBlock`/`ListRow` over `buildIdleActions(...)` — `grep "buildIdleActions" PrintStatusField.kt` finds the call; the old `LauncherDest` standby tile-grid is gone (grep for `LauncherTile(` in the standby path returns zero, or only in the printing-shortcut path).
    - An idle `FootButtonBar` contains exactly two `Intent.Neutral` `OutlinedControl`s: Preheat (wired to runPreheat) and System (onClick = onOpenDrawer). Neither uses `Intent.Danger`.
    - FIX-5: `buildIdleActions` Webcam row references `DinghyIcons.LauncherWebcam` (no `PLACEHOLDER(24-04)` comment remains); `grep "PLACEHOLDER(24-04)" HomeAction.kt` returns zero; `HomeActionTest` GREEN.
    - Token compliance: zero raw `Color(0x` and zero bare `.sp` (non-`fsSp`) introduced in the new idle UI; `:app:compileDebugKotlin` BUILD SUCCESSFUL.
  </acceptance_criteria>
  <done>The idle Field is the data-driven HomeAction list with D-08 hides + the Preheat/System neutral foot bar; the Webcam icon is the real LauncherWebcam; tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Crossfade morph wrapper + onNavigate(NavDest) seam + REMOVE in-screen e-stop (hoisted to AppShell)</name>
  <files>app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusScreen.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusScreen.kt (the PrintStatusContent when(mode) dispatch ~438-540; the existing estop `showEstopGuard` state + ConfirmGuard wiring ~292-305 to be REMOVED — hoisted to AppShell in 24-03; the onNavigate seam ~71/365/438 now typed (NavDest)->Unit after 24-01; the runPreheat lambda ~173-200)
    - app/src/main/java/works/mees/dinghy/ui/shell/AppShell.kt (confirm 24-03 now owns the FloatingEStop + estop ConfirmGuard at the overlay layer — do NOT duplicate them here)
    - 24-RESEARCH.md § Pattern 7 (Crossfade vs AnimatedContent; tween(150); hard-cut fallback)
    - 24-PATTERNS.md § ui/printstatus/PrintStatusScreen.kt (Crossfade wrapper shape)
    - docs/ui_design/LAYOUT.md (Focus/Field grammar, gutter removed — the morphing root has no gutter slot)
  </read_first>
  <action>
    Wrap the `when(mode)` morph dispatch in `Crossfade(targetState = classifyPrintStatus(state),
    animationSpec = tween(durationMillis = 150), label = "PrintStatusMorph") { mode -> when(mode) {...} }`
    so idle↔printing↔terminal cross-fades in one shot (D-13). No continuous/looping animation. Keep the
    `onNavigate: (NavDest) -> Unit` seam (typed by 24-01) and ensure every internal nav dispatch passes a
    `NavDest.*`. ⚠ FIX-1: REMOVE the in-screen e-stop entirely from this screen — delete the `showEstopGuard`
    state and its `ConfirmGuard` block (~292-305) and any `FloatingEStop` placement inside the Focus. 24-03
    hoisted both to AppShell so the e-stop renders on every destination while printing; a duplicate here would
    double-render and only cover the root. (If the screen still needs the emergency-stop command for some other
    affordance, leave that path untouched — only the floating-estop + its guard move out.) Confirm the morphing
    root renders with NO gutter slot (the redesign removed it; its jobs moved to the idle foot bar in Task 1 and
    the printing-state controls). Remove any now-orphaned gutter scaffolding for the Standby mode.
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:assembleDebug --no-daemon" 2>&1 | tr -d '\r' | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `PrintStatusScreen.kt` wraps the mode dispatch in `Crossfade(... tween(durationMillis = 150) ...)` — `grep -c "Crossfade" PrintStatusScreen.kt` ≥ 1; no continuous/`infiniteRepeatable` animation introduced.
    - FIX-1: the in-screen floating e-stop + its Stop Confirm guard are GONE from PrintStatusScreen — `grep "FloatingEStop" PrintStatusScreen.kt` returns zero AND the estop `ConfirmGuard`/`showEstopGuard` block is removed (the AppShell overlay owns them now); no duplicate e-stop renders.
    - The `onNavigate` seam is `(NavDest) -> Unit` and all internal dispatches pass `NavDest.*` (zero `Dest.` references).
    - The Standby morph renders no gutter (the redesign's gutter-removed root); `:app:assembleDebug` BUILD SUCCESSFUL.
  </acceptance_criteria>
  <done>The root morphs via one-shot Crossfade and navigates with NavDest; the floating e-stop is NOT here (hoisted to AppShell in 24-03); assembleDebug green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (e-stop boundary moved) | The FloatingEStop → EMERGENCY_STOP boundary now lives in AppShell (24-03, FIX-1); this plan removes the in-screen copy and introduces no new boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-04-01 | (N/A) | Crossfade morph / idle list / foot bar | accept | No new network/auth/data surface — UI reshape over the existing PrintStatus/Moonraker integration. The e-stop safety property is now owned + mitigated in 24-03 (T-24-03-02), not here. |

This plan reshapes existing local-Moonraker UI and REMOVES the in-screen e-stop (now in AppShell):
**no new network surface, auth, or data handling.** Block-on severity is `high`; there are none.
</threat_model>

<verification>
- `:app:assembleDebug` BUILD SUCCESSFUL.
- `HomeActionTest` GREEN (with the real LauncherWebcam icon).
- Full host suite GREEN at wave merge.
- Grep gates: ≥1 `Crossfade`, `buildIdleActions` consumed in the idle Field, two `Intent.Neutral` idle foot buttons, zero `FloatingEStop` in PrintStatusScreen (hoisted to AppShell), zero `Dest.` in PrintStatusScreen, zero `PLACEHOLDER(24-04)` in HomeAction.kt.
- Visual/frame-budget confirmation (incl. the app-level e-stop appearing on drill-down screens) is the 24-05 flox gate (Manual-Only).
</verification>

<success_criteria>
SC-1 (the morphing root surface), SC-3 (gutter gone, rehomed to foot-of-list) — at the code/host level;
SC-4 is delivered jointly with 24-03 (the e-stop now lives at the AppShell overlay layer, FIX-1).
On-device owner-approval in both orientations is the 24-05 gate.
</success_criteria>

<output>
Create `.planning/phases/24-navigation-spine/24-04-SUMMARY.md` when done. Note the FIX-1 e-stop removal
(now owned by AppShell/24-03) and the FIX-5 Webcam-icon swap.
</output>
