---
phase: 24-navigation-spine
plan: 02
type: execute
wave: 0
depends_on: []
files_modified:
  - app/src/main/java/works/mees/dinghy/designsystem/icons/DinghyIcons.kt
  - app/src/main/res/values/strings.xml
  - app/src/test/java/works/mees/dinghy/designsystem/icons/DinghyIconsTest.kt
  - tools/verify_ligatures.py
autonomous: false
requirements: [SC-1]
must_haves:
  truths:
    - "The owner has confirmed the glyph for the new idle-list Webcam row, the idle Preheat foot button, and the idle System foot button"
    - "Each confirmed glyph is a registered DinghyIcons token (no raw ligature strings reach the idle UI)"
    - "verify_ligatures.py confirms every new ligature resolves in the bundled v2.944 font; DinghyIconsTest stays GREEN"
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/designsystem/icons/DinghyIcons.kt"
      provides: "LauncherWebcam + idle foot-button glyph tokens (owner-confirmed)"
      contains: "LauncherWebcam"
  key_links:
    - from: "DinghyIcons.all"
      to: "the new tokens"
      via: "explicit registration in the drift-guard list"
      pattern: "LauncherWebcam"
---

<objective>
Resolve the icon glyphs the morphing root (24-04) needs but the registry does not yet have, and
register them as `DinghyIcons` tokens. Per the HARD OWNER LAW ([[dinghy-never-pick-icons-ask]]),
Claude must NEVER pick a glyph independently — this plan ASKS the owner for the three unmapped
surfaces, then registers exactly what is confirmed.

The three unmapped surfaces (confirmed absent in the registry during planning):
1. Idle-list Webcam row — no `LauncherWebcam` token exists; the drawer uses raw `photo_camera`.
2. Idle Preheat foot button — the idle foot bar is NEW this phase; no Preheat foot glyph exists.
3. Idle System foot button (D-09, neutral) — no "System" glyph exists; the drawer Power tile uses
   raw `power_settings_new` (wrong — System is not Power and not red).

The seven OTHER idle rows already have registry tokens (Spool/Files/Move/Extrude/Macros/Calibration
via `Launcher*`, Outputs via `OutputSection`) and need NO owner input — they are reused as-is in 24-04.

This plan runs in Wave 0 alongside 24-01 (it touches only DinghyIcons.kt + strings.xml + the icon
tests — zero overlap with 24-01's route/shell files), so the owner-confirmed tokens exist before
24-04 (Wave 2) wires them.

Purpose: Keep the icon-registry-only law intact and unblock 24-04 without Claude guessing a glyph.
Output: Owner-confirmed `LauncherWebcam` + Preheat + System tokens registered and font-verified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/24-navigation-spine/24-CONTEXT.md
@.planning/phases/24-navigation-spine/24-RESEARCH.md
@.planning/phases/24-navigation-spine/24-PATTERNS.md
@docs/ui_design/CLAUDE.md
@docs/ui_design/THEMING.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: ASK OWNER — confirm the Webcam / Preheat / System glyphs</name>
  <read_first>
    - app/src/main/java/works/mees/dinghy/designsystem/icons/DinghyIcons.kt lines 46-62 (existing Launcher* token glyphs for visual consistency) and 89-98 (Output*)
    - app/src/main/java/works/mees/dinghy/ui/shell/AppDrawer.kt lines 195-258 (drawer raw glyph strings: Webcam=photo_camera, Power=power_settings_new)
    - .claude/skills/sketch-findings-dinghy-display/references/layout-navigation.md (foot-button spec — D-09: third foot button is "System", neutral, NOT "Power"/red)
    - docs/ui_design/CLAUDE.md (HARD RULE: never invent/choose an icon — ask the owner)
  </read_first>
  <action>Present the three unmapped idle surfaces (Webcam row, Preheat foot button, System foot button) to the owner and ask which registered glyph each should use. Do NOT pick any glyph — surface the options below, capture the owner's exact choice for each of the three, and stop. Task 2 registers exactly what is chosen.</action>
  <decision>Which registered glyph should each of these three idle surfaces use?</decision>
  <context>
    The morphing-root idle surface (24-04) needs three glyphs the registry does not have. The seven other
    idle rows already map to existing tokens and need no input. Per the icon law Claude will NOT pick — the
    owner chooses. The drawer currently uses raw photo_camera for Webcam and power_settings_new for Power;
    the owner may bless those as registered tokens or pick different Material Symbols ligatures. The font is
    the bundled v2.944 Material Symbols (any chosen ligature must resolve in it — Task 2 verifies). For the
    System foot button (D-09) the glyph reads NEUTRAL intent — a settings/nav door, not the destructive
    Power-off (which lives inside, behind the Confirm guard).
  </context>
  <options>
    <option id="webcam">
      <name>Webcam idle-list row glyph</name>
      <pros>Drawer already uses photo_camera; reusing it as a new LauncherWebcam token keeps continuity.</pros>
      <cons>Owner may prefer a different camera/video glyph for the lists-first idle surface.</cons>
    </option>
    <option id="preheat">
      <name>Preheat idle foot-button glyph</name>
      <pros>Heat-prep affordance; a flame/heat glyph distinct from OutputHeater (mode_heat).</pros>
      <cons>Must be visually distinct from the Nozzle/HeatBed/OutputHeater glyphs already in use.</cons>
    </option>
    <option id="system">
      <name>System idle foot-button glyph (D-09, neutral)</name>
      <pros>Opens the App Drawer (interim System hub, D-10); a settings/apps/dashboard glyph reads as a door.</pros>
      <cons>Must NOT read as Power; must be distinct from Settings/Calibration tokens.</cons>
    </option>
  </options>
  <resume-signal>Owner states the chosen ligature (or "reuse photo_camera"/drawable) for each of the three: Webcam, Preheat, System.</resume-signal>
  <verify><human-check>Owner has named a specific registered glyph for each of Webcam / Preheat / System.</human-check></verify>
  <done>The owner has chosen a glyph for each of the three idle surfaces; no glyph was picked by Claude.</done>
  <acceptance_criteria>
    - The owner has named a specific glyph (Material Symbols ligature or existing drawable) for EACH of: Webcam idle row, Preheat foot button, System foot button.
    - No glyph was chosen by Claude; the decisions are recorded in the SUMMARY with the owner's exact words.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Register the owner-confirmed tokens + font-verify + drift-guard</name>
  <files>app/src/main/java/works/mees/dinghy/designsystem/icons/DinghyIcons.kt, app/src/main/res/values/strings.xml, tools/verify_ligatures.py, app/src/test/java/works/mees/dinghy/designsystem/icons/DinghyIconsTest.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/designsystem/icons/DinghyIcons.kt lines 46-62 (the Launcher* block to add LauncherWebcam into) and 163-167 (the `val all: List<DinghyIcon>` drift-guard list — every new token MUST be added here)
    - tools/verify_ligatures.py (the NEEDED ligature list the font-resolution gate checks)
    - app/src/test/java/works/mees/dinghy/designsystem/icons/DinghyIconsTest.kt (dynamic uniqueness/drift checks derived from DinghyIcons.all)
    - app/src/main/res/values/strings.xml lines 101-109 (the cd_launcher_* content-description convention)
  </read_first>
  <action>
    Register exactly the three owner-confirmed glyphs from Task 1 as new `DinghyIcons` tokens: `LauncherWebcam`
    (idle Webcam row), and a Preheat foot token + a System foot token (descriptive val names like `FootPreheat`/
    `FootSystem` matching registry style), each as `DinghyIcon(IconRef.Ligature("<owner-ligature>"), alternate
    = "<snake_name>")` — or `IconRef.Drawable(...)` if the owner picked an existing drawable. Add all three to
    the `DinghyIcons.all` drift-guard list. Add `cd_launcher_webcam` and label strings `home_foot_preheat` +
    `home_foot_system` to `strings.xml` following the existing `cd_launcher_*` convention. Add each new ligature
    to `tools/verify_ligatures.py`'s NEEDED list. If 24-01's `HomeAction.kt` has already landed with the
    `// PLACEHOLDER(24-02)` Webcam-icon stub, replace it with `DinghyIcons.LauncherWebcam`; if 24-01 has not yet
    landed (both are Wave 0), leave the swap for 24-04 Wave 2 and record this in the SUMMARY. Do NOT pick any
    glyph not confirmed in Task 1.
  </action>
  <verify>
    <automated>python tools/verify_ligatures.py 2>&1 | tail -5; /mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --tests works.mees.dinghy.designsystem.icons.DinghyIconsTest --no-daemon" 2>&1 | tr -d '\r' | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `DinghyIcons.kt` defines `LauncherWebcam` + a Preheat foot token + a System foot token, all three present in `DinghyIcons.all` (grep each val name in the `all` list).
    - `verify_ligatures.py` exits 0 with `missing: []` — every new ligature resolves in the bundled v2.944 font.
    - `DinghyIconsTest` GREEN (uniqueness + drift checks pass with the three new tokens).
    - `strings.xml` contains `cd_launcher_webcam`, `home_foot_preheat`, `home_foot_system`.
    - No glyph in the diff was chosen by Claude — each matches the owner's Task-1 decision (cross-check against the SUMMARY record).
  </acceptance_criteria>
  <done>The three owner-confirmed idle glyphs are registered, font-verified, and drift-guarded; idle UI has registry tokens only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Icon-registry + string-resource edits only; no input, network, or data boundary crossed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-02-01 | (N/A) | DinghyIcons token registration | accept | No new attack surface — registry/strings edits only; local-LAN UI asset wiring. No network, auth, input, or data handling introduced. |

This plan registers font glyph tokens and strings. Block-on severity is `high`; there are none.
</threat_model>

<verification>
- `verify_ligatures.py` exits 0 (`missing: []`); all three new ligatures resolve in v2.944.
- `DinghyIconsTest` GREEN.
- The owner confirmed each glyph (recorded in SUMMARY); zero Claude-chosen glyphs.
</verification>

<success_criteria>
SC-1 (foundation): the morphing root's idle Webcam row + Preheat/System foot buttons have
owner-confirmed, registered, font-verified `DinghyIcons` tokens to consume in 24-04 — preserving the
icon-registry-only law with no Claude-picked glyphs.
</success_criteria>

<output>
Create `.planning/phases/24-navigation-spine/24-02-SUMMARY.md` when done. Record the owner's exact
glyph choices for Webcam / Preheat / System.
</output>
