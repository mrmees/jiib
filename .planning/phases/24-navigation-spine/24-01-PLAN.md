---
phase: 24-navigation-spine
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - gradle/libs.versions.toml
  - app/build.gradle.kts
  - app/src/main/java/works/mees/dinghy/ui/route/NavDest.kt
  - app/src/main/java/works/mees/dinghy/ui/route/TopRoute.kt
  - app/src/main/java/works/mees/dinghy/ui/route/HomeAction.kt
  - app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusField.kt
  - app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusScreen.kt
  - app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusUiModel.kt
  - app/src/main/java/works/mees/dinghy/ui/screen/PrintersScreen.kt
  - app/src/main/java/works/mees/dinghy/ui/screen/ThemeScreen.kt
  - app/src/main/java/works/mees/dinghy/ui/screen/ThemeEditorScreen.kt
  - app/src/main/java/works/mees/dinghy/ui/spool/SpoolScreen.kt
  - app/src/main/java/works/mees/dinghy/ui/shell/StartDestMapping.kt
  - app/src/main/java/works/mees/dinghy/ui/shell/ShellNavState.kt
  - app/src/main/java/works/mees/dinghy/ui/shell/AppShell.kt
  - app/src/main/java/works/mees/dinghy/ui/shell/AppDrawer.kt
  - app/src/main/java/works/mees/dinghy/ui/shell/RootController.kt
  - app/src/main/java/works/mees/dinghy/MainActivity.kt
  - app/src/test/java/works/mees/dinghy/ui/route/HomeActionTest.kt
  - app/src/test/java/works/mees/dinghy/ui/route/PopToRootTest.kt
  - app/src/test/java/works/mees/dinghy/ui/route/TopRouteTest.kt
  - app/src/test/java/works/mees/dinghy/shell/StartDestMappingTest.kt
  - app/src/test/java/works/mees/dinghy/ui/printstatus/PrintStatusUiModelTest.kt
  - app/src/test/java/works/mees/dinghy/ui/shell/AppDrawerOutputsGateTest.kt
  - app/src/androidTest/java/works/mees/dinghy/ui/finetune/FineTuneNavTest.kt
autonomous: true
requirements: [SC-2]
must_haves:
  truths:
    - "navigation-compose 2.8.9 resolves and the app still builds"
    - "NavDest sealed @Serializable route hierarchy replaces the Dest enum across EVERY call site (main + test + androidTest); all three source sets compile GREEN"
    - "buildIdleActions() produces the D-06 order and applies the D-08 HIDE rules (Spool/Outputs/Webcam/Macros) using ONLY already-registered DinghyIcons tokens + strings — no symbol another Wave-0 plan creates (FIX-5)"
    - "FOOT_GUN_DESTS + shouldPopToRoot() is a PURE route-layer predicate, host-tested GREEN by PopToRootTest in src/test (NO TestNavController, NO NavHost — FIX-8)"
    - "RED test scaffold (HomeActionTest GREEN, PopToRootTest GREEN against the pure predicate) compiles against the whole test source set"
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/ui/route/NavDest.kt"
      provides: "Type-safe @Serializable NavDest route hierarchy (17 destinations) + FOOT_GUN_DESTS + shouldPopToRoot() pure predicate"
      contains: "sealed interface NavDest"
    - path: "app/src/main/java/works/mees/dinghy/ui/route/HomeAction.kt"
      provides: "Typed idle action list model + buildIdleActions()"
      contains: "fun buildIdleActions"
    - path: "app/src/test/java/works/mees/dinghy/ui/route/HomeActionTest.kt"
      provides: "D-06 order + D-08 HIDE-rule coverage"
    - path: "app/src/test/java/works/mees/dinghy/ui/route/PopToRootTest.kt"
      provides: "D-04 foot-gun pop-to-root coverage via the PURE shouldPopToRoot predicate (GREEN this wave; no TestNavController)"
  key_links:
    - from: "buildIdleActions"
      to: "NavDest"
      via: "HomeAction.Destination(dest = NavDest.*)"
      pattern: "HomeAction.Destination"
    - from: "shouldPopToRoot"
      to: "FOOT_GUN_DESTS"
      via: "pure membership check on NavDest"
      pattern: "FOOT_GUN_DESTS"
    - from: "parseStartDest"
      to: "NavDest"
      via: "knownNavDests list (no .entries on a sealed interface)"
      pattern: "knownNavDests"
---

<objective>
Stand up the navigation-spine foundation: add the Navigation-Compose dependency, introduce the
type-safe `NavDest` route model (retiring the `Dest` enum), add the PURE `FOOT_GUN_DESTS`/`shouldPopToRoot`
route-layer predicate (D-04), introduce the data-driven `HomeAction` idle-list model, and write the
host-test scaffolds the migration plans consume.

This is pure model + tooling work. It touches the `Dest` enum's full blast radius — **18 files across
main + test + androidTest** (FIX-4): a mechanical rename — but introduces NO NavHost and NO UI change
yet — the `when(dest)` shell still runs, now keyed on `NavDest` objects. Splitting the rename out de-risks
the big AppShell migration in 24-03.

⚠ FIX-5 (Wave-0 isolation): `buildIdleActions` is built so it references **only already-registered**
`DinghyIcons` tokens and strings. The NEW Webcam glyph/label and the Preheat/System foot glyphs/labels are
owner-confirmed in 24-02 (the ASK-OWNER gate) — so this plan does NOT reference them. The Webcam row uses a
compile-safe `// PLACEHOLDER(24-04)` icon (an existing token), wired to the real `DinghyIcons.LauncherWebcam`
in 24-04 Wave 2 (which depends on both 24-01 and 24-02). No Wave-0 plan references a symbol another Wave-0
plan creates.

Purpose: Everything downstream (the NavHost migration in 24-03, the morphing root in 24-04) consumes
`NavDest`, `HomeAction`, `buildIdleActions`, `shouldPopToRoot`. Land them first, behind a green build of
ALL THREE source sets, so 24-03/24-04 build against a stable contract instead of a moving one.

Output: navigation-compose 2.8.9 pinned; `NavDest.kt` (with `FOOT_GUN_DESTS`/`shouldPopToRoot`);
`HomeAction.kt`; `Dest`→`NavDest` mechanically applied across all 18 files; `parseStartDest` fixed for
sealed objects; `HomeActionTest` GREEN; `PopToRootTest` GREEN against the pure predicate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-navigation-spine/24-CONTEXT.md
@.planning/phases/24-navigation-spine/24-RESEARCH.md
@.planning/phases/24-navigation-spine/24-PATTERNS.md
@.planning/phases/24-navigation-spine/24-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add navigation-compose 2.8.9 dependency</name>
  <files>gradle/libs.versions.toml, app/build.gradle.kts</files>
  <read_first>
    - gradle/libs.versions.toml (the `[versions]` + `[libraries]` block style; `composeBom = "2026.05.00"` pin; `kotlinxSerialization = "1.7.3"`; `kotlin-serialization` plugin already declared at line 165)
    - app/build.gradle.kts (the `dependencies { implementation(libs.androidx.…) }` block, lines 110-170)
    - 24-RESEARCH.md § Standard Stack + § Installation (the exact 2.8.9 pin rationale: NOT 2.9.x, NOT BOM-governed, minSdk 21, no compileSdk-37/AGP-9 floor)
  </read_first>
  <action>
    In `gradle/libs.versions.toml` add a `[versions]` entry `navigation = "2.8.9"` (with a comment noting:
    navigation-compose 2.8.x line — minSdk 21, NOT BOM-governed, pinned 2.8 per the AGP-8.7/compileSdk-36
    conservative stance, NOT 2.9.x). Add ONE `[libraries]` entry:
    `androidx-navigation-compose = { group = "androidx.navigation", name = "navigation-compose", version.ref = "navigation" }`.
    Do NOT add `navigation-testing` — FIX-8 makes the D-04 pop-to-root test a PURE Kotlin predicate test in
    the JVM `src/test` set (no `TestNavHostController`, which this project's JVM test set cannot RUN — there is
    no Robolectric/test-core in `src/test`). In `app/build.gradle.kts` add
    `implementation(libs.androidx.navigation.compose)` alongside the other `androidx` implementation lines.
    Do NOT add the `kotlin-serialization` plugin — it is already declared. Do NOT change the Compose BOM,
    compileSdk, or AGP.
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:dependencies --configuration debugRuntimeClasspath --no-daemon" 2>&1 | tr -d '\r' | grep -E "navigation-compose:2.8.9|navigation:2.8.9"</automated>
  </verify>
  <acceptance_criteria>
    - `gradle/libs.versions.toml` contains `navigation = "2.8.9"` and the `androidx-navigation-compose` library entry; NO `navigation-testing` entry (FIX-8).
    - `app/build.gradle.kts` contains `implementation(libs.androidx.navigation.compose)`; NO `testImplementation(libs.androidx.navigation.testing)`.
    - The dependency resolves: the gw.bat `:app:dependencies` output shows `navigation-compose:2.8.9` on the debug runtime classpath (grep exits 0).
    - Compose BOM pin (`composeBom = "2026.05.00"`), compileSdk, and AGP lines are unchanged (`git diff` shows no edits to those lines).
  </acceptance_criteria>
  <done>navigation-compose:2.8.9 resolves; no navigation-testing dep; no BOM/compileSdk/AGP drift.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create NavDest route model (+ FOOT_GUN_DESTS/shouldPopToRoot) + HomeAction model + GREEN host tests</name>
  <files>app/src/main/java/works/mees/dinghy/ui/route/NavDest.kt, app/src/main/java/works/mees/dinghy/ui/route/HomeAction.kt, app/src/test/java/works/mees/dinghy/ui/route/HomeActionTest.kt, app/src/test/java/works/mees/dinghy/ui/route/PopToRootTest.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/route/TopRoute.kt (the `sealed interface` + `data object` pattern; the `Dest` enum at line 37 — the 17 destination names to carry over)
    - app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusUiModel.kt lines 115-134 (`standbyLauncherDests` — the EXISTING capability-gated idle builder; the direct analog for `buildIdleActions`, but the NEW D-06 order/membership differs)
    - app/src/main/java/works/mees/dinghy/designsystem/icons/DinghyIcons.kt lines 46-62, 89-98 (the `Launcher*` tokens + `OutputSection`; the icon tokens available for the SEVEN already-mapped idle rows)
    - app/src/main/java/works/mees/dinghy/net/JsonRpc.kt lines 26-32 (the `@Serializable` annotation pattern)
    - app/src/test/java/works/mees/dinghy/ui/route/TopRouteTest.kt (the pure-JUnit4 test style to copy)
    - 24-RESEARCH.md § HomeAction Typed List Model + § Pattern 4 (FOOT_GUN_DESTS/shouldPopToRoot); 24-PATTERNS.md § ui/route/HomeAction.kt
  </read_first>
  <behavior>
    HomeAction (buildIdleActions):
    - buildIdleActions(spoolman=T, bookmarks=T, outputs=T, webcam=T) → 8 rows in D-06 order: Spool, Files, Move, Extrude, Macros, Calibration, Outputs, Webcam.
    - spoolman=F → Spool row absent (D-08); list compacts (Files is first).
    - bookmarks=F → Macros row absent (D-08).
    - outputs=F → Outputs row absent (D-08).
    - webcam=F → Webcam row absent (D-08).
    - all-false → 4 rows: Files, Move, Extrude, Calibration (the always-present set; D-07 keeps Temperature/Console OFF).
    - Every HomeAction.Destination carries a NavDest, a @StringRes label, and a DinghyIcons IconRef token (never a raw ligature string).
    FOOT_GUN pure predicate (D-04):
    - shouldPopToRoot(NavDest.Move, printActive=true|false) → true (Move ∈ FOOT_GUN_DESTS).
    - shouldPopToRoot(NavDest.Extrude, …) → true; shouldPopToRoot(NavDest.Calibration, …) → true.
    - shouldPopToRoot(NavDest.Temperature, …) → false; Macros/FineTune/Console/Webcam/WaterfallHome → false.
    - shouldPopToRoot(null, …) → false (no current dest, nothing to pop).
  </behavior>
  <action>
    Create `NavDest.kt`: a `@Serializable sealed interface NavDest` with `@Serializable data object`
    members for all 17 destinations from the `Dest` enum (WaterfallHome [renamed from PrintStatus],
    Temperature, Move, Extrude, Files, Macros, Console, Calibration, FineTune, Webcam, Spool, Outputs,
    SystemInfo, Devices, Theme, Settings, About). Add a top-level `val knownNavDests: List<NavDest>`
    enumerating all members (sealed interfaces have no `.entries`; this list is the parse/iteration source
    for StartDestMapping in Task 3). ALSO add, in this same route file, the D-04 pure predicate so it is
    host-testable WITHOUT a NavHost (FIX-8): `val FOOT_GUN_DESTS: Set<NavDest> = setOf(NavDest.Move,
    NavDest.Extrude, NavDest.Calibration)` and `fun shouldPopToRoot(current: NavDest?, printActive: Boolean):
    Boolean = current != null && current in FOOT_GUN_DESTS` (the `printActive` param is the call-site signal
    that a print just started/ended; the predicate itself returns true for any foot-gun current dest — the
    AppShell LaunchedEffect in 24-03 already fires only on a printState change, so `printActive` documents the
    contract and leaves room for a future "only when transitioning into printing" tightening without changing
    the test surface).

    Create `HomeAction.kt` per the PATTERNS shape: `sealed interface HomeAction` with
    `data class Destination(val dest: NavDest, @StringRes val labelRes: Int, val icon: works.mees.dinghy.designsystem.icons.DinghyIcon)`
    and `data object OpenDrawer`. Add a KDoc comment marking the v2 `InlineControl` extension point (D-05)
    but do NOT implement it. Add `fun buildIdleActions(spoolmanPresent, bookmarksExist, outputsPresent,
    webcamEnabled): List<HomeAction>` building the D-06 order with the D-08 capability gates, using
    `DinghyIcons.LauncherSpool/LauncherFiles/LauncherMove/LauncherExtrude/LauncherMacros/LauncherCalibration/OutputSection`
    for the seven ALREADY-REGISTERED rows. ⚠ FIX-5: do NOT reference `DinghyIcons.LauncherWebcam` or any
    24-02 token/string — it does not exist yet (24-02 is a parallel Wave-0 ASK-OWNER plan). For the WEBCAM row
    icon use an EXISTING already-registered token (e.g. `DinghyIcons.LauncherFiles` or `DinghyIcons.LauncherDrawer`)
    with a `// PLACEHOLDER(24-04): owner-confirmed Webcam glyph DinghyIcons.LauncherWebcam wired in 24-04 (after
    24-02 registers it)` comment. Use already-registered label strings for all rows; the Webcam row may reuse an
    existing string with a `// PLACEHOLDER(24-04)` note. (24-04 — which depends on both 24-01 and 24-02 — swaps
    the placeholder icon + label for the owner-confirmed `LauncherWebcam` + `cd_launcher_webcam`.) This keeps
    24-01 compiling in ISOLATION with no cross-Wave-0 dependency.

    Create `HomeActionTest.kt` (FULL assertions) covering the eight buildIdleActions cases above plus a
    `v1OrderIsPreserved()` test asserting the exact D-06 destination order.

    Create `PopToRootTest.kt` as a GREEN pure-predicate test (FIX-8): assert `shouldPopToRoot` for every
    behavior case above (Move/Extrude/Calibration → true; Temperature/Macros/FineTune/Console/Webcam/
    WaterfallHome → false; null → false). Use ONLY the pure `shouldPopToRoot`/`FOOT_GUN_DESTS` symbols and
    `NavDest.*` — NO `TestNavHostController`, NO `NavController`, NO navigation-testing import (those need an
    Android runtime the JVM `src/test` set lacks). This satisfies the Wave-0 RED→GREEN requirement against a
    pure predicate, per [[dinghy-wave0-red-scaffold-compile.md]] and the dinghy-instrumented-swipe-threshold
    lesson (a test that only compiles but can't run is unverified).
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --tests works.mees.dinghy.ui.route.HomeActionTest --tests works.mees.dinghy.ui.route.PopToRootTest --no-daemon" 2>&1 | tr -d '\r' | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `NavDest.kt` defines `@Serializable sealed interface NavDest` with 17 `@Serializable data object` members, a `val knownNavDests: List<NavDest>`, a `val FOOT_GUN_DESTS: Set<NavDest>` (Move/Extrude/Calibration), and `fun shouldPopToRoot(current: NavDest?, printActive: Boolean): Boolean`.
    - `HomeAction.kt` defines the sealed `HomeAction` (`Destination` + `OpenDrawer`) and `buildIdleActions(...)`; grep `grep -c "@Serializable" NavDest.kt` returns ≥ 18 (interface + 17 objects).
    - `HomeActionTest` GREEN: all eight capability cases + `v1OrderIsPreserved` pass (D-06 order: Spool, Files, Move, Extrude, Macros, Calibration, Outputs, Webcam).
    - `PopToRootTest` GREEN against the PURE `shouldPopToRoot` predicate — NO `TestNavHostController`/`NavController`/`navigation.testing` reference appears in the test (grep returns zero).
    - 24-01 compiles in ISOLATION: `buildIdleActions` references NO `LauncherWebcam` / 24-02-created token or string — grep for `LauncherWebcam` in HomeAction.kt returns zero; the Webcam row carries a `PLACEHOLDER(24-04)` comment.
    - Every `HomeAction.Destination` icon argument is a `DinghyIcons.*` token (grep finds no raw `IconRef.Ligature("` inside `buildIdleActions`).
  </acceptance_criteria>
  <done>NavDest (+ FOOT_GUN_DESTS/shouldPopToRoot) + HomeAction models exist; HomeActionTest + PopToRootTest are GREEN host tests with no NavHost/24-02 dependency.</done>
</task>

<task type="auto">
  <name>Task 3: Mechanical Dest→NavDest rename across ALL 18 call-site files (main + test + androidTest) + fix parseStartDest</name>
  <files>app/src/main/java/works/mees/dinghy/ui/route/TopRoute.kt, app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusField.kt, app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusScreen.kt, app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusUiModel.kt, app/src/main/java/works/mees/dinghy/ui/screen/PrintersScreen.kt, app/src/main/java/works/mees/dinghy/ui/screen/ThemeScreen.kt, app/src/main/java/works/mees/dinghy/ui/screen/ThemeEditorScreen.kt, app/src/main/java/works/mees/dinghy/ui/spool/SpoolScreen.kt, app/src/main/java/works/mees/dinghy/ui/shell/StartDestMapping.kt, app/src/main/java/works/mees/dinghy/ui/shell/ShellNavState.kt, app/src/main/java/works/mees/dinghy/ui/shell/AppShell.kt, app/src/main/java/works/mees/dinghy/ui/shell/AppDrawer.kt, app/src/main/java/works/mees/dinghy/ui/shell/RootController.kt, app/src/main/java/works/mees/dinghy/MainActivity.kt, app/src/test/java/works/mees/dinghy/ui/route/TopRouteTest.kt, app/src/test/java/works/mees/dinghy/shell/StartDestMappingTest.kt, app/src/test/java/works/mees/dinghy/shell/ShellNavStateTest.kt, app/src/test/java/works/mees/dinghy/ui/printstatus/PrintStatusUiModelTest.kt, app/src/test/java/works/mees/dinghy/ui/shell/AppDrawerOutputsGateTest.kt, app/src/androidTest/java/works/mees/dinghy/ui/finetune/FineTuneNavTest.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/route/TopRoute.kt (the `Dest` enum + `TopRoute.Shell(val dest: Dest)` + `derive()` ending in `TopRoute.Shell(Dest.PrintStatus)`)
    - app/src/main/java/works/mees/dinghy/ui/shell/StartDestMapping.kt (the `Dest.entries.firstOrNull { it.name == name }` body — sealed objects have no `.entries`)
    - app/src/main/java/works/mees/dinghy/MainActivity.kt lines 18/63/82 (`import …route.Dest`; `val startDest: Dest?`; `RootController(container, startDest = startDest)` — the type flips to `NavDest?`)
    - 24-PATTERNS.md § Migration Critical Paths (the call-site enumeration + the `TopRoute.Shell` payload-drop note)
    - ⚠ FIX-4: the FULL verified `Dest` blast radius is THESE 19 files incl. ShellNavStateTest.kt (run `grep -rln "Dest" app/src/main/java app/src/test/java app/src/androidTest/java` to reconfirm before editing — the unit-test compile gate is the backstop for any miss). PrintStatusField.kt alone has ~39+ `Dest.` refs; AppShell ~52. Do NOT skip the test/androidTest files — Gradle compiles the WHOLE test + androidTest source sets, so a missed file leaves a RED build.
  </read_first>
  <action>
    Delete the `Dest` enum from `TopRoute.kt` (it now lives as `NavDest` from Task 2). Change
    `TopRoute.Shell(val dest: Dest)` to `data object Shell : TopRoute` (drop the payload — the NavHost owns
    routing per D-01; the start destination is fixed at `WaterfallHome`). Update `derive()`'s final branch
    from `TopRoute.Shell(Dest.PrintStatus)` to `TopRoute.Shell`. Mechanically replace every remaining
    `Dest.` reference with `NavDest.` and `Dest.PrintStatus` → `NavDest.WaterfallHome` across ALL 18
    source + test + androidTest files listed in `<files>` (FIX-4 — this is the full verified blast radius).
    In `MainActivity.kt` flip `import works.mees.dinghy.ui.route.Dest` → `import works.mees.dinghy.ui.route.NavDest`
    and `val startDest: Dest?` → `val startDest: NavDest?` (the `parseStartDest` return type flips too).
    In `StartDestMapping.kt` replace `Dest.entries.firstOrNull { it.name == name }` with
    `knownNavDests.firstOrNull { it::class.simpleName == name }` (sealed objects expose their name via the
    class simpleName; keep the total/never-throws contract — null/blank/unknown → null per the V5 mitigation).
    Update `StartDestMappingTest`, `TopRouteTest`, `PrintStatusUiModelTest`, `AppDrawerOutputsGateTest`, and
    the androidTest `FineTuneNavTest` for the new `NavDest` cases and the payload-free `TopRoute.Shell`.
    Add `import kotlinx.serialization.Serializable` / the `NavDest` import where needed. This task does NOT
    introduce a NavHost — `AppShell` still uses `when(dest)` (now keyed on `NavDest`); the NavHost swap is 24-03.
    Keep `ShellNavState.dest`/`backStack`/`navigateTo`/`goBack` as-is this plan (24-03 slims them down) — just
    retype their `Dest` → `NavDest`. ⚠ Preserve the 4 leak-cancel `DisposableEffect`s in AppShell VERBATIM
    (webcam ~275, spool ~313, console ~428, macro ~468 — it is 4, not 5; the line ~528 `onDispose` mention is
    a code COMMENT, not an effect). The rename must not touch their bodies.
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:compileDebugKotlin :app:compileDebugUnitTestKotlin :app:compileDebugAndroidTestKotlin --no-daemon" 2>&1 | tr -d '\r' | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "\bDest\b" app/src/main/java app/src/test/java app/src/androidTest/java` returns zero hits for the old enum type (only `NavDest` remains; every `import ...route.Dest` is gone).
    - `TopRoute.Shell` is a `data object` (no `dest` payload); `derive()` returns `TopRoute.Shell`.
    - `StartDestMapping.parseStartDest` returns a `NavDest?` using `knownNavDests` (no `.entries` call); `StartDestMappingTest` GREEN.
    - All THREE source sets compile GREEN: `:app:compileDebugKotlin` AND `:app:compileDebugUnitTestKotlin` AND `:app:compileDebugAndroidTestKotlin` all BUILD SUCCESSFUL (FIX-4 — the androidTest set is included precisely because FineTuneNavTest references `Dest`).
    - `TopRouteTest`, `PrintStatusUiModelTest`, `AppDrawerOutputsGateTest`, and `FineTuneNavTest` compile against the new `NavDest` / payload-free `TopRoute.Shell`.
    - The 4 AppShell leak-cancel `DisposableEffect`s are byte-for-byte preserved (only `Dest.`→`NavDest.` token swaps elsewhere); `grep -c "onDispose { .*cancel() }" AppShell.kt` ≥ 4.
  </acceptance_criteria>
  <done>Dest enum fully retired to NavDest across all 18 files; main + unit-test + androidTest source sets all compile; parseStartDest is sealed-safe; the 4 leak-cancels preserved; no NavHost yet.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `am start --es start_dest` → MainActivity | Untrusted intent extra crosses the exported-Activity boundary (existing surface; `parseStartDest`) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-01-01 | Tampering/DoS | `parseStartDest(raw)` | mitigate | Keep the total/never-throws parse on the `knownNavDests` rename (null/blank/unknown → null); preserved verbatim from the existing V5/T-18-04-02 mitigation. Dev-gate remains the first line of defense. |
| T-24-01-SC | Tampering | navigation-compose install | mitigate | AndroidX first-party artifact from Google Maven (`dl.google.com`), provenance beyond question per RESEARCH § Package Legitimacy Audit; not npm-installed, no slopcheck applicable. No `[ASSUMED]`/`[SUS]` packages. |

This plan is a local-LAN nav model refactor: **no new network surface, no new auth, no new data handling.**
The only crossing is the pre-existing untrusted `start_dest` extra, whose mitigation is carried unchanged.
Block-on severity is `high`; there are none.
</threat_model>

<verification>
- `:app:compileDebugKotlin` + `:app:compileDebugUnitTestKotlin` + `:app:compileDebugAndroidTestKotlin` BUILD SUCCESSFUL (whole tree — all three source sets — compiles post-rename, FIX-4).
- `HomeActionTest` GREEN (D-06 order + D-08 hides). `PopToRootTest` GREEN against the pure `shouldPopToRoot` predicate (FIX-8). `TopRouteTest` + `StartDestMappingTest` GREEN.
- navigation-compose:2.8.9 resolves on the debug runtime classpath; no BOM/compileSdk/AGP drift; no navigation-testing dep.
</verification>

<success_criteria>
SC-2 (foundation): the type-safe `NavDest` route model, the `FOOT_GUN_DESTS`/`shouldPopToRoot` pure
predicate, and the nav-compose dependency are in place, the `Dest` enum is retired across all 18 files,
and the data-driven `buildIdleActions` model + its tests are GREEN — the contract 24-03 (NavHost migration)
and 24-04 (morphing root) build against.
</success_criteria>

<output>
Create `.planning/phases/24-navigation-spine/24-01-SUMMARY.md` when done.
</output>
