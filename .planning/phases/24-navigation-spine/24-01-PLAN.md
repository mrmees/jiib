---
phase: 24-navigation-spine
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - gradle/libs.versions.toml
  - app/build.gradle.kts
  - app/src/main/java/works/mees/dinghy/ui/route/NavDest.kt
  - app/src/main/java/works/mees/dinghy/ui/route/TopRoute.kt
  - app/src/main/java/works/mees/dinghy/ui/route/HomeAction.kt
  - app/src/main/java/works/mees/dinghy/ui/shell/StartDestMapping.kt
  - app/src/main/java/works/mees/dinghy/ui/shell/ShellNavState.kt
  - app/src/main/java/works/mees/dinghy/ui/shell/AppShell.kt
  - app/src/main/java/works/mees/dinghy/ui/shell/AppDrawer.kt
  - app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusScreen.kt
  - app/src/test/java/works/mees/dinghy/ui/route/HomeActionTest.kt
  - app/src/test/java/works/mees/dinghy/ui/route/PopToRootTest.kt
  - app/src/test/java/works/mees/dinghy/ui/route/TopRouteTest.kt
  - app/src/test/java/works/mees/dinghy/shell/StartDestMappingTest.kt
autonomous: true
requirements: [SC-2]
must_haves:
  truths:
    - "navigation-compose 2.8.9 + navigation-testing 2.8.9 resolve and the app still builds"
    - "NavDest sealed @Serializable route hierarchy replaces the Dest enum across all call sites; the app compiles"
    - "buildIdleActions() produces the D-06 order and applies the D-08 HIDE rules (Spool/Outputs/Webcam/Macros)"
    - "RED test scaffolds (HomeActionTest, PopToRootTest) compile against the whole test source set"
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/ui/route/NavDest.kt"
      provides: "Type-safe @Serializable NavDest route hierarchy (17 destinations)"
      contains: "sealed interface NavDest"
    - path: "app/src/main/java/works/mees/dinghy/ui/route/HomeAction.kt"
      provides: "Typed idle action list model + buildIdleActions()"
      contains: "fun buildIdleActions"
    - path: "app/src/test/java/works/mees/dinghy/ui/route/HomeActionTest.kt"
      provides: "D-06 order + D-08 HIDE-rule coverage"
    - path: "app/src/test/java/works/mees/dinghy/ui/route/PopToRootTest.kt"
      provides: "D-04 foot-gun pop-to-root coverage (RED scaffold this wave)"
  key_links:
    - from: "buildIdleActions"
      to: "NavDest"
      via: "HomeAction.Destination(dest = NavDest.*)"
      pattern: "HomeAction.Destination"
    - from: "parseStartDest"
      to: "NavDest"
      via: "knownNavDests list (no .entries on a sealed interface)"
      pattern: "knownNavDests"
---

<objective>
Stand up the navigation-spine foundation: add the Navigation-Compose dependency, introduce the
type-safe `NavDest` route model (retiring the `Dest` enum), introduce the data-driven `HomeAction`
idle-list model, and write the RED host-test scaffolds the migration plans will turn GREEN.

This is pure model + tooling work. It touches the `Dest` enum's ~204 call sites (mechanical rename)
but introduces NO NavHost and NO UI change yet — the `when(dest)` shell still runs, now keyed on
`NavDest` objects. Splitting the rename out de-risks the big AppShell migration in 24-02.

Purpose: Everything downstream (the NavHost migration in 24-02, the morphing root in 24-03) consumes
`NavDest`, `HomeAction`, `buildIdleActions`, and the nav-testing artifact. Land them first, behind a
green build, so 24-02/24-03 build against a stable contract instead of a moving one.

Output: navigation-compose 2.8.9 pinned; `NavDest.kt`; `HomeAction.kt`; `Dest`→`NavDest` mechanically
applied; `parseStartDest` fixed for sealed objects; RED scaffolds for `HomeActionTest` + `PopToRootTest`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-navigation-spine/24-CONTEXT.md
@.planning/phases/24-navigation-spine/24-RESEARCH.md
@.planning/phases/24-navigation-spine/24-PATTERNS.md
@.planning/phases/24-navigation-spine/24-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add navigation-compose 2.8.9 + navigation-testing dependency</name>
  <files>gradle/libs.versions.toml, app/build.gradle.kts</files>
  <read_first>
    - gradle/libs.versions.toml (the `[versions]` + `[libraries]` block style; `composeBom = "2026.05.00"` pin; `kotlinxSerialization = "1.7.3"`; `kotlin-serialization` plugin already declared at line 165)
    - app/build.gradle.kts (the `dependencies { implementation(libs.androidx.…) }` block, lines 110-170; the `testImplementation` block)
    - 24-RESEARCH.md § Standard Stack + § Installation (the exact 2.8.9 pin rationale: NOT 2.9.x, NOT BOM-governed, minSdk 21, no compileSdk-37/AGP-9 floor)
  </read_first>
  <action>
    In `gradle/libs.versions.toml` add a `[versions]` entry `navigation = "2.8.9"` (with a comment noting:
    navigation-compose 2.8.x line — minSdk 21, NOT BOM-governed, pinned 2.8 per the AGP-8.7/compileSdk-36
    conservative stance, NOT 2.9.x). Add two `[libraries]` entries:
    `androidx-navigation-compose = { group = "androidx.navigation", name = "navigation-compose", version.ref = "navigation" }`
    and `androidx-navigation-testing = { group = "androidx.navigation", name = "navigation-testing", version.ref = "navigation" }`.
    In `app/build.gradle.kts` add `implementation(libs.androidx.navigation.compose)` alongside the other
    `androidx` implementation lines, and `testImplementation(libs.androidx.navigation.testing)` in the test
    deps block. Do NOT add the `kotlin-serialization` plugin — it is already declared. Do NOT change the
    Compose BOM, compileSdk, or AGP.
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:dependencies --configuration debugRuntimeClasspath --no-daemon" 2>&1 | tr -d '\r' | grep -E "navigation-compose:2.8.9|navigation:2.8.9"</automated>
  </verify>
  <acceptance_criteria>
    - `gradle/libs.versions.toml` contains `navigation = "2.8.9"` and both `androidx-navigation-compose` and `androidx-navigation-testing` library entries.
    - `app/build.gradle.kts` contains `implementation(libs.androidx.navigation.compose)` and `testImplementation(libs.androidx.navigation.testing)`.
    - The dependency resolves: the gw.bat `:app:dependencies` output shows `navigation-compose:2.8.9` on the debug runtime classpath (grep exits 0).
    - Compose BOM pin (`composeBom = "2026.05.00"`), compileSdk, and AGP lines are unchanged (`git diff` shows no edits to those lines).
  </acceptance_criteria>
  <done>navigation-compose:2.8.9 and navigation-testing:2.8.9 resolve; no BOM/compileSdk/AGP drift.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create NavDest route model + HomeAction model + RED test scaffolds</name>
  <files>app/src/main/java/works/mees/dinghy/ui/route/NavDest.kt, app/src/main/java/works/mees/dinghy/ui/route/HomeAction.kt, app/src/test/java/works/mees/dinghy/ui/route/HomeActionTest.kt, app/src/test/java/works/mees/dinghy/ui/route/PopToRootTest.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/route/TopRoute.kt (the `sealed interface` + `data object` pattern; the `Dest` enum at line 37 — the 17 destination names to carry over)
    - app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusUiModel.kt lines 115-134 (`standbyLauncherDests` — the EXISTING capability-gated idle builder; the direct analog for `buildIdleActions`, but the NEW D-06 order/membership differs)
    - app/src/main/java/works/mees/dinghy/designsystem/icons/DinghyIcons.kt lines 46-62, 89-98 (the `Launcher*` tokens + `OutputSection`; the icon tokens available for the idle rows)
    - app/src/test/java/works/mees/dinghy/ui/route/TopRouteTest.kt (the pure-JUnit4 test style to copy)
    - 24-RESEARCH.md § HomeAction Typed List Model; 24-PATTERNS.md § ui/route/HomeAction.kt
  </read_first>
  <behavior>
    - buildIdleActions(spoolman=T, bookmarks=T, outputs=T, webcam=T) → 8 rows in D-06 order: Spool, Files, Move, Extrude, Macros, Calibration, Outputs, Webcam.
    - spoolman=F → Spool row absent (D-08); list compacts (Files is first).
    - bookmarks=F → Macros row absent (D-08).
    - outputs=F → Outputs row absent (D-08).
    - webcam=F → Webcam row absent (D-08).
    - all-false → 4 rows: Files, Move, Extrude, Calibration (the always-present set; D-07 keeps Temperature/Console OFF).
    - Every HomeAction.Destination carries a NavDest, a @StringRes label, and a DinghyIcons IconRef token (never a raw ligature string).
  </behavior>
  <action>
    Create `NavDest.kt`: a `@Serializable sealed interface NavDest` with `@Serializable data object`
    members for all 17 destinations from the `Dest` enum (WaterfallHome [renamed from PrintStatus],
    Temperature, Move, Extrude, Files, Macros, Console, Calibration, FineTune, Webcam, Spool, Outputs,
    SystemInfo, Devices, Theme, Settings, About). Add a top-level `val knownNavDests: List<NavDest>`
    enumerating all members (sealed interfaces have no `.entries`; this list is the parse/iteration source
    for StartDestMapping in Task 3).

    Create `HomeAction.kt` per the PATTERNS shape: `sealed interface HomeAction` with
    `data class Destination(val dest: NavDest, @StringRes val labelRes: Int, val icon: works.mees.dinghy.designsystem.icons.DinghyIcon)`
    and `data object OpenDrawer`. Add a KDoc comment marking the v2 `InlineControl` extension point (D-05)
    but do NOT implement it. Add `fun buildIdleActions(spoolmanPresent, bookmarksExist, outputsPresent,
    webcamEnabled): List<HomeAction>` building the D-06 order with the D-08 capability gates, using
    `DinghyIcons.LauncherSpool/LauncherFiles/LauncherMove/LauncherExtrude/LauncherMacros/LauncherCalibration/OutputSection`
    for the seven mapped rows. For the WEBCAM row icon use a NEW token `DinghyIcons.LauncherWebcam` —
    this token is added in 24-02-PLAN.md Task 1 (the owner-confirmed glyph). To keep THIS plan compiling
    standalone, reference `DinghyIcons.LauncherWebcam` only if it already exists; otherwise leave the
    Webcam row's icon as a `TODO(24-02)` compile-safe placeholder using `DinghyIcons.LauncherDrawer` and
    a `// PLACEHOLDER(24-02): owner-confirmed Webcam glyph wired in 24-02 Task 1` comment. (24-02 replaces it.)

    Create `HomeActionTest.kt` (FULL assertions, not RED — this seam is pure and buildable now) covering the
    eight behavior cases above plus a `v1OrderIsPreserved()` test asserting the exact D-06 destination order.

    Create `PopToRootTest.kt` as a RED scaffold (per [[dinghy-wave0-red-scaffold-compile.md]]): the test
    methods must COMPILE against the whole test source set but assert `fail("pending 24-02 NavHost")` bodies
    — do NOT reference `TestNavController` or any NavHost symbol yet (the NavHost does not exist until 24-02),
    so the scaffold has no unbuilt-symbol references. 24-02 replaces the fail() bodies with real
    TestNavController pop assertions.
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --tests works.mees.dinghy.ui.route.HomeActionTest --no-daemon" 2>&1 | tr -d '\r' | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `NavDest.kt` defines `@Serializable sealed interface NavDest` with 17 `@Serializable data object` members and a `val knownNavDests: List<NavDest>`.
    - `HomeAction.kt` defines the sealed `HomeAction` (`Destination` + `OpenDrawer`) and `buildIdleActions(...)`; grep `grep -c "@Serializable" NavDest.kt` returns ≥ 18 (interface + 17 objects).
    - `HomeActionTest` GREEN: all eight capability cases + `v1OrderIsPreserved` pass (D-06 order: Spool, Files, Move, Extrude, Macros, Calibration, Outputs, Webcam).
    - `PopToRootTest` compiles (the whole test source set compiles) with `fail(...)` bodies and no reference to any not-yet-built NavHost symbol.
    - Every `HomeAction.Destination` icon argument is a `DinghyIcons.*` token (grep finds no raw `IconRef.Ligature("` inside `buildIdleActions`).
  </acceptance_criteria>
  <done>NavDest + HomeAction models exist; HomeActionTest is GREEN; PopToRootTest is a compiling RED scaffold.</done>
</task>

<task type="auto">
  <name>Task 3: Mechanical Dest→NavDest rename across all call sites + fix parseStartDest</name>
  <files>app/src/main/java/works/mees/dinghy/ui/route/TopRoute.kt, app/src/main/java/works/mees/dinghy/ui/shell/StartDestMapping.kt, app/src/main/java/works/mees/dinghy/ui/shell/ShellNavState.kt, app/src/main/java/works/mees/dinghy/ui/shell/AppShell.kt, app/src/main/java/works/mees/dinghy/ui/shell/AppDrawer.kt, app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusScreen.kt, app/src/test/java/works/mees/dinghy/ui/route/TopRouteTest.kt, app/src/test/java/works/mees/dinghy/shell/StartDestMappingTest.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/route/TopRoute.kt (the `Dest` enum + `TopRoute.Shell(val dest: Dest)` + `derive()` ending in `TopRoute.Shell(Dest.PrintStatus)`)
    - app/src/main/java/works/mees/dinghy/ui/shell/StartDestMapping.kt (the `Dest.entries.firstOrNull { it.name == name }` body — sealed objects have no `.entries`)
    - 24-PATTERNS.md § Migration Critical Paths (the ~93/204 call-site enumeration + the `TopRoute.Shell` payload-drop note)
    - the 17 files containing `Dest.` (run `grep -rln "Dest\." app/src/main/java app/src/test/java` first)
  </read_first>
  <action>
    Delete the `Dest` enum from `TopRoute.kt` (it now lives as `NavDest` from Task 2). Change
    `TopRoute.Shell(val dest: Dest)` to `data object Shell : TopRoute` (drop the payload — the NavHost owns
    routing per D-01; the start destination is fixed at `WaterfallHome`). Update `derive()`'s final branch
    from `TopRoute.Shell(Dest.PrintStatus)` to `TopRoute.Shell`. Mechanically replace every remaining
    `Dest.` reference with `NavDest.` across all 17 source+test files, and `Dest.PrintStatus` → `NavDest.WaterfallHome`.
    In `StartDestMapping.kt` replace `Dest.entries.firstOrNull { it.name == name }` with
    `knownNavDests.firstOrNull { it::class.simpleName == name }` (sealed objects expose their name via the
    class simpleName; keep the total/never-throws contract — null/blank/unknown → null per the V5 mitigation).
    Update `StartDestMappingTest` + `TopRouteTest` for the new `NavDest` cases and the payload-free `TopRoute.Shell`.
    Add `import kotlinx.serialization.Serializable` where needed. This task does NOT introduce a NavHost —
    `AppShell` still uses `when(dest)` (now `when(nav.dest)` keyed on `NavDest`); the NavHost swap is 24-02.
    Keep `ShellNavState.dest`/`backStack`/`navigateTo`/`goBack` as-is this plan (24-02 slims them down).
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:compileDebugKotlin :app:compileDebugUnitTestKotlin --no-daemon" 2>&1 | tr -d '\r' | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "\bDest\b" app/src/main/java app/src/test/java` returns zero hits for the old enum type (only `NavDest` remains; `import ...route.Dest` is gone).
    - `TopRoute.Shell` is a `data object` (no `dest` payload); `derive()` returns `TopRoute.Shell`.
    - `StartDestMapping.parseStartDest` returns a `NavDest?` using `knownNavDests` (no `.entries` call); `StartDestMappingTest` GREEN.
    - `:app:compileDebugKotlin` AND `:app:compileDebugUnitTestKotlin` both BUILD SUCCESSFUL (whole main + test source set compiles).
    - `TopRouteTest` GREEN against the payload-free `TopRoute.Shell`.
  </acceptance_criteria>
  <done>Dest enum fully retired to NavDest; app + tests compile; parseStartDest is sealed-safe; no NavHost yet.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `am start --es start_dest` → MainActivity | Untrusted intent extra crosses the exported-Activity boundary (existing surface; `parseStartDest`) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-01-01 | Tampering/DoS | `parseStartDest(raw)` | mitigate | Keep the total/never-throws parse on the `knownNavDests` rename (null/blank/unknown → null); preserved verbatim from the existing V5/T-18-04-02 mitigation. Dev-gate remains the first line of defense. |
| T-24-01-SC | Tampering | navigation-compose/navigation-testing install | mitigate | AndroidX first-party artifacts from Google Maven (`dl.google.com`), provenance beyond question per RESEARCH § Package Legitimacy Audit; not npm-installed, no slopcheck applicable. No `[ASSUMED]`/`[SUS]` packages. |

This plan is a local-LAN nav model refactor: **no new network surface, no new auth, no new data handling.**
The only crossing is the pre-existing untrusted `start_dest` extra, whose mitigation is carried unchanged.
Block-on severity is `high`; there are none.
</threat_model>

<verification>
- `:app:compileDebugKotlin` + `:app:compileDebugUnitTestKotlin` BUILD SUCCESSFUL (whole tree compiles post-rename).
- `HomeActionTest` GREEN (D-06 order + D-08 hides). `TopRouteTest` + `StartDestMappingTest` GREEN.
- `PopToRootTest` compiles as a RED scaffold (no unbuilt-symbol references).
- navigation-compose:2.8.9 resolves on the debug runtime classpath; no BOM/compileSdk/AGP drift.
</verification>

<success_criteria>
SC-2 (foundation): the type-safe `NavDest` route model and the nav-compose dependency are in place,
the `Dest` enum is retired, and the data-driven `buildIdleActions` model + its tests are GREEN — the
contract 24-02 (NavHost migration) and 24-03 (morphing root) build against.
</success_criteria>

<output>
Create `.planning/phases/24-navigation-spine/24-01-SUMMARY.md` when done.
</output>
