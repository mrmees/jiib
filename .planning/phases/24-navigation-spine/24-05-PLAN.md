---
phase: 24-navigation-spine
plan: 05
type: execute
wave: 3
depends_on: [24-03, 24-04]
files_modified:
  - .planning/phases/24-navigation-spine/24-UAT.md
autonomous: false
requirements: [SC-1, SC-2, SC-3, SC-4, SC-5]
must_haves:
  truths:
    - "The full host unit suite is GREEN on the merged Wave-0..2 tree before the device gate"
    - "On flox: the morph holds frame budget (no frozen frames) idle↔printing↔terminal; owner approves the morphing root in BOTH orientations"
    - "On flox: the floating e-stop appears AND is FUNCTIONAL on a DRILL-DOWN screen (e.g. Move) while printing — visible top-left of Focus, tap opens the full-screen Stop Confirm guard — and is ABSENT when idle (FIX-1)"
    - "On flox: the System foot button opens the App Drawer; PrintStatus-as-root introduces no print-monitoring regression"
    - "On flox: a recovery-Splash blip returns the user to WaterfallHome AND resets in-screen sub-nav to its hub (owner-accepted FIX-3 regression — expected, not a bug; sub-nav does NOT survive)"
  artifacts:
    - path: ".planning/phases/24-navigation-spine/24-UAT.md"
      provides: "On-device flox UAT results for the SC-1..SC-5 gates"
  key_links:
    - from: "release/debug APK on flox"
      to: "SC-1..SC-5 owner approval"
      via: "on-device walk + gfxinfo framestats for the morph"
      pattern: "approved"
---

<objective>
The load-bearing on-device gate. The whole phase's visual/runtime claims — the morph frame budget, the
floating e-stop, both orientations, System-opens-drawer, and the no-print-monitoring-regression smoke —
are emulator-untrustworthy (Adreno-320 is the floor; [[dinghy-display-mock-vs-reality]]). This plan
builds a fresh APK, runs the full host suite on the merged tree, and gates the phase on the owner's
on-device flox walk.

⚠ Build hygiene: force a clean rebuild and verify the APK mtime is AFTER the last Wave-2 fix commit
before installing — Gradle UP-TO-DATE has shipped a STALE APK into a UAT before
([[dinghy-stale-apk-uat-gate]]). Suspect the build before re-diagnosing code.

Purpose: convert the host-green spine into an owner-verified, real-hardware-true phase completion.
Output: `24-UAT.md` with the SC-1..SC-5 device results recorded honestly (PASS / PARTIAL / deferred).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/24-navigation-spine/24-CONTEXT.md
@.planning/phases/24-navigation-spine/24-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full host suite GREEN + fresh APK build + 24-UAT.md scaffold</name>
  <files>.planning/phases/24-navigation-spine/24-UAT.md</files>
  <read_first>
    - .planning/phases/24-navigation-spine/24-VALIDATION.md (the Manual-Only on-device gates table — the exact behaviors to script into the UAT)
    - CLAUDE.md § Local Build Environment (gw.bat invocation; sign-release.bat for installing a release APK; flox = LineageOS 18.1/API 30, genuine Adreno 320)
    - memory notes [[dinghy-stale-apk-uat-gate]] + [[dinghy-display-ondevice-iteration]] (force-rebuild + verify mtime; owner navigates/eyeballs when present)
  </read_first>
  <action>
    Run the FULL host unit suite on the merged Wave-0..2 tree:
    `gw.bat :app:testDebugUnitTest --no-daemon` — must be GREEN before any device step. Force a clean
    rebuild of the APK (`--rerun-tasks` or `clean` the relevant assemble task) and confirm the produced
    APK file mtime is AFTER the last Wave-2 commit (stale-APK guard). Build the RELEASE APK
    (`:app:assembleRelease`) and zipalign+debug-sign it via `E:\Android\sign-release.bat` for on-device
    install (release is unsigned until PKG-01). Author `24-UAT.md` scaffolding the SC-1..SC-5 device checks
    from the 24-VALIDATION Manual-Only table: (1) morph frame budget via `adb shell dumpsys gfxinfo
    works.mees.dinghy framestats` around a print start/end, no frozen frames; (2) morphing-root
    owner-approval in portrait AND landscape; (3) floating e-stop printing-only, top-left, opens Confirm
    guard; (4) System foot button opens drawer + print-monitoring-unaffected smoke; (5) recovery-Splash →
    lands on WaterfallHome AND in-screen sub-nav resets to its hub (expected/accepted FIX-3 regression).
    Mark each check PENDING.
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --no-daemon" 2>&1 | tr -d '\r' | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - Full host suite `:app:testDebugUnitTest` BUILD SUCCESSFUL (zero failing tests) on the merged tree.
    - A freshly-built signed APK exists with mtime AFTER the last Wave-2 fix commit (record the path + mtime + commit hash in the SUMMARY).
    - `24-UAT.md` exists with the five SC-1..SC-5 device checks scaffolded and marked PENDING, each with concrete on-device steps (incl. the gfxinfo command for the morph).
  </acceptance_criteria>
  <done>Host suite green, a fresh non-stale signed APK is ready, and 24-UAT.md scaffolds the five device gates.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Owner on-device flox UAT — morph / e-stop / orientations / System / regression</name>
  <action>Install the fresh signed APK on flox against a live printer and have the owner run the six on-device checks below; record each SC-1..SC-5 verdict (PASS/PARTIAL/FAIL) in 24-UAT.md with the owner's words, the morph gfxinfo result, and the recovery-Splash land-on-root confirmation. Do not write the SUMMARY until the owner replies with a verdict.</action>
  <what-built>
    The navigation spine: PrintStatusScreen as the morphing waterfall root (idle/printing/terminal,
    one-shot ~150ms Crossfade, no gutter), a Navigation-Compose drill-down back-stack replacing the old
    when(dest) hub, the data-driven idle action list (Spool→File→Move→Extrude→Macros→Calibration→Outputs→
    Webcam with capability HIDE), the idle Preheat + neutral System foot bar (System opens the App Drawer),
    and the printing-only FloatingEStop hoisted to the AppShell overlay layer so it appears on EVERY screen
    (drill-downs included) top-left of the Focus into the shared full-screen Stop Confirm guard (FIX-1).
  </what-built>
  <how-to-verify>
    On flox (genuine Nexus 7 2013 / Adreno 320 / LineageOS 18.1) with the freshly-installed signed APK,
    pointed at a live printer (E5+ 192.168.1.120:7125 or E3 192.168.1.121:7125):
    1. IDLE root: confirm the morphing root in PORTRAIT then LANDSCAPE — the idle action list shows the
       D-06 rows present for this printer's capabilities (Spool only if Spoolman, Webcam only if a cam,
       Macros only if bookmarks, Outputs only if controllable outputs); no Temperature/Console rows.
    2. MORPH: start a print, then end/cancel it — the root cross-fades idle↔printing↔terminal smoothly;
       capture `dumpsys gfxinfo works.mees.dinghy framestats` and confirm NO frozen frames. (If it janks,
       report it — the documented fallback is a hard-cut.)
    3. E-STOP (FIX-1 — on drill-down, not just root): while printing, drill into Move (and a couple more
       screens) — confirm the red floating e-stop is visible top-left on EACH drill-down screen, and tapping
       it on Move opens the FULL-SCREEN Stop Confirm guard (not a dialog/hold) AND a confirm issues the
       emergency stop. Confirm the e-stop is ABSENT when idle. (This is the key FIX-1 check: the e-stop must
       work on Move/Extrude/Console/etc. while printing, not only on the morphing root.)
    4. SYSTEM: tap the System foot button → the App Drawer opens (neutral, not a power action).
    5. NAV + REGRESSION: drill into Files/Move/Calibration etc. and Back out — the back-stack works; drive
       a print and confirm monitoring (progress/temps/stats) is unaffected by PrintStatus-as-root.
    6. RECOVERY SPLASH (expected FIX-3 regression): while deep in a drill-down (e.g. an open Calibration
       routine page or a FineTune group), force a reconnect blip (toggle WiFi / power-cycle the printer link)
       — on return the app lands on WaterfallHome (the morphing root), NOT the prior drill-down, AND the
       in-screen sub-nav has RESET to its hub (the Calibration routine / FineTune group is cleared by
       applyEntryReset on re-entry). This is the owner-accepted FIX-3 regression — confirm BOTH the
       land-on-root and the sub-nav reset behave as expected; neither is a bug.
  </how-to-verify>
  <resume-signal>Type "approved" (record any PARTIAL/deferred items + the morph fallback decision), or describe issues.</resume-signal>
  <verify><human-check>Owner ran the six flox checks against a live printer and replied with an explicit SC-1..SC-5 verdict.</human-check></verify>
  <done>Owner has verified the spine on flox and 24-UAT.md records each SC gate's verdict; the recovery-Splash land-on-root is logged as expected.</done>
  <acceptance_criteria>
    - Owner has run the six checks on flox against a live printer and replied with an explicit verdict.
    - 24-UAT.md records each SC-1..SC-5 check as PASS / PARTIAL / FAIL with the owner's words and (for the morph) the gfxinfo frozen-frame result + any hard-cut fallback decision.
    - The recovery-Splash check is recorded as the EXPECTED land-on-root + sub-nav-reset behavior (FIX-3, not logged as a failure).
    - Any non-blocking polish or deferred item is captured as a pending todo, not silently dropped.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | On-device verification only; exercises the existing local-Moonraker integration |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-05-01 | (N/A) | on-device UAT | accept | Verification activity only — no new attack surface. The e-stop's Confirm-guard safety property is exercised (not introduced) here. |

This plan is a build + on-device verification gate: **no new network surface, auth, or data handling.**
Block-on severity is `high`; there are none.
</threat_model>

<verification>
- Full host suite GREEN on the merged tree.
- Fresh non-stale signed APK installed on flox.
- Owner on-device verdict recorded in 24-UAT.md for all five SC gates (PASS/PARTIAL/deferred), with the
  morph gfxinfo result + the recovery-Splash land-on-root confirmation.
</verification>

<success_criteria>
SC-1..SC-5 confirmed on the real hardware floor: the morphing root (both orientations), the working
Nav-Compose back-stack, the gutter-gone rehoming, the printing-only floating e-stop, and the
System-page-via-drawer with no print-monitoring regression — owner-approved on flox.
</success_criteria>

<output>
Create `.planning/phases/24-navigation-spine/24-05-SUMMARY.md` when done. Results live in 24-UAT.md.
</output>
