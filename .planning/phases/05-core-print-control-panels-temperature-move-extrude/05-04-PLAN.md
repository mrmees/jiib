---
phase: 05-core-print-control-panels-temperature-move-extrude
plan: 04
type: execute
wave: 2
depends_on: ["05-02"]
files_modified:
  - app/src/main/java/works/mees/dinghy/render/GraphView.kt
  - app/src/main/java/works/mees/dinghy/render/GraphViewHost.kt
  - app/src/test/java/works/mees/dinghy/render/GraphDownsampleTest.kt
autonomous: true
requirements: [TEMP-04]
must_haves:
  truths:
    - "GraphView draws N traces (one pre-allocated Path + Paint per drawn sensor) on one shared axis, keeping the allocation-free onDraw discipline."
    - "GraphView uses a FIXED/stable shared Y-range across all traces — NOT per-frame window min/max (the Phase-4 G-1 fix)."
    - "GraphViewHost passes N per-sensor snapshots + per-sensor token colors (heat/accent/violet) into the same View instance; a theme flip recolors all traces with no recreation."
    - "A single-trace call still renders correctly (backward compatible with the Phase-4 Print Status sparkline)."
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/render/GraphView.kt"
      provides: "N-trace allocation-free render with fixed Y-range"
      contains: "Array<Path>"
    - path: "app/src/main/java/works/mees/dinghy/render/GraphViewHost.kt"
      provides: "multi-snapshot + multi-color host"
      contains: "GraphViewHost"
  key_links:
    - from: "render/GraphViewHost.kt"
      to: "render/GraphView.kt"
      via: "per-sensor snapshots + token-derived colors pushed in update block"
      pattern: "applyTokens|setData"
---

<objective>
EXTEND the single-trace `GraphView` in place to N traces with a fixed shared Y-range — the only
genuinely new render work in the phase (D-05) and the architectural fix for Phase-4 gap G-1. Do NOT
fork it (CONTEXT D-05 / RESEARCH anti-pattern): the allocation-free contract and the perf gate stay
anchored to the ONE primitive.

Work (PATTERNS "render/GraphView.kt MODIFY", RESEARCH Pattern 3 / Pitfall 4):
- Generalize the single `linePath`/`linePaint` to `Array<Path>`/`Array<Paint>`, one per drawn sensor,
  pre-allocated; keep `rewind()`-per-draw, never allocate in `onDraw`.
- Replace the per-frame window min/max Y auto-range (current lines 116-125) with a FIXED shared
  Y-range (e.g. 0..300 °C, or a stable rounded range) so steady temps no longer noise-fill the panel
  (G-1 fix). One shared X time window across traces.
- `applyTokens` sets each trace's color from tokens: nozzle=`t.heat`, bed=`t.accent`, chamber=`t.violet`
  (added 05-02). No raw hex (THEME-01). Per Pitfall 4, draw the area-fill for the primary trace only
  (or none) to bound fill cost on the Adreno 320 — but this is a perf lever, MEASURE in 05-08.
- `GraphViewHost` widens to accept N per-sensor snapshots + the token colors and pushes them into the
  same instance (`factory` once); a theme flip recolors all traces with no recreation.

Keep the existing single-trace entry path working (Print Status sparkline still calls it).

Purpose: the multi-trace history graph the Temperature panel (05-05) hosts, with the G-1 fixed-range fix.
Output: N-trace GraphView + multi-snapshot host, with extended downsample/sanitize tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-core-print-control-panels-temperature-move-extrude/05-RESEARCH.md
@.planning/phases/05-core-print-control-panels-temperature-move-extrude/05-PATTERNS.md
@.planning/phases/03-design-system-theming-foundation/03-PERF-RESULTS.md
@.planning/phases/04-service-shell-settings-print-status-home/04-HUMAN-UAT.md
@docs/ui_design/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generalize GraphView to N pre-allocated traces + fixed Y-range</name>
  <read_first>
    - app/src/main/java/works/mees/dinghy/render/GraphView.kt (the WHOLE file — single linePath/areaPath/linePaint/fillPaint lines 40-57; applyTokens lines 85-92; onDraw lines 106-156 incl. the window min/max to REPLACE lines 116-125; sanitize lines 183-212 to apply per-trace)
    - app/src/main/java/works/mees/dinghy/render/RingBuffer.kt (snapshot order oldest→newest)
    - .planning/phases/05-core-print-control-panels-temperature-move-extrude/05-RESEARCH.md Pattern 3 + Pitfall 4
    - .planning/phases/04-service-shell-settings-print-status-home/04-HUMAN-UAT.md (gap G-1 this fixes: auto-range noise / lone-dot)
    - docs/ui_design/images/09-temperature-graph.png + README §9 (the locked graph look: multi-sensor on one axis, legend colors)
  </read_first>
  <action>
    Replace the single path/paint fields with pre-allocated arrays sized to a small max trace count (e.g. MAX_TRACES=3 — nozzle/bed/chamber): `linePaths: Array<Path>`, `linePaints: Array<Paint>` (each a STROKE paint configured like the current linePaint), plus ONE optional fill path/paint for the primary trace only. Add a `setData(series: List<FloatArray>)` overload (sanitize EACH series once via the existing `sanitize`, store the bounded working copies) while KEEPING the existing single `setData(FloatArray)` delegating to the list form with one element (back-compat for the Print Status sparkline). Replace the window-min/max block with a FIXED shared Y-range: introduce `var yRange: ClosedFloatingPointRange<Float> = 0f..300f` (settable; default sensible for °C) and map all traces against it in `onDraw` (guard a degenerate range). `onDraw` loops traces: rewind each pre-allocated path, build it from its bounded series against the shared X (n-1 step) and shared Y-range, draw stroke; draw the single area-fill only for trace index 0 when `drawArea`. NO per-frame allocation (Pitfall 4). Extend `applyTokens` to set `linePaints[0]=t.heat`, `[1]=t.accent`, `[2]=t.violet` (toArgb), fill from trace-0 color at FILL_ALPHA. Keep `drawArea` as the perf isolation lever.
  </action>
  <verify>
    <automated>cmd.exe /c "E:\Android\gw.bat :app:testReleaseUnitTest --tests *GraphDownsampleTest --no-daemon"</automated>
  </verify>
  <acceptance_criteria>
    - `GraphView.kt` contains `Array<Path>` and `Array<Paint>` (pre-allocated, not allocated in onDraw).
    - `GraphView.kt` contains a fixed `yRange` field used in the y-mapping; the old per-frame `minV`/`maxV` window scan is gone (grep for `var minV` over non-comment lines returns 0 in onDraw).
    - `GraphView.kt` has both `fun setData(snapshot: FloatArray)` and `fun setData(series: List<FloatArray>)`; the single-arg form delegates to the list form.
    - `applyTokens` sets per-trace colors from `t.heat`, `t.accent`, `t.violet` — `grep "Color(0x" GraphView.kt` returns nothing.
    - Extended GraphDownsampleTest still proves sanitize cap/filter/empty/constant per series; `:app:testReleaseUnitTest` for it passes.
  </acceptance_criteria>
  <done>N traces render allocation-free against a fixed shared Y-range; single-trace path back-compatible; sanitize tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Widen GraphViewHost to N snapshots + token trace colors</name>
  <read_first>
    - app/src/main/java/works/mees/dinghy/render/GraphViewHost.kt (the WHOLE file — factory-once/update-pushes-tokens-and-data seam lines 27-43)
    - app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusScreen.kt (the existing single-snapshot call site lines 131-137 — must keep compiling)
    - app/src/main/java/works/mees/dinghy/render/GraphView.kt (the new setData(List) + applyTokens from Task 1)
  </read_first>
  <action>
    Add a `GraphViewHost` overload (or widen with a default) taking `series: List<FloatArray>` alongside `tokens` and the existing `drawArea`/`modifier`; the `update` block calls `view.applyTokens(tokens)` then `view.setData(series)` into the single factory-created instance (no recreation on theme/data change). KEEP the existing single-`snapshot: FloatArray` overload so PrintStatusScreen's call (line 131) still compiles unchanged — it delegates to `listOf(snapshot)`. Optionally accept an explicit `yRange` param to pass through to the view. Token colors are derived inside the view's `applyTokens` (Task 1), so the host just forwards `tokens`.
  </action>
  <verify>
    <automated>cmd.exe /c "E:\Android\gw.bat :app:compileReleaseKotlin --no-daemon" 2>&1 | tr -d '\r' | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `GraphViewHost.kt` has both a `snapshot: FloatArray` and a `series: List<FloatArray>` form.
    - The existing PrintStatusScreen call site compiles unchanged (single-snapshot overload retained).
    - `:app:compileReleaseKotlin` succeeds.
  </acceptance_criteria>
  <done>Host forwards N snapshots + tokens to one View instance; single-snapshot back-compat retained; compiles.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Render-only extension; no network/input boundary added. The data is already-sanitized in-process float arrays. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04-D | DoS | per-frame allocation / unbounded vertices in onDraw (jank → frozen frames on Adreno 320) | mitigate | Pre-allocated arrays, `rewind()` per draw, `sanitize` caps each series to pixel width ONCE in setData — onDraw stays allocation-free and bounded. The D-06 perf gate (05-08) re-measures this on real flox. |
</threat_model>

<verification>
Release compile + GraphDownsampleTest green. The multi-trace fill-rate on the real Adreno 320 is the
D-06 MANDATE — re-measured against the two-part gate on the FULL Temperature screen in 05-08 (do NOT
grandfather the Phase-3 isolated 50.1 ms number).
</verification>

<success_criteria>
- GraphView draws N traces allocation-free against a fixed shared Y-range (G-1 fixed).
- Per-trace colors come from heat/accent/violet tokens (no raw hex).
- Single-trace Print Status sparkline path still works.
</success_criteria>

<output>
Create `.planning/phases/05-core-print-control-panels-temperature-move-extrude/05-04-SUMMARY.md` when done.
</output>
