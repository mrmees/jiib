---
phase: 05-core-print-control-panels-temperature-move-extrude
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/main/java/works/mees/dinghy/state/PrinterState.kt
  - app/src/main/java/works/mees/dinghy/state/PrinterStateReducer.kt
  - app/src/main/java/works/mees/dinghy/state/Capabilities.kt
  - app/src/main/java/works/mees/dinghy/state/DeriveCapabilities.kt
  - app/src/main/java/works/mees/dinghy/net/JsonRpc.kt
  - app/src/test/java/works/mees/dinghy/state/PrinterStateReducerTest.kt
  - app/src/test/java/works/mees/dinghy/state/DeriveCapabilitiesTest.kt
autonomous: true
requirements: [MOVE-04, EXTR-02, EXTR-04]
must_haves:
  truths:
    - "The reducer captures gcode_move.gcode_position into PrinterState (MOVE-04 source)."
    - "The reducer captures extruder.can_extrude per extruder into PrinterState (EXTR-04 gate)."
    - "Capabilities can answer hasMacroIgnoreCase(\"LOAD_FILAMENT\") for a lowercased Moonraker macro name (EXTR-02 / Pitfall 2)."
    - "JsonRpcMethods exposes GCODE_SCRIPT and TEMPERATURE_STORE constants."
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/state/PrinterState.kt"
      provides: "gcodePosition + canExtrude fields"
      contains: "gcodePosition"
    - path: "app/src/main/java/works/mees/dinghy/state/Capabilities.kt"
      provides: "hasMacroIgnoreCase helper"
      contains: "hasMacroIgnoreCase"
    - path: "app/src/main/java/works/mees/dinghy/net/JsonRpc.kt"
      provides: "GCODE_SCRIPT + TEMPERATURE_STORE method constants"
      contains: "TEMPERATURE_STORE"
  key_links:
    - from: "state/PrinterStateReducer.kt"
      to: "state/PrinterState.kt"
      via: "gcode_move.gcode_position → gcodePosition copy"
      pattern: "gcode_position"
    - from: "state/PrinterStateReducer.kt"
      to: "state/PrinterState.kt"
      via: "extruder.can_extrude → canExtrude capture"
      pattern: "can_extrude"
---

<objective>
Extend the PURE state layer with the four new live/derived fields every Phase-5 panel reads, and
register the two new JSON-RPC method names. This is the data contract for the whole phase — built
first so every downstream holder/screen codes against settled fields. No UI, no I/O — pure reducer
and capability additions, unit-tested in the Phase-2 discipline.

New fields (RESEARCH §2/§3, Open Q3; PATTERNS "State-model additions"):
- `PrinterState.gcodePosition: List<Double>?` from `gcode_move.gcode_position` (MOVE-04 user-facing X/Y/Z — NOT toolheadPosition, Pitfall 1).
- `can_extrude` per extruder — captured into the heater/extruder model (EXTR-04 cold-extrude gate).
- `Capabilities.hasMacroIgnoreCase(name)` helper — case-insensitive macro presence (EXTR-02 / D-10, Pitfall 2: Moonraker lowercases macro object names).
- `JsonRpcMethods.GCODE_SCRIPT` ("printer.gcode.script") and `TEMPERATURE_STORE` ("server.temperature_store").

Purpose: settle the shared spine contract before any panel consumes it.
Output: extended PrinterState/Capabilities/reducer/derivation + method constants + unit tests proving each transition.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-core-print-control-panels-temperature-move-extrude/05-RESEARCH.md
@.planning/phases/05-core-print-control-panels-temperature-move-extrude/05-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Capture gcode_position + can_extrude in PrinterState/reducer</name>
  <read_first>
    - app/src/main/java/works/mees/dinghy/state/PrinterState.kt (the immutable contract; toolheadPosition line 40, homedAxes line 42, HeaterState lines 68-72 are the analogs)
    - app/src/main/java/works/mees/dinghy/state/PrinterStateReducer.kt (applyStatus walker lines 64-118; the gcode_move block lines 92-95 and heater merge loop lines 102-112 are where the new reads go; null-safe accessors lines 146-155)
    - app/src/test/java/works/mees/dinghy/state/PrinterStateReducerTest.kt (the existing reducer test harness to extend)
    - .planning/phases/05-core-print-control-panels-temperature-move-extrude/05-RESEARCH.md §3 (gcode_position semantics) + §2 (can_extrude) + Pitfall 1/3
  </read_first>
  <action>
    Add `gcodePosition: List<Double>? = null` to PrinterState (KDoc: "gcode_move.gcode_position [X,Y,Z,E], offsets-stripped user-facing source per MOVE-04 / RESEARCH Pitfall 1 — NOT toolheadPosition"). In PrinterStateReducer.applyStatus, inside the existing `gcode_move` block, add `gm.doubleListOrNull("gcode_position")?.let { s = s.copy(gcodePosition = it) }` reusing the existing private `doubleListOrNull` accessor.
    For can_extrude (EXTR-04, D-07): extend HeaterState with `canExtrude: Boolean = false` (KDoc: "extruder objects only; true = current temp ≥ min_extrude_temp; non-extruder heaters leave default false"). In the heater merge loop, when the object carries a `can_extrude` field, capture it via a new `booleanOrNull` private accessor mirroring `doubleOrNullAt` (use `?.jsonPrimitive?.booleanOrNull` from kotlinx.serialization.json, wrapped in runCatching). Merge it field-by-field onto the retained HeaterState (`canExtrude = obj.booleanOrNull("can_extrude") ?: prev.canExtrude`) so a temp-only diff retains the prior flag (STATE-01 merge discipline). Keep every walk null-safe — never `!!` (house rule).
    Do NOT add gcode_move to the subscribe set — it is already there (DeriveCapabilities V1_SUBSCRIBE_CORE line 54) and `extruder` already requests all fields, so can_extrude rides the existing subscription.
  </action>
  <verify>
    <automated>cmd.exe /c "E:\Android\gw.bat :app:testReleaseUnitTest --tests *PrinterStateReducerTest --no-daemon"</automated>
  </verify>
  <acceptance_criteria>
    - `PrinterState.kt` contains `val gcodePosition: List<Double>?` and `HeaterState` contains `val canExtrude: Boolean`.
    - `PrinterStateReducer.kt` contains a `gcode_position` read in the gcode_move block and a `can_extrude` read in the heater merge.
    - New test: a snapshot with `gcode_move.gcode_position = [10.0, 20.0, 5.0, 0.0]` yields `state.gcodePosition == listOf(10.0,20.0,5.0,0.0)`.
    - New test: a snapshot with `extruder.can_extrude = true` yields `state.heaters["extruder"]!!.canExtrude == true`; the boundary case `can_extrude = false` yields `false`.
    - New test: a temp-only `extruder` diff (no can_extrude key) RETAINS the previously-set canExtrude (merge-not-replace, STATE-01).
    - `:app:testReleaseUnitTest` for PrinterStateReducerTest passes.
  </acceptance_criteria>
  <done>Reducer captures both new fields; merge/retain semantics proven; reducer test green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add hasMacroIgnoreCase helper + GCODE_SCRIPT/TEMPERATURE_STORE methods</name>
  <read_first>
    - app/src/main/java/works/mees/dinghy/state/Capabilities.kt (the data class; `macros` field line 20)
    - app/src/main/java/works/mees/dinghy/state/DeriveCapabilities.kt (deriveCapabilities lines 20-41; macro extraction line 21 — `removePrefix("gcode_macro ")`)
    - app/src/main/java/works/mees/dinghy/net/JsonRpc.kt (JsonRpcMethods object lines 93-117; grouping comments)
    - app/src/test/java/works/mees/dinghy/state/DeriveCapabilitiesTest.kt (test harness to extend)
    - .planning/phases/05-core-print-control-panels-temperature-move-extrude/05-RESEARCH.md §4 (macro lowercase) + §5 (method names) + Pitfall 2
  </read_first>
  <action>
    Add to Capabilities a member fun `hasMacroIgnoreCase(name: String): Boolean = macros.any { it.equals(name, ignoreCase = true) }` (KDoc: "EXTR-02/D-10 — Moonraker reports macro object names LOWERCASE (Pitfall 2); a case-sensitive == would miss `gcode_macro load_filament`"). Leave `deriveCapabilities` macro extraction unchanged (it keeps the verbatim name); the case-insensitive comparison is the new helper only.
    Add to JsonRpcMethods: `const val GCODE_SCRIPT = "printer.gcode.script"` (under the "Action requests" group comment — it runs ALL action gcodes per RESEARCH §5) and `const val TEMPERATURE_STORE = "server.temperature_store"` (under the "Requests" group — one-shot backfill, RESEARCH §1).
  </action>
  <verify>
    <automated>cmd.exe /c "E:\Android\gw.bat :app:testReleaseUnitTest --tests *DeriveCapabilitiesTest --no-daemon"</automated>
  </verify>
  <acceptance_criteria>
    - `Capabilities.kt` contains `fun hasMacroIgnoreCase(`.
    - `JsonRpc.kt` contains `GCODE_SCRIPT = "printer.gcode.script"` and `TEMPERATURE_STORE = "server.temperature_store"`.
    - New test: `deriveCapabilities(listOf("gcode_macro load_filament")).hasMacroIgnoreCase("LOAD_FILAMENT") == true`.
    - New test: `deriveCapabilities(emptyList()).hasMacroIgnoreCase("LOAD_FILAMENT") == false`.
    - `:app:testReleaseUnitTest` for DeriveCapabilitiesTest passes.
  </acceptance_criteria>
  <done>Case-insensitive macro helper proven against a lowercased name; both method constants present; derivation test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Moonraker socket → reducer | Untrusted-ish loose JSON crosses into PrinterState; every walk is null-safe (existing house rule). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01-T | Tampering | reducer parsing wire JSON (can_extrude/gcode_position) | mitigate | Null-safe accessors (`booleanOrNull`/`doubleListOrNull` in runCatching); a missing/garbage field is SKIPPED and the retained value kept — never `!!`, never fatal (existing reducer contract). |
| T-05-01-Safety | Safety | EXTR-04 cold-extrude gate data source | mitigate | `canExtrude` defaults `false` (fail-safe: a printer that never reports can_extrude reads as "cannot extrude", disabling the control — never accidentally enables a cold extrude). |
</threat_model>

<verification>
Run the full state suite: `cmd.exe /c "E:\Android\gw.bat :app:testReleaseUnitTest --no-daemon"` — all existing reducer/derivation/conflation/lifecycle tests still green plus the new cases. No new dependencies; no manifest change.
</verification>

<success_criteria>
- PrinterState carries `gcodePosition` and HeaterState carries `canExtrude`.
- Capabilities answers `hasMacroIgnoreCase` correctly for lowercased Moonraker macro names.
- JsonRpcMethods exposes GCODE_SCRIPT + TEMPERATURE_STORE.
- All four transitions are unit-proven; full state test suite green.
</success_criteria>

<output>
Create `.planning/phases/05-core-print-control-panels-temperature-move-extrude/05-01-SUMMARY.md` when done.
</output>
