---
phase: 05-core-print-control-panels-temperature-move-extrude
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - app/src/main/java/works/mees/dinghy/state/PrinterStateStore.kt
  - app/src/main/java/works/mees/dinghy/net/MoonrakerSession.kt
  - app/src/main/java/works/mees/dinghy/di/SpineHandle.kt
  - app/src/main/java/works/mees/dinghy/service/MoonrakerService.kt
  - app/src/main/java/works/mees/dinghy/state/TemperatureStore.kt
  - app/src/test/java/works/mees/dinghy/state/TemperatureStoreBackfillTest.kt
autonomous: true
requirements: [TEMP-04, EXTR-04]
must_haves:
  truths:
    - "On connect, the handshake performs a one-shot server.temperature_store request and seeds per-sensor history oldest→newest."
    - "The pure backfill mapper turns the store response into per-sensor float series in correct (oldest-first) order, mapping only drawn sensors by exact object name."
    - "min_extrude_temp and max_extrude_only_distance are queried once at handshake (configfile.settings.extruder.*) and stashed — NOT in the throttled hot path. min_extrude_temp reads the PRIMARY `extruder` (hint is single-extruder-accurate; the per-tool safety gate is the live can_extrude boolean, not this number)."
    - "The one-shot values (minExtrudeTemp, maxExtrudeDistance) and the initial backfill are exposed as StateFlows the holders OBSERVE — so 'graph full immediately on connect' and the real min-temp hint are driven by the data arriving, not by a later notify_status_update diff."
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/state/TemperatureStore.kt"
      provides: "pure temperature_store → per-sensor series mapper"
      contains: "fun parseTemperatureStore"
    - path: "app/src/main/java/works/mees/dinghy/di/SpineHandle.kt"
      provides: "minExtrudeTemp + maxExtrudeDistance + initial backfill exposed as StateFlows"
      contains: "minExtrudeTemp"
  key_links:
    - from: "net/MoonrakerSession.kt"
      to: "server.temperature_store"
      via: "one-shot rpc.request after subscribe in runHandshake"
      pattern: "TEMPERATURE_STORE"
    - from: "net/MoonrakerSession.kt"
      to: "configfile.settings.extruder.min_extrude_temp + max_extrude_only_distance"
      via: "one-shot configfile query at handshake"
      pattern: "min_extrude_temp|max_extrude_only_distance"
---

<objective>
Wire the two one-shot history/config reads into the connection handshake and expose their results on
the spine, plus a pure mapper for the backfill. This is the only NEW networking in the phase
(RESEARCH §1/§2) and the proper fix for the Phase-4 sparkline gap G-1.

Work (RESEARCH §1/§2, PATTERNS "History backfill"):
1. `state/TemperatureStore.kt` (NEW) — a PURE mapper `parseTemperatureStore(JsonObject, sensors)` →
   `Map<String, FloatArray>` (per-sensor `temperatures` arrays, index 0 = OLDEST, only the requested
   drawn sensors). Host-testable; mirrors the DeriveCapabilities pure-module shape.
2. `MoonrakerSession.runHandshake()` — after the existing subscribe seed, add a one-shot
   `rpc.request(TEMPERATURE_STORE)` (NOT subscribed) and a one-shot `objects.query` of `configfile` to
   read `configfile.settings.extruder.min_extrude_temp` AND `configfile.settings.extruder.max_extrude_only_distance`
   (both from the PRIMARY `extruder` — single-extruder-accurate; the per-tool safety gate stays the live
   `can_extrude` boolean). Store all on the store.
3. `PrinterStateStore` — expose `minExtrudeTemp`, `maxExtrudeDistance`, and the initial per-sensor backfill
   as **StateFlows** (`MutableStateFlow` set once at handshake, exposed read-only) — capability-like, NOT
   the throttled hot path. Holders OBSERVE these so they fire deterministically when the one-shot read
   lands, independent of any later printerState diff (review finding: don't make connect-time fullness
   depend on a status diff).
4. `SpineHandle` + `MoonrakerService.buildSpineAndLaunch` — surface the three StateFlows so 05-05
   (Temperature) and 05-07 (Extrude) collect them off the live handle.

Live points keep arriving via the existing notify_status_update stream — do NOT subscribe to the store.

Purpose: give the Temperature graph a full history on connect (G-1 fix) and the Extrude panel the real
min_extrude_temp hint number, both as one-shot reads.
Output: pure backfill mapper + handshake hooks + spine exposure, with a backfill unit test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-core-print-control-panels-temperature-move-extrude/05-RESEARCH.md
@.planning/phases/05-core-print-control-panels-temperature-move-extrude/05-PATTERNS.md
@.planning/phases/04-service-shell-settings-print-status-home/04-HUMAN-UAT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure parseTemperatureStore mapper</name>
  <read_first>
    - app/src/main/java/works/mees/dinghy/state/DeriveCapabilities.kt (the pure free-function module shape to mirror)
    - app/src/main/java/works/mees/dinghy/state/PrinterStateReducer.kt (the null-safe JsonObject walking idiom: objectOrNull/doubleListOrNull, lines 146-155)
    - app/src/main/java/works/mees/dinghy/render/GraphView.kt (RingBuffer/FloatArray snapshot order; sanitize lines 183-212 already host-tested by GraphDownsampleTest)
    - .planning/phases/05-core-print-control-panels-temperature-move-extrude/05-RESEARCH.md §1 (store shape, index 0 = OLDEST, 1200@1Hz, sensor-name alignment) + Code Examples "One-shot history backfill"
  </read_first>
  <action>
    Create `state/TemperatureStore.kt`, package `works.mees.dinghy.state`, pure module (KDoc: purity + "first history-endpoint use"). Provide `parseTemperatureStore(result: JsonObject, sensors: Set<String>): Map<String, FloatArray>` that, for each requested sensor name present as a top-level key, reads its `temperatures` JsonArray, maps to `Float` (null-safe, skip non-finite/garbage like the reducer), and returns the array in the SAME order received (index 0 = oldest → keep as-is; the RingBuffer pushes oldest→newest). Sensors not present in the response (or with no `temperatures` array) are omitted from the map (never fabricate). Walk the JSON null-safely — never `!!`. Reuse `MoonrakerJson` element accessors; do not introduce a second Json instance.
  </action>
  <verify>
    <automated>cmd.exe /c "E:\Android\gw.bat :app:testReleaseUnitTest --tests *TemperatureStoreBackfillTest --no-daemon"</automated>
  </verify>
  <acceptance_criteria>
    - `TemperatureStore.kt` contains `fun parseTemperatureStore(`.
    - New `TemperatureStoreBackfillTest`: a response `{"extruder":{"temperatures":[21.0,22.0,23.0]},"heater_bed":{"temperatures":[60.0,61.0]}}` queried with `sensors={"extruder","heater_bed"}` yields `extruder=[21.0,22.0,23.0]` (oldest first) and `heater_bed=[60.0,61.0]`.
    - Test: a sensor requested but absent from the response is omitted from the map (no key, no empty fabrication).
    - Test: a `temperature_sensor X` entry in the response but NOT in `sensors` is not returned.
    - `:app:testReleaseUnitTest` for TemperatureStoreBackfillTest passes.
  </acceptance_criteria>
  <done>Pure mapper proven for order, sensor selection, and absence; backfill test green.</done>
</task>

<task type="auto">
  <name>Task 2: Handshake one-shot reads + spine exposure (temperature_store + min_extrude_temp)</name>
  <read_first>
    - app/src/main/java/works/mees/dinghy/net/MoonrakerSession.kt (runHandshake lines 247-271 — the identify→list→query→subscribe sequence; objectsParam helper lines 285-289; this is where the one-shot reads append)
    - app/src/main/java/works/mees/dinghy/net/JsonRpcClient.kt (request signature line 89: `suspend fun request(method, params, timeoutMs): JsonElement`)
    - app/src/main/java/works/mees/dinghy/state/PrinterStateStore.kt (the capability stash pattern: _capabilities/setCapabilities; add the minExtrudeTemp + backfill stashes the same way — NOT in the throttled accumulator)
    - app/src/main/java/works/mees/dinghy/di/SpineHandle.kt (the immutable handle; add the two new fields)
    - app/src/main/java/works/mees/dinghy/service/MoonrakerService.kt (buildSpineAndLaunch lines 107-142 — where the handle is assembled and published)
    - .planning/phases/05-core-print-control-panels-temperature-move-extrude/05-RESEARCH.md §1/§2 + net/JsonRpc.kt (TEMPERATURE_STORE/GCODE_SCRIPT from 05-01)
  </read_first>
  <action>
    In `runHandshake()`, AFTER the existing subscribe seed (line ~270): (a) one-shot
    `rpc.request(JsonRpcMethods.TEMPERATURE_STORE)`, run `parseTemperatureStore(result, deriveSubscribeSet-heaters-intersection)` for the heater sensors the graph draws, and publish the per-sensor series via a new `store.setTemperatureBackfill(map)`. (b) one-shot `rpc.request(OBJECTS_QUERY, objectsParam(setOf("configfile")))`, read BOTH `result.status.configfile.settings.extruder.min_extrude_temp` and `...extruder.max_extrude_only_distance` (null-safe walks; static config from the PRIMARY `extruder`), and publish via `store.setMinExtrudeTemp(value)` / `store.setMaxExtrudeDistance(value)`. All best-effort: wrap each in runCatching so a printer that lacks the endpoint/field does not break the handshake (leave the value null — Extrude then shows a generic "Heat nozzle to extrude" without the number and keeps the 100 mm default distance ceiling; EXTR-04/D-07 degrades gracefully).
    In `PrinterStateStore`, expose three observables: `minExtrudeTemp: StateFlow<Float?>`, `maxExtrudeDistance: StateFlow<Float?>`, and `temperatureBackfill: StateFlow<Map<String,FloatArray>>` — each backed by a `MutableStateFlow` (initial null/empty) written ONCE by the set* methods at handshake. Keep them OUT of the conflated accumulator (RESEARCH anti-pattern — not the throttled hot path). Using StateFlow (not `@Volatile var`) is the determinism fix: holders collect them and react the instant the one-shot read lands.
    In `SpineHandle`, add `minExtrudeTemp: StateFlow<Float?>`, `maxExtrudeDistance: StateFlow<Float?>`, and `temperatureBackfill: StateFlow<Map<String, FloatArray>>`; in `MoonrakerService.buildSpineAndLaunch`, forward the store's StateFlows (the handle is published before the handshake fills them, but StateFlows carry the value forward to late collectors — no snapshot-freezing, no re-publish needed). Keep the atomic-publish discipline (review #6) intact.
  </action>
  <verify>
    <automated>cmd.exe /c "E:\Android\gw.bat :app:testReleaseUnitTest :app:compileReleaseKotlin --no-daemon" 2>&1 | tr -d '\r' | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `MoonrakerSession.kt` runHandshake contains a `TEMPERATURE_STORE` request and a `configfile` read of BOTH `min_extrude_temp` and `max_extrude_only_distance`, each in a runCatching (best-effort).
    - `PrinterStateStore.kt` contains `setMinExtrudeTemp`, `setMaxExtrudeDistance`, and `setTemperatureBackfill`, and exposes `minExtrudeTemp`/`maxExtrudeDistance`/`temperatureBackfill` as read-only `StateFlow`s (NOT `@Volatile var`); none is written into the throttled `accumulator`.
    - `SpineHandle.kt` exposes `minExtrudeTemp`, `maxExtrudeDistance`, and `temperatureBackfill` as `StateFlow`s forwarded from the store.
    - `:app:compileReleaseKotlin` succeeds; full unit suite green (existing session/store tests unaffected).
  </acceptance_criteria>
  <done>Both one-shot reads wired best-effort into the handshake; results reachable off the spine; compiles; suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Moonraker socket → backfill/config parse | Untrusted loose JSON from temperature_store/configfile crosses into the store. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03-T | Tampering | parseTemperatureStore / configfile walk | mitigate | Null-safe walks; non-finite samples skipped (reducer discipline); absent sensors/fields omitted, never fabricated. |
| T-05-03-D | DoS | one-shot reads hanging the handshake | mitigate | Each one-shot read in runCatching with JsonRpcClient's per-request timeout; a failed/slow read is best-effort and does not block Connected (the handshake already reached subscribe before these). |
| T-05-03-Safety | Safety | missing min_extrude_temp leaving cold-extrude ungated | accept | EXTR-04 gate keys off the LIVE `can_extrude` boolean (05-01), not this number — min_extrude_temp is hint TEXT only; absence degrades to a generic hint, the gate still holds. |
</threat_model>

<verification>
Full unit suite + release compile green. The REAL temperature_store/configfile shapes are exercised on
the live Ender 5 Plus during 05-08 UAT (mock-vs-reality lesson — harden the FakeWebSocket to the real
shapes captured from flox; do not let a lenient mock pass).
</verification>

<success_criteria>
- Pure backfill mapper proven (order, selection, absence).
- temperature_store + min_extrude_temp + max_extrude_only_distance read once at handshake, best-effort, exposed as StateFlows on the spine.
- None of the reads pollute the throttled hot path; holders observe the StateFlows (deterministic on-connect fullness).
</success_criteria>

<output>
Create `.planning/phases/05-core-print-control-panels-temperature-move-extrude/05-03-SUMMARY.md` when done.
</output>
