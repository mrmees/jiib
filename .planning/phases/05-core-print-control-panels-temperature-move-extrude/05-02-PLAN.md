---
phase: 05-core-print-control-panels-temperature-move-extrude
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/main/java/works/mees/dinghy/command/PrinterCommands.kt
  - app/src/test/java/works/mees/dinghy/command/PrinterCommandsTest.kt
  - app/src/main/java/works/mees/dinghy/theme/ThemeTokens.kt
  - app/src/main/java/works/mees/dinghy/theme/BakedTokens.kt
  - tools/oklch-bake/bake_tokens.py
autonomous: true
requirements: [TEMP-02, TEMP-03, MOVE-01, MOVE-02, MOVE-03, EXTR-01]
must_haves:
  truths:
    - "PrinterCommands builds exact, bounded gcode strings for set-heater, preset, cooldown, jog, home-all/axis, disable-steppers, extrude/retract, tool-select."
    - "scriptParams(gcode) produces the {\"script\": <gcode>} JsonElement the dispatcher sends."
    - "All numeric params are clamped to bounded ranges before formatting (ASVS V5 — no free-text interpolation)."
    - "ThemeTokens carries a third sensor-trace color (violet/trace3) baked for dark + light; GraphView can read it (no raw hex)."
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/command/PrinterCommands.kt"
      provides: "pure gcode builders + scriptParams"
      contains: "fun setHeater"
    - path: "app/src/main/java/works/mees/dinghy/theme/ThemeTokens.kt"
      provides: "third trace token field"
      contains: "violet"
    - path: "app/src/main/java/works/mees/dinghy/theme/BakedTokens.kt"
      provides: "baked dark+light values for the third trace token"
      contains: "violet"
  key_links:
    - from: "command/PrinterCommands.kt"
      to: "kotlinx.serialization JsonElement"
      via: "scriptParams buildJsonObject put(\"script\", gcode)"
      pattern: "put\\(\"script\""
---

<objective>
Create the pure gcode-builder module the dispatch layer calls, and add the missing third sensor-trace
semantic token. Both are pure/config additions with no I/O — disjoint from 05-01 so they run in
parallel.

`command/PrinterCommands.kt` (NEW, RESEARCH §5 + Code Examples; analog `state/DeriveCapabilities.kt`):
a file of pure free functions building exact gcode strings, every action wrapped in
SAVE_GCODE_STATE/RESTORE_GCODE_STATE where it changes motion mode, plus `scriptParams(gcode)`. ASVS V5:
numerics come ONLY from bounded scrubber/selector input — clamp before formatting; never interpolate
free text.

Third trace token (RESEARCH Open Q2, PATTERNS "Gaps" row 1): README §9 names chamber=violet but
`ThemeTokens` has no such role. Add a `violet` (3rd sensor trace) token field + baked dark/light values
so the multi-trace GraphView (05-04) reads it via tokens, never a raw hex (THEME-01). v1 draws up to 3
traces: nozzle=heat, bed=accent, chamber=violet.

Purpose: settle the action-string contract + the trace palette before any panel/graph consumes them.
Output: PrinterCommands builders + scriptParams + the third trace token, with builder unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-core-print-control-panels-temperature-move-extrude/05-RESEARCH.md
@.planning/phases/05-core-print-control-panels-temperature-move-extrude/05-PATTERNS.md
@docs/ui_design/THEMING.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PrinterCommands pure gcode builders + scriptParams</name>
  <read_first>
    - app/src/main/java/works/mees/dinghy/state/DeriveCapabilities.kt (the pure-free-function module shape + top-of-file purity KDoc to mirror)
    - app/src/main/java/works/mees/dinghy/net/JsonRpc.kt (MoonrakerJson; JsonElement import idiom; buildJsonObject usage analog in MoonrakerSession identifyParams lines 273-282)
    - .planning/phases/05-core-print-control-panels-temperature-move-extrude/05-RESEARCH.md §5 (the verified gcode table) + §6 (default values) + Code Examples (exact strings)
  </read_first>
  <action>
    Create `command/PrinterCommands.kt` in package `works.mees.dinghy.command`, an `object PrinterCommands` (or top-level pure funs) with a top-of-file KDoc mirroring DeriveCapabilities' purity note (no I/O, no coroutines, no Compose; same input → same output; host-testable). Provide:
    - `setHeater(heater: String, target: Int)` → `"SET_HEATER_TEMPERATURE HEATER=$heater TARGET=$target"`. Clamp target to 0..350 before formatting.
    - `applyPreset(nozzle: Int, bed: Int)` → two newline-joined SET_HEATER_TEMPERATURE lines (extruder + heater_bed), clamped.
    - `const val COOLDOWN = "TURN_OFF_HEATERS"`.
    - `const val DISABLE_STEPPERS = "M84"`.
    - `jog(axis: String, mm: Double, feedMmMin: Int)` → `"SAVE_GCODE_STATE NAME=dd_jog\nG91\nG1 $axis$mm F$feedMmMin\nRESTORE_GCODE_STATE NAME=dd_jog"`. Require axis ∈ {X,Y,Z}; clamp |mm| ≤ 200 and feed to 1..30000.
    - `homeAll()` → `"G28"`; `homeAxis(a: String)` → `"G28 $a"` (a ∈ {X,Y,Z}).
    - `extrude(mm: Double, feedMmMin: Int)` → `"SAVE_GCODE_STATE NAME=dd_ext\nM83\nG1 E$mm F$feedMmMin\nRESTORE_GCODE_STATE NAME=dd_ext"` (negative mm = retract). Clamp |mm| ≤ 100, feed 1..6000.
    - `selectTool(index: Int)` → `"T$index"` (clamp index ≥ 0).
    - `loadFilament()` → `"LOAD_FILAMENT"`; `unloadFilament()` → `"UNLOAD_FILAMENT"`.
    - Define a fixed preset list (D-02, RESEARCH §6): PLA 200/60, PETG 240/80, ABS 245/100, TPU 220/50 as a `data class Preset(name, nozzle, bed)` + `val MATERIAL_PRESETS: List<Preset>`.
    - `scriptParams(gcode: String): JsonElement = buildJsonObject { put("script", gcode) }` using MoonrakerJson's element types.
    Define numeric clamp bounds as named consts. Do NOT concatenate any free-text/string-typed user value into the script — only clamped numerics and the fixed axis/heater identifiers.
    NOTE for Move panel (05-06): the amber-Override unhomed-jog gcode is INTENTIONALLY NOT defined here — it is LOW-confidence (RESEARCH Open Q1) and confirmed live on flox in 05-08. Add a clearly-commented placeholder `overrideJog(...)` stub OR leave it out and document in the SUMMARY that 05-06/05-08 owns it.
  </action>
  <verify>
    <automated>cmd.exe /c "E:\Android\gw.bat :app:testReleaseUnitTest --tests *PrinterCommandsTest --no-daemon"</automated>
  </verify>
  <acceptance_criteria>
    - `PrinterCommands.kt` contains `fun setHeater(`, `fun jog(`, `fun extrude(`, `const val COOLDOWN`, `const val DISABLE_STEPPERS`, `fun scriptParams(`, `MATERIAL_PRESETS`.
    - New `PrinterCommandsTest` asserts exact strings: `setHeater("extruder",200) == "SET_HEATER_TEMPERATURE HEATER=extruder TARGET=200"`; `jog("X",10.0,3000)` equals the SAVE/G91/G1/RESTORE 4-line string; `extrude(-5.0,300)` contains `G1 E-5.0 F300`; `COOLDOWN == "TURN_OFF_HEATERS"`; `DISABLE_STEPPERS == "M84"`.
    - Test asserts clamping: `setHeater("extruder", 9999)` caps TARGET at 350; `extrude(500.0,300)` caps |E| at 100.
    - Test asserts `scriptParams("M84")` serializes to `{"script":"M84"}`.
    - `:app:testReleaseUnitTest` for PrinterCommandsTest passes.
  </acceptance_criteria>
  <done>All builders produce verified bounded gcode; scriptParams serializes correctly; builder test green.</done>
</task>

<task type="auto">
  <name>Task 2: Add the third sensor-trace token (violet) baked dark+light</name>
  <read_first>
    - app/src/main/java/works/mees/dinghy/theme/ThemeTokens.kt (the @Immutable data class; heat field lines 58-63 is the analog trace-color field; per-field KDoc style)
    - app/src/main/java/works/mees/dinghy/theme/BakedTokens.kt (the ONLY sanctioned home for Color(0x..) literals; TokensDark lines 33-62, TokensLight lines 65-94; the "GENERATED — re-bake" header)
    - tools/oklch-bake/bake_tokens.py (the bake script that owns the oklch→sRGB table)
    - docs/ui_design/THEMING.md (sensor colors heat/accent/violet) + README §9
    - .planning/phases/05-core-print-control-panels-temperature-move-extrude/05-RESEARCH.md Open Q2
  </read_first>
  <action>
    Add a `violet: Color` field to ThemeTokens (KDoc: "`--violet` third sensor trace (chamber/generic) — RESEARCH Open Q2; nozzle=heat, bed=accent, chamber=violet per README §9"). Place it adjacent to the heat trace fields. Because BakedTokens is GENERATED, add the violet oklch source to `tools/oklch-bake/bake_tokens.py` (a sensible violet, e.g. dark `oklch(0.70 0.16 300)`, light `oklch(0.52 0.18 300)` — pick values consistent with the existing palette's L/C feel), then re-bake: run the script per its header instruction to regenerate BakedTokens with the new `violet = Color(0x..)` literal in BOTH TokensDark and TokensLight. If re-running the python writer is impractical in the build env, hand-add the baked literal to BOTH bases in BakedTokens AND add the source oklch to the python so the next bake stays consistent — but the literal MUST land only in BakedTokens (no raw hex anywhere else, THEME-01). Verify the project still compiles (every ThemeTokens construction site now needs the field — TokensDark/TokensLight are the only two constructors).
  </action>
  <verify>
    <automated>cmd.exe /c "E:\Android\gw.bat :app:compileReleaseKotlin --no-daemon" 2>&1 | tr -d '\r' | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `ThemeTokens.kt` contains `val violet: Color`.
    - `BakedTokens.kt` contains `violet = Color(0x` in BOTH TokensDark and TokensLight blocks.
    - `tools/oklch-bake/bake_tokens.py` contains a violet oklch source entry.
    - `grep -rn "Color(0x" app/src/main/java/works/mees/dinghy/theme/ThemeTokens.kt` returns nothing (no raw hex outside BakedTokens).
    - `:app:compileReleaseKotlin` succeeds (both ThemeTokens constructors updated).
  </acceptance_criteria>
  <done>Third trace token present + baked for dark/light via the bake script; compiles; no raw hex leaked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI numeric input → gcode string | Scrubber/selector numerics are formatted into a script string sent to the printer. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02-T | Tampering | gcode string built from numeric input (PrinterCommands) | mitigate | Numerics clamped to named bounds BEFORE formatting (ASVS V5); axis/heater identifiers are fixed enums/constants, never free-text; no string concatenation of user-supplied text (RESEARCH Security Domain). |
| T-05-02-Safety | Safety | jog/extrude leaving printer in relative mode | mitigate | jog/extrude builders wrap the move in SAVE_GCODE_STATE/RESTORE_GCODE_STATE so the printer is never left in G91/M83 (RESEARCH §5). |
</threat_model>

<verification>
Full unit suite stays green: `cmd.exe /c "E:\Android\gw.bat :app:testReleaseUnitTest --no-daemon"`. Compile gate confirms both ThemeTokens constructors carry the new field. No new dependencies.
</verification>

<success_criteria>
- PrinterCommands produces verified, bounded gcode for every Phase-5 action; scriptParams wraps it.
- Numeric clamping proven (ASVS V5).
- ThemeTokens has a baked third trace color for dark + light, sourced from the bake script, no raw hex.
</success_criteria>

<output>
Create `.planning/phases/05-core-print-control-panels-temperature-move-extrude/05-02-SUMMARY.md` when done.
</output>
