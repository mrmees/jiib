---
phase: 05-core-print-control-panels-temperature-move-extrude
plan: 09
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/main/java/works/mees/dinghy/command/CommandDispatcher.kt
  - app/src/test/java/works/mees/dinghy/command/CommandDispatcherTest.kt
autonomous: true
gap_closure: true
requirements: [EXTR-04, MOVE-02, MOVE-01]

must_haves:
  truths:
    - "A printer-rejected gcode (out-of-range move, failing macro, heater fault) does NOT crash the app — the process survives and the rejection is surfaced as a non-fatal toast."
    - "The dispatched key is re-enabled (removed from inFlight) after an RpcError, exactly as it is after a transport failure or timeout."
    - "A unit test feeds a JSON-RPC error response for printer.gcode.script and asserts no exception escapes the dispatch scope AND a DispatchEvent.Failure is emitted."
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/command/CommandDispatcher.kt"
      provides: "RpcError handling in the dispatch() launch block"
      contains: "catch (e: RpcError)"
    - path: "app/src/test/java/works/mees/dinghy/command/CommandDispatcherTest.kt"
      provides: "Regression test: gcode.script JSON-RPC error → Failure event, no crash"
      contains: "RpcError"
  key_links:
    - from: "command/CommandDispatcher.kt"
      to: "net/RpcError.kt"
      via: "catch (e: RpcError) in dispatch() launch block"
      pattern: "catch \\(e: RpcError\\)"
---

<objective>
Close gap G1 (BLOCKER): `CommandDispatcher.dispatch()` launches a coroutine that catches only
`RpcConnectionException` and `TimeoutCancellationException`. When Moonraker returns a JSON-RPC error
envelope for `printer.gcode.script` (an out-of-range move, a failing macro, a heater fault),
`JsonRpcClient.request()` completes the pending deferred with an `RpcError` (JsonRpcClient.kt line 155).
`RpcError` extends `Exception` and is distinct from `RpcConnectionException`, so it re-throws UNCAUGHT in
the unsupervised `scope.launch` lambda — killing the process and tripping FGS auto-restart. Confirmed
repeatable on flox during Phase-5 UAT (FATAL EXCEPTION on DefaultDispatcher-worker-N, "Move out of range").

Purpose: A printer rejecting any gcode must be a non-fatal, user-visible event — never a crash. This is
the one defect that blocked the MOVE-02 amber Override path (it jogs unhomed axes → same rejection) from
being tested at all.

Output: An `RpcError` catch clause in the dispatch launch block that emits `DispatchEvent.Failure`, plus a
regression test that hardens the dispatcher test fake to emit a JSON-RPC error for `gcode.script` (the
exact mock-vs-reality gap that let this bug ship green).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

Build/test environment: this repo builds WINDOWS-SIDE via `E:\Android\gw.bat`. `./gradlew` does NOT run
from WSL bash. Run any Gradle task from the repo root as:
`/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat <gradle args>"`
Pipe through `tr -d '\r'` to strip the CR progress bars; the process exit code is authoritative.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-core-print-control-panels-temperature-move-extrude/05-VERIFICATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Catch RpcError in dispatch() and emit a non-fatal Failure event</name>
  <files>app/src/main/java/works/mees/dinghy/command/CommandDispatcher.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/command/CommandDispatcher.kt (the dispatch() launch block at lines ~104-116 catches only RpcConnectionException + TimeoutCancellationException; RpcError is not imported)
    - app/src/main/java/works/mees/dinghy/net/RpcError.kt (RpcError extends Exception, carries nullable code + message; distinct class from RpcConnectionException)
    - app/src/main/java/works/mees/dinghy/net/JsonRpcClient.kt (line 155: dispatch() completes the pending deferred with RpcError on an error envelope; request() @throws RpcError documented at lines ~88-90)
  </read_first>
  <behavior>
    - When request() completes exceptionally with RpcError, dispatch() catches it, emits exactly one DispatchEvent.Failure(key, ...), and removes the key from inFlight via the existing finally block. No exception escapes the launch lambda.
    - The emitted Failure message is human-readable and carries the printer's rejection text (e.RpcError.message) — e.g. "Move out of range" — so the existing SeverityToast pipeline shows the user WHY the command was rejected.
    - The Failure message must NOT embed an API key or a `?token=` URL (same redaction contract the existing RpcConnectionException and timeout branches honor). RpcError.message is the server's gcode-rejection text, which never contains credentials, but keep the message construction consistent with the existing branches.
    - Existing behaviors (debounce, in-flight guard, timeout, transport-failure Failure) are unchanged.
  </behavior>
  <action>
    Add `import works.mees.dinghy.net.RpcError` to CommandDispatcher.kt. Inside the `scope.launch { try { ... }` block in dispatch(), add a new `catch (e: RpcError)` clause ALONGSIDE the existing RpcConnectionException and TimeoutCancellationException catches (and BEFORE the finally). In that clause emit `_events.tryEmit(DispatchEvent.Failure(key, e.message ?: method))` — surfacing the printer's own rejection message so the user sees why the gcode was rejected; fall back to the bare method name when the message is null. Order the catch clauses so RpcError (a peer of Exception) is caught explicitly and never falls through to an unsupervised re-throw. Do NOT widen any catch to a blanket `Throwable` or `Exception` — catch RpcError specifically, leaving cancellation and genuinely-unexpected exceptions to propagate as before. Keep the finally block (`_inFlight.update { it - key }`) unchanged so the key is always re-enabled. Update the DispatchEvent.Failure KDoc to note it now also covers a server-side gcode rejection (RpcError), not only transport/timeout.
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:compileReleaseKotlin --no-daemon" 2>&1 | tr -d '\r' | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "catch (e: RpcError)" app/src/main/java/works/mees/dinghy/command/CommandDispatcher.kt` returns a match inside the dispatch() launch block.
    - `grep -n "import works.mees.dinghy.net.RpcError" app/src/main/java/works/mees/dinghy/command/CommandDispatcher.kt` returns a match.
    - The RpcError catch clause emits `DispatchEvent.Failure(key, e.message ?: method)` (grep for `DispatchEvent.Failure(key, e.message`).
    - No catch clause was widened to `Throwable` or bare `Exception`.
    - `:app:compileReleaseKotlin` succeeds (BUILD SUCCESSFUL).
  </acceptance_criteria>
  <done>A printer-rejected gcode no longer escapes the dispatch scope; it is caught as RpcError and surfaced as a non-fatal DispatchEvent.Failure carrying the printer's rejection message, and the key is re-enabled.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Regression test — harden the dispatcher fake to emit a JSON-RPC error for gcode.script</name>
  <files>app/src/test/java/works/mees/dinghy/command/CommandDispatcherTest.kt</files>
  <read_first>
    - app/src/test/java/works/mees/dinghy/command/CommandDispatcherTest.kt (the FakeRpc stub at lines ~34-52 and the existing failure tests rpcConnectionException_emitsFailureEvent_andReEnablesKey at line ~126; FakeRpc.fail(index, cause) already exists)
    - app/src/main/java/works/mees/dinghy/command/CommandDispatcher.kt (the catch clause added in Task 1)
    - app/src/main/java/works/mees/dinghy/net/RpcError.kt (RpcError(code, message) constructor signature — code is Int?, message is String)
  </read_first>
  <behavior>
    - A new test dispatches `printer.gcode.script`, then fails the underlying request with an `RpcError(code = -32000 (or any non-null code), message = "Move out of range: 418.000 -9.000 32.000 [20.000]")` exactly the way JsonRpcClient.dispatch() completes the deferred on a server error envelope.
    - The test asserts: (a) the key is removed from inFlight after the RpcError (no in-flight leak), (b) a DispatchEvent.Failure is emitted, and (c) the Failure.message contains the printer's rejection text ("Move out of range"). The mere fact that runTest completes without an unhandled exception proves no crash escaped the launch scope.
    - This is the mock-vs-reality regression guard: prior to Task 1 the FakeRpc never returned a JSON-RPC error for gcode.script, so the crash shipped behind green tests. The new test exercises the real RpcError completion path.
  </behavior>
  <action>
    Add a new `@Test fun gcodeScriptRpcError_emitsFailureEvent_andDoesNotCrash()` to CommandDispatcherTest, modeled on the existing `rpcConnectionException_emitsFailureEvent_andReEnablesKey` test (same runTest(UnconfinedTestDispatcher()) + events collector + runCurrent() cadence). Dispatch under a key with method `printer.gcode.script` (use the JsonRpcMethods.GCODE_SCRIPT constant or the literal string), then call `rpc.fail(0, RpcError(code = -32000, message = "Move out of range: 418.000 -9.000 32.000 [20.000]"))` to complete the underlying deferred exceptionally with an RpcError — mirroring JsonRpcClient line 155. Import `works.mees.dinghy.net.RpcError`. Assert: the key is no longer in dispatcher.inFlight.value; a DispatchEvent.Failure was emitted; the surfaced Failure.message contains "Move out of range". Cancel the collect job at the end. Do NOT loosen FakeRpc — it already supports fail(index, cause); the regression is closed by feeding it an RpcError specifically (the cause the real client produces for a rejected gcode.script).
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testReleaseUnitTest --tests works.mees.dinghy.command.CommandDispatcherTest --no-daemon" 2>&1 | tr -d '\r' | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "RpcError" app/src/test/java/works/mees/dinghy/command/CommandDispatcherTest.kt` returns matches (import + the fail(...) call).
    - A test named `gcodeScriptRpcError_emitsFailureEvent_andDoesNotCrash` (or equivalent) exists and feeds an RpcError to the underlying request for a gcode.script dispatch.
    - The test asserts a DispatchEvent.Failure is emitted whose message contains "Move out of range", and asserts the key is removed from inFlight.
    - `:app:testReleaseUnitTest --tests ...CommandDispatcherTest` passes (the new test PASSES, and would FAIL against the pre-Task-1 dispatcher because the RpcError would escape).
  </acceptance_criteria>
  <done>A faithful regression test feeds the exact RpcError that a rejected gcode.script produces and proves no exception escapes + a Failure is surfaced — closing the mock-vs-reality gap that concealed G1.</done>
</task>

</tasks>

<verification>
- `:app:compileReleaseKotlin` succeeds.
- `:app:testReleaseUnitTest --tests works.mees.dinghy.command.CommandDispatcherTest` passes (all existing tests + the new RpcError regression test).
- `grep -c "catch (e: RpcError)" app/src/main/java/works/mees/dinghy/command/CommandDispatcher.kt` returns 1.
</verification>

<success_criteria>
- A printer-rejected gcode is caught as RpcError and surfaced as a non-fatal DispatchEvent.Failure carrying the rejection message; the app process survives.
- The dispatched key is re-enabled after the RpcError.
- A regression test exercises the real RpcError completion path for gcode.script and would have caught this bug.
</success_criteria>

<output>
Create `.planning/phases/05-core-print-control-panels-temperature-move-extrude/05-09-SUMMARY.md` when done.
</output>
