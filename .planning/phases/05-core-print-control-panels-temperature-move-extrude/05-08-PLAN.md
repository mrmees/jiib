---
phase: 05-core-print-control-panels-temperature-move-extrude
plan: 08
type: execute
wave: 4
depends_on: ["05-05", "05-06", "05-07"]
files_modified:
  - app/src/main/java/works/mees/dinghy/ui/route/TopRoute.kt
  - app/src/main/java/works/mees/dinghy/ui/shell/AppDrawer.kt
  - app/src/main/java/works/mees/dinghy/ui/shell/AppShell.kt
  - .planning/phases/05-core-print-control-panels-temperature-move-extrude/05-PERF-RESULTS.md
autonomous: false
requirements: [TEMP-04, MOVE-01, MOVE-02, MOVE-03, EXTR-01, EXTR-02, EXTR-04]
user_setup: []
must_haves:
  truths:
    - "The App Drawer's Move/Temp/Tools tiles are LIVE and route to the three new panels; the shell renders each full-bleed."
    - "The multi-trace Temperature screen passes the two-part Adreno-320 perf gate on real flox (liveness + sparse-redraw p95 ≤ ~66 ms during a live heat) — re-measured, NOT grandfathered (D-06)."
    - "On the live Ender 5 Plus: preheat → wait for temp → extrude → jog all work end-to-end without the browser (SC-5)."
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/ui/route/TopRoute.kt"
      provides: "Dest enum extended with Temperature/Move/Extrude"
      contains: "Temperature"
    - path: "app/src/main/java/works/mees/dinghy/ui/shell/AppShell.kt"
      provides: "when(dest) routes the three new panels with per-session holders"
      contains: "MoveScreen"
    - path: ".planning/phases/05-core-print-control-panels-temperature-move-extrude/05-PERF-RESULTS.md"
      provides: "the D-06 perf re-measurement record"
      contains: "p95"
  key_links:
    - from: "ui/shell/AppShell.kt"
      to: "ui/temperature|move|extrude screens"
      via: "when(dest) branches with remember(store){ *Holder(...) }"
      pattern: "TemperatureScreen|MoveScreen|ExtrudeScreen"
    - from: "ui/shell/AppDrawer.kt"
      to: "ui/route/Dest"
      via: "DRAWER_TILES dest = Dest.Move/Temperature/Extrude"
      pattern: "Dest\\.(Move|Temperature|Extrude)"
---

<objective>
Wire the three panels into navigation, then prove the phase on real hardware: the D-06 multi-trace perf
re-measure and the SC-5 end-to-end print-control loop. This plan has checkpoints (on-device gates) so it
is NOT autonomous.

Routing (PATTERNS "Routing"):
- `TopRoute.Dest` enum += `Temperature, Move, Extrude` (one-line addition, D-05).
- `AppDrawer.DRAWER_TILES` — set the greyed "Move"/"Temp"/"Tools" tiles' `dest` to the new destinations
  (Tools→Extrude per the planner; document the tile→panel mapping). A live tile auto-renders accent +
  clickable (existing DrawerTile logic, no other change).
- `AppShell.when(dest)` += branches constructing each holder `remember(store){ *Holder(scope, store) }`
  off the live per-session store (exactly like the existing PrintStatusHolder line 80) and rendering the
  screen with `onBack = { dest = Dest.PrintStatus }`.

On-device gates (checkpoints):
- D-06 perf re-measure of the FULL Temperature screen on flox `0a64b42e` (RELEASE, debug-signed) against
  the two-part Adreno-320 gate; record in `05-PERF-RESULTS.md` mirroring `03-PERF-RESULTS.md`.
- SC-5 end-to-end UAT on the live Ender 5 Plus: preheat → wait → extrude → jog (+ MOVE-03 disable, the
  EXTR-02 missing-macro popup, and confirm the amber Override unhomed-jog gcode — RESEARCH Open Q1).

Purpose: make the panels reachable and close the phase's two hardware gates.
Output: live routing + the perf-results record + UAT sign-off.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-core-print-control-panels-temperature-move-extrude/05-RESEARCH.md
@.planning/phases/05-core-print-control-panels-temperature-move-extrude/05-PATTERNS.md
@.planning/phases/03-design-system-theming-foundation/03-PERF-RESULTS.md
@.planning/phases/05-core-print-control-panels-temperature-move-extrude/05-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire Dest + AppDrawer tiles + AppShell routing for the three panels</name>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/route/TopRoute.kt (Dest enum line 30 — extend it)
    - app/src/main/java/works/mees/dinghy/ui/shell/AppDrawer.kt (DRAWER_TILES lines 112-122; DrawerTile live-vs-greyed logic lines 130-169 — no change needed, just set dest)
    - app/src/main/java/works/mees/dinghy/ui/shell/AppShell.kt (when(dest) lines 97-103; the per-session holder remember(store) pattern lines 76-80; onConnectionSaved→PrintStatus idiom line 101)
    - app/src/main/java/works/mees/dinghy/ui/temperature/TemperatureScreen.kt, ui/move/MoveScreen.kt, ui/extrude/ExtrudeScreen.kt (the three screen signatures from 05-05/06/07)
    - .planning/phases/05-core-print-control-panels-temperature-move-extrude/05-PATTERNS.md "Routing"
  </read_first>
  <action>
    Extend `Dest` to `{ PrintStatus, Temperature, Move, Extrude, Settings }`. In `AppDrawer.DRAWER_TILES` set `dest = Dest.Move` on the "Move" tile, `dest = Dest.Temperature` on the "Temp" tile, and `dest = Dest.Extrude` on the "Tools" tile (or rename "Tools"→"Extrude" if clearer — document the mapping in the SUMMARY). Leave Files/Macros/Devices/Power greyed. In `AppShell`, add to the `when(dest)`: build a `TemperatureHolder`/`MoveHolder`/`ExtrudeHolder` each `remember(store) { ...Holder(scope = scope, store = store) }` off the SAME live `store` the PrintStatusHolder uses, and render `TemperatureScreen(container, holder, onBack = { dest = Dest.PrintStatus })` etc. Keep the lean route holder (NOT Navigation-Compose, D-05). No new shell chrome.
  </action>
  <verify>
    <automated>cmd.exe /c "E:\Android\gw.bat :app:testReleaseUnitTest :app:compileReleaseKotlin --no-daemon" 2>&1 | tr -d '\r' | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `TopRoute.kt` `Dest` contains `Temperature`, `Move`, `Extrude`.
    - `AppDrawer.kt` DRAWER_TILES set `dest = Dest.Move` / `Dest.Temperature` / `Dest.Extrude` on three previously-greyed tiles.
    - `AppShell.kt` `when(dest)` has branches rendering `TemperatureScreen`, `MoveScreen`, `ExtrudeScreen` with per-session holders `remember(store)`'d and `onBack` returning to PrintStatus.
    - `:app:compileReleaseKotlin` succeeds; full unit suite green.
  </acceptance_criteria>
  <done>Three panels reachable from the App Drawer and rendered full-bleed by the shell; compiles; suite green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: D-06 multi-trace Temperature perf gate on real flox</name>
  <what-built>
    Routing wired; the full multi-trace Temperature screen (N-trace GraphView + Focus values + gutter)
    is reachable on-device. The release APK is built and debug-signed for flox (E:\Android\sign-release.bat),
    installed via adb. This re-measures the Phase-3 two-part Adreno-320 gate on the FULL screen — per the
    D-06 MANDATE, the isolated Phase-3 50.1 ms number is NOT grandfathered.
  </what-built>
  <action>
    Build the release APK (`cmd.exe /c "E:\Android\gw.bat :app:assembleRelease --no-daemon"`), debug-sign it
    via `E:\Android\sign-release.bat`, and `adb -s 0a64b42e install -r` onto flox. Confirm the app launches to
    the routed shell (not the Phase-1 scaffold). Then perform the on-device capture below and record the result
    in 05-PERF-RESULTS.md. Claude automates the build/install/capture/parse; the human confirms the verdict.
  </action>
  <how-to-verify>
    On the real flox device (`adb 0a64b42e`, LineageOS 18.1 / Adreno 320) connected to the live Ender 5 Plus:
    1. Install the release build: `E:\Android\sign-release.bat <release.apk> <signed.apk>` then `adb install -r`.
    2. Open the App Drawer → Temp. Apply a PLA preset so multiple heaters actively ramp (worst case = more traces moving).
    3. LIVENESS GATE: confirm no animation loop (graph repaints track the ~3 Hz feed, not Choreographer), allocation-free onDraw (no GC churn), and ZERO frozen frames (0 frames > 700 ms).
    4. SPARSE-REDRAW GATE: capture `adb shell dumpsys gfxinfo works.mees.dinghy framestats` during the live heat ramp; parse with `tools/gfxinfo-parser/parse_framestats.py`; confirm p95 ≤ ~66 ms.
    5. Record the raw capture + verdict in `.planning/phases/05-core-print-control-panels-temperature-move-extrude/05-PERF-RESULTS.md` mirroring 03-PERF-RESULTS.md (include device id, build, fill-on/fill-off if a re-open trigger fires).
    6. If a re-open trigger fires (composite floor unstable / p95 breaches ~66 ms / cadence assumption breaks), document it and the re-derived budget per the 03-PERF-RESULTS re-open protocol.
  </how-to-verify>
  <acceptance_criteria>
    - `05-PERF-RESULTS.md` exists with a recorded p95 number and a PASS/re-open verdict from a REAL flox capture (not fabricated, not grandfathered).
    - Liveness gate: 0 frozen frames, no animation loop, allocation-free onDraw confirmed.
  </acceptance_criteria>
  <resume-signal>Type "approved" with the measured p95 / verdict, or report which gate failed and the re-open decision.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: SC-5 end-to-end print-control UAT on the live Ender 5 Plus</name>
  <what-built>
    All three panels wired and live. This is the phase's core gate: drive manual print control end-to-end
    on real hardware without the browser, plus confirm the LOW-confidence amber Override gcode (RESEARCH
    Open Q1) and the safety/popup behaviors that mocks cannot prove (mock-vs-reality lesson).
  </what-built>
  <action>
    Ensure the routed release build from Task 2 is installed on flox and connected to the live Ender 5 Plus.
    Claude drives any scriptable steps (adb-observable state, gcode_response capture via logcat) and records
    observations; the human performs the physical confirmations (filament moves, position matches Mainsail)
    and reports the Override-cell gcode result so PrinterCommands.overrideJog can be finalized.
  </action>
  <how-to-verify>
    On flox + the live Ender 5 Plus:
    1. PREHEAT (TEMP-03): App Drawer → Temp → apply PLA preset; confirm nozzle+bed targets set and the graph fills immediately from temperature_store backfill (no lone-dot / no auto-range noise — G-1 fixed).
    2. EXACT SET (TEMP-02): tap a live temp value → ScrubberPage → set an exact target → confirm it applies (no keyboard).
    3. WAIT + EXTRUDE (EXTR-04 then EXTR-01): while cold, confirm Extrude/Retract are DISABLED with the inline reason showing the real min_extrude_temp; once nozzle ≥ min_extrude_temp confirm they ENABLE; extrude 5 mm and confirm filament moves.
    4. EXTR-02 popup: tap Load/Unload; if the printer lacks the macro confirm the informational popup; if present confirm it runs.
    5. JOG + HOME (MOVE-01/02/04): home all; jog X/Y/Z by a preset; confirm gcode_position updates value-on-glyph and matches Mainsail's readout (Pitfall 1); confirm per-axis labels read green when homed.
    6. MOVE-03: Disable steppers → ConfirmGuard → confirm; confirm axes go un-homed (amber labels).
    7. OVERRIDE (RESEARCH Open Q1): with an axis un-homed, tap the amber Override cell; confirm the chosen Override gcode actually jogs without faulting (or capture the gcode error via SeverityToast and record the correct sequence to wire). Update PrinterCommands.overrideJog with the confirmed gcode if it differed from the placeholder.
    8. PRIM-05 in anger (SC-4): double-tap a destructive control (debounce holds); pull network mid-action (timeout, control re-enables, no infinite hang).
  </how-to-verify>
  <acceptance_criteria>
    - Preheat → wait → extrude → jog all succeed end-to-end on the live printer without the browser (SC-5).
    - The amber Override gcode is confirmed working on flox (or the correct sequence is recorded + PrinterCommands updated).
    - EXTR-04 cold-extrude gate transitions correctly at the temp boundary; the missing-macro popup behaves per D-10.
    - PRIM-05 debounce + timeout observed under real conditions.
  </acceptance_criteria>
  <resume-signal>Type "approved" with notes, or list which step(s) failed (esp. the Override gcode result) for gap closure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App Drawer tile → panel route | Navigation reaches the new control surfaces (in-process, no new external boundary). |
| User tap → live printer | The UAT exercises the real action path under real network latency. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-08-Safety | Safety | unhomed Override jog faulting or crashing into endstops | mitigate | Override kept behind the explicit amber affordance; the exact gcode is CONFIRMED LIVE on flox before sign-off (RESEARCH Open Q1); gcode errors surfaced via SeverityToast, never silently retried. |
| T-05-08-D | DoS | a dropped packet hanging an action mid-UAT | mitigate | PRIM-05 timeout (verified in anger in Task 3, SC-4) clears the in-flight key; the UAT explicitly exercises the dropped-packet path. |
| T-05-08-Perf | (Adreno-320 budget) | multi-trace graph exceeding the fill-rate floor (jank) | mitigate | D-06 two-part gate re-measured on real flox (Task 2); re-open protocol applies if it breaches ~66 ms. |
</threat_model>

<verification>
Task 1 automated (compile + suite). Tasks 2 and 3 are blocking on-device human-verify gates on real
flox + live Ender 5 Plus — the mock-vs-reality backstop the project mandates. The perf result is
recorded in 05-PERF-RESULTS.md; any failed UAT step routes to gap closure (`/gsd-plan-phase 5 --gaps`).
</verification>

<success_criteria>
- The three panels are reachable from the App Drawer and render full-bleed.
- The multi-trace Temperature screen passes the re-measured two-part Adreno-320 perf gate on flox (D-06).
- preheat → wait → extrude → jog proven end-to-end on the live Ender 5 Plus (SC-5).
- The amber Override gcode is confirmed (or corrected) live.
</success_criteria>

<output>
Create `.planning/phases/05-core-print-control-panels-temperature-move-extrude/05-08-SUMMARY.md` when done.
</output>
