---
phase: 05-core-print-control-panels-temperature-move-extrude
plan: 05
type: execute
wave: 3
depends_on: ["05-02", "05-03", "05-04"]
files_modified:
  - app/src/main/java/works/mees/dinghy/ui/temperature/TemperatureHolder.kt
  - app/src/main/java/works/mees/dinghy/ui/temperature/TemperatureScreen.kt
  - app/src/test/java/works/mees/dinghy/ui/temperature/TemperatureHolderTest.kt
autonomous: true
requirements: [TEMP-01, TEMP-02, TEMP-03, TEMP-04]
must_haves:
  truths:
    - "The Temperature panel shows each heater/sensor's current and target temperature (TEMP-01)."
    - "Tapping a live temperature value opens a ScrubberPage that sets that heater's target via SET_HEATER_TEMPERATURE through the CommandDispatcher (TEMP-02)."
    - "Preset buttons (PLA/PETG/ABS/TPU) and Cooldown dispatch the correct gcode; Cooldown = TURN_OFF_HEATERS (TEMP-03)."
    - "The Field hosts the multi-trace GraphView backfilled from temperature_store on connect and appended live, with a fixed Y-range (TEMP-04, G-1 fix)."
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/ui/temperature/TemperatureHolder.kt"
      provides: "per-sensor legend + per-sensor backfilled rings"
      contains: "class TemperatureHolder"
    - path: "app/src/main/java/works/mees/dinghy/ui/temperature/TemperatureScreen.kt"
      provides: "Focus values + Field graph + Back/Presets/Cooldown gutter"
      contains: "GraphViewHost"
  key_links:
    - from: "ui/temperature/TemperatureScreen.kt"
      to: "command/CommandDispatcher"
      via: "dispatcher.dispatch(key, GCODE_SCRIPT, scriptParams(PrinterCommands.*))"
      pattern: "dispatch\\("
    - from: "ui/temperature/TemperatureHolder.kt"
      to: "state/TemperatureStore backfill"
      via: "seed per-sensor rings from store.temperatureBackfill, then push live temps"
      pattern: "temperatureBackfill|push"
---

<objective>
Build the Temperature panel (TEMP-01..04) — LAW per `09-temperature-graph.png` + README §9. Mirror the
Phase-4 Print Status surface exactly (holder + ScreenScaffold screen + dispatch + GraphViewHost).

Layout (D-04): Focus = live current/setpoint value(s) (single sensor = value-on-glyph; multi = legend
list, nozzle=heat/bed=accent/chamber=violet). Field = the multi-trace history graph (05-04 GraphView).
Gutter = Back (Danger/red) · Presets (Neutral) · Cooldown (Warn/amber).

Behaviors:
- TEMP-01: per-heater current/target read from PrinterState.heaters into the legend.
- TEMP-02: tapping a live temp value opens ScrubberPage (0..max °C); Apply → dispatch GCODE_SCRIPT with
  `PrinterCommands.setHeater(name, target)`. NO keypad (D-01).
- TEMP-03: Presets button reveals the fixed PLA/PETG/ABS/TPU set (PrinterCommands.MATERIAL_PRESETS) —
  each dispatches `applyPreset(nozzle,bed)`; Cooldown dispatches `COOLDOWN` (TURN_OFF_HEATERS).
- TEMP-04: the holder owns one RingBuffer per drawn sensor, seeded oldest→newest from the spine's
  `temperatureBackfill` on first emission, then appends each sensor's live temp; the screen hosts the
  N-trace GraphViewHost with a fixed Y-range.

Purpose: the first sustained high-rate render panel and the G-1 fix in anger.
Output: TemperatureHolder + TemperatureScreen, with a holder unit test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-core-print-control-panels-temperature-move-extrude/05-RESEARCH.md
@.planning/phases/05-core-print-control-panels-temperature-move-extrude/05-PATTERNS.md
@docs/ui_design/LAYOUT.md
@docs/ui_design/THEMING.md
@docs/ui_design/README.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TemperatureHolder — per-sensor legend + backfilled rings</name>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusHolder.kt (MIRROR — constructor+collect loop lines 46-74; the ONE-ring + push idiom lines 51/69-70; capability resolution lines 79-99; NO second throttle)
    - app/src/test/java/works/mees/dinghy/ui/printstatus/PrintStatusHolderTest.kt (the runTest/store.seed/runCurrent harness to copy)
    - app/src/main/java/works/mees/dinghy/state/PrinterStateStore.kt (printerState/capabilities flows + the new temperatureBackfill/minExtrudeTemp stashes from 05-03)
    - app/src/main/java/works/mees/dinghy/render/RingBuffer.kt (push/snapshot)
    - .planning/phases/05-core-print-control-panels-temperature-move-extrude/05-RESEARCH.md §1 (backfill order) + Pattern 1
  </read_first>
  <action>
    Create `ui/temperature/TemperatureHolder.kt` (plain Kotlin, no Compose, host-testable; mirror PrintStatusHolder). Resolve the DRAWN sensors deterministically from `store.capabilities.heaters` (authoritative) — up to 3 traces in trace order: nozzle (`extruder`/first extruder-prefixed) → bed (`heater_bed`) → chamber (first `heater_generic`). Own one RingBuffer per drawn sensor. On the FIRST store emission (or when backfill becomes available), seed each ring from `store.temperatureBackfill[sensorName]` oldest→newest (push each value) BEFORE pushing the live point; guard against double-seeding (seed once). On every store emission push each drawn sensor's current temperature and expose: `series: StateFlow<List<FloatArray>>` (one snapshot per drawn trace, in trace order) and `legend: StateFlow<List<SensorReadout>>` where `SensorReadout(name, label, current, target?)`. NO `sample`/`debounce`/`delay` — consume the store's already-throttled flow (Phase-4 lesson). Reuse the PrintStatusScreen `label()` mapping idea for sensor labels (NOZZLE/BED/<generic name>).
  </action>
  <verify>
    <automated>cmd.exe /c "E:\Android\gw.bat :app:testReleaseUnitTest --tests *TemperatureHolderTest --no-daemon"</automated>
  </verify>
  <acceptance_criteria>
    - `TemperatureHolder.kt` contains `class TemperatureHolder` with `series: StateFlow<List<FloatArray>>` and `legend: StateFlow<...>`.
    - New TemperatureHolderTest (copying the PrintStatusHolderTest harness): seed a store with extruder+heater_bed temps; assert `legend` carries both with correct current/target and `series` has 2 traces.
    - Test: with `store.temperatureBackfill = {"extruder":[20f,21f,22f]}` seeded, the extruder ring snapshot STARTS with the backfill values (oldest first) before the live push; seeding happens once (a second emission does not re-prepend the backfill).
    - Test: target 0 surfaces as null setpoint (off), mirroring PrintStatusHolder heaterCell rule.
    - `:app:testReleaseUnitTest` for TemperatureHolderTest passes.
  </acceptance_criteria>
  <done>Holder produces per-sensor legend + backfilled-then-live rings, no second throttle; holder test green.</done>
</task>

<task type="auto">
  <name>Task 2: TemperatureScreen — Focus values + Field graph + gutter + scrubber/presets</name>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusScreen.kt (MIRROR — imports lines 30-47; signature/collect lines 80-90; dispatch-failure toast plumbing lines 92-113; ScreenScaffold slots lines 115-159; GraphViewHost call site lines 131-137; gutter OutlinedControl Intent usage)
    - app/src/main/java/works/mees/dinghy/designsystem/ScrubberPage.kt (signature lines 87-99 — label/value/range/step/unit/onValueChange/onCancel/onApply)
    - app/src/main/java/works/mees/dinghy/render/GraphViewHost.kt (the new series:List overload from 05-04)
    - app/src/main/java/works/mees/dinghy/command/PrinterCommands.kt (setHeater/applyPreset/COOLDOWN/scriptParams/MATERIAL_PRESETS from 05-02)
    - app/src/main/java/works/mees/dinghy/designsystem/control/OutlinedControl.kt (Intent enum: Neutral/Accent/Warn/Danger/Go)
    - docs/ui_design/images/09-temperature-graph.png (LAW) + README §9 + LAYOUT.md
  </read_first>
  <action>
    Create `ui/temperature/TemperatureScreen.kt`, `@Composable fun TemperatureScreen(container: AppContainer, holder: TemperatureHolder, onBack: () -> Unit, modifier)`. Collect `container.printerState`, `container.dispatcher`, `holder.series`, `holder.legend`, `LocalTokens.current`. Copy the dispatch-failure→SeverityToast plumbing verbatim. Build on ScreenScaffold:
    - Focus: the legend — for a single sensor render value-on-glyph (large current + dashed-setpoint style); for multiple, a small legend list colored per trace (heat/accent/violet). Tapping a sensor's value sets `scrubberTarget = thatSensor` (state) to open the ScrubberPage.
    - Field: `GraphViewHost(tokens, series = series, yRange = 0f..300f, modifier = fillMaxWidth().weight(1f))`.
    - Gutter: `OutlinedControl("Back", onBack, Intent.Danger)` · `OutlinedControl("Presets", { showPresets = true }, Intent.Neutral)` · `OutlinedControl("Cooldown", { dispatcher?.dispatch("cooldown", GCODE_SCRIPT, scriptParams(PrinterCommands.COOLDOWN)) }, Intent.Warn)`.
    - When `scrubberTarget != null`: show `ScrubberPage(label, value=currentTarget, range=0f..maxFor(sensor), step=5f, unit="°C", onCancel={scrubberTarget=null}, onApply={ v -> dispatcher?.dispatch("set_${name}", GCODE_SCRIPT, scriptParams(PrinterCommands.setHeater(name, v.toInt()))); scrubberTarget=null })`. (bed max ~120, nozzle ~300 — Claude's discretion via a small helper.)
    - When `showPresets`: a simple selector of `PrinterCommands.MATERIAL_PRESETS` (each → `dispatch("preset_${p.name}", GCODE_SCRIPT, scriptParams(PrinterCommands.applyPreset(p.nozzle,p.bed)))`), dismissable. Reuse OutlinedControl tiles; keyboard-free.
    All color via LocalTokens (zero raw Color); all layout via ScreenScaffold; live numbers GeistMono. Disable a control while its dispatch key is in `dispatcher.inFlight` (PRIM-05 busy state) — mirror how the dispatcher exposes inFlight.
  </action>
  <verify>
    <automated>cmd.exe /c "E:\Android\gw.bat :app:compileReleaseKotlin --no-daemon" 2>&1 | tr -d '\r' | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `TemperatureScreen.kt` contains `GraphViewHost(`, a `ScrubberPage(` invocation, and three gutter `OutlinedControl` with `Intent.Danger`/`Intent.Neutral`/`Intent.Warn`.
    - It dispatches via `dispatcher?.dispatch(... JsonRpcMethods.GCODE_SCRIPT, scriptParams(PrinterCommands.setHeater(...)))` and `PrinterCommands.COOLDOWN` and `PrinterCommands.applyPreset(...)` — never a raw rpc.request.
    - `grep "Color(0x" TemperatureScreen.kt` returns nothing (token-pure).
    - `:app:compileReleaseKotlin` succeeds.
  </acceptance_criteria>
  <done>Temperature panel composes per the locked mockup; all actions route through the dispatcher as GCODE_SCRIPT; token-pure; compiles.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User tap → action dispatch | Heater-set / preset / cooldown taps cross into the dispatcher → Moonraker. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05-T | Tampering | double-tap re-issues a heater command | mitigate | All taps via CommandDispatcher (debounce + in-flight key); controls disabled off `dispatcher.inFlight` (PRIM-05). |
| T-05-05-T2 | Tampering | target temp built from scrubber input | mitigate | Scrubber emits bounded numerics; `PrinterCommands.setHeater` clamps to 0..350 (05-02) before formatting — no free-text. |
| T-05-05-D | DoS | hung set/cooldown wedging a control | mitigate | Dispatcher `withTimeout` removes the in-flight key in `finally` (PRIM-05). |
| T-05-05-I | Injection (XSS-analog) | gcode_response error text rendered | mitigate | Failures shown verbatim via SeverityToast (redacted dispatcher message), never interpreted. |
</threat_model>

<verification>
Release compile + holder unit test green. The live temperature_store contract, the multi-heater graph
during an active heat, and TEMP-02/03 dispatch on real hardware are exercised in 05-08 UAT on flox +
live Ender 5 Plus (mock-vs-reality lesson). The D-06 perf re-measure of THIS screen is the 05-08 gate.
</verification>

<success_criteria>
- TEMP-01..04 all present: legend current/target, scrubber-set, presets+cooldown, backfilled multi-trace graph.
- Every action routes through the dispatcher as GCODE_SCRIPT; token-pure; ScreenScaffold layout.
</success_criteria>

<output>
Create `.planning/phases/05-core-print-control-panels-temperature-move-extrude/05-05-SUMMARY.md` when done.
</output>
