---
phase: 05-core-print-control-panels-temperature-move-extrude
plan: 06
type: execute
wave: 3
depends_on: ["05-01", "05-02"]
files_modified:
  - app/src/main/java/works/mees/dinghy/ui/move/MoveHolder.kt
  - app/src/main/java/works/mees/dinghy/ui/move/MoveScreen.kt
  - app/src/test/java/works/mees/dinghy/ui/move/MoveHolderTest.kt
autonomous: true
requirements: [MOVE-01, MOVE-02, MOVE-03, MOVE-04]
must_haves:
  truths:
    - "The Move panel jogs X/Y/Z by a selected distance preset (0.1–100 mm) via SAVE/G91/G1/RESTORE through the dispatcher (MOVE-01)."
    - "Home-all (center cell) and per-axis home dispatch G28 / G28 <axis> (MOVE-02)."
    - "Disable steppers routes through the full-screen ConfirmGuard → M84 (MOVE-03)."
    - "Live toolhead X/Y/Z from gcode_move.gcode_position renders value-on-glyph in the jog-pad corners, colored green=homed/amber=unhomed per axis (MOVE-04)."
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/ui/move/MoveHolder.kt"
      provides: "gcode_position X/Y/Z + per-axis homed gating model"
      contains: "class MoveHolder"
    - path: "app/src/main/java/works/mees/dinghy/ui/move/MoveScreen.kt"
      provides: "3×3 jog pad + Z row + distance selector + Home/Disable/Back gutter"
      contains: "ConfirmGuard"
  key_links:
    - from: "ui/move/MoveScreen.kt"
      to: "command/CommandDispatcher"
      via: "dispatch(key, GCODE_SCRIPT, scriptParams(PrinterCommands.jog/homeAll/homeAxis/DISABLE_STEPPERS))"
      pattern: "dispatch\\("
    - from: "ui/move/MoveHolder.kt"
      to: "state/PrinterState.gcodePosition + homedAxes"
      via: "read new gcodePosition field + per-axis homed flags"
      pattern: "gcodePosition|homedAxes"
---

<objective>
Build the Move panel (MOVE-01..04) — LAW per `04-move.png` + README §4, captured verbatim (D-03).
Mirror the Phase-4 Print Status holder+screen+dispatch+ConfirmGuard patterns.

Layout (D-03): Focus = 3×3 XY jog pad (edge arrows; center = XY home; corner cells = live X/Y/Z
value-on-glyph, label green=homed/amber=unhomed; one corner = amber Override to jog unhomed). Field =
Z row (∧/Z/∨, glyphs only at 3 cols) + a 6-up distance selector (0.1/1/5/10/50/100 mm, square buttons).
Gutter = Home (Accent) · Disable (Warn/amber) · Back (Danger/red).

Behaviors:
- MOVE-01: jog dispatches `PrinterCommands.jog(axis, ±selectedDist, feed)` (SAVE/G91/G1/RESTORE).
- MOVE-02: center cell → `homeAll()` (G28); per-axis home affordance → `homeAxis(a)`.
- MOVE-03: Disable → full-screen ConfirmGuard (amber, `destructive=false`) → on confirm dispatch
  `DISABLE_STEPPERS` (M84).
- MOVE-04: corner cells render `gcodePosition` X/Y/Z value-on-glyph (NOT toolheadPosition), label
  colored from per-axis `'x'/'y'/'z' in homedAxes`.
- Un-homed gating: amber axis labels; the amber Override cell is the ONLY way to jog an unhomed axis.
  The exact Override gcode is LOW-confidence (RESEARCH Open Q1) — confirmed live on flox in 05-08; for
  now wire the Override cell to a clearly-labeled placeholder action + surface any gcode error via
  SeverityToast, and document the open item in the SUMMARY.

Purpose: prove capability gating + confirm policy on a simpler surface before Files/Job.
Output: MoveHolder + MoveScreen, with a holder unit test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-core-print-control-panels-temperature-move-extrude/05-RESEARCH.md
@.planning/phases/05-core-print-control-panels-temperature-move-extrude/05-PATTERNS.md
@docs/ui_design/LAYOUT.md
@docs/ui_design/README.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MoveHolder — gcode_position + per-axis homed gating</name>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusHolder.kt (MIRROR — constructor+collect loop lines 46-74; NO second throttle)
    - app/src/test/java/works/mees/dinghy/ui/printstatus/PrintStatusHolderTest.kt (harness to copy)
    - app/src/main/java/works/mees/dinghy/state/PrinterState.kt (the new gcodePosition field from 05-01; homedAxes line 42)
    - .planning/phases/05-core-print-control-panels-temperature-move-extrude/05-RESEARCH.md §3 (gcode_position is X/Y/Z[/E]; homed_axes string lowercase) + Pitfall 1
  </read_first>
  <action>
    Create `ui/move/MoveHolder.kt` (plain Kotlin, host-testable; mirror PrintStatusHolder, no second throttle). Expose `vm: StateFlow<MoveVm>` where `MoveVm(x: Double?, y: Double?, z: Double?, xHomed: Boolean, yHomed: Boolean, zHomed: Boolean, allHomed: Boolean)`. On each `store.printerState` emission: read `state.gcodePosition` (`getOrNull(0/1/2)` → x/y/z; null when no position yet — never fabricate 0), and derive per-axis homed from `'x'/'y'/'z' in state.homedAxes` (lowercase, RESEARCH §3). `allHomed = xHomed && yHomed && zHomed`. Do NOT read toolheadPosition (Pitfall 1).
  </action>
  <verify>
    <automated>cmd.exe /c "E:\Android\gw.bat :app:testReleaseUnitTest --tests *MoveHolderTest --no-daemon"</automated>
  </verify>
  <acceptance_criteria>
    - `MoveHolder.kt` contains `class MoveHolder` and reads `gcodePosition` + `homedAxes` (not `toolheadPosition`).
    - New MoveHolderTest: seed `gcodePosition=[12.5,40.0,3.2,0.0]`, `homedAxes="xy"` → vm.x==12.5, vm.z==3.2, xHomed&&yHomed true, zHomed false, allHomed false.
    - Test: with no gcodePosition (null) the vm x/y/z are null (no fabricated 0).
    - `:app:testReleaseUnitTest` for MoveHolderTest passes.
  </acceptance_criteria>
  <done>Holder surfaces gcode_position + per-axis homed gating from the new field; holder test green.</done>
</task>

<task type="auto">
  <name>Task 2: MoveScreen — jog pad + Z row + distance selector + Home/Disable/Back gutter</name>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusScreen.kt (MIRROR — imports/collect/dispatch-failure toast plumbing; ScreenScaffold slots; ConfirmGuard estop pattern lines 161-176; gutter OutlinedControl Intent usage)
    - app/src/main/java/works/mees/dinghy/designsystem/ConfirmGuard.kt (signature lines 53-62; `destructive=false` = amber/green confirm)
    - app/src/main/java/works/mees/dinghy/command/PrinterCommands.kt (jog/homeAll/homeAxis/DISABLE_STEPPERS/scriptParams from 05-02)
    - app/src/main/java/works/mees/dinghy/designsystem/control/OutlinedControl.kt (Intent enum)
    - docs/ui_design/images/04-move.png (LAW) + README §4 + LAYOUT.md (Focus/Field/Gutter, sacred squares, ratio-only sizing)
  </read_first>
  <action>
    Create `ui/move/MoveScreen.kt`, `@Composable fun MoveScreen(container, holder: MoveHolder, onBack, modifier)`. Collect dispatcher + holder.vm + tokens. Copy the failure-toast plumbing. ScreenScaffold:
    - Focus: a 3×3 grid (each cell `aspectRatio(1f)` sacred square, ratio-only). Edge cells = directional jog arrows: top/bottom = Y+/Y−, left/right = X−/X+, dispatch `jog("Y",±dist,feed)` / `jog("X",±dist,feed)` keyed per direction. Center cell = XY home → `dispatch("home_xy", GCODE_SCRIPT, scriptParams(PrinterCommands.homeAll()))` (per mockup center = XY home). Corner cells render live X/Y/Z value-on-glyph (GeistMono) with label color `t.go` when that axis homed else `t.heat` (amber). One designated corner = the amber Override (Intent.Warn) — wire to the placeholder Override action (document the open gcode in SUMMARY) and gate normal jog on `vm.<axis>Homed` (an unhomed axis's normal jog is disabled/amber; Override is the escape).
    - Field: a Z row of 3 OutlinedControls (∧ = `jog("Z",+dist,feed)`, center Z label/glyph, ∨ = `jog("Z",-dist,feed)`) + a 6-up distance selector (0.1/1/5/10/50/100 mm) — selecting sets the active `distance` state (highlight the chosen, accent outline); square OutlinedControl tiles.
    - Gutter: `OutlinedControl("Home", { dispatch("home_all", GCODE_SCRIPT, scriptParams(PrinterCommands.homeAll())) }, Intent.Accent)` · `OutlinedControl("Disable", { showDisableGuard = true }, Intent.Warn)` · `OutlinedControl("Back", onBack, Intent.Danger)`.
    - `showDisableGuard` → `ConfirmGuard(title="Disable steppers?", message="Motors release; the toolhead can be moved by hand and axes become un-homed.", confirmLabel="DISABLE", onConfirm={ dispatcher?.dispatch("disable_steppers", GCODE_SCRIPT, scriptParams(PrinterCommands.DISABLE_STEPPERS)); showDisableGuard=false }, onCancel={showDisableGuard=false}, destructive=false)` (amber proceed-at-peril).
    Feed mm/min Claude's discretion (e.g. 3000 for XY, 600 for Z). Disable controls whose dispatch key is in `dispatcher.inFlight`. All color via LocalTokens; all layout via ScreenScaffold; live numbers GeistMono.
  </action>
  <verify>
    <automated>cmd.exe /c "E:\Android\gw.bat :app:compileReleaseKotlin --no-daemon" 2>&1 | tr -d '\r' | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `MoveScreen.kt` contains `ConfirmGuard(` with `destructive = false`, a 6-up distance selector, and gutter `OutlinedControl` with `Intent.Accent`/`Intent.Warn`/`Intent.Danger`.
    - Jog/home/disable all dispatch via `JsonRpcMethods.GCODE_SCRIPT` + `scriptParams(PrinterCommands.*)` — no raw rpc.request.
    - Corner cells read `holder.vm` x/y/z and color labels by per-axis homed (`t.go`/`t.heat`).
    - `grep "Color(0x" MoveScreen.kt` returns nothing.
    - `:app:compileReleaseKotlin` succeeds.
  </acceptance_criteria>
  <done>Move panel matches the locked mockup; jog/home/disable route through the dispatcher; disable behind ConfirmGuard; token-pure; compiles.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User tap → motion command | Jog / home / disable taps cross into the dispatcher → Moonraker motion. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06-T | Tampering | double-tap re-issues a jog / disable mid-move | mitigate | All taps via CommandDispatcher (debounce + in-flight); Disable additionally behind the full-screen ConfirmGuard (MOVE-03). |
| T-05-06-Safety | Safety | jogging an unhomed axis into a hard endstop / leaving relative mode | mitigate | Normal jog disabled on an unhomed axis (amber gating); the amber Override is the explicit, deliberate escape; jog gcode wraps SAVE/RESTORE_GCODE_STATE so the printer never stays in G91 (05-02). |
| T-05-06-T2 | Tampering | jog distance built from selector | mitigate | Distance is a FIXED 6-item set; `PrinterCommands.jog` clamps mm/feed (05-02) — no free-text. |
| T-05-06-D | DoS | hung jog wedging a control | mitigate | Dispatcher timeout clears the in-flight key in `finally`. |
</threat_model>

<verification>
Release compile + MoveHolder unit test green. Live jog/home/disable on real hardware + the amber
Override unhomed-jog gcode confirmation are 05-08 UAT items on flox + live Ender 5 Plus.
</verification>

<success_criteria>
- MOVE-01..04 all present: jog by preset, home all/per-axis, disable-behind-confirm, live gcode_position with per-axis homed gating.
- Every action via the dispatcher as GCODE_SCRIPT; token-pure; ScreenScaffold layout matching 04-move.png.
</success_criteria>

<output>
Create `.planning/phases/05-core-print-control-panels-temperature-move-extrude/05-06-SUMMARY.md` when done.
</output>
