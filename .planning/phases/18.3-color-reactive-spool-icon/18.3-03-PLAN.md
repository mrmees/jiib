---
phase: 18.3-color-reactive-spool-icon
plan: 03
type: execute
wave: 0
depends_on: []
files_modified:
  - docs/ui_design/THEMING.md
  - docs/ui_design/CLAUDE.md
autonomous: true
requirements: [SC3, D-10]
must_haves:
  truths:
    - "THEMING.md's bounded token carve-out explicitly sanctions the color-reactive spool filament band"
    - "ui_design/CLAUDE.md's token-carve-out bullet names the spool glyph as a literal second instance"
    - "The carve-out is scoped to the filament BAND only — body, keyline, and chrome stay token-routed"
    - "The docs record that brandTint was considered and rejected for the band (would distort true color)"
  artifacts:
    - path: "docs/ui_design/THEMING.md"
      provides: "Extended carve-out covering the spool band"
      contains: "spool"
    - path: "docs/ui_design/CLAUDE.md"
      provides: "Token-carve-out bullet made literal for the spool glyph"
      contains: "spool"
  key_links:
    - from: "THEMING.md carve-out §"
      to: "color-reactive spool band"
      via: "second instance of filament-color-is-data exception"
      pattern: "spool"
---

<objective>
Document the THEME-01 exception (SC3, D-10): EXTEND the existing bounded token carve-out in
`docs/ui_design/THEMING.md` and the `docs/ui_design/CLAUDE.md` token-carve-out bullet to explicitly
sanction the color-reactive spool filament band as a second instance of the same "filament color is
DATA, not chrome" exception. Both docs already NAME a Spoolman spool's filament color as the
precedent — this plan makes that precedent literal.

Purpose: SC3 requires the override of semantic-token tinting to be documented as a deliberate,
bounded exception — not silent rule-breaking. Keeping it as an EXTENSION of the existing PromptMarkup
author-hex carve-out (not a new rule) keeps the theming law coherent.
Output: extended carve-out text in both UI-LAW docs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/18.3-color-reactive-spool-icon/18.3-CONTEXT.md
@docs/ui_design/THEMING.md
@docs/ui_design/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend the token carve-out in THEMING.md + ui_design/CLAUDE.md for the spool band</name>
  <files>docs/ui_design/THEMING.md, docs/ui_design/CLAUDE.md</files>
  <read_first>
    - docs/ui_design/THEMING.md §"Carve-out: macro-authored PromptMarkup author-hex (D-03)" (lines 317-336 — the existing bounded exception to EXTEND; already cites a Spoolman spool's filament color as the same class of content-data-not-chrome)
    - docs/ui_design/CLAUDE.md "Token carve-out" bullet (already names Spoolman spool color as the precedent) + the "Icons are real Material Symbols by default" icon-source policy bullet
    - app/src/main/java/works/mees/dinghy/theme/BrandTint.kt (the `brandTint` WCAG-3:1 helper — considered and REJECTED for the band; stays correct for brand chrome only)
    - 18.3-CONTEXT.md D-10 (extend the existing carve-out; scope to band only; record brandTint rejection)
    - 18.3-PATTERNS.md §THEMING.md + CLAUDE.md (the change shape)
  </read_first>
  <action>
    EXTEND (do NOT invent a new section) the existing carve-out in `THEMING.md`
    §"Carve-out: macro-authored PromptMarkup author-hex (D-03)": add the color-reactive spool glyph
    as a SECOND named instance of the same "filament color is DATA, not chrome" exception (per D-10).
    State explicitly that the carve-out applies to the filament BAND ONLY — the spool body/flanges,
    the band keyline, and all surrounding chrome stay fully token-routed (`--text`/`--text2`/
    `--outline` etc.). Note that the band renders the EXACT filament hex (never clamped) and that
    `brandTint` (the WCAG-3:1 contrast-floor helper) was considered for legibility and REJECTED for
    the band because clamping would distort the true filament color — legibility is solved by the
    neutral keyline (framing, not distortion); `brandTint` remains correct for brand chrome only.
    Make the SAME extension to the `docs/ui_design/CLAUDE.md` "Token carve-out" bullet: turn its
    existing "a Spoolman spool's filament color" precedent reference into a literal sanction of the
    color-reactive spool band (band-only scope, keyline + body token-routed). Reference the phase
    decision IDs (D-02 body neutral, D-05 keyline + no-clamp, D-10 carve-out) for traceability.
  </action>
  <verify>
    <automated>grep -n -i "spool" docs/ui_design/THEMING.md docs/ui_design/CLAUDE.md | grep -i "band\|carve\|filament\|data" | head</automated>
  </verify>
  <acceptance_criteria>
    - THEMING.md carve-out section names the color-reactive spool band as a sanctioned second instance, scoped to the BAND only, and records the brandTint rejection.
    - ui_design/CLAUDE.md token-carve-out bullet literally sanctions the spool glyph band (band-only; keyline + body token-routed).
    - Both files mention "band" + "spool" in the carve-out context (grep returns matching lines); no NEW separate carve-out rule was introduced (it extends the existing PromptMarkup author-hex carve-out).
  </acceptance_criteria>
  <done>Both UI-LAW docs explicitly sanction the spool band as a bounded, band-only theming exception (SC3).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Documentation-only plan; no code, no input, no execution path. |

## STRIDE Threat Register

Documentation-only change. No trust boundary is crossed; no STRIDE category applies.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18.3-03 | N/A | docs/ui_design/*.md | accept | Markdown edits only — no executable surface, no input, no dependency change. |
| T-18.3-SC | Tampering | npm/pip/cargo installs | accept | No package installs. |
</threat_model>

<verification>
- grep confirms "spool"/"band" appear in the carve-out context of both docs (SC3 doc-review gate).
- Manual doc review: the carve-out is band-scoped and the brandTint rejection is recorded.
</verification>

<success_criteria>
- SC3: THEME-01 exception documented (THEMING.md + ui_design/CLAUDE.md), band-scoped, brandTint rejection noted.
- D-10: existing carve-out extended (not a new rule).
</success_criteria>

<output>
Create `.planning/phases/18.3-color-reactive-spool-icon/18.3-03-SUMMARY.md` when done.
</output>
