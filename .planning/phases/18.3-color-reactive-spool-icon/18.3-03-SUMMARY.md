---
phase: 18.3-color-reactive-spool-icon
plan: 03
subsystem: ui-design-docs
tags: [theming, carve-out, documentation, spool, THEME-01]
requires:
  - "docs/ui_design/THEMING.md PromptMarkup author-hex carve-out (Phase 12, D-03)"
  - "docs/ui_design/CLAUDE.md token-carve-out bullet"
provides:
  - "THEMING.md carve-out extended to sanction the color-reactive spool filament band"
  - "ui_design/CLAUDE.md token-carve-out bullet made literal for the spool glyph"
affects:
  - "Phase 18.3 implementation plans (01/02/04) — band-only reactivity is now documented LAW"
  - "future theme-UI conformance audit (treats the band as sanctioned, not a violation)"
tech-stack:
  added: []
  patterns:
    - "Extend an existing bounded token carve-out instead of inventing a new theming rule (D-10)"
key-files:
  created: []
  modified:
    - "docs/ui_design/THEMING.md"
    - "docs/ui_design/CLAUDE.md"
decisions:
  - "D-10: extend the existing PromptMarkup author-hex carve-out, not a new rule"
  - "Carve-out scoped to the filament BAND only; body, keyline, chrome stay token-routed (D-02)"
  - "Band renders true filament hex, never clamped; legibility via neutral keyline (D-05)"
  - "brandTint considered and REJECTED for the band (would distort true color); brand-chrome only"
metrics:
  duration: ~6min
  completed: 2026-06-07
---

# Phase 18.3 Plan 03: Document the THEME-01 Spool-Band Carve-out Summary

Documented the color-reactive spool filament band as a bounded, band-only theming exception by
EXTENDING the existing PromptMarkup author-hex carve-out in both UI-LAW docs — making the
"a Spoolman spool's filament color" precedent that both docs already named into a literal sanction.

## What Was Built

A docs-only change (SC3 / D-10) across the two UI-LAW files:

- **`docs/ui_design/THEMING.md`** — added a "Second instance: the color-reactive spool glyph's
  filament band (Phase 18.3, D-10)" subsection inside the existing
  §"Carve-out: macro-authored PromptMarkup author-hex (D-03)". It names the spool band as the second
  instance of the same "filament color is DATA, not chrome" exception, scopes the carve-out to the
  BAND only (body/flanges, keyline, and chrome stay `--text`/`--text2`/`--outline` token-routed),
  records that the band renders the EXACT filament hex (never clamped) with legibility from the
  neutral keyline (framing, not distortion), and documents that `brandTint` was considered and
  rejected for the band. The closing audit note now covers both the PromptMarkup runs and the band.
- **`docs/ui_design/CLAUDE.md`** — the "Token carve-out" bullet header now reads "…author-hex (D-03)
  AND the color-reactive spool band (18.3 D-10)"; the body turns the existing Spoolman-spool-color
  precedent into a literal sanction of the spool glyph band (band-only scope; body + keyline
  token-routed; empty-spool fallback = pure token chrome; brandTint rejected for the band, kept for
  brand chrome only). References D-02/D-05/D-10 for traceability and dated 2026-06-07; 18.3-03.

## Tasks Completed

| Task | Name | Commit | Files |
| ---- | ---- | ------ | ----- |
| 1 | Extend the token carve-out in THEMING.md + ui_design/CLAUDE.md for the spool band | `8fa948f` | docs/ui_design/THEMING.md, docs/ui_design/CLAUDE.md |

## Verification

Automated grep gate (plan `<verify>`):
```
grep -n -i "spool" docs/ui_design/THEMING.md docs/ui_design/CLAUDE.md | grep -i "band\|carve\|filament\|data"
```
Returns matching lines in BOTH files in the carve-out context — PASS. The new content extends the
existing PromptMarkup author-hex carve-out (no new separate carve-out section was introduced); both
files mention "band" + "spool" in the carve-out context.

All four must-have truths satisfied:
1. THEMING.md carve-out explicitly sanctions the color-reactive spool filament band. ✓
2. ui_design/CLAUDE.md token-carve-out bullet names the spool glyph as a literal second instance. ✓
3. Carve-out scoped to the filament BAND only — body, keyline, chrome stay token-routed. ✓
4. Docs record that brandTint was considered and rejected for the band. ✓

## Deviations from Plan

None — plan executed exactly as written.

## Success Criteria

- **SC3:** THEME-01 exception documented in both THEMING.md + ui_design/CLAUDE.md, band-scoped, with
  the brandTint rejection recorded. ✓
- **D-10:** existing carve-out EXTENDED (not a new rule). ✓

## Self-Check: PASSED

- FOUND: docs/ui_design/THEMING.md (modified, contains spool band carve-out subsection)
- FOUND: docs/ui_design/CLAUDE.md (modified, token-carve-out bullet literal for spool band)
- FOUND: commit 8fa948f
