---
phase: 18.3-color-reactive-spool-icon
plan: 02
type: execute
wave: 1
depends_on: ["18.3-01"]
files_modified:
  - app/src/main/java/works/mees/dinghy/designsystem/icons/SpoolGlyph.kt
  - app/src/main/java/works/mees/dinghy/preview/SpoolGlyphPreviews.kt
  - app/src/test/java/works/mees/dinghy/designsystem/icons/SpoolGlyphTest.kt
autonomous: true
requirements: [SC2, SC4, SC5, SC6, D-02, D-03, D-04, D-05, D-08]
must_haves:
  truths:
    - "SpoolGlyph draws a neutral-token body + a runtime-colored filament band in one shared coordinate frame"
    - "Empty swatch list omits the band entirely (honest empty spool, not a recolored placeholder) — D-03"
    - "A single swatch fills the band flat; two+ swatches render a two-stop linear gradient across the band — D-04/D-08"
    - "The band carries a thin neutral-token keyline so its edge reads against any background — D-05"
    - "A @Preview matrix renders empty / single / two-color band across dark + light + custom at fs=L"
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/designsystem/icons/SpoolGlyph.kt"
      provides: "The reactive spool composable (body Icon + band Canvas + keyline)"
      contains: "fun SpoolGlyph"
    - path: "app/src/main/java/works/mees/dinghy/preview/SpoolGlyphPreviews.kt"
      provides: "@Preview matrix (empty/single/two-color × dark/light/custom × fs=L, white-PLA + black filament)"
      contains: "@Preview"
  key_links:
    - from: "SpoolGlyph body Icon"
      to: "R.drawable.spool"
      via: "painterResource flat-tint"
      pattern: "painterResource\\(R\\.drawable\\.spool\\)"
    - from: "SpoolGlyph band Canvas"
      to: "Brush.linearGradient / SolidColor"
      via: "swatch-count branch"
      pattern: "Brush\\.linearGradient|SolidColor"
---

<objective>
Build the dedicated `SpoolGlyph` composable — the single rendering seam for the color-reactive spool
across all five surfaces — plus its `@Preview` matrix (Wave 0 validation requirement) and an optional
pure render-branch host test. The body is a token-tinted `Icon` (existing flat-tint contract); the
band is a Compose `Canvas` drawing a runtime gradient/solid/nothing with a neutral keyline.

Purpose: Isolate the only genuinely-new code (the two-layer glyph) so it can be designed, previewed,
and unit-checked before being wired into screens (plan 03). Mirrors `ColorWheel.kt`'s
literal-data-color + token-outline precedent on the Adreno-320 budget.
Output: `SpoolGlyph.kt`, `SpoolGlyphPreviews.kt`, and (optional) `SpoolGlyphTest.kt`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18.3-color-reactive-spool-icon/18.3-CONTEXT.md
@.planning/phases/18.3-color-reactive-spool-icon/18.3-RESEARCH.md
@.planning/phases/18.3-color-reactive-spool-icon/18.3-PATTERNS.md
@docs/ui_design/PREVIEW_AND_TOKENS.md
@app/src/main/res/drawable/spool.xml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SpoolGlyph composable + pure swatch→branch helper + host test</name>
  <files>app/src/main/java/works/mees/dinghy/designsystem/icons/SpoolGlyph.kt, app/src/test/java/works/mees/dinghy/designsystem/icons/SpoolGlyphTest.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/designsystem/ColorWheel.kt (the EXACT analog: Canvas + Brush + literal-color fill + token-keyline; geometry from `size` :114-118; explicit gradient stops :184-188; static-no-animation discipline :141)
    - app/src/main/java/works/mees/dinghy/designsystem/icons/DinghyIconView.kt (lines 63-69 the flat-tint `Icon(painter, tint)` body contract to REUSE — do NOT modify this file)
    - app/src/main/res/drawable/spool.xml (the body vector + the documented band-rectangle fractions to reproduce in the Canvas)
    - app/src/main/java/works/mees/dinghy/ui/spool/SpoolScreen.kt (the promoted `parseNormalizedHex` from plan 01 — call it, do not duplicate)
    - 18.3-RESEARCH.md "The dedicated reactive glyph (recommended seam — Option C)" + Pitfalls 3/4/5
    - 18.3-PATTERNS.md §SpoolGlyph.kt (the recommended shape + imports)
    - 18.3-CONTEXT.md D-02/D-03/D-04/D-05/D-08
  </read_first>
  <behavior>
    - Test (pure helper): swatch count 0 → render branch = Empty (band omitted) — D-03
    - Test (pure helper): swatch count 1 → render branch = Solid(swatch[0]) — D-08
    - Test (pure helper): swatch count 2 → render branch = Gradient(swatch[0], swatch[1]) — D-04
    - Test (pure helper): swatch count 3+ → render branch = Gradient using FIRST TWO swatches only (take 2) — D-04/D-09
  </behavior>
  <action>
    Create `SpoolGlyph.kt` in the `designsystem.icons` package with a
    `@Composable fun SpoolGlyph(swatches: List<Color>, bodyTint: Color, keyline: Color, sizeDp: Dp,
    modifier: Modifier = Modifier, contentDescription: String? = null)`. Wrap both layers in ONE
    `Box(modifier.size(sizeDp))` so the body and band share one coordinate frame (Pitfall 3):
    (1) body = `Icon(painterResource(R.drawable.spool), contentDescription, Modifier.fillMaxSize(),
    tint = bodyTint)` — reuses the existing flat-tint contract for the neutral body (D-02, body NOT
    reactive); (2) band = `Canvas(Modifier.fillMaxSize())` that, when `swatches.isEmpty()`,
    returns immediately drawing NOTHING in the band (D-03 empty spool — Pitfall 5: never a dead-token
    placeholder). Otherwise compute the band path from the SAME 0..1 fractions spool.xml left empty
    (derive ALL geometry from `size`, ColorWheel idiom), then fill it: 1 swatch → `SolidColor`;
    2+ swatches → `Brush.linearGradient(0f to swatches[0], 1f to swatches[1], start = Offset(bandLeft,
    bandCenterY), end = Offset(bandRight, bandCenterY))` with EXPLICIT start/end to the band bounds
    (Pitfall 4 — default brush spans the whole DrawScope and washes the gradient out). Take only the
    first two swatches (D-09 index [0..1]). Finally stroke the band path with the `keyline` color
    (`Stroke(width = …)`, neutral token, D-05) so the edge reads against any background — do NOT
    clamp the fill color via `brandTint` (D-05 explicit rejection). Gradient direction + keyline
    width are Claude's discretion (RESEARCH Q3: horizontal flange-to-flange, ~1–1.5dp-equivalent).
    Keep the band STATIC — no animation (Adreno-320 floor); build the brush inside the DrawScope
    (cheap, one instance per surface — do NOT add a remember-cache unless profiling demands it).
    Factor the swatch-count → render-branch decision into a small PURE function (e.g.
    `internal sealed`/`enum BandRender` + `internal fun bandRenderFor(swatches): BandRender`) so it
    is host-testable without Compose. Write `SpoolGlyphTest.kt` asserting the 4 behaviors above
    against that pure helper. Do NOT extend `DinghyIconView`'s `tint` to a `Brush`.
  </action>
  <verify>
    <automated>cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --tests works.mees.dinghy.designsystem.icons.SpoolGlyphTest --no-daemon"</automated>
  </verify>
  <acceptance_criteria>
    - `SpoolGlyph.kt` contains `fun SpoolGlyph(...)`, references `painterResource(R.drawable.spool)`, and matches `Brush\.linearGradient|SolidColor`; does NOT reference `brandTint` and does NOT modify `DinghyIconView`.
    - Empty-swatch branch returns from the Canvas without drawing a band fill (verified via the pure `bandRenderFor` helper → Empty).
    - `SpoolGlyphTest` GREEN: 0→Empty, 1→Solid, 2→Gradient, 3+→Gradient(first two only).
    - Gradient uses explicit `start`/`end` Offsets bound to the band rectangle (not default DrawScope span).
  </acceptance_criteria>
  <done>SpoolGlyph renders body+band+keyline with correct swatch-count branching, host-tested.</done>
</task>

<task type="auto">
  <name>Task 2: @Preview matrix for SpoolGlyph (Wave 0 validation)</name>
  <files>app/src/main/java/works/mees/dinghy/preview/SpoolGlyphPreviews.kt</files>
  <read_first>
    - docs/ui_design/PREVIEW_AND_TOKENS.md (the preview-first convention: PreviewBox idiom, minimized matrix shape, fs=L via fsLargeSeed gotcha, @Nexus7Previews, no live Moonraker)
    - app/src/main/java/works/mees/dinghy/preview/SpoolPreviews.kt (the existing Phase-18 spool preview analog — PreviewBox, SampleFixtures, theme combos)
    - app/src/main/java/works/mees/dinghy/designsystem/icons/SpoolGlyph.kt (the composable under preview, from Task 1)
    - 18.3-VALIDATION.md §Wave 0 Requirements (the required @Preview matrix spec)
    - 18.3-CONTEXT.md D-03/D-05 (empty fallback + keyline-against-extreme-fill legibility)
  </read_first>
  <action>
    Create `SpoolGlyphPreviews.kt` in the `preview` package following `SpoolPreviews.kt` and
    `PREVIEW_AND_TOKENS.md` exactly. Render the `SpoolGlyph` in a `PreviewBox` matrix that exercises:
    THREE band states — empty (`emptyList()`), single-color, and two-color — across the theme combos
    (dark + light + custom) at `fs=L` (use the `fsLargeSeed` gotcha noted in PREVIEW_AND_TOKENS).
    Include the keyline stress cases REQUIRED by SC5: white PLA (`#FFFFFF`) on the light theme and
    black filament (`#000000`) on the dark theme — these are the cases where the band fill ≈ surface
    and only the neutral keyline makes the edge read. Pass `bodyTint`/`keyline` from `LocalTokens`
    (neutral roles, e.g. `t.text2`/`t.outline`). No live Moonraker — feed literal `Color` lists.
    Annotate with `@Nexus7Previews` (or the project's standard device/locale multipreview). This is
    the SC2/SC4/SC5 visual gate authors eyeball in Studio before the on-device flox UAT (plan 04).
  </action>
  <verify>
    <automated>cmd.exe /c "E:\Android\gw.bat :app:compileDebugKotlin --no-daemon"</automated>
  </verify>
  <acceptance_criteria>
    - `SpoolGlyphPreviews.kt` exists with `@Preview` (or `@Nexus7Previews`) functions covering empty + single + two-color band on dark + light + custom at fs=L, including a `#FFFFFF` and a `#000000` filament case.
    - Compiles clean (`:app:compileDebugKotlin` BUILD SUCCESSFUL); uses `PreviewBox`/`LocalTokens`, no live Moonraker calls.
    - @Preview empty-spool variant renders the body with the band ABSENT (D-03) — visual confirm in Studio.
  </acceptance_criteria>
  <done>The SpoolGlyph @Preview matrix compiles and covers the empty/single/two-color × theme × keyline-stress cases.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Resolved `List<Color>` swatches → Canvas | Colors already parsed upstream; SpoolGlyph receives typed `Color`, no string parsing here. |

## STRIDE Threat Register

Pure-rendering composable; no network, no auth, no I/O, no persistence. Colors arrive as typed
`Color` values (parsing happens in plan 01's promoted helper, consumed in plan 03).

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18.3-02 | Denial of Service | Canvas draw on Adreno 320 | accept | Band is static (no animation), one instance per surface, brush built once per recomposition — negligible fill cost (RESEARCH A2). On-device UAT (plan 04) is the verification; discretion watch-item is the documented fallback (flat dominant swatch at small sizes). |
| T-18.3-SC | Tampering | npm/pip/cargo installs | accept | No package installs (no new dependencies). |
</threat_model>

<verification>
- `SpoolGlyphTest` GREEN (pure branch logic, SC2/SC4 decision coverage).
- `:app:compileDebugKotlin` BUILD SUCCESSFUL with the preview matrix.
- Studio-eyeball of the @Preview matrix (SC2/SC4/SC5 — flagged manual; on-device flox UAT is plan 04).
</verification>

<success_criteria>
- SC2 (partial): band renders solid (1) / two-stop gradient (2+) — host-tested branch + preview.
- SC4 (partial): empty swatch list → band omitted (empty spool) — host-tested + preview.
- SC5 (partial): neutral keyline present; white/black filament keyline-stress cases in the preview matrix.
- SC6 (partial): SpoolGlyph + preview + test compile and pass green.
- D-02/D-03/D-04/D-05/D-08 implemented in the glyph.
</success_criteria>

<output>
Create `.planning/phases/18.3-color-reactive-spool-icon/18.3-02-SUMMARY.md` when done.
</output>
