---
phase: 18.3-color-reactive-spool-icon
plan: 04
type: execute
wave: 2
depends_on: ["18.3-01", "18.3-02"]
files_modified:
  - app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusScreen.kt
  - app/src/main/java/works/mees/dinghy/ui/shell/AppDrawer.kt
  - app/src/main/java/works/mees/dinghy/ui/shell/AppShell.kt
  - app/src/main/java/works/mees/dinghy/ui/spool/SpoolScreen.kt
autonomous: false
requirements: [SC2, SC4, SC5, SC6, D-06, D-07, D-08, D-09]
must_haves:
  truths:
    - "The Standby launcher Spool tile renders the reactive SpoolGlyph tinted by the active spool's color (D-06.1)"
    - "The mid-print shortcut-row Spool slot renders the reactive SpoolGlyph (D-06.3)"
    - "The App Drawer Spool tile renders the reactive SpoolGlyph via the optional-glyph escape hatch (D-06.2)"
    - "SpoolScreen empty-state shows the empty SpoolGlyph instead of the generic inventory_2 box (D-06.4)"
    - "SpoolScreen detail pane shows a SpoolGlyph tinted by the SELECTED spool's own color (D-06.5/D-08)"
    - "Loaded-context surfaces resolve color Spoolman-FIRST → gcode filament_colors[0] → empty spool (D-07)"
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusScreen.kt"
      provides: "D-07 precedence resolution + SpoolGlyph at launcher tile + shortcut row"
      contains: "SpoolGlyph"
    - path: "app/src/main/java/works/mees/dinghy/ui/shell/AppDrawer.kt"
      provides: "DrawerTileSpec.glyph escape hatch + Spool tile reactive glyph"
      contains: "glyph"
  key_links:
    - from: "PrintStatusScreen spoolDetail/printMetadata"
      to: "spoolSwatches (D-07 precedence)"
      via: "Spoolman-first then gcode fallback"
      pattern: "colorSwatches|filamentColors"
    - from: "AppShell active-spool state"
      to: "AppDrawer spoolSwatches param"
      via: "stateless threading (sibling to spoolEnabled)"
      pattern: "spoolSwatches"
    - from: "AppDrawer Spool tile glyph lambda"
      to: "SpoolGlyph"
      via: "DrawerTileSpec.glyph escape hatch"
      pattern: "SpoolGlyph"
---

<objective>
Wire `SpoolGlyph` into all five agreed surfaces (D-06.1–.5) with the D-07/D-08 color precedence, then
verify legibility on-device (flox, real Adreno 320). This is the payoff wave: every spool-icon
surface becomes color-reactive with the universal empty-spool fallback.

Purpose: Deliver SC2 (renders tinted by actual filament color on the agreed surfaces), SC4 (sensible
empty-spool fallback everywhere), and SC5 (legible across themes — verified on the hardware floor).
Output: reactive spool on the launcher tile, mid-print shortcut, drawer tile, and both SpoolScreen
surfaces; the on-device UAT sign-off.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18.3-color-reactive-spool-icon/18.3-CONTEXT.md
@.planning/phases/18.3-color-reactive-spool-icon/18.3-RESEARCH.md
@.planning/phases/18.3-color-reactive-spool-icon/18.3-PATTERNS.md
@app/src/main/java/works/mees/dinghy/designsystem/icons/SpoolGlyph.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: PrintStatusScreen — D-07 precedence + SpoolGlyph at launcher tile & shortcut row</name>
  <files>app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusScreen.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusScreen.kt (active-spool seam :154-176 `activeSpool`/`spoolDetail`; `metadata` collected :135; launcher tile `LauncherTile`/`DinghyIconView` ~:1118-1123; shortcut `ShortcutRow` ~:1185-1215)
    - app/src/main/java/works/mees/dinghy/spool/SpoolmanModels.kt (`SpoolmanSpool.filament` :101; `colorSwatches` :82-90 — read-only)
    - app/src/main/java/works/mees/dinghy/state/PrintMetadata.kt (`PrintMetadata.filamentColors` — added in plan 01; the gcode fallback source)
    - app/src/main/java/works/mees/dinghy/designsystem/icons/SpoolGlyph.kt (the composable to call; from plan 02)
    - app/src/main/java/works/mees/dinghy/ui/spool/SpoolScreen.kt (the promoted `internal parseNormalizedHex` from plan 01)
    - 18.3-RESEARCH.md "Resolving the D-07 precedence" code example + Open Q1/A1
    - 18.3-CONTEXT.md D-06.1/D-06.3/D-07/D-09; memory: dinghy-font-sizes-too-small (use fsSp, never hardcode)
  </read_first>
  <action>
    In `PrintStatusScreen`, compute a `spoolSwatches: List<Color>` from the state ALREADY collected
    (no new fetch — D-07 note), applying the precedence: `spoolDetail?.filament?.colorSwatches`
    mapped via the promoted `parseNormalizedHex` (taking it if non-empty) → ELSE
    `metadata?.filamentColors?.firstOrNull()` parsed to a single-element list (gcode fallback, D-07,
    using the `PrintMetadata.filamentColors` plumbed in plan 01) → ELSE `emptyList()` (empty spool,
    D-03). Use index [0] / first loaded spool only (D-09). Thread `spoolSwatches` down into
    `LauncherTile` and `ShortcutRow`. At the launcher-tile icon slot (~:1118): when the tile's dest
    is the Spool launcher dest, render `SpoolGlyph(swatches = spoolSwatches, bodyTint = t.text2,
    keyline = <neutral token, e.g. t.outline>, sizeDp = fsSp(40f, t.fs).dp, contentDescription =
    label)` INSTEAD of `DinghyIconView(launcherIcon(dest), …)`; keep the `t.text2` body tint and
    `fsSp(40f, t.fs).dp` size unchanged (matches sibling tiles). The mid-print shortcut Spool slot
    routes through the same per-dest `LauncherTile`, so the launcher-tile swap covers it — just
    ensure `ShortcutRow` receives + forwards `spoolSwatches` alongside its existing `spoolmanPresent`.
    NEVER hardcode sizes — use the established `fsSp(baseSp, t.fs)` scale.
  </action>
  <verify>
    <automated>cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --no-daemon"</automated>
  </verify>
  <acceptance_criteria>
    - PrintStatusScreen contains a `spoolSwatches` resolution that references `colorSwatches` (Spoolman first) AND `filamentColors` (gcode fallback), terminating in `emptyList()`.
    - The Spool launcher tile + shortcut Spool slot render `SpoolGlyph(...)` (grep matches `SpoolGlyph`) at `fsSp(40f, t.fs)`; other launcher tiles still use `DinghyIconView`.
    - Full `:app:testDebugUnitTest` GREEN (no regression).
  </acceptance_criteria>
  <done>Launcher tile + mid-print shortcut render the reactive spool with D-07 precedence.</done>
</task>

<task type="auto">
  <name>Task 2: AppDrawer escape hatch + SpoolScreen empty-state + detail pane</name>
  <files>app/src/main/java/works/mees/dinghy/ui/shell/AppDrawer.kt, app/src/main/java/works/mees/dinghy/ui/shell/AppShell.kt, app/src/main/java/works/mees/dinghy/ui/spool/SpoolScreen.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/shell/AppDrawer.kt (`DrawerTileSpec` :129-135; Spool tile `symbol = "inventory_2"` :178; icon render slot :258-262; `spoolEnabled` param :84 + how spoolEnabled/webcamEnabled are threaded :78-86)
    - app/src/main/java/works/mees/dinghy/ui/shell/AppShell.kt (active-spool collection :270-281 `spoolEnabled`/`activeSpoolStatus`/`spoolHolder`; AppDrawer call site :842-846)
    - app/src/main/java/works/mees/dinghy/ui/spool/SpoolScreen.kt (empty-state `SpoolDetailFocus` :321-329 `DinghyIcons.Inventory`; detail-pane `spoolColor` :312; `DetailSwatch` :432-448; promoted `parseNormalizedHex`)
    - app/src/main/java/works/mees/dinghy/designsystem/icons/SpoolGlyph.kt (the composable to call)
    - 18.3-RESEARCH.md "The drawer-tile escape hatch (D-06.2)" + Assumption A3 (drawer uses active-spool color)
    - 18.3-CONTEXT.md D-06.2/D-06.4/D-06.5/D-08
  </read_first>
  <action>
    AppDrawer (D-06.2 escape hatch): add an OPTIONAL
    `glyph: (@Composable (tint: Color) -> Unit)? = null` to `DrawerTileSpec` (only the Spool tile
    sets it; every other tile keeps `symbol: String`). At the icon render slot (:258-262) replace the
    bare `MaterialSymbol(...)` with `tile.glyph?.invoke(contentColor) ?: MaterialSymbol(name =
    tile.symbol, tint = contentColor, sizeSp = fsSp(40f, t.fs))`. Add a stateless
    `spoolSwatches: List<Color> = emptyList()` param to `AppDrawer(...)` (sibling to `spoolEnabled`),
    and have the Spool tile's `glyph` lambda call `SpoolGlyph(swatches = spoolSwatches, bodyTint =
    contentColor, keyline = <neutral token>, sizeDp = fsSp(40f, t.fs).dp)`. The `spoolEnabled`
    capability gate is unchanged. In AppShell (the call site :842-846), resolve the active-spool
    swatches from the already-collected `activeSpoolStatus`/`spoolHolder` detail (same source the
    launcher tile uses — A3 confirms the drawer wants the active-spool color), map via the promoted
    `parseNormalizedHex`, and pass `spoolSwatches = …` into `AppDrawer`. Mirror exactly how
    `spoolEnabled` is threaded — the drawer stays stateless.
    SpoolScreen empty-state (D-06.4): at `SpoolDetailFocus` :321-329, replace
    `DinghyIconView(DinghyIcons.Inventory, …)` with `SpoolGlyph(swatches = emptyList(), bodyTint =
    t.text3, keyline = <neutral token>, sizeDp = fsSp(64f, t.fs).dp, contentDescription =
    stringResource(R.string.cd_spool_empty))` — the honest empty (band-omitted) spool. Keep the
    `t.text3` tint + `fsSp(64f, t.fs).dp` size.
    SpoolScreen detail pane (D-06.5/D-08): add a reactive `SpoolGlyph` tinted by the SELECTED spool's
    OWN color (NOT the active spool) — resolve from `spool.filament?.colorSwatches?.mapNotNull
    (::parseNormalizedHex)` (the existing `:312` swatch pattern), at `fsSp(64f, t.fs).dp`. Reuse the
    promoted `parseNormalizedHex`; do NOT duplicate parsing. `DinghyIcons.Inventory` stays in the
    registry (verify no other consumer before any removal — leave it).
  </action>
  <verify>
    <automated>cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --no-daemon"</automated>
  </verify>
  <acceptance_criteria>
    - `DrawerTileSpec` has an optional `glyph: (@Composable (tint: Color) -> Unit)?`; the render slot calls `tile.glyph?.invoke(contentColor) ?: MaterialSymbol(...)`; only the Spool tile sets `glyph`.
    - `AppDrawer` gains a `spoolSwatches: List<Color>` param; AppShell threads it from the active-spool state (grep matches `spoolSwatches` in both files).
    - SpoolScreen empty-state renders `SpoolGlyph(swatches = emptyList(), …)` (no longer `DinghyIcons.Inventory`); detail pane renders a `SpoolGlyph` from the selected spool's `colorSwatches`.
    - Full `:app:testDebugUnitTest` GREEN.
  </acceptance_criteria>
  <done>All five surfaces render the reactive (or empty) SpoolGlyph; the drawer escape hatch is in place.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: On-device flox UAT — band legibility, empty fallback, theme legibility</name>
  <files>(no files — on-device verification of the work from Tasks 1-2)</files>
  <action>
    This is a blocking human-verification checkpoint, not an autonomous edit. The orchestrator builds
    and installs the debug APK on flox; Matthew navigates and eyeballs the wired surfaces per the
    how-to-verify steps below. No code is written in this task unless the watch-item fallback is
    triggered (gradient illegible at small tile size → apply flat dominant-swatch fill on small
    surfaces), in which case a follow-up edit to SpoolGlyph/its call sites is made and re-verified.
  </action>
  <what-built>
    The color-reactive spool glyph is wired on all five surfaces (launcher tile, mid-print shortcut,
    App Drawer Spool tile, SpoolScreen empty-state + detail pane) with Spoolman-first → gcode →
    empty-spool color precedence and a neutral keyline. Full host unit suite is green.
  </what-built>
  <how-to-verify>
    Orchestrator builds + installs the debug APK on flox (LineageOS 18.1 / API 30, genuine Adreno 320).
    Matthew navigates and eyeballs (per the dinghy-display-ondevice-iteration memory — do NOT burn
    turns adb-driving the app). Verify, using a live printer / Spoolman:
    1. SC2 — load a Spoolman spool with a known color; the launcher Spool tile + App Drawer Spool tile
       + mid-print shortcut band show that color. Two-color filament: confirm the two-stop gradient is
       distinguishable at launcher/drawer tile size — if it does NOT read small, apply the CONTEXT
       watch-item (flat dominant-swatch fill on small surfaces, keep gradient on the larger
       SpoolScreen icon).
    2. SC4 — with no loaded filament / unknown color, confirm the body draws and the band is ABSENT
       (honest empty spool, not a recolored placeholder) on every surface, including SpoolScreen
       empty-state.
    3. SC5 — white PLA on the LIGHT theme and black filament on the DARK theme: confirm the band edge
       reads via the neutral keyline on each of dark / light / custom. Use the dev theme cycler.
    4. D-07 — without Spoolman but mid-print with a sliced filament_color, confirm the gcode fallback
       color appears; D-08 — in SpoolScreen detail, the band reflects the TAPPED spool's own color.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (e.g. "gradient doesn't read at tile size — apply flat-fill fallback").</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Spoolman/gcode color strings → UI band | LAN-fetched `colorHex`/`filament_colors` strings flow through the promoted `parseNormalizedHex` into the SpoolGlyph band. |

## STRIDE Threat Register

Wiring phase: no new network ingress (consumes already-fetched Spoolman/gcode color flows), no auth,
no persistence change.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18.3-04 | Tampering | malformed `colorHex`/`filament_colors` reaching the band | mitigate | All color strings pass through `parseNormalizedHex` → `normalizeColorHex` (null on malformed) before becoming a `Color`; a null/empty resolution renders the empty spool (D-03), never crashes. `colorSwatches`/`stringArray` drop invalid entries. |
| T-18.3-05 | Denial of Service | reactive recomposition of the glyph on color flow updates | accept | Band is static (no animation); recomposes only when resolved swatches change; one instance per surface. On-device UAT (Task 3) confirms no jank on the Adreno-320 floor. |
| T-18.3-SC | Tampering | npm/pip/cargo installs | accept | No package installs (no new dependencies). |
</threat_model>

<verification>
- Full `:app:testDebugUnitTest` GREEN after both wiring tasks (SC6).
- On-device flox UAT (Task 3, blocking): SC2 band-by-color, SC4 empty fallback, SC5 keyline legibility across themes, D-07/D-08 precedence — manual, on real Adreno 320.
</verification>

<success_criteria>
- SC2: reactive band on all five agreed surfaces (D-06.1–.5) — verified on-device.
- SC4: empty-spool fallback on every surface — verified on-device.
- SC5: legible across dark + light + custom (keyline) — verified on-device.
- SC6: full unit suite green.
- D-06/D-07/D-08/D-09 wired.
</success_criteria>

<output>
Create `.planning/phases/18.3-color-reactive-spool-icon/18.3-04-SUMMARY.md` when done (after the UAT sign-off).
</output>
