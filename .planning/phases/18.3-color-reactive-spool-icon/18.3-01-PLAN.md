---
phase: 18.3-color-reactive-spool-icon
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - app/src/main/res/drawable/spool.xml
  - app/src/main/java/works/mees/dinghy/designsystem/icons/DinghyIcons.kt
  - app/src/test/java/works/mees/dinghy/designsystem/icons/DinghyIconsTest.kt
  - app/src/main/java/works/mees/dinghy/ui/spool/SpoolScreen.kt
  - app/src/main/java/works/mees/dinghy/state/PrintMetadata.kt
autonomous: true
requirements: [SC1, SC6, D-01, D-07]
must_haves:
  truths:
    - "DinghyIcons.LauncherSpool resolves to a custom spool VectorDrawable (no longer the `database` ligature)"
    - "spool.xml draws the spool body/flanges ONLY — the wound-filament band region is empty negative space"
    - "DinghyIconsTest passes green with the spool drawable in the keeper set"
    - "parseNormalizedHex is callable from outside SpoolScreen (no duplicated parse logic)"
    - "The live active-file print metadata carries filament_colors for the gcode fallback (D-07)"
  artifacts:
    - path: "app/src/main/res/drawable/spool.xml"
      provides: "Custom side-view spool body/flanges vector (band region excluded)"
      contains: "viewportWidth"
    - path: "app/src/main/java/works/mees/dinghy/designsystem/icons/DinghyIcons.kt"
      provides: "LauncherSpool flipped to IconRef.Drawable(R.drawable.spool)"
      contains: "R.drawable.spool"
  key_links:
    - from: "DinghyIcons.LauncherSpool"
      to: "R.drawable.spool"
      via: "IconRef.Drawable"
      pattern: "IconRef\\.Drawable\\(R\\.drawable\\.spool\\)"
    - from: "DinghyIconsTest.registryDrawableEntries_areOnlyTheCustomKeepers"
      to: "DinghyIcons.all drawable keepers"
      via: "expected keeper set"
      pattern: "launcher_spool"
---

<objective>
Lay the registry + asset + data foundation for the color-reactive spool glyph: author the custom
spool body vector, flip `LauncherSpool` from the `database` ligature to that drawable (SC1, D-01),
update the drift-guard test in the SAME wave so the build stays green (SC6, Pitfall 1), promote the
shared hex→Color parser, and thread `filament_colors` onto the live active-file metadata so the
gcode color fallback (D-07) has a real source.

Purpose: Every downstream plan (`SpoolGlyph`, surface wiring) depends on the asset, the flipped
registry entry, the promoted parser, and the gcode color source existing first. This is the
interface-defining wave.
Output: `spool.xml`, flipped `DinghyIcons.LauncherSpool`, green `DinghyIconsTest`, an `internal`
top-level `parseNormalizedHex`, and `PrintMetadata.filamentColors`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18.3-color-reactive-spool-icon/18.3-CONTEXT.md
@.planning/phases/18.3-color-reactive-spool-icon/18.3-RESEARCH.md
@.planning/phases/18.3-color-reactive-spool-icon/18.3-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author spool.xml body vector + flip LauncherSpool + update the drift-guard test</name>
  <files>app/src/main/res/drawable/spool.xml, app/src/main/java/works/mees/dinghy/designsystem/icons/DinghyIcons.kt, app/src/test/java/works/mees/dinghy/designsystem/icons/DinghyIconsTest.kt</files>
  <read_first>
    - app/src/main/res/drawable/nozzle.xml (authoring analog: 96×96 viewBox, white-stroke `<path>`s, header comment documenting the SVG→VectorDrawable conversion)
    - app/src/main/res/drawable/heat_bed.xml (second authoring analog)
    - app/src/main/java/works/mees/dinghy/designsystem/icons/DinghyIcons.kt (lines 41 Inventory, 53-57 LauncherSpool, 74-75 Nozzle/HeatBed drawable form, 108-117 `all`, 16-21 icon-source policy listing "spool" as sanctioned)
    - app/src/test/java/works/mees/dinghy/designsystem/icons/DinghyIconsTest.kt (lines 90-111 the keeper-set drift-guard + KDoc)
    - 18.3-PATTERNS.md §spool.xml and §DinghyIcons.kt (authoring convention + flip shape)
    - 18.3-CONTEXT.md D-01 (custom side-view spool, two regions, band EXCLUDED)
  </read_first>
  <action>
    Author `app/src/main/res/drawable/spool.xml` following the `nozzle.xml` convention EXACTLY:
    `android:width/height="96dp"`, `viewportWidth/Height="96"`, every region a `<path>` with
    `strokeColor="#FFFFFFFF"`, `strokeWidth="4"`, `strokeLineCap`/`strokeLineJoin="round"`, and a
    leading header comment documenting the SVG→VectorDrawable conversion. Draw a side-view spool:
    the two circular flanges + the central hub/axle silhouette (body/flanges ONLY, per D-01/D-02).
    CRITICAL CONSTRAINT (D-04 anti-pattern): the vector MUST EXCLUDE the wound-filament band region
    entirely — author the silhouette so the band rectangle between the flanges is empty negative
    space the `SpoolGlyph` Canvas owns. Do NOT bake a `<gradient>` into the XML (filament color is
    dynamic runtime data). Document the band rectangle as fixed fractions of the 96-unit box in a
    comment (e.g. band spans the vertical/horizontal center region between the flanges) so plan 02's
    Canvas reproduces the identical rectangle (Pitfall 3) — Claude's discretion on exact proportions
    and gradient direction per CONTEXT.
    Then flip `DinghyIcons.LauncherSpool` (DinghyIcons.kt:57) from `IconRef.Ligature("database")` to
    `IconRef.Drawable(R.drawable.spool)`, keeping `alternate = "launcher_spool"` (Pitfall 2 — do NOT
    reuse an existing resId; the new Drawable IconRef value stays unique). Mirror the existing
    `Nozzle`/`HeatBed` form (line 74). Update the obsolete WR-02/`database` rationale comment
    (lines 53-56) to describe the new custom spool vector. Keep `LauncherSpool` in the `all` list.
    In the SAME edit, update `DinghyIconsTest.registryDrawableEntries_areOnlyTheCustomKeepers`
    (line 100-111): change the expected set from `setOf("nozzle", "heat_bed")` to
    `setOf("nozzle", "heat_bed", "launcher_spool")`, and update the assertion message string + the
    method KDoc (lines 90-98) to enumerate the spool as a SANCTIONED custom printer-domain keeper
    (cite the icon-source policy at DinghyIcons.kt:16-21). Do NOT touch `alternate_isUnique` or
    `iconRef_isUnique` (still satisfied).
  </action>
  <verify>
    <automated>cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --tests works.mees.dinghy.designsystem.icons.DinghyIconsTest --no-daemon"</automated>
  </verify>
  <acceptance_criteria>
    - `spool.xml` exists with `viewportWidth="96"`/`viewportHeight="96"`, white-stroke `<path>`s, and a header comment; contains NO `<gradient>` element and NO band fill path (body/flanges only).
    - `DinghyIcons.kt` matches `IconRef\.Drawable\(R\.drawable\.spool\)` for `LauncherSpool`; `alternate = "launcher_spool"` preserved.
    - `DinghyIconsTest` keeper set is `setOf("nozzle", "heat_bed", "launcher_spool")`; `DinghyIconsTest` runs GREEN (all methods incl. registryDrawableEntries, alternate_isUnique, iconRef_isUnique).
  </acceptance_criteria>
  <done>The spool body vector exists, LauncherSpool is a Drawable, and DinghyIconsTest passes green.</done>
</task>

<task type="auto">
  <name>Task 2: Promote parseNormalizedHex + thread filament_colors onto live PrintMetadata</name>
  <files>app/src/main/java/works/mees/dinghy/ui/spool/SpoolScreen.kt, app/src/main/java/works/mees/dinghy/state/PrintMetadata.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/spool/SpoolScreen.kt (lines 502-505 the `private fun parseNormalizedHex`; call sites at :312, :432-448 DetailSwatch)
    - app/src/main/java/works/mees/dinghy/spool/SpoolmanModels.kt (`normalizeColorHex` :147-152; `colorSwatches` :82-90 — read-only, do not modify)
    - app/src/main/java/works/mees/dinghy/state/PrintMetadata.kt (PrintMetadata data class :26-35; parsePrintMetadata :67-78; FilePreviewMetadata.filamentColors :51-52 + parseFilePreviewMetadata :80-96; stringArray helper :104-107)
    - 18.3-RESEARCH.md Open Question 1 / Assumption A1 (the gcode fallback source caveat) + Pitfall 6
    - 18.3-CONTEXT.md D-07 (Spoolman FIRST → gcode filament_colors[0] FALLBACK → empty spool)
  </read_first>
  <action>
    Promote `parseNormalizedHex` (SpoolScreen.kt:502-505) from `private fun` to an `internal`
    top-level util so `SpoolGlyph` (plan 02) and the wiring plan (plan 03) call the one helper —
    do NOT duplicate the parse logic (Pitfall 6, RESEARCH Open Q2). Place it as an `internal`
    top-level function (keep it in the spool package file, or a small color util in the spool
    package); update SpoolScreen's existing call sites (:312, :432-448) to the promoted symbol.
    Keep behavior byte-identical (guards via `normalizeColorHex`, never throws, null on garbage).
    Then resolve the gcode-fallback source gap (RESEARCH Open Q1 / A1): the live flow
    `container.printMetadata` carries a `PrintMetadata`, which currently has NO colors — only
    `FilePreviewMetadata.filamentColors` does, and that is not exposed as a live active-file flow.
    Add a `filamentColors: List<String> = emptyList()` field to the `PrintMetadata` data class and
    populate it in `parsePrintMetadata` by reading `filament_colors` from the SAME `result`
    JsonObject via the existing tolerant `stringArray(result, "filament_colors")` helper (the field
    is already proven parseable in `parseFilePreviewMetadata`). This is null-safe by construction
    (missing/garbage → empty list). This makes the active print's gcode colors reachable in
    `PrintStatusScreen` for the D-07 fallback without a new flow or fetch. Do NOT modify
    `SpoolmanModels.kt` (consume-only).
  </action>
  <verify>
    <automated>cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --no-daemon"</automated>
  </verify>
  <acceptance_criteria>
    - `parseNormalizedHex` is `internal` top-level (not `private`); SpoolScreen call sites compile against the promoted symbol; no duplicate parse function exists in SpoolGlyph or wiring code.
    - `PrintMetadata` has a `filamentColors: List<String>` field defaulting to `emptyList()`; `parsePrintMetadata` populates it from `stringArray(result, "filament_colors")`.
    - Full host unit suite (`:app:testDebugUnitTest`) is GREEN (no regression in existing PrintMetadata/SpoolScreen tests).
  </acceptance_criteria>
  <done>parseNormalizedHex is shared, and PrintMetadata.filamentColors carries the active-file gcode colors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Spoolman/gcode color strings → glyph | Attacker-influenced `colorHex`/`filament_colors` strings already fetched over the LAN cross into a parser that feeds a UI color. |

## STRIDE Threat Register

This is a local-only UI/asset phase: no network input is newly ingested, no auth surface, no new
data egress. The only real consideration is safe parsing of attacker-influenced color-hex strings.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18.3-01 | Tampering | `parseNormalizedHex` / `stringArray("filament_colors")` on attacker-influenced color strings | mitigate | Reuse `normalizeColorHex()` (returns null on malformed) + `runCatching` → null; `stringArray` is already null-safe (missing/garbage → empty list). A null/empty result renders the empty spool (D-03), never crashes. No new parser is hand-rolled. |
| T-18.3-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (Gradle project, no new dependencies — RESEARCH "Installation: none"). |
</threat_model>

<verification>
- `DinghyIconsTest` GREEN (registry flip + keeper-set update in lockstep, SC6).
- Full `:app:testDebugUnitTest` GREEN (no regression from the parser promotion or metadata field).
- `spool.xml` body-only (no `<gradient>`, no band path) — manual visual confirm against `nozzle.xml` convention.
</verification>

<success_criteria>
- SC1 (partial): custom spool drawable authored + registered as `IconRef.Drawable`, replacing `database`.
- SC6 (partial): build + unit tests green after the flip + keeper-set update.
- D-01 asset authored (body-only, band excluded); D-07 gcode fallback source plumbed.
</success_criteria>

<output>
Create `.planning/phases/18.3-color-reactive-spool-icon/18.3-01-SUMMARY.md` when done.
</output>
