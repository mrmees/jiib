---
phase: 18.2-jiib-rebrand
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/main/res/values/strings.xml
  - app/src/main/AndroidManifest.xml
  - app/src/main/java/works/mees/dinghy/service/MoonrakerService.kt
autonomous: true
requirements: []
must_haves:
  truths:
    - "The app label reads 'jiib' in the launcher and recents (D-09, SC-2)"
    - "The persistent foreground-service notification title reads 'jiib', not 'Dinghy Display' (SC-4)"
    - "The brand label is routed through an app_name string resource, not a hardcoded literal (D-09)"
    - "String keys exist for the About tagline (D-10) and the jib-explainer note (D-11) for 18.2-04 to consume"
  artifacts:
    - path: "app/src/main/res/values/strings.xml"
      provides: "app_name='jiib', about tagline + jib-note + cd_jiib_logo string resources (D-09/D-10/D-11)"
      contains: "app_name"
    - path: "app/src/main/AndroidManifest.xml"
      provides: "android:label=@string/app_name in both application + activity (D-09)"
      contains: "@string/app_name"
  key_links:
    - from: "app/src/main/AndroidManifest.xml"
      to: "app/src/main/res/values/strings.xml"
      via: "android:label resource reference"
      pattern: "android:label=\"@string/app_name\""
    - from: "app/src/main/java/works/mees/dinghy/service/MoonrakerService.kt"
      to: "app/src/main/res/values/strings.xml"
      via: "getString(R.string.app_name) for the notification title"
      pattern: "R.string.app_name"
---

<objective>
Swap the user-facing app label and notification title from "Dinghy Display" to "jiib", and add the
string resources the in-app brand surfaces (18.2-04) will consume. This plan owns the label/string
files exclusively (strings.xml, AndroidManifest.xml, MoonrakerService.kt) and runs in Wave 1 in
parallel with the asset conversion (18.2-01) — zero file overlap.

Per D-09 the label routes through an `app_name` string resource (preview-first / tokenized
convention, `docs/ui_design/PREVIEW_AND_TOKENS.md`), not a hardcoded manifest literal. This plan
also fixes the persistent FGS notification title (`MoonrakerService.kt:276`), which is a genuinely
user-visible "Dinghy Display" string in the notification shade — required by Success Criterion 4
("no user-visible 'Dinghy Display' string remains"). Both the manifest and the service title point
at the SAME `app_name` resource so the brand has one source of truth.

Purpose: brand the label + notification and seed the copy strings for 18.2-04.
Output: app_name + tagline + jib-note strings; manifest + service title rebranded.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18.2-jiib-rebrand/18.2-CONTEXT.md
@.planning/phases/18.2-jiib-rebrand/18.2-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add app_name + tagline + jib-note + cd_jiib_logo string resources</name>
  <files>app/src/main/res/values/strings.xml</files>
  <read_first>
    - app/src/main/res/values/strings.xml (the header convention block lines 2-21: `<area>_<element>` for labels, `cd_*` for contentDescription; and existing entry format lines 23-31)
    - .planning/phases/18.2-jiib-rebrand/18.2-CONTEXT.md (D-09 app_name, D-10 exact tagline, D-11 EXACT locked jib-note copy — transcribe verbatim)
    - .planning/phases/18.2-jiib-rebrand/18.2-PATTERNS.md ("res/values/strings.xml" section — escaping note for the quote + ellipsis)
  </read_first>
  <action>
    Add these string resources to `app/src/main/res/values/strings.xml`, following the file's
    documented `<area>_<element>` / `cd_*` convention:
    - `<string name="app_name">jiib</string>` (D-09 — all-lowercase, matches the wordmark; consumed by manifest label + notification title).
    - `<string name="about_tagline">A direct Moonraker touchscreen for Klipper.</string>` (D-10 — the existing tagline, now a resource; replaces the hardcoded literal at AboutScreen.kt:92).
    - `<string name="about_jib_note">…</string>` with the D-11 EXACT locked copy (transcribe verbatim from CONTEXT D-11): leads with the "it's a sail" definition, attributes the quote to Wikipedia, lands the "So is this." parallel. Escape per Android string rules — the copy contains a double-quote (escape as `\"` or wrap the run in `<![CDATA[...]]>`) and an ellipsis `…` (use the literal `…` character, NOT `...`). Verify it round-trips (no truncation at the quote).
    - `<string name="cd_jiib_logo">jiib</string>` (contentDescription for the brand `Icon` on splash/about; used by 18.2-04).
    Place them under a clearly-commented `about`/brand area block. Do NOT touch unrelated strings.
  </action>
  <verify>
    <automated>grep -q '<string name="app_name">jiib</string>' app/src/main/res/values/strings.xml && grep -q 'name="about_tagline"' app/src/main/res/values/strings.xml && grep -q 'name="about_jib_note"' app/src/main/res/values/strings.xml && grep -q 'name="cd_jiib_logo"' app/src/main/res/values/strings.xml && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `strings.xml` contains `<string name="app_name">jiib</string>` (exact, lowercase).
    - `strings.xml` contains `about_tagline`, `about_jib_note`, `cd_jiib_logo` keys.
    - `about_jib_note` value matches the D-11 locked copy verbatim (leads with the sail definition, Wikipedia-attributed quote, "So is this." closer), with the embedded quote properly escaped (no XML parse error, no truncation).
    - `:app:assembleDebug` resource merge succeeds (no malformed-string aapt error) — proven in the 18.2-05 build.
  </acceptance_criteria>
  <done>The brand label string and About copy strings exist and compile, ready for the manifest, service, and 18.2-04 to reference.</done>
</task>

<task type="auto">
  <name>Task 2: Route app label through @string/app_name + rebrand the notification title</name>
  <files>app/src/main/AndroidManifest.xml, app/src/main/java/works/mees/dinghy/service/MoonrakerService.kt</files>
  <read_first>
    - app/src/main/AndroidManifest.xml (the two `android:label="Dinghy Display"` occurrences: application at line 56, activity `.MainActivity` at line 66 — icon/roundIcon at 54-55 stay)
    - app/src/main/java/works/mees/dinghy/service/MoonrakerService.kt (around line 276 — `.setContentTitle("Dinghy Display")` in the persistent FGS notification builder; this is a `Service`, so `getString(R.string.app_name)` is available via the service Context)
    - .planning/phases/18.2-jiib-rebrand/18.2-PATTERNS.md ("AndroidManifest.xml" section — D-09 routes label through @string, icon refs unchanged)
  </read_first>
  <action>
    (1) In `AndroidManifest.xml`, change BOTH `android:label="Dinghy Display"` occurrences (the
    `<application>` at ~line 56 and the `.MainActivity` `<activity>` at ~line 66) to
    `android:label="@string/app_name"` (D-09). Leave `android:icon="@mipmap/ic_launcher"` and
    `android:roundIcon="@mipmap/ic_launcher_round"` (lines 54-55) UNCHANGED — the icon swap happens
    inside the mipmap XML in 18.2-03, not here. Do NOT touch the package name, applicationId, theme
    (`Theme.DinghyDisplay`), or `.DinghyApp` (all out of scope).
    (2) In `MoonrakerService.kt` around line 276, replace the hardcoded
    `.setContentTitle("Dinghy Display")` with `.setContentTitle(getString(R.string.app_name))` so
    the persistent foreground-service notification title reads "jiib" (SC-4 — this is a user-visible
    string in the notification shade). Add the `works.mees.dinghy.R` import if not already present.
    The `setContentText(text)` line (the connection/status string) is UNCHANGED.
  </action>
  <verify>
    <automated>test $(grep -c 'android:label="@string/app_name"' app/src/main/AndroidManifest.xml) -eq 2 && ! grep -q 'Dinghy Display' app/src/main/AndroidManifest.xml && grep -q 'setContentTitle(getString(R.string.app_name))' app/src/main/java/works/mees/dinghy/service/MoonrakerService.kt && ! grep -q 'setContentTitle("Dinghy Display")' app/src/main/java/works/mees/dinghy/service/MoonrakerService.kt && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `AndroidManifest.xml` has exactly two `android:label="@string/app_name"` and ZERO `Dinghy Display` literals.
    - `android:icon`/`android:roundIcon`/`android:theme`/`android:name` are unchanged (package + applicationId untouched).
    - `MoonrakerService.kt` notification title is `getString(R.string.app_name)`; no `"Dinghy Display"` literal remains in that file.
    - `:app:assembleDebug` succeeds via `cmd.exe /c "E:\Android\gw.bat :app:assembleDebug --no-daemon"` (proven at the 18.2-05 gate; this plan compiles clean against the new resource).
  </acceptance_criteria>
  <done>The launcher/recents label and the FGS notification both read "jiib" via the single app_name resource; no Dinghy Display literal remains in the manifest or service.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | String/manifest/notification-title edits only. No new I/O, network, auth, or input surface. The notification `setContentText` (the only dynamic field) is unchanged and already documented as never carrying the API key. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18.2-02-01 | Information disclosure | FGS notification title | accept | Title is a static brand string ("jiib"); no secrets. The pre-existing `setContentText` (status only, never the key) is untouched. No new disclosure surface. |

No `npm`/`pip`/`cargo` installs — package legitimacy gate N/A.
</threat_model>

<verification>
- Both manifest labels and the notification title resolve to "jiib" via `@string/app_name`.
- No `Dinghy Display` literal remains in AndroidManifest.xml or MoonrakerService.kt.
- About tagline + jib-note + cd_jiib_logo strings exist for 18.2-04.
- Debug build compiles against the new resource.
</verification>

<success_criteria>
SC-2 (app label reads "jiib") satisfied. SC-4 advanced (manifest + notification de-branded). D-09
(label via string resource), D-10 (tagline string), D-11 (jib-note string) satisfied/seeded.
</success_criteria>

<output>
Create `.planning/phases/18.2-jiib-rebrand/18.2-02-SUMMARY.md` when done.
</output>
