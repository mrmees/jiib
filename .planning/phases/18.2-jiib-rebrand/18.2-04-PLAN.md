---
phase: 18.2-jiib-rebrand
plan: 04
type: execute
wave: 2
depends_on: ["18.2-01", "18.2-02"]
files_modified:
  - app/src/main/java/works/mees/dinghy/theme/BrandTint.kt
  - app/src/main/java/works/mees/dinghy/ui/screen/SplashScreen.kt
  - app/src/main/java/works/mees/dinghy/ui/screen/AboutScreen.kt
  - app/src/main/java/works/mees/dinghy/preview/BrandPreviews.kt
  - app/src/test/java/works/mees/dinghy/theme/BrandTintTest.kt
autonomous: true
requirements: []
must_haves:
  truths:
    - "The splash screen shows the jiib stacked icon+wordmark lockup in place of the 'Dinghy Display' text (D-04, SC-3)"
    - "The About screen shows the jiib wordmark in place of the 'Dinghy Display' SectionLabel (D-05, SC-3)"
    - "Both in-app marks tint to a brand color derived from t.accent, with a contrast-floor fallback to t.text when accent vs bg contrast is too low (D-06)"
    - "The About jib-explainer note (D-11) renders below the kept tagline (D-10)"
    - "No 'Dinghy Display' literal remains in SplashScreen.kt or AboutScreen.kt (SC-4)"
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/theme/BrandTint.kt"
      provides: "brandTint(accent,bg,text) contrast-floor helper resolving the D-06 watch-item"
      contains: "fun brandTint"
    - path: "app/src/main/java/works/mees/dinghy/ui/screen/SplashScreen.kt"
      provides: "jiib lockup Image tinted brandTint, replacing the brand Text (D-04/D-06)"
      contains: "jiib_lockup"
    - path: "app/src/main/java/works/mees/dinghy/ui/screen/AboutScreen.kt"
      provides: "jiib wordmark tinted brandTint + jib-note, replacing SectionLabel (D-05/D-06/D-11)"
      contains: "jiib_wordmark"
    - path: "app/src/main/java/works/mees/dinghy/preview/BrandPreviews.kt"
      provides: "Preview matrix (6 theme combos) for the rebranded Splash + About surfaces (PREVIEW_AND_TOKENS.md)"
      contains: "PreviewBox"
  key_links:
    - from: "app/src/main/java/works/mees/dinghy/ui/screen/SplashScreen.kt"
      to: "app/src/main/res/drawable/jiib_lockup.xml"
      via: "Icon painterResource jiib_lockup tinted brandTint"
      pattern: "jiib_lockup"
    - from: "app/src/main/java/works/mees/dinghy/ui/screen/AboutScreen.kt"
      to: "app/src/main/res/values/strings.xml"
      via: "stringResource about_jib_note"
      pattern: "about_jib_note"
---

<objective>
Apply the jiib brand to the two in-app surfaces: the Splash screen (stacked icon+wordmark lockup,
D-04) and the About screen (wordmark + jib-explainer note, D-05/D-10/D-11). Both marks tint to the
brand accent via a new brandTint helper that resolves the D-06 accent-vs-bg contrast watch-item
with a contrast-floor fallback (falls back to t.text when t.accent would wash out against t.bg on a
user-custom theme). Ships a Preview matrix for both changed surfaces per PREVIEW_AND_TOKENS.md —
which is ALSO how the D-06 contrast risk gets eyeballed across the six themes before device.

Depends on 18.2-01 (the jiib_lockup.xml + jiib_wordmark.xml drawables) and 18.2-02 (the
about_tagline / about_jib_note / cd_jiib_logo strings) — Wave 2.

Purpose: the strongest in-app brand moments, theme-compliant.
Output: brandTint helper + test, rebranded Splash + About, brand preview matrix.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18.2-jiib-rebrand/18.2-CONTEXT.md
@.planning/phases/18.2-jiib-rebrand/18.2-PATTERNS.md
@.planning/phases/18.2-jiib-rebrand/18.2-01-SUMMARY.md
@.planning/phases/18.2-jiib-rebrand/18.2-02-SUMMARY.md
@docs/ui_design/THEMING.md
@docs/ui_design/PREVIEW_AND_TOKENS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: brandTint contrast-floor helper (D-06) + host test</name>
  <files>app/src/main/java/works/mees/dinghy/theme/BrandTint.kt, app/src/test/java/works/mees/dinghy/theme/BrandTintTest.kt</files>
  <behavior>
    - High contrast: brandTint(accent=blue, bg=darkNavy, text=white) returns the accent (ratio above the floor).
    - Low contrast: brandTint(accent=color near bg, bg, text) returns text (accent washes out, fall back so the mark stays legible — D-06).
    - The floor is a documented WCAG-style luminance contrast ratio (>= 3.0:1 for a large graphical mark); contrast computed via Compose Color.luminance().
    - Pure function of (accent, bg, text); no Compose runtime needed — host-testable.
  </behavior>
  <read_first>
    - app/src/main/java/works/mees/dinghy/theme/ThemeTokens.kt (the token type — val bg, val accent, val text Color fields, lines ~31/41/53)
    - app/src/main/java/works/mees/dinghy/ui/screen/ThemeEditorScreen.kt (line ~466 — the existing fill.luminance() > 0.5f pattern; Compose luminance is the in-repo precedent)
    - docs/ui_design/THEMING.md (THEME-01 — the in-app brand color MUST be token-derived, never a literal; brandTint keeps the mark inside the token system while guarding contrast)
    - .planning/phases/18.2-jiib-rebrand/18.2-PATTERNS.md (the "Accent vs bg contrast floor (D-06 watch-item)" section — no existing helper, net-new; theme_theory contrast logic is NOT in runtime)
  </read_first>
  <action>
    Create app/src/main/java/works/mees/dinghy/theme/BrandTint.kt with a PURE function
    brandTint(accent: Color, bg: Color, text: Color): Color (D-06). Compute the WCAG-style contrast
    ratio between accent and bg using relative luminance: ratio = (Lmax + 0.05) / (Lmin + 0.05)
    where L is Color.luminance() (the Compose extension already used in ThemeEditorScreen). If the
    ratio is at least a named constant BRAND_CONTRAST_FLOOR (set it to 3.0f — the WCAG large-graphic
    non-text minimum; document the choice in KDoc), return accent; otherwise return text (the
    always-legible token fallback). Keep it a top-level pure function (no Composable, no token
    dependency beyond the three Color args) so it is host-unit testable and reusable by both Splash
    and About.
    Create app/src/test/java/works/mees/dinghy/theme/BrandTintTest.kt (JUnit, host) asserting:
    (1) high-contrast accent-on-navy returns the accent; (2) a near-bg accent returns text;
    (3) the boundary behaves per the floor. Use plain androidx.compose.ui.graphics.Color constants
    (value classes — no Android runtime needed for .luminance()).
  </action>
  <verify>
    <automated>cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --tests *BrandTintTest --no-daemon" 2>&1 | tr -d '\r' | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - BrandTint.kt defines a pure top-level brandTint(accent, bg, text): Color with a BRAND_CONTRAST_FLOOR constant (3.0f) and KDoc citing D-06.
    - Contrast computed via Color.luminance() and the WCAG (L+0.05) ratio formula.
    - BrandTintTest passes: high-contrast returns accent; low-contrast (accent near bg) returns text.
    - The :app:testDebugUnitTest run filtered to *BrandTintTest is GREEN via gw.bat.
  </acceptance_criteria>
  <done>brandTint resolves the D-06 watch-item with a tested contrast-floor fallback; ready for Splash + About to consume.</done>
</task>

<task type="auto">
  <name>Task 2: Replace the splash brand Text with the jiib lockup (tinted brandTint)</name>
  <files>app/src/main/java/works/mees/dinghy/ui/screen/SplashScreen.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/screen/SplashScreen.kt (the brand Text at lines 84-91 inside the centered Column at 76-101; val t = LocalTokens.current at line 69; the reason Text at 92-101 + recovery Row at 102+ which STAY put per D-04)
    - app/src/main/res/drawable/jiib_lockup.xml (the tintable lockup from 18.2-01)
    - app/src/main/java/works/mees/dinghy/theme/BrandTint.kt (the helper from Task 1)
    - .planning/phases/18.2-jiib-rebrand/18.2-PATTERNS.md (the "How to render a tinted VectorDrawable in Compose" section — Icon with painterResource + tint + Modifier.size; ExtrudeScreen.kt:235-240 is the canonical in-repo example; the needed imports list)
    - docs/ui_design/LAYOUT.md (ratio-only sizing — size the lockup by fraction/fsSp-derived dp, NOT a hardcoded px)
  </read_first>
  <action>
    In SplashScreen.kt, replace the brand Text block at lines ~84-91 (the one whose text is the old
    brand name) with a Material3 Icon whose painter is painterResource(R.drawable.jiib_lockup),
    contentDescription is stringResource(R.string.cd_jiib_logo), and tint is
    brandTint(t.accent, t.bg, t.text) (D-04 lockup, D-06 tint). Size by RATIO, not a magic px — e.g.
    Modifier.fillMaxWidth(0.6f) with wrapContentHeight, or an fsSp-derived dp; the lockup intrinsic
    aspect (square-ish 600x600) keeps it un-stretched, and the Column is already
    horizontalAlignment = CenterHorizontally (LAYOUT.md ratio-only rule). The reason Text (92-101)
    and the recovery Row (102+) stay EXACTLY as they are (D-04 — reason + buttons keep position
    directly below the lockup). Add the imports not yet present: androidx.compose.material3.Icon,
    androidx.compose.ui.res.painterResource, androidx.compose.ui.res.stringResource,
    androidx.compose.foundation.layout.wrapContentHeight (if used), works.mees.dinghy.R, and
    works.mees.dinghy.theme.brandTint. Remove now-unused imports (Geist/FontWeight/TextAlign) ONLY
    if no longer referenced — the reason Text uses GeistMono + TextAlign, so re-check before deleting
    any import. No old brand-name literal remains.
  </action>
  <verify>
    <automated>grep -q 'jiib_lockup' app/src/main/java/works/mees/dinghy/ui/screen/SplashScreen.kt && grep -q 'brandTint(' app/src/main/java/works/mees/dinghy/ui/screen/SplashScreen.kt && test $(grep -c 'Dinghy Display' app/src/main/java/works/mees/dinghy/ui/screen/SplashScreen.kt) -eq 0 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - SplashScreen renders an Icon with painterResource(R.drawable.jiib_lockup) and tint = brandTint(t.accent, t.bg, t.text) where the brand Text used to be.
    - The mark is sized by ratio/fsSp-derived dimension (no hardcoded px constant for the lockup size — LAYOUT.md).
    - contentDescription = stringResource(R.string.cd_jiib_logo).
    - The reason Text and recovery Row are unchanged (D-04).
    - grep for "Dinghy Display" in SplashScreen.kt returns zero (SC-4).
    - File compiles (covered by the 18.2-05 build; no unresolved/unused imports escalated to errors).
  </acceptance_criteria>
  <done>The splash front door shows the accent-tinted (contrast-guarded) jiib lockup above the reason text and recovery buttons.</done>
</task>

<task type="auto">
  <name>Task 3: About — wordmark replaces SectionLabel, add tagline + jib note; brand preview matrix</name>
  <files>app/src/main/java/works/mees/dinghy/ui/screen/AboutScreen.kt, app/src/main/java/works/mees/dinghy/preview/BrandPreviews.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/screen/AboutScreen.kt (the APP block lines 85-96: the SectionLabel brand call at 86, the description Text at 91-96 — the typography template; val t = LocalTokens.current at 70; note SectionLabel("Developer") at line 100 KEEPS using the helper)
    - app/src/main/res/drawable/jiib_wordmark.xml (the tintable wordmark from 18.2-01)
    - app/src/main/java/works/mees/dinghy/theme/BrandTint.kt (the helper from Task 1)
    - app/src/main/java/works/mees/dinghy/preview/PreviewTheming.kt (the PreviewBox wrapper, the themeCombos 6-tuple list, named seeds colorfulDark/colorfulLight)
    - app/src/main/java/works/mees/dinghy/preview/FineTunePreviews.kt (the exemplar preview-file shape: @Nexus7Previews + @Composable funcs wrapping screens in PreviewBox(seed){...})
    - app/src/main/java/works/mees/dinghy/preview/DinghyPreviews.kt (@Nexus7Previews multipreview + the en-XA pseudolocale single-preview note)
    - docs/ui_design/PREVIEW_AND_TOKENS.md (preview-first: 6 theme combos + fs=L, no live Moonraker, for every changed surface)
  </read_first>
  <action>
    (1) In AboutScreen.kt, replace the SectionLabel brand call at line 86 with a Material3 Icon whose
    painter is painterResource(R.drawable.jiib_wordmark), contentDescription is
    stringResource(R.string.cd_jiib_logo), and tint is brandTint(t.accent, t.bg, t.text) (D-05
    wordmark only — no stacked lockup here; D-06 tint). Size by ratio/fsSp so it carries roughly the
    visual weight of the 20sp SectionLabel it replaces (the 433x289 aspect keeps it un-stretched;
    LAYOUT.md ratio-only). Route the existing tagline through the resource: change the hardcoded
    description Text at lines 91-96 to text = stringResource(R.string.about_tagline) (D-10 keeps the
    copy, now tokenized). Directly BELOW it, add the D-11 jib-explainer note: a Text with
    text = stringResource(R.string.about_jib_note), color = t.text3, fontFamily = Geist,
    fontSize = fsSp(15f, t.fs).sp — mirroring the description Text treatment exactly (Claude's
    discretion per D-11; matches the 15sp metadata floor). Add imports: Icon, painterResource,
    stringResource, R, brandTint. KEEP the private SectionLabel helper (still used by the Developer
    label at line 100). No old brand-name literal remains.
    (2) Create app/src/main/java/works/mees/dinghy/preview/BrandPreviews.kt following the
    FineTunePreviews shape: a @Nexus7Previews @Composable per theme rendering the rebranded
    AboutScreen content across the six themeCombos seeds via PreviewBox(seed){...}, plus at least one
    preview of the SplashScreen brand lockup. Use fake/no-op callbacks and the fake AppContainer /
    state path the existing previews use (mirror how FineTune/PrintStatus previews supply fixtures —
    NO live Moonraker). The six-combo matrix is what surfaces the D-06 accent-vs-bg contrast risk
    visually before device.
  </action>
  <verify>
    <automated>grep -q 'jiib_wordmark' app/src/main/java/works/mees/dinghy/ui/screen/AboutScreen.kt && grep -q 'about_jib_note' app/src/main/java/works/mees/dinghy/ui/screen/AboutScreen.kt && grep -q 'about_tagline' app/src/main/java/works/mees/dinghy/ui/screen/AboutScreen.kt && test $(grep -c 'Dinghy Display' app/src/main/java/works/mees/dinghy/ui/screen/AboutScreen.kt) -eq 0 && grep -q 'PreviewBox' app/src/main/java/works/mees/dinghy/preview/BrandPreviews.kt && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - AboutScreen renders an Icon with painterResource(R.drawable.jiib_wordmark) and tint = brandTint(t.accent, t.bg, t.text) where the brand SectionLabel used to be.
    - The tagline renders via stringResource(R.string.about_tagline) (D-10); the jib note renders via stringResource(R.string.about_jib_note) directly below it (D-11), styled t.text3 / Geist / fsSp(15f).
    - The SectionLabel helper is retained (Developer label still uses it).
    - grep for "Dinghy Display" in AboutScreen.kt returns zero (SC-4).
    - BrandPreviews.kt contains a 6-theme PreviewBox matrix for About + at least one Splash brand preview; it compiles and renders without live Moonraker.
  </acceptance_criteria>
  <done>About shows the accent-tinted (contrast-guarded) jiib wordmark, the kept tagline, and the jib-explainer note; both changed surfaces have a theme-matrix preview that exposes the D-06 contrast risk.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Compose UI rendering + a pure color-math helper. No I/O, network, auth, or user-input surface. brandTint consumes only theme Color values already in scope. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18.2-04-01 | Tampering | brandTint / static brand drawables | accept | Pure function over theme tokens; static vectors. No executable/external content, no new attack surface. |

No npm/pip/cargo installs — package legitimacy gate N/A.
</threat_model>

<verification>
- Splash shows the jiib lockup; About shows the jiib wordmark; both tinted via brandTint (D-06 guard).
- About tagline (D-10) + jib note (D-11) render via string resources.
- No "Dinghy Display" literal in SplashScreen.kt or AboutScreen.kt.
- brandTint host test GREEN; brand preview matrix compiles across the six themes.
- Visual contrast across themes is eyeballed via BrandPreviews (and the on-device spot-check in 18.2-05).
</verification>

<success_criteria>
SC-3 (wordmark/lockup applied to in-app surfaces) satisfied. SC-4 advanced (in-app brand strings
de-branded). D-04/D-05/D-06/D-10/D-11 satisfied. The D-06 watch-item is IMPLEMENTED (contrast-floor
fallback), not deferred.
</success_criteria>

<output>
Create `.planning/phases/18.2-jiib-rebrand/18.2-04-SUMMARY.md` when done.
</output>
