---
phase: 18.2-jiib-rebrand
plan: 03
type: execute
wave: 2
depends_on: ["18.2-01"]
files_modified:
  - app/src/main/res/mipmap-anydpi-v26/ic_launcher.xml
  - app/src/main/res/mipmap-anydpi-v26/ic_launcher_round.xml
  - app/src/main/res/mipmap-anydpi/ic_launcher.xml
  - app/src/main/res/mipmap-anydpi/ic_launcher_round.xml
  - app/src/main/res/values/ic_launcher_background.xml
autonomous: true
requirements: []
must_haves:
  truths:
    - "The launcher icon renders the jiib sail mark as a proper adaptive icon at all densities (SC-1, D-01)"
    - "The monochrome/themed-icon layer is the jiib mark (Material You hook on API 33+) (D-02)"
    - "The pre-API-26 vector fallback shows the jiib mark on navy (minSdk 23, D-03)"
    - "The navy #141A24 adaptive-icon background is retained, not recolored (D-01)"
  artifacts:
    - path: "app/src/main/res/mipmap-anydpi-v26/ic_launcher.xml"
      provides: "Adaptive icon: navy bg + jiib foreground + jiib monochrome (D-01/D-02)"
      contains: "ic_jiib_foreground"
    - path: "app/src/main/res/mipmap-anydpi/ic_launcher.xml"
      provides: "Pre-26 vector fallback: navy box + jiib mark (D-03)"
      contains: "ic_jiib_foreground"
  key_links:
    - from: "app/src/main/res/mipmap-anydpi-v26/ic_launcher.xml"
      to: "app/src/main/res/drawable/ic_jiib_foreground.xml"
      via: "<foreground>/<monochrome> drawable reference"
      pattern: "@drawable/ic_jiib_foreground"
    - from: "app/src/main/res/mipmap-anydpi-v26/ic_launcher.xml"
      to: "app/src/main/res/values/ic_launcher_background.xml"
      via: "<background> color reference (navy, unchanged)"
      pattern: "@color/ic_launcher_background"
---

<objective>
Wire the new jiib launcher foreground (`ic_jiib_foreground.xml`, produced by 18.2-01) into the
adaptive-icon set and the pre-26 vector fallback, retaining the navy `#141A24` background (D-01).
Runs in Wave 2 because it depends on the foreground drawable from Wave 1.

Per D-01 only the foreground changes (Benchy line-art -> jiib sail mark, white on navy). Per D-02
the `<monochrome>` layer is the jiib mark itself (the Material You hook — tinted to the wallpaper
palette on API 33+ launchers with themed icons on; everyone else, including the floor device flox
at API 30, gets the fixed navy+white static icon). Per D-03 the pre-26 fallback
(`mipmap-anydpi/ic_launcher*.xml`) is updated to the jiib mark too (navy box + white mark, no
adaptive layering) because minSdk 23 predates adaptive icons.

Purpose: deliver the jiib launcher identity across all densities + API levels.
Output: rebranded adaptive + fallback launcher XMLs; confirmed-navy background.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18.2-jiib-rebrand/18.2-CONTEXT.md
@.planning/phases/18.2-jiib-rebrand/18.2-PATTERNS.md
@.planning/phases/18.2-jiib-rebrand/18.2-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Point the adaptive icon (v26) foreground + monochrome at the jiib mark; confirm navy bg</name>
  <files>app/src/main/res/mipmap-anydpi-v26/ic_launcher.xml, app/src/main/res/mipmap-anydpi-v26/ic_launcher_round.xml, app/src/main/res/values/ic_launcher_background.xml</files>
  <read_first>
    - app/src/main/res/mipmap-anydpi-v26/ic_launcher.xml (current: `<background @color/ic_launcher_background>` + `<foreground @drawable/ic_launcher_foreground>` + `<monochrome @drawable/ic_launcher_foreground>`)
    - app/src/main/res/mipmap-anydpi-v26/ic_launcher_round.xml (identical structure)
    - app/src/main/res/values/ic_launcher_background.xml (`#141A24` — confirm unchanged per D-01)
    - app/src/main/res/drawable/ic_jiib_foreground.xml (the new foreground from 18.2-01)
    - .planning/phases/18.2-jiib-rebrand/18.2-CONTEXT.md (D-01 keep navy bg / swap foreground; D-02 monochrome = jiib mark)
  </read_first>
  <action>
    In BOTH `mipmap-anydpi-v26/ic_launcher.xml` and `ic_launcher_round.xml`, change the
    `<foreground android:drawable="@drawable/ic_launcher_foreground" />` and `<monochrome
    android:drawable="@drawable/ic_launcher_foreground" />` references to point at
    `@drawable/ic_jiib_foreground` (D-01 foreground swap, D-02 monochrome = the jiib mark). Leave
    `<background android:drawable="@color/ic_launcher_background" />` UNCHANGED. Update the leading
    XML comment to say "jiib sail mark" instead of "Benchy".
    Confirm `app/src/main/res/values/ic_launcher_background.xml` still defines
    `ic_launcher_background = #141A24` (D-01 — navy retained; the brand-background recolor is a
    DEFERRED idea, do NOT change it). Optionally refresh that file's comment from "Benchy line art"
    to "jiib mark" — value MUST stay `#141A24`.
  </action>
  <verify>
    <automated>for f in app/src/main/res/mipmap-anydpi-v26/ic_launcher.xml app/src/main/res/mipmap-anydpi-v26/ic_launcher_round.xml; do grep -q '@drawable/ic_jiib_foreground' $f && ! grep -q '@drawable/ic_launcher_foreground' $f && grep -q '@color/ic_launcher_background' $f || { echo "FAIL $f"; exit 1; }; done; grep -q '#141A24' app/src/main/res/values/ic_launcher_background.xml && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Both v26 adaptive XMLs reference `@drawable/ic_jiib_foreground` in BOTH `<foreground>` and `<monochrome>`.
    - Neither v26 XML still references `@drawable/ic_launcher_foreground`.
    - Both retain `<background android:drawable="@color/ic_launcher_background" />`.
    - `ic_launcher_background.xml` value remains exactly `#141A24` (navy, D-01).
  </acceptance_criteria>
  <done>API-26+ launchers render the jiib sail mark white on navy, with the jiib monochrome layer available for Material You themed-icon tinting on API 33+.</done>
</task>

<task type="auto">
  <name>Task 2: Update the pre-API-26 vector fallback to the jiib mark (navy box + white mark)</name>
  <files>app/src/main/res/mipmap-anydpi/ic_launcher.xml, app/src/main/res/mipmap-anydpi/ic_launcher_round.xml</files>
  <read_first>
    - app/src/main/res/mipmap-anydpi/ic_launcher.xml (current: a `<vector>` 108x108 with a navy `#141A24` box `<path>` then a `<group scaleX/Y=0.045 …>` wrapping the Benchy stroked paths — the pre-26 fallback, no adaptive layering)
    - app/src/main/res/drawable/ic_jiib_foreground.xml (the new jiib `<group>` + filled-white paths from 18.2-01 — reuse its group + paths here)
    - .planning/phases/18.2-jiib-rebrand/18.2-CONTEXT.md (D-03 — keep the pre-26 fallback for minSdk 23; update it to the jiib mark)
  </read_first>
  <action>
    In BOTH `mipmap-anydpi/ic_launcher.xml` and `ic_launcher_round.xml`, KEEP the navy background
    box path `<path android:pathData="M0,0h108v108h-108z" android:fillColor="#141A24" />` (D-01/D-03
    — the fallback bakes the navy in because there is no adaptive `<background>` layer), and REPLACE
    the Benchy `<group>` (the `scaleX/Y=0.045 …` group with the stroked white paths) with the jiib
    `<group>` + filled-white paths from `ic_jiib_foreground.xml` (same scale/translate-into-safe-zone
    `<group>` and the same `android:fillColor="#FFFFFFFF"` paths produced in 18.2-01). The result is
    a flat navy box with the white jiib sail mark — parallel to the current Benchy fallback. Update
    the leading comment from "Benchy line art" to "jiib mark". Color stays fixed white (D-03 — the
    launcher mark is outside the theme system).
  </action>
  <verify>
    <automated>for f in app/src/main/res/mipmap-anydpi/ic_launcher.xml app/src/main/res/mipmap-anydpi/ic_launcher_round.xml; do grep -q 'fillColor="#141A24"' $f && grep -q 'fillColor="#FFFFFFFF"' $f && grep -q '<group' $f && ! grep -qi 'benchy' $f || { echo "FAIL $f"; exit 1; }; done; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Both pre-26 fallback XMLs keep the navy box `<path … fillColor="#141A24">` and render the white jiib mark via the same `<group>` + `fillColor="#FFFFFFFF"` paths as `ic_jiib_foreground.xml`.
    - No Benchy paths/comments remain (grep for "benchy" is clean, case-insensitive).
    - Both are valid `<vector>` 108x108 drawables (no `<adaptive-icon>` here — pre-26 has no layering).
    - `:app:assembleDebug` builds clean via `cmd.exe /c "E:\Android\gw.bat :app:assembleDebug --no-daemon"` (run this build as the launcher gate; note flox=API30 renders the v26 adaptive path, the static navy+white icon, NOT the API-33 monochrome path).
  </acceptance_criteria>
  <done>Pre-26 devices (minSdk 23 floor) and the debug build both render the jiib mark on navy; the full launcher icon set is rebranded across all API levels and densities.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Launcher-icon resource XML edits only. No I/O, network, auth, or input surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18.2-03-01 | Tampering | Adaptive-icon / fallback XML | accept | Static resource references to compile-time vectors and a color resource. No executable content, no new attack surface. |

No `npm`/`pip`/`cargo` installs — package legitimacy gate N/A.
</threat_model>

<verification>
- v26 adaptive icons reference the jiib foreground + monochrome; navy bg retained.
- Pre-26 fallback renders navy box + white jiib mark; no Benchy remnants.
- Debug build succeeds (Windows-side via gw.bat).
- On-device launcher render is owner-eyeballed at the 18.2-05 spot-check (flox = API 30, static path).
</verification>

<success_criteria>
SC-1 (adaptive icon renders the jiib mark, square + round, art in the safe zone, all densities)
satisfied. SC-4 advanced (Benchy launcher art removed). D-01/D-02/D-03 satisfied.
</success_criteria>

<output>
Create `.planning/phases/18.2-jiib-rebrand/18.2-03-SUMMARY.md` when done.
</output>
