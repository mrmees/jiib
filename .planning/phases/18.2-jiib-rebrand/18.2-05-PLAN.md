---
phase: 18.2-jiib-rebrand
plan: 05
type: execute
wave: 3
depends_on: ["18.2-01", "18.2-02", "18.2-03", "18.2-04"]
files_modified:
  - app/src/main/java/works/mees/dinghy/gallery/GalleryScreen.kt
  - .planning/phases/18.2-jiib-rebrand/18.2-DEBRAND-AUDIT.md
autonomous: false
requirements: []
must_haves:
  truths:
    - "No user-visible 'Dinghy Display' string remains anywhere in the shipped app (SC-4)"
    - "Every surviving 'Dinghy Display' occurrence is an explicitly-documented out-of-scope identity string (package/protocol/code-comment), not a missed user-facing brand surface (SC-4)"
    - "The release APK builds green via gw.bat (SC-5)"
    - "The jiib launcher icon + label + splash lockup + about wordmark render correctly on the floor device flox (SC-1/SC-2/SC-3)"
  artifacts:
    - path: ".planning/phases/18.2-jiib-rebrand/18.2-DEBRAND-AUDIT.md"
      provides: "The SC-4 audit: every 'Dinghy Display' / 'dinghy' occurrence classified user-visible (fixed) vs out-of-scope identity (documented)"
      contains: "SC-4"
  key_links:
    - from: ".planning/phases/18.2-jiib-rebrand/18.2-DEBRAND-AUDIT.md"
      to: "app/src/main"
      via: "exhaustive grep of every Dinghy Display occurrence with disposition"
      pattern: "Dinghy Display"
---

<objective>
Close Success Criterion 4 ("No user-visible 'Dinghy Display' string remains") with an exhaustive
audit, fix the one remaining user-reachable literal (the debug GalleryScreen header), explicitly
classify the surviving occurrences as out-of-scope identity strings, prove the release build is
green (SC-5), and run the on-device launcher/label/splash/about spot-check on the floor device flox
(SC-1/SC-2/SC-3). Runs in Wave 3 after every other plan lands.

Background — the grep-known "Dinghy Display" occurrences and their disposition:
  - SplashScreen.kt / AboutScreen.kt -> FIXED in 18.2-04 (user-visible brand surfaces).
  - AndroidManifest.xml / MoonrakerService.kt notification title -> FIXED in 18.2-02 (label + notif).
  - GalleryScreen.kt:136 SectionHeader -> a DEBUG component-gallery surface; this plan fixes it for a
    clean sweep (trivial, keeps the audit honest).
  - MoonrakerSession.kt:80 clientName (+ clientUrl mees.works/dinghy-display) -> the JSON-RPC
    connection-IDENTITY sent to Moonraker; surfaces only in MOONRAKER/Mainsail's server-side
    connection list, NOT on jiib's own UI. This is protocol/package-tier identity (CONTEXT out-of-scope:
    package + repo name stay). DECISION: leave as-is, DOCUMENT explicitly in the audit (not a silent miss).
  - ScreenScaffold.kt:17 / other code comments -> non-shipping source comments, not user-visible.

Purpose: prove SC-4 closed and the build/device gates pass.
Output: the de-brand audit, the gallery fix, build-green + on-device sign-off.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18.2-jiib-rebrand/18.2-CONTEXT.md
@.planning/phases/18.2-jiib-rebrand/18.2-01-SUMMARY.md
@.planning/phases/18.2-jiib-rebrand/18.2-02-SUMMARY.md
@.planning/phases/18.2-jiib-rebrand/18.2-03-SUMMARY.md
@.planning/phases/18.2-jiib-rebrand/18.2-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix the debug GalleryScreen header + write the SC-4 de-brand audit</name>
  <files>app/src/main/java/works/mees/dinghy/gallery/GalleryScreen.kt, .planning/phases/18.2-jiib-rebrand/18.2-DEBRAND-AUDIT.md</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/gallery/GalleryScreen.kt (line ~136 — SectionHeader with the old brand name in the debug component gallery)
    - app/src/main/java/works/mees/dinghy/net/MoonrakerSession.kt (lines 80-85 — clientName + clientUrl; read the KDoc explaining Moonraker records these for its connection list only)
    - app/src/main/AndroidManifest.xml + app/src/main/java/works/mees/dinghy/service/MoonrakerService.kt (confirm 18.2-02 already de-branded these)
    - .planning/phases/18.2-jiib-rebrand/18.2-CONTEXT.md (the out-of-scope boundary: package + repo name stay; only the four user-facing surfaces + the notification are in scope)
  </read_first>
  <action>
    (1) In GalleryScreen.kt around line 136, change the SectionHeader literal from the old brand name
    to "jiib" (or "jiib — Component Gallery (debug)"). This is a debug-only surface, but fixing it
    keeps the SC-4 grep clean and the audit honest.
    (2) Create .planning/phases/18.2-jiib-rebrand/18.2-DEBRAND-AUDIT.md as a table listing EVERY
    occurrence of "Dinghy Display" and "dinghy" (case-insensitive) found by grep across
    app/src/main, with a disposition column for each: FIXED (this phase, name the plan) or
    OUT-OF-SCOPE (with the reason). Required rows / classifications:
      - SplashScreen.kt, AboutScreen.kt -> FIXED (18.2-04, user-visible brand).
      - AndroidManifest.xml (x2), MoonrakerService.kt notification title -> FIXED (18.2-02).
      - GalleryScreen.kt -> FIXED (this plan, debug gallery).
      - MoonrakerSession.kt clientName + clientUrl (mees.works/dinghy-display) -> OUT-OF-SCOPE:
        JSON-RPC connection identity recorded only in Moonraker's server-side connection list;
        not a jiib UI surface; package/repo identity is explicitly out of scope per CONTEXT. State
        this is a CONSCIOUS retention, not a missed surface (D-06 discretion-style explicit decision).
      - Code comments (ScreenScaffold.kt, ic_launcher background/comment, any others) -> OUT-OF-SCOPE
        (non-shipping source comments; not rendered to users).
      - Package name works.mees.dinghy, applicationId, Theme.DinghyDisplay, DinghyApp/DinghyIcon/etc.
        -> OUT-OF-SCOPE (locked: package + repo + class names stay).
    The audit's conclusion must state: zero USER-VISIBLE "Dinghy Display" strings remain; every
    survivor is an out-of-scope identity/comment.
  </action>
  <verify>
    <automated>test $(grep -c 'Dinghy Display' app/src/main/java/works/mees/dinghy/gallery/GalleryScreen.kt) -eq 0 && grep -q 'SC-4' .planning/phases/18.2-jiib-rebrand/18.2-DEBRAND-AUDIT.md && grep -qi 'clientName\|MoonrakerSession' .planning/phases/18.2-jiib-rebrand/18.2-DEBRAND-AUDIT.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - GalleryScreen.kt no longer contains "Dinghy Display".
    - 18.2-DEBRAND-AUDIT.md exists and classifies EVERY "Dinghy Display"/"dinghy" occurrence in app/src/main as FIXED or OUT-OF-SCOPE with a reason.
    - The audit explicitly documents the MoonrakerSession clientName/clientUrl retention as a conscious out-of-scope decision (not a silent miss).
    - The audit concludes zero user-visible "Dinghy Display" strings remain (SC-4).
    - After this task, the only "Dinghy Display" hits in app/src/main/java + app/src/main/res are: MoonrakerSession.kt (clientName/clientUrl) and source code comments — verified by the Task-2 grep gate.
  </acceptance_criteria>
  <done>Every user-reachable brand string is "jiib"; the audit proves SC-4 and records the deliberate out-of-scope survivors.</done>
</task>

<task type="auto">
  <name>Task 2: SC-4 grep gate + release build green (SC-5)</name>
  <files>.planning/phases/18.2-jiib-rebrand/18.2-DEBRAND-AUDIT.md</files>
  <read_first>
    - .planning/phases/18.2-jiib-rebrand/18.2-DEBRAND-AUDIT.md (the audit from Task 1 — the gate output is appended here)
    - CLAUDE.md ("Local Build Environment" — builds run Windows-side via cmd.exe /c "E:\Android\gw.bat ..."; ./gradlew does NOT run from WSL; pipe CR progress through tr -d '\r'; exit code is authoritative)
  </read_first>
  <action>
    Run the SC-4 grep gate: enumerate every "Dinghy Display" occurrence under app/src/main and
    confirm each is on the documented OUT-OF-SCOPE allow-list from Task 1 (MoonrakerSession
    clientName/clientUrl + source comments only). Specifically assert NO "Dinghy Display" remains in
    user-facing-string positions: it must NOT appear in any res/values/*.xml string body, in
    AndroidManifest android:label, in a Compose Text(...)/SectionLabel/SectionHeader/setContentTitle
    call, or in any .xml drawable. Append the gate result (the grep output + pass/fail) to
    18.2-DEBRAND-AUDIT.md.
    Then run the release build green gate (SC-5):
    cmd.exe /c "E:\Android\gw.bat :app:assembleRelease --no-daemon" piped through tr -d '\r'; the
    process exit code is authoritative (BUILD SUCCESSFUL / exit 0). Also run the full host unit suite
    to catch any regression from the brand changes:
    cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --no-daemon". Record both results in the
    audit. (The release APK is unsigned until PKG-01/Phase 8 — assembleRelease succeeding is the SC-5
    gate; signing/install for the device check uses the debug build or E:\Android\sign-release.bat.)
  </action>
  <verify>
    <automated>cmd.exe /c "E:\Android\gw.bat :app:assembleRelease --no-daemon" 2>&1 | tr -d '\r' | grep -E 'BUILD SUCCESSFUL|BUILD FAILED'</automated>
  </verify>
  <acceptance_criteria>
    - The SC-4 grep gate confirms no "Dinghy Display" appears in any string resource body, manifest label, Compose text call, or drawable; every survivor matches the documented out-of-scope allow-list.
    - :app:assembleRelease prints BUILD SUCCESSFUL (exit 0) via gw.bat (SC-5).
    - :app:testDebugUnitTest is GREEN via gw.bat (no regression, incl. the new BrandTintTest).
    - The grep output + both build/test results are appended to 18.2-DEBRAND-AUDIT.md.
  </acceptance_criteria>
  <done>SC-4 is provably closed and the release build + host suite are green (SC-5).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: On-device flox spot-check of the full jiib rebrand</name>
  <action>Build + install the debug APK on flox and visually confirm the launcher icon, app label/recents, FGS notification title, splash lockup, and about wordmark+note all render as jiib. See how-to-verify for the exact steps.</action>
  <what-built>
    The full jiib rebrand is wired and built: launcher icon (adaptive v26 + pre-26 fallback, navy +
    white jiib sail mark), app label + recents = "jiib", FGS notification title = "jiib", splash
    stacked lockup (accent-tinted, contrast-guarded), about wordmark + jib-explainer note. Debug APK
    built and installed on flox for visual confirmation.
  </what-built>
  <how-to-verify>
    On the floor device flox (LineageOS 18.1 / API 30 — note: the v26 adaptive STATIC navy+white path
    renders here, NOT the API-33 Material You monochrome tint path):
    1. Build + install the debug APK: cmd.exe /c "E:\Android\gw.bat :app:installDebug --no-daemon"
       (or assembleDebug then adb install). Then from the launcher home screen + app drawer:
       confirm the icon is the white jiib sail mark on navy (square AND round masks), art centered in
       the safe zone, crisp at the launcher's density.
    2. Confirm the launcher/app-drawer label reads "jiib" (lowercase). Open recents (overview) —
       the task title reads "jiib".
    3. Pull down the notification shade while the app's foreground service is running — the persistent
       notification title reads "jiib".
    4. Open the app to the Splash surface (kill WiFi or use first-run so the splash shows) — confirm
       the stacked jiib icon+wordmark lockup renders centered above the reason text + recovery
       buttons, tinted to the accent and clearly legible against the background.
    5. Navigate to About (drawer -> About) — confirm the jiib wordmark renders (accent-tinted) where
       the brand label was, the tagline "A direct Moonraker touchscreen for Klipper." shows, and the
       jib-explainer note renders beneath it.
    6. (Optional, D-06) Switch to a custom/low-contrast accent theme and re-check Splash + About — the
       mark must stay legible (brandTint falls back to t.text when accent washes out).
    Report "approved" if all render correctly, or describe any issue (clipping, off-center icon,
    illegible tint, wrong label).
  </how-to-verify>
  <resume-signal>Type "approved" or describe the rendering issues found.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | A debug-string edit, an audit doc, and build/device verification. No I/O, network, auth, or input surface introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18.2-05-01 | Information disclosure | MoonrakerSession client identity (retained) | accept | clientName/clientUrl are non-secret protocol identifiers recorded in Moonraker's connection list; no PII/secret. Retention is documented, out of scope. No new attack surface. |

No npm/pip/cargo installs — package legitimacy gate N/A.
</threat_model>

<verification>
- SC-4 grep gate: no user-visible "Dinghy Display" remains; survivors documented as out-of-scope.
- :app:assembleRelease BUILD SUCCESSFUL (SC-5); host suite GREEN.
- On-device flox spot-check confirms icon (SC-1), label + recents + notification (SC-2), splash
  lockup + about wordmark/note (SC-3) all render correctly.
</verification>

<success_criteria>
SC-4 (no user-visible Dinghy Display string) closed and audited. SC-5 (build green) proven.
SC-1/SC-2/SC-3 confirmed on-device. The phase is complete once the human-verify gate returns
"approved" (or the owner defers the on-device gate per the project's UAT-deferral posture).
</success_criteria>

<output>
Create `.planning/phases/18.2-jiib-rebrand/18.2-05-SUMMARY.md` when done (after the on-device gate
returns, per the project's deferral posture).
</output>
