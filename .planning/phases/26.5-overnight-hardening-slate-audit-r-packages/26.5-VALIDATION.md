---
phase: 26.5
slug: overnight-hardening-slate-audit-r-packages
status: final
nyquist_compliant: true
wave_0_complete: false
created: 2026-06-11
---

# Phase 26.5 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
>
> **The authoritative content lives in `26.5-RESEARCH.md` §"Validation Architecture"**
> (test framework, Phase-Requirements→Test Map, sampling rate, Wave-0 gaps). This file
> satisfies the Nyquist artifact gate and records the contract summary; the plan-checker
> verified the inline section substantively passes Check 8e (sampling continuity holds,
> every implementation task has a runnable `gw.bat` verify, no watch-mode/E2E latency traps).

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | JUnit 4 + kotlinx-coroutines-test 1.9.0 (host unit tests only tonight) |
| **Config file** | none (default Gradle test task) |
| **Quick run command** | `/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --no-daemon" \| tr -d '\r'` |
| **Full suite command** | same (no instrumented tests added tonight) |
| **Release gate (plans 01/04)** | `… gw.bat :app:testDebugUnitTest :app:assembleRelease verifyMinSdkRelease --no-daemon` |

## Sampling Rate

- **After every task commit:** quick run command (exit code authoritative; pipe `tr -d '\r'`).
- **After every plan:** plan build gate (`:app:testDebugUnitTest` + `:app:assembleDebug`; plans 01/04 add `assembleRelease` + `verifyMinSdkRelease`).
- **Device-only criteria:** `checkpoint:human-verify` final tasks → morning UAT checklist; verifier records `human_needed`. NOTHING blocks on flox/S25 overnight.

## Phase Requirements → Test Map

See `26.5-RESEARCH.md` §"Validation Architecture" → "Phase Requirements → Test Map" (R5a/R9/R10/R1/R2/R4/R7 rows with automated commands and grep assertions).
