---
phase: 26.5-overnight-hardening-slate-audit-r-packages
plan: 04
type: execute
wave: 3
depends_on: [26.5-03]
files_modified:
  - app/build.gradle.kts
  - app/src/main/AndroidManifest.xml
  - app/src/main/java/works/mees/dinghy/MainActivity.kt
autonomous: true
requirements:
  - "R1 — docs/top-down-audit-roadmap.md §R1 (install & display correctness, code-only; device acceptance → morning UAT)"
must_haves:
  truths:
    - "assembleRelease produces BOTH per-ABI APKs (armeabi-v7a AND arm64-v8a), isUniversalApk stays false, with distinct versionCodes (v7a=base, arm64=base+1)"
    - "Content renders edge-to-edge behind transparent system bars with safeDrawingPadding keeping it chrome-clear (status/nav/cutout)"
    - "Predictive back is opted in at the manifest (silently ignored on API 23-32; BackHandler/NavHost already compatible)"
    - "POST_NOTIFICATIONS is requested exactly once via the Activity Result API at first connect on API 33+; denial changes nothing but the notification"
    - "verifyMinSdkRelease stays green (minSdk 23 floor intact after arm64 lands)"
  artifacts:
    - path: "app/build.gradle.kts"
      provides: "ABI splits both arches + per-ABI versionCode via androidComponents.onVariants + corrected D-01a comment"
      contains: "arm64-v8a"
    - path: "app/src/main/AndroidManifest.xml"
      provides: "enableOnBackInvokedCallback + REQUEST_IGNORE_BATTERY_OPTIMIZATIONS (pre-staged for plan 26.5-05's R2 flow — this plan owns the manifest in wave 3)"
      contains: "enableOnBackInvokedCallback"
    - path: "app/src/main/java/works/mees/dinghy/MainActivity.kt"
      provides: "enableEdgeToEdge + safeDrawingPadding root + notification permission launcher"
      contains: "enableEdgeToEdge"
  key_links:
    - from: "app/src/main/java/works/mees/dinghy/MainActivity.kt"
      to: "android.permission.POST_NOTIFICATIONS"
      via: "registerForActivityResult(RequestPermission()) triggered at first connect, API>=33 gated"
      pattern: "POST_NOTIFICATIONS"
---

<objective>
R1 install & display correctness, code-only (docs/top-down-audit-roadmap.md §R1 steps 1-4):
both-ABI splits with per-ABI versionCodes, edge-to-edge/insets, predictive-back manifest opt-in,
POST_NOTIFICATIONS one-shot runtime request. Signed-APK/Release-asset publication and all
on-device acceptance (S25 Ultra, Nexus 7 unchanged) ride Phase 29 / the morning UAT checklist.

Wave-3 file ownership: this plan owns build.gradle.kts, MainActivity.kt, and the manifest —
including R2's REQUEST_IGNORE_BATTERY_OPTIMIZATIONS permission declaration (pre-staged here so
plan 26.5-05 never touches the manifest; CONTEXT wave-3 disjointness rule).

Purpose: modern arm64-only phones can install and render the app chrome-clear.
Output: two per-ABI release APKs, inset-correct UI, predictive back, notification permission flow.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/26.5-overnight-hardening-slate-audit-r-packages/26.5-CONTEXT.md
@docs/top-down-audit-roadmap.md
@.planning/phases/26.5-overnight-hardening-slate-audit-r-packages/26.5-RESEARCH.md
@.planning/phases/26.5-overnight-hardening-slate-audit-r-packages/26.5-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: ABI splits both arches + per-ABI versionCode (androidComponents.onVariants)</name>
  <files>app/build.gradle.kts</files>
  <read_first>
    - docs/top-down-audit-roadmap.md §R1 step 1 — both ABIs, isUniversalApk=false, D-01a comment fix
    - app/build.gradle.kts — splits block (~L53-59), defaultConfig versionCode literal (~L36), the D-01a comment, AND plan 26.5-01's edits already in the file (lint/toolchain) — do not disturb them
    - 26.5-RESEARCH.md "R1: ABI Splits" + Pitfall 3 (versionCode collision) + assumption A5
  </read_first>
  <action>
    1. splits.abi: `include("armeabi-v7a", "arm64-v8a")`, keep `isEnable = true`, `reset()`,
       `isUniversalApk = false` (CONTEXT-locked).
    2. Per-ABI versionCode via the modern variant API (CONTEXT-locked: androidComponents.onVariants,
       NOT the legacy applicationVariants.all shown in PATTERNS): add an `androidComponents { onVariants { ... } }`
       block that, for each variant output, reads the ABI filter via
       `output.filters` (`FilterConfiguration.FilterType.ABI`, import `com.android.build.api.variant.FilterConfiguration`)
       and sets `output.versionCode` to the defaultConfig base for armeabi-v7a and base+1 for
       arm64-v8a (read the current base literal from defaultConfig and reference it, don't
       hardcode a stale copy). Do NOT add APK-renaming logic tonight — release asset naming is
       Phase-29 packaging.
    3. Fix the D-01a comment (§R1 step 1): remove the "can never carry an arm64 slice" claim;
       state that the Nexus 7 2013 (dev floor) installs the v7a APK while ship artifacts publish
       BOTH per-ABI APKs.
  </action>
  <verify>
    <automated>grep -q 'include("armeabi-v7a", "arm64-v8a")' app/build.gradle.kts && grep -q "isUniversalApk = false" app/build.gradle.kts && grep -q "onVariants" app/build.gradle.kts</automated>
  </verify>
  <acceptance_criteria>
    - app/build.gradle.kts contains `include("armeabi-v7a", "arm64-v8a")` and `isUniversalApk = false`
    - An `androidComponents { onVariants` block sets versionCode per ABI (v7a = base, arm64 = base+1); no `applicationVariants.all` legacy API used
    - The D-01a comment no longer claims the build can never carry arm64
    - Plan 26.5-01's lint/toolchain edits in the same file are untouched (diff scoped to splits/onVariants/comment)
  </acceptance_criteria>
  <done>Release builds emit two per-ABI APKs with non-colliding versionCodes.</done>
</task>

<task type="auto">
  <name>Task 2: Edge-to-edge, predictive back, POST_NOTIFICATIONS one-shot (+ R2 permission pre-stage)</name>
  <files>app/src/main/AndroidManifest.xml, app/src/main/java/works/mees/dinghy/MainActivity.kt</files>
  <read_first>
    - docs/top-down-audit-roadmap.md §R1 steps 2-4
    - 26.5-PATTERNS.md "AndroidManifest.xml" + "MainActivity.kt" sections — exact attribute/launcher/insets shapes + the new-imports list
    - app/src/main/java/works/mees/dinghy/MainActivity.kt — onCreate structure (~L36-96): super.onCreate position, container acquisition, startForegroundService call, setContent root Box (~L79)
    - app/src/main/AndroidManifest.xml — permission block (~L29-32, POST_NOTIFICATIONS already declared) and application element (~L51)
    - app/src/main/java/works/mees/dinghy/di/AppContainer.kt — find the existing connection/session state flow MainActivity can observe for the first-connect trigger (e.g. the session/connection StateFlow near activeConfig ~L218)
    - 26.5-RESEARCH.md "R1: Edge-to-Edge" + "R1: POST_NOTIFICATIONS" + Pitfall 1 (enableEdgeToEdge placement)
  </read_first>
  <action>
    Manifest:
    1. `<application android:enableOnBackInvokedCallback="true" ...>` (§R1 step 3 — required at
       targetSdk 35, auto at 36; silently ignored on API 23-32; existing BackHandler + NavHost
       already compatible, no code change).
    2. Add `<uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS"/>`
       next to the existing permissions — this is R2's declaration (roadmap §R2 step 2),
       pre-staged HERE because this plan owns the manifest in wave 3; plan 26.5-05 consumes it.
       Comment it as such.
    MainActivity (§R1 steps 2+4):
    3. `enableEdgeToEdge()` as the FIRST statement of onCreate, before super.onCreate (Pitfall 1).
       Append `.safeDrawingPadding()` to the root Box's modifier chain AFTER `.background(...)`
       (background paints behind the system bars; content stays chrome-clear — covers status bar,
       nav bar, and cutouts; returns 0 where hardware lacks them, API-23 safe). The four
       Views-hosted surfaces live inside this root and inherit (verify listed on morning UAT).
    4. POST_NOTIFICATIONS one-shot: a `notificationPermLauncher` field via
       `registerForActivityResult(ActivityResultContracts.RequestPermission())` with a no-op
       result handler (denial changes nothing except the notification — §R1 step 4). Trigger:
       observe the existing connection-state flow from AppContainer (identified in read_first)
       inside setContent via LaunchedEffect; on the FIRST transition to a connected state, if
       `Build.VERSION.SDK_INT >= 33` and the permission is not granted
       (ContextCompat.checkSelfPermission), launch the request — at most once per process
       (latch with a remembered/field boolean). If no suitable connection flow is exposed,
       fall back to triggering once after startForegroundService when an active config exists,
       and note the substitution in the SUMMARY. Imports per the PATTERNS list.
  </action>
  <verify>
    <automated>grep -q "enableOnBackInvokedCallback" app/src/main/AndroidManifest.xml && grep -q "REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" app/src/main/AndroidManifest.xml && grep -q "enableEdgeToEdge" app/src/main/java/works/mees/dinghy/MainActivity.kt && grep -q "safeDrawingPadding" app/src/main/java/works/mees/dinghy/MainActivity.kt && grep -q "RequestPermission" app/src/main/java/works/mees/dinghy/MainActivity.kt</automated>
  </verify>
  <acceptance_criteria>
    - Manifest application element carries `android:enableOnBackInvokedCallback="true"`; both POST_NOTIFICATIONS (pre-existing) and REQUEST_IGNORE_BATTERY_OPTIMIZATIONS permissions declared
    - MainActivity: `enableEdgeToEdge()` precedes `super.onCreate`; root Box modifier chain contains `safeDrawingPadding()`
    - `registerForActivityResult(ActivityResultContracts.RequestPermission())` is a MainActivity field (NOT inside a LaunchedEffect); launch is gated by `Build.VERSION_CODES.TIRAMISU`/SDK 33 check + checkSelfPermission + a once-per-process latch
    - No behavior change on denial (handler body is a documented no-op)
  </acceptance_criteria>
  <done>Edge-to-edge + predictive back + one-shot notification request landed; R2's manifest permission pre-staged.</done>
</task>

<task type="auto">
  <name>Task 3: Build gate (R1 extended: release + both APKs + minSdk verify)</name>
  <files>(none — verification only)</files>
  <read_first>
    - CLAUDE.md "Local Build Environment"
  </read_first>
  <action>
    Run `timeout 1500 /mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest :app:assembleDebug :app:assembleRelease verifyMinSdkRelease --no-daemon" | tr -d '\r'`
    (R5a+R1 are the two extended-gate plans per CONTEXT). Then assert BOTH per-ABI release APKs
    exist: `ls app/build/outputs/apk/release/` must show one `*armeabi-v7a*` and one
    `*arm64-v8a*` APK and no universal APK. Exit codes authoritative. 3 strikes / ~90 min →
    revert, mark failed, move on.
  </action>
  <verify>
    <automated>timeout 1500 /mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest :app:assembleDebug :app:assembleRelease verifyMinSdkRelease --no-daemon" | tr -d '\r' | tail -3 && ls app/build/outputs/apk/release/ | grep -c "armeabi-v7a\|arm64-v8a" | grep -q "2"</automated>
  </verify>
  <acceptance_criteria>
    - Combined Gradle invocation exits 0 (tests, debug, release incl. lintVital, verifyMinSdkRelease)
    - `ls app/build/outputs/apk/release/` lists exactly one armeabi-v7a APK and one arm64-v8a APK, zero universal APKs
    - `git status --porcelain` clean after final commit
  </acceptance_criteria>
  <done>Both per-ABI APKs build green with the minSdk floor verified.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 4: Morning UAT — S25 Ultra install/display + Nexus 7 unchanged (roadmap §R1 acceptance)</name>
  <what-built>Both-ABI release builds, edge-to-edge/insets, predictive back opt-in, notification one-shot — host-verified only.</what-built>
  <how-to-verify>
    Deferred to morning UAT checklist — verifier records human_needed; do NOT block the wave on this.
    S25 Ultra (arm64-only Android 15/16):
    1. Sideload the arm64-v8a APK (debug-sign the release per CLAUDE.md sign-release.bat) — installs cleanly.
    2. Both orientations: no content under cutout/status/nav; background still paints edge-to-edge.
    3. Back gesture: predictive-back preview animates drawer-close and stack-pop.
    4. First connect: POST_NOTIFICATIONS dialog appears exactly once; deny → app fully functional, FGS notification absent.
    flox (Nexus 7):
    5. Install the armeabi-v7a APK — behavior unchanged (no inset regressions on the bar-less landscape kiosk layout).
  </how-to-verify>
  <action>Morning-only on-device verification: do nothing tonight beyond ensuring the items appear on the morning UAT checklist. Verifier records human_needed; the wave proceeds.</action>
  <verify>
    <human-check>Owner runs the S25 Ultra install/edge-to-edge/predictive-back/notification protocol and the flox unchanged-regression check above.</human-check>
  </verify>
  <done>arm64 APK installs and renders chrome-clear both orientations; predictive back previews; notification one-shot behaves; Nexus 7 unchanged — owner-approved.</done>

  <resume-signal>Type "approved" or describe the rendering/install defect</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OS permission dialog → app | Runtime permission grant/denial crosses from the user/OS into app state |
| APK artifacts → sideload users | Per-ABI APKs must not confuse installs (versionCode collision) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26.5-10 | Elevation of Privilege | POST_NOTIFICATIONS request | mitigate | One-shot Activity Result API; API-33 gate; denial path is a no-op (status-only notification); no rationale-nag loop |
| T-26.5-11 | Spoofing/Confusion | per-ABI versionCodes | mitigate | Deterministic offset (v7a=base, arm64=base+1) via onVariants — Pitfall 3 collision avoided |
| T-26.5-12 | Information Disclosure | edge-to-edge behind system bars | accept | Only the app's own background paints behind bars; no sensitive content rendered there (safeDrawingPadding keeps content inside) |
| T-26.5-SC | Tampering | package installs | accept | None this plan (no new dependencies; manifest/buildscript only) |
</threat_model>

<verification>
- Grep proofs for splits/onVariants/manifest attributes/MainActivity calls
- Extended build gate green; both per-ABI APKs present
- Morning UAT items recorded for the verifier as human_needed
</verification>

<success_criteria>
Roadmap §R1 acceptance code-half: both per-ABI APKs build with distinct versionCodes,
edge-to-edge + predictive back + notification one-shot landed, verifyMinSdkRelease green.
Device-half (S25 chrome-clear/back-preview/notification, Nexus 7 unchanged) → morning UAT.
</success_criteria>

<output>
Create `.planning/phases/26.5-overnight-hardening-slate-audit-r-packages/26.5-04-SUMMARY.md` when done.
Include a `## Report Notes` section noting the R2 manifest permission was pre-staged here.
</output>
