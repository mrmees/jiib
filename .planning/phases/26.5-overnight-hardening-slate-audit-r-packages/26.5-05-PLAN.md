---
phase: 26.5-overnight-hardening-slate-audit-r-packages
plan: 05
type: execute
wave: 3
depends_on: [26.5-03]
files_modified:
  - app/src/main/java/works/mees/dinghy/ui/settings/DisplayPrefs.kt
  - app/src/main/java/works/mees/dinghy/DinghyApp.kt
  - app/src/main/java/works/mees/dinghy/di/AppContainer.kt
  - app/src/main/java/works/mees/dinghy/ui/screen/SettingsScreen.kt
  - app/src/main/java/works/mees/dinghy/ui/shell/AppShell.kt
  - app/src/main/res/values/strings.xml
  - app/src/test/java/works/mees/dinghy/ui/settings/DisplayPrefsTest.kt
autonomous: true
requirements:
  - "R2 — docs/top-down-audit-roadmap.md §R2 (always-on, code-only; doze UAT → morning)"
must_haves:
  truths:
    - "A 'Keep screen awake' Settings toggle (default ON) gates FLAG_KEEP_SCREEN_ON on the app's window, cleared when toggled off or the UI leaves the foreground"
    - "The toggle's DataStore write routes through an AppContainer writeScope intent method — NEVER a composition scope ([[dinghy-compose-write-scope-cancellation]])"
    - "Settings surfaces current battery-optimization exemption state and a tap deep-links to the system exemption dialog (with the LineageOS redirect fallback)"
    - "Read failure of the preference fails safe to the default (true) — never crashes"
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/ui/settings/DisplayPrefs.kt"
      provides: "keepScreenOn Flow<Boolean> default TRUE + setKeepScreenOn, BabystepPrefs shape"
      contains: "keep_screen_on"
    - path: "app/src/test/java/works/mees/dinghy/ui/settings/DisplayPrefsTest.kt"
      provides: "default-true, IOException fail-safe, round-trip cases"
    - path: "app/src/main/java/works/mees/dinghy/ui/screen/SettingsScreen.kt"
      provides: "Display section: keep-screen-on ToggleRow + battery-exemption status/deep-link row"
  key_links:
    - from: "app/src/main/java/works/mees/dinghy/ui/screen/SettingsScreen.kt"
      to: "di/AppContainer.kt"
      via: "container.setKeepScreenOn(it) writeScope intent"
      pattern: "setKeepScreenOn"
    - from: "app/src/main/java/works/mees/dinghy/ui/shell/AppShell.kt"
      to: "DisplayPrefs.keepScreenOn"
      via: "collectAsStateWithLifecycle → LocalView.current.keepScreenOn"
      pattern: "keepScreenOn"
---

<objective>
R2 always-on survival, code-only (docs/top-down-audit-roadmap.md §R2 steps 1-2; step 3's doze
UAT goes to the morning checklist): keep-screen-on Settings toggle (default ON for the
dedicated-display use case) and the battery-optimization exemption flow (state surface +
deep-link). The REQUEST_IGNORE_BATTERY_OPTIMIZATIONS manifest declaration was pre-staged by
plan 26.5-04 (which owns the manifest in wave 3) — this plan consumes it.

File-ownership note (CONTEXT wave-3 disjointness): MainActivity.kt is owned by 26.5-04, so the
screen-on flag is applied via `View.keepScreenOn` at the AppShell root — the Compose-idiomatic
mechanism that sets/clears FLAG_KEEP_SCREEN_ON on the hosting window while the view is attached,
satisfying §R2 step 1's flag-on-the-window-with-clear semantics without touching MainActivity.

Purpose: the tablet-on-a-wall use case survives the screen-timeout and Doze halves are wired.
Output: DisplayPrefs store + toggle + window flag + exemption flow, host-tested.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/26.5-overnight-hardening-slate-audit-r-packages/26.5-CONTEXT.md
@docs/top-down-audit-roadmap.md
@.planning/phases/26.5-overnight-hardening-slate-audit-r-packages/26.5-PATTERNS.md
@.planning/phases/26.5-overnight-hardening-slate-audit-r-packages/26.5-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DisplayPrefs store + AppContainer wiring + tests</name>
  <files>app/src/main/java/works/mees/dinghy/ui/settings/DisplayPrefs.kt, app/src/main/java/works/mees/dinghy/DinghyApp.kt, app/src/main/java/works/mees/dinghy/di/AppContainer.kt, app/src/test/java/works/mees/dinghy/ui/settings/DisplayPrefsTest.kt</files>
  <read_first>
    - docs/top-down-audit-roadmap.md §R2 step 1 — toggle semantics, default ON
    - app/src/main/java/works/mees/dinghy/ui/settings/BabystepPrefs.kt — the EXACT shape to copy (imports, fail-safe catch, key naming, KDoc write-scope contract)
    - app/src/test/java/works/mees/dinghy/ui/settings/BabystepPrefsTest.kt — verbatim test-harness structure (temp-file DataStore factory)
    - app/src/main/java/works/mees/dinghy/DinghyApp.kt — the 7 existing PreferenceDataStoreFactory.create blocks (~L42-93) + plan 26.5-01's StrictMode addition at onCreate end (do not disturb)
    - app/src/main/java/works/mees/dinghy/di/AppContainer.kt — writeScope field (~L142) + setBabystepEnabled intent (~L303-304), the pattern to mirror
    - 26.5-PATTERNS.md "DisplayPrefs.kt" + "Shared Patterns" (writeScope, test harness)
  </read_first>
  <behavior>
    - Test 1: empty store → keepScreenOn emits true (DEFAULT_KEEP_SCREEN_ON, §R2 default-ON)
    - Test 2: IOException on read → emits true (fail-safe, never crashes)
    - Test 3: setKeepScreenOn(false) → keepScreenOn emits false (round-trip)
    - Test 4: setKeepScreenOn(true) after false → emits true
  </behavior>
  <action>
    Create `DisplayPrefs` (ui/settings) copying BabystepPrefs verbatim in structure: injected
    `DataStore<Preferences>`, `keepScreenOn: Flow<Boolean>` with the IOException→emptyPreferences
    catch and `?: DEFAULT_KEEP_SCREEN_ON` (const true), `suspend fun setKeepScreenOn(on: Boolean)`
    via dataStore.edit, companion with `booleanPreferencesKey("keep_screen_on")`. KDoc carries the
    write-scope contract ([[dinghy-compose-write-scope-cancellation]]): UI never calls the suspend
    setter from a composition scope — route through AppContainer.
    DinghyApp: add an 8th DataStore `"display.preferences_pb"` following the existing factory
    comment/naming pattern; pass it where the container is constructed.
    AppContainer: `displayPrefs` field, expose `keepScreenOn: Flow<Boolean>`, add
    `fun setKeepScreenOn(on: Boolean)` launching on the existing writeScope — identical in shape
    to setBabystepEnabled.
    Write DisplayPrefsTest per the behavior table using BabystepPrefsTest's temp-file harness.
  </action>
  <verify>
    <automated>timeout 900 /mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --tests *DisplayPrefsTest* --no-daemon" | tr -d '\r' | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - DisplayPrefs.kt exists with `DEFAULT_KEEP_SCREEN_ON = true` and key literal `"keep_screen_on"`; catch block matches BabystepPrefs (IOException → emptyPreferences, else rethrow)
    - DinghyApp creates `display.preferences_pb` as the 8th store; StrictMode block from 26.5-01 untouched
    - AppContainer has `setKeepScreenOn` launching on `writeScope` (grep shows NO `rememberCoroutineScope` anywhere in this plan's diff)
    - All 4 DisplayPrefsTest cases pass
  </acceptance_criteria>
  <done>Preference store wired end-to-end with the process-lifetime write path, default ON, fail-safe, host-proven.</done>
</task>

<task type="auto">
  <name>Task 2: Settings Display section + window-flag application + exemption flow</name>
  <files>app/src/main/java/works/mees/dinghy/ui/screen/SettingsScreen.kt, app/src/main/java/works/mees/dinghy/ui/shell/AppShell.kt, app/src/main/res/values/strings.xml</files>
  <read_first>
    - docs/top-down-audit-roadmap.md §R2 steps 1-2 — toggle wording intent, exemption deep-link + state surface
    - app/src/main/java/works/mees/dinghy/ui/screen/SettingsScreen.kt — SectionLabel/ToggleRow structure (~L100-238) and the babystep toggle row to mirror; how it reaches `container`
    - 26.5-PATTERNS.md "SettingsScreen.kt" section — toggle + battery-row shapes, fsSp sizes, token rules
    - 26.5-RESEARCH.md "R2: Battery Exemption Flow" — API-23+ PowerManager check, ACTION_REQUEST_IGNORE_BATTERY_OPTIMIZATIONS intent, LineageOS redirect gotcha (A10)
    - app/src/main/java/works/mees/dinghy/ui/shell/AppShell.kt — root composable structure (plan 26.5-03 edited the swipe detector; touch only the root-effect insertion point)
  </read_first>
  <action>
    SettingsScreen — new "Display" section after the existing feature toggles:
    1. Keep-screen-on ToggleRow: label/subLabel via NEW strings.xml entries (e.g.
       `settings_keep_screen_on`, plus on/off sublabels) — stringResource ONLY, fsSp() for all
       text sizes, LocalTokens colors only, NO new icons (icon law — text rows, or existing
       DinghyIcons tokens if the section header convention requires one already in the registry).
       checked = `container.keepScreenOn.collectAsStateWithLifecycle(initialValue = true)`;
       onToggle = `container.setKeepScreenOn(it)` (writeScope intent — never a local scope).
    2. Battery-optimization row (§R2 step 2): status text "Exempt"/"Optimized" from
       `PowerManager.isIgnoringBatteryOptimizations(packageName)` (API 23+, synchronous),
       re-checked on ON_RESUME via a DisposableEffect + LifecycleEventObserver so the row updates
       after the user returns from the system dialog. Tap (when not exempt) fires
       `Intent(Settings.ACTION_REQUEST_IGNORE_BATTERY_OPTIMIZATIONS, Uri.parse("package:$packageName"))`
       with one explanatory sentence as the subLabel; catch ActivityNotFoundException/any throw →
       fall back to `ACTION_IGNORE_BATTERY_OPTIMIZATION_SETTINGS` (LineageOS may redirect — A10).
       New strings for label/sublabels/explanation.
    Window flag application (§R2 step 1, applied at the shell root per the wave-3 ownership note
    in the objective): in AppShell's root composable, collect `container.keepScreenOn`
    (initialValue = true) and run `val view = LocalView.current; DisposableEffect(keepScreenOn) {
    view.keepScreenOn = keepScreenOn; onDispose { view.keepScreenOn = false } }` — sets
    FLAG_KEEP_SCREEN_ON on the hosting window while attached; cleared on dispose and on
    toggle-off (live response, no resume cycle needed). Skip composition in @Preview contexts is
    unnecessary — LocalView is preview-safe for property writes, but if previews complain, guard
    with `LocalInspectionMode`.
  </action>
  <verify>
    <automated>grep -q "setKeepScreenOn" app/src/main/java/works/mees/dinghy/ui/screen/SettingsScreen.kt && grep -q "isIgnoringBatteryOptimizations" app/src/main/java/works/mees/dinghy/ui/screen/SettingsScreen.kt && grep -q "ACTION_REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" app/src/main/java/works/mees/dinghy/ui/screen/SettingsScreen.kt && grep -q "keepScreenOn" app/src/main/java/works/mees/dinghy/ui/shell/AppShell.kt && grep -q "settings_keep_screen_on" app/src/main/res/values/strings.xml</automated>
  </verify>
  <acceptance_criteria>
    - SettingsScreen Display section: ToggleRow wired to container.keepScreenOn/setKeepScreenOn; battery row shows exemption state, re-checks on ON_RESUME, deep-links with the LineageOS fallback intent
    - All new user-visible strings live in strings.xml (zero hardcoded literals in the new @Composable code); all new text uses fsSp(); zero raw Color values; zero NEW icon glyphs
    - AppShell contains the LocalView.keepScreenOn DisposableEffect keyed on the collected preference, clearing in onDispose
    - `rememberCoroutineScope` does not appear in this plan's diff
  </acceptance_criteria>
  <done>Toggle drives the window flag live; exemption state visible with a working deep-link; conventions (strings/fsSp/tokens/icon-law/writeScope) all honored.</done>
</task>

<task type="auto">
  <name>Task 3: Build gate</name>
  <files>(none — verification only)</files>
  <read_first>
    - CLAUDE.md "Local Build Environment"
  </read_first>
  <action>
    Run `timeout 1200 /mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest :app:assembleDebug --no-daemon" | tr -d '\r'`.
    Exit code authoritative. 3 strikes / ~90 min → revert, mark failed, move on (fail-forward).
  </action>
  <verify>
    <automated>timeout 1200 /mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest :app:assembleDebug --no-daemon" | tr -d '\r' | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - Combined invocation exits 0
    - `git status --porcelain` clean after final commit
  </acceptance_criteria>
  <done>Full host suite + debug assemble green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 4: Morning UAT — doze survival + keep-awake (roadmap §R2 acceptance / PITFALLS Pitfall 5)</name>
  <what-built>Keep-screen-on toggle (default ON) driving the window flag; battery-exemption state + deep-link flow.</what-built>
  <how-to-verify>
    Deferred to morning UAT checklist — verifier records human_needed; do NOT block the wave on this.
    On flox (and the modern phone if handy):
    1. Settings → Display: toggle ON → screen never times out on the print surface; toggle OFF → system timeout applies again (no relaunch needed).
    2. Battery row: shows "Optimized" initially; tap → system exemption dialog (or LineageOS App-Info battery page via the fallback); grant → row reads "Exempt" on return.
    3. Doze test (§R2 step 3): unplugged, screen off, 20+ minutes → reconnect is alive or resyncs cleanly on wake.
  </how-to-verify>
  <action>Morning-only on-device verification: do nothing tonight beyond ensuring the items appear on the morning UAT checklist. Verifier records human_needed; the wave proceeds.</action>
  <verify>
    <human-check>Owner runs the keep-awake toggle, exemption-flow, and 20+ minute doze protocol above on flox.</human-check>
  </verify>
  <done>Keep-awake toggle drives the screen live; exemption state updates after grant; doze test passes (connection alive or clean resync) — owner-approved.</done>

  <resume-signal>Type "approved" or describe the doze/keep-awake failure</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app → system Settings | Battery-exemption intent asks the OS to whitelist the app from Doze |
| Settings UI → DataStore | Toggle writes persist user preference to disk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26.5-13 | Elevation of Privilege | REQUEST_IGNORE_BATTERY_OPTIMIZATIONS | mitigate | User-initiated system dialog only (never silent); state surfaced honestly; sideload context — Play policy N/A (RESEARCH: Gemini-verified) |
| T-26.5-14 | Denial of Service (data loss) | toggle write path | mitigate | writeScope intent method — composition-scope cancellation trap ([[dinghy-compose-write-scope-cancellation]]) structurally avoided; fail-safe read default |
| T-26.5-15 | Denial of Service (battery/panel) | FLAG_KEEP_SCREEN_ON default ON | accept | Dedicated-display use case by design (§R2); user toggle provided; burn-in screensaver is Phase 29 scope |
| T-26.5-SC | Tampering | package installs | accept | None this plan |
</threat_model>

<verification>
- DisplayPrefsTest 4/4 green; full suite green
- Grep proofs: writeScope route, no rememberCoroutineScope, stringResource/fsSp/token conventions, LocalView.keepScreenOn effect
- Build gate exit 0
</verification>

<success_criteria>
Roadmap §R2 acceptance code-half: toggle (default ON) + window flag + exemption declaration/
deep-link/state surface landed and host-tested. The doze test itself (§R2 step 3) → morning UAT.
</success_criteria>

<output>
Create `.planning/phases/26.5-overnight-hardening-slate-audit-r-packages/26.5-05-SUMMARY.md` when done.
Include a `## Report Notes` section: exemption-flow behavior notes for the morning doze test.
</output>
