---
phase: 26.5-overnight-hardening-slate-audit-r-packages
plan: 03
type: execute
wave: 2
depends_on: [26.5-02]
files_modified:
  - app/src/main/java/works/mees/dinghy/designsystem/control/OutlinedControl.kt
  - app/src/main/java/works/mees/dinghy/designsystem/components/AdjusterPanel.kt
  - app/src/main/java/works/mees/dinghy/designsystem/components/ListRow.kt
  - app/src/main/java/works/mees/dinghy/command/CommandDispatcher.kt
  - app/src/main/java/works/mees/dinghy/ui/shell/AppShell.kt
  - app/src/main/java/works/mees/dinghy/ui/shell/SwipeUpAccumulator.kt
  - app/src/main/java/works/mees/dinghy/ui/finetune/FineTuneScreen.kt
  - app/src/main/java/works/mees/dinghy/ui/temperature/TemperatureScreen.kt
  - app/src/test/java/works/mees/dinghy/command/CommandDispatcherTest.kt
  - app/src/test/java/works/mees/dinghy/ui/shell/SwipeUpAccumulatorTest.kt
autonomous: true
requirements:
  - "R10 steps 2-4 + step-1 instrumentation behind a debug flag — docs/top-down-audit-roadmap.md §R10 + Part 5 (causes #1-#4); step 5 Crossfade guard explicitly NOT implemented blind"
must_haves:
  truths:
    - "Disabled stepper controls are truly non-clickable — no clickable modifier installed, no ripple, no swallowed tap (Part 5 cause #1)"
    - "Dispatcher busy/debounce rejections emit a rejectedKey event that the adjuster control class renders as a brief one-shot visible flash (Part 5 cause #2) — debounce SEMANTICS and 400ms timing UNCHANGED"
    - "Drawer swipe-up accumulates dragAmount per gesture against the unchanged 80f threshold — a slow 800px/12-event swipe now opens the drawer (Part 5 cause #3)"
    - "Kit controls in scrollables show immediate press indication via explicit MutableInteractionSource (Part 5 cause #4)"
    - "All R10 instrumentation logging is behind BuildConfig.DEBUG (code only; no on-device session tonight)"
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/command/CommandDispatcher.kt"
      provides: "rejectedKey SharedFlow emitted at both early returns"
      contains: "rejectedKey"
    - path: "app/src/main/java/works/mees/dinghy/ui/shell/SwipeUpAccumulator.kt"
      provides: "Pure, host-testable per-gesture drag accumulator"
    - path: "app/src/test/java/works/mees/dinghy/ui/shell/SwipeUpAccumulatorTest.kt"
      provides: "Proof the 12-event slow swipe crosses the threshold"
  key_links:
    - from: "app/src/main/java/works/mees/dinghy/ui/shell/AppShell.kt"
      to: "SwipeUpAccumulator"
      via: "detectVerticalDragGestures onDragStart/onVerticalDrag"
      pattern: "SwipeUpAccumulator"
    - from: "app/src/main/java/works/mees/dinghy/ui/finetune/FineTuneScreen.kt"
      to: "CommandDispatcher.rejectedKey"
      via: "collect → per-panel rejectTick passed to AdjusterPanel"
      pattern: "rejectedKey"
---

<objective>
R10 touch responsiveness, steps 2-4 plus step-1 instrumentation behind a debug flag
(docs/top-down-audit-roadmap.md §R10; analysis + fix directions in Part 5). Kills the two
HIGH-confidence silent-rejection mechanisms (cause #1 busy-lock swallow, cause #2 dispatcher
debounce), hardens the swipe-up detector (cause #3 — also fixes the FineTuneNavTest harness
defect), and adds indication immediacy in scrollables (cause #4). Step 5 (Crossfade guard) is
NOT implemented blind, per CONTEXT.

PROTECTED (CONTEXT): the 400ms debounce SEMANTICS and in-flight guard behavior are unchanged —
this plan adds FEEDBACK only. No render-hot-path edits (GraphView/ProgressRing/RecyclerViews).
No new icons/glyphs (icon law); feedback uses existing tokens, one-shot animation only (no
looping — Adreno-320 motion law).

Purpose: taps land reliably with immediate feedback before Phases 27-28 build on these components.
Output: true disablement, visible rejection feedback, accumulated swipe, fast indication, debug instrumentation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/26.5-overnight-hardening-slate-audit-r-packages/26.5-CONTEXT.md
@docs/top-down-audit-roadmap.md
@.planning/phases/26.5-overnight-hardening-slate-audit-r-packages/26.5-PATTERNS.md
@.claude/skills/sketch-findings-dinghy-display/SKILL.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: True disablement + dispatcher rejection feedback (Part 5 causes #1+#2)</name>
  <files>app/src/main/java/works/mees/dinghy/designsystem/control/OutlinedControl.kt, app/src/main/java/works/mees/dinghy/designsystem/components/AdjusterPanel.kt, app/src/main/java/works/mees/dinghy/command/CommandDispatcher.kt, app/src/main/java/works/mees/dinghy/ui/finetune/FineTuneScreen.kt, app/src/main/java/works/mees/dinghy/ui/temperature/TemperatureScreen.kt, app/src/test/java/works/mees/dinghy/command/CommandDispatcherTest.kt</files>
  <read_first>
    - docs/top-down-audit-roadmap.md §R10 step 2 + Part 5 causes #1/#2 and "Fix directions"
    - 26.5-PATTERNS.md sections: "CommandDispatcher.kt", "AdjusterPanel.kt" (Option B is the locked approach), "Clickable True-Disablement", "CommandDispatcher SharedFlow Pattern", test shapes for CommandDispatcherTest
    - app/src/main/java/works/mees/dinghy/designsystem/control/OutlinedControl.kt — BOTH overloads (~L99 and ~L199) and the clickMod construction
    - app/src/main/java/works/mees/dinghy/designsystem/components/AdjusterPanel.kt — controlsEnabled at ~L104-107, guarded onClicks at ~L137/192/200, the WR-07 dim convention comment
    - app/src/main/java/works/mees/dinghy/command/CommandDispatcher.kt — _events SharedFlow shape (~L89-94), the two early returns in dispatch() (~L110/115)
    - app/src/main/java/works/mees/dinghy/ui/finetune/FineTuneScreen.kt + ui/temperature/TemperatureScreen.kt — how each obtains `dispatcher` (container.dispatcher collectAsStateWithLifecycle) and which CommandRegistry specs it dispatches (the spec keys are the rejection-filter keys)
    - app/src/test/java/works/mees/dinghy/command/CommandDispatcherTest.kt — FakeRpc harness + runTest pattern
  </read_first>
  <behavior>
    - Test 1: second dispatch of an in-flight key emits that key on rejectedKey (existing inFlight behavior unchanged)
    - Test 2: re-dispatch inside the 400ms debounce window emits the key on rejectedKey; the wrapped call is NOT re-issued
    - Test 3 (regression): accepted dispatches emit NOTHING on rejectedKey; existing dispatcher tests stay green (semantics untouched)
  </behavior>
  <action>
    Dispatcher feedback (§R10 step 2, second half): add `_rejectedKey = MutableSharedFlow<String>(extraBufferCapacity = 4)`
    + public `rejectedKey` accessor to CommandDispatcher, mirroring the existing `_events` shape.
    At BOTH early returns in dispatch() (in-flight guard and debounce guard) call
    `_rejectedKey.tryEmit(key)` and, behind `BuildConfig.DEBUG`, `Log.d("Dispatcher", ...)` with
    key + reason (in_flight / debounce + remaining ms) — this IS the step-1 instrumentation for
    cause #2, landed behind the debug flag per CONTEXT. The guards' order, timing, and return
    behavior are byte-for-byte semantically unchanged (protected surface).

    True disablement (§R10 step 2, first half — PATTERNS Option B, the locked two-file change):
    add `enabled: Boolean = true` to BOTH OutlinedControl overloads; when false the click modifier
    is `Modifier` (no clickable/combinedClickable installed at all → no ripple, no event consumed
    — NOT a guarded lambda). Default true keeps every existing call site source-compatible.
    In AdjusterPanel: replace the three `onClick = { if (controlsEnabled) ... }` guards with
    direct lambda references + `enabled = controlsEnabled`; KEEP the WR-07 dim
    (alpha 0.38 + semantics disabled) so the lock stays visible.

    Visible rejection flash (Part 5 fix direction "heard you, still settling"): add an optional
    `rejectTick: Long = 0L` param to AdjusterPanel. On each tick change (LaunchedEffect, ignore
    initial 0), run a ONE-SHOT ~200ms flash of the value/name row — animate a color or alpha from
    an accent/warn token back to normal using LocalTokens roles only (no raw colors, no new
    glyphs, no looping animation). In FineTuneScreen and TemperatureScreen: collect
    `dispatcher.rejectedKey`, filter by each panel's dispatch key(s) (read the
    `dispatcher?.dispatch(CommandRegistry.<spec>, ...)` call sites to learn the key strings),
    hold a per-panel tick counter in remembered state, increment on matching rejection, pass to
    that AdjusterPanel. Previews and other call sites are untouched (param defaults).
  </action>
  <verify>
    <automated>timeout 900 /mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --tests *CommandDispatcherTest* --no-daemon" | tr -d '\r' | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - CommandDispatcher.kt contains `rejectedKey` SharedFlow; `tryEmit` appears at BOTH early-return sites; `debounceMs` default value and guard expressions unchanged (diff shows no timing edits)
    - Both OutlinedControl overloads accept `enabled: Boolean = true`; grep shows the disabled branch yields a bare `Modifier` (no `clickable(` in that branch)
    - AdjusterPanel.kt contains zero `if (controlsEnabled)` click guards; passes `enabled = controlsEnabled` to its OutlinedControls; dim/semantics-disabled retained
    - AdjusterPanel has `rejectTick: Long = 0L`; FineTuneScreen and TemperatureScreen both reference `rejectedKey`
    - New CommandDispatcherTest cases (in-flight emission + debounce emission) pass; ALL pre-existing dispatcher tests pass unchanged
    - Debug logs guarded by `BuildConfig.DEBUG` only
  </acceptance_criteria>
  <done>Disabled steppers cannot ripple-and-swallow; every intentional rejection produces a collectable event rendered as a one-shot flash; dispatcher semantics provably unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Swipe-up accumulation + indication immediacy (Part 5 causes #3+#4)</name>
  <files>app/src/main/java/works/mees/dinghy/ui/shell/SwipeUpAccumulator.kt, app/src/main/java/works/mees/dinghy/ui/shell/AppShell.kt, app/src/main/java/works/mees/dinghy/designsystem/components/ListRow.kt, app/src/test/java/works/mees/dinghy/ui/shell/SwipeUpAccumulatorTest.kt</files>
  <read_first>
    - docs/top-down-audit-roadmap.md §R10 steps 3-4 + Part 5 causes #3/#4
    - 26.5-PATTERNS.md "AppShell.kt" (accumulation shape) + "ListRow.kt" (interactionSource shape)
    - app/src/main/java/works/mees/dinghy/ui/shell/AppShell.kt — the detectVerticalDragGestures block (~L549-553) and SWIPE_UP_THRESHOLD_PX = 80f (~L952); note what `suppressSwipe`/pointerInput keys exist at HEAD
    - app/src/main/java/works/mees/dinghy/designsystem/components/ListRow.kt — current clickable chain (~L108-123)
  </read_first>
  <behavior>
    - Test 1: 12 drag events of -66.7px each (the FineTuneNavTest swipeUp profile) → accumulator fires exactly once
    - Test 2: single -100px event → fires (fast-flick regression guard)
    - Test 3: -60px then +60px then -60px in one gesture → never fires (net never crosses -80)
    - Test 4: -70px, gesture ends, new gesture -70px → never fires (reset between gestures)
  </behavior>
  <action>
    Create `SwipeUpAccumulator` (new file, ui/shell): a tiny pure class taking
    `thresholdPx: Float`; `onDragStart()` zeroes the accumulator; `onDrag(dragAmount: Float): Boolean`
    adds the delta and returns true (then resets) when the accumulated total ≤ -thresholdPx.
    Pure Kotlin, no Compose imports — host-testable (this is how §R10 step 3's behavior gets a
    real unit test; the prior per-event check is exactly why FineTuneNavTest could not open the
    drawer). Write SwipeUpAccumulatorTest per the behavior table.

    Wire it in AppShell's swipe detector: `detectVerticalDragGestures(onDragStart = { acc reset },
    onVerticalDrag = { _, dragAmount -> if (acc.onDrag(dragAmount)) drawerOpen = true })`, with a
    `BuildConfig.DEBUG`-guarded Log.d of per-event delta + running total (step-1 instrumentation
    for cause #3). `SWIPE_UP_THRESHOLD_PX = 80f` is UNCHANGED. Keep the existing pointerInput keys
    and suppressSwipe gating exactly as found. NOTE on the doc's "requireUnconsumed" sub-item:
    detectVerticalDragGestures owns its down-handling internally; rewriting the detector to change
    down-consumption is out of overnight scope — accumulation alone closes the proven defect.
    Record that scoping note in the SUMMARY.

    Indication immediacy (§R10 step 4): in ListRow, create
    `val interactionSource = remember { MutableInteractionSource() }` and pass it with
    `indication = LocalIndication.current` to the existing `clickable` call. Apply the same
    explicit-interactionSource treatment to OutlinedControl's clickable/combinedClickable
    construction (it gained `enabled` in Task 1 — touch the same clickMod site once, coordinated).
    No visual redesign — same indication, delivered immediately.
  </action>
  <verify>
    <automated>timeout 900 /mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --tests *SwipeUpAccumulatorTest* --no-daemon" | tr -d '\r' | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - SwipeUpAccumulator.kt exists with zero androidx/compose imports; all 4 behavior tests pass
    - AppShell.kt: the per-event `dragAmount < -SWIPE_UP_THRESHOLD_PX` comparison is GONE; accumulator wired via onDragStart/onVerticalDrag; `SWIPE_UP_THRESHOLD_PX = 80f` literal unchanged
    - ListRow.kt contains `MutableInteractionSource` and passes `interactionSource` + `indication` to clickable
    - Swipe debug logging present and BuildConfig.DEBUG-guarded
  </acceptance_criteria>
  <done>Slow multi-event swipes open the drawer (unit-proven), kit rows indicate immediately, threshold untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Build gate</name>
  <files>(none — verification only)</files>
  <read_first>
    - CLAUDE.md "Local Build Environment"
  </read_first>
  <action>
    Run `timeout 1200 /mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest :app:assembleDebug --no-daemon" | tr -d '\r'`.
    Exit code authoritative. Full unit suite (not just the new tests) — the dispatcher and kit
    components have wide existing coverage that must stay green. 3 strikes / ~90 min → revert,
    mark failed, move on (fail-forward).
  </action>
  <verify>
    <automated>timeout 1200 /mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest :app:assembleDebug --no-daemon" | tr -d '\r' | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - Combined invocation exits 0
    - `git status --porcelain` clean after final commit
  </acceptance_criteria>
  <done>Full host suite + debug assemble green at HEAD.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 4: Morning flox UAT — tap torture + drawer swipe (roadmap §R10 acceptance)</name>
  <what-built>True disablement, rejection flash, accumulated swipe, fast indication — all code-landed and host-tested; on-device behavior unverified.</what-built>
  <how-to-verify>
    Deferred to morning UAT checklist — verifier records human_needed; do NOT block the wave on this.
    On flox (debug build so the BuildConfig.DEBUG instrumentation is live; force-rebuild + check
    APK mtime per [[dinghy-stale-apk-uat-gate]]):
    1. 20-tap torture per control class (stepper, ListRow, foot button, scrubber, drawer tile):
       ≥95% registered with visible same-frame feedback.
    2. Rapid-tap a stepper during a busy window: see the one-shot rejection flash (not nothing).
    3. Disabled (dimmed) stepper: tap produces NO ripple at all.
    4. Drawer: slow deliberate swipe-up opens it reliably; fast flick still works.
    5. Optional: run FineTuneNavTest on-device — the accumulation fix should let swipeUp() open the drawer.
    6. Logcat tags Dispatcher/SwipeDetector show the instrumentation counts for any remaining misses.
  </how-to-verify>
  <action>Morning-only on-device verification: do nothing tonight beyond ensuring the items appear on the OVERNIGHT-REPORT.md morning UAT checklist. Verifier records human_needed; the wave proceeds.</action>
  <verify>
    <human-check>Owner runs the flox tap-torture/drawer-swipe protocol above on a fresh debug build ([[dinghy-stale-apk-uat-gate]]).</human-check>
  </verify>
  <done>20-tap torture >=95% visible feedback per control class; rejection flash visible; disabled steppers ripple-free; drawer opens on slow swipe — owner-approved on flox.</done>

  <resume-signal>Type "approved" or describe which control class still misses taps</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI taps → CommandDispatcher → printer | Rejection feedback must not weaken the guards that protect the printer/SBC |
| debug logs → logcat | Instrumentation emits dispatch keys |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26.5-07 | Denial of Service (printer) | CommandDispatcher guards | mitigate | Guards untouched (protected surface); rejectedKey is emit-only; regression tests assert accepted-path behavior unchanged |
| T-26.5-08 | Tampering | rejectedKey SharedFlow consumers | accept | Event carries only the dispatch key string; consumers render UI, never re-dispatch |
| T-26.5-09 | Information Disclosure | BuildConfig.DEBUG logs | accept | Keys only, no params/secrets; stripped from release by the guard + R8 |
| T-26.5-SC | Tampering | package installs | accept | None this plan |
</threat_model>

<verification>
- New dispatcher rejection tests + accumulator tests green; full suite green
- Grep proofs: no guarded-onClick pattern left in AdjusterPanel; threshold literal unchanged; debounceMs unchanged
- assembleDebug green
</verification>

<success_criteria>
Roadmap §R10 acceptance code-half: silent drops killed (true disablement + visible rejection),
swipe accumulates (unit-proven against the FineTuneNavTest profile), indication immediate.
Device-half (20-tap torture ≥95%, on-device FineTuneNavTest) → morning UAT checklist.
</success_criteria>

<output>
Create `.planning/phases/26.5-overnight-hardening-slate-audit-r-packages/26.5-03-SUMMARY.md` when done.
Include a `## Report Notes` section: instrumentation log tags + how to read them during morning UAT.
</output>
