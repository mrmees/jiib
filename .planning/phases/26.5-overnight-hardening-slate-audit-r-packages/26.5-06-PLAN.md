---
phase: 26.5-overnight-hardening-slate-audit-r-packages
plan: 06
type: execute
wave: 4
depends_on: [26.5-04, 26.5-05]
files_modified:
  - app/src/main/java/works/mees/dinghy/command/CommandMap.kt
  - app/src/main/java/works/mees/dinghy/command/PrinterCommands.kt
  - app/src/main/java/works/mees/dinghy/command/CommandRegistry.kt
  - app/src/main/java/works/mees/dinghy/ui/extrude/ExtrudeHolder.kt
  - app/src/test/java/works/mees/dinghy/command/CommandMapTest.kt
  - app/src/test/java/works/mees/dinghy/ui/extrude/ExtrudeHolderTest.kt
autonomous: true
requirements:
  - "R4 — docs/top-down-audit-roadmap.md §R4 + Part 3 (the literal design spec; README section deferred to R5b — the KDoc header carries the doc burden tonight)"
must_haves:
  truths:
    - "Changing one constant in CommandMap.kt makes a renamed load/unload-filament macro both GATE (capability check) and FIRE (gcode emission) correctly with no other edits — proven by tests"
    - "ALL SIX production hardcode sites read from CommandMap (RESEARCH corrected the audit's four): PrinterCommands.kt:330,333 + CommandRegistry.kt:491,498 + ExtrudeHolder.kt:106,107; preview fixtures stay literal"
    - "M112/emergency_stop is untouched — safety commands are excluded from the map by design (Part 3 explicit non-goals)"
    - "CommandCatalogDriftTest stays green — R4 rewires EXISTING specs, adds no CommandSpec, catalogIds unchanged ([[dinghy-command-catalog-drift]])"
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/command/CommandMap.kt"
      provides: "THE fork-and-edit customization point, Part 3 design verbatim with full KDoc"
      contains: "object CommandMap"
    - path: "app/src/test/java/works/mees/dinghy/command/CommandMapTest.kt"
      provides: "Default-slot, custom-gcode independence, renamed-slot mechanics tests"
  key_links:
    - from: "app/src/main/java/works/mees/dinghy/command/PrinterCommands.kt"
      to: "CommandMap"
      via: "loadFilament()/unloadFilament() return CommandMap.*.gcode"
      pattern: "CommandMap\\.(load|unload)Filament\\.gcode"
    - from: "app/src/main/java/works/mees/dinghy/ui/extrude/ExtrudeHolder.kt"
      to: "CommandMap"
      via: "hasMacroIgnoreCase(CommandMap.*.macro) capability gating"
      pattern: "CommandMap\\.(load|unload)Filament\\.macro"
---

<objective>
R4 build-time command map (docs/top-down-audit-roadmap.md §R4; Part 3 is the LITERAL design
spec): create `command/CommandMap.kt` verbatim per Part 3, rewire all six production hardcode
sites, and prove with unit tests that a renamed slot both gates and emits. Build-time-only by
locked decision — no runtime remap UI, no safety-command remapping (M112/emergency_stop
absolute), no per-command UI. The README "Customizing for your printer" section is R5b-deferred;
CommandMap's KDoc header carries the documentation burden tonight.

Purpose: fork-and-edit customization ready for the eventual README — one file, one constant, rebuild.
Output: CommandMap.kt + 6 rewired sites + tests proving gate-and-emit propagation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/26.5-overnight-hardening-slate-audit-r-packages/26.5-CONTEXT.md
@docs/top-down-audit-roadmap.md
@.planning/phases/26.5-overnight-hardening-slate-audit-r-packages/26.5-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: CommandMap.kt (Part 3 verbatim) + rewire all six sites</name>
  <files>app/src/main/java/works/mees/dinghy/command/CommandMap.kt, app/src/main/java/works/mees/dinghy/command/PrinterCommands.kt, app/src/main/java/works/mees/dinghy/command/CommandRegistry.kt, app/src/main/java/works/mees/dinghy/ui/extrude/ExtrudeHolder.kt</files>
  <read_first>
    - docs/top-down-audit-roadmap.md Part 3 — the LITERAL design (object CommandMap, data class Slot(macro, gcode = macro), loadFilament/unloadFilament entries, commented forward slots, the full KDoc header text, explicit non-goals)
    - 26.5-PATTERNS.md "CommandMap.kt" section — the verbatim file body with package line + the six rewire-site target expressions
    - app/src/main/java/works/mees/dinghy/command/PrinterCommands.kt ~L330,333 — current literal returns
    - app/src/main/java/works/mees/dinghy/command/CommandRegistry.kt ~L491,498 — MacroPresent("LOAD_FILAMENT"/"UNLOAD_FILAMENT") availability predicates; confirm catalogId values (KGC-LOAD_FILAMENT/KGC-UNLOAD_FILAMENT) and do NOT touch them
    - app/src/main/java/works/mees/dinghy/ui/extrude/ExtrudeHolder.kt ~L106,107 — hasMacroIgnoreCase literals
  </read_first>
  <action>
    1. Create `command/CommandMap.kt` exactly per Part 3: package works.mees.dinghy.command;
       the full fork-and-edit KDoc header (it IS tonight's documentation deliverable — include
       the macro-vs-gcode explanation and the override examples from Part 3); `object CommandMap`
       with `data class Slot(val macro: String, val gcode: String = macro)`,
       `loadFilament = Slot("LOAD_FILAMENT")`, `unloadFilament = Slot("UNLOAD_FILAMENT")`, and
       the two COMMENTED forward slots (parkToolhead/purgeLine). Pure Kotlin, no imports.
       Add one KDoc line stating the explicit non-goal: safety commands (M112/emergency_stop)
       are NEVER mapped here (Part 3 non-goals).
    2. Rewire the SIX production sites (RESEARCH A7: six, not the audit's four — ExtrudeHolder
       has two more; record the count correction in the SUMMARY):
       - PrinterCommands.kt:330 → return `CommandMap.loadFilament.gcode`; :333 → `CommandMap.unloadFilament.gcode`
       - CommandRegistry.kt:491 → `MacroPresent(CommandMap.loadFilament.macro)`; :498 → `MacroPresent(CommandMap.unloadFilament.macro)`
       - ExtrudeHolder.kt:106 → `caps.hasMacroIgnoreCase(CommandMap.loadFilament.macro)`; :107 → `caps.hasMacroIgnoreCase(CommandMap.unloadFilament.macro)`
       (Line numbers are HEAD-verified anchors — re-locate by content if drifted.)
    3. Preview fixtures (MacrosPreviews.kt / ConsolePreviews.kt) keep their literal
       "LOAD_FILAMENT"/"UNLOAD_FILAMENT" strings — they are display data, not wiring (CONTEXT lock).
    4. Touch NOTHING in docs/commands/catalog.json or printer-matrix.json — no CommandSpec is
       added or renamed; the drift gate must stay green without data edits.
  </action>
  <verify>
    <automated>test -f app/src/main/java/works/mees/dinghy/command/CommandMap.kt && test "$(grep -rln '"LOAD_FILAMENT"\|"UNLOAD_FILAMENT"' app/src/main/java/works/mees/dinghy/ --include=*.kt | grep -cv 'CommandMap.kt\|preview')" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - CommandMap.kt exists; contains `data class Slot(val macro: String, val gcode: String = macro)`, both live slots, both commented forward slots, and the fork-and-edit KDoc
    - `grep -rn '"LOAD_FILAMENT"\|"UNLOAD_FILAMENT"' app/src/main/java --include=*.kt` hits ONLY CommandMap.kt and preview/* files — zero hits in PrinterCommands, CommandRegistry, ExtrudeHolder
    - CommandRegistry catalogId literals KGC-LOAD_FILAMENT / KGC-UNLOAD_FILAMENT are UNCHANGED in the diff
    - `grep -rn "emergency_stop\|M112" app/src/main/java/works/mees/dinghy/command/CommandMap.kt` shows only the non-goal comment (no mapping entry)
  </acceptance_criteria>
  <done>One file owns the remappable macro names; all six wiring sites read from it; safety commands and the catalog untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Renamed-slot gate-and-emit proof + drift-gate green + build gate</name>
  <files>app/src/test/java/works/mees/dinghy/command/CommandMapTest.kt, app/src/test/java/works/mees/dinghy/ui/extrude/ExtrudeHolderTest.kt</files>
  <read_first>
    - docs/top-down-audit-roadmap.md §R4 acceptance — "changing one constant... makes a renamed macro gate and fire correctly; test proves it"
    - 26.5-PATTERNS.md "CommandMapTest.kt" section — the three test shapes
    - app/src/test/java/works/mees/dinghy/ui/extrude/ExtrudeHolderTest.kt — existing capability-gating harness (how caps/hasMacroIgnoreCase fixtures are built)
    - app/src/test/java/works/mees/dinghy/command/CommandCatalogDriftTest.kt — what the D-10 gate asserts (run, don't modify)
  </read_first>
  <behavior>
    - Test 1 (CommandMapTest): default slots — loadFilament.macro == loadFilament.gcode == "LOAD_FILAMENT"; same for unload
    - Test 2 (CommandMapTest): Slot(macro="M701", gcode="M701 PURGE=1") keeps macro and gcode independent (the override use case)
    - Test 3 (ExtrudeHolderTest additions, the PROPAGATION proof): capabilities containing CommandMap.loadFilament.macro (referenced symbolically, NOT a re-hardcoded literal) → load gate true; capabilities containing a DIFFERENT name → gate false; PrinterCommands.loadFilament() == CommandMap.loadFilament.gcode — together proving a one-constant rename would both gate and emit through the real wiring
    - Test 4: CommandCatalogDriftTest passes unmodified (Pitfall 4)
  </behavior>
  <action>
    Write CommandMapTest.kt (command package, plain JUnit4, CommandDispatcherTest's file
    conventions) per the behavior table. Extend ExtrudeHolderTest with the symbolic-reference
    gating cases — the key discipline: the new assertions reference `CommandMap.loadFilament.macro`
    / `.gcode`, never the string literal, so the tests stay correct under any future rename
    (that's what makes them a propagation proof rather than a tautology).
    Then run the full build gate (mandatory last automated task):
    `timeout 1200 /mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest :app:assembleDebug --no-daemon" | tr -d '\r'`
    — full suite includes CommandCatalogDriftTest (must be green WITHOUT catalog.json edits) and
    every pre-existing macro/extrude test. Exit code authoritative. 3 strikes / ~90 min → revert,
    mark failed, move on.
  </action>
  <verify>
    <automated>timeout 1200 /mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest :app:assembleDebug --no-daemon" | tr -d '\r' | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - CommandMapTest exists with the default-slot + independence cases passing
    - ExtrudeHolderTest's new cases reference CommandMap symbolically (grep the test diff: zero new "LOAD_FILAMENT" string literals) and pass
    - CommandCatalogDriftTest passes with ZERO changes to catalog.json / printer-matrix.json / the test itself
    - Full suite + assembleDebug exit 0; `git status --porcelain` clean after final commit
  </acceptance_criteria>
  <done>Roadmap §R4 acceptance proven host-side: one-constant rename gates and fires through the real wiring; drift gate green untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CommandMap constants → printer gcode | Mapped names are sent to the printer verbatim via gcode.script |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26.5-16 | Tampering (command injection) | renamed-slot gcode emission | mitigate | Build-time constants only — no runtime input reaches the map (no remap UI by locked decision); the existing MacroPresent/hasMacroIgnoreCase allowlist gating still guards execution (button absent unless the macro exists on the printer) |
| T-26.5-17 | Elevation of Privilege | safety commands | mitigate | M112/emergency_stop explicitly excluded from the map (Part 3 non-goal, asserted in acceptance criteria) |
| T-26.5-18 | Repudiation | catalog drift | mitigate | CommandCatalogDriftTest (D-10) runs unmodified and green — spec inventory provably unchanged |
| T-26.5-SC | Tampering | package installs | accept | None this plan |
</threat_model>

<verification>
- Grep proof: literals exist only in CommandMap.kt + previews
- New tests symbolic, passing; drift test green unmodified
- Build gate exit 0
</verification>

<success_criteria>
Roadmap §R4 acceptance: changing one constant in one file makes a renamed load-filament macro
gate and fire correctly with no other edits — the test proves it. KDoc carries the doc burden;
README section deferred to R5b.
</success_criteria>

<output>
Create `.planning/phases/26.5-overnight-hardening-slate-audit-r-packages/26.5-06-SUMMARY.md` when done.
Include a `## Report Notes` section: the 4-vs-6 site-count correction for the audit doc.
</output>
