---
phase: 26.5-overnight-hardening-slate-audit-r-packages
plan: 02
type: execute
wave: 1
depends_on: [26.5-01]
files_modified:
  - CLAUDE.md
  - .planning/research/STACK.md
  - .planning/research/ARCHITECTURE.md
  - .planning/research/FEATURES.md
  - .planning/research/PITFALLS.md
  - .planning/research/SUMMARY.md
  - .planning/codebase/STACK.md
  - .planning/codebase/CONCERNS.md
  - .planning/codebase/UI-REVIEW.md
  - .planning/codebase/ARCHITECTURE.md
  - .planning/codebase/CONVENTIONS.md
  - .planning/codebase/INTEGRATIONS.md
  - .planning/codebase/STRUCTURE.md
  - .planning/codebase/TESTING.md
  - .planning/PROJECT.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - docs/request-cadence-contract.md
  - .planning/phases/07-files-print-control-core-print-loop-gate/07-UI-SPEC.md
  - .planning/phases/08-macros-console-functional-core-complete/08-UI-SPEC.md
  - .planning/phases/09-calibration-maintenance/09-UI-SPEC.md
  - .planning/phases/12-macro-prompt-protocol/12-UI-SPEC.md
  - .planning/phases/14-multi-printer-switching/14-UI-SPEC.md
  - .planning/phases/15-theme-system-settings-redesign/15-UI-SPEC.md
  - .planning/phases/16-home-print-status-redesign/16-UI-SPEC.md
  - .planning/todos/pending/
  - .planning/todos/completed/
  - .github/workflows/ci.yml
  - .planning/phases/26.5-overnight-hardening-slate-audit-r-packages/26.5-r9-archive-script.sh
autonomous: true
requirements:
  - "R9 steps 1-3+5 — docs/top-down-audit-roadmap.md §R9 (step 4 emitted as a reviewed script, NOT executed)"
must_haves:
  truths:
    - "Zero contradictions among active memory files (no 'compileSdk 35', no 'Architecture not yet mapped' in the active context set)"
    - "Every active memory file carries a current 'Last verified' header; historical files are stamped historical"
    - "UI-SPECs 07/08/09/12/14/15/16 carry status: superseded markers (matching 04's existing form)"
    - "The 4 stale todos are archived; remaining pending todos carry a 2026-06-11 triage annotation"
    - "The step-4 bulk-archive script EXISTS as a reviewed artifact and was NOT executed (no files moved/deleted by it)"
    - "CI gains a hygiene job scoped ONLY to active context files — never .planning/phases/"
  artifacts:
    - path: ".planning/phases/26.5-overnight-hardening-slate-audit-r-packages/26.5-r9-archive-script.sh"
      provides: "Commented, protected-list-guarded bulk-archive script for owner review (NOT run)"
    - path: ".github/workflows/ci.yml"
      provides: "hygiene job: stale-phrase grep + internal-link check, active set only"
      contains: "hygiene"
    - path: "docs/request-cadence-contract.md"
      provides: "Spoolman-notify row added to the call inventory"
  key_links:
    - from: "CLAUDE.md"
      to: ".planning/codebase/ARCHITECTURE.md"
      via: "Architecture section pointer replacing 'not yet mapped'"
      pattern: "ARCHITECTURE.md"
---

<objective>
R9 memory/docs delint, steps 1-3 + 5 (docs/top-down-audit-roadmap.md §R9): correctness edits so
the memory stops lying to the sessions that consume it, todo re-triage, single-sourcing +
metadata headers, and a scoped hygiene CI guardrail. Step 4 (bulk archive) is emitted as a
REVIEWED SCRIPT artifact only — never executed tonight (CONTEXT locked). Runs in wave 1, before
any code package consumes the memory.

Purpose: the memory is true before R10/R1/R2/R4/R7 sessions read it (roadmap Part 4 ordering).
Output: corrected active docs, supersession markers, triaged todos, archive script, hygiene CI job.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/26.5-overnight-hardening-slate-audit-r-packages/26.5-CONTEXT.md
@docs/top-down-audit-roadmap.md
@.planning/phases/26.5-overnight-hardening-slate-audit-r-packages/26.5-RESEARCH.md
</context>

<tasks>

<!-- Plan-checker advisory (scope_sanity): ~25 of the 30 files_modified are mechanical
     metadata/header/supersession stamps. Batch them — do the 7 UI-SPEC front-matter edits as one
     pass and the ~13 Last-verified header stamps as one pass; do NOT deep-read each stamped file.
     Reserve real attention for the substantive correctness edits. -->

<task type="auto">
  <name>Task 1: Step-1 correctness edits (the docs that lie)</name>
  <files>CLAUDE.md, .planning/research/STACK.md, .planning/codebase/STACK.md, .planning/codebase/CONCERNS.md, .planning/codebase/UI-REVIEW.md, docs/request-cadence-contract.md, the 7 UI-SPEC files listed in frontmatter</files>
  <read_first>
    - docs/top-down-audit-roadmap.md §R9 step 1 — the authoritative fix list (items struck through there are already DONE on master; skip them)
    - 26.5-RESEARCH.md "R9 Step 1/2" code examples — exact replacement wording
    - .planning/phases/04-service-shell-settings-print-status-home/04-UI-SPEC.md — the EXISTING supersession front-matter form to replicate
    - docs/commands/moonraker-api.md (~lines 166-188) — the Spoolman notify entries the cadence-contract row must be consistent with
    - docs/request-cadence-contract.md — the call-inventory table missing the Spoolman row
  </read_first>
  <action>
    Per roadmap §R9 step 1, current state re-verified at HEAD 2026-06-11:
    1. `.planning/research/STACK.md`: sweep all 4 remaining `compileSdk 35` occurrences → 36
       (grep-verified count; sweep whatever the live count is).
    2. `.planning/codebase/STACK.md`: 1 remaining `compileSdk 35` occurrence → 36.
    3. Root `CLAUDE.md`: the What-NOT-to-Use compileSdk-35 phrase is ALREADY fixed (grep = 0 —
       record as pre-done in SUMMARY). Still to do: (a) replace the Architecture section body
       "Architecture not yet mapped. Follow existing patterns found in the codebase." with a
       pointer to `.planning/codebase/ARCHITECTURE.md` + `docs/adr/0001-ui-toolkit-decision.md`;
       (b) soften the Baseline-Profile "NO-OP" framing in the stack table per §R9 step 1: true
       only for stock API 23 — flox runs LineageOS 18.1 / API 30 where profiles DO apply (note it
       raises the `macrobenchmark-module-wiring` todo's priority).
    4. UI-SPECs 07, 08, 09, 12, 14, 15, 16: add the supersession front-matter matching 04's
       existing form (`status: superseded`, `superseded_by: docs/ui_design/`, dated 2026-06-11).
       Do not edit their bodies.
    5. `docs/request-cadence-contract.md`: add the missing Spoolman-notify row to the call
       inventory, consistent with the entries already in docs/commands/moonraker-api.md.
    6. Stamp `.planning/codebase/CONCERNS.md` + `.planning/codebase/UI-REVIEW.md` at the very top:
       `**Status:** historical — findings resolved by Phases 22–26 (stamped 2026-06-11)`.
       Bodies unchanged (CONCERNS.md has already misled a session into reporting the @Immutable
       migration as un-landed — the stamp is the fix, per §R9 step 1).
    PROTECTED: do not touch anything under .planning/phases/ except the 7 UI-SPEC front-matters.
  </action>
  <verify>
    <automated>test "$(grep -rc 'compileSdk 35' .planning/research/STACK.md .planning/codebase/STACK.md CLAUDE.md | awk -F: '{s+=$2} END {print s}')" = "0" && test "$(grep -lc 'status: superseded' .planning/phases/0[789]-*/0?-UI-SPEC.md .planning/phases/1[2456]-*/1?-UI-SPEC.md | wc -l)" -ge 7 && head -5 .planning/codebase/CONCERNS.md | grep -qi historical && grep -qi spoolman docs/request-cadence-contract.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "compileSdk 35" CLAUDE.md .planning/research/STACK.md .planning/codebase/STACK.md` returns 0 hits
    - `grep -c "Architecture not yet mapped" CLAUDE.md` returns 0; the Architecture section names `.planning/codebase/ARCHITECTURE.md` and `docs/adr/0001-ui-toolkit-decision.md`
    - CLAUDE.md Baseline-Profile entry mentions LineageOS/API 30 applicability (no longer a flat NO-OP claim)
    - All 7 UI-SPEC files (07/08/09/12/14/15/16) contain `status: superseded` AND `superseded_by: docs/ui_design/`
    - First 5 lines of CONCERNS.md and UI-REVIEW.md each contain "historical"
    - docs/request-cadence-contract.md call inventory contains a Spoolman row
  </acceptance_criteria>
  <done>Every §R9-step-1 lie is corrected or stamped; struck-through (already-done) items recorded as pre-done in SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 2: Todo re-triage + single-sourcing + metadata headers (steps 2-3)</name>
  <files>.planning/todos/pending/*, .planning/todos/completed/*, .planning/PROJECT.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md, CLAUDE.md, .planning/research/*.md, .planning/codebase/*.md</files>
  <read_first>
    - docs/top-down-audit-roadmap.md §R9 steps 2-3 — archive list, ownership map, header format
    - 26.5-RESEARCH.md "R9 Step 3 — Active Memory File Header Pattern" — exact header shape
    - .planning/todos/pending/ directory listing — current 17 files
    - .planning/PROJECT.md — locate the duplicated Constraints + Core-Value sections before deleting
    - .planning/STATE.md first 20 lines — YAML frontmatter must stay intact; header goes BELOW it
  </read_first>
  <action>
    Step 2 (todo re-triage):
    1. `git mv` these 4 stale todos from `.planning/todos/pending/` to `.planning/todos/completed/`
       (the repo's existing archive convention), appending a one-line closing note to each:
       `status-progress-ring-dual-source-jump.md`, `console-macro-page-ux-flow.md`,
       `benchmark-harness-fairness-fixes.md`, `2026-06-06-shellpresencetest-device-determinism.md`
       (the last one's note: "superseded — the per-event swipe threshold defect is fixed by R10
       step 3 in plan 26.5-03; FineTuneNavTest re-run lands on the morning UAT checklist").
    2. Re-triage every remaining pending todo against the Phase-27/28/29 map (roadmap §R9 step 2):
       append one line to each: `Triage 2026-06-11: target Phase 27|28|29|standing` with a
       half-line rationale. Judgment call per file content (Claude's discretion per RESEARCH).
    Step 3 (single-sourcing + headers):
    3. `.planning/PROJECT.md`: delete the duplicated Constraints and Core-Value section BODIES,
       replacing each with a single pointer line per the ownership map (CLAUDE.md owns
       stack+constraints; REQUIREMENTS.md owns core value; STATE.md owns status; ROADMAP.md owns
       phases). Keep the file structurally valid (it is @-referenced by GSD plans).
    4. Add the header `**Last verified:** 2026-06-11` + `**Status:** active` near the top of:
       root CLAUDE.md, .planning/PROJECT.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md,
       .planning/STATE.md (BELOW its YAML frontmatter), and the 6 non-historical
       .planning/codebase/ files (ARCHITECTURE, CONVENTIONS, INTEGRATIONS, STACK, STRUCTURE,
       TESTING).
    5. Stamp all 5 `.planning/research/*.md` with `**Status:** historical (research snapshot;
       see CLAUDE.md for current stack)` — content otherwise UNEDITED except STACK.md's step-1
       sweep already done in Task 1.
  </action>
  <verify>
    <automated>test ! -f .planning/todos/pending/status-progress-ring-dual-source-jump.md && test -f .planning/todos/completed/status-progress-ring-dual-source-jump.md && grep -q "Last verified" CLAUDE.md && grep -q "Last verified" .planning/ROADMAP.md && head -3 .planning/STATE.md | grep -q "^---" && grep -qi "historical" .planning/research/SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - The 4 named todo files exist in `.planning/todos/completed/` and NOT in `pending/`; each ends with a closing note
    - Every file remaining in `.planning/todos/pending/` contains the literal string `Triage 2026-06-11:`
    - PROJECT.md no longer duplicates the Constraints/Core-Value bodies (pointer lines instead); file still parses as markdown with its title intact
    - `grep -l "Last verified" CLAUDE.md .planning/PROJECT.md .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/STATE.md` lists all 5
    - STATE.md line 1 is still `---` (frontmatter intact)
    - All 5 .planning/research/*.md files contain `Status:` + `historical`
  </acceptance_criteria>
  <done>One owner per fact, headers on every active memory file, stale todos archived, survivors triaged.</done>
</task>

<task type="auto">
  <name>Task 3: Step-4 archive script (NOT run) + step-5 hygiene CI + build gate</name>
  <files>.planning/phases/26.5-overnight-hardening-slate-audit-r-packages/26.5-r9-archive-script.sh, .github/workflows/ci.yml</files>
  <read_first>
    - docs/top-down-audit-roadmap.md §R9 step 4 — the protected list (NEVER move/delete: phases/01+13 captures, 15.2-AUDIT.md, PATTERNS 02/03/18, docs/commands/spoolman-live-*.json + e3/e5 .jsonl, all PLAN/SUMMARY pairs) and step 5 — hygiene scope rules
    - 26.5-RESEARCH.md "R9 Step 5 — Hygiene CI Step" + assumption A8 (markdown-link-check action UNVERIFIED — do not use it)
    - .github/workflows/ci.yml as committed by plan 26.5-01
  </read_first>
  <action>
    1. Write `26.5-r9-archive-script.sh` in the phase dir: bash, `set -euo pipefail`, heavily
       commented, header block stating "REVIEW BEFORE RUNNING — emitted by plan 26.5-02, NOT
       executed overnight". Body per §R9 step 4: delete the ~26 *DISCUSSION-LOG* files under
       .planning/phases/ (each self-declares audit-trail-only); `git mv` shipped-phase
       CONTEXT/VALIDATION/VERIFICATION/REVIEW files to `.planning/archive/<phase>/`, keeping
       PLAN/SUMMARY pairs in place. Implement the protected list as an explicit bash array +
       a guard function every mv/rm passes through (skip + warn on match); include the
       15.2-AUDIT.md link-update reminder from the doc as a comment. Do NOT chmod +x; do NOT
       execute it. Reference its path from the SUMMARY Report Notes.
    2. Append a `hygiene` job to `.github/workflows/ci.yml` (checkout only, no JDK): 
       (a) stale-phrase grep for `compileSdk 35` and `Architecture not yet mapped` over the
       ACTIVE SET ONLY: `CLAUDE.md`, `.planning/PROJECT.md`, `.planning/REQUIREMENTS.md`,
       `.planning/ROADMAP.md`, `.planning/STATE.md`, and `docs/` with
       `--exclude=top-down-audit-roadmap.md` — the audit doc QUOTES the stale phrases as work
       items and would self-trip the gate; NEVER scan `.planning/phases/` (historical vocabulary
       is correct there, §R9 step 5). Grep finding matches = FAIL exit 1; no matches = OK.
       (b) dead-internal-link check: a self-contained bash loop extracting relative `.md` link
       targets from the same active set and `test -e`-ing each — deliberately NOT the
       `markdown-link-check` marketplace action (RESEARCH A8: unverifiable tonight; the do-NOT-do
       list prefers deterministic gates). Record this substitution in the SUMMARY.
    3. Run the hygiene checks LOCALLY (same grep + link loop) — must pass now that Tasks 1-2 landed.
    4. Build gate (mandatory every plan; docs-only plan still proves HEAD healthy):
       `timeout 900 /mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest :app:assembleDebug --no-daemon" | tr -d '\r'` — exit 0.
  </action>
  <verify>
    <automated>test -f .planning/phases/26.5-overnight-hardening-slate-audit-r-packages/26.5-r9-archive-script.sh && test ! -x .planning/phases/26.5-overnight-hardening-slate-audit-r-packages/26.5-r9-archive-script.sh && grep -q "hygiene" .github/workflows/ci.yml && ! grep -rn "compileSdk 35\|Architecture not yet mapped" CLAUDE.md .planning/PROJECT.md .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/STATE.md && timeout 900 /mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest :app:assembleDebug --no-daemon" | tr -d '\r' | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - Script exists, mode is NOT executable (`test ! -x` passes), contains `set -euo pipefail`, an explicit protected-list array naming phases/01 + phases/13 captures, 15.2-AUDIT.md, PATTERNS 02/03/18, spoolman-live fixtures, and a guard function; `git log --diff-filter=D` shows zero deletions from this plan (script not run)
    - ci.yml contains a `hygiene:` job whose grep line excludes `top-down-audit-roadmap.md` and never references `.planning/phases`
    - The local stale-phrase grep over the active set returns 0 hits (grep exits 1 → gate OK)
    - Build gate exits 0
  </acceptance_criteria>
  <done>Reviewed archive script exists un-run, hygiene CI lands scoped, local hygiene pass proves the delint took.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs → future CLI sessions | Memory files are executable context — wrong facts become wrong code |
| archive script → repo history | A bulk-deletion script is a tampering hazard if run unreviewed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26.5-04 | Tampering | 26.5-r9-archive-script.sh | mitigate | NOT executed; not executable; set -euo pipefail; protected-list guard array; owner review gate before any run |
| T-26.5-05 | Repudiation/Integrity | active memory files | mitigate | Last-verified headers + hygiene CI grep make future drift detectable |
| T-26.5-06 | Denial of Service (CI) | hygiene job false positives | mitigate | Scope excludes .planning/phases/ and the audit doc itself; deterministic grep, no third-party action (A8) |
| T-26.5-SC | Tampering | package installs | accept | None this plan (docs + YAML only) |
</threat_model>

<verification>
- Stale-phrase grep over the active set: 0 hits
- 7 UI-SPEC supersession markers, 2 historical stamps, 5 research historical stamps, headers on all active files
- 4 todos archived, survivors triaged
- Script artifact present, un-run, guarded
- hygiene job in ci.yml, correctly scoped
- Build gate green
</verification>

<success_criteria>
Roadmap §R9 acceptance, overnight-scoped: zero contradictions among active memory files; every
active file carries a current Last-verified header; 4 stale todos archived; hygiene check
implemented (CI-green confirmed with plan 26.5-01's morning checkpoint); step-4 script written
but NOT run.
</success_criteria>

<output>
Create `.planning/phases/26.5-overnight-hardening-slate-audit-r-packages/26.5-02-SUMMARY.md` when done.
Include a `## Report Notes` section: archive-script pointer + any DECISIONS-NEEDED discovered during triage.
</output>
