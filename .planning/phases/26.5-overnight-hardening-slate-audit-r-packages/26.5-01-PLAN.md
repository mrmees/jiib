---
phase: 26.5-overnight-hardening-slate-audit-r-packages
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - gradlew
  - .github/workflows/ci.yml
  - app/build.gradle.kts
  - gradle.properties
  - settings.gradle.kts
  - app/lint-baseline.xml
  - app/src/main/java/works/mees/dinghy/DinghyApp.kt
  - .planning/phases/26.5-overnight-hardening-slate-audit-r-packages/OVERNIGHT-REPORT.md
autonomous: true
requirements:
  - "R5a — docs/top-down-audit-roadmap.md §R5a (guardrail infra, MINUS the LICENSE file)"
must_haves:
  truths:
    - "A fresh clone can run ./gradlew on Linux (gradlew committed with the executable bit)"
    - "Every push to master or gsd/** runs unit tests + assembleDebug in GitHub Actions CI"
    - "Lint is baselined and ENFORCED (abortOnError=true) — any NEW violation fails the build; the baseline may only shrink"
    - "Debug builds run StrictMode detect-all with penaltyLog (release builds unaffected)"
    - "NO LICENSE file exists — the recommendation lives in OVERNIGHT-REPORT.md (owner decision)"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "CI workflow: ubuntu-latest, temurin JDK 17, testDebugUnitTest + assembleDebug"
    - path: "app/lint-baseline.xml"
      provides: "Generated lint baseline (NOT hand-authored)"
    - path: "app/src/main/java/works/mees/dinghy/DinghyApp.kt"
      provides: "StrictMode thread+VM policy in onCreate, BuildConfig.DEBUG-guarded"
      contains: "StrictMode"
    - path: ".planning/phases/26.5-overnight-hardening-slate-audit-r-packages/OVERNIGHT-REPORT.md"
      provides: "Report skeleton: per-package status, DECISIONS-NEEDED (LICENSE rec), morning UAT checklist"
  key_links:
    - from: "app/build.gradle.kts"
      to: "app/lint-baseline.xml"
      via: "lint { baseline = file(\"lint-baseline.xml\") }"
      pattern: "baseline = file"
    - from: "gradle.properties"
      to: "lint JVM selection"
      via: "android.experimental.runLintInProcess=false + kotlin jvmToolchain(17)"
      pattern: "runLintInProcess"
---

<objective>
R5a guardrail infra (docs/top-down-audit-roadmap.md §R5a steps 1, 3, 4, 5 — step 2 LICENSE is
owner-decision, recommend-only): gradlew +x, GitHub Actions CI, lint baselined under a JDK-17
toolchain then enforced shrink-only, debug StrictMode. Runs FIRST (wave 0) so the lint baseline
is minimal before any other package lands code. Also creates the OVERNIGHT-REPORT.md skeleton
the rest of the night appends to (via SUMMARYs; plan 26.5-07 finalizes it).

Purpose: guardrails exist before any code lands (roadmap Part 4 ordering logic).
Output: ci.yml, enforced lint baseline, StrictMode, executable gradlew, report skeleton.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/26.5-overnight-hardening-slate-audit-r-packages/26.5-CONTEXT.md
@docs/top-down-audit-roadmap.md
@.planning/phases/26.5-overnight-hardening-slate-audit-r-packages/26.5-RESEARCH.md
@.planning/phases/26.5-overnight-hardening-slate-audit-r-packages/26.5-PATTERNS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: gradlew +x, CI workflow, debug StrictMode, OVERNIGHT-REPORT skeleton</name>
  <files>gradlew, .github/workflows/ci.yml, app/src/main/java/works/mees/dinghy/DinghyApp.kt, .planning/phases/26.5-overnight-hardening-slate-audit-r-packages/OVERNIGHT-REPORT.md</files>
  <read_first>
    - docs/top-down-audit-roadmap.md §R5a (lines 251-265) — the requirement source
    - .planning/phases/26.5-overnight-hardening-slate-audit-r-packages/26.5-PATTERNS.md — "ci.yml" + "DinghyApp.kt (StrictMode)" sections (exact YAML and Kotlin shapes)
    - app/src/main/java/works/mees/dinghy/DinghyApp.kt — current onCreate end (insert point is after container.seedTheme)
    - .planning/phases/26.5-overnight-hardening-slate-audit-r-packages/26.5-CONTEXT.md §decisions — LICENSE = recommend-only; morning-UAT-checklist convention
  </read_first>
  <action>
    1. Executable bit (roadmap §R5a step 1): run `git update-index --chmod=+x gradlew` with Linux
       git from the repo root (index ops work on this drvfs repo; only push needs Windows git.exe).
    2. Create `.github/workflows/ci.yml` per the PATTERNS shape: name `CI`; trigger on push to
       branches `[master, "gsd/**"]` and pull_request to master; single `build` job on
       `ubuntu-latest` with steps `actions/checkout@v4`, `actions/setup-java@v4`
       (distribution temurin, java-version 17), `gradle/actions/setup-gradle@v4`, then
       `./gradlew :app:testDebugUnitTest :app:assembleDebug --no-daemon`. These are first-party
       GitHub/Gradle actions (RESEARCH A2/A3 — if CI fails on the runner it is a morning item, not
       a tonight blocker; local gates below are authoritative tonight).
    3. StrictMode (roadmap §R5a step 5): at the END of DinghyApp.onCreate, inside an
       `if (BuildConfig.DEBUG)` guard, set `StrictMode.setThreadPolicy` and `setVmPolicy` each
       with `.detectAll().penaltyLog()` — penaltyLog NOT penaltyDeath (violations surface in
       logcat without crashing). Import `android.os.StrictMode` and `works.mees.dinghy.BuildConfig`
       (or use fully-qualified names per the PATTERNS note).
    4. Do NOT create a LICENSE file (owner decision, CONTEXT §decisions). Instead create
       `OVERNIGHT-REPORT.md` in the phase dir with these sections: `## Run Summary` (placeholder),
       `## Per-Package Status` (table R5a/R9/R10/R1/R2/R4/R7, all rows "pending"; plan 26.5-07
       finalizes from SUMMARYs), `## DECISIONS-NEEDED` containing the LICENSE recommendation —
       2-4 sentences comparing GPLv3 (Klipper-ecosystem norm; Klipper/Moonraker/Fluidd are GPL)
       vs MIT/Apache-2.0 (max adoption), with a recommendation and the explicit note that NO file
       was created pending the owner's call — `## R9 Archive Script` (placeholder pointing at
       `26.5-r9-archive-script.sh`, written by plan 26.5-02), and `## Morning UAT Checklist`
       pre-filled from the roadmap acceptance lines, ordered for ONE sitting per CONTEXT
       §specifics: flox group first (R10 20-tap torture per control class + drawer swipe +
       on-device FineTuneNavTest; R2 doze 20+min + keep-awake toggle; R1 Nexus-7-unchanged
       regression), then S25 Ultra group (R1 arm64 install + edge-to-edge both orientations +
       predictive back + POST_NOTIFICATIONS one-shot; R7 wss connect vs TLS-fronted Moonraker +
       cert-failure message), then a CI-green check for this plan.
  </action>
  <verify>
    <automated>git ls-files -s gradlew | grep -q "^100755" && test -f .github/workflows/ci.yml && grep -q "setup-java@v4" .github/workflows/ci.yml && grep -q "StrictMode" app/src/main/java/works/mees/dinghy/DinghyApp.kt && test ! -f LICENSE && test -f .planning/phases/26.5-overnight-hardening-slate-audit-r-packages/OVERNIGHT-REPORT.md</automated>
  </verify>
  <acceptance_criteria>
    - `git ls-files -s gradlew` prints mode `100755`
    - `.github/workflows/ci.yml` exists and contains all of: `actions/checkout@v4`, `actions/setup-java@v4`, `temurin`, `java-version: 17`, `gradle/actions/setup-gradle@v4`, `:app:testDebugUnitTest :app:assembleDebug`
    - DinghyApp.kt contains `StrictMode.setThreadPolicy` AND `StrictMode.setVmPolicy` AND `penaltyLog` inside a `BuildConfig.DEBUG` guard; zero occurrences of `penaltyDeath`
    - `test ! -f LICENSE` passes (no LICENSE file created)
    - OVERNIGHT-REPORT.md exists with the five named sections; the DECISIONS-NEEDED section mentions both GPLv3 and MIT/Apache-2.0
  </acceptance_criteria>
  <done>gradlew is executable in the index, CI workflow committed, StrictMode active in debug only, report skeleton with LICENSE recommendation and full morning UAT checklist exists.</done>
</task>

<task type="auto">
  <name>Task 2: Lint baseline under JDK-17, then enforce shrink-only</name>
  <files>app/build.gradle.kts, gradle.properties, settings.gradle.kts, app/lint-baseline.xml</files>
  <read_first>
    - docs/top-down-audit-roadmap.md §R5a step 4 — baseline-before-enforce sequence + shrink-only house rule
    - app/build.gradle.kts — current lint block (~L91-94: checkReleaseBuilds=false, abortOnError=false, with the JDK-21 UAST-crash comment), compileOptions/kotlinOptions (~L96-103)
    - 26.5-PATTERNS.md "app/build.gradle.kts" section — lint/toolchain shapes; note PATTERNS misplaces `kotlin {}` INSIDE android{} — it is a TOP-LEVEL extension, see below
    - 26.5-RESEARCH.md "R5a: Lint Baseline" + Pitfall 8 (stale daemon) + Pitfall 2 (baseline growth)
  </read_first>
  <action>
    Sequence matters — baseline BEFORE enforcement (roadmap §R5a step 4):
    1. `gradle.properties`: add `android.experimental.runLintInProcess=false` (lint runs in its
       own worker JVM instead of the JDK-21 daemon — the CONTEXT-locked half of the JDK-17 fix).
    2. `app/build.gradle.kts`: add `kotlin { jvmToolchain(17) }` as a TOP-LEVEL block (the
       KotlinAndroidProjectExtension — NOT inside `android {}`; placing it inside android{} is a
       build error). Existing compileOptions/kotlinOptions already target 17 and stay.
    3. Edit the `lint {}` block: add `baseline = file("lint-baseline.xml")`; leave
       `abortOnError = false` for the moment. Keep/update the explanatory comment: the AGP-8.7
       UAST crash is JDK-21-specific; lint now runs under a JDK-17 toolchain.
    4. Kill stale daemons then generate the baseline:
       `/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat --stop"` then
       `/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:updateLintBaseline --no-daemon" | tr -d '\r'`.
       TOOLCHAIN FALLBACK: if Gradle reports no matching JDK-17 toolchain, add the first-party
       Gradle plugin `org.gradle.toolchains.foojay-resolver-convention` version `0.8.0` to
       `settings.gradle.kts` plugins block (auto-provisions the JDK) and retry. UAST-CRASH
       FALLBACK (RESEARCH A1): if lint still crashes under the 17 toolchain after 2 attempts,
       keep the baseline + `abortOnError=true` but set `checkReleaseBuilds=false`, note "lint
       enforced via CI (ubuntu/JDK17) only" in the lint comment, and record the deviation in the
       SUMMARY — do not burn the night on it (fail-forward).
    5. Flip enforcement: `checkReleaseBuilds = true`, `abortOnError = true`. Add the house-rule
       comment verbatim intent: "the baseline may only shrink, never grow — never re-run
       updateLintBaseline without reviewing what new violations it would absorb."
    6. Confirm green: `gw.bat :app:lintDebug --no-daemon` exits 0 with the baseline in place.
  </action>
  <verify>
    <automated>test -f app/lint-baseline.xml && grep -q "jvmToolchain(17)" app/build.gradle.kts && grep -q "abortOnError = true" app/build.gradle.kts && grep -q "android.experimental.runLintInProcess=false" gradle.properties && timeout 900 /mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:lintDebug --no-daemon" | tr -d '\r' | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `app/lint-baseline.xml` exists and is git-tracked (generated by updateLintBaseline, NOT hand-authored)
    - app/build.gradle.kts contains all of: `baseline = file("lint-baseline.xml")`, `checkReleaseBuilds = true`, `abortOnError = true`, top-level `kotlin { jvmToolchain(17) }`
    - gradle.properties contains `android.experimental.runLintInProcess=false`
    - `gw.bat :app:lintDebug --no-daemon` exits 0 (exit code authoritative)
  </acceptance_criteria>
  <done>Lint runs green under the JDK-17 toolchain with a committed baseline and abortOnError=true — any new violation now fails the build.</done>
</task>

<task type="auto">
  <name>Task 3: Build gate (R5a extended gate: release + minSdk verify)</name>
  <files>(none — verification only)</files>
  <read_first>
    - CLAUDE.md "Local Build Environment" — gw.bat invocation, `tr -d '\r'`, exit code authoritative
  </read_first>
  <action>
    Run the R5a extended build gate (CONTEXT §decisions: R5a and R1 additionally gate release):
    `/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest :app:assembleDebug :app:assembleRelease verifyMinSdkRelease --no-daemon" | tr -d '\r'`
    wrapped in `timeout 1200` ([[dinghy-display-gradle-hang-interop]] — hung Windows java children
    outlive WSL timeouts; if it hangs, `taskkill /F /T` the java tree and retry). Exit code
    authoritative. Note: assembleRelease now exercises lintVitalRelease under the new enforcement —
    this is the real proof the JDK-17 lint path holds. 3 failed attempts or ~90 min → revert this
    plan's commits, mark `failed` in SUMMARY, move on (fail-forward rail).
  </action>
  <verify>
    <automated>timeout 1200 /mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest :app:assembleDebug :app:assembleRelease verifyMinSdkRelease --no-daemon" | tr -d '\r' | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - The combined Gradle invocation exits 0 (unit tests green, debug + release assemble green, verifyMinSdkRelease green)
    - `git status --porcelain` shows no unstaged source changes after the final commit ([[dinghy-uncommitted-fix-masks-broken-head]])
  </acceptance_criteria>
  <done>Full R5a gate green: tests, debug, release (incl. lintVital under JDK-17), minSdk floor verified.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 4: Morning check — CI green on the pushed branch</name>
  <what-built>CI workflow + enforced lint baseline + StrictMode + executable gradlew (pushed to gsd/phase-26.5-overnight-hardening at the wave boundary).</what-built>
  <how-to-verify>
    Deferred to morning UAT checklist — verifier records human_needed; do NOT block the wave on this.
    1. Open GitHub Actions for mrmees/dinghy-display, branch gsd/phase-26.5-overnight-hardening.
    2. Confirm the CI run for the wave-0 push is GREEN (this also proves the fresh-clone
       `./gradlew assembleDebug` acceptance — the ubuntu runner IS a fresh clone with +x gradlew).
    3. If red: triage per the failing step (likely RESEARCH A2/A3 action-version or SDK-presence assumptions).
  </how-to-verify>
  <action>Morning-only check: do nothing tonight. The orchestrator records this as a morning UAT item; the verifier marks it human_needed.</action>
  <verify>
    <human-check>Owner confirms the GitHub Actions CI run on gsd/phase-26.5-overnight-hardening is green (or triages the failing step).</human-check>
  </verify>
  <done>CI green on the pushed branch, owner-confirmed in the morning sitting.</done>

  <resume-signal>Type "approved" or paste the failing CI step</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → GitHub Actions | CI executes workflow-declared third-party actions on push |
| debug APK → logcat | StrictMode penaltyLog writes violation traces to the device log |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26.5-01 | Tampering (supply chain) | .github/workflows/ci.yml actions | mitigate | Only first-party actions (actions/*, gradle/actions) pinned to major version tags; no third-party marketplace actions; no secrets consumed by the workflow |
| T-26.5-02 | Information Disclosure | StrictMode penaltyLog | accept | Debug builds only (BuildConfig.DEBUG guard); logs contain stack traces, never the apiKey (redacted at toString per T-04-01-I) |
| T-26.5-03 | Denial of Service (build) | abortOnError=true lint gate | mitigate | Baseline absorbs pre-existing violations; shrink-only house rule documented in the lint block comment |
| T-26.5-SC | Tampering | package installs | accept | RESEARCH Package Legitimacy Audit: NO npm/pip/cargo installs this phase; CI uses first-party GitHub/Gradle actions only; foojay-resolver-convention (if needed) is Gradle-org first-party |
</threat_model>

<verification>
- gradlew mode 100755 in the index
- ci.yml present with pinned first-party actions and the test+assemble command
- lint-baseline.xml committed; abortOnError=true; lintDebug green locally
- StrictMode debug-only in DinghyApp
- NO LICENSE file; recommendation in OVERNIGHT-REPORT.md DECISIONS-NEEDED
- Extended build gate exits 0
</verification>

<success_criteria>
Roadmap §R5a acceptance, overnight-scoped: gradlew +x committed, CI workflow committed (green
confirmed at morning checkpoint), lint baseline checked in and enforced, StrictMode in debug,
LICENSE deliberately absent with the recommendation recorded.
</success_criteria>

<output>
Create `.planning/phases/26.5-overnight-hardening-slate-audit-r-packages/26.5-01-SUMMARY.md` when done.
Include a `## Report Notes` section (content plan 26.5-07 lifts into OVERNIGHT-REPORT.md).
</output>
