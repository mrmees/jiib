---
phase: 26.5-overnight-hardening-slate-audit-r-packages
plan: 02
subsystem: docs
tags: [r9, memory-delint, hygiene-ci, todo-triage, single-sourcing]
requires: [26.5-01]
provides:
  - "Active memory files corrected + stamped (Last verified 2026-06-11)"
  - "Hygiene CI job (stale-phrase grep + dead-link check, active set only)"
  - "Reviewed R9 step-4 archive script (NOT executed)"
affects: [26.5-03, 26.5-04, 26.5-05, 26.5-06, 26.5-07]
tech-stack:
  added: []
  patterns:
    - "Last verified / Status header on every active memory file"
    - "One owner per fact: CLAUDE.md=stack+constraints, REQUIREMENTS=core value, STATE=status, ROADMAP=phases"
key-files:
  created:
    - .planning/phases/26.5-overnight-hardening-slate-audit-r-packages/26.5-r9-archive-script.sh
  modified:
    - CLAUDE.md
    - .github/workflows/ci.yml
    - docs/request-cadence-contract.md
    - .planning/PROJECT.md
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
    - .planning/STATE.md
    - .planning/research/*.md (5)
    - .planning/codebase/*.md (8)
    - 7 UI-SPEC frontmatters (07/08/09/12/14/15/16)
    - .planning/todos/ (4 archived, 13 triaged)
decisions:
  - "codebase/STACK.md macrobenchmark line reworded truthfully instead of swept to 36 — the module IS still compileSdk=35"
  - "STATE.md Phase-21 decision line reworded ('compileSdk bump 35→36') so truthful history clears the literal stale-phrase gate"
  - "Dead-link check is a deterministic bash loop, not the markdown-link-check marketplace action (RESEARCH A8)"
metrics:
  duration: "~11 min (05:35:55Z → 05:46:49Z)"
  completed: 2026-06-11
---

# Phase 26.5 Plan 02: R9 Memory/Docs Delint (steps 1-3+5) Summary

**One-liner:** Active memory stops lying — compileSdk sweep, Architecture pointer, Baseline-Profile reframe, 7 UI-SPEC supersessions, Spoolman cadence row, 4 todos archived + 13 triaged, ownership single-sourcing, Last-verified headers everywhere, scoped hygiene CI, and the step-4 bulk-archive script emitted as a guarded reviewed artifact that was never run.

## What was done

**Task 1 — step-1 correctness edits (commit 841a981):**
- `.planning/research/STACK.md`: all 4 `compileSdk 35` occurrences → 36.
- `.planning/codebase/STACK.md`: macrobenchmark line reworded (see Deviations).
- `CLAUDE.md`: Architecture section now points at `.planning/codebase/ARCHITECTURE.md` + `docs/adr/0001-ui-toolkit-decision.md`; Baseline-Profile entry reframed — NO-OP claim scoped to STOCK API 23 only, flox (LineageOS 18.1 / API 30) and all modern phones DO consult profiles; explicitly raises `macrobenchmark-module-wiring` todo priority.
- UI-SPECs 07/08/09/12/14/15/16: `status: superseded` + `superseded_by: docs/ui_design/` + `superseded_on: 2026-06-11` (04's existing form). Bodies untouched.
- `docs/request-cadence-contract.md`: Spoolman-notify push row added to the LIVE-calls inventory (`notify_active_spool_set` / `notify_spoolman_status_changed`, push-driven reconcile in `ActiveSpoolFacade`, consistent with `docs/commands/moonraker-api.md` ~166-188).
- `CONCERNS.md` + `UI-REVIEW.md` stamped `**Status:** historical — findings resolved by Phases 22–26 (stamped 2026-06-11)` at top.

**Pre-done items (recorded per plan, not re-edited):** root CLAUDE.md What-NOT-to-Use compileSdk-35 phrase (grep = 0 at start); PROJECT.md Tegra paragraph; REQUIREMENTS.md Phase-19 TBD; moonraker-api.md Spoolman notify entries — all already fixed on master per §R9 strikethroughs.

**Task 2 — todo re-triage + single-sourcing + headers (commit 27039a0):**
- 4 stale todos `git mv`'d to `completed/` with closing notes: `status-progress-ring-dual-source-jump` (surface rebuilt P16/P24), `console-macro-page-ux-flow` (P25 collapsed those screens), `benchmark-harness-fairness-fixes` (perf gating moved to gfxinfo method), `2026-06-06-shellpresencetest-device-determinism` (superseded — R10 step 3 in 26.5-03 fixes the swipe defect; FineTuneNavTest re-run on the morning UAT checklist).
- All 13 surviving pending todos annotated `Triage 2026-06-11: target Phase 27|28|29|standing` with rationale (see Report Notes for the breakdown).
- `PROJECT.md`: Constraints + Core-Value bodies deleted, replaced with single ownership-pointer lines (file structure intact).
- `**Last verified:** 2026-06-11` / `**Status:** active` headers on: root CLAUDE.md (placed ABOVE the GSD section markers so regeneration won't wipe it), PROJECT.md, REQUIREMENTS.md, ROADMAP.md, STATE.md (below YAML frontmatter — line 1 still `---`), and the 6 non-historical codebase/ files.
- All 5 `research/*.md` stamped `**Status:** historical (research snapshot; see CLAUDE.md for current stack)`.

**Task 3 — archive script + hygiene CI + build gate (commit cbe1963):**
- `26.5-r9-archive-script.sh` written, NEVER executed (only `bash -n` syntax-parsed). Dry-run by default; `--execute` owner gate; `--self-test` mode asserts the guard blocks every protected path with zero working-tree mutations. Protected list baked in as a bash array gating every rm/mv: phases/01+13 captures, 15.2-AUDIT.md, PATTERNS 02/03/18, all `spoolman-live-*.json` + e3/e5 `.jsonl`, ALL PLAN/SUMMARY pairs, plus the in-flight 26.5 phase dir. 15.2-AUDIT→THEMING.md link reminder included as comments.
- `ci.yml` gained the `hygiene` job: checkout-only; stale-phrase grep (`compileSdk 35`, `Architecture not yet mapped`) over CLAUDE.md + 4 .planning root files + docs/ with `--exclude=top-down-audit-roadmap.md`; never references `.planning/phases`; dead-internal-link check is a self-contained bash loop (A8 substitution — no third-party action).
- Both hygiene checks run locally: PASS (after the STATE.md reword below).
- Build gate: `:app:testDebugUnitTest :app:assembleDebug` exit 0 (BUILD SUCCESSFUL).

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug avoided] codebase/STACK.md macrobenchmark line NOT swept to 36**
- **Found during:** Task 1
- **Issue:** The plan said sweep the 1 remaining `compileSdk 35` → 36, but `macrobenchmark/build.gradle.kts:19` is genuinely `compileSdk = 35` — the sweep would have made the doc LIE (the exact failure mode R9 exists to fix).
- **Fix:** Reworded truthfully: `` `compileSdk = 35` in that module (deliberately NOT bumped to the app's 36 — the benchmark module trails the app) ``. Clears the literal stale-phrase grep AND stays accurate.
- **Files modified:** .planning/codebase/STACK.md
- **Commit:** 841a981

**2. [Rule 3 - Blocking] STATE.md decision-log line tripped the new hygiene gate**
- **Found during:** Task 3 local hygiene run
- **Issue:** STATE.md:489 ("accepted compileSdk 35→36") contains the literal `compileSdk 35` substring — truthful history, false positive.
- **Fix:** Reworded to "accepted the compileSdk bump 35→36" (meaning identical; matches CLAUDE.md's own "bumped 35→36" phrasing). Gate kept at full strength rather than weakening the regex.
- **Files modified:** .planning/STATE.md
- **Commit:** cbe1963

**3. [Rule 1 - Bug] sed stamp insertion left a stray `n` prefix**
- **Found during:** Task 2 header pass
- **Issue:** The batch `sed 1a` rendered the leading `\n` of the stamp as a literal `n` ("n**Last verified:**") in 16 files.
- **Fix:** Follow-up sed removed the stray character in all files before commit; spot-checked CLAUDE.md, STATE.md, research/SUMMARY.md.
- **Commit:** 27039a0 (fixed pre-commit, never landed broken)

## TDD Gate Compliance

N/A — docs-only plan, no `tdd` tasks.

## Known Stubs

None.

## Threat Flags

None — T-26.5-04/05/06 mitigations implemented as planned (script un-run + guarded; headers + hygiene grep landed; scope excludes phases/ and the audit doc).

## Report Notes

**Archive script (R9 step 4 — owner review-then-run):**
- Path: `.planning/phases/26.5-overnight-hardening-slate-audit-r-packages/26.5-r9-archive-script.sh`
- Would DELETE: **31** `*DISCUSSION-LOG*` files under `.planning/phases/` (§R9 said "~26"; actual count at HEAD is 31 — phases kept accruing since the audit).
- Would MOVE: **113** shipped-phase CONTEXT/VALIDATION/VERIFICATION/REVIEW files (115 total minus 2 in-flight 26.5 files) → `.planning/archive/<phase>/`. PLAN/SUMMARY pairs stay put (protected pattern).
- Run `bash <script> --self-test` first (asserts guards + zero mutations), then dry-run (no args), then `--execute`.
- ⚠ drvfs caveat: on the `/mnt/e` mount every file shows fs-mode 0777 and chmod is a no-op, so `test -x` on the filesystem is meaningless here. The authoritative non-executable state is the **git index mode = 100644** (verified; repo runs `core.filemode false`).

**Todo re-triage outcome (17 pending → 4 archived + 13 triaged):**
- Phase 27: 1 (move-z-vertical-layout-rework — rides the Motion redesign)
- Phase 28: 7 (bookmarked-macros-density, phase-15.1 review deferreds, printers-edit-delete-mode, settings-densify C6, fw-retraction-glyphs [owner glyph pick required], temperature-adjuster-fire-without-wait [dispatcher-semantics change — protected tonight], webcam-aspect-ratio-overlay-back)
- Phase 29: 4 (phase-11 spool robustness, phase-14 review deferreds WR-02/03, arm64-ABI release publishing [code lands tonight in R1], macrobenchmark-module-wiring — **priority RAISED**: flox is API 30, Baseline Profiles apply)
- Standing: 1 (dev-overlay-panel-drag — debug tooling, no ship gate)
- §R9's adopted rule "re-triage todos/pending/ at every phase close" is now recorded in the triage annotations themselves; consider adding it to the GSD transition checklist.

**Contradictions §R9 didn't anticipate:**
1. The macrobenchmark `compileSdk = 35` is REAL (module never bumped) — the audit's "sweep all 35s" framing assumed they were all drift. One was truth. (Deviation 1.)
2. STATE.md's truthful Phase-21 decision history collides with a literal stale-phrase gate — any future doc that NARRATES the 35→36 bump must avoid the exact substring `compileSdk 35` (write "bump 35→36"). Worth knowing before anyone re-words those entries back.
3. DISCUSSION-LOG count is 31, not ~26 (the audit predates phases 23-26.5 logs).

**DECISIONS-NEEDED:**
- Run (or decline) the step-4 archive script after reviewing it — owner-gated by CONTEXT lock; nothing was moved tonight.
- `macrobenchmark-module-wiring` todo: now that Baseline Profiles are established as applicable on flox + modern devices, decide whether it lands in Phase 29 (recommended, with the release-build work) or earlier.

## Self-Check: PASSED

- 26.5-r9-archive-script.sh exists: FOUND
- ci.yml hygiene job: FOUND
- Commits 841a981, 27039a0, cbe1963 in git log: FOUND
- 4 todos in completed/, 0 pending without triage line: VERIFIED
- Stale-phrase grep over active set: 0 hits
- Build gate exit 0: VERIFIED
