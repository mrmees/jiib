---
phase: 26.5-overnight-hardening-slate-audit-r-packages
verified: 2026-06-11T00:00:00Z
status: human_needed
score: 7/7 must-haves verified (code-side); device/CI acceptance pending morning UAT
overrides_applied: 0
human_verification:
  - test: "R5a CI green on GitHub"
    expected: "The GitHub Actions run on the final gsd/phase-26.5-overnight-hardening push is GREEN — testDebugUnitTest + lintDebug (baseline-enforced) + assembleDebug + hygiene job (stale-phrase + dead-link). The ubuntu runner doubles as the fresh-clone ./gradlew +x acceptance."
    why_human: "Requires the workflow to actually run on GitHub's runner; cannot be exercised locally."
  - test: "R10 tap torture / rejection flash / disabled-no-ripple / drawer swipe (flox, DEBUG build)"
    expected: "≥95% taps register with same-frame feedback; busy-window stepper flashes amber; disabled stepper produces no ripple; swipe-up opens drawer with slow AND fast gestures. Logcat: -s Dispatcher SwipeDetector."
    why_human: "Touch latency / visible feedback / gesture accumulation are on-device perceptual checks on the Adreno-320 floor."
  - test: "R10 cause #4 verdict (deferred indication-immediacy)"
    expected: "If taps still feel laggy in scrollables, the deferred (intentionally reverted) cause-#4 fix is implicated — log for a follow-up phase. NOT a gap tonight."
    why_human: "Requires the on-device instrumentation session to decide whether the deferred fix is warranted."
  - test: "R1 arm64 install + edge-to-edge + predictive back + POST_NOTIFICATIONS one-shot (S25 Ultra)"
    expected: "arm64-v8a APK (vc=2) installs/runs; content respects insets both orientations; back-gesture preview animates; first connect triggers exactly one notification dialog, DENY leaves app functional."
    why_human: "64-bit-only device + edge-to-edge + predictive-back are S25-only runtime behaviors."
  - test: "R1 Nexus-7-unchanged regression (flox, armeabi-v7a vc=1)"
    expected: "App behaves exactly as before — no inset regressions on API 30, no permission-prompt surprises."
    why_human: "On-device regression check on the perf floor."
  - test: "R2 keep-awake toggle / exemption flow / doze survival (flox)"
    expected: "Settings>Display ON keeps screen awake live (no relaunch); battery row deep-links to exemption and re-reads Exempt on return; 20+ min unplugged screen-off doze with exemption granted keeps reconnect alive."
    why_human: "Window-flag behavior + Doze survival are runtime/device-time-dependent."
  - test: "R7 toggle-ON-no-proxy comprehensible+reversible; wss connect; cert-failure message (S25 / live TLS)"
    expected: "Toggling Use HTTPS/WSS ON against a non-TLS printer fails readably on Splash (no hang/crash) and reverts cleanly OFF; wss connects against a TLS-fronted Moonraker; self-signed endpoint shows the distinct 'TLS certificate not trusted' message."
    why_human: "Requires live TLS / self-signed reverse-proxy endpoints; cannot be exercised in host tests."
  - test: "R7 plain-ws regression BOTH printers (flox)"
    expected: "E5+ (192.168.1.120) and E3 (192.168.1.121) both still connect over plain ws:// with the toggle OFF (default); old profiles decode to OFF."
    why_human: "Live LAN connection against both physical printers."
---

# Phase 26.5: Overnight Hardening Slate (audit R-packages) Verification Report

**Phase Goal:** Execute the overnight-safe subset of `docs/top-down-audit-roadmap.md` (R5a guardrail infra minus LICENSE · R9 docs delint 1–3+5 · R10 touch responsiveness 2–4 · R1 install/display code-only · R2 always-on code-only · R4 command map · R7 network posture). All on-device acceptance → consolidated morning UAT checklist (the morning UAT run is the phase gate).
**Verified:** 2026-06-11
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

All seven ROADMAP success criteria are CODE-COMPLETE and verified against the codebase (not just the SUMMARYs). The 7/7 score reflects automated/code-side achievement; the phase is correctly NOT marked complete because every package carries device/CI acceptance items that route to the morning UAT gate (`human_verify_mode = end-of-phase`, per CONTEXT). This `human_needed` status is the intended terminal state for the night, not a deficiency.

### Observable Truths

| # | Truth (ROADMAP SC) | Status | Evidence |
|---|---|---|---|
| 1 | **R5a:** gradlew +x in index; CI workflow (test+lintDebug+assembleDebug + scoped hygiene); enforced lint baseline; debug StrictMode; NO LICENSE | ✓ VERIFIED (code) | `git ls-files -s gradlew` = `100755`; `.github/workflows/ci.yml` runs `:app:testDebugUnitTest :app:lintDebug :app:assembleDebug` + `hygiene` job (stale-phrase + dead-link, active-set scoped); `app/build.gradle.kts:101-114` baseline + `abortOnError=true` + `checkReleaseBuilds=true` (only inert `NullSafeMutableLiveData` detector disabled — zero LiveData in app); `DinghyApp.kt:134` StrictMode under `BuildConfig.DEBUG`; `ls LICENSE` → absent |
| 2 | **R9:** active memory trued up; Last-verified headers; todos re-triaged; step-4 archive script written NON-executable, guarded, NEVER run | ✓ VERIFIED | `**Last verified:** 2026-06-11` on CLAUDE.md + all 4 `.planning` root files; hygiene grep (`compileSdk 35`/`Architecture not yet mapped`) returns clean on active set; `26.5-r9-archive-script.sh` index mode `100644` (non-exec; drvfs no-op noted), guards protected list + unshipped 26.5/27/28/29 + PLAN/SUMMARY pairs; no `.planning/archive/` dir, zero D/move commits on branch |
| 3 | **R10:** OutlinedControl `enabled` (no clickable when false); rejectedKey SharedFlow at both early returns (timing intact, DEBOUNCE=400); SwipeUpAccumulator + AppShell (80f intact); DEBUG-gated instrumentation; step-4 REVERTED (plain clickable restored, deferral recorded) | ✓ VERIFIED | `OutlinedControl.kt:130-138` `!enabled -> Modifier` (bare), `else -> Modifier.clickable` (plain pre-plan form); `CommandDispatcher.kt:128,138` `_rejectedKey.tryEmit(key)` at in_flight + debounce returns, `DEFAULT_DEBOUNCE_MS=400L`, DEBUG-gated `Log.d`; `SwipeUpAccumulator.kt` + `AppShell.kt:573` accumulator, `SWIPE_UP_THRESHOLD_PX=80f` (986); step-4 reverted per `OutlinedControl.kt:122-129` comment + commit `2dd4bc7` |
| 4 | **R1:** both-ABI splits + release-scoped onVariants versionCode; enableEdgeToEdge + safeDrawingPadding; predictive-back manifest attr; POST_NOTIFICATIONS one-shot; ContextCompat.startForegroundService | ✓ VERIFIED (code) | `build.gradle.kts:65` `include("armeabi-v7a","arm64-v8a")` + `isUniversalApk=false`, `:148` `onVariants(selector().withBuildType("release"))` versionCode offsets (v7a=0/arm64=+1) via Property `.set()`; `MainActivity.kt:87` `enableEdgeToEdge()` + `:149` `safeDrawingPadding()`; manifest `:68` `enableOnBackInvokedCallback="true"`, `:31` POST_NOTIFICATIONS; `MainActivity.kt:74-78` one-shot guarded (≥TIRAMISU, not granted, not asked), `:98` `ContextCompat.startForegroundService` |
| 5 | **R2:** DisplayPrefs default-true via AppContainer writeScope; Settings Display toggle + battery-exemption row; View.keepScreenOn at AppShell root; REQUEST_IGNORE_BATTERY declared | ✓ VERIFIED (code) | `DisplayPrefs.kt:50` `DEFAULT_KEEP_SCREEN_ON=true`, fail-safe to default; `AppContainer.kt:332,343` `displayPrefs` + `setKeepScreenOn` via process-lifetime `writeScope` (avoids [[dinghy-compose-write-scope-cancellation]]); `SettingsScreen.kt:200-228` Display section toggle + battery-optimization row w/ deep-link + ON_RESUME re-check; `AppShell.kt:175-177` `rootView.keepScreenOn` via DisposableEffect; manifest `:42` REQUEST_IGNORE_BATTERY_OPTIMIZATIONS |
| 6 | **R4:** CommandMap.kt per Part 3; all six sites rewired symbolically; CommandCatalogDriftTest green untouched; real wiring tests | ✓ VERIFIED | `CommandMap.kt:17-21` object w/ `Slot(macro,gcode)`, M112/emergency_stop explicit non-goal (`:14`); six symbolic sites: CommandRegistry:491/498, PrinterCommands:330/333, ExtrudeScreen:301/303, ExtrudeHolder:108/109; `CommandCatalogDriftTest` NOT in branch diff (untouched) + GREEN locally; `CommandMapTest.kt:51+` real registry/wire assertions (codex amendment `1114670`) |
| 7 | **R7:** useSecure default-false migration-safe; wss/https centralized getters; editor toggle; TlsTrustFailure UX incl. auth-token-fetch path; NSC decision comment, still LAN-permissive | ✓ VERIFIED (code) | `ConnectionConfig.kt:20` `useSecure=false`, `:27/:33` `httpBase`/`wsUrl` switch http↔https / ws↔wss; `ConnectionStore.kt:91` `useSecure ?: false` (missing key → false, migration-safe); `PrintersScreen.kt:295-301,394` editor toggle row; `RpcError.kt:33` `TlsTrustFailure` (no trust-all escape); `MoonrakerSession.kt:164-172,200-202,391-396` routes socket-close + AUTH-TOKEN-FETCH path to TlsTrust; `MoonrakerAuth.kt:75-78` oneshot-token SSLHandshake→TlsTrustFailure (codex amendment `1a79761`); `SplashScreen.kt:228-229` distinct trust message; `network_security_config.xml:7-32` decision record + RFC-1918-descope rationale + `cleartextTrafficPermitted="true"` base |

**Score:** 7/7 truths verified (code-side). Device/CI acceptance for SCs 1, 3, 4, 5, 7 → morning UAT (human_needed).

### Protected-Surface Audit (hard requirement)

**Result: CLEAN — the night touched none of the protected surfaces.**

| Protected surface | Result | Evidence |
|---|---|---|
| PrinterStateStore cadence / DEFAULT_SAMPLE_MS / V1_SUBSCRIBE_CORE | ✓ untouched | `PrinterStateStore.kt` NOT in `git diff --name-only master...HEAD` |
| GraphView / ProgressRing / Files+Console RecyclerView render paths | ✓ untouched | `git diff` of `*GraphView* *ProgressRing* *FilesAdapter* *ConsoleView* *FileRowsAdapter*` = empty |
| M112 / emergency_stop semantics | ✓ untouched | CommandRegistry diff = only the two LOAD/UNLOAD macro-name hardcodes → symbolic; `emergency_stop` catalogId untouched; CommandMap explicitly non-maps M112 (`CommandMap.kt:14`) |
| 400 ms dispatcher debounce timing | ✓ untouched | `DEFAULT_DEBOUNCE_MS = 400L` intact; R10 added emission only (`CommandDispatcher.kt:134` "expression and 400ms default UNCHANGED — feedback emission only") |
| R9 step-4 protected file list (01/13 captures, 15.2-AUDIT, PATTERNS 02/03/18, spoolman fixtures, PLAN/SUMMARY pairs) | ✓ untouched | archive script never executed; no archive moves/deletes on branch; guards self-test-asserted in the script |

CommandRegistry.kt IS in the changed set (R4) but its only edits are the two macro-name hardcodes → symbolic references; no safety-command or subscribe-core lines changed.

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|---|---|---|---|
| Phase host tests compile + pass | `gw.bat :app:testDebugUnitTest --tests *CommandMapTest *DisplayPrefsTest *SwipeUpAccumulatorTest *ConnectionConfigTest *ConnectionStoreTest *CommandDispatcherTest *CommandCatalogDriftTest` | BUILD SUCCESSFUL | ✓ PASS |
| D-10 drift guard still green | (above, CommandCatalogDriftTest) | green, source untouched | ✓ PASS |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|---|---|---|---|---|
| `.planning/config.json` | 27, 42 | `auto_advance: true` + `code_review_command: codex` night-mode config NOT reverted | ℹ️ Info | CONTEXT said to revert these "as the phase's final commit." Still in place. Process-bookkeeping only — does NOT affect any of the 7 SCs or any code deliverable. Flag for owner to revert; not goal-blocking. |

No unreferenced debt markers (TODO/FIXME/XXX/TBD/HACK) found in any touched Kotlin/Kotlin-script file. No stub returns, no hollow props, no orphaned artifacts.

### Gaps Summary

**No goal-blocking gaps.** All 7 ROADMAP success criteria are code-complete, substantive, and wired; host tests pass; protected surfaces are untouched; the R9 archive script was written but never executed exactly as required. The codex post-review amendments (CI lintDebug enforcement, archive-script unshipped-phase guards, R10 cause-#4 revert + lifecycle-aware collection, ContextCompat.startForegroundService + release-scoped onVariants, CommandMap real wiring assertions, R7 auth-token-fetch TLS classification) are all present in the working tree and committed.

The phase status is `human_needed` because — by design (CONTEXT: morning UAT is the phase gate) — every package's final acceptance is device- or CI-dependent (flox / S25 Ultra / live TLS / Doze / CI-on-GitHub). These are recorded in the `human_verification` frontmatter, mirroring the OVERNIGHT-REPORT.md morning UAT checklist. Do NOT mark the phase passed/complete until that sitting closes.

One informational item: revert the night-mode `.planning/config.json` overrides (`auto_advance`, `code_review_command`) — CONTEXT mandated this as the phase's final commit and it has not yet landed.

---

_Verified: 2026-06-11_
_Verifier: Claude (gsd-verifier)_
