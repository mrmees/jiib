---
phase: 26.5-overnight-hardening-slate-audit-r-packages
plan: 07
subsystem: net-and-config
tags: [r7, network-posture, wss, https, usesecure, tls, cert-failure, nsc, overnight-report]
requires:
  - phase: "26.5-04"
    provides: "wave dependency — R1 manifest/build state this diff sits on"
  - phase: "26.5-05"
    provides: "wave dependency — Settings/AppContainer state (writeScope intent precedent)"
  - phase: "26.5-06"
    provides: "wave-4 sibling (no file overlap) — R4 landed first"
provides:
  - "ConnectionConfig.useSecure: Boolean = false — both centralized URL getters (httpBase/wsUrl) scheme-switch ws/http ↔ wss/https; every consumer (socket, REST, webcam, thumbnails) rides them"
  - "useSecure persisted per-printer through Profile/PersistedProfile (defaulted @Serializable field + ignoreUnknownKeys = old blobs decode to false, migration-safe) AND through ConnectionStore (KEY_USE_SECURE, sanitize null→false)"
  - "PrintersScreen connection editor: Use HTTPS/WSS toggle row (stringResource, tokens+fsSp, no icon), seeded from profile.useSecure, persisted via the SAME durable saveProfile intent"
  - "ConnectionError.TlsTrustFailure: MoonrakerSocket.onFailure types SSLHandshakeException/SSLPeerUnverifiedException (bounded cause-chain walk); MoonrakerSession preserves the typed pre-open close cause and surfaces Error(TlsTrustFailure) with backoff; SplashScreen shows the distinct trust message on the existing Unreachable surface"
  - "network_security_config.xml R7 decision record: LAN-permissive cleartext is DELIBERATE (NSC cannot express RFC-1918 CIDR; useSecure is the mechanism; §R7 public-IP half descoped + documented)"
  - "OVERNIGHT-REPORT.md FINALIZED: 7/7 per-package status + SHAs, codex acted/deferred table, DECISIONS-NEEDED, R9 script pointer, one-sitting morning UAT checklist"
affects: [phase-29-ship, morning-uat, r5b-readme]
tech-stack:
  added: []
  patterns:
    - "scheme switching lives ONLY in ConnectionConfig's centralized getters — never parallel URL builders"
    - "typed transport causes: SocketEvent.Closed carries a distinct ConnectionError variant end-to-end (socket → session → store → Splash)"
    - "per-profile boolean = defaulted field on BOTH Profile shapes + ignoreUnknownKeys (the webcamEnabled migration precedent)"
key-files:
  created:
    - app/src/test/java/works/mees/dinghy/config/ConnectionConfigTest.kt
  modified:
    - app/src/main/java/works/mees/dinghy/config/ConnectionConfig.kt
    - app/src/main/java/works/mees/dinghy/config/Profile.kt
    - app/src/main/java/works/mees/dinghy/config/ConnectionStore.kt
    - app/src/main/java/works/mees/dinghy/ui/screen/PrintersScreen.kt
    - app/src/main/java/works/mees/dinghy/net/MoonrakerSocket.kt
    - app/src/main/java/works/mees/dinghy/net/MoonrakerSession.kt
    - app/src/main/java/works/mees/dinghy/net/RpcError.kt
    - app/src/main/java/works/mees/dinghy/ui/screen/SplashScreen.kt
    - app/src/main/res/xml/network_security_config.xml
    - app/src/main/res/values/strings.xml
    - app/src/test/java/works/mees/dinghy/config/ConnectionStoreTest.kt
    - .planning/phases/26.5-overnight-hardening-slate-audit-r-packages/OVERNIGHT-REPORT.md
key-decisions:
  - "TLS cause propagation extended past the plan's file list (Rule 2): the must-have 'distinct trust message on the EXISTING connection-error UX' is unreachable from MoonrakerSocket alone — the session collapsed every pre-open failure to Disconnected and the Splash never read the cause. Added ConnectionError.TlsTrustFailure + ConnectAttempt.TlsTrust (backoff like Network, no quiescence) + a reasonText branch; UI surface itself unchanged (same Retry + Edit connection)"
  - "TLS failures back off and retry like network failures (NOT AuthRequired-style quiescence) — toggling useSecure off or fixing the cert plus Retry recovers; a wrong-cert state self-heals when the proxy is fixed"
  - "sanitize's useSecure param defaulted to null instead of required (PATTERNS showed required) — every existing call site/test compiles unchanged, identical behavior, smaller diff"
  - "AppContainer.kt needed ZERO changes despite being in files_modified: saveProfile takes a whole Profile and activeConfig's distinctUntilChanged keys on ConnectionConfig equality — a useSecure flip changes the config so the spine rebinds automatically (pinned by test)"
  - "Splash trust message is a literal string, matching that file's existing reasonText convention (plan-sanctioned: consistency with the existing path wins)"
patterns-established:
  - "useSecure flip MUST churn the spine: useSecureFlip_changesConnectionConfig_soSpineRebinds pins that distinctUntilChanged rebinds on toggle"
requirements-completed: ["R7 — docs/top-down-audit-roadmap.md §R7 as overnight-scoped by CONTEXT option B (wss/https plumbing + toggle + cert-failure UX + documented NSC posture; live-TLS connect = morning UAT; public-IP cleartext half descoped + documented in the NSC comment)"]
duration: ~18min
completed: 2026-06-11
---

# Phase 26.5 Plan 07: R7 Network Posture (option B) Summary

**One-liner:** Per-printer `useSecure` toggle flips both centralized URL getters to wss/https (old profile blobs decode to plain-ws unchanged, proven), an untrusted TLS cert now names itself on the existing Splash Unreachable surface instead of masquerading as "can't reach", the NSC's LAN-cleartext posture is a written decision record instead of a deferred TODO — and OVERNIGHT-REPORT.md is finalized as the morning handoff for the whole night.

## Performance

- **Duration:** ~18 min (07:41:50Z → ~08:00Z)
- **Tasks:** 3/3 automated complete; Task 4 (checkpoint:human-verify, gate=blocking-human) deferred to morning UAT by design — items are on the report checklist
- **Files:** 13 (1 created)

## What Was Built

**Task 1 — useSecure model + persistence, TDD (`31ad990` RED → `e508f6c` GREEN):**
- RED: new `ConnectionConfigTest` (9 cases — the 5 behavior-table cases plus redaction + spine-rebind guards) + a `roundTrip_useSecureTrue_persists` DataStore case in `ConnectionStoreTest`; the field plumbing landed data-only (getters still hardcoded) so the whole sourceset compiled ([[dinghy-wave0-red-scaffold-compile]]) while `useSecureTrue_producesWssAndHttpsUrls` genuinely FAILED (verified: 24 tests, exactly 1 failure).
- GREEN: `httpBase`/`wsUrl` scheme-switch on `useSecure`; `toString` keeps the `***` apiKey redaction on ConnectionConfig AND PersistedProfile/Profile (asserted); D-10/D-11 getter comments now reference R7 + the toggle.
- Old-blob migration proof runs through the PRODUCTION path (`ProfileStore.sanitize` on a literal pre-R7 JSON blob → `useSecure=false` → `ws://`). Theme-only edits still produce EQUAL ConnectionConfigs (no spine churn); a useSecure flip produces an UNEQUAL config (spine rebinds — that's how the toggle takes effect live).

**Task 2 — editor toggle + cert-failure UX + NSC record (`b38f132`):**
- **Toggle:** `SecureToggleRow` in the PrintersScreen connection editor (SettingsScreen's ToggleRow grammar verbatim — outlined row + ON/OFF pill, ≥64dp target, tokens + fsSp, NO icon), co-located with host/port/apiKey, seeded in the existing `LaunchedEffect(profile?.id)`, committed by Save through the SAME durable `container.saveProfile` intent (write-scope law). 3 new strings, all `stringResource`-routed.
- **Cert-failure UX:** new `ConnectionError.TlsTrustFailure`; `MoonrakerSocket.onFailure` types it via a bounded cause-chain walk over `SSLHandshakeException`/`SSLPeerUnverifiedException`; `MoonrakerSession` preserves the typed pre-open close cause (`closed.getCompleted()`) and a new `ConnectAttempt.TlsTrust` branch emits `ConnectionState.Error(TlsTrustFailure)` + markStale with normal backoff; `SplashScreen.reasonText` returns "TLS certificate not trusted — check the Moonraker reverse-proxy certificate" ahead of any retained klippyStateMessage. Same Unreachable surface, same Retry + Edit connection controls. Zero TrustManager code anywhere (grep-asserted).
- **NSC:** `cleartextTrafficPermitted="true"` retained; the D-11 deferred-scoping note REPLACED with the R7 decision record (NSC cannot express RFC-1918 CIDR — domain entries are hostname-suffix only; useSecure is the mechanism; the §R7 "cleartext to public IP refused" acceptance half is descoped and documented; T-26.5-19 accepted).

**Task 3 — build gate + report (`7828472`):**
- Gate: `:app:testDebugUnitTest :app:assembleDebug` → **BUILD SUCCESSFUL, exit 0** (first attempt; full host suite green — no fail-forward).
- OVERNIGHT-REPORT.md finalized: Run Summary (7/7 plans, zero failures), Per-Package Status with all commit SHAs incl. the four orchestrator codex-fix commits (332c0ff/c92aa03/2dd4bc7/0ae62c4/1114670) lifted from the post-review amendments, codex acted/deferred table, DECISIONS-NEEDED (GPLv3 LICENSE rec; R9 archive-script review-then-run; macrobenchmark todo placement), R9 script pointer (31 deletes / 113 moves, self-test → dry-run → execute), and the one-sitting morning UAT checklist: flox group (R10 torture/flash/disablement/swipe + logcat tags, R2 keep-awake/exemption/doze, R1 v7a regression, R7 plain-ws regression on BOTH printers) → S25 Ultra group (R1 arm64/edge-to-edge/predictive-back/notifications incl. the two codex deferred-MEDIUM watch items, R7 toggle-ON-no-proxy comprehensible+reversible, best-effort wss + self-signed cert message) → R5a CI-green wrap-up. Zero "pending" rows (grep-verified).

## TDD Gate Compliance

RED gate: `test(26.5-07)` commit `31ad990` — verified failing (1/24, the scheme-behavior case) with the whole test sourceset compiling. GREEN gate: `feat(26.5-07)` commit `e508f6c` — all green. No refactor commit needed.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 - Missing critical] TLS cause propagation required three files beyond the plan's list**
- **Found during:** Task 2 (tracing how a Closed cause reaches the user)
- **Issue:** The plan's must-have ("TLS failure surfaces through the EXISTING connection-error UX with a distinct trust message") is unreachable from `MoonrakerSocket.kt` alone: the session's supervisor collapsed every pre-open failure to `ConnectAttempt.Network` → `ConnectionState.Disconnected`, and `SplashScreen.reasonText` never read a `ConnectionError` — the message would have been typed at the socket and then silently discarded.
- **Fix:** Minimal end-to-end plumbing on the existing path: `ConnectionError.TlsTrustFailure` variant (`RpcError.kt`), pre-open cause preservation + `ConnectAttempt.TlsTrust` branch (`MoonrakerSession.kt`), and one reasonText branch (`SplashScreen.kt`). No UI redesign — same surface, same recovery controls. All `when`s over ConnectionError carry `else` branches (verified), so the new variant breaks nothing.
- **Files modified:** RpcError.kt, MoonrakerSession.kt, SplashScreen.kt
- **Commit:** `b38f132`

**2. [Rule 3 - Blocking] RpcError KDoc tripped the no-TrustManager verify grep**
- **Found during:** Task 2 verification
- **Issue:** The new TlsTrustFailure KDoc said "no trust-all/custom-TrustManager escape" — the plan's gate is a bare `! grep -rn "TrustManager" app/src/main/java`, which a doc comment false-positives.
- **Fix:** Reworded to "custom trust-manager escape" (same meaning); the grep now measures code, which is the check's intent. Zero actual TrustManager code exists.
- **Commit:** `b38f132`

### Notes (not code deviations)

- **AppContainer.kt: zero changes needed** despite appearing in the plan's `files_modified` — `saveProfile(Profile)` already carries the new field whole, and `activeConfig`'s `distinctUntilChanged` keys on ConnectionConfig equality, so a useSecure flip rebinds the spine automatically (pinned by `useSecureFlip_changesConnectionConfig_soSpineRebinds`).
- **`sanitize` gained `useSecure: Boolean? = null` (defaulted)** rather than PATTERNS' required param — every existing call site and the 8 pre-existing sanitize tests compile unchanged; behavior identical (null → false).

## Deferred Checkpoint (Task 4 — morning UAT)

`checkpoint:human-verify` (gate=blocking-human) deferred per the unattended rails — the verifier should record `human_needed`. All four R7 items are pre-filled on the OVERNIGHT-REPORT.md checklist: plain-ws regression on both printers (flox group), toggle-ON-without-proxy comprehensible+reversible failure, best-effort wss connect, best-effort self-signed cert message (S25 group).

## Verification Results

- New config tests green incl. old-blob decode + churn regression (24/24 after GREEN) ✓
- Grep proofs: `useSecure` in PrintersScreen ✓; `SSLHandshakeException` in MoonrakerSocket ✓; `RFC-1918` in NSC ✓; `printers_use_secure` in strings.xml ✓; zero `TrustManager` hits in app/src/main ✓
- `gradle/libs.versions.toml` untouched (zero new dependencies) ✓
- Build gate exit 0; full host suite green ✓
- Report: "Per-Package Status" present; zero literal "pending" occurrences ✓
- `git status --porcelain` clean after final task commit ✓

## Known Stubs

None — the toggle drives live persistence, the socket types real exceptions, and the Splash renders the real cause. Nothing placeholder.

## Threat Flags

None beyond the plan's register. T-26.5-20 mitigated (default trust chain only, grep-asserted; distinct message, no bypass). T-26.5-21 mitigated (redaction preserved + asserted on every touched type; useSecure non-secret). T-26.5-22 mitigated (defaulted field + ignoreUnknownKeys, decode-old-blob test). T-26.5-19 accepted + documented in the NSC comment.

## Report Notes

(Consumed by this same plan — OVERNIGHT-REPORT.md is finalized; the R7 morning items live in its S25 Ultra + flox groups.)

## Commits

| Task | Commit | Message |
|---|---|---|
| 1 (RED) | `31ad990` | test(26.5-07): add failing wss/https scheme tests + useSecure field plumbing (RED) |
| 1 (GREEN) | `e508f6c` | feat(26.5-07): scheme-aware httpBase/wsUrl — useSecure switches ws/http to wss/https (GREEN) |
| 2 | `b38f132` | feat(26.5-07): connection-editor HTTPS/WSS toggle + TLS cert-failure UX + NSC decision record |
| 3 | `7828472` | docs(26.5-07): finalize OVERNIGHT-REPORT — 7/7 packages done, codex acted/deferred table, morning UAT sitting |

## Self-Check: PASSED

## Post-review amendment (orchestrator, codex review ~04:30)

Codex REVISE — 3 findings:
- **HIGH fixed:** the keyed-auth oneshot-token fetch (https) collapsed SSL trust failures into
  NetworkUnavailable, bypassing the new cert UX — now classified via
  `MoonrakerSocket.isTlsTrustFailure` and routed `ConnectionError.TlsTrustFailure →
  ConnectAttempt.TlsTrust` in MoonrakerSession.
- **MEDIUM fixed:** `deriveNativeStreamUrl`'s HLS branch hardcoded `http://` — now preserves the
  source stream_url's scheme (behavior unchanged today; no silent downgrade if ravens-perch ever
  publishes https). Camera hosts are deliberately NOT governed by the Moonraker useSecure toggle.
- **LOW deferred:** the Splash TLS message is hardcoded — consistent with the entire Splash
  string cluster (the known Phase-28/R6 tokenization remainder); tokenize the cluster together.
Gate re-run green after fixes.
