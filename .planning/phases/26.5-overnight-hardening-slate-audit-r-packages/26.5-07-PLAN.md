---
phase: 26.5-overnight-hardening-slate-audit-r-packages
plan: 07
type: execute
wave: 4
depends_on: [26.5-04, 26.5-05]
files_modified:
  - app/src/main/java/works/mees/dinghy/config/ConnectionConfig.kt
  - app/src/main/java/works/mees/dinghy/config/ConnectionStore.kt
  - app/src/main/java/works/mees/dinghy/config/Profile.kt
  - app/src/main/java/works/mees/dinghy/di/AppContainer.kt
  - app/src/main/java/works/mees/dinghy/ui/screen/PrintersScreen.kt
  - app/src/main/java/works/mees/dinghy/net/MoonrakerSocket.kt
  - app/src/main/res/xml/network_security_config.xml
  - app/src/main/res/values/strings.xml
  - app/src/test/java/works/mees/dinghy/config/ConnectionConfigTest.kt
  - app/src/test/java/works/mees/dinghy/config/ConnectionStoreTest.kt
  - .planning/phases/26.5-overnight-hardening-slate-audit-r-packages/OVERNIGHT-REPORT.md
autonomous: true
requirements:
  - "R7 — docs/top-down-audit-roadmap.md §R7 (network posture; owner-blessed option B: useSecure toggle + wss/https plumbing; NSC stays LAN-permissive — CIDR impossible — with the decision documented; live-TLS check → morning UAT)"
must_haves:
  truths:
    - "ConnectionConfig(useSecure=true) produces wss:// and https:// URLs; useSecure=false (the default) produces today's ws:// and http:// — proven by unit tests"
    - "useSecure persists per-printer through the profile blob (old blobs decode with default false — migration-safe) and through ConnectionStore, and is editable as a toggle in the connection editor"
    - "A TLS certificate failure surfaces through the EXISTING connection-error UX with a distinct trust message — never a silent fail, never a trust-all bypass"
    - "NSC remains LAN-permissive with the decision documented in the file comment (NSC cannot express RFC-1918 CIDR; the useSecure toggle is the mechanism) — D-11 acknowledgment updated"
    - "apiKey redaction in toString is preserved in every touched config type"
    - "NO new dependencies (OkHttp's default trust chain handles TLS)"
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/config/ConnectionConfig.kt"
      provides: "useSecure field + scheme-aware httpBase/wsUrl"
      contains: "useSecure"
    - path: "app/src/test/java/works/mees/dinghy/config/ConnectionConfigTest.kt"
      provides: "ws/http vs wss/https URL-building proofs"
    - path: "app/src/main/res/xml/network_security_config.xml"
      provides: "Documented LAN-permissive cleartext posture (option B rationale)"
  key_links:
    - from: "app/src/main/java/works/mees/dinghy/config/Profile.kt"
      to: "ConnectionConfig"
      via: "toConnectionConfig() passes useSecure"
      pattern: "useSecure"
    - from: "app/src/main/java/works/mees/dinghy/net/MoonrakerSocket.kt"
      to: "connection-error UX"
      via: "SSLHandshakeException typed at onFailure → existing SocketEvent error path"
      pattern: "SSL"
---

<objective>
R7 network posture (docs/top-down-audit-roadmap.md §R7), owner-blessed OPTION B (CONTEXT
§decisions): `useSecure` Boolean on ConnectionConfig plumbing ws/wss + http/https through the
URL getters, a connection-settings toggle, cert-failure surfaced through the existing
connection-error UX, and the NSC kept LAN-permissive with the decision documented in the file
comment (NSC cannot express RFC-1918 CIDR — RESEARCH Pitfall 5). NO new dependencies. This plan
also FINALIZES OVERNIGHT-REPORT.md as the last plan of the night.

Purpose: hardening before strangers run it — TLS-fronted Moonraker setups become first-class.
Output: scheme plumbing end-to-end (config → profile → socket), toggle UI, cert-failure UX, final report.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/26.5-overnight-hardening-slate-audit-r-packages/26.5-CONTEXT.md
@docs/top-down-audit-roadmap.md
@.planning/phases/26.5-overnight-hardening-slate-audit-r-packages/26.5-RESEARCH.md
@.planning/phases/26.5-overnight-hardening-slate-audit-r-packages/26.5-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: useSecure model + persistence plumbing + tests</name>
  <files>app/src/main/java/works/mees/dinghy/config/ConnectionConfig.kt, app/src/main/java/works/mees/dinghy/config/ConnectionStore.kt, app/src/main/java/works/mees/dinghy/config/Profile.kt, app/src/test/java/works/mees/dinghy/config/ConnectionConfigTest.kt, app/src/test/java/works/mees/dinghy/config/ConnectionStoreTest.kt</files>
  <read_first>
    - docs/top-down-audit-roadmap.md §R7 — scope + acceptance
    - app/src/main/java/works/mees/dinghy/config/ConnectionConfig.kt — current httpBase/wsUrl getters (hardcoded schemes, D-10/D-11 comments), redacting toString
    - app/src/main/java/works/mees/dinghy/config/Profile.kt — PersistedProfile (@Serializable, ignoreUnknownKeys json) + Profile + toConnectionConfig() at ~L90; the distinctUntilChanged churn warning in its KDoc
    - app/src/main/java/works/mees/dinghy/config/ConnectionStore.kt — keys, config flow map, save(), pure sanitize()
    - app/src/test/java/works/mees/dinghy/config/ConnectionStoreTest.kt — existing sanitize + DataStore harness shapes
    - 26.5-PATTERNS.md "ConnectionConfig.kt" / "ConnectionStore.kt" / "ConnectionConfigTest.kt" sections — exact field/getter/test shapes
  </read_first>
  <behavior>
    - Test 1: ConnectionConfig(host, 7125, useSecure=false).wsUrl == "ws://host:7125/websocket"; httpBase == "http://host:7125"
    - Test 2: useSecure=true → "wss://..." and "https://..."
    - Test 3: ConnectionStore.sanitize(..., useSecure=true) round-trips true; useSecure=null defaults false
    - Test 4: PersistedProfile JSON WITHOUT a useSecure key decodes to useSecure=false (old-blob migration safety)
    - Test 5 (regression): two Profiles differing only in theme still produce EQUAL ConnectionConfigs (the distinctUntilChanged contract survives the new field)
  </behavior>
  <action>
    ConnectionConfig: add `val useSecure: Boolean = false`; httpBase/wsUrl become scheme-aware
    (`if (useSecure) "https"/"wss" else "http"/"ws"`); toString includes useSecure while KEEPING
    the apiKey `***` redaction (T-04-01-I); update the D-10/D-11 getter comments to reference R7
    + the toggle. Profile + PersistedProfile: add `val useSecure: Boolean = false` to both
    (kotlinx ignoreUnknownKeys + a defaulted field = old blobs decode safely, mirroring the
    webcamEnabled precedent); `toConnectionConfig()` passes it; extend PersistedProfile.toString
    with useSecure (not secret). ConnectionStore: `KEY_USE_SECURE = booleanPreferencesKey("use_secure")`,
    sanitize() gains `useSecure: Boolean?` (null → false), config-flow map reads it, save()
    persists it. Update sanitize call sites. Write the new ConnectionConfigTest.kt + extend
    ConnectionStoreTest per the behavior table (PATTERNS has the exact cases).
  </action>
  <verify>
    <automated>timeout 900 /mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --tests *ConnectionConfigTest* --tests *ConnectionStoreTest* --no-daemon" | tr -d '\r' | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - ConnectionConfig.kt: `useSecure: Boolean = false`; both getters scheme-switch; toString still contains the `***` apiKey redaction
    - Profile.kt: both data classes carry `useSecure: Boolean = false`; toConnectionConfig passes it
    - ConnectionStore: key literal `"use_secure"`; sanitize(null) → useSecure false
    - All 5 behavior-table cases pass; full pre-existing config test set stays green
  </acceptance_criteria>
  <done>Scheme plumbing model-complete and migration-safe, host-proven.</done>
</task>

<task type="auto">
  <name>Task 2: Connection-editor toggle + cert-failure UX + NSC decision comment</name>
  <files>app/src/main/java/works/mees/dinghy/ui/screen/PrintersScreen.kt, app/src/main/java/works/mees/dinghy/di/AppContainer.kt, app/src/main/java/works/mees/dinghy/net/MoonrakerSocket.kt, app/src/main/res/xml/network_security_config.xml, app/src/main/res/values/strings.xml</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/screen/PrintersScreen.kt — the absorbed Connection editor (~L172-270: host/port/apiKey fields, seeding LaunchedEffect, validators) and which AppContainer intent persists the edited profile
    - app/src/main/java/works/mees/dinghy/di/AppContainer.kt — the profile upsert/save intent the editor calls (extend its signature/profile construction with useSecure)
    - app/src/main/java/works/mees/dinghy/net/MoonrakerSocket.kt — onFailure handler (~L88) and how the typed cause reaches SocketEvent.Closed / the reconnect-status UX
    - app/src/main/res/xml/network_security_config.xml — current base-config + the D-11 deferred-scoping comment
    - 26.5-PATTERNS.md "network_security_config.xml" section — the locked option-B comment text direction
    - 26.5-RESEARCH.md "R7: NSC RFC-1918 Scoping" + Pitfall 5 — why CIDR/domain enumeration is impossible
  </read_first>
  <action>
    1. Connection editor (PrintersScreen): add a "Use HTTPS/WSS" boolean toggle row co-located
       with host/port/apiKey (this IS the connection-settings surface post-Phase-14). Seed from
       `profile?.useSecure ?: false` in the existing seeding LaunchedEffect; persist through the
       SAME AppContainer upsert intent the other fields use (extend the intent/profile
       construction — writeScope path already; never a composition scope). New strings via
       stringResource (e.g. `printers_use_secure` + a one-line sublabel like "wss/https — requires
       a TLS reverse proxy in front of Moonraker"); fsSp() text; token colors; NO new icons
       (icon law — plain toggle row).
    2. Cert-failure UX: in MoonrakerSocket's onFailure typing, recognize
       `javax.net.ssl.SSLHandshakeException` (and SSLPeerUnverifiedException) and carry a
       distinct TLS-trust cause/message (e.g. "TLS certificate not trusted — check the Moonraker
       reverse-proxy certificate") through the EXISTING SocketEvent error path so the current
       connection-error surface displays it. Do NOT add a trust-all/custom TrustManager escape —
       OkHttp's default trust chain is the verifier (ASVS V6/V9). Match how existing causes are
       typed/stringified in that file (read first; if causes are non-resource strings there, a
       literal is acceptable — consistency with the existing path wins).
    3. NSC: keep `<base-config cleartextTrafficPermitted="true"/>` and REPLACE the D-11 comment
       with the decision record per PATTERNS: LAN-permissive is deliberate (NSC cannot express
       RFC-1918 CIDR; domain entries are hostname-suffix only); the useSecure toggle is the
       hardening mechanism; cleartext-to-public-IP exposure is bounded by the user configuring a
       LAN host. Reference §R7 + this plan ID.
  </action>
  <verify>
    <automated>grep -q "useSecure" app/src/main/java/works/mees/dinghy/ui/screen/PrintersScreen.kt && grep -q "SSLHandshakeException" app/src/main/java/works/mees/dinghy/net/MoonrakerSocket.kt && grep -qi "RFC-1918\|rfc1918" app/src/main/res/xml/network_security_config.xml && grep -q "printers_use_secure" app/src/main/res/values/strings.xml && ! grep -rn "TrustManager" app/src/main/java/works/mees/dinghy/</automated>
  </verify>
  <acceptance_criteria>
    - Editor toggle seeds from and persists to the profile's useSecure via the existing AppContainer intent path; new strings in strings.xml; fsSp/token/icon-law conventions honored
    - MoonrakerSocket maps SSLHandshakeException to a distinct user-visible trust message on the existing error path; `grep -rn "TrustManager" app/src/main/java` returns 0 hits (no trust-all bypass)
    - network_security_config.xml still permits LAN cleartext AND its comment names the CIDR limitation, the useSecure mechanism, and §R7
    - No new entries in gradle/libs.versions.toml (zero new dependencies)
  </acceptance_criteria>
  <done>A user can flip one toggle per printer to go wss/https; a bad cert tells them what's wrong; the cleartext posture is a documented decision, not an accident.</done>
</task>

<task type="auto">
  <name>Task 3: Build gate + finalize OVERNIGHT-REPORT.md</name>
  <files>.planning/phases/26.5-overnight-hardening-slate-audit-r-packages/OVERNIGHT-REPORT.md</files>
  <read_first>
    - CLAUDE.md "Local Build Environment"
    - .planning/phases/26.5-overnight-hardening-slate-audit-r-packages/OVERNIGHT-REPORT.md — the skeleton from plan 26.5-01
    - All six prior SUMMARYs in the phase dir — their `## Report Notes` sections are the inputs
  </read_first>
  <action>
    1. Build gate: `timeout 1200 /mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest :app:assembleDebug --no-daemon" | tr -d '\r'`
       — exit 0 authoritative. 3 strikes / ~90 min → revert the R7 code commits, mark failed,
       BUT still complete step 2 (the report must exist even on a failed R7).
    2. Finalize OVERNIGHT-REPORT.md (this is the LAST plan of the night): fill the Per-Package
       Status table from the six prior SUMMARYs (done / failed+blocker / deviations); lift every
       `## Report Notes` section in (R9 archive-script pointer, R10 instrumentation tags, R1
       manifest pre-stage note, R4 site-count correction, codex-review acted/deferred items if
       the orchestrator recorded any); consolidate DECISIONS-NEEDED (LICENSE recommendation from
       26.5-01 + anything discovered overnight); prune or annotate Morning-UAT-checklist items
       belonging to any failed/reverted plan; append the R7 morning items (wss connect against a
       TLS-fronted Moonraker if one is available — best-effort; plain LAN ws:// regression on
       both printers; cert-failure message check against a self-signed endpoint if handy).
  </action>
  <verify>
    <automated>timeout 1200 /mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest :app:assembleDebug --no-daemon" | tr -d '\r' | tail -3 && grep -q "Per-Package Status" .planning/phases/26.5-overnight-hardening-slate-audit-r-packages/OVERNIGHT-REPORT.md && ! grep -q "pending" .planning/phases/26.5-overnight-hardening-slate-audit-r-packages/OVERNIGHT-REPORT.md</automated>
  </verify>
  <acceptance_criteria>
    - Build gate exits 0; `git status --porcelain` clean after final commit
    - Report's status table has NO remaining "pending" rows (every package resolved to done/failed/partial with a one-line note)
    - DECISIONS-NEEDED contains the LICENSE recommendation; Morning UAT Checklist reflects only landed code
  </acceptance_criteria>
  <done>Night closed out: suite green, report complete and self-sufficient for the morning sitting.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 4: Morning UAT — TLS posture (roadmap §R7 acceptance, environmental best-effort)</name>
  <what-built>useSecure scheme plumbing + editor toggle + cert-failure message + documented NSC posture.</what-built>
  <how-to-verify>
    Deferred to morning UAT checklist — verifier records human_needed; do NOT block the wave on this.
    1. Regression first: both printers (192.168.1.120 / .121) still connect over plain ws:// with the toggle OFF.
    2. Toggle ON against a printer with NO TLS proxy → connection fails with a comprehensible error (not a hang/crash); toggle back OFF reconnects.
    3. Best-effort (only if a TLS-fronted Moonraker is available): wss:// connect succeeds; a self-signed cert shows the TLS-trust message.
    Note: the doc's "cleartext to a public IP refused on API 24+" half was descoped to the documented LAN-permissive posture (CONTEXT option B — NSC cannot express CIDR); confirm the NSC comment tells that story.
  </how-to-verify>
  <action>Morning-only verification: do nothing tonight beyond ensuring the items appear on the morning UAT checklist. Verifier records human_needed; the wave proceeds. The TLS-fronted half is environmental best-effort.</action>
  <verify>
    <human-check>Owner runs the plain-ws regression on both printers, the toggle-ON-without-proxy failure check, and (best-effort) the wss/self-signed checks above.</human-check>
  </verify>
  <done>Plain ws:// regression clean on both printers; toggle-ON failure is comprehensible and reversible; wss/cert checks done if a TLS endpoint exists — owner-approved.</done>

  <resume-signal>Type "approved" or describe the connection failure</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app ↔ Moonraker over LAN | Cleartext ws/http by default; wss/https when useSecure — TLS posture is THE surface of this plan |
| TLS handshake → user | Cert validation outcome must reach the user truthfully |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26.5-19 | Information Disclosure | LAN cleartext (NSC permissive) | accept | LAN-only product; documented decision in the NSC comment (option B, CIDR impossible); useSecure toggle offered per printer; revisit if a public-host story ever appears |
| T-26.5-20 | Spoofing | TLS cert validation | mitigate | OkHttp default trust chain ONLY — zero TrustManager overrides (grep-asserted); SSLHandshakeException surfaced with a distinct trust message, never auto-bypassed |
| T-26.5-21 | Information Disclosure | apiKey in logs | mitigate | toString redaction preserved in ConnectionConfig + PersistedProfile (acceptance-asserted); useSecure is non-secret |
| T-26.5-22 | Tampering | old profile blobs | mitigate | Defaulted @Serializable field + ignoreUnknownKeys = old blobs decode to useSecure=false (tested) — no migration corruption path |
| T-26.5-SC | Tampering | package installs | accept | None this plan (NO new dependencies — locked) |
</threat_model>

<verification>
- New config tests green incl. old-blob decode + churn regression
- Grep proofs: no TrustManager, redaction intact, NSC comment, toggle string
- Build gate green; report finalized with no pending rows
</verification>

<success_criteria>
Roadmap §R7 acceptance as overnight-scoped by CONTEXT option B: wss/https plumbing landed and
host-proven end-to-end (config → profile → editor → socket), cert failure surfaced through the
existing UX, NSC posture documented. Live-TLS connect → morning UAT. OVERNIGHT-REPORT.md
finalized as the phase's morning handoff.
</success_criteria>

<output>
Create `.planning/phases/26.5-overnight-hardening-slate-audit-r-packages/26.5-07-SUMMARY.md` when done.
</output>
