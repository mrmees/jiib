---
phase: 08-macros-console-functional-core-complete
plan: 04
type: execute
wave: 2
depends_on: ["08-02", "08-03"]
files_modified:
  - app/src/main/java/works/mees/dinghy/net/JsonRpc.kt
  - app/src/main/java/works/mees/dinghy/command/CommandRegistry.kt
  - app/src/main/java/works/mees/dinghy/net/MoonrakerSession.kt
  - app/src/main/java/works/mees/dinghy/state/PrinterStateStore.kt
  - app/src/main/java/works/mees/dinghy/ui/console/GcodeStoreParse.kt
autonomous: true
requirements: [CONS-02, MACRO-02]

must_haves:
  truths:
    - "On every (re)connect AND on notify_klippy_ready re-handshake, server.gcode_store backfills the console history and REPLACES the scrollback (D-02 Mainsail-parity), recovering disconnect-window lines"
    - "The macro gcode bodies are read from the SAME single configfile query already issued for the extruder (Pitfall 3 — no duplicate configfile query)"
    - "A printer lacking gcode_store/configfile degrades gracefully — the read is best-effort runCatching and never breaks Connected"
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/ui/console/GcodeStoreParse.kt"
      provides: "Pure gcode_store JSON → List<ConsoleLine> walker (CONS-02)"
      contains: "fun parseGcodeStore"
    - path: "app/src/main/java/works/mees/dinghy/state/PrinterStateStore.kt"
      provides: "setGcodeBackfill seam + macro-bodies StateFlow seam"
      contains: "setGcodeBackfill"
    - path: "app/src/main/java/works/mees/dinghy/command/CommandRegistry.kt"
      provides: "gcodeStore CommandSpec registered"
      contains: "gcodeStore"
  key_links:
    - from: "app/src/main/java/works/mees/dinghy/net/MoonrakerSession.kt"
      to: "store.setGcodeBackfill"
      via: "runCatching one-shot read inside runHandshake step 7 (reruns on reconnect/klippy_ready)"
      pattern: "gcodeStore"
    - from: "app/src/main/java/works/mees/dinghy/net/MoonrakerSession.kt"
      to: "the existing single configfile query"
      via: "extended to also extract gcode_macro * sections (no 2nd query)"
      pattern: "configfile"
---

<objective>
The two new backend one-shot reads for CONS-02 (console backfill) and MACRO-02 (macro bodies) — both done exactly like the existing handshake reads, inside `runHandshake()` so they auto-rerun on every reconnect AND on `notify_klippy_ready` re-handshake (Pitfall 4). Add the `server.gcode_store` registry spec + method const + a pure `parseGcodeStore` walker, wire the backfill REPLACE into the store (D-02 Mainsail-parity Option A), and EXTEND the single existing `configfile` query to also pull `gcode_macro *` sections (Pitfall 3 — no duplicate query) so the macro-body StateFlow feeds the parser. Turns the `GcodeStoreParseTest` from 08-01 GREEN.

Purpose: Recover disconnect-window console lines on reconnect (success-criterion #3, the functional-core gate) and supply macro bodies for param detection — both riding the SINGLE existing handshake (no new subscription, project DISCIPLINE per STATE.md).
Output: JsonRpc.kt (+GCODE_STORE), CommandRegistry.kt (+gcodeStore), MoonrakerSession.kt (+backfill read, +macro-section extract), PrinterStateStore.kt (+seams), GcodeStoreParse.kt.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/08-macros-console-functional-core-complete/08-RESEARCH.md
@.planning/phases/08-macros-console-functional-core-complete/08-PATTERNS.md
@app/src/main/java/works/mees/dinghy/net/MoonrakerSession.kt
@app/src/main/java/works/mees/dinghy/state/PrinterStateStore.kt
@app/src/main/java/works/mees/dinghy/command/CommandRegistry.kt
@app/src/main/java/works/mees/dinghy/net/JsonRpc.kt
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: parseGcodeStore walker + gcodeStore registry spec + GCODE_STORE method const (CONS-02)</name>
  <files>app/src/main/java/works/mees/dinghy/ui/console/GcodeStoreParse.kt, app/src/main/java/works/mees/dinghy/net/JsonRpc.kt, app/src/main/java/works/mees/dinghy/command/CommandRegistry.kt</files>
  <read_first>
    - .planning/phases/08-macros-console-functional-core-complete/08-RESEARCH.md (§1 gcode_store shape + 1000-default + parseGcodeStore Code Example + registration note)
    - .planning/phases/08-macros-console-functional-core-complete/08-PATTERNS.md (Registry + methods section: temperatureStore spec template CommandRegistry.kt:110-116, TEMPERATURE_STORE const JsonRpc.kt:112, append to `all` line 326)
    - app/src/main/java/works/mees/dinghy/command/CommandRegistry.kt (temperatureStore spec + objectsQuery + the `all` list)
    - app/src/test/java/works/mees/dinghy/ui/console/GcodeStoreParseTest.kt (the RED test + gcode_store_e5.json fixture)
    - app/src/test/resources/fixtures/gcode_store_e5.json (the real probed shape)
  </read_first>
  <action>
    Add `const val GCODE_STORE = "server.gcode_store"` to `JsonRpcMethods` in JsonRpc.kt beside `TEMPERATURE_STORE` (line 112). Add a `gcodeStore: CommandSpec<GcodeStoreArgs>` to CommandRegistry.kt modeled on `temperatureStore` (catalogId `MR-server.gcode_store` which RESEARCH says is already catalogued `planned_v1`, method `JsonRpcMethods.GCODE_STORE`, key `{ "gcode_store" }`, params building `{"count": <int>}`); define `GcodeStoreArgs(val count: Int)` (a @Serializable args type or a params-builder lambda matching the existing CommandSpec param shape). Append `gcodeStore` to the `all` list (line 326). Create `parseGcodeStore(result: JsonObject): List<ConsoleLine>` in GcodeStoreParse.kt: walk `result["gcode_store"] as? JsonArray`, per-element `mapNotNull` extracting `message` (required → skip if absent), `time` (doubleOrNull), severity via `ConsoleSeverity.classify(message)`; wrap in runCatching → empty on malformed (the "bad field skipped, never fatal" house rule). Mirror the null-safe walkers in MoonrakerSession (objectOrNull/floatOrNullAt). Keep raw prefix in `rawMessage` (D-04 — display layer decides strip-vs-keep).
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testReleaseUnitTest --tests *GcodeStoreParseTest* --no-daemon" 2>&1 | tr -d '\r' | grep -E 'BUILD SUCCESSFUL|BUILD FAILED|FAILED'</automated>
  </verify>
  <acceptance_criteria>
    - grep JsonRpc.kt shows `GCODE_STORE = "server.gcode_store"`; grep CommandRegistry.kt shows `gcodeStore` in both the spec definition and the `all` list.
    - parseGcodeStore over gcode_store_e5.json yields ConsoleLines whose severities match each message's prefix; an entry missing `message` is skipped (not fatal); an empty/garbage store → empty list.
    - GcodeStoreParseTest PASS (GREEN).
  </acceptance_criteria>
  <done>gcode_store registered + parsed; CONS-02 backfill parser GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Handshake backfill read (REPLACE) + extend configfile query for macro bodies + store seams</name>
  <files>app/src/main/java/works/mees/dinghy/net/MoonrakerSession.kt, app/src/main/java/works/mees/dinghy/state/PrinterStateStore.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/net/MoonrakerSession.kt (runHandshake step 7 — the temperature_store + configfile runCatching reads at lines 344-362; the null-safe walkers objectOrNull/floatOrNullAt; the notify_klippy_ready re-handshake at ~217-226)
    - app/src/main/java/works/mees/dinghy/state/PrinterStateStore.kt (setTemperatureBackfill at 165 + gcodeResponses SharedFlow at 57-59 — the seam templates)
    - .planning/phases/08-macros-console-functional-core-complete/08-RESEARCH.md (Pattern 1 backfill; Pitfall 3 extend-not-duplicate; Pitfall 4 reruns-on-reconnect; §1 REPLACE strategy)
    - .planning/phases/08-macros-console-functional-core-complete/08-PATTERNS.md (Handshake read section — the exact runCatching block to copy + the configfile-extend note)
  </read_first>
  <action>
    In PrinterStateStore.kt add two seams next to `setTemperatureBackfill` (line 165): `setGcodeBackfill(lines: List<ConsoleLine>)` exposing a `consoleBackfill` flow/StateFlow the ConsoleHolder collects (a SharedFlow replay-1 OR a StateFlow<List<ConsoleLine>> — choose the StateFlow form for deterministic on-connect fullness, mirroring the temperature-backfill seam), and `setMacroBodies(bodies: Map<String, String>)` exposing a `macroBodies: StateFlow<Map<String,String>>` (macro-name-lowercased → gcode body) the MacroHolder reads. In MoonrakerSession.kt runHandshake step 7: (a) add a NEW best-effort `runCatching { val r = rpc.request(CommandRegistry.gcodeStore, GcodeStoreArgs(count = 1000)); store.setGcodeBackfill(parseGcodeStore(r.jsonObject)) }` — REPLACE semantics (D-02 Mainsail-parity Option A recovers disconnect-window lines automatically; placed inside runHandshake so it reruns on reconnect AND klippy_ready per Pitfall 4). (b) EXTEND the EXISTING `configfile` query runCatching block (the one already reading extruder min_extrude_temp at ~355) to ALSO walk `settings` for every key starting `gcode_macro ` and collect `{lowercased-name → .gcode string}` into `store.setMacroBodies(...)` — do NOT issue a second `objects.query{configfile}` (Pitfall 3). If the 08-01 probe found `.gcode` is an array, join with `\n` here before storing (MacroParamParser also handles it, but store a single string). Use the existing null-safe walkers. Add a focused test (extend SessionTestHarness / a MoonrakerSession handshake test if present) proving the backfill REPLACE fires and macro bodies populate from a faithful configfile fixture; if no host-test seam exists for the session, assert via a small unit over the new store seams + parseGcodeStore wired together.
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testReleaseUnitTest --no-daemon" 2>&1 | tr -d '\r' | grep -E 'BUILD SUCCESSFUL|BUILD FAILED|FAILED'</automated>
  </verify>
  <acceptance_criteria>
    - grep MoonrakerSession.kt shows exactly ONE `objects.query` involving `configfile` (no duplicate — Pitfall 3); the macro-section extraction reads from that same result.
    - grep MoonrakerSession.kt shows the `gcodeStore` read inside `runHandshake` (so it inherits reconnect + klippy_ready reruns).
    - PrinterStateStore exposes `setGcodeBackfill` + `consoleBackfill` and `setMacroBodies` + `macroBodies`.
    - The backfill uses REPLACE (a fresh snapshot supersedes prior contents — proves D-02 disconnect-window recovery), not append.
    - Full `:app:testReleaseUnitTest` BUILD SUCCESSFUL (no regressions; existing 05-03 handshake reads still work).
  </acceptance_criteria>
  <done>Backfill + macro-body reads ride the single handshake; reconnect replace recovers disconnect-window lines (SC #3 backend).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| printer → app (gcode_store + configfile JSON over websocket) | untrusted wire JSON parsed into store seams; walkers are skip-bad-field tolerant |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04-T | Tampering | parseGcodeStore + macro-section walk over wire JSON | mitigate | runCatching + per-element mapNotNull → malformed entry skipped, never fatal; a failed read leaves the seam at empty default (Connected unaffected) |
| T-08-04-A | Availability (DoS) | gcode_store count=1000 read on every reconnect | accept | one-shot best-effort read bounded to 1000 entries, on the SINGLE existing handshake (no new subscription); Phase-13 cadence audit owns further tuning |
| T-08-04-I | Information Disclosure | a failed backfill read | accept | read failure is silent runCatching; no error text embeds the API key/token (reuses redaction discipline) |
| T-08-04-SC | Tampering | npm/pip/cargo installs | accept | zero new packages — composition over existing pinned stack |
</threat_model>

<verification>
- `:app:testReleaseUnitTest` BUILD SUCCESSFUL including GcodeStoreParseTest.
- grep proves single configfile query + gcodeStore-in-handshake + new store seams.
</verification>

<success_criteria>
The console backfill and macro-body reads ride the single existing handshake, rerun on reconnect/klippy_ready, REPLACE the scrollback to recover disconnect-window lines (SC #3), and add no duplicate configfile query — all best-effort and Connected-safe.
</success_criteria>

<output>
Create `.planning/phases/08-macros-console-functional-core-complete/08-04-SUMMARY.md` when done.
</output>
