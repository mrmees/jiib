---
phase: 08-macros-console-functional-core-complete
plan: 02
type: execute
wave: 1
depends_on: ["08-01"]
files_modified:
  - app/src/main/java/works/mees/dinghy/ui/console/ConsoleLine.kt
  - app/src/main/java/works/mees/dinghy/ui/console/ConsoleSeverity.kt
  - app/src/main/java/works/mees/dinghy/ui/console/ConsoleFilters.kt
  - app/src/main/java/works/mees/dinghy/state/ConsoleScrollback.kt
  - app/src/test/java/works/mees/dinghy/state/ConsoleScrollbackTest.kt
autonomous: true
requirements: [CONS-02]

must_haves:
  truths:
    - "A console line is classified into error/warning/normal/action/debug tiers from its raw prefix (D-02)"
    - "The 3 built-in noise filters match exactly the Mainsail fork rules and are applied at the VIEW layer only, never mutating the raw line list (D-03/D-04)"
    - "A bounded object-typed ring evicts oldest at ~1000-line cap with O(1) push (NOT the FloatArray RingBuffer)"
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/ui/console/ConsoleSeverity.kt"
      provides: "Pure prefix→tier classifier (D-02)"
      contains: "fun classify"
    - path: "app/src/main/java/works/mees/dinghy/ui/console/ConsoleFilters.kt"
      provides: "The 3 verbatim Mainsail filter regexes + pure apply (D-03/D-04)"
      contains: "HIDE_TEMPERATURES"
    - path: "app/src/main/java/works/mees/dinghy/state/ConsoleScrollback.kt"
      provides: "Object-typed bounded ring for ConsoleLine (NOT RingBuffer/FloatArray)"
      contains: "ConsoleLine"
  key_links:
    - from: "app/src/main/java/works/mees/dinghy/ui/console/ConsoleFilters.kt"
      to: "raw ConsoleLine list"
      via: "apply returns a filtered VIEW; input list is never mutated (D-04)"
      pattern: "fun apply"
---

<objective>
The pure, headless console layer for CONS-02 — no I/O, no Compose, fully host-testable (mirrors `state/DeriveCapabilities.kt` / `command/PrinterCommands.kt` discipline). Four files: `ConsoleLine` (raw model), `ConsoleSeverity` (prefix→tier classifier, D-02), `ConsoleFilters` (the 3 verbatim Mainsail filter regexes applied at the VIEW layer only, D-03/D-04), and `ConsoleScrollback` (object-typed bounded ring — explicitly NOT the FloatArray `RingBuffer`, RESEARCH Pitfall 1). Turns the `ConsoleSeverityTest`/`ConsoleFiltersTest` scaffolds from 08-01 GREEN.

Purpose: Establish the raw-stream-independent-of-filter architecture (D-04) so the Phase-12 prompt engine and backfill are never starved by what the console hides.
Output: ConsoleLine.kt, ConsoleSeverity.kt, ConsoleFilters.kt, ConsoleScrollback.kt.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/08-macros-console-functional-core-complete/08-RESEARCH.md
@.planning/phases/08-macros-console-functional-core-complete/08-PATTERNS.md
@app/src/main/java/works/mees/dinghy/render/RingBuffer.kt
@app/src/main/java/works/mees/dinghy/state/DeriveCapabilities.kt
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ConsoleLine model + ConsoleSeverity classifier + ConsoleFilters (pure, D-02/D-03/D-04)</name>
  <files>app/src/main/java/works/mees/dinghy/ui/console/ConsoleLine.kt, app/src/main/java/works/mees/dinghy/ui/console/ConsoleSeverity.kt, app/src/main/java/works/mees/dinghy/ui/console/ConsoleFilters.kt</files>
  <read_first>
    - .planning/phases/08-macros-console-functional-core-complete/08-RESEARCH.md (Code Examples: classify() + filter regexes verbatim; §2 severity encoding; D-04 anti-pattern)
    - .planning/phases/08-macros-console-functional-core-complete/08-PATTERNS.md (ConsoleSeverity + ConsoleFilters section — exact code)
    - app/src/main/java/works/mees/dinghy/state/DeriveCapabilities.kt (the pure-function discipline to mirror)
    - app/src/test/java/works/mees/dinghy/ui/console/ConsoleSeverityTest.kt + ConsoleFiltersTest.kt (the RED tests to satisfy)
  </read_first>
  <action>
    Create `ConsoleLine` as a headless immutable data class: `data class ConsoleLine(val rawMessage: String, val severity: ConsoleSeverity, val timeEpoch: Double?)`. Create `ConsoleSeverity` enum {ERROR, WARNING, NORMAL, ACTION, DEBUG} with `fun classify(rawMessage: String): ConsoleSeverity` mapping prefixes EXACTLY per RESEARCH: `"!! "`→ERROR, `"// action:"`→ACTION, `"// debug:"`→DEBUG, `"// "`→WARNING, else NORMAL (robust: absent/empty prefix → NORMAL, never throws). Create `ConsoleFilters` holding the three VERBATIM regexes from the fork @76fcbd2 — `HIDE_TEMPERATURES = Regex("""^(?:ok\s+)?(B|C|T\d*):""")`, `HIDE_TIMELAPSE` (the 6-element list of `_TIMELAPSE_NEW_FRAME`/`TIMELAPSE_TAKE_FRAME`/`TIMELAPSE_RENDER`/`_SET_TIMELAPSE_SETUP`/`HYPERLAPSE ACTION=`/`SET_GCODE_VARIABLE MACRO=TIMELAPSE_`), `HIDE_PROMPT_COMMANDS = Regex("""^(?:// )?action:prompt""")` — plus a pure `fun apply(lines: List<ConsoleLine>, hideTemps: Boolean, hideTimelapse: Boolean, hidePrompt: Boolean): List<ConsoleLine>` that returns a NEW filtered list (a line is hidden if any ACTIVE rule matches its rawMessage); the input list is NEVER mutated (D-04 — filters are reversible toggles, raw must survive toggle-off). No I/O, no Compose imports.
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testReleaseUnitTest --tests *ConsoleSeverityTest* --tests *ConsoleFiltersTest* --no-daemon" 2>&1 | tr -d '\r' | grep -E 'BUILD SUCCESSFUL|BUILD FAILED|FAILED'</automated>
  </verify>
  <acceptance_criteria>
    - `classify("!! out of range")` == ERROR; `classify("// action:prompt_begin")` == ACTION; `classify("// debug:x")` == DEBUG; `classify("// echo: hi")` == WARNING; `classify("ok")` == NORMAL; `classify("")` == NORMAL.
    - `ConsoleFilters.apply` with all flags false returns a list equal to input (no filtering); with hideTemps true drops `ok T0:19.5` but keeps `// echo`; the returned list is a different instance and the input list size is unchanged after the call (D-04).
    - grep `ConsoleFilters.kt` shows the literal regex `^(?:ok\s+)?(B|C|T\d*):` and `^(?:// )?action:prompt` present verbatim.
    - ConsoleSeverityTest + ConsoleFiltersTest PASS (GREEN).
  </acceptance_criteria>
  <done>Pure classifier + filters GREEN; D-04 raw-survives invariant proven.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ConsoleScrollback — object-typed bounded ring (NOT RingBuffer)</name>
  <files>app/src/main/java/works/mees/dinghy/state/ConsoleScrollback.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/render/RingBuffer.kt (copy the @Synchronized O(1)-eviction + defensive snapshot IDIOM only — it is FloatArray-backed and CANNOT hold ConsoleLine, RESEARCH Pitfall 1)
    - .planning/phases/08-macros-console-functional-core-complete/08-PATTERNS.md (No Analog Found section — confirms build-fresh, object-typed)
    - .planning/phases/08-macros-console-functional-core-complete/08-RESEARCH.md (Pitfall 1; D-02 ~1000-line cap aligning 1:1 with Moonraker gcode_store_size default)
  </read_first>
  <action>
    Create `ConsoleScrollback` as a `@Synchronized`-guarded bounded ring of `ConsoleLine` objects with default capacity 1000 (aligns 1:1 with Moonraker's `gcode_store_size` default per RESEARCH §1). Provide: `push(line: ConsoleLine)` (O(1); when over cap, evict oldest — a capped `ArrayDeque<ConsoleLine>` with `removeFirst()` is the simplest correct form per RESEARCH), `replaceAll(lines: List<ConsoleLine>)` (atomic clear+fill capped to capacity, for the backfill replace per D-02/Mainsail-parity Option A), and `snapshot(): List<ConsoleLine>` (defensive copy, oldest→newest). Do NOT touch or extend `render/RingBuffer.kt` (FloatArray — type wall). No Compose, no I/O. Add a unit test `ConsoleScrollbackTest` proving: push past cap evicts oldest (size stays at cap, newest retained); replaceAll caps to capacity keeping the newest; snapshot is a defensive copy (mutating it does not affect the ring); concurrent pushes do not corrupt size.
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testReleaseUnitTest --tests *ConsoleScrollbackTest* --no-daemon" 2>&1 | tr -d '\r' | grep -E 'BUILD SUCCESSFUL|BUILD FAILED|FAILED'</automated>
  </verify>
  <acceptance_criteria>
    - ConsoleScrollback stores `ConsoleLine` (grep shows `ConsoleLine` not `Float`); it does NOT import or subclass `render.RingBuffer`.
    - Pushing 1001 lines into a cap-1000 ring → snapshot size 1000, oldest line evicted, newest present.
    - `replaceAll` of 1500 lines → snapshot size 1000 keeping the last 1000.
    - ConsoleScrollbackTest PASS.
  </acceptance_criteria>
  <done>Object-typed bounded ring GREEN; Pitfall 1 avoided.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| printer text → classifier/filters | untrusted printer response text classified/filtered; pure functions, no eval/markup |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-02-T | Tampering | ConsoleSeverity.classify over untrusted text | mitigate | total function — absent/garbage prefix → NORMAL, never throws; ConsoleSeverityTest covers empty/no-prefix |
| T-08-02-D | DoS | filter regexes on every line | accept | only 3 FIXED built-in regexes (no user-supplied regex this phase — deferred); linear anchored matches, ~1000-line bound |
| T-08-02-SC | Tampering | npm/pip/cargo installs | accept | zero new packages — composition over existing pinned stack |
</threat_model>

<verification>
- `:app:testReleaseUnitTest --tests *Console*` GREEN for Severity/Filters/Scrollback.
- grep confirms verbatim filter regexes and object-typed ring.
</verification>

<success_criteria>
The pure console layer (model, classifier, filters, bounded ring) exists, is host-tested GREEN against the real-shape fixtures, and enforces the D-04 raw-upstream-of-filter architecture.
</success_criteria>

<output>
Create `.planning/phases/08-macros-console-functional-core-complete/08-02-SUMMARY.md` when done.
</output>
