---
phase: 08-macros-console-functional-core-complete
plan: 05
type: execute
wave: 3
depends_on: ["08-02", "08-04"]
files_modified:
  - app/src/main/java/works/mees/dinghy/ui/console/ConsoleHolder.kt
  - app/src/main/java/works/mees/dinghy/ui/console/ConsoleListView.kt
  - app/src/main/java/works/mees/dinghy/ui/console/ConsoleRowsAdapter.kt
  - app/src/main/java/works/mees/dinghy/ui/console/ConsoleScreen.kt
autonomous: true
requirements: [CONS-02]

must_haves:
  truths:
    - "The console shows a read-only severity-colored history (D-01/D-02): error red, warning amber, normal text, backfilled + live"
    - "The 3 opt-in noise filters (default OFF) hide temps/timelapse/prompt at the VIEW layer only; toggling off re-reveals (D-03/D-04)"
    - "The scrollback is a RecyclerView-in-AndroidView pinned-height + clipToBounds surface and auto-sticks to bottom unless scrolled up (D-05)"
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/ui/console/ConsoleHolder.kt"
      provides: "Collects store.gcodeResponses (live) + consoleBackfill (replace) into StateFlow<List<ConsoleLine>> (raw, D-04)"
      contains: "gcodeResponses"
    - path: "app/src/main/java/works/mees/dinghy/ui/console/ConsoleListView.kt"
      provides: "RecyclerView-in-AndroidView severity-colored scrollback (D-05)"
      contains: "clipToBounds"
    - path: "app/src/main/java/works/mees/dinghy/ui/console/ConsoleScreen.kt"
      provides: "Field-only ScreenScaffold console + Gutter filter toggles + Back (read-only, D-01)"
      contains: "ScreenScaffold"
  key_links:
    - from: "app/src/main/java/works/mees/dinghy/ui/console/ConsoleHolder.kt"
      to: "PrinterStateStore.gcodeResponses + consoleBackfill"
      via: "init scope.launch collect — RAW, upstream of filter (D-04)"
      pattern: "collect"
    - from: "app/src/main/java/works/mees/dinghy/ui/console/ConsoleScreen.kt"
      to: "ConsoleFilters.apply"
      via: "filter applied at render only; holder stores raw (D-04)"
      pattern: "ConsoleFilters"
---

<objective>
The read-only Console screen (CONS-02 / D-01..D-05). `ConsoleHolder` collects the ALREADY-WIRED `store.gcodeResponses` SharedFlow (live) and the `consoleBackfill` StateFlow (replace-on-reconnect) into a RAW bounded `StateFlow<List<ConsoleLine>>` (D-04 — filtering is the view's job, holder stores raw). `ConsoleListView` + `ConsoleRowsAdapter` reuse the proven `FileListView` RecyclerView-in-AndroidView pattern VERBATIM (3rd such surface, D-05) with severity colors bridged via a token palette. `ConsoleScreen` is a Field-only `ScreenScaffold` with the 3 opt-in noise filters + a green Back in the Gutter — applying `ConsoleFilters` at RENDER only. Turns `ConsoleHolderTest` GREEN. NO keyboard / no send (D-01).

Purpose: A passive printer-output monitor that survives reconnect with full history and never starves the raw stream the Phase-12 prompt engine taps.
Output: ConsoleHolder.kt, ConsoleListView.kt, ConsoleRowsAdapter.kt, ConsoleScreen.kt. (Nav wiring is 08-07.)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/08-macros-console-functional-core-complete/08-RESEARCH.md
@.planning/phases/08-macros-console-functional-core-complete/08-PATTERNS.md
@.planning/phases/08-macros-console-functional-core-complete/08-UI-SPEC.md
@docs/ui_design/LAYOUT.md
@docs/ui_design/THEMING.md
@app/src/main/java/works/mees/dinghy/ui/files/FileListView.kt
@app/src/main/java/works/mees/dinghy/ui/files/FileRowsAdapter.kt
@app/src/main/java/works/mees/dinghy/ui/files/FilesScreen.kt
@app/src/main/java/works/mees/dinghy/ui/files/FileBrowserHolder.kt
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ConsoleHolder — collect live + backfill into a raw bounded StateFlow (D-02/D-04)</name>
  <files>app/src/main/java/works/mees/dinghy/ui/console/ConsoleHolder.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/files/FileBrowserHolder.kt (the toolkit-agnostic StateFlow holder shape, lines 32-53)
    - app/src/main/java/works/mees/dinghy/state/PrinterStateStore.kt (gcodeResponses SharedFlow 57-59 + consoleBackfill seam from 08-04)
    - app/src/main/java/works/mees/dinghy/state/ConsoleScrollback.kt (the bounded ring from 08-02)
    - .planning/phases/08-macros-console-functional-core-complete/08-PATTERNS.md (ConsoleHolder section — collect-in-init skeleton, D-04 raw-store)
    - .planning/phases/08-macros-console-functional-core-complete/08-RESEARCH.md (§1 backfill REPLACE; §2 already-plumbed live flow; D-04)
  </read_first>
  <action>
    Create `ConsoleHolder(scope, store)` mirroring `FileBrowserHolder`. In `init`: `scope.launch { store.gcodeResponses.collect { line -> appendRaw(line) } }` and `scope.launch { store.consoleBackfill.collect { snapshot -> replaceRaw(snapshot) } }`. Back the buffer with the 08-02 `ConsoleScrollback` ring (cap 1000). `appendRaw(line)` classifies via `ConsoleSeverity.classify(line)` → `ConsoleLine(rawMessage=line, severity, timeEpoch=null)` → ring.push → re-emit `_state` snapshot. `replaceRaw(snapshot)` → ring.replaceAll(snapshot) → re-emit (D-02 Mainsail-parity REPLACE — recovers disconnect-window lines). Expose `state: StateFlow<List<ConsoleLine>>` of the RAW (unfiltered) snapshot — D-04: the holder NEVER filters; the screen applies ConsoleFilters at render. Add `ConsoleHolderTest` proving: a backfill snapshot REPLACES prior content (disconnect-window recovery); live appends accrue after backfill; the ring evicts at cap; the exposed state is the RAW list (a filterable-but-unfiltered stream).
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testReleaseUnitTest --tests *ConsoleHolderTest* --no-daemon" 2>&1 | tr -d '\r' | grep -E 'BUILD SUCCESSFUL|BUILD FAILED|FAILED'</automated>
  </verify>
  <acceptance_criteria>
    - ConsoleHolder collects BOTH `store.gcodeResponses` and `store.consoleBackfill` (grep shows two collects in init).
    - The holder applies NO filter (grep shows no ConsoleFilters import — D-04 raw-store).
    - ConsoleHolderTest: a consoleBackfill emission replaces the buffer (proves SC #3 reconnect recovery); pushing past cap evicts oldest.
    - ConsoleHolderTest PASS (GREEN).
  </acceptance_criteria>
  <done>ConsoleHolder GREEN; raw-upstream-of-filter (D-04) + replace-backfill (D-02) proven.</done>
</task>

<task type="auto">
  <name>Task 2: ConsoleListView + ConsoleRowsAdapter — RecyclerView-in-AndroidView severity rows (D-05)</name>
  <files>app/src/main/java/works/mees/dinghy/ui/console/ConsoleListView.kt, app/src/main/java/works/mees/dinghy/ui/console/ConsoleRowsAdapter.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/files/FileListView.kt (copy VERBATIM: clipToBounds on AndroidView, MATCH_PARENT layout params, itemAnimator=null, the token→argb palette bridge at 35-47)
    - app/src/main/java/works/mees/dinghy/ui/files/FileRowsAdapter.kt (the ListAdapter+DiffUtil template)
    - .planning/phases/08-macros-console-functional-core-complete/08-PATTERNS.md (ConsoleListView load-bearing analog — the 3 pieces + severity palette additions)
    - .planning/phases/08-macros-console-functional-core-complete/08-UI-SPEC.md (Console line: severity color + raw text in Geist Mono, full cell width, no animated insertion; stick-to-bottom)
  </read_first>
  <action>
    Create `ConsoleListView` copying `FileListView` VERBATIM for the load-bearing bits: `modifier.clipToBounds()` on the AndroidView, RecyclerView with `LayoutParams.MATCH_PARENT/MATCH_PARENT`, `LinearLayoutManager`, `setHasFixedSize(true)`, `itemAnimator = null` (Adreno-320, no animated insertion). Extend the token palette with severity colors resolved Composable-side to `.toArgb()`: `error = t.stop.toArgb()`, `warning = t.heat.toArgb()`, `normal = t.text.toArgb()`, `success = t.go.toArgb()`, plus row bg `t.surface`/`t.surface2`, and pass that palette down (Views can't read LocalTokens). Create `ConsoleRowsAdapter` as a `ListAdapter<ConsoleLine, VH>` + DiffUtil; the ViewHolder renders the line text in **Geist Mono** (UI-SPEC mandatory tabular) left-aligned full-width, colored per `ConsoleSeverity` from the palette; ACTION/DEBUG tiers render dimmed (t.text3). Strip the `!! `/`// ` prefix for display but derive severity from the ORIGINAL prefix first (RESEARCH Open-Q2 recommendation); keep rawMessage intact in the model. Implement stick-to-bottom: on `submitRows`, `scrollToPosition(itemCount-1)` ONLY when already at bottom (track via the LayoutManager's last-visible == itemCount-1), else hold position (D-02). No timestamp by default (planner discretion — omit for density).
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:assembleRelease --no-daemon" 2>&1 | tr -d '\r' | grep -E 'BUILD SUCCESSFUL|BUILD FAILED'</automated>
  </verify>
  <acceptance_criteria>
    - grep ConsoleListView.kt shows `clipToBounds`, `MATCH_PARENT`, `itemAnimator = null`, and severity palette entries (`stop`/`heat`/`go`).
    - The adapter colors rows per ConsoleSeverity and uses GeistMono.
    - Stick-to-bottom only auto-scrolls when already at bottom (grep shows a last-visible check, not unconditional scrollToPosition).
    - `:app:assembleRelease` BUILD SUCCESSFUL.
  </acceptance_criteria>
  <done>Console scrollback Views surface built on the proven Files pattern.</done>
</task>

<task type="auto">
  <name>Task 3: ConsoleScreen — Field-only ScreenScaffold + filter toggles + Back (D-01/D-03/D-04)</name>
  <files>app/src/main/java/works/mees/dinghy/ui/console/ConsoleScreen.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/files/FilesScreen.kt (pinned-height host BoxWithConstraints→.height(maxHeight) at 195-205; the gutter Row + intentColor mapping 118-137/418-424; collectAsStateWithLifecycle at 73)
    - app/src/main/java/works/mees/dinghy/designsystem/layout (ScreenScaffold signature — nullable focus/field/gutter slots)
    - .planning/phases/08-macros-console-functional-core-complete/08-UI-SPEC.md (Console Screen section — Field-only, filter toggles active=accent/inactive=outline, Back=green, empty-state copy, no keyboard)
    - app/src/main/java/works/mees/dinghy/ui/console/ConsoleFilters.kt (apply — render-layer only)
  </read_first>
  <action>
    Create `ConsoleScreen(holder, onBack)` using `ScreenScaffold(focus = null, field = {...}, gutter = {...})` (Field-only — UI-SPEC: freed height goes to scrollback). Collect `holder.state.collectAsStateWithLifecycle()` (RAW lines). Hold three local toggle states (hideTemps/hideTimelapse/hidePrompt, default OFF) — apply `ConsoleFilters.apply(rawLines, ...)` at RENDER ONLY (D-04), feeding the filtered list to `ConsoleListView`. Field = `BoxWithConstraints(Modifier.fillMaxWidth().weight(1f)) { ConsoleListView(lines = filtered, palette..., modifier = Modifier.fillMaxWidth().height(maxHeight)) }` (the `.height(maxHeight)` is load-bearing — pins the RecyclerView). Gutter = a Row of three filter toggle controls (`Hide temperatures` / `Hide Timelapse` / `Hide prompt commands`, active edge = `t.accentLine`, inactive = `t.outline`, ≥64dp, DISTINCT glyphs — icon-no-repeat law) + a green **Back** control (`Intent.Go` → `t.go`, safe dismiss, copy `Back`) calling `onBack`. Empty state (no lines): center the UI-SPEC copy `Console is quiet` + body. Backfill-failed state: keep scrollback stable + the UI-SPEC `History unavailable...` copy (do not blank). NO TextField, NO keyboard, NO send button (D-01). Token-pure (no raw Color literals). NOTE: drawer-swipe suppression for this screen is wired in 08-07 (it owns AppShell).
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:assembleRelease --no-daemon" 2>&1 | tr -d '\r' | grep -E 'BUILD SUCCESSFUL|BUILD FAILED'</automated>
  </verify>
  <acceptance_criteria>
    - ConsoleScreen uses `focus = null` (Field-only) and applies `ConsoleFilters.apply` in the render path (filters at view layer, D-04).
    - The three filter toggles + a green Back are in the Gutter with distinct glyphs; active toggle edge = accentLine.
    - NO `TextField`/`keyboard`/send affordance present (grep: zero `TextField`/`BasicTextField`/`onSend`) — read-only (D-01).
    - Empty-state copy `Console is quiet` present verbatim; token-purity grep over the file = zero raw Color literals.
    - `:app:assembleRelease` BUILD SUCCESSFUL.
  </acceptance_criteria>
  <done>Read-only Console screen complete; filters render-only; no keyboard.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| printer response text → console render | untrusted printer text rendered as PLAIN TEXT (Geist Mono) — no markup/HTML interpretation (unlike Mainsail web), no injection surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-05-T | Tampering | rendering untrusted printer text | mitigate | render as plain text in a Views TextView (Geist Mono) — no HTML/markup surface; the Phase-12 prompt protocol is the controlled exception, not this read-only phase |
| T-08-05-I | Information Disclosure | console shows raw printer output | accept | LAN-trusted printer; read-only monitor by design (D-01); no secrets in gcode responses |
| T-08-05-SC | Tampering | npm/pip/cargo installs | accept | zero new packages — composition over existing pinned stack |
</threat_model>

<verification>
- `:app:testReleaseUnitTest --tests *ConsoleHolderTest*` GREEN.
- `:app:assembleRelease` BUILD SUCCESSFUL.
- grep: no keyboard/TextField (D-01); filters applied at render (D-04); clipToBounds + pinned height (D-05).
</verification>

<success_criteria>
A read-only, severity-colored, reconnect-resilient Console screen on the proven Views-in-Compose scroll pattern, with the 3 opt-in filters applied at the view layer only and no keyboard — CONS-02 UI complete.
</success_criteria>

<output>
Create `.planning/phases/08-macros-console-functional-core-complete/08-05-SUMMARY.md` when done.
</output>
