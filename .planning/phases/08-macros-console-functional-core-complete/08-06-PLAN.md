---
phase: 08-macros-console-functional-core-complete
plan: 06
type: execute
wave: 3
depends_on: ["08-03", "08-04"]
files_modified:
  - app/src/main/java/works/mees/dinghy/ui/macros/MacroHolder.kt
  - app/src/main/java/works/mees/dinghy/ui/macros/BookmarkedMacrosScreen.kt
  - app/src/main/java/works/mees/dinghy/ui/macros/SystemMacrosScreen.kt
  - app/src/main/java/works/mees/dinghy/ui/macros/MacroExecutionPopup.kt
autonomous: true
requirements: [MACRO-01, MACRO-02, MACRO-03]

must_haves:
  truths:
    - "The Bookmarked launcher shows ONLY user-selected macros and the System list shows ALL macros with check/select, underscore-hidden by default + reveal toggle (D-06/D-07/MACRO-03)"
    - "Tapping a macro opens an Execution popup with auto-detected param fields (numeric→NumpadPage, string→system keyboard) and Execute/Cancel; the popup IS the action gate (D-08/D-09/D-10)"
    - "Execute dispatches a SANITIZED macro invocation via CommandDispatcher GCODE_SCRIPT with a per-macro busy key; a rejection surfaces as a SeverityToast (MACRO-01)"
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/ui/macros/MacroHolder.kt"
      provides: "combine(Capabilities.macros, MacroPrefs, macroBodies) → StateFlow<MacroVm list> (MACRO-01/03)"
      contains: "macros"
    - path: "app/src/main/java/works/mees/dinghy/ui/macros/MacroExecutionPopup.kt"
      provides: "Param-entry popup = action gate; Execute→sanitized dispatch (D-08/D-09/D-10)"
      contains: "NumpadPage"
    - path: "app/src/main/java/works/mees/dinghy/ui/macros/SystemMacrosScreen.kt"
      provides: "ALL-macros check/select + underscore-default-hide + reveal toggle (D-06/MACRO-03)"
      contains: "Show hidden"
  key_links:
    - from: "app/src/main/java/works/mees/dinghy/ui/macros/MacroExecutionPopup.kt"
      to: "command/MacroInvocation.build + PrinterCommands.scriptParams"
      via: "Execute → build(sanitized) → scriptParams → dispatcher.dispatch(GCODE_SCRIPT)"
      pattern: "MacroInvocation"
    - from: "app/src/main/java/works/mees/dinghy/ui/macros/MacroHolder.kt"
      to: "Capabilities.macros + MacroPrefs + store.macroBodies"
      via: "combine into bookmarked/system view models"
      pattern: "combine"
---

<objective>
The three Macro screens (MACRO-01/02/03 / D-06..D-10). `MacroHolder` combines `Capabilities.macros` (already derived), `MacroPrefs` (bookmarks/reveal), and `store.macroBodies` (parsed per-macro params) into the view models. `BookmarkedMacrosScreen` is the everyday launcher (only selected macros). `SystemMacrosScreen` is the ALL-macros check/select manager (underscore-default-hide + reveal, MACRO-03). `MacroExecutionPopup` IS the deliberate action gate (D-08 — no separate ConfirmGuard) with auto-detected param fields (numeric→`NumpadPage`, string→system keyboard, D-09/D-10) and Execute→SANITIZED dispatch via the existing `CommandDispatcher` path. Turns `MacroHolderTest` GREEN.

Purpose: Run gcode_macros with parameter entry from a touch surface — the escape hatch that means no SSH/browser for one-off macros — capability-gated, with the one sanctioned alpha-keyboard site fully sanitized.
Output: MacroHolder.kt, BookmarkedMacrosScreen.kt, SystemMacrosScreen.kt, MacroExecutionPopup.kt. (Nav wiring is 08-07.)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/08-macros-console-functional-core-complete/08-RESEARCH.md
@.planning/phases/08-macros-console-functional-core-complete/08-PATTERNS.md
@.planning/phases/08-macros-console-functional-core-complete/08-UI-SPEC.md
@docs/ui_design/THEMING.md
@app/src/main/java/works/mees/dinghy/ui/files/FileBrowserHolder.kt
@app/src/main/java/works/mees/dinghy/ui/files/FilesScreen.kt
@app/src/main/java/works/mees/dinghy/designsystem/NumpadPage.kt
@app/src/main/java/works/mees/dinghy/state/Capabilities.kt
@app/src/main/java/works/mees/dinghy/command/CommandDispatcher.kt
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MacroHolder — combine capabilities + prefs + parsed bodies; underscore-hide (MACRO-01/03)</name>
  <files>app/src/main/java/works/mees/dinghy/ui/macros/MacroHolder.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/files/FileBrowserHolder.kt (the StateFlow holder shape)
    - app/src/main/java/works/mees/dinghy/state/Capabilities.kt (macros field at ~27/49 + hasMacroIgnoreCase lowercase quirk)
    - app/src/main/java/works/mees/dinghy/ui/macros/MacroPrefs.kt (08-03 bookmarks/revealHidden)
    - app/src/main/java/works/mees/dinghy/ui/macros/MacroParamParser.kt (08-03 parser) + state/PrinterStateStore.kt macroBodies seam (08-04)
    - .planning/phases/08-macros-console-functional-core-complete/08-PATTERNS.md (MacroHolder section — combine + capability gating + underscore render-time filter)
    - app/src/test/java/works/mees/dinghy/ui/macros/MacroHolderTest.kt (the RED scaffold pre-created in 08-01 Task 2 — this task turns it GREEN)
  </read_first>
  <action>
    Create `MacroHolder(scope, printerState/capabilities, prefs: MacroPrefs, macroBodies: StateFlow<Map<String,String>>)`. `combine` the capability macro list (`Capabilities.macros` — the NAME of each `gcode_macro NAME`), `prefs` (bookmarks Set + revealHidden), and `macroBodies` (→ `MacroParamParser.parseMacroParams(body)` per macro) into a `StateFlow<MacroScreensVm>` exposing: `bookmarked: List<MacroVm>` (ONLY names in prefs.bookmarks — D-07 launcher), `system: List<MacroVm>` (ALL macros; underscore-prefixed `name.startsWith("_")` EXCLUDED unless prefs.revealHidden — D-06/MACRO-03), and per-macro `params` from the parser. Feature gate: if `Capabilities.macros.isEmpty()` expose an `unavailable` flag (the screens show the no-macros copy — not dead tiles). Use `hasMacroIgnoreCase` semantics for matching bodies (Moonraker lowercases names). Expose intents: `toggleBookmark(name)`, `setRevealHidden(bool)` (delegate to prefs). This turns the pre-existing `MacroHolderTest` RED scaffold (from 08-01 Task 2) GREEN — it proves: ALL macros enumerated; underscore macros hidden by default and revealed when revealHidden=true (MACRO-03); bookmarked list = only selected; params populate from a real probed body; empty capability → unavailable. Do NOT recreate the test file; if its stub needs a fake-store/prefs shape adjustment to compile against the now-real MacroHolder, edit it in place.
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testReleaseUnitTest --tests *MacroHolderTest* --no-daemon" 2>&1 | tr -d '\r' | grep -E 'BUILD SUCCESSFUL|BUILD FAILED|FAILED'</automated>
  </verify>
  <acceptance_criteria>
    - System list excludes `_`-prefixed macros when revealHidden=false and includes them when true (MACRO-03).
    - Bookmarked list contains only names in prefs.bookmarks (D-07).
    - A macro with a real probed body exposes its parsed params; empty Capabilities.macros → unavailable flag set.
    - MacroHolderTest PASS (GREEN).
  </acceptance_criteria>
  <done>MacroHolder GREEN; underscore-default-hide + bookmark partition + param wiring proven.</done>
</task>

<task type="auto">
  <name>Task 2: MacroExecutionPopup — action-gate popup, NumpadPage/keyboard param entry, sanitized dispatch (D-08/D-09/D-10, MACRO-01/02)</name>
  <files>app/src/main/java/works/mees/dinghy/ui/macros/MacroExecutionPopup.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/designsystem/NumpadPage.kt (signature: label/initial/range/unit/onCommit/onCancel — the numeric clamp source)
    - app/src/main/java/works/mees/dinghy/command/CommandDispatcher.kt (dispatch(key, method, params): pending/debounce/120s timeout/redacted Failure)
    - app/src/main/java/works/mees/dinghy/command/MacroInvocation.kt (08-03 sanitizer — build())
    - app/src/main/java/works/mees/dinghy/command/PrinterCommands.kt (scriptParams wrap)
    - .planning/phases/08-macros-console-functional-core-complete/08-PATTERNS.md (MacroExecutionPopup section — popup IS the gate, no ConfirmGuard; FileActionControl gutter template; dispatch pattern)
    - .planning/phases/08-macros-console-functional-core-complete/08-UI-SPEC.md (Execution popup: --r-card 22px, Execute=accent/Cancel=go, param widget triage, pending state, copy `Run {MACRO_NAME}`)
  </read_first>
  <action>
    Create `MacroExecutionPopup(macro: MacroVm, dispatcher, onDismiss)` as a full-screen overlay surface (`--r-card` 22px) — it IS the deliberate action gate, DO NOT layer a ConfirmGuard on top (D-08). Title `Run {MACRO_NAME}` (Geist Mono prominent). Render one entry field per detected `MacroParam`, pre-filled with the parsed `|default(...)` where present: a param whose `type` is `int`/`double` → route to `NumpadPage` (keyboard-free numeric, clamped via a sane `range`) — **NumpadPage is the SOLE owner of numeric range-clamping (S4)**: the value handed to `MacroInvocation.build` is ALREADY clamped by NumpadPage's `range`; `MacroInvocation` does NOT re-clamp (it only string-validates + formats, 08-03); a param whose `type` is `string`/null → the on-screen SYSTEM keyboard (this popup is the ONE sanctioned alpha-keyboard site in printer controls — D-10). A param with a default and no user edit sends the default; an unclassified param (D-09 heuristic miss) renders a generic field and NEVER blocks Execute. Gutter: **Execute** (`Intent.Accent` → `t.accentLine`; promote to `Intent.Warn`/`t.heat` only if the macro is statically known to drive heat/motion — planner's call, default accent) and **Cancel** (`Intent.Go` → `t.go`, safe dismiss). Use the `FilesScreen` `FileActionControl` gutter-control template (≥64dp, 2px outline, intent color). Execute → `val gcode = MacroInvocation.build(macro.name, collectedParams)` (SANITIZED — 08-03) → `dispatcher.dispatch(key = "macro_${macro.name}", method = JsonRpcMethods.GCODE_SCRIPT, params = PrinterCommands.scriptParams(gcode))`. NEVER pass raw keyboard text to scriptParams (route through MacroInvocation.build). Show the bounded in-flight/disabled state on Execute from `dispatcher.inFlight` keyed `macro_<name>` (PRIM-05); a `!!` rejection arrives as `DispatchEvent.Failure` → SeverityToast (UI-SPEC copy `{MACRO_NAME} was rejected: {printer message}`) AND in console history. Token-pure.
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:assembleRelease --no-daemon" 2>&1 | tr -d '\r' | grep -E 'BUILD SUCCESSFUL|BUILD FAILED'</automated>
  </verify>
  <acceptance_criteria>
    - Execute routes through `MacroInvocation.build(...)` then `PrinterCommands.scriptParams(...)` then `dispatcher.dispatch(...)` — grep shows raw param strings are NEVER passed to scriptParams directly (the sanitizer is between).
    - The per-macro busy key is `macro_<name>` and Execute disables on in-flight (PRIM-05).
    - Numeric params route to NumpadPage; string params use the system keyboard; NO ConfirmGuard wraps the popup (D-08).
    - Numeric range-clamping is performed by NumpadPage's `range` (the SOLE clamp owner, S4) — the value passed to `MacroInvocation.build` is already clamped; grep shows no clamp logic in MacroInvocation or this popup beyond NumpadPage's `range`.
    - Popup title copy `Run {MACRO_NAME}`; Execute=accent, Cancel=go; token-purity grep = zero raw Color literals.
    - `:app:assembleRelease` BUILD SUCCESSFUL.
  </acceptance_criteria>
  <done>Execution popup = action gate; sanitized dispatch; param-widget triage per D-10.</done>
</task>

<task type="auto">
  <name>Task 3: BookmarkedMacrosScreen + SystemMacrosScreen — launcher + manage-visibility (D-06/D-07, MACRO-01/03)</name>
  <files>app/src/main/java/works/mees/dinghy/ui/macros/BookmarkedMacrosScreen.kt, app/src/main/java/works/mees/dinghy/ui/macros/SystemMacrosScreen.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/files/FilesScreen.kt (ScreenScaffold + scroll-Field + gutter controls + collectAsStateWithLifecycle)
    - .planning/phases/08-macros-console-functional-core-complete/08-UI-SPEC.md (Bookmarked launcher + System list sections: tiles, Manage control, Show-hidden toggle, empty states, accent selected-state, copy table)
    - .planning/phases/08-macros-console-functional-core-complete/08-PATTERNS.md (MacroHolder/screens role-match; suppress drawer swipe on a finger-scrollable System list — wired in 08-07)
    - app/src/main/java/works/mees/dinghy/ui/macros/MacroHolder.kt (Task 1 — the view models + intents)
  </read_first>
  <action>
    Create `BookmarkedMacrosScreen(holder, onRunMacro, onManage, onBack)` — the everyday launcher (D-07, the drawer Macros tile opens THIS). Field = a grid/list of tappable `OutlinedControl` tiles for ONLY `holder.state.bookmarked`; tapping a tile → `onRunMacro(macroVm)` (opens MacroExecutionPopup). Gutter: a `Manage macros` control → `onManage` (opens the System list) + green **Back**. Empty state (no bookmarks): UI-SPEC copy `No macros pinned` + body + a route to Manage. Macros-unavailable (holder.unavailable): UI-SPEC copy `This printer reports no gcode macros...` (no dead tiles). Create `SystemMacrosScreen(holder, onBack)` — the ALL-macros manager (D-06/MACRO-03). Field = a scrollable list of every macro in `holder.state.system`, each row = name (Geist Mono) + a check/select control toggling `holder.toggleBookmark(name)` (selected = `--accent` checkmark/outline). A `Show hidden` toggle in the Gutter drives `holder.setRevealHidden(...)` (underscore helpers hidden by default — UI-SPEC intro copy `Underscore-prefixed helper macros are hidden...`). Gutter also carries green **Back**. Both screens token-pure, ≥64dp targets, distinct glyphs. (Drawer-swipe suppression on the scrollable System list is wired in 08-07.)
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:assembleRelease --no-daemon" 2>&1 | tr -d '\r' | grep -E 'BUILD SUCCESSFUL|BUILD FAILED'</automated>
  </verify>
  <acceptance_criteria>
    - Bookmarked screen renders ONLY bookmarked macros + a `Manage macros` control + Back; empty-state copy `No macros pinned` present.
    - System screen lists ALL macros with a per-row check/select (toggleBookmark) + a `Show hidden` toggle (setRevealHidden); selected state = accent.
    - Macros-unavailable copy present; no dead tiles when capability absent.
    - `:app:assembleRelease` BUILD SUCCESSFUL; token-purity grep = zero raw Color literals across both files.
  </acceptance_criteria>
  <done>Three macro screens complete (launcher + manager + popup wired); MACRO-01/02/03 UI done.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user keyboard → macro string param → dispatched gcode | THE phase boundary (D-10); neutralized by MacroInvocation.build (08-03) before dispatch |
| printer rejection text → SeverityToast | rejection message shown; composed from method/key only (redacted), never the API key |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-06-T1 | Tampering / Elevation | Execute dispatch of a user-param macro | mitigate (block_on:high) | Execute routes raw param text through MacroInvocation.build sanitizer (08-03, MacroInvocationTest) BEFORE scriptParams — no raw string reaches the script |
| T-08-06-T2 | Tampering | numeric param out-of-range | mitigate | NumpadPage `range` is the SOLE numeric clamp owner (S4) — clamps before build() formats; MacroInvocation.build does NOT clamp |
| T-08-06-I | Information Disclosure | macro-rejected SeverityToast | mitigate | DispatchEvent.Failure composed from method/key only (existing redaction, V7) — never embeds the API key/token |
| T-08-06-SC | Tampering | npm/pip/cargo installs | accept | zero new packages — composition over existing pinned stack |
</threat_model>

<verification>
- `:app:testReleaseUnitTest --tests *MacroHolderTest*` GREEN.
- `:app:assembleRelease` BUILD SUCCESSFUL.
- grep proves Execute → MacroInvocation.build → scriptParams → dispatch (sanitizer in the path); underscore-default-hide; no ConfirmGuard on the popup.
</verification>

<success_criteria>
Three macro screens deliver list/run/manage with auto-detected param entry, the popup as the action gate, capability gating, and SANITIZED dispatch — MACRO-01/02/03 UI complete and the V5 keyboard site contained.
</success_criteria>

<output>
Create `.planning/phases/08-macros-console-functional-core-complete/08-06-SUMMARY.md` when done.
</output>
