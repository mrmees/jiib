---
phase: 08-macros-console-functional-core-complete
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - docs/moonraker-capabilities.md
  - app/src/test/java/works/mees/dinghy/ui/macros/MacroParamParserTest.kt
  - app/src/test/java/works/mees/dinghy/command/MacroInvocationTest.kt
  - app/src/test/java/works/mees/dinghy/ui/console/ConsoleSeverityTest.kt
  - app/src/test/java/works/mees/dinghy/ui/console/ConsoleFiltersTest.kt
  - app/src/test/java/works/mees/dinghy/ui/console/GcodeStoreParseTest.kt
  - app/src/test/java/works/mees/dinghy/ui/console/ConsoleHolderTest.kt
  - app/src/test/java/works/mees/dinghy/ui/macros/MacroHolderTest.kt
  - app/src/test/java/works/mees/dinghy/ui/macros/MacroPrefsTest.kt
  - app/src/test/resources/fixtures/gcode_store_e5.json
  - app/src/test/resources/fixtures/macro_bodies_e5.json
autonomous: false
requirements: [CONS-02, MACRO-02]
user_setup: []

must_haves:
  truths:
    - "The exact server.gcode_store response shape from the real Ender 5 is recorded in docs/moonraker-capabilities.md"
    - "The exact configfile.settings[\"gcode_macro <name>\"].gcode JSON nesting (string vs array) from the real Ender 5 is recorded in docs/moonraker-capabilities.md"
    - "RED test scaffolds exist for the pure parsers/classifiers AND the holders/prefs and reference REAL probed fixtures, not invented strings"
  artifacts:
    - path: "docs/moonraker-capabilities.md"
      provides: "Recorded live gcode_store + configfile-macro-body shapes (mock-vs-reality gate)"
      contains: "gcode_store"
    - path: "app/src/test/resources/fixtures/macro_bodies_e5.json"
      provides: "Real probed macro gcode bodies used as parser fixtures"
    - path: "app/src/test/resources/fixtures/gcode_store_e5.json"
      provides: "Real probed gcode_store snapshot used as parse fixtures"
  key_links:
    - from: "docs/moonraker-capabilities.md"
      to: "app/src/test/resources/fixtures/macro_bodies_e5.json"
      via: "the recorded live shape IS the fixture shape (no invented strings)"
      pattern: "gcode_macro"
---

<objective>
Wave-0 mock-vs-reality gate. Before any console/macro parsing code is written, confirm against the LIVE Ender 5 Plus (`192.168.1.120:7125`) the two API shapes RESEARCH flagged as the one low-risk unconfirmed item (Assumptions A1/A4, Pitfall 6): the `server.gcode_store` response shape and the `configfile.settings["gcode_macro <name>"].gcode` JSON nesting (single newline-joined string vs. array). Record both in `docs/moonraker-capabilities.md`, then create the RED test scaffolds whose fixtures are the REAL probed shapes — NOT invented strings. This is the exact gap class that bit Phases 2 and 5 (green unit suites hiding live protocol bugs).

Purpose: Establish ground-truth fixtures so every downstream pure parser is tested against reality, satisfying the Nyquist gate before implementation. ALL eight Wave-0 RED test stubs (the five pure parsers/classifiers + the three holders/prefs) are created here so they are the single RED guard wave for the phase (project-consistent — every Nyquist stub lands in 08-01), turned GREEN by the downstream implementation tasks.
Output: Recorded capability shapes + probed fixture files + RED test scaffolds for MacroParamParser, MacroInvocation, ConsoleSeverity, ConsoleFilters, GcodeStoreParse, ConsoleHolder, MacroHolder, MacroPrefs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-macros-console-functional-core-complete/08-RESEARCH.md
@.planning/phases/08-macros-console-functional-core-complete/08-CONTEXT.md
@docs/moonraker-capabilities.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Live read-only probe of gcode_store + configfile macro-body shape (mock-vs-reality gate)</name>
  <read_first>
    - .planning/phases/08-macros-console-functional-core-complete/08-RESEARCH.md (Assumptions A1/A4, Pitfall 6, §1 + §3 — the confirmed-but-unprobed shapes)
    - docs/moonraker-capabilities.md (the authoritative real-printer catalog this appends to)
  </read_first>
  <what-built>
    No code yet — this is a read-only data-gathering probe against the live printer. The executor proposes the two curl commands; Matthew (or the executor if the printer is reachable from the build host) runs them and pastes the JSON back.
  </what-built>
  <how-to-verify>
    1. Confirm Ender 5 reachable: `curl -s http://192.168.1.120:7125/printer/info` returns Klippy info.
    2. Probe gcode_store shape (REST mirror of the RPC): `curl -s "http://192.168.1.120:7125/server/gcode_store?count=20" | python3 -m json.tool` — confirm the `{gcode_store:[{message,time,type}]}` shape and the `type` enum values actually returned (expect "command"/"response").
    3. Probe the macro-body nesting: `curl -s "http://192.168.1.120:7125/printer/objects/query?configfile" | python3 -m json.tool | grep -A4 '"gcode_macro'` — confirm whether `settings["gcode_macro <name>"].gcode` is a single newline-joined STRING or an ARRAY, and confirm keys are lowercased. Capture at least 2-3 real macro bodies that DECLARE params (e.g. a START_PRINT / LOAD_FILAMENT with `params.X` + `|default(...)`).
    4. Record findings into docs/moonraker-capabilities.md under a new "## Phase 8 — gcode_store + macro-body shapes (probed YYYY-MM-DD)" section: the gcode_store entry shape, the observed `type` values, and the macro `.gcode` nesting verdict (string vs array). Paste the 2-3 real macro bodies verbatim.
    5. Save the raw probed JSON into app/src/test/resources/fixtures/gcode_store_e5.json and app/src/test/resources/fixtures/macro_bodies_e5.json — these become the test fixtures (Pitfall 6: fixtures from REAL bodies, not invented).
  </how-to-verify>
  <acceptance_criteria>
    - docs/moonraker-capabilities.md contains a dated Phase-8 section recording: the gcode_store entry shape `{message,time,type}`, the actual `type` enum values observed, and an explicit verdict "macro .gcode is a {STRING|ARRAY}".
    - app/src/test/resources/fixtures/gcode_store_e5.json and macro_bodies_e5.json exist and contain real probed JSON (at least 2 macro bodies that declare `params.X`).
    - If the live verdict CONTRADICTS RESEARCH A1 (.gcode is an array, not a string): note it explicitly so the parser in 08-03 splits on the real shape before regex.
  </acceptance_criteria>
  <resume-signal>Paste the probed shapes / confirm recorded into docs + fixtures, or report "printer unreachable" to fall back to the synthetic GoldenFixtures precedent (and flag the live gate as still-open for the phase UAT).</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RED test scaffolds for the pure console/macro layer + the holders/prefs (fixtures = real probed shapes)</name>
  <files>app/src/test/java/works/mees/dinghy/ui/macros/MacroParamParserTest.kt, app/src/test/java/works/mees/dinghy/command/MacroInvocationTest.kt, app/src/test/java/works/mees/dinghy/ui/console/ConsoleSeverityTest.kt, app/src/test/java/works/mees/dinghy/ui/console/ConsoleFiltersTest.kt, app/src/test/java/works/mees/dinghy/ui/console/GcodeStoreParseTest.kt, app/src/test/java/works/mees/dinghy/ui/console/ConsoleHolderTest.kt, app/src/test/java/works/mees/dinghy/ui/macros/MacroHolderTest.kt, app/src/test/java/works/mees/dinghy/ui/macros/MacroPrefsTest.kt</files>
  <read_first>
    - .planning/phases/08-macros-console-functional-core-complete/08-RESEARCH.md (Code Examples: exact PARAM_REGEX, filter regexes, classify(), parseGcodeStore; Validation Architecture → Test Map; Wave 0 Gaps lists all 8 stubs incl. ConsoleHolder/MacroHolder/MacroPrefs)
    - .planning/phases/08-macros-console-functional-core-complete/08-PATTERNS.md (pure-function discipline analog: state/DeriveCapabilities.kt, command/PrinterCommands.kt:7-23; FileBrowserHolder holder shape for the holder stubs)
    - app/src/test/resources/fixtures/macro_bodies_e5.json and gcode_store_e5.json (the real shapes from Task 1)
  </read_first>
  <behavior>
    - MacroParamParserTest: feed REAL probed macro bodies → expect the param names/types/defaults the body actually declares (e.g. a body with `params.BED|default(60)` → MacroParam("BED", null|int, "60")); a no-param macro → empty list; the `'X' in params` guard form → param with null type/default.
    - MacroInvocationTest (SECURITY, block_on:high): a string param value containing `\n` or `M112` or a second gcode token → the assembled gcode line MUST NOT contain a line break or the injected command; a clean value → assembled as `MACRO KEY="value"`; numeric param → clamped to range before formatting.
    - ConsoleSeverityTest: `!! x`→ERROR, `// action:x`→ACTION, `// debug:x`→DEBUG, `// x`→WARNING, plain→NORMAL, empty/no-prefix→NORMAL (never throws).
    - ConsoleFiltersTest: HIDE_TEMPERATURES matches `ok T:`/`B:`/`T0:`; HIDE_TIMELAPSE matches the 6 rule prefixes; HIDE_PROMPT_COMMANDS matches both `// action:prompt` and `action:prompt`; a normal line matches none; filters NEVER mutate the input list (D-04 raw-survives).
    - GcodeStoreParseTest: the real gcode_store fixture → List<ConsoleLine> with severities derived from each message prefix; a malformed entry (missing message) → skipped, not fatal; empty store → empty list.
    - ConsoleHolderTest: a backfill snapshot REPLACES prior content (disconnect-window recovery, D-02); live appends accrue after backfill; the ring evicts at the ~1000 cap; the exposed state is the RAW (unfiltered) list (D-04). Uses `runTest` + a TestScope and a fake/stub PrinterStateStore exposing `gcodeResponses` + `consoleBackfill`.
    - MacroHolderTest: ALL macros enumerated; underscore (`_`-prefixed) macros hidden when revealHidden=false and revealed when true (MACRO-03); bookmarked list = only selected names; params populate from a real probed body; empty Capabilities.macros → `unavailable` flag set.
    - MacroPrefsTest: a bookmark add then read round-trips; revealHidden persists; a read error path yields defaults (never throws). (Per STATE.md 04-01: back-to-back DataStore writes aren't reliably host-testable on the Windows build host — prove the single-write + read path like ConnectionStore; defer multi-write atomicity to on-device.)
  </behavior>
  <action>
    Create the eight test classes as JUnit 4 host tests under app/src/test (release unit test source set). They reference the not-yet-created production symbols `works.mees.dinghy.ui.macros.MacroParamParser.parseMacroParams`, `works.mees.dinghy.command.MacroInvocation.build`, `works.mees.dinghy.ui.console.ConsoleSeverity.classify`, `works.mees.dinghy.ui.console.ConsoleFilters` (HIDE_TEMPERATURES/HIDE_TIMELAPSE/HIDE_PROMPT_COMMANDS + apply), `works.mees.dinghy.ui.console.parseGcodeStore`, `works.mees.dinghy.ui.console.ConsoleHolder`, `works.mees.dinghy.ui.macros.MacroHolder`, and `works.mees.dinghy.ui.macros.MacroPrefs` — so they FAIL TO COMPILE (RED) until the implementation tasks land them (ConsoleSeverity/ConsoleFilters → 08-02 T1; ConsoleScrollback ring → 08-02 T2; MacroParamParser/MacroInvocation/MacroPrefs → 08-03; parseGcodeStore + backfill seam → 08-04; ConsoleHolder → 08-05 T1; MacroHolder → 08-06 T1). Load fixtures via `javaClass.getResource("/fixtures/...json")`. Use the EXACT regexes from RESEARCH Code Examples verbatim in the assertions' expected values. Do NOT invent macro bodies — load from macro_bodies_e5.json. For the holder stubs use `kotlinx-coroutines-test` `runTest` + a stub store/prefs (no live I/O). Commit as the RED guard wave (Nyquist: tests-before-impl).
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testReleaseUnitTest --no-daemon" 2>&1 | tr -d '\r' | grep -Ei 'unresolved reference|compilation error|cannot access' </automated>
  </verify>
  <acceptance_criteria>
    - The 8 test files exist and reference the production symbols listed in `<action>` (5 pure-layer + ConsoleHolder/MacroHolder/MacroPrefs).
    - The test compile FAILS with unresolved references to the not-yet-built production classes (RED state confirmed) — OR, if a stub is added to make them compile, the tests FAIL at runtime (red), never pass against absent logic.
    - MacroParamParserTest and GcodeStoreParseTest load their inputs from the `/fixtures/*_e5.json` resources (grep the test source for `getResource("/fixtures/`), NOT inline invented bodies.
  </acceptance_criteria>
  <done>Eight RED test scaffolds committed (5 pure + 3 holders/prefs); fixtures are the real probed shapes; downstream plans turn them GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| printer → app (gcode_store / configfile JSON) | untrusted printer text crosses here; parsers must be skip-bad-field tolerant |
| build host → printer (probe curl) | read-only GET only; no writes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01-T | Tampering | parseGcodeStore over wire JSON | mitigate | malformed entry skipped via runCatching/mapNotNull (house rule); GcodeStoreParseTest proves it |
| T-08-01-I | Information Disclosure | probe curl output recorded into docs | accept | gcode_store/config carry no secrets (no API key in payloads); LAN-trusted scope |
| T-08-01-SC | Tampering | npm/pip/cargo installs | accept | zero new packages this phase (RESEARCH Package Legitimacy Audit: N/A) — no install task exists |
</threat_model>

<verification>
- docs/moonraker-capabilities.md has the dated Phase-8 shapes section.
- Fixtures contain real probed JSON.
- `:app:testReleaseUnitTest` is RED (downstream plans turn it green).
</verification>

<success_criteria>
The two unconfirmed live shapes (gcode_store, macro-body nesting) are recorded ground-truth, and ALL eight pure-layer + holder/prefs tests are scaffolded against those real shapes — the mock-vs-reality gate is closed and the single Nyquist RED wave is established before implementation.
</success_criteria>

<output>
Create `.planning/phases/08-macros-console-functional-core-complete/08-01-SUMMARY.md` when done.
</output>
