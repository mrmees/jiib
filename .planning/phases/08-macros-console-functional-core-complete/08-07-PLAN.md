---
phase: 08-macros-console-functional-core-complete
plan: 07
type: execute
wave: 4
depends_on: ["08-05", "08-06"]
files_modified:
  - app/src/main/java/works/mees/dinghy/DinghyApp.kt
  - app/src/main/java/works/mees/dinghy/di/AppContainer.kt
  - app/src/main/java/works/mees/dinghy/ui/route/TopRoute.kt
  - app/src/main/java/works/mees/dinghy/ui/shell/AppDrawer.kt
  - app/src/main/java/works/mees/dinghy/ui/shell/AppShell.kt
  - docs/moonraker-capabilities.md
autonomous: false
requirements: [MACRO-01, MACRO-02, MACRO-03, CONS-02]

must_haves:
  truths:
    - "A process-scoped macros.preferences_pb DataStore is created in DinghyApp, threaded into AppContainer, and a MacroPrefs instance is exposed for the MacroHolder (B1 — no dangling MacroPrefs dependency)"
    - "The drawer Macros tile (was greyed) opens the Bookmarked launcher and a new Console tile opens the Console screen (D-07/D-11)"
    - "The swipe-up App Drawer gesture is suppressed on Console AND on the finger-scrollable System macro list (D-05)"
    - "On the real Ender 5 the console populates from server.gcode_store, a real macro with params executes, and scroll is smooth on flox at the Adreno-320 floor"
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/DinghyApp.kt"
      provides: "macros.preferences_pb DataStore created + passed into AppContainer (B1)"
      contains: "macros.preferences_pb"
    - path: "app/src/main/java/works/mees/dinghy/di/AppContainer.kt"
      provides: "macroDataStore constructor param + exposed MacroPrefs instance (B1)"
      contains: "MacroPrefs"
    - path: "app/src/main/java/works/mees/dinghy/ui/route/TopRoute.kt"
      provides: "Dest enum += Macros, Console"
      contains: "Macros"
    - path: "app/src/main/java/works/mees/dinghy/ui/shell/AppDrawer.kt"
      provides: "Live Macros tile (dest=Dest.Macros) + new Console tile (terminal glyph)"
      contains: "Dest.Macros"
    - path: "app/src/main/java/works/mees/dinghy/ui/shell/AppShell.kt"
      provides: "when(dest) branches for Macros/Console + drawer-swipe suppression on both scroll-Fields"
      contains: "Dest.Console"
  key_links:
    - from: "app/src/main/java/works/mees/dinghy/DinghyApp.kt"
      to: "AppContainer macroDataStore param"
      via: "PreferenceDataStoreFactory.create(macros.preferences_pb) passed into the container ctor (mirrors theme/connection)"
      pattern: "macroDataStore"
    - from: "app/src/main/java/works/mees/dinghy/di/AppContainer.kt"
      to: "MacroHolder construction in AppShell"
      via: "container.macroPrefs is the process-scoped MacroPrefs the MacroHolder reads"
      pattern: "macroPrefs"
    - from: "app/src/main/java/works/mees/dinghy/ui/shell/AppShell.kt"
      to: "ConsoleScreen + BookmarkedMacrosScreen + holders"
      via: "when(dest) renders the active screen with session-owned holders"
      pattern: "when"
    - from: "app/src/main/java/works/mees/dinghy/ui/shell/AppShell.kt"
      to: "drawer swipe-up gesture"
      via: "pointerInput suppresses detectVerticalDragGestures on Files+Console+Macros system-list (D-05)"
      pattern: "pointerInput"
---

<objective>
Wire the four new screens into navigation and prove the phase on real hardware — the functional-core-complete gate. FIRST create the missing `macros.preferences_pb` DataStore wiring (B1 — `MacroPrefs` from 08-03 has NO production DataStore yet; 08-03/08-06 depend on it), mirroring the existing theme/connection DataStore pattern. Then add `Dest.Macros`/`Dest.Console` to the route enum, flip the greyed Macros drawer tile live + add a Console tile (`terminal` glyph, unused — icon-no-repeat law), add the `when(dest)` branches in AppShell building the ConsoleHolder/MacroHolder from the session-owned store + the process-scoped `MacroPrefs`, and SUPPRESS the swipe-up drawer gesture on Console AND the finger-scrollable System macro list (D-05). Then run the on-device UAT + console-scroll perf gate on flox + live Ender 5 Plus — the mock-vs-reality backstop (two prior phases shipped green suites that hid live bugs).

Purpose: Make the escape hatches reachable and PROVE the functional core is complete: connect → console backfills from gcode_store → run a real macro with params (its bookmarks/reveal prefs now actually persist) → reconnect recovers history (SC #3) → scroll smooth on the Adreno-320 floor.
Output: DinghyApp.kt (+macroDataStore), AppContainer.kt (+macroPrefs + holder wiring), TopRoute.kt, AppDrawer.kt, AppShell.kt, recorded UAT/perf into docs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/08-macros-console-functional-core-complete/08-RESEARCH.md
@.planning/phases/08-macros-console-functional-core-complete/08-PATTERNS.md
@.planning/phases/08-macros-console-functional-core-complete/08-UI-SPEC.md
@app/src/main/java/works/mees/dinghy/DinghyApp.kt
@app/src/main/java/works/mees/dinghy/di/AppContainer.kt
@app/src/main/java/works/mees/dinghy/ui/route/TopRoute.kt
@app/src/main/java/works/mees/dinghy/ui/shell/AppDrawer.kt
@app/src/main/java/works/mees/dinghy/ui/shell/AppShell.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1 [B1]: macros.preferences_pb DataStore wiring — DinghyApp → AppContainer → MacroPrefs</name>
  <files>app/src/main/java/works/mees/dinghy/DinghyApp.kt, app/src/main/java/works/mees/dinghy/di/AppContainer.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/DinghyApp.kt (onCreate creates `themeDataStore` + `connectionDataStore` via `PreferenceDataStoreFactory.create(scope=appScope, produceFile={ preferencesDataStoreFile("X.preferences_pb") })` then passes both into the `AppContainer(...)` ctor — the EXACT analog to copy)
    - app/src/main/java/works/mees/dinghy/di/AppContainer.kt (the ctor params `themeDataStore`/`connectionDataStore` at lines 48-49 and the `val themePrefs = ThemePrefs(themeDataStore)` / `val connectionStore = ConnectionStore(connectionDataStore)` properties at 61-64 — the EXACT analog to copy)
    - app/src/main/java/works/mees/dinghy/ui/macros/MacroPrefs.kt (08-03 — the MacroPrefs(injected DataStore) shape that needs a real store handed to it)
    - app/src/main/java/works/mees/dinghy/config/ConnectionStore.kt (ConnectionStore(dataStore) ctor — the prefs-class shape MacroPrefs mirrors)
  </read_first>
  <action>
    Close the B1 dangling dependency: `MacroPrefs` (08-03) exists but nothing constructs its DataStore. (a) In DinghyApp.onCreate, add a THIRD DataStore beside `themeDataStore`/`connectionDataStore`: `val macroDataStore: DataStore<Preferences> = PreferenceDataStoreFactory.create(scope = appScope, produceFile = { applicationContext.preferencesDataStoreFile("macros.preferences_pb") })` — its OWN file (NOT shared with connection/theme — the separate-file discipline already established for the redaction boundary; macros carry no secrets but keep the lifecycle independent). Pass it into the `AppContainer(...)` ctor as a new `macroDataStore` argument. (b) In AppContainer add the ctor param `macroDataStore: DataStore<Preferences>` (beside `themeDataStore`/`connectionDataStore`) and expose `val macroPrefs: MacroPrefs = MacroPrefs(macroDataStore)` (beside `themePrefs`/`connectionStore`, with a KDoc noting it is the process-scoped, connection-independent bookmarks/revealHidden store per MACRO-03). Do NOT route it through SpineHandle — MacroPrefs is process-scoped like ThemePrefs/ConnectionStore, NOT session-scoped (bookmarks survive reconnects). The MacroHolder (built in Task 2's `when(dest)` wiring) reads `container.macroPrefs`.
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:assembleRelease --no-daemon" 2>&1 | tr -d '\r' | grep -E 'BUILD SUCCESSFUL|BUILD FAILED'</automated>
  </verify>
  <acceptance_criteria>
    - grep DinghyApp.kt shows a `macros.preferences_pb` DataStore created via `PreferenceDataStoreFactory.create` and passed into the `AppContainer(...)` ctor (a new `macroDataStore =` argument).
    - grep AppContainer.kt shows the `macroDataStore: DataStore<Preferences>` ctor param AND `val macroPrefs: MacroPrefs = MacroPrefs(macroDataStore)`.
    - `macroPrefs` is a property on AppContainer (process-scoped), NOT a field on SpineHandle (grep SpineHandle.kt shows no MacroPrefs).
    - `:app:assembleRelease` BUILD SUCCESSFUL.
  </acceptance_criteria>
  <done>[B1] MacroPrefs now has a real production DataStore; 08-03/08-06's prefs dependency is satisfied at integration.</done>
</task>

<task type="auto">
  <name>Task 2: Nav wiring — Dest enum, drawer tiles, AppShell branches + drawer-swipe suppression (D-05/D-07/D-11)</name>
  <files>app/src/main/java/works/mees/dinghy/ui/route/TopRoute.kt, app/src/main/java/works/mees/dinghy/ui/shell/AppDrawer.kt, app/src/main/java/works/mees/dinghy/ui/shell/AppShell.kt, app/src/main/java/works/mees/dinghy/di/AppContainer.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/route/TopRoute.kt (enum class Dest at line 30 — "extra panels are a one-line addition")
    - app/src/main/java/works/mees/dinghy/ui/shell/AppDrawer.kt (DRAWER_TILES list at 116-125 — the greyed Macros entry `dest = null` at 122; existing glyphs to avoid repeating)
    - app/src/main/java/works/mees/dinghy/ui/shell/AppShell.kt (the `when(dest)` at 145-176 + the pointerInput swipe-suppress at 136-142 — `if (dest != Dest.Files)`)
    - app/src/main/java/works/mees/dinghy/di/AppContainer.kt (how session-scoped holders like FileBrowserHolder are built from the SpineHandle/store; the new `macroPrefs` property from Task 1 the MacroHolder reads)
    - .planning/phases/08-macros-console-functional-core-complete/08-PATTERNS.md (Nav wiring section — the exact edits + the swipe-suppress set extension)
  </read_first>
  <action>
    In TopRoute.kt extend `enum class Dest { PrintStatus, Temperature, Move, Extrude, Files, Settings }` → add `Macros, Console`. In AppDrawer.kt flip the greyed Macros entry: `DrawerTileSpec(label = "Macros", symbol = "code", dest = Dest.Macros)` (was `dest = null`) and ADD `DrawerTileSpec(label = "Console", symbol = "terminal", dest = Dest.Console)` — `terminal` is unused on the current drawer (existing: monitoring/open_with/thermostat/folder/output_circle/code/cable/settings/power_settings_new) so it satisfies the icon-no-repeat law (RESEARCH Open-Q1). In AppShell.kt add to `when(dest)`: `Dest.Macros -> BookmarkedMacrosScreen(holder = macroHolder, onRunMacro = { showMacroPopup(it) }, onManage = { navigateTo(...System list...) }, onBack = { goBack() })` and `Dest.Console -> ConsoleScreen(holder = consoleHolder, onBack = { goBack() })`. The System macro list is reached from the Bookmarked screen's Manage control — render it (e.g. a nested route/overlay or a `Dest`-less sub-state) and the MacroExecutionPopup as an overlay over the launcher. Build `consoleHolder`/`macroHolder` from the SESSION-owned store (the per-session PrinterStateStore the shell already uses for PrintStatusHolder/FileBrowserHolder — wire in AppContainer.kt alongside those); construct the MacroHolder with `container.macroPrefs` (the process-scoped prefs from Task 1) for bookmarks/revealHidden. EXTEND the swipe-suppress at AppShell.kt:137 from `if (dest != Dest.Files)` to `if (dest !in setOf(Dest.Files, Dest.Console, Dest.Macros))` so the drawer swipe is suppressed on Console AND the (finger-scrollable) System macro list; keep the explicit green Back in each gutter as the exit (D-05).
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:assembleRelease --no-daemon" 2>&1 | tr -d '\r' | grep -E 'BUILD SUCCESSFUL|BUILD FAILED'</automated>
  </verify>
  <acceptance_criteria>
    - grep TopRoute.kt shows `Macros` and `Console` in the Dest enum.
    - grep AppDrawer.kt shows the Macros tile with `dest = Dest.Macros` (no longer `dest = null`) and a Console tile with `symbol = "terminal"` (glyph unused elsewhere in DRAWER_TILES).
    - grep AppShell.kt shows `Dest.Macros ->` and `Dest.Console ->` branches AND the swipe-suppress set includes Console + Macros.
    - consoleHolder/macroHolder are built from the session store (not a fresh idle client), and the MacroHolder is constructed with `container.macroPrefs` — grep AppContainer.kt / AppShell.kt.
    - `:app:assembleRelease` BUILD SUCCESSFUL.
  </acceptance_criteria>
  <done>Both screens reachable; drawer swipe suppressed on scroll-Fields; holders session-owned; MacroHolder reads the real MacroPrefs.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: On-device UAT + console-scroll perf gate on flox + live Ender 5 Plus (functional-core gate)</name>
  <read_first>
    - .planning/phases/08-macros-console-functional-core-complete/08-RESEARCH.md (Validation Architecture → Phase gate + Sampling Rate; Environment Availability — live gate non-negotiable; FrameTimingMetric may not work on API 23 → fall back to gfxinfo framestats per CLAUDE.md)
    - CLAUDE.md (Local Build Environment — gw.bat build, sign-release.bat, adb path; flox = LineageOS 18.1/API 30, Adreno 320 perf FLOOR)
    - docs/moonraker-capabilities.md (record results here)
  </read_first>
  <what-built>
    The full Macros & Console feature wired into the shell: read-only Console (severity-colored, backfilled from gcode_store, live, opt-in filters), three Macro screens (Bookmarked launcher / System manage-visibility / Execution popup with param entry), reachable from the drawer, with macro bookmarks/reveal now persisting via the real macros.preferences_pb DataStore (B1). Build + sign + install the release on flox (gw.bat assembleRelease → sign-release.bat → adb install), connected to the live Ender 5 Plus (192.168.1.120:7125).
  </what-built>
  <how-to-verify>
    1. CONSOLE BACKFILL: open Console — it populates with recent printer history from `server.gcode_store` (not blank). Run a gcode/macro elsewhere and confirm a new line appears LIVE.
    2. SEVERITY COLOR: confirm a `!!` line shows red, a `// ` echo shows amber, a normal line shows default text (color + text, never color-alone).
    3. FILTERS (D-03/D-04): toggle "Hide temperatures" ON during an active heat → temp-report lines hide; toggle OFF → they RE-APPEAR (proves raw survives, D-04). Repeat for Timelapse / prompt toggles if such lines are present.
    4. RECONNECT (SC #3, the functional-core gate): with Console open, yank Wi-Fi briefly (or FIRMWARE_RESTART) then restore → history BACKFILLS correctly (lines that arrived while disconnected are present, not silently dropped).
    5. MACROS: drawer Macros tile opens the Bookmarked launcher; Manage → System list shows ALL macros (underscore helpers hidden until Show hidden); bookmark a couple AND confirm the bookmarks PERSIST across an app restart (proves the B1 DataStore wiring); back on the launcher tap a macro that DECLARES params → the Execution popup shows the param fields (numeric→Numpad, string→keyboard) → Execute → the printer runs it (confirm via the printer / console line). A rejected macro shows a SeverityToast AND a console line.
    6. SECURITY (manual): in a string param field type a value containing a newline or `;` or `M112` → confirm the invocation is REJECTED (NO emergency-stop/extra-command fires; the sanitizer rejected it per B2).
    7. PERF (Adreno-320 floor): scroll the Console scrollback during live line flow; capture `adb shell dumpsys gfxinfo works.mees.dinghy framestats` (FrameTimingMetric may not work on API 23 → gfxinfo is system-of-record per CLAUDE.md). Confirm no frozen frames / responsive scroll consistent with the prior Files perf gate (07-06: steady-scroll p95 ~15ms, 0 frozen). Record p95/max + frozen count.
    8. Record all results + the perf capture into docs/moonraker-capabilities.md (and/or an 08-UAT note). Two prior phases shipped green unit suites that hid live bugs — this gate is the backstop.
  </how-to-verify>
  <acceptance_criteria>
    - Console backfills from real gcode_store on connect AND after a reconnect (SC #3 PASS — disconnect-window lines recovered, not dropped).
    - Severity coloring + at least one filter toggle ON→OFF re-reveal verified live (D-04).
    - A real macro WITH params executes from the popup on the live Ender 5; macro bookmarks persist across an app restart (B1 DataStore proven on-device); a rejection surfaces as toast + console line.
    - String-param injection (newline/`;`/M112) is REJECTED and does NOT fire extra gcode (security gate verified on-device — B2 reject policy).
    - Console scroll perf recorded on flox with no frozen frames / responsiveness consistent with the Files gate; numbers written to docs (not fabricated).
  </acceptance_criteria>
  <resume-signal>Type "approved" with the recorded UAT + perf numbers, or report which check failed (gap → /gsd-plan-phase 8 --gaps).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → drawer/screens → dispatched macro | navigation reaches the one sanctioned keyboard site; dispatch is sanitized (08-03/08-06) |
| printer → console render (live, on-device) | untrusted printer text rendered plain-text; verified on real hardware |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-07-T | Tampering / Elevation | on-device macro string-param injection | mitigate (block_on:high) | UAT step 6 manually verifies the sanitizer REJECTS newline/`;`/M112 on the LIVE printer (backstop to the MacroInvocationTest unit gate) |
| T-08-07-A | Availability | console-scroll jank on Adreno-320 | mitigate | gfxinfo perf gate on flox; itemAnimator=null + incremental notifyItemInserted (S2) + ConsoleScrollback bound + Views-in-Compose (no LazyColumn) hold the floor |
| T-08-07-SC | Tampering | npm/pip/cargo installs | accept | zero new packages this phase — no install/checkpoint needed |
</threat_model>

<verification>
- `:app:assembleRelease` BUILD SUCCESSFUL.
- grep proves the macros.preferences_pb DataStore wiring (DinghyApp → AppContainer.macroPrefs → MacroHolder) closing B1.
- On-device UAT 5-check set + security check + perf capture PASS on flox + live Ender 5.
- Results recorded in docs (not fabricated).
</verification>

<success_criteria>
The four screens are reachable, MacroPrefs has a real production DataStore (bookmarks persist), and drawer-swipe is correctly suppressed; the functional core is PROVEN complete on real hardware — console backfills/recovers history, a real parametered macro runs, injection is rejected, and scroll holds the Adreno-320 floor.
</success_criteria>

<output>
Create `.planning/phases/08-macros-console-functional-core-complete/08-07-SUMMARY.md` when done.
</output>
