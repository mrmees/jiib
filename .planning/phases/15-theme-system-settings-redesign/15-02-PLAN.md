---
phase: 15-theme-system-settings-redesign
plan: 02
type: execute
wave: 1
depends_on:
  - 15-01
files_modified:
  - app/src/main/java/works/mees/dinghy/theme/Palette.kt
  - app/src/test/java/works/mees/dinghy/theme/PaletteGoldenTest.kt
  - app/src/test/java/works/mees/dinghy/theme/PaletteMathTest.kt
autonomous: true
requirements:
  - "D-01 (hand-port color.js → Kotlin; Palette.generate() is pure/dependency-free; ship statusFromPool:true)"
  - "Refines THEME-01 (the seed-generated token engine)"
must_haves:
  truths:
    - "A pure Kotlin Palette.generate() with ZERO Android imports produces sRGB hexes bit-identical to the color.js oracle (D-01)"
    - "PaletteGoldenTest is GREEN across default dark/light, Simple, High-Contrast, edge-hue seeds, and poolShift"
    - "PaletteMathTest is GREEN — OKLCH↔sRGB round-trips and gamut clamp stays in [0,1]"
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/theme/Palette.kt"
      provides: "Pure host-testable OKLCH→sRGB palette generator (faithful 1:1 port of color.js)"
      contains: "object Palette"
      min_lines: 200
  key_links:
    - from: "app/src/main/java/works/mees/dinghy/theme/Palette.kt"
      to: "../theme_theory/app/color.js"
      via: "verbatim math port (matrices, iteration counts, Double accumulation)"
      pattern: "fun generate"
    - from: "PaletteGoldenTest.kt"
      to: "Palette.generate"
      via: "asserts Kotlin output == color-golden.json oracle"
      pattern: "Palette\\.generate"
---

<objective>
Port the pure, dependency-free `color.js` palette generator to a Kotlin `object Palette` with ZERO Android imports (mirrors `PrinterStateReducer`'s host-pure discipline), and turn the Wave-0 golden + math scaffolds GREEN against the committed oracle fixture.

Purpose: This is the heart of the phase — the cusp-anchored accent, contrast-ranked data pool, status slots, and pure-neutral surfaces all flow from this generator. It must match the owner-reviewed `color.js` oracle bit-for-bit (D-01), so it lands and is verified BEFORE the substrate rewires onto it. Faithful 1:1 port (no refactors of the float-accumulation loops) keeps golden tests clean.
Output: `theme/Palette.kt` + GREEN `PaletteGoldenTest`/`PaletteMathTest`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-theme-system-settings-redesign/15-RESEARCH.md
@.planning/phases/15-theme-system-settings-redesign/15-PATTERNS.md
@app/src/main/java/works/mees/dinghy/theme/BakedTokens.kt
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Port color.js → pure Kotlin object Palette</name>
  <read_first>
    - ../theme_theory/app/color.js (THE port source — read in full; ~270 LOC; copy matrices/iteration-counts verbatim)
    - .planning/phases/15-theme-system-settings-redesign/15-RESEARCH.md (§"The Port Surface" — the function-by-function table + JS→Kotlin idiom landmines)
    - app/src/main/java/works/mees/dinghy/theme/BakedTokens.kt (lines 6-30 — the "literal sRGB Color(0x..) only, oklch is wrong on API<26" header law the generator's output policy inherits)
    - app/src/main/java/works/mees/dinghy/state/PrinterStateReducer.kt (the host-pure no-Android-import core discipline to mirror)
    - .planning/phases/15-theme-system-settings-redesign/15-PATTERNS.md (§NEW theme/Palette.kt — the value-type-not-ThemeTokens decision)
  </read_first>
  <behavior>
    - Golden: `Palette.generate(seedHex="#3f78ff", dark=true, maxItems=3, poolShift=0, statusFromPool=true)` returns accent `#3c75fb`, surfaces.bg `#0b0b0b`, surfaces.text `#e8e8e8`, pool `["#6895f4","#866200","#c575cc"]`, directional {temperature `#c575cc`, xy `#6895f4`, z `#866200`}, status {stop `#af3c3c`, caution `#007780`, go `#57af58`}, minHueGap 60.
    - Light: same seed `dark=false` matches the `defaultLight` oracle entry.
    - Simple mode (`simple=true`): pool + status collapse to text gray `#e8e8e8`, accent kept.
    - High-Contrast (`highContrast=true`): pool collapses to gray, status restores stoplight RYG (`#de3d30`/`#f5ae39`/`#3bac56`).
    - poolShift 120: pool hues rotate but accent stays `#3c75fb`.
    - Edge-hue seeds (red ~25°, yellow ~85°) match their oracle entries.
    - Math: OKLCH↔sRGB round-trip on a sample of hexes returns within the gamut epsilon; `oklchToHex` output channels stay in [0,1] (no overflow).
  </behavior>
  <action>
    Create `app/src/main/java/works/mees/dinghy/theme/Palette.kt` as `object Palette` (or top-level functions in a `Palette` object) with NO androidx/Compose import — pure `kotlin.math` only. Port every function enumerated in RESEARCH §"The Port Surface": `clamp01` (`coerceIn(0.0,1.0)`), `sToL`/`lToS`, `hexToRgb`/`rgbToHex` (lowercase 2-pad output to match the oracle), `linToLab`/`labToLin` (copy the 3×3 OKLab matrices VERBATIM — a single transcription typo = silent drift), `hexToOklch` (atan2/hypot, H wrapped +360 if <0), `inGamut` (keep the ±0.0002 epsilon EXACTLY), `oklchToHex` (KEEP the 20-iter binary search), `grayHex`, `maxChromaAt` (KEEP 18 iterations), `cuspL` (KEEP the literal `L += 0.02` loop over 0.30..0.92 — do NOT refactor to integer-step; float accumulation must match IEEE-754 the oracle uses), `hueDiff`/`inSpan`/`isReserved` (port for parity though dead when `statusFromPool=true`), `spreadHues` (step 0.5°, exact run-length accumulation), `minHueGap`, `rankByContrast` (farthest-point reorder; JS `slice/shift/splice` → `toMutableList()`/`removeAt`), and `generate`. Use `Double` THROUGHOUT the math (never `Float` — `cuspL` accumulation drift); narrow to Int/hex only at the rgbToHex boundary. `generate` signature: `generate(seedHex: String, dark: Boolean, maxItems: Int, poolShift: Int = 0, statusFromPool: Boolean = true, simple: Boolean = false, highContrast: Boolean = false): Generated`. Return a plain `@Immutable`-free value type `data class Generated` of STRING hexes (accent/theme.primary, surfaces.bg/surface/text/muted/divider, pool: List<String>, poolRoles: List<String?>, directional with temperature/xy/z String, status stop/caution/go String, minHueGap: Int) — NOT a `ThemeTokens` (the bridge bakes to Color; keeping it a 1:1 of `color.js` keeps golden tests host-pure). `kotlin.math.cbrt` is the cube-root.
    Then REPLACE the RED stub bodies in `PaletteGoldenTest.kt` and `PaletteMathTest.kt` with real assertions: golden test loads `color-golden.json` from the test classpath and asserts each fixture entry's fields equal `Palette.generate(...)` output for the matching opts; math test asserts round-trip + gamut bounds. Do NOT use a `pkg.*` glob in any documented test command — list classes explicitly.
  </action>
  <verify>
    <automated>cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --tests works.mees.dinghy.theme.PaletteGoldenTest --tests works.mees.dinghy.theme.PaletteMathTest --no-daemon" | tr -d '\r'</automated>
  </verify>
  <acceptance_criteria>
    - `theme/Palette.kt` exists, is `object Palette`, and has NO `androidx` / `android.` / `compose` import (host-pure). Verify: `grep -E "import (androidx|android\.|.*compose)" app/src/main/java/works/mees/dinghy/theme/Palette.kt` returns nothing.
    - `PaletteGoldenTest` is GREEN for all 7 named methods (`defaultSeedDark_matchesOracle`, `defaultSeedLight_matchesOracle`, `simpleMode_matchesOracle`, `highContrastMode_matchesOracle`, `edgeHueRed_matchesOracle`, `edgeHueYellow_matchesOracle`, `poolShift120_matchesOracle`).
    - `PaletteMathTest` is GREEN (`oklchToSrgbRoundTrip_staysInGamut`, `gamutClamp_neverExceedsUnitInterval`).
    - `Palette.generate("#3f78ff", dark=true, maxItems=3)` produces accent `#3c75fb`, bg `#0b0b0b`, pool `["#6895f4","#866200","#c575cc"]`, minHueGap 60 (oracle parity).
    - `cuspL` retains the literal `L += 0.02` accumulation loop (no integer-step refactor) — source assertion.
  </acceptance_criteria>
  <done>`Palette.generate` is a faithful, host-pure 1:1 port of `color.js`; the golden + math tests GREEN against the committed oracle, proving bit-for-bit parity including modes, edge hues, and poolShift.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| persisted theme tuple → Palette.generate | (downstream) untrusted seed/mode flow into generation; this plan keeps generate total/non-throwing so junk-seed handling in the sanitize layer (plan 15-05) always has a valid generator to call. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-02-01 | Tampering | OKLab matrix transcription typo → silent color drift | mitigate | Golden test asserts bit-for-bit against the oracle across 7 vectors incl. edge hues; matrices copied verbatim per RESEARCH. |
| T-15-02-02 | Denial of Service | `Palette.generate` throws on a malformed seed → black-screen risk downstream | mitigate | Math is total (clamp/gamut binary-search never diverge); the sanitize layer (15-05) guarantees a valid seedHex before calling. Generator stays pure with no I/O. |
| T-15-02-03 | Information disclosure | Oklab-space Compose Color silently wrong on API<26 | mitigate | Generator emits STRING sRGB hexes only; no Compose Color constructed here (BakedTokens header law). |
| T-15-02-SC | Tampering | npm/pip/cargo installs | accept | Zero external packages — pure `kotlin.math` stdlib (RESEARCH §"Package Legitimacy Audit": N/A). |
</threat_model>

<verification>
- Golden + math unit tests GREEN (the < 60s quick gate per VALIDATION).
- `theme/Palette.kt` has zero Android/Compose imports (host-pure core).
</verification>

<success_criteria>
- Pure Kotlin `Palette.generate` matching the `color.js` oracle bit-for-bit across all golden vectors and modes.
- `cuspL`/gamut/contrast-rank loops ported verbatim (no refactors that change float accumulation).
- statusFromPool=true is the shipped default (D-01).
</success_criteria>

<output>
Create `.planning/phases/15-theme-system-settings-redesign/15-02-SUMMARY.md` when done.
</output>
