---
phase: 15-theme-system-settings-redesign
plan: 06
type: execute
wave: 5
depends_on:
  - 15-05
files_modified:
  - app/src/main/java/works/mees/dinghy/ui/screen/SettingsScreen.kt
  - app/src/main/java/works/mees/dinghy/ui/screen/ThemeEditorScreen.kt
  - app/src/main/java/works/mees/dinghy/designsystem/ColorWheel.kt
  - app/src/main/java/works/mees/dinghy/ui/shell/AppShell.kt
  - app/src/main/java/works/mees/dinghy/ui/shell/ShellNavState.kt
autonomous: false
requirements:
  - "D-06 (seed picker = touch color wheel/ring + curated preset seed swatches)"
  - "D-07 (regenerate + retheme on release/settle, not per-pixel)"
  - "D-08 (preview = live app retheme + a generated-swatch strip)"
  - "D-09 (Randomize button + per-slot data-pool override; Reset clears overrides + poolShift)"
  - "D-10 (hybrid structure — flat Settings hub + a pushed theme-editor sub-page)"
  - "D-11 (scaffold all sections now, greyed forward entries for not-yet-built features)"
  - "D-12 (System/About = app version + build only; Reset-to-default-seed lives in the editor)"
  - "D-15 (default palette mode = Colorful; Simple + High-Contrast user-selectable)"
must_haves:
  truths:
    - "Settings is a hybrid hub: Printers · Connection · Appearance · Feature toggles · System, with the old accent picker REMOVED and a palette-mode chip row + 'Edit theme…' forward entry added (D-10/D-11/D-15)"
    - "A pushed theme-editor sub-page hosts the color wheel + presets + generated-swatch-strip preview + per-slot pool override grid + Randomize/Reset (D-06/D-08/D-09)"
    - "The color wheel regenerates + rethemes the app on gesture SETTLE only (D-07), never per-pixel"
    - "Greyed capability-gated placeholders scaffold outputs/WebRTC/fine-tune; System shows version+build only (D-11/D-12)"
    - "On-device flox UAT confirms the editor flow rethemes live with no per-pixel regen-storm and no connection churn"
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/ui/screen/ThemeEditorScreen.kt"
      provides: "Pushed theme-editor sub-page (wheel/presets/preview/pool-override/Randomize/Reset)"
      contains: "fun ThemeEditorScreen"
    - path: "app/src/main/java/works/mees/dinghy/designsystem/ColorWheel.kt"
      provides: "Compose Canvas hue-ring with awaitEachGesture settle-regen"
      contains: "fun ColorWheel"
    - path: "app/src/main/java/works/mees/dinghy/ui/screen/SettingsScreen.kt"
      provides: "Rebuilt hybrid hub; accent picker removed; palette-mode + Edit-theme rows"
  key_links:
    - from: "app/src/main/java/works/mees/dinghy/designsystem/ColorWheel.kt"
      to: "AppContainer theme write surface"
      via: "onSettle → setActiveSeed (regen + persist via writeScope)"
      pattern: "onSettle"
    - from: "app/src/main/java/works/mees/dinghy/ui/screen/ThemeEditorScreen.kt"
      to: "app/src/main/java/works/mees/dinghy/ui/shell/AppShell.kt"
      via: "pushed sub-page within Dest.Settings local back-stack"
      pattern: "ThemeEditorScreen"
---

<objective>
Rebuild `SettingsScreen` into the hybrid hub (Printers · Connection · Appearance · Feature toggles · System with greyed forward entries) and build the pushed theme-editor sub-page — a Compose Canvas color wheel (settle-regen), curated preset swatches, a generated-swatch-strip preview, a per-slot pool-override grid, and Randomize/Reset — wired to the durable theme write surface. Remove the old accent picker. Verify on-device on flox.

Purpose: This is the real test/config surface for the whole engine. The editor must regenerate on gesture-settle only (D-07) to protect the Adreno-320 floor from a regen-storm, and a reseed must retheme live with no connection churn and no flicker. This project has a documented string of green-suite-vs-reality misses, so an on-device UAT is mandatory.
Output: rebuilt `SettingsScreen.kt`, new `ThemeEditorScreen.kt` + `ColorWheel.kt`, AppShell/ShellNavState routing, on-device sign-off.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-theme-system-settings-redesign/15-UI-SPEC.md
@.planning/phases/15-theme-system-settings-redesign/15-RESEARCH.md
@.planning/phases/15-theme-system-settings-redesign/15-PATTERNS.md
@docs/ui_design/THEMING.md
@docs/ui_design/LAYOUT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the ColorWheel control + theme-editor sub-page; route it off Settings</name>
  <read_first>
    - app/src/main/java/works/mees/dinghy/designsystem/ScrubberPage.kt (lines 153-176 — the ONE awaitEachGesture settle-on-up WR-01 pattern; the gesture analog for the wheel)
    - app/src/main/java/works/mees/dinghy/ui/temperature/TemperatureScreen.kt (line ~200 — the state-gated full-screen overlay sub-page hosting pattern)
    - app/src/main/java/works/mees/dinghy/ui/shell/AppShell.kt (lines 148-153 + 386-387 + 510-521 — the Dest.Calibration local back-stack (calibrationRoutine null-or-page) to MIRROR; line 431 swipe-drawer suppression set; line 602 Dest.Settings branch)
    - app/src/main/java/works/mees/dinghy/ui/shell/ShellNavState.kt (line 56 calibrationRoutine — add a sibling editor-open flag for Settings)
    - app/src/main/java/works/mees/dinghy/designsystem/OutlinedControl.kt (the Intent → outline color map; Warn/Danger/Go)
    - app/src/main/java/works/mees/dinghy/designsystem/ConfirmGuard.kt (the Reset confirm guard)
    - app/src/main/java/works/mees/dinghy/ui/screen/SettingsScreen.kt (lines 585-628 AccentSwatch — the literal-data-color carve-out precedent for swatches)
    - .planning/phases/15-theme-system-settings-redesign/15-UI-SPEC.md (§"New surface — Theme-editor sub-page" — the full section order, intents, copy, states, fsSp floors)
    - .planning/phases/15-theme-system-settings-redesign/15-PATTERNS.md (§NEW color-wheel control + §NEW theme-editor sub-page — routing option 1)
  </read_first>
  <action>
    Create `designsystem/ColorWheel.kt`: `fun ColorWheel(hue: Float, onHandleMove: (Float)->Unit, onSettle: (Float)->Unit, modifier)` — a Compose `Canvas` hue-ring sized by its cell via `aspect-ratio(1f)` (sacred circle, must render circular) using ONE `pointerInput`/`awaitEachGesture` mirroring ScrubberPage: `onHandleMove(hueAt(pos))` on each move (cheap — repaint handle, NO regen), `onSettle(hue)` on pointer-up ONLY (D-07). Handle has a ≥64dp touch hit area + 2dp outline + static glow (no looping animation — Adreno-320 floor). The ring/handle render their literal hue color (the precedented data-color carve-out, like AccentSwatch); all chrome routes through `LocalTokens.current`.
    Create `ui/screen/ThemeEditorScreen.kt`: `fun ThemeEditorScreen(...)` — a plain `Column.verticalScroll` (FFG-exempt), token-themed, sized via `fsSp(baseSp, t.fs)` floors (title 22sp / SectionLabel 20sp / body 17sp / hex Geist-Mono 17sp / metadata 15sp per UI-SPEC — honor `[[dinghy-font-sizes-too-small]]`). Sections top→bottom: (1) "Seed color" → the ColorWheel, `onSettle` → `container.setActiveSeed(hueToSeedHex)` (regen + persist); (2) "Presets" → a row of curated preset seed swatches (pills rendering their seed color; tap lands the seed → settle semantics; active gets an accent emphasis ring); (3) "Preview" → the generated swatch strip (accent · pool[0..n] · status colors, each a swatch rendering its actual generated color — the carve-out) — the live app retheme is the other half (no separate preview screen); (4) "Pool colors" → a per-slot override grid: each pool[i] a ≥64dp tappable swatch (its actual/overridden color, 2dp outline); tapping opens a picker (reuse the wheel or a swatch grid) → `container.setActiveOverride(i, argb)`; overridden slots show an accent override marker (must not repeat a glyph already on screen); DATA-POOL slots ONLY (chrome stays seed-only, D-04); (5) Actions: "Randomize" (amber `Intent.Warn`, rolls a new poolShift via `container.setActiveShift(random)`), "Reset" (red `Intent.Danger`, guarded by `ConfirmGuard` — title "Reset theme?", clears overrides+poolShift via `container.resetActiveTheme()`), "Done"/"Back" (green `Intent.Go`). All theme writes go through the AppContainer durable write surface from 15-05 (writeScope/mutateActiveProfile) — NEVER a `rememberCoroutineScope()`.
    Route it as a pushed sub-page WITHIN `Dest.Settings` (MIRROR Dest.Calibration): add a `settingsEditorOpen: Boolean` (or nullable editor state) to `ShellNavState.kt`; in `AppShell.kt`'s `Dest.Settings` branch (line 602) render the editor when open else the hub; add a `BackHandler` popping the editor to the hub (mirror lines 386-387); add `Dest.Settings` to the swipe-drawer-suppression set when the editor is scrollable (line 431).
  </action>
  <verify>
    <automated>cmd.exe /c "E:\Android\gw.bat :app:assembleDebug --no-daemon" | tr -d '\r'</automated>
  </verify>
  <acceptance_criteria>
    - `designsystem/ColorWheel.kt` exists with ONE `awaitEachGesture` and a distinct `onSettle` callback fired on pointer-up only (D-07). Verify: `grep -n "awaitEachGesture" ColorWheel.kt` matches exactly one; `grep -n "onSettle" ColorWheel.kt` matches.
    - `ui/screen/ThemeEditorScreen.kt` exists with the 5 sections (Seed color / Presets / Preview / Pool colors / Actions) and Randomize=`Intent.Warn`, Reset=`Intent.Danger` (guarded by ConfirmGuard), Done=`Intent.Go`. Source assertions on the Intent usages.
    - Editor routed within `Dest.Settings` (no new top-level Dest); a BackHandler pops editor→hub. Verify the ShellNavState editor flag + AppShell branch.
    - All theme writes call the AppContainer durable surface (setActiveSeed/setActiveOverride/setActiveShift/resetActiveTheme); NO `rememberCoroutineScope()` in the write path. Verify: `grep -n "rememberCoroutineScope" ThemeEditorScreen.kt` returns nothing in a write path.
    - Editor/wheel labels use `fsSp(...)` (no raw sp literals below the floors). Verify grep for `fsSp(` present, no bare `.sp` size on a primary label.
    - `:app:assembleDebug` SUCCEEDS.
  </acceptance_criteria>
  <done>The color wheel (settle-regen) + theme-editor sub-page exist, are routed off Settings, and write through the durable surface; the app assembles.</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild SettingsScreen into the hybrid hub; remove the accent picker</name>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/screen/SettingsScreen.kt (whole file — lines 174-180 the verticalScroll/padding grammar; 181-220 Printers/header; 184-415 Connection; 423-468 Appearance dark/light + S/M/L + the accent picker to REMOVE; 466-492 + 553-628 AccentSwatch/ACCENT_PALETTE/persistDeltas to delete; 520-547 persistBase/persistFs helpers to reuse for palette-mode)
    - app/src/main/java/works/mees/dinghy/ui/screen/ThemeEditorScreen.kt (the editor this hub pushes to — from Task 1)
    - .planning/phases/15-theme-system-settings-redesign/15-UI-SPEC.md (§"Reused-surface contracts" + §Copywriting — exact section labels/copy/order; greyed forward-entry treatment)
    - .planning/phases/15-theme-system-settings-redesign/15-PATTERNS.md (§MODIFY SettingsScreen.kt — reuse SectionHeader/SectionLabel/ProfileRow/AddPrinterRow verbatim; greyed forward-entry on the existing row pattern)
  </read_first>
  <action>
    Rebuild `ui/screen/SettingsScreen.kt` keeping the existing `Column.verticalScroll(rememberScrollState()).padding(16.dp)` grammar and reusing `SectionHeader`/`SectionLabel`/`ProfileRow`/`AddPrinterRow`/`OutlinedControl`/`TokenTextField` VERBATIM — only the section SET + order changes. Sections in order (D-11): "Printers" (the Phase-14 profile CRUD list — reused 1:1) · "Connection" (host/port/key + mDNS form — reused) · "Appearance" (the existing dark/light chips + S/M/L chips, UNCHANGED + a NEW "Palette mode" chip row of three `OutlinedControl` chips "Colorful"(default)/"Simple"/"High contrast" — active=`Intent.Accent`+`--accent-soft`, inactive=`Intent.Neutral`, persisting via a `persistMode` helper modeled on `persistBase`/`persistFs` routing through `mutateActiveProfile`/global-fallback + a NEW "Edit theme…" forward-entry row, sub-label "Seed color, palette & pool", that opens the editor) · "Feature toggles" ("Webcam" live; "Output controls"/"Camera (WebRTC)"/"Fine-tune" as greyed disabled placeholders, faint `t.text3`, sub-label "Coming soon" — the established forward-entry pattern) · "System" (one "Version" row → `{versionName} ({versionCode})` in Geist Mono — app version+build ONLY, D-12; NO printer/Klipper/Moonraker info, NO restart). REMOVE the accent picker entirely: delete the `AccentSwatch` rows, `ACCENT_PALETTE`, and `persistDeltas` (D-04 retires per-role chrome overrides). Honor `fsSp` floors on every label (UI-SPEC typography table). RootController.kt:105's SettingsScreen call site — verify it still compiles against the new signature (update the call if the param list changed).
  </action>
  <verify>
    <automated>cmd.exe /c "E:\Android\gw.bat :app:assembleDebug --no-daemon" | tr -d '\r'</automated>
  </verify>
  <acceptance_criteria>
    - Section labels in order: "Printers", "Connection", "Appearance", "Feature toggles", "System". Verify greps for each SectionLabel string.
    - The accent picker is GONE. Verify: `grep -nE "AccentSwatch|ACCENT_PALETTE|persistDeltas" app/src/main/java/works/mees/dinghy/ui/screen/SettingsScreen.kt` returns nothing.
    - Appearance has a 3-chip palette-mode row ("Colorful"/"Simple"/"High contrast", default Colorful) + an "Edit theme…" forward-entry row (sub-label "Seed color, palette & pool") that opens the editor.
    - Feature toggles shows "Webcam" live + "Output controls"/"Camera (WebRTC)"/"Fine-tune" greyed with "Coming soon" sub-labels (D-11 forward entries).
    - System shows ONLY a "Version" row `{versionName} ({versionCode})` in Geist Mono (D-12) — no printer/Klipper info, no restart control. Source assertion.
    - Palette-mode persists via `mutateActiveProfile`/global fallback through writeScope (no composition scope).
    - `:app:assembleDebug` SUCCEEDS; RootController call site compiles.
  </acceptance_criteria>
  <done>Settings is the hybrid hub with the five ordered sections, the accent picker removed, the palette-mode row + Edit-theme seam added, greyed forward entries, and version-only System.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: On-device flox UAT — Settings hub + theme-editor flow</name>
  <action>Build/sign/install the debug APK on flox, then drive the verification steps below with the user; this is a blocking human-verify gate — do NOT write the SUMMARY or mark the plan complete until the user reports results.</action>
  <what-built>The redesigned Settings hub + pushed theme-editor sub-page (color wheel with settle-regen, preset swatches, generated-swatch-strip preview, per-slot pool override grid, Randomize/Reset), wired to the seed-generated engine and durable per-profile persistence. Built/signed/installed on flox.</what-built>
  <how-to-verify>
    On the real flox device (LineageOS/API 30, Adreno 320), with a live printer profile active:
    1. Open Settings → confirm the five sections in order (Printers, Connection, Appearance, Feature toggles, System); the old accent picker is gone; greyed "Coming soon" placeholders show for Output controls/WebRTC/Fine-tune; System shows version+build only.
    2. Appearance → tap the palette-mode chips Colorful/Simple/High-contrast → confirm a VISIBLE difference each (Colorful = full color pool; Simple = mono; High-contrast = stoplight status). Toggle dark/light → backgrounds are PURE NEUTRAL (no cool tint, D-16). Toggle S/M/L → fonts scale, none too small.
    3. Tap "Edit theme…" → the editor pushes. Drag the color wheel handle → the handle tracks the finger but the app does NOT retheme mid-drag; on LIFT the whole app rethemes ONCE (D-07 settle-regen — confirm no per-pixel regen-storm / no jank). Tap a preset swatch → reseeds on land.
    4. Preview strip shows accent · pool · status swatches matching the live app.
    5. Pool colors grid → tap a slot, set an override → that slot recolors + shows an override marker, and the live data surfaces (graph/readouts) update. Randomize (amber) → pool reshuffles (poolShift), accent unchanged. Reset (red, guarded) → confirm → overrides + poolShift clear back to the seed-derived palette.
    6. Reseed while the printer is connected → the connection stays UP (no reconnect/Splash), no black-screen flash (no churn, no flicker).
    7. Force-stop + relaunch → the chosen seed/mode/overrides PERSIST (per-profile).
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (which step, what you saw vs expected).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| editor input (wheel/override) → durable write surface | User-driven theme edits cross into persistence; writes must route through writeScope and the sanitized tuple, never a cancellable composition scope. |
| theme reseed → connection spine | A reseed must not disturb the live socket (theme is connection-independent). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-06-01 | Denial of Service | per-pixel regen-storm on wheel drag janks the Adreno-320 floor | mitigate | ONE `awaitEachGesture`, regen on `onSettle` (pointer-up) ONLY (D-07); UAT step 3 confirms no mid-drag retheme. |
| T-15-06-02 | Tampering | theme edit dropped by same-frame nav cancellation | mitigate | All writes via AppContainer writeScope/mutateActiveProfile ([[dinghy-compose-write-scope-cancellation]]); UAT step 7 confirms persistence survives relaunch. |
| T-15-06-03 | Denial of Service | a reseed churns the connection spine / black-screens | mitigate | Theme excluded from ConnectionConfig (15-05); resolver single re-emit; UAT step 6 confirms no churn/flicker. |
| T-15-06-04 | Tampering | re-introducing raw color literals (token-purity regression) | mitigate | Only swatch/wheel render literal DATA color (precedented carve-out); all chrome via LocalTokens; source review. |
| T-15-06-SC | Tampering | npm/pip/cargo installs | accept | Zero external packages (RESEARCH §"Package Legitimacy Audit": N/A). |
</threat_model>

<verification>
- `:app:assembleDebug` SUCCEEDS; RootController call site compiles.
- On-device flox UAT 7-step sign-off (settle-regen, modes, pure-neutral, persistence, no churn).
</verification>

<success_criteria>
- Hybrid Settings hub + pushed theme-editor sub-page per UI-SPEC; accent picker removed.
- Settle-regen wheel (D-07); preview strip; per-slot overrides; Randomize/Reset; greyed forward entries; version-only System.
- On-device sign-off: live retheme, modes, pure-neutral surfaces, persistence, no connection churn.
</success_criteria>

<output>
Create `.planning/phases/15-theme-system-settings-redesign/15-06-SUMMARY.md` when the UAT passes.
</output>
