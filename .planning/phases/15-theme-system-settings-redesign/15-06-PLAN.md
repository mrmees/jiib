---
phase: 15-theme-system-settings-redesign
plan: 06
type: execute
wave: 5
depends_on:
  - 15-05
files_modified:
  - app/src/main/java/works/mees/dinghy/ui/screen/SettingsScreen.kt
  - app/src/main/java/works/mees/dinghy/ui/screen/ThemeEditorScreen.kt
  - app/src/main/java/works/mees/dinghy/designsystem/ColorWheel.kt
  - app/src/main/java/works/mees/dinghy/ui/shell/AppShell.kt
  - app/src/main/java/works/mees/dinghy/ui/shell/RootController.kt
  - app/src/main/java/works/mees/dinghy/ui/shell/ShellNavState.kt
  - app/src/main/java/works/mees/dinghy/gallery/GalleryScreen.kt
  - app/src/main/java/works/mees/dinghy/theme/ThemeResolver.kt
  - app/src/main/java/works/mees/dinghy/theme/ThemeTokens.kt
  - app/src/main/java/works/mees/dinghy/config/Profile.kt
  - app/src/test/java/works/mees/dinghy/theme/ThemeResolverTest.kt
  - app/src/test/java/works/mees/dinghy/theme/FontScaleTest.kt
  - app/src/test/java/works/mees/dinghy/theme/ThemePrefsFallbackTest.kt
  - app/src/test/java/works/mees/dinghy/prompt/PromptStyleColorsTest.kt
  - app/src/test/java/works/mees/dinghy/config/ProfileStoreTest.kt
  - app/src/test/java/works/mees/dinghy/di/ActiveConfigDerivationTest.kt
autonomous: false
requirements:
  - "D-04 (seed-only chrome — COMPLETE the two-step retirement: DELETE the @Deprecated TokenDelta/setBase/setDeltas/resolve shims + Profile.themeBase/themeDeltaArgb + obsolete tests, now that SettingsScreen/GalleryScreen are rebuilt — retired BY PHASE END)"
  - "D-06 (seed picker = touch color wheel/ring + curated preset seed swatches)"
  - "D-07 (regenerate + retheme on release/settle, not per-pixel)"
  - "D-08 (preview = live app retheme + a generated-swatch strip)"
  - "D-09 (Randomize button + per-slot data-pool override; Reset clears overrides + poolShift)"
  - "D-10 (hybrid structure — flat Settings hub + a pushed theme-editor sub-page)"
  - "D-11 (scaffold all sections now, greyed forward entries for not-yet-built features)"
  - "D-12 (System/About = app version + build only; Reset-to-default-seed lives in the editor)"
  - "D-15 (default palette mode = Colorful; Simple + High-Contrast user-selectable)"
must_haves:
  truths:
    - "Settings is a hybrid hub: Printers · Connection · Appearance · Feature toggles · System, with the old accent picker REMOVED and a palette-mode chip row + 'Edit theme…' forward entry added (D-10/D-11/D-15)"
    - "A pushed theme-editor sub-page hosts the color wheel + presets + generated-swatch-strip preview + per-slot pool override grid + Randomize/Reset (D-06/D-08/D-09)"
    - "The editor open-state is hosted INSIDE SettingsScreen so BOTH entry paths reach it — the AppShell Dest.Settings route AND the RootController first-run/Connect/settings-escape render (RootController.kt:105)"
    - "The color wheel regenerates + rethemes the app on gesture SETTLE only (D-07), never per-pixel; the hue-ring is drawn via drawWithCache + a pre-built sweep gradient (not arc-by-arc per drag frame) to protect the Adreno-320 budget"
    - "The two-step D-04 retirement COMPLETES: the @Deprecated TokenDelta/setBase/setDeltas/resolve shims + Profile.themeBase/themeDeltaArgb are DELETED; GalleryScreen is re-pointed to the new seed API; the obsolete TokenDelta-shaped tests are deleted/rewritten; zero references to the retired symbols remain"
    - "Greyed capability-gated placeholders scaffold outputs/WebRTC/fine-tune; System shows version+build only (D-11/D-12)"
    - "On-device flox UAT confirms the editor flow rethemes live with no per-pixel regen-storm and no connection churn"
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/ui/screen/ThemeEditorScreen.kt"
      provides: "Pushed theme-editor sub-page (wheel/presets/preview/pool-override/Randomize/Reset)"
      contains: "fun ThemeEditorScreen"
    - path: "app/src/main/java/works/mees/dinghy/designsystem/ColorWheel.kt"
      provides: "Compose Canvas hue-ring (drawWithCache sweep gradient) with awaitEachGesture settle-regen"
      contains: "fun ColorWheel"
    - path: "app/src/main/java/works/mees/dinghy/ui/screen/SettingsScreen.kt"
      provides: "Rebuilt hybrid hub; accent picker removed; palette-mode + Edit-theme rows; editor open-state owned here"
  key_links:
    - from: "app/src/main/java/works/mees/dinghy/designsystem/ColorWheel.kt"
      to: "AppContainer theme write surface"
      via: "onSettle → setActiveSeed (regen + persist via writeScope)"
      pattern: "onSettle"
    - from: "app/src/main/java/works/mees/dinghy/ui/screen/SettingsScreen.kt"
      to: "app/src/main/java/works/mees/dinghy/ui/screen/ThemeEditorScreen.kt"
      via: "SettingsScreen owns an editor-open state and renders the editor sub-page (reachable from BOTH AppShell + RootController)"
      pattern: "ThemeEditorScreen"
---

<objective>
Rebuild `SettingsScreen` into the hybrid hub (Printers · Connection · Appearance · Feature toggles · System with greyed forward entries) and build the pushed theme-editor sub-page — a Compose Canvas color wheel (settle-regen, drawWithCache sweep gradient), curated preset swatches, a generated-swatch-strip preview, a per-slot pool-override grid, and Randomize/Reset — wired to the durable theme write surface. **The editor open-state is hosted INSIDE `SettingsScreen` (a local back-stack the screen owns), so both entry paths reach it: the AppShell `Dest.Settings` route AND the `RootController` first-run/Connect/settings-escape render.** Remove the old accent picker, then COMPLETE the two-step D-04 retirement: delete the `@Deprecated` `TokenDelta`/`setBase`/`setDeltas`/`resolve` shims + `Profile.themeBase`/`themeDeltaArgb`, re-point `GalleryScreen` to the new seed API, and delete/rewrite the obsolete TokenDelta-shaped tests. Verify on-device on flox.

Purpose: This is the real test/config surface for the whole engine, and the wave where the legacy chrome-override API finally dies (it survived waves 3-4 only as compile shims). The editor must regenerate on gesture-settle only (D-07) — with a cached sweep-gradient ring (no per-frame arc redraw) — to protect the Adreno-320 floor. A reseed must retheme live with no connection churn and no flicker. RootController renders SettingsScreen independently of AppShell for the first-run/escape case, so the editor MUST be reachable from the screen itself, not only via an AppShell-level flag. This project has a documented string of green-suite-vs-reality misses, so an on-device UAT is mandatory.
Output: rebuilt `SettingsScreen.kt` (owns editor state), new `ThemeEditorScreen.kt` + `ColorWheel.kt`, AppShell/RootController/ShellNavState routing, retired legacy API + cleaned tests, on-device sign-off.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-theme-system-settings-redesign/15-UI-SPEC.md
@.planning/phases/15-theme-system-settings-redesign/15-RESEARCH.md
@.planning/phases/15-theme-system-settings-redesign/15-PATTERNS.md
@docs/ui_design/THEMING.md
@docs/ui_design/LAYOUT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the ColorWheel control + theme-editor sub-page; host editor open-state inside SettingsScreen</name>
  <read_first>
    - app/src/main/java/works/mees/dinghy/designsystem/ScrubberPage.kt (lines 153-176 — the ONE awaitEachGesture settle-on-up WR-01 pattern; the gesture analog for the wheel)
    - app/src/main/java/works/mees/dinghy/ui/temperature/TemperatureScreen.kt (line ~200 — the state-gated full-screen overlay sub-page hosting pattern; the IN-SCREEN-OWNED open-state model to mirror for the editor)
    - app/src/main/java/works/mees/dinghy/ui/shell/RootController.kt (lines 32/39 the routing doc; lines 103-111 — the FIRST-RUN/Connect/settings-escape `SettingsScreen(container, onConnectionSaved)` render that BYPASSES AppShell; this is why the editor open-state must live INSIDE SettingsScreen, not at AppShell level)
    - app/src/main/java/works/mees/dinghy/ui/shell/AppShell.kt (lines 386-387 BackHandler; line 431 swipe-drawer suppression set; line 602 the OTHER `Dest.Settings → SettingsScreen(...)` render)
    - app/src/main/java/works/mees/dinghy/ui/shell/ShellNavState.kt (state holder — only needed if the editor flag is hoisted; default is to OWN it inside SettingsScreen)
    - app/src/main/java/works/mees/dinghy/designsystem/control/OutlinedControl.kt (the Intent → outline color map; Warn/Danger/Go)
    - app/src/main/java/works/mees/dinghy/designsystem/ConfirmGuard.kt (the Reset confirm guard)
    - app/src/main/java/works/mees/dinghy/ui/screen/SettingsScreen.kt (lines 585-628 AccentSwatch — the literal-data-color carve-out precedent for swatches)
    - .planning/phases/15-theme-system-settings-redesign/15-UI-SPEC.md (§"New surface — Theme-editor sub-page" — the full section order, intents, copy, states, fsSp floors)
    - .planning/phases/15-theme-system-settings-redesign/15-PATTERNS.md (§NEW color-wheel control + §NEW theme-editor sub-page — routing)
  </read_first>
  <action>
    Create `designsystem/ColorWheel.kt`: `fun ColorWheel(hue: Float, onHandleMove: (Float)->Unit, onSettle: (Float)->Unit, modifier)` — a Compose `Canvas` hue-ring sized by its cell via `aspect-ratio(1f)` (sacred circle, must render circular) using ONE `pointerInput`/`awaitEachGesture` mirroring ScrubberPage: `onHandleMove(hueAt(pos))` on each move (cheap — repaint handle, NO regen), `onSettle(hue)` on pointer-up ONLY (D-07). **Draw the hue-ring with `Modifier.drawWithCache` + a pre-built `Brush.sweepGradient` of the hue wheel (CONFIRMED Codex finding) — the ring is cached and reused across drag frames; only the small handle repaints per move. Do NOT redraw the ring arc-by-arc each drag frame (Adreno-320 budget).** Handle has a ≥64dp touch hit area + 2dp outline + static glow (no looping animation). The ring/handle render their literal hue color (the precedented data-color carve-out, like AccentSwatch); all chrome routes through `LocalTokens.current`.
    Create `ui/screen/ThemeEditorScreen.kt`: `fun ThemeEditorScreen(...)` — a plain `Column.verticalScroll` (FFG-exempt), token-themed, sized via `fsSp(baseSp, t.fs)` floors (title 22sp / SectionLabel 20sp / body 17sp / hex Geist-Mono 17sp / metadata 15sp per UI-SPEC — honor `[[dinghy-font-sizes-too-small]]`). Sections top→bottom: (1) "Seed color" → the ColorWheel, `onSettle` → `container.setActiveSeed(hueToSeedHex)`; (2) "Presets" → curated preset seed swatches (tap lands seed → settle semantics; active gets an accent ring); (3) "Preview" → the generated swatch strip (accent · pool[0..n] · status colors, each rendering its actual generated color — the carve-out); (4) "Pool colors" → a per-slot override grid: each pool[i] a ≥64dp tappable swatch (its actual/overridden color, 2dp outline); tapping opens a picker → `container.setActiveOverride(i, argb)`; overridden slots show an accent marker; DATA-POOL slots ONLY (chrome stays seed-only, D-04); (5) Actions: "Randomize" (amber `Intent.Warn`, `container.setActiveShift(random)`), "Reset" (red `Intent.Danger`, `ConfirmGuard` → `container.resetActiveTheme()`), "Done"/"Back" (green `Intent.Go`). All theme writes go through the AppContainer durable write surface from 15-05 — NEVER a `rememberCoroutineScope()`.
    **Host the editor open-state INSIDE `SettingsScreen` (CONFIRMED Codex finding — RootController dual-path):** SettingsScreen owns a local `var editorOpen by rememberSaveable { mutableStateOf(false) }` (or a small in-screen back-stack); the "Edit theme…" row sets it true; the screen renders `ThemeEditorScreen` when open else the hub, with a `BackHandler(editorOpen) { editorOpen = false }`. Because BOTH `RootController.kt:105` (first-run/escape) and `AppShell.kt:602` (Dest.Settings route) render `SettingsScreen` the same way, owning the state in the screen makes the editor reachable from BOTH paths with no AppShell/RootController wiring divergence. (Do NOT hoist the open-flag to AppShell/ShellNavState — that would strand the RootController first-run path.) The Task-1 deliverable here is the wheel + editor + the in-screen hosting hook; Task 2 finishes the hub body and the "Edit theme…" row.
  </action>
  <verify>
    <automated>cmd.exe /c "E:\Android\gw.bat :app:assembleDebug --no-daemon" | tr -d '\r'</automated>
  </verify>
  <acceptance_criteria>
    - `designsystem/ColorWheel.kt` exists with ONE `awaitEachGesture` and a distinct `onSettle` callback fired on pointer-up only (D-07). Verify: `grep -c "awaitEachGesture" ColorWheel.kt` returns 1; `grep -n "onSettle" ColorWheel.kt` matches.
    - The hue-ring uses `drawWithCache` + a sweep gradient (cached, not per-frame arc redraw). Verify: `grep -nE "drawWithCache|sweepGradient" app/src/main/java/works/mees/dinghy/designsystem/ColorWheel.kt` matches.
    - `ui/screen/ThemeEditorScreen.kt` exists with the 5 sections and Randomize=`Intent.Warn`, Reset=`Intent.Danger` (ConfirmGuard), Done=`Intent.Go`. Source assertions.
    - The editor open-state lives INSIDE SettingsScreen (rememberSaveable / in-screen back-stack), NOT hoisted to AppShell/ShellNavState. Verify: SettingsScreen owns the editorOpen state + a BackHandler; no editor-open flag added to ShellNavState.
    - All theme writes call the AppContainer durable surface; NO `rememberCoroutineScope()` in the write path. Verify: `grep -n "rememberCoroutineScope" ThemeEditorScreen.kt` returns nothing in a write path.
    - Editor/wheel labels use `fsSp(...)`. Verify grep for `fsSp(` present.
    - `:app:assembleDebug` SUCCEEDS.
  </acceptance_criteria>
  <done>The color wheel (settle-regen, cached sweep-gradient ring) + theme-editor sub-page exist, the editor open-state is owned inside SettingsScreen (reachable from both AppShell + RootController), and all writes route through the durable surface; the app assembles.</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild SettingsScreen into the hybrid hub; remove accent picker; COMPLETE the D-04 retirement (delete shims + legacy fields + obsolete tests; re-point GalleryScreen)</name>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/screen/SettingsScreen.kt (whole file — lines 174-180 verticalScroll/padding; 181-220 Printers/header; 184-415 Connection; 423-468 Appearance dark/light + S/M/L + the accent picker to REMOVE; 466-492 + 553-628 AccentSwatch/ACCENT_PALETTE/persistDeltas to delete; ~377/529/560/562 the themeBase/themeDeltaArgb create/copy sites to drop; 520-547 persistBase/persistFs helpers to reuse for palette-mode)
    - app/src/main/java/works/mees/dinghy/ui/screen/ThemeEditorScreen.kt (the editor this hub pushes to — from Task 1)
    - app/src/main/java/works/mees/dinghy/gallery/GalleryScreen.kt (lines 44 import TokenDelta; 141/147 resolver.setBase; 155 resolver.setDeltas + TokenDelta.EMPTY; 165 resolver.setFs; 307-311 SAMPLE_CUSTOM_DELTA = TokenDelta.of(...) — RE-POINT to the new seed API: setBase→setDark, the custom-delta toggle→a seed/mode demo or drop it, setFs stays)
    - app/src/main/java/works/mees/dinghy/theme/ThemeResolver.kt (DELETE the @Deprecated TokenDelta/Role/setBase/setDeltas/resolve shims now that no live consumer remains)
    - app/src/main/java/works/mees/dinghy/theme/ThemeTokens.kt (DELETE the @Deprecated violet shim is NOT here — that is 15-07; here only the TokenDelta-related KDoc cleanup if any)
    - app/src/main/java/works/mees/dinghy/config/Profile.kt (DELETE the @Deprecated themeBase/themeDeltaArgb fields now that SettingsScreen no longer sets them)
    - the obsolete tests: app/src/test/java/works/mees/dinghy/theme/FontScaleTest.kt (resolve(...,TokenDelta.EMPTY,...) → rewrite to the new API or a retained fs-only check), ThemePrefsFallbackTest.kt (already reworked in 15-05 — verify no TokenDelta ref remains), prompt/PromptStyleColorsTest.kt (resolve(Dark,TokenDelta.EMPTY,1f) → rewrite to build a default-seed ThemeTokens via the new resolver/bridge), config/ProfileStoreTest.kt (themeBase test factory L70-71/153-154 → drop the param), di/ActiveConfigDerivationTest.kt (themeBase L40-41 → drop the param), theme/ThemeResolverTest.kt (delete any remaining shim-backed setBase/setDeltas/resolve tests left from 15-04)
    - .planning/phases/15-theme-system-settings-redesign/15-UI-SPEC.md (§"Reused-surface contracts" + §Copywriting)
    - .planning/phases/15-theme-system-settings-redesign/15-PATTERNS.md (§MODIFY SettingsScreen.kt)
  </read_first>
  <action>
    Rebuild `ui/screen/SettingsScreen.kt` keeping the existing `Column.verticalScroll(rememberScrollState()).padding(16.dp)` grammar and reusing `SectionHeader`/`SectionLabel`/`ProfileRow`/`AddPrinterRow`/`OutlinedControl`/`TokenTextField` VERBATIM. Sections in order (D-11): "Printers" (Phase-14 profile CRUD — reused; DROP the `themeBase`/`themeDeltaArgb` args from any profile create/copy sites ~377/529/560/562) · "Connection" (reused) · "Appearance" (the existing dark/light chips + S/M/L chips UNCHANGED + a NEW "Palette mode" chip row of three `OutlinedControl` chips "Colorful"(default)/"Simple"/"High contrast" — active=`Intent.Accent`+`--accent-soft`, inactive=`Intent.Neutral`, persisting via a `persistMode` helper modeled on `persistBase`/`persistFs` routing through `mutateActiveProfile`/global-fallback + a NEW "Edit theme…" forward-entry row, sub-label "Seed color, palette & pool", that sets the in-screen `editorOpen=true` from Task 1) · "Feature toggles" ("Webcam" live; "Output controls"/"Camera (WebRTC)"/"Fine-tune" greyed disabled, faint `t.text3`, "Coming soon") · "System" (one "Version" row → `{versionName} ({versionCode})` in Geist Mono — D-12; NO printer/Klipper/Moonraker info, NO restart). REMOVE the accent picker entirely: delete `AccentSwatch`, `ACCENT_PALETTE`, `persistDeltas`. Honor `fsSp` floors. RootController.kt:105 + AppShell.kt:602 call sites: verify both still compile against the new signature (update the call if the param list changed — keep them IDENTICAL so both paths behave the same).

    **COMPLETE the two-step D-04 retirement (the shims survived waves 3-5 only to keep compilation green; now delete them):**
    - In `theme/ThemeResolver.kt`: DELETE `data class TokenDelta`, `Role`, `TokenDelta.of`/`EMPTY`, the `@Deprecated` `setBase`/`setDeltas` shims, and the top-level `resolve(base, deltas, fs)` shim (+ the old 3-arg `apply` overload if it lingers). `setFs` stays (fs is unchanged this phase) unless it was only a shim — keep the real fs path.
    - In `config/Profile.kt`: DELETE the `@Deprecated themeBase`/`themeDeltaArgb` fields from `Profile` + `PersistedProfile` + the round-trip (old persisted blobs still decode via `ignoreUnknownKeys` — fresh-start, no migration; the keys are simply ignored).
    - RE-POINT `gallery/GalleryScreen.kt`: remove `import TokenDelta` (L44) and `SAMPLE_CUSTOM_DELTA` (L307-311); change `resolver.setBase(ThemeBase.Dark/Light)` (L141/147) → `resolver.setDark(true/false)`; replace the custom-delta toggle (L155 `resolver.setDeltas(...)`) with either a seed/mode demo using the new API (e.g. toggle a sample seed via `setSeed`) or drop that toggle row; `resolver.setFs(choice.multiplier)` (L165) stays. The gallery must demonstrate the NEW model, not the retired one.
    - DELETE/REWRITE the obsolete tests to the new model: `prompt/PromptStyleColorsTest.kt` — replace `resolve(ThemeBase.Dark, TokenDelta.EMPTY, 1.0f)` with a default-seed `ThemeTokens` built via the new resolver/`TokenBridge` (the `promptStyleColor` mapping contract is unchanged; only the token-source construction changes). `theme/FontScaleTest.kt` — replace the `resolve(..., TokenDelta.EMPTY, fs)` calls with the new-API equivalent that still asserts `fs` flows through (or assert fs on the resolver's emitted tokens). `theme/ThemeResolverTest.kt` — delete any residual shim-backed `setBase`/`setDeltas`/`resolve` tests (the new-API tests from 15-04 stay). `config/ProfileStoreTest.kt` (L70-71/153-154) + `di/ActiveConfigDerivationTest.kt` (L40-41) — drop the `themeBase` test-factory param/args (those fields no longer exist). `theme/ThemePrefsFallbackTest.kt` — confirm 15-05's rework left no `TokenDelta` reference.
  </action>
  <verify>
    <automated>cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --no-daemon" | tr -d '\r' ; echo '--- assemble ---' ; cmd.exe /c "E:\Android\gw.bat :app:assembleDebug --no-daemon" | tr -d '\r'</automated>
  </verify>
  <acceptance_criteria>
    - Section labels in order: "Printers", "Connection", "Appearance", "Feature toggles", "System". Verify greps.
    - The accent picker is GONE. Verify: `grep -nE "AccentSwatch|ACCENT_PALETTE|persistDeltas" app/src/main/java/works/mees/dinghy/ui/screen/SettingsScreen.kt` returns nothing.
    - Appearance has the 3-chip palette-mode row + an "Edit theme…" row that opens the in-screen editor; Feature toggles shows Webcam live + 3 greyed "Coming soon"; System shows ONLY a version row (D-12).
    - **D-04 retirement COMPLETE — zero references to the retired symbols remain across `app/src`.** Verify ALL of:
      - `grep -rc "TokenDelta" app/src/` returns 0 (main + test).
      - `grep -rcE "\.setBase\(|\.setDeltas\(" app/src/` returns 0.
      - `grep -rcE "themeBase|themeDeltaArgb" app/src/` returns 0.
      - the top-level `resolve(` chrome shim is gone from ThemeResolver.kt (`grep -nE "fun resolve\(" app/src/main/java/works/mees/dinghy/theme/ThemeResolver.kt` returns nothing).
    - `GalleryScreen.kt` uses the new seed API (`setDark`/`setSeed`/`setFs`), no `TokenDelta`. Verify grep.
    - `FontScaleTest`, `ThemePrefsFallbackTest`, `PromptStyleColorsTest`, `ProfileStoreTest`, `ActiveConfigDerivationTest`, `ThemeResolverTest` all COMPILE and PASS on the new model (no TokenDelta/themeBase refs). The FULL `:app:testDebugUnitTest` is GREEN (whole test sourceset compiles + passes).
    - `:app:assembleDebug` SUCCEEDS; RootController.kt:105 + AppShell.kt:602 call sites compile identically.
  </acceptance_criteria>
  <done>Settings is the hybrid hub with the five ordered sections + the editor seam; the accent picker is removed; and the D-04 two-step retirement is COMPLETE — the TokenDelta/setBase/setDeltas/resolve shims + Profile.themeBase/themeDeltaArgb are deleted, GalleryScreen is on the new seed API, the obsolete tests are reworked, and the full unit suite + assemble are green with zero references to the retired symbols.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: On-device flox UAT — Settings hub + theme-editor flow (both entry paths)</name>
  <action>Build/sign/install the debug APK on flox, then drive the verification steps below with the user; this is a blocking human-verify gate — do NOT write the SUMMARY or mark the plan complete until the user reports results.</action>
  <what-built>The redesigned Settings hub + pushed theme-editor sub-page (color wheel with settle-regen + cached sweep-gradient ring, preset swatches, generated-swatch-strip preview, per-slot pool override grid, Randomize/Reset), reachable from BOTH the in-shell Settings route and the first-run/escape path, wired to the seed-generated engine and durable per-profile persistence; legacy TokenDelta chrome-override API fully retired. Built/signed/installed on flox.</what-built>
  <how-to-verify>
    On the real flox device (LineageOS/API 30, Adreno 320), with a live printer profile active:
    1. Open Settings → confirm the five sections in order; old accent picker gone; greyed "Coming soon" placeholders; System shows version+build only.
    2. Appearance → tap palette-mode chips Colorful/Simple/High-contrast → confirm a VISIBLE difference each. Toggle dark/light → backgrounds PURE NEUTRAL (no cool tint, D-16). Toggle S/M/L → fonts scale, none too small.
    3. Tap "Edit theme…" → editor pushes. Drag the wheel handle → handle tracks the finger, app does NOT retheme mid-drag, NO jank from the ring (cached gradient); on LIFT the app rethemes ONCE (D-07). Tap a preset → reseeds on land.
    4. Preview strip shows accent · pool · status swatches matching the live app.
    5. Pool colors grid → set an override → that slot recolors + marker, live data surfaces update. Randomize (amber) → pool reshuffles, accent unchanged. Reset (red, guarded) → confirm → overrides + poolShift clear.
    6. Reseed while connected → the connection stays UP (no reconnect/Splash), no black-screen flash.
    7. Force-stop + relaunch → seed/mode/overrides PERSIST.
    8. FIRST-RUN/ESCAPE PATH: from the Splash/Connect surface (or "Edit connection"), reach Settings via the escape path → confirm "Edit theme…" STILL opens the editor here too (the RootController path, not just the in-shell drawer route).
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (which step, what you saw vs expected).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| editor input (wheel/override) → durable write surface | User-driven theme edits cross into persistence; writes must route through writeScope and the sanitized tuple, never a cancellable composition scope. |
| theme reseed → connection spine | A reseed must not disturb the live socket (theme is connection-independent). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-06-01 | Denial of Service | per-pixel regen-storm OR per-frame ring redraw janks the Adreno-320 floor | mitigate | ONE `awaitEachGesture`, regen on `onSettle` only (D-07); hue-ring via `drawWithCache`+sweep gradient (cached, not arc-by-arc); UAT step 3 confirms no mid-drag retheme/jank. |
| T-15-06-02 | Tampering | theme edit dropped by same-frame nav cancellation | mitigate | All writes via AppContainer writeScope/mutateActiveProfile; UAT step 7 confirms persistence survives relaunch. |
| T-15-06-03 | Denial of Service | a reseed churns the connection spine / black-screens | mitigate | Theme excluded from ConnectionConfig (15-05); resolver single re-emit; UAT step 6 confirms no churn/flicker. |
| T-15-06-04 | Spoofing | editor unreachable on the first-run/escape path (RootController renders SettingsScreen sans AppShell) | mitigate | Editor open-state hosted INSIDE SettingsScreen; UAT step 8 confirms the editor opens from the RootController path too. |
| T-15-06-05 | Tampering | a stale retired symbol left behind → dead chrome-override path | mitigate | D-04 retirement completed: grep-asserted zero `TokenDelta`/`setBase`/`setDeltas`/`themeBase` refs across app/src. |
| T-15-06-SC | Tampering | npm/pip/cargo installs | accept | Zero external packages (RESEARCH §"Package Legitimacy Audit": N/A). |
</threat_model>

<verification>
- Full `:app:testDebugUnitTest` GREEN + `:app:assembleDebug` SUCCEEDS; zero references to the retired symbols (grep-asserted).
- Editor reachable from BOTH AppShell + RootController paths (state owned in SettingsScreen).
- On-device flox UAT 8-step sign-off (settle-regen, cached ring, modes, pure-neutral, persistence, no churn, both entry paths).
</verification>

<success_criteria>
- Hybrid Settings hub + pushed theme-editor sub-page per UI-SPEC; accent picker removed; editor reachable from both entry paths.
- Settle-regen wheel with cached sweep-gradient ring (D-07); preview strip; per-slot overrides; Randomize/Reset; greyed forward entries; version-only System.
- D-04 two-step retirement COMPLETE: shims + legacy Profile fields + obsolete tests deleted/reworked; GalleryScreen on the new API; zero retired-symbol references.
- On-device sign-off: live retheme, modes, pure-neutral surfaces, persistence, no connection churn, both entry paths.
</success_criteria>

<output>
Create `.planning/phases/15-theme-system-settings-redesign/15-06-SUMMARY.md` when the UAT passes.
</output>
