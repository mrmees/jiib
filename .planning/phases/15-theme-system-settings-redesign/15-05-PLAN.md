---
phase: 15-theme-system-settings-redesign
plan: 05
type: execute
wave: 4
depends_on:
  - 15-04
files_modified:
  - app/src/main/java/works/mees/dinghy/config/Profile.kt
  - app/src/main/java/works/mees/dinghy/config/ProfileStore.kt
  - app/src/main/java/works/mees/dinghy/theme/ThemePrefs.kt
  - app/src/main/java/works/mees/dinghy/di/AppContainer.kt
  - app/src/test/java/works/mees/dinghy/theme/ProfileThemeSeedTest.kt
  - app/src/test/java/works/mees/dinghy/theme/TokenDeltaSerializationTest.kt
autonomous: true
requirements:
  - "D-03 (all theme params stored per-profile — the tuple; global fallback when no active profile)"
  - "D-05 (fresh-start, no migration — old themeBase/fsChoice/themeDeltaArgb replaced; fsChoice retained separately)"
  - "D-09 (poolShift + poolOverrides persisted with the theme; Reset clears them)"
  - "Refines THEME-02 (S/M/L fsChoice stays a separate, unchanged setting)"
must_haves:
  truths:
    - "Profile carries the theme tuple (seedHex, dark, paletteMode, poolShift, maxItems, poolOverrides); themeBase/themeDeltaArgb removed; fsChoice retained (D-03/D-05)"
    - "A junk seed/mode/shift/override sanitizes to defaults and NEVER throws or black-screens (V5 fail-safe)"
    - "Old profile blobs decode cleanly — dropped keys ignored, new tuple fields default (D-05 fresh-start, no migration)"
    - "AppContainer.seedTheme applies the full tuple in ONE call; the no-active branch collects the global theme REACTIVELY (WR-02 fix)"
    - "Theme fields stay OUT of ConnectionConfig — a theme edit does NOT churn the connection spine"
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/config/Profile.kt"
      provides: "Theme-tuple persisted primitives + sanitizing toThemeResolved; redacted toString preserved"
      contains: "val seedHex"
    - path: "app/src/main/java/works/mees/dinghy/theme/ThemePrefs.kt"
      provides: "Global theme-tuple fallback + never-throws sanitize"
    - path: "app/src/main/java/works/mees/dinghy/di/AppContainer.kt"
      provides: "seedTheme reactive no-active branch (WR-02) + single-apply re-seed"
  key_links:
    - from: "app/src/main/java/works/mees/dinghy/di/AppContainer.kt"
      to: "app/src/main/java/works/mees/dinghy/theme/ThemeResolver.kt"
      via: "seedTheme collects the tuple and calls themeResolver.apply once"
      pattern: "themeResolver\\.apply"
    - from: "app/src/main/java/works/mees/dinghy/config/Profile.kt"
      to: "ConnectionConfig"
      via: "toConnectionConfig stays host/port/apiKey-only (theme excluded)"
      pattern: "toConnectionConfig"
---

<objective>
Rework per-profile theme persistence from the retired `(themeBase, fsChoice, themeDeltaArgb)` triple to the full theme tuple `(seedHex, dark, paletteMode, poolShift, maxItems, poolOverrides)` — reusing Phase-14's `mutateActive`/`writeScope` durable write path — with a never-throws fail-safe sanitize, a fresh-start (no-migration) decode, and the WR-02 reactive no-active-branch fix in `AppContainer.seedTheme`. `fsChoice` (S/M/L) stays a separate, unchanged setting.

Purpose: This is the durable substrate the UI (15-06) writes to. It must be stable and fail-safe BEFORE the editor consumes it: the device is a printer surface that must never black-screen on a corrupt theme blob (V5). And the theme tuple must stay OUT of `ConnectionConfig` so a theme edit doesn't churn the connection spine.
Output: tupled `Profile`/`ProfileStore`/`ThemePrefs`, rewired `AppContainer.seedTheme`, extended `ProfileThemeSeedTest` + serialization test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-theme-system-settings-redesign/15-RESEARCH.md
@.planning/phases/15-theme-system-settings-redesign/15-PATTERNS.md
@.planning/phases/14-multi-printer-switching/14-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace Profile theme fields with the tuple; tuple ThemePrefs + never-throws sanitize</name>
  <read_first>
    - app/src/main/java/works/mees/dinghy/config/Profile.kt (lines 26-42 PersistedProfile + redacted toString; lines 51-59 Profile theme fields; line 66 toConnectionConfig; lines 76-82 toThemeResolved; lines 92-116 round-trip)
    - app/src/main/java/works/mees/dinghy/theme/ThemePrefs.kt (lines 39-52 catch→emptyPreferences; lines 54-58 setBase/setFs; lines 78-91 Resolved + DEFAULT; lines 103+ sanitize — the never-throws contract to preserve)
    - app/src/main/java/works/mees/dinghy/config/ProfileStore.kt (line 128 ignoreUnknownKeys; lines 109-118 mutateActive; line 151 sanitize)
    - .planning/phases/15-theme-system-settings-redesign/15-RESEARCH.md (§"Per-Profile Persistence Rework" — exact field defaults + the sanitize rules + V5/V7 security)
    - .planning/phases/15-theme-system-settings-redesign/15-PATTERNS.md (§MODIFY Profile.kt/ProfileStore.kt + §"Deterministic fail-safe sanitize")
  </read_first>
  <action>
    In `config/Profile.kt`: on BOTH `PersistedProfile` (@Serializable wire form) and `Profile` (runtime), REMOVE `themeBase: String` and `themeDeltaArgb: Map<String,Long>`; KEEP `fsChoice: String` unchanged (D-05). ADD the tuple primitives with defaults: `seedHex: String = "#3f78ff"`, `dark: Boolean = true`, `paletteMode: String = "Colorful"`, `poolShift: Int = 0`, `maxItems: Int = 4`, `poolOverrides: Map<Int, Long> = emptyMap()` (sparse poolIndex → unsigned-32 ARGB). Update the `PersistedProfile`↔`Profile` round-trip (lines 92-116) to carry the new fields. PRESERVE the redacted `toString` (lines 39-42, 97-100) — replace the `themeBase=…themeDeltaArgb=` portion with the new theme fields, but keep `apiKey=***` redaction; theme fields carry NO secrets, do NOT widen the redaction surface (V7). KEEP `toConnectionConfig()` (line 66) host/port/apiKey-ONLY — the theme tuple MUST NOT enter `ConnectionConfig` (so a theme edit yields an EQUAL ConnectionConfig and does not churn the spine). Rewire `toThemeResolved()` (lines 76-82) to return the new theme-tuple resolved value (or feed straight into the resolver), reusing the `ThemePrefs.sanitize` parity.
    In `theme/ThemePrefs.kt`: KEEP the `.catch { IOException → emptyPreferences() }` + `sanitize` never-throws contract. Replace the persisted keys + `Resolved` (lines 78-91) with the tuple. `DEFAULT` = the validated default seed (`#3f78ff`/dark/Colorful/0/4/empty). Replace `setBase`→`setSeed`/`setDark`/`setMode`/`setShift`/`setMaxItems`/`setOverrides`; KEEP `setFs` unchanged. EXTEND `sanitize`: unparseable `seedHex` (not a valid 6/8-digit hex) → default seed; bad `paletteMode` (not in Colorful|Simple|HighContrast) → Colorful; junk `poolShift` (out of 0..360) / `maxItems` (out of a sane 1..64 range) → defaults; a garbage `poolOverrides` entry (bad index or ARGB) → DROP just that index, keep the rest. NEVER throws, NEVER black-screens (the Phase-3 D-02 deterministic fail-safe contract).
    In `config/ProfileStore.kt`: `mutateActive` (lines 109-118) is REUSED VERBATIM as the durable lost-update-safe write path for theme edits. `ignoreUnknownKeys=true` (line 128) means old blobs with the dropped `themeBase`/`themeDeltaArgb` keys decode cleanly — old keys ignored, new fields default (D-05 fresh-start, NO migration code).
  </action>
  <verify>
    <automated>cmd.exe /c "E:\Android\gw.bat :app:compileDebugKotlin --no-daemon" | tr -d '\r'</automated>
  </verify>
  <acceptance_criteria>
    - `Profile` and `PersistedProfile` declare `seedHex`/`dark`/`paletteMode`/`poolShift`/`maxItems`/`poolOverrides` and NO `themeBase`/`themeDeltaArgb`. Verify: `grep -nE "themeBase|themeDeltaArgb" app/src/main/java/works/mees/dinghy/config/Profile.kt` returns nothing; `grep -n "val seedHex" Profile.kt` matches.
    - `fsChoice` retained on both forms (D-05). Verify grep matches.
    - `toString` still redacts apiKey as `***` and carries NO secret in the new theme fields (V7). Verify: `grep -n "apiKey=\${if" Profile.kt`-equivalent redaction present.
    - `toConnectionConfig()` constructs `ConnectionConfig` from host/port/apiKey ONLY (no theme field). Source assertion.
    - `ThemePrefs.sanitize` maps junk seed→default / bad mode→Colorful / out-of-range shift|maxItems→defaults / bad override entry→dropped, and never throws.
    - `:app:compileDebugKotlin` SUCCEEDS.
  </acceptance_criteria>
  <done>`Profile`/`ProfileStore`/`ThemePrefs` carry the theme tuple with a never-throws sanitize, fresh-start decode, redaction preserved, and theme kept out of `ConnectionConfig`.</done>
</task>

<task type="auto">
  <name>Task 2: Rewire AppContainer.seedTheme (single-apply + WR-02 reactive) + extend persistence tests</name>
  <read_first>
    - app/src/main/java/works/mees/dinghy/di/AppContainer.kt (lines 105 writeScope; 128-130 mutateActiveProfile; 138-151 activeProfile/activeConfig; 185-186 themeResolver = ThemeResolver(); 314-324 seedTheme — the one-shot firstOrNull to make reactive)
    - app/src/test/java/works/mees/dinghy/theme/ProfileThemeSeedTest.kt (lines 44-93 — the corrupt-primitive table idiom to extend)
    - app/src/test/java/works/mees/dinghy/theme/TokenDeltaSerializationTest.kt (the serialization round-trip to repurpose for fresh-start decode)
    - .planning/phases/15-theme-system-settings-redesign/15-RESEARCH.md (§"WR-02 fix" + §"Durable write path")
    - .planning/phases/15-theme-system-settings-redesign/15-PATTERNS.md (§MODIFY AppContainer.kt — the reactive no-active branch)
  </read_first>
  <action>
    In `di/AppContainer.kt`: update the `themeResolver` construction (line 186) to use the validated default seed (matching ThemeResolver's new default ctor from 15-04). Rewire `seedTheme(scope)` (lines 314-324): KEEP applying the full tuple in ONE `themeResolver.apply(...)` call (no flicker), but FIX the no-active branch to be REACTIVE (WR-02): replace `flowOf(p?.toThemeResolved() ?: themePrefs.flow.firstOrNull() ?: DEFAULT)` with a `flatMapLatest { p -> if (p != null) flowOf(p.toThemeResolved()) else themePrefs.flow }` so a global-default edit while idle re-emits (mirror the active branch). The collector calls `themeResolver.apply(<full tuple from resolved>)`. Ensure theme edits route through `mutateActiveProfile { it.copy(seedHex = …) }` (active) / `themePrefs.set*` via `writeScope` (idle) — NEVER a `rememberCoroutineScope()` (`[[dinghy-compose-write-scope-cancellation]]`). Add the corresponding theme-edit intent helpers on AppContainer if the editor (15-06) needs them (e.g. `setActiveSeed`/`setActiveMode`/`setActiveShift`/`setActiveOverride`/`resetActiveTheme` routing to mutateActiveProfile-or-global via writeScope) — these are the durable write surface 15-06 calls.
    Then EXTEND the tests: `ProfileThemeSeedTest.kt` — keep the corrupt-primitive table; add rows for junk seedHex→default, bad paletteMode→Colorful, out-of-range poolShift/maxItems→defaults, garbage poolOverrides entry→dropped, and assert NONE throw (the V5 fail-safe). `TokenDeltaSerializationTest.kt` (extend/replace) — a fresh-start decode test: a serialized old-shape blob containing `themeBase`/`themeDeltaArgb` keys decodes via `ignoreUnknownKeys` to a `Profile` where those keys are ignored and the new tuple fields take defaults; a round-trip of a new-shape `PersistedProfile` preserves the tuple. List classes explicitly.
  </action>
  <verify>
    <automated>cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --tests works.mees.dinghy.theme.ProfileThemeSeedTest --tests works.mees.dinghy.theme.TokenDeltaSerializationTest --no-daemon" | tr -d '\r'</automated>
  </verify>
  <acceptance_criteria>
    - `seedTheme`'s no-active branch is reactive — `themePrefs.flow` is flat-mapped, NOT `firstOrNull()`. Verify: `grep -n "firstOrNull" app/src/main/java/works/mees/dinghy/di/AppContainer.kt` no longer appears in the seedTheme body; `flatMapLatest` into `themePrefs.flow` present (WR-02).
    - `seedTheme` applies the tuple via a single `themeResolver.apply(...)` call.
    - `ProfileThemeSeedTest` GREEN — every junk-input row sanitizes to defaults and NO row throws.
    - `TokenDeltaSerializationTest` (or replacement) GREEN — old-blob fresh-start decode (dropped keys ignored, new fields default) + new-shape round-trip.
    - Theme edits route through `mutateActiveProfile`/`writeScope`, never a composition scope. Source assertion.
    - Test classes listed explicitly (no glob).
  </acceptance_criteria>
  <done>`AppContainer.seedTheme` re-seeds reactively (WR-02 closed) with a single apply; persistence tests prove fail-safe sanitize and fresh-start decode.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DataStore blob → Profile/ThemePrefs | Untrusted persisted bytes (possibly corrupt/old-shape) cross into the runtime theme; sanitize is the validation gate. |
| Profile → ConnectionConfig | The connection projection must exclude theme so a theme edit cannot disturb the spine. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-05-01 | Denial of Service | corrupt theme blob crashes/black-screens the printer display | mitigate | Pure `sanitize` fail-safe — junk seed/mode/shift/override → defaults per-field, drop bad override entries, NEVER throw (V5). ProfileThemeSeedTest asserts no-throw. |
| T-15-05-02 | Tampering | a theme edit churns the connection spine | mitigate | `toConnectionConfig` stays host/port/apiKey-only; `activeConfig.distinctUntilChanged` keys on those; theme fields excluded (T-14-04). |
| T-15-05-03 | Information disclosure | widened toString leaks apiKey | mitigate | `toString` keeps `apiKey=***`; new theme fields carry no secrets (V7). |
| T-15-05-04 | Tampering | persistence write dropped by same-frame nav cancellation | mitigate | All theme writes route through process-lifetime `writeScope`/`mutateActiveProfile` ([[dinghy-compose-write-scope-cancellation]]); read-modify-write in one `dataStore.edit`. |
| T-15-05-SC | Tampering | npm/pip/cargo installs | accept | Zero external packages (RESEARCH §"Package Legitimacy Audit": N/A). |
</threat_model>

<verification>
- `ProfileThemeSeedTest` + `TokenDeltaSerializationTest` GREEN; module compiles.
- seedTheme reactive (WR-02); theme out of ConnectionConfig; redaction preserved.
</verification>

<success_criteria>
- Theme tuple persisted per-profile + global fallback; fsChoice separate; fresh-start no-migration.
- Never-throws sanitize (V5); theme excluded from ConnectionConfig.
- WR-02 reactive no-active branch; durable writeScope path.
</success_criteria>

<output>
Create `.planning/phases/15-theme-system-settings-redesign/15-05-SUMMARY.md` when done.
</output>
