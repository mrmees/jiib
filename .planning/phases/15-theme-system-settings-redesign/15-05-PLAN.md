---
phase: 15-theme-system-settings-redesign
plan: 05
type: execute
wave: 4
depends_on:
  - 15-04
files_modified:
  - app/src/main/java/works/mees/dinghy/config/Profile.kt
  - app/src/main/java/works/mees/dinghy/config/ProfileStore.kt
  - app/src/main/java/works/mees/dinghy/theme/ThemePrefs.kt
  - app/src/main/java/works/mees/dinghy/di/AppContainer.kt
  - app/src/test/java/works/mees/dinghy/theme/ProfileThemeSeedTest.kt
  - app/src/test/java/works/mees/dinghy/theme/TokenDeltaSerializationTest.kt
  - app/src/test/java/works/mees/dinghy/theme/ThemePrefsFallbackTest.kt
autonomous: true
requirements:
  - "D-03 (all theme params stored per-profile — the tuple; global fallback when no active profile)"
  - "D-05 (fresh-start, no migration — old themeBase/themeDeltaArgb are DEPRECATED decoded-but-unused fields; fsChoice retained separately)"
  - "D-09 (poolShift + poolOverrides persisted with the theme; Reset clears them)"
  - "Refines THEME-02 (S/M/L fsChoice stays a separate, unchanged setting)"
must_haves:
  truths:
    - "Profile carries the theme tuple (seedHex, dark, paletteMode, poolShift, maxItems, poolOverrides); fsChoice retained; themeBase/themeDeltaArgb kept as @Deprecated ignored fields (decoded-but-unused) so existing consumers compile — deleted in 15-06 (D-03/D-05)"
    - "A junk seed/mode/shift/override sanitizes to defaults and NEVER throws or black-screens (V5 fail-safe)"
    - "poolOverrides persists as Map<String,Long> and sanitizes PER-ENTRY — one malformed entry is dropped, the good entries survive (kotlinx ignoreUnknownKeys does NOT cover malformed VALUES)"
    - "Old profile blobs decode cleanly — the new tuple fields default (D-05 fresh-start, no migration)"
    - "AppContainer.seedTheme applies the full tuple in ONE call; the no-active branch collects the global theme REACTIVELY (WR-02 fix)"
    - "Theme fields stay OUT of ConnectionConfig — a theme edit does NOT churn the connection spine"
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/config/Profile.kt"
      provides: "Theme-tuple persisted primitives + @Deprecated themeBase/themeDeltaArgb ignored fields + sanitizing toThemeResolved; redacted toString preserved"
      contains: "val seedHex"
    - path: "app/src/main/java/works/mees/dinghy/theme/ThemePrefs.kt"
      provides: "Global theme-tuple fallback + never-throws per-entry sanitize"
    - path: "app/src/main/java/works/mees/dinghy/di/AppContainer.kt"
      provides: "seedTheme reactive no-active branch (WR-02) + single-apply re-seed"
  key_links:
    - from: "app/src/main/java/works/mees/dinghy/di/AppContainer.kt"
      to: "app/src/main/java/works/mees/dinghy/theme/ThemeResolver.kt"
      via: "seedTheme collects the tuple and calls themeResolver.apply once"
      pattern: "themeResolver\\.apply"
    - from: "app/src/main/java/works/mees/dinghy/config/Profile.kt"
      to: "ConnectionConfig"
      via: "toConnectionConfig stays host/port/apiKey-only (theme excluded)"
      pattern: "toConnectionConfig"
---

<objective>
Rework per-profile theme persistence to carry the full theme tuple `(seedHex, dark, paletteMode, poolShift, maxItems, poolOverrides)` — reusing Phase-14's `mutateActive`/`writeScope` durable write path — with a never-throws PER-ENTRY fail-safe sanitize, a fresh-start (no-migration) decode, and the WR-02 reactive no-active-branch fix in `AppContainer.seedTheme`. The retired `themeBase`/`themeDeltaArgb` fields are kept as `@Deprecated` decoded-but-unused fields (NOT hard-removed) so the still-standing SettingsScreen + `ProfileStoreTest`/`ActiveConfigDerivationTest` compile at this wave; 15-06 deletes them when SettingsScreen is rebuilt. `fsChoice` (S/M/L) stays a separate, unchanged setting.

Purpose: This is the durable substrate the UI (15-06) writes to. It must be stable and fail-safe BEFORE the editor consumes it: the device is a printer surface that must never black-screen on a corrupt theme blob (V5). The tuple must stay OUT of `ConnectionConfig` so a theme edit doesn't churn the spine. And — Gradle compiles the WHOLE main+test sourcesets before any `--tests` — hard-removing `themeBase`/`themeDeltaArgb` now (while SettingsScreen and two profile tests still read them) would brick compilation; the deprecated-field approach keeps both sourcesets green.
Output: tupled `Profile`/`ProfileStore`/`ThemePrefs` (+ deprecated legacy fields), rewired `AppContainer.seedTheme`, extended/reworked `ProfileThemeSeedTest` + `TokenDeltaSerializationTest` + `ThemePrefsFallbackTest`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-theme-system-settings-redesign/15-RESEARCH.md
@.planning/phases/15-theme-system-settings-redesign/15-PATTERNS.md
@.planning/phases/14-multi-printer-switching/14-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the theme tuple to Profile (legacy fields @Deprecated); tuple ThemePrefs + per-entry never-throws sanitize</name>
  <read_first>
    - app/src/main/java/works/mees/dinghy/config/Profile.kt (lines 26-42 PersistedProfile + redacted toString; lines 51-59 Profile theme fields; line 66 toConnectionConfig; lines 76-82 toThemeResolved; lines 92-116 round-trip)
    - app/src/main/java/works/mees/dinghy/theme/ThemePrefs.kt (lines 39-52 catch→emptyPreferences; lines 54-58 setBase/setFs; lines 78-91 Resolved + DEFAULT; lines 103+ sanitize — the never-throws contract to preserve)
    - app/src/main/java/works/mees/dinghy/config/ProfileStore.kt (line 128 ignoreUnknownKeys; lines 109-118 mutateActive; line 151 sanitize)
    - app/src/main/java/works/mees/dinghy/ui/screen/SettingsScreen.kt (lines ~377/529/560/562 — the profile create/copy sites that still set/read themeBase/themeDeltaArgb; they must keep compiling against the @Deprecated fields until 15-06 rebuilds the screen)
    - app/src/test/java/works/mees/dinghy/config/ProfileStoreTest.kt (lines 70-71/153-154 — the `themeBase` test factory + equality tests that must keep compiling against the @Deprecated field)
    - app/src/test/java/works/mees/dinghy/di/ActiveConfigDerivationTest.kt (lines 40-41 — `Profile(..., themeBase = "Dark")` that must keep compiling)
    - .planning/phases/15-theme-system-settings-redesign/15-RESEARCH.md (§"Per-Profile Persistence Rework" — exact field defaults + the sanitize rules + V5/V7 security)
    - .planning/phases/15-theme-system-settings-redesign/15-PATTERNS.md (§MODIFY Profile.kt/ProfileStore.kt + §"Deterministic fail-safe sanitize")
  </read_first>
  <action>
    In `config/Profile.kt`: on BOTH `PersistedProfile` (@Serializable wire form) and `Profile` (runtime), ADD the tuple primitives with defaults: `seedHex: String = "#3f78ff"`, `dark: Boolean = true`, `paletteMode: String = "Colorful"`, `poolShift: Int = 0`, `maxItems: Int = 4`, `poolOverrides: Map<String, Long> = emptyMap()` (sparse poolIndex-AS-STRING → unsigned-32 ARGB). **Use `Map<String, Long>`, NOT `Map<Int, Long>` (CONFIRMED Codex finding):** kotlinx `ignoreUnknownKeys` only tolerates unknown KEYS, not malformed VALUES — with an `Int`-keyed/typed map a single bad entry can fail deserialization and drop the WHOLE map. A `Map<String, Long>` (plus per-entry parse in sanitize, below) lets one bad entry drop in isolation while good entries survive. KEEP `fsChoice: String` unchanged (D-05). For `themeBase: String` and `themeDeltaArgb: Map<String,Long>`: **KEEP them as `@Deprecated("retired — fresh-start, no migration (D-05); deleted in 15-06") ` decoded-but-UNUSED fields** (do NOT hard-remove). They stay on both forms so SettingsScreen's create/copy sites + `ProfileStoreTest`/`ActiveConfigDerivationTest` keep compiling; the runtime IGNORES them (the new tuple is the source of truth; old persisted values are not migrated — fresh-start). 15-06 deletes them + their last call sites. Update the `PersistedProfile`↔`Profile` round-trip (lines 92-116) to carry the new tuple fields (and pass the deprecated fields through untouched). PRESERVE the redacted `toString` (lines 39-42, 97-100) — add the new theme fields, keep `apiKey=***` redaction; theme fields carry NO secrets, do NOT widen the redaction surface (V7). KEEP `toConnectionConfig()` (line 66) host/port/apiKey-ONLY — the theme tuple MUST NOT enter `ConnectionConfig`. Rewire `toThemeResolved()` (lines 76-82) to return the new theme-tuple resolved value from the NEW fields (NOT themeBase/themeDeltaArgb), reusing the `ThemePrefs.sanitize` parity.
    In `theme/ThemePrefs.kt`: KEEP the `.catch { IOException → emptyPreferences() }` + `sanitize` never-throws contract. Replace the persisted keys + `Resolved` (lines 78-91) with the tuple. `DEFAULT` = the validated default seed (`#3f78ff`/dark/Colorful/0/4/empty). Replace `setBase`→`setSeed`/`setDark`/`setMode`/`setShift`/`setMaxItems`/`setOverrides`; KEEP `setFs` unchanged. REWRITE `sanitize` to the new tuple shape with PER-ENTRY override tolerance: unparseable `seedHex` (not a valid 6/8-digit hex) → default seed; bad `paletteMode` (not in Colorful|Simple|HighContrast) → Colorful; junk `poolShift` (out of 0..360) / `maxItems` (out of 1..64) → defaults; for `poolOverrides`, iterate the persisted `Map<String,Long>` and per-entry: parse the String key to an Int index and validate the ARGB — DROP only the bad entry, KEEP the good ones (never let one malformed entry nuke the map). NEVER throws, NEVER black-screens. **NOTE:** `sanitize`'s SIGNATURE changes (the old `(base, fs, roleKeys, argb)` shape is gone) — `ThemePrefsFallbackTest` calls the old signature and is reworked in Task 2.
    In `config/ProfileStore.kt`: `mutateActive` (lines 109-118) is REUSED VERBATIM as the durable lost-update-safe write path for theme edits. `ignoreUnknownKeys=true` (line 128) keeps old blobs decoding cleanly.
  </action>
  <verify>
    <automated>cmd.exe /c "E:\Android\gw.bat :app:compileDebugKotlin --no-daemon" | tr -d '\r'</automated>
  </verify>
  <acceptance_criteria>
    - `Profile` and `PersistedProfile` declare `seedHex`/`dark`/`paletteMode`/`poolShift`/`maxItems`/`poolOverrides: Map<String, Long>`. Verify: `grep -n "val seedHex" app/src/main/java/works/mees/dinghy/config/Profile.kt` matches; `grep -n "poolOverrides: Map<String, Long>" Profile.kt` matches (String-keyed, NOT Int).
    - `themeBase`/`themeDeltaArgb` are RETAINED as `@Deprecated` fields (decoded-but-unused). Verify: `grep -nE "@Deprecated.*\n?.*themeBase|themeBase" Profile.kt` shows them present with a deprecation annotation; `toThemeResolved` reads the NEW fields not themeBase.
    - `fsChoice` retained on both forms (D-05). Verify grep matches.
    - `toString` still redacts apiKey as `***`; new theme fields carry NO secret (V7). Source assertion.
    - `toConnectionConfig()` constructs `ConnectionConfig` from host/port/apiKey ONLY (no theme field). Source assertion.
    - `ThemePrefs.sanitize` maps junk seed→default / bad mode→Colorful / out-of-range shift|maxItems→defaults / and drops ONLY the bad `poolOverrides` entry (good entries survive), never throws.
    - `:app:compileDebugKotlin` SUCCEEDS — the MAIN sourceset compiles (SettingsScreen's themeBase/themeDeltaArgb sites still resolve against the deprecated fields).
  </acceptance_criteria>
  <done>`Profile`/`ProfileStore`/`ThemePrefs` carry the theme tuple (`poolOverrides` String-keyed for per-entry tolerance) with a never-throws per-entry sanitize, fresh-start decode, deprecated legacy fields retained for compile, redaction preserved, and theme kept out of `ConnectionConfig`.</done>
</task>

<task type="auto">
  <name>Task 2: Rewire AppContainer.seedTheme (single-apply + WR-02 reactive) + rework the persistence tests</name>
  <read_first>
    - app/src/main/java/works/mees/dinghy/di/AppContainer.kt (lines 105 writeScope; 128-130 mutateActiveProfile; 138-151 activeProfile/activeConfig; 185-186 themeResolver = ThemeResolver(); 314-324 seedTheme — the one-shot firstOrNull to make reactive)
    - app/src/test/java/works/mees/dinghy/theme/ProfileThemeSeedTest.kt (lines 44-93 — the corrupt-primitive table idiom to extend)
    - app/src/test/java/works/mees/dinghy/theme/TokenDeltaSerializationTest.kt (the round-trip whose ENTIRE PURPOSE is the retired TokenDelta type — REWRITE it to the fresh-start decode + tuple round-trip; or delete + fold into ProfileThemeSeedTest)
    - app/src/test/java/works/mees/dinghy/theme/ThemePrefsFallbackTest.kt (calls the OLD `ThemePrefs.sanitize(base, fs, roleKeys, argb)` + asserts on `r.deltas.overrides`/`TokenDelta.Role` — REWORK it to the new tuple sanitize signature + per-entry-tolerance assertions; the per-entry "one bad entry doesn't nuke the rest" test lives here or in ProfileThemeSeedTest)
    - .planning/phases/15-theme-system-settings-redesign/15-RESEARCH.md (§"WR-02 fix" + §"Durable write path")
    - .planning/phases/15-theme-system-settings-redesign/15-PATTERNS.md (§MODIFY AppContainer.kt — the reactive no-active branch)
  </read_first>
  <action>
    In `di/AppContainer.kt`: update the `themeResolver` construction (line 186) to use the validated default seed (matching ThemeResolver's new default ctor from 15-04). Rewire `seedTheme(scope)` (lines 314-324): KEEP applying the full tuple in ONE `themeResolver.apply(...)` call (no flicker), but FIX the no-active branch to be REACTIVE (WR-02): replace the one-shot `flowOf(p?.toThemeResolved() ?: themePrefs.flow.firstOrNull() ?: DEFAULT)` with a `flatMapLatest { p -> if (p != null) flowOf(p.toThemeResolved()) else themePrefs.flow }` so a global-default edit while idle re-emits (mirror the active branch). The collector calls `themeResolver.apply(<full tuple from resolved>)`. Ensure theme edits route through `mutateActiveProfile { it.copy(seedHex = …) }` (active) / `themePrefs.set*` via `writeScope` (idle) — NEVER a `rememberCoroutineScope()` (`[[dinghy-compose-write-scope-cancellation]]`). Add the theme-edit intent helpers on AppContainer the editor (15-06) needs: `setActiveSeed`/`setActiveMode`/`setActiveShift`/`setActiveOverride`/`resetActiveTheme` routing to mutateActiveProfile-or-global via writeScope — the durable write surface 15-06 calls.
    Then REWORK the tests (this is where the obsolete TokenDelta-shaped tests get reconciled to the new model — they keep compiling on 15-04's deprecated shims, but their CONTRACT is gone, so rewrite their bodies here):
    - `ProfileThemeSeedTest.kt` — keep the corrupt-primitive table; add rows for junk seedHex→default, bad paletteMode→Colorful, out-of-range poolShift/maxItems→defaults, and a `poolOverrides` map with one GOOD + one MALFORMED entry → assert the good entry survives and only the bad one is dropped (the per-entry-tolerance test) — and assert NONE throw (V5).
    - `TokenDeltaSerializationTest.kt` — REWRITE (its TokenDelta round-trip contract is retired): a fresh-start decode test — a serialized old-shape blob containing `themeBase`/`themeDeltaArgb` decodes to a `Profile` where those map onto the deprecated (ignored) fields and the new tuple fields take defaults; plus a round-trip of a new-shape `PersistedProfile` preserving the tuple incl. a 2-entry `poolOverrides`. (Alternatively delete this file and fold both cases into ProfileThemeSeedTest — but then add it to files_modified as a deletion.)
    - `ThemePrefsFallbackTest.kt` — REWORK to the new `ThemePrefs.sanitize` tuple signature: drop the `r.deltas.overrides`/`TokenDelta.Role` assertions (that shape is gone); assert junk-tuple → complete usable theme with no throw, and the per-entry override tolerance. (If overlapping with ProfileThemeSeedTest, keep this focused on the ThemePrefs.sanitize fail-safe specifically.)
    List all classes explicitly.
  </action>
  <verify>
    <automated>cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --tests works.mees.dinghy.theme.ProfileThemeSeedTest --tests works.mees.dinghy.theme.TokenDeltaSerializationTest --tests works.mees.dinghy.theme.ThemePrefsFallbackTest --no-daemon" | tr -d '\r'</automated>
  </verify>
  <acceptance_criteria>
    - `seedTheme`'s no-active branch is reactive — `themePrefs.flow` is flat-mapped, NOT `firstOrNull()`. Verify: `firstOrNull` no longer in the seedTheme body; `flatMapLatest` into `themePrefs.flow` present (WR-02).
    - `seedTheme` applies the tuple via a single `themeResolver.apply(...)` call; intent helpers (`setActiveSeed`/`setActiveOverride`/`setActiveShift`/`resetActiveTheme`) exist for 15-06. Verify greps.
    - `ProfileThemeSeedTest` GREEN — every junk-input row sanitizes to defaults, the per-entry test proves one bad `poolOverrides` entry is dropped while the good one survives, and NO row throws.
    - `TokenDeltaSerializationTest` (rewritten or deleted+folded) GREEN — old-blob fresh-start decode (legacy fields ignored, new fields default) + new-shape tuple round-trip incl. poolOverrides.
    - `ThemePrefsFallbackTest` reworked to the new sanitize signature, GREEN — no `r.deltas`/`TokenDelta.Role` references remain. Verify: `grep -c "deltas.overrides\|TokenDelta.Role" app/src/test/java/works/mees/dinghy/theme/ThemePrefsFallbackTest.kt` returns 0.
    - Theme edits route through `mutateActiveProfile`/`writeScope`, never a composition scope. Source assertion.
    - Both main AND test sourcesets compile; the three named test classes are GREEN; classes listed explicitly (no glob).
  </acceptance_criteria>
  <done>`AppContainer.seedTheme` re-seeds reactively (WR-02 closed) with a single apply and editor intent helpers; the persistence tests prove per-entry fail-safe sanitize and fresh-start decode, with the obsolete TokenDelta-shaped tests reworked to the new model.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DataStore blob → Profile/ThemePrefs | Untrusted persisted bytes (possibly corrupt/old-shape) cross into the runtime theme; per-entry sanitize is the validation gate. |
| Profile → ConnectionConfig | The connection projection must exclude theme so a theme edit cannot disturb the spine. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-05-01 | Denial of Service | corrupt theme blob crashes/black-screens the printer display | mitigate | Pure per-entry `sanitize` fail-safe — junk seed/mode/shift → defaults; one bad `poolOverrides` entry dropped (String-keyed map + per-entry parse, NOT whole-map loss), NEVER throw (V5). ProfileThemeSeedTest asserts no-throw + survivor entry. |
| T-15-05-02 | Tampering | a theme edit churns the connection spine | mitigate | `toConnectionConfig` stays host/port/apiKey-only; `activeConfig.distinctUntilChanged` keys on those; theme fields excluded (T-14-04). |
| T-15-05-03 | Information disclosure | widened toString leaks apiKey | mitigate | `toString` keeps `apiKey=***`; new theme fields carry no secrets (V7). |
| T-15-05-04 | Tampering | persistence write dropped by same-frame nav cancellation | mitigate | All theme writes route through process-lifetime `writeScope`/`mutateActiveProfile` ([[dinghy-compose-write-scope-cancellation]]); read-modify-write in one `dataStore.edit`. |
| T-15-05-SC | Tampering | npm/pip/cargo installs | accept | Zero external packages (RESEARCH §"Package Legitimacy Audit": N/A). |
</threat_model>

<verification>
- `ProfileThemeSeedTest` + `TokenDeltaSerializationTest` + `ThemePrefsFallbackTest` GREEN; both sourcesets compile.
- seedTheme reactive (WR-02); theme out of ConnectionConfig; redaction preserved; per-entry override tolerance.
</verification>

<success_criteria>
- Theme tuple persisted per-profile + global fallback; `poolOverrides` String-keyed for per-entry tolerance; fsChoice separate; fresh-start no-migration (legacy fields @Deprecated, deleted in 15-06).
- Never-throws per-entry sanitize (V5); theme excluded from ConnectionConfig.
- WR-02 reactive no-active branch; durable writeScope path.
- Both sourcesets compile at this wave boundary.
</success_criteria>

<output>
Create `.planning/phases/15-theme-system-settings-redesign/15-05-SUMMARY.md` when done.
</output>
