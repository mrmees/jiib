---
phase: 15-theme-system-settings-redesign
plan: 04
type: execute
wave: 3
depends_on:
  - 15-03
files_modified:
  - app/src/main/java/works/mees/dinghy/theme/ThemeResolver.kt
  - app/src/main/java/works/mees/dinghy/theme/BakedTokens.kt
  - app/src/main/java/works/mees/dinghy/di/AppContainer.kt
  - app/src/main/java/works/mees/dinghy/ui/screen/SettingsScreen.kt
  - app/src/test/java/works/mees/dinghy/theme/ThemeResolverTest.kt
autonomous: true
requirements:
  - "D-02 (single runtime generation path — generate-and-cache in ThemeResolver, retire build-time BakedTokens as source of truth)"
  - "D-04 (seed-only chrome — retire per-role TokenDelta overrides; only data-pool slots editable)"
  - "D-15 (default palette mode = Colorful — the resolver maps paletteMode → simple/highContrast flags)"
  - "Refines THEME-01 (the resolved-theme source of truth)"
must_haves:
  truths:
    - "ThemeResolver calls Palette.generate() + TokenBridge.build() once per change and caches the ThemeTokens on the UNCHANGED StateFlow<ThemeTokens> boundary (D-02)"
    - "The resolver is constructed with the validated default seed and generates the out-of-box palette at first launch (D-02)"
    - "TokenDelta/Role per-role chrome overrides are retired; seed + mode + poolShift + sparse pool overrides are the only knobs (D-04)"
    - "BakedTokens is demoted to a fail-safe default-seed snapshot used only if generation ever throws"
    - "paletteMode maps to Colorful(default)/Simple/HighContrast (D-15); a full tuple applies in ONE re-emit (no flicker)"
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/theme/ThemeResolver.kt"
      provides: "Generate-and-cache resolver: setSeed/setMode/setShift/setOverride + apply(full tuple)"
      contains: "fun apply"
    - path: "app/src/main/java/works/mees/dinghy/theme/BakedTokens.kt"
      provides: "Default-seed fail-safe snapshot (demoted from source of truth)"
  key_links:
    - from: "app/src/main/java/works/mees/dinghy/theme/ThemeResolver.kt"
      to: "app/src/main/java/works/mees/dinghy/theme/Palette.kt"
      via: "recompute() calls Palette.generate then TokenBridge.build"
      pattern: "Palette\\.generate"
    - from: "app/src/main/java/works/mees/dinghy/theme/ThemeResolver.kt"
      to: "StateFlow<ThemeTokens>"
      via: "the unchanged boundary Compose/Views consume"
      pattern: "StateFlow<ThemeTokens>"
---

<objective>
Rewire `ThemeResolver` from baked-table-lookup + per-role `TokenDelta` overrides to a runtime generate-and-cache model: `recompute()` calls `Palette.generate()` then `TokenBridge.build()` once per discrete change, caches the result on the UNCHANGED `StateFlow<ThemeTokens>` boundary, retires `TokenDelta`/`Role`, demotes `BakedTokens` to a fail-safe snapshot, and constructs with the validated default seed.

Purpose: This is the substrate seam that makes everything "extended not rewritten" — Compose `DinghyTheme` and Views `ThemeableView` consume the same `StateFlow<ThemeTokens>` untouched. The render loop reads CACHED Color ints; the device never does color math (Adreno-320 floor). It must be stable BEFORE the UI (15-06) and pool wiring (15-07) consume it.
Output: rewired `ThemeResolver.kt`, demoted `BakedTokens.kt`, extended `ThemeResolverTest`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-theme-system-settings-redesign/15-RESEARCH.md
@.planning/phases/15-theme-system-settings-redesign/15-PATTERNS.md
@app/src/main/java/works/mees/dinghy/theme/TokenBridge.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewire ThemeResolver to generate-and-cache; demote BakedTokens to fail-safe</name>
  <read_first>
    - app/src/main/java/works/mees/dinghy/theme/ThemeResolver.kt (the whole file — lines 20-71 TokenDelta/Role to retire; lines 54-72 resolve(); lines 82-125 the MutableStateFlow + setBase/setDeltas/setFs/apply/recompute idiom to PRESERVE in shape, swap in body)
    - app/src/main/java/works/mees/dinghy/theme/BakedTokens.kt (lines 6-30 the literal-sRGB header law that stays; the TokensDark/TokensLight tables to demote to a single default-seed snapshot)
    - app/src/main/java/works/mees/dinghy/theme/Palette.kt + TokenBridge.kt (the generate + build calls recompute() now makes)
    - .planning/phases/15-theme-system-settings-redesign/15-PATTERNS.md (§MODIFY ThemeResolver.kt — the preserve-the-StateFlow-shape + single-apply re-emit + Pitfall 3)
    - .planning/phases/15-theme-system-settings-redesign/15-RESEARCH.md (§"The Generate-and-Cache Rewire" + Pitfall 3 theme-switch flicker)
    - app/src/main/java/works/mees/dinghy/di/AppContainer.kt (~line 321 — the ONLY in-module caller of the old 3-arg `themeResolver.apply(resolved.base, resolved.deltas, resolved.fs)`; must adapt to the new tuple `apply(...)` signature)
    - app/src/main/java/works/mees/dinghy/ui/screen/SettingsScreen.kt (the `TokenDelta` import ~line 48 + the 15+ `TokenDelta`/`setBase`/`setDeltas` usages ~lines 149/369/431/441/467-487/509-562 that go dead after the symbol removals — interim compile-stub only; SettingsScreen is fully rebuilt in 15-06)
  </read_first>
  <action>
    In `theme/ThemeResolver.kt`: KEEP the `MutableStateFlow(...)` + `asStateFlow()` + recompute-and-re-emit idiom EXACTLY (the StateFlow<ThemeTokens> boundary is the whole reason the substrate is extended-not-rewritten). Change the resolver's held state from `(base, deltas, fs)` to the tuple `(seedHex, dark, paletteMode, poolShift, maxItems, poolOverrides: Map<Int,Color>, fs)`. Add mutators mirroring the old `setBase`/`setFs`: `setSeed(next: String)`, `setMode(next: String)`, `setShift(next: Int)`, `setOverride(i: Int, argb: Color?)` (sparse-map edit: null removes), `setDark(next: Boolean)` — each sets its field then calls `recompute()`. Add/keep `fun apply(seedHex, dark, paletteMode, poolShift, maxItems, overrides, fs)` that sets ALL fields then calls `recompute()` ONCE (Pitfall 3: the full tuple in ONE call — never 3 sequential set*() = 3 flickers). `recompute()` body: map `paletteMode` → `simple`/`highContrast` flags (Colorful→both false (D-15 default); "Simple"→simple=true; "HighContrast"→highContrast=true), call `Palette.generate(seedHex, dark, maxItems, poolShift, statusFromPool=true, simple, highContrast)`, then `TokenBridge.build(generated, poolOverrides, fs)`, and assign to `_tokens.value`. WRAP `recompute()`'s generation in a try/catch: on any throw, fall back to the `BakedTokens` default-seed snapshot (the math is total so this should never fire, but the device is a printer surface that must never go dark). Construct the resolver with the validated default seed `#3f78ff`, dark=true, Colorful, poolShift=0, maxItems=4, emptyMap, default fs (replace the no-arg `ThemeResolver()` default at lines 82-86). REMOVE `data class TokenDelta`, `Role`, `resolve(base, deltas, fs)` and the per-role copy switch (D-04 — chrome is seed-only). In `BakedTokens.kt`: demote to a SINGLE validated default-seed snapshot used only as the recompute() fail-safe — either keep one TokensDark snapshot or regenerate it from `Palette` + default seed; KEEP the lines 6-30 literal-sRGB header law (it now governs the generator's output policy too). Update the two in-module callers of the removed `setBase`/`setDeltas`/`resolve`/`TokenDelta` symbols so the module still compiles (this plan's `:app:compileDebugKotlin` gate). These are INTERIM COMPILE-STUBS — the real wiring lands in 15-05 (persistence) and 15-06 (the full SettingsScreen rebuild); do NOT pre-empt that here:
    - `di/AppContainer.kt` (~line 321): adapt the single `themeResolver.apply(resolved.base, resolved.deltas, resolved.fs)` call to the new tuple-based `apply(seedHex, dark, paletteMode, poolShift, maxItems, overrides, fs)` signature — feed the default-seed tuple (seed `#3f78ff`, dark from the existing base flag, Colorful, poolShift=0, maxItems=4, emptyMap, `resolved.fs`). 15-05 replaces this with the persisted tuple.
    - `ui/screen/SettingsScreen.kt`: REMOVE the `import works.mees.dinghy.theme.TokenDelta` (~line 48) and comment-out / minimally stub the now-dead `setBase(...)` / `setDeltas(...)` / `TokenDelta.*` call sites (~lines 149/369/431/441/467-487/509-562) so the file compiles. Add an explicit `// TODO(15-06): SettingsScreen rebuilt with seed/mode/pool controls` note at each stubbed site. This is an interim compile-stub, NOT the final accent-picker UI (15-06 owns the rebuild).
  </action>
  <verify>
    <automated>cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --tests works.mees.dinghy.theme.ThemeResolverTest --no-daemon" | tr -d '\r'</automated>
  </verify>
  <acceptance_criteria>
    - `ThemeResolver` exposes `val tokens: StateFlow<ThemeTokens>` UNCHANGED (boundary preserved). Verify: `grep -n "StateFlow<ThemeTokens>" app/src/main/java/works/mees/dinghy/theme/ThemeResolver.kt` matches.
    - `recompute()` calls `Palette.generate(...)` then `TokenBridge.build(...)`. Verify both symbols grep-present in ThemeResolver.kt.
    - `TokenDelta`, `Role`, and `resolve(base, deltas, fs)` are removed (D-04). Verify: `grep -nE "data class TokenDelta|sealed.*Role|fun resolve\(" app/src/main/java/works/mees/dinghy/theme/ThemeResolver.kt` returns nothing.
    - The resolver default-constructs with seed `#3f78ff` / dark / Colorful (validated default seed at first launch).
    - `recompute()` has a try/catch falling back to the `BakedTokens` default-seed snapshot.
    - `di/AppContainer.kt` ~line 321 calls the new tuple-based `apply(...)` (no `resolved.deltas` arg). Verify: `grep -n "themeResolver.apply" app/src/main/java/works/mees/dinghy/di/AppContainer.kt` matches and no `resolved.deltas` remains on that line.
    - `ui/screen/SettingsScreen.kt` no longer imports/uses `TokenDelta` un-stubbed. Verify: `grep -c "import works.mees.dinghy.theme.TokenDelta" app/src/main/java/works/mees/dinghy/ui/screen/SettingsScreen.kt` returns 0; dead `setBase`/`setDeltas` sites are stubbed with a `// TODO(15-06)` note (interim only — 15-06 rebuilds the screen).
    - `:app:compileDebugKotlin` SUCCEEDS — the whole module compiles after the symbol removals, with AppContainer adapted and SettingsScreen interim-stubbed.
  </acceptance_criteria>
  <done>`ThemeResolver` generates-and-caches the full `ThemeTokens` once per change on the unchanged StateFlow boundary, with seed-only chrome, a validated default seed, and a BakedTokens fail-safe.</done>
</task>

<task type="auto">
  <name>Task 2: Extend ThemeResolverTest for generate-and-cache + overrides + single re-emit</name>
  <read_first>
    - app/src/test/java/works/mees/dinghy/theme/ThemeResolverTest.kt (lines 24-76 — the runTest/UnconfinedTestDispatcher/emission-count idiom to keep)
    - app/src/main/java/works/mees/dinghy/theme/ThemeResolver.kt (the rewired resolver from Task 1)
    - .planning/phases/15-theme-system-settings-redesign/15-VALIDATION.md (the behavior→test map rows for ThemeResolver)
  </read_first>
  <action>
    Extend `theme/ThemeResolverTest.kt` (keep the `runTest`/`UnconfinedTestDispatcher`/emission-count idiom): add tests asserting (a) the default-constructed resolver emits a `ThemeTokens` whose accent/bg match the default-seed oracle (generate-and-cache produces the expected tokens); (b) `setOverride(i, color)` results in `tokens.value.pool[i] == color` and Reset-equivalent (`setOverride(i, null)` / clearing overrides via `apply(... emptyMap ...)`) restores the seed-derived `pool[i]`; (c) calling `apply(full tuple)` once emits exactly ONE new `ThemeTokens` (single re-emit, no flicker — Pitfall 3) — assert the emission count increments by 1, not 3; (d) `setMode("Simple")` collapses the pool to the mono gray (mode wiring). List the class explicitly in any documented command.
  </action>
  <verify>
    <automated>cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --tests works.mees.dinghy.theme.ThemeResolverTest --no-daemon" | tr -d '\r'</automated>
  </verify>
  <acceptance_criteria>
    - `ThemeResolverTest` GREEN with the new methods covering: default-seed emission, `setOverride` at index, Reset restores seed-derived pool, single-re-emit count (1 not 3), Simple-mode collapse.
    - The single-re-emit test asserts `apply(...)` increments the emission count by exactly 1.
    - Test class listed explicitly (no `pkg.*` glob).
  </acceptance_criteria>
  <done>`ThemeResolverTest` proves generate-and-cache, indexed overrides + reset, single-re-emit, and mode wiring on the rewired resolver.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| persisted tuple → resolver.apply | The tuple may carry junk (validated upstream in 15-05); the resolver's recompute try/catch is the last-resort fail-safe so the display never goes dark. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-04-01 | Denial of Service | generation throws → black-screen printer display | mitigate | `recompute()` try/catch falls back to the `BakedTokens` default-seed snapshot — always a complete usable theme (the load-bearing Phase-3 contract). |
| T-15-04-02 | Tampering | theme-switch flicker from 3 sequential set*() | mitigate | `apply(full tuple)` re-emits ONCE; the single-re-emit test asserts count==+1 (Pitfall 3). |
| T-15-04-03 | Spoofing | a stale per-role override path persists | mitigate | `TokenDelta`/`Role` removed entirely (D-04); only the sparse data-pool override path remains. |
| T-15-04-SC | Tampering | npm/pip/cargo installs | accept | Zero external packages (RESEARCH §"Package Legitimacy Audit": N/A). |
</threat_model>

<verification>
- `ThemeResolverTest` GREEN; module compiles; StateFlow boundary unchanged.
- TokenDelta/Role removed; default seed at construction; BakedTokens fail-safe in place.
</verification>

<success_criteria>
- Generate-and-cache on the unchanged StateFlow<ThemeTokens> boundary.
- Seed-only chrome (TokenDelta retired); BakedTokens demoted to fail-safe; default seed at first launch.
- Single re-emit per apply; Simple/High-Contrast mode wiring.
</success_criteria>

<output>
Create `.planning/phases/15-theme-system-settings-redesign/15-04-SUMMARY.md` when done.
</output>
