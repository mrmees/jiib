---
phase: 15-theme-system-settings-redesign
plan: 07
type: execute
wave: 6
depends_on:
  - 15-06
files_modified:
  - app/src/main/java/works/mees/dinghy/render/GraphView.kt
  - app/src/main/java/works/mees/dinghy/ui/temperature/TemperatureScreen.kt
  - app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusScreen.kt
  - app/src/main/java/works/mees/dinghy/ui/move/MoveScreen.kt
autonomous: false
requirements:
  - "D-13 (pool wiring this phase = temp/heater surfaces + Move directional; GraphView traces → pool[i]; heater readouts → matching pool index; Move outlines → directional.xy/.z)"
  - "D-14 (no arbitrary pool-size cap — consumers wrap pool[i % size]; maxItems is an internal boundary)"
  - "D-16 (surfaces stay pure-neutral — no hand-picked colors reintroduced on the wired surfaces)"
must_haves:
  truths:
    - "GraphView traces read pool[i % pool.size] (violet retired); the fill takes pool[0] (D-13/D-14)"
    - "Print-Status nozzle/bed/chamber + Temperature readouts use the SAME pool index as the graph trace (stable identity: same sensor = same color everywhere)"
    - "Move jog-pad/XY outline → directional.xy; Z-row outline → directional.z (D-13)"
    - "Only the ~6 heater-readout consumers migrate to the pool; the ~21 caution/Warn-intent .heat uses STAY (heat = caution)"
    - "On-device flox UAT confirms traces stay visually separated and stable across reseed, with same-sensor-same-color identity"
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/render/GraphView.kt"
      provides: "Trace colors rewired to pool[i % size]; violet shim deleted"
      contains: "pool"
    - path: "app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusScreen.kt"
      provides: "Heater readouts on pool index (heater identity = pool[0] = directional.temperature)"
  key_links:
    - from: "app/src/main/java/works/mees/dinghy/render/GraphView.kt"
      to: "ThemeTokens.pool"
      via: "applyTokens sets linePaints[i] = pool[i % size]"
      pattern: "pool\\["
    - from: "app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusScreen.kt"
      to: "ThemeTokens.pool / directional"
      via: "nozzle/bed/chamber readout color = pool[index] matching the graph"
      pattern: "pool\\[|directional"
---

<objective>
Rewire the pool's real consumers: `GraphView` traces → `pool[i % size]` (retire `violet`), Print-Status + Temperature heater readouts → the matching pool index by canonical sensor order, and Move's directional-plane outlines → `directional.xy`/`directional.z`. Honor the `heat`-semantic split (ONLY heater readouts migrate; the ~21 caution/Warn-intent `.heat` uses stay). Verify on-device that traces stay separated with stable same-sensor-same-color identity.

Purpose: This is the payoff — the single screen the whole exercise turns on. The stable-identity rule (same sensor = same color everywhere) is load-bearing: the Print-Status readout index MUST equal the GraphView trace index. Over-migrating `.heat` would break Console WARNING and Warn-intent buttons. On-device UAT is mandatory (visual distinctness on the real GPU; this project's green-suite-vs-reality history).
Output: rewired GraphView/Temperature/PrintStatus/Move, on-device sign-off; phase closes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-theme-system-settings-redesign/15-UI-SPEC.md
@.planning/phases/15-theme-system-settings-redesign/15-RESEARCH.md
@.planning/phases/15-theme-system-settings-redesign/15-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewire GraphView + Temperature + Print-Status heater readouts to the pool (stable index)</name>
  <read_first>
    - app/src/main/java/works/mees/dinghy/render/GraphView.kt (lines 46-49 + 180-193 applyTokens trace0=heat/1=accent/2=violet; line ~343 MAX_TRACES=3 — the hard cap, flagged-not-changed; lines 65-81 linePaths/linePaints/fillPaint)
    - app/src/main/java/works/mees/dinghy/ui/temperature/TemperatureScreen.kt (lines 446-451 traceColor 0→heat/1→accent/2→violet; lines 235/271/293 call sites)
    - app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusScreen.kt (lines 463-469 nozzle/bed readouts using t.heat)
    - app/src/main/java/works/mees/dinghy/theme/ThemeTokens.kt (the pool + directional fields from 15-03; delete any transitional violet shim)
    - .planning/phases/15-theme-system-settings-redesign/15-RESEARCH.md (§"Pool Wiring (D-13)" — the consumer table + the heat-semantic-split + MAX_TRACES Pitfall 5)
    - .planning/phases/15-theme-system-settings-redesign/15-PATTERNS.md (§MODIFY GraphView.kt + §MODIFY Temperature/PrintStatus)
    - .planning/phases/15-theme-system-settings-redesign/15-UI-SPEC.md (§"Pool-wiring visual-consequence rules" — same-sensor-same-index)
  </read_first>
  <action>
    In `render/GraphView.kt` `applyTokens(t)` (lines 182-193): set `linePaints[i].color = t.pool[i % t.pool.size].toArgb()` for `i in 0 until MAX_TRACES` (D-14 wrap); `fillPaint.color = t.pool[0].toArgb()` (primary trace at low alpha). Update the class KDoc (lines 46-48) — the nozzle=heat/bed=accent/chamber=violet line becomes pool-indexed. Leave `MAX_TRACES = 3` UNCHANGED (flagged: no current printer shows >3 graph traces — RESEARCH Pitfall 5/A4); the consumers wrap. DELETE any transitional `violet` shim left by 15-03.
    In `ui/temperature/TemperatureScreen.kt`: rewire `traceColor(index, t)` (lines 446-451) to `t.pool[index % t.pool.size]` (retire the heat/accent/violet switch); update the KDoc/comments at lines 75/235/446 to the pool model. The call sites (235/271/293) keep using `traceColor(i, t)`.
    In `ui/printstatus/PrintStatusScreen.kt`: rewire the nozzle/bed heater readouts (lines 463-469) from `t.heat` to the matching pool index — nozzle = `t.pool[0]` (= `directional.temperature` = heater identity, NOT `heat`), bed = `t.pool[1]` (and chamber = `t.pool[2]` if a chamber readout is present), using `pool[i % size]` wrap. The readout index MUST equal the GraphView trace index (canonical order nozzle/bed/chamber). DO NOT touch the line-383 `accent2` use or any of the other ~21 `.heat` references elsewhere (Console WARNING, ConfirmGuard, Warn-intent buttons) — `heat` = caution stays (Pitfall 4).
  </action>
  <verify>
    <automated>cmd.exe /c "E:\Android\gw.bat :app:assembleDebug --no-daemon" | tr -d '\r'</automated>
  </verify>
  <acceptance_criteria>
    - `GraphView.applyTokens` reads `t.pool[i % t.pool.size]` for traces and `t.pool[0]` for the fill. Verify: `grep -n "pool\[" app/src/main/java/works/mees/dinghy/render/GraphView.kt` matches; no `violet` reference remains.
    - `MAX_TRACES = 3` unchanged. Verify: `grep -n "MAX_TRACES = 3" GraphView.kt` matches.
    - `TemperatureScreen.traceColor` returns `t.pool[index % t.pool.size]`; no `t.violet`/`t.heat`/`t.accent` switch remains in `traceColor`. Verify grep.
    - Print-Status nozzle readout = `t.pool[0]`, bed = `t.pool[1]` (heater identity = pool[0], NOT `t.heat`). Source assertion; the readout index matches the graph trace index.
    - No `.violet` reference remains anywhere. Verify: `grep -rn "\.violet" app/src/main/java/` returns nothing.
    - The other ~21 `.heat` references (Console/ConfirmGuard/Warn buttons) are UNCHANGED — heater migration touched only the readout uses. Verify: `grep -rc "\.heat" app/src/main/java/ | ...` count of non-readout uses is unchanged from baseline (spot-check Console/ConfirmGuard still use `.heat`).
    - `:app:assembleDebug` SUCCEEDS.
  </acceptance_criteria>
  <done>GraphView + Temperature + Print-Status heater readouts read the pool at a stable canonical index; violet is fully retired; caution `.heat` uses untouched; the app assembles.</done>
</task>

<task type="auto">
  <name>Task 2: Rewire Move directional-plane outlines to directional.xy / directional.z</name>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/move/MoveScreen.kt (lines 68-86 the jog-pad/Z-row doc; lines 327/349/417/473/501/526 the accentLine outline usages; lines 162/184 the jog/Z dispatch sites that identify XY vs Z controls)
    - app/src/main/java/works/mees/dinghy/theme/ThemeTokens.kt (the Directional(temperature, xy, z) value from 15-03)
    - .planning/phases/15-theme-system-settings-redesign/15-RESEARCH.md (§"Pool Wiring" Move rows: jog-pad XY outline → directional.xy; Z-row outline → directional.z; homed/unhomed status color stays with the follow-on)
    - .planning/phases/15-theme-system-settings-redesign/15-UI-SPEC.md (§"Pool-wiring visual-consequence rules" — Move directional; OUT-of-scope: homed/unhomed status color + force-move lock shape)
  </read_first>
  <action>
    In `ui/move/MoveScreen.kt`: rewire the directional-PLANE outline colors — the XY jog-pad edge/control outlines (the X/Y directional jog arrows, dispatch site ~line 162) from `t.accentLine` to `t.directional.xy`; the Z-row outline (the Z▲/Z▼ controls, dispatch site ~line 184) from `t.accentLine` to `t.directional.z`. Apply only to the DIRECTIONAL-plane outlines that identify an axis plane (the jog arrows / Z controls). DO NOT touch the Home button (`Intent.Accent`, line 210), the distance-selector active-step outline (line 526 — that is a setting-selection accent, not a directional plane), or any homed/unhomed STATUS color / force-move lock shape — those are OUT OF SCOPE (the shape-status follow-on). Keep `t.hair`/disabled treatments. If an outline is ambiguous (plane vs generic chrome), default to leaving it as `t.accentLine` and note the decision; the load-bearing change is the XY jog arrows → `directional.xy` and the Z controls → `directional.z`.
  </action>
  <verify>
    <automated>cmd.exe /c "E:\Android\gw.bat :app:assembleDebug --no-daemon" | tr -d '\r'</automated>
  </verify>
  <acceptance_criteria>
    - The XY directional jog outlines use `t.directional.xy`; the Z-row outlines use `t.directional.z`. Verify: `grep -n "directional.xy\|directional.z" app/src/main/java/works/mees/dinghy/ui/move/MoveScreen.kt` matches both.
    - The Home button stays `Intent.Accent` and the distance-selector active-step stays `t.accentLine` (NOT directional) — out-of-scope guardrail. Source assertion.
    - No homed/unhomed status-color or force-move lock-shape change introduced (deferred). Source assertion.
    - `:app:assembleDebug` SUCCEEDS.
  </acceptance_criteria>
  <done>Move's XY jog-pad and Z-row directional outlines read `directional.xy`/`directional.z`; status-color/lock-shape changes stay deferred.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: On-device flox UAT — pool-wired data surfaces</name>
  <action>Build/sign/install the debug APK on flox with a live printer, then drive the verification steps below with the user; this is a blocking human-verify gate — do NOT write the SUMMARY or mark the plan/phase complete until the user reports results.</action>
  <what-built>Pool-wired data surfaces: GraphView traces + Print-Status/Temperature heater readouts on the contrast-ranked pool (stable same-sensor-same-color index), Move directional outlines on directional.xy/.z, surfaces pure-neutral. Built/signed/installed on flox.</what-built>
  <how-to-verify>
    On the real flox device with a live printer (E5 at 192.168.1.120:7125 or E3 at 192.168.1.121:7125):
    1. Open the Temperature graph → confirm the nozzle/bed/chamber traces are VISUALLY DISTINCT (contrast-ranked pool, not the old heat/accent/violet) and readable on the Adreno-320 GPU — no two traces collapse to the same hue.
    2. Confirm SAME-SENSOR-SAME-COLOR: the nozzle trace color on the graph == the nozzle readout color on Print-Status == the nozzle color on the Temperature screen (and same for bed/chamber). The heater numbers read as heaters by color+label even though they are no longer amber.
    3. Open Settings → Edit theme → reseed (wheel or preset) or Randomize → return to the graph: traces stay SEPARATED and the same-sensor-same-color identity holds across the reseed.
    4. Switch palette mode to Simple → traces collapse to mono (expected); High-Contrast → status uses stoplight; back to Colorful → full pool.
    5. Open Move → the XY jog-pad arrows and the Z-row controls show their directional outline colors (directional.xy / directional.z); the Home button and active distance-step are unchanged (accent). Homed/unhomed status color is unchanged (deferred — not part of this phase).
    6. Confirm surfaces are PURE NEUTRAL across all these screens in dark + light (no cool tint).
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues (which step, what you saw vs expected).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ThemeTokens.pool → render consumers | Cached Color ints flow into the render loop; consumers must wrap `pool[i % size]` so an index never exceeds the pool and crashes a draw. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-07-01 | Denial of Service | a readout/trace index exceeds pool.size → IndexOutOfBounds in onDraw | mitigate | All consumers wrap `pool[i % pool.size]` (D-14); GraphView caps at MAX_TRACES=3. |
| T-15-07-02 | Tampering | over-migrating `.heat` breaks Console WARNING / Warn-intent buttons | mitigate | Only the ~6 heater readouts migrate; the ~21 caution uses stay (Pitfall 4); acceptance greps the non-readout `.heat` count is unchanged. |
| T-15-07-03 | Tampering | accidentally recoloring Move homed/unhomed status (out of scope) | mitigate | Out-of-scope guardrail: only directional-plane outlines change; Home/status/lock-shape untouched (source assertion). |
| T-15-07-SC | Tampering | npm/pip/cargo installs | accept | Zero external packages (RESEARCH §"Package Legitimacy Audit": N/A). |
</threat_model>

<verification>
- `:app:assembleDebug` SUCCEEDS; no `.violet` remains; caution `.heat` count unchanged.
- On-device flox UAT 6-step sign-off (trace distinctness, same-sensor-same-color, reseed stability, modes, Move directional, pure-neutral).
</verification>

<success_criteria>
- GraphView/Temperature/Print-Status heater readouts on the pool with stable same-sensor-same-color identity; violet retired.
- Move XY/Z directional outlines on directional.xy/.z; status-color/lock-shape deferred.
- Heat-semantic split honored; consumers wrap pool[i % size].
- On-device sign-off across reseed + modes + pure-neutral.
</success_criteria>

<output>
Create `.planning/phases/15-theme-system-settings-redesign/15-07-SUMMARY.md` when the UAT passes.
</output>
