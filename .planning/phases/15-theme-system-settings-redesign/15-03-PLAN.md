---
phase: 15-theme-system-settings-redesign
plan: 03
type: execute
wave: 2
depends_on:
  - 15-02
files_modified:
  - app/src/main/java/works/mees/dinghy/theme/ThemeTokens.kt
  - app/src/main/java/works/mees/dinghy/theme/TokenBridge.kt
  - app/src/main/java/works/mees/dinghy/theme/BakedTokens.kt
  - app/src/test/java/works/mees/dinghy/theme/TokenBridgeTest.kt
autonomous: true
requirements:
  - "D-02 (single runtime generation path — generator emits a slim set, the bridge derives the full ThemeTokens)"
  - "D-04 (seed-only chrome — accent/surfaces/text derive from the seed; data-pool slots individually editable via overrides)"
  - "D-16 (surfaces go pure-neutral — drop the faint cool tint; bg flips polarity per the generator)"
  - "Refines THEME-01 (extend the token vocabulary with pool + directional)"
must_haves:
  truths:
    - "ThemeTokens carries a contrast-ranked pool: List<Color> + a Directional(temperature,xy,z) value, with violet retired (D-02/D-13)"
    - "TokenBridge.build() maps Palette.Generated → a complete ThemeTokens, deriving the in-between tiers (bg2/surface2/3/text3/hair) and applying sparse poolOverrides (D-04/D-09)"
    - "TokenBridgeTest is GREEN — derivation parity vs dinghy.js, overrides applied at index"
    - "Surfaces are pure-neutral — the bridge emits the generator's neutral bg, no surface tint reintroduced (D-16)"
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/theme/ThemeTokens.kt"
      provides: "Extended immutable token set: pool[], Directional, violet removed"
      contains: "val pool: List<Color>"
    - path: "app/src/main/java/works/mees/dinghy/theme/TokenBridge.kt"
      provides: "tokensFromPalette port: Generated → ThemeTokens + tier derivation + override application"
      contains: "fun build"
  key_links:
    - from: "app/src/main/java/works/mees/dinghy/theme/TokenBridge.kt"
      to: "app/src/main/java/works/mees/dinghy/theme/Palette.kt"
      via: "consumes Palette.Generated + calls the internal hexToOklch/oklchToHex helpers for tier derivation"
      pattern: "Palette\\.Generated|Generated|hexToOklch"
    - from: "app/src/main/java/works/mees/dinghy/theme/TokenBridge.kt"
      to: "app/src/main/java/works/mees/dinghy/theme/ThemeTokens.kt"
      via: "constructs the complete ThemeTokens"
      pattern: "ThemeTokens\\("
---

<objective>
Extend `ThemeTokens` with the data pool + directional standards (retiring `violet`), and port `tokensFromPalette()` from `dinghy.js` into a new `TokenBridge` that maps the generator's slim output onto a complete `ThemeTokens` — deriving the in-between surface tiers in Kotlin (calling 15-02's `internal` `hexToOklch`/`oklchToHex` helpers, so `Palette.generate` stays a faithful 1:1 of `color.js`) and applying sparse pool overrides. Turn the Wave-0 bridge scaffold GREEN.

Purpose: The generator emits a slim set; dinghy needs more tiers (bg2/surface2/3/text3/hair) and the new pool/directional fields the pool-wiring waves consume. The bridge is the dinghy-specific glue and the SECOND sanctioned producer of a complete `ThemeTokens` (besides the substrate's `resolve`). This is the stable boundary the resolver rewire (15-04) and all consumers (15-06/15-07) build on.
Output: extended `ThemeTokens.kt`, new `TokenBridge.kt`, GREEN `TokenBridgeTest`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-theme-system-settings-redesign/15-RESEARCH.md
@.planning/phases/15-theme-system-settings-redesign/15-PATTERNS.md
@app/src/main/java/works/mees/dinghy/theme/Palette.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend ThemeTokens with pool[] + Directional; retire violet</name>
  <read_first>
    - app/src/main/java/works/mees/dinghy/theme/ThemeTokens.kt (the existing @Immutable data class — lines 21-22 "Custom-theme scope D-01" KDoc to replace; line 59 heat KDoc; lines 64-69 violet field to remove; line 97 fs)
    - app/src/main/java/works/mees/dinghy/theme/BakedTokens.kt (the TokensDark + TokensLight literal `ThemeTokens(...)` constructor calls — `violet = Color(0xFFAE84F2)` ~line 53, `violet = Color(0xFF7B47BF)` ~line 86 — the NAMED-ARG call sites that must drop the removed param)
    - .planning/phases/15-theme-system-settings-redesign/15-PATTERNS.md (§MODIFY ThemeTokens.kt — the discretion calls: pool as List<Color> under @Immutable; Directional value class)
    - .planning/phases/15-theme-system-settings-redesign/15-RESEARCH.md (§"NEW fields to add to ThemeTokens" table)
  </read_first>
  <action>
    In `theme/ThemeTokens.kt`: add `val pool: List<Color>` (rely on the existing `@Immutable` annotation on the data class covering the field — no `kotlinx-collections-immutable` dep, per RESEARCH A3) with KDoc "Generated contrast-ranked DATA pool (D-13). Consumers wrap `pool[i % pool.size]` (D-14)." Add `val directional: Directional` and a sibling `@Immutable data class Directional(val temperature: Color, val xy: Color, val z: Color)`. KEEP the `val violet: Color` field as a **`@Deprecated` get-only shim**: `@Deprecated("retired — use pool[2 % pool.size]; deleted in 15-07") val violet: Color get() = pool[getOrElse...]`. The two-step retirement (deprecate-here, delete-in-15-07) keeps GraphView + TemperatureScreen compiling at THIS wave boundary; 15-07 deletes the shim and migrates those reads to `pool[...]`. REPLACE the "Custom-theme scope (D-01)" KDoc (lines 21-22) with a note that chrome derives from the seed (D-04) and pool slots are individually overridable (D-09). REWORD the `heat` KDoc (line 59): `heat` now means ONLY caution, not heater identity (D-13). Because `violet` becomes a computed property (a `get()`), REMOVE the `violet = Color(0xFF...)` named CONSTRUCTOR argument from BOTH `ThemeTokens(...)` constructor calls in `theme/BakedTokens.kt` (TokensDark ~line 53, TokensLight ~line 86) — a computed property is NOT a constructor parameter, so leaving the named arg = "no parameter named violet" compile failure on this task's own `:app:compileDebugKotlin` gate. The `@Deprecated get()` shim then keeps every downstream `.violet` READ (GraphView, TemperatureScreen) compiling (with a deprecation warning, not an error) until 15-07 deletes the shim.
  </action>
  <verify>
    <automated>cmd.exe /c "E:\Android\gw.bat :app:compileDebugKotlin --no-daemon" | tr -d '\r'</automated>
  </verify>
  <acceptance_criteria>
    - `ThemeTokens` declares `val pool: List<Color>` and `val directional: Directional`. Verify: `grep -n "val pool: List<Color>" app/src/main/java/works/mees/dinghy/theme/ThemeTokens.kt` and `grep -n "data class Directional" app/src/main/java/works/mees/dinghy/theme/ThemeTokens.kt` both match.
    - The stored `val violet: Color` constructor field is replaced by a `@Deprecated` get-only computed property delegating to the pool (flag it for 15-07 to delete). Verify: `grep -n "@Deprecated" app/src/main/java/works/mees/dinghy/theme/ThemeTokens.kt` near a `violet` get.
    - `heat` KDoc states it means caution-only (D-13); the D-01 custom-scope KDoc is replaced with the seed model.
    - Both `ThemeTokens(...)` constructor calls in `BakedTokens.kt` drop the `violet = Color(...)` named argument. Verify: `grep -c "violet = Color" app/src/main/java/works/mees/dinghy/theme/BakedTokens.kt` returns 0.
    - `:app:compileDebugKotlin` SUCCEEDS (whole MAIN sourceset compiles with the new fields + the deprecated violet shim, GraphView/TemperatureScreen `.violet` reads still resolve, and BakedTokens no longer passes the removed param).
  </acceptance_criteria>
  <done>`ThemeTokens` carries the pool + directional vocabulary, `violet` is a flagged `@Deprecated` transitional shim (deleted in 15-07), and the module compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Port tokensFromPalette into TokenBridge.build() + GREEN the bridge test</name>
  <read_first>
    - ../theme_theory/app/dinghy.js (lines 25 + 34-86 — `lShift` (L25 — `U.hexToOklch`/`U.oklchToHex`), `tokensFromPalette()`, `rgbaOf`, `swatches()` — THE port source for the bridge; note `U = Palette.util`)
    - app/src/main/java/works/mees/dinghy/theme/BakedTokens.kt (lines 33-63 — the verbatim ThemeTokens-constructor shape the bridge output mirrors; the literal-sRGB law)
    - app/src/main/java/works/mees/dinghy/theme/Palette.kt (the Generated value type this bridge consumes + the `internal` `hexToOklch`/`oklchToHex` helpers `lShift` calls — built in 15-02; if 15-02's SUMMARY says the tier derivation was moved into Palette, call THAT instead)
    - app/src/main/java/works/mees/dinghy/theme/ThemeTokens.kt (the target type — extended in Task 1)
    - app/src/test/java/works/mees/dinghy/theme/TokenBridgeTest.kt (the RED scaffold to GREEN)
    - .planning/phases/15-theme-system-settings-redesign/15-RESEARCH.md (§"The Token Bridge" — the full field-by-field derivation table, dark/light lShift values, alpha variants)
  </read_first>
  <action>
    Create `app/src/main/java/works/mees/dinghy/theme/TokenBridge.kt`: a function `fun build(gen: Palette.Generated, overrides: Map<Int, Color>, fs: Float): ThemeTokens` (object `TokenBridge` or top-level). Port `lShift(hex, dL)` — `Palette.hexToOklch(hex)` → bump L by `dL` clamped to [0,1] → `Palette.oklchToHex(...)` — calling 15-02's **`internal`** helpers (same-module visible; if 15-02 moved the derivation into `Palette` per its SUMMARY, call that surface instead — do NOT re-port the OKLCH math here, that would risk golden drift). Port `rgbaOf(hex, a)` (pack alpha into ARGB Color). Map 1:1: `surface`←surfaces.surface, `text`←surfaces.text, `text2`←surfaces.muted, `accent`←theme.primary, `go`/`stop`←status.go/stop, `heat`←status.caution (heat=caution per D-13). DERIVE the in-between tiers per the RESEARCH dark/light table: `bg`←surfaces.bg (pure neutral, D-16 — do NOT add a tint), `bg2`=lShift(bg,+0.03/-0.022), `surface2`=lShift(surface,+0.04/-0.045), `surface3`=lShift(surface,+0.085/-0.09), `text3`=lShift(muted,-0.12/+0.13), `hair`=rgbaOf(text,0.08/0.11), `outline`←surfaces.divider, `outline2`=lShift(divider,+0.11/-0.12), `accent2`=lShift(primary,+0.08/-0.05), `accentSoft`=rgbaOf(primary,0.16/0.12), `accentLine`=rgbaOf(primary,0.55/0.50), `accentGlow`=rgbaOf(primary,0.35/0.20), `edgeGlow`=rgbaOf(muted,0.24/0.12), and the `*Soft`/`*Glow` alpha variants for heat/go/stop per dinghy.js. Build `pool` = gen.pool mapped to baked sRGB `Color`, then APPLY `overrides`: for each `(i, color)` in the sparse map, replace `pool[i]` only if `i in 0 until pool.size` (data-pool slots only — D-09; ignore out-of-range). Build `directional` = Directional(temperature/xy/z baked from gen.directional). Pass `fs` and the radii (rScreen=30.dp, rCard=22.dp, rCtrl=16.dp, rPill=999.dp) through. Every value is a baked sRGB `Color(0x..)` int — never an Oklab-space Color (BakedTokens header law). REPLACE the RED bodies in `TokenBridgeTest.kt`: `derivesInBetweenTiers_matchesDinghyJs` asserts a sampled set of derived tiers (e.g. bg2/surface2/text3/hair) equal the dinghy.js-derived values for the default-dark generated palette; `appliesPoolOverrides_atIndex` asserts an override at index i replaces `pool[i]` and leaves other slots untouched.
  </action>
  <verify>
    <automated>cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --tests works.mees.dinghy.theme.TokenBridgeTest --no-daemon" | tr -d '\r'</automated>
  </verify>
  <acceptance_criteria>
    - `theme/TokenBridge.kt` exists with `fun build(gen, overrides, fs): ThemeTokens`. Verify: `grep -n "fun build" app/src/main/java/works/mees/dinghy/theme/TokenBridge.kt` matches.
    - `lShift` calls `Palette`'s `internal` `hexToOklch`/`oklchToHex` (no re-ported OKLCH math in TokenBridge). Verify: `grep -nE "hexToOklch|oklchToHex" app/src/main/java/works/mees/dinghy/theme/TokenBridge.kt` matches (unless 15-02's SUMMARY moved derivation into Palette — then a Palette tier call is present instead).
    - `TokenBridgeTest.derivesInBetweenTiers_matchesDinghyJs` GREEN — derived tiers match dinghy.js for the default-dark palette.
    - `TokenBridgeTest.appliesPoolOverrides_atIndex` GREEN — `overrides[i]=c` replaces `pool[i]`, other indices unchanged; out-of-range index ignored (no crash).
    - `bg` equals the generator's pure-neutral bg (no tint added) — D-16. Source assertion: no `oklch .012` / chroma-tint added in the bridge.
    - All bridge output is baked sRGB Color (no Color constructed in a non-sRGB color space). Verify: `grep -nE "ColorSpaces|Oklab" app/src/main/java/works/mees/dinghy/theme/TokenBridge.kt` returns nothing.
    - Both main AND test sourcesets compile (`:app:testDebugUnitTest` running proves it).
  </acceptance_criteria>
  <done>`TokenBridge.build` produces a complete `ThemeTokens` from a generated palette with derived tiers (via Palette's internal helpers) + applied overrides + pure-neutral surfaces; the bridge test is GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Palette.Generated (hex strings) → ThemeTokens (baked Color) | The bridge is where pure math output becomes Android Color; the literal-sRGB-only law applies at this seam. |
| sparse poolOverrides map → pool[i] replacement | Override indices/colors originate from persisted user data (validated in 15-05); the bridge applies them defensively (out-of-range index ignored). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-03-01 | Tampering | poolOverrides index out of range → crash on pool[i] | mitigate | Bridge applies overrides defensively — only replace indices in `0 until pool.size`; ignore (drop) out-of-range entries. |
| T-15-03-02 | Information disclosure | Oklab-space Color silently wrong on API<26 | mitigate | Bridge bakes only sRGB Color(0x..) ints; acceptance grep forbids ColorSpaces/Oklab. |
| T-15-03-03 | Denial of Service | tier derivation throws on a degenerate palette | accept | `lShift` clamps L to [0,1]; generator output is total. Low risk — no I/O, pure math. |
| T-15-03-SC | Tampering | npm/pip/cargo installs | accept | Zero external packages (RESEARCH §"Package Legitimacy Audit": N/A). |
</threat_model>

<verification>
- `TokenBridgeTest` GREEN; module compiles with extended `ThemeTokens` (main + test sourcesets).
- Surfaces pure-neutral (D-16); pool/directional fields present; violet is a flagged @Deprecated shim.
</verification>

<success_criteria>
- `ThemeTokens` extended (pool + directional), violet retired to a flagged @Deprecated shim (deleted in 15-07).
- `TokenBridge.build` derives tiers (via Palette's internal helpers) + applies overrides + emits pure-neutral surfaces, all baked sRGB.
- Bridge test GREEN.
</success_criteria>

<output>
Create `.planning/phases/15-theme-system-settings-redesign/15-03-SUMMARY.md` when done.
</output>
