---
phase: 25-browse-screens
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/main/java/works/mees/dinghy/designsystem/icons/DinghyIcons.kt
  - app/src/main/res/values/strings.xml
  - tools/verify_ligatures.py
files_modified_note: "DinghyIcons.kt AND strings.xml are owned EXCLUSIVELY by this Wave-1 plan. It registers ALL browse-screen string resources (cd_* + labels for Files/Console/Macros/Webcam) up front so the Wave-2 screen plans reference stringResource IDs without editing strings.xml — preventing a same-wave strings.xml conflict."
autonomous: false
requirements: []
must_haves:
  truths:
    - "Every glyph the four migrated screens need is either already in DinghyIcons or registered here from an OWNER-ASSIGNED source (bucket note or existing screen), never auto-picked (D-21)"
    - "Console filter toggles have registered tokens for hide-temperatures, hide-timelapse, hide-prompts per the owner's bucket assignments (D-15 + D-21)"
    - "Files action glyphs (start-print, delete, back, sort-by-age) resolve to registered tokens (D-07 + D-21)"
    - "Macros glyphs (the macros leader, bookmark/manage, execute, pin/unpin affordance) resolve to registered tokens or are escalated to the owner (D-09/D-10/D-12 + D-21)"
    - "Any function lacking an owner-assigned glyph STOPS and is surfaced to the owner — no Material Symbol chosen independently (D-21)"
    - "verify_ligatures.py NEEDED set includes every newly-registered ligature and the gate reports missing: [] (D-20)"
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/designsystem/icons/DinghyIcons.kt"
      provides: "Registered DinghyIcon tokens for all browse-screen functions"
      contains: "DinghyIcon(IconRef.Ligature"
    - path: "tools/verify_ligatures.py"
      provides: "Drift-guard NEEDED set extended with the new browse glyphs"
      contains: "NEEDED"
  key_links:
    - from: "DinghyIcons.all"
      to: "DinghyIconsTest"
      via: "drift-guard derives uniqueness from the all list"
      pattern: "val all"
---

<objective>
Register the DinghyIcon tokens the four migrated browse screens consume, so 25-03..25-06 ship
tokenized-first (D-20) and never use raw ligature strings. The current Files/Console/Macros screens
use raw `symbol = "..."` ligatures (print, thermostat, videocam, chat_bubble, etc.) — these become
registered `DinghyIcons.*` tokens sourced ONLY from the owner's `img/material-icon-bucket.json`
assignments and existing screens. Any function WITHOUT an owner-assigned glyph triggers the D-21
ASK gate — STOP and ask the owner, never auto-pick.

Purpose: Unblock preview-first/tokenized-first migration for all four screens with owner-sanctioned glyphs only.
Output: New DinghyIcons tokens + strings + an extended verify_ligatures drift guard.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/25-browse-screens/25-CONTEXT.md
@.claude/skills/sketch-findings-dinghy-display/references/foundations.md
@.planning/notes/2026-06-09-icon-assignments-redesign.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Resolve every browse-screen glyph against the owner registry; ASK on any gap (D-21)</name>
  <files>.planning/phases/25-browse-screens/25-ICONMAP.md</files>
  <read_first>
    - img/material-icon-bucket.json (the owner-curated glyph assignments — the SOLE auto-approvable source)
    - .planning/notes/2026-06-09-icon-assignments-redesign.md (owner glyph assignments from the redesign sketches)
    - app/src/main/java/works/mees/dinghy/designsystem/icons/DinghyIcons.kt (what is already registered — do not re-register)
    - app/src/main/java/works/mees/dinghy/ui/files/FilesScreen.kt (current raw ligatures: "schedule", "inventory_2", "qr_code_scanner", "print", "delete")
    - app/src/main/java/works/mees/dinghy/ui/console/ConsoleScreen.kt (current raw ligatures: "thermostat", "videocam", "chat_bubble")
    - app/src/main/java/works/mees/dinghy/ui/macros/BookmarkedMacrosScreen.kt + SystemMacrosScreen.kt (current macro glyphs)
    - .claude/skills/sketch-findings-dinghy-display/references/foundations.md (icon-registry never-auto-pick law)
  </read_first>
  <decision>For each function the four browse screens need, which OWNER-ASSIGNED glyph (ligature) does it map to — and which functions have NO assignment and must be escalated?</decision>
  <context>
    The owner bucket (`img/material-icon-bucket.json`) ALREADY assigns the console-filter and Files
    glyphs explicitly. From the bucket notes (these are auto-approvable — they ARE owner assignments):
      - Console hide-temperatures: `mode_heat_off` (note "hide temperature messages in console"); show = `mode_heat`
      - Console hide-timelapse: `video_camera_back` (note "webcam menu icon, also show timelapse in console")
      - Console hide-prompts: `chat_error` (note "hide macro prompt messages in console"); show = `chat`
      - Files start-print: `print` (note "start a print")
      - Files delete: `delete` (note present)
      - Back: `arrow_back` (registered as DinghyIcons.Back already) or `keyboard_return` (bucket note "Back button")
      - Files sort-by-age/date: `calendar_clock` (note "sort by date in spoolman page"; reuse DinghyIcons.CalendarClock)
      - Hidden files / reveal: `visibility_off` (note "hidden files or items") + `disabled_visible` (note "hidden items being shown")
      - Macros leader: `code` (note "macros") — and `bolt` is already DinghyIcons.LauncherMacros
      - Bookmark/manage: `bookmark_manager` / `bookmark_add` (bucket entries); general settings = `tune`
    The bucket does NOT clearly assign: the SORT-DIRECTION arrow indicator, the macro "Execute"
    action glyph, the macro pin/unpin TRAILING affordance in Manage mode (check_circle vs.
    radio_button_unchecked vs. a bookmark glyph), and possibly the Files "sort" leader vs. a leaner
    age-direction control. These are D-21 GAPS — escalate, do NOT pick.
  </context>
  <options>
    <option id="map-and-ask">
      <name>Map every function to its bucket/existing-screen assignment; escalate ONLY the genuine gaps</name>
      <pros>Honors D-21 exactly; the console-filter + Files glyphs are owner-assigned so they proceed; only the truly-unassigned handful interrupts the owner</pros>
      <cons>Requires one owner round-trip for the gap glyphs (sort-direction arrow, macro Execute, pin/unpin trailing affordance)</cons>
    </option>
  </options>
  <action>
    Produce `25-ICONMAP.md`: a table of EVERY glyph the four screens need → its owner-assigned source
    (bucket note quote or existing DinghyIcons entry) → registration name. For console filters, register
    the HIDE-state glyph the toggle shows (the toggle means "hide X"): temps→`mode_heat_off`,
    timelapse→`video_camera_back`, prompts→`chat_error` (these are the bucket's exact assignments). For
    Files: `print` (start), `delete` (delete), reuse `DinghyIcons.Back` + `DinghyIcons.CalendarClock`.
    For Macros: `code` (leader), `bookmark_manager`/`tune` (manage), reuse `DinghyIcons.CheckCircle` for a
    bookmarked-state trailing tick. For the UNASSIGNED functions (sort-direction arrow, macro "Execute"
    glyph, the unbookmarked/pin trailing affordance) — STOP and ASK the owner in this checkpoint; quote
    the gap list and the candidate glyphs you found in the bucket (do not pick). Record the owner's
    answers in `25-ICONMAP.md`. Do NOT author any new drawable; all browse glyphs are Material Symbols
    by ligature.
  </action>
  <verify>
    <automated>test -f .planning/phases/25-browse-screens/25-ICONMAP.md && grep -iq glyph .planning/phases/25-browse-screens/25-ICONMAP.md && echo PRESENT</automated>
  </verify>
  <done>25-ICONMAP.md maps every browse-screen function to an owner-assigned glyph; all gaps are owner-resolved (none auto-picked).</done>
  <resume-signal>Owner confirms the glyph map and assigns ligatures for any escalated gap. Type "approved" or list the glyph(s) for each gap.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Register the confirmed tokens in DinghyIcons + strings + extend verify_ligatures</name>
  <files>app/src/main/java/works/mees/dinghy/designsystem/icons/DinghyIcons.kt, app/src/main/res/values/strings.xml, tools/verify_ligatures.py</files>
  <read_first>
    - .planning/phases/25-browse-screens/25-ICONMAP.md (the confirmed glyph map from Task 1 — the source of truth for registration)
    - app/src/main/java/works/mees/dinghy/designsystem/icons/DinghyIcons.kt (registration pattern: `val X = DinghyIcon(IconRef.Ligature("lig"), alternate = "unique_handle")`; the `all` list at line ~171 is the drift-guard list)
    - app/src/main/java/works/mees/dinghy/designsystem/icons/DinghyIcon.kt (DinghyIcon / IconRef shape)
    - tools/verify_ligatures.py (the NEEDED set at line ~63; gate dedups on ligature string)
    - app/src/test/java/works/mees/dinghy/designsystem/icons/DinghyIconsTest.kt (derives uniqueness from `all` — confirm new entries keep alternates unique)
    - app/src/main/res/values/strings.xml (where cd_* content-description strings live)
  </read_first>
  <action>
    Register each confirmed-but-not-yet-present token from `25-ICONMAP.md` as a `DinghyIcon(IconRef.Ligature("<lig>"), alternate = "<unique_handle>")` in DinghyIcons.kt, grouped under a
    `// --- Browse screens (Phase 25) ---` section. Concrete registrations (only those NOT already
    present — reuse DinghyIcons.Back / .CalendarClock / .CheckCircle / .QrCode / .Scale / .LauncherMacros /
    .LauncherConsole / .LauncherWebcam / .Home as-is, do NOT duplicate):
      - Files: `Print` ("print"), `Delete` ("delete")
      - Console filters (hide-state glyphs): `HideTemps` ("mode_heat_off"), `HideTimelapse` ("video_camera_back"), `HidePrompts` ("chat_error")
      - Macros: `MacrosLeader` ("code"), `ManageMacros` (the owner-confirmed bucket glyph — `bookmark_manager` or `tune` per Task 1), plus any owner-assigned Execute / pin glyphs from Task 1
    Add each new ligature to the `all` list AND to `tools/verify_ligatures.py` NEEDED set (dedup on the
    ligature string — do not re-add a ligature already present, e.g. video_camera_back may already be
    needed by another surface). Add `cd_*` content-description strings to strings.xml for each new icon
    use (e.g. `cd_files_print`, `cd_files_delete`, `cd_console_hide_temps`, `cd_console_hide_timelapse`,
    `cd_console_hide_prompts`, `cd_macros_manage`, `cd_macros_execute`). Every alternate handle MUST be
    unique (DinghyIconsTest enforces this).
  </action>
  <verify>
    <automated>python3 tools/verify_ligatures.py 2>&1 | tail -2; cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --tests *DinghyIconsTest* --no-daemon" 2>&1 | tr -d '\r' | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `python3 tools/verify_ligatures.py` prints `missing: []` (every NEEDED ligature resolves in the bundled font)
    - `DinghyIconsTest` passes (all alternates unique; no drift)
    - `grep -c "IconRef.Ligature" app/src/main/java/works/mees/dinghy/designsystem/icons/DinghyIcons.kt` increased by exactly the count of new tokens in 25-ICONMAP.md
    - Every new token appears in the `all` list (grep each new val name in the listOf block)
    - No new `IconRef.Drawable(` entry was added (browse glyphs are all Material Symbols)
    - strings.xml contains a `cd_*` entry for each newly-registered browse icon use
  </acceptance_criteria>
  <done>All owner-confirmed browse-screen glyphs are registered tokens, the drift guard is green, and verify_ligatures reports no missing ligatures.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Icon registration is build-time only; no runtime trust boundary crossed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-02-01 | Tampering | An auto-picked glyph diverges from owner intent | mitigate | D-21 ASK gate (Task 1 checkpoint) blocks registration of any unassigned glyph |
| T-25-02-SC | Tampering | npm/pip/cargo installs | accept | No package installs (registers existing-font ligatures only) |
</threat_model>

<verification>
- verify_ligatures.py reports missing: [].
- DinghyIconsTest green (unique alternates, drift-clean).
- 25-ICONMAP.md records every glyph's owner-assigned source.
</verification>

<success_criteria>
- Every browse-screen function maps to an owner-assigned glyph (D-21 honored; gaps escalated, none auto-picked).
- New tokens registered, drift-guard green, content-descriptions present (tokenized-first foundation for D-20).
</success_criteria>

<output>
Create `.planning/phases/25-browse-screens/25-02-SUMMARY.md` when done. List the registered tokens and their owner-assigned sources.
</output>
