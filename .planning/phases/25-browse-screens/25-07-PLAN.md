---
phase: 25-browse-screens
plan: 07
type: execute
wave: 4
depends_on: ["25-03", "25-04", "25-05", "25-06"]
files_modified:
  - app/src/main/java/works/mees/dinghy/dev/BrowseSpikeActivity.kt
  - app/src/main/AndroidManifest.xml
  - .planning/phases/25-browse-screens/25-UAT.md
autonomous: false
requirements: []
must_haves:
  truths:
    - "All four migrated screens (Files, Macros, Console, Webcam) are owner-approved on flox in BOTH portrait and landscape (SC-1)"
    - "Per-screen conformance is verified on-device: >=64px targets (or documented exception), fsSp at S/M/L without clipping, correct rotation (SC-3 / D-19)"
    - "No functional regression on-device: Files delete/print, macro run with params, console scrollback + filter toggles, webcam H.264 playback all work (SC-5)"
    - "The Files delete-scoping regression is re-verified on-device: during an active print, delete is blocked ONLY for the printing file; other files remain deletable (D-08)"
    - "The webcam idle-list HIDE gating is verified on-device: toggle webcam per-profile + switch printers -> independent state; the idle-list Webcam row appears/disappears correctly (D-18)"
    - "The throwaway BrowseSpikeActivity is deleted and de-registered now that the spike decision is locked (25-01 cleanup)"
    - "The full host unit suite is green and the release build is clean before the gate"
  artifacts:
    - path: ".planning/phases/25-browse-screens/25-UAT.md"
      provides: "The on-device UAT checklist + owner results for all four screens + D-08/D-18 checks"
      contains: "approved"
  key_links:
    - from: "25-UAT.md"
      to: "flox device"
      via: "owner on-device verification in both orientations"
      pattern: "flox"
---

<objective>
The load-bearing on-device phase gate. Build the release APK, run the per-screen owner UAT on flox
(genuine Nexus 7 2013 / Adreno 320) in BOTH orientations for all four migrated screens, re-verify the
two folded regression/verification items (D-08 delete-scoping, D-18 webcam idle-list gating), and
delete the throwaway spike harness. This is the SC-1/SC-3/SC-5 owner-approval gate.

Purpose: Prove the migration on the real perf-floor hardware with the owner's eyes — the mock-vs-reality
discipline that makes the gate authoritative.
Output: 25-UAT.md with owner sign-off + spike-harness cleanup.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/25-browse-screens/25-CONTEXT.md
@.planning/phases/25-browse-screens/25-VALIDATION.md
@.planning/todos/pending/files-delete-gating-too-broad.md
@.planning/todos/pending/2026-06-05-webcam-tile-gating-verification.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete the throwaway spike harness + build the release APK + scaffold 25-UAT.md</name>
  <files>app/src/main/java/works/mees/dinghy/dev/BrowseSpikeActivity.kt, app/src/main/AndroidManifest.xml, .planning/phases/25-browse-screens/25-UAT.md</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/dev/BrowseSpikeActivity.kt (the throwaway harness from 25-01 — delete it now that 25-SPIKE.md locked the verdict)
    - app/src/main/AndroidManifest.xml (the spike activity entry to remove)
    - .planning/phases/25-browse-screens/25-VALIDATION.md (the manual-only verification table — the source of the UAT checklist items)
    - .planning/phases/25-browse-screens/25-SPIKE.md (confirm the verdict is recorded before deleting the harness)
    - CLAUDE.md (release build: gw.bat :app:assembleRelease + sign-release.bat; stale-APK gate — force-rebuild + verify APK mtime postdates the latest fix commit)
  </read_first>
  <action>
    Delete `BrowseSpikeActivity.kt` and remove its `<activity>` entry from AndroidManifest.xml (the
    spike decision is locked in 25-SPIKE.md — the harness is throwaway per 25-01). Run the full host unit
    suite to confirm green, then build the release APK (`gw.bat :app:assembleRelease`). Per the
    stale-APK gate, force a clean rebuild if needed and confirm the APK mtime postdates the latest
    screen-fix commit. Scaffold `25-UAT.md` with a checklist row per item: (1) Files portrait + landscape
    owner-approve · (2) Macros portrait + landscape · (3) Console portrait + landscape · (4) Webcam
    portrait + landscape · (5) D-08 delete-scoping during an active print · (6) D-18 webcam idle-list
    per-profile HIDE gating · (7) fsSp S/M/L no-clip + rotation per screen · (8) functional smokes (Files
    delete/print, macro run w/ params, console scrollback+filters, webcam H.264 playback). Each row stays
    PENDING until the Task-2 checkpoint records the owner result.
  </action>
  <verify>
    <automated>test ! -f app/src/main/java/works/mees/dinghy/dev/BrowseSpikeActivity.kt && cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --no-daemon" 2>&1 | tr -d '\r' | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `BrowseSpikeActivity.kt` no longer exists; `grep -c BrowseSpikeActivity app/src/main/AndroidManifest.xml` returns 0
    - Full `:app:testDebugUnitTest` exits 0 (BUILD SUCCESSFUL, no regressions across all four migrations)
    - `:app:assembleRelease` exits 0 and the APK mtime postdates the latest screen-fix commit
    - `25-UAT.md` exists with a PENDING row for each of the 8 checklist items (4 screens x both orientations + D-08 + D-18 + conformance + smokes)
  </acceptance_criteria>
  <done>The spike harness is gone, the release APK is freshly built, and 25-UAT.md is scaffolded for the owner gate.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: On-device flox UAT — owner approval of all four screens + D-08/D-18 re-verification</name>
  <files>.planning/phases/25-browse-screens/25-UAT.md</files>
  <read_first>
    - .planning/phases/25-browse-screens/25-UAT.md (the checklist scaffolded in Task 1)
    - .planning/todos/pending/files-delete-gating-too-broad.md (the D-08 on-device check definition)
    - .planning/todos/pending/2026-06-05-webcam-tile-gating-verification.md (the D-18 on-device check definition)
    - memory: dinghy-display-printer-addresses (E5+ 192.168.1.120:7125, E3 192.168.1.121:7125 — for webcam H.264 + multi-printer D-18)
  </read_first>
  <action>
    Install the fresh release APK on flox; have the owner verify each migrated screen (Files/Macros/Console/Webcam) in BOTH orientations, run the functional smokes, and re-verify D-08 (delete-scoping during an active print) and D-18 (webcam idle-list per-profile HIDE gating). Record each row's result in 25-UAT.md.
  </action>
  <what-built>
    The full release build with all four migrated browse screens. The owner verifies each on flox in both
    orientations, runs the functional smokes, and re-verifies the two folded items (D-08 delete-scoping,
    D-18 webcam idle-list gating). Results are recorded per row in 25-UAT.md.
  </what-built>
  <how-to-verify>
    Install the release APK (debug-signed) on flox. Connect to a live printer (E5+ or E3 — both ravens-perch
    MediaMTX H.264). For EACH of Files / Macros / Console / Webcam, in BOTH portrait and landscape:
    1. Open the screen; confirm it renders on the kit (translucent ListRows, FootButtonBar, no bespoke
       styling), >=64px targets, fsSp legible at the current text size, no clipping; rotate and re-check.
    2. Cycle text size S/M/L (via the dev cycler or Settings) and confirm no clipping at any size.
    Functional smokes:
    - Files: select a file, tap Print (confirm guard shows thumbnail/details), tap Delete (confirm). D-08:
      START a print, then confirm Delete is BLOCKED only for the printing file and ENABLED for others.
    - Macros: run a parametered macro — confirm the param Field-takeover (numeric numpad + string field),
      Execute fires, and an injection-y string param is rejected (toast, no run); run Manage mode pin/unpin
      + Show-hidden.
    - Console: confirm live scrollback auto-scrolls; toggle all 3 filters ON then OFF — confirm hidden lines
      RE-APPEAR (render-time filter, D-15).
    - Webcam: open the tile — NO CRASH (D-17); confirm H.264 playback renders; the FootButtonBar Back works.
      D-18: toggle webcam per-profile in Settings, switch printers — confirm the idle-list Webcam row
      appears/disappears independently per profile.
    Record each row's result (approved / issue) in 25-UAT.md.
  </how-to-verify>
  <verify>
    <automated>test -f .planning/phases/25-browse-screens/25-UAT.md && grep -iqE "approved|resolved|pass" .planning/phases/25-browse-screens/25-UAT.md && echo PRESENT</automated>
  </verify>
  <done>25-UAT.md records owner per-screen approval in both orientations + the D-08/D-18 results.</done>
  <resume-signal>Owner reports per-screen approval + the D-08/D-18 results on flox in both orientations. Type "approved" or list issues for gap closure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Files delete → Moonraker (mid-print) | The destructive delete during an active print is the sensitive on-device re-verification (D-08) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-07-01 | Tampering | Deleting the active print file mid-print regressed | mitigate | D-08 re-verified live on flox during an active print (the files-delete-gating-too-broad.md check) |
| T-25-07-02 | Denial of Service | Webcam crash still present on the real device | mitigate | Owner opens the Webcam tile on flox; no-crash is an explicit UAT row (D-17) |
| T-25-07-SC | Tampering | npm/pip/cargo installs | accept | No package installs |
</threat_model>

<verification>
- Spike harness deleted; full host suite green; release APK fresh (stale-APK gate honored).
- Owner sign-off recorded per row in 25-UAT.md, both orientations.
- D-08 + D-18 re-verified live on flox.
</verification>

<success_criteria>
- All four screens owner-approved on flox in both orientations (SC-1).
- Per-screen conformance verified on-device (SC-3 / D-19).
- No functional regressions; D-08 + D-18 re-verified (SC-5 / D-08 / D-18).
</success_criteria>

<output>
Create `.planning/phases/25-browse-screens/25-07-SUMMARY.md` when done. Record the per-screen UAT outcomes + the D-08/D-18 results; flag any issues for gap closure.
</output>
