---
phase: 25-browse-screens
plan: 03
type: execute
wave: 2
depends_on: ["25-01", "25-02"]
files_modified:
  - app/src/main/java/works/mees/dinghy/ui/files/FilesScreen.kt
  - app/src/main/java/works/mees/dinghy/ui/files/FileListView.kt
  - app/src/main/java/works/mees/dinghy/preview/FilesPreviews.kt
files_modified_note: "This plan APPENDS its own Files screen-label strings to the shared app/src/main/res/values/strings.xml (shared with 25-04/25-05). strings.xml is append-only across the Wave-2 screen plans: safe under this project's sequential-on-master execution (each plan appends distinct cd_*/label keys, no plan rewrites the file); would conflict only under parallel worktrees (not used here). The shared icon cd_* strings are owned by 25-02; this plan adds only Files-specific labels not already present."
autonomous: true
requirements: []
must_haves:
  truths:
    - "FilesScreen is rebuilt on the kit: image-backed DetailCard Focus (future-print fields only) + ListBlock/ListRow-or-conformed-Views Field + FootButtonBar, gutter = null (D-04)"
    - "The file list is FLAT — only FileBrowserRowKind.File rows render; Up and Directory rows are never shown; holder.loadRoot() is the only list call (D-05)"
    - "A single age/date sort with a direction toggle is present; NO filter facet, NO name/size sort (D-06)"
    - "FootButtonBar = Start-print (Accent, through the full-screen Confirm guard with thumbnail/details) · Delete (red, Confirm) · Back; SpoolWarningGuard preserved on print (D-07)"
    - "The deleteAllowed(selectedPath, activePrintFilename, printState) predicate gates BOTH the screen Delete button AND survives in the holder dispatch path — not regressed (D-08)"
    - "The Files list surface uses the toolkit chosen by the 25-01 spike verdict (literal Compose ListRow if PASS, or visually-conformed Views documented as a class-equivalent exception if FAIL) (D-01/D-02)"
    - "FloatingEStop is wired to live printerState (Files is mid-print-valid per Phase-24 D-04)"
    - "An @Preview matrix (6 theme combos + fs=L + ≥1 landscape) renders with fake state and no live Moonraker; strings are stringResource; icons are DinghyIcons tokens (D-19/D-20)"
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/ui/files/FilesScreen.kt"
      provides: "Rebuilt Files screen on the Phase-23 kit"
      contains: "ScreenScaffold"
    - path: "app/src/main/java/works/mees/dinghy/preview/FilesPreviews.kt"
      provides: "@Preview matrix for FilesScreen"
      contains: "@Preview"
  key_links:
    - from: "FilesScreen Delete button"
      to: "deleteAllowed(...)"
      via: "enabled gate"
      pattern: "deleteAllowed"
    - from: "FilesScreen"
      to: "FileBrowserHolder.loadRoot"
      via: "LaunchedEffect on entry"
      pattern: "loadRoot"
---

<objective>
Rebuild FilesScreen on the Phase-23 component kit, modeled on the SpoolScreen two-pane pilot:
image-backed `DetailCard` Focus (future-print fields only) + a flat file `ListBlock`/`ListRow`
(or visually-conformed Views) Field + a `FootButtonBar` (Print · Delete · Back), `gutter = null`.
Add a single age/date sort with direction toggle (no filters). Preserve verbatim the delete-scoping
predicate, the SpoolWarningGuard, and both Confirm guards. Ship the @Preview matrix and tokenized
strings/icons in this same plan (D-20).

The Files list surface toolkit is decided by the 25-01 spike — read `25-01-SUMMARY.md` / `25-SPIKE.md`
verdict and branch: literal Compose `ListRow` (PASS) OR visually-conformed `FileListView` Views
documented in COMPONENTS.md as a "class-equivalent (Views)" exception (FAIL).

NOTE on strings.xml: this plan appends its own Files screen-label strings to the shared
`app/src/main/res/values/strings.xml`. That file is append-only across the Wave-2 screen plans
(25-03/04/05) — safe under sequential-on-master execution; only the 25-02-owned icon cd_* strings
are exclusive. Append distinct Files keys; do not rewrite the file.

Purpose: Migrate the Files browse screen to the redesign grammar with zero functional regression.
Output: Rebuilt FilesScreen + FilesPreviews + tokenized strings.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/25-browse-screens/25-CONTEXT.md
@.planning/phases/25-browse-screens/25-RESEARCH.md
@.planning/phases/25-browse-screens/25-PATTERNS.md
@.planning/phases/25-browse-screens/25-01-SUMMARY.md
@.claude/skills/sketch-findings-dinghy-display/references/lists-and-detail.md
@docs/ui_design/COMPONENTS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rebuild FilesScreen on the kit (two-pane, flat list, age sort, delete-scoping preserved)</name>
  <files>app/src/main/java/works/mees/dinghy/ui/files/FilesScreen.kt, app/src/main/java/works/mees/dinghy/ui/files/FileListView.kt, app/src/main/res/values/strings.xml</files>
  <behavior>
    - deleteAllowed gate: when printState=Printing and selected file == active print file → Delete disabled; when selected file != active print file → Delete enabled (FilesDeleteGateTest must stay green)
    - Flat list: a directory listing containing File + Directory + Up rows renders ONLY the File rows
    - Sort: tapping the age sort toggles ascending/descending; the rendered order reflects the direction
    - Print gate: canStartPrint=false → Print button disabled; SpoolWarningGuard still evaluated (warn-only, never blocks)
  </behavior>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/files/FilesScreen.kt (the CURRENT 680-line screen — preserve ALL guard/gate logic; lines 379-468 FilePreviewFocus is the DetailCard content template; lines 151-155 deleteAllowed; 161-280 SpoolWarningGuard+ConfirmGuard)
    - app/src/main/java/works/mees/dinghy/ui/spool/SpoolScreen.kt (THE template: two-overload seam, BoxWithConstraints+rememberUnitGrid, ScreenScaffold focus/field/gutter=null, DetailCard Focus, ListBlock Field, FootButtonBar, FloatingEStop wiring)
    - app/src/main/java/works/mees/dinghy/ui/files/FileBrowserHolder.kt (state shape: directory.rows, selectedFile, selectedPreview, FileBrowserRowKind; loadRoot/selectFile/requestStartSelected/requestDeleteSelected)
    - app/src/main/java/works/mees/dinghy/ui/files/FileListView.kt (current Views surface — retain pinned-height pattern only if spike FAIL)
    - .planning/phases/25-browse-screens/25-01-SUMMARY.md (the Files toolkit verdict — Compose or Views)
    - app/src/main/java/works/mees/dinghy/designsystem/components/DetailCard.kt + ListRow.kt + FootButtonBar.kt + SortFilterControlRow.kt + FloatingEStop.kt
    - app/src/main/java/works/mees/dinghy/designsystem/layout/ListBlock.kt + UnitGrid.kt
    - app/src/main/java/works/mees/dinghy/designsystem/icons/DinghyIcons.kt (Print, Delete, Back, CalendarClock, Sort — registered in 25-02)
    - app/src/test/java/works/mees/dinghy/ui/files/FilesDeleteGateTest.kt (the regression anchor — must stay green)
    - .planning/phases/25-browse-screens/25-PATTERNS.md (FilesScreen section: exact Focus/Field/Foot layout, D-05 row-kind filter, image-backed DetailCard content)
  </read_first>
  <action>
    Rebuild FilesScreen with the SpoolScreen two-overload seam (live `FilesScreen(holder, dispatcher,
    container, onBack, ...)` collecting StateFlow + delegating to a `private fun FilesContent(...)`, plus
    a stateless `FilesScreen(state, onBack, onPrint, onDelete, onSelectSort, onEmergencyStop, isPrinting)`
    preview seam). Inside FilesContent: `BoxWithConstraints` → `rememberUnitGrid(minOf(maxWidth,maxHeight))`
    → `ScreenScaffold(focus = {...}, field = {...}, gutter = null)`.
    Focus = an image-backed `DetailCard` of `state.selectedFile` showing FUTURE-PRINT fields ONLY (est
    time · filament · layers · height · size · modified) — port the FilePreviewFocus content (current
    lines 379-468) INTO `DetailCard { }` with the dimmed AsyncImage background (ContentScale.Fit alpha
    0.3f matchParentSize), filename in GeistMono, stat rows via fsSp. NEVER show elapsed/finished/status
    (the established "Files Focus = future-print fields only" grammar). Place `FloatingEStop(visible =
    isPrinting, ...)` in the Focus Box, aligned TopStart, opening the e-stop ConfirmGuard sibling.
    Add the D-06 single age/date SORT with direction toggle: per discretion, use a `SortRow` with ONE
    SortOption (icon = DinghyIcons.CalendarClock; leading Sort type-tile) whose re-tap toggles
    ascending/descending — sort state lives in the screen (a `FileSortAscending: Boolean` reordering the
    File rows in-place). NO filter facet, NO name/size sort (D-06). If the owner-assigned sort-direction
    glyph from 25-02 differs, use that token; do NOT auto-pick.
    Field = the flat file list (D-05 — render ONLY `FileBrowserRowKind.File` rows; never Up/Directory;
    call `holder.loadRoot()` once in `LaunchedEffect(holder)`), then `FootButtonBar` (Back Neutral
    DinghyIcons.Back · Print Accent DinghyIcons.Print · Delete Danger DinghyIcons.Delete). Branch the list
    surface on the 25-01 verdict: if "Files: Compose" → `ListBlock { items(fileRows, key={it.stableId}) {
    ListRow(selected = it.stableId==state.selectedFile?.stableId, onClick=..., uDp=grid.uDp,
    leadingContent={thumbnail}, trailingContent={size+modified}) { filename GeistMono } } }`; if "Files:
    Views" → keep `FileListView` with the load-bearing `clipToBounds()` + `MATCH_PARENT` + `height(maxHeight)`
    pin AND the Phase-22 applyTokens equality guard, restyle the RecyclerView item to MATCH ListRow
    (transparent/outline fill, fsSp, token colors), and ADD a "class-equivalent (Views)" exception note to
    docs/ui_design/COMPONENTS.md.
    PRESERVE VERBATIM (only reskin buttons to OutlinedControl + DinghyIcons): `deleteAllowed(selectedPath =
    selected?.relativeFilename, activePrintFilename = printerState.printFilename, printState =
    printerState.printState)` gating the Delete button; the SpoolWarningGuard + evaluatePrintStartGate
    warn-only path; the full-screen ConfirmGuard for Start (with thumbnail/details) and Delete; the
    `canStartPrint` Print gate. Route all text through stringResource + fsSp; all icons via DinghyIcons tokens.
  </action>
  <verify>
    <automated>cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --tests *FilesDeleteGateTest* --tests *FileBrowserHolderDeleteTest* --no-daemon" 2>&1 | tr -d '\r' | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - `FilesDeleteGateTest` and `FileBrowserHolderDeleteTest` pass unchanged (D-08 not regressed)
    - `grep -c "ScreenScaffold" app/src/main/java/works/mees/dinghy/ui/files/FilesScreen.kt` ≥ 1 and `grep -c "gutter = null" FilesScreen.kt` ≥ 1
    - `grep -c "deleteAllowed" app/src/main/java/works/mees/dinghy/ui/files/FilesScreen.kt` ≥ 1
    - `grep -c "loadRoot" FilesScreen.kt` ≥ 1; `grep -v '^#' FilesScreen.kt | grep -cE "enterFolder|goUp"` returns 0 (flat list — no folder nav calls)
    - `grep -cE "FileBrowserRowKind\.File" FilesScreen.kt` ≥ 1 (renders File rows only)
    - No raw `symbol = "print"|"delete"|"arrow_back"` ligatures remain in the FootButtonBar (grep returns 0); icons use `DinghyIcons.`
    - `:app:assembleDebug` compiles (run as part of the wave merge)
  </acceptance_criteria>
  <done>FilesScreen renders on the kit with a flat age-sorted list, image-backed DetailCard, FootButtonBar, the spike-chosen toolkit, and all delete/print guards intact.</done>
</task>

<task type="auto">
  <name>Task 2: Ship the FilesScreen @Preview matrix + tokenized strings (D-20)</name>
  <files>app/src/main/java/works/mees/dinghy/preview/FilesPreviews.kt, app/src/main/res/values/strings.xml</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/preview/SpoolPreviews.kt (the template: stateless overload + fake fixtures + 6-combo matrix + landscape; SampleFixtures usage)
    - app/src/main/java/works/mees/dinghy/preview/PreviewPlaceholders.kt + PreviewTheming.kt (PreviewBox idiom, fsLargeSeed for fs=L)
    - docs/ui_design/PREVIEW_AND_TOKENS.md (the matrix shape: Dark/M, Dark/L, Light/M, Custom/M, Dark/S, Light/S + ≥1 landscape)
    - app/src/main/java/works/mees/dinghy/ui/files/FilesScreen.kt (the stateless overload to drive)
  </read_first>
  <action>
    Create `FilesPreviews.kt` mirroring SpoolPreviews: a fake FilesScreenState fixture (a handful of
    File rows, one selected with a FilePreviewMetadata of est-time/filament/layers/height/size/modified,
    isPrinting both false and true variants) driving the stateless `FilesScreen(state = ...)` overload —
    NO live Moonraker. Emit the 6-combo theme matrix (Dark/M, Dark/L, Light/M, Custom/M, Dark/S, Light/S)
    via the PreviewBox idiom plus at least one landscape `@Preview(widthDp=800, heightDp=480)`. Ensure any
    preview-unsafe surface (the thumbnail AsyncImage) uses a `LocalInspectionMode` placeholder so the
    matrix renders without network. Add any missing `cd_*`/label strings to strings.xml referenced by the
    rebuilt screen.
  </action>
  <verify>
    <automated>cmd.exe /c "E:\Android\gw.bat :app:assembleDebug --no-daemon" 2>&1 | tr -d '\r' | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `FilesPreviews.kt` exists with ≥6 `@Preview` annotations (the theme matrix) and ≥1 landscape preview
    - `grep -c "FilesScreen(" app/src/main/java/works/mees/dinghy/preview/FilesPreviews.kt` ≥ 1 (drives the stateless overload)
    - No `collectAsStateWithLifecycle` / holder reference in FilesPreviews.kt (preview is Moonraker-free)
    - `:app:assembleDebug` exits 0 (the @Preview compile gate passes — SC-4)
    - Every user-visible string in the rebuilt FilesScreen resolves via `stringResource` (grep shows no hardcoded English Text literals in the FootButtonBar/labels)
  </acceptance_criteria>
  <done>The FilesScreen @Preview matrix compiles across all 6 theme combos + landscape with fake state and no live Moonraker.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Files screen → Moonraker | Delete/print commands cross to the printer; the destructive delete is the sensitive surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-03-01 | Tampering | Deleting the active print file mid-print | mitigate | `deleteAllowed(...)` predicate gates the Delete button AND the holder dispatch (preserved verbatim, D-08); FilesDeleteGateTest enforces |
| T-25-03-02 | Denial of Service | Starting a print without a loaded/correct spool | accept | SpoolWarningGuard is warn-only by design (never blocks); preserved as-is |
| T-25-03-SC | Tampering | npm/pip/cargo installs | accept | No package installs (reuses existing kit) |
</threat_model>

<verification>
- FilesDeleteGateTest + FileBrowserHolderDeleteTest green (D-08).
- assembleDebug green (@Preview compile gate, SC-4).
- Flat-list, gutter=null, DinghyIcons tokens enforced via grep gates.
</verification>

<success_criteria>
- FilesScreen rebuilt on the kit per D-04/D-05/D-06/D-07, spike-chosen toolkit (D-01/D-02), delete-scoping preserved (D-08).
- @Preview matrix + tokenized strings/icons ship in-plan (D-19/D-20).
- No functional regression to delete/print (host tests green).
</success_criteria>

<output>
Create `.planning/phases/25-browse-screens/25-03-SUMMARY.md` when done. State which toolkit was used (per the 25-01 verdict) and whether a COMPONENTS.md class-equivalent exception was added.
</output>
