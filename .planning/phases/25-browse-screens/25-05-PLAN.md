---
phase: 25-browse-screens
plan: 05
type: execute
wave: 2
depends_on: ["25-02"]
files_modified:
  - app/src/main/java/works/mees/dinghy/ui/macros/BookmarkedMacrosScreen.kt
  - app/src/main/java/works/mees/dinghy/ui/macros/SystemMacrosScreen.kt
  - app/src/main/java/works/mees/dinghy/ui/macros/MacroExecutionPopup.kt
  - app/src/main/java/works/mees/dinghy/ui/shell/AppShell.kt
  - app/src/main/java/works/mees/dinghy/preview/MacrosPreviews.kt
files_modified_note: "AppShell.kt is touched here ONLY for the Macros nav wiring (remove SystemMacrosScreen route + MacroExecutionPopup overlay). 25-06 also touches AppShell.kt (webcam WR-02) — these two are in the SAME wave; assign 25-06 to a LATER wave OR coordinate the disjoint line ranges. See wave note."
autonomous: true
requirements: []
must_haves:
  truths:
    - "Macros is ONE screen: Bookmarked launcher PRIMARY + System Manage as an in-screen MODE via a foot button, sharing MacroHolder; the separate SystemMacrosScreen nav route is removed (D-09)"
    - "The Bookmarked launcher is a ListRow LIST (translucent rows, tap-to-execute), NOT a LazyVerticalGrid of MacroTiles (D-10)"
    - "The Bookmarked list is sized so ~9-12 entries fill the Field responsively (portrait+landscape) and scroll only beyond that, not wasting vertical space for a handful (D-11)"
    - "Macro param entry is a per-param Field-takeover (numeric→NumpadPage, string→TokenTextField), replacing the full-screen MacroExecutionPopup; an Execute foot button fires (D-12)"
    - "The existing MacroInvocation.buildTyped string sanitizer + MacroParamRejected handling stays on the dispatch path — never bypassed (D-12 / V5)"
    - "Macro PROMPT-protocol dialogs (action:prompt_*) remain floating overlays, untouched and out of scope (D-13)"
    - "The WR-03 reactive body resolution is preserved: live VM re-resolved from holder by name each recomposition; Execute disabled until paramsKnown(name) (cold-connect safety)"
    - "An @Preview matrix (6 theme combos + fs=L + ≥1 landscape) renders the launcher + a param-entry mode with fake state, no live Moonraker; strings stringResource; icons DinghyIcons (D-19/D-20)"
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/ui/macros/BookmarkedMacrosScreen.kt"
      provides: "Merged single Macros screen with MacroFieldMode (Launcher/ParamEntry/ManageMode)"
      contains: "MacroFieldMode"
    - path: "app/src/main/java/works/mees/dinghy/preview/MacrosPreviews.kt"
      provides: "@Preview matrix for the Macros screen"
      contains: "@Preview"
  key_links:
    - from: "Macro ParamEntry Execute"
      to: "MacroInvocation.buildTyped(...)"
      via: "sanitized dispatch"
      pattern: "MacroInvocation\\.buildTyped"
    - from: "ParamEntry field"
      to: "holder.paramsKnown(name)"
      via: "Execute gate (WR-03 cold-connect)"
      pattern: "paramsKnown"
---

<objective>
Collapse the three Macros surfaces (BookmarkedMacrosScreen + SystemMacrosScreen + MacroExecutionPopup)
into ONE screen built on the SpoolScreen FieldMode pattern: a Bookmarked launcher (ListRow list,
D-10), a System "Manage" in-screen mode reached by a foot button (D-09), and a per-param Field-takeover
param entry (D-12) replacing the full-screen popup. Preserve the MacroInvocation sanitizer, the WR-03
reactive body resolution, and the floating PROMPT-protocol dialogs (D-13). Ship the @Preview matrix +
tokenized strings/icons in-plan (D-20).

Purpose: Migrate the Macros browse surface to the redesign grammar (lists-everywhere, Field-takeover)
with no functional regression to macro execution or param sanitization.
Output: Merged Macros screen + removed SystemMacrosScreen/MacroExecutionPopup + MacrosPreviews + AppShell rewire.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/25-browse-screens/25-CONTEXT.md
@.planning/phases/25-browse-screens/25-RESEARCH.md
@.planning/phases/25-browse-screens/25-PATTERNS.md
@.claude/skills/sketch-findings-dinghy-display/references/lists-and-detail.md
@.planning/phases/24-navigation-spine/24-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Merge Macros into one FieldMode screen (Launcher list + Manage mode + param Field-takeover)</name>
  <files>app/src/main/java/works/mees/dinghy/ui/macros/BookmarkedMacrosScreen.kt, app/src/main/java/works/mees/dinghy/ui/macros/SystemMacrosScreen.kt, app/src/main/java/works/mees/dinghy/ui/macros/MacroExecutionPopup.kt, app/src/main/res/values/strings.xml</files>
  <behavior>
    - Sanitizer: a macro string param containing a forbidden injection char is rejected by MacroInvocation.buildTyped → MacroParamRejected → toast, NO dispatch (MacroInvocationTest stays green)
    - Cold-connect gate: a parametered macro whose body has not loaded → Execute disabled (paramsKnown(name)=false); after body loads → enabled
    - Field-takeover: tapping a numeric param row replaces the Field with NumpadPage; back returns to the param list with the committed value shown
    - Mode swap: tapping the Manage foot button swaps the Launcher list to the system manage list in-place (no nav push); Back returns to Launcher
    - Execute key: dispatch uses key "macro_<name>" (PRIM-05)
  </behavior>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/macros/BookmarkedMacrosScreen.kt (current launcher — lines 61-106 the LazyVerticalGrid of MacroTiles being replaced by a ListRow list; state.bookmarkedMacros, state.unavailable, onRunMacro, onManage)
    - app/src/main/java/works/mees/dinghy/ui/macros/MacroExecutionPopup.kt (the 363-line popup whose ENTIRE logic must migrate into a ParamEntry Field: lines 104-277 param flow; WR-03 re-resolve live VM by name; paramsKnown gate; mutableStateMapOf seeded from default; numeric→NumpadPage MACRO_NUMERIC_RANGE; string→TokenTextField; buildTyped sanitizer lines 145-163; DispatchEvent.Failure collect lines 136-142; running=busyKey in inFlight; Execute key macro_<name>)
    - app/src/main/java/works/mees/dinghy/ui/macros/SystemMacrosScreen.kt (the manage list being absorbed — state.visibleMacros, state.revealHidden, onToggleBookmark, onSetRevealHidden; MacroSelectRow lines 122-161 = manage-row template; Show-hidden toggle lines 110-115)
    - app/src/main/java/works/mees/dinghy/ui/macros/MacroHolder.kt (shared holder — UNTOUCHED; paramsKnown(name), state.macros/bookmarkedMacros/visibleMacros/revealHidden)
    - app/src/main/java/works/mees/dinghy/command/MacroInvocation.kt + MacroParamParser.kt (UNTOUCHED — the V5 sanitizer on the dispatch path)
    - app/src/main/java/works/mees/dinghy/ui/spool/SpoolScreen.kt (FieldMode sealed-class pattern + the when(fieldMode) field render + two-overload seam)
    - app/src/main/java/works/mees/dinghy/designsystem/components/ListRow.kt + FootButtonBar.kt; designsystem/layout/ListBlock.kt + UnitGrid.kt
    - app/src/main/java/works/mees/dinghy/designsystem/icons/DinghyIcons.kt (MacrosLeader, ManageMacros, CheckCircle, Back + the owner-confirmed Execute glyph from 25-02)
    - app/src/test/java/works/mees/dinghy/command/MacroInvocationTest.kt (the regression anchor)
    - .planning/phases/25-browse-screens/25-PATTERNS.md (BookmarkedMacrosScreen section: MacroFieldMode, MacroLauncherField, MacroParamEntryField, MacroManageField — the exact target structure)
  </read_first>
  <action>
    Define `sealed class MacroFieldMode { data object Launcher; data class ParamEntry(val macro:
    MacroVm); data object ManageMode }` (in the macros package). Rebuild BookmarkedMacrosScreen with the
    two-overload seam: live `BookmarkedMacrosScreen(holder, dispatcher, onBack)` collecting
    `holder.state` + a `var fieldMode by remember { mutableStateOf<MacroFieldMode>(Launcher) }`,
    delegating to `private fun MacrosContent(...)`; plus a stateless preview overload. Inside MacrosContent:
    `BoxWithConstraints` → `rememberUnitGrid(...)` → `ScreenScaffold(focus = null, field = {
    when(fieldMode) { Launcher -> MacroLauncherField(...); is ParamEntry -> MacroParamEntryField(macro,
    ...); ManageMode -> MacroManageField(...) } }, gutter = null)`.
    - LAUNCHER (D-10): a `ListBlock` of `ListRow`s over `state.bookmarkedMacros` (key = name; tap →
      `fieldMode = ParamEntry(macro)`; macro.name in GeistMono, fsSp). Show the no-macros copy when
      `state.unavailable`. SIZE the list per D-11 so ~9-12 entries fill the Field responsively before
      scrolling (the Field weight(1f) + the U-derived row height already yields ~5-7 rows landscape /
      more portrait — verify the typical handful does NOT leave dead space; if it does, the ListBlock
      should not force a min-height that wastes space). Foot: Back (Neutral, DinghyIcons.Back) + Manage
      (Neutral, DinghyIcons.ManageMacros → `fieldMode = ManageMode`).
    - PARAMENTRY (D-12): migrate the ENTIRE MacroExecutionPopup logic INTO this Field. Re-resolve the
      live VM from holder by name each recomposition (WR-03); `val bodyLoaded = holder.paramsKnown(name)`;
      `values = remember(name, params){ mutableStateMapOf(*params seeded from default) }`; a `var
      numpadParam` that, when non-null, REPLACES the Field with `NumpadPage(range = MACRO_NUMERIC_RANGE,
      onSet = { values[name]=fmt; numpadParam=null }, onCancel={numpadParam=null})` (Field-takeover).
      Otherwise a `ListBlock` with one row per param: numeric → tappable row opening NumpadPage; string →
      `TokenTextField` (the ONE sanctioned alpha keyboard). Foot: Back (→ Launcher) + Execute (Accent,
      disabled while `!bodyLoaded || running`). `execute()` calls `MacroInvocation.buildTyped(name,
      params.map{Triple(name, values[name].orEmpty(), isNumeric)})` inside try/catch MacroParamRejected
      → toast + return; on success `dispatcher.dispatch(key="macro_$name", method=GCODE_SCRIPT,
      params=scriptParams(gcode))` then `fieldMode = Launcher`. Collect `dispatcher.events` for
      `DispatchEvent.Failure` keyed on `macro_$name` → toast.
    - MANAGEMODE (D-09): a `ListBlock` over `state.visibleMacros` (key = name; tap → onToggleBookmark;
      trailing = CheckCircle when bookmarked / the owner-confirmed unbookmarked glyph from 25-02). Foot:
      Back (→ Launcher) + Show-hidden toggle (Accent if revealHidden else Neutral → onSetRevealHidden).
    DELETE the now-absorbed MacroExecutionPopup.kt and SystemMacrosScreen.kt bodies (or reduce to nothing
    and remove their nav references in Task 2). Keep MacroHolder/MacroInvocation/MacroParamParser
    UNTOUCHED. Do NOT touch the PROMPT-protocol dialogs (D-13). Route all strings via stringResource +
    fsSp; all icons via DinghyIcons tokens.
  </action>
  <verify>
    <automated>cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --tests *MacroInvocationTest* --no-daemon" 2>&1 | tr -d '\r' | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - `MacroInvocationTest` passes unchanged (sanitizer not bypassed, D-12/V5)
    - `grep -c "MacroFieldMode" app/src/main/java/works/mees/dinghy/ui/macros/BookmarkedMacrosScreen.kt` ≥ 1 (Launcher/ParamEntry/ManageMode)
    - `grep -c "MacroInvocation.buildTyped" BookmarkedMacrosScreen.kt` ≥ 1 AND `grep -c "paramsKnown" BookmarkedMacrosScreen.kt` ≥ 1 (sanitizer + cold-connect gate preserved)
    - `grep -vE '^\s*//' BookmarkedMacrosScreen.kt | grep -cE "LazyVerticalGrid|MacroTile"` returns 0 (grid replaced by ListRow list, D-10)
    - `grep -c "gutter = null" BookmarkedMacrosScreen.kt` ≥ 1
    - The macro Execute dispatch uses key `"macro_"` (grep) — PRIM-05 preserved
    - `:app:assembleDebug` compiles (wave merge)
  </acceptance_criteria>
  <done>Macros is one FieldMode screen: ListRow launcher + foot-button Manage mode + per-param Field-takeover, with the sanitizer/WR-03/PRIM-05 behavior intact.</done>
</task>

<task type="auto">
  <name>Task 2: Rewire AppShell to the merged Macros screen + ship the @Preview matrix (D-20)</name>
  <files>app/src/main/java/works/mees/dinghy/ui/shell/AppShell.kt, app/src/main/java/works/mees/dinghy/preview/MacrosPreviews.kt, app/src/main/res/values/strings.xml</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/shell/AppShell.kt (the Macros nav wiring: import lines 81/82/84, the BookmarkedMacrosScreen composable ~677, SystemMacrosScreen ~670, MacroExecutionPopup overlay ~936 — these references must be removed/reduced to the single merged screen)
    - app/src/main/java/works/mees/dinghy/preview/SpoolPreviews.kt (matrix template)
    - app/src/main/java/works/mees/dinghy/preview/PreviewTheming.kt (theme combos, fs=L)
    - docs/ui_design/PREVIEW_AND_TOKENS.md (6-combo matrix shape)
    - app/src/main/java/works/mees/dinghy/ui/macros/BookmarkedMacrosScreen.kt (the stateless overload + MacroFieldMode to drive in previews)
  </read_first>
  <action>
    In AppShell.kt: remove the SystemMacrosScreen nav route and the MacroExecutionPopup overlay; route the
    single Macros nav entry (Phase-24 idle-list "Macros" + drawer tile) to the merged
    `BookmarkedMacrosScreen(holder, dispatcher, onBack)`. Drop the now-unused imports
    (SystemMacrosScreen, MacroExecutionPopup). Preserve the Phase-24 holder hoisting + nav entry contract
    (do NOT re-wire navigation shape — just collapse the two macro destinations into one and remove the
    popup overlay). IMPORTANT: AppShell.kt is also touched by 25-06 (webcam WR-02) — touch ONLY the macro
    lines here; if 25-06 runs in the same wave, coordinate disjoint ranges (the orchestrator assigns
    25-06 to a later wave to avoid the overlap — confirm via depends_on).
    Create `MacrosPreviews.kt`: fake MacroHolder-state fixtures driving the stateless overload in BOTH
    Launcher mode (a list of ~10 bookmarked macros) and ParamEntry mode (a macro with a numeric + a string
    param) — NO live Moonraker. Emit the 6-combo theme matrix + ≥1 landscape preview. Add any missing
    label/cd_* strings.
  </action>
  <verify>
    <automated>cmd.exe /c "E:\Android\gw.bat :app:assembleDebug --no-daemon" 2>&1 | tr -d '\r' | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `:app:assembleDebug` exits 0 with no unresolved reference to SystemMacrosScreen or MacroExecutionPopup
    - `grep -c "SystemMacrosScreen\|MacroExecutionPopup" app/src/main/java/works/mees/dinghy/ui/shell/AppShell.kt` returns 0 (routes removed)
    - `git diff --stat app/src/main/java/works/mees/dinghy/ui/shell/AppShell.kt` shows ONLY macro-related line changes (no webcam/line-214 changes — that is 25-06's territory)
    - `MacrosPreviews.kt` exists with ≥6 `@Preview` + ≥1 landscape; drives the stateless overload in Launcher AND ParamEntry modes
    - No holder/collect reference in MacrosPreviews.kt (Moonraker-free); @Preview compile gate green (SC-4)
  </acceptance_criteria>
  <done>AppShell routes to the single merged Macros screen, the old route + popup are gone, and the @Preview matrix compiles.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Macro param entry → Moonraker GCODE_SCRIPT | User-entered param strings cross into a dispatched gcode command — the primary injection surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-05-01 | Tampering | G-code injection via a macro string param | mitigate | `MacroInvocation.buildTyped` V5 sanitizer + `MacroParamRejected` stays on the dispatch path (D-12); string param routes through buildTyped, never directly to scriptParams; MacroInvocationTest enforces |
| T-25-05-02 | Tampering | Executing a parametered macro before its body loads (cold connect) → bare command | mitigate | `paramsKnown(name)` gate disables Execute until body loaded (WR-03) |
| T-25-05-SC | Tampering | npm/pip/cargo installs | accept | No package installs (reuses existing kit + existing sanitizer) |
</threat_model>

<verification>
- MacroInvocationTest green (sanitizer not bypassed, D-12).
- assembleDebug green (no dangling SystemMacrosScreen/MacroExecutionPopup; @Preview gate, SC-4).
- FieldMode + ListRow-launcher + paramsKnown gate enforced via grep gates.
</verification>

<success_criteria>
- Macros merged to one FieldMode screen per D-09/D-10/D-11/D-12; PROMPT dialogs untouched (D-13).
- Sanitizer + WR-03 + PRIM-05 behavior preserved (no regression).
- @Preview matrix + tokenized strings/icons in-plan (D-19/D-20).
</success_criteria>

<output>
Create `.planning/phases/25-browse-screens/25-05-SUMMARY.md` when done. Note that SystemMacrosScreen + MacroExecutionPopup were absorbed/removed and the AppShell macro routes collapsed to one.
</output>
