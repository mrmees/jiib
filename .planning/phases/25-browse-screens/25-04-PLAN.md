---
phase: 25-browse-screens
plan: 04
type: execute
wave: 2
depends_on: ["25-01", "25-02"]
files_modified:
  - app/src/main/java/works/mees/dinghy/ui/console/ConsoleScreen.kt
  - app/src/main/java/works/mees/dinghy/ui/console/ConsoleListView.kt
  - app/src/main/java/works/mees/dinghy/preview/ConsolePreviews.kt
files_modified_note: "This plan APPENDS its own Console screen-label strings to app/src/main/res/values/strings.xml (shared with 25-03/25-05). strings.xml is append-only across the Wave-2 screen plans: safe under this project's sequential-on-master execution (each plan appends distinct cd_*/label keys, no plan rewrites the file); would conflict only under parallel worktrees (not used here). The shared icon cd_* strings are owned by 25-02; this plan adds only Console-specific labels not already present."
autonomous: true
requirements: []
must_haves:
  truths:
    - "ConsoleScreen is rebuilt as a Field-only live log with NO Focus region; the scrollback fills the usable Field (D-14)"
    - "The 3 filters (hide-temperatures / hide-timelapse / hide-prompts) render as always-visible restyled FootButtonBar toggles — NOT a Field-takeover picker, NOT a separate route (D-15)"
    - "ConsoleHolder keeps RAW unfiltered scrollback; ConsoleFilters.apply is called at RENDER time only, so toggling a filter OFF re-reveals previously-hidden lines (D-15 / Phase-8 D-04 preserved)"
    - "The Console list surface uses the toolkit chosen by the 25-01 spike verdict (literal Compose LazyColumn/ListRow if PASS, or visually-conformed Views with stackFromEnd preserved if FAIL) (D-01/D-02)"
    - "gutter = null; the filter toggles + Back live in the FootButtonBar inside the field (D-15)"
    - "An @Preview matrix (6 theme combos + fs=L + ≥1 landscape) renders pre-filtered fake lines with no live Moonraker; strings are stringResource; filter icons are DinghyIcons tokens (D-19/D-20)"
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/ui/console/ConsoleScreen.kt"
      provides: "Rebuilt field-only Console screen on the kit"
      contains: "ConsoleFilters.apply"
    - path: "app/src/main/java/works/mees/dinghy/preview/ConsolePreviews.kt"
      provides: "@Preview matrix for ConsoleScreen"
      contains: "@Preview"
  key_links:
    - from: "ConsoleScreen render"
      to: "ConsoleFilters.apply(rawLines, ...)"
      via: "render-time filter off raw holder state"
      pattern: "ConsoleFilters\\.apply"
---

<objective>
Rebuild ConsoleScreen as a Field-only live log on the Phase-23 kit: no Focus region, the scrollback
fills the Field, and the 3 filters (hide-temps / hide-timelapse / hide-prompts) become always-visible
restyled `FootButtonBar` toggles (NOT a Field-takeover, NOT a separate route — D-15). Preserve the
Phase-8 D-04 invariant: ConsoleHolder keeps RAW lines, `ConsoleFilters.apply` runs at RENDER time only.
Branch the list surface on the 25-01 spike verdict. Ship the @Preview matrix + tokenized strings/icons
in-plan (D-20).

NOTE on strings.xml: this plan appends its own Console screen-label strings to the shared
`app/src/main/res/values/strings.xml`. That file is append-only across the Wave-2 screen plans
(25-03/04/05) — safe under sequential-on-master execution; only the 25-02-owned icon cd_* strings are
exclusive. Append distinct Console keys; do not rewrite the file.

Purpose: Migrate the Console browse screen to the redesign grammar with no functional regression.
Output: Rebuilt ConsoleScreen + ConsolePreviews + tokenized strings.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/25-browse-screens/25-CONTEXT.md
@.planning/phases/25-browse-screens/25-RESEARCH.md
@.planning/phases/25-browse-screens/25-PATTERNS.md
@.planning/phases/25-browse-screens/25-01-SUMMARY.md
@docs/ui_design/COMPONENTS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rebuild ConsoleScreen field-only with render-time filter toggles (spike-chosen surface)</name>
  <files>app/src/main/java/works/mees/dinghy/ui/console/ConsoleScreen.kt, app/src/main/java/works/mees/dinghy/ui/console/ConsoleListView.kt, app/src/main/res/values/strings.xml</files>
  <behavior>
    - ConsoleFilters render-time: with hideTemps=true a temperature line is hidden; toggling hideTemps=false RE-REVEALS it from the same raw source (no permanent loss — ConsoleFiltersTest stays green)
    - Field-only: the screen renders no Focus/DetailCard region; the log fills the Field
    - Toggles always visible: all 3 filter buttons + Back are present at the foot regardless of filter state
    - Auto-scroll: when new lines append and the view was at bottom, it scrolls to the latest line
  </behavior>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/console/ConsoleScreen.kt (the CURRENT 257-line screen — lines 66-75 local filter state + ConsoleFilters.apply; lines 104-122 the 3 FilterToggles with raw symbols; lines 139-176 FilterToggle composable)
    - app/src/main/java/works/mees/dinghy/ui/console/ConsoleListView.kt (current Views surface — stackFromEnd, isSingleAppend/isAppendEvict live-append paths, wasAtBottom race fix; retain only if spike FAIL)
    - app/src/main/java/works/mees/dinghy/ui/console/ConsoleLine.kt (the line model — data class ConsoleLine(rawMessage: String, severity: ConsoleSeverity, timeEpoch: Double?). NOTE: there is NO `id` field — do NOT reference one)
    - app/src/main/java/works/mees/dinghy/ui/console/ConsoleFilters.kt (the render-time view — UNTOUCHED; apply(lines, hideTemps, hideTimelapse, hidePrompt))
    - app/src/main/java/works/mees/dinghy/ui/console/ConsoleHolder.kt (raw lines StateFlow — UNTOUCHED)
    - app/src/main/java/works/mees/dinghy/ui/spool/SpoolScreen.kt (the field-only variant reference: two-overload seam, BoxWithConstraints+rememberUnitGrid, ScreenScaffold focus=null/field/gutter=null, FootButtonBar)
    - .planning/phases/25-browse-screens/25-01-SUMMARY.md (the Console toolkit verdict)
    - app/src/main/java/works/mees/dinghy/designsystem/control/OutlinedControl.kt (intent-colored toggle: Intent.Accent active / Intent.Neutral inactive)
    - app/src/main/java/works/mees/dinghy/designsystem/icons/DinghyIcons.kt (HideTemps, HideTimelapse, HidePrompts, Back — registered in 25-02)
    - app/src/test/java/works/mees/dinghy/ui/console/ConsoleFiltersTest.kt (the regression anchor)
    - .planning/phases/25-browse-screens/25-PATTERNS.md (ConsoleScreen section: two-overload seam, field-only ScreenScaffold, render-time filter, spike branch)
  </read_first>
  <action>
    Rebuild ConsoleScreen with the two-overload seam: live `ConsoleScreen(holder, onBack,
    backfillFailed)` collecting `holder.state` (RAW lines) + the 3 local `mutableStateOf` filter flags,
    computing `val filtered = ConsoleFilters.apply(rawLines, hideTemps, hideTimelapse, hidePrompt)` AT
    RENDER (D-15 — never move this into the holder), delegating to `private fun ConsoleContent(...)`; plus
    a stateless `ConsoleScreen(lines, backfillFailed, hideTemps, hideTimelapse, hidePrompt, onToggle*,
    onBack)` preview seam receiving pre-filtered lines + toggle states.
    Inside ConsoleContent: `BoxWithConstraints` → `rememberUnitGrid(minOf(maxWidth,maxHeight))` →
    `ScreenScaffold(focus = null, field = {...}, gutter = null)` (D-14 — NO Focus). Field = the scrollback
    (filling weight(1f)) then a `FootButtonBar` holding the 3 filter toggles + Back. Each toggle is an
    `OutlinedControl(label="", onClick=onToggle*, modifier=weight(1f), intent = if(hideX) Intent.Accent
    else Intent.Neutral, icon = DinghyIcons.HideTemps|HideTimelapse|HidePrompts)` plus
    `OutlinedControl(onBack, Intent.Neutral, DinghyIcons.Back)`. Preserve the backfillFailed warning notice.
    Branch the list surface on the 25-01 verdict: if "Console: Compose" → a `LazyColumn` with
    `rememberLazyListState` + `LaunchedEffect(lines.size){ if(at-bottom) scrollToItem(lines.size-1) }`,
    using `items(lines)` with NO item `key` lambda — positional diffing is correct and sufficient for an
    append/evict log, and `ConsoleLine` has no stable id to key on (do NOT invent `it.id`; do NOT key on
    rawMessage, which is non-unique). (If a stable key ever becomes warranted for animation, wrap lines at
    RENDER with a screen-side monotonic counter — but the ConsoleHolder stays UNTOUCHED, so do not add an
    id to the model.) If "Console: Views" → keep `ConsoleListView` with stackFromEnd + the isSingleAppend/
    isAppendEvict live-append paths + the wasAtBottom race fix + clipToBounds + MATCH_PARENT +
    height(maxHeight) pin, restyle the row to match the kit (token colors, fsSp, GeistMono), and ADD a
    "class-equivalent (Views)" exception note to docs/ui_design/COMPONENTS.md.
    Route all strings through stringResource + fsSp; all filter icons via DinghyIcons tokens (no raw
    "thermostat"/"videocam"/"chat_bubble" symbols remain).
  </action>
  <verify>
    <automated>cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --tests *ConsoleFiltersTest* --no-daemon" 2>&1 | tr -d '\r' | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - `ConsoleFiltersTest` passes unchanged (render-time re-reveal invariant intact, D-15)
    - `grep -c "ConsoleFilters.apply" app/src/main/java/works/mees/dinghy/ui/console/ConsoleScreen.kt` ≥ 1 AND `grep -c "ConsoleFilters.apply" app/src/main/java/works/mees/dinghy/ui/console/ConsoleHolder.kt` returns 0 (filter stays at render, never in holder)
    - `grep -c "\.id" app/src/main/java/works/mees/dinghy/ui/console/ConsoleScreen.kt` returns 0 for any `ConsoleLine` `.id` reference (ConsoleLine has no id — no unresolved-reference build-breaker)
    - `grep -c "focus = null" ConsoleScreen.kt` ≥ 1 and `grep -c "gutter = null" ConsoleScreen.kt` ≥ 1 (field-only, D-14)
    - `grep -vE '^\s*//' ConsoleScreen.kt | grep -cE 'symbol *= *"(thermostat|videocam|chat_bubble)"'` returns 0 (no raw filter ligatures); filter icons use `DinghyIcons.`
    - The 3 filter toggles are present in the FootButtonBar (grep for the 3 onToggle handlers)
    - `:app:assembleDebug` compiles (wave merge)
  </acceptance_criteria>
  <done>ConsoleScreen is a field-only live log with always-visible render-time filter toggles, the spike-chosen surface, and the raw-holder invariant intact.</done>
</task>

<task type="auto">
  <name>Task 2: Ship the ConsoleScreen @Preview matrix + tokenized strings (D-20)</name>
  <files>app/src/main/java/works/mees/dinghy/preview/ConsolePreviews.kt, app/src/main/res/values/strings.xml</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/preview/SpoolPreviews.kt (matrix template + PreviewBox idiom)
    - app/src/main/java/works/mees/dinghy/preview/PreviewTheming.kt (fs=L seeding, theme combos)
    - docs/ui_design/PREVIEW_AND_TOKENS.md (6-combo matrix shape)
    - app/src/main/java/works/mees/dinghy/ui/console/ConsoleScreen.kt (the stateless overload to drive)
    - app/src/main/java/works/mees/dinghy/ui/console/ConsoleLine.kt (ConsoleLine shape for fake fixtures — rawMessage/severity/timeEpoch, no id)
  </read_first>
  <action>
    Create `ConsolePreviews.kt`: a fake list of ConsoleLines (a mix of normal, temperature, timelapse,
    and prompt lines, plus an error/warning line) driving the stateless `ConsoleScreen(lines = ..., ...)`
    overload — NO live Moonraker. Show variants: all-filters-off and at least one filter-on (so the
    toggle Accent state renders). Emit the 6-combo theme matrix (Dark/M, Dark/L, Light/M, Custom/M,
    Dark/S, Light/S) + ≥1 landscape `@Preview(widthDp=800,heightDp=480)`. Add any missing label/cd_*
    strings to strings.xml (append-only; do not rewrite the file).
  </action>
  <verify>
    <automated>cmd.exe /c "E:\Android\gw.bat :app:assembleDebug --no-daemon" 2>&1 | tr -d '\r' | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `ConsolePreviews.kt` exists with ≥6 `@Preview` annotations + ≥1 landscape preview
    - `grep -c "ConsoleScreen(" app/src/main/java/works/mees/dinghy/preview/ConsolePreviews.kt` ≥ 1 (drives stateless overload)
    - No holder/collectAsStateWithLifecycle reference in ConsolePreviews.kt (Moonraker-free)
    - `:app:assembleDebug` exits 0 (@Preview compile gate — SC-4)
  </acceptance_criteria>
  <done>The ConsoleScreen @Preview matrix compiles across 6 theme combos + landscape with fake lines and no live Moonraker.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Moonraker gcode_response → Console | Untrusted printer/console text rendered as log lines (read-only; CONS-01 send deferred) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-04-01 | Information Disclosure | Filter permanently discards lines from the source | mitigate | ConsoleFilters applied at render only; holder keeps RAW lines (D-15); ConsoleFiltersTest enforces re-reveal |
| T-25-04-02 | Tampering | Rendering untrusted console text | accept | Console is read-only display (no send path — CONS-01 deferred); text rendered as inert Text, no eval |
| T-25-04-SC | Tampering | npm/pip/cargo installs | accept | No package installs (reuses existing kit) |
</threat_model>

<verification>
- ConsoleFiltersTest green (render-time re-reveal, D-15).
- assembleDebug green (@Preview gate, SC-4).
- field-only + render-time-filter + token-icons enforced via grep gates.
</verification>

<success_criteria>
- ConsoleScreen rebuilt field-only per D-14, filter toggles restyled per D-15, raw-holder invariant preserved.
- Spike-chosen surface (D-01/D-02); @Preview matrix + tokenized strings/icons in-plan (D-19/D-20).
- No functional regression (ConsoleFiltersTest green).
</success_criteria>

<output>
Create `.planning/phases/25-browse-screens/25-04-SUMMARY.md` when done. State which toolkit was used (per 25-01 verdict) and whether a COMPONENTS.md class-equivalent exception was added.
</output>
