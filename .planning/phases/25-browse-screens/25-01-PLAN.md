---
phase: 25-browse-screens
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/main/java/works/mees/dinghy/dev/BrowseSpikeActivity.kt
  - app/src/main/AndroidManifest.xml
  - .planning/phases/25-browse-screens/25-SPIKE.md
autonomous: false
requirements: []
must_haves:
  truths:
    - "A throwaway Compose LazyColumn + literal ListRow build of the Files list surface runs in a release APK on flox (D-01)"
    - "A throwaway Compose LazyColumn + literal ListRow build of the Console list surface runs in a release APK on flox (D-01)"
    - "Release-mode gfxinfo framestats are captured on flox for each Compose spike surface and parsed for p50/p90/p95 + frozen frames (D-01)"
    - "Each surface's Compose numbers are compared against the Phase-22 Views baseline (Files p90 41.95ms, Console p90 9.26ms) and a per-surface Views-vs-Compose verdict is recorded (D-02)"
    - "The pass/fail verdict applies the ADR-0001 Addendum-2 gate (0 frozen frames AND p90 within the established floor budget) per surface, not a pre-decided toolkit (D-03)"
    - "If either surface is ambiguous/borderline, the captured numbers are surfaced to the owner for the decision rather than guessed (D-03)"
  artifacts:
    - path: ".planning/phases/25-browse-screens/25-SPIKE.md"
      provides: "Per-surface gfxinfo numbers + Views-vs-Compose verdict for Files and Console"
      contains: "verdict"
    - path: "app/src/main/java/works/mees/dinghy/dev/BrowseSpikeActivity.kt"
      provides: "Throwaway release-buildable Compose spike harness for the two list surfaces"
  key_links:
    - from: "BrowseSpikeActivity"
      to: "ListBlock / ListRow"
      via: "literal Compose list build of Files + Console rows"
      pattern: "ListRow"
---

<objective>
Run the D-01/D-02/D-03 toolkit spike: build throwaway Compose `LazyColumn` + literal `ListRow`
versions of the Files and Console list surfaces, capture release-mode `gfxinfo` framestats on the
real flox device, and decide — per surface, on the measured data — whether that surface migrates
to literal Compose `ListRow` or stays classic Views (RecyclerView) conformed visually.

This is a HARD GATE. The result drives the FilesScreen (25-03) and ConsoleScreen (25-04)
migrations. Do NOT pre-decide the toolkit. If results are ambiguous/borderline, surface the
captured numbers to the owner and let them decide.

Purpose: De-risk the consequential fork before committing either screen to a toolkit.
Output: A throwaway spike harness + `25-SPIKE.md` recording per-surface numbers and verdicts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-browse-screens/25-CONTEXT.md
@.planning/phases/25-browse-screens/25-RESEARCH.md
@docs/adr/0001-ui-toolkit-decision.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the throwaway Compose spike harness for Files + Console list surfaces</name>
  <files>app/src/main/java/works/mees/dinghy/dev/BrowseSpikeActivity.kt, app/src/main/AndroidManifest.xml</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/designsystem/layout/ListBlock.kt (the edge-faded LazyColumn the spike must use literally)
    - app/src/main/java/works/mees/dinghy/designsystem/components/ListRow.kt (the row class; signature ListRow(selected, onClick, uDp, leadingContent?, trailingContent?) { content })
    - app/src/main/java/works/mees/dinghy/designsystem/layout/UnitGrid.kt (rememberUnitGrid(minOf(maxWidth,maxHeight)))
    - app/src/main/java/works/mees/dinghy/ui/files/FileListView.kt (current Views Files surface — the row shape to replicate in Compose)
    - app/src/main/java/works/mees/dinghy/ui/console/ConsoleListView.kt (current Views Console surface — stackFromEnd live-log shape)
    - app/src/main/java/works/mees/dinghy/bench/BenchActivity.kt (if present — the existing throwaway-activity + manifest-entry precedent to mirror; otherwise grep AndroidManifest.xml for any dev/bench activity entry)
    - app/src/main/AndroidManifest.xml (where to add the spike activity entry)
  </read_first>
  <action>
    Create `BrowseSpikeActivity` (package `works.mees.dinghy.dev`) — a throwaway ComponentActivity
    that renders TWO selectable Compose list scenes with NO live Moonraker: (A) a Files-shaped
    `ListBlock { items(fakeFiles, key) { ListRow(...) } }` populated with ~60 synthetic file rows
    (filename in GeistMono + size + modified date trailing), and (B) a Console-shaped scene using a
    LazyColumn with `reverseLayout`/stackFromEnd-equivalent + `rememberLazyListState` programmatic
    scroll-to-bottom, populated with ~300 synthetic console lines. Switch scenes by an intent extra
    `spike_surface` ("files" | "console"), mirroring `BenchActivity.EXTRA_SCENE` if that precedent
    exists. Wrap content in `DinghyTheme` with the default dark theme. Use `rememberUnitGrid` for U.
    Use literal `ListRow`/`ListBlock` — this is the candidate Compose path under test, so it must be
    representative of what 25-03/25-04 would ship (stable `key` on items, fsSp text, token colors).
    Register the activity in AndroidManifest.xml as a non-default, non-exported dev activity (no
    intent-filter MAIN/LAUNCHER), launchable via `adb shell am start -n works.mees.dinghy/.dev.BrowseSpikeActivity --es spike_surface files`.
    This file is THROWAWAY — it is deleted in the on-device gate plan (25-07) after the spike decision is locked.
    Do NOT auto-pick any new icon glyph; the spike rows need no icons beyond what already exists. If a
    glyph is tempting, omit it (the spike measures list scroll perf, not chrome).
  </action>
  <verify>
    <automated>cmd.exe /c "E:\Android\gw.bat :app:assembleRelease --no-daemon" 2>&1 | tr -d '\r' | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `BrowseSpikeActivity.kt` exists in `app/src/main/java/works/mees/dinghy/dev/` and compiles into the release APK
    - `grep -c "ListRow(" app/src/main/java/works/mees/dinghy/dev/BrowseSpikeActivity.kt` returns ≥1 (uses the literal kit class, not a hand-rolled row)
    - `grep -c "key *=" app/src/main/java/works/mees/dinghy/dev/BrowseSpikeActivity.kt` returns ≥2 (stable keys on both list scenes)
    - AndroidManifest.xml contains an `<activity ...BrowseSpikeActivity...>` entry with NO `<category android:name="android.intent.category.LAUNCHER"/>`
    - `:app:assembleRelease` exits 0 (BUILD SUCCESSFUL)
  </acceptance_criteria>
  <done>The release APK builds with a launchable, Moonraker-free Compose spike harness for both the Files and Console list surfaces.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Capture flox gfxinfo for both Compose surfaces + record per-surface verdict</name>
  <files>.planning/phases/25-browse-screens/25-SPIKE.md</files>
  <read_first>
    - .planning/phases/22-*/22-GFXINFO-BASELINE.md (the Phase-22 Views baselines: Files p50=8.91/p90=41.95/p95=42.96, 0 frozen; Console p50=7.79/p90=9.26/p95=10.29, 0 frozen — the comparison floor)
    - tools/gfxinfo-parser/parse_framestats.py (the parser; same one Phase 22 used)
    - .planning/phases/25-browse-screens/25-RESEARCH.md (Pattern 1: the capture protocol; Spike Reference section)
    - docs/adr/0001-ui-toolkit-decision.md (Addendum-2: the 0-frozen-frames + p90-floor gate definition)
  </read_first>
  <action>
    Install the release APK on flox, capture release-mode gfxinfo framestats for the Files and Console Compose spike surfaces per the protocol below, parse them with parse_framestats.py, and write a per-surface Compose-vs-Views verdict into 25-SPIKE.md against the ADR-0001 Addendum-2 gate (0 frozen frames + p90 within the floor budget). Surface borderline cases to the owner with the numbers — do not guess. The verdict in 25-SPIKE.md is what 25-03/25-04 branch on.
  </action>
  <what-built>
    A release APK with `BrowseSpikeActivity` rendering literal-Compose Files and Console list surfaces.
    The owner runs the gfxinfo capture protocol on flox (the genuine Nexus 7 2013 / Adreno 320), and
    the captured numbers are parsed and recorded with a per-surface Views-vs-Compose verdict.
  </what-built>
  <how-to-verify>
    Build + debug-sign + install the release APK on flox (per CLAUDE.md sign-release.bat + adb install).
    For EACH surface ("files", then "console"):
    1. `adb shell am start -n works.mees.dinghy/.dev.BrowseSpikeActivity --es spike_surface <surface>`
    2. `adb shell dumpsys gfxinfo works.mees.dinghy reset`
    3. Scroll the list up/down ~4 full sweeps (for Console, let live-append run if simulated, then scroll)
    4. `adb shell dumpsys gfxinfo works.mees.dinghy framestats > /tmp/spike_<surface>.txt` (or pull to repo)
    5. `python3 tools/gfxinfo-parser/parse_framestats.py /tmp/spike_<surface>.txt --warmup 10`
    Record p50/p90/p95 + frozen-frame count for each Compose surface in `25-SPIKE.md`.
    Apply the ADR-0001 Addendum-2 gate PER SURFACE:
      - PASS (→ migrate to literal Compose ListRow): 0 frozen frames AND p90 within the floor budget
        (Files: p90 ≤ ~42ms parity-or-better; Console: p90 ≤ ~10ms parity-or-better).
      - FAIL (→ keep Views, conform visually per D-02): frozen frames OR a meaningful p90 regression.
      - AMBIGUOUS/BORDERLINE: present BOTH surfaces' numbers to the owner and ask for the call.
    Write the verdict (Compose | Views) for EACH surface into `25-SPIKE.md`, with the raw numbers.
  </how-to-verify>
  <verify>
    <automated>test -f .planning/phases/25-browse-screens/25-SPIKE.md && grep -iqE "files.*(compose|views)" .planning/phases/25-browse-screens/25-SPIKE.md && grep -iqE "console.*(compose|views)" .planning/phases/25-browse-screens/25-SPIKE.md && echo PRESENT</automated>
  </verify>
  <acceptance_criteria>
    - `25-SPIKE.md` exists and records, for BOTH Files and Console: the Compose p50/p90/p95 + frozen-frame count, the Phase-22 Views baseline numbers, and the floor-budget gate result
    - `25-SPIKE.md` records a single explicit verdict per surface: "Files: Compose" or "Files: Views" (and same for Console)
    - The verdict cites the ADR-0001 Addendum-2 criterion that produced it (0 frozen frames + p90 budget)
    - If either verdict was owner-decided due to ambiguity, the owner's reply is quoted in `25-SPIKE.md`
  </acceptance_criteria>
  <done>25-SPIKE.md records a per-surface Compose-vs-Views verdict for both Files and Console, owner-confirmed.</done>
  <resume-signal>Owner confirms the captured numbers and the per-surface verdict (or selects the toolkit for any borderline surface). Type "approved" or state the per-surface decision.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev activity → app process | The throwaway spike activity runs in the app process but is dev-only, not exported, no launcher entry |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-01-01 | Elevation | BrowseSpikeActivity exported/launchable by other apps | mitigate | Activity registered with NO intent-filter MAIN/LAUNCHER and `android:exported="false"`; throwaway, deleted in 25-07 |
| T-25-01-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan (uses existing kit + existing gfxinfo parser) |
</threat_model>

<verification>
- `:app:assembleRelease` builds the spike harness into the release APK.
- gfxinfo framestats captured on flox and parsed for both surfaces.
- `25-SPIKE.md` records per-surface Compose-vs-Views verdict against the Addendum-2 gate.
</verification>

<success_criteria>
- Throwaway Compose spike harness builds in release mode.
- Per-surface flox gfxinfo numbers captured and parsed.
- `25-SPIKE.md` records the binding Files and Console toolkit verdicts that 25-03/25-04 branch on.
</success_criteria>

<output>
Create `.planning/phases/25-browse-screens/25-01-SUMMARY.md` when done. The SUMMARY MUST state, verbatim,
the two verdicts ("Files: Compose|Views", "Console: Compose|Views") so 25-03/25-04 executors read them directly.
</output>
