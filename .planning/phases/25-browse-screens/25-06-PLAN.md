---
phase: 25-browse-screens
plan: 06
type: execute
wave: 3
depends_on: ["25-02", "25-05"]
files_modified:
  - app/src/main/java/works/mees/dinghy/ui/shell/AppShell.kt
  - app/src/main/java/works/mees/dinghy/ui/webcam/WebcamScreen.kt
  - app/src/main/java/works/mees/dinghy/preview/WebcamPreviews.kt
  - .planning/phases/25-browse-screens/25-WEBCAM-CRASH.md
wave_note: "Wave 3 because it shares AppShell.kt with 25-05 (Wave 2). 25-05 touches only the macro nav lines; this plan touches only the webcam line-214 region — but they are the same file, so this is sequenced after 25-05 to avoid a merge conflict."
autonomous: false
requirements: []
must_haves:
  truths:
    - "The webcam-screen crash is diagnosed from a captured logcat stack trace FIRST, then fixed; the trace is recorded before any code change (D-17)"
    - "The WR-02 URL-resolver bug is fixed: AppShell reads container.activeConfig (the live Phase-14 source), NOT the write-dead container.connectionStore.config (D-17)"
    - "After the fix, the webcamSurfaceProvider + webcamHolder remember-keys consistently source activeCfg from container.activeConfig (no stale/mismatched key — Pitfall 7)"
    - "WebcamScreen is token-conformed only: chrome re-tokened + a FootButtonBar (Back) added; the SurfaceView/Bitmap render path, rung-select, and cam-cycle logic are UNTOUCHED (D-16)"
    - "The DisposableEffect(holder){ start(); onDispose{ stop() } } lifecycle is preserved verbatim (D-16)"
    - "gutter = null; the Back button moves from the gutter into a FootButtonBar in the field"
    - "An @Preview matrix (6 theme combos + fs=L + ≥1 landscape) renders the cam-picker chrome with a placeholder feed (no live stream); strings stringResource; icons DinghyIcons (D-19/D-20)"
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/ui/webcam/WebcamScreen.kt"
      provides: "Token-conformed Webcam screen with FootButtonBar, render path intact"
      contains: "FootButtonBar"
    - path: ".planning/phases/25-browse-screens/25-WEBCAM-CRASH.md"
      provides: "The captured crash logcat + root-cause diagnosis"
      contains: "stack"
  key_links:
    - from: "AppShell webcam config read"
      to: "container.activeConfig"
      via: "collectAsStateWithLifecycle (replacing connectionStore.config)"
      pattern: "container\\.activeConfig"
---

<objective>
Fix the mandatory webcam-screen crash (D-17) and the latent WR-02 URL-resolver bug, then
token-conform WebcamScreen with a light touch (D-16): re-token the chrome, add a `FootButtonBar`
(Back), move Back out of the gutter, `gutter = null`. The SurfaceView/Bitmap render path,
rung-select, cam-cycle logic, and the holder lifecycle are UNTOUCHED. Diagnose the crash from a
captured logcat trace FIRST. Ship the @Preview matrix + tokenized strings/icons in-plan (D-20).

The webcam idle-list HIDE gating (D-18) on-device verification runs in the phase UAT gate (25-07);
this plan delivers the code (the crash fix + WR-02) that makes that verification possible.

Purpose: A crashing screen can't be owner-approved (SC-1) and is a functional regression (SC-5) —
fix it; then conform the chrome to the redesign grammar without reopening Phase-21 streaming work.
Output: Crash-fixed + WR-02-fixed + token-conformed WebcamScreen + WebcamPreviews.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/25-browse-screens/25-CONTEXT.md
@.planning/phases/25-browse-screens/25-RESEARCH.md
@.planning/phases/25-browse-screens/25-PATTERNS.md
@.planning/todos/pending/2026-06-05-webcam-screen-crash.md
@.planning/todos/pending/2026-06-05-webcam-tile-gating-verification.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Capture the webcam crash logcat on flox + record the root-cause diagnosis (D-17)</name>
  <files>.planning/phases/25-browse-screens/25-WEBCAM-CRASH.md</files>
  <read_first>
    - .planning/todos/pending/2026-06-05-webcam-screen-crash.md (the crash report — undiagnosed, no trace yet)
    - .planning/phases/25-browse-screens/25-RESEARCH.md (Webcam Crash & URL Resolver section: candidate causes — NPE in URL resolution, SurfaceView lifecycle ordering; the WR-02 empty-host hypothesis)
    - app/src/main/java/works/mees/dinghy/ui/shell/AppShell.kt (lines 213-256: the connectionStore.config read + webcamSurfaceProvider/webcamHolder remember keys)
    - app/src/main/java/works/mees/dinghy/ui/webcam/WebcamScreen.kt (the screen that crashes on open)
  </read_first>
  <action>
    Capture the webcam-screen crash on flox via adb logcat (capture FIRST, per D-17), reproduce the crash by opening the Webcam tile, and record the FATAL stack trace + a root-cause diagnosis in 25-WEBCAM-CRASH.md before any code change. The diagnosis drives the Task-2 fix.
  </action>
  <what-built>
    The owner opens the Webcam tile on flox while `adb logcat` captures the crash. The stack trace is
    recorded in `25-WEBCAM-CRASH.md` with a root-cause diagnosis BEFORE any code is changed — this is the
    D-17 "capture logcat first" rule (the bug is undiagnosed; do not guess-fix).
  </what-built>
  <how-to-verify>
    Build + install the current debug APK on flox. Start `adb logcat -c` then `adb logcat *:E | grep -i
    "works.mees\|AndroidRuntime\|FATAL"` (or full `adb logcat > trace.txt`). Open the Webcam tile;
    reproduce the crash. Paste the FATAL stack trace into `25-WEBCAM-CRASH.md` and write the root-cause
    line (likely: empty-host config from the write-dead connectionStore → malformed webcam URL →
    IllegalArgumentException on the network path, per the WR-02 hypothesis; or a SurfaceView lifecycle
    ordering issue). The diagnosis drives Task 2.
  </how-to-verify>
  <verify>
    <automated>test -f .planning/phases/25-browse-screens/25-WEBCAM-CRASH.md && grep -iqE "stack|exception|fatal|androidruntime" .planning/phases/25-browse-screens/25-WEBCAM-CRASH.md && echo PRESENT</automated>
  </verify>
  <done>25-WEBCAM-CRASH.md holds the captured crash stack trace + root-cause diagnosis (pre-fix).</done>
  <resume-signal>Owner pastes the crash logcat trace (or confirms the captured trace). Type "captured" with the trace in 25-WEBCAM-CRASH.md.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Fix the WR-02 resolver + crash + token-conform WebcamScreen (light touch, render path untouched)</name>
  <files>app/src/main/java/works/mees/dinghy/ui/shell/AppShell.kt, app/src/main/java/works/mees/dinghy/ui/webcam/WebcamScreen.kt, app/src/main/res/values/strings.xml</files>
  <read_first>
    - .planning/phases/25-browse-screens/25-WEBCAM-CRASH.md (the Task-1 diagnosis — the authoritative fix target)
    - app/src/main/java/works/mees/dinghy/ui/shell/AppShell.kt (lines 213-256: `val cfg by container.connectionStore.config.collectAsStateWithLifecycle(initialValue=null)` + `val activeCfg = cfg ?: ConnectionConfig(host="")` → REPLACE; the webcamSurfaceProvider/webcamHolder remember keys on ~248/250 use activeCfg.host)
    - app/src/main/java/works/mees/dinghy/di/AppContainer.kt (lines 179-203: `activeConfig: Flow<ConnectionConfig?> = activeProfile.map { it?.toConnectionConfig() }` — the correct source; activeProfileId StateFlow)
    - app/src/main/java/works/mees/dinghy/ui/webcam/WebcamScreen.kt (the 267-line screen: FeedFocus lines 150-194 = render path UNTOUCHED; CamPicker lines 199-250 = chrome to re-token; DisposableEffect lines 92-95 = preserve verbatim; gutter Back lines 132-144 = move to FootButtonBar)
    - app/src/main/java/works/mees/dinghy/designsystem/components/FootButtonBar.kt + control/OutlinedControl.kt
    - app/src/main/java/works/mees/dinghy/designsystem/icons/DinghyIcons.kt (Back; LauncherWebcam already exists)
    - .planning/phases/25-browse-screens/25-PATTERNS.md (WebcamScreen section: the exact WR-02 fix lines, FootButtonBar migration, BoxWithConstraints+grid addition)
  </read_first>
  <action>
    WR-02 fix (AppShell.kt): replace `val cfg by container.connectionStore.config.collectAsStateWithLifecycle(initialValue = null)` + `val activeCfg = cfg ?: ConnectionConfig(host = "")`
    with `val activeCfg by container.activeConfig.collectAsStateWithLifecycle(initialValue = null)` (note:
    activeConfig is `Flow<ConnectionConfig?>` — activeCfg is now NULLABLE). Update every downstream use:
    the webcamSurfaceProvider + webcamHolder `remember(store, activeCfg.host, activeProfileId)` keys →
    `activeCfg?.host ?: ""`; the holder factory `cfg = activeCfg` → `cfg = activeCfg ?: ConnectionConfig(host = "")`.
    Ensure BOTH remember keys and the factory arg source from the SAME `container.activeConfig` collection
    (Pitfall 7 — no second flow). Touch ONLY the webcam config region; do NOT touch the macro lines (those
    are 25-05's, already merged in Wave 2).
    Crash fix: apply the fix indicated by the Task-1 diagnosis. If the diagnosis confirms the empty-host
    hypothesis, the WR-02 fix above resolves it (active profile now feeds a real host → valid URL). If the
    trace shows a different cause (e.g. SurfaceView lifecycle ordering, an NPE in the resolver), fix THAT
    specifically and add a guard (e.g. resolveWebcamUrl returns null on an unparseable/empty base rather
    than throwing — mirror the Phase-21 `49f3fe2` graceful-degrade pattern if relevant). Add a regression
    host test if the cause is host-testable.
    Token-conform WebcamScreen (D-16 light touch): in CamPicker, route any bare `.sp` through `fsSp(n,
    t.fs).sp` and any remaining raw colors through tokens (it already uses t.accentLine/t.outline mostly).
    Move the Back button OUT of the `gutter` into a `FootButtonBar(uDp = grid.uDp){ OutlinedControl("",
    onBack, Modifier.weight(1f), Intent.Neutral, icon = DinghyIcons.Back) }` inside the field; set `gutter
    = null`. Add `rememberUnitGrid(minOf(maxWidth,maxHeight))` in the existing BoxWithConstraints for U.
    PRESERVE VERBATIM: FeedFocus / Media3SurfaceHost / WebcamViewHost render path; rung-select + cam-cycle
    logic; the `DisposableEffect(holder){ start(); onDispose{ stop() } }` lifecycle. Do NOT restructure the
    picker into a ListRow list (D-16 — owner chose light touch). Do NOT auto-pick any new glyph.
  </action>
  <verify>
    <automated>cmd.exe /c "E:\Android\gw.bat :app:assembleDebug :app:testDebugUnitTest --no-daemon" 2>&1 | tr -d '\r' | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "container.activeConfig" app/src/main/java/works/mees/dinghy/ui/shell/AppShell.kt` ≥ 1 AND `grep -vE '^\s*//' AppShell.kt | grep -c "connectionStore.config"` returns 0 (WR-02 fixed — write-dead read removed)
    - The webcamSurfaceProvider + webcamHolder remember keys use `activeCfg?.host` (null-safe; grep)
    - `grep -c "FootButtonBar" app/src/main/java/works/mees/dinghy/ui/webcam/WebcamScreen.kt` ≥ 1 AND `grep -c "gutter = null" WebcamScreen.kt` ≥ 1 (Back moved to field foot)
    - `grep -c "DisposableEffect" WebcamScreen.kt` ≥ 1 and `grep -cE "Media3SurfaceHost|WebcamViewHost" WebcamScreen.kt` is UNCHANGED from before (render path untouched — diff the FeedFocus region shows no logic change)
    - `git diff app/src/main/java/works/mees/dinghy/ui/shell/AppShell.kt` shows ONLY the webcam config region changed (no macro-line changes)
    - `:app:assembleDebug` + `:app:testDebugUnitTest` exit 0
  </acceptance_criteria>
  <done>The WR-02 resolver reads activeConfig, the crash cause is fixed per the captured trace, and WebcamScreen is token-conformed with a FootButtonBar — render path intact.</done>
</task>

<task type="auto">
  <name>Task 3: Ship the WebcamScreen @Preview matrix + tokenized strings (D-20)</name>
  <files>app/src/main/java/works/mees/dinghy/preview/WebcamPreviews.kt, app/src/main/res/values/strings.xml</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/preview/SpoolPreviews.kt (matrix template)
    - app/src/main/java/works/mees/dinghy/preview/PreviewPlaceholders.kt (the preview-unsafe-surface placeholder idiom — the feed SurfaceView is preview-unsafe, use a placeholder via LocalInspectionMode)
    - app/src/main/java/works/mees/dinghy/preview/PreviewTheming.kt (theme combos, fs=L)
    - docs/ui_design/PREVIEW_AND_TOKENS.md (6-combo matrix shape; D-05 preview-unsafe-surface placeholder rule)
    - app/src/main/java/works/mees/dinghy/ui/webcam/WebcamScreen.kt (the stateless seam / cam-picker chrome to preview)
  </read_first>
  <action>
    Create `WebcamPreviews.kt`: drive the WebcamScreen chrome (cam-picker + FootButtonBar) with a fake
    cam list and a `LocalInspectionMode` PLACEHOLDER for the feed (the SurfaceView/Media3 host is
    preview-unsafe — show a PreviewPlaceholderBox, never attempt a live stream). If WebcamScreen lacks a
    stateless seam suitable for preview, add a minimal stateless overload that takes the cam list +
    selected-cam + callbacks (NO holder) and have the live overload delegate to it for the chrome — keep
    the render path behind the inspection-mode placeholder. Emit the 6-combo theme matrix + ≥1 landscape
    preview. Add any missing label/cd_* strings.
  </action>
  <verify>
    <automated>cmd.exe /c "E:\Android\gw.bat :app:assembleDebug --no-daemon" 2>&1 | tr -d '\r' | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `WebcamPreviews.kt` exists with ≥6 `@Preview` + ≥1 landscape preview
    - The feed surface in previews is a placeholder gated on `LocalInspectionMode` (no live stream attempted — grep for LocalInspectionMode or a PreviewPlaceholder)
    - No holder/collect reference in WebcamPreviews.kt (Moonraker-free)
    - `:app:assembleDebug` exits 0 (@Preview compile gate — SC-4)
  </acceptance_criteria>
  <done>The WebcamScreen @Preview matrix compiles across 6 theme combos + landscape with a placeholder feed and no live stream.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Profile config → webcam URL resolution | The active-profile host feeds the webcam URL; an empty/stale host produced the crash |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-06-01 | Denial of Service | Empty-host config → malformed URL → process crash on Webcam open | mitigate | WR-02 fix points the resolver at the live container.activeConfig; resolver guards an unparseable/empty base by returning null instead of throwing (per captured-trace diagnosis) |
| T-25-06-02 | Tampering | Reopening the Phase-21 render path introduces a streaming regression | mitigate | D-16 light-touch scope: FeedFocus/Media3SurfaceHost/WebcamViewHost UNTOUCHED; grep gate confirms render region unchanged |
| T-25-06-SC | Tampering | npm/pip/cargo installs | accept | No package installs (reuses existing kit + existing media3 dep) |
</threat_model>

<verification>
- Crash trace captured BEFORE the fix (D-17, 25-WEBCAM-CRASH.md).
- WR-02 grep gate: activeConfig present, connectionStore.config gone.
- Render path untouched (diff gate); assembleDebug + testDebugUnitTest green; @Preview gate (SC-4).
</verification>

<success_criteria>
- Webcam crash diagnosed-then-fixed (D-17); WR-02 resolver reads activeConfig.
- WebcamScreen token-conformed light-touch with FootButtonBar; render path intact (D-16).
- @Preview matrix + tokenized strings/icons in-plan (D-19/D-20).
</success_criteria>

<output>
Create `.planning/phases/25-browse-screens/25-06-SUMMARY.md` when done. State the crash root cause + the specific fix applied, and confirm the render path was untouched.
</output>
