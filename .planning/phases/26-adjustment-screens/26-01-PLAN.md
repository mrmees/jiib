---
phase: 26-adjustment-screens
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/main/java/works/mees/dinghy/designsystem/components/IncrementPicker.kt
  - app/src/main/java/works/mees/dinghy/designsystem/components/AdjusterPanel.kt
  - app/src/test/java/works/mees/dinghy/designsystem/components/IncrementPickerTest.kt
  - app/src/test/java/works/mees/dinghy/designsystem/components/AdjusterPanelTest.kt
  - app/src/main/java/works/mees/dinghy/preview/AdjusterPreviews.kt
autonomous: true
requirements: []
must_haves:
  truths:
    - "A shared IncrementPicker renders a row of step tiles with the active step shown in accent and inactive steps neutral"
    - "A shared AdjusterPanel renders the 3-zone adjuster (header + Reset, centered value, stepper + increment picker)"
    - "The inline 'was X' baseline shows only when the value differs from baseline at the value's decimal precision and disappears at baseline"
    - "Both components render correctly across the 6-combo + fs=L + landscape preview matrix"
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/designsystem/components/IncrementPicker.kt"
      provides: "Shared step-set selector component"
      contains: "fun IncrementPicker"
    - path: "app/src/main/java/works/mees/dinghy/designsystem/components/AdjusterPanel.kt"
      provides: "3-zone adjuster composable + shouldShowBaseline pure predicate"
      contains: "fun AdjusterPanel"
    - path: "app/src/test/java/works/mees/dinghy/designsystem/components/IncrementPickerTest.kt"
      provides: "formatStep + active-step contract tests"
    - path: "app/src/test/java/works/mees/dinghy/designsystem/components/AdjusterPanelTest.kt"
      provides: "shouldShowBaseline pure-predicate tests"
  key_links:
    - from: "AdjusterPanel.kt"
      to: "designsystem/control/OutlinedControl.kt"
      via: "OutlinedControl with Intent routing"
      pattern: "OutlinedControl"
    - from: "IncrementPicker.kt"
      to: "Intent.Accent / Intent.Neutral"
      via: "active-step intent selection"
      pattern: "Intent\\.(Accent|Neutral)"
---

<objective>
Build the two shared design-system components every adjustment screen depends on: `IncrementPicker` (the sketch-003 step-set selector) and `AdjusterPanel` (the 3-zone adjuster Focus surface). These fold/supersede the C2 increment-picker todo (D-20) and implement the sketch-003 adjuster archetype (D-21 inline baseline).

Purpose: Wave 1 foundation. Fine-Tune, Temperature, and Outputs all consume these. Building them first prevents the scavenger-hunt anti-pattern — downstream screens receive the contracts in their plans.
Output: `IncrementPicker.kt`, `AdjusterPanel.kt`, their RED-first host tests, and a preview file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-adjustment-screens/26-CONTEXT.md
@.planning/phases/26-adjustment-screens/26-PATTERNS.md
@.claude/skills/sketch-findings-dinghy-display/references/adjustment-controls.md
@docs/ui_design/PREVIEW_AND_TOKENS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED test scaffolds for IncrementPicker + AdjusterPanel</name>
  <files>app/src/test/java/works/mees/dinghy/designsystem/components/IncrementPickerTest.kt, app/src/test/java/works/mees/dinghy/designsystem/components/AdjusterPanelTest.kt</files>
  <read_first>
    - .planning/phases/26-adjustment-screens/26-PATTERNS.md (IncrementPicker formatStep + AdjusterPanel shouldShowBaseline pure predicate)
    - .planning/phases/26-adjustment-screens/26-RESEARCH.md (Code Examples: shouldShowBaseline)
    - app/src/main/java/works/mees/dinghy/ui/finetune/FineTuneShared.kt (existing fmtValue + step constants, the formatting conventions to mirror)
    - .planning/notes is not present; rely on PATTERNS.md
  </read_first>
  <behavior>
    - IncrementPickerTest: formatStep(5.0) == "±5"; formatStep(0.001) == "±0.001"; formatStep(10.0) == "±10"; formatStep(0.05) == "±0.05" (no trailing-zero noise).
    - AdjusterPanelTest: shouldShowBaseline(105.0, 100.0, 0) == true; shouldShowBaseline(100.0, 100.0, 0) == false; shouldShowBaseline(null, 100.0, 0) == false; shouldShowBaseline(100.0, null, 0) == false; shouldShowBaseline(0.2001, 0.200, 3) == false (rounds equal at 3dp); shouldShowBaseline(0.205, 0.200, 3) == true.
  </behavior>
  <action>
    Create both test files referencing only `formatStep(Double): String` and `shouldShowBaseline(value: Double?, baseline: Double?, decimalPrecision: Int): Boolean` — both `internal` top-level functions that Task 2/3 will create in the same package. The scaffolds MUST COMPILE day-one per the Wave-0 RED-scaffold rule: do NOT reference any composable or unbuilt symbol other than these two pure functions. Write real assertions (not `fail()` bodies) so the tests go GREEN when Tasks 2-3 land. Place tests in package `works.mees.dinghy.designsystem.components`.
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:compileDebugUnitTestKotlin --no-daemon" 2>&1 | tr -d '\r'</automated>
  </verify>
  <acceptance_criteria>
    - Both test files compile (test source set compiles; tests will FAIL only because production symbols are absent until Task 2-3).
    - No reference to any composable or symbol other than `formatStep` and `shouldShowBaseline`.
    - Assertions are concrete (`assertEquals`/`assertFalse`/`assertTrue`), not `fail()` placeholders.
  </acceptance_criteria>
  <done>IncrementPickerTest.kt and AdjusterPanelTest.kt exist, compile, and encode the formatStep + shouldShowBaseline contracts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: IncrementPicker shared component</name>
  <files>app/src/main/java/works/mees/dinghy/designsystem/components/IncrementPicker.kt</files>
  <read_first>
    - .planning/phases/26-adjustment-screens/26-PATTERNS.md (IncrementPicker analog: FootButtonBar.kt + OutlinedControl)
    - app/src/main/java/works/mees/dinghy/designsystem/components/FootButtonBar.kt (the structural Row analog)
    - app/src/main/java/works/mees/dinghy/designsystem/control/OutlinedControl.kt (Intent enum + control signature)
    - app/src/main/java/works/mees/dinghy/designsystem/layout/UnitGrid.kt (uDp touch-floor convention)
  </read_first>
  <action>
    Create `IncrementPicker(steps: ImmutableList<Double>, activeStep: Double, onSelect: (Double) -> Unit, uDp: Dp, modifier: Modifier = Modifier)`. Render a `Row(fillMaxWidth, heightIn(min = uDp), Arrangement.spacedBy(8.dp))` mapping each step to an `OutlinedControl(label = formatStep(step), onClick = { onSelect(step) }, modifier = Modifier.weight(1f), intent = if (step == activeStep) Intent.Accent else Intent.Neutral)`. Add the `internal fun formatStep(step: Double): String` top-level function: whole numbers → "±N" (via `step.toLong()`), otherwise "±" + `step.toBigDecimal().stripTrailingZeros().toPlainString()`. Use `ImmutableList` from `kotlinx.collections.immutable`. Never hardcode a Dp touch floor — use `uDp`. Active step = `Intent.Accent` per the intent law (D-20: active increment = accent).
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --tests works.mees.dinghy.designsystem.components.IncrementPickerTest --no-daemon" 2>&1 | tr -d '\r'</automated>
  </verify>
  <acceptance_criteria>
    - `IncrementPickerTest` passes (formatStep cases all green).
    - `IncrementPicker.kt` contains `fun IncrementPicker(` and `internal fun formatStep(`.
    - Active step branch uses `Intent.Accent`; inactive uses `Intent.Neutral` (source assertion).
    - heightIn uses `uDp`, not a literal `.dp` floor (source assertion: no `heightIn(min = 64.dp)`).
  </acceptance_criteria>
  <done>IncrementPicker renders step tiles, active=accent, and formatStep passes its tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: AdjusterPanel 3-zone composable + baseline predicate + previews</name>
  <files>app/src/main/java/works/mees/dinghy/designsystem/components/AdjusterPanel.kt, app/src/main/java/works/mees/dinghy/preview/AdjusterPreviews.kt</files>
  <read_first>
    - .planning/phases/26-adjustment-screens/26-PATTERNS.md (AdjusterPanel core pattern — exact zone structure + shouldShowBaseline)
    - .claude/skills/sketch-findings-dinghy-display/references/adjustment-controls.md (sketch-003 3-zone spec, inline-baseline rule, what-to-avoid)
    - app/src/main/java/works/mees/dinghy/designsystem/ScrubberPage.kt (zone structure analog)
    - app/src/main/java/works/mees/dinghy/designsystem/components/DetailCard.kt (surface container)
    - app/src/main/java/works/mees/dinghy/ui/finetune/FineTuneShared.kt (DASH + fmtValue — import, do not redefine)
    - app/src/main/java/works/mees/dinghy/theme/ThemeTokens.kt (t.accent2, t.text, t.text3, t.heat tokens)
    - docs/ui_design/PREVIEW_AND_TOKENS.md (the @Preview 6-combo + fs=L matrix shape)
  </read_first>
  <behavior>
    - shouldShowBaseline(value, baseline, decimals) returns false when either is null; true only when rounded-to-decimals values differ.
    - "was X" text renders only when shouldShowBaseline is true; never as a stacked second line — inline on the value's baseline Row.
  </behavior>
  <action>
    Create `AdjusterPanel(icon: DinghyIcon, name: String, value: Double?, unit: String, baseline: Double?, decimals: Int, onDecrement, onIncrement, onReset: (() -> Unit)?, enabled: Boolean, incrementPicker: @Composable () -> Unit, modifier)` exactly per the PATTERNS.md core pattern: a `Column(verticalArrangement = Arrangement.SpaceBetween)` with three zones — Zone 1 header (DinghyIconView tint = t.accent2 + name in Geist SemiBold + Spacer weight + optional `OutlinedControl("Reset", intent = Intent.Warn)` only when onReset != null), Zone 2 centered `Box(weight(1f))` with a `Row(verticalAlignment = Alignment.Bottom)` holding the big GeistMono value (`fsSp(48f, t.fs).sp`, DASH when null) plus the inline "was {baseline}{unit}" span (`t.text3`, `fsSp(18f, t.fs).sp`) gated by `shouldShowBaseline`, Zone 3 a stepper Row (two `OutlinedControl` "−"/"+", `Intent.Accent`, `enabled = enabled && value != null`, weight(1f)) above the caller-provided `incrementPicker()`. Add `internal fun shouldShowBaseline(value: Double?, baseline: Double?, decimalPrecision: Int): Boolean` using `10.0.pow(decimalPrecision)` rounding compare. Reset button uses `Intent.Warn` (routes to t.heat directly — NOT color-mix, the red-bleed bug from adjustment-controls.md). All text via `fsSp(...)`. Create `AdjusterPreviews.kt` with the 6-combo (Dark/Light/Custom × S/M) + fs=L + one landscape (widthDp=800, heightDp=480) `@Preview` matrix driving AdjusterPanel with fake values (value changed from baseline so "was X" shows in at least one preview, and a null-value preview showing DASH + disabled controls).
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --tests works.mees.dinghy.designsystem.components.AdjusterPanelTest --no-daemon" 2>&1 | tr -d '\r'</automated>
  </verify>
  <acceptance_criteria>
    - `AdjusterPanelTest` passes (all shouldShowBaseline cases green).
    - `AdjusterPanel.kt` contains `fun AdjusterPanel(` and `internal fun shouldShowBaseline(`.
    - "was X" Text is inside the SAME Row as the value (source assertion: no separate Column/second Text row below value); reset uses `Intent.Warn` (source assertion).
    - `AdjusterPreviews.kt` has ≥7 `@Preview` annotations covering the 6-combo + fs=L + landscape.
    - `assembleDebug` compiles (preview file + component compile clean).
  </acceptance_criteria>
  <done>AdjusterPanel renders the 3-zone adjuster with inline baseline; shouldShowBaseline tests green; preview matrix compiles.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI → command layer | These are pure presentation components; they emit `onDecrement`/`onIncrement` callbacks that the SCREEN (not this plan) routes through PrinterCommands clamps. No value crosses to the wire here. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-01-01 | Tampering | AdjusterPanel value display | accept | Display-only; the panel formats `value` for show, never builds gcode. Clamp authority lives at the screen call sites (plans 02-05). No input validation obligation here. |
| T-26-01-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan — components reuse existing in-repo kit (`kotlinx-collections-immutable` already in libs.versions.toml). No new dependency. |
</threat_model>

<verification>
- `IncrementPickerTest` + `AdjusterPanelTest` green (pure predicates).
- `assembleDebug` compiles with the new preview file.
- Preview matrix renders both components across 6 theme combos + fs=L + landscape (visual, confirmed via @Preview render in Android Studio at phase UAT).
</verification>

<success_criteria>
- IncrementPicker + AdjusterPanel exist as shared `designsystem/components/` files.
- formatStep + shouldShowBaseline are host-testable internal functions with passing tests.
- Inline-baseline rule honored (never stacked); Reset = Intent.Warn; active step = Intent.Accent.
- Preview matrix compiles.
</success_criteria>

<output>
Create `.planning/phases/26-adjustment-screens/26-01-SUMMARY.md` when done.
</output>

<!-- Decisions implemented: D-20 (IncrementPicker shared component), D-21 (inline baseline + amber Reset), D-23 (preview-first matrix), D-24 (registry icons only). -->
