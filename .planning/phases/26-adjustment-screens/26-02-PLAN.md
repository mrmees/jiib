---
phase: 26-adjustment-screens
plan: 02
type: execute
wave: 2
depends_on: ["26-01"]
files_modified:
  - app/src/main/java/works/mees/dinghy/ui/finetune/FineTuneScreen.kt
  - app/src/main/java/works/mees/dinghy/ui/finetune/FineTuneParams.kt
  - app/src/main/java/works/mees/dinghy/ui/shell/AppShell.kt
  - app/src/main/java/works/mees/dinghy/ui/shell/ShellNavState.kt
  - app/src/main/java/works/mees/dinghy/preview/FineTunePreviews.kt
  - app/src/test/java/works/mees/dinghy/ui/finetune/FineTuneScreenNudgeTest.kt
  - app/src/main/java/works/mees/dinghy/ui/finetune/FineTuneHubScreen.kt
  - app/src/main/java/works/mees/dinghy/ui/finetune/ExtrusionScreen.kt
  - app/src/main/java/works/mees/dinghy/ui/finetune/MotionScreen.kt
  - app/src/main/java/works/mees/dinghy/ui/finetune/FwRetractionScreen.kt
  - app/src/main/java/works/mees/dinghy/ui/finetune/FineTuneGroup.kt
  - app/src/main/java/works/mees/dinghy/ui/finetune/FineTuneTile.kt
autonomous: true
requirements: []
user_setup: []
must_haves:
  truths:
    - "Fine-Tune is one flat-list screen: Field = scrollable param list, Focus = the 3-zone AdjusterPanel"
    - "Selecting a param row loads it into the Focus adjuster; the last-adjusted param is remembered for the session and the first param is the fresh-entry fallback"
    - "Group identity (Extrusion / Motion / FW-retraction) is shown by a pool-color tint on each leading param icon, with no text group labels"
    - "FW-retraction rows hide entirely (not greyed) on printers without firmware retraction"
    - "Every nudge routes the target through the matching PrinterCommands clamp BEFORE markPending, so the armed target equals the wire value"
    - "The four old Fine-Tune screens and the sub-nav back-stack are deleted; one NavDest.FineTune route renders the single screen"
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/ui/finetune/FineTuneScreen.kt"
      provides: "Single flat-list Fine-Tune screen with AdjusterPanel Focus"
      contains: "fun FineTuneScreen"
    - path: "app/src/main/java/works/mees/dinghy/ui/finetune/FineTuneParams.kt"
      provides: "Param descriptors (tuner, icon, group color, clamp fn, step set) + nudge routing"
    - path: "app/src/test/java/works/mees/dinghy/ui/finetune/FineTuneScreenNudgeTest.kt"
      provides: "Proof every nudge clamps before markPending"
  key_links:
    - from: "FineTuneScreen.kt"
      to: "FineTuneHolder.markPending"
      via: "clamped-target nudge"
      pattern: "markPending"
    - from: "FineTuneScreen.kt"
      to: "PrinterCommands clamp functions"
      via: "clamp before markPending"
      pattern: "clamp(Speed|Flow|Velocity|Accel|Scv|PressureAdvance|SmoothTime|MinCruiseRatio)"
    - from: "AppShell.kt"
      to: "FineTuneScreen"
      via: "NavDest.FineTune composable"
      pattern: "FineTuneScreen"
---

<objective>
Collapse the four Fine-Tune screens (Hub + Extrusion + Motion + FwRetraction) into ONE flat-list screen built on the Wave-1 AdjusterPanel + IncrementPicker (D-01/D-02/D-04). The `FineTuneHolder` / `FineTuneVm` / `FineTuneShared` semantic core is UNCHANGED — this is a display-surface rebuild that must preserve the Phase-17 clamp authority and busy-lock discipline (D-22) exactly. Dissolve the in-screen sub-nav back-stack and delete the dead files.

Purpose: The archetype proof. Fine-Tune is the canonical sketch-003 screen; getting the clamp-routing + session-memory + capability-gating right here sets the template for Temperature and Outputs.
Output: `FineTuneScreen.kt`, `FineTuneParams.kt`, AppShell route surgery, the nudge-clamp test, previews, and four file deletions.

Decisions implemented: D-01 (one flat-list screen), D-02 (pool-color group identity, no labels), D-03 (part-cooling fan stays a Fine-Tune param AND remains in Outputs — same command path, intentional duplication), D-04 (session-remember last-adjusted, first-param fallback), D-05 (stepper is the default control here — AdjusterPanel, no scrubber on Fine-Tune params), D-08 (hide-not-grey FW-retraction gating), D-20 (IncrementPicker shared component), D-21 (inline "was X" baseline + amber Reset/Reset-all), D-22 (clamp authority + busy-lock load-bearing), D-23 (conformance + preview-first + tokenized-first fold in), D-24 (never auto-pick icons — STOP and ASK if a param glyph is unregistered).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-adjustment-screens/26-CONTEXT.md
@.planning/phases/26-adjustment-screens/26-PATTERNS.md
@.planning/phases/26-adjustment-screens/26-RESEARCH.md
@.claude/skills/sketch-findings-dinghy-display/references/adjustment-controls.md
@.claude/skills/sketch-findings-dinghy-display/references/foundations.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Param descriptors + clamp-routing nudge + RED nudge test</name>
  <files>app/src/main/java/works/mees/dinghy/ui/finetune/FineTuneParams.kt, app/src/test/java/works/mees/dinghy/ui/finetune/FineTuneScreenNudgeTest.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/finetune/FineTuneShared.kt (step constants: SPEED_STEP, FLOW_STEP, PA_STEP, SMOOTH_STEP, FAN_STEP_PCT, VEL_STEP, ACCEL_STEP, MIN_CRUISE_STEP_PCT, SCV_STEP, RETRACT_LEN_STEP, RETRACT_SPEED_STEP; fmtValue; clampVelocityLimitTarget; the existing nudge pattern)
    - app/src/main/java/works/mees/dinghy/ui/finetune/FineTuneVm.kt (the 13 tuner fields + valueForTuner / baselineForTuner accessors)
    - app/src/main/java/works/mees/dinghy/ui/finetune/FineTuneHolder.kt (markPending contract lines ~169-197, toleranceFor, wirePrecisionFor, roundToWirePrecision — DO NOT MODIFY)
    - app/src/main/java/works/mees/dinghy/command/PrinterCommands.kt (clampSpeedPct, clampFlowPct, clampVelocity, clampAccel, clampScv, clampPressureAdvance, clampSmoothTime, clampMinCruiseRatio — lines ~123-150; RETRACT_LEN_MIN/MAX; command args for each tuner)
    - app/src/main/java/works/mees/dinghy/theme/ThemeTokens.kt (t.pool: List<Color> — the COLORFUL pool for group hues)
    - .planning/phases/26-adjustment-screens/26-PATTERNS.md (clamp-routing nudge invariant; the flat param list with steps; FW-retraction gating)
  </read_first>
  <behavior>
    - For each adjustable tuner, nudge(tuner, rawTarget) calls the matching PrinterCommands clamp, passes the CLAMPED result to holder.markPending, and dispatches with rawTarget (the wire builder re-clamps identically).
    - FineTuneScreenNudgeTest asserts: a nudge to a value ABOVE a tuner's cap arms markPending at the CLAMPED cap value (not the raw over-cap value) — i.e. the armed pending-flip target equals the clamp ceiling, proving no permanent busy-lock wedge (P17 Check-6 regression guard).
  </behavior>
  <action>
    Create `FineTuneParams.kt` defining a `FineTuneParam` descriptor list with exactly 13 entries — ONE per `FineTuneTuner` enum constant (the enum has 13: SPEED, MAX_VELOCITY, MAX_ACCEL, MIN_CRUISE, SCV, FLOW, PRESSURE_ADVANCE, SMOOTH_TIME, PART_FAN, RETRACT_LENGTH, UNRETRACT_EXTRA_LENGTH, RETRACT_SPEED, UNRETRACT_SPEED — verified in FineTuneHolder.kt:22-26). Order the list per D-02 flattened display order — Extrusion (SPEED, FLOW, PRESSURE_ADVANCE, SMOOTH_TIME) → Motion (MAX_VELOCITY, MAX_ACCEL, MIN_CRUISE, SCV) → FW-retraction (RETRACT_LENGTH, RETRACT_SPEED, UNRETRACT_EXTRA_LENGTH, UNRETRACT_SPEED) — with the part-cooling Fan (PART_FAN) included as the 13th param in the Extrusion group (D-03: it stays a Fine-Tune param AND remains in Outputs; place it adjacent to the Extrusion rows). Each descriptor carries its `FineTuneTuner`, display name, `DinghyIcon` (registry icon — if a needed param glyph is NOT already in DinghyIcons / the sketches / img/, STOP and surface it; do not auto-pick — D-24), the increment `steps` (ImmutableList seeded from FineTuneShared step constants, e.g. Speed=[1,5,10], Flow=[1,5,10], PA=[0.001,0.005,0.01], Smooth=[0.01,0.02,0.05], etc — exact sets are Claude's discretion seeded from the constants), default active step, unit, decimals, a `group` enum (EXTRUSION / MOTION / FW_RETRACTION), and `requiresFwRetraction: Boolean`. Map each group to one of three distinct hues from `t.pool` (D-02 group colors — pick three clearly distinct Colorful-pool indices; document the indices chosen). Add `groupColorFor(group, pool: List<Color>): Color` using `pool.take(...)`-guarded indexing (never bare `pool[i]`). Provide a single `nudge(param, rawTarget, vm, holder, dispatcher)` that follows the D-22 invariant: `val clamped = clampFor(param.tuner, rawTarget); holder.markPending(param.tuner, clamped); dispatcher?.dispatch(commandFor(param.tuner), argsFor(param.tuner, rawTarget))`. `clampFor` maps each tuner to its exact PrinterCommands clamp (reuse `clampVelocityLimitTarget` from FineTuneShared where it already exists). Then create `FineTuneScreenNudgeTest.kt` (RED-first, must compile) using the existing `FineTuneHolderTest` fakes/harness as the analog: drive a nudge above cap for at least Velocity and SCV and assert the armed `pendingStateFlip` target equals the clamp ceiling. Reference only symbols this task creates plus existing holder/clamp APIs.
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --tests works.mees.dinghy.ui.finetune.FineTuneScreenNudgeTest --tests works.mees.dinghy.ui.finetune.FineTuneHolderTest --no-daemon" 2>&1 | tr -d '\r'</automated>
  </verify>
  <acceptance_criteria>
    - `FineTuneScreenNudgeTest` passes: over-cap nudge arms markPending at the clamp ceiling for Velocity and SCV.
    - `FineTuneHolderTest` still passes (clamp authority + busy-lock unchanged).
    - `FineTuneParams.kt` contains a 13-entry param list — one per FineTuneTuner enum constant, including the part-cooling Fan (PART_FAN, D-03) and `nudge(` routes through a clamp before `markPending` (source assertion: every `markPending` call site is preceded by a `clamp` call).
    - No bare `pool[i]` indexing — `take(...)`/size-guarded (source assertion).
  </acceptance_criteria>
  <done>Param descriptors exist; nudge clamp-routing is proven by the nudge test; holder tests stay green.</done>
</task>

<task type="auto">
  <name>Task 2: FineTuneScreen flat-list + AdjusterPanel Focus + previews</name>
  <files>app/src/main/java/works/mees/dinghy/ui/finetune/FineTuneScreen.kt, app/src/main/java/works/mees/dinghy/preview/FineTunePreviews.kt</files>
  <read_first>
    - .planning/phases/26-adjustment-screens/26-PATTERNS.md (FineTuneScreen full pattern — two-overload seam, ScreenScaffold focus/field, ListBlock+ListRow, FootButtonBar, FloatingEStop, gutter=null)
    - app/src/main/java/works/mees/dinghy/ui/spool/SpoolScreen.kt (the exact two-overload + FieldMode + DetailCard-Focus + FloatingEStop + ConfirmGuard analog)
    - app/src/main/java/works/mees/dinghy/designsystem/components/AdjusterPanel.kt (Wave-1 component — the Focus surface)
    - app/src/main/java/works/mees/dinghy/designsystem/components/IncrementPicker.kt (Wave-1 component)
    - app/src/main/java/works/mees/dinghy/designsystem/components/ListRow.kt, ListBlock.kt, FootButtonBar.kt, DetailCard.kt (the Phase-23 kit)
    - app/src/main/java/works/mees/dinghy/designsystem/components/FloatingEStop.kt (printing-valid e-stop)
    - app/src/main/java/works/mees/dinghy/ui/finetune/FineTuneParams.kt (Task 1 descriptors + nudge)
    - app/src/main/java/works/mees/dinghy/ui/finetune/FineTuneVm.kt (valueForTuner / baselineForTuner / groupBusy)
    - docs/ui_design/PREVIEW_AND_TOKENS.md + docs/ui_design/LAYOUT.md (the grammar + preview matrix)
    - app/src/main/java/works/mees/dinghy/theme/fsSp (font scale)
  </read_first>
  <action>
    Create `FineTuneScreen.kt` with the two-overload stateless seam (live overload consuming `FineTuneHolder` + `AppContainer` + `onBack`; stateless `FineTuneScreen(vm: FineTuneVm, isPrinting, onBack, ...)` seam for previews) exactly per the PATTERNS.md FineTuneScreen pattern. Inside `BoxWithConstraints` derive `grid = rememberUnitGrid(minOf(maxWidth, maxHeight))`. Hold `selectedTuner` + `activeStep` as session-remembered `remember { mutableStateOf(...) }` defaulting to the first param (D-04 fallback). Focus = `DetailCard` wrapping `AdjusterPanel` (D-05 stepper, D-21 inline baseline; icon/name/value/unit/baseline/decimals from the selected param + vm; onDecrement/onIncrement call `nudge(param, ±activeStep, ...)`; onReset = baseline?.let { nudge to baseline }; enabled = !vm.groupBusy; incrementPicker = IncrementPicker(param.steps, activeStep, onSelect = { activeStep = it }, grid.uDp)). Field = `ListBlock` of `ListRow` per `visibleParams` (filter FW-retraction rows by `vm.hasFwRetraction` — hide-not-grey, D-08), leading icon tinted to `groupColorFor(param.group, t.pool)` (D-02), trailing current-value readout in GeistMono, selected = param.tuner == selectedTuner, onClick sets selectedTuner + resets activeStep to the param default. Foot = `FootButtonBar` with Back (Intent.Neutral, DinghyIcons.ArrowBack) + "Reset All" (Intent.Warn — D-21). `gutter = null`. Add `FloatingEStop(visible = isPrinting, ...)` as a Focus-box sibling + a ConfirmGuard for the e-stop (Fine-Tune is printing-list-valid per P24 D-04). All text via `fsSp(...)` (D-23). Create `FineTunePreviews.kt` with the 6-combo + fs=L + landscape (800×480) matrix (D-23) driving the stateless overload with a fake `FineTuneVm` (one with FW-retraction present, one without, to prove hide-not-grey).
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:assembleDebug --no-daemon" 2>&1 | tr -d '\r'</automated>
  </verify>
  <acceptance_criteria>
    - `assembleDebug` compiles.
    - `FineTuneScreen.kt` contains two `fun FineTuneScreen(` overloads; the stateless one takes `vm: FineTuneVm`.
    - `gutter = null` present; `FootButtonBar` is the last element inside the `field` lambda (source assertion — not in the gutter slot).
    - FW-retraction rows are filtered by `hasFwRetraction` (source assertion: a `.filter { ... hasFwRetraction ... }` or equivalent gate).
    - Leading icon tint uses `groupColorFor(...)` / `t.pool` (source assertion).
    - `FineTunePreviews.kt` has ≥7 `@Preview` annotations and at least one preview with FW-retraction absent.
    - No `markPending(` call in the screen bypasses `nudge` / clamp (source assertion: screen calls `nudge(...)`, not `holder.markPending` directly).
  </acceptance_criteria>
  <done>FineTuneScreen renders the flat list + AdjusterPanel Focus; previews compile; FW-retraction hides; group colors tint icons.</done>
</task>

<task type="auto">
  <name>Task 3: AppShell route surgery + delete the four old screens</name>
  <files>app/src/main/java/works/mees/dinghy/ui/shell/AppShell.kt, app/src/main/java/works/mees/dinghy/ui/shell/ShellNavState.kt, app/src/main/java/works/mees/dinghy/ui/finetune/FineTuneHubScreen.kt, app/src/main/java/works/mees/dinghy/ui/finetune/ExtrusionScreen.kt, app/src/main/java/works/mees/dinghy/ui/finetune/MotionScreen.kt, app/src/main/java/works/mees/dinghy/ui/finetune/FwRetractionScreen.kt, app/src/main/java/works/mees/dinghy/ui/finetune/FineTuneGroup.kt, app/src/main/java/works/mees/dinghy/ui/finetune/FineTuneTile.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/shell/AppShell.kt (lines ~72-77 imports; ~402-410 holder creation; ~525-535 drawer-swipe gate; ~720-755 composable<NavDest.FineTune> with the fineTuneGroup when-branch sub-nav)
    - app/src/main/java/works/mees/dinghy/ui/shell/ShellNavState.kt (fineTuneGroup var lines ~61-66; applyEntryReset lines ~100-110)
    - app/src/main/java/works/mees/dinghy/ui/finetune/FineTuneScreen.kt (Task 2 — the live overload signature to wire)
    - .planning/phases/26-adjustment-screens/26-PATTERNS.md (Open Questions #2: NavHost route shape; the sub-nav must dissolve, Back re-anchors to NavHost)
  </read_first>
  <action>
    Rewire `composable<NavDest.FineTune>` in AppShell.kt to render the single `FineTuneScreen(holder = fineTuneHolder, container = container, onBack = { nav.popBackStack/route-back })` — remove the `when (fineTuneGroup) { null -> FineTuneHubScreen ... MOTION -> MotionScreen ... }` sub-nav block entirely. Delete the imports for `ExtrusionScreen`, `MotionScreen`, `FwRetractionScreen`, `FineTuneHubScreen`, `FineTuneGroup`. Remove the `fineTuneGroup`-based drawer-swipe-suppression gate at ~line 530 (the new screen's Field scrollability handles swipe suppression via ListBlock; preserve correct drawer-swipe behavior — Fine-Tune's Field is scrollable so the swipe-up drawer must stay suppressed when the list can scroll). In `ShellNavState.kt`, delete the `fineTuneGroup` var and the FineTune branch of `applyEntryReset` (the single screen needs no sub-route reset). Delete the six dead files: `FineTuneHubScreen.kt`, `ExtrusionScreen.kt`, `MotionScreen.kt`, `FwRetractionScreen.kt`, `FineTuneGroup.kt`, `FineTuneTile.kt`. Grep the whole app for any remaining reference to these symbols (`FineTuneGroup`, `FineTuneHubScreen`, `ExtrusionScreen`, `MotionScreen`, `FwRetractionScreen`, `FineTuneTile`) and fix/remove every one. Keep `FineTuneHolder`, `FineTuneVm`, `FineTuneShared`, `FineTuneParams` intact.
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:assembleDebug :app:testDebugUnitTest --no-daemon" 2>&1 | tr -d '\r'</automated>
  </verify>
  <acceptance_criteria>
    - `assembleDebug` compiles and the full host unit suite is green.
    - No references remain to `FineTuneGroup`, `FineTuneHubScreen`, `ExtrusionScreen`, `MotionScreen`, `FwRetractionScreen`, `FineTuneTile` anywhere under `app/src/` (grep returns nothing in main/preview/test).
    - `composable<NavDest.FineTune>` renders `FineTuneScreen(` (source assertion).
    - `fineTuneGroup` no longer exists in `ShellNavState.kt` (grep returns nothing).
  </acceptance_criteria>
  <done>Single NavDest.FineTune route renders the new screen; sub-nav back-stack and four old screens are deleted; app builds and tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI → command layer | Fine-Tune adjuster values cross to gcode via PrinterCommands clamp + FineTuneHolder.markPending. Untrusted only in the sense of out-of-range values; no external attacker (local LAN single-user). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-02-01 | Tampering | nudge() → gcode | mitigate | Every nudge routes the value through the matching `PrinterCommands.clamp*()` BEFORE `markPending` and dispatch. Values are formatted by PrinterCommands wire builders, never concatenated raw. Proven by `FineTuneScreenNudgeTest`. |
| T-26-02-02 | Denial of Service | FineTuneHolder busy-lock wedge | mitigate | P17 invariant preserved: armed target == clamped wire value, so an at-cap nudge skip-arms instead of wedging. Regression guarded by `FineTuneHolderTest` + `FineTuneScreenNudgeTest`. |
| T-26-02-03 | Tampering | Capability gating (FW-retraction rows) | mitigate | FW-retraction rows hide when `!hasFwRetraction` (D-08), so unsupported commands are never reachable from the UI. |
| T-26-02-SC | Tampering | npm/pip/cargo installs | accept | No package installs — reuses existing kit + holder. No new dependency. |
</threat_model>

<verification>
- `FineTuneScreenNudgeTest` + `FineTuneHolderTest` green (clamp authority intact).
- Full host suite green after route surgery + deletions.
- `assembleDebug` compiles.
- Grep proves zero dangling references to the six deleted symbols.
- (Phase UAT) Fine-Tune renders one flat list, params load into the Focus adjuster, FW-retraction hides on the E5/E3, group colors visible, Reset/Reset-All work.
</verification>

<success_criteria>
- One `FineTuneScreen` replaces the four old screens; sub-nav back-stack dissolved.
- Session memory + first-param fallback (D-04); group pool colors (D-02); hide-not-grey FW-ret (D-08); fan duplication (D-03).
- Clamp-routed nudge invariant proven (D-22); holder API unchanged.
</success_criteria>

<output>
Create `.planning/phases/26-adjustment-screens/26-02-SUMMARY.md` when done.
</output>
