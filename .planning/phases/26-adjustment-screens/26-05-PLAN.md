---
phase: 26-adjustment-screens
plan: 05
type: execute
wave: 5
depends_on: ["26-02"]
files_modified:
  - app/src/main/java/works/mees/dinghy/ui/outputs/OutputsScreen.kt
  - app/src/main/java/works/mees/dinghy/ui/outputs/OutputFocusControl.kt
  - app/src/main/java/works/mees/dinghy/ui/shell/AppShell.kt
  - app/src/main/java/works/mees/dinghy/ui/shell/ShellNavState.kt
  - app/src/main/java/works/mees/dinghy/preview/OutputsPreviews.kt
  - app/src/main/java/works/mees/dinghy/ui/outputs/OutputScrubberDetail.kt
  - app/src/main/java/works/mees/dinghy/ui/outputs/OutputPinDetail.kt
  - app/src/main/java/works/mees/dinghy/ui/outputs/OutputLedDetail.kt
autonomous: true
requirements: []
user_setup: []
must_haves:
  truths:
    - "Outputs is a list+detail-in-Focus screen: Field = the capability-discovered output list, Focus = the selected output's control surface in place"
    - "Each output type renders its correct control: fan/PWM/servo/heater_generic = the 0–100% (or angle/temp) scrubber on-release, output_pin = a toggle, led = brightness scrubber + capability-gated channels + ColorWheel"
    - "The scrubber follows the build-once rule (never keyed on the live value; updates fill/thumb/value in place during drag; one command on release) and every dispatch routes through the output clamp authority"
    - "The three detail-page pushes are deleted and the list↔detail local back-stack dissolves; Back from a selected output returns to the no-selection list state"
    - "Capability gating (P19 GAP-B LED channel gating, hide-not-grey) is preserved"
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/ui/outputs/OutputsScreen.kt"
      provides: "Rebuilt list+detail-in-Focus Outputs screen"
      contains: "fun OutputsScreen"
    - path: "app/src/main/java/works/mees/dinghy/ui/outputs/OutputFocusControl.kt"
      provides: "Per-output-type inline Focus control surface"
  key_links:
    - from: "OutputFocusControl.kt"
      to: "OutputsHolder.markPending"
      via: "clamped-wire-value flip"
      pattern: "markPending"
    - from: "OutputFocusControl.kt"
      to: "ScrubberPage OnSettle"
      via: "dispatch on release"
      pattern: "OnSettle"
    - from: "OutputFocusControl.kt"
      to: "PrinterCommands.outputPctToWire / clampOutputPct"
      via: "clamp before dispatch"
      pattern: "outputPctToWire|clampOutputPct"
---

<objective>
Collapse the Outputs detail pages into the Focus (D-18/D-19): Field = the capability-discovered output list, Focus = the selected output's inline control surface (fan/PWM/servo/heater scrubber, output_pin toggle, LED brightness + channels + ColorWheel). Delete the three `Output*Detail.kt` page pushes and dissolve the list↔detail local back-stack in AppShell. The `OutputsHolder` API is UNCHANGED; the P19 build-once scrubber rule (SC-3) and P17 output clamp authority must survive.

Purpose: Outputs adopts the Spoolman Detail-in-Focus shape. The scrubber regression (P19 fa97efb) and the output clamp authority (markPending == wire value) are the two load-bearing risks.
Output: rebuilt `OutputsScreen.kt`, new `OutputFocusControl.kt`, AppShell route surgery, previews, three deletions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-adjustment-screens/26-CONTEXT.md
@.planning/phases/26-adjustment-screens/26-PATTERNS.md
@.planning/phases/26-adjustment-screens/26-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: OutputFocusControl — per-type inline control surface</name>
  <files>app/src/main/java/works/mees/dinghy/ui/outputs/OutputFocusControl.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/outputs/OutputScrubberDetail.kt (the EXACT OnSettle dispatch + Off + clamp routing to migrate — lines ~119-165)
    - app/src/main/java/works/mees/dinghy/ui/outputs/OutputPinDetail.kt (toggle dispatch path)
    - app/src/main/java/works/mees/dinghy/ui/outputs/OutputLedDetail.kt (brightness scrubber + P19 GAP-B channel gating + ColorWheel composition)
    - app/src/main/java/works/mees/dinghy/ui/outputs/OutputToggleControl.kt (reusable toggle)
    - app/src/main/java/works/mees/dinghy/outputs/OutputsHolder.kt (OutputRowVm + descriptor.family/objectKey/commandName/prettyName + markPending + OutputPending — the per-output busy lock)
    - app/src/main/java/works/mees/dinghy/designsystem/ScrubberPage.kt (OnSettle mode; the build-once internal working state — lines ~156-238; NEVER key on live value)
    - app/src/main/java/works/mees/dinghy/designsystem/ColorWheel.kt (reused for LED — unchanged)
    - app/src/main/java/works/mees/dinghy/command/PrinterCommands.kt (clampOutputPct/outputPctToWire line ~170-180; clampServoAngle line ~173; clampHeaterTarget line ~255)
    - .planning/phases/26-adjustment-screens/26-PATTERNS.md (OutputFocusControl dispatch patterns; the scrubber build-once rule)
  </read_first>
  <action>
    Create `OutputFocusControl(output: OutputRowVm, holder: OutputsHolder, dispatch..., onBack: () -> Unit)` that switches on `output.descriptor.family` and renders the correct in-place control, migrating the dispatch logic VERBATIM from the three detail files: FAN / PWM_PIN / PWM_TOOL / heater_generic → `ScrubberPage` OnSettle (0–100% for fans/PWM, 0..servoMax for servo, 0..MAX_TEMP for heater_generic) — on settle, clamp via the matching PrinterCommands clamp, `holder.markPending(objectKey, outputPctToWire(clamped))` (the P17 invariant: armed == wire value), then dispatch the clamped value; onOff dispatches the off value; onBack = onBack. output_pin (digital) → the toggle `OutlinedControl`/`OutputToggleControl` (on/off intent). led → brightness `ScrubberPage` (OnSettle) + the P19 GAP-B capability-gated channel controls (preserve the exact gating that ignores channels the light lacks) + the existing `ColorWheel`. CRITICAL: host `ScrubberPage` as-is — do NOT wrap it in `key(output.currentPct)` or any block that changes during drag; let it hold its own `working` state seeded via `remember(value, range)` (the P19 build-once rule, SC-3). Do NOT re-implement the gesture. Use registry icons only; if a needed glyph is missing, STOP and ASK.
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --tests works.mees.dinghy.designsystem.*Scrubber* --tests works.mees.dinghy.ui.outputs.* --tests works.mees.dinghy.command.PrinterCommandsOutputs* --no-daemon" 2>&1 | tr -d '\r'</automated>
  </verify>
  <acceptance_criteria>
    - Existing scrubber/output tests green (`OutputScrubberSettleTest`, `ScrubberMappingTest`, `OutputLedCommandTest`, `PrinterCommandsOutputsTest`).
    - `OutputFocusControl.kt` hosts `ScrubberPage` without a `key(...)` wrapper on the live value (source assertion).
    - Every scrubber `onSettle` calls a `PrinterCommands.clamp*`/`outputPctToWire` before `markPending`+dispatch (source assertion).
    - LED channel gating preserved (source assertion: the capability check from OutputLedDetail is carried over).
  </acceptance_criteria>
  <done>OutputFocusControl renders each output type's inline control with clamp authority + build-once scrubber preserved; output tests green.</done>
</task>

<task type="auto">
  <name>Task 2: OutputsScreen list+detail-in-Focus rebuild + previews</name>
  <files>app/src/main/java/works/mees/dinghy/ui/outputs/OutputsScreen.kt, app/src/main/java/works/mees/dinghy/preview/OutputsPreviews.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/outputs/OutputsScreen.kt (FULL current — the OutputRow + glyphFor family glyphs to preserve; the existing stateless seam at line ~85)
    - .planning/phases/26-adjustment-screens/26-PATTERNS.md (OutputsScreen full pattern: two-overload seam, ScreenScaffold Detail-in-Focus, ListBlock+ListRow, selectedKey, empty-Focus prompt, FootButtonBar Back, gutter=null)
    - app/src/main/java/works/mees/dinghy/ui/spool/SpoolScreen.kt (the Detail-in-Focus / Spoolman shape analog)
    - app/src/main/java/works/mees/dinghy/ui/outputs/OutputFocusControl.kt (Task 1 — the Focus control)
    - app/src/main/java/works/mees/dinghy/designsystem/components/ListRow.kt, ListBlock.kt, FootButtonBar.kt, DetailCard.kt
    - docs/ui_design/PREVIEW_AND_TOKENS.md
  </read_first>
  <action>
    Rebuild `OutputsScreen.kt` with the two-overload stateless seam (live overload consuming `OutputsHolder` + dispatch + `onBack`; stateless seam taking `rows: List<OutputRowVm>` + `selectedKey` for previews) per PATTERNS.md. Inside `BoxWithConstraints` + `rememberUnitGrid`. Hold `selectedKey: String?` as `remember { mutableStateOf(null) }`. Focus = if a row is selected, `DetailCard` wrapping `OutputFocusControl(selectedRow, ...)`; else an empty-Focus "Select an output" prompt. Field = `ListBlock` of `ListRow` per `rows` (key = descriptor.objectKey), leading the OWNER-LOCKED family glyph (preserve `glyphFor(family)` — do NOT change the icons), trailing `displayValue` when non-null, selected = objectKey == selectedKey, onClick sets selectedKey. Foot = `FootButtonBar` Back (Intent.Neutral, ArrowBack). `gutter = null`. All text `fsSp(...)`. Create/replace `OutputsPreviews.kt` with the 6-combo + fs=L + landscape matrix driving the stateless overload with fake rows (multiple families) + one with a selected output showing the Focus control, one with no selection.
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:assembleDebug --no-daemon" 2>&1 | tr -d '\r'</automated>
  </verify>
  <acceptance_criteria>
    - `assembleDebug` compiles.
    - `OutputsScreen.kt` has the two-overload seam; Focus shows `OutputFocusControl` for the selected row and an empty prompt otherwise (source assertion).
    - Family glyphs unchanged (source assertion: `glyphFor` preserved, no new icon invented).
    - `gutter = null`; FootButtonBar inside `field` (source assertion).
    - `OutputsPreviews.kt` has ≥7 `@Preview` annotations + a selected-output preview.
  </acceptance_criteria>
  <done>Outputs renders the list+detail-in-Focus shape; previews compile; family glyphs preserved.</done>
</task>

<task type="auto">
  <name>Task 3: AppShell route surgery + delete the three detail pages</name>
  <files>app/src/main/java/works/mees/dinghy/ui/shell/AppShell.kt, app/src/main/java/works/mees/dinghy/ui/shell/ShellNavState.kt, app/src/main/java/works/mees/dinghy/ui/outputs/OutputScrubberDetail.kt, app/src/main/java/works/mees/dinghy/ui/outputs/OutputPinDetail.kt, app/src/main/java/works/mees/dinghy/ui/outputs/OutputLedDetail.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/shell/AppShell.kt (imports lines ~101-105; outputsHolder + the selected-objectKey local back-stack lines ~335-340; the Back-intercept at ~522; the composable<NavDest.Outputs> block + its list↔detail when-branch)
    - app/src/main/java/works/mees/dinghy/ui/shell/ShellNavState.kt (any Outputs selected-key sub-state + applyEntryReset Outputs branch)
    - app/src/main/java/works/mees/dinghy/ui/outputs/OutputsScreen.kt (Task 2 — the new live overload signature; selection is now INTERNAL to the screen)
    - .planning/phases/26-adjustment-screens/26-PATTERNS.md (the list↔detail local back-stack dissolves; Back re-anchors)
  </read_first>
  <action>
    Rewire `composable<NavDest.Outputs>` in AppShell.kt to render the single `OutputsScreen(holder = outputsHolder, ...)` — selection is now internal to the screen (its own `selectedKey`). Remove the list↔detail when-branch that previously pushed `OutputScrubberDetail`/`OutputPinDetail`/`OutputLedDetail`. Delete the imports for those three. Remove the Outputs selected-objectKey local back-stack state in AppShell/ShellNavState and the Back-intercept that returned an open detail to the list (the screen's internal selectedKey + a Back foot button handle this now; preserve sane NavHost Back: when no output is selected, Back exits the screen). If the drawer-swipe-suppression gate references the Outputs detail sub-state, update it to the scrollable-Field rule (Outputs Field scrolls → suppress swipe drawer). Delete the three files `OutputScrubberDetail.kt`, `OutputPinDetail.kt`, `OutputLedDetail.kt`. Grep the whole app for any remaining reference to these three symbols and fix every one. Keep `OutputsHolder`, `OutputToggleControl`, `ColorWheel` intact.
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:assembleDebug :app:testDebugUnitTest --no-daemon" 2>&1 | tr -d '\r'</automated>
  </verify>
  <acceptance_criteria>
    - `assembleDebug` + full host suite green.
    - No references remain to `OutputScrubberDetail`, `OutputPinDetail`, `OutputLedDetail` anywhere under `app/src/` (grep returns nothing).
    - `composable<NavDest.Outputs>` renders `OutputsScreen(` with no list↔detail sub-nav branch (source assertion).
    - Back behavior: selecting an output then Back returns to the list (in-screen), Back with no selection exits the route (source/behavior assertion).
  </acceptance_criteria>
  <done>Single NavDest.Outputs route renders the collapsed screen; three detail pages deleted; build + tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI → command layer | Output set-values (fan %, PWM, servo angle, heater temp, LED brightness/channels) cross to gcode via the output clamp authority + OutputsHolder.markPending. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-05-01 | Tampering | Scrubber settle → gcode | mitigate | Every onSettle clamps via `PrinterCommands.clampOutputPct`/`outputPctToWire`/`clampServoAngle`/`clampHeaterTarget` before markPending+dispatch; the wire builder re-clamps. Proven by `OutputScrubberSettleTest`/`PrinterCommandsOutputsTest`. |
| T-26-05-02 | Denial of Service | Build-once scrubber regression (P19) | mitigate | `ScrubberPage` hosted as-is, never keyed on the live value; the internal `working` state + on-release settle prevent the fa97efb fill-from-middle/value-not-sticking failure. |
| T-26-05-03 | Tampering | LED channel command on unsupported channel | mitigate | P19 GAP-B capability gating preserved — channels the light lacks are hidden/ignored (hide-not-grey). |
| T-26-05-SC | Tampering | npm/pip/cargo installs | accept | No package installs — reuses ScrubberPage, ColorWheel, OutputsHolder, PrinterCommands. |
</threat_model>

<verification>
- Output + scrubber + clamp tests green; full host suite green; `assembleDebug` compiles.
- Grep proves zero dangling references to the three deleted detail files.
- (Phase UAT) On flox: each output type's control works in-Focus, scrubber drags smoothly + sticks on release, LED channels gated correctly, Back returns to list, both orientations.
</verification>

<success_criteria>
- List+detail-in-Focus collapse (D-18); LED minimal-rework Focus (D-19); build-once scrubber (SC-3); output clamp authority (D-22).
- Three detail pages deleted; local back-stack dissolved; OutputsHolder API unchanged.
</success_criteria>

<output>
Create `.planning/phases/26-adjustment-screens/26-05-SUMMARY.md` when done.
</output>

<!-- Decisions implemented: D-05 (scrubber reserved for 0-100% outputs), D-06 (detail pushes retire — in-place Focus), D-09 (scrubber dispatches on release), D-18 (Outputs detail-in-Focus collapse), D-19 (LED minimal-rework Focus), D-22 (output clamp authority), D-23 (preview matrix), D-24 (registry icons only). -->
