---
phase: 26-adjustment-screens
plan: 04
type: execute
wave: 4
depends_on: ["26-03"]
files_modified:
  - app/src/main/java/works/mees/dinghy/ui/temperature/TemperatureScreen.kt
  - app/src/main/java/works/mees/dinghy/preview/TemperaturePreviews.kt
autonomous: true
requirements: []
user_setup: []
must_haves:
  truths:
    - "The Temperature Focus morphs between the live graph (default) and the selected sensor's adjuster, returning to the graph on done/back, without re-creating the GraphViewHost AndroidView"
    - "Selecting any sensor row shows at minimum a show/hide trace toggle + an inline row of 8 Colorful-pool color swatches that set the trace draw color; heaters additionally get the target-temp stepper adjuster"
    - "The chosen trace color tints the sensor's row icon (same-hue invariant) and persists across re-entry"
    - "Presets open as a Field-takeover (the full-screen scrim retires on this screen); Cooldown stays an amber foot button (TURN_OFF_HEATERS); each heater adjuster carries a per-heater Off (target 0)"
    - "Hidden traces are filtered out of the series before the graph draws them; the graph never receives per-trace color overrides"
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/ui/temperature/TemperatureScreen.kt"
      provides: "Rebuilt Temperature screen: morph Focus + typed rows + graph controls + Field-takeover presets"
      contains: "fun TemperatureScreen"
  key_links:
    - from: "TemperatureScreen.kt"
      to: "TemperatureHolder.traceColors / traceVisibility / setTraceColor / setTraceVisibility"
      via: "graph controls"
      pattern: "setTraceColor|setTraceVisibility"
    - from: "TemperatureScreen.kt"
      to: "AppContainer.writeScope"
      via: "persist on color/visibility change"
      pattern: "writeScope"
    - from: "TemperatureScreen.kt"
      to: "GraphViewHost"
      via: "visibility-filtered series (graph factory runs once)"
      pattern: "GraphViewHost"
---

<objective>
Rebuild `TemperatureScreen` onto the redesigned grammar: a Focus that MORPHS between the live graph and the selected sensor's adjuster (D-10), a typed sensor row model (D-11), per-sensor graph controls — show/hide toggle + 8-swatch Colorful-pool color row, heaters adding the target-temp AdjusterPanel (D-14), Field-takeover presets (D-12), the amber Cooldown foot button + per-heater Off (D-13). Consumes the plan-03 holder extensions. Clamp authority for heater targets routes through `PrinterCommands.clampHeaterTarget`.

Purpose: Temperature is a printing-list destination with the richest morph behavior. The graph must not thrash (Phase-22 equality guards), the color persistence must use writeScope, and the same-hue invariant (graph trace = row icon = readout) now flows from the user's chosen color.
Output: rebuilt `TemperatureScreen.kt` + preview matrix.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-adjustment-screens/26-CONTEXT.md
@.planning/phases/26-adjustment-screens/26-PATTERNS.md
@.planning/phases/26-adjustment-screens/26-RESEARCH.md
@docs/ui_design/LAYOUT.md
@docs/ui_design/THEMING.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: TemperatureScreen morph Focus + typed rows + graph controls</name>
  <files>app/src/main/java/works/mees/dinghy/ui/temperature/TemperatureScreen.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/temperature/TemperatureScreen.kt (FULL current screen — the PresetSelector at lines ~226/405-439; seriesColor at line ~461; Cooldown foot at ~195; the existing graph + legend wiring)
    - .planning/phases/26-adjustment-screens/26-PATTERNS.md (TemperatureScreen full pattern: two-overload seam, graph↔adjuster morph if/else, TempFieldMode, 8-swatch row, per-heater Off, the equality-guard invariant)
    - app/src/main/java/works/mees/dinghy/ui/temperature/TemperatureHolder.kt (plan-03 API: series, setpoints, legend, traceColors, traceVisibility, setTraceColor, setTraceVisibility, SensorReadout.isAdjustable)
    - app/src/main/java/works/mees/dinghy/render/GraphViewHost.kt (the AndroidView factory-runs-once contract; PRESERVE the Phase-22 equality guards)
    - app/src/main/java/works/mees/dinghy/ui/spool/SpoolScreen.kt (FieldMode + ScreenScaffold + FloatingEStop + ConfirmGuard analog)
    - app/src/main/java/works/mees/dinghy/designsystem/components/AdjusterPanel.kt + IncrementPicker.kt (Wave-1 components for the heater stepper)
    - app/src/main/java/works/mees/dinghy/command/PrinterCommands.kt (clampHeaterTarget line ~255; MATERIAL_PRESETS line ~244; CommandRegistry.cooldown / setHeater)
    - app/src/main/java/works/mees/dinghy/theme/ThemeTokens.kt (t.pool — take(8) for swatches; seriesColor for default trace color)
    - app/src/main/java/works/mees/dinghy/di/AppContainer.kt (writeScope-backed setTraceColor/setTraceVisibility path from plan 03)
  </read_first>
  <action>
    Rebuild `TemperatureScreen.kt` with the two-overload stateless seam (live overload consuming `AppContainer` + `TemperatureHolder` + `onBack`; stateless seam taking series/setpoints/legend/traceColors/traceVisibility/isPrinting for previews) per the PATTERNS.md TemperatureScreen pattern. Inside `BoxWithConstraints` + `rememberUnitGrid`. Focus MORPH: a simple `if (selectedSensor == null) { GraphViewHost(...) } else { TemperatureAdjusterFocus(...) }` — NOT AnimatedContent that removes/re-adds the graph node (that thrashes the AndroidView factory and resets trace data; D-10 morph is Claude's discretion but MUST keep the factory single-run). Pass the graph a `visibleSeries` filtered by `traceVisibility` (remember(series, traceVisibility) — filter the positional series against the `drawn`/legend names); the graph never learns about visibility or per-trace color. `TemperatureAdjusterFocus` (private composable) shows, for any selected row: (1) a show/hide trace toggle (OutlinedControl Intent on/neutral) calling `setTraceVisibility`, (2) an inline `Row` of `t.pool.take(8)` circular swatches (THEME-01 data carve-out; selected swatch = thicker border) calling `setTraceColor`; both mutations also persist via the container's writeScope-backed mutator (NOT rememberCoroutineScope). For adjustable heaters add the `AdjusterPanel` with `clampHeaterTarget`-routed nudge for target temp + a per-heater Off (Intent.Warn, target=0 — P19 GAP-A precedent). Field: `TempFieldMode` sealed class (SensorList | PresetPicker). SensorList = `ListBlock` of `ListRow` per legend (leading icon tinted to `traceColors[name] ?: seriesColor(index)` — same-hue invariant D-14; trailing current/target readout GeistMono), foot = Back + "Presets" (Field-takeover, D-12) + "Cooldown" (Intent.Warn, dispatch CommandRegistry.cooldown). PresetPicker = `ListBlock` of `MATERIAL_PRESETS` rows applying the preset + returning to SensorList; foot = Back. `gutter = null`. `FloatingEStop(visible = isPrinting)` as Focus-box sibling + ConfirmGuard (Temperature is printing-list-valid). All text via `fsSp(...)`. Icons: use registry icons only; if a needed glyph (e.g. show/hide eye) is NOT already in DinghyIcons / sketches / img/, STOP and surface it — do not auto-pick.
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:assembleDebug :app:testDebugUnitTest --no-daemon" 2>&1 | tr -d '\r'</automated>
  </verify>
  <acceptance_criteria>
    - `assembleDebug` + full host suite green.
    - `TemperatureScreen.kt` Focus uses a plain `if (selectedSensor == null)` branch for the graph (source assertion — no `AnimatedContent` wrapping GraphViewHost that removes it on morph).
    - Graph receives a visibility-filtered series; no per-trace color param passed to `GraphViewHost` (source assertion).
    - 8-swatch row uses `t.pool.take(8)` (source assertion — guarded, no bare `t.pool[i]`).
    - Color/visibility mutations route through the writeScope-backed mutator, not `rememberCoroutineScope` (source assertion).
    - Row icon tint reads `traceColors[name] ?: seriesColor(...)` (same-hue invariant, source assertion).
    - Heater target nudge routes through `clampHeaterTarget` before dispatch (source assertion).
    - `gutter = null`; FootButtonBar inside `field` (source assertion).
  </acceptance_criteria>
  <done>Temperature Focus morphs graph↔adjuster, graph controls + 8-swatch color row work, presets are a Field-takeover, Cooldown + per-heater Off present, clamp + writeScope rules honored.</done>
</task>

<task type="auto">
  <name>Task 2: Temperature preview matrix</name>
  <files>app/src/main/java/works/mees/dinghy/preview/TemperaturePreviews.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/preview/TemperaturePreviews.kt (existing previews if present — extend/replace)
    - app/src/main/java/works/mees/dinghy/ui/temperature/TemperatureScreen.kt (Task 1 stateless overload signature)
    - docs/ui_design/PREVIEW_AND_TOKENS.md (the 6-combo + fs=L + landscape matrix)
    - app/src/main/java/works/mees/dinghy/preview (existing fake-state fixtures for sensors/series)
  </read_first>
  <action>
    Create/replace `TemperaturePreviews.kt` with the 6-combo (Dark/Light/Custom × S/M) + fs=L + landscape (800×480) `@Preview` matrix driving the stateless `TemperatureScreen` overload with fake series/setpoints/legend + a chosen-trace-color and a hidden-trace fixture, AND at least one preview with a sensor selected (adjuster + swatch row visible) and one default (graph). No live Moonraker / holder. Reuse existing preview fake-state fixtures.
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:assembleDebug --no-daemon" 2>&1 | tr -d '\r'</automated>
  </verify>
  <acceptance_criteria>
    - `assembleDebug` compiles with the preview file.
    - `TemperaturePreviews.kt` has ≥7 `@Preview` annotations (6-combo + fs=L + landscape) plus a sensor-selected preview.
  </acceptance_criteria>
  <done>Preview matrix compiles covering graph + adjuster states across the theme/fs/orientation matrix.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI → command layer | Heater target temps cross to gcode via clampHeaterTarget + setHeater. Trace color/visibility are visual-only persistence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-04-01 | Tampering | Heater target → gcode | mitigate | Heater target routes through `PrinterCommands.clampHeaterTarget` before dispatch; the wire builder re-clamps. No raw value reaches the wire. |
| T-26-04-02 | Tampering | 8-swatch trace color | accept | Color is one of a fixed 8-color Colorful pool (THEME-01 data carve-out), not arbitrary user input. |
| T-26-04-03 | Denial of Service | GraphViewHost factory thrash on morph | mitigate | Simple `if/else` keeps the AndroidView factory single-run; Phase-22 equality guards intact; visibility filter happens before the graph, not inside it. |
| T-26-04-04 | Denial of Service | Trace color persistence drop (P14 class) | mitigate | Mutations persist via the writeScope-backed mutator, never a composition scope. |
| T-26-04-SC | Tampering | npm/pip/cargo installs | accept | No package installs — reuses existing kit + holder + graph. |
</threat_model>

<verification>
- `assembleDebug` + full host suite green.
- Source assertions: if/else morph, visibility-filtered series, take(8) swatches, writeScope persistence, clampHeaterTarget, same-hue tint, gutter=null.
- (Phase UAT) On flox: morph works without graph flicker, color swatches recolor row icon + persist across re-entry, presets takeover, Cooldown + per-heater Off fire correctly, both orientations.
</verification>

<success_criteria>
- Morph Focus (D-10), typed rows (D-11), Field-takeover presets (D-12), Cooldown + per-heater Off (D-13), graph controls + 8-swatch color (D-14) all delivered.
- Graph equality guards untouched; clamp + writeScope rules honored.
</success_criteria>

<output>
Create `.planning/phases/26-adjustment-screens/26-04-SUMMARY.md` when done.
</output>

<!-- Decisions implemented: D-06 (single-setting pushes retire — presets become an in-place Field-takeover), D-09 (no scrubber here; stepper dispatch), D-10 (graph<->adjuster morph), D-11 (typed rows), D-12 (Field-takeover presets), D-13 (Cooldown + per-heater Off), D-14 (graph controls + 8-swatch color), D-22 (clampHeaterTarget), D-23 (preview matrix), D-24 (registry icons only). -->
