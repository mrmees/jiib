---
phase: 26-adjustment-screens
plan: 04
type: execute
wave: 4
depends_on: ["26-03"]
files_modified:
  - app/src/main/java/works/mees/dinghy/ui/temperature/TemperatureScreen.kt
  - app/src/main/java/works/mees/dinghy/render/GraphView.kt
  - app/src/main/java/works/mees/dinghy/render/GraphViewHost.kt
  - app/src/main/java/works/mees/dinghy/preview/TemperaturePreviews.kt
autonomous: true
requirements: []
user_setup: []
must_haves:
  truths:
    - "The Temperature Focus morphs between the live graph (default) and the selected sensor's adjuster, returning to the graph on done/back, without re-creating the GraphViewHost AndroidView"
    - "Selecting any sensor row shows at minimum a show/hide trace toggle + an inline row of EXACTLY 8 Colorful-pool color swatches (sourced from a dedicated Colorful palette generation, never the active palette pool) that set the trace draw color; heaters additionally get the target-temp stepper adjuster"
    - "The chosen trace color tints the sensor's row icon, the Focus panel, AND the GRAPH TRACE ITSELF (D-14 same-hue invariant: graph trace = row icon = readout), and persists across re-entry"
    - "Presets open as a Field-takeover (the full-screen scrim retires on this screen); Cooldown stays an amber foot button (TURN_OFF_HEATERS); each heater adjuster carries a per-heater Off (target 0)"
    - "Hidden traces are filtered out of ONE aligned visible-trace model (series + setpoints + names + colors filtered together) before the graph draws them; per-trace color overrides flow into the graph via an equality-guarded setter that preserves the Phase-22 factory-runs-once guard"
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/ui/temperature/TemperatureScreen.kt"
      provides: "Rebuilt Temperature screen: morph Focus + typed rows + graph controls + Field-takeover presets"
      contains: "fun TemperatureScreen"
    - path: "app/src/main/java/works/mees/dinghy/render/GraphView.kt"
      provides: "Equality-guarded setTraceColorOverrides — per-trace chosen-color draw (D-14)"
      contains: "setTraceColorOverrides"
    - path: "app/src/main/java/works/mees/dinghy/render/GraphViewHost.kt"
      provides: "traceColors param threaded into the GraphView override setter"
      contains: "traceColors"
  key_links:
    - from: "TemperatureScreen.kt"
      to: "TemperatureHolder.traceColors / traceVisibility / setTraceColor / setTraceVisibility"
      via: "graph controls"
      pattern: "setTraceColor|setTraceVisibility"
    - from: "TemperatureScreen.kt"
      to: "AppContainer.writeScope"
      via: "persist on color/visibility change"
      pattern: "writeScope"
    - from: "TemperatureScreen.kt"
      to: "GraphViewHost traceColors"
      via: "aligned visible-trace color overrides (graph factory runs once)"
      pattern: "GraphViewHost"
    - from: "GraphViewHost.kt"
      to: "GraphView.setTraceColorOverrides"
      via: "equality-guarded per-trace color push"
      pattern: "setTraceColorOverrides"
---

<objective>
Rebuild `TemperatureScreen` onto the redesigned grammar: a Focus that MORPHS between the live graph and the selected sensor's adjuster (D-10), a typed sensor row model (D-11), per-sensor graph controls — show/hide toggle + an 8-swatch Colorful-pool color row, heaters adding the target-temp AdjusterPanel (D-14), Field-takeover presets (D-12), the amber Cooldown foot button + per-heater Off (D-13). Per D-14 the GRAPH draws each trace in its chosen color, so this plan also adds an equality-guarded `setTraceColorOverrides` to `GraphView` + a `traceColors` param to `GraphViewHost`. Consumes the plan-03 holder extensions. Clamp authority for heater targets routes through `PrinterCommands.clampHeaterTarget`.

Purpose: Temperature is a printing-list destination with the richest morph behavior. The graph must not thrash (Phase-22 equality guards extended, not broken), the color persistence must use writeScope, and the same-hue invariant (graph trace = row icon = readout) now flows from the user's chosen color through the graph, the row icon, and the Focus panel together.
Output: rebuilt `TemperatureScreen.kt` + GraphView/GraphViewHost per-trace color override + preview matrix.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-adjustment-screens/26-CONTEXT.md
@.planning/phases/26-adjustment-screens/26-PATTERNS.md
@.planning/phases/26-adjustment-screens/26-RESEARCH.md
@docs/ui_design/LAYOUT.md
@docs/ui_design/THEMING.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: GraphView/GraphViewHost per-trace color override (D-14) + TemperatureScreen morph Focus + typed rows + graph controls</name>
  <files>app/src/main/java/works/mees/dinghy/render/GraphView.kt, app/src/main/java/works/mees/dinghy/render/GraphViewHost.kt, app/src/main/java/works/mees/dinghy/ui/temperature/TemperatureScreen.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/render/GraphView.kt (FULL — applyTokens line ~207-219 sets linePaints[i].color = seriesColor(i); the yRange equality-guard pattern lines ~143-149; setData/setSetpoints lines ~225-281; the raw-index setpoint draw at ~344-355 — the alignment that finding 4 protects)
    - app/src/main/java/works/mees/dinghy/render/GraphViewHost.kt (FULL — the multi-trace overload lines ~73-101; the factory-runs-once + equality-guarded update contract)
    - app/src/main/java/works/mees/dinghy/ui/temperature/TemperatureScreen.kt (FULL current screen — the PresetSelector at lines ~226/405-439; seriesColor at line ~461; Cooldown foot at ~195; the existing graph + legend wiring)
    - .planning/phases/26-adjustment-screens/26-PATTERNS.md (TemperatureScreen full pattern: two-overload seam, graph↔adjuster morph if/else, TempFieldMode, the 8-swatch Colorful-pool row, per-heater Off, the CORRECTED equality-guard invariant + visible-trace model + GraphViewHost override section)
    - app/src/main/java/works/mees/dinghy/ui/temperature/TemperatureHolder.kt (plan-03 API: series, setpoints, legend, traceColors, traceVisibility, setTraceColor, setTraceVisibility, SensorReadout.isAdjustable)
    - app/src/main/java/works/mees/dinghy/theme/Palette.kt (Palette.generate(seedHex, dark, maxItems, simple, highContrast) → .pool: List<String>; line ~332)
    - app/src/main/java/works/mees/dinghy/theme/ThemeResolver.kt (how simple/highContrast flags drive Palette.generate, lines ~184-202; MODE_COLORFUL)
    - app/src/main/java/works/mees/dinghy/theme/ThemePrefs.kt (the active seedHex + dark in the theme tuple; DEFAULT_MAX_ITEMS=4 at line 185 — proves the active pool is too small for 8 swatches)
    - app/src/main/java/works/mees/dinghy/theme/TokenBridge.kt (how generated String hexes are baked to Color)
    - app/src/main/java/works/mees/dinghy/ui/spool/SpoolScreen.kt (FieldMode + ScreenScaffold + FloatingEStop + ConfirmGuard analog)
    - app/src/main/java/works/mees/dinghy/designsystem/components/AdjusterPanel.kt + IncrementPicker.kt (Wave-1 components for the heater stepper)
    - app/src/main/java/works/mees/dinghy/command/PrinterCommands.kt (clampHeaterTarget line ~255; MATERIAL_PRESETS line ~244; CommandRegistry.cooldown / setHeater)
    - app/src/main/java/works/mees/dinghy/di/AppContainer.kt (writeScope-backed setTraceColor/setTraceVisibility path from plan 03; the active seedHex/dark accessor)
  </read_first>
  <action>
    PART A — GraphView per-trace color override (D-14: the graph draws the CHOSEN color). Add to `GraphView` an equality-guarded `fun setTraceColorOverrides(overrides: List<Int?>)` (ARGB Int per trace, index-aligned with the series passed; `null` = `seriesColor(i)`): store `lastOverrides`, return early if structurally equal, else recompute each `linePaints[i].color` = `overrides.getOrNull(i) ?: t.seriesColor(i).toArgb()` (keep the last applied tokens for the fallback), then `invalidate()`. ALSO update `applyTokens` so that when tokens change it re-applies the current overrides on top of the new `seriesColor(i)` defaults (override-wins). The setpoint dashes already read `linePaints[t].color` (GraphView.kt:350) so they inherit the override for free. Then add a `traceColors: List<Int?> = emptyList()` param to the MULTI-TRACE `GraphViewHost` overload and call `view.setTraceColorOverrides(traceColors)` inside the existing `update` lambda (keep the single-snapshot sparkline overload UNCHANGED). PRESERVE the factory-runs-once + per-push equality-guard discipline (Phase-22 D-12).

    PART B — the Colorful 8-swatch pool helper (D-14 / finding 5): the active `t.pool` is NOT usable (DEFAULT_MAX_ITEMS=4 at ThemePrefs.kt:185; Simple/HighContrast collapse the pool). Generate a DEDICATED Colorful pool of EXACTLY 8: `Palette.generate(seedHex = activeSeed, dark = activeDark, maxItems = 8, simple = false, highContrast = false).pool.take(8)` baked to `Color` (use the same bake/parse path TokenBridge uses for hex→Color). Source `activeSeed` + `activeDark` from the active theme tuple (via AppContainer / ThemePrefs — read the existing accessor). `remember(activeSeed, activeDark)` it. Assert the row renders exactly 8 swatches.

    PART C — rebuild `TemperatureScreen.kt` with the two-overload stateless seam (live overload consuming `AppContainer` + `TemperatureHolder` + `onBack`; stateless seam taking series/setpoints/legend/traceColors/traceVisibility/colorfulSwatches/isPrinting for previews) per the PATTERNS.md TemperatureScreen pattern. Inside `BoxWithConstraints` + `rememberUnitGrid`. Build ONE aligned visible-trace model with `remember(series, setpoints, legend, traceVisibility, traceColors)`: compute the kept indices (`legend.indices.filter { traceVisibility[legend[it].name] ?: true }`), then map series, setpoints, names, AND colors (`traceColors[legend[it].name]?.toArgb()`) through those SAME indices so all lists stay aligned (finding 4 — do NOT filter only `series`). Pass `visible.series` + `visible.setpoints` + `visible.colors` to `GraphViewHost(traceColors = visible.colors, ...)`. Focus MORPH: a simple `if (selectedSensor == null) { GraphViewHost(...) } else { TemperatureAdjusterFocus(...) }` — NOT AnimatedContent that removes/re-adds the graph node (that thrashes the AndroidView factory and resets trace data; D-10 morph is Claude's discretion but MUST keep the factory single-run). `TemperatureAdjusterFocus` (private composable) shows, for any selected row: (1) a show/hide trace toggle (OutlinedControl Intent on/neutral) calling `setTraceVisibility`, (2) the inline `Row` of the 8 PART-B Colorful swatches (THEME-01 data carve-out; selected swatch = thicker border) calling `setTraceColor`; both mutations also persist via the container's writeScope-backed mutator (NOT rememberCoroutineScope). For adjustable heaters add the `AdjusterPanel` with `clampHeaterTarget`-routed nudge for target temp + a per-heater Off (Intent.Warn, target=0 — P19 GAP-A precedent). Field: `TempFieldMode` sealed class (SensorList | PresetPicker). SensorList = `ListBlock` of `ListRow` per legend (leading icon tinted to `traceColors[name] ?: seriesColor(index)` — same-hue invariant D-14; trailing current/target readout GeistMono), foot = Back + "Presets" (Field-takeover, D-12) + "Cooldown" (Intent.Warn, dispatch CommandRegistry.cooldown). PresetPicker = `ListBlock` of `MATERIAL_PRESETS` rows applying the preset + returning to SensorList; foot = Back. `gutter = null`. `FloatingEStop(visible = isPrinting)` as Focus-box sibling + ConfirmGuard (Temperature is printing-list-valid). All text via `fsSp(...)`. Icons: use registry icons only; if a needed glyph (e.g. show/hide eye) is NOT already in DinghyIcons / sketches / img/, STOP and surface it — do not auto-pick.
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:assembleDebug :app:testDebugUnitTest --no-daemon" 2>&1 | tr -d '\r'</automated>
  </verify>
  <acceptance_criteria>
    - `assembleDebug` + full host suite green.
    - `GraphView.kt` has an equality-guarded `setTraceColorOverrides(List<Int?>)` (source assertion: a `lastOverrides` compare + early return + override-wins recompute); `applyTokens` re-applies overrides over the seriesColor defaults (source assertion).
    - `GraphViewHost` multi-trace overload has a `traceColors: List<Int?>` param wired into the `update` lambda; the sparkline overload is unchanged (source assertion).
    - `TemperatureScreen.kt` builds ONE aligned visible-trace model filtering series + setpoints + names + colors together (source assertion — NOT a bare `series.filterIndexed` that leaves setpoints/colors unfiltered).
    - The 8-swatch row is sourced from a dedicated `Palette.generate(..., maxItems = 8, simple = false, highContrast = false)` Colorful pool, NOT `t.pool.take(8)` (source assertion); the row renders exactly 8 swatches.
    - Focus uses a plain `if (selectedSensor == null)` branch for the graph (source assertion — no `AnimatedContent` wrapping GraphViewHost that removes it on morph).
    - Color/visibility mutations route through the writeScope-backed mutator, not `rememberCoroutineScope` (source assertion).
    - Row icon tint reads `traceColors[name] ?: seriesColor(...)` (same-hue invariant, source assertion).
    - Heater target nudge routes through `clampHeaterTarget` before dispatch (source assertion).
    - `gutter = null`; FootButtonBar inside `field` (source assertion).
  </acceptance_criteria>
  <done>The graph draws each trace in its chosen color via an equality-guarded override; the Temperature Focus morphs graph↔adjuster; graph controls + 8-swatch Colorful-pool row work; presets are a Field-takeover; Cooldown + per-heater Off present; clamp + writeScope rules honored; the visible-trace model keeps series/setpoints/colors aligned.</done>
</task>

<task type="auto">
  <name>Task 2: Temperature preview matrix</name>
  <files>app/src/main/java/works/mees/dinghy/preview/TemperaturePreviews.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/preview/TemperaturePreviews.kt (existing previews if present — extend/replace)
    - app/src/main/java/works/mees/dinghy/ui/temperature/TemperatureScreen.kt (Task 1 stateless overload signature — note it now takes colorfulSwatches + traceColors)
    - docs/ui_design/PREVIEW_AND_TOKENS.md (the 6-combo + fs=L + landscape matrix)
    - app/src/main/java/works/mees/dinghy/preview (existing fake-state fixtures for sensors/series)
  </read_first>
  <action>
    Create/replace `TemperaturePreviews.kt` with the 6-combo (Dark/Light/Custom × S/M) + fs=L + landscape (800×480) `@Preview` matrix driving the stateless `TemperatureScreen` overload with fake series/setpoints/legend + a chosen-trace-color and a hidden-trace fixture + a fake 8-color Colorful swatch list, AND at least one preview with a sensor selected (adjuster + swatch row visible) and one default (graph). No live Moonraker / holder. Reuse existing preview fake-state fixtures.
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:assembleDebug --no-daemon" 2>&1 | tr -d '\r'</automated>
  </verify>
  <acceptance_criteria>
    - `assembleDebug` compiles with the preview file.
    - `TemperaturePreviews.kt` has ≥7 `@Preview` annotations (6-combo + fs=L + landscape) plus a sensor-selected preview.
  </acceptance_criteria>
  <done>Preview matrix compiles covering graph + adjuster states across the theme/fs/orientation matrix.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI → command layer | Heater target temps cross to gcode via clampHeaterTarget + setHeater. Trace color/visibility are visual-only persistence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-04-01 | Tampering | Heater target → gcode | mitigate | Heater target routes through `PrinterCommands.clampHeaterTarget` before dispatch; the wire builder re-clamps. No raw value reaches the wire. |
| T-26-04-02 | Tampering | 8-swatch trace color | accept | Color is one of a fixed 8-color Colorful pool (THEME-01 data carve-out), not arbitrary user input. |
| T-26-04-03 | Denial of Service | GraphViewHost factory thrash on morph / per-trace override churn | mitigate | Simple `if/else` keeps the AndroidView factory single-run; `setTraceColorOverrides` is equality-guarded; the override list is `remember`-stabilized so the update lambda only sees a new reference on real change; Phase-22 guards intact. |
| T-26-04-04 | Denial of Service | Trace color persistence drop (P14 class) | mitigate | Mutations persist via the writeScope-backed mutator, never a composition scope. |
| T-26-04-SC | Tampering | npm/pip/cargo installs | accept | No package installs — reuses existing kit + holder + graph + Palette generator. |
</threat_model>

<verification>
- `assembleDebug` + full host suite green.
- Source assertions: equality-guarded GraphView override + override-wins applyTokens; GraphViewHost traceColors param; aligned visible-trace model (series+setpoints+names+colors); dedicated Colorful-8 swatch generation (not t.pool.take(8)); if/else morph; writeScope persistence; clampHeaterTarget; same-hue tint; gutter=null.
- (Phase UAT) On flox: morph works without graph flicker, color swatches recolor the GRAPH TRACE + row icon + persist across re-entry, presets takeover, Cooldown + per-heater Off fire correctly, both orientations.
</verification>

<success_criteria>
- Morph Focus (D-10), typed rows (D-11), Field-takeover presets (D-12), Cooldown + per-heater Off (D-13), graph controls + 8-swatch Colorful color row that recolors the GRAPH TRACE (D-14) all delivered.
- Graph equality guards extended-not-broken; the visible model keeps all per-trace lists aligned; clamp + writeScope rules honored.
</success_criteria>

<output>
Create `.planning/phases/26-adjustment-screens/26-04-SUMMARY.md` when done.
</output>

<!-- Decisions implemented: D-06 (single-setting pushes retire — presets become an in-place Field-takeover), D-09 (no scrubber here; stepper dispatch), D-10 (graph<->adjuster morph), D-11 (typed rows), D-12 (Field-takeover presets), D-13 (Cooldown + per-heater Off), D-14 (graph controls + 8-swatch color drawn on the graph trace via equality-guarded override), D-22 (clampHeaterTarget), D-23 (preview matrix), D-24 (registry icons only). -->
