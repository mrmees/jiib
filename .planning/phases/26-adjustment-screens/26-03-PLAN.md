---
phase: 26-adjustment-screens
plan: 03
type: execute
wave: 3
depends_on: ["26-02"]
files_modified:
  - app/src/main/java/works/mees/dinghy/ui/temperature/TemperatureHolder.kt
  - app/src/main/java/works/mees/dinghy/ui/settings/TraceStylePrefs.kt
  - app/src/main/java/works/mees/dinghy/di/AppContainer.kt
  - app/src/main/java/works/mees/dinghy/DinghyApp.kt
  - app/src/test/java/works/mees/dinghy/ui/temperature/TemperatureHolderTraceStyleTest.kt
autonomous: true
requirements: []
user_setup: []
must_haves:
  truths:
    - "TemperatureHolder exposes per-sensor trace color and trace visibility as observable state, keyed by sensor name"
    - "Setting a trace color or visibility persists durably through the process-lifetime write scope (never a composition scope) and is restored on relaunch"
    - "SensorReadout carries an isAdjustable flag so future read-only sensors can join without rework"
    - "The Phase-22 GraphViewHost equality guards are NOT disturbed — this plan only adds holder color/visibility STATE + persistence; the graph's per-trace color override wiring (D-14) lands in plan 26-04, applied through an equality-guarded GraphView setter"
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/ui/temperature/TemperatureHolder.kt"
      provides: "traceColors + traceVisibility StateFlows + setters + isAdjustable on SensorReadout"
      contains: "traceColors"
    - path: "app/src/main/java/works/mees/dinghy/ui/settings/TraceStylePrefs.kt"
      provides: "DataStore-backed persistence of per-sensor trace color/visibility"
    - path: "app/src/test/java/works/mees/dinghy/ui/temperature/TemperatureHolderTraceStyleTest.kt"
      provides: "trace color/visibility state-transition tests"
  key_links:
    - from: "TemperatureHolder.setTraceColor"
      to: "AppContainer.writeScope"
      via: "durable persistence"
      pattern: "writeScope"
    - from: "AppContainer"
      to: "TraceStylePrefs"
      via: "setTraceColor / setTraceVisibility mutators"
      pattern: "TraceStylePrefs"
---

<objective>
Extend `TemperatureHolder` and the persistence layer to support per-sensor trace color and show/hide (D-14), plus the typed-row `isAdjustable` flag (D-11) — WITHOUT rebuilding the holder and WITHOUT disturbing the Phase-22 GraphViewHost equality guards. This is the data/persistence half of the Temperature rebuild; the screen UI + the graph's per-trace color override (D-14: the graph draws the chosen color) land in plan 26-04.

Purpose: D-14 requires trace color/visibility to persist durably (the P14 write-scope-cancellation bug class must not recur). The holder is the right tier for the state; a new `TraceStylePrefs` (mirroring `BabystepPrefs`) is the durable store; AppContainer exposes the writeScope-backed mutators.
Output: extended `TemperatureHolder`, new `TraceStylePrefs`, AppContainer mutators + DinghyApp wiring, and state-transition tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-adjustment-screens/26-CONTEXT.md
@.planning/phases/26-adjustment-screens/26-PATTERNS.md
@.planning/phases/26-adjustment-screens/26-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TraceStylePrefs DataStore store + holder API stubs + RED state-transition test</name>
  <files>app/src/main/java/works/mees/dinghy/ui/settings/TraceStylePrefs.kt, app/src/main/java/works/mees/dinghy/ui/temperature/TemperatureHolder.kt, app/src/test/java/works/mees/dinghy/ui/temperature/TemperatureHolderTraceStyleTest.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/settings/BabystepPrefs.kt (the EXACT analog: DataStore Preferences store with a suspend setter + a Flow getter, process-lifetime safe)
    - app/src/main/java/works/mees/dinghy/ui/temperature/TemperatureHolder.kt (FULL — class TemperatureHolder line ~49; _series/_setpoints/_legend lines 64-74; SensorReadout data class line ~187; the holder constructor + scope)
    - app/src/main/java/works/mees/dinghy/di/AppContainer.kt (lines ~132 writeScope; ~289-294 setBabystepEnabled mutator pattern; how prefs stores are constructed/injected)
    - app/src/main/java/works/mees/dinghy/DinghyApp.kt (how BabystepPrefs is constructed and handed to AppContainer)
    - .planning/phases/26-adjustment-screens/26-PATTERNS.md (TemperatureHolder extension pattern; the D-14 persistence rule)
    - .claude memory note dinghy-compose-write-scope-cancellation (the P14 bug class this guards against — summarized in CONTEXT/RESEARCH)
  </read_first>
  <behavior>
    - TraceStylePrefs persists a Map<String, Int (ARGB)> trace color and Map<String, Boolean> visibility keyed by sensor name; round-trips through DataStore.
    - TemperatureHolderTraceStyleTest: setTraceColor(name, color) then traceColors.value[name] == color; setTraceVisibility(name, false) then traceVisibility.value[name] == false; default visibility for an unseen sensor is true (absent-means-visible).
  </behavior>
  <action>
    CRITICAL (Wave-0 compile-brick guard): `TemperatureHolder` currently exposes only `series`/`setpoints`/`legend` (TemperatureHolder.kt:64-74) and `SensorReadout` has no `isAdjustable` (line ~187). A test referencing the not-yet-built `traceColors`/`traceVisibility`/`setTraceColor`/`setTraceVisibility` API would fail `:app:compileDebugUnitTestKotlin` and brick the whole test source set. So this task ADDS the holder API as compiling STUBS first, then the test, then the persistence store.

    Step 1 — TraceStylePrefs: create `TraceStylePrefs.kt` mirroring `BabystepPrefs.kt`: a Preferences-DataStore store exposing `traceColors: Flow<Map<String, Int>>` (ARGB Int) + `traceVisibility: Flow<Map<String, Boolean>>` and suspend setters `setTraceColor(sensorName, argb)` / `setTraceVisibility(sensorName, visible)`. Encode the per-sensor maps as DataStore preference keys (e.g. `trace_color_<name>` Int, `trace_visible_<name>` Boolean) so the store stays flat and key-stable.

    Step 2 — holder API stubs: add to `TemperatureHolder` the new public surface so the test compiles: `val traceColors: StateFlow<Map<String, Color>>` (back it with a `MutableStateFlow(emptyMap())`), `val traceVisibility: StateFlow<Map<String, Boolean>>` (same), and `fun setTraceColor(sensorName: String, color: Color)` / `fun setTraceVisibility(sensorName: String, visible: Boolean)` with minimal in-memory bodies (update the MutableStateFlow). These stub bodies make the test PASS for the in-memory transitions; Task 2 wires persistence + seeding from TraceStylePrefs. Do NOT add the `isAdjustable` field yet if it risks breaking existing `SensorReadout` construction — Task 2 owns that, but if a default `= true` keeps everything compiling you may add it here. Do NOT touch GraphView/GraphViewHost in this task.

    Step 3 — RED/GREEN test: create `TemperatureHolderTraceStyleTest.kt` referencing ONLY the holder's new `traceColors`/`traceVisibility`/`setTraceColor`/`setTraceVisibility` API — assert the state transitions in the behavior block. Use the existing `TemperatureHolder` test harness/fakes as the analog; if none exists, construct the holder with a fake store as the analog tests do. Reference no unbuilt symbol beyond the now-stubbed holder API. With the Step-2 in-memory stubs the transition assertions GO GREEN this task; the durable-persistence proof is Task 2's responsibility.
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --tests works.mees.dinghy.ui.temperature.TemperatureHolderTraceStyleTest --no-daemon" 2>&1 | tr -d '\r'</automated>
  </verify>
  <acceptance_criteria>
    - `:app:compileDebugUnitTestKotlin` succeeds (the new holder API stubs prevent the test-source-set brick).
    - `TraceStylePrefs.kt` compiles; exposes color + visibility Flows and suspend setters mirroring BabystepPrefs.
    - `TemperatureHolder` exposes `traceColors`, `traceVisibility`, `setTraceColor`, `setTraceVisibility` (stub bodies acceptable this task).
    - `TemperatureHolderTraceStyleTest` passes for the in-memory state transitions.
    - Color stored as ARGB Int in the prefs store (source assertion — no raw String hex parsing on the hot path).
  </acceptance_criteria>
  <done>TraceStylePrefs persists per-sensor color/visibility; the holder API stubs exist so the test compiles and passes the in-memory transitions.</done>
</task>

<task type="auto">
  <name>Task 2: Wire TemperatureHolder trace state to TraceStylePrefs + isAdjustable</name>
  <files>app/src/main/java/works/mees/dinghy/ui/temperature/TemperatureHolder.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/temperature/TemperatureHolder.kt (FULL — Task-1 stub StateFlows + setters; _series/_setpoints/_legend lines 64-74; readout() line ~140; SensorReadout data class line ~187; combine pipeline lines ~100-130)
    - app/src/main/java/works/mees/dinghy/ui/settings/TraceStylePrefs.kt (Task 1 store — the durable source feeding the holder StateFlows)
    - .planning/phases/26-adjustment-screens/26-PATTERNS.md (TemperatureHolder EXTEND pattern — the new StateFlows + mutators + isAdjustable; the corrected EQUALITY GUARD INVARIANT — the graph color override lands in 26-04, NOT here)
    - app/src/main/java/works/mees/dinghy/render/GraphView.kt (confirm trace colors are token-based via applyTokens/seriesColor at GraphView.kt:207-218; the per-trace OVERRIDE setter is ADDED in plan 26-04, NOT this plan)
  </read_first>
  <action>
    Promote the Task-1 stubs to the real wiring. Seed `_traceColors` / `_traceVisibility` from the injected `TraceStylePrefs` at construction (collect the prefs Flows into the maps within the holder scope, mapping ARGB Int → Compose `Color`). Make `setTraceColor(sensorName, color)` and `setTraceVisibility(sensorName, visible)` update the StateFlow AND persist via the injected TraceStylePrefs (the persistence itself runs on the prefs store's writeScope-backed setters — the holder must NOT use a composition scope). Add `isAdjustable: Boolean = true` to the `SensorReadout` data class (D-11) and set it in `readout()` (v1: all drawn sensors are heaters → true). CLARIFICATION (per the corrected D-14 plan): do NOT add the per-trace color OVERRIDE to `GraphView` / `GraphViewHost` IN THIS PLAN — that wiring (an equality-guarded `setTraceColorOverrides`) is plan 26-04's job, applied through the screen's visible-trace model. This plan only EXPOSES the holder's color + visibility STATE. Preserve `@Stable`/`@Immutable` on the holder's published types.
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testDebugUnitTest --tests works.mees.dinghy.ui.temperature.TemperatureHolderTraceStyleTest --no-daemon" 2>&1 | tr -d '\r'</automated>
  </verify>
  <acceptance_criteria>
    - `TemperatureHolderTraceStyleTest` passes (color/visibility set + default transitions green; now backed by real persistence seeding).
    - `TemperatureHolder.kt` contains `traceColors`, `traceVisibility`, `setTraceColor`, `setTraceVisibility` wired to TraceStylePrefs.
    - `SensorReadout` has `isAdjustable` field; `readout()` sets it (source assertion).
    - No per-trace color override parameter added to `GraphView` / `GraphViewHost` in THIS plan (source assertion — the render files are unchanged here; the override lands in 26-04).
  </acceptance_criteria>
  <done>Holder exposes persisted trace color/visibility + isAdjustable; the graph override is left for 26-04; test green.</done>
</task>

<task type="auto">
  <name>Task 3: AppContainer + DinghyApp wiring for TraceStylePrefs</name>
  <files>app/src/main/java/works/mees/dinghy/di/AppContainer.kt, app/src/main/java/works/mees/dinghy/DinghyApp.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/di/AppContainer.kt (writeScope line ~132; setBabystepEnabled line ~289-294; constructor params + how prefs stores are held)
    - app/src/main/java/works/mees/dinghy/DinghyApp.kt (BabystepPrefs construction + AppContainer instantiation)
    - app/src/main/java/works/mees/dinghy/ui/settings/TraceStylePrefs.kt (Task 1)
    - app/src/main/java/works/mees/dinghy/ui/temperature/TemperatureHolder.kt (Task 2 — how the holder receives TraceStylePrefs)
  </read_first>
  <action>
    Construct `TraceStylePrefs` in `DinghyApp` (alongside `BabystepPrefs`) and pass it into `AppContainer`. In `AppContainer`, hold the `TraceStylePrefs` reference and expose it (or a thin accessor) so the `TemperatureHolder` it constructs receives it. If AppContainer constructs the TemperatureHolder (or the holder is created in AppShell with the container), thread TraceStylePrefs through the same path. Keep all DataStore writes on the process-lifetime `writeScope` (TraceStylePrefs setters are suspend; AppContainer/holder must launch them on writeScope, never `rememberCoroutineScope`). Mirror the exact `setBabystepEnabled` wiring shape.
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:assembleDebug :app:testDebugUnitTest --no-daemon" 2>&1 | tr -d '\r'</automated>
  </verify>
  <acceptance_criteria>
    - `assembleDebug` compiles; full host suite green.
    - `DinghyApp.kt` constructs `TraceStylePrefs` and passes it to AppContainer (source assertion).
    - `AppContainer.kt` references `TraceStylePrefs` and routes any direct writes through `writeScope` (source assertion: no `rememberCoroutineScope` in this layer).
  </acceptance_criteria>
  <done>TraceStylePrefs is wired through DinghyApp → AppContainer → TemperatureHolder; build + tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI → persistence | Trace color (from a fixed 8-swatch Colorful pool) + visibility boolean persist to DataStore. No external input; color is selected from a fixed Colorful-pool list, never constructed from user text. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-03-01 | Tampering | Trace color value | accept | THEME-01 data-color carve-out: color is an ARGB Int chosen from a fixed 8-color pool, not arbitrary user input. No injection surface. |
| T-26-03-02 | Denial of Service | Persistence write dropped (P14 class) | mitigate | All DataStore writes run on the process-lifetime `writeScope`, never a composition scope — guards the P14 switch-revert bug class. |
| T-26-03-SC | Tampering | npm/pip/cargo installs | accept | No package installs — DataStore + Compose Color already in libs.versions.toml. |
</threat_model>

<verification>
- `:app:compileDebugUnitTestKotlin` succeeds after Task 1 (holder API stubs prevent the test-source-set brick).
- `TemperatureHolderTraceStyleTest` green.
- Full host suite green; `assembleDebug` compiles.
- Source: no `GraphView` per-trace color API added in this plan (it lands in 26-04); no composition-scope writes; isAdjustable on SensorReadout.
</verification>

<success_criteria>
- Per-sensor trace color + visibility are observable, mutable, and durably persisted via writeScope (D-14).
- `isAdjustable` typed-row flag added (D-11).
- Graph equality guards untouched (the graph override is plan 26-04's work).
</success_criteria>

<output>
Create `.planning/phases/26-adjustment-screens/26-03-SUMMARY.md` when done.
</output>

<!-- Decisions implemented: D-11 (typed isAdjustable row model), D-14 (per-trace color/visibility persisted via writeScope; the graph DRAWS the chosen color — override wiring in 26-04), D-22 (writeScope persistence discipline), D-23/D-24 fold in at the screen plan 26-04. -->
