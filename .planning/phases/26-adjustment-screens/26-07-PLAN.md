---
phase: 26-adjustment-screens
plan: 07
type: execute
wave: 6
depends_on: ["26-06"]
files_modified:
  - app/src/main/java/works/mees/dinghy/ui/macros/BookmarkedMacrosScreen.kt
  - app/src/main/java/works/mees/dinghy/ui/spool/SpoolScreen.kt
  - app/src/main/java/works/mees/dinghy/preview/MacrosPreviews.kt
  - app/src/main/java/works/mees/dinghy/preview/SpoolPreviews.kt
  - app/src/main/java/works/mees/dinghy/designsystem/NumpadPage.kt
  - app/src/main/java/works/mees/dinghy/ui/spool/MeasuredWeightPage.kt
autonomous: true
requirements: []
user_setup: []
must_haves:
  truths:
    - "Macro numeric params use the Android numeric IME Field-takeover (clamped to the existing safe range), not the custom NumpadPage"
    - "Measured weight is entered via a numeric-IME Field-takeover inside SpoolScreen's flow (the MeasuredWeightPage full-screen push retires)"
    - "NumpadPage and MeasuredWeightPage are deleted; no caller references them anywhere in the app"
    - "Alphanumeric input remains barred from printer controls — only NUMERIC IME is used for numeric entry"
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/ui/macros/BookmarkedMacrosScreen.kt"
      provides: "Macro numeric-param numeric-IME Field-takeover"
    - path: "app/src/main/java/works/mees/dinghy/ui/spool/SpoolScreen.kt"
      provides: "Measured-weight numeric-IME Field-takeover (D-08)"
  key_links:
    - from: "BookmarkedMacrosScreen.kt"
      to: "macro param clamp range"
      via: "parse + clamp IME value before dispatch"
      pattern: "coerceIn|clamp"
    - from: "SpoolScreen.kt"
      to: "applyMeasuredWeight"
      via: "numeric-IME Field-takeover"
      pattern: "MeasureWeight|applyMeasuredWeight"
---

<objective>
Complete the app-wide NumpadPage retirement (D-07): convert the macro numeric-param entry (`BookmarkedMacrosScreen`, a Phase-25 screen) and the measured-weight flow (`SpoolScreen`, D-08) to the Android numeric IME, then delete `NumpadPage.kt` and `MeasuredWeightPage.kt`. This supersedes Phase-25 D-12's "NumpadPage for numeric" for macro params. Alphanumeric stays barred from printer controls — only numeric IME for numeric entry.

Purpose: D-07 drops the custom numpad app-wide; this plan finishes the conversion (Extrude was done in 26-06) and removes the dead files. Macro params currently rely on NumpadPage as the sole clamp owner — the IME replacement MUST keep the clamp.
Output: macro + spool numeric-IME takeovers, preview updates, and two file deletions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-adjustment-screens/26-CONTEXT.md
@.planning/phases/26-adjustment-screens/26-PATTERNS.md
@.planning/phases/26-adjustment-screens/26-RESEARCH.md
@.planning/phases/25-browse-screens/25-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Macro numeric-param numeric-IME Field-takeover</name>
  <files>app/src/main/java/works/mees/dinghy/ui/macros/BookmarkedMacrosScreen.kt, app/src/main/java/works/mees/dinghy/preview/MacrosPreviews.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/macros/BookmarkedMacrosScreen.kt (FULL — MacroFieldMode.ParamEntry; the numpadParam state ~line 378; the NumpadPage Field-takeover ~lines 407-423; the wide macro clamp range ~lines 60-61; how onSet dispatches; TokenTextField for non-numeric params)
    - app/src/main/java/works/mees/dinghy/command/MacroInvocation.kt + ui/macros/MacroModels.kt (the macro param model; where NumpadPage / range is referenced; the dispatch path)
    - .planning/phases/26-adjustment-screens/26-PATTERNS.md (numeric IME pattern; D-07 supersedes P25 D-12 for macro params)
    - .planning/phases/25-browse-screens/25-CONTEXT.md (D-12 macro-param Field-takeover — its NumpadPage wording is superseded here)
    - app/src/main/java/works/mees/dinghy/preview/MacrosPreviews.kt (existing previews referencing NumpadPage — update)
  </read_first>
  <action>
    Replace the macro numeric-param `NumpadPage` Field-takeover with a numeric-IME `BasicTextField`/`OutlinedTextField` takeover inside the existing `MacroFieldMode.ParamEntry` flow: `KeyboardType.Decimal` (or `.Number` for integer params), `ImeAction.Done`. The takeover MUST remain the clamp owner — parse the entered value and clamp to the existing wide macro range (the constant at ~lines 60-61) before dispatch; never pass raw IME text into the macro invocation string. Keep the Back/Cancel + Set/Apply foot affordances (NumpadPage previously provided its own onCancel/onSet — replace with FootButtonBar Back + Apply, or keep the takeover's own buttons, matching the screen's existing FootButtonBar grammar). Non-numeric (token) params keep their TokenTextField path unchanged. Remove the `NumpadPage` import + all NumpadPage references from this screen and from `MacroModels.kt`/`MacroInvocation.kt` if they reference it (preserve the clamp range constant — move it inline if it lived in NumpadPage). Update `MacrosPreviews.kt` to drive the new IME takeover (drop any NumpadPage preview).
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:assembleDebug :app:testDebugUnitTest --no-daemon" 2>&1 | tr -d '\r'</automated>
  </verify>
  <acceptance_criteria>
    - `assembleDebug` + full host suite green.
    - Macro numeric param entry uses a numeric-IME TextField (`KeyboardType.Number`/`Decimal`), not `NumpadPage` (source assertion).
    - The entered value is parsed + clamped to the macro range before dispatch (source assertion — clamp owner preserved).
    - No `NumpadPage` reference remains in `BookmarkedMacrosScreen.kt`, `MacroModels.kt`, `MacroInvocation.kt`, `MacrosPreviews.kt` (grep returns nothing).
  </acceptance_criteria>
  <done>Macro numeric params use numeric IME with clamping; NumpadPage references removed from the macro path; build + tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Measured-weight numeric-IME Field-takeover in SpoolScreen (D-08)</name>
  <files>app/src/main/java/works/mees/dinghy/ui/spool/SpoolScreen.kt, app/src/main/java/works/mees/dinghy/preview/SpoolPreviews.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/spool/SpoolScreen.kt (FULL — measureSpool state ~line 113; the MeasuredWeightPage push ~lines 188-200; the FieldMode sealed class ~lines 263-264/394-408; applyMeasuredWeight / the onMeasure callback)
    - app/src/main/java/works/mees/dinghy/ui/spool/MeasuredWeightPage.kt (the existing weight-entry + apply logic to migrate inline)
    - .planning/phases/26-adjustment-screens/26-PATTERNS.md (SpoolScreen D-08 numeric-IME Field-takeover pattern: MeasureWeight FieldMode variant)
    - app/src/main/java/works/mees/dinghy/preview/SpoolPreviews.kt (existing spool previews — add a MeasureWeight preview)
    - app/src/main/java/works/mees/dinghy/designsystem/components/FootButtonBar.kt + control/OutlinedControl.kt
  </read_first>
  <action>
    Convert the measured-weight flow to an in-Field numeric-IME takeover (D-08): add a `MeasureWeight(spool)` variant to SpoolScreen's `FieldMode` sealed class; replace the `measureSpool?.let { MeasuredWeightPage(...) }` full-screen push with a `when (fieldMode)` branch rendering a numeric-IME `BasicTextField` (`KeyboardType.Decimal`, `ImeAction.Done`) for the weight, accepting digits + one decimal only (no alpha), clamped to weight >= 0; on Done/Apply call the existing `applyMeasuredWeight(spool, weight)` then return to the spool list; foot = Back + Apply (Intent.Go). Migrate the weight-entry + apply logic from `MeasuredWeightPage.kt` inline (the Phase-23 pilot edit is confined to this takeover wiring per D-08). Remove the `MeasuredWeightPage` import + reference. Update `SpoolPreviews.kt` to add a MeasureWeight Field-takeover preview.
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:assembleDebug :app:testDebugUnitTest --no-daemon" 2>&1 | tr -d '\r'</automated>
  </verify>
  <acceptance_criteria>
    - `assembleDebug` + full host suite green.
    - SpoolScreen `FieldMode` has a `MeasureWeight` variant; the weight entry is a numeric-IME TextField (source assertion).
    - Weight is parsed + validated (>= 0, digits/decimal only) before `applyMeasuredWeight` (source assertion).
    - No `MeasuredWeightPage` reference remains in SpoolScreen.kt or SpoolPreviews.kt (grep returns nothing).
    - `SpoolPreviews.kt` includes a MeasureWeight preview.
  </acceptance_criteria>
  <done>Measured weight is a numeric-IME Field-takeover in SpoolScreen; MeasuredWeightPage no longer referenced; build + tests green.</done>
</task>

<task type="auto">
  <name>Task 3: Delete NumpadPage + MeasuredWeightPage and prove no callers remain</name>
  <files>app/src/main/java/works/mees/dinghy/designsystem/NumpadPage.kt, app/src/main/java/works/mees/dinghy/ui/spool/MeasuredWeightPage.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/designsystem/NumpadPage.kt (confirm no remaining public API consumed elsewhere — e.g. a shared formatter; if a helper is reused, relocate it before deleting)
    - app/src/main/java/works/mees/dinghy/ui/spool/MeasuredWeightPage.kt (confirm logic fully migrated to Task 2)
    - app/src/main/java/works/mees/dinghy/designsystem/control/OutlinedControl.kt (it referenced MeasuredWeightPage per the grep — confirm/remove any dangling KDoc/import)
  </read_first>
  <action>
    Grep the entire `app/src/` tree for `NumpadPage` and `MeasuredWeightPage`. Confirm the only remaining references are the definitions themselves (all callers were converted in 26-06 Extrude + Task 1 macros + Task 2 spool). If any helper from NumpadPage (e.g. a number-formatting util) is still consumed, relocate it to a shared util file first. Then delete `NumpadPage.kt` and `MeasuredWeightPage.kt`. Fix any remaining import/KDoc reference (e.g. the OutlinedControl.kt KDoc reference flagged in the grep). Re-grep to prove zero references remain.
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:assembleDebug :app:testDebugUnitTest --no-daemon" 2>&1 | tr -d '\r'</automated>
  </verify>
  <acceptance_criteria>
    - `NumpadPage.kt` and `MeasuredWeightPage.kt` are deleted.
    - `grep -rn "NumpadPage\|MeasuredWeightPage" app/src/` returns nothing (no callers, no imports, no KDoc refs).
    - `assembleDebug` + full host suite green.
  </acceptance_criteria>
  <done>Both retired files are deleted; the app has zero references to them; build + tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI → command layer | Macro numeric params + measured weight (now numeric IME) cross to gcode/Spoolman. The IME is the untrusted-input surface; macro params previously had NumpadPage as the sole clamp owner. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-07-01 | Tampering | Macro numeric param → macro invocation | mitigate | The IME takeover remains the clamp owner: `KeyboardType.Number`/`Decimal` restricts input + parse-and-clamp to the existing wide macro range before building the invocation string. Raw IME text never concatenated into the macro command. |
| T-26-07-02 | Tampering | Measured weight → Spoolman update | mitigate | Numeric IME + parse-and-validate (digits/decimal only, >= 0) before `applyMeasuredWeight`. No alpha input; no injection surface. |
| T-26-07-03 | Tampering | Alphanumeric leak into printer controls | mitigate | Only NUMERIC IME used for numeric entry; alphanumeric remains barred from printer controls (D-07 amendment scope). |
| T-26-07-SC | Tampering | npm/pip/cargo installs | accept | No package installs — numeric IME is platform Compose; deletions only. |
</threat_model>

<verification>
- `assembleDebug` + full host suite green.
- Grep proves zero `NumpadPage` / `MeasuredWeightPage` references app-wide.
- Source assertions: macro + weight use numeric IME with clamp/validation; alphanumeric not used for numeric entry.
- (Phase UAT) On flox: macro numeric param entry + measured weight via numeric keyboard, clamped, both orientations.
</verification>

<success_criteria>
- NumpadPage dropped app-wide (D-07); macro params + measured weight on numeric IME with clamping.
- MeasuredWeight converted to a SpoolScreen Field-takeover (D-08); both retired files deleted.
</success_criteria>

<output>
Create `.planning/phases/26-adjustment-screens/26-07-SUMMARY.md` when done.
</output>

<!-- Decisions implemented: D-06 (single-setting pushes retire — Field-takeovers), D-07 (numeric IME app-wide, NumpadPage dropped), D-08 (measured-weight Field-takeover), D-12 (macro-param Field-takeover, superseded NumpadPage wording), D-23/D-24 fold in. -->
