---
phase: 26-adjustment-screens
plan: 06
type: execute
wave: 6
depends_on: ["26-01", "26-05"]
files_modified:
  - app/src/main/java/works/mees/dinghy/ui/extrude/ExtrudeScreen.kt
  - app/src/main/java/works/mees/dinghy/ui/shell/AppShell.kt
  - app/src/main/java/works/mees/dinghy/preview/ExtrudePreviews.kt
autonomous: true
requirements: []
user_setup: []
must_haves:
  truths:
    - "Extrude keeps its specialized command-centric layout (NOT migrated to list+adjuster) but gains full conformance: tokens, FootButtonBar replacing the gutter, ≥64px, fsSp, rotation, @Preview matrix, tokenized strings, registry icons"
    - "Extrude distance and speed entry use the Android numeric IME (clamped to existing ranges), not the custom NumpadPage"
    - "The nozzle-temp button opens a filament-preset Field-list (standard presets + the loaded spool's filament when Spoolman provides one); selecting applies the EXTRUDER temperature only, closes the page, with a Back at the bottom"
    - "Cold-extrude icon gating (EXTR-04) and the Load/Unload/Back foot intents are preserved"
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/ui/extrude/ExtrudeScreen.kt"
      provides: "Conformance-only Extrude rebuild + numeric IME + filament-preset Field-takeover"
      contains: "fun ExtrudeScreen"
  key_links:
    - from: "ExtrudeScreen.kt"
      to: "PrinterCommands extrude clamp ranges"
      via: "parse + clamp IME value before dispatch"
      pattern: "coerceIn|clamp"
    - from: "ExtrudeScreen.kt"
      to: "SpoolHolder.activeSpoolDetail"
      via: "loaded-filament preset row"
      pattern: "activeSpoolDetail"
    - from: "AppShell.kt"
      to: "ExtrudeScreen activeSpoolDetail param"
      via: "thread the collected activeSpoolDetail into the Extrude route"
      pattern: "ExtrudeScreen"
---

<objective>
Bring `ExtrudeScreen` to full conformance WITHOUT migrating its specialized command-centric layout (D-15 — like Move's jog grid, the function requires its shape). Replace the gutter with a `FootButtonBar`, swap the custom `NumpadPage` distance/speed entry for the Android numeric IME (D-16), and convert the nozzle-temp button into a filament-preset Field-takeover that applies the extruder temperature only (D-17). Cold-extrude gating (EXTR-04) and Load/Unload intents are preserved.

Purpose: Extrude is the specialized-layout exemption. Conformance folds in but the layout stays; the numeric-IME pivot (D-07) lands here for distance/speed; the filament-preset page is the one feature-flavored addition.
Output: rebuilt-for-conformance `ExtrudeScreen.kt` + preview matrix. (NumpadPage.kt deletion happens in plan 26-07 after all callers convert.)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-adjustment-screens/26-CONTEXT.md
@.planning/phases/26-adjustment-screens/26-PATTERNS.md
@.planning/phases/26-adjustment-screens/26-RESEARCH.md
@docs/ui_design/LAYOUT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extrude conformance — FootButtonBar, tokens, fsSp, numeric IME for distance/speed</name>
  <files>app/src/main/java/works/mees/dinghy/ui/extrude/ExtrudeScreen.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/extrude/ExtrudeScreen.kt (FULL current — the NumpadTarget enum line ~80; the gutter at ~260; the three NumpadPage call sites at ~298/308/317; the nozzle-temp button at ~219-236; cold-extrude gate; ScreenScaffold)
    - app/src/main/java/works/mees/dinghy/ui/extrude/ExtrudeHolder.kt (vm fields: nozzleTemp, nozzleTarget, distance, speed; the dispatch path — UNCHANGED)
    - .planning/phases/26-adjustment-screens/26-PATTERNS.md (ExtrudeScreen conformance pattern: FootButtonBar+gutter=null; numeric IME BasicTextField with clamp; never pass raw IME text to gcode)
    - app/src/main/java/works/mees/dinghy/designsystem/components/FootButtonBar.kt + control/OutlinedControl.kt
    - app/src/main/java/works/mees/dinghy/command/PrinterCommands.kt (extrude() clamp ranges: MIN_FEED_MM_MIN..MAX_EXTRUDE_FEED_MM_MIN line ~318-322; the extrude distance bounds)
    - app/src/main/java/works/mees/dinghy/ui/macros/BookmarkedMacrosScreen.kt (a numeric-entry Field-takeover analog, ~lines 407-423 — though it currently uses NumpadPage; the IME pattern is in PATTERNS.md)
    - app/src/main/java/works/mees/dinghy/theme/fsSp + ThemeTokens.kt
  </read_first>
  <action>
    Apply conformance to ExtrudeScreen WITHOUT changing its layout shape: move the `gutter` content into a `FootButtonBar` placed as the last element inside the `field` lambda and set `gutter = null` (preserve Load/Unload/Back — Intent.Accent for the physical Load, Intent.Neutral for Back; registry icons only, if a Load/Unload glyph is not already chosen STOP and ASK). Replace the distance + speed `NumpadPage` pushes (NumpadTarget.Distance / NumpadTarget.Speed) with Android numeric IME entry: a `BasicTextField` (or OutlinedTextField) with `KeyboardOptions(keyboardType = KeyboardType.Decimal, imeAction = ImeAction.Done)`; on each change parse to Double and ALWAYS clamp to the existing extrude distance / `MIN_FEED_MM_MIN..MAX_EXTRUDE_FEED_MM_MIN` speed ranges before storing/dispatch — NEVER pass raw IME text to gcode. Keep the presets fast path. Ensure every text uses `fsSp(...)`, every touch target ≥ grid.uDp / ≥64px, the cold-extrude icon gate (EXTR-04) is unchanged, and `@Preview` rotation works (configChanges already handled app-wide). Leave the nozzle-temp `NumpadTarget.Temp` path for Task 2. Do NOT delete NumpadPage.kt (plan 26-07 owns the deletion after all callers convert) — but remove Extrude's NumpadPage usage for Distance/Speed in this task.
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:assembleDebug :app:testDebugUnitTest --no-daemon" 2>&1 | tr -d '\r'</automated>
  </verify>
  <acceptance_criteria>
    - `assembleDebug` + full host suite green.
    - `gutter = null`; FootButtonBar inside `field` (source assertion).
    - Distance + Speed use a numeric-IME TextField (`KeyboardType.Decimal`), not `NumpadPage` (source assertion: NumpadTarget.Distance/Speed call sites removed).
    - Every IME value is parsed + clamped (`coerceIn`/`clamp`) before dispatch (source assertion — no raw text concatenated into gcode).
    - All text wrapped in `fsSp(...)` (source assertion — no bare `.sp` on literals).
  </acceptance_criteria>
  <done>Extrude has FootButtonBar conformance + numeric IME distance/speed with clamping; build + tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Filament-preset Field-takeover (nozzle-temp button) + activeSpoolDetail wiring + previews</name>
  <files>app/src/main/java/works/mees/dinghy/ui/extrude/ExtrudeScreen.kt, app/src/main/java/works/mees/dinghy/ui/shell/AppShell.kt, app/src/main/java/works/mees/dinghy/preview/ExtrudePreviews.kt</files>
  <read_first>
    - app/src/main/java/works/mees/dinghy/ui/extrude/ExtrudeScreen.kt (Task 1 state — the nozzle-temp button at ~219-236; NumpadTarget.Temp at ~317)
    - .planning/phases/26-adjustment-screens/26-PATTERNS.md (ExtrudeFieldMode sealed class; FilamentPresets Field-takeover; applies SetHeaterArgs for the EXTRUDER only, never bed)
    - app/src/main/java/works/mees/dinghy/command/PrinterCommands.kt (MATERIAL_PRESETS line ~244; the Preset type with nozzle/bed temps; clampHeaterTarget; CommandRegistry.setHeater)
    - app/src/main/java/works/mees/dinghy/ui/spool/SpoolHolder.kt + the SpoolHolder.activeSpoolDetail flow (the loaded-spool filament name/type to add as the extra preset row — P18.3 SpoolHolder.activeSpoolDetail; type = the SpoolmanSpoolDetail the screen renders)
    - app/src/main/java/works/mees/dinghy/ui/extrude/ExtrudeScreen.kt (the LIVE overload at ~line 117 takes `container` + `holder` + `onBack` — it currently has NO spool detail; the screen needs an explicit `activeSpoolDetail` parameter)
    - app/src/main/java/works/mees/dinghy/ui/shell/AppShell.kt (activeSpoolDetail is ALREADY collected at ~line 317 `val activeSpoolDetail by spoolHolder.activeSpoolDetail.collectAsStateWithLifecycle()`; the `composable<NavDest.Extrude>` ExtrudeScreen call site is at ~line 623-628 — thread activeSpoolDetail in there. WAVE NOTE: 26-05 (wave 5) does its AppShell Outputs route surgery FIRST; this plan is wave 6 so its AppShell edit lands cleanly after — no same-wave collision)
    - app/src/main/java/works/mees/dinghy/designsystem/components/ListBlock.kt, ListRow.kt, FootButtonBar.kt
    - docs/ui_design/PREVIEW_AND_TOKENS.md
  </read_first>
  <action>
    Convert the nozzle-temp button to open a filament-preset Field-takeover (D-17): add an `ExtrudeFieldMode` sealed class (Main | FilamentPresets) held in `remember`. Tapping the nozzle-temp button switches fieldMode to FilamentPresets. First, add an explicit `activeSpoolDetail` parameter to BOTH `ExtrudeScreen` overloads (live + stateless seam) — the screen currently takes no spool detail (ExtrudeScreen.kt:117). In `AppShell.kt`, pass the already-collected `activeSpoolDetail` (AppShell.kt:317) into the `composable<NavDest.Extrude>` ExtrudeScreen call (AppShell.kt:623-628). The takeover renders a `ListBlock` of `MATERIAL_PRESETS` rows PLUS, when the injected `activeSpoolDetail` provides a loaded filament, a top row for the currently-loaded spool's filament (name + its target temp). Selecting any row dispatches `SetHeaterArgs(extruderName, clampHeaterTarget(presetNozzleTemp))` for the EXTRUDER ONLY (NEVER bed — D-17), closes the page back to Main; a Back foot button sits at the bottom. Remove the `NumpadTarget.Temp` NumpadPage path. Then create/replace `ExtrudePreviews.kt` with the 6-combo + fs=L + landscape matrix driving the stateless seam, including one preview in FilamentPresets mode and one with a loaded-spool row present + one cold-extrude-gated state.
  </action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:assembleDebug :app:testDebugUnitTest --no-daemon" 2>&1 | tr -d '\r'</automated>
  </verify>
  <acceptance_criteria>
    - `assembleDebug` + full host suite green.
    - `ExtrudeFieldMode` sealed class exists with Main + FilamentPresets (source assertion).
    - Preset selection dispatches `setHeater` for the extruder heater only; no bed target set from this screen (source assertion — only the extruder heater name is targeted).
    - `ExtrudeScreen` takes an explicit `activeSpoolDetail` parameter (both overloads); `AppShell.kt` passes the collected `activeSpoolDetail` into the Extrude route call site (source assertion).
    - The loaded-spool filament row appears when `activeSpoolDetail` is present (source assertion: a conditional row reading activeSpoolDetail).
    - `NumpadTarget` / `NumpadPage` no longer referenced in ExtrudeScreen.kt (source assertion: grep ExtrudeScreen for NumpadPage returns nothing).
    - `ExtrudePreviews.kt` has ≥7 `@Preview` + a FilamentPresets preview.
  </acceptance_criteria>
  <done>Nozzle-temp button opens the filament-preset takeover applying extruder temp only; Extrude no longer uses NumpadPage; previews compile.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI → command layer | Extrude distance/speed (now numeric IME) + preset nozzle temp cross to gcode. The IME is the new untrusted-input surface introduced by D-07/D-16. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-06-01 | Tampering | Numeric IME distance/speed → gcode | mitigate | `KeyboardType.Decimal` restricts input; every value is parsed to Double + clamped to the existing extrude ranges (`MIN_FEED_MM_MIN..MAX_EXTRUDE_FEED_MM_MIN`, extrude distance bounds) before dispatch. Raw IME text NEVER concatenated into gcode (the single new input-validation obligation from D-07). |
| T-26-06-02 | Tampering | Preset nozzle temp → gcode | mitigate | Preset temps route through `clampHeaterTarget`; only the extruder heater is targeted (never bed). |
| T-26-06-03 | Spoofing | Loaded-spool filament row | accept | Read-only display sourced from Spoolman's active-spool flow; applies a clamped preset temp, no injection surface. |
| T-26-06-SC | Tampering | npm/pip/cargo installs | accept | No package installs — numeric IME is platform Compose; reuses MATERIAL_PRESETS + SpoolHolder + PrinterCommands. |
</threat_model>

<verification>
- `assembleDebug` + full host suite green.
- Source assertions: gutter=null + FootButtonBar; numeric-IME distance/speed with clamp; ExtrudeFieldMode FilamentPresets applying extruder-only; no NumpadPage reference in ExtrudeScreen.
- (Phase UAT) On flox: distance/speed numeric IME works + clamps; filament presets apply extruder temp only; cold-extrude gating intact; both orientations.
</verification>

<success_criteria>
- Extrude conformance within its specialized layout (D-15); numeric IME distance/speed (D-16); filament-preset extruder-only takeover (D-17).
- No NumpadPage use remains in Extrude (clears the way for plan 26-07's app-wide deletion).
</success_criteria>

<output>
Create `.planning/phases/26-adjustment-screens/26-06-SUMMARY.md` when done.
</output>

<!-- Decisions implemented: D-06 (single-setting pushes retire — filament presets Field-takeover), D-07 (numeric IME replaces NumpadPage), D-15 (specialized-layout exemption, conformance only), D-16 (numeric IME distance/speed), D-17 (filament-preset extruder-only page), D-23 (preview matrix), D-24 (registry icons only). -->
