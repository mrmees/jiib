---
phase: 07-files-print-control-core-print-loop-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/main/java/works/mees/dinghy/net/JsonRpc.kt
  - app/src/main/java/works/mees/dinghy/command/CommandRegistry.kt
  - app/src/main/java/works/mees/dinghy/state/PrinterState.kt
  - app/src/main/java/works/mees/dinghy/state/PrinterStateReducer.kt
  - app/src/main/java/works/mees/dinghy/state/DeriveCapabilities.kt
  - app/src/test/java/works/mees/dinghy/command/CommandCatalogDriftTest.kt
  - app/src/test/java/works/mees/dinghy/command/CommandRegistryFilesTest.kt
  - app/src/test/java/works/mees/dinghy/state/PrinterStateReducerTest.kt
  - app/src/test/java/works/mees/dinghy/state/DeriveCapabilitiesTest.kt
  - docs/commands/catalog.json
  - docs/commands/printer-matrix.json
autonomous: true
requirements:
  - FILE-01
  - FILE-02
  - FILE-03
  - FILE-04
  - JOB-03
  - JOB-04
  - JOB-05
must_haves:
  truths:
    - "Phase 7 runtime commands are registered before Files or Print Status UI consumes them."
    - "D-07: print start success is confirmed from printer state, not command acknowledgement."
    - "D-13/D-14/D-15/D-16: pause, resume, graceful cancel, restart, and Files actions are typed registry entries."
    - "Claude H-2: pause/resume/cancel confirmation uses Wave-1 reducer and subscribe-set evidence, not a Wave-4 conditional."
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/net/JsonRpc.kt"
      provides: "Phase 7 JSON-RPC method constants"
    - path: "app/src/main/java/works/mees/dinghy/command/CommandRegistry.kt"
      provides: "Typed CommandSpec entries for Files and print-control operations"
    - path: "docs/commands/catalog.json"
      provides: "Registered runtime metadata for Phase 7 command IDs"
    - path: "docs/commands/printer-matrix.json"
      provides: "Availability evidence rows for every new runtime registry command"
    - path: "app/src/main/java/works/mees/dinghy/state/PrinterStateReducer.kt"
      provides: "print_stats and pause_resume state-confirmation reducer coverage"
  key_links:
    - from: "CommandRegistry Phase 7 entries"
      to: "docs/commands/catalog.json"
      via: "catalog_id"
      pattern: "MR-server.files|MR-printer.print"
    - from: "CommandRegistry availability predicates"
      to: "docs/commands/printer-matrix.json"
      via: "command_availability and printer evidence"
      pattern: "file_manager|virtual_sdcard|pause_resume|print_stats"
---

<objective>
Promote Phase 7 Moonraker file and print-control operations into the runtime command registry.

Purpose: Files UI and Print Status controls must consume typed registry specs with sidecar drift protection before any screen dispatches start, delete, pause, resume, cancel, or restart.
Output: JSON-RPC constants, registry args/specs, catalog runtime metadata, matrix availability rows, and targeted registry tests.
</objective>

<execution_context>
@/mnt/c/Users/matth/.codex/get-shit-done/workflows/execute-plan.md
@/mnt/c/Users/matth/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-files-print-control-core-print-loop-gate/07-CONTEXT.md
@.planning/phases/07-files-print-control-core-print-loop-gate/07-RESEARCH.md
@.planning/phases/07-files-print-control-core-print-loop-gate/07-UI-SPEC.md
@.planning/phases/07-files-print-control-core-print-loop-gate/07-PATTERNS.md
@docs/commands/catalog.json
@docs/commands/printer-matrix.json
@app/src/main/java/works/mees/dinghy/net/JsonRpc.kt
@app/src/main/java/works/mees/dinghy/command/CommandRegistry.kt
@app/src/test/java/works/mees/dinghy/command/CommandCatalogDriftTest.kt
</context>

<source_audit>
- GOAL: ROADMAP Phase 7 requires browse, preview, start, delete, pause, resume, cancel, restart, and live core-loop proof.
- REQ: FILE-01/02/03/04 and JOB-03/04/05 require file manager and print-control command coverage.
- RESEARCH: Moonraker official docs identify `server.files.*` and `printer.print.*` JSON-RPC methods and state-confirmed acceptance.
- CONTEXT: D-06, D-07, D-10, D-13, D-14, D-15, and D-16 constrain how registry commands will be consumed.
- PATTERNS: 07-PATTERNS requires registry entries, catalog rows, and matrix rows in the same wave.
- REVIEWS: Claude H-2 requires pause/resume state-confirmation seam to be resolved in Wave 1; M-2/L-2 require runtime registry scope to exclude unused roots/list and pin pause controls to `pause_resume`.
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Phase 7 JSON-RPC constants and registry specs</name>
  <files>app/src/main/java/works/mees/dinghy/net/JsonRpc.kt, app/src/main/java/works/mees/dinghy/command/CommandRegistry.kt, app/src/test/java/works/mees/dinghy/command/CommandRegistryFilesTest.kt</files>
  <read_first>
    @app/src/main/java/works/mees/dinghy/net/JsonRpc.kt
    @app/src/main/java/works/mees/dinghy/command/CommandRegistry.kt
    @app/src/main/java/works/mees/dinghy/command/CommandDispatchExtensions.kt
    @app/src/main/java/works/mees/dinghy/state/DeriveCapabilities.kt
    @.planning/phases/07-files-print-control-core-print-loop-gate/07-RESEARCH.md
    @.planning/phases/07-files-print-control-core-print-loop-gate/07-PATTERNS.md
    @.planning/phases/07-files-print-control-core-print-loop-gate/07-REVIEWS.md
  </read_first>
  <behavior>
    - Registry specs use stable catalog IDs already present in `docs/commands/catalog.json`.
    - File manager calls use `AvailabilityPredicate.ComponentPresent("file_manager")`.
    - Print start uses `AvailabilityPredicate.ObjectPresent("virtual_sdcard")`.
    - Pause/resume/cancel availability is pinned to `AvailabilityPredicate.ObjectPresent("pause_resume")`; no hedged predicate wording.
    - Command semantics explicitly say user-visible success is interpreted by holder/state logic, not by the JSON-RPC acknowledgement.
  </behavior>
  <action>Add `JsonRpcMethods` constants and `CommandRegistry` entries only for Phase 7 runtime commands the app actually sends: `server.files.get_directory`, `server.files.thumbnails`, `server.files.delete_file`, `printer.print.start`, `printer.print.pause`, `printer.print.resume`, and `printer.print.cancel`. Keep `server.files.roots` and `server.files.list` as command-catalog reference rows only unless execution proves a call site dispatches them. Add typed args for directory browse (`path`, `extended`), filename operations (`filename`), delete (`path` as root-prefixed `gcodes/...`), and print start (`filename` relative to `gcodes`). Keep existing `filesMetadata` unchanged except for shared arg reuse if that reduces duplication. Add `CommandRegistryFilesTest` to prove params are exactly shaped for D-03 path semantics, D-06 start ConfirmGuard flow, D-07 state-confirmed semantics, D-14/D-15 cancel separation, and pinned `pause_resume` availability.</action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testReleaseUnitTest --tests *CommandRegistryFilesTest --no-daemon" | tr -d '\r'</automated>
  </verify>
  <acceptance_criteria>
    - Tests prove browse params include `path` only when provided and preserve `extended`.
    - Tests prove delete params use `path: "gcodes/<relative>"` and start params use `filename: "<relative>"`.
    - Tests prove pause/resume/cancel params are null or empty per Moonraker docs.
    - Tests prove roots/list are not added to `CommandRegistry.all` unless an implementation call site dispatches them.
    - `CommandRegistry.all` contains every new Phase 7 runtime command exactly once.
  </acceptance_criteria>
  <done>Phase 7 registry specs and param tests exist and pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update command catalog and printer matrix evidence</name>
  <files>docs/commands/catalog.json, docs/commands/printer-matrix.json, app/src/test/java/works/mees/dinghy/command/CommandCatalogDriftTest.kt</files>
  <read_first>
    @docs/commands/catalog.json
    @docs/commands/printer-matrix.json
    @app/src/test/java/works/mees/dinghy/command/CommandCatalogDriftTest.kt
    @.planning/phases/07-files-print-control-core-print-loop-gate/07-RESEARCH.md
    @.planning/phases/07-files-print-control-core-print-loop-gate/07-PATTERNS.md
    @.planning/phases/07-files-print-control-core-print-loop-gate/07-REVIEWS.md
  </read_first>
  <behavior>
    - Every newly registered Phase 7 command has a `command_availability` row.
    - Catalog entries for new runtime commands record `runtime_registry.registered: true` and registered status.
    - Matrix evidence ties predicates to live objects/components already captured for both printers or explicit documented exclusions.
  </behavior>
  <action>Update catalog entries only for newly registered runtime commands so `runtime_registry` marks them registered and acceptance semantics point to state/holder confirmation where applicable. Add `docs/commands/printer-matrix.json` `command_availability` rows for `MR-server.files.get_directory`, `MR-server.files.thumbnails`, `MR-server.files.delete_file`, `MR-printer.print.start`, `MR-printer.print.pause`, `MR-printer.print.resume`, and `MR-printer.print.cancel`. Keep the existing metadata row. Leave `MR-server.files.roots` and `MR-server.files.list` as planned/reference catalog entries, not registered runtime commands, unless a Phase 7 call site actually sends them. If `CommandCatalogDriftTest` needs a clearer assertion message for matrix row failures, update only the message without weakening the invariant. Do not add static runtime gating data to app code.</action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testReleaseUnitTest --tests *CommandCatalogDriftTest --tests *CommandRegistryFilesTest --no-daemon" | tr -d '\r'</automated>
  </verify>
  <acceptance_criteria>
    - `CommandCatalogDriftTest.registryCommandsHaveMatrixAvailabilityRows` passes.
    - `CommandCatalogDriftTest.predicateReferencesAreBackedByMatrixEvidence` passes.
    - `jq '.commands[] | select(.catalog_id=="MR-printer.print.start").runtime_registry.registered' docs/commands/catalog.json` prints `true`.
    - `jq '.commands[] | select(.catalog_id=="MR-server.files.list").runtime_registry.registered' docs/commands/catalog.json` remains `false` unless execution adds a real dispatch call site.
    - No Phase 7 registry command lacks a catalog row or matrix row.
  </acceptance_criteria>
  <done>Command sidecars match Phase 7 runtime registry entries and drift tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Lock pause/resume state confirmation in reducer and subscribe set</name>
  <files>app/src/main/java/works/mees/dinghy/state/PrinterState.kt, app/src/main/java/works/mees/dinghy/state/PrinterStateReducer.kt, app/src/main/java/works/mees/dinghy/state/DeriveCapabilities.kt, app/src/test/java/works/mees/dinghy/state/PrinterStateReducerTest.kt, app/src/test/java/works/mees/dinghy/state/DeriveCapabilitiesTest.kt</files>
  <read_first>
    @app/src/main/java/works/mees/dinghy/state/PrinterState.kt
    @app/src/main/java/works/mees/dinghy/state/PrinterStateReducer.kt
    @app/src/main/java/works/mees/dinghy/state/DeriveCapabilities.kt
    @app/src/test/java/works/mees/dinghy/state/PrinterStateReducerTest.kt
    @app/src/test/java/works/mees/dinghy/state/DeriveCapabilitiesTest.kt
    @.planning/phases/07-files-print-control-core-print-loop-gate/07-RESEARCH.md
    @.planning/phases/07-files-print-control-core-print-loop-gate/07-REVIEWS.md
  </read_first>
  <behavior>
    - `deriveSubscribeSet()` includes `pause_resume` when the printer exposes it.
    - `PrinterState` exposes enough reducer truth for pending pause/resume/cancel actions: `print_stats.state` must distinguish printing, paused, and cancelled, and `pause_resume.is_paused` must be retained if modeled.
    - Reducer tests include a golden frame sequence for pause -> paused -> resume -> printing -> cancel.
    - This task removes the Wave-4 "add if needed" ambiguity called out by Claude H-2.
  </behavior>
  <action>Resolve the state-confirmation seam before any UI work. Add `pause_resume` to the subscribe superset and capability/predicate tests. Extend `PrinterState` / `PrinterStateReducer` only as needed to retain `pause_resume.is_paused` alongside existing `PrintState.Paused`; if the existing model is sufficient, still add reducer tests that prove `print_stats.state` transitions through `printing`, `paused`, `printing`, and `cancelled` from partial diffs without being wiped by unrelated frames. Add the golden-frame test in `PrinterStateReducerTest` and subscribe-set assertion in `DeriveCapabilitiesTest`. Addresses review concern: Claude H-2.</action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testReleaseUnitTest --tests works.mees.dinghy.state.PrinterStateReducerTest --tests works.mees.dinghy.state.DeriveCapabilitiesTest --tests *CommandCatalogDriftTest --no-daemon" | tr -d '\r'</automated>
  </verify>
  <acceptance_criteria>
    - `deriveSubscribeSet(listOf("print_stats", "pause_resume", "virtual_sdcard"))` includes `pause_resume`.
    - Reducer tests prove pause/resume/cancel state transitions from Moonraker-shaped diffs.
    - Later Print Status pending states can read reducer truth without adding model fields in Wave 4.
    - `AvailabilityPredicate.ObjectPresent("pause_resume")` is backed by matrix evidence.
  </acceptance_criteria>
  <done>Pause/resume confirmation is a Wave-1 tested state seam, not a conditional UI task.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| UI/service -> CommandRegistry | File and print-control user actions resolve to typed specs before transport. |
| CommandRegistry -> Moonraker JSON-RPC | Registry params become Moonraker request payloads. |
| CommandRegistry -> docs sidecars | Runtime command surface is checked against machine-readable docs and live-printer evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-07-01 | Tampering | Delete file command | mitigate | Root-prefixed delete path is tested and delete remains Focus/ConfirmGuard gated by downstream plans. |
| T-07-02 | Spoofing | Print start success | mitigate | Registry semantics and tests preserve state-confirmed acceptance; ack does not equal success. |
| T-07-03 | Repudiation | Cancel vs stop commands | mitigate | Cancel and emergency stop remain distinct method specs and downstream UI actions. |
| T-07-04 | Tampering | Command drift | mitigate | Catalog and matrix rows land with registry entries and pass drift tests. |
| T-07-05 | Denial of Service | Stale pending actions | mitigate | Registry keys and reducer state are stable per action/file so holders can clear pending markers by state. |
</threat_model>

<verification>
Run targeted registry tests after each task, then run:
`/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testReleaseUnitTest --tests *CommandCatalogDriftTest --tests *CommandRegistryFilesTest --tests works.mees.dinghy.state.PrinterStateReducerTest --tests works.mees.dinghy.state.DeriveCapabilitiesTest --no-daemon" | tr -d '\r'`
</verification>

<success_criteria>
- All Phase 7 runtime commands are typed and sidecar-backed.
- Registry drift tests pass.
- File and print-control params match official Moonraker request shapes.
- Pause/resume/cancel confirmation is covered by reducer and subscribe-set tests before UI execution.
</success_criteria>

<output>
Create `.planning/phases/07-files-print-control-core-print-loop-gate/07-01-SUMMARY.md` when done.
</output>
