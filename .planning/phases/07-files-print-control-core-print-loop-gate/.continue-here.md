---
context: phase
phase: 07-files-print-control-core-print-loop-gate
task: 3
total_tasks: 3
status: paused
last_updated: 2026-06-02T12:25:00-05:00
---

## Where Phase 7 stands

Plan `07-06` is the only open plan. Task status:
- **Task 1 — automated release verification:** DONE (`2c52731`, `07-VERIFICATION.md`).
- **Task 2 — Files route-render + large-library perf/OOM gate:** DONE → **PASS** (`07-PERF.md`, commit `083709f`).
  - Route-render `ShellPresenceTest` passed on flox (`da9a406`).
  - Large-library gate run against the REAL Ender 3 library (420 files / 404 thumbnails) on flox —
    no throwaway fixture written to Moonraker. Steady-scroll p95 15.48 ms, 0 frozen frames >700 ms, no
    OOM/ANR/death (signed release build). First-load p95 24.97 ms / max 173 ms (one brief layout/parse
    spike = the observed first-page lag), 0 frozen. Raw dumps committed (`gfxinfo-files-seg-1..12.txt`,
    `gfxinfo-files-firstload.txt`).
- **Task 3 — live core print-loop UAT:** **DEFERRED until an official UAT round** (Matthew, 2026-06-02).
  He has informally confirmed most behaviors hands-on, but `07-UAT.md` stays PENDING — do NOT mark rows
  passed without a real, recorded run. Requires a real print on a safe file (start/pause/resume/graceful
  cancel/restart) + a safe throwaway delete.

## On-device Files UI polish (this session — committed)

The Codex-built Files screen had basics wrong; fixed live on flox + verified by Matthew. Commits:
`e2ff21c` (code), `6ae4ff5` (design-law doc sync). See [[dinghy-display-ui-polish]] memory for detail.
- List clip/overflow + pinned height (Views-in-Compose scroll lesson), smallest-thumb-per-row, dotfile-
  dir hiding, future-print image-backed Focus card, Fit-not-Crop, full-width panel text, drawer-swipe
  suppressed on Files.

## Deferred defects (logged, not fixed)

- **Files Delete gating too broad** (`07-UAT.md` Deferred Defects): blocks deleting ANY file during any
  print, not just the actively-printing file. Should key off `print_stats.filename`. Relaxes the
  UI-SPEC "Delete idle-only" rule — confirm when fixing.
- **Status progress ring jumps down then up on a short print** (`.planning/todos/pending/status-progress-ring-dual-source-jump.md`,
  commit `015e1b1`): NOT filament-based — `PrinterStateReducer` lets `virtual_sdcard.progress` and
  `display_status.progress` fight (last-writer-wins per delta). Status = Phase-4 work.
- **Possible Status last-job card red-diagonal-slash artifact** — seen on the cancelled-job card during
  this session (screenshot); unverified whether intentional. Not investigated. Phase-4 Status code.

## Infrastructure state

- Repo `/mnt/e/claude/personal/github/dinghy-display`, branch `master`, tree clean at last check.
- ADB `/mnt/c/Android/Sdk/platform-tools/adb.exe`; device `0a64b42e` (flox, Nexus 7 / Android 11).
- Installed build: **signed release** `app/build/outputs/apk/release/app-armeabi-v7a-release-debugsigned.apk`
  (R8-minified; pkg `works.mees.dinghy`). Sign helper: `E:\Android\sign-release.bat <in> <out>`.
- Printers: Ender 5 Plus `192.168.1.120:7125`, Ender 3 Pro `192.168.1.121:7125` (flox was connected to
  the Ender 3 — 420-file library; that's the perf fixture).

## Required reading (in order)

1. `07-06-PLAN.md` — the active plan + task criteria.
2. `07-PERF.md` — perf gate PASS (task 2).
3. `07-UAT.md` — the live UAT checklist still to run (task 3) + the deferred Delete defect.
4. `07-VERIFICATION.md` — automated release gate (task 1).
5. [[dinghy-display-ui-polish]] memory — full Files-polish detail + Views-in-Compose lesson.

## Next action

When Matthew is ready for the official UAT: run the live core print-loop UAT on a real print (his
hardware, his safe-file choice — do NOT autonomously start a print), record results into `07-UAT.md`,
then write `07-06-SUMMARY.md` and close the phase. Do NOT drive the tablet UI for the print lifecycle
unless he asks.
