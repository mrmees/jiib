---
phase: 07-files-print-control-core-print-loop-gate
plan: 05
type: execute
wave: 4
depends_on:
  - 07-01
  - 07-04
files_modified:
  - app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusControlModel.kt
  - app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusScreen.kt
  - app/src/main/java/works/mees/dinghy/ui/printstatus/LastJobHolder.kt
  - app/src/main/java/works/mees/dinghy/ui/shell/AppShell.kt
  - app/src/test/java/works/mees/dinghy/ui/printstatus/PrintStatusControlModelTest.kt
  - app/src/test/java/works/mees/dinghy/ui/printstatus/LastJobHolderTest.kt
autonomous: true
requirements:
  - FILE-03
  - JOB-03
  - JOB-04
  - JOB-05
must_haves:
  truths:
    - "D-13: printing gutter keeps Tune / Pause / Stop; Tune is disabled."
    - "D-14: graceful cancel is long-press Pause while printing; Stop remains emergency stop."
    - "D-15: paused gutter shows Tune / Resume / Stop; long-press Resume opens graceful cancel."
    - "D-16: terminal states with filename show Files / Restart / Stop."
    - "UI-SPEC pending state clears from Wave-1 reducer state, not command ack."
    - "Claude M-1: Restart filename source is explicit and tested across complete/error/cancelled states."
    - "Claude L-1: long-press graceful cancel also exposes an accessibility action."
  artifacts:
    - path: "app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusControlModel.kt"
      provides: "Pure state-to-control model for Print Status gutter"
    - path: "app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusScreen.kt"
      provides: "State-adaptive gutter actions and guards"
    - path: "app/src/main/java/works/mees/dinghy/ui/shell/AppShell.kt"
      provides: "Print Status terminal Files action routes to Dest.Files"
    - path: "app/src/main/java/works/mees/dinghy/ui/printstatus/LastJobHolder.kt"
      provides: "Terminal restart filename fallback when current print_stats filename is cleared"
---

<objective>
Wire Print Status pause/resume/graceful-cancel/restart/Files controls.

Purpose: Starting a print is not enough for the core print-loop gate; Print Status must provide state-adaptive controls while preserving emergency-stop separation.
Output: Pure control model, tested state mapping, Print Status gutter wiring, ConfirmGuard copy, dispatcher calls, and pending-state handling.
</objective>

<execution_context>
@/mnt/c/Users/matth/.codex/get-shit-done/workflows/execute-plan.md
@/mnt/c/Users/matth/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-files-print-control-core-print-loop-gate/07-CONTEXT.md
@.planning/phases/07-files-print-control-core-print-loop-gate/07-RESEARCH.md
@.planning/phases/07-files-print-control-core-print-loop-gate/07-UI-SPEC.md
@.planning/phases/07-files-print-control-core-print-loop-gate/07-PATTERNS.md
@app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusScreen.kt
@app/src/main/java/works/mees/dinghy/ui/shell/AppShell.kt
@app/src/main/java/works/mees/dinghy/command/CommandRegistry.kt
</context>

<source_audit>
- GOAL: ROADMAP requires pause, resume, cancel, restart, Files, and live monitoring as one print-loop capability.
- REQ: JOB-03/04/05 define Print Status controls; FILE-03 provides restart-from-file semantics.
- RESEARCH: `printer.print.*` acks are not user-visible success; state confirmation comes from printer objects.
- CONTEXT: D-13, D-14, D-15, and D-16 define exact gutter states and cancel/stop separation.
- UI-SPEC: Print Status gutter labels, long-press behavior, pending state, and guard dismiss labels are binding.
- PATTERNS: Sequence after `Dest.Files` route to avoid same-wave `AppShell.kt` conflicts.
- REVIEWS: Claude H-2 is addressed by Wave-1 reducer/subscribe tests in 07-01; M-1/L-1 require explicit restart filename source and graceful-cancel accessibility action here.
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add pure Print Status control model</name>
  <files>app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusControlModel.kt, app/src/test/java/works/mees/dinghy/ui/printstatus/PrintStatusControlModelTest.kt</files>
  <read_first>
    @app/src/main/java/works/mees/dinghy/state/PrinterState.kt
    @app/src/main/java/works/mees/dinghy/state/PrinterStateReducer.kt
    @app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusScreen.kt
    @.planning/phases/07-files-print-control-core-print-loop-gate/07-01-PLAN.md
    @.planning/phases/07-files-print-control-core-print-loop-gate/07-CONTEXT.md
    @.planning/phases/07-files-print-control-core-print-loop-gate/07-UI-SPEC.md
    @.planning/phases/07-files-print-control-core-print-loop-gate/07-REVIEWS.md
  </read_first>
  <behavior>
    - Control model is pure Kotlin with no Compose dependency.
    - Control model consumes the Wave-1 reducer truth for printing, paused, cancelled, complete, and error states.
    - Printing state maps to Tune disabled, Pause tap, graceful Cancel hold, Stop emergency.
    - Paused state maps to Tune disabled, Resume tap, graceful Cancel hold, Stop emergency.
    - Complete/error/cancelled with filename maps to Files, Restart, Stop according to D-16.
    - Standby/no-filename states do not expose Restart.
  </behavior>
  <action>Create `PrintStatusControlModel.kt` with a small state-to-actions model consumed by `PrintStatusScreen`. Use the Wave-1 `PrinterState` reducer contract from 07-01 for pause/resume/cancel state confirmation; do not add conditional state-model work in this plan. Add tests for printing, paused, complete with filename, error with filename, cancelled with filename, standby, no filename, pending action labels, and restart availability when the filename is present or absent. Addresses review concern: Claude H-2.</action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testReleaseUnitTest --tests works.mees.dinghy.ui.printstatus.PrintStatusControlModelTest --no-daemon" | tr -d '\r'</automated>
  </verify>
  <acceptance_criteria>
    - Tests cover every D-13 through D-16 state.
    - Control model distinguishes graceful cancel from emergency Stop.
    - Restart is unavailable without a known filename.
    - Tune is present and disabled for printing and paused states.
    - No task in this plan needs to add pause/resume reducer fields; that seam is handled by 07-01.
  </acceptance_criteria>
  <done>Pure Print Status control model is green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire Print Status gutter actions and pending guards</name>
  <files>app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusScreen.kt, app/src/main/java/works/mees/dinghy/ui/shell/AppShell.kt, app/src/test/java/works/mees/dinghy/ui/printstatus/PrintStatusControlModelTest.kt</files>
  <read_first>
    @app/src/main/java/works/mees/dinghy/ui/printstatus/PrintStatusScreen.kt
    @app/src/main/java/works/mees/dinghy/ui/shell/AppShell.kt
    @app/src/main/java/works/mees/dinghy/designsystem/ConfirmGuard.kt
    @app/src/main/java/works/mees/dinghy/command/CommandRegistry.kt
    @app/src/main/java/works/mees/dinghy/ui/printstatus/LastJobHolder.kt
    @app/src/test/java/works/mees/dinghy/ui/printstatus/LastJobHolderTest.kt
    @.planning/phases/07-files-print-control-core-print-loop-gate/07-UI-SPEC.md
    @.planning/phases/07-files-print-control-core-print-loop-gate/07-PATTERNS.md
    @.planning/phases/07-files-print-control-core-print-loop-gate/07-REVIEWS.md
  </read_first>
  <behavior>
    - Pause/resume/restart/cancel actions dispatch through Phase 7 registry specs.
    - Graceful cancel uses ConfirmGuard with `Cancel print` / `Keep printing`.
    - Restart uses ConfirmGuard with `Restart print` / `Not now`.
    - Terminal Files action calls back to AppShell and navigates to `Dest.Files`.
    - Pending state clears from printer state transitions, not command ack.
    - Restart filename source is current `print_stats.filename` first, then `LastJobHolder` terminal filename if the current filename is cleared; Restart is hidden if neither source is available.
    - Long-press graceful cancel also has a `Cancel print` accessibility custom action.
  </behavior>
  <action>Update `PrintStatusScreen` to consume the pure control model and replace the current disabled inert Pause/Resume behavior with real Pause/Resume tap actions plus long-press graceful cancel guards. Keep Stop wired to emergency stop and visually distinct. Add an `onOpenFiles` callback from AppShell for the terminal Files control. Wire Restart to `printer.print.start` for the resolved filename: use current `print_stats.filename` when present, otherwise use the terminal `LastJobHolder` filename if available; hide Restart when both are blank. Track pending pause/resume/cancel/restart labels locally and clear them when Wave-1 reducer state matches the requested transition or becomes incompatible. Add a `Cancel print` accessibility custom action on Pause/Resume so graceful cancel is not long-press-only for accessibility services. Preserve existing error toast behavior from dispatcher events. Addresses review concerns: Claude M-1 and L-1.</action>
  <verify>
    <automated>/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testReleaseUnitTest --tests works.mees.dinghy.ui.printstatus.PrintStatusControlModelTest --tests works.mees.dinghy.ui.printstatus.LastJobHolderTest :app:compileReleaseKotlin --no-daemon" | tr -d '\r'</automated>
  </verify>
  <acceptance_criteria>
    - Release Kotlin compile passes.
    - Print Status uses registry specs for pause/resume/cancel/restart.
    - Guard copy matches `07-UI-SPEC.md`.
    - Stop remains emergency-stop path and is not reused for graceful cancel.
    - Terminal Files control navigates to the Files screen.
    - Restart is tested for complete/error/cancelled with current filename present, current filename cleared but last-job filename present, and both filenames absent.
    - Pause/Resume exposes graceful cancel by long press and by accessibility custom action.
  </acceptance_criteria>
  <done>Print Status gutter is state-adaptive and compile-verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Print Status UI -> Dispatcher | User controls issue print-control commands. |
| Command ack -> UI pending state | Acknowledgement must not become visible success. |
| Graceful cancel -> emergency Stop | Separate user intents must remain separate command paths. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-07-02 | Spoofing | Restart/start success | mitigate | Restart filename source is resolved and tested; pending restart clears from printer state matching filename/lifecycle. |
| T-07-03 | Tampering | Cancel vs Stop | mitigate | Long-press graceful cancel and Stop emergency have distinct controls, labels, guards, and commands. |
| T-07-04 | Tampering | Command dispatch | mitigate | Controls use registry specs from Plan 01. |
| T-07-05 | Denial of Service | Pending state | mitigate | Pending markers clear on matching or incompatible printer state. |
</threat_model>

<verification>
Run control model tests and compile after each task, then run:
`/mnt/c/Windows/System32/cmd.exe /c "E:\Android\gw.bat :app:testReleaseUnitTest --tests works.mees.dinghy.ui.printstatus.PrintStatusControlModelTest --tests works.mees.dinghy.ui.printstatus.LastJobHolderTest :app:compileReleaseKotlin --no-daemon" | tr -d '\r'`
</verification>

<success_criteria>
- Print Status controls match D-13 through D-16.
- Graceful cancel and emergency Stop remain separate.
- Terminal Files and Restart actions are live.
- Restart filename source and accessibility custom cancel action are tested.
</success_criteria>

<output>
Create `.planning/phases/07-files-print-control-core-print-loop-gate/07-05-SUMMARY.md` when done.
</output>
